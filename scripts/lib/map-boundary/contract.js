/**
 * ΤΟ ΣΥΜΒΟΛΑΙΟ ΤΟΥ ΣΥΝΟΡΟΥ ΤΟΥ ΧΑΡΤΗ — **μία** αλήθεια (ADR-777 §8.56)
 *
 * ─────────────────────────────────────────────────────────────────────────────
 * ΤΙ ΑΠΑΝΤΑ
 * ─────────────────────────────────────────────────────────────────────────────
 * *«Ποιος επιτρέπεται να ζητήσει τον χάρτη ωμό — και κατέχει το σύνορο αυτό που
 * υπόσχεται;»*
 *
 * ⛔ **ΜΗΝ αντιγράψεις αυτούς τους πίνακες αλλού.** Δύο αλήθειες για ένα ερώτημα
 * είναι το σχήμα του **ADR-749** (τέσσερις μηχανές, πέντε διάλεκτοι regex, τρεις
 * αριθμοί για το ίδιο δέντρο).
 *
 * @module scripts/lib/map-boundary/contract
 */

/** Ο κανονικός ειδικευτής του συνόρου. */
const BOUNDARY_MODULE = '@/lib/maps/maplibre';

/** Το αρχείο του συνόρου, σχετικά με τη ρίζα (POSIX). */
const BOUNDARY_FILE = 'src/lib/maps/maplibre.ts';

/**
 * 🔑 **Η ΜΙΑ ΓΡΑΜΜΗ ΠΟΥ ΤΟ ΣΥΝΟΡΟ ΠΡΕΠΕΙ ΝΑ ΚΑΤΕΧΕΙ.** Χωρίς αυτήν το σύνορο είναι
 * απλώς ένα barrel: κάθε εισαγωγή θα περνούσε από εδώ, η πύλη θα ήταν **πράσινη**,
 * και **κάθε χάρτης της εφαρμογής θα έχανε τη διάταξή του** — ακριβώς η βλάβη που
 * γέννησε αυτό το σύνορο. Το κριτήριο Κ3 το ρωτά σε κάθε commit.
 */
const REQUIRED_STYLESHEET = 'maplibre-gl/dist/maplibre-gl.css';

/**
 * **Οι ωμές πόρτες** — τα modules που κανείς δεν επιτρέπεται να ζητήσει ως **τιμή**
 * έξω από το σύνορο, με τον λόγο του καθενός.
 *
 * 🔑 Κριτήριο ένταξης, **μετρήσιμο, όχι γούστο**: *το module παραδίδει σύμβολο που
 * επιβιώνει στον περιηγητή και ζωγραφίζει (ή χειρίζεται) επιφάνεια MapLibre* —
 * δηλαδή σύμβολο που **μπορεί να ξεχάσει το φύλλο στυλ του**.
 *
 * ⚠️ Τα κλειδιά είναι **ΡΙΖΕΣ πακέτου, όχι πλήρεις ειδικευτές**, και ο έλεγχος είναι
 * `spec === ρίζα || spec.startsWith(ρίζα + '/')`. Χωρίς αυτό, τρεις διαδρομές θα
 * έμεναν **ανοιχτές σιωπηλά**: το `react-map-gl/mapbox`, το `maplibre-gl/dist/...` —
 * και, κυρίως, το **ίδιο το φύλλο στυλ**, `maplibre-gl/dist/maplibre-gl.css`, που
 * ήταν αντιγραμμένο σε **τέσσερα** αρχεία. Ένα σύνορο που φυλάει τα σύμβολα αλλά
 * αφήνει το στυλ να εισάγεται όπου να 'ναι, φυλάει το μισό ερώτημα.
 */
const GUARDED_MODULES = Object.freeze({
  'react-map-gl':
    'Η ΔΗΛΩΤΙΚΗ ΕΠΙΦΑΝΕΙΑ. Παραδίδει `Map`, `Source`, `Layer`, `Marker`, `Popup` — κάθε ένα ' +
    'από αυτά ζωγραφίζει DOM που η MapLibre στυλίζει με ΤΟ ΔΙΚΟ ΤΗΣ φύλλο. Χωρίς εκείνο ο ' +
    'καμβάς μένει `position: static` και το popup χωρίς φούσκα.',
  'maplibre-gl':
    'Η ΠΡΟΣΤΑΚΤΙΚΗ ΕΠΙΦΑΝΕΙΑ. Το `Marker` και το `LngLatBounds` κατασκευάζονται με `new` και ' +
    'μπαίνουν στον ίδιο χάρτη — ίδια εξάρτηση από το ίδιο φύλλο στυλ. Ο `AddressMap` το είχε ' +
    'ήδη πληρώσει μία φορά («*was missing, causing invisible pins*»).',
});

/**
 * **Ποιος επιτρέπεται να εισάγει ωμά** — κλειστό σύνολο, **υποχρεωτικός λόγος** ανά
 * εγγραφή (πρότυπο CHECK 3.35/3.50/3.58/3.61: *δήλωση χωρίς λόγο είναι παράκαμψη με
 * άλλο όνομα*).
 *
 * ⚠️ Οι διαδρομές είναι **POSIX, σχετικές με τη ρίζα** — ο ταξινομητής κανονικοποιεί
 * τα backslash των Windows πριν ρωτήσει. Χωρίς αυτό το σύνολο θα ήταν **κενό σε
 * Windows** και η πύλη θα κατήγγελλε τον ίδιο της τον ιδιοκτήτη (μάθημα CHECK 3.61).
 */
const RAW_IMPORT_OWNERS = Object.freeze({
  [BOUNDARY_FILE]:
    'ΤΟ ΙΔΙΟ ΤΟ ΣΥΝΟΡΟ. Είναι ο μοναδικός νόμιμος καταναλωτής των ωμών modules: τα ' +
    'επανεξάγει ΑΦΟΥ έχει εισαγάγει το φύλλο στυλ τους. Πύλη που το κατήγγελλε θα ' +
    'κοκκίνιζε πάνω στη ΘΕΡΑΠΕΙΑ (μάθημα CHECK 3.50 Κ7β · 3.58 · 3.61).',
});

/**
 * Οι καταστάσεις της πύλης — **κλειστό σύνολο, fail-closed**.
 *
 * ⚠️ Το {@link GATE_STATES.TYPE_ONLY} είναι **ρητά ΕΠΙΤΡΕΠΤΟ**, και δεν είναι
 * χαλάρωση: ένας τύπος **σβήνεται στη μεταγλώττιση**, δηλαδή δεν φτάνει ποτέ στον
 * περιηγητή, δηλαδή **δεν μπορεί να ξεχάσει φύλλο στυλ**. Πύλη που τον κατήγγελλε θα
 * κοκκίνιζε σε **σωστό** κώδικα — ο δρόμος προς το `SKIP_`. Ίδιο κριτήριο με το
 * CHECK 3.61 (`clause.isTypeOnly ⇒ continue`). Μετριέται **ονομαστικά** και δεν
 * κρύβεται στον παρονομαστή, γιατί «0 που δεν φαίνεται» διαβάζεται ως «δεν υπάρχει
 * τέτοιος έλεγχος».
 */
const GATE_STATES = Object.freeze({
  /** ⛔ Κ1 — εισάγει ωμή **τιμή** από φυλασσόμενο module χωρίς να είναι ιδιοκτήτης. */
  BOUNDARY_BYPASS: 'boundary-bypass',
  /** ⛔ Κ1′ — δήλωση ιδιοκτήτη που έπαψε να ισχύει: το κλειστό σύνολο σαπίζει σιωπηλά. */
  ORPHAN_OWNER: 'orphan-owner',
  /** ⛔ Κ1′ — δήλωση χωρίς ουσιαστικό λόγο. */
  REASONLESS_OWNER: 'reasonless-owner',
  /** ✅ Ο δηλωμένος ιδιοκτήτης των ωμών modules. */
  OWNER: 'owner',
  /** ✅ Ζητά τον χάρτη από το σύνορο — ο **παρονομαστής του Κ2**. */
  AT_BOUNDARY: 'at-boundary',
  /** ✅ Αγγίζει τα ωμά modules **μόνο** με `import type`. */
  TYPE_ONLY: 'type-only',
  /** Δεν αναφέρει καν χάρτη — ο μεγάλος πληθυσμός. */
  NOT_A_MAP_FILE: 'not-a-map-file',
});

/**
 * Οι καταστάσεις **του συνόρου του ίδιου** (Κ2 + Κ3) — τρίτο κατάστιχο, ξεχωριστό
 * επίτηδες (πρότυπο CHECK 3.50 / 3.74 Κ2).
 *
 * 🔴 **ΓΙΑΤΙ ΥΠΑΡΧΟΥΝ ΚΑΘΟΛΟΥ.** Χωρίς αυτά, ο ευκολότερος τρόπος να γίνει η πύλη
 * πράσινη είναι να **σβηστεί η θεραπεία**: αφαιρείς τη γραμμή του φύλλου στυλ από το
 * σύνορο (Κ3), ή σβήνεις τον τελευταίο καταναλωτή του (Κ2), και η πύλη δηλώνει
 * «καθαρό» ενώ **κανείς δεν προστατεύεται**. Είναι το σχήμα «*0 = κανείς δεν
 * κοίταξε*», που αυτό το repo έχει μετρήσει πάνω από πέντε φορές (N.11 · N.12 ·
 * N.18 · CHECK 3.18 · 3.74).
 */
const BOUNDARY_STATES = Object.freeze({
  /** ⛔ Κ2 — το σύνορο δεν έχει **κανέναν** καταναλωτή: πράσινο με μηδέν προστασία. */
  BOUNDARY_ABANDONED: 'boundary-abandoned',
  /** ⛔ Κ3 — το σύνορο **δεν κατέχει** το φύλλο στυλ που υπόσχεται. */
  STYLESHEET_MISSING: 'stylesheet-missing',
  /** ⛔ Κ3 — το αρχείο του συνόρου **δεν υπάρχει**. */
  BOUNDARY_ABSENT: 'boundary-absent',
  /** ✅ Το σύνορο υπάρχει, κατέχει το φύλλο στυλ, και το ζητά κόσμος. */
  BOUNDARY_HEALTHY: 'boundary-healthy',
});

/** Κανονικοποίηση σε POSIX, σχετική με τη ρίζα — **μία** φορά, για όλους. */
function repoRelativePosix(absPath, root) {
  const posix = (p) => p.split('\\').join('/');
  return posix(absPath).replace(`${posix(root)}/`, '');
}

/** Είναι αυτό το αρχείο δηλωμένος ιδιοκτήτης των ωμών modules; */
function isRawImportOwner(repoRelPath, owners = RAW_IMPORT_OWNERS) {
  return Object.hasOwn(owners, repoRelPath);
}

module.exports = {
  BOUNDARY_FILE,
  BOUNDARY_MODULE,
  BOUNDARY_STATES,
  GATE_STATES,
  GUARDED_MODULES,
  RAW_IMPORT_OWNERS,
  REQUIRED_STYLESHEET,
  isRawImportOwner,
  repoRelativePosix,
};
