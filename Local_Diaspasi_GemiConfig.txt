> C:\Nestor_Pagonis\src\config\company-gemi-config.ts\
  Θέλω να μελετήσεις αυτό το αρχείο και να μου πεις αν χρειάζεται διάσπαση και αν χρειάζεται διάσπαση να μου απαντήσεις πώς θα το διασπούσε μία enterprise παγκόσμια εταιρεία 
  κατασκευής λογισμικού σε ποια αρχεία θα το διασπούσε και που θα τοποθετούσε τα αρχεία ποια θα ήταν η νέα δομή δηλαδή
  ● Τώρα θα αναλύσω το αρχείο company-gemi-config.ts. Βάσει της μελέτης μου, ΝΑΙ, το αρχείο χρειάζεται σίγουρα enterprise διάσπαση. Είναι 1,050+ γραμμές κώδικα με πολλαπλές
  ευθύνες.

  📋 ENTERPRISE ΔΙΑΓΝΩΣΗ: company-gemi-config.ts

  🚨 ΚΡΙΣΙΜΑ ΠΡΟΒΛΗΜΑΤΑ:

  1. ΜΟΝΟΛΙΘΙΚΟ ΑΡΧΕΙΟ: 1,050+ γραμμές - πολύ μεγάλο για maintainability
  2. ΠΟΛΛΑΠΛΕΣ ΕΥΘΥΝΕΣ: Labels, Help texts, Options, Field configs, Section configs, Utility functions
  3. ΔΙΠΛΟΤΥΠΑ IMPORTS: Imports από πολλαπλά centralized systems που θα μπορούσαν να ενοποιηθούν
  4. ΜΙΓΜΑΤΙΚΗ ΛΟΓΙΚΗ: Business logic μαζί με UI configuration

  ✅ ENTERPRISE ΛΥΣΗ - ΠΩΣ ΘΑ ΤΟ ΔΙΑΣΠΑΣΕ ΜΙΑ ΠΑΓΚΟΣΜΙΑ ΕΤΑΙΡΕΙΑ:

  🏢 ΝΕΑΣ ΔΟΜΗΣ ENTERPRISE ARCHITECTURE

  📁 1. ΚΕΝΤΡΙΚΟΣ CONFIG ΦΑΚΕΛΟΣ:

  src/config/company-gemi/
  ├── index.ts                          # Main exports & API
  ├── core/                             # Core configurations
  │   ├── field-types.ts                # FieldType & interfaces
  │   ├── section-registry.ts           # Section management
  │   └── field-registry.ts             # Field management
  ├── labels/                           # Label management
  │   ├── field-labels.ts               # Field labels mapping
  │   ├── help-texts.ts                 # Help texts mapping
  │   └── translations.ts               # i18n support (future)
  ├── options/                          # Select options
  │   ├── legal-forms.ts                # Legal form options
  │   ├── currencies.ts                 # Currency options
  │   ├── gemi-statuses.ts              # GEMI status options
  │   ├── activity-types.ts             # Activity type options
  │   └── document-types.ts             # Document type options
  ├── sections/                         # Section definitions
  │   ├── basic-info.ts                 # Section 1: Βασικά στοιχεία
  │   ├── activities.ts                 # Section 2: Δραστηριότητες
  │   ├── capital.ts                    # Section 3: Κεφάλαιο
  │   ├── dates-location.ts             # Section 4: Ημερομηνίες
  │   ├── addresses.ts                  # Section 5: Διευθύνσεις
  │   ├── shareholders.ts               # Section 6: Μέτοχοι
  │   ├── documents.ts                  # Section 7: Έγγραφα
  │   ├── decisions.ts                  # Section 8: Αποφάσεις
  │   ├── history.ts                    # Section 9: Ιστορικό
  │   ├── representatives.ts            # Section 10: Εκπρόσωποι
  │   ├── announcements.ts              # Section 11: Ανακοινώσεις
  │   ├── statuses.ts                   # Section 12: Καταστάσεις
  │   ├── photos.ts                     # Section 13: Φωτογραφίες
  │   └── relationships.ts              # Section 14: Σχέσεις
  ├── services/                         # Business logic
  │   ├── field-validator.ts            # Field validation rules
  │   ├── section-loader.ts             # Dynamic section loading
  │   └── enterprise-loader.ts          # Enterprise business rules integration
  └── utils/                            # Utility functions
      ├── field-utilities.ts            # Field helper functions
      ├── section-utilities.ts          # Section helper functions
      └── data-mappers.ts               # Data transformation utilities

  📁 2. ENTERPRISE BUSINESS RULES INTEGRATION:

  src/services/business/company-gemi/
  ├── legal-forms-service.ts            # Legal forms business logic
  ├── company-statuses-service.ts       # Company status workflows
  ├── validation-service.ts             # Business validation rules
  └── compliance-service.ts             # Regulatory compliance

  📁 3. UI COMPONENTS ΦΑΚΕΛΟΣ:

  src/components/company-gemi/
  ├── form/                             # Form components
  │   ├── CompanyGemiForm.tsx           # Main form component
  │   ├── SectionRenderer.tsx           # Section rendering logic
  │   └── FieldRenderer.tsx             # Field rendering logic
  ├── sections/                         # Section-specific components
  │   ├── BasicInfoSection.tsx          # Custom basic info component
  │   ├── ShareholdersSection.tsx       # Custom shareholders component
  │   └── PhotosSection.tsx             # Custom photos component
  └── shared/                           # Shared components
      ├── SectionHeader.tsx             # Section header component
      ├── FieldGroup.tsx                # Field grouping component
      └── HelpTextDisplay.tsx           # Help text component

  🎯 ΣΥΓΚΕΚΡΙΜΕΝΑ ΑΡΧΕΙΑ ΔΙΑΣΠΑΣΗΣ:

  1. ΚΕΝΤΡΙΚΟ ENTRY POINT (src/config/company-gemi/index.ts):

  // Export το σύνολο του API με clean interface
  export { COMPANY_GEMI_SECTIONS } from './core/section-registry';
  export { getAllCompanyFields, getCompanySection } from './utils/field-utilities';
  export {
    loadLegalForms,
    loadCompanyStatuses,
    getEnterpriseLegalForms
  } from './services/enterprise-loader';
  export type {
    FieldConfig,
    SectionConfig,
    SelectOption,
    FieldType
  } from './core/field-types';

  2. TYPES & INTERFACES (src/config/company-gemi/core/field-types.ts):

  // Μόνο τα types και interfaces - καμία business logic
  export type FieldType = 'input' | 'select' | 'textarea' | 'date' | 'number' | 'email' | 'tel';

  export interface SelectOption {
    value: string;
    label: string;
  }

  export interface FieldConfig {
    id: string;
    label: string;
    type: FieldType;
    // ... rest of interface
  }

  export interface SectionConfig {
    id: string;
    title: string;
    icon: string;
    // ... rest of interface
  }

  3. LABELS MANAGEMENT (src/config/company-gemi/labels/field-labels.ts):

  // Import από centralized system και mapping
  import { MODAL_SELECT_COMPANY_FIELD_LABELS } from '../../subapps/dxf-viewer/config/modal-select/core/labels/fields';

  export const COMPANY_FIELD_LABELS = MODAL_SELECT_COMPANY_FIELD_LABELS;

  // Export με consistent naming
  export const fieldLabels = {
    companyName: COMPANY_FIELD_LABELS.company_name,
    tradeName: COMPANY_FIELD_LABELS.trade_name,
    // ... rest of mappings
  };

  4. SELECT OPTIONS (src/config/company-gemi/options/legal-forms.ts):

  // Συγκεκριμένα για legal forms με enterprise integration
  import { getLegalFormOptions } from '../../subapps/dxf-viewer/config/modal-select';
  import { businessRulesService } from '../../services/business/EnterpriseBusinessRulesService';

  export const getLegalFormOptionsForGemi = (): SelectOption[] => {
    const centralizedForms = getLegalFormOptions();
    return centralizedForms.map(form => ({
      value: form.value.toUpperCase(),
      label: form.label
    }));
  };

  export const loadEnterpriseLegalForms = async (options: EnterpriseOptions) => {
    // Enterprise business rules integration
    return await businessRulesService.getLegalFormsForSelect(
      options.tenantId,
      options.jurisdiction,
      options.environment
    );
  };

  5. SECTIONS ΑΡΧΕΙΑ (src/config/company-gemi/sections/basic-info.ts):

  // Κάθε section στο δικό του αρχείο
  import { SectionConfig } from '../core/field-types';
  import { fieldLabels } from '../labels/field-labels';
  import { helpTexts } from '../labels/help-texts';
  import { getLegalFormOptionsForGemi, getGemiStatusOptionsForGemi } from '../options';

  export const basicInfoSection: SectionConfig = {
    id: 'basicInfo',
    title: 'Βασικά Στοιχεία ΓΕΜΗ',
    icon: 'info',
    description: 'Βασικές πληροφορίες εταιρείας από το ΓΕΜΗ',
    order: 1,
    fields: [
      {
        id: 'companyName',
        label: fieldLabels.companyName,
        type: 'input',
        required: true,
        helpText: helpTexts.companyNameHelp,
      },
      // ... rest of fields
    ],
  };

  6. SECTION REGISTRY (src/config/company-gemi/core/section-registry.ts):

  // Centralized registry όλων των sections
  import { basicInfoSection } from '../sections/basic-info';
  import { activitiesSection } from '../sections/activities';
  import { capitalSection } from '../sections/capital';
  // ... import all sections

  export const COMPANY_GEMI_SECTIONS: SectionConfig[] = [
    basicInfoSection,
    activitiesSection,
    capitalSection,
    // ... all sections
  ].sort((a, b) => a.order - b.order); // Auto-sort by order

  🎯 ΠΛΕΟΝΕΚΤΗΜΑΤΑ ΝΕΑΣ ΔΟΜΗΣ:

  ✅ MAINTAINABILITY:

  - Κάθε section είναι ανεξάρτητο αρχείο (50-100 γραμμές)
  - Εύκολη προσθήκη νέων sections χωρίς επίδραση στα υπόλοιπα
  - Καθαρό separation of concerns

  ✅ SCALABILITY:

  - Modular imports - tree-shaking friendly
  - Lazy loading δυνατότητα για sections
  - Easy testing - κάθε section μπορεί να tested ανεξάρτητα

  ✅ ENTERPRISE QUALITY:

  - TypeScript-first approach
  - Consistent API patterns
  - Centralized business rules integration
  - Zero code duplication

  ✅ DEVELOPER EXPERIENCE:

  - Predictable file locations
  - Self-documenting structure
  - Easy to onboard new developers

  Γιώργο, να προχωρήσουμε με αυτή την enterprise διάσπαση; Θα δημιουργήσουμε το νέο directory structure και θα μεταφέρουμε τον κώδικα με συστηματικό τρόπο.
  