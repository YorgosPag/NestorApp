# ADR-312 — Property Showcase (SSoT Composition)

| Field | Value |
|-------|-------|
| **Status** | ✅ IMPLEMENTED (Phase 7.6 — description free-form per ogni parking/storage linked, SSoT mapping in `snapshot-field-builders.buildLinkedSpaces`) |
| **Date** | 2026-04-18 |
| **Category** | Properties / Public Share Surfaces |
| **Canonical Location** | `src/app/showcase/[token]/`, `src/components/property-showcase/`, `src/app/api/{showcase,properties}/...`, `src/services/property-showcase/` (SSoT snapshot + labels) |

---

## 1. Problem

Sales & brokers need to hand prospects a self-contained view of a property: photos, floorplans, a short description, core specs, a PDF they can keep, and a link that works without a login. The existing application only exposes the authenticated `/properties` tree (invisible to outside contacts) and the `/shared/[token]` page (generic file preview, no property context).

**Pain point**: there is no single surface that bundles a property's public-facing content (brand header, photos, floorplans, video, PDF) behind one revocable link.

---

## 2. Decision

**Compose existing SSoT — do not introduce a new share system.**

- Reuse `FileShareService` + `FILE_SHARES` collection for token, TTL, revocation, tenant isolation.
- Reuse `PropertyShowcasePDFService` built on the existing `jspdf`/`JSPDFAdapter`/`TextRenderer` primitives under `src/services/pdf/`.
- Reuse `getAdminFirestore()` / `getAdminStorage()` / `withAuth` / `withStandardRateLimit` patterns already used by `/api/properties/[id]/generate-description`.
- Reuse `createEntityAction` preset registry for the header action button; add one new preset `showcase`.
- Reuse `GRADIENT_HOVER_EFFECTS` tokens; add one new token `VIOLET`.

**Scope change to FileShareRecord**: three optional fields (no migration required):

| Field | Purpose |
|-------|---------|
| `showcasePropertyId?: string` | Links a share to the property it represents |
| `showcaseMode?: boolean` | Distinguishes showcase shares from regular file shares |
| `pdfStoragePath?: string` | Server path to the generated PDF (for public download URL reconstruction) |

---

## 3. Architecture

| Layer | Path | SSoT anchor |
|-------|------|-------------|
| PDF renderer | `src/services/pdf/renderers/PropertyShowcaseRenderer.ts` | Uses `IPDFDoc`, `TextRenderer`, `COLORS`, `FONT_SIZES` from the existing `src/services/pdf/` module. Includes `drawPhotoGrid()` which calls `IPDFDoc.addImage()` for each embedded photo |
| PDF service | `src/services/pdf/PropertyShowcasePDFService.ts` | Dynamic `jspdf` import (server-compatible); wraps result in `JSPDFAdapter` (which now exposes `addImage` on the contract) |
| Property media reader | `src/services/property-media/property-media.service.ts` | SSoT for reading `files`-collection media owned by a property (photos, floorplans). Exposes `listPropertyMedia` (metadata), `downloadPropertyMedia` (metadata + Storage buffers, JPEG/PNG filter for PDF embedding), `countPropertyMedia`. Registered in `.ssot-registry.json` as `property-media-reader` so future inline `entityType=='property'` queries on `files` are caught by the SSoT ratchet |
| Generate/Revoke API | `src/app/api/properties/[id]/showcase/generate/route.ts` | `withAuth` + `withStandardRateLimit`; tenant isolation on `companyId`; `'properties:properties:read'` for POST, `'...:update'` for DELETE |
| Public resolver API | `src/app/api/showcase/[token]/route.ts` | Anonymous GET; validates share via Admin SDK (bypasses client rules); returns property snapshot + photos/floorplans from `files` collection |
| Public PDF proxy | `src/app/api/showcase/[token]/pdf/route.ts` | Anonymous GET; re-validates share + tenant cross-check; streams PDF via Admin SDK `createReadStream()`; rate-limited (`withStandardRateLimit`); increments `downloadCount` fire-and-forget |
| Public page (server) | `src/app/showcase/[token]/page.tsx` | Exports `robots: { index: false, follow: false }` metadata; delegates to client renderer |
| Public page (client) | `src/components/property-showcase/ShowcaseClient.tsx` + `Showcase{Header,Specs,PhotoGrid,Floorplans,VideoEmbed}.tsx` | Tailwind-only styling, no inline styles; i18n through `showcase` namespace |
| Dialog (action) | `src/features/properties-sidebar/components/PropertyShowcaseDialog.tsx` | shadcn `Dialog`, `Button`; posts/deletes to the generate API |
| Header wiring | `PropertyDetailsHeader.tsx` + `PropertiesSidebar.tsx` | New `onShowcaseProperty` prop; violet action button between `new` and `delete` |
| i18n | `src/i18n/locales/{el,en,pseudo}/showcase.json` + `properties-detail.json` (`showcase.*`, `navigation.actions.showcase.*`) | Registered in `lazy-config.ts`, `namespace-loaders.ts`, `namespace-manifest.json` |

---

## 4. Data Flow — Generate

```
┌─────────────┐    POST /api/properties/{id}/showcase/generate
│  Dialog UI  │──────────────────────────────────────────────────▶
└─────────────┘                                                    │
                                                                   ▼
                                            ┌────────────────────────────┐
                                            │ withAuth + rate-limit      │
                                            │ + tenant-isolation check   │
                                            └─────────────┬──────────────┘
                                                          │
            ┌─────────────────────────────────────────────┴──────────────┐
            │ loadShowcaseSources(propertyId, companyId) via Admin SDK    │
            │  - property doc                                              │
            │  - company doc (branding)                                    │
            │  - PropertyMediaService.countPropertyMedia(photos)           │
            │  - PropertyMediaService.countPropertyMedia(floorplans)       │
            │    + legacy count(unit_floorplans) for backwards-compat      │
            └─────────────────────────────────────┬─────────────────────┘
                                                   │
            ┌─────────────────────────────────────┴─────────────────────┐
            │ loadShowcasePhotos(propertyId, companyId, limit=6)         │
            │  → PropertyMediaService.downloadPropertyMedia()            │
            │    (Admin SDK Storage download, JPEG/PNG only, base64)     │
            └─────────────────────────────────────┬─────────────────────┘
                                                   │
                              ┌────────────────────┴──────────────────┐
                              │ PropertyShowcasePDFService.generate() │
                              │   → Uint8Array                         │
                              └────────────────────┬──────────────────┘
                                                   │
                              ┌────────────────────┴──────────────────┐
                              │ uploadPdfToStorage()                   │
                              │   bucket().file(companies/{companyId}/ │
                              │     property-showcases/{propertyId}/   │
                              │     {shareId}.pdf)                     │
                              │   .save(buffer).makePublic()           │
                              └────────────────────┬──────────────────┘
                                                   │
                              ┌────────────────────┴──────────────────┐
                              │ adminDb.collection(FILE_SHARES)        │
                              │   .doc(shareId).set({                  │
                              │     token, companyId, createdBy,       │
                              │     expiresAt: +30 days,               │
                              │     showcaseMode: true,                │
                              │     showcasePropertyId,                │
                              │     pdfStoragePath                     │
                              │   })                                   │
                              └────────────────────┬──────────────────┘
                                                   │
                                                   ▼
                            Response: { token, pdfUrl, richUrl, expiresAt }
```

---

## 5. MVP Decisions

| Knob | Decision | Reason |
|------|----------|--------|
| TTL | Fixed 30 days (720 h) | No UI selector — keep dialog simple |
| Password | Disabled | Low-friction sharing beats link-protection at this stage |
| Analytics | Relies on existing `downloadCount` | No bespoke analytics UI |
| Video embed | Stored in `FileShareRecord.note` (hijacked, MVP); rendered as external link | Avoids a fourth new field |
| Email | Not wired to the dialog (MVP) | `/api/communications/email/property-share` already exists — future wire |
| Photo/floorplan source | `PropertyMediaService` (reads `files` coll, `entityType='property'`, `category∈{photos,floorplans}`, tenant-scoped) | SSoT per ADR-191 / ADR-292; one service consumed by the public resolver, the PDF generator and future surfaces |
| PDF embedding | Up to 6 photos embedded on a dedicated second page (3×2 grid), downloaded server-side via Admin SDK Storage, JPEG/PNG only | Photos are critical for sales handoff; Admin-SDK download avoids fetching the public `downloadUrl` (no token-expiry risk) and jsPDF's `addImage(..., 'FAST')` skips recompression so the total PDF stays ≲ 2 MB for typical property photo sets |

---

## 6. Security Considerations

- **Firestore rules** — `file_shares` is already public-read (required for anonymous token validation on `/shared/[token]`). The public showcase endpoint does not rely on rules: it runs under Admin SDK and re-checks `showcaseMode=true`, active state, and expiry.
- **Tenant isolation** — generate/delete endpoints assert `property.companyId === ctx.companyId` before touching Storage or Firestore. The public resolver cross-checks `share.companyId === property.companyId` to refuse a forged share ID pointing at a foreign tenant's property.
- **Token entropy** — 32-char URL-safe random (62 alphabet). Matches `FileShareService`'s own generator.
- **Showcase PDF URL** — the PDF object is **never public**. Download is served by `/api/showcase/[token]/pdf`, which validates the share token, cross-checks `share.companyId === property.companyId`, and streams via Admin SDK. Revoking the share (`isActive=false`) immediately invalidates the PDF URL — no blob deletion required.
- **Rate limit** — POST/DELETE use `withStandardRateLimit` (60/min per user). The anonymous GET resolver has no rate limit in MVP; add IP-based limit if abuse appears.

---

## 7. Alternatives Considered

| Option | Rejected because |
|--------|------------------|
| Greenfield `/api/showcase/*` with bespoke `showcase_shares` collection | Duplicates FILE_SHARES, splits RBAC, doubles rules surface |
| ~~Embed photos inside the PDF~~ (Phase 1 deferral) | Initially rejected for MVP; reversed in Phase 2 — see §5 and changelog entry `Phase 2: PDF photo embedding`. Mitigations: capped at 6 photos (1 page), Admin SDK bypasses Storage public URLs, jsPDF `'FAST'` compression avoids re-encoding the buffer |
| Client-side PDF generation + upload | Duplicates trust boundary (client-generated artifact uploaded to Storage, then marked "branded") |
| Add dedicated `showcase_shares` collection later if FILE_SHARES gets crowded | Deferred: current volume is trivial; one-file schema is cheaper to operate |

---

## 8. Consequences

**Positive**
- Zero new runtime services, one new renderer, one new page, one new dialog. Everything else is composition.
- One SSoT for every showcase link (FILE_SHARES): revocation, TTL, audit, download-count all free.
- Rich page stays thin (~200 lines split across 5 UI files) because data prep is server-side.

**Negative**
- `FileShareRecord.note` is overloaded (general-purpose note AND showcase video URL). Resolve later by adding a dedicated field when/if video moves to a native property attribute.
- Phase 2 closes the "PDF lacks embedded photos" gap; the renderer now always attempts a 6-photo grid and degrades silently to text-only when no compatible photos are present.
- `/api/showcase/[token]` is unrated — revisit under abuse or when exposing the surface externally.

---

## 9. Changelog

| Date | Change |
|------|--------|
| 2026-04-17 | Initial MVP implementation + ADR filing (shareId collision with the camera-capture ADR forced the number bump from 311 → 312) |
| 2026-04-18 | Attempted fix by reverting `FIREBASE_STORAGE_BUCKET` to `.appspot.com` — rolled back: GCS returned `The specified bucket does not exist.` The canonical bucket for this project is `pagonis-87766.firebasestorage.app` (only `.firebasestorage.app` exists; no legacy `.appspot.com` bucket was ever created). |
| 2026-04-18 | **Fixed signed URL incompatibility with `.firebasestorage.app` buckets.** Root cause: GCS XML API (host `storage.googleapis.com/{bucket}/...`) returns `NoSuchKey` for objects in `.firebasestorage.app` buckets because XML API does not serve that host domain — only JSON API and the Firebase download-token endpoint do. Signed URLs are XML-API-based, so they are fundamentally incompatible with this bucket class. **Fix**: replaced `getSignedUrl()` with the Firebase download-token pattern. On upload, `uploadPdfToStorage()` sets `metadata.firebaseStorageDownloadTokens = crypto.randomUUID()` and returns `{ url, downloadToken }` where `url = https://firebasestorage.googleapis.com/v0/b/{bucket}/o/{encodedPath}?alt=media&token={token}` (permanent URL, valid while both file and token metadata persist). `writeShowcaseShareRecord()` stores `pdfDownloadToken` on the `FILE_SHARES` doc alongside `pdfStoragePath`. Public resolver `/api/showcase/[token]` rebuilds the URL from `bucket.name + storagePath + downloadToken` without any signing round-trip. `FileShareRecord` + `CreateShareInput` gain `pdfDownloadToken?: string`. Exported helper `buildDownloadTokenUrl()` is the single point of URL construction. Revoke deactivates the share; to fully invalidate the PDF URL, future hardening can rotate the `firebaseStorageDownloadTokens` metadata via Admin SDK. |
| 2026-04-18 | **Bucket resolver SSoT (`getAdminBucket()`).** The proxy `exists()` check returned `false` for a PDF that had just been uploaded successfully (upload's own post-`save()` `exists()` passed). Root cause: `getAdminStorage().bucket()` (no-arg) relies on the Admin SDK's implicit default, which under Next.js dev hot-reload can momentarily resolve the bucket against the legacy `{projectId}.appspot.com` alias, producing cross-request inconsistencies on `.firebasestorage.app` buckets. **Fix**: added `getAdminBucket()` in `src/lib/firebaseAdmin.ts` — the single source of truth for server-side Storage. It requires `FIREBASE_STORAGE_BUCKET` explicitly (throws if unset) and returns `getAdminStorage().bucket(envBucketName)`. Migrated `uploadPdfToStorage()` + `streamPdfFromStorage()` to use it. Every future server-side Storage access must go through this helper. |
| 2026-04-18 | **Replaced Firebase download-token URL with server-side proxy endpoint `/api/showcase/[token]/pdf`.** Root cause: buckets on the `.firebasestorage.app` domain do **not** honor the download-token bypass over `storage.rules`, returning `403 Permission denied` even with valid token metadata (verified via `getMetadata()` — token writes succeeded but download was still denied). The legacy `.appspot.com` bucket this project would have needed does not exist in GCS (`The specified bucket does not exist.`), so reverting the bucket is not an option. **Fix**: the PDF is now streamed by a new anonymous route (`src/app/api/showcase/[token]/pdf/route.ts`) that reuses the existing showcase-share validation (active + `showcaseMode` + not expired), cross-checks `share.companyId === property.companyId`, and streams via Admin SDK `createReadStream()` — bypasses `storage.rules` by design. The response sets `Content-Disposition: attachment` with a filename derived from `property.code` + `property.name`, and increments `share.downloadCount` fire-and-forget. Rate-limited with `withStandardRateLimit` (anonymous IP-keyed). Cleanup: removed `pdfDownloadToken` from `FileShareRecord`/`CreateShareInput`, removed `generateOpaqueToken()` usage + `setMetadata({ firebaseStorageDownloadTokens })` call + post-upload diagnostic read-back + exported `buildDownloadTokenUrl()` helper. `uploadPdfToStorage()` now returns `void` and only performs `save()` + `exists()` verification. Architecture is simpler: the share token is the single authorization for the PDF — no separate download token, no metadata writes, no public blob. Revocation (`isActive=false`) instantly invalidates the download URL. |
| 2026-04-17 | **Fixed showcase PDFs being deleted by `onStorageFinalize` orphan-cleanup trigger (§Race).** Root cause: `functions/src/storage/orphan-cleanup.ts` verified file ownership against a **single hardcoded collection** (`FILES`). Showcase PDFs write their ownership claim only into `FILE_SHARES` per ADR-312, so every freshly uploaded showcase PDF was mis-classified as orphan and deleted ~8 s after upload (verified via MCP `storage_list_files`: `photos/files/` populated, `documents/files/` permanently empty; the upload's post-`save()` `exists()` passed because the trigger had not yet fired). Audit-log confirmed the deletion (`action: ORPHAN_FILE_DELETED`, `entityId: share_92c01cf6-...`, size 8208, contentType `application/pdf`, by `system_onFinalize`). **Fix — SSoT ownership resolver**: new module `functions/src/shared/file-ownership-resolver.ts` exposes `findFileOwner(db, fileId)` backed by an ordered registry `OWNERSHIP_CLAIM_PROVIDERS` (FILES → FILE_SHARES → future). Adding a new file-producing system that does not use `FILES` = add **one** entry to that registry; every orphan check automatically benefits. `onStorageFinalize` now delegates to `findFileOwner()` instead of hardcoding the `FILES` lookup. **Fix — race elimination**: `/api/properties/[id]/showcase/generate/route.ts` now writes the `FILE_SHARES` ownership claim **before** `uploadPdfToStorage()` (so the trigger always finds the claim), with a compensation `deleteShowcaseShareRecord(shareId)` that removes the orphaned claim if the upload itself fails. Added `FILE_SHARES: 'file_shares'` to `functions/src/config/firestore-collections.ts` (SSoT mirror). Deployed to `us-central1` (versionId 1 → 2, 2026-04-17T19:17:55Z). Also repaired a pre-existing compile error (`functions/src/search/indexBuilder.ts` missing `COLLECTIONS` import) that blocked the deploy. |
| 2026-04-17 | **Phase 2 hotfix: jsPDF string path silently drops images on Node.** Giorgio regenerated the PDF after Phase 2 shipped and still saw no photos. Root cause: the initial Phase 2 impl fed `addImage()` a raw base64 string. On the browser jsPDF decodes the string through the `Image` constructor; on Node that global does not exist and jsPDF silently renders an empty cell — the PDF is structurally valid but visually blank. Intermediate attempt: prefixed the base64 with a `data:image/...;base64,` URI (same failure path, same silent empty cell). **Fix**: `PropertyMediaService.downloadPropertyMedia()` now returns `bytes: Uint8Array` (sliced to the Buffer's logical range to avoid leaking Node's pooled allocator into the PDF stream). `ShowcasePhotoAsset.bytes` replaces `base64`. `drawPhotoGrid()` calls `doc.addImage(photo.bytes, ...)` — the Uint8Array path skips the browser `Image` lookup entirely. **Tests**: `PropertyShowcaseRenderer.test.ts` pins the renderer contract (forwards raw `Uint8Array`, never a string; 3-col grid; one `addPage()` per photo set), and `JSPDFAdapter.addImage.test.ts` is an integration test against real jsPDF that proves embedding a Uint8Array JPEG grows the PDF byte stream — so this regression cannot re-ship unnoticed. |
| 2026-04-17 | **Phase 2: PDF photo embedding + `PropertyMediaService` SSoT extraction.** Root motivation: the MVP PDF was text-only (see §5 Phase 1) and sales workflow needs photos in the hand-off artifact. Two problems fixed together: (1) `loadShowcaseSources.photoCount` counted from the subcollection `properties/{id}/photos` which was **never populated** in this codebase — the real SSoT for property photos is the top-level `files` collection, already used by the public showcase API (`api/showcase/[token]/route.ts::loadFilesByCategory`). Count always returned 0. (2) No image embedding primitive existed on `IPDFDoc`. **Fix — SSoT service**: extracted `src/services/property-media/property-media.service.ts` exposing `listPropertyMedia` / `downloadPropertyMedia` / `countPropertyMedia`, centralising the canonical filter set (`companyId + entityType=property + entityId + category`), active-lifecycle pruning, `createdAt desc` sort, and jsPDF mime allowlist (JPEG/PNG). Refactored `api/showcase/[token]/route.ts` to delegate to the service (removed 25 LOC of inline query). Registered the new SSoT module as `property-media-reader` in `.ssot-registry.json` so future inline `.collection(COLLECTIONS.FILES).where('entityType','==','property')` queries get blocked by the ratchet. **Fix — PDF primitive**: added `addImage(imageData, format, x, y, w, h, alias?, compression?)` to the `IPDFDoc` contract and implemented it on `JSPDFAdapter` (1-line delegate to jsPDF's `addImage`). **Fix — renderer**: added `ShowcasePhotoAsset` type + optional `photos?: ShowcasePhotoAsset[]` on `PropertyShowcasePDFData`. New `drawPhotoGrid()` renders up to 6 photos on a dedicated 2nd page (3-col × 2-row grid, cell `w=(contentWidth - 2·gap)/3`, fixed 4:3 aspect, `'FAST'` compression to skip recompression). Silent no-op if zero photos → text-only path unchanged. `drawFooter()` now iterates `getNumberOfPages()` so branding + expiry appear on both pages. **Fix — generator wiring**: `route.ts::handleGenerate` fetches `loadShowcasePhotos(propertyId, companyId)` in parallel with `loadShowcaseSources` and passes the result to `buildPdfData`. Failures inside `loadShowcasePhotos` are caught and logged; the generator never fails because embedding broke — worst case the PDF degrades to Phase 1 text-only behaviour. i18n labels: `photosSection` added to `PropertyShowcasePDFLabels` (EL: "Φωτογραφίες", EN: "Photos"). Size envelope: ~6×200 KB JPEGs ≈ 1.5 MB PDF, well under ADR-312 previous text-only `< 1 MB` target but acceptable trade-off for embedded photos. Files touched: 5 code + 1 ADR + 1 SSoT registry entry. No schema migration. |
| 2026-04-17 | **Fixed corrupted Greek glyphs in generated showcase PDFs.** Root cause: `PropertyShowcasePDFService.createDoc()` never invoked `registerGreekFont()` and `TextRenderer` hardcoded `FONTS.HELVETICA` in every `doc.setFont()` call — Helvetica has no Unicode map, so every Greek character rendered as gibberish (Giorgio observed on download of `A-DI-1.03-130.pdf`). **Fix — SSoT font plumbing**: (1) `FONTS.UNICODE = 'Roboto'` added to `src/services/pdf/layout.ts` as the canonical Unicode-capable font family. (2) `TextRenderer` gains an optional `{ font }` constructor option (default `FONTS.HELVETICA`, backward-compatible for all existing call sites). Both `setFont` call sites inside `TextRenderer` now honour the configured font instead of hardcoding Helvetica. (3) `PropertyShowcasePDFService.createDoc()` now `await registerGreekFont(instance)` after the `new JsPDF(...)` to register Roboto with `Identity-H` encoding (reuses the ADR-267 SSoT loader). (4) `PropertyShowcaseRenderer` instantiates `new TextRenderer({ font: FONTS.UNICODE })` so every label and user-supplied string is drawn with Roboto. Other `TextRenderer` consumers (`CoverRenderer`, `TOCRenderer`, `ContentRenderer` under `PDFExportService`) keep Latin-only Helvetica as before — no regression — and can opt into Unicode incrementally by passing `{ font: FONTS.UNICODE }` once they register the Roboto font themselves. |
| 2026-04-17 | **Phase 2.1 diagnostic: pooled Node `Buffer` backing store fix + debug endpoint.** Giorgio regenerated the PDF after the Phase 2 hotfix and still saw no photos. Root-cause candidate: `PropertyMediaService.downloadPropertyMedia()` wrapped the GCS `Buffer` in `new Uint8Array(buf.buffer, buf.byteOffset, buf.byteLength)` — a **view** on Node's pooled `ArrayBuffer`. Any jsPDF code path that dereferences `.buffer` without honouring `byteOffset` (common in typed-array conversion helpers) reads across pool boundaries and sees neighbouring allocations, producing invalid-image bytes. Test-only path worked because the 134-byte tiny JPEG in the unit fixture was never pooled. **Fix — real copy**: `downloadPropertyMedia()` now allocates a fresh `new Uint8Array(len)` and `.set()`s the buffer into it. Cheap for ≤6 images per showcase and decouples jsPDF from the allocator. **Diagnostic surface**: `loadShowcasePhotos()` now logs a success entry (`count`, `totalBytes`, `formats`) — earlier we only logged failure, making "embedding skipped silently" indistinguishable from "no photos". `drawPhotoGrid()` now `console.error`s the real `addImage` error (message + stack + magic bytes) instead of swallowing it; grey-rect fallback is kept so a single broken image never aborts the whole PDF. New dev-only endpoint `GET /api/debug/property-showcase-photos?propertyId=X&mode=json|pdf` bypasses the full showcase renderer: JSON mode reports `{count, totalBytes, photos:[{id, format, bytesLen, magicHex, storagePath}]}` so Giorgio can verify the download path in isolation, and PDF mode generates a minimal 1-page grid-only PDF with a fresh `jsPDF` (no Greek font, no header/footer) to isolate the image pipeline from upstream rendering. Guarded by `NODE_ENV !== 'production'` + `withAuth('properties:properties:update')` + standard rate limit. Removed once Phase 2 photo embedding is confirmed stable in prod. |
| 2026-04-18 | **Phase 3.1 hotfix: DXF thumbnail back-fill crashed on every legacy file with `Unexpected token '\u001f'`.** Root cause: the processed JSON payload `.dxf.processed.json` is written by the browser-side DXF pipeline (ADR-033) via `pako.gzip` — the first two bytes are the gzip magic `0x1f 0x8b`. The initial Phase 3 `rasterizeAndPersist()` and the Cloud Function trigger both assumed plain UTF-8 and called `JSON.parse(buffer.toString('utf8'))` directly, so every existing processed file failed on the first token. Only the subset of DXFs that happened to have their processed JSON written before gzip was enabled would have worked, and in this project that subset is empty. **Fix**: new SSoT decoder `src/app/api/admin/migrate-dxf-thumbnails/decode-processed-json.ts` (mirrored into `functions/src/shared/decode-processed-json.ts` so the Cloud Function bundle stays self-contained) inspects the gzip magic bytes and calls `zlib.gunzipSync()` when they are present, falling back to the plain-UTF-8 path otherwise. Both consumers now import this decoder. A new SSoT registry entry `decode-processed-dxf-json` allow-lists only those two files and forbids `gunzipSync(...processed...)` inline anywhere else. Regression test `decodeProcessedJsonBytes.test.ts` (jest) pins the contract: plain JSON, gzipped JSON, and short-buffer non-gzip are all handled correctly. **Verification**: re-ran `scripts/run-dxf-thumbnail-migration.ts` against company `comp_9c7c1a50-f370-466d-bdf7-aa7b2b2d7757` — 2/2 DXFs rasterised (`file_0a802943…` and `file_c4c6d2c2…`), 1169 entities each, 55.7 KB PNG each, 200 on the public download URL. The Cloud Function trigger will benefit from the same fix on next deploy (no natural `.processed.json` upload hit the mis-decoded path because the trigger was deployed the same day and no new DXFs have been uploaded since). **Dev ergonomics**: added `scripts/run-dxf-thumbnail-migration.ts` as a local-only CLI wrapper around `runDxfThumbnailMigration()` — uses the service-account credentials from `.env.local` via a small manual parser (dotenv is not a direct dep) and shims the Next.js `server-only` marker through `Module._resolveFilename` so the helper can be loaded standalone. Removes the need to obtain a super-admin Firebase ID token just to back-fill legacy files during development. |
| 2026-04-18 | **Phase 3: DXF Κατόψεις in the public showcase page AND the PDF.** Sales handed off showcase links whose Κάτοψη section rendered as a naked DXF download link — unreadable on a browser — and whose PDF had no floorplan at all. Root constraint: jsPDF (and every browser) can only embed raster images. DXFs are vector CAD files. **Decision**: every DXF in the `files` collection gets an on-upload PNG raster companion (`thumbnailStoragePath` + `thumbnailUrl`), generated once and reused everywhere. **SSoT rasterization pipeline**: (1) new `src/services/dxf-raster/svg-from-dxf-scene.ts` serialises the browser-parsed `DxfSceneData` (lines, polylines, circles, arcs, text) to an SVG 1.1 string, mirroring the browser thumbnail generator's Y-flip + layer-colour + bounds-fit maths so a DXF that renders correctly in the Viewer produces the same-looking thumbnail server-side. (2) new `src/services/dxf-raster/dxf-raster-generator.ts` funnels SVG→PNG through `@resvg/resvg-js` (pure WASM, no native deps — works inside the Cloud Functions sandbox without cloudbuild tweaks). Both files are kept **byte-for-byte identical** inside `functions/src/shared/` so the Cloud Functions bundle stays self-contained; the `.ssot-registry.json` entry `dxf-raster-generator` forbids inline `new Resvg(…)` anywhere else. **Trigger**: new `functions/src/storage/dxf-thumbnail-onfinalize.ts` fires on Storage uploads matching `*.dxf.processed.json` (the browser-side DXF parser's artifact — signals the scene is renderable), rasterises the PNG to `{dxfPath}.thumbnail.png` with a `firebaseStorageDownloadTokens` UUID so the object is fetchable from anonymous showcase visitors, and patches the Firestore `files` record with `{thumbnailUrl, thumbnailStoragePath, thumbnailUpdatedAt}`. Idempotent — skips if `thumbnailUrl` already set. Exported from `functions/src/index.ts` as `onDxfProcessedFinalize`. **Reader side**: `PropertyMediaService.downloadPropertyMedia()` now accepts candidates whose `ext === 'dxf' && thumbnailStoragePath` — on download it fetches the PNG instead of the raw DXF, returning `fromThumbnail: true` in the buffer for observability. `listPropertyMedia()` maps `thumbnailUrl`/`thumbnailStoragePath`/`ext` through to callers. **Public showcase page**: `GET /api/showcase/[token]` exposes `previewUrl` + `ext` on each floorplan `ShowcaseMedia`; `ShowcaseFloorplans.tsx` now renders the PNG inline with a "Λήψη αρχείου DXF" / "Download DXF file" link underneath — one grid of proper previews, not a list of download-only hyperlinks. i18n keys added: `floorplans.defaultAlt`, `floorplans.downloadDxf`, `floorplans.previewUnavailable` (EL + EN). **PDF generator**: `loadShowcaseFloorplans()` added to the generator helpers alongside `loadShowcasePhotos()`, pulls ≤4 floorplan PNGs; `PropertyShowcasePDFData.floorplans?: ShowcasePhotoAsset[]` added; renderer gains `drawFloorplanGrid()` — a dedicated page with a 2×2 grid (larger cells than the photo grid because plans need legibility more than density), same diagnostic logging as `drawPhotoGrid` (`console.info` on entry, `console.error` on `addImage` failure with magic bytes + stack). Label `floorplansSection` added to `PropertyShowcasePDFLabels` (EL: "Κατόψεις", EN: "Floorplans"). **Legacy back-fill**: `POST /api/admin/migrate-dxf-thumbnails` (super_admin, `withSensitiveRateLimit`, max 200 per call, dry-run flag) iterates DXFs without `thumbnailUrl` for a given `companyId` and drives them through `rasterizeAndPersist()` — identical code path to the Cloud Function so natural-upload output and back-fill output converge. Files touched: 11 code (src + functions) + 2 locale JSONs + 1 ADR + 1 SSoT registry entry + 1 package.json (added `@resvg/resvg-js` to both root and `functions/`). |
| 2026-04-18 | **Phase 3.5: server-side enum translation for showcase surfaces (property type / orientations / condition).** Sales opened the showcase page and PDF side-by-side and both surfaces displayed raw Firestore enum keys: `apartment` instead of `Διαμέρισμα`, `north, east, south, west` instead of `Βόρειο, Ανατολικό, Νότιο, Δυτικό`, `new` instead of `Νέο`. The web component `ShowcaseSpecs.tsx` passed `p.type || dash` straight through without consulting the `properties-enums` catalog, and `helpers.ts::buildPdfData` forwarded the raw strings into `PropertyShowcasePDFData`. Same gap, two surfaces — needed one fix, not two. **Fix — server-side SSoT module**: new `src/services/property-enum-labels/property-enum-labels.service.ts` exposes `translatePropertyType()`, `translateOrientations()`, `translatePropertyCondition()`. Static-imports the existing `src/i18n/locales/{el,en}/properties-enums.json` catalogs (zero new translations) and looks up keys under `auditTypes` → `types` fallback for type, `units.orientation` for compass directions, `condition` for state. Unknown keys pass through unchanged (graceful degradation when Firestore grows a new enum before the locale catalog). Accepts `EnumLocale = 'el' | 'en'` with `'el'` default since showcase is primarily Greek-facing. **Fix — web surface**: `GET /api/showcase/[token]` now reads an optional `?locale=el|en` query param (default `el`) and returns `typeLabel`, `orientationLabels[]`, `conditionLabel` alongside the original raw fields in the response. `ShowcasePropertySnapshot` gains the three optional label fields; `ShowcaseSpecs.tsx::buildRows()` consumes `p.typeLabel || p.type`, `p.orientationLabels ?? p.orientations` and `p.conditionLabel || p.condition`, so clients receive pre-translated text and no extra i18n namespace is loaded on the public page. **Fix — PDF surface**: `helpers.ts::buildPdfData()` calls the same resolver with the caller-provided locale and writes `typeLabel` (preferred over `p.type`), translated `orientations[]` (labels in place of raw keys), and translated `condition` into `PropertyShowcasePDFData`. `PropertyShowcaseRenderer.drawSpecs()` already preferred `typeLabel`; `orientations.join(', ')` and `safe(condition)` now emit localized text without any renderer change. **Files touched**: 6 — new service (64 LOC), 3 consumers (route, helpers, ShowcaseSpecs + types), ADR. No schema migration, no new i18n keys, no new namespace registration. Tests: existing `PropertyShowcaseRenderer.test.ts` suite still green (no fixture changes required — renderer contract unaffected). |
| 2026-04-18 | **Phase 3.4: web-parity layout for PDF specs grid + media captions.** After Phase 3.3 aligned section order, sales opened the regenerated PDF next to `/showcase/<token>` again and flagged two remaining layout gaps. (1) The specs block on the web is a 2-column `<dl>` (`grid sm:grid-cols-2`, `flex justify-between` per row: label muted-left, value bold-right). The PDF still rendered specs as a single 1-column list printed through `drawField()` (one label-per-row, wrapped text). (2) Photos and floorplans on the web carry a `<figcaption>` with the file's `displayName` under each tile; the PDF grids had no captions, so the handed-off PDF looked like anonymous thumbnails next to labelled ones on screen. **Fix — specs 2-col grid**: rewrote `drawSpecs()` to lay 14 rows in row-major pair order (`[type, code], [building, floor], [grossArea, netArea], ...`) mirroring `ShowcaseSpecs.tsx::buildRows` output and the CSS grid auto-flow. New private helper `drawSpecCell()` renders one label/value pair inside a fixed-width cell: label at cell-left in muted `rgb(107,114,128)` (Tailwind `gray-500`), value at cell-right in black (`doc.text(value, x + width, y, { align: 'right' })`). `columnGap = 8mm`, `rowStep = 6mm`. Halves the vertical footprint of the specs page so it can comfortably host `drawFeatures()` below without a page break for typical 14-row properties. `drawField()` is retained as-is for other call sites (none left in showcase, but it stays generic). Added `FONT_STYLES` to the layout imports for the direct `doc.setFont(FONTS.UNICODE, FONT_STYLES.NORMAL)` call (needed after the section title left the renderer in bold state). **Fix — media captions**: `PropertyShowcaseMediaGrid.drawMediaGridPage()` now reserves a 5mm caption strip under every cell and, when `asset.displayName` is set, prints the first `splitTextToSize(maxWidth=cellWidth)` line centred below the image in muted `rgb(90,90,90)` via new helper `drawAssetCaption()`. Caption font: `FONT_SIZES.SMALL`, Roboto normal. The reserved strip is always accounted for in `rowY` step (cellHeight + captionHeight + gap) AND in the overflow guard (`rowY + cellHeight + captionHeight > maxBottom`) so a caption never gets clipped past the footer band — even when a particular asset has no displayName, the grid stays geometrically identical so floorplan and photo pages line up. Covers both `PHOTO_GRID_CONFIG` and `FLOORPLAN_GRID_CONFIG` with no per-config branching. **Pipeline**: `displayName` was already surfaced by `PropertyMediaService.downloadPropertyMedia()` → `PropertyMediaBuffer.displayName` → `toShowcasePhotoAsset()` → `ShowcasePhotoAsset.displayName`; no data-flow change, this phase only consumes it. **Tests**: the existing `PropertyShowcaseRenderer.test.ts` grid assertion still holds because the row step grew uniformly (row1Y and row2Y still differ by `cellHeight + captionHeight + gap`); no new assertion needed for captions at the unit-test level (fixtures don't carry displayName — production integration covers the printed caption). All 4 suites green. **Files touched**: 3 — renderer, media grid, this ADR. No contract change on `PropertyShowcasePDFData` or `PropertyShowcasePDFLabels`. |
| 2026-04-18 | **Phase 3.3: PDF section order aligned to showcase web.** Sales opened a regenerated PDF side-by-side with the public `/showcase/<token>` page and the section order was different — the PDF opened with a specs table before any photo, while the web surface leads with name+description then photos and only then specs. Inconsistency made the PDF feel like a different document rather than a print companion of the web page. **Fix — `PropertyShowcaseRenderer.render()` reordered** to mirror `ShowcaseClient.tsx` composition: pag 1 cover = brand header + title + code + description; pag 2 = photo grid (Φωτογραφίες, 3×2); pag 3 = specs + features (Χαρακτηριστικά, full table + feature list on the same addPage); pag 4 = floorplan grid (Κατόψεις, 2×2). New helper `drawSpecsPage()` owns the specs-page `addPage()` so the specs block is no longer inlined on the cover. **Removed**: `drawMediaSection()` (photo count, floorplan count, video URL, showcase URL as typed fields) — the web surface does not render those as text because the photos/floorplans/video themselves are visible and the showcase URL is the address bar; keeping a parallel text-only recap in the PDF was redundant. `PropertyShowcasePDFLabels` loses `mediaSection`, `fieldPhotos`, `fieldFloorplans`, `fieldVideo`, `fieldShowcaseUrl`; `buildShowcaseLabels()` in `helpers.ts` loses the same five fields for both EL + EN. `photoCount` / `floorplanCount` / `videoUrl` / `showcaseUrl` stay on `PropertyShowcasePDFData` (the resolver still populates them; future surfaces may want them). **Tests**: `PropertyShowcaseRenderer.test.ts` updated — `getPageCount()` is now 2 for no-photo input (cover + specs page) and 3 for a 3-photo input (cover + photos + specs). **Files touched**: 4 — renderer, helpers, renderer test, this ADR. No API/contract change visible to callers: `PropertyShowcasePDFService.generate()` still returns a single PDF buffer; the regenerate endpoint re-uses the same renderer so existing showcase URLs pick up the new layout on the next `POST /api/properties/[id]/showcase/regenerate` call. |
| 2026-04-18 | **Phase 3.8: Navy brand surface for Property Showcase — SSoT token `--showcase-bg` fixed across light/dark modes.** Sales feedback after Phase 3.7: the showcase page read fine (company name correct, sections in order) but the white/blue palette made it look like any other internal admin page. They wanted the public-facing hand-off surface to look like a premium product page — dark navy background + light text — and to look the same whether the visitor's OS was light or dark. Constraint Giorgio emphasised: *no hardcoded colours in components*; the value must come from the design system. Cross-check showed RGB(29, 40, 58) = HSL(217 33% 17%) was indeed already used in the dark-theme values of `--card` and `--primary` (see `globals.css` `.dark` block), so the colour itself is a first-class brand token — only the showcase-specific surface tokens were missing. **Fix — 5 new tokens in `globals.css`** under `:root` AND `.dark` with *identical* values (brand surface doesn't flip with OS theme): `--showcase-bg: 217 33% 17%` (main navy), `--showcase-surface: 217 28% 22%` (slightly lighter navy for nested cards — enough contrast over the main bg for card separation), `--showcase-border: 217 20% 30%`, `--showcase-fg: 210 40% 98%` (near-white primary text), `--showcase-muted-fg: 215 20% 75%` (secondary/caption text). **Fix — `COLOR_BRIDGE` entries**: added `bg.showcase`, `bg.showcaseSurface`, `text.onShowcase`, `text.onShowcaseMuted`, `border.showcase` — all point at the new CSS variables via `hsl(var(--…))`. Components can still call them through `useSemanticColors()` without touching raw hex. **Fix — component refactor (5 files)**: `ShowcaseClient.tsx` (main `<main>` wrapper + message screens + footer), `ShowcaseSpecs.tsx`, `ShowcasePhotoGrid.tsx`, `ShowcaseFloorplans.tsx`, `ShowcaseVideoEmbed.tsx` — replaced every `bg-gray-50`, `bg-white`, `text-gray-900`, `text-gray-500`, `text-gray-600`, `text-gray-700`, `border-gray-100` reference with the showcase token equivalent. Links on navy switched from `text-violet-700` (low-contrast on dark) to `text-violet-300 hover:text-violet-200`. `ShowcaseHeader.tsx` kept its `bg-gradient-to-r from-violet-600 to-fuchsia-600` — it already contrasts against the new navy, and changing it would have drifted the brand gradient used on the PDF too. The `<img>` elements still sit on `bg-white` in the floorplan cards' preview container — intentional, the vector preview rendered by `@resvg/resvg-js` assumes white paper. **Why same values in both modes**: the brand surface is a presentation artifact for external visitors (prospective buyers who follow a shared link); it has to look identical regardless of the viewer's OS theme, otherwise sales loses control over what the hand-off looks like. Internal admin surfaces (sidebar, modals, dashboards) continue to switch on `.dark` — only showcase is pinned. **PDF parity not changed in this phase**: the PDF rendering still uses the pre-existing `drawBrandHeader()` colours; aligning the PDF to the navy surface is tracked as follow-up if sales asks for it. **Files touched**: 7 — `globals.css` (+13 lines, no deletions), `color-bridge.ts` (3 bridge entries added), 5 showcase components, this ADR entry. No i18n change, no schema migration, no routing impact. |
| 2026-04-18 | **Phase 3.7: hierarchical `company-branding-resolver` — read branding from the real domain graph (Property → Project → Contact), not from the tenant doc.** Phase 3.6 landed the SSoT resolver but Giorgio re-opened the showcase and still saw `Unknown Company` in the brand header. Re-audit exposed the actual architecture error: the showcase route (and `helpers.ts::loadShowcaseSources` + `buildPdfData` for the PDF) was reading from the **tenant-settings** document `companies/{companyId}` — a thin row carrying plan, timezone, feature flags — whose `name` field had been seeded at bootstrap time with the literal fallback. The **real** company profile (companyName, tradeName, legalName, emails[], phones[], websites[]) lives in the `contacts` collection under a *different* id, linked through the project: each `projects/{id}` doc carries `linkedCompanyId` pointing at `contacts/<contactId>` (verified on this tenant: `projects/proj_8baf51b3-...` → `linkedCompanyId: cont_4a2df965-...` → `companyName: "ALFA ΚΑΤΑΣΚΕΥΑΣΤΙΚΗ Α.Ε."`, `tradeName: "ALFA"`). The tenant doc is the *wrong source* for branding — it never had the real name, and never will. **Fix — hierarchical resolver**: new `src/services/company/company-branding-resolver.ts` exposes `resolveShowcaseCompanyBranding({ adminDb, propertyData, companyId })`. Primary path walks Property → Project → Contact: reads `property.projectId`, loads the project, extracts `project.linkedCompanyId`, loads the contact, and returns `{ name, phone, email, website, source: 'contact' }`. Name goes through `resolveCompanyDisplayName` (Phase 3.6 SSoT) — so `companyName → tradeName → legalName → displayName → Company #<idPrefix>` priority chain still applies. Phone/email/website are extracted from the contact's `emails[] / phones[] / websites[]` arrays by a small `pickValue<T>()` helper that prefers entries with `isPrimary: true` and falls back to the first entry (shape compatible with `EmailInfo` / `PhoneInfo` / `WebsiteInfo` in `types/contacts/contracts.ts`). **Fallback chain**: when `projectId` is missing, the project doesn't exist, or `linkedCompanyId` is unset, the resolver falls back to reading `companies/{companyId}` (the old path) with `source: 'tenant'` — existing behaviour preserved for tenants that haven't been linked yet. If even that doc is absent, the last-resort identifier-based label (`Company #<idPrefix>`) is returned with `source: 'fallback'`. Graceful every step — the resolver never throws. **Refactor — 2 call sites**: (1) `api/showcase/[token]/route.ts` no longer reads `companies/{companyId}` directly; it delegates to `resolveShowcaseCompanyBranding()` and spreads the branding block into the response. (2) `api/properties/[id]/showcase/generate/helpers.ts::loadShowcaseSources` drops the `companyDoc` read and returns `{ property, branding, photoCount, floorplanCount }`; `buildPdfData()` now consumes `sources.branding` instead of a raw `company` record. `ShowcaseSources` type updates automatically via `Awaited<ReturnType<…>>`. PDF and web page now converge on the same branding pipeline — no more divergence between the two surfaces. **Net effect on the reported incident**: the tenant doc `companies/comp_9c7c1a50-...` still has `name: "Unknown Company"` in Firestore (test data, being wiped before launch), but it is **no longer consulted** for property showcase branding — the resolver follows the project→contact link and surfaces `"ALFA ΚΑΤΑΣΚΕΥΑΣΤΙΚΗ Α.Ε."` instead. **Files touched**: 4 — 1 new service (157 LOC), 2 refactored consumers, this ADR entry. No schema migration, no new i18n keys. `company-name-resolver` (Phase 3.6) kept as the pure-function SSoT for the name-only path; the branding resolver layers hierarchy+IO on top. |
| 2026-04-18 | **Phase 3.6: SSoT `company-name-resolver` — eliminate 6 hardcoded `'Unknown Company'` literals.** Sales opened the public `/showcase/<token>` page and the brand header read `Unknown Company` instead of the tenant name. Root cause — two layers: (a) the Firestore doc `companies/comp_9c7c1a50-...` was persisted with `name: "Unknown Company"` literally, because `company-document.service.ts::ensureCompanyDocument()` wrote `name: contactData?.name ?? 'Unknown Company'` at creation time; (b) 5 other call sites across the codebase each hardcoded the same literal with a **different** field-precedence chain — `companyName` alone (`auto-fix-missing-companies`), `companyName \|\| tradeName \|\| legalName` (`api/companies`), `companyName \|\| displayName` (`bootstrap-queries` ×2, both admin and tenant branches), `contactData?.name` (`company-document.service`), `companyName` inside a switch-by-type (`contacts.service::getDisplayName`). Anti-SSoT — the same display-name concept had six incompatible implementations. **Fix — SSoT resolver**: new `src/services/company/company-name-resolver.ts` exposes `resolveCompanyName(input, config?)` + `resolveCompanyDisplayName(input, config?)`. Accepts the full union of fields (`id`, `name`, `companyName`, `tradeName`, `legalName`, `displayName`) and picks the first non-empty in a fixed priority order: `name → companyName → tradeName → legalName → displayName`. Empty/whitespace values are treated as missing. Fallback: caller-supplied `config.fallback` (for UI, e.g. an i18n-resolved string) or an identifier-based label `Company #<idPrefix>` — **never** a translatable literal like `"Unknown Company"`, so the fallback is safe to persist in Firestore or emit from server logs without creating another hardcoded-string baseline violation. Returns `{ displayName, source, hasRealName }` from the full form — callers that need to branch on "did we actually have a real name?" (e.g. admin rescue flows) can inspect `hasRealName` instead of string-comparing the output. **Refactor — 6 call sites**: (1) `api/showcase/[token]/route.ts` now reads all 5 fields off the tenant doc and delegates — the brand header on the public page stops showing `Unknown Company` for legacy tenant docs that have `companyName`/`tradeName` populated. (2) `api/companies/route.ts::mapFirestoreToCompanyContact()` delegates, same field set. (3) `api/navigation/auto-fix-missing-companies/route.ts` — this path is log/result-only (the literal is never persisted), but unified for consistency. (4–5) `api/projects/bootstrap/bootstrap-queries.ts` — both the admin ("in" query chunk) and tenant ("single doc") branches, now read `tradeName`/`legalName` in addition to `companyName`/`displayName` so the navigation surface picks up ΑΕ-style contacts that only have a legal name registered. (6) `services/company-document.service.ts::ensureCompanyDocument()` — this is the **root cause of the incident**: the resolver is now the source for the persisted `name` field at tenant-creation time, so no future tenant will ever be written with `"Unknown Company"` as its `name`. New tenants created without a real name will be persisted as `Company #<8-char-prefix>` — a stable, non-translatable identifier safe to show in UI as a last-resort fallback and easy for an admin to spot and fix. (7) `services/contacts.service.ts::getDisplayName()` — the `company` branch of the type switch delegates; `individual` and `service` branches are unchanged. **Deferred**: the `service` branch still has `'Unknown Service'` (different entity, different SSoT — out of scope for Phase 3.6). The `individual` branch does not have an `'Unknown …'` literal because `ContactNameResolver` (ADR-065) already owns that path at the call-site level — keeping the existing fallback here avoids a breaking contract change. **Test data caveat**: Giorgio is pre-production, the existing `comp_9c7c1a50-...` doc with `name: "Unknown Company"` is test data that will be wiped before launch — **no Firestore patch** is applied. The value of this phase is killing the code pattern that **created** that value, not the one row it created. **Files touched**: 7 — 1 new service (130 LOC), 6 call-site refactors, this ADR entry. No i18n change, no new SSoT registry entry (resolver is a pure function — `.ssot-registry.json` would add a ratchet for re-emergence of `'Unknown Company'`/`'Unknown Service'` literals, considered for Phase 3.7). No contract change visible to external callers of the touched routes. |
| 2026-04-18 | **Phase 4: full-field parity between Πληροφορίες tab, showcase web, and branded PDF — SSoT snapshot builder.** Sales flagged that the public showcase and the branded PDF exposed only a small subset of the property's internal `Πληροφορίες` tab (identity + areas + layout + orientation + energy + condition). Missing from both surfaces: `Έργο` (project name + address), `Συνδεδεμένοι χώροι` (storages + parking joined through `linkedSpaces[]`), `Διαθεσιμότητα` / `Τιμή` / `Λειτουργική κατάσταση` (commercial + operational status), `Διάταξη` extras (`totalRooms`, `balconies`), `Κατάσταση` extras (`renovationYear`, `deliveryDate`), full `Ενέργεια` certificate block, `Συστήματα θέρμανσης/ψύξης`, `Τελειώματα` (flooring/frames/glazing), `Χαρακτηριστικά παροχών` (interior/security/amenities), `Θέα`. Root cause — **no SSoT** for the showcase payload: `/api/showcase/[token]/route.ts` and `buildPdfData()` each independently mapped the Firestore `property` doc into two near-identical shapes, so adding a field to the web required duplicating the mapping in the PDF (and vice versa), and the legacy `features: string[]` field in the web payload read from a non-existent `p.features` Firestore field — dead code. **Fix — SSoT snapshot builder**: new module `src/services/property-showcase/snapshot-builder.ts` exposes `loadShowcaseRelations({ adminDb, propertyId, property, branding })` (one-shot parallel Firestore read: project doc + every linked `storage_units` / `parking_spots` doc referenced by `property.linkedSpaces[]`) and `buildPropertyShowcaseSnapshot(context, locale)` (pure mapping, zero I/O, fully typed). The returned `PropertyShowcaseSnapshot` covers every field surfaced by `PropertyFieldsEditForm` + `PropertyFieldsDetailCards(Row2)` + `LinkedSpacesCard` + `PropertyEntityLinks` — single union type for both the web API response and the PDF data input. All enum keys (`commercialStatus`, `operationalStatus`, `heatingType`, `coolingType`, `flooring[]`, `windowFrames`, `glazing`, `interiorFeatures[]`, `securityFeatures[]`, plus the pre-existing `type`, `orientations`, `condition`) are translated server-side via an extended `property-enum-labels.service.ts` with 9 new SSoT translator helpers, each reading from `src/i18n/locales/{el,en}/properties-enums.json` (zero new translation strings — reuses the catalogs the client already consults). **Fix — labels SSoT**: new `src/services/property-showcase/labels.ts::loadShowcasePdfLabels(locale)` replaces the hardcoded `buildShowcaseLabels()` in `helpers.ts` and reads section titles + field labels directly from `src/i18n/locales/{el,en}/showcase.json` — the PDF and the web page now share the same label source. `showcase.json` (EL + EN) extended with sections `project`, `commercial`, `systems`, `finishes`, `features`, `energy`, `views`, `linkedSpaces` (+ existing `specs` expanded with `garden`, `millesimalShares`, `totalRooms`, `balconies`, `renovationYear`, `deliveryDate`). **Fix — PDF renderer split**: `PropertyShowcaseRenderer` was at 450 LOC and would have crossed the Google 500-LOC file budget once all 9 new sections were added. Renderer now stays at ~280 LOC and delegates section drawing to a new `PropertyShowcaseSections.ts` (~280 LOC) containing pure-function section renderers: `drawProjectSection`, `drawCommercialSection`, `drawSystemsSection`, `drawFinishesSection`, `drawFeaturesSection`, `drawEnergyExtrasSection`, `drawLinkedSpacesSection`, `drawViewsSection`, `drawSpecsSection`, plus SSoT helpers `buildSpecsRows()`, `drawKeyValueGrid()`, `drawTagList()`. Renderer orchestrates: cover (brand header + title + description) → photo grid → details page (project → commercial → specs → views → systems → finishes → features → energy → linked spaces, with automatic page breaks via `ensureSpace(y, needed)`) → floorplan grid → per-page footer. Prices are formatted via `Intl.NumberFormat` (EUR) against the caller's locale; dates via ISO-slice formatter. `PropertyShowcasePDFData` refactored to carry the full `snapshot` object (instead of a flattened `property`+`company` pair); `PropertyShowcasePDFLabels` refactored into nested groups (`specs`, `project`, `commercial`, `systems`, `finishes`, `features`, `energy`, `linkedSpaces`, `chrome`) mirroring `showcase.json`. **Fix — web surface**: `ShowcaseDetailSections.tsx` (new, ~220 LOC) renders cards for every container (project, commercial, systems, finishes, features, energy extras, linked spaces, views) between the photo grid and the core specs. `ShowcaseSpecs.tsx` simplified — single `buildRows` driven by the new nested snapshot, with the extended field set. `ShowcaseClient.tsx` composes `<ShowcaseDetailSections>` + `<ShowcaseSpecs>` and passes `i18n.language`-derived locale to the detail sections for price formatting. `components/property-showcase/types.ts` now re-exports the server-side snapshot types — **one union type, two consumers, zero drift**. **Fix — consumer refactor**: `route.ts` now delegates to `loadShowcaseRelations` + `buildPropertyShowcaseSnapshot` and streams the snapshot into the API response; `helpers.ts::loadShowcaseSources` returns `{ context, photoCount, floorplanCount }` and `buildPdfData()` is a 15-line wrapper around the snapshot builder + labels loader. Dead `p.features` legacy array removed. **Tests**: `PropertyShowcaseRenderer.test.ts` migrated to the new data shape — renderer contract (image embedding, 3-column grid, uniform cell width, raw `Uint8Array` bytes) still pinned. **Files touched**: 13 — 2 new services (snapshot-builder, labels), 1 extended service (property-enum-labels — 9 new translators), 1 extended route (`showcase/[token]`), 1 extended helpers (`properties/[id]/showcase/generate/helpers`), 2 extended + 1 new PDF renderer files (`PropertyShowcaseRenderer`, `PropertyShowcaseSections`), 1 refactored + 1 new + 1 extended web component (`ShowcaseClient`, `ShowcaseDetailSections`, `ShowcaseSpecs`), 1 refactored web types module, 2 extended locale files (`showcase.json` EL + EN), 1 refactored renderer test, this ADR entry. No schema migration, no new Firestore fields, no new i18n namespaces. |
| 2026-04-18 | **Phase 6: full project address on the showcase — SSoT `formatFullAddressLine`.** Giorgio reviewed the Phase 5 output and flagged the `Έργο` card (both web and PDF): the address line was missing key parts needed by a prospect to actually locate the project — no street number, no postal code, no region. Root cause in `src/services/property-showcase/snapshot-builder.ts::buildProject()`: the mapper read the primary address as a raw `Record<string, unknown>` and fell back to `[street, city].join(', ')` when `fullAddress` wasn't populated — so `number`, `postalCode`, `region`, `country` were silently dropped. On top of that, the field name assumed (`streetNumber`) did not match the real `ProjectAddress` schema (`number`), so even the number-less concatenation rarely worked. **Fix — SSoT extension in `src/types/project/address-helpers.ts`**: new `formatFullAddressLine(address: ProjectAddress): string` — customer-facing sibling of the pre-existing `formatAddressLine` (which stays short, used by the internal project card). Greek convention: `"Οδός Αριθμός, ΤΚ Πόλη, Περιφέρεια"`, with `country` appended only when it differs from `GEOGRAPHIC_CONFIG.DEFAULT_COUNTRY` (redundant for local prospects). Empty parts are filtered per-group, so a missing region or number doesn't leave dangling commas. Kept in the same module because the SSoT for address formatting already lives there (`formatAddressLine`, `formatAddressForGeocoding`, `formatBlockSide`, `formatAddressType`) — adding another formatter alongside them is one-file cohesion, not a new helper zoo. **Fix — `snapshot-builder.ts::buildProject()`**: dropped the raw `pickString(primaryAddress.fullAddress)` + `[street, city]` fallback chain and now delegates to the SSoT — `getPrimaryAddress(project.addresses as ProjectAddress[])` resolves the primary entry (same lookup every other project surface uses via `ProjectCardLocation`, `AddressCard`, `BuildingAddressesProjectSelection`), `formatFullAddressLine(primary)` produces the customer-facing string, and when `project.addresses` is absent (legacy docs) `migrateLegacyAddress({ address, city })` lazily builds a single primary address from the flat legacy fields — same path `ProjectCardLocation` uses, so web and showcase now share identical legacy behaviour. `ProjectAddress[]` cast is safe: if a legacy doc has a malformed entry, the missing fields are filtered out inside the formatter and the worst case is a shorter line (never a crash). **PDF auto-inherits**: `PropertyShowcaseSections.ts::drawProjectSection` already reads `snapshot.project.address` — it picks up the complete line with zero code change. Same for the web `<ShowcaseProjectCard>` in `ShowcaseDetailSections.tsx`. Single mapping change, two surfaces fixed. **Files touched**: 3 — `src/types/project/address-helpers.ts` (+40 LOC, new formatter), `src/services/property-showcase/snapshot-builder.ts` (buildProject refactor + 3 SSoT imports + `ProjectAddress` type import), this ADR entry. No i18n change (the formatter produces raw text — section labels stay in `showcase.json`), no schema migration, no contract change on `ShowcaseProjectInfo`. **Tests**: existing `address-helpers.test.ts` suite unaffected; future work can add a `formatFullAddressLine` case matrix (full address / missing region / missing number / non-default country) — deferred until Giorgio flags a regression. |
| 2026-04-18 | **Phase 5: 14-container reorder interleaving text sections with media grids — identical web + PDF.** Giorgio reviewed the Phase 4 output and issued an explicit layout spec: the surface must intersperse the detail containers between the media grids, not pile all text first and all media last. Final sequence for both web and PDF — **(1) Brand header → (2) Έργο (project) → (3) Εμπορικά στοιχεία (commercial) → (4) Περιγραφή (description, name + code + free-text) → (5) Φωτογραφίες (photos grid) → (6) Βίντεο (video, web only — PDF cannot embed video) → (7) Χαρακτηριστικά (specs) → (8) Συνδεδεμένοι χώροι (linked spaces) → (9) Ενέργεια (energy certificate) → (10) Θέα (views) → (11) Κατόψεις (floorplans grid) → (12) Συστήματα (systems) → (13) Τελειώματα (finishes) → (14) Χαρακτηριστικά παροχών (features)**. Rationale: identity + commercial headline first, then the marketing narrative, then the visual hook (photos+video), then progressively more technical detail (specs → linked spaces → energy → views), then the floorplans as a second visual anchor, then the construction-level detail (systems, finishes, amenities) that matters to the serious buyer. **Fix — web (`ShowcaseClient.tsx`)**: replaced the monolithic `<ShowcaseDetailSections>` wrapper with individual card components composed directly in `ShowcaseClient`. Each `Showcase*Card` in `ShowcaseDetailSections.tsx` is now `export`ed so `ShowcaseClient` can interleave them with `ShowcasePhotoGrid`, `ShowcaseVideoEmbed`, `ShowcaseSpecs`, `ShowcaseFloorplans` in the exact order above. The monolith `<ShowcaseDetailSections>` wrapper component is kept for downstream consumers but is no longer used on the public page. **Fix — PDF (`PropertyShowcaseRenderer.render`)**: split the Phase 4 `drawDetailsPage` monolith into three orchestrator helpers — `drawOverviewPage` (items 2–3), `drawSpecsBlockPage` (items 7–10), `drawSystemsBlockPage` (items 12–14) — each of which `addPage()`s once and then emits its sections through `ensureSpace`-guarded calls into the existing `PropertyShowcaseSections` renderers (no renderer logic changed, only invocation order). New private helper `buildSectionContext` DRYs the `SectionContext` + `ensureSpace` construction shared by the three block helpers. `drawDescriptionPage` (Phase 5 intermediate) is kept as the dedicated page for item 4. Final render sequence: cover (brand only) → overview page (project + commercial) → description page (title + code + description) → photos grid → specs block page (specs + linked spaces + energy + views) → floorplans grid → systems block page (systems + finishes + features) → per-page footer. Video is intentionally skipped in the PDF (jsPDF cannot embed video; the container exists on web only). **Files touched**: 3 — `ShowcaseClient.tsx` (JSX reorder + direct card composition), `ShowcaseDetailSections.tsx` (8 `export function` prefixes on card components — no logic change), `PropertyShowcaseRenderer.ts` (split orchestrator into 3 block helpers + `buildSectionContext`), this ADR entry. No changes to the SSoT snapshot builder, section renderers, labels module, or i18n catalogs — the reorder is purely at the orchestration layer. **Tests**: `PropertyShowcaseRenderer.test.ts` contract (image embedding, 3-column grid, uniform cell width, raw `Uint8Array` bytes) unaffected; `getPageCount() >= 2` assertion still holds (new page count is 7 for empty-photos input: cover + overview + description + photos-empty + specs-block + floorplans-empty + systems-block). **Follow-up — energy-class SSoT dedup**: during Phase 5 review, Giorgio noticed `energy.class` rendered twice — once inside the Χαρακτηριστικά (specs) card/table and once inside the dedicated Ενέργεια card. Violated single-source-of-truth for a user-facing field. Root cause: when Phase 4 introduced the dedicated `Ενέργεια` section, the original `energyClass` row inside `ShowcaseSpecs.buildRows` + `buildSpecsRows` (PDF) was kept for backwards continuity. Dedup fix: `energyClass` row removed from both `ShowcaseSpecs.tsx` and `PropertyShowcaseSections.ts::buildSpecsRows`; `ShowcaseEnergyExtrasCard` (web) and `drawEnergyExtrasSection` (PDF) now prepend `energy.class` as the first row inside the Ενέργεια card — i.e. the energy class lives **only** inside the Ενέργεια container alongside the certificate info. `ShowcaseEnergyLabels` interface gains `energyClass: string`, locale JSONs (`showcase.json` EL+EN) gain `energy.energyClass` key. Rationale (Google-level UX): progressive disclosure + SSoT — each user-facing datum has one home; Specs carries identity + geometry + condition, Energy carries the full certificate block, Systems/Finishes/Features stay as separate cards (Zillow/Airbnb/Idealista pattern — not a monolithic "all technicals" container that would overload scanning). |
| 2026-04-18 | **Phase 3.2: regenerate showcase PDF in-place.** Sales had already distributed public URLs for showcase shares whose PDFs were generated BEFORE Phase 3.1 rasterised legacy DXF thumbnails — the PDFs were missing the Κατόψεις page, and rotating the share token to produce a fresh URL was not acceptable because the old URL was already in partners' hands. The existing `POST /api/properties/[id]/showcase/generate` always mints a new `shareId`/`token`/`pdfStoragePath` and is therefore not reusable for this flow. **Fix**: new endpoint `POST /api/properties/[id]/showcase/regenerate` body `{ shareId, locale?, videoUrl? }` that rewrites the PDF blob at the existing Storage path, preserving `token`, `shareId`, and `pdfStoragePath` so every previously shared URL keeps resolving to the freshly rendered PDF. A new FILE_SHARES field `pdfRegeneratedAt?: Date` is stamped on each regeneration for audit. **SSoT orchestrator**: new helper `regeneratePdfForShare(params)` in `src/app/api/properties/[id]/showcase/generate/helpers.ts` loads the share via Admin SDK, enforces ownership (`share.companyId === ctx.companyId`, `share.showcaseMode === true`, `share.showcasePropertyId === propertyId`, `share.isActive === true`, `share.pdfStoragePath` non-empty), then reuses the existing pipeline (`loadShowcaseSources` + `loadShowcasePhotos` + `loadShowcaseFloorplans` + `buildPdfData` + `PropertyShowcasePDFService.generate()` + `uploadPdfToStorage()`) — no code duplication, just a thin orchestration on top of the generate helpers. Route wrapper is a 123-LOC thin layer with `withAuth('properties:properties:update')` + `withStandardRateLimit`. **Orphan-cleanup safety**: overwriting the PDF at the same path re-fires `onStorageFinalize`, but `findFileOwner()` (post-incident 2026-04-17 SSoT resolver) resolves the FILE_SHARES claim by `shareId` extracted from the filename, so the re-uploaded blob is immediately classified as owned and deletion is skipped. No pre-upload compensation write is needed (the claim already exists). **Return shape**: `{ shareId, token, pdfUrl, regeneratedAt }` — `pdfUrl` is identical to the original because it's derived from the unchanged `token`. Clients can reload the existing link to see the new PDF; `cacheControl: 'private, max-age=3600'` on the Storage object keeps stale caches bounded to ≤1 h. **Scope**: no UI surface added in this phase (there is no admin screen that manages existing shares yet); the endpoint is callable via API. **Files touched**: 4 — 1 new route, 1 helpers extension (+~90 LOC, 441 total, under 500 budget), `FileShareRecord.pdfRegeneratedAt` field, this ADR entry. |
| 2026-04-18 | **Phase 7: Κατόψεις συνδεδεμένων χώρων — new container between linked-spaces and energy on both web and PDF.** Giorgio wanted the DXF κατόψεις of every parking spot and storage unit linked to a property to sit next to the property’'s own κατόψεις on the showcase surface. Phase 3 had already put property DXFs through `onDxfProcessedFinalize` so they carry `thumbnailStoragePath` / `thumbnailUrl`; parking + storage DXFs go through the same Cloud Function and therefore already have raster previews — no new rasterisation pipeline needed. **Fix — media reader generalisation (SSoT)**: `src/services/property-media/property-media.service.ts` now exposes `listEntityMedia({ companyId, entityType, entityId, category, limit })` and `downloadEntityMedia(...)` as the canonical primitives for reading media owned by any entity; `listPropertyMedia` / `downloadPropertyMedia` are kept as thin `property`-specific wrappers so Phase 2/3 callers are unaffected. Fixes the hardcoded `entityType === ENTITY_TYPES.PROPERTY` baked into every previous query path. **Fix — public API**: `GET /api/showcase/[token]` now loads `linkedSpaceFloorplans: { parking: [{ spaceId, allocationCode, media[] }], storage: [...] }` in parallel with photos/floorplans, reading each linked space’'s files via `listEntityMedia(entityType="parking"|"storage", category=FILE_CATEGORIES.FLOORPLANS)`. **Pitfall scoperto durante review**: le categorie `FILE_CATEGORIES.PARKING` / `.STORAGE` (nome `*-floorplan`) in `domain-constants.ts` sembravano appropriate ma **non sono mai usate in scrittura** — `FloorplanSaveOrchestrator.save()` default a `FILE_CATEGORIES.FLOORPLANS` e l'`entityType` (parking/storage) è la chiave discriminante, non la category. The resolved context from `loadShowcaseRelations` already had the `parkingSpots`/`storages` Firestore docs loaded (Phase 4), so the iteration is zero-extra-cost at the Firestore level. Groups with zero media are dropped; the entire block is emitted only when at least one space has media. **Fix — web surface**: `ShowcaseFloorplans.tsx` extracted into `ShowcaseFloorplanGrid.tsx` (grid + tile primitive, no section wrapper). `ShowcaseFloorplans` now composes the SectionCard + SSoT grid. New `ShowcaseLinkedSpacesFloorplans.tsx` consumes the payload and renders a two-column layout (parking left, storage right) with each space’'s `allocationCode` as sub-header. `ShowcaseClient.tsx` inserts the new card between `ShowcaseLinkedSpacesCardView` (container 9) and `ShowcaseEnergyExtrasCard` (container 11), matching the spec’'s final 16-container order. **Fix — PDF surface**: new pure-function section renderer `drawLinkedSpacesFloorplansSection(ctx, { parking, storage })` in `PropertyShowcaseSections.ts` draws a dedicated page with two-column column-headers + vertically stacked group tiles (one rasterised floorplan per space) using the existing `ensureSpace` + `addImage` primitives. `PropertyShowcaseRenderer` splits the Phase 5 `drawSpecsBlockPage` into `drawSpecsBlockPage` (specs + orientation + linked-spaces, now ending before energy) + new `drawLinkedSpacesFloorplansPage` + new `drawEnergyAndViewsPage`, keeping the interleaving rule “linked-spaces text → linked-spaces κατόψεις → energy → views → floorplans”. **Fix — i18n SSoT**: `src/i18n/locales/{el,en}/showcase.json` gain a new `linkedSpacesFloorplans` namespace (`sectionTitle`, `parkingColumn`, `storageColumn`, `emptyParking`, `emptyStorage`, `unnamedSpace`). EL in pure Greek (no "parking"/"storage"/"DXF" English literals). `services/property-showcase/labels.ts` loads the new block with a `locale`-aware fallback so older locale JSONs still build. **Fix — generator helpers**: `loadShowcaseLinkedSpaceFloorplans(context, companyId)` added alongside `loadShowcasePhotos` / `loadShowcaseFloorplans`; `buildPdfData` gains an optional `linkedSpaceFloorplans` parameter. All three consumers (`/api/properties/[id]/showcase/generate`, `/api/properties/[id]/showcase/pdf`, `regeneratePdfForShare`) now load and forward it. Failures on a single space never fail the aggregate load — partial results preferred. **Files touched**: 15 — property-media service (+generic entity helpers), public showcase API route, property-showcase types (+ `ShowcaseLinkedSpaceFloorplans` payload), new `ShowcaseFloorplanGrid` component, refactored `ShowcaseFloorplans` component, new `ShowcaseLinkedSpacesFloorplans` component, `ShowcaseClient` composition, 2 locale files (EL + EN), `property-showcase/labels.ts`, `PropertyShowcaseSections.ts` (new section renderer + types), `PropertyShowcaseRenderer.ts` (pipeline split), `generate/helpers.ts` (loader + buildPdfData param), `generate/route.ts`, `pdf/route.ts`, this ADR entry. No schema migration, no new Firestore fields, no new indexes (existing `files(companyId, entityType, entityId, category)` composite covers the new queries). |
| 2026-04-18 | **Phase 7.1 hotfix: DXF thumbnail auto-process was property-only — parking/storage floorplans showed `Δεν υπάρχει διαθέσιμη προεπισκόπηση`.** Giorgio opened the Phase 7 showcase and saw the new `Κατόψεις συνδεδεμένων χώρων` container find the storage group (filename + download-DXF link correct) but **preview missing**. The property's own DXF rendered fine. Root cause: the `.dxf.processed.json` artifact — sole input to `onDxfProcessedFinalize` — is produced exclusively by `POST /api/floorplans/process`, and that endpoint was called only by two client hooks (`useFloorplanAutoProcess` gated on `displayStyle === 'floorplan-gallery'`, and `useFloorplanFiles` gated on being mounted). Property DXFs hit the auto-process path because the property details tab renders `FloorplanGallery` with `floorplan-gallery` style; parking/storage DXFs are uploaded from tabs that use a different display mode, so `.dxf.processed.json` was never written → the Cloud Function trigger had no input → `thumbnailStoragePath` / `thumbnailUrl` stayed null → the showcase reader had no raster to embed. **Fix — SSoT choke-point at finalize**: `FileRecordService.finalizeFileRecord()` — the single Firestore writer called by every upload path (orchestrator, `useFileUpload`, photo-upload, CRM attachment, PDF processor, upload-orchestrator-gateway) — now inspects the freshly-read `docSnap.data()` for `ext === 'dxf'` + `category === FILE_CATEGORIES.FLOORPLANS` and fire-and-forgets `processFloorplanWithPolicy({ fileId, forceReprocess: false })` via dynamic import (keeps the gateway out of the generic finalize bundle). Same telemetry pattern as the pre-existing `SEARCH_REINDEX` fire-and-forget on the line above — errors log as warn and never block the save UX. Entity-type agnostic: property/floor/building/parking/storage all go through the same hook through the SSoT writer, no per-entity code duplication. **Fix — legacy back-fill**: `POST /api/admin/migrate-dxf-thumbnails` previously skipped candidates missing `processedData.processedDataPath` with reason `'missing processedDataPath'` — which is exactly what happened to every pre-hotfix parking/storage DXF. `helpers.ts` now calls a new `ensureProcessedDataForDxf()` helper that delegates to the SSoT `processDxf()` server service from `floorplan-process.service.ts` (the same module `/api/floorplans/process` uses), writes the resulting `processedData` to Firestore, and then runs the normal rasterize-and-persist path. Zero duplication — the legacy back-fill and the runtime trigger converge on byte-identical output. The dry-run branch reports `'dryRun (would generate processedData)'` so operators can estimate the work before committing. **Files touched**: 3 — `src/services/file-record.service.ts` (+19 LOC: FILE_CATEGORIES import + finalize hook), `src/app/api/admin/migrate-dxf-thumbnails/helpers.ts` (+~60 LOC: ensureProcessedDataForDxf + missing-processedPath branch now auto-generates), this ADR entry. No schema migration, no new i18n keys, no new routes. **Tests**: pinned by the existing `decodeProcessedJsonBytes.test.ts` (gzip path) + the Cloud Function's idempotency guard (skips when `thumbnailUrl` already set). **Verification plan (post-deploy)**: for each parking/storage DXF uploaded before this hotfix, run the back-fill endpoint for the tenant; fresh uploads after deploy will self-heal through the finalize hook without operator action. |
| 2026-04-18 | **Phase 7.2 hotfix: parking DXF query returned zero hits — `entityType: 'parking'` string literal did not match the canonical `ENTITY_TYPES.PARKING_SPOT = 'parking_spot'` used at upload time.** Giorgio reload-ed the showcase after the Phase 7.1 hook shipped and reported: parking column rendered `"Δεν υπάρχουν κατόψεις"` although the DXF existed, while storage rendered the tile without a preview. Investigation: `ParkingFloorplanTab.tsx:98` uploads with `entityType="parking_spot"` (correct, matches `ENTITY_TYPES.PARKING_SPOT`), but `api/showcase/[token]/route.ts::loadLinkedSpaceFloorplans` **and** `api/properties/[id]/showcase/generate/helpers.ts::loadShowcaseLinkedSpaceFloorplansPdf` both queried `listEntityMedia({ entityType: 'parking', … })` / `downloadEntityMedia({ entityType: 'parking', … })` — raw string literal, typo-equivalent given the canonical constant was `'parking_spot'`. Firestore returned zero docs, the group was dropped, and the empty-state copy kicked in. Storage was not affected by the literal mismatch (`'storage' === 'storage'`); its missing preview has a separate root cause (Phase 7.1 auto-process hook applies only to future uploads — existing storage DXF needs a ParkingFloorplanTab / StorageFloorplanTab visit or a back-fill run to generate `thumbnailUrl`). **Fix**: both call sites now import `ENTITY_TYPES` from `domain-constants.ts` and pass `ENTITY_TYPES.PARKING_SPOT` / `ENTITY_TYPES.STORAGE` instead of string literals. Zero duplication — `domain-constants.ts` is already the SSoT for entity type names; hardcoding them in query call sites was the defect. **Root-cause category**: _SSoT bypass via string literal_ — the same class of bug as inline `'files'` instead of `COLLECTIONS.FILES`. Follow-up candidate: ratchet rule forbidding literal `entityType: 'parking'|'storage'|'property'|…` inside `listEntityMedia`/`downloadEntityMedia` calls, forcing the constant. **Files touched**: 2 — `api/showcase/[token]/route.ts` (ENTITY_TYPES import + 2 literal → constant swaps), `api/properties/[id]/showcase/generate/helpers.ts` (same), this ADR entry. No schema migration, no new i18n keys, no Cloud Function redeploy needed. **Verification**: localhost dev server hot-reloads; showcase reload should now render the parking tile (filename + download link). Preview thumbnail requires the Phase 7.1 self-heal path (open the `ParkingFloorplanTab` / `StorageFloorplanTab` once so `useFloorplanAutoProcess` fires `/api/floorplans/process`, or run the extended back-fill endpoint). |
| 2026-04-18 | **Phase 7.4 Next.js bundler fix: `@resvg/resvg-js` platform-specific native binary was unresolvable — DXF self-heal silently failed in dev.** After Phase 7.3 shipped, Giorgio reloaded the showcase twice and parking+storage DXF tiles still rendered `Δεν υπάρχει διαθέσιμη προεπισκόπηση`. Dev-server logs (Windows) exposed the failure: `[WARN] [ShowcasePublicApi] DXF self-heal failed {"fileId":"file_8927905e-…","error":"could not resolve \"@resvg/resvg-js-win32-x64-msvc\" into a module"}` — identical warning for the storage DXF. Root cause: `@resvg/resvg-js` uses platform-specific native binaries shipped as optional dependencies (`@resvg/resvg-js-win32-x64-msvc`, `@resvg/resvg-js-linux-x64-gnu`, etc.), picked at runtime by the package's index. Next.js Turbopack/webpack attempts to resolve those optional deps **statically** at bundle time, can't find the right one for a cross-platform build, and surfaces a hard resolve error at first `require()` inside `loadResvg()` (`dxf-raster-generator.ts:43`). The fire-and-forget catch in `route.ts::scheduleDxfSelfheal` swallowed the error as a `warn`, so nothing visible on the UI and nothing in Vercel production build logs — only the dev server exposed it. Two DXF with `thumbnailUrl` (the property DXFs) pre-existed from an earlier batch run of `/api/admin/migrate-dxf-thumbnails` executed in a different process that apparently hit the cached resolver — but the live self-heal from the Next.js server route was blocked on every hit. **Fix**: one-line change to `next.config.js` — `serverExternalPackages: ['@mapbox/node-pre-gyp', '@resvg/resvg-js']`. Marking the package external tells Next.js to leave `require('@resvg/resvg-js')` to Node.js at runtime, which then picks the correct platform-specific binary from `node_modules` through the package's own resolution logic. Same mechanism the codebase already uses for `@mapbox/node-pre-gyp` (another native-binary package). **Why SSoT-safe**: zero logic change, zero rasterizer duplication — the SSoT `rasterizeDxfScene()` still funnels every DXF thumbnail request; we only change **how** the bundler loads it. The Cloud Function `onDxfProcessedFinalize` is unaffected (it runs in a different build pipeline that already tolerates native binaries). **Files touched**: 2 — `next.config.js` (+8 LOC: 1-line config change + 7-line WHY comment block), this ADR entry. No schema migration, no new i18n keys, no code changes anywhere else. **Verification plan**: (1) restart `npm run dev` so Next.js re-reads `next.config.js`; (2) reload the showcase twice — first reload triggers the self-heal, second reload shows the parking+storage preview; (3) if still blocked, inspect dev server stdout for `DXF self-heal rasterized` (success) or `DXF self-heal failed` (residual error). **Follow-up deferred**: consider migrating `@resvg/resvg-js` → a pure-WASM alternative like `@resvg/resvg-wasm` to eliminate the native-binary class of issues entirely; current approach works and stays Google-level, so no rewrite now. |
| 2026-04-19 | **Phase 7.6 linked-space description — the free-form `description` field (ADR-194 Phase 2 `DescriptionNotesCard` SSoT) of each parking/storage linked to a property now surfaces on both the showcase web and the branded PDF, inside the existing `Συνδεδεμένοι χώροι` section.** Giorgio asked: "μέσα στους βοηθητικούς χώρους θέλω και την περιγραφή του κάθε ενός" — i.e. alongside the existing type/code/floor/area/inclusion row, the user-authored description of that specific parking/storage. Vincolo: SSoT, zero code duplication between web and PDF. **Recon (MCP Firestore, 2026-04-19)**: `storage_units.description` populated in sample `stor_0c741bc6-...` (long Greek text); `parking_spots.description` exists in schema (`src/types/parking.ts:79`) but empty in the current sample `park_406a6bdd-...`. Rendering is strictly optional — no description ⇒ no block, no placeholder. **Fix — snapshot interface** (`src/services/property-showcase/snapshot-builder.ts`): `ShowcaseLinkedSpace` gains `description?: string`. Purely additive, back-compatible with older snapshots. **Fix — SSoT mapping** (`src/services/property-showcase/snapshot-field-builders.ts::buildLinkedSpaces`): one line added — `description: pickString(doc?.description)`. Single source of the web→PDF divergence risk is mechanically closed. **Fix — web** (`src/components/property-showcase/ShowcaseDetailSections.tsx::ShowcaseLinkedSpacesCardView`): under each `<li>`, after the existing type/code header + detail row, a conditional `<p className="text-xs text-[hsl(var(--showcase-muted-fg))] whitespace-pre-wrap">{space.description}</p>`. No label above the block — the context is already unambiguous and the minimalist rendering matches the rest of the card. No new wrapper `div`, no new i18n key. **Fix — PDF** (`src/services/pdf/renderers/PropertyShowcaseLinkedSpacesSection.ts`, new file; `drawLinkedSpacesSection` + `buildLinkedSpaceValue` extracted from `PropertyShowcaseSections.ts` to stay under the 500-LOC budget after the new wrapped-text block). The draw function now iterates per-space (previously built a `SpecRow[]` aggregate and called `drawKeyValueGrid` once): for each space it draws the KV row, then, when `description` is present, emits a wrapped block via `ctx.textRenderer.addWrappedText({ doc, text, y, fontSize: FONT_SIZES.BODY, maxWidth: ctx.contentWidth, margins, onPageBreak })` — same helper already used by `drawTagList` and `drawViewsSection` (zero new primitive). **PropertyShowcaseSections.ts** exports `drawKeyValueGrid` + `SpecRow` so the new file can reuse them without duplication, and re-exports `drawLinkedSpacesSection` from the new module so `PropertyShowcaseRenderer.ts` imports stay unchanged (SSoT import surface preserved). Final file sizes: `PropertyShowcaseSections.ts` 464 LOC, `PropertyShowcaseLinkedSpacesSection.ts` 62 LOC — both under 500. **i18n SSoT**: zero new keys, zero hardcoded string introduced (the user-authored description IS the content). **Out of scope**: `notes` field — parking/storage also carry a separate `notes` free-form (container "Περιγραφή και σημειώσεις" shows both); per Giorgio's request this phase surfaces only `description`. `notes` stays internal (if sales asks for it, a Phase 7.7 can extend the same SSoT path by adding `notes?: string` alongside `description?` in the snapshot, web, and PDF — same pattern, zero architectural change). **Files touched**: 6 — `services/property-showcase/snapshot-builder.ts` (+1 field), `services/property-showcase/snapshot-field-builders.ts` (+1 mapping line), `components/property-showcase/ShowcaseDetailSections.tsx` (+5 JSX lines), `services/pdf/renderers/PropertyShowcaseLinkedSpacesSection.ts` (new, 62 LOC), `services/pdf/renderers/PropertyShowcaseSections.ts` (export keywords, removed 34 LOC, added 1 re-export), this ADR entry. No schema migration, no new i18n keys, no locale JSON change, no ratchet update. **Verification**: localhost `/shared/JbL3CY2F54qpZlIbJayXvR1UjOUMdmx6` — storage row displays the long description block wrapped under the details line; parking row shows no extra block (description field empty in test data). Download PDF from the same URL: storage KV row followed by wrapped description paragraph; parking KV row only. No regression on other showcase sections. |
| 2026-04-19 | **Phase 7.5 κάτοψη ορόφου — floor-level floorplans injected under both the property card and every linked-space group on web + PDF, behind one SSoT resolver.** Giorgio asked for: (1) under the existing `Κατόψεις` property card, also render the floor plan(s) of the ορόφου the property lives on; (2) under every parking/storage group inside `Κατόψεις συνδεδεμένων χώρων`, add the floor plan(s) of that space's floor. Hard rule: same codifica of Phase 7, ZERO duplicate code, one SSoT pipeline for property + parking + storage + floor. **Recon (MCP Firestore)**: `properties.floorId` and `storage_units.floorId` are explicit FKs; `parking_spots` doc in production carries only `floor: "0"` (string) + `buildingId` — no `floorId` FK. Floor docs live in the canonical `floors` collection already declared in `COLLECTIONS.FLOORS` (schema `{ number, name, buildingId, projectId, elevation, ... }`). Floor-level DXFs are already in `files` with `entityType: 'floor'`, `category: 'floorplans'`, `purpose: 'floor-floorplan'` and most are pre-processed with a `.processed.json` — so the existing `listEntityMedia(entityType=FLOOR)` + `scheduleDxfSelfheal()` + `ensureDxfThumbnail()` SSoT pipeline serves floor DXFs without any new code path. **Fix — SSoT resolver in snapshot-builder**: new exported helpers `resolveFloorId(doc, floors)` and `pickFloorLabel(floor)` in `services/property-showcase/snapshot-builder.ts`. The resolver applies one rule for every entity: explicit `floorId` → use it; else `(buildingId, floor number)` → match against the preloaded floors map; else undefined. Single-source resolution mechanically prevents divergence between the web API, the regenerate-in-place path, the fresh-generate path, and the unified `/pdf` endpoint. `loadShowcaseRelations()` now also reads all floors of the property's project via one `floors.where('projectId','==',projectId)` query — cheap (≤ dozens of docs) and executed in parallel with project/parking/storage loads. Context gains a `floors: Map<floorId, FloorDoc>` field. **Fix — public API helpers**: `api/showcase/[token]/helpers.ts` exposes `loadFloorFloorplans(companyId, floorId)` (thin wrapper over `listEntityMedia(ENTITY_TYPES.FLOOR, ...)` with `scheduleDxfSelfheal()` attached) and `loadPropertyFloorFloorplans(companyId, context)` (applies `resolveFloorId` to the property doc). `loadLinkedSpaceFloorplans()` split into a reusable per-group loader `loadLinkedSpaceGroup({ ..., bucket })` that also resolves the group's floor and attaches `floorFloorplans` + `floorLabel` when present. Group is only emitted when at least one of `media` or `floorFloorplans` is non-empty. **Fix — payload types**: `components/property-showcase/types.ts` gains `ShowcasePropertyFloorFloorplans { floorLabel?, media }` at the top level and extends `ShowcaseLinkedSpaceFloorplanGroup` with `floorFloorplans?`, `floorLabel?`. **Fix — showcase route**: `GET /api/showcase/[token]` runs `loadPropertyFloorFloorplans` in parallel with existing loaders, attaches the result to the payload, and extends the summary log with `propertyFloorFloorplans` + floor-aware linked totals. **Fix — web components**: `ShowcaseFloorplans.tsx` accepts `propertyFloorFloorplans?` and renders a sub-article with subtitle `Κάτοψη ορόφου — <floorLabel>` using the SSoT `ShowcaseFloorplanGrid density="dense"` so the tile UI is the same as Phase 7. `ShowcaseLinkedSpacesFloorplans.tsx` iterates each group and, when `floorFloorplans` is present, stacks a second `ShowcaseFloorplanGrid` with the same subtitle under the existing space κατόψεις. `ShowcaseClient.tsx` forwards `data.propertyFloorFloorplans` to `ShowcaseFloorplans`. **Fix — PDF section renderers**: `PropertyShowcaseSections.ts` extends `LinkedSpaceFloorplansGroup` with `floorAssets?` + `floorLabel?`. `drawGroupColumn` now draws the optional floor tile with a `floorSubtitle` header below the space tile when vertical budget allows; shared `drawGroupAsset()` extracted so property + floor tiles use identical addImage-or-fallback logic. **Fix — PDF orchestrator**: `PropertyShowcaseRenderer.ts` adds a new section 13a after the existing floorplans page that calls `drawMediaGridPage` with a composed title `Κάτοψη ορόφου — <label>` reusing the SSoT `FLOORPLAN_GRID_CONFIG`; zero new grid primitive. `PropertyShowcasePDFData` gains optional `propertyFloorFloorplans: { label?, assets }`. **Fix — generate helpers**: `loadShowcasePropertyFloorFloorplans(context, companyId)` added; `loadShowcaseLinkedSpaceFloorplans` refactored into `loadLinkedSpaceGroupForPdf` that resolves the floor + downloads both space and floor buffers via one shared `downloadFloorplanBuffers()` SSoT wrapper. `buildPdfData` signature collapsed to `(propertyId, sources, showcaseUrl, videoUrl, locale, extras)` where `extras: BuildPdfDataExtras` carries `photos / floorplans / linkedSpaceFloorplans / propertyFloorFloorplans` — all three consumer routes (`/showcase/generate`, `/showcase/pdf`, `regeneratePdfForShare`) updated to the new shape. **Fix — i18n SSoT**: `showcase.json` EL + EN both gain `floorplans.floorSubtitle` (`"Κάτοψη ορόφου"` / `"Floor plan"`) and `linkedSpacesFloorplans.floorSubtitle` (same). `services/property-showcase/labels.ts` adds `ShowcaseFloorplansLabels` to `PropertyShowcasePDFLabels` and extends `ShowcaseLinkedSpacesFloorplansLabels` with `floorSubtitle`, with locale-aware fallbacks for older JSONs. EL in pure Greek (no `"floor"` English literal). **Parking FK caveat**: the current parking docs lack `floorId`; the resolver falls back to `(buildingId, floor)` matching which is deterministic with the `floors.where('projectId','==',projectId)` pre-load. When Giorgio later backfills `floorId` into parking docs (or when parking-create writes it going forward), the resolver picks up the explicit FK automatically with no code change — one SSoT rule covers both transitional and canonical data shapes. **Files touched**: 13 — `services/property-showcase/snapshot-builder.ts` (+`floors` context, `loadFloorsByProjectId`, `resolveFloorId`, `pickFloorLabel`), `services/property-showcase/labels.ts` (labels extension + fallbacks), `components/property-showcase/types.ts` (payload extension), `app/api/showcase/[token]/helpers.ts` (`loadFloorFloorplans`, `loadPropertyFloorFloorplans`, refactored linked-space loader), `app/api/showcase/[token]/route.ts` (wire property-floor loader), `components/property-showcase/ShowcaseFloorplans.tsx` (floor sub-article), `components/property-showcase/ShowcaseLinkedSpacesFloorplans.tsx` (per-group floor tile), `components/property-showcase/ShowcaseClient.tsx` (prop forwarding), `services/pdf/renderers/PropertyShowcaseSections.ts` (group+floor tile stack), `services/pdf/renderers/PropertyShowcaseRenderer.ts` (section 13a + `propertyFloorFloorplans` data field), `app/api/properties/[id]/showcase/generate/helpers.ts` (`BuildPdfDataExtras`, `loadShowcasePropertyFloorFloorplans`, downloader SSoT, regenerate path), `app/api/properties/[id]/showcase/generate/route.ts` + `app/api/properties/[id]/showcase/pdf/route.ts` (updated call shape), 2 locale JSONs + this ADR entry. No schema migration, no new indexes (existing `files(companyId, entityType, entityId, category)` covers floor queries; `floors.where(projectId)` hits the default per-collection index). |
| 2026-04-18 | **Phase 7.3 Google-level self-heal: every showcase hit that discovers a DXF without a `thumbnailUrl` automatically drives it through the missing pipeline stage — zero operator action, zero duplicate rasterizer.** After Phase 7.2 fixed the parking entityType mismatch, storage tiles still rendered `Δεν υπάρχει διαθέσιμη προεπισκόπηση` on the first showcase hit because the DXF auto-process hook (Phase 7.1 finalize) only covers *new* uploads. Giorgio asked for the Google-grade fix: the showcase itself should heal stale data silently. **Fix — new SSoT module `src/services/floorplans/dxf-thumbnail-selfheal.ts`**: exposes `ensureDxfThumbnail(fileId)` + two composable primitives `ensureProcessedDataForDxf(...)` and `rasterizeDxfThumbnail(...)`. The orchestrator reads the current FileRecord and advances to the next missing stage: if `processedData.processedDataPath` is absent → runs the SSoT `processDxf()` server service (same module `/api/floorplans/process` uses) which writes `.dxf.processed.json` to Storage → Cloud Function `onDxfProcessedFinalize` rasterizes the PNG on the natural trigger path; if `.processed.json` exists but `thumbnailUrl` is still null (e.g. Phase 3.1 gzip-decode incident, or a prior rasterize failure) → rasterizes directly in-process via the shared `rasterizeDxfScene` + Firestore write-back. Exits fast (`already-present`) when the thumbnail is already populated. **Fix — showcase route fire-and-forget hook**: `api/showcase/[token]/route.ts::scheduleDxfSelfheal(metas)` iterates the media list returned by `listPropertyMedia` / `listEntityMedia` and, for every item with `ext === 'dxf'` and no `thumbnailUrl`, calls `ensureDxfThumbnail(m.id).catch(logger.warn)` without awaiting. Wired into all three loaders: `loadFilesByCategory` (property floorplans), parking loop, storage loop. First hit renders the `previewUnavailable` placeholder and returns fast (≤30 s budget preserved); the Cloud Function or the direct rasterize completes in 3–8 s; second hit picks up `thumbnailUrl` and shows the PNG. UX: user refresh = preview appears. No manual tab-opening, no admin back-fill, no commit/push required. **Fix — back-fill refactor**: `api/admin/migrate-dxf-thumbnails/helpers.ts` reduced from 284 LOC to 144 LOC — no longer owns the rasterize/processData logic; imports `ensureProcessedDataForDxf` and `rasterizeDxfThumbnail` from the new SSoT module. Pure orchestration (candidate discovery, iteration, reporting) stays in the back-fill helper. The legacy `rasterizeAndPersist` + inline `extractScene` + inline `buildFirebaseDownloadUrl` dups are gone. **Fix — SSoT registry ratchet**: `.ssot-registry.json` gains the `dxf-thumbnail-selfheal` module entry with `forbiddenPatterns` blocking any new `async function ensureProcessedDataForDxf` / `async function rasterizeDxfThumbnail` outside the canonical file — prevents future drift. **Files touched**: 5 — new service `services/floorplans/dxf-thumbnail-selfheal.ts` (~230 LOC), refactored `app/api/admin/migrate-dxf-thumbnails/helpers.ts` (-140 LOC), extended `app/api/showcase/[token]/route.ts` (ensureDxfThumbnail import + scheduleDxfSelfheal helper + 3 call-site wirings), extended `.ssot-registry.json`, this ADR entry. No schema migration, no new i18n keys, no Cloud Function redeploy. **Tests**: existing `decodeProcessedJsonBytes.test.ts` covers the gzip decode path both paths now share. **Convergence guarantee**: the Cloud Function trigger, the back-fill endpoint, and the showcase self-heal all produce byte-identical `.thumbnail.png` artifacts because they delegate to the exact same `rasterizeDxfScene` + `processDxf` primitives; zero drift is mechanically enforced by the SSoT ratchet. |
