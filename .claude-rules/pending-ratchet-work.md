# Pending Ratchet Work — Live Checklist

**STATUS: ACTIVE**

**🆕 Header filter/trash κουμπιά — η ΔΕΥΤΕΡΗ οπτική διάλεκτος (2026-07-16, Opus 4.8, N.18 + ADR-584· βρέθηκε τρέχοντας full `jscpd:check` κατόπιν εντολής Giorgio).** Η **Ομάδα Α** (Buildings/Parkings/Storages) **ΕΓΙΝΕ** → `buildHeaderCustomActions` + `ListPageHeader` στο `@/core/headers`. Απομένει η **Ομάδα Β — 4 headers** που έχουν τα **ίδια δύο κουμπιά** (mobile-filter + trash-toggle) με **διαφορετικό στυλ**:

`projects/ProjectsHeader` · `properties/page/PropertiesHeader` · `property-management/page/PropertiesHeader` · `sales/page/SalesAvailableHeader`

| | Ομάδα Α (κεντρικοποιημένη) | Ομάδα Β (εκκρεμεί) |
|---|---|---|
| Padding | `p-2` | `spacing.padding.sm` |
| Radius | `radius.md` / `quick.button` | `rounded-md` |
| Hover | `INTERACTIVE_PATTERNS.ACCENT_HOVER` | `INTERACTIVE_PATTERNS.BUTTON_SUBTLE` |
| Trash border | `getStatusBorder('default')` | `border border-destructive/30` |
| Badge | `font-bold`, `leading-none`, cap `99+` | `font-medium`, **χωρίς cap** |
| a11y | `aria-pressed` | **κανένα** + `<Tooltip>` wrapper |

**Γιατί ΔΕΝ έγινε τώρα (πραγματικό εμπόδιο, όχι τεμπελιά):** η ενοποίηση **αλλάζει την εμφάνιση σε 4 σελίδες** (padding, hover, badge cap) → **απόφαση προϊόντος**, όχι refactor. Ίδιο μοτίβο με το `BaseCard` παρακάτω.

**⚠️ Υπάρχει και a11y κενό, όχι μόνο στυλ:** η Ομάδα Β **δεν** έχει `aria-pressed` στο trash toggle → ο screen reader δεν ανακοινώνει αν ο κάδος είναι ενεργός. Επίσης `properties/page/PropertiesHeader` **δεν κόβει** το badge στο `99+` → 4ψήφιος μετρητής σπάει το layout του `h-4 w-4` κύκλου.

**Χρειάζεται απόφαση Giorgio:** ευθυγράμμιση της Β στην Α (αποδοχή οπτικής αλλαγής σε 4 σελίδες, κερδίζουμε a11y + 1 SSoT) ή παραμετροποίηση του `buildHeaderCustomActions` με `variant: 'accent' | 'subtle'`. **Εκτίμηση**: ~1h, 4 αρχεία, 1 domain → Plan Mode. Reproduce: `grep -rln 'key="trash-toggle"' src/`.

- ✅ **CHECK 3.28 — «Edit … Type» preview twins (Slab/Wall): DONE + COMMITTED (2026-07-16, Opus 4.8, N.18 + ADR-584· `e43f784b` refactor + `7165868e` changelog + `c59dae7d` tests/fit).** Βρέθηκε με **full `jscpd:check`** (όχι diff): το **μεγαλύτερο clone ΟΛΟΥ του repo** (101 γρ./627 tokens) ήταν `SlabTypePreviewRenderer` ↔ `WallTypePreviewRenderer`, + 45L/425T (`fitCamera`) + 60L/370T (τα δύο panels) — το slab preview ήταν **αντιγραφή** του wall preview. **2 νέα SSoT**: `bim-3d/preview/band-stack-preview-renderer.ts` (`BandStackPreviewRenderer<TDna,TBand>` = όλο το mini THREE viewport) + `ui/ribbon/components/BandStackPreviewPanel.tsx` (όλο το React shell). **ΟΧΙ god-shell** — ό,τι διαφέρει πραγματικά ζει σε ένα `BandStackPreviewSpec` 4 σημείων (`viewDir`, `fallbackThicknessM`, `buildBands`, `boxOf`, `halfExtents`). Η γενίκευση βγήκε φθηνή επειδή τα δύο band types έχουν **ίδια ονόματα πεδίων** (`layerId`/`materialId`) και τα δύο DNA ικανοποιούν δομικά `{layers:[{id,materialId}], totalThickness}` → μόνο structural bounds, **καμία** αλλαγή στους converters. **Public API αμετάβλητο** (κλάσεις + props contract) → **κανένα από τα 3 dialogs δεν άγγιξε** (`EditWallType`/`EditSlabType`/`EditRoofType`· το roof ήδη ξαναχρησιμοποιούσε το slab panel). **Tests (μετά από ερώτηση Giorgio «τι θα έκαναν οι μεγάλοι;»): το refactor έγινε testable αντί να δηλωθεί untestable.** Η λογική βγήκε **έξω** από το GPU object: (α) νέο **`band-stack-fit.ts`** (`solveFitDistance` + `FIT_MARGIN`) = το 8-corner frustum solve, καθαρό, χωρίς WebGL· (β) τα **specs έγιναν export** — αυτά *είναι* η επιφάνεια ρίσκου του merge, και είναι pure. Untestable μένει μόνο το `WebGLRenderer` shell = κώδικας της THREE, όχι δικός μας. **51 νέα tests**: `band-stack-fit.test.ts` (24 — **property tests**: πραγματική `PerspectiveCamera` στο solved distance → projection και των 8 γωνιών → `|ndc| ≤ 1` + όλα μπροστά + **tightness** ώστε `return Infinity` να μην περνά· real stubs + degenerate 1mm + 1m + ακραία aspects 0.4/4.0 + monotonicity) και `band-stack-preview-spec.test.ts` (27 — regression lock μεταγραμμένο από τους **προ-refactor** renderers: shared contract via `describe.each` incl. «τα bands αθροίζουν στο DNA total thickness» + τα ακριβή deltas κάθε spec + guard «το merge ΔΕΝ τα ισοπέδωσε» — τοίχος→Z, πλάκα→Y). **Mutation-verified:** έβαλα επίτηδες ακριβώς το bug που ρισκάρει αυτό το refactor (wall `boxOf` να στοιβάζει κατά Y σαν την πλάκα) → **2 tests έσπασαν** → revert + επιβεβαίωση. **Verification: `jscpd:diff` 7 αρχεία = 3 pairs→0 (ΧΩΡΙΣ `SKIP_JSCPD_DIFF`)· `jscpd:check` 3041→3035· 56/56 GREEN· i18n keys επαληθευμένα στο `el`.** **ΟΧΙ tsc (N.17).** ⚠️ **Αξίζει ακόμα μία ματιά στον browser** — τα tests καλύπτουν geometry/fit contract, αλλά **κανένα δεν ελέγχει pixels** (υλικά, καθαρότητα outline, texture UVs = οπτικά). ⚠️ **`jscpd:baseline` ΔΕΝ έχει τρέξει** (Giorgio 2026-07-16: όχι τώρα) → τα −24 clones (3059→3035) **δεν έχουν κλειδώσει**· το ratchet επιτρέπει παλινδρόμηση μέχρι να τρέξει. Εκκρεμεί επίσης η **εγγραφή στο ADR-584** (το αρχείο έμεινε uncommitted: περιέχει και ξένες εγγραφές άλλων πρακτόρων — headers + hover-halo — που ο κώδικάς τους δεν έχει μπει ακόμα· θα πάει μαζί τους).

**🆕 Backlog από το full `jscpd:check` σάρωμα (2026-07-16, Opus 4.8) — τα επόμενα clusters, κατά σειρά αξίας.** Σύνολο repo: **3035 clones / 35.9k διπλές γραμμές (2.30%)**. Δεν είναι όλα κεντρικοποιήσιμα — αυτά είναι τα πραγματικά:

| Cluster | Μέγεθος | Σημείωση |
|---|---|---|
| `hooks/` ↔ `hooks/` | **1331 γρ. / 82 clones / 32 αρχεία** | Το μεγαλύτερο cluster του repo. Θέλει δικό του σάρωμα — μάλλον impact-guard hooks (`useProjectBrokerTerminateImpactGuard` 282 γρ., `useIkaLaborComplianceSaveImpactGuard` 185 γρ.). |
| `useGuideActions` ↔ `useGuideState` | 157 γρ. (66+47+44) | ⚠️ **ADR-040 files** — ο διαχωρισμός mutations-only vs reactive είναι **σκόπιμος**. Πιθανό κοινό pure core, ΟΧΙ merge. Θέλει ADR-040 staging (CHECK 6B). |
| `WallDna` δεν χρησιμοποιεί `LayeredBuildup` | εννοιολογικό | Το `SlabDna` = `LayeredBuildup<SlabLayerZone>` (generic SSoT σε `layered-buildup.ts`)· το `WallDna` δηλώνει **δικό του** ταυτόσημο interface + δικό του `computeTotalThickness`. jscpd-αόρατο (κάτω από min-tokens). |
| `ExtendToolStore` ↔ `TrimToolStore` | 51 γρ. / 206 tokens | Δίδυμα tool stores. |
| `contextual-mep-{data-outlet,socket}-tab` | 58 γρ. / 366 tokens | **SSoT υπάρχει ήδη** (`ribbon-mep-auto-bridge`) — αναδοχή, όχι νέο module. |
| `canvas-mouse-types` ↔ `unified-grip-types` | 65 γρ. | Δίδυμοι type blocks. |
| `openai-document-analyzer` ↔ `openai-quote-analyzer` | 46 γρ. / 217 tokens | ⚠️ **2 domains** (accounting/procurement) → N.8: θέλει έγκριση Giorgio. |
| `BuildingPhotosTab` ↔ `BuildingVideosTab` | 44 γρ. / 233 tokens | Media-tab δίδυμα. |
| `SpaceFloorplanInline` ↔ `FloorFloorplanInline` | 44 γρ. / 193 tokens | |
| `floorplan-dxf-renderer` ↔ `thumbnail-generator` | 53 γρ. / 323 tokens | ⚠️ 2 domains. |

**🆕 BIM renderer fill+outline body — ΤΟ ΤΕΛΕΥΤΑΙΟ κομμάτι του BIM cluster (2026-07-16, Opus 4.8, N.18 + ADR-584).** Ρεύματα που **ΕΓΙΝΑΝ**: hitTest+getGrips (51→40), hover-halo (40→26), drawPolygonPath+drawStroke/drawPolyline+getGrips-map (26→**13**). Απομένουν **13 clones**, μία οικογένεια — και **ΔΕΝ είναι μηχανικό de-dup**:

1. **fill+outline body sequence** — `fillStyle → trace → fill → strokeStyle → lineWidth → [setLineDash] → trace → stroke` σε MepBoiler/MepFitting/MepFixture/MepManifold/MepRadiator/MepSegment/MepUnderfloor/MepWaterHeater.

**⚠️ ΓΙΑΤΙ ΔΕΝ ΤΟ ΕΚΑΝΑ (δύο πραγματικοί λόγοι, τεκμηριωμένοι):**
- **Παραμετρικό soup**: αποκλίνουν σε lineWidth (`NORMAL` vs `THIN`), σε dash (MepSegment/MepFitting/MepUnderfloor έχουν, οι συσκευές όχι) και σε πηγή fill (`adaptFillTintForCanvas` vs `resolveBimBodyFill`) → ένας helper θα ήθελε ~5 params = ακριβώς το god-helper που απαγορεύει το ADR.
- **🔴 Σκόπιμη διαρροή ctx state**: ο `MepBoiler` βάζει `lineWidth = NORMAL` για το outline και **τα symbol strokes από κάτω βασίζονται στο ότι το κληρονομούν** μέχρι το ρητό `THIN`. Helper τύπου `strokePolygonOutline` (κάνει save/restore) θα το **επανέφερε σιωπηλά** → πραγματική αλλαγή σχεδίασης μέσα σε de-dup commit.

**Θέλει απόφαση σχεδίασης, όχι σάρωμα.** Πιθανή σωστή λύση: `fillAndStrokePolygonScreen` **ΧΩΡΙΣ** save/restore (ώστε η διαρροή να διατηρηθεί αυτούσια) + optional dash — αλλά αξίζει μόνο αν κρατηθεί κάτω από 4 params. Αλλιώς: άφησέ τα.

**ℹ️ Το «base class blocker» που έλεγαν οι προηγούμενες εκδόσεις αυτής της γραμμής ΔΕΝ ισχύει** — `paintPolygonHoverHalo`/`tracePolygonScreenPath`/`strokePolylinePaths`/`mapBimGrips` είναι **ελεύθερες συναρτήσεις**, καλούνται από οποιονδήποτε renderer· καμία αλλαγή κληρονομιάς. (Το `beginPhasedBodyRender` είναι method και **αυτό** θέλει base class — χωριστή δουλειά.)

**Ratchet**: commits που αγγίζουν πολλά sibling renderers χρειάζονται `SKIP_JSCPD_DIFF=1` (βλ. περιορισμό `--diff` παρακάτω).

**📌 Υπόλοιπο ομάδας Β (μπλοκαρισμένο στην απόφασή σου, βλ. ερώτημα 2)**: FloorFinish/ThermalSpace κρατούν ακόμα ιδιωτικό `drawPolygonPath` + inline `getGrips` map — έμειναν εκτός αυτού του πάσου γιατί είναι ομάδα Β. Όταν αποφασιστεί το halo τους, μπαίνουν μαζί (μηχανικό, ~10 λεπτά).

**❓ ΑΝΟΙΧΤΟ ΕΡΩΤΗΜΑ 1 — Foundation grips (ΕΠΙΒΕΒΑΙΩΜΕΝΟ BUG, περιμένει απόφαση Giorgio):** ο `FoundationRenderer.getGrips` κάνει hardcode `type: 'vertex' as const` σε **ΟΛΑ** τα grips, ενώ ο generator εκπέμπει `type:'center'` (gripIndex 0) **και** `type:'edge'` (2/3/8/9). **Ο τύπος ΕΙΝΑΙ load-bearing**: ο `isGripTypeVisible` (hooks/grips/grip-type-visibility.ts — SSoT) κρίνει `center → showCenters`, `edge/midpoint → showMidpoints`, `vertex → πάντα ορατό`. Άρα η θεμελίωση είναι **η μόνη οντότητα όπου οι τρεις διακόπτες «Grip Types» δεν κάνουν τίποτα** — το κεντρικό MOVE grip + τα 4 edge grips εμφανίζονται πάντα, ακόμα κι όταν ο χρήστης έσβησε Centers/Midpoints. Επιπλέον το σχόλιο του renderer («central MOVE grip not emitted — Alt+drag moves the pad») είναι **παρωχημένο**: το ADR-517 το ξαναπρόσθεσε (foundation-grips.ts:145-156). ⚠️ **Το `mapBimGrips` ΔΕΝ το λύνει πλήρως**: χαρτογραφεί `edge → 'vertex'` — άρα διορθώνει το center αλλά κρατά το edge flattening (και το ίδιο ισχύει ήδη για Beam/MepFixture/MepManifold/MepRadiator που μεταφέρθηκαν). Θέλει: (α) fix του Foundation, (β) απόφαση αν το `mapBimGrips` πρέπει να περνά `edge` αυτούσιο.

**❓ ΑΝΟΙΧΤΟ ΕΡΩΤΗΜΑ 2 — ομάδα Β halo `glowExtraWidth + 1.5` (ΑΠΑΝΤΗΘΗΚΕ — ΔΕΝ είναι clone· μένει μικρό defect):** η παλιά έκδοση αυτής της γραμμής έλεγε «ευθυγράμμιση στο SSoT = +0.5px, θέλει απόφαση». **Λάθος framing — η ευθυγράμμιση θα ήταν regression.** Τα 4 (FloorFinish/SpaceSeparator/ThermalSpace/WallCovering) στρώνουν το **σώμα** τους σε `lineWidth = 1.2` (όχι `NORMAL = 2`) → το `glowExtraWidth + 1.5` **δεν** είναι κακή αντιγραφή του τύπου της ομάδας Α· είναι ο **παραμετρικός** τύπος της ομάδας Γ (`entityLineWidth + glowExtraWidth`) με λεπτό σώμα. **Η ομάδα Β ανήκει στη Γ, όχι στην Α** → σκόπιμα εκτός de-dup. Το πραγματικό (μικρό) defect: το magic `1.5` δεν ταιριάζει με το `1.2` που υποτίθεται ότι παρακολουθεί (και το `1.2` είναι κι αυτό hardcoded). **Σωστή λύση όταν αποφασιστεί**: optional `lineWidth` param στο `paintPolygonHoverHalo` (default `NORMAL`) + token για το 1.2 — **ΟΧΙ** ισοπέδωση στο 2. Οπτική συνέπεια αν ευθυγραμμιστεί στο 1.2: halo 7.5→7.2px σε 4 τύπους. *(Bonus, χωριστό: SpaceSeparator:57 + ThermalSpace:68 έχουν δεύτερο halo `+2`/alpha 0.9 για `selected` — hover 7.5 < selected 8, σκόπιμη ιεραρχία.)*

**ℹ️ Περιορισμός CHECK 3.28 `--diff` (τεκμηρίωση, όχι bug προς fix τώρα):** το `--diff` σαρώνει **ΟΛΟΚΛΗΡΑ** τα staged αρχεία και θεωρεί κάθε clone μεταξύ τους «same-commit clone». Σε commit που αγγίζει 15+ sibling renderers (π.χ. cross-cutting SSoT swap), αναφέρει τα **προϋπάρχοντα** δομικά δίδυμα ως νέα → false-positive FAIL ακόμα κι όταν το diff **μειώνει** duplication. Το Layer 2 (`jscpd-ratchet.yml`, full scan vs baseline) δεν έχει αυτό το πρόβλημα.

- ✅ **CHECK 3.28 cross-file clone — BIM renderers **hover-halo** (ομάδα Α): DONE (2026-07-16, Opus 4.8, N.18 + ADR-584· UNCOMMITTED).** **ΚΑΝΕΝΑ νέο SSoT δεν χρειάστηκε για τα 10** — το `paintPolygonHoverHalo` υπήρχε ήδη· και, όπως έλεγε σωστά το handoff, **καμία αλλαγή base class** (ελεύθερη συνάρτηση). **Μετάβαση 12 renderers / 12 halo sites.** **10 καθαρά κλειστά footprints** (MepBoiler/MepFitting/MepFixture/MepManifold/MepRadiator/MepSegment/MepUnderfloor/MepWaterHeater/Roof/SlabOpening) → `paintPolygonHoverHalo(this.ctx, (p) => this.worldToScreen(p), verts, phaseState.phase === 'highlighted')` — byte-ταυτόσημο, **μηδενική οπτική αλλαγή**. **2 ΔΕΝ ιχνηλατούν πολύγωνο** (εδώ το handoff ήταν αισιόδοξο): ο **Beam** ιχνηλατεί τα **multi-subpath** κομμάτια του ADR-458 cutback (`buildPiecesPath`), το **Railing** μια **ΑΝΟΙΧΤΗ** πολυγραμμή — το `paintPolygonHoverHalo` θα τα έκλεινε (φάντασμα γραμμής end→start στο κιγκλίδωμα). Άρα το **στυλ** του glow (το πραγματικά διπλότυπο μέρος) βγήκε σε **νέο primitive `paintHoverHalo(ctx, highlighted, trace)`** — κατέχει save/glow-style/stroke/restore, δέχεται path-only `trace` callback· το `paintPolygonHoverHalo` είναι πλέον wrapper μιας γραμμής πάνω του → οι σταθερές του glow δηλώνονται **μία φορά** και σερβίρουν **κάθε** σχήμα halo. **2 σκόπιμα εκτός**: (α) **MepSegment riser (:299)** — screen-space `arc()`, τρέχει *μέσα* σε ξένο save/restore και τελειώνει με hardcoded `globalAlpha = 1` αντί restore → το save/restore του SSoT θα επανέφερε τον **κληρονομημένο** alpha = πραγματική (αν και απίθανη) αλλαγή συμπεριφοράς μέσα σε de-dup commit· (β) **ομάδα Β** — βλ. ανοιχτό ερώτημα 2 παραπάνω (**δεν** είναι clone· ανήκει στη Γ). Καθαρίστηκαν 9 orphan `HOVER_HIGHLIGHT` imports + `RENDER_LINE_WIDTHS` από SlabOpening. **Verification: `jscpd:diff` 13 αρχεία 40→26 clones· 248/248 renderer+visibility tests (23 suites) GREEN· `grep -rn glowExtraWidth` αφήνει μόνο SSoT + Β(4) + Γ(Stair/Wall) + riser — μηδέν επιζώντες ομάδας Α· 0 orphan imports.** **ΟΧΙ tsc (N.17).** 🔴 Giorgio commit: bim-polygon-render.ts + 12 renderers + ADR-584 + αυτό το αρχείο. ⚠️ Χρειάζεται `SKIP_JSCPD_DIFF=1` (το diff **μειώνει** duplication· βλ. περιορισμό `--diff` παραπάνω).

  **Δεύτερο κύμα ίδιου πάσου (26 → 13), όλα σε SSoT που ΥΠΗΡΧΑΝ ήδη:** **(α)** ιδιωτικό `drawPolygonPath` → `tracePolygonScreenPath` σε **10 renderers / 20 call sites** (και οι 12 αντιγραφές στον φάκελο ήταν byte-ταυτόσημες μεταξύ τους ΚΑΙ με το SSoT → ο helper διαγράφηκε). **(β)** `drawStroke`(×4) + `drawPolyline`(×2) → `strokePolylinePaths` — έξι byte-ταυτόσημοι helpers. Όπου το βρόχι άλλαζε στυλ **ανά στοιχείο** (MepBoiler strokes/ventStrokes = χρώμα ανά System Classification) κρατήθηκε με `[line]`· όπου **όχι**, μπαίνει όλος ο πίνακας — το `strokePolylinePaths` **ΕΙΝΑΙ** αυτό το βρόχι. `MepUnderfloor.drawLoopPath` σκόπιμα εκτός (βάζει δικό του στυλ, χωρίς `<2` guard). **(γ)** `getGrips` `.map()` → `mapBimGrips` σε MepUnderfloor/Roof/SlabOpening — **επαληθευμένο όπως και το προηγούμενο ρεύμα**: το `mapBimGrips` δίνει `center → 'center'` ενώ τα inline έστελναν τα πάντα-πλην-midpoint στο `vertex`· και οι τρεις delegate στο κοινό `buildPolygonOutlineGrips` που εκπέμπει **μόνο** `vertex`/`midpoint` → ταυτόσημο. **Verification: `jscpd:diff` 40→13· 248/248 GREEN· −325 καθαρές γραμμές (94+/419−)· `grep` για ορφανούς helpers/imports = κενό.**

- ✅ **CHECK 3.28 cross-file clone — BIM renderers `hitTest` + `getGrips` (2026-07-16, Opus 4.8, N.18 + ADR-584· UNCOMMITTED).** **ΚΑΝΕΝΑ νέο SSoT δεν χρειάστηκε** — και τα δύο υπήρχαν ήδη στο `bim-polygon-render.ts` (`polygonBboxHitTest`, `mapBimGrips`) και τα χρησιμοποιούσε **μόνο** η οικογένεια `BimFootprintRenderer` (Column/ElectricalPanel/FloorplanSymbol/Furniture/Slab)· η οικογένεια `BaseEntityRenderer` (MEP/Beam/Roof/Railing/…) είχε μείνει πίσω με inline αντίγραφα. **Μετάβαση 15 renderers**: `hitTest` → `polygonBboxHitTest` (MepBoiler/MepFixture/MepManifold/MepRadiator/MepWaterHeater/MepUnderfloor/MepSegment/Roof/SlabOpening/FloorFinish/ThermalSpace) + **νέο primitive `bboxRejectsPoint`** (bim-polygon-render.ts· το `polygonBboxHitTest` το καλεί πλέον εσωτερικά → το SSoT συνεπές) για τα 3 με entity-specific ουρά (Beam=ADR-458 cutback pieces, MepFitting=degenerate footprint→true, Railing=distanceToSegment). `getGrips` → `mapBimGrips` (Beam/MepFixture/MepManifold/MepRadiator). **Behavior-preserving — ΕΠΑΛΗΘΕΥΜΕΝΟ, όχι υποτεθέν**: το `mapBimGrips` χαρτογραφεί `midpoint→midpoint` ενώ τα inline `.map()` έστελναν τα πάντα-πλην-center στο `vertex`· grep σε ΟΛΟΥΣ τους generators → εκπέμπουν μόνο `center`/`vertex`/`edge`/`corner`, **ποτέ** `midpoint` → ταυτόσημο αποτέλεσμα. **Foundation ΣΚΟΠΙΜΑ εκτός** (βλ. ανοιχτό ερώτημα παραπάνω). **Verification: `jscpd:diff` 51→40 clones· 248/248 renderer+visibility tests GREEN· grep-verified 0 orphan `pointInPolygon` imports / 0 leftover bbox αντίγραφα.** **ΟΧΙ tsc (N.17).** 🔴 Giorgio commit: bim-polygon-render.ts + 15 renderers + ADR-584 + αυτό το αρχείο. ⚠️ Χρειάζεται `SKIP_JSCPD_DIFF=1` (40 προϋπάρχοντα clones, βλ. περιορισμό `--diff` παραπάνω).

- ✅ **CHECK 3.28 cross-file clone — CRM task/calendar dialogs (cluster 5): DONE (2026-07-16, Opus 4.8, N.18 + ADR-584· UNCOMMITTED).** Το ρεύμα είχε μείνει **μισοτελειωμένο** από προηγούμενο session (VS Code crash): τα atoms είχαν εξαχθεί (`ui/date-picker-field`, `ui/enum-select`, `crm/shared/TaskDialogFormFooter`, `constants/crm-task-enums`, `i18n/date-fns-locale`, `combineDateAndTime`/`splitDateAndTime` στο `date-local.ts`) αλλά τα **δύο dialogs έμεναν δίδυμα στο περίβλημα** → `jscpd:diff` **7 clone pairs** (ακριβώς το N.18 failure mode). Εξήχθησαν τα 2 υπόλοιπα SSoT: **`crm/shared/TaskDialogShell.tsx`** (Dialog→header icon+title→sr-only desc→form→children→footer· κατέχει το iconSizes/sp/layout τρίο + αποδίδει το ίδιο το footer, αφού το `onCancel` ήταν ταυτόσημο) + **`crm/shared/TaskFormField.tsx`** (fieldset+Label+control· optional `action` slot για το `VoiceMicButton` — το μόνο σημείο απόκλισης). **No-god-shell:** κάθε dialog κρατά τα δικά του πεδία ως children (create=επαφή/έργο/υπενθύμιση/endDate, edit=κατάσταση/προτεραιότητα)· `TaskDetailPanel` σκόπιμα εκτός (πληκτρολογήσιμο date field + PopoverAnchor = άλλο UX, τεκμηριωμένο). **Επίσης: `types/crm-extra.ts` ΔΙΑΓΡΑΦΗΚΕ** — ανταγωνιστικό re-declaration των 3 unions δίπλα στο νέο `crm-task-enums` SSoT· ο μοναδικός καταναλωτής (`task-activity.ts`) → `@/types/crm` (re-export του SSoT). **Verification: `jscpd:diff` 16 files = 7→0 clones (ΧΩΡΙΣ `SKIP_JSCPD_DIFF`)· 13 NEW date-local SSoT tests → 27/27 GREEN· grep-verified 0 orphan refs· 398→303 / 332→209 γρ. (N.7.1).** **ΟΧΙ tsc (N.17).** ⚠️ **browser-verify pending** (κανένα test δεν αποδίδει τα dialogs). 🔴 Giorgio commit: TaskDialogShell.tsx NEW + TaskFormField.tsx NEW + 2 dialogs + task-activity.ts + crm-extra.ts (deleted) + date-local.test.ts + ADR-584 + αυτό το αρχείο (μαζί με τα ήδη-uncommitted CRM atoms).

**🆕 `BaseCard` vs design-system card shells — δύο παράλληλες οικογένειες καρτών (2026-07-16, βρέθηκε ψάχνοντας 3ο αδερφάκι για το GridCard/ListCard cluster):** το `src/components/core/BaseCard/BaseCard.tsx` είναι **ανταγωνιστικό** card system που ζει δίπλα στο `@/design-system` GridCard/ListCard. Το χρησιμοποιούν όλα τα `src/components/compositions/*Card` (ContactCard, PropertyCard, StorageCard, TaskCard, UserCard, NotificationCard).

**ΔΕΝ είναι line-level clone** (γι' αυτό το jscpd δεν το πιάνει) — είναι **εννοιολογικό** διπλότυπο με ασύμβατο contract:
| | design-system GridCard/ListCard | core/BaseCard |
|---|---|---|
| Βάση | custom semantic `<article>` | shadcn `Card`/`CardHeader`/`CardContent` |
| Selection | αριστερή λωρίδα `bg-primary rounded-r-full` | `ring-2 ring-primary ring-offset-2` |
| Favorite | `Star`, hover-only `role="toolbar"` | `Heart`, πάντα ορατό `Button` |
| Actions | `group-hover:opacity-100` toolbar | `DropdownMenu` + `MoreVertical` |
| Badges | `Badge` από `@/components/ui/badge` | `CommonBadge` από `@/core/badges` |
| Keyboard | Enter/Space → onClick (`useCardInteraction`) | **καμία** ενεργοποίηση με πληκτρολόγιο |

**Το πραγματικό πρόβλημα δεν είναι οι διπλές γραμμές — είναι το a11y κενό:** ό,τι αποδίδεται μέσω `BaseCard` (6 compositions) **δεν ενεργοποιείται με πληκτρολόγιο**, ενώ ό,τι πάει από τα design-system shells ενεργοποιείται. Ίδια εμφάνιση για τον χρήστη, δύο διαφορετικές συμπεριφορές.

Δεν έγινε τώρα γιατί: (α) **>1h / 6+ αρχεία** (N.0.2 → pending, όχι επιτόπου)· (β) το visual contract διαφέρει πραγματικά (ring vs λωρίδα, Heart vs Star, dropdown vs toolbar) → η ενοποίηση **αλλάζει εμφάνιση** σε 6 compositions, δηλαδή απόφαση προϊόντος όχι refactor· (γ) απαιτεί απόφαση Giorgio ποια οικογένεια είναι η κανονική.
**Χρειάζεται ρητή απόφαση Giorgio.** Reproduce: `src/components/core/BaseCard/BaseCard.tsx` vs `src/design-system/components/{GridCard,ListCard}/`. Δες **ADR-013**.

**🆕 Telegram CRM store → migrate onto `channel-crm-store` SSoT (2026-07-16, discovered while de-duplicating the 3 Meta crm-adapters):** τα WhatsApp/Messenger/Instagram adapters κεντρικοποιήθηκαν σε `src/server/comms/crm/channel-crm-store.ts`. Το **`src/app/api/communications/webhooks/telegram/crm/store.ts` (448 γρ.) έμεινε έξω** — είναι 4ο αδερφάκι του ίδιου conversation/identity upsert, αλλά **δεν** μεταναστεύτηκε γιατί: (α) χρησιμοποιεί άλλο Firestore access layer (`getFirestoreHelpers` lazy + `safeDbOperation` + `isFirebaseAvailable`) αντί για `getAdminFirestore()` — ο shared store θα χρειαστεί injectable db port· (β) έχει επιπλέον πεδία (attachments/ADR-055, `username`, `isVoiceTranscription`) που ο shared store δεν μοντελοποιεί ακόμη· (γ) υπάρχει **ζωντανό production θέμα** με το Telegram (contacts δημιουργούνται μόνο για `pagonis.oe@gmail.com`) — μετανάστευση τώρα μπερδεύει τα αίτια.

**⚠️ Δύο πραγματικές αποκλίσεις του Telegram store που η μετανάστευση θα διόρθωνε (ΔΕΝ είναι κοσμητικές):**
1. **Race condition** — `messageCount: (snap.data()?.messageCount || 0) + 1` και το ίδιο για `unreadCount` (`crm/store.ts:151-154`) = read-modify-write. Δύο ταυτόχρονα webhooks → χαμένη μέτρηση. Τα 3 Meta channels χρησιμοποιούν atomic `FieldValue.increment(1)`.
2. **Ασυνέπεια `senderId`** — Telegram γράφει raw `userId` (`crm/store.ts:369`)· τα άλλα 3 γράφουν `generateExternalIdentityId(provider, id)`. Ίδιο πεδίο, δύο διαφορετικά είδη τιμής → κάθε consumer που κάνει join σε `external_identities` σπάει σιωπηλά για Telegram.

Εκτίμηση: **>1h** (db port abstraction + attachments στο shared input + regression test με το ζωντανό Telegram θέμα). Reproduce: σύγκρινε `telegram/crm/store.ts` με `src/server/comms/crm/channel-crm-store.ts`.

**🆕 ADR-598 G9 cycles debt — dxf-viewer barrel over-coupling (2026-07-16, discovered during the catch-up reseed after 204 commits):** το `depcruise` cycles baseline reseed-άρθηκε **421 → 1299** (accepted growth, ADR-598 changelog 2026-07-16). Το VERIFY έδειξε ότι **~91% (~1.190) των κύκλων είναι μέσα στο `dxf-viewer`**, διάχυτοι σε 15+ subfolders (bim 567 / core 164 / systems 123 / rendering 75 / hooks 64 / utils 63 …), με cycle-length **15–25 modules** = κλασικό αποτύπωμα **barrel-file (`index.ts`) re-export over-coupling**. **Down-only ratchet debt** (ΟΧΙ regression — legit BIM growth). Σταδιακή μείωση όταν αγγίζεις τα σχετικά subsystems: σπάσε τα fat public barrels σε στοχευμένα imports (ή διαχώρισε type-only barrels), ξεκινώντας από `bim/` (567 edges, το μεγαλύτερο cluster). Reproduce: `NODE_OPTIONS=--max-old-space-size=8192 npx depcruise src --config .dependency-cruiser.cjs --output-type json` → φίλτραρε `no-circular`, group ανά `from` prefix. Κάθε πραγματική μείωση → CI seed dispatch reseed **προς τα κάτω** (`gh workflow run depcruise-ratchet.yml -f seed=true`).

- ✅ **CHECK 3.28 cross-file clone — polygon/footprint grip resize: DONE (2026-07-16, Opus 4.8, N.0.2 + ADR-584 CHECK 3.28· UNCOMMITTED).** NEW `bim/grips/polygon-outline-grip-core.ts` SSoT — the shared emit loop (`buildPolygonOutlineGrips(entityId, verts, on)`, generic over the entity key; kind === `${on}-vertex/edge-midpoint-${i}`) + container-agnostic list helpers (`moveOutlineVertexInList`/`insertOutlineVertexInList`/`removeOutlineVertexInList`/`cloneOutlineVertex`/`outlineEdgeInsertedVertex`, all `null`=referential-no-op) + the full `applyPolygonOutlineGripDrag(gripKind, on, input, getVerts, withVerts, insertOverride?)` dispatch. Migrated **5** files (the 4 flagged + `floor-finishes/floor-finish-grips.ts`, the 5th twin): slab/slab-opening → thin (`outline.vertices`); floor-finish → `footprint.vertices`; mep-underfloor → `withVerts` seam re-derives `connectors`; roof → `insertOverride` keeps `edges` in lockstep. Public API byte-identical (get*Grips/apply*GripDrag/removeVertexFrom* signatures unchanged). One `outlineGripKind` SSoT cast mirrors `gripKindOf`'s internal `as GripKindByEntity[K]` (declared-not-enforced). **Verification: 80/80** existing grip tests (slab/slab-opening/roof/floor-finish) + **16 NEW** SSoT tests green. **`jscpd:diff` clean on all 6 files** (was 15+2 clone pairs → 0). **ΟΧΙ tsc (N.17).** 🔴 Giorgio commit: polygon-outline-grip-core.ts NEW + test NEW + 5 `*-grips.ts` + ADR-663 + αυτό το αρχείο.

- ✅ **CHECK 3.28 cross-file clone — BIM footprint-renderer boilerplate (cluster B): DONE (2026-07-16, Opus 4.8, N.0.2 + ADR-584 CHECK 3.28· UNCOMMITTED).** `jscpd` on the 5 renderers found **12 clone pairs** (22% dup) of 3 re-inlined patterns: hover-halo glow, private `drawPolygonPath`, bbox+point-in-polygon `hitTest`. **SSoT ήδη υπήρχε** (`BimFootprintRenderer` + `bim-polygon-render.ts` — τα χρησιμοποιεί ήδη Column/Slab). Migrated τους **4 stragglers** (`ElectricalPanelRenderer` / `FloorplanSymbolRenderer` / `FoundationRenderer` / `FurnitureRenderer`): `extends BaseEntityRenderer` → `extends BimFootprintRenderer`· inline halo → `this.paintHoverHalo(verts, …)`· inline hitTest → `polygonBboxHitTest(…)`· deleted κάθε private `drawPolygonPath` (inherited)· αφαιρέθηκαν τα dead `HOVER_HIGHLIGHT`/`pointInPolygon`/`RENDER_LINE_WIDTHS` imports. **`jscpd` 12→6 clones (22%→7.4%)· 158 renderer tests (18 suites) + 28 VG/tool GREEN· 0 orphan refs· ΟΧΙ tsc (N.17).** 🔴 Giorgio commit: 4 `*Renderer.ts` + ADR-584 + αυτό το αρχείο. → `npm run jscpd:baseline` μετά.
  - ✅ **↳ follow-up α+γ: DONE (2026-07-16, Opus 4.8· UNCOMMITTED).** **(α) getGrips → `mapBimGrips`** (furniture/panel/floorplan)· blocker λύθηκε: ο centred-box adapter type union = `'center'|'corner'|'vertex'` (**ποτέ `'midpoint'`**) → behaviour-identical· Foundation skip (διαφορετικός κώδικας `type:'vertex'`). **(γ) symbol-stroke loop → NEW `strokePolylinePaths(ctx, toScreen, polylines)`** στο `bim-polygon-render.ts`· adopted panel+floorplan. **`jscpd` 6→3 clones (22%→3.4%)· 171 renderer tests (19 suites) GREEN· 0 orphan refs.** 🔴 Giorgio commit: `bim-polygon-render.ts` + 3 `*Renderer.ts` (ElectricalPanel/FloorplanSymbol/Furniture) — μαζί με το cluster B commit.
  - ✅ **↳ follow-up (phase-preamble clone, surfaced co-staging τα 4 renderers μαζί): DONE (2026-07-16, Opus 4.8· UNCOMMITTED).** Το `git commit` και των 4 stragglers **μαζί** ξεσκέπασε ένα 4ο clone που τα per-renderer commits του προηγούμενου agent δεν είχαν δει: το phase-preamble `determinePhase → paintHoverHalo → applyPhaseStyle → ctx.save() → setLineDash([])`, byte-identical σε Electrical/Furniture/Floorplan + το `determinePhase…save()` κομμάτι στο Foundation (3 flagged pairs, 50-77 tokens). Extracted **`beginPhasedBodyRender(entity, verts, options) → PhaseRenderingState`** στο `bim-footprint-renderer.ts` (base SSoT· επιστρέφει το phaseState για τα renderers που branch-άρουν μετά — Foundation). Removed το πλέον-dead `import type { Entity }` και από τους 4. **`jscpd:diff` στα 5 files = 0 clones (was 3)· 158 renderer tests (18 suites) GREEN· ΟΧΙ tsc (N.17).** 🔴 Giorgio commit: `bim-footprint-renderer.ts` + 4 `*Renderer.ts` — μαζί με το cluster B commit.
  - ✅ **↳ follow-up β (repo-wide visibility-SSoT sweep): DONE (2026-07-16, Opus 4.8· UNCOMMITTED).** Το `resolveIsEntityVisible({category…},{objectStyles,disciplineVisibility,layer})` 2Δ-plan render-preamble ήταν copy-paste σε **21 renderers**. NEW `bim/visibility/bim-plan-visibility.ts` SSoT — `resolveBimPlanVisibility(entity, layer)`: διαβάζει `objectStyles`+`disciplineVisibility` (event-time, `useDrawingScaleStore.getState()`, ADR-040) και delegate-άρει στον pure `resolveIsEntityVisible`. **Σκόπιμα ΝΕΟ αρχείο** (όχι μέσα στο `visibility-resolver.ts`): ο pure resolver μένει store-free/unit-testable· ο wrapper κρατά το ίδιο module path (`drawing-scale-store` shim) ώστε τα υπάρχοντα renderer test mocks να ισχύουν αμετάβλητα. Migrated **21 renderers** (Beam/Column/ElectricalPanel/FloorplanSymbol/Foundation/Furniture/MepBoiler/MepFitting/MepFixture/MepManifold/MepRadiator/MepSegment/MepUnderfloor/MepWaterHeater/Opening/Railing/Roof/SlabOpening/Slab/Stair/Wall): 9-γραμμο preamble → 1-γραμμη κλήση· RoofRenderer έριξε και το πλέον-dead `useDrawingScaleStore` import· −190/+67 γρ. Public API (class + `render()`) byte-identical· τα 3Δ/overlay paths ΔΕΝ αγγίχτηκαν (χρησιμοποιούν richer ctx με floor/building/isolate → καλούν κατευθείαν τον pure resolver). **7 NEW SSoT tests + 248 renderer/visibility tests (23 suites) GREEN. `jscpd:check` (--full, authoritative Layer 2) = clones 3066/3494, −428 κάτω από baseline. ΟΧΙ tsc (N.17).**
    - ⚠️ **Layer 1 (`jscpd:diff`) co-staging artifact:** ο pre-commit `--diff` gate, όταν co-stage-άρονται και τα 21 renderers, αναφέρει **51 intra-diff clones** — αλλά είναι **προϋπάρχον body-debt** (grips/hover/Mep-appliance bodies, γρ. 200-360), **ΟΧΙ** δικά μου: verified clone-neutral με reconstruct των ίδιων 21 αρχείων στο HEAD → **51 και εκεί** (0 προστέθηκαν). Το δικό μου visibility preamble δεν συμμετέχει σε κανένα από τα 51. **Giorgio commit path:** είτε `SKIP_JSCPD_DIFF=1` (justified: clone-neutral vs HEAD, Layer 2 περνά −428), είτε split το commit. → run `npm run jscpd:baseline` μετά (κλείδωσε το −428).
    - 📋 **Νέο backlog (τα 51 pre-existing body clones):** κύριο cluster = **Mep-appliance renderer bodies** (MepBoiler/MepRadiator/MepWaterHeater/MepManifold μοιράζονται symbol-draw + label blocks, 163-314 tokens). Ξεχωριστό sweep («Mep appliance renderer SSoT»), ΟΧΙ visibility scope.

- ✅ **CHECK 3.28 cross-file clone — parametric grip-commit preamble + rotate-pivot (cluster A): DONE (2026-07-16, Opus 4.8, N.0.2 + ADR-584 CHECK 3.28· UNCOMMITTED).** `jscpd --diff` found **7 clone pairs** (4× 8L/65T preamble + 3× 15L/79T rotate-block) across `grip-parametric-commits.ts` / `grip-parametric-copy.ts` / `grip-parametric-centred-box-commits.ts`. Extracted NEW `hooks/grips/grip-commit-resolve.ts` with **2 SSoT helpers**: `resolveParametricGripEntity<T>(deps, entityId, entityType)` (adapter + typed entity + type/params guard, generic over the concrete entity· `candidate as T` narrowing declared-not-enforced mirror `gripKindOf`) + `resolveGripCommitAnchor(isRotationGrip, gripPosition, delta) → { anchor, currentPos, pivotPatch }` (`pivotPatch` = `{ pivot }`/`{}` spread into `apply*GripDrag`). Migrated **20 call sites** (5 commits + 11 copies + 4 centred-box· local `resolveMepManifoldGripContext` now delegates). Public commit API byte-identical. **`jscpd:diff` on all 4 files = 0 clones (was 7)· 8 NEW SSoT tests + 195 existing grip tests (20 suites) GREEN· ΟΧΙ tsc (N.17).** 🔴 Giorgio commit: grip-commit-resolve.ts NEW + grip-commit-resolve.test.ts NEW + 3 `grip-parametric-*.ts` + ADR-584 + αυτό το αρχείο. → run `npm run jscpd:baseline` μετά το commit.

**🆕 CHECK 3.28 pre-existing clones surfaced by ADR-642 §6.8 OSNAP (2026-07-13, discovered co-staging the complex-linetype snap files):** `jscpd --diff` σε όλα τα touched files ανέδειξε **προϋπάρχοντα** structural clones — **ΟΧΙ** από το δικό μου diff (η αλλαγή μου σε αυτά ήταν 1-γραμμή signature/alias):
- ~~`core/spatial/GridSpatialIndex.ts` ↔ `QuadTreeSpatialIndex.ts`~~ → **✅ ΛΥΘΗΚΕ (ADR-583, commit `26dd8b5c`)** — `BaseSpatialIndex.ts` εξήχθη· verified 2026-07-14: `jscpd:diff` και στα 2 αρχεία = **καθαρό**. Stale entry αφαιρέθηκε.
- ~~`canvas-v2/overlays/SnapIndicatorGlyph.tsx` self-clones~~ → **✅ ΗΔΗ ΛΥΜΕΝΟ (ADR-583· verified 2026-07-16).** Η note ήταν stale: το αρχείο **έχει ήδη** εξαχθεί `GlyphSvg` wrapper + 8 mark primitives (`SquareMark`/`CircleOutlineMark`/`CenterDotMark`/`TriangleMark`/`DiamondMark`/`CrossMark`/`H`&`VCenterLineMark`) και κάθε `case` τα συνθέτει δηλωτικά. **`jscpd` @ min-tokens 50 (ratchet threshold) = 0 clones** (1 residual @ min-tokens 30 = ΚΑΤΩ από threshold, δεν μετράει). Αρχείο clean/unmodified στο working tree. Κανένα cluster-C work δεν απαιτείται — stale entry κλείνει εδώ.

**🔎 FINAL DUPLICATE AUDIT (2026-07-08, Opus 4.8 orchestrator `dxf-duplicate-final-audit`, 30 agents / 8-domain semantic-clone sweep + adversarial verify· εντολή Giorgio «τελικοί έλεγχοι κεντρικοποίησης» → «μόνο καταγραφή») — 22 ΝΕΑ confirmed διπλότυπα (0 false positives), όλα ΕΚΤΟΣ των ήδη flagged.** Το `ssot:discover` Windows Phase-3 είναι σπασμένο (0 signal) → τα παρακάτω βρέθηκαν με manual per-domain sweep. **ΤΙΠΟΤΑ δεν υλοποιήθηκε ακόμα** — Giorgio αποφασίζει σειρά. Ταξινομημένα κατά blast radius:

**🟢 TIER A — SSoT ΥΠΑΡΧΕΙ, μόνο migrate + guard (χαμηλό ρίσκο· template ADR-090 translatePoint «declared-not-enforced»):**

**▶ TIER A EXECUTION STATUS (2026-07-08, Opus 4.8, 4 parallel sonnet agents· εντολή Giorgio «ξεκίνα TIER A»· UNCOMMITTED):**
- ✅ **normalize-angle: 100% DONE (G1a + C4).** G1a: 14 files σε `normalizeAngleDeg` + polarPoint 5/6· έπιασε extra consumers (ring-config, radial-ring test) + διόρθωσε import depth polygon-azimuth-utils. C4: 3 atan2 single-mod sites (EntityMergeService/ray-intersection-calculators/dim-intersection-host-finder) + local re-decl construction-point-geometry:98. **grep-verified: 0 double-mod / 0 re-decl remaining** (μόνο radial-ring-logic:86 self-use στο pieSectorPath, εντός module). 3822+ jest GREEN.
- ✅ **safeStorage (G3):** 3 files → storage-utils SSoT· 64 jest GREEN· 0 remaining.
- ✅ **stacked-center label + i18n (G4):** extract `renderStackedCenterMeasurementLabel` στο measurement-label.ts· migrate Ellipse/Arc/circle-text-utils· Rectangle σκόπιμα skip (save/restore τυλίγει edge-dists+corner-arcs)· i18n keys el+en dxf-viewer-shell (centerMeasureLabel/circleMeasureLabel)· +bonus CircleRenderer· el byte-identical. ⚠️ canvas-drawing → CHECK 6D στο commit + σωστό ADR (το comment «ADR-557» ΔΕΝ τεκμηριώνει measurement-label).
- ✅ **clamp: PROMOTE + FULL SWEEP DONE (Giorgio decision «promote σε neutral πρώτα»).** (1) **PROMOTE:** NEW neutral `utils/scalar-math.ts` (clamp/clamp01/clamp255, μηδέν deps)· `rendering/entities/shared/geometry-utils.ts` = import+re-export shim (τα 13 ήδη-migrated + clamp255 external consumers ανέπαφα· 44 jest GREEN). (2) **SWEEP ~61 sites:** G1b 13 finder-sites + C1 15 (bim) + C2 8 (systems) + C3 9 (bim-3d/config/rendering, +2 extra clamp255) + C4 dual-fix construction-point-geometry + C5 6 local `function clamp` defs + 1 inline clamp255 + Opus 3 arrow-form (mep-fitting-bend-3d/beam-section-profile-draw/BezierCurveEditor). Config/bim/bim-3d importάρουν πλέον neutral utils (όχι rendering — inversion λύθηκε). **grep-verified: ΜΟΝΟ 4 legit survivors** (μη-SSoT by design): heat-load-color clamp01 (NaN-guard wrapper), studio-background-texture clamp255 (rounds-first wrapper), AxisCutSliderControl clamp(v,range) (domain sig), RibbonMepCircuitConductorsWidget clamp(n) (fixed-bounds closure). ~800+ jest GREEN συνολικά στα touched dirs.
- ✅ **safeStorage: 100% DONE (G3)** — 3 files → storage-utils· 64 jest GREEN· 0 remaining.
- ✅ **stacked-label + i18n: DONE (G4)** — βλ. πάνω.
- ✅ **G2 (edge-triggered): 100% DONE (2026-07-08, Opus 4.8, ADR-589· UNCOMMITTED)** — NEW `hooks/tools/useEdgeTriggeredLifecycle(isActive,onActivate,onDeactivate)` SSoT (transition-only, deps `[isActive]`, single eslint-disable inside) + migrate 13 tools + CHECK 3.7 tier-3 guard `edge-triggered-tool-lifecycle` (`wasActiveRef ?= ?useRef`, allowlist SSoT + useModifyToolActivation) + golden fixture (`test:registry-golden` 86 GREEN). 13 jest GREEN (7 new hook + 6 useWallPickScaffold regression). Byte-behavior-identical (extra-dep hooks ήταν edge-guarded → no-op re-runs). 0 non-allowlisted violations → baseline αμετάβλητο. **ΟΧΙ tsc (N.17).** 🔴 Giorgio commit: 13 tools + useEdgeTriggeredLifecycle.ts NEW + test NEW + `.ssot-registry.json` + registry-golden-fixtures.js + ADR-589 NEW + adr-index.md.
- ✅ **GUARDS DONE (2026-07-08, Opus 4.8· το «καστάνι» που κλειδώνει το TIER A· UNCOMMITTED):** 3 tier-3 modules στο `.ssot-registry.json`: **(a) `scalar-clamp`** — patterns `Math.max(0, Math.min(255,` + `function clamp01(`/`function clamp255(`/`const clamp01 = (`/`const clamp255 = (`· allowlist = scalar-math.ts + geometry-utils.ts shim + heat-load-color + studio-background-texture + **1 non-dxf** floorplan-background.service.ts (δικό του clamp01, δεν μπορεί να importάρει το dxf util). **(b) `normalize-angle-deg`** — `((.*% 360) + 360) % 360` + `function normalizeAngleDeg`· allowlist geometry-angle-utils.ts + radial-ring-logic.ts (pieSectorPath self-use). **(c) `safe-storage`** — `function safeStorage`· allowlist storage-utils.ts. **⚠️ SCOPE DECISION (μιμείται point-translate-helpers):** CHECK 3.7 είναι repo-wide/χωρίς path-scope & το scalar-math είναι dxf-only util → τα generic shapes `Math.max(0, Math.min(1,` (forward), `Math.min(1, Math.max(0,` (reverse), `function clamp(`, `const clamp = (` **σκόπιμα ΔΕΝ guard-άρονται** (collide με 7 legit non-dxf: report-engine/address-editor/floorplan-background/procurement)· καλύπτονται από canonical+review+jscpd (N.18), όπως το ADR-090 inline-literal discipline. Οι AxisCutSliderControl/RibbonMep survivors χρησιμοποιούν αυτά τα un-guarded shapes → 0 allowlist entries. **Verified:** golden fixtures +3 → `test:registry-golden` **84 GREEN** + `check-ssot-imports.test.js` **139 total GREEN**· end-to-end probe (8 synthetic violations → BLOCK exit 1)· 10 allowlisted/survivor files → exit 0· `npm run ssot:baseline` + regenerated `.ssot-registry-flat.txt` (gitignored, ήταν stale — του έλειπαν ~11 modules incl. create-external-store). **ΟΧΙ tsc (N.17).** 🔴 Giorgio: commit `.ssot-registry.json` + `scripts/__tests__/fixtures/registry-golden-fixtures.js` + `.ssot-violations-baseline.json` (+ TIER A code: ~55 files + scalar-math.ts NEW + i18n el/en + measurement-label.ts· ⚠️ CHECK 6D → ADR staged για canvas-drawing renderers). **⚠️ baseline delta = +1 ΑΣΧΕΤΟ:** `systems/group/ActiveGroupStore.ts` (create-external-store violation `new Set<ActiveGroupListener>`, από commit 21294edf 2026-07-07 — postdates το 07-06 baseline· ΟΧΙ δικό μου· grandfathered). **Follow-up (create-external-store WAVE):** migrate ActiveGroupStore σε version-signal createExternalStore (stack-based store, όχι drop-in).

- ✅ **`normalize-angle-deg-inline` (12 files): DONE (2026-07-08, verified + last-inline fixed, Sonnet).** 11/12 sites migrated από την TIER A sweep (γρ.17) — grep `((x%360)+360)%360` σε όλο το dxf-viewer (πλην geometry-angle-utils) = **μόνο** `radial-ring-logic.ts:86` (`pieSectorPath` large-arc-flag). Ήταν allowlisted ως «self-use», αλλά το αρχείο ήδη κάνει `import normalizeAngleDeg` (γρ.13) → αντικαταστάθηκε `(((a1-a0)%360)+360)%360 > 180` με `normalizeAngleDeg(a1-a0) > 180` (ταυτόσημο [0,360)· `normalizeAngleDeg` = `deg%360; if(<0)+=360`). Το «local re-decl σε :189» ήταν stale (δεν υπάρχει). jest `radial-ring-logic.test.ts` 51/51 ✅ (incl. large-arc-flag 90° wedge)· jscpd:diff 0 clones. **ΟΧΙ tsc (N.17).** ⚠️ η `normalize-angle-deg` allowlist entry για radial-ring-logic είναι πλέον vestigial (αβλαβής· ο TIER A owner μπορεί να την αφαιρέσει στο commit). 🔴 Giorgio commit: radial-ring-logic.ts + αυτό το αρχείο.
- ✅ **`clamp01-reimplemented` (13 files): DONE (verified 2026-07-08, Sonnet, grep).** Migrated από την TIER A sweep (γρ.17) → grep και των δύο patterns σε όλο το dxf-viewer (πλην scalar-math/geometry-utils SSoT): `(t<0?0:t>1?1:t)` ternary = **exit 1** · `Math.max(0,Math.min(1,…))` (εξαιρώντας [0,100]/255) = **exit 1**. Μηδέν residual· καλύπτεται από guard `scalar-clamp` (γρ.17). Καμία ενέργεια.
- ✅ `edge-triggered-tool-activation-lifecycle` (**13 files**) — **DONE (2026-07-08, ADR-589).** NEW `useEdgeTriggeredLifecycle` SSoT + 13 migrated + CHECK 3.7 guard. Βλ. G2 status line πάνω. Sites: hooks/tools/{useTrimTool,useExtendTool,useFilletTool,useChamferTool,useOffsetTool,useArrayTool,useArrayPathTool,useArrayPolarTool,useWallAttachTool,useWallSplitTool,useScheduleRegionPickTool,useStretchTool,useWallPickScaffold}.
- ✅ **`polar-point-from-angle-distance` (6 files): DONE (verified 2026-07-08, Sonnet, grep).** Και τα 6 named sites ελέγχθηκαν: 5 consumers (`coordinate-parser`/`length-angle-lock`/`line-keyboard-handler`/`polar-utils`/`constraints-geometry`) κάνουν `import polarPoint`· το 6ο (`radial-ring-logic`) είναι το SSoT home (μόνη cos/sin χρήση = μέσα στο ίδιο το `polarPoint`). Μηδέν inline. Καμία ενέργεια.
- ✅ **`stacked-center-measurement-label` (5 files): DONE (dedup, verified 2026-07-08, Sonnet, grep).** SSoT = `rendering/entities/shared/measurement-label.ts` (η γραμμή έλεγε λάθος όνομα `polygon-measurement-label` = stale). Εξάγει `buildAreaLabel`/`buildAreaPerimeterLabelLines`/`buildCircleAreaCircumferenceLines`/`paintMeasurementText`/`paintStackedMeasurementLabel`. Και τα 5 renderers καταναλώνουν τον SSoT (π.χ. Rectangle→`buildAreaPerimeterLabelLines,paintMeasurementText`· preview→import από measurement-label). **Μηδέν hardcoded «Ε:»/«Περ:»/«Εμβαδόν» σε κώδικα** (exit 1· μόνο σε σχόλια που περιγράφουν το killed literal). ⚠️ Το i18n half (αν το `measurement-label.ts` builder ακόμη hardcode-άρει τα labels) είναι πλέον **single-source** εκεί → ξεχωριστό 1-file i18n cleanup, ΟΧΙ dedup. Καμία ενέργεια στο dedup.
- ✅ **`safestorage-reimpl-bypasses-storage-utils` (3 files): DONE (verified 2026-07-08, Sonnet, grep).** Και στα 3 sites (`layer-state-persistence`/`layer-filter-persistence`/`layer-picker-persistence`): grep `function safeStorage|const safeStorage` = **exit 1** (κανένα local re-decl). Καλύπτεται από guard `safe-storage` (γρ.17· allowlist=storage-utils.ts). Καμία ενέργεια.
- ✅ **`generic-clamp-reimplemented` (2 files): DONE (verified 2026-07-08, Sonnet, grep· committed `e79ee4dc`).** SSoT `clamp` ζει στο neutral `utils/scalar-math` (ADR-071), re-exported από `geometry-utils`. Και τα 2 sites (`member-column-face-snap`:61 · `column-face-snap-helpers`:33) κάνουν `import { clamp } from '.../geometry-utils'`· grep inline `Math.min(Math.max(…` = μηδέν. Καμία ενέργεια.

**🟠 TIER B — Μεγάλα scaffolds ΧΩΡΙΣ SSoT (χρειάζονται factory design + N.8 approval· dedicated pass ο καθένας):**
- ✅ **`bim-entity-firestore-persistence-hook` (21/24 files): DONE (2026-07-08, B2, ADR-594· ΤΟ ΜΕΓΑΛΥΤΕΡΟ single win).** NEW `hooks/data/create-bim-entity-persistence-hook.ts` factory + `bim-entity-persistence-hook-types.ts` config contract (minimal required core + optional overrides + ΕΝΑ `useExtra` escape hatch + lifecycle callbacks onPersisted/onDeleted/onDeleteCleanup/onRestored — big-player «shared primitive + per-instance binding», ΟΧΙ god-config). Migrated **21** thin call-sites (beam/slab/roof/column/foundation/railing/slab-opening/hatch/floor-finish/thermal-space/space-separator/wall-covering/furniture/floorplan-symbol/mep-boiler/mep-radiator/mep-manifold/mep-water-heater/mep-fixture/mep-underfloor/electrical-panel), public API byte-identical (hosts ανέπαφα). **✅ PHASE 2 (Opus 4.8, εντολή Giorgio «force-migrate τα 3»): τα 3 πρώην-excluded ΤΩΡΑ FOLDED (24/24).** wall/opening/mep-segment → thin configs μέσω 5 additive off-by-default core-hooks (`beforeSave` async pre-save / `raceGuardDelete` / `markDeletedOnRequest` / `autoSaveTrigger` event-autosave / `onAfterOptimisticRemoval`) + `primarySelected` στο useExtra ctx + `lastSavedComparable` στο onDeleted. Τα 21 Phase-1 **byte-unaffected**. Params interfaces → `Omit<BimEntityPersistenceParams<T>,…>`, wall-BOQ + factory restore-write extracted → **`jscpd:diff` clean** (Phase-2 fileset). smoke 21→24 → **34 jest GREEN**. ⚠️ **browser-verify pending** (τα 3 δεν έχουν behaviour tests)· minor deviation: wall restore δεν re-acquire soft-lock (advisory TTL). 🔴 Giorgio commit +Phase2: useWall/useOpening/useMepSegmentPersistence + factory + types + smoke test + ADR-594. _(Phase-1 orig — τα 4 special gridGuide/mepSystem/wallMerge/wallSplit μένουν out of scope):_ Reused SSoT: mergeDocsIntoScene/resolveBimPersistenceScope/useBimFirestoreWriteGrace/createPersistSerializer/useBimEntityMoved+RestoredPersistEffect. **Verification: 148 jest GREEN / 18 suites** (9 factory-behavior + 22 import-smoke + zero regression). **No CHECK 3.7 guard** (pattern collides με τα 3 excluded + 4 special hooks → jscpd CHECK 3.28 + review καλύπτουν· consistent με ΟΛΑ τα sweep siblings ADR-585…592). **ΟΧΙ tsc (N.17).** 🔴 Giorgio commit: 21 hooks + factory NEW + types NEW + `mep-connector-merge-config.ts` NEW + 2 tests NEW + ADR-594 NEW + adr-index.md + αυτό το αρχείο. **⚠️ Commit με `SKIP_JSCPD_DIFF=1`** (justified: 39 residual same-commit clones ≤60 tokens = irreducible public-API interfaces + service-adapter shape + useMemo return tail· το 200γρ scaffold clone εξαλείφθηκε). Layer-2 ratchet πέφτει πολύ → **μετά το commit: `npm run jscpd:baseline`.**
- ✅ **`level-manager-like-interface` (45 files): DONE (2026-07-08, B1, ADR-591).** NEW `systems/levels/level-scene-accessor.ts` με capability-scoped interfaces `CurrentLevelRef ⊂ LevelSceneReader ⊂ LevelSceneWriter` (big-player: read/write model, όχι god-type). Και τα 45 hand-written `interface LevelManagerLike` migrated (codemod-1 τα 39 απλά, codemod-2 τα 6 levels-bearing/exported, codemod-3 sweep ~40 unused `SceneModel` + ~29 `SceneWriteOrigin` imports). Type-only, μηδέν runtime change. canvas-click-tool-types κρατά exported `LevelManagerLike` (extends Reader, optional set). **No CHECK 3.7 guard** (shape overlaps legit exported/Pick/extends usage → jscpd 3.28 + review καλύπτουν· βλ. ADR-591 Guard decision). ⚠️ CHECK 3.28 στο commit: `SKIP_JSCPD_DIFF=1` (justified — αναδύει προϋπάρχοντα B2 scaffold clones, όχι δικά του B1· βλ. ADR-591). UNCOMMITTED.
- ✅ **`level-manager-like-interface-bis` (106 files): DONE (2026-07-08, B1-bis, ADR-591 changelog).** Και τα 106 `type LevelManagerLike = Pick<ReturnType<typeof useLevels>, …>` migrated στα accessor types. Codemod τα 102 mechanical (79→`LevelSceneWriter`, 18→`LevelSceneReader`, 4 getter-only DetailHosts→νέο `LevelSceneGetter`, 1 PsetEditorHost→`LevelSceneWriter`) + 4 edge χειροκίνητα (3× levels, 1× saveContext → `interface extends LevelSceneWriter, Pick<…>`, κρατούν `useLevels` import). **Διόρθωση B1 note:** και τα 106 ήταν `import **type** { useLevels }` (μόνο `typeof`, 0 runtime κλήσεις) → το unused `useLevels` type import αφαιρέθηκε στα 102. Νέο minimal `LevelSceneGetter` (ISP)· `LevelSceneReader extends CurrentLevelRef, LevelSceneGetter` (μηδέν αλλαγή για τα 45 B1). Type-only. No CHECK 3.7 guard· CHECK 3.28 → `SKIP_JSCPD_DIFF=1` (ίδιο σκεπτικό B1). UNCOMMITTED.
- ✅ **`store-backed-ribbon-toggle-widget` (13 files): DONE (2026-07-08, B3, ADR-599).** NEW atom `ui/ribbon/components/RibbonInlineToggleButton.tsx` (presentational `<button>` body· props pressed/onClick/ariaLabel/icon/label/colorClass) + shell `RibbonToggleWidget.tsx` (config-driven row+label+atom· exports `RibbonToggleConfig`). Migrated **12** single toggles → ~10γρ. config literal + one-liner (public API byte-identical· RibbonPanel ανέπαφο). **DisciplineVisibility = partial** (No-God-shell): κρατά multi-chip row + secondary/muted ramp, τα chips καταναλώνουν το atom (όχι το shell). Visibility-style (Drain/MepWire) = `value` μοντελοποιεί "visible"· HideBim = "BIM hidden" (constant outer label). 1 behaviour-neutral normalisation: DimRowHandles inactive icon opacity-80→60. **NOT confused με το `RibbonToggleButton`** (ADR-345/547 — command-context/dispatch-driven, διαφορετικό). **Verification: 7 jest GREEN / 2 suites** (atom+shell branch/icon/color/click/visibility-inverted + 13-import smoke). **jscpd CHECK 3.28 (diff) — 0 new clones / 15 files → ΧΩΡΙΣ `SKIP_JSCPD_DIFF`.** **No CHECK 3.7 guard** (markup regex false-positive-άρει στο divergent DisciplineVisibility row → jscpd 3.28 + review καλύπτουν· i18n literal keys μένουν inline per-file για 3.13 reachability). **ΟΧΙ tsc (N.17).** 🔴 Giorgio commit: RibbonInlineToggleButton.tsx NEW + RibbonToggleWidget.tsx NEW + 2 tests NEW + 13 `*Toggle.tsx` + ADR-599 NEW + adr-index.md + αυτό το αρχείο. Sites: ui/ribbon/components/{ShowHeatLoad,ShowPipeSizing,ShowBalancing,ShowUtilization,ShowReinforcement,ShowAnalysisDiagrams,ShowFinishSkin,ColorBySystem,HideBim,DrainPipe,MepWire,DimRowHandles,DisciplineVisibility}Toggle.
- ✅ **`centred-box-grip-adapter-boilerplate` (8 files): DONE (2026-07-08, B5, ADR-602).** NEW `bim/grips/create-centred-box-grip-adapter.ts` factory (`createCentredBoxGripAdapter`) — owns το invariant emit+drag delegation γύρω από το ΥΠΑΡΧΟΝ `centred-box-grips.ts` core (ΟΧΙ νέο grip core). Config inject-άρει `buildCentredBoxKindMaps(prefix)` (template-literal `CentredBoxKind<P>` = `${prefix}-${role}`, αντικαθιστά τα 12 hand-written map entries) + `minDimensionMm` + param bridge + `toGripInfo` (type-safe discriminant-field closure, ΜΗΔΕΝ cast). Furniture+floorplan-symbol field-remap = κοινό `mmSuffixedBoxBridge<T>()` (rotationDeg/widthMm/depthMm) — εξήχθη ΑΦΟΥ το jscpd diff έπιασε το sibling-twin. Migrated **8** (boiler/radiator/waterHeater/electricalPanel/manifold/fixture/furniture/floorplanSymbol)· public API byte-identical (get{X}Grips/apply{X}GripDrag/{X}GripDragInput). Escape-hatch (No-God-shell): fixture (circular diameter) + manifold (▲/▼ outlet grips) COMPOSE τον adapter τοπικά. **`bim/walls/opening-grips.ts` EXCLUDED** (genuinely divergent: outline-derived positions + wall-constrained drag + flip/facing/re-host· δανείζεται μόνο ROLE vocab). 1 cleanup: water-heater έριξε stale legacy `MepWaterHeaterGripInfo` intersection (unused externally). **Verification: 68 jest GREEN / 7 suites** (νέο factory test: kind-map round-trip + emit→discriminant + corner-drag delegate + referential no-op + unknown-kind guard + field-remap round-trip· + 6 pre-existing box/adapter suites ανέπαφα). **jscpd CHECK 3.28 (diff) — 0 new clones / 9 files → ΧΩΡΙΣ `SKIP_JSCPD_DIFF`.** **No CHECK 3.7 guard** (adapter-shape regex false-positive-άρει στο divergent opening-grips → jscpd 3.28 + review, consistent με sweep siblings). **ΟΧΙ tsc (N.17).** 🔴 Giorgio commit: factory NEW + factory test NEW + 8 `{entity}-grips.ts` + ADR-602 NEW + adr-index.md + αυτό. Sites: bim/{mep-boilers,mep-radiators,mep-water-heaters,electrical-panels,mep-manifolds,mep-fixtures,furniture,floorplan-symbols}-grips.
- ✅ **`single-click-placeable-tool-hook` (8 files): DONE (2026-07-08, B4, ADR-600).** NEW `hooks/drawing/create-single-click-placement-tool.ts` factory (`createSingleClickPlacementTool`) — owns το invariant FSM (state triple + refs + activate/deactivate/reset/setParamOverrides + commitFromState + onCanvasClick phase-guard + bim:place-*-3d bridge + getStatusText + getGhostFootprint + is{Active,AwaitingPosition}). Config inject-άρει builders/geometry/status + optional `place3dEvent`· ΕΝΑ `useExtension` escape-hatch κρατά per-tool extra state (assetId/shape) + setters + bespoke bridge publish + extra getters (boiler `getGhostSymbol`). Extra state μένει ΜΕΣΑ στο state → public `.state` byte-identical· `UseXToolResult = CorePlacementResult<...>[& ExtraApi]` (result contract = core type, όχι re-declaration → μηδέν clone). Migrated **και τα 8** (radiator/waterHeater/manifold/boiler/fixture/electricalPanel/furniture/floorplanSymbol)· **κανένα excluded** (floorplan= χωρίς `place3dEvent`, fixture= 11-branch status ως config fn). **Verification: 11 jest GREEN / 3 suites** (6 factory-FSM + 8-import smoke + 4 boiler ghost regression) + **498 GREEN / 48 suites** στο drawing+ribbon-MEP regression. **jscpd CHECK 3.28 (diff) — 0 new clones / 9 files → ΧΩΡΙΣ `SKIP_JSCPD_DIFF`.** **No CHECK 3.7 guard** (FSM shape overlaps legit tool hooks → jscpd 3.28 + review, consistent με sweep siblings). **ΟΧΙ tsc (N.17).** 🔴 Giorgio commit: factory NEW + 8 hooks + 2 tests NEW + ADR-600 NEW + adr-index.md + αυτό. Sites: hooks/drawing/{useMepRadiator,useMepWaterHeater,useMepManifold,useMepBoiler,useMepFixture,useElectricalPanel,useFurniture,useFloorplanSymbol}Tool.
- ✅ **`rebar-2d-plan-path-stroke-loop` (4 files): DONE (2026-07-08, B4, ADR-505 phase-m).** NEW `bim/renderers/draw-rebar-plan-geometry.ts` — `drawRebarPlanGeometry(ctx, geo, pxPerMm, worldToScreen)` ενοποιεί τον verbatim ctx draw loop (save→strokeStyle/fillStyle=REBAR_COLOR_HEX→paths stroke [setLineDash(dashed?[6,4]:[]) · lineWidth=max(0.6,dia·px) · closed?closePath]→dots fill [r=max(0.8,dia/2·px)]→restore) που ήταν αντιγραμμένος στους 4 renderers. Migrated `column-/footing-/slab-/linear-member-rebar-2d.ts` → thin (resolve own geo + null-guard → κοινός loop· public signatures byte-identical → consumers overlays/ghost ανέπαφοι). Behavior-preserving: οι 3 αποκλίσεις κωδικοποιούνται στα δεδομένα του geometry SSoT — `dots:[]` (μόνο κολώνα), `path.dashed` (άνω σχάρα πλάκας), `path.closed:false` (σχάρες) → no-op όπου δεν εφαρμόζονται. `beam-rebar-2d.ts` αμετάβλητο (ήδη thin wrapper). **6 NEW jest GREEN** (`draw-rebar-plan-geometry.test.ts`) · **jscpd CHECK 3.28 (diff) 0 new clones / 5 files → ΧΩΡΙΣ `SKIP_JSCPD_DIFF`.** **No CHECK 3.7 guard** (draw-loop token shape overlaps legit per-renderer ctx sequences → jscpd 3.28 + review καλύπτουν). **ΟΧΙ tsc (N.17).** 🔴 Giorgio commit: draw-rebar-plan-geometry.ts NEW + test NEW + 4 renderers + ADR-505 + αυτό το αρχείο. UNCOMMITTED.
- ✅ **`tool-hint-prompt-sync-effect` (5 files): DONE (2026-07-08, G13, ADR-589 companion).** NEW `hooks/tools/useToolHintPrompt(isActive, key)` SSoT (key=null→clear ενσωματώνει clear-condition· `[isActive,key]` σωστό dep + store field-equals dedupe → user-visible-identical). Migrated Trim/Extend/Fillet/Chamfer/Offset (+ αφαιρέθηκε unused `useEffect` import από Chamfer/Fillet/Offset). 6 jest GREEN. **No CHECK 3.7 guard** (token shape overlaps legit imperative `setOverride(t('tool-hints:...'))` → jscpd 3.28 + review καλύπτουν). UNCOMMITTED.

**🟠 TIER B-bis — Family-type ΤΕΤΡΑΔΑ (Wall/Slab/Roof/Opening):**
- ✅ **`family-type-framework` (16 files): DONE (2026-07-08, B-bis, ADR-604).** 4 shared primitives (μίμηση ADR-594 «shared core + per-instance binding», ΟΧΙ god-config): (1) **Φ1** `hooks/data/create-type-reresolution-hook.ts::createTypeReresolutionHook(reresolveSceneFn)` → 4 `use{X}TypeReresolution` one-liners. (2) **Φ2** `bim/family-types/create-edit-type-dialog-store.ts::createEditTypeDialogStore()` → 4 `edit-{x}-type-store` re-export wrappers (named exports + type aliases kept → consumers ανέπαφοι). (3) **Φ3** `ui/ribbon/hooks/create-family-type-controller.ts::useFamilyTypeController(config)` generic πάνω σε `C extends FamilyTypeCategory` (→ `typeParams=BimTypeParamsByCategory[C]`, μηδέν cast)· config inject-άρει category/isEntity/helpers + **command factories** (`makeAssign/Update/DeleteCommand`) ώστε κάθε entity να κρατά το per-entity command του (**zero behavior change** — δεν force-migrate στο generic `UpdateFamilyTypeCommand`, θα άλλαζε serialized `type` string + `thickness>0` validate)· wall `.kind` + `saveNewType` absorbed στο wrapper· 4 wrappers spread+rename (public API byte-identical). (4) **Φ4** = **2 designs** (ΟΧΙ merge = θα ήταν UX change): `create-family-type-selector-widget.tsx` (Wall/Opening, duplicate-only, `bimFamilyType` i18n) + `FamilyTypeEditorWidget.tsx` (Slab/Roof, edit/delete, per-entity i18n **pre-translated labels → CHECK 3.13 safe**)· κοινό `FamilyTypeSelect.tsx` dropdown + `FamilyTypeWidgetCommon` base interface. **Verification: 7 jest GREEN / 2 suites** (edit-store isolation/open/close/idempotent + reresolution-hook shape). **jscpd — 0 clones / 24 files** (spread pattern + shared FamilyTypeSelect + common base interface εξάλειψαν όλα τα thin-binding echoes) → ΧΩΡΙΣ `SKIP_JSCPD_DIFF`. **No CHECK 3.7 guard** (thin-wrapper shapes overlap legit → jscpd 3.28 + review). **ΟΧΙ tsc (N.17).** ⚠️ browser-verify pending (controllers/widgets χωρίς behaviour tests). 🔴 Giorgio commit: 4 factories NEW + FamilyTypeSelect NEW + 2 tests NEW + 16 wrappers + ADR-604 NEW + adr-index.md + αυτό. - ✅ **Deferred → DONE (2026-07-08, Opus 4.8, ADR-604 §4 follow-up): command dedup.** Migrated `use{Wall,Slab,Roof}FamilyTypeController` `makeUpdate/makeDeleteCommand` onto the generic `UpdateFamilyTypeCommand<TP>` / `createDeleteFamilyTypeCommand(label,…)` (mirror Opening) → **deleted 6 per-entity dupes** (`Update{Wall,Slab,Roof}FamilyTypeCommand.ts` + `Delete{Wall,Slab,Roof}FamilyTypeCommand.ts`). SSoT-audit: serialization-string change safe (in-memory undo only — **κανένα** `CommandRegistry.register` για deserialize/replay)· wall `thickness>0` validate = **dead code** (`CommandHistory.execute` δεν καλεί `validate()`) → dropped, τεκμηριωμένο. 3 tests retargeted στο generic (`UpdateFamilyTypeCommand.test.ts` 7 + `DeleteFamilyTypeCommand.test.ts` 5 + `roof-family-type-commands.test.ts` Assign kept) = **20/20 jest GREEN**· 4 JSDoc refs updated· **jscpd:diff 0 new clones / 8 files.** **ΟΧΙ tsc (N.17).** 🔴 Giorgio commit: 3 controllers + 2 generic commands (comments) + 4 JSDoc consumers + 2 NEW tests + roof test + 6 deletions + 2 old-test deletions + ADR-604 + αυτό. UNCOMMITTED.

**🔵 TIER C — Μικρά/ασφαλή, boy-scout on-touch (≤3 files):**
- ✅ **`project-scoped-localstorage-pubsub-crud-collection` (2 files): ΗΔΗ DONE & COMMITTED (verified 2026-07-08, Sonnet, grep).** Και τα δύο (`services/layer-state-persistence.ts` + `layer-filter-persistence.ts`) είναι ήδη thin adapters πάνω στο SSoT `services/project-scoped-persistence.ts::createProjectScopedStore` (safeStorage/storageKey/listeners Map/subscribe/get/set/hydrate εξαλείφθηκαν). Committed στο `e79ee4dc` (annotation symbol renderer + geometry/persistence SSoT)· 3 αρχεία clean vs HEAD. Καμία ενέργεια — κλείνει με το TIER A safestorage pass (όπως προέβλεψε το handoff «skip αν το πιάνει ήδη»).
- ✅ **`ring-config-member-override-field` (2 files): DONE (2026-07-08, Opus 4.8, TIER C, ADR-513 §override-fields).** NEW generic `createOverrideRingFields<TOverrides>(bridge, preview)` στο `systems/dynamic-input/ring-config.ts` (canonical home) → `currentOverrides`/`setOverride`/`numericOverrideField`. Το idiom «bridge-ή-preview reader + bridge `setParamOverrides` writer + numeric `RingFieldDef` builder» ήταν byte-identical σε `wall-ring-config.ts` (Πάχος/Ύψος) + `beam-ring-config.ts` (Πλάτος/Ύψος) → migrated σε thin `numericOverrideField({key,labelKey,resolveSeedMm})` (public `WALL_RING_CONFIG`/`BEAM_RING_CONFIG` shape αμετάβλητο). **No-God-shell:** line/grip-linear/perpendicular/rotation ring-configs ΔΕΝ έχουν το override idiom (grep-verified) → εξαιρέθηκαν. **8 NEW jest GREEN** (`ring-config-override-fields.test.ts`) + **22/22 regression** (beam-ring-config + ring-config). **jscpd CHECK 3.28 (diff) 0 new clones / 3 files.** **No new registry guard** (jscpd 3.28 + review καλύπτουν· mirror rebar-2d TIER C) · **ΟΧΙ CHECK 6D** (config/builders, όχι canvas-drawing) · **ΟΧΙ tsc (N.17).** 🔴 Giorgio commit: ring-config.ts + wall-ring-config.ts + beam-ring-config.ts + ring-config-override-fields.test.ts NEW + ADR-513 + αυτό το αρχείο. UNCOMMITTED.
- ✅ **`tek-color-to-hex` (2 files): DONE (2026-07-08, Opus 4.8, TIER C).** NEW `io/tek/tek-color.ts` — `tekColorToHex(raw) => `#${colorHex6(raw)}`` (reuse export SSoT `colorHex6` για normalize/uppercase/strip/fallback + import-side `#` prefix για renderer/CSS). Ο wrapper ήταν αντιγραμμένος byte-identical ως local `tekColorToHex` σε `tek-primitive-to-scene.ts` + `tek-structural-to-scene.ts` → migrated σε `import { tekColorToHex } from './tek-color'` (public signatures αμετάβλητα). **«entity-construction idioms» ΕΞΑΙΡΕΘΗΚΑΝ** — real inspection: αποκλίνουν (line/circle/arc/text· `segToLine` structural-only) → No-God-shell, μόνο το color wrapper είναι genuine duplicate. **Guard αντί νέου ADR** (2-file boy-scout, ΟΧΙ canvas-drawing → κανένα CHECK 6D): `.ssot-registry.json` module `tek-color-to-hex` (tier 3· forbid inline `#${colorHex6`· allowlist `io/tek/tek-color.ts`· ERE `#\$\{colorHex6` verified real `grep -E` = μόνο canonical). **4 NEW jest GREEN** (`tek-color.test.ts`) + **10/10 mapper regression** (primitive+structural) · **jscpd CHECK 3.28 (diff) 0 new clones / 3 files.** **ΟΧΙ tsc (N.17).** 🔴 Giorgio commit: tek-color.ts NEW + tek-color.test.ts NEW + 2 mappers + .ssot-registry.json + αυτό το αρχείο. UNCOMMITTED.
- ✅ **`bim-entity-model-passthrough-branch` (1 file / 21 branches): DONE (2026-07-08, Sonnet, TIER C, ADR-587 Φ5 seam).** SSoT audit (grep, code=truth): το «data-driven map + generic branch» **ΑΠΟΡΡΙΦΘΗΚΕ** — το ίδιο το Φ5 seam τεκμηρίωσε (ADR-587 §5.3 + `build-entity-model-coverage.test.ts` γρ.240-242) ότι switch→Record **ΧΑΝΕΙ** τον `default: never` exhaustiveness (strictly stronger από `Object.keys`). Big-player-faithful λύση (Revit/C4D idiom· ΚΡΑΤΑ switch→κρατά `never`): **switch case fall-through consolidation** — 21 near-identical BIM passthrough cases → **2 fall-through blocks** (1 return καθένα). **Variance No-God-shell:** Group A (17· quartet `{kind,params,geometry,validation}`) vs Group B (4· floor-finish/thermal-space/wall-covering/space-separator — **ΧΩΡΙΣ** validation) χωριστά (force-merge = behavior change). Divergent (opening/slab/slab-opening/stair nested· text ADR-557· hatch· xline/ray· annotation-symbol) ανέγγιχτα. **ΟΧΙ νέο ADR-605** → επέκταση **ADR-587** (η αλλαγή είναι μέσα στο Φ5 seam → καλύπτει CHECK 6D). **Μηδέν αλλαγή συμπεριφοράς** (`entity.type`===matched case) + **NEW behavioral pins** (Group A quartet + Group B no-validation). jest: coverage 6/6 + line 4/4 ✅· jscpd:diff 0 clones / 2 files. **ΟΧΙ tsc (N.17).** 🔴 Giorgio commit: dxf-renderer-entity-model.ts + build-entity-model-coverage.test.ts + ADR-587 + αυτό το αρχείο. UNCOMMITTED.

**Raw output (full what/reason ανά finding):** `tasks/w32ej77cd.output` (session scratch· workflow script `dxf-duplicate-final-audit-wf_18c21ad4-a04.js` — resumable για re-scan).

**✅ DONE (2026-07-08, Opus 4.8, ADR-363 §crop-hatch-bim· commit: Giorgio) — Crop clip services region-strategy unification (ΠΛΗΡΗΣ κεντρικοποίηση, εντολή Giorgio «ΠΡΟΧΩΡΑ ΑΜΕΣΑ»):** Οι `ClipToRegionService` ↔ `ClipToPolygonService` είχαν **~10 byte-parallel per-type clippers ο καθένας** (line/circle/arc/polyline/ellipse/rect/text/point/spline/angle-measurement) + διπλά low-level primitives (`sutherland`/`pointInPolygon`/`normAngle`/`arcSweepDeg`/…), διαφέροντας ΜΟΝΟ στο region primitive. **Ενοποιήθηκαν σε big-player region-strategy SSoT:** NEW `services/clip/clip-geometry.ts` (όλα τα low-level primitives μία φορά), `clip/clip-region.ts` (`ClipRegion` interface + `RectClipRegion`[Liang-Barsky/S-H-rect] + `PolygonClipRegion`[parametric/S-H-general]· 4 primitives: `containsPoint`/`clipSegment`/`clipLoop`/`bboxOverlaps`), `clip/clip-entity.ts` (ΟΛΟΙ οι per-type clippers ΜΙΑ φορά, region-agnostic + `clipEntity(entity,region)` + `clipRegionLoop` για overlay), `clip/clip-entity-helpers.ts` (μετακινήθηκε εδώ). **Οι 2 services έγιναν thin wrappers** (~30γρ. έκαστος· public API αμετάβλητο: `clip`/`clipOverlayPolygon`/`clipByPolygon`/`clipOverlayPolygonByLasso`· `ClipRect` re-exported). **Behavior improvements** (unified): circle fully-inside→κρατιέται ΩΣ circle (και στο polygon, πριν γινόταν arc)· angle-measurement arms clip-to-boundary και στο polygon (πριν κρατιόταν whole)· spline→bbox-overlap (πιο σωστό). **Tests: 46/46 GREEN** (νέο parametrized `clip-entity.test.ts` τρέχει κάθε case σε ΚΑΙ ΤΑ ΔΥΟ regions· + 3 service/helper suites). tsc SKIP (N.17). ✅ Google-level: YES — μηδέν διπλότυπο (strategy pattern· κάθε clipper μία φορά), primitives SSoT, public API/undo-redo ανέπαφα. ⚠️ Follow-up (optional, ΟΧΙ blocking): `.ssot-registry` guard «forbid per-type clipper εκτός clip/» (δύσκολο ως ERE — αφέθηκε). 🔴 browser-verify (rect+polygon+lasso crop → hatch/BIM/circle/text σωστά) + commit.

**`maxGripsPerEntity` setting — VESTIGIAL μετά το big-player fix (2026-07-07, Opus 4.8, ADR-559) — small/medium, UNCOMMITTED:** Το ADR-559 §big-player αφαίρεσε το per-object grip truncation (`grip-registry.ts`) → «visible ≡ editable» (Revit/Figma/C4D). Το setting `maxGripsPerEntity` **δεν έχει πλέον λειτουργικό consumer** για truncation, αλλά επιβιώνει σε: UI slider `ui/components/dxf-settings/settings/core/GripSettings.tsx`, το preview στο `ui/components/dxf-settings/settings/shared/LinePreview.tsx` (Max: X grips), + όλο το settings chain (`grip-settings-schema.ts`/`gripSettings.ts`/`GripStyleStore.ts`/`grip-style-sync.ts`/`FACTORY_DEFAULTS`/`settings-core/defaults.ts`/`useUnifiedSpecificSettings.ts`/`ZustandToConsolidatedAdapter.ts`). **Full-SSoT fix:** αφαίρεση του field + του UI slider + του LinePreview cap-visual (big players ΔΕΝ έχουν per-object cap· μόνο object-COUNT `gripObjLimit`). ΓΙΑΤΙ flagged: ~10 files settings chain, ξεχωριστό pass για να μη μείνει half-done. Κράτα το `gripObjLimit` (AutoCAD GRIPOBJLIMIT — legit).

**Grip-source διπλοτυπία render vs interaction — cross-entity (2026-07-07, Opus 4.8, ADR-507/559) — LARGE:** Κάθε renderer `getGrips` (`HatchRenderer`/`PolylineRenderer`/…) ΚΑΙ κάθε `computeDxfEntityGrips` case υπολογίζουν **ανεξάρτητα** το ίδιο grip set από την ίδια geometry → invariant «visible ≡ pickable» που πρέπει να συντηρείται χειροκίνητα (και **απέκλινε** για hatch — βλ. ADR-559 §big-player). Έκλεισα ΜΟΝΟ το **hatch** (NEW `getHatchBoundaryGrips` SSoT, καλείται και από τα δύο). **Full-SSoT fix:** ΕΝΑ grip-source ανά entity type που καταναλώνουν και render + interaction (ή ο visible path να διαβάζει το `computeDxfEntityGrips` output). ΓΙΑΤΙ flagged: κάθε entity type (~25), cross-cutting render+interaction pipelines (ADR-559-level· ADR-559 σκόπιμα τα κράτησε ξεχωριστά με invariant) → dedicated pass, N.8.

**`resolve{Kind}SectionLock` — 4 manual callers, νέο ενοποιημένο SSoT δεν καταναλώνεται ακόμη (2026-07-07, Opus 4.8, ADR-581 Φ6/ADR-503) — SMALL/MEDIUM:** Το Φ6 έφτιαξε **NEW** `bim/structural/sizing/member-section-lock.ts::resolveMemberSectionLock` (ΕΝΑ dispatch column/beam/slab + resolve provider/design-context) και το καταναλώνει η σύριγγα (commit+ghost). Οι **4 υπάρχοντες** manual callers κάνουν ακόμη copy-paste το per-kind pattern (provider+context+resolve{Kind}SectionLock): `hooks/grips/grip-parametric-commits.ts` (column @392 + beam @270), `ui/ribbon/hooks/bridge/useColumnParamsDispatcher.ts` @78, `useBeamParamsDispatcher.ts` @65, `useSlabParamsDispatcher.ts` @60. **Fix:** migrate και τους 4 στο `resolveMemberSectionLock` (κρατώντας το ανά-kind rejection emit που διαφέρει). **ΓΙΑΤΙ deferred:** committed grip/ribbon commit paths + shared tree· το rejection-event ανά kind χρειάζεται προσεκτικό threading → dedicated pass. Επίσης: section-rejected **toast** στη σύριγγα (ο bump εφαρμόζεται ήδη, λείπει το μήνυμα).

**`type → compute{Kind}Geometry` — 3 αποκλίνοντα recompute sites (2026-07-07, Opus 4.8, ADR-581 Φ6) — MEDIUM:** Ο τύπος «parametric `params` → `geometry`» ζει σε **3** ανεξάρτητα switch: (a) κάθε `Update{Kind}ParamsCommand.applyPatch` (commit path — column/wall/slab/opening/beam/stair), (b) `rendering/ghost/apply-parametric-box-preview.ts` (grip-drag live preview — column/foundation/beam/mep-fixture/electrical-panel/mep-manifold/mep-segment/furniture), (c) **NEW** `systems/match-properties/match-preview-entity.ts::recomputeParametricGeometry` (σύριγγα ghost — column/beam/slab/stair/wall). Το invariant «ghost ≡ commit» συντηρείται χειροκίνητα (ίδια `compute*Geometry` calls). **Full-SSoT fix:** ΕΝΑ `recomputeEntityGeometry(entity, nextParams) → geometry` που καταναλώνουν και τα 3 (context-aware: opening host-wall, slab openings/beam-deduction). **ΓΙΑΤΙ deferred (N.8):** διαφορετικά inputs/context ανά site (grip-drag: grip-kind+delta+ortho· command: bare params+validation+emit· ghost: bare params style-only fallback), cross-cutting σε commands+ghost+match → dedicated pass. Το Φ6 ήδη κρατά ghost ≡ commit με test.

**✅ DONE (2026-07-06, Opus 4.8, ADR-362 Round 38, shared tree· commit: Giorgio) — `drawArrowheads` unify (main ↔ preview renderer):** Το mirror-duplicated block-pair resolution + per-side gate + `renderArrowhead`-loop ενοποιήθηκε σε **NEW** `renderDimArrowheadPair(ctx, {...})` στο ΥΠΑΡΧΟΝ SSoT `dim-arrowhead-renderer.ts` (resolve+gate+2×renderArrowhead· μοναδικός κάτοχος του visibility gate) + tiny `resolveArrowBlockNames(style)` στο `dim-arrowhead-blocks.ts` (μοναδικός κάτοχος `dimblk{1,2}||dimblk`, reused από **4** consumers: main/preview renderers + `dim-block-primitives` + `useRibbonDimBridge`). Οι 2 `drawArrowheads` έγιναν thin (~13γρ. έκαστη)· διαφορές (unitPx/color/ctx/anchors)=inputs. `dim-block-primitives` reuse names μόνο (κρατά `pushArrowhead`, δεν αλλάζει export suppress-συμπεριφορά — arrow visibility δεν κάνει DXF roundtrip). Ακολουθήθηκε precedent `resolveDimStroke` (ADR-562 Φ2). **Tests:** 17 νέα (`dim-arrowhead-pair.test.ts`) + regression 50/51 (το 1 fail=pre-existing `getGrips` ADR-561, ΑΣΧΕΤΟ). ΟΧΙ tsc (N.17). Commit: Giorgio (5 MOD + 1 NEW test + ADR-362 §7).

**✅ DONE (2026-07-07, Opus 4.8, ADR-510 Φ2, shared tree· commit: Giorgio) — item B: linetype name→def SSoT convergence (latent bug fixed):** NEW def-level SSoT **`resolveLinetypeDef(name) = resolveAnyLinetype ?? resolveLinetype`** στο ΥΠΑΡΧΟΝ `rendering/linetype-dash-resolver.ts` (μόνο αρχείο που ήδη importάρει και τους δύο, cycle-safe)· `resolveLinetypePatternMm` → thin derivation· catalog∪registry ένωση σε **ΕΝΑ** σημείο (catalog wins). **Migrated ΟΛΟΙ οι path-(b) consumers:** `resolve-entity-style.resolveLinetypeName` (THE latent bug — DotX2/Dashed2 έβγαινε solid· τώρα resolve), `LayerOperationsService.validateLinetype` (layer με variant δεν rejectάρεται πια), + Boy-Scout `settings-core/defaults.getDashArray` (preview customs). `bim-dash-resolver` = BIM keys (fixed enum) → σωστά catalog-only, ΔΕΝ άλλαξε. **Tests:** +4 (`linetype-dash-resolver.test.ts`: DotX2 regression/custom/collision/null) +1 (`resolve-entity-style.test.ts`: entity DotX2 cascade)· 81 linetype-suite + 12 downstream + 39 LayerOps GREEN. ΟΧΙ tsc (N.17). Commit: Giorgio (4 MOD: linetype-dash-resolver + resolve-entity-style + LayerOperationsService + settings-core/defaults + 2 tests + ADR-510 changelog + αυτό το αρχείο). _(Ιστορικό flag παρακάτω:)_ **Linetype name→pattern resolution — 3 αποκλίνοντα paths, κανένα πλήρες (2026-07-06, Opus 4.8, ADR-362/510) — pre-existing fragmentation, medium/ευαίσθητο:** Το «linetype ΟΝΟΜΑ → mm pattern» resolve-άρεται με **3 ασύμβατους** τρόπους: (a) `resolveAnyDashMm`/`resolveAnyLinetype` (config/linetype-aliases — catalog: 8 base + variants + legacy + BIM + CI· **ΧΑΝΕΙ** user-created customs) → `bim-dash-resolver`, `settings-core/defaults.ts:225`· (b) `resolveLinetype(name)?.pattern` (LinetypeRegistry — 8 base + customs· **ΧΑΝΕΙ** τα catalog density variants Dashed2/DotX2… γιατί το registry seed-άρει ΜΟΝΟ τα 8 ISO baseline) → `resolve-entity-style.ts resolveLinetypeName` (κύριο entity cascade!), `LayerOperationsService`· (c) **ΝΕΟ πλήρες** `resolveLinetypePatternMm` (rendering/linetype-dash-resolver — catalog ∪ registry) → dim-stroke-resolver + linetype-thumbnail + (τώρα) dxf-renderer-style-resolve fallback. **Συνέπεια:** entity με linetype `DotX2` στο κύριο cascade → registry miss → default Continuous → **ζωγραφίζεται solid** (latent bug). **Fix:** def-level combined `resolveLinetypeDef(name) = resolveAnyLinetype ?? resolveLinetype` στο ΕΝΑ SSoT, να το χρησιμοποιεί το `resolve-entity-style.resolveLinetypeName` (κλείνει το variants-gap) + `settings/defaults` + `bim-dash-resolver` (αν χρειαστεί customs). **ΓΙΑΤΙ flagged όχι fixed τώρα (N.0.2/N.8):** το `resolve-entity-style` είναι κρίσιμο committed rendering cascade με tests + shared tree· η αλλαγή resolution αλλάζει συμπεριφορά (variants ξαφνικά dashed) → dedicated pass + test update. Έκλεισα ήδη ΜΟΝΟ το ασφαλές `dxf-renderer-style-resolve` fallback (ίδια catalog σημασιολογία + προσθήκη customs).

**localStorage persist/hydrate boilerplate — hand-rolled σε 8+ stores, κανένα SSoT (2026-07-06, Opus 4.8) — pre-existing duplication, LARGE:** Το μοτίβο `getItem`+parse+SSR-guard / `setItem`+try/catch / remove-on-default είναι copy-pasted: `LinetypeScaleStore`, `LinetypeRegistry` (νέο, ADR-362 — user-created persistence), `QuickStyleStore`, `bim-3d/performance/{auto-submit,baseline-tracker,PerformanceHistoryStore,PerformanceHUDStore,regression-detector}`, `bim-3d/properties/last-active-tab-tracker`, … (10+). **Big-player fix (Zustand `persist` middleware / VS Code Memento):** ΕΝΑ `createPersistedValue<T>(key, {serialize,deserialize,default})` ή `persist()` wrapper πάνω στο ΥΠΑΡΧΟΝ `createExternalStore` (lib/state) — hydrate-on-init + save-on-set + SSR/quota guard μία φορά. **ΓΙΑΤΙ flagged όχι fixed (N.0.2):** 10+ committed files cross-domain (2D + 3D perf + BIM), εκτός scope· δουλεύει με το createExternalStore-promote WAVE (lib/state) → dedicated pass. _(Το δικό μου `LinetypeRegistry` persistence ακολούθησε το ΥΠΑΡΧΟΝ `LinetypeScaleStore` μοτίβο — δεν πρόσθεσε νέο είδος διπλότυπου· θα migrate-άρει μαζί με τα υπόλοιπα όταν φτιαχτεί το persist SSoT.)_

**✅ DONE (2026-07-06, Opus 4.8, ADR-561/397/577· commit: Giorgio) — 3-way rigid-translate SSoT convergence (ολοκληρώθηκε, ΟΧΙ πια deferred):** Στο move/copy ενωμένης lwpolyline βρέθηκαν **3 αποκλίνουσες rigid-translate υλοποιήσεις**, καμία με lwpolyline. **Ενοποιήθηκαν και οι 3** στην canonical `calculateMovedGeometry`: (1) canonical native `lwpolyline`+`mtext`+`block`+`rect`+`angle-measurement`· (2) `translateEntityByAnchor` (stretch) → delegate· (3) `applyClassicEntityPreview` movesEntity switch → delegate (αφαιρέθηκε ~90γρ. inline BIM + 28 imports νεκρού/αποκλίνοντος κώδικα· το `calculateMovedGeometry` ήδη δρομολογούσε BIM/hatch στο κοινό `calculateBimMovedGeometry`). **Bonus:** διορθώθηκε preview≠commit bug (inline `opening` μετέφραζε outline ενώ commit=host-derived `{}`). Η αρχική αναβολή (perf-path/BIM-equivalence/shared-tree) **ακυρώθηκε** αφού ο Giorgio επιβεβαίωσε «κανείς άλλος agent δεν αγγίζει τον κώδικά μου» + επαληθεύτηκε ότι η BIM διαδρομή ήδη περνούσε από το ίδιο SSoT. **648 GREEN** στα σχετικά dirs (incl. BIM ghost column/foundation/alt-move + copy). ΟΧΙ tsc (N.17). Commit: Giorgio.

**✅ DONE (2026-07-06, Opus 4.8, ADR-577, shared tree· commit: Giorgio) — `getSceneManager` adapter-builder SSoT (+ διπλότυπος `LevelManagerLike` τύπος):** Το memoized `const getSceneManager = useCallback(() => { if(!levelManager.currentLevelId) return null; return createLevelSceneManagerAdapter(getLevelScene,setLevelScene,currentLevelId); }, [levelManager])` ήταν copy-pasted **byte-identical** (handoff έλεγε ~10· grep = **20**: 17 memoized-idiom + 3 inline-adapter). **NEW SSoT** `systems/entity-creation/useSceneManagerAdapter.ts` (`useSceneManagerAdapter(levelManager): () => ISceneManager|null`, delegate στο ADR-527 factory, ίδιο null-guard + `[levelManager]` dep = byte-identical) + exported `SceneAdapterLevelManager` + barrel export. **20 files migrated** (copy/move/mirror/rotation/scale/trim/stretch/offset/fillet/chamfer/extend/array + entity-clipboard + wall-split/merge/attach/gap-opening + array-polar/path + body-drag-commit)· οι 16 «Pick» consumers → `SceneAdapterLevelManager` (σβήστηκε ο διπλότυπος `Pick<ReturnType<typeof useLevels>,3>` + το `import type { useLevels }`)· `useWallGapOpeningTool` κρατά `LevelsHookReturn` (full, για `buildOpeningResolvers`). **GUARD** tier-3 module `scene-manager-adapter-hook` (ERE `const getSceneManager = useCallback\(`· allowlist=SSoT hook doc-comment)· **dxf 0-violation** grep-verified· golden fixture. **Tests:** νέο `useSceneManagerAdapter.test.ts` 6 GREEN + `test:ssot-suite` **233 GREEN** (golden 66) + 39 regression (copy/move/scale activation). ΟΧΙ tsc (N.17). **🔴 Giorgio: `npm run ssot:baseline` (register 0-violation module) + commit** (νέος hook + test + barrel + 20 consumers + `.ssot-registry.json` + golden fixture + ADR-577 + αυτό το αρχείο). **⏸️ ΑΝΑΒΛΗΘΗΚΕ (ίδιο pass):** ο FSM-refs guard «forbid inline `wasActiveRef`+`prevEntityCountRef`» — grep έδειξε **13 tool files** με τα refs (όχι μόνο stretch/array/wall· +extend/trim/offset/chamfer/fillet/region-pick), δηλ. πολλά un-migrated tools· guard τώρα = allowlist 13+ = θόρυβος. Ξεκίνα ΜΕΤΑ τη migration των υπόλοιπων 2-click tools στο `useModifyToolActivation` (τότε allowlist συρρικνώνεται). **📊 ssot:discover Phase 3 broken σε Windows (/tmp race)· grep-verified priority-list large duplicates ΠΑΡΑΜΕΝΟΥΝ (χρειάζονται Giorgio approval, N.8):** translatePoint 48–84 sites/38 files + 5 helpers (LARGE)· wall-pick-scaffold 3× (medium)· ddeBufferRef (medium)· grip-defaults VALUES (θέλει απόφαση τιμών).**

**✅ WAVE 3 DONE — ΚΛΕΙΣΙΜΟ ΟΛΟΚΛΗΡΗΣ ΤΗΣ store-SSoT ΣΕΙΡΑΣ (2026-07-05 Session 9, Opus 4.8 orchestrator, shared tree· commit: commit-coordinator/Giorgio) — CROSS-SUBAPP promote + migrate:** Το hand-rolled `new Set<...>` pub/sub duplication υπήρχε ΚΑΙ σε 6 non-dxf files, αλλά το SSoT factory ζούσε ΜΟΝΟ μέσα στο `dxf-viewer/stores/` (cross-subapp import = παραβίαση ορίων). **ΒΗΜΑ 1 — PROMOTE (big-player canonical, Zustand/Redux/Jotai core-level):** το `createExternalStore` μετακινήθηκε ΦΥΣΙΚΑ σε shared platform lib **`src/lib/state/createExternalStore.ts`** (+ test)· το παλιό `dxf-viewer/stores/createExternalStore.ts` έγινε **thin re-export shim** (`export * from '@/lib/state/createExternalStore'`) → ΟΛΑ τα ~126 dxf import sites ΑΜΕΤΑΒΛΗΤΑ (relative imports). `.ssot-registry` module `create-external-store`: `ssotFile`→νέο path, allowlist += νέο path + shim path. Τα 3 siblings (confirm/toggle/toolBridge) έμειναν στο dxf (importάρουν via shim). **ΒΗΜΑ 2 — MIGRATE 5 fits** στο ΥΠΑΡΧΟΝ (promoted) factory (public API αμετάβλητο): `useFirestoreBuildings` (Firestore single-state, refCount lifecycle wrapper, no-equals — equality-guard ζει στο firestoreQueryService/ADR-361) + `super-admin-active-company` (`equals:Object.is` — το `if(id===activeCompanyId)return`, drop try/catch) + `addressEditorMachine` (FSM per-instance, no-equals + manual `next!==prev` guard στο send, payload subscribe wrapper, `reset()`=public runtime→`store.set` force-notify) + `GeoCanvasConfig` (**version-signal** `createExternalStore<number>(0)`, `this.config` μένει SSoT, payload subscribe wrapper — mirror CanvasSettings WAVE 2.8, drop try/catch) + `useFloorsByBuilding` (per-key Map multiplexer· ΚΑΘΕ entry→single-snapshot store + `subscriberCount` side-channel για ref-counted Firestore teardown — precedent ModalPresenceStore WAVE 2.7· payload wrapper + immediate-emit-on-subscribe κρατήθηκαν· ΠΡΟΣΟΧΗ b815 SDK assertion — refcount semantics αμετάβλητα). **1 NOT-A-FIT (allowlisted):** `CanvasRegistry` (2-arg `(event, data)=>void` event bus, μηδέν readable state — buses=bespoke, precedent clash-focus-bus/aria-live-bus/regression-alert-bus). **GUARD:** module allowlist += CanvasRegistry· **0-violation ΟΛΟ ΤΟ REPO** (όχι μόνο dxf) επιβεβαιωμένο προγραμματικά (κάθε forbiddenPattern match ∈ allowlist ή test-exempt). `test:ssot-suite` **221/221 GREEN** (golden 56)· promoted factory 9 jest + addressEditorMachine/useFirestoreBuildings-dedup **30 jest GREEN**· shim resolve-verified via createToolBridgeStore 9 GREEN. **🏁 Η store-SSoT σειρά (WAVE 1→2.8→3) ΕΚΛΕΙΣΕ ΤΕΛΕΙΩΣ** — ΕΝΑ shared `createExternalStore` SSoT, 0-violation repo-wide. Μένουν ΜΟΝΟ 2 non-Set one-offs (OBSERVATIONS, low-pri, ΟΧΙ Set-idiom): `dxf-viewer/services/ServiceHealthMonitor.ts` (`Array<(report)=>void>`) + `dxf-viewer/systems/collaboration/CollaborationEngine.ts` (`extends EventEmitter`) — διαφορετικός μηχανισμός, big-player bespoke, ΑΦΗΝΟΝΤΑΙ. _(Ο commit-coordinator ήδη έκανε commit ΒΗΜΑ 1 + 2 Firestore stores: `2898e9d5`/`f94d6d21`/`a9cb7ebe`/`ee52a986`· εκκρεμεί commit των `.ssot-registry.json` + 3 migrations (useFloorsByBuilding/addressEditorMachine/GeoCanvasConfig) + `npm run ssot:baseline`.)_ **🔴 Giorgio: `npm run ssot:baseline` (register 0-violation repo-wide) + commit των εναπομείναντων.**

**✅ SESSION 4 DONE (2026-07-05, Opus 4.8) — WAVE 2.5 `createExternalStore` extra single-state stores + follow-up guard (shared tree με άλλον agent· `git add <specific>`· commit: Giorgio):** grep-audit `new Set<\(\) => void>` σε όλο το dxf-viewer (38 files) → κατηγοριοποίηση single-state/reducer/hot-path/not-a-fit. **22 stores migrated** στο ΥΠΑΡΧΟΝ `stores/createExternalStore.ts` (public API αμετάβλητο ανά store): **Group A** (10) edit-{opening,wall,roof,slab}-type / AdminLayerManagerDialog / stair-status (κράτησε `useStairStatusKey()`) / SelectionCycling / **DynamicInputLock** (2 πηγαία fields→ένα derived snapshot· 11 jest GREEN) / CommandLine / CommandHistory (+localStorage side-effect)· **Group B** cursor-safe (2) ToolCursor / CrosshairSuppression· **Group C** tool FSM (8, reducer-over-single-object via `_patch`) Chamfer/Fillet/Offset/Trim/Stretch/Scale/Extend/Array (Trim+Extend κράτησαν τα 4 pick-fn registries ως plain `let`· **offset-command-store 29 jest GREEN**)· **Group D** ADR-040 hot-path (2) Lasso/cursor-Selection — big-player call (Giorgio): το `createExternalStore` ΕΙΝΑΙ το vanilla-store primitive (Zustand/Redux/Valtio) που χρησιμοποιούν οι μεγάλοι για high-freq· notify **byte-identical** → μηδέν perf change· migrated **ΧΩΡΙΣ `equals`** (always-notify), guards (min-dist/pointsEqual) κρατήθηκαν. **NOT-A-FIT (bespoke, allowlisted):** ImmediateSnap/ImmediateTransform (multi-cell hot-path 6B/ADR-040), envelope-spec (Map cache), opening-tag-style-service/material-thumbnail-resolver (async), RibbonFieldStore (per-key slice). **Follow-up guard:** `.ssot-registry` module `create-external-store` (tier 3· forbid inline `new Set<\(\) => void>`· ERE `new Set<\\(\\) => void>`· allowlist = 3 factories + 6 not-a-fit· tests exempt via exemptPatterns → **0-violation**· `test:registry-golden` 56 GREEN). CHECK 6D → ADR-040 changelog staged (2026-07-05 WAVE 2.5 entry). **createExternalStore + offset-command-store + length-angle-lock: 29+11 jest GREEN.** 🔴 Giorgio: `npm run ssot:baseline` (register 0-violation module) + commit ΟΛΩΝ (22 stores + `.ssot-registry.json` + ADR-040). **WAVE 2.5 = ΟΛΟΚΛΗΡΩΘΗΚΕ** — δεν μένει store προς migration (tool stores incl.).

**✅ SESSION 3 DONE (2026-07-05, Opus 4.8) — Store-factory family 2C+2D (clean tree, solo· commit: Giorgio):** **2C** `createToolBridgeStore<THandle>()` — **NEW SSoT** `stores/createToolBridgeStore.ts` (sibling του `createExternalStore`, delegate-άρει σε αυτό: `createExternalStore<THandle|null>(null,{equals:Object.is})` + bundled React `use()` via `useSyncExternalStore(subscribe,get,()=>null)`). **grep-audit: undercount 15→16** — εκτός των 15 στο `ui/ribbon/hooks/bridge/` βρέθηκε το `bim/beams/beam-tool-bridge-store.ts` (degenerate `{set,get}`, single-reader). **ΟΛΑ τα 16 migrated** → one-liner (public API = superset και των 3 legacy shapes: `{set,get,use}`×14, `{set,get,subscribe}`×1 slab, `{set,get}`×1 beam → μηδέν consumer αλλαγή). **`stair-status-store` (πηγή του pattern, `string` state) κρατήθηκε ΕΞΩ** (big-player: άλλο domain=status text → WAVE 2.5 `createExternalStore`, όχι «tool bridge» misname). **2D** registry guard module `create-tool-bridge-store` (tier 3· forbid `let handle: \w+ToolBridgeHandle`· allowlist=factory· **0-violation** direct-grep confirmed· `test:registry-golden` 56 GREEN). **Verification:** factory 9 jest + 19 bridge suites/155 GREEN (incl. wall-draw-mode/column-draw-kind/slab-slope-param) + BeamFromWallGhost 8 GREEN. ADR-404 changelog updated. 🔴 ΕΚΚΡΕΜΕΙ: **WAVE 2.5** (extra single-state stores + tool stores, incl. πλέον **stair-status-store**) + follow-up 0-violation `new Set<() => void>` guard (μετά WAVE 2.5).

**✅ SESSION 2 DONE (2026-07-05, Opus 4.8) — Store-factory family 2A+2B (clean tree, solo· commit: Giorgio):** **2A** `createExternalStore` WAVE 2 (9 stores) + WAVE 3 (3 stores) migrated· factory extended με additive `reset(next)` (silent set + clearListeners) +2 jest. **2B** `createConfirmStore` — undercount 2→**7** confirm stores, ΟΛΑ migrated + registry guard `create-confirm-store` (0-violation, registry-golden 56 GREEN). **~184 targeted jest GREEN** (2 pre-existing failures στο `BimSceneLayer-multifloor` = `wallToMesh` converter signature drift, ΑΣΧΕΤΟ — επιβεβαιώθηκε με stash). **🔴 ΕΚΚΡΕΜΕΙ (Session 3):** **2C** `createToolBridgeStore` (νέο factory + 15 bridge stores, orchestrator-scale) + **2D** bridge registry guard· **WAVE 2.5** extra single-state stores. Λεπτομέρειες παρακάτω.

**✅ SESSION 1 DONE (2026-07-05, Opus 4.8) — A-items batch + registry guards (clean tree, solo agent· commit: Giorgio):** A1 `inheritEntityStyle` fillet→SSoT (`fillet-curve-geometry.ts` σβήστηκε local + `ARC_STYLE_SKIP`)· A2 `findPolylineSegment`→`nearestPolylineSegment` (useCircleTTT+useLinePerpendicular· +πιο σωστό: segment-clamped distance)· **A3** NEW SSoT `systems/grip/grip-kind-index.ts` `parseGripKindIndex` — migrated **7 files/13 sites** (slab/roof/floor-finish/slab-opening/mep-underfloor/column/wall· ⚠️ backlog έλεγε 3 → undercount· wall κράτησε `idx>=1`· column έφυγε το `prefix` var) + `parsePolylineSegIndex` → delegate· **A4** `hexToRgb`: το parse-SSoT ήταν **ήδη** `config/color-math.parseHex` (ADR-573)· migrated μόνο το `TextSettings.tsx` inline `parseInt(slice)` → `parseHex` (detail-pdf-renderer ήδη delegate-άρει, δεν το άγγιξα)· A5 `getDefaultLayerId()` unify (2 drawing sites· έκλεισε latent `string|undefined`→`string` type-gap). **Registry:** +3 guard modules `ortho-delta` / `inherit-entity-style` / `grip-kind-index` (0-violation, allowlist SSoT files· registry-golden 56 GREEN). **263 targeted jest GREEN.**

**✅ DONE (2026-07-06, Opus 4.8, ADR-397/513, shared tree· commit: Giorgio) — Comma-decimal normalization κεντρικοποιήθηκε στο ΥΠΑΡΧΟΝ SSoT `normalizeNumber`:** Το `systems/dynamic-input/utils/number.ts → normalizeNumber` **επεκτάθηκε σε `/,/g`** (superset: single-comma sites αμετάβλητα· expression sites όπως RadialCommandRing `evalExpr("1,5+2,5")` πλέον καλυμμένα από ΕΝΑΝ canonical normalizer). **12 inline sites / 7 files migrated** → `import { normalizeNumber }`: `ui/toolbar/shared/input-validation.ts` (normalizeNumericInput), `ui/ribbon/components/buttons/ribbon-combobox-numeric.ts` (parseOptionNumber), `systems/prompt-dialog/PromptDialog.tsx` (normalizeValue), `systems/grip/grip-menu-actions.ts` (parseFiniteFloat), `RadialCommandRing.tsx` (evalExpr path), `DynamicInputField.tsx` (×2 angle+coord), `DynamicInputFields.tsx` (×5 x/y/rise/tread/width). **GUARD:** `.ssot-registry` tier-3 module `comma-normalize` (forbid inline `.replace(',', '.')` / `.replace(/,/g, '.')`, single & double quote· ERE `\.replace\(['"],['"], ?['"]\.['"]\)` + `\.replace\(/,/g, ?['"]\.['"]\)`)· allowlist = **2 SSoT domains**: dxf `number.ts` + app `src/lib/number/greek-decimal.ts` (`parseGreekDecimal` — currency/thousands, `number|null` contract, ΔΙΑΦΟΡΕΤΙΚΟ domain, ΔΕΝ συγχωνεύεται). **dxf-viewer = 0-violation** (grep-verified: εναπομείναντα hits μόνο σε tests/docs = exempt + number.ts allowlisted). Golden fixture (shouldMatch/shouldSkip) προστέθηκε. **number.test.ts (νέο) + registry-golden + 25 affected suites = 306 jest GREEN.** _(Per-keystroke `applyTypedAngleKey` = single-char inline buffer, ΟΧΙ full-string — σκόπιμα ΕΞΩ, documented στο number.ts docstring.)_ **🔴 Giorgio: `npm run ssot:baseline` (register module — παγώνει 4 non-dxf app-consumers / 6 violations, βλ. επόμενη γραμμή) + commit (number.ts + 7 migrated + `.ssot-registry.json` + number.test.ts + golden fixture + αυτό το αρχείο + Validation_logic.md).**

**✅ DONE (2026-07-06, Opus 4.8, ADR-576, shared tree· commit: Giorgio) — App-domain currency/decimal parsing κεντρικοποιήθηκε σε locale-aware building blocks:** NEW `src/lib/number/locale-number.ts` — `normalizeDecimalString` (string→canonical machine string) + `parseLocaleNumber` (→number|null, strips currency/garbage). Σημασιολογία (Giorgio): ΕΝΑ separator = υποδιαστολή· και-τα-δύο → last = decimal· repeated = thousands· fixed-locale bank mode via `decimalSeparator` opt. `parseGreekDecimal` → **thin wrapper**. **4 consumers migrated** (behavior-preserving εκτός property): accounting `csv-import-service.ts` (parseBankDecimal fixed / parseAutoAmount auto — multi-locale διατηρήθηκε, parity fixtures) + procurement `quote-search.ts` (`?? NaN`) + building-code `BuildingCodeForm.tsx` (`?? 0`) + property `PropertyCommercialPriceFields.tsx` (**format-on-blur** + extracted `PriceInputField` — «125500.50»/«125500,50»→125.500,50, χωρίς καταστροφή υπαρχουσών τιμών). **GUARD:** module `comma-normalize` allowlist += `locale-number.ts` + description update· τα 6 inline sites → 0 (grep-verified: εναπομείναντα hits μόνο σε dxf `number.ts` allowlisted + tests exempt). **23/23 jest GREEN** (`src/lib/number/__tests__/locale-number.test.ts`). 🔴 Giorgio: `npm run ssot:baseline` (register 0-violation για τους 4 consumers) + commit (locale-number.ts + greek-decimal.ts + 4 consumers + `.ssot-registry.json` + locale-number.test.ts + ADR-576 + adr-index + αυτό το αρχείο + Validation_logic.md).

**2-click modify-tool activation FSM — SSoT + 5/5 migrated ΟΛΟΚΛΗΡΩΘΗΚΕ (2026-07-06, Opus 4.8, ADR-577· Giorgio decision «big-player consistent framework»):** **✅ NEW SSoT** `systems/tools/useModifyToolActivation.ts` (storage-agnostic· `onActivate` override· 8 unit tests). **✅ ΟΛΑ ΤΑ 5 migrated test-guarded (+νέα characterization tests, ήταν untested):** `useCopyTool` (23), `useMoveTool` (+5), `useMirrorTool` (+5, onActivate handoff), `useRotationTool` (+5, onActivate 2-sub-case handoff), `useScaleTool` (+5, store-based, onDeactivate=reset). **51 tests GREEN.** Behavior alignment: Rotation (dead lost-branch) + Mirror/Scale (missing) → consistent (deselect→entity-pick, Revit/C4D/Figma-style ΕΝΑ FSM). **ΕΚΤΟΣ (τεκμηριωμένα):** `useStretchTool` (partial), Array×3/Wall×4 (pick-based). **🔴 ΕΚΚΡΕΜΕΙ (final):** registry guard «forbid inline `wasActiveRef`+`prevEntityCountRef` modify-FSM στα hooks/tools» (allowlist stretch/array/wall) + `ssot:baseline`. Λεπτομέρειες: ADR-577 §«FSM centralization».**

**✅ DONE (2026-07-06, Opus 4.8, ADR-090 REACTIVATED & ENFORCED, shared tree· commit: Giorgio) — Point+delta translate SSoT consolidation ΟΛΟΚΛΗΡΩΘΗΚΕ:** Big-player-verified (three.js/glMatrix: ΕΝΑ `add` ανά διάσταση). NEW canonical στο `geometry-vector-utils.ts`: `translatePoint`/`translatePoints` (2D z-preserving), `translatePoint3D`/`translatePoints3D` (Firestore-safe 3D z:=z??0), `addPoint3D` (3D z-sum)· `addPoints`→delegate. **~150 sites migrated / ~90 files** (2 orchestrator rounds, 13 agents)· **13 duplicate helpers deleted** (`applyDelta`×2, `shiftPoint3D`×2, `shiftPolygon3D`, `translateVertex`×5, private `translatePoint`/`offsetPoint`/`translate3D`)· grep-verified 4 dead names = 0 repo-wide. **GUARD** tier-3 module `point-translate-helpers` (zero-tolerance `function applyDelta|shiftPoint3D|shiftPolygon3D|translateVertex`) + golden fixture· `test:registry-golden` **68 GREEN**. ~4 z-coalesce sites (z από άλλη πηγή) grandfathered στο baseline. 🔴 Giorgio: `npm run ssot:baseline` (done 129→131 από shared tree, module=0 impact) + commit (canonical + ~90 migrated + `.ssot-registry.json` + golden fixture + ADR-090 + αυτό το αρχείο). _(Ιστορικό context παρακάτω:)_ **[πρώην PENDING]:** Το πιο θεμελιώδες geometry op είναι διάσπαρτο: `grep '{x:*.x+delta.x, y:*.y+delta.y}'` → **84 occurrences / 38 files**. Παράλληλα υπάρχουν **≥5 ανταγωνιστικοί "κεντρικοί" helpers που είναι διπλότυπα μεταξύ τους**: `geometry-vector-utils.ts addPoints` (ADR-090 "Centralized Point Vector Operations", Point2D, χάνει z), `stretch-entity-transform.ts translatePoint` (ίδιο), `move-entity-geometry.ts applyDelta` (ίδιο), `bim-move-geometry.ts shiftPoint3D` (private, z-aware), `apply-entity-preview-helpers.ts offsetPoint` (private). Το ADR-090 δήλωσε SSoT αλλά ΔΕΝ επιβλήθηκε → 4 re-implementations + 84 inline. **Big-player fix (three.js Vector2.add / Figma / CAD kernels):** ΕΝΑ z-aware `translatePoint(p, delta)` + `translatePoints(pts, delta)` SSoT (promote το z-aware `shiftPoint3D` ως canonical, τα υπόλοιπα → thin re-export ή delete), + ratchet guard «forbid inline `.x + *.x` translate». **ΓΙΑΤΙ flagged όχι fixed (N.0.2/N.8):** 38 committed files cross-domain, χωρίς tsc = υψηλό ρίσκο· dedicated orchestrator pass + Giorgio approval. _(Το ΤΩΡΙΝΟ hatch-move diff ΔΕΝ πρόσθεσε νέο inline — reuse `shiftPoint3D`.)_ **Template:** ADR-090 (canonical + derivations + guard).**

**✅ DONE (2026-07-07, Opus 4.8, ADR-357/397/513, shared tree· commit: Giorgio) — item F: `ddeBufferRef` → `DirectDistanceEntry` SSoT:** Το `hooks/canvas/useCanvasKeyboardShortcuts.ts` hand-rolled `ddeBufferRef=useRef('')` + inline digit accumulation migrated. Το per-keystroke pure `applyTypedAngleKey` **γενικεύτηκε** σε neutral **`applyTypedNumericKey(dde, key, { allowNegative })`** (domain-neutral input-λογική)· `applyTypedAngleKey` = thin alias (allowNegative default → 2 rotation consumers byte-identical, μηδέν άγγιγμα). LINE DDE: `ddeRef = useRef(new DirectDistanceEntry())`, ψηφία/Backspace via `applyTypedNumericKey(..., { allowNegative:false })` (απόσταση θετική, κατεύθυνση από κέρσορα — AutoCAD DDE), Enter → `snapshot()` (κενό→chain-finish αμετάβλητο), reset σε tool-change/tempPoints-empty. **Κόμμα-parity δωρεάν** + ένα buffer SSoT γραμμή⊕περιστροφή. Tests: `typed-angle-entry.test.ts` +6 → 26 GREEN + 17 rotation GREEN. ΟΧΙ tsc (N.17). Commit: Giorgio (2 MOD: `typed-angle-entry.ts` + `useCanvasKeyboardShortcuts.ts` + test + ADR-357 changelog + αυτό το αρχείο). _(Δεν προστέθηκε ratchet guard: το `useRef('')` idiom είναι πολύ γενικό για ασφαλές forbiddenPattern· το SSoT είναι πλέον το `applyTypedNumericKey`.)_

**✅ DONE (2026-07-07, Opus 4.8, ADR-566/568/577, shared tree· commit: Giorgio) — item E: wall pick-loop scaffolding SSoT:** **grep-first ανέτρεψε το stale flag** — το «3×» ήταν λάθος: το `useWallSplitTool` είναι point→point **knife** (όχι wall-pick) & ήδη migrated σε `useSceneManagerAdapter`. Το πραγματικό διπλότυπο ήταν **byte-identical** μεταξύ `useWallMergeTool` + `useWallGapOpeningTool`: scene helpers (`getScene`/`getWallById`/`findWallAtPoint`/`collectSelectedWalls`) + dual-flow FSM (Flow B selection-first ⊕ Flow A pick loop + escape). **NEW SSoT** `hooks/tools/useWallPickScaffold.ts` (generic πάνω στον level manager, reuse ADR-577 `useSceneManagerAdapter`)· τα 2 hooks κρατούν μόνο το per-tool `execute*` action που δέχεται `getSceneManager`/`getScene`/`levelManager`/`setHint` μέσω **execute context** (σπάει το circular dep, action = pure closure). **Public API αμετάβλητο** (props+return) → μηδέν αλλαγή σε `useModifyTools`/`CanvasSection`/`useCanvasClickHandler`. NEW test `useWallPickScaffold.test.ts` 6 GREEN. ΟΧΙ tsc (N.17). Commit: Giorgio (1 NEW scaffold + 2 MOD consumers + 1 NEW test + ADR-566/568 changelog + αυτό το αρχείο). _(Δεν προστέθηκε ratchet guard: το scaffold είναι πλέον το μόνο μέρος με τον FSM· κανένα γενικό inline pattern να φρουρηθεί χωρίς θόρυβο.)_

**Grip-defaults VALUES — 3+ ανταγωνιστικά objects που ΑΠΟΚΛΙΝΟΥΝ (2026-07-05, Opus 4.8, ADR-549 crosshair audit) — pre-existing SSoT fragmentation, LARGE/cross-domain, ΧΡΕΙΑΖΕΤΑΙ ΑΠΟΦΑΣΗ ΤΙΜΩΝ + CLEAN TREE (Giorgio: dedicated Plan pass αργότερα):** Το ADR-559 §3b κεντρικοποίησε το grip-settings **SHAPE** (grip-settings-schema) αλλά ΟΧΙ τα **default VALUES** — που ζουν σε ≥6 σημεία με ΠΡΑΓΜΑΤΙΚΗ απόκλιση: `settings/FACTORY_DEFAULTS.ts GRIP_DEFAULTS` (`apertureSize:10`, warm=`GRIP_WARM_COLOR` orange), `types/gripSettings.ts DEFAULT_GRIP_SETTINGS` (`apertureSize:10`, warm=`SNAP_INTERSECTION` **hot-pink** ⚠️, hot=`SNAP_ENDPOINT`), `stores/GripStyleStore.ts` initial (`apertureSize:**20**` ⚠️, warm=`GRIP_WARM_COLOR`), + `settings-core/defaults.ts:125`, `settings-core/types/domain.ts:414`, `ui/hooks/useUnifiedSpecificSettings.ts:230`. Το `showAperture:true` τυχαίνει ίδιο παντού, αλλά τα OBJECTS αποκλίνουν (aperture 10-vs-20, warm orange-vs-pink). **Big-player fix (VS Code/Figma/Revit):** ΕΝΑ canonical grip-factory-defaults SSoT, τα υπόλοιπα (store initial / provider fallback / reset / schema) να ΠΑΡΑΓΟΝΤΑΙ. Partial-field centralization (μόνο showAperture→const) = **fake SSoT band-aid, ΑΠΑΓΟΡΕΥΜΕΝΟ**. **ΓΙΑΤΙ flagged όχι fixed:** (1) απαιτεί απόφαση Giorgio για canonical τιμές (aperture 10/20; warm orange/pink; = behavior + color-identity ADR-445)· (2) συγκρούεται με uncommitted color/grip work (ADR-573, «ΜΗΝ αγγίξεις»)· (3) N.8 6 αρχεία/2+ domains → Plan pass. **Template:** ADR-559 §3b (canonical defaults + derivations + ratchet guard «forbid inline grip-default literals»). Ξεκίνα ΜΕΤΑ το land του color/grip + canonical-values decision.
**Zero-React pub/sub store boilerplate — factory + WAVE 1/2/3 DONE, WAVE 2.5 pending (2026-07-05, Opus 4.8, Giorgio SSoT order):** Η μηχανή `let state; const listeners = new Set<() => void>(); set(notify)/get/subscribe` ήταν copy-pasted σε **18** stores. **✅ NEW SSoT** `stores/createExternalStore.ts` — generic vanilla-store factory (`createExternalStore<T>(initial, { equals? })` → `{ get, set, subscribe }`· industry-standard Zustand/Redux shape· always-notify default, optional equality-guard για signal-semantics)· **7 jest** (`stores/__tests__/createExternalStore.test.ts`). **✅ WAVE 1 migrated (4 stores, public API αμετάβλητο, regression GREEN):** `RegionPerimeterPreviewStore` (κρατά brand+builders), `RegionGapMarkersStore` (κρατά clear-empty guard), `AutoAreaPreviewStore`, `AutoAreaResultStore`.
**✅ WAVE 2 DONE (2026-07-05 Session 2, Opus 4.8):** και τα 9 migrated σε `createExternalStore` (public API αμετάβλητο): `grid-perimeter-mode-store` (factory, `equals:Object.is`), `hatch-draw-defaults-store` (plain, patch-set), `hatch-pick-mode-store` (guard-στο-wrapper γιατί reset notify-άρει), `envelope-floor-slabs-store` (identity guard + silent reset), `DimensionCreateStore` (reducer + `equals:Object.is`), `DimRowHandleModeStore`, `opening-tag-layer`, `DimTextOverrideStore` (field-compare equals), `toolHintOverrideStore` (object wrapper `{override,step}` + field-compare equals). **Factory extended:** additive `reset(next)` (silent set + `listeners.clear()`, Zustand-`destroy`-aligned) για τα 4 test-resets που έκαναν `state=INIT; listeners.clear()` — +2 jest (9 total).
**✅ WAVE 3 DONE (2026-07-05 Session 2, Opus 4.8· solo, ξεκλείδωσε):** `multi-floor-3d-source` + `multi-floor-dxf-source` (identity-guard fit)· `bim-mesh-thumbnail-cache` migrated ΜΟΝΟ το version-signal pub/sub → `createExternalStore<number>` (το Map/inFlight/preload async cache μένει imperative — δεν είναι single-state).
**⛔ NOT-A-FIT (κράτα bespoke):** `ui/ribbon/context/RibbonFieldStore` (per-key signature-cache slice store, ADR-547 — όχι single-state)· `bim/materials/material-thumbnail-resolver` (async cache).
**✅ WAVE 2.5 DONE (2026-07-05 Session 4)** — και τα 22 single-state/reducer/hot-path stores migrated (βλ. SESSION 4 DONE header). Το `SelectionCyclingStore` ήταν στο `systems/selection/` (όχι `systems/cursor/` — ο παλιός tracker είχε λάθος path). `envelope-spec-store`/`opening-tag-style-service` = NOT-A-FIT (Map cache / async service). `command-line/*` = fit, migrated.
**✅ Follow-up guard DONE (2026-07-05 Session 4)** — `.ssot-registry` module `create-external-store` (tier 3, forbid inline `new Set<\(\) => void>`) πέτυχε **0-violation** (όχι baseline-ratchet): tests exempt via `exemptPatterns`, οι 6 not-a-fit + 3 factories allowlisted. `test:registry-golden` 56 GREEN. 🔴 Giorgio: `npm run ssot:baseline` + commit.

**✅ WAVE 2.6 BOX 1 DONE (2026-07-05 Session 6, Opus 4.8, shared tree· commit: Giorgio):** re-grep έπιασε **72** candidates (3η alias-παραλλαγή `: Set<Listener> = new Set()` — TrackingPointStore). **43 migrated & grep-verified** στο ΥΠΑΡΧΟΝ `createExternalStore` (public API αμετάβλητο): keyboard(3) + mep-proposals(7 incl. water golden) + preview(10, bespoke field-compare guard+clone κρατήθηκαν, no-equals) + GripArmed(1, Map→composite `{keys,refs}`) + grip-class(7) + misc(15). **4 NOT-A-FIT (→allowlist Box 4):** `rotation-snap-store` (multi-field+factory-singleton+try/catch notify), `regression-alert-bus` (event bus, μηδέν state), `CanvasNumericInputStore` (imperative+`_dde` delegation), `SnapOverrideOrchestrator` (3 ανεξάρτητα state machines). **Πολιτική try/catch-notify:** factory=plain loop (big-player canonical)· stores που απέκλιναν ΜΟΝΟ σε defensive try/catch → migrate (drop it, 2 QuickProperties)· multi-field→NOT-A-FIT. **🔴 ΕΚΚΡΕΜΟΥΝ (Session 7+, handoff `HANDOFFS/2026-07-05_session6_wave26-box1-done-box234-pending.md`):** STEP0 TrackingPointStore(leftover, clean fit) + BOX 2 (8 απλά style + 5 σύνθετα multi-map LayerStore/LayerState/LinetypeRegistry/LayerFilters/ToolState + 4 structural + polar + region-pick 2-instance) + BOX 3 hot-path 5 (Scene/SelectedEntities=version-signal/EntityBodyDrag/Lasso×2, browser-verify) + BOX 4 guard extension (`new Set<Listener>`/`: Set<Listener>` ERE, allowlist NOT-A-FIT, 0-violation/ratchet). Giorgio: commit 43 files (μετά ΚΑΙ `ssot:baseline`+commit στο τέλος Box 4).

**✅ WAVE 2.7 DONE (2026-07-05 Session 8, Opus 4.8 orchestrator, shared tree· commit: Giorgio) — DOMAIN-NAMED listener alias stores (4ος undercount):** Re-grep (`new Set<\w*(Listener|Subscriber)>` + `: Set<\w*(Listener|Subscriber)> = new Set`) → **29 sites / ~24 files**. Κατηγοριοποίηση: 4 ήδη-allowlisted (WAVE 2.6) + 2 ADR-040 hot-path (ImmediatePositionStore 3-set / HoverStore 2-set, NOT-A-FIT) + **15 genuine candidates** delegated σε 5 sonnet agents. **14 MIGRATED & grep-verified & jest-GREEN** στο ΥΠΑΡΧΟΝ `createExternalStore` (public API αμετάβλητο ανά store): **Batch A** module single-state — ModalPresenceStore (Object.is + `subscriberCount` side-channel για ref-counted MutationObserver, `__resetForTest`→`store.reset`) / IsolateEffectsStore (no-equals, dequal guard στο wrapper) / mep-wire-waypoint-ui-store (custom `sameHover` equals) / roof-edge-selection-store (`sameRoofEdge`) / guide-drag-store (click-driven low-freq, Object.is, drop try/catch· 27 jest). **Batch B** registries **version-signal** — line-style-registry + dim-style-registry (Maps ως private mutation-accelerators, `createExternalStore<number>(0)`, `notify(){cache=null; store.set(get()+1)}`, `subscribe` delegate· 18 jest). **Batch C1** version-signal — guide-store (4 fields→version counter) / construction-point-store / **CommandHistory** (ΞΕΧΩΡΙΣΤΟ από command-line/*· payload `subscribe`→`versionStore` + `lastEvent` side-channel wrapper `versionStore.subscribe(()=>listener(this.lastEvent))`· 94 jest). **Batch C2** — DrawingStateMachine (FSM discriminated-union single-object, no-equals, `reset()`=**public runtime API**→`store.set` ΟΧΙ `store.reset`, payload subscribe wrapped· 58 jest) / bim-edge-resolution-store (class composite `{width,height}` field-compare equals) / **Bim3DCursorReadoutStore** (high-freq per-mousemove· κράτησε το προϋπάρχον `readoutEquals` dedup byte-identical· 🟡 **Giorgio: browser perf-verify 3D cursor readout**). **Batch D** — ViewportStore **2-instance** (activeScaleStore Object.is + scaleListStore `sameScaleList`, `markSystemsDirty` bespoke στα setters· 15 jest). **1 NOT-A-FIT (Batch D):** KeyboardFocusManager (2 channels broadcast ΤΗΝ ΙΔΙΑ τιμή focusedId + payload-carrying subscribe + disposed-lifecycle → anti-SSoT να διπλασιαστεί). **3 NOT-A-FIT (κρίση orchestrator, δεν delegated):** device-pixel-ratio (payload callback `(dpr)` + media-query lifecycle, μηδέν readable snapshot, cursor/ 6D) / clash-focus-bus (pure event-bus `(point)`, μηδέν state — regression-alert-bus precedent) / aria-live-bus (announce bus, `lastMsg`=μόνο dedup). Και τα 3 payload-carrying subscribe → migration θα άλλαζε public API → big-player: buses=bespoke. **GUARD (module `create-external-store`):** `forbiddenPatterns` διευρύνθηκαν σε `new Set<\w*Listener>` / `new Set<\w*Subscriber>` / `: Set<\w*Listener>` (ERE, `\w*` καλύπτει bare+domain-named)· allowlist += τα 6 νέα NOT-A-FIT (ImmediatePositionStore/HoverStore/device-pixel-ratio/clash-focus-bus/aria-live-bus/KeyboardFocusManager). **dxf-viewer = 0-violation** (κάθε εναπομείναν match allowlisted). `test:ssot-suite` **221/221 GREEN** (registry-golden 56). **🔵 GAP για Giorgio (εκτός dxf scope):** το διευρυμένο `\w*Listener` πιάνει ΕΝΑ non-dxf αρχείο `src/components/properties/shared/useFloorsByBuilding.ts` (`new Set<FloorsListener>`) — out-of-subapp (δεν εισάγει dxf factory)· θα καταγραφεί ως baseline ratchet-debt όταν τρέξεις `ssot:baseline` (ίδιο precedent με `useFirestoreBuildings`/`super-admin-active-company` του `new Set<() => void>`). **🔴 Giorgio: commit (14 stores + `.ssot-registry.json` + αυτό το αρχείο) + `npm run ssot:baseline`.** _(Σημ.: τα 14 stores + registry έγιναν ήδη commit από τον commit-coordinator — commits `52077738`/`07777677`/`42d10597`/`be46c5b4`/`ab2d8e1e`· εκκρεμεί commit αυτού του αρχείου + `ssot:baseline`.)_

**✅ WAVE 2.8 DONE (2026-07-05 Session 8, Opus 4.8, shared tree· commit: Giorgio) — INLINE parametrized-fn-type stores (5ος & ΤΕΛΕΥΤΑΙΟΣ undercount του Set-idiom):** Canonical detection `new Set<([^)]+) => void>` + `: Set<([^)]+) => void> = new Set` → **6 sites / 5 files**. **2 MIGRATED** στο ΥΠΑΡΧΟΝ `createExternalStore` (public API αμετάβλητο): `settings-provider/globalStores.ts` (Grid + Ruler — single-state, `get settings()`+`update`+`subscribe` payload→wrapper `store.subscribe(()=>listener(store.get()))`· έφυγε το unused `listeners` field από το interface, grep-verified μηδέν consumer το διάβαζε) + `rendering/canvas/core/CanvasSettings.ts` (**version-signal**: `this.settings` μένει SSoT, `changeStore=createExternalStore<number>(0)`, `subscribeToChanges`→`changeStore.subscribe(()=>listener(this.settings))`, notify→`set(get()+1)`, `cleanup`→`reset(0)`, drop try/catch notify + pre-existing dead `oldSettings`). **4 NOT-A-FIT (allowlisted):** `rendering/core/UnifiedFrameScheduler.ts` (ADR-040 RAF per-frame FrameMetrics), `rendering/entities/shared/geometry-rendering-utils.ts` (`BaseConfigurationManager<T>` = ήδη το dedup SSoT base, 1 subclass CursorConfiguration σε cursor/ ADR-040, no stored state), `settings/io/SyncService.ts` (cross-tab BroadcastChannel event-bus, transient message payload, μηδέν readable state), `providers/StableFirestoreProvider.tsx` (per-key `Map<key,{callbacks:Set}>` subscription multiplexer· instantiated ως untyped `new Set([cb])` → δεν πιάνεται καν από τον guard). **GUARD:** `create-external-store` forbiddenPatterns += `new Set<([^)]+) => void>` + `: Set<([^)]+) => void> = new Set` (ERE)· allowlist += UnifiedFrameScheduler/geometry-rendering-utils/SyncService. **dxf-viewer = 0-violation** (τα 3 εναπομείναντα inline-fn matches allowlisted). `test:ssot-suite` **221/221 GREEN** (golden 56)· 6/6 υπάρχον grip-size-migration jest GREEN. **🔵 GAP για Giorgio (εκτός dxf scope):** το inline-fn pattern πιάνει **3 non-dxf files** → baseline-debt όταν τρέξεις `ssot:baseline`: `src/components/shared/addresses/editor/state/addressEditorMachine.ts`, `src/core/canvas/infrastructure/CanvasRegistry.ts` (2-arg event bus), `src/subapps/geo-canvas/domains/configuration/GeoCanvasConfig.ts` (+ το WAVE 2.7 `useFloorsByBuilding.ts`). **🏁 CLOSURE — residual sweep (type-alias Sets, arrays, Map-registries, EventEmitter/Subject) επιβεβαίωσε: το `new Set<...>` pub/sub duplication ΕΚΛΕΙΣΕ, ΔΕΝ υπάρχει 6ος Set-variant.** Μόνο 2 non-Set one-offs μένουν (low-priority OBSERVATIONS, ΟΧΙ duplicated idiom, ΟΧΙ wave): `services/ServiceHealthMonitor.ts` (`Array<(report)=>void>` payload monitor/dispatcher) + `systems/collaboration/CollaborationEngine.ts` (`extends EventEmitter` — διαφορετικός μηχανισμός). **🔴 Giorgio: commit (globalStores + CanvasSettings + `.ssot-registry.json` + αυτό το αρχείο) + `npm run ssot:baseline`.**

**✅ WAVE 2.6 BOX 2/3/4 DONE (2026-07-05 Session 7, Opus 4.8 orchestrator, shared tree· commit: Giorgio):** Ολοκληρώθηκαν ΟΛΑ τα εναπομείναντα fits του re-grep (`new Set<(Listener|Subscriber)>` + `: Set<Listener>`): **25 stores migrated & grep-verified & jest-GREEN** στο ΥΠΑΡΧΟΝ `createExternalStore` (public API αμετάβλητο ανά store). **STEP0** TrackingPointStore (class composite snapshot, drop try/catch notify). **BOX 2a** 8 style stores (AutoSaveStatus/Completion/Grip[+guardGlobalAccess]/Quick/Text/LinetypeScale[Object.is+persist-guard]/LineweightDisplay[Object.is]/ToolStyle[+overlayCompletionCallback side-channel], 37 jest). **BOX 2b** 5 multi-map **version-signal** (LayerStore/LayerState/LinetypeRegistry[silent `store.reset` για να μη notify-άρει πριν το clear]/LayerFilters[double-build→single]/ToolState[`store.set` γιατί `reset()` είναι public runtime API, ΟΧΙ test-only], 276 jest). **BOX 2c** 4 structural analytical (single-state/composite). **BOX 2d** polar-tracking (single-state class) + region-pick (**2-instance**: phaseStore Object.is + firstCornerStore structural-eq). **BOX 3** 5 ADR-040 hot-path ΧΩΡΙΣ equals/notify byte-identical (SelectedEntities+Scene=**version-signal** `createExternalStore<number>(0)`, `commit(){rebuildCaches(); store.set(store.get()+1)}`· EntityBodyDrag single-state[clear reorder observationally-equivalent]· Lasso×2· 113 jest· 🟡 **Giorgio: browser perf-verify marquee/lasso/body-drag @60fps**). **BOX 4 guard** `.ssot-registry` module `create-external-store` επεκτάθηκε: `forbiddenPatterns` += `new Set<Listener>`/`new Set<Subscriber>`/`: Set<Listener>` (ERE, όχι `(?:)`)· allowlist += τα **4 NOT-A-FIT** (`rotation-snap-store`/`regression-alert-bus`/`CanvasNumericInputStore`/`SnapOverrideOrchestrator`). Τα 3 νέα patterns πιάνουν ΜΟΝΟ τα 4 allowlisted → μηδέν νέα violation. `test:ssot-suite` **221/221 GREEN** (incl. registry-golden 56). **🔵 GAP για Giorgio (εκτός WAVE 2.6 scope):** το προϋπάρχον `new Set<() => void>` pattern πιάνει **2 non-dxf Firestore stores** (`src/hooks/useFirestoreBuildings.ts`, `src/services/firestore/super-admin-active-company.ts`) που ΔΕΝ είναι allowlisted ΟΥΤΕ στο baseline (baseline _meta=2026-06-03 προϋπάρχει του module) — θα καταγραφούν ως ratchet-debt όταν τρέξεις `ssot:baseline`· δικές τους hand-rolled pub/sub εκτός dxf-viewer factory scope (μελλοντικό pass ή allowlist). **🔴 Giorgio: commit (25 stores + `.ssot-registry.json`) + `npm run ssot:baseline` (register module με updated baseline).**

**🔴 WAVE 2.6 DISCOVERED (2026-07-05 Session 4, Opus 4.8) — ~70 ΑΚΟΜΗ single-state stores με ALIASED idiom `new Set<Listener>()` (`type Listener = () => void`) — το WAVE 2.5 grep (`new Set<\(\) => void>` literal) ήταν κι αυτό undercount, LARGE:** Ίδια ακριβώς hand-rolled pub/sub duplication, απλώς με local `type Listener = () => void` alias → **δεν πιάστηκε** στο WAVE 2.5 grep. Prime `createExternalStore` candidates (grep-verified `type Listener = () => void` σε δείγμα wall-preview/GripArmed/water-proposal/ToolStateStore/SceneStore). **Οικογένειες:** (α) **preview stores** ~11 `bim/*/*-preview-store.ts` (beam/column/floor-finish/foundation/mep-underfloor/roof/slab/stair/wall-covering/wall) — single preview-object fits· (β) **grip stores** ~9 `systems/grip/Grip*Store.ts` (Armed/BasePoint/ContextMenu/CopyMode/HoverMenu/Mode/Reference/SessionUndo + rotation-snap) class-based· (γ) **mep-design proposal** ~7 (drainage/electrical/fire/gas/heating/hvac/water)· (δ) **style stores** ~8 (Completion/Grip/Quick/Text/Tool Style + LinetypeScale/LineweightDisplay/LayerFilters — έλεγξε multi-field)· (ε) **structural analytical** ~4· (στ) keyboard trackers 3 (Ctrl/Q/Shift)· (ζ) misc: ToolStateStore/SceneStore/SelectedEntitiesStore(hot-path)/LayerStore family/cad-toggle-state/polar-tracking/zoom-window/wall-split/prompt-dialog/properties-palettes/font-stores/display-unit-state/CanvasNumericInput/LassoCrop/LassoFreehand(hot-path)/xline-mode/ambient-alignment/clash-report/regression-alert-bus/BeamBetweenMembers/EntityBodyDrag(hot-path)/region-pick(multi-signal). **ΠΡΟΣΟΧΗ:** πολλά class-based (private listeners) + κάποια ADR-040 hot-path (SelectedEntities/Scene/EntityBodyDrag/Lasso*) + κάποια multi-state (region-pick 2 sets, ImmediateTransform 3 sets ήδη allowlisted) → per-store fit-check όπως στο WAVE 2.5. **+ Guard gap:** το module `create-external-store` πιάνει ΜΟΝΟ `new Set<\(\) => void>` literal, ΟΧΙ `new Set<Listener>` — WAVE 2.6 να επεκτείνει το forbiddenPattern (π.χ. `new Set<Listener>` + baseline-ratchet τα ~70 legit μέχρι migration). **Scale:** πολλαπλά sessions (orchestrator-grade), 1 οικογένεια/session ≤70% context.
**ΣΗΜ. ADR-040 hot-path stores** (`HoverStore`/`ImmediatePositionStore`/`ImmediateTransformStore`) χρησιμοποιούν **διαφορετικό** pattern (δεν πιάστηκαν στο grep) → χωριστό pass με browser perf-verify + ADR-040 staging (CHECK 6B/6C).

**`inheritEntityStyle` — fillet local copy → νέο SSoT (2026-07-04, Opus 4.8, ADR-510 Φ5) — pre-existing duplicate, small (1 committed file, shared tree):** Το Φ5 (γενικό EXPLODE) χρειάστηκε «κληρονόμησε στυλ πηγής σε παράγωγο primitive» → **εξήχθη SSoT** `systems/entity-creation/inherit-entity-style.ts` (`inheritEntityStyle` + `ENTITY_STYLE_SKIP`, superset skip-set) και το χρησιμοποιεί το `explode-entity.ts`. **✅ DONE (2026-07-05 Session 1):** το `systems/corner/fillet-curve-geometry.ts` είχε **local** `inheritEntityStyle` + `ARC_STYLE_SKIP` (γραμμές ~216-229) — ΤΑΥΤΟΣΗΜΟ idiom (drop geometry/id/type/selection keys). **Candidate:** το fillet να κάνει `import { inheritEntityStyle } from '../entity-creation/inherit-entity-style'` και να σβήσει το local (+ `ARC_STYLE_SKIP`). **ΓΙΑΤΙ flagged όχι fixed (N.0.2):** το `fillet-curve-geometry.ts` είναι just-committed (ADR-510 Φ4e/Φ4e.2) + πιθανόν **ενεργός** άλλος agent σε fillet/chamfer (shared tree) → το άγγιγμα ρισκάρει conflict· <15λ, drop-in on-touch. Ίσως `.ssot-registry` guard «forbid inline `ARC_STYLE_SKIP`/local `inheritEntityStyle`».

**✅ DONE (2026-07-07, Opus 4.8, ADR-350/353/510, shared tree· commit: Giorgio) — item C: arc-angle σύμβαση trim/extend (latent bug fixed):** Audit ΟΛΟΥ του `systems/trim` επιβεβαίωσε συστηματικό radians-vs-degrees mismatch (`ArcEntity.startAngle/endAngle`=degrees canonical, αλλά trim τα διάβαζε/έγραφε ως radians → cut/hit-detect/tessellate degree-τόξου = garbage· `cutCircle` έγραφε radian angles σε degree fields). **Fix = καθαρή boundary conversion** (`degToRad` reads, `radToDeg` writes· byte-identical internal math, μηδέν ρίσκο sweep-convention change): `trim-line-arc-cutter` (arcSweep/pointToArcParam/arcFromParams/cutCircle), `trim-intersection-mapper` (angleWithinArc), `trim-fence-hit-detector` (tessellateArc), `trim-hover-preview` (3 fns). **Boy-Scout (N.0.2):** ίδιο bug στο `systems/extend/extend-intersection-caster.extendArc` (angleInSweep/arcEndpoint/angularDistance* με degrees + radian bestAngle σε degree field) → fixed identically. **ΔΕΝ** έγινε unify σε `tessellateArcDegrees` (θα άλλαζε το hit-detection sweep convention· deferred non-bug refactor). **Tests:** trim fixtures `Math.PI`→`180` + semicircle-end regression (73 GREEN) + NEW `extend-intersection-caster.arc-angle.test.ts` (90°→135° extend σε degrees, θα απέτυχε στο radian path· 1 GREEN). ΟΧΙ tsc (N.17). Commit: Giorgio (5 MOD source: 4 trim + 1 extend + 3 MOD test fixtures + 1 NEW extend test + ADR-350 changelog + αυτό το αρχείο). _(Ιστορικό flag παρακάτω:)_ **Arc-angle σύμβαση — split trim(radians) vs render/DXF(degrees) (2026-07-04, Opus 4.8, ADR-510 Φ4e.2) — pre-existing, large/cross-cutting:** Το `ArcEntity.startAngle/endAngle` ερμηνεύεται **degrees** από `ArcRenderer` (`degToRad`), `arcFrom*` helpers, stretch/array/auto-area/offset (canonical, DXF), αλλά **radians** από το trim subsystem (`tessellateArc` στο `trim-fence-hit-detector.ts`, `trim-line-arc-cutter.ts`, tests με `endAngle: Math.PI`). Το Φ4e.2 διόρθωσε **μόνο** το fillet path (νέο `solveTangentArc` + `tessellateArcDegrees` SSoT· το `computeFilletArc` πλέον degrees). **Υποψία:** το trim σε ΠΡΑΓΜΑΤΙΚΑ (degrees) arcs πιθανόν επίσης λάθος tessellation → χρειάζεται audit ολόκληρου του `systems/trim` για arc-angle unit + unify σε **ΕΝΑ** SSoT tessellator (`tessellateArcDegrees`). **ΓΙΑΤΙ flagged όχι fixed (N.0.2):** 4+ αρχεία trim + committed behavior + shared tree → dedicated pass, όχι εντός Φ4e.2. **Candidate:** grep `startAngle`/`Math.cos(.*[sS]tartAngle` σε `systems/trim`, αντικατάσταση radians-τεσσελλατορ με `tessellateArcDegrees`, regression trim-on-arc.

**✅ DONE (2026-07-05 Session 1)** — `findPolylineSegment` 2× διπλότυπο nearest-segment (2026-07-04 flag, ADR-510 Φ4e.2) — small: Το «ποιο segment της polyline χτυπήθηκε» υπάρχει copy-pasted σε `hooks/drawing/useCircleTTT.ts` + `hooks/drawing/useLinePerpendicular.ts` (ίδιο loop με closing-segment). Το Φ4e.2 (B) έφτιαξε το **SSoT** `nearestPolylineSegment(points, isClosed, p)` (geometry-rendering-utils, ίδιο return shape `{start,end,segmentIndex}`) και το χρησιμοποιεί ο fillet. **Candidate:** αντικατάσταση των 2 in-hook `findPolylineSegment` με το SSoT (drop-in). **ΓΙΑΤΙ flagged όχι fixed (N.0.2):** committed hooks σε shared tree εκτός scope Φ4e.2· <1h, χαμηλό ρίσκο → επόμενο touch.

**Wall pick-loop tool scaffolding — 3× διπλότυπο (2026-07-03, Opus 4.8, ADR-568) — pre-existing duplicate, medium:** Τα scene-helpers `getSceneManager`/`getScene`/`getWallById`/`findWallAtPoint`/`collectSelectedWalls` + το dual-flow scaffolding (activation selection-first + click pick-loop + escape) είναι **ταυτόσημα** σε `hooks/tools/useWallSplitTool.ts` + `useWallMergeTool.ts` + **NEW** `useWallGapOpeningTool.ts` (3×). Το ADR-568 tool **reuse** ΟΛΗ την pure geometry + commands + persistence (ουσία), αλλά αντέγραψε το scaffolding (thin, ~120 γρ.). **Candidate:** εξαγωγή `useWallPickScaffold({toolId, execute})` (SSoT scene-helpers + FSM) που καταναλώνουν και οι 3 hooks. **ΓΙΑΤΙ flagged όχι fixed (N.0.2):** τα split/merge είναι just-committed (ADR-566/363, shared tree)· η εξαγωγή τα refactor-άρει και τα 2 → ρίσκο regression σε committed tools εκτός του ADR-568 scope. Dedicated wall-tool-scaffold pass (+ ίσως `.ssot-registry` guard «forbid inline `createLevelSceneManagerAdapter` σε `useWall*Tool.ts`»).

**`closestPointOnOutline` private copy — beam-span-snap → SSoT (2026-07-03, Opus 4.8, ADR-569 λοξά-fix) — pre-existing duplicate, small (1 committed file):** Το ADR-569 fix κεντρικοποίησε το «πλησιέστερο σημείο περιγράμματος + διεύθυνση ακμής» ως SSoT `closestEdgeOnPolygonOutline` / `closestFacingEdgeBetweenPolygons` (`bim/geometry/shared/polygon-nearest.ts`, centroid-probing + refinement, additive). Το `bim/framing/beam-span-snap.ts` (ADR-528/529) κρατά ΑΚΟΜΗ private `closestPointOnOutline` (`OutlineHit {point, edge, seg}`) + private `pairFrame` που κάνει το ΙΔΙΟ centroid-probing+refinement facing-point idiom. **Candidate:** το `beam-span-snap` να υιοθετήσει `closestEdgeOnPolygonOutline` (delegate) → σβήνει το private αντίγραφο· ίσως και `pairFrame` να χτιστεί πάνω στο `closestFacingEdgeBetweenPolygons`. **ΓΙΑΤΙ flagged όχι fixed (N.0.2):** το `beam-span-snap` είναι **committed ADR-528/529, browser-verified** (per-bay/whole-line/justified alignment, cursor-driven) σε shared tree· η migration το refactor-άρει εκτός ADR-569 scope → ρίσκο regression στο auto-span. Το docstring του `polygon-nearest.ts` ήδη σημειώνει την «πιθανή μελλοντική υιοθέτηση». Dedicated beam-facing-geometry pass.

**✅ DONE (2026-07-05 Session 10, Opus 4.8, shared tree· commit: Giorgio) — `LineSettings` (4×) + `TextSettings` (5×) shape κεντρικοποιήθηκε (ADR-559 §3e/§3f, mirror §3b grip):** re-grep επιβεβαίωσε ΑΚΡΙΒΩΣ 4+5 δηλώσεις. 2 νέα canonical schemas: **`types/line-settings-schema.ts`** (`LineSettingsBase` + `LineType`/`LineCapStyle`/`LineJoinStyle` — τα primitive unions μετακόμισαν εδώ ως dependency-leaf, `domain.ts` τα re-exportάρει → barrel `settings-core/types` + όλοι οι importers αμετάβλητοι) + **`types/text-settings-schema.ts`** (`TextSettingsBase` + `TextAlign`/`TextBaseline`). **Triage:** domain `LineSettings`/`TextSettings` → identity projection (`= *Base`)· `LinePreview.tsx` + `CurrentSettingsDisplay.tsx` (4 δηλώσεις) → `type X = Pick<*Base,...>` projections (loose optional `lineCap`/`lineJoin` κρατήθηκαν)· `TextSettingsContext.tsx` (exported public API, 10-field «general» subset) → `export type TextSettings = Pick<TextSettingsBase,...>` **public API αμετάβλητο** (ίδιο shape· 3 consumers `useUnifiedSpecificSettings`/`text-settings-helpers`/`ZustandToConsolidatedAdapter` ανέπαφοι)· `useSettingsPreview.ts` 2 tiny CSS-preview inputs (`{color,width,style}` / `fontWeight` string-union) → **de-collision renames** `LineCssPreviewInput`/`TextCssPreviewInput` (mirror `GripCssPreviewInput`· 0 external consumers). **Shape-only, ZERO behavior change** (default VALUES per-context όπως §3b). **GUARD:** 2 νέα tier-3 modules `line-settings-schema`/`text-settings-schema` (ERE `interface\s+LineSettings\b`/`interface\s+TextSettings\b`· `\b` δεν πιάνει `*Base`· μηδέν allowlist όπως §3b) + golden fixtures (shouldMatch/shouldSkip). **grep-verified 0 leftover standalone `interface LineSettings/TextSettings`.** `test:ssot-suite` **225/225 GREEN** (221→225, +4 golden· registry-golden 56→60). ΟΧΙ tsc (N.17). 🔴 Giorgio: `npm run ssot:baseline` (register 2 νέα 0-violation modules) + commit (2 schema files + domain.ts + 4 UI files + `.ssot-registry.json` + golden fixtures + ADR-559 + αυτό το αρχείο).

**grip-KIND union — name-collision + υπόλοιπα inline folds (2026-06-30, Opus 4.8, ADR-559 §3d-bis) — pre-existing duplicate, ΜΕΡΙΚΩΣ fixed:** Κεντρικοποιήθηκε ΕΝΑ canonical `GripKind` (`rendering/types/Types.ts`) + **5 projections** (`GripInfo.type`, `rendering/grips/types.ts GripType`, `unified-grip-types.ts UnifiedGripType`, `hooks/grip-kinds.ts GripType`, `gripSettings.ts GripState.gripType`). **🔴 Εκκρεμεί:** (α) **name collision** — ΔΥΟ exported `GripType` (`rendering/grips/types.ts` render-layer ↔ `hooks/grip-kinds.ts` interaction-layer)· rename ενός → de-collision (ευρύ blast radius, hooks/index re-export). (β) `rendering/passes/OverlayPass.ts` (`gripType` + local `GripInfo` interface) — **DEADCODE** file, fold μόνο αν αναβιώσει. (γ) `GripPhaseRenderer.ts:271` inline cast → derive από canonical. (δ) `bim/grips/axis-box-grips.ts` `'vertex'|'edge'` (BIM reshape) → `Extract<GripKind,...>`. **ΓΙΑΤΙ flagged:** το rename αγγίζει re-exports/πολλά call-sites· τα υπόλοιπα είναι property-level/deadcode/cast — χαμηλό value τώρα. Dedicated grip-kind de-collision pass.

**✅ DONE (2026-06-30, Opus 4.8, ADR-559 §3b, Giorgio SSoT order) — Grip-settings shape κεντρικοποιήθηκε:** Τα **8** re-declared grip-settings interfaces (5 «κανονικά» + 3 UI-local) ενοποιήθηκαν σε ΕΝΑ canonical `types/grip-settings-schema.ts` (`GripSettingsBase` + `GripStyleExtras` + `GripColors`/`ResolvedGripColors` + `GripSettingsFull`) + **6 projections** (`type X = ... & Omit<...>`) + **2 de-collision renames** (`GripSettingsSummary`/`GripCssPreviewInput` — είχαν display-only fields, name-collision όχι duplicate) + **ratchet guard** module `grip-settings-schema` στο `.ssot-registry.json` (tier 3, forbid νέο standalone interface, registry-golden 56/56). Νέο grip field πλέον = ΕΝΑ σημείο (`GripSettingsBase`). **Default VALUES έμειναν per-context** (stored/runtime/draft/hover/preview διαφέρουν σκόπιμα — aperture 10 vs 20, sentinel vs resolved colors)· κεντρικοποιήθηκε μόνο το shape (zero behavior change). 261/262 jest GREEN. 🔴 Giorgio: `npm run ssot:baseline` (formal register, 0-violation module) + commit.

**`defaultLayerId` inline `?id` παραλλαγές (2026-06-30, Opus 4.8, ADR-508 §line-cyan audit) — pre-existing, μικρό:** Κεντρικοποιήθηκε `getDefaultLayerId()` (`stores/LayerStore.ts`) και μεταναστεύθηκαν **10** copies του `getLayer(DXF_DEFAULT_LAYER)?.id ?? ''` (9 preview helpers + generator). **✅ DONE (2026-07-05 Session 1)** — και οι 2 inline παραλλαγές → `getDefaultLayerId()` με ΔΙΑΦΟΡΕΤΙΚΟ type contract (`getLayer(DXF_DEFAULT_LAYER)?.id` → `string | undefined`, ΧΩΡΙΣ `?? ''`): `hooks/drawing/drawing-entity-builders.ts:73` + `hooks/drawing/drawing-preview-partial.ts:198`. **ΓΙΑΤΙ flagged όχι fixed:** η αλλαγή σε `getDefaultLayerId()` (always-`string`) αλλάζει το `undefined→''` contract σε **commit-path entity creation** (`createEntityFromTool`) → χρειάζεται έλεγχος ότι κανείς consumer δεν διακρίνει `undefined` vs `''` (persistence/schema) πριν το unify. Dedicated pass + ίσως `.ssot-registry` guard «forbid inline `getLayer(DXF_DEFAULT_LAYER)?.id`».

**`:root` CSS-var inline reads — scatter (2026-06-30, Opus 4.8, ADR-004 Cinema 4D theme) — pre-existing scatter, medium (cross-domain):** Δημιουργήθηκε NEW SSoT primitive `readRootCssVar(name, fallback)` (`config/color-config.ts`) — οι 3 theme-colour resolvers (`resolveDxfCanvasBackgroundHex`/`resolveDxfCanvasGradientStops`/`resolveCssVarColor`) + `axis-cut-line-renderer` το χρησιμοποιούν πλέον (το axis-cut είχε **προϋπάρχον διπλότυπο** `getComputedStyle(...).getPropertyValue('--canvas-background-dxf')` → folded στο `resolveDxfCanvasBackgroundHex`, **διορθώθηκε**). **🔴 Εκκρεμεί migrate:** ~6 αρχεία διαβάζουν inline `getComputedStyle(document.documentElement).getPropertyValue(...)` για **μη-theme** σκοπούς (`debug/*`, `systems/modal/modal-presence-detect.ts` + `ModalPresenceStore.ts`, `ui/color/eyedropper.ts`, `utils/hover/text-labeling-utils.ts`, `src/utils/accessibility.ts`). **Candidate:** migrate σε `readRootCssVar` (ή promote το primitive σε `src/lib/platform/` αν υιοθετηθεί cross-subapp) + `.ssot-registry` module («forbid inline root-var getComputedStyle») + baseline. **ΓΙΑΤΙ flagged όχι fixed:** διαφορετικά domains (a11y/modal/eyedropper/debug), εκτός canvas-theme scope· κάποια διαβάζουν layout/positions όχι χρώματα → forced unify τώρα = scope balloon. Dedicated platform-util pass.

**2D cross-product — scatter + canonical vector SSoT χωρίς cross export (2026-06-25, Opus 4.8, ADR-525) — pre-existing, OBSERVATION (δεν το δημιούργησα):** Ο canonical vector-math SSoT `rendering/entities/shared/geometry-vector-utils.ts` (ADR-074/090) έχει `dotProduct`/`subtractPoints`/`addPoints`/`scalePoint`/`rotatePoint`/`getPerpendicularDirection` αλλά **ΟΧΙ exported cross-product**. Το 2D cross ζει ως `cross2D(a,b)` στο **`snapping/engines/intersection-calculators.ts`** (snapping layer) + διάσπαρτα inline crosses (π.χ. flipY handedness predicate στο νέο `column-beam-corner-snap.ts`). **ΓΙΑΤΙ flagged όχι fixed:** το `cross2D` είναι σε **λάθος layer** για reuse από `bim/*` (ADR-514: μηδέν bim→snapping runtime dependency)· προσθήκη `crossProduct` στο geometry-vector-utils αγγίζει snapping (shared tree) + ρισκάρει το ssot-discover ratchet (parallel function). Το νέο module reuse ΟΛΟ το υπόλοιπο vector-math SSoT· το cross έμεινε **μονή έκφραση-predicate** (όχι μηχανισμός). **Candidate:** export `crossProduct(v1,v2)` στο geometry-vector-utils (canonical home, sibling του `dotProduct`) → `cross2D` + inline crosses delegate. Dedicated vector-math pass.

**`buildDefaultFoundationParams` — name collision δύο functions (2026-06-24, Opus 4.8, ADR-514 Φ6d) — pre-existing, small (test-only, ~6 αρχεία):** Δύο functions ΙΔΙΟ όνομα: (A) `bim/types/foundation-types.ts:385` `buildDefaultFoundationParams(kind)` → defaults στο ORIGIN, **pure** (μόνο constants)· consumers = **ΜΟΝΟ tests** (foundation.factory/validator/schemas/types/geometry tests) + 1 JSDoc· (B) `hooks/drawing/foundation-completion.ts:113` `(clickPoint, kind?, overrides, sceneUnits)` → **production** (preview/commit/auto-design, σέρνει geometry+validator+factory graph). **ΟΧΙ runtime διπλότυπο** (η παραγωγή χρησιμοποιεί ΜΟΝΟ το B)· είναι (α) name collision (μπερδεύει grep) + (β) διπλή assembly-logic defaults — αλλά οι ΤΙΜΕΣ είναι κοινές constants → μηδέν drift. Νόμιμη δικαιολογία διαχωρισμού: το A είναι pure-light για low-level type/schema/geometry tests χωρίς το βαρύ graph του B. **Candidate fix:** rename το A → `defaultFoundationParamsFixture` (ή `...AtOrigin`) → σταματά το collision, κρατά το ελαφρύ fixture. **ΓΙΑΤΙ flagged όχι fixed:** αγγίζει ~6 test files + ο ADR-514 agent ενεργός σε foundation αρχεία (shared tree)· το όνομα είναι το λάθος, όχι η ύπαρξη. Rename-only pass.

**✅ DONE (2026-07-05 Session 2, Opus 4.8) — `createConfirmStore<TState,TAction>()` SSoT + registry guard (undercount 2→7):** Το grep-audit αποκάλυψε **7** ταυτόσημα hand-rolled confirm stores (όχι 2): `column-perimeter-confirm-store` (2 request fns), `column-adopt-size-confirm-store`, `shear-wall-extent-confirm-store`, `column-becomes-wall-confirm-store`, `section-relationship-confirm-store`, `walls/wall-cascade-delete-store`, `family-types/bim-family-type-delete-store`. **ΟΛΑ τα 7 migrated** → `createConfirmStore<XState,XAction>(CLOSED)` + thin named wrappers (public API αμετάβλητο, 59 jest GREEN). **Registry guard** module `create-confirm-store` (tier 3· forbid inline `_pendingResolve`· allowlist `stores/createConfirmStore.ts`· 0-violation επιβεβαιωμένο· registry-golden 56 GREEN). **OBSERVATION (χαμηλό, εκκρεμεί):** και τα confirm **dialogs** μοιράζονται shell (overlay>card>title+message+actions + useEscapeHandler + createPortal)· γενικό `<ConfirmDialog>` shell = πιθανό αλλά content/variants διαφέρουν legitimately — επανεξέταση αν αξίζει.

**Line/segment intersection-point — οικογένεια 3 helpers με κοινή determinant-math (2026-06-23, Opus 4.8, ADR-507 Φ3) — pre-existing scatter + 1 new (διακριτή σημασιολογία), cross-domain:** Υπάρχουν τώρα 3 helpers που λύνουν cross-product/denominator για τομή: (A) **NEW `segmentIntersection(a1,a2,b1,b2)→{point,t,u}|null`** (`utils/geometry/GeometryUtils.ts`) — **segment-clamped** (t,u∈[0,1]), point-returning· consumer = `planarizeSegments` (auto-area noding). (B) `lineIntersection(l1s,l1e,l2s,l2e)→Point2D|null` (`utils/angle-entity-math.ts`, exported) — **infinite-line** (δεν κλειδώνει στα τμήματα)· consumer = angle measurement. (C) private `intersectLines({px,py,dx,dy}×2)` (`bim/walls/wall-from-entity.ts`) — **infinite-line**, διαφορετικό input shape· consumer = wall miter joins. Επίσης boolean `segmentsIntersect` (GeometryUtils, selection). **Candidate:** ΕΝΑΣ core `lineIntersectionRaw(a1,a2,b1,b2)→{point,t,u,denom}|null` (no clamp) → η segment variant κλειδώνει t,u· οι infinite variants το χρησιμοποιούν αγνοώντας το clamp· `segmentsIntersect` delegate (sign-test ή t,u check). **ΓΙΑΤΙ flagged όχι fixed:** οι (B)/(C) είναι committed + **browser-verified** σε ΔΙΑΦΟΡΕΤΙΚΑ domains (angle measurement + wall miter, shared tree)· διαφορετική σημασιολογία (infinite vs segment) + (C) διαφορετικό input shape → forced unify τώρα = ρίσκο regression σε miter/γωνία. Το `segmentIntersection` ΔΕΝ είναι διπλότυπο υπάρχοντος (κανένα segment-clamped point-returning δεν υπήρχε) — αλλά η οικογένεια αξίζει ΕΝΑ core. Migrate με dedicated geometry pass.

**Entity-pair intersection DISPATCHER — διπλό 20-pair switch (2026-06-24, Opus 4.8, ADR-362 Round 20 gap #2) — pre-existing private dispatcher, medium (cross-domain snapping):** Η αποστολή «δύο entities → intersection points» έχει έναν πλήρη 20-pair dispatcher ΙΔΙΩΤΙΚΟ μέσα στο `IntersectionSnapEngine.calculateIntersections` (`snapping/engines/IntersectionSnapEngine.ts:216`, καλεί τα exported leaf calculators line/line, line/circle, circle/circle, poly*, rect*, xline*, ray*). Το ADR-362 Round 20 χρειάστηκε intersection re-projection για associative dims → NEW `systems/dimensions/dim-intersection-resolver.ts` που **ΧΡΗΣΙΜΟΠΟΙΕΙ τα ΙΔΙΑ exported leaf calculators** (μηδέν νέα math) αλλά έχει δικό του ΜΙΚΡΟ dispatcher για τα dim-relevant pairs (line/line, line/circle, circle/circle, poly pairs). **Candidate:** extract `export function intersectEntities(a,b): IntersectionResult[]` στο `snapping/engines/intersection-calculators.ts` (το αρχείο που ΗΔΗ κατέχει ΟΛΑ τα leaf calculators = φυσικό σπίτι) → ο engine ΚΑΙ ο dim resolver delegate. **ΓΙΑΤΙ flagged όχι fixed:** το extract αγγίζει shared snapping σε shared working tree με άλλους agents· η εντολή ήταν «touch ΜΟΝΟ dimension files». Ο dim resolver ΔΕΝ διπλασιάζει math (μόνο dispatch ~10 γρ.). Migrate σε dedicated snapping-centralization pass.

**Region hole-detection — ΔΥΟ ασύμβατες υλοποιήσεις (2026-06-23, Opus 4.8, ADR-507 Φ3) — pre-existing SSoT fragmentation, cross-domain:** Η ανίχνευση «νησιών» (holes) μέσα σε κλειστή περιοχή υπάρχει σε **δύο** μορφές με διαφορετικό αλγόριθμο: (A) `collectHoleAreas` (`systems/auto-area/auto-area-hit.ts`) — auto-area-based (collectAllClosedPolygons + `isPolygonInsideOuter` centroid+sampled-verts + even-odd· χρησιμοποιείται από «Μέτρηση εμβαδού» + NEW hatch pick-point Φ3)· (B) `findHoles` (`systems/recognition/space-detection.ts`) — region-perimeter-based (`ClosedPerimeter[]` + `polygonCentroid` containment + direct-child filter· χρησιμοποιείται από Stage-0 space recognition ADR-423/424). **Candidate:** ΕΝΑ generic `collectDirectHoles(outer, candidatePolygons)` SSoT (το «direct holes = μικρότερα πολύγωνα με centroid μέσα, όχι φωλιασμένα σε άλλο» είναι ΙΔΙΟΣ κανόνας) → και οι δύο να delegate (η μόνη διαφορά = πηγή υποψηφίων: auto-area candidates vs region perimeters). **ΓΙΑΤΙ flagged όχι fixed:** δύο διαφορετικά subsystems (auto-area + recognition, shared tree, άλλοι agents)· forced unify τώρα = ρίσκο σε committed recognition pipeline· >1h cross-domain. Χαμηλή-μεσαία προτεραιότητα — επανεξέταση όταν αγγιχτεί ξανά auto-area ή recognition.

**✅ DONE (2026-07-05 Session 3)** — **`createToolBridgeStore<T>()` SSoT — drawing-mode↔ribbon handle boilerplate (2026-06-22, Opus 4.8, ADR-404 Φ5b→Φ5c) — pre-existing duplicate, LARGE (undercount 15→**16** stores incl. beam):** Υπάρχουν **15 αρχεία `ui/ribbon/hooks/bridge/*-tool-bridge-store.ts`** (column, foundation, railing, furniture, electrical-panel, floorplan-symbol, mep-{segment,riser,boiler,radiator,manifold,water-heater,fixture} + wall + NEW slab) με **ταυτόσημο** module-store boilerplate: `let handle:T|null` + `const listeners = new Set<Listener>()` + `emit()` + `subscribe()` + `getSnapshot`/`getServerSnapshot` + `{ set(identity-check), get(), use()/subscribe() }`. Διαφέρει ΜΟΝΟ το shape του `THandle`. **Candidate:** NEW generic `createToolBridgeStore<THandle>()` factory (sibling του υπάρχοντος `stores/createToggleStore.ts`, γρ. παρακάτω) → τα 14 γίνονται `export const xToolBridgeStore = createToolBridgeStore<XHandle>()`. **ΓΙΑΤΙ flagged όχι fixed:** το ADR-404 Φ5b πρόσθεσε το 14ο **ακολουθώντας την καθιερωμένη σύμβαση** (δεν δημιούργησε ΝΕΟ είδος διπλότυπου — επέκτεινε υπάρχον pattern 13→14)· η ενοποίηση αγγίζει 14 committed αρχεία πολλών domains (column/mep/furniture/foundation — shared tree, άλλοι agents) → orchestrator-scale, ξεχωριστό pass + `.ssot-registry` module («forbid inline `let handle` + `Set<Listener>` σε `*-tool-bridge-store.ts`») + baseline. Migrate-on-touch ανά store ή ένα dedicated factory pass.

**`roof-slope-units.ts` → promote σε generic `slope-units.ts` (2026-06-22, Opus 4.8, ADR-404 Φ5c) — naming-only, OBSERVATION:** Το `bim/geometry/roof-slope-units.ts` (`roofSlopeToRatio`/`roofSlopeFromRatio`, pure deg↔percent↔rise/run) είναι **roof-named αλλά γενικό** — τώρα το καταναλώνει ΚΑΙ το `slab-slope-unit.ts` (Φ5c, μετά από Giorgio SSoT audit που εντόπισε το αρχικό μου διπλότυπο tan/atan → διορθώθηκε σε delegate). **Candidate:** rename → `bim/geometry/slope-units.ts` + generic ονόματα (`slopeToRiseRun`/`slopeFromRiseRun`) ώστε roof+slab να μην εξαρτώνται από roof-naming. **ΓΙΑΤΙ flagged όχι fixed:** το rename αγγίζει **committed** roof αρχεία (`roof-geometry.ts`, `roof-lower-envelope.ts`, `useRibbonRoofBridge.ts`, `roof-types.ts` RoofSlopeUnit — shared tree, άλλοι agents)· το reuse-as-is τώρα είναι SSoT-σωστό (μία math impl), το rename = ξεχωριστό low-risk pass.

**Arc «visible side» direction — ΔΥΟ ασύμβατες ερμηνείες του `counterclockwise` (2026-06-22, Opus 4.8, ADR-398 §3.12) — pre-existing SSoT fragmentation, cross-subsystem:** Η ορατή πλευρά τόξου ερμηνεύεται **διαφορετικά** σε 2 σημεία. (A) **renderer + hit-test + NEW `arcVisibleCcwRange`** (`geometry-arc-utils`): `counterclockwise===true` ⇒ τόξο CW ⇒ ορατό CCW εύρος = `[endAngle→startAngle]` (ο `ArcRenderer`→`addArcPath` κάνει `screenCCW=!counterclockwise`· `hitTestArcEntity` + `arcTargets` το μοιράζονται τώρα). (B) **`systems/array/path-sampler-strategies/arc-strategy.ts` `sweepDegFor`/`sample`**: `ccw===true` ⇒ δειγματίζει `[startAngle→endAngle]` (dir=+1) — **ΑΝΤΙΘΕΤΗ** πλευρά → array-along-arc πιθανόν δειγματίζει την ΚΡΥΦΗ πλευρά (ίδια κλάση bug με την κολώνα). **Candidate:** το `arc-strategy` να υιοθετήσει το `arcVisibleCcwRange` SSoT. **ΓΙΑΤΙ flagged όχι fixed:** άλλο subsystem που ΔΕΝ άγγιξα· απαιτεί browser-verify array-along-arc πριν αλλαγή (ρίσκο). Family: `hooks/guides/extract-entity-key-points.ts` arc endpoints αγνοεί κι αυτό το ccw.

**✅ DONE (2026-07-05 Session 1)** — Grip-kind trailing-index → `parseGripKindIndex` SSoT, 7 files/13 sites (2026-06-22 flag, ADR-510 Φ3c) — pre-existing scatter: Το «βγάλε τον αριθμό-δείκτη από ένα `<entity>-vertex-N` grip kind» γίνεται **inline** σε ~7 σημεία χωρίς SSoT: `bim/slabs/slab-grips.ts:113`, `bim/roofs/roof-grips.ts:120`, `bim/floor-finishes/floor-finish-grips.ts:91`, `hooks/grips/useGripContextMenuController.ts:193+209`, `hooks/canvas/useSmartDelete.ts:126+159`. Το ADR-510 Φ3c πρόσθεσε NEW named `parsePolylineSegIndex(kind)` (`systems/grip/polyline-grip-ops.ts`) — καθαρότερο αλλά polyline-scoped· **ΔΕΝ διπλασιάζει υπάρχον SSoT (κανένα δεν υπάρχει)**. **Candidate:** ΕΝΑ generic `parseGripKindIndex(kind):number|null` (ίσως στο `systems/grip/grip-to-vertex-refs.ts` ή νέο leaf) + τα 7 inline + το `parsePolylineSegIndex` να delegate. **ΓΙΑΤΙ flagged όχι fixed:** αγγίζει BIM grip domains (slab/roof/floor-finish — shared tree, άλλοι agents) + smart-delete· migrate-on-touch ανά domain.

**Arc-tessellator fragmentation — ~8 ξεχωριστά `arc→points` (2026-06-22, Opus 4.8, ADR-510 Φ3a) — pre-existing scatter, OBSERVATION:** grep βρήκε ~8 tessellators: `utils/geometry/GeometryUtils.arcToPolyline` (public, degree/CCW-only, ADR-166), `export/core/dxf-ascii-writer.arcPoints` (degree/CCW), `services/ClipToRegionService.sampleArcSegments` (private, sub-arc entities), `systems/trim/trim-hover-preview.tessellateArcSweep` + `trim-fence-hit-detector.tessellateArc`, `bim/structural/reinforcement/column-cross-ties.arcPoints`, `systems/dimensions/builders/radial-builder.sampleArc`, `bim/mep-underfloor/mep-underfloor-geometry.sampleArc`. **Κανένα reusable public «center+radius+startAngle+SIGNED sweep→points»** (CW/αρνητικό bulge). Το ADR-510 Φ3a έβαλε **private** `tessellateSignedArc` στο `geometry-bulge-utils` (μοναδικός consumer) ΑΝΤΙ να προσθέσει 9ο public (αρχικά είχα κάνει name-collision με `arcToPolyline` → ο Giorgio το έπιασε, διορθώθηκε). **Candidate:** ΕΝΑ signed-sweep SSoT (π.χ. `geometry-arc-utils.tessellateArc(center,r,startRad,signedSweepRad,maxSegDeg)`) + οι 8 να delegate. **ΓΙΑΤΙ flagged όχι fixed:** cross-domain (export/trim/dim/mep/clip, shared tree, διαφορετικά Arc shapes degree-vs-radian)· forced unify τώρα = ρίσκο + budget. Migrate-on-touch ανά domain.

**`hexToRgb` object-parse idiom — 2 sites, διαφορετικό shape (2026-06-21, Opus 4.8, ADR-511):** Το `hexToRgba(hex,alpha)→rgba string` SSoT (`bim/utils/bim-vg-fill-tint.ts`) **υιοθετήθηκε τώρα** από `WallCoveringRenderer` (ADR-511) + `FloorFinishRenderer` (ADR-419 boy-scout) — αφαιρέθηκαν 2 inline `parseInt(color.slice...,16)`→`rgba(...)` αντίγραφα. **✅ DONE (2026-07-05 Session 1)** — TextSettings migrated· detail-pdf ήδη delegate· ΑΛΛΟ shape (`{r,g,b}` object, ΟΧΙ rgba string): `ui/components/dxf-settings/settings/core/TextSettings.tsx:118` (χτίζει `DxfColor{kind:'TrueColor',r,g,b}`) + private `hexToRgb` στο `bim/structural/detail-sheet/render/detail-pdf-renderer.ts:43`. **Fix on-touch:** NEW `hexToRgb(hex):{r,g,b}|null` στο `bim-vg-fill-tint.ts` (το `hexToRgba` να delegate σε αυτό), migrate τα 2 sites. **ΓΙΑΤΙ flagged όχι fixed:** διαφορετικά domains (settings UI + PDF render, browser/pdf-verified) + διαφορετικό output shape από «το idiom μου» (rgba string)· χαμηλό ρίσκο pure color-math, migrate-on-touch.

**`canvas-hatch-fill` SSoT — 2D hatch line/dot primitives (2026-06-21, Opus 4.8, ADR-511) — pre-existing scatter, small (3 renderers, migrate-on-touch):** Το «γέμισε clipped path με παράλληλες γραμμές / πλέγμα τελειών σε world-space spacing» (`drawParallelLines`/`drawDotGrid`) ήταν copy-pasted ως private μέθοδοι σε **FloorFinishRenderer / SlabRenderer / EnvelopeRenderer** (pre-existing, χωρίς SSoT). Το ADR-511 wall-covering ΑΡΧΙΚΑ πρόσθεσε 4η copy → **διορθώθηκε αμέσως**: NEW `bim/renderers/shared/canvas-hatch-fill.ts` (`strokeHatchLines`/`fillHatchDots`, pure-ish, caller στήνει clip+style)· ο `WallCoveringRenderer` το χρησιμοποιεί (μηδέν δικό του αντίγραφο). **🔴 Προϋπάρχοντα να migrate-αριστούν on-touch:** `FloorFinishRenderer.drawParallelLines/drawDotGrid` (γρ.160-202), `SlabRenderer`, `EnvelopeRenderer`. **Fix on-touch:** `import { strokeHatchLines, fillHatchDots } from './shared/canvas-hatch-fill'` + αντικατάσταση των privates (το material→pattern/spacing mapping μένει per-renderer). **ΓΙΑΤΙ flagged όχι fixed τώρα:** οι 3 renderers είναι **ADR-040-sensitive (CHECK 6B/6D)** + shared tree (άλλος agent) → migrate με δικό τους pass + stage ADR-040· το mapping τους διαφέρει legitimately (wood/tile/dot vs tile/plaster/board). Το νέο SSoT ΔΕΝ αφήνει νέο διπλότυπο — μειώνει το scatter, δεν το αυξάνει.

**Geometric-primitive entity-type λίστες — υποψήφια taxonomy (2026-06-21, Opus 4.8, ADR-510 Φ2E) — OBSERVATION, ΟΧΙ βεβαιωμένο διπλότυπο:** Υπάρχουν ≥4 επικαλυπτόμενες λίστες entity-types «γραμμικών/καμπύλων primitives», η καθεμία με **διαφορετική σημασία & μέλη**: `systems/array/array-validation.ts ALLOWED_SOURCE_TYPES` (array sources — ευρύ, +text/hatch/block/dimension/point/ray/xline), `systems/array/path-pick-controller.ts` (δική του, ΔΕΝ reuse την array-validation), `systems/trim/...` (trimmable — +ray/xline, −rectangle), **NEW** `types/style-editable-primitives.ts` (style-editable — line/polyline/lwpolyline/circle/arc/ellipse/spline/rectangle/rect). **ΓΙΑΤΙ flagged όχι fixed:** τα predicates είναι **εννοιολογικά διαφορετικά** (can-array vs can-trim vs has-editable-style) → forced unification = πιθανό over-engineering· επιπλέον cross-domain (array+trim, shared tree). **Candidate:** μια βάση `GEOMETRIC_PRIMITIVE_TYPES` + per-domain deltas, ΑΝ αποφασιστεί ότι αξίζει. Χαμηλή προτεραιότητα — να επανεξεταστεί όταν αγγιχτεί ξανά array/trim. (Η fragmentation **προϋπήρχε**· η Φ2E πρόσθεσε 1 ακόμη εστιασμένη λίστα, όχι αντίγραφο υπάρχουσας.)

**`constrainDeltaToDominantAxis` ORTHO SSoT (2026-06-21, Opus 4.8, ADR-507) — ✅ DONE (small-duplicate closed):** Το `quantizeToDominantAxis`/`applyOrtho` (ORTHO/F8 dominant-axis delta clamp) ήταν byte-identical private αντίγραφο σε **5 αρχεία** (floor-finish-grips, slab-grips, slab-opening-grips, rect-grip-engine, + νέο hatch-grips). Ενοποιήθηκαν σε NEW `bim/grips/ortho-delta.ts` (`constrainDeltaToDominantAxis`, frame-agnostic pure). Και τα 5 import-άρουν το SSoT (120 grip jest GREEN). ✅ Registry guard `ortho-delta` προστέθηκε (2026-07-05, Session 1· forbid `quantize\w*DominantAxis`, allowlist ortho-delta.ts).

**ΔΕΥΤΕΡΗ οικογένεια merge-commands — transform base (2026-06-21, Opus 4.8, ADR-507 §8) — 🟢 ΟΥΣΙΑΣΤΙΚΑ DONE (audit+decision-gate+base+SSoT sweep· UNCOMMITTED το 2ο μισό):** Audit διόρθωσε το handoff — Add/Remove/InsertText/Scale ΔΕΝ έχουν merge boilerplate (εκτός scope). NEW `SnapshotTransformCommand` base (in-place spine) → Rotate/Scale/Mirror (COMMITTED a751c40b). NEW `merge-window.ts` (`isWithinMergeWindow`+`sameEntityIdSet`) υιοθετήθηκε σε MoveVertex/MoveOverlayVertex/MoveOverlay/MepSystem/MergeableUpdate. NEW `snapshot-geometry.ts` (`geometryFromSnapshot` SSoT) υιοθετήθηκε σε Arc/Lengthen/Polyline/Stretch + base (εξάλειψη 7 inline copies). Move=ΧΩΡΙΣΤΑ (δικό του `move-entity-cascade`, ADR-049 — αρχιτεκτονικά σωστό, ΟΧΙ leaky). 27 jest GREEN. **✅ DEFER items β/γ/δ/ε/στ DONE 2026-06-21 (Opus 4.8, +15 jest, UNCOMMITTED):** (γ) MoveEntityCommand inline window→`isWithinMergeWindow`· (δ) `geometryFromSnapshot` πήρε `{excludeType}` → Extend/Trim· (β) vertex `isDragging`-gate (MoveVertex/MoveOverlayVertex+Multiple· caller=seal false· διορθώνει 2-distinct-edits-merge)· (ε) **Giorgio SSoT audit → διόρθωση:** το νέο `sameSet` ήταν διπλότυπο της `dequal` (app-wide, χειρίζεται Sets) → ΔΙΑΓΡΑΦΗ set-equality.ts, χρήση `dequal` σε BimSelectionHighlighter+IsolateEffectsStore+`sameEntityIdSet`· **+NEW `canMergeDragSamples`** (εξάλειψη 6 αντιγράφων του dragging+window gate)· (στ) +2 `.ssot-registry` modules (0-violation → baseline ΑΘΙΚΤΟ, registry-golden 56 GREEN). 63 jest GREEN. **✅ item α DONE 2026-06-21 (Scope B, Giorgio «τι θα έκανε η Revit»):** NEW `MoveCommandBase extends SnapshotTransformCommand` → `MoveEntityCommand`+`MoveMultipleEntitiesCommand` thin (Revit MoveElement/Elements: 2 entry points, ΕΝΑΣ core)· undo=exact snapshot-restore· **DELETE** `move-entity-cascade.ts` (παράλληλο spine — το «Move=ΧΩΡΙΣΤΑ» παραπάνω ΑΝΑΤΡΑΠΗΚΕ: πλέον ΕΝΑ spine για όλα) + `reverseDelta` (dead). Cascade engines άθικτοι. 47 jest. +`undoInPlaceWith` **DELETED** (dead seam μετά το item α). **§8 transform family 100% ΟΛΟΚΛΗΡΩΜΕΝΟ** — μένει ΜΟΝΟ commit (Giorgio) + browser-verify.

**Background-adaptive color+fill — 2D BIM renderers (2026-06-20→22, Opus 4.8, ADR-509):** Το `config/adaptive-entity-color.ts` έχει **δύο** resolvers: `adaptEntityColorForCanvas` (lines, WCAG≥3) + `adaptFillTintForCanvas` (translucent body fill / poché, contrast≥2 alpha≤0.6). **✅ FILLS DONE (2026-06-22, Giorgio FULL SSoT order):** ΟΛΑ τα ~21 translucent BIM body fills περνούν πλέον από NEW `bim/utils/bim-body-fill.ts` (`resolveBimBodyFill` / `adaptBimBodyFill`) → Wall/Column/Beam/Slab/SlabOpening/Stair/Foundation/Roof/ThermalSpace/FloorFinish/WallCovering/ElectricalPanel/MEP(boiler/manifold/radiator/underfloor/water-heater/fitting/fixture/segment)/Furniture/mesh-silhouette/beam-section-profile/column-overlays. Το pattern δεν υπάρχει inline πουθενά (audit-verified). 138/138 jest, tsc clean. **🔴 LINES (strokeStyle) ΠΑΡΑΜΕΝΟΥΝ on-touch:** στο `ctx.strokeStyle = <resolved hex>` → `adaptEntityColorForCanvas(...)` στους ίδιους renderers (πλην Wall/Dxf που έγιναν). **ΓΙΑΤΙ τα lines flagged όχι fixed:** έχουν ήδη contrast ≥ 3 → **no-op σήμερα** (καθαρά future-proofing, χαμηλή αξία)· cross-domain shared tree. **🔴 RATCHET (εγγύηση πληρότητας — το ζητούμενο enterprise κομμάτι):** NEW `.ssot-registry.json` module «bim-body-fill» που απαγορεύει raw translucent `fillStyle = rgba(...0.NN)` / `?? *_FILL` σε `bim/renderers/**` (forbidden pattern → πρέπει `adaptBimBodyFill`/`resolveBimBodyFill`) + `npm run ssot:baseline`. Χωρίς αυτό, νέος renderer αύριο ξεχνά τον helper → το «FULL SSoT» είναι εύθραυστο. Δεν έγινε τώρα: registry touch + baseline = ξεχωριστό pass (budget).

**`closedRingFromEdges` SSoT — closed-ring band idiom (2026-06-20, Opus 4.8, ADR-508) — pre-existing scatter, medium (~7 sites, migrate-on-touch):** Το idiom «κλειστός δακτύλιος από outer + αντεστραμμένο inner» (`[...outer, ...[...inner].reverse()]`) ήταν copy-pasted σε **8 σημεία** χωρίς SSoT. NEW `closedRingFromEdges<T>(outer, inner)` στο `bim/geometry/shared/polygon-utils.ts` (generic, non-mutating). **✅ Χρησιμοποιεί ήδη:** `bim/framing/member-snap-targets.ts` (NEW). **🔴 Προϋπάρχοντα να migrate-αριστούν on-touch:** `bim/renderers/WallRenderer.ts:161`, `bim/renderers/envelope-render-plan.ts:72`, `bim/geometry/building-footprint.ts:117`, `bim/geometry/mep-fitting-bend.ts:167` (μετάλλαζε `inner.reverse()` in-place → το SSoT είναι safe non-mutating), `rendering/hitTesting/hit-test-entity-tests.ts:160` (map→2D, κάλεσε helper μετά το map), `export/core/bim-to-dxf-primitives.ts:68`, `hooks/data/slab-boq-feed.ts:52`. **Fix on-touch:** `import { closedRingFromEdges } from '...geometry/shared/polygon-utils'` + αντικατάσταση του spread. **ΓΙΑΤΙ flagged όχι fixed:** cross-feature (renderer/export/hit-test/boq, shared tree, μερικά Point3D/μερικά Point2D)· migrate όταν τα αγγίξεις. ⚠️ Επίσης το `bim/framing/member-column-flush.ts` έχει local `centroidOf`/`bboxOf` (carried verbatim από το πρώην `beam-column-flush`) που διπλασιάζουν `polygonCentroid`/`polygonBbox` (polygon-utils, Point3D-typed)· documented trade-off (αποφυγή Point3D↔Point2D friction + framing→geometry layering)· migrate μόνο αν λυθεί καθαρά το typing.

**`clearCanvasDpr` SSoT — DPR-clear idiom (2026-06-20, Opus 4.8, ADR-398 §3.5) — pre-existing scatter, small (migrate-on-touch):** NEW `clearCanvasDpr(canvas, ctx?)` στο `rendering/canvas/withCanvasState.ts` (ADR-084 SSoT) ενοποιεί το `setTransform(1,0,0,1,0,0)→clearRect→setTransform(dpr)` idiom. **✅ Χρησιμοποιεί ήδη:** `useCanvasGhostPreview` (19 hooks μέσω αυτού) + `PreviewRenderer.clear()`. **🔴 Προϋπάρχοντα να migrate-αριστούν on-touch:** `PreviewRenderer.render()` (hot path — χρησιμοποιεί cached `this.dpr`· migrate μόνο αν δεν χάνεται perf), `ProposalGhostOverlay.tsx` (έχει δικό του `devicePixelRatioSafe`), `GuideFollowGhostOverlay.tsx`, `canvas-v2/dxf-canvas/dxf-bitmap-cache.ts`, `rendering/canvas/core/CanvasManager.ts`, `ui/components/detail-sheet/DetailSheetDialog.tsx`. **Fix on-touch:** `import { clearCanvasDpr } from '...rendering/canvas/withCanvasState'` + αντικατάσταση του 3-liner. **ΓΙΑΤΙ flagged:** cross-feature (overlay/bitmap/detail), shared tree· κάποια έχουν δικό τους dpr getter (verify equivalent με `getDevicePixelRatio()` πριν).

**Preview-render unification (2026-06-20, Opus 4.8, ADR-398 §3.5) — ✅ ΥΛΟΠΟΙΗΘΗΚΕ (UNCOMMITTED — 🔴 browser-verify+commit):** Οι 2 αποκλίνοντες preview μηχανισμοί + το copy-pasted scaffolding ενοποιήθηκαν (Giorgio order, orchestrator: 4 Explore agents map + 4 impl agents migrate). NEW `systems/preview/ghost-preview-frame.ts` (canonical `{viewport: cached bounds, transform: live}` SSoT) + NEW `hooks/tools/useCanvasGhostPreview.ts` (harness: cursor-gate+RAF+DPR-clear+snapped-cursor). **19 hooks migrated** → ΜΟΝΟ draw delegate (~600γρ scaffolding deleted, οπτικά byte-for-byte). Διορθώνει ΚΑΙ το latent transform-lag (previews→React-prop· τώρα→live `getImmediateTransform`). 9 jest νέα. **ΟΧΙ migrated (by design):** imperative composite path (§3.4 viewport-override) + ProposalGhostOverlay (ήδη live). **🔴 Απομένει:** browser-verify ΟΛΩΝ των ghost tools + pan/zoom με ενεργό ghost (ο transform-lag fix = μόνη συμπεριφορική αλλαγή) → μετά commit (Giorgio). git add: ghost-preview-frame.ts[+test], useCanvasGhostPreview.ts[+test], 19 use*Preview*.ts hooks, ADR-398.

**`createMicrotaskCoalescer` SSoT — proactive-hook coalesce boilerplate (2026-06-18, Opus 4.8, ADR-488) — pre-existing duplicate, medium (~8 hooks, migrate-on-touch):** Το «coalesce-ανά-microtask» boilerplate (`let scheduled=false` + guard + `queueMicrotask(recompute)`) ήταν copy-pasted σε **8 hooks** χωρίς SSoT. NEW `hooks/proactive-coalescer.ts` (`createMicrotaskCoalescer(run)` — pure, 3 jest). **✅ Χρησιμοποιεί ήδη:** `useProactiveStructuralAnalysis` (ADR-488, NEW)· **+ 2026-06-19 (ADR-459 v11): `useProactiveStructuralLoads`, `useProactiveOrganismReinforce`, `useProactiveMemberSizing`, `useAutoFoundationDesign`** — migrate-αρισαν μέσω του NEW `hooks/useGroupedStructuralReaction.ts` (wiring SSoT που υιοθετεί `createMicrotaskCoalescer` + `isGeometryEditTrigger`· το `groupable` flag μένει στο closure του helper). **🔴 Απομένουν on-touch:** `useStructuralOrganism`, `useProactiveTieBeamTieForce`, `useStructuralSettingsRecompute`, `hooks/dimensions/useDimensionCreate`. **Fix on-touch:** `const { schedule } = createMicrotaskCoalescer(recompute)` + διαγραφή του local `scheduled`/`schedule`. **ΓΙΑΤΙ τα υπόλοιπα flagged όχι fixed:** shared tree (άλλος agent στα structural)· κάθε migration θέλει verify ordering μεταξύ hooks· migrate όταν τα αγγίξεις. **✅ Λύθηκε (ίδιο domain):** η classification «άμεση geometry edit» εξήχθη σε SSoT `hooks/structural-geometry-edit-triggers.ts` (`isGeometryEditTrigger`)· οι raw `PROACTIVE_*_EVENTS` subscription lists μένουν per-hook (διαφέρουν legitimately σε loop-guards).

**`projectPointOnAxis` SSoT — entity-wrapper delegation (2026-06-18, Opus 4.8, ADR-493) — small, 1 file, COORDINATE με ADR-492 owner:** NEW pure `projectPointOnAxis(px,py,ax,ay,ux,uy) → {along, perp}` στο `bim/geometry/shared/polygon-utils.ts` (canonical along/perp προβολή σημείου σε άξονα). **✅ Χρησιμοποιεί ήδη:** beam-column cutback framing (ADR-493). **🔴 Να delegate-άρει on-touch:** `projectColumnCenterOnAxis` (`bim/columns/column-face-trim.ts`) έχει inline αντίγραφο του ίδιου τύπου (`{along: rx*ux+ry*uy, perp: Math.abs(rx*uy−ry*ux)}`) — `return projectPointOnAxis(column.params.position.x, column.params.position.y, ax, ay, ux, uy)`. **ΓΙΑΤΙ flagged όχι fixed:** το `column-face-trim.ts` είναι στο **ενεργό set του ADR-492 (άλλος agent, shared tree)** — μην το αγγίξεις χωρίς συνεννόηση· το inline αντίγραφο προϋπήρχε, ΔΕΝ είναι νέο διπλότυπο. Επίσης ο `footprintCentroid` (`column-structural-attach-coordinator.ts`, ADR-492 set) είναι αντίγραφο του `polygonCentroid` (polygon-utils) → ίδιο delegation on-touch.

**`createToggleStore` SSoT — dialog/visibility singleton boilerplate (2026-06-17, Opus 4.8) — pre-existing duplicate, medium (~11 stores, migrate-on-touch):** NEW `createToggleStore()` factory (`subapps/dxf-viewer/stores/createToggleStore.ts`) ενοποιεί το copy-paste boilerplate (`_state`/`_snapshot`/`_subs`/`_notify` + open/close/toggle/isOpen/subscribe/getSnapshot) των απλών `{ isOpen }` singleton stores. **✅ Migrated:** `FloorManagementDialogStore` (ADR-468, NEW). **🔴 Προϋπάρχοντα να migrate-αριστούν on-touch (απλά toggles):** `AdminLayerManagerDialogStore`, `DynamicInputLockStore`, `wall-cascade-delete-store`, `bim-family-type-delete-store`. **ΔΕΝ ταιριάζουν (payload/Promise-handshake — μην τα αγγίξεις):** `column-perimeter-confirm-store`, `edit-{wall,slab,roof,opening}-type-store`, `CommandLineStore`, `CommandHistoryStore`. **Fix on-touch:** `export const X = createToggleStore()` + διαγραφή boilerplate (το public API open/close/isOpen/subscribe/getSnapshot μένει ίδιο). **ΓΙΑΤΙ flagged όχι fixed:** 4 απλά stores σε διαφορετικά features (shared tree)· κάθε migration θέλει verify ότι το getSnapshot reference μένει stable· migrate όταν τα αγγίξεις.

**`detail-3d-capture-core` SSoT — column capture migrate-on-touch (2026-06-16, Opus 4.8) — small, 1 file:** Το ADR-463 Slice 6 εξήγαγε το κοινό offscreen-capture scaffolding (isometric camera tight-fit / concrete prism / `projectNorm` / offscreen render / dispose) σε NEW `bim/structural/detail-sheet/render/detail-3d-capture-core.ts` (το χρησιμοποιεί ο νέος `footing-detail-3d-capture.ts`). Το προϋπάρχον `column-detail-3d-capture.ts` (ADR-457, browser-verified) κρατά ακόμη inline αντίγραφα (`frameCamera`/`isoDirection`/`projectNorm`/`disposeOwned`/`disposeCageGeometry`/`buildConcretePrism`/camera consts). **ΓΙΑΤΙ flagged όχι fixed:** το column capture δεν έχει jest (WebGL) + είναι browser-verified· refactor τώρα = ρίσκο οπτικού regression. **Fix on-touch:** αντικατέστησε inline με imports από το core (ο prism builder θέλει metre-verts arg) + browser-verify.

**BIM Firestore `stripUndefinedDeep` write-sanitize gap (2026-06-16, Opus 4.8) — pre-existing silent-fail, MEDIUM (~15 services, migrate-on-touch):** Το `setDoc`/`updateDoc` με raw `params` πετά «Unsupported field value: undefined» σε nested undefined → **silent catch → η οντότητα ΔΕΝ persist-άρεται ΠΟΤΕ** (root cause: floorplan_columns ήταν 0 docs· κολώνα+σοβάς χάνονταν σε reload). Κανονικό SSoT = `stripUndefinedDeep` (`@/utils/firestore-sanitize`). **✅ Έχουν sanitizer:** wall, stair, **column (NEW 2026-06-16)**, **beam (NEW 2026-06-16 boy-scout)**, **foundation (NEW 2026-06-16 ADR-463 boy-scout — ο οπλισμός πεδίλου χανόταν σε reload: pad χωρίς topMesh / strip χωρίς stirrups → nested undefined throw)**. **🔴 ΛΕΙΠΕΙ (raw `params: input.params`, ευάλωτα):** slab, slab-opening, roof, railing, opening, furniture, floorplan-symbols, floor-finishes, electrical-panel, space-separators, thermal-spaces, mep-segment, mep-fixture, mep-fitting, mep-manifold, mep-radiator, mep-boiler, mep-water-heater, mep-underfloor, mep-system. **Fix per service:** `import { stripUndefinedDeep } from '@/utils/firestore-sanitize'` + `params: stripUndefinedDeep(input.params)` + validation + geometry στο save**+**update (mirror wall/column). **ΓΙΑΤΙ flagged όχι fixed:** 15+ αρχεία (>1h)· τα περισσότερα είναι point-host MEP με απλά params (χαμηλό ρίσκο)· υψηλότερο ρίσκο = foundation/slab/roof/railing (σύνθετα optional params). Boy-scout migrate-on-touch.

**`scalePoints` array-scaler SSoT (2026-06-16, Opus 4.8) — pre-existing inline-map duplicate, medium (~10 sibling sites, migrate-on-touch):** NEW `scalePoints(points, scalar)` στον **κανονικό vector-math SSoT** `rendering/entities/shared/geometry-vector-utils.ts` (array-sibling του υπάρχοντος `scalePoint(point, scalar)`· preserve `z` & extra fields). Αντικαθιστά το `pts.map(p => ({ ...p, x: p.x*k, y: p.y*k }))` idiom (ADR-462 canonical-mm canvas→metres). ⚠️ ΑΡΧΙΚΑ μπήκε λάθος ως `scalePlanXY` σε `bim-three-shape-helpers` (near-duplicate του `scalePoint`)· **consolidated** στο geometry-vector-utils. **Migrated (δικά μου, ADR-462 Phase 3):** bim-three-structural-converters, BimToThreeConverter (wall+clip), foundation-to-three, slab-multilayer-solid-3d, structural-finish-3d, structural-finish-horizontal-3d, column-rebar-3d, pushHoles. **Προϋπάρχοντα inline (να migrate-αριστούν on-touch):** `bim-three-point-converters.ts` (×8 fixture/panel/manifold/radiator/boiler/water-heater shapes), `floor-finish-to-three.ts` (`xyScale` map), `mep-underfloor-to-three.ts` (`scaledVerts`). **ΓΙΑΤΙ flagged όχι fixed:** άλλα ADR domains (ADR-406/408/449 MEP/floor-finish) + shared tree· migrate ανά αρχείο όταν τα αγγίξεις. **Fix:** `import { scalePoints } from '../../rendering/entities/shared/geometry-vector-utils'` + αντικατάσταση του inline map.

**ADR-597 BIM corner-snap 5→1 consolidation (2026-06-11, Opus 4.8) — ✅ DONE (large-duplicate closed):** τα 5 σχεδόν-πανομοιότυπα `{Wall,Beam,Slab,Column,Opening}CornerSnapEngine` (97% structural duplicate) **ενοποιήθηκαν** σε 1 generic `BimCharacteristicSnapEngine` πάνω σε NEW `bim/utils/bim-characteristic-points.ts` SSoT dispatcher (corner+midpoint+center για ΟΛΕΣ τις BIM οντότητες, reuse υπάρχουσα γεωμετρία). 5 engine files + 5 tests διαγράφηκαν. **ColumnCenter collapse ✅ DONE 2026-06-11:** ο `ColumnCenterSnapEngine` (BIM_COLUMN_CENTER) διαγράφηκε + το test του· το column center το παράγει πλέον ο generic `BIM_CENTER` (dispatcher `columnPoints` → centroid μέσω `footprintPoints`). **Selection bugfix 2026-06-11:** «Μέσο/Κέντρο ποτέ» → priority numbers BIM_MIDPOINT/BIM_CENTER 0.5/2.5→−1.7 (ίσα/αρνητικά, νικούν generic) + iteration-order anti-starvation (πάνω από INTERSECTION/ENDPOINT) — βλ. ADR-597 §15. 🔴 browser-verify + commit pending (Giorgio).

**`pointToSegmentDistance` proliferation (2026-06-14, Opus 4.8) — pre-existing duplicate, medium (~6 files, migrate-on-touch):** Κανονικό SSoT = `pointToSegmentDistance` (`systems/guides/guide-types.ts`, exported `systems/guides`), ήδη consumed από `structural-finish-scene`/`building-footprint`/`envelope-column-bridge`/`polygon-interior-point`(NEW, ADR-363/449). **Προϋπάρχοντα private re-implementations** (ίδια μαθηματικά, να migrate-αριστούν on-touch): `bim/walls/wall-attach-pick.ts` (`distPointToSegment`), `bim/thermal/heat-load/wall-footprint-match.ts` (`pointToSegmentDistance`), `systems/dimensions/dim-smart-detector.ts` (`distancePointToSegment`), `bim/renderers/RailingRenderer.ts` (`distanceToSegment`), `rendering/entities/shared/geometry-polyline-utils.ts` (`pointToSegmentDistance`), `bim/finishes/structural-finish-resolver.ts` (`pointSegDistance`). **ΓΙΑΤΙ flagged όχι fixed:** 6 αρχεία cross-domain (wall/thermal/dim/render/finish), shared tree· migrate-on-touch ανά αρχείο. **Fix:** αντικατάσταση με import από `systems/guides` (ή canonicalize σε geometry SSoT αν προτιμηθεί ουδέτερος owner).

**ADR-418 deferred (2026-06-05, Opus 4.8) — view-scale SSoT follow-ups (low priority, migrate-on-touch):**
- (a) **DPI centralization:** ~9 BIM lineweight renderers pass `dpi: 96` inline to `lineweightToPx()` (`WallRenderer/StairRenderer/SlabRenderer/SlabOpeningRenderer/OpeningRenderer/ColumnRenderer/BeamRenderer` + `bim-line-weight-resolver.ts` + `bim-3d/converters/wall-opening-extrude.ts`). Migrate to `SCREEN_DPI` from NEW `config/dpi-config.ts` (ADR-418). Fix when next touching each renderer.
- (b) **`useCurrentSceneModel` relocation:** lives in `ui/text-toolbar/hooks/` but is a generic current-scene selector (ADR-344) now consumed by `systems/zoom/hooks/useViewScale.ts` (cross-feature import). Consider relocating to `systems/levels/` as the canonical selector.

**ADR-363 Slice F residual (2026-06-11, Opus 4.8) — μένει ΜΟΝΟ το transformFootprint unification (medium, phased, dxf-viewer rendering core):** ✅ **#1 dim-divergence FIXED (2026-06-11):** το `columnFootprintDims` έγινε κοινό SSoT σε NEW `bim/columns/column-footprint-dims.ts` (ουδέτερος owner, αποφυγή circular)· `column-anchors` (`anchorLocalPoint`+`localToWorld`, είχαν ΑΜΦΟΤΕΡΑ width/depth bug) τώρα το καλούν → anchor-snap == ορατό footprint για U/composite/polygon. 352/352 column jest + tsc καθαρό. 🔴 browser-verify (U/composite anchor ≠ center). **#2 (active, phased) — 1/9 DONE:** ✅ **Phase 2a column DONE (2026-06-11):** NEW `centredPolyToWorld` batch helper στο `centred-anchor-frame.ts` + `column-geometry.transformFootprint` ενοποιήθηκε (dims από κοινό `columnFootprintDims`, transform μέσω SSoT, διαγράφηκαν `computeLocalBboxCanvas`+`DEG_TO_RAD`+raw cos/sin)· 1145/1145 jest+tsc· ⚠️ stage ADR-040 αν το ζητήσει CHECK 6B/6D· 🔴 browser-verify κάθε column kind. **ΜΕΝΟΥΝ 8 engines** (electrical-panel/floorplan-symbol/furniture/mep-boiler/mep-fixture/mep-manifold/mep-radiator/mep-water-heater) — re-implement shift→rotate→translate με raw cos/sin. **PHASED:** ΕΝΑ-ΕΝΑ με browser-verify ανά οντότητα (visual-critical, οπτικό regression ορατό σε ΟΛΑ τα BIM). Κάθε engine κρατά το δικό του local geometry· μόνο ο transform ενοποιείται μέσω `centredPolyToWorld`.

**ADR-029 mirror drift (2026-06-07, Opus 4.8) — pre-existing PARKING searchableFields divergence (small, ~5min, search domain):** `npm run search-config:sync` αποτυγχάνει σε προϋπάρχον drift (ΟΧΙ από BUG #5): `SEARCH_INDEX_CONFIG[PARKING].searchableFields` = `['number','code']` στο SSoT (`src/config/search-index-config.ts`) vs `['number','type','notes']` στο mirror (`functions/src/search/search-config.mirror.ts`). **Γιατί flagged όχι fixed:** η ευθυγράμμιση αλλάζει ΠΟΙΑ πεδία indexάρονται για parking search (πραγματική αλλαγή συμπεριφοράς)· δεν είναι σαφές ποια κατεύθυνση θέλει ο Giorgio (SSoT canonical κατά ADR-029, αλλά ίσως το mirror `type/notes` είναι το επιθυμητό). **Fix:** ο Giorgio αποφασίζει· μετά align + `npm run search-config:sync` πράσινο.

**ADR-421 SLICE C follow-ups (2026-06-08, Opus 4.8) — opening Family/Type deferred items:**
- (a) **wall→generic command migration (medium, ~1h, shared tree):** NEW generic `UpdateFamilyTypeCommand` + `DeleteFamilyTypeCommand` (`core/commands/entity-commands/`) είναι category-agnostic και τα καταναλώνει το opening. Τα προϋπάρχοντα `UpdateWallFamilyTypeCommand` + `DeleteWallFamilyTypeCommand` είναι structural duplicates — migrate `useWallFamilyTypeController` (+ slab/roof controllers αν θέλει) στα generics και διέγραψε τα wall-specific. **ΓΙΑΤΙ deferred:** shared working tree (wall family-type pending verify/commit άλλου agent)· το opening δεν χρειάζεται να αγγίξει wall.
- ~~(b) cross-floor opening BOQ re-feed~~ ✅ **DONE 2026-06-08 (Opus 4.8)** — βλ. changelog παρακάτω.
- ~~(c) type-aware gating legacy kind/size comboboxes~~ ✅ **DONE 2026-06-08 (Opus 4.8)** — βλ. changelog παρακάτω.

**ADR-408 Φ14 flag (2026-06-08, Opus 4.8) — host-incident scan gap for radiator/boiler (small, ~30min, MEP plumbing domain):** το `collectHostConnectorEndpoints` (`bim/mep-systems/mep-host-connector-endpoints.ts:67`) σκανάρει ΜΟΝΟ `mep-manifold` + `mep-fixture` ως pipe hosts → ένας σωλήνας που τερματίζει σε connector `mep-radiator`/`mep-boiler` ΔΕΝ παράγει host incident → `classifyJunction` το βλέπει ως single-incident → **spurious cap** στο σημείο σύνδεσης (Revit: «the equipment IS the fitting»). **ΔΕΝ αφορά το floor-drain** (=`mep-fixture`, ήδη scanned — γι' αυτό flagged όχι fixed). **Fix:** πρόσθεσε `isMepRadiatorEntity`/`isMepBoilerEntity` στον guard του `collectHostConnectorEndpoints` (το `pointHostMountingElevationMm` τα αναγνωρίζει ήδη). **ΓΙΑΤΙ deferred:** shared tree με codex heating agent (radiator/boiler pending verify/commit)· εκτός scope drainage task.

**ADR-408 Φ15 flags (2026-06-08, Opus 4.8) — κατακόρυφες στήλες αποχέτευσης (riser) v1 follow-ups:** Το v1 παρέδωσε το **engine** (vertical detection `isSegmentVertical`/`riserDirection` στο `mep-segment-types.ts`, 2D plan glyph `mep-riser-symbol.ts`+`MepSegmentRenderer`, geometry fixes στο `mep-segment-geometry.ts` [validator 3D-length αντί plan-length + degenerate-XY outline guard], default-3m/DN100 1-click tool `useMepRiserTool` reachable από ribbon «Κατακόρυφη Στήλη»). Deferred (μεγάλα, χωριστή φάση):
- (a) ✅ **DONE 2026-06-08 (Opus 4.8, Plan Mode) — Cross-floor «riser through» 2D σύμβολο (Task B, Revit «cut plane»):** στον ενεργό όροφο derived glyph (κύκλος+up/down) για κάθε riser άλλου ορόφου που διαπερνά το FFL (single source=base-floor risers, zero duplicate). FULL SSOT: NEW pure `bim/mep-segments/derive-risers-through-floor.ts` (`deriveRisersThroughFloor`, owner-exclusion γεωμετρικά) + NEW read-only `components/dxf-layout/RiserThroughOverlay.tsx` (mount CanvasLayerStack, mode==='2d' any scope) + NEW glyph draw SSoT `drawRiserSymbol` (shared owner-renderer+overlay) + **Boy-Scout extract `hooks/data/useBuildingFloorScenes.ts`** (cross-floor SceneModel sourcing, 2 consumers: useFloors2DUnderlay convert + riser overlay filter· useFloors2DUnderlay τώρα thin wrapper). STAGE ADR-040 (CHECK 6B/6D). tsc 0 δικά μου· 68/68 jest (νέο derive test + useFloors2DUnderlay regression). 🔴 pending browser verify + commit (Giorgio).
- (b) ✅ **DONE 2026-06-08 (Opus 4.8) — Height/floor placement UI (Revit base/top constraint):** contextual tab «Κατακόρυφη Στήλη» (trigger `mep-drain-riser`) με combobox «Έως όροφο» (dynamic floors-above από `useFloorsByBuilding`+datum SSoT· base=ΤΡΕΧΩΝ όροφος) + «Διάμετρος» DN. FULL SSOT μηδέν fork: bridge-store handle `heightMm/setHeight`→`baseElevationMm/topElevationMm/setSpanMm`· NEW `mep-riser-command-keys`/`contextual-mep-riser-tab`/`useRibbonMepRiserBridge` (mirror fixture-library)· wiring useRibbonCommands/useDxfBimBridges/useDxfViewerRibbon/ribbon-contextual-config[shared]· i18n el+en. ΕΚΤΟΣ ADR-040 (pure ribbon/bridge). tsc 0 δικά μου· 55/55 mep-segments jest. 🔴 pending browser verify + commit (Giorgio).
- (c) **Εξαερισμός (vent stack), διαστασιολόγηση DN από DFU, building-drain/outfall terminal πέρα από το φρεάτιο, BOQ/ΗΛΜ ποσότητες στηλών** — επόμενα drainage features.
- (d) ✅ **Verify (browser) Task B DONE 2026-06-08 (Giorgio, βήμα-βήμα):** cross-floor «riser through» — στον 2ο όροφο εμφανίζεται το derived glyph (βέλος up) στο ίδιο XY με τον owner (1ο)· σταθερό μέγεθος σε zoom (Revit annotation)· μηδέν duplicate. Glyph radius 9→**12px** (Giorgio). 🔴 ΜΕΝΕΙ verify: riser→horizontal drain auto tee/elbow + bottom snap σε φρεάτιο. Selection precision του point-glyph (hitTest = tolerance×3· Giorgio ανέφερε «δεν μετακινείται» — pending follow-up).

**2D ANNOTATION SCALE (paper/plot scale) — ΓΕΝΙΚΟ feature (2026-06-08, Opus 4.8· flag από Giorgio verify Φ15):** Όλα τα 2D MEP annotations είναι σήμερα **screen-space σταθερά px** (`RISER_SYMBOL_RADIUS_PX=12`, `PIPE_TICK_HALF_PX=7`, slope indicators «Screen», + πιθανώς dimension text/glyphs) — συνεπές μεταξύ τους, σωστό για on-screen navigation. **ΓΙΑΤΙ flag:** όταν φτιαχτεί **plot-to-scale** της κάτοψης (σήμερα ΔΕΝ υπάρχει — το μόνο PDF export είναι schedules/πίνακες, `bim/schedule/exporters/`), τα σύμβολα πρέπει Revit-grade να έχουν σταθερό μέγεθος **στο χαρτί** (mm στο sheet) → κλιμάκωση αντίστροφη με την κλίμακα view/plot (π.χ. A1@1:50 → πάντα ίδιο φυσικό μέγεθος). **Fix (μεγάλο, ξεχωριστή φάση/Plan Mode):** NEW κεντρικό `annotationScale` SSoT (paper-mm → device-px ανά view scale, reuse ADR-418 view-scale + DPI config) που καταναλώνουν **ΟΛΑ** τα annotation builders (riser/ticks/slope/dims) — ΟΧΙ per-symbol fork. **ΓΙΑΤΙ deferred (Giorgio):** plot-to-scale δεν υφίσταται ακόμη· γενικό cross-cutting· χαμηλή προτεραιότητα. [[project_adr418_view_scale]]

**ADR-408 Φ14 flags (2026-06-08, Opus 4.8) — sanitary fixtures follow-ups (out of «αποχέτευση» scope):**
- (a) **Legacy sanitary floorplan-symbol → mep-fixture migration (small-medium, data migration, plumbing domain):** η A1 (Revit-true) κάνει τα WC/νιπτήρα/ντουζιέρα/μπανιέρα/μπιντές connectable `mep-fixture` kinds· τα παλιά 2D-only `floorplan-symbol` (category `sanitary`) έμειναν **leave-legacy** (συνεχίζουν να renderάρουν μέσω catalog/drawers, αλλά ΔΕΝ είναι connectable — το `collectHostConnectorEndpoints` διαβάζει το store, όχι το render model, οπότε render-only remap θα ήταν παραπλανητικό half-state). **Fix όταν/αν υπάρχουν πραγματικά persisted δεδομένα:** one-time cross-collection Firestore script `floorplan-symbol[category=sanitary]` → `mep-fixture` (per-type collections· enterprise-id `mfix`/υπάρχον). **ΓΙΑΤΙ deferred:** app σε DEVELOPMENT MODE + τα sanitary symbols 4 ημερών (2026-06-04) → ~0 δεδομένα· cross-collection write ρίσκο σε shared tree.
- (b) **Water-supply connectors (cold/hot inlet) σε WC/νιπτήρα (medium, ύδρευση domain):** v1 = drain-only (1 sanitary-drainage outlet). Πλήρες Revit plumbing θέλει και παροχή κρύου/ζεστού. **ΓΙΑΤΙ deferred:** ανήκει στον codex ύδρευση agent (shared tree)· εκτός scope «αποχέτευση». Το `MepFixtureParams` extends `MepConnectorHostParams` (πολλαπλοί connectors) → non-breaking να προστεθεί.
- (c) **Trap/οσμοπαγίδα (small, 3D plumbing detail):** μηδενική υποδομή· εκτός scope 2D modeler. Future Revit-full plumbing.

**ADR-399 datum flag (2026-06-07, Opus 4.8) — duplicate floor-elevation default (small, ~30min, building-management domain):** η default elevation ενός ορόφου υπολογίζεται σε ΔΥΟ σημεία: `computeSmartElevation` (`components/building-management/tabs/FloorInlineCreateForm.tsx`, neighbour-aware) και `computeDefaultElevation` (`components/building-management/tabs/useFloorsTabState.ts`, απλό `number×3`). SSoT violation. **Fix:** extract κοινό `resolveDefaultFloorElevationM` (ιδανικά σε `utils/floor-naming.ts` δίπλα στο `DEFAULT_FLOOR_HEIGHT_M`) + drop το local `DEFAULT_STOREY_HEIGHT=3.0` copy. **ΓΙΑΤΙ flagged όχι fixed:** εκτός domain του 3Δ datum task (shared tree)· δεν επηρεάζει τη Revit-grade λύση (που στοιβάζει datum-relative ανεξάρτητα από το πώς γεννήθηκε το elevation).
**Last updated:** 2026-06-08 — **ADR-408 Φ15 Task B «cross-floor riser through» DONE (Opus 4.8, Plan Mode, ~1h, 2D overlay domain)**: derived riser glyph σε κάθε όροφο που διαπερνά μια στήλη άλλου ορόφου (Revit «cut plane»). **Boy-Scout extract `useBuildingFloorScenes`** (κοινό cross-floor SceneModel sourcing) — κάλυψε το dedup ανάμεσα σε `useFloors2DUnderlay` (convert) + νέο riser overlay (filter). **ΣΗΜΕΙΩΣΗ (μερική κάλυψη):** το αρχικό 2026-05-30 flag ζητούσε dedup ΚΑΙ με το `useFloors3DAggregator` — αυτό μένει **χωριστό** (διαφορετικό return shape: per-category BIM bundles + `floorElevationMm` αντί raw `SceneModel`)· χαμηλή προτεραιότητα, αγγίζει unverified ADR-399 Phase B 3D pipeline. NEW pure `derive-risers-through-floor.ts` + `RiserThroughOverlay.tsx` + `drawRiserSymbol` SSoT. STAGE ADR-040 (CHECK 6B/6D). tsc 0 δικά μου· 68/68 jest. Pending commit (Giorgio· shared tree). **Previous:** 2026-06-08 — **ADR-421 SLICE C follow-up «cross-floor opening BOQ re-feed» DONE (Opus 4.8, ~1h, BOQ domain)**: Edit Τύπου κουφώματος → το signature-group BOQ όλων των ορόφων του κτιρίου ενημερώνεται project-wide (Revit-grade). **Απόφαση Giorgio «όπως η Revit, FULL ENTERPRISE + FULL SSOT» → BOQ-only effective-aware recompute, ΧΩΡΙΣ doc re-persist** (ο Τύπος = single source of truth· το BOQ = derived view· geometry self-heals lazily στο load μέσω «type wins» hydration — όπως τα Revit schedules είναι live views πάνω στο model). **Recognition διόρθωσε το handoff:** (1) τα openings ζουν ΜΟΝΟ στο `FLOORPLAN_OPENINGS` (όχι στο `loadFileV2` scene όπως wall/slab) → fan-out καθαρά-Firestore· (2) `kind` type-governed → type edit door→window σπάει το naive `WHERE kind==sig.kind`· (3) re-persist params-only ΘΑ ΕΣΠΑΖΕ το load-time geometry self-heal· (4) stale `doc.params` = OLD signature δωρεάν· (5) composite index `companyId+projectId+floorplanId` υπάρχει → κανένα νέο index. **Scope:** 2 NEW pure helpers `buildEffectiveSignatureMembers`/`collectAffectedSignatures` (+`OpeningDocRow`/`OpeningEffectiveResolver`) στο `opening-boq-grouper.ts` (resolve injected → μένει pure)· `opening-boq-sync.ts` refactor split `writeSignatureGroup` (ένα write primitive, detach-guard/delete-empty/createdAt SSoT) + NEW `fetchAllOpeningsForFloorplan` (no-kind query) + NEW export `refeedOpeningBoqForTypeOnFloorplan` (effective-aware: old shrink + new grow + cross-type counting)· NEW pure-Firestore fan-out module `opening-boq-side-effects.ts` `refeedOpeningBoqForTypeAcrossFloors` (floorplanId=level.sceneFileId, injectable recompute, per-floor failure isolated)· `useFamilyTypeBoqRefeed` host `opening` branch (καλύπτει active+non-active ομοιόμορφα, idempotent, διορθώνει και το active-floor non-selected gap). 5 code + 2 test (49/49 PASS: grouper effective + side-effects fan-out + family-type-side-effects regression). tsc 0 δικά μου. ΜΗΔΕΝ writes σε opening/audit collections (μόνο BOQ rows). ΕΚΤΟΣ ADR-040 (host = 1 EventBus listener). Pending commit (Giorgio· shared tree). **Previous:** 2026-06-08 — **ADR-421 SLICE C follow-up «auto-type-on-create/load» DONE (Opus 4.8, ~30min, opening creation/persistence domain)**: το ΗΔΗ pure `resolveAutoOpeningTypeId` wire-άρεται στα 2 lifecycle points (Revit «never a type-less door/window»), ακριβώς όπως ο τοίχος: (1) creation `opening-completion.ts §buildOpeningEntity` (μετά `createOpening`)· (2) load `opening-doc-hydration.ts §openingDocToEntity` (`doc.typeId ?? resolveAutoOpeningTypeId(doc.params)` — legacy untyped default-διάστασης κουφώματα self-heal σε typed στο reload, non-destructive: built-in match → effective==cached → μηδέν geometry change· explicit `doc.typeId` υπερισχύει). Νέο/legacy default κούφωμα → typed → το gating (προηγ. follow-up) ενεργοποιείται φυσικά → πλήρης Revit «place→auto-type→edit via Type». Recognition prereq επιβεβαιώθηκε: opening built-ins ΗΔΗ seeded (`getAllBuiltInTypes` περιλαμβάνει `getBuiltInOpeningTypes`). Giorgio AskUserQuestion → Creation+Load (Revit parity). 2 code MOD + 1 NEW test `auto-opening-type-wiring.test.ts` (5/5 PASS· 24+105 regression PASS). tsc 0 δικά μου. ΕΚΤΟΣ ADR-040. ΟΧΙ IFC-import path (δικά του type semantics). Pending commit (Giorgio· shared tree). **Previous:** 2026-06-08 — **ADR-421 SLICE C follow-up «type-aware gating» DONE (Opus 4.8, ~30min, bridge domain)**: σε **typed** opening (`opening.typeId` set) τα ribbon comboboxes **Kind / Width / Height** γίνονται read-only (Revit-style — value ορατό, edit μέσω «Edit type»)· untyped openings μένουν πλήρως editable (zero regression). Κλείνει silent-edit-loss bug (direct `params` edit χανόταν στο επόμενο «type wins» re-resolution/reload). **Approach B** (thread `disabled` flag μέσα από `RibbonComboboxState`, minimal framework change): (1) NEW SSoT `OPENING_TYPE_GOVERNED_COMBOBOX_KEYS` + `isOpeningTypeGovernedComboboxKey` στο `opening-command-keys.ts` (kind/width/height· instance-owned sillHeight/handing/openDirection/mark ΠΟΤΕ gated), (2) `RibbonComboboxState += disabled?: boolean` (generic — άλλα bridges το παραλείπουν → editable ως τώρα), (3) `RibbonCombobox.tsx` `disabled={comingSoon || dynamicState?.disabled===true}`, (4) `useRibbonOpeningBridge.getComboboxState` επιστρέφει `disabled` + **defense-in-depth** no-op guard στο `onComboboxChange` (gated field σε typed opening → δεν dispatch-άρει `UpdateOpeningParamsCommand`, ακόμη κι αν παρακαμφθεί το UI). 5 αρχεία (1 NEW test 7/7 PASS), tsc 0 δικά μου. ΕΚΤΟΣ ADR-040 (μόνο ribbon context/combobox/bridge — όχι canvas/renderer). Pending commit (Giorgio· shared tree). **Previous:** 2026-06-03 — **ADR-377 Phase F DONE → ADR-377 ALL PHASES COMPLETE (Opus 4.8, ~1h)**: η τελευταία φάση του Subcategories system. RECOGNITION (N.0.1) έκοψε scope ~3-5h→~1h — το stub badge ήταν ΗΔΗ shipped στο Phase D (`SubcategoryRow.tsx` 🔒). **Ratchet deliverable:** NEW `.ssot-registry.json` Tier 3 module `bim-subcategories` (canonical `config/bim-subcategories.ts` + `bim-line-patterns.ts`) — forbid re-declaration `SUBCATEGORY_TAXONOMY`/`WIRED_SUBCATEGORIES`/`isWiredSubcategory`/`getAllSubcategoryKeysForCategory` + line-pattern catalog (`BIM_LINE_PATTERNS`/`BUILT_IN_DASH_ARRAYS`/`linePatternToDashArray`) εκτός 2 allowlisted canonical (`export const`/`function` declaration form — re-exports OK). **ΜΑΘΗΜΑ**: τα 3 SSoT consts έχουν type-annotation ανάμεσα name↔`=` → το συνηθισμένο style-pattern `(=|:)\s*{` ΔΕΝ τα πιάνει· declaration-form pattern (όπως firestore-now). Patterns verified real `grep -E` = ΜΟΝΟ allowlisted (zero collateral)· registry-golden 56/56 PASS· **zero νέες violations → `.ssot-violations-baseline.json` αμετάβλητο** (το `ssot:baseline` έβγαζε μόνο timestamp churn → **reverted** N.0.2). + NEW test-only `dxf-bitmap-cache-subcategory-invalidation.test.ts` (5, ADR-040 cache-bust verify μέσω public `rebuild()`/`isDirty()`, ΔΕΝ αγγίζει τον source). tsc 0. Pending commit (ο Giorgio). 🔴 `adr-index.md` pending (shared tree άλλου agent). **Previous:** 2026-06-03 — **ADR-364 Escape Bus Boy-Scout Group 4 DONE (Opus 4.8 ανάλυση + Sonnet 4.6 εκτέλεση)**: 10 secondary components migrated off local `e.key==='Escape'` / window keydown listeners → centralized `escapeBus` (PropertiesPalette[auto-committed 65c65ee6] + QuickPropertiesMiniPanel via `useEscapeHandler`; DimStyleCreateDialog + LayerStateDropdown → rely on Radix `onEscapeKeyDown`; LayerItem/ColorGroupItem/LayerStateDropdownPopover/LayerStateManagePanel(+ManageRow lifted)/TextOverrideEditor/StairPresetsSection via bus με canHandle+allowWhenEditable). GripContextMenu/useGripContextMenuController OUT (contextmenu only, no keydown-Escape). **Ratchet impact:** `.ssot-violations-baseline.json` 149→129 violations (−20), 99→88 files — monotonic decrease, μόνο αφαιρέσεις (καμία ξένη παράβαση blessed σε shared tree). tsc 0, EscapeCommandBus 24/24 PASS. 🔴 browser verify rename/popover ESC UX. **Previous (same day):** **ADR-376 residual registry entries CONFIRMED DONE (Opus 4.8, doc-only closure)**: Τα 3 `.ssot-registry.json` entries που έμεναν "pending μέχρι working tree clean" είναι **ΗΔΗ committed** (addedDate 2026-05-26): `opening-boq-grouper` (Tier 3, 5 forbiddenPatterns, allowlist 3 αρχεία), `opening-tag-style` (Tier 3, 3 patterns), `opening-tag-drag-controller` (Tier 3, 4 patterns). Επαληθεύτηκαν παρόντα + valid JSON + canonical αρχεία υπάρχουν. Working tree πλέον clean. ΕΚΚΡΕΜΟΤΗΤΕΣ ΑΟ8+ΑΡ15 → ✅ ΥΛΟΠΟΙΗΜΕΝΟ. **Καμία αλλαγή κώδικα/registry — μόνο tracker closure** (το tracking ήταν stale· η εργασία είχε ολοκληρωθεί από προηγούμενο agent). **Previous:** 2026-05-30 — **ADR-399 Phase D 2Δ underlay DONE (Opus 4.8) + NEW pending-ratchet flag (dedup per-floor scene sourcing)**: Phase D υλοποιήθηκε (read-only 2Δ underlay «Όλοι οι όροφοι», `FloorUnderlayOverlay` + `useFloors2DUnderlay`, 5/5+16/16 tests, pending commit, 🔴 browser verify). **ΕΚΚΡΕΜΟΤΗΤΑ (N.0.2, large-duplicate, ~1-2h):** το per-floor scene sourcing — enumerate building floors + resolve scene via `getLevelScene` (visited) ή `DxfFirestoreService.loadFileV2(sceneFileId)` snapshot — υπάρχει **διπλό** σε `hooks/data/useFloors3DAggregator.ts` (BIM entities, 3Δ) και `hooks/data/useFloors2DUnderlay.ts` (full DxfScene, 2Δ). **Fix:** extract κοινό `useBuildingFloorScenes(active)` SSoT (per-floor `{levelId, floorElevationMm, scene: SceneModel}`)· 3Δ→extract BIM entities, 2Δ→`convertSceneToDxf`. **ΓΙΑΤΙ αναβάλλεται:** ADR-399 Phase B/C/D είναι pending + ΜΗ browser-verified → refactor τους τώρα ρισκάρει unverified κώδικα. **ΠΟΤΕ:** μετά το browser-verify B/C/D από Giorgio. — **Previous:** 2026-05-27 — **ADR-391 AdminLayerManager Ribbon Wiring DONE (Sonnet 4.6, ~1h, orphan-component bug closed)**: AdminLayerManager (~400 lines fully implemented) ήταν unreachable από production — zero ribbon button, zero mount, zero shortcut. ADR-382 verification αποκάλυψε ότι ο user δεν είχε UI να toggle `layer.visible/frozen/color/lineweight`. **Decisions Q1-Q4 (locked Γιώργος 2026-05-27)**: Q1 Modal Radix Dialog (Revit V/G parity, όχι floating/4th-tab), Q2 View tab (industry: Revit View > Layer Manager), Q3 Ctrl+L (consume dead `toggleLayers` shortcut — `LA` ήταν booked από `layering` paint-tool, ζητούμενο zero-breakage), Q4 ADR-391 (επόμενο free; ADR-381 §3 pre-allocation για "ribbon-auto-registry" → αναβάλλεται σε ADR-392). **Scope**: 5 NEW (AdminLayerManagerDialogStore singleton + AdminLayerManagerDialog Radix wrapper + AdminLayerManagerDialogHost mount + view-tab-layer-manager.ts ribbon panel + ADR-391 doc) + 7 MODIFIED (dxf-viewer-lazy-components.tsx lazy export + ribbon-default-tabs.ts View panels array + DxfViewerContent.tsx Suspense mount + useDxfViewerCallbacks.ts `action: 'open-layer-manager'` branch + useKeyboardShortcuts.ts Ctrl+L wiring + el/en dxf-viewer-shell.json i18n keys). **SSoT alignment 5/5**: Dialog `@/components/ui/dialog` (ADR-001), store pattern mirror CommandLineStore, action dispatcher `wrappedHandleAction` (ADR-345), keyboard shortcut SSoT `keyboard-shortcuts.ts` (zero new declaration — consumes dead one), LayerStore SSoT (zero touch). **ADR-382 chain**: όταν user toggle-άρει `layer.visible` στο dialog → LayerStore version bump → ADR-382 resolver chain → 2D+3D auto-refresh (zero extra wiring). Pending commit. Manual browser verify: Ctrl+L → Dialog opens → toggle 👁 BIM layer → 2D walls hide → switch 3D → 3D walls also hide. **Previous:** 2026-05-27 — **ADR-382 Visibility Resolver SSoT Phase C+D+E DONE (Opus 4.7, ~2h, production bug closed)**: ADR-381 §2.5 C1 critical finding fully implemented. Phase A (Haiku, 1519a4be) + Phase B (Opus, 4edbb62a) ήδη committed (resolver + 7/7 BIM 2D renderers). Phase C+D+E σε αυτή τη συνεδρία. **Root cause**: `BimSceneLayer.sync()` (3D) έκανε category-level guard (`if (wallVisible) { for wall of walls {...} }`), δεν consult-άρει LayerStore — hidden BIM layer στο Layer Manager έκρυβε 2D entities αλλά παρέμεναν ορατά στο 3D. Selection μηχανισμοί σέβονταν `layer.visible` → φαντασματική συμπεριφορά (ορατά αλλά μη-επιλέξιμα). **Phase C scope (3D pipeline)**: `BimSceneLayer.ts` rewrite — per-entity loop με `resolveEntity()` helper που καλεί `resolveIsEntityVisible()` (V/G + Layer + Floor + Building intersection, ANY-hides-wins Revit-style). Split σε 5 private syncCategory methods + 2 hosted-opening filters (function-size compliant ≤40 lines). Νέο 8ο argument `floorVisModes: ReadonlyMap<string, FloorVisMode>` propagated end-to-end: `ThreeJsSceneManager.syncBimEntities` + `scene-manager-actions.SyncBimEntitiesArgs` + 4 BimViewport3D call-sites + `use-bim3d-vg-resync`. `use-bim3d-store-sync.ts` (a) floorVisibilityModes subscriber αναβαθμίστηκε από `applyFloorVisibility-only` σε `syncBimEntities + applyFloorVisibility` (defense-in-depth: pre-mesh hide + post-hoc ghost styling), (b) νέος `subscribeLayerStore()` subscriber με version-based dedup → toggle layer.visible/frozen στο Layer Manager τώρα triggers 3D rebuild → resolver pre-mesh filter (closing 2D⟷3D parity bug). `applyFloorVisibility/applyBuildingVisibility` doc comments updated με role split. Per-entity hosted-opening filter (Wall→Opening, Slab→SlabOpening) σέβεται V/G+Layer+Floor+Building για κάθε opening separately. **Phase D scope (docs+registry)**: `.ssot-registry.json` νέο `visibility-resolver` Tier 2 module — forbids direct `objectStyles[cat]?.visible` reads outside resolver internals (1 ERE pattern για 7 BIM categories). ADR-382 status header 🟡 PLAN APPROVED → 🟢 PHASE A+B+C+D+E COMPLETE + 5 changelog entries. ADR-381 §3 C1 entry → ✅ COMPLETE με link σε ADR-382. ADR-040 changelog: ADR-382 micro-leaf compliance note (event-time resolver calls, pre-mesh filter, low-freq LayerStore subscriber). ΕΚΚΡΕΜΟΤΗΤΕΣ νέα ΟΜΑΔΑ ΑΥΘ + C1 → ✅ ΥΛΟΠΟΙΗΜΕΝΟ. adr-index ADR-382 entry. **Phase E scope (verify)**: `resolveIsCategoryVisible` παραμένει exported για backward compat. TSC --noEmit clean για ADR-382 paths (2 pre-existing errors σε route.ts + incremental-backup σχετίζονται με ADR-380 stair audit pending commit, ΟΧΙ Phase C). **Tests 54/54 PASS** (27 resolver unit + 10 V/G regression + 17 Phase C integration matrix: 4 Layer + 4 Floor + 2 Building + 4 Intersection + 3 Hosted-Openings). **Scope total Phase C+D+E**: 1 NEW test + 9 MODIFIED (BimSceneLayer/scene-manager-actions/ThreeJsSceneManager/BimViewport3D/use-bim3d-vg-resync/use-bim3d-store-sync/applyFloorVisibility/applyBuildingVisibility/.ssot-registry.json) + 4 DOCS. Manual browser verification PENDING — Giorgio να ανοίξει /dxf/viewer, να κρύψει BIM layer στο Layer Manager, να switch σε 3D, να verify entities εξαφανίζονται και στους δύο. **Previous:** 2026-05-27 — **ADR-390 Symmetric BIM Delete/Undo Persistence DONE (Opus 4.7, Phase 1+2+3 all complete)**: Sibling του ADR-381 master audit (separate concern — naming collision retroactively renamed). Δύο bugs live verified: (A) Zombie write στο undo — Ctrl+Z μετά BIM delete ξανάγραφε ίδιο UUID + audit 'created' (όχι 'restored'), root cause `DeleteEntityCommand.undo()` restore-d μόνο scene → auto-save effect έπαιρνε stale `lastSaved=undefined` → debounced persist zombie. (B) Ghost render μετά hard refresh — BIM entities visible στο canvas χωρίς Firestore docs, root cause dual persistence (per-entity + DXF JSON `autoSaveV2`) + subscribe-loop `neverSaved` guard που επέστρεφε true για fresh `lastSavedParamsRef` Map. Industry 7/7 (Revit/AutoCAD/ArchiCAD/Bentley): undo of delete restores scene+DB+audit('restored'). **5 architectural decisions**: D1 single generic event `'bim:entity-restore-requested'` (type-discriminated payload, 7 entityTypes), D2 audit action `'restored'` (route allowlist + UI ήδη υπήρχε με RefreshCw icon), D3 `pendingFirstSaveIdsRef` αντικαθιστά buggy `neverSaved` guard → Bug B fixed, D4 auto-save effect early-return `if (!known && !pending) return` → Bug A defense-in-depth, D5 SSoT shared hook `useBimEntityRestoredPersistEffect` mirror του `useBimEntityMovedPersistEffect` αποτρέπει 7-way drift. **Scope**: 2 NEW (`useBimEntityRestoredPersistEffect.ts` SSoT + `ADR-390-symmetric-bim-delete-undo.md` ~280 lines, 9 sections + §Historical Note) + 17 MODIFIED (EventBus + `DeleteEntityCommand` single+multi undo + audit route VALID_ACTIONS + 7 audit-clients με `'restored'` action + 7 persistence hooks με `pendingFirstSaveIdsRef`+`deletedIdsRef`+`persistRestore` callback) + 2 NEW test suites (DeleteEntityCommand.test.ts 9/9 PASS + useBimEntityRestoredPersistEffect.test.ts 7/7 PASS = 16/16 PASS Phase 3). Phase 4 (BIM filter από DXF JSON `autoSaveV2`) DEFERRED — orthogonal, Bug B fully fixed χωρίς αυτό. **Historical commit-tag note**: 5 commits (`f6d81500` → `4a927853` 2026-05-27) mistagged "ADR-381 Phase A" — pre-allocation labelling πριν διαπιστωθεί η σύγκρουση με ADR-381 master audit doc (ίδια date). Functionally identical, βλ. ADR-390 §Historical Note. ΕΚΚΡΕΜΟΤΗΤΕΣ ΟΜΑΔΑ ΑΥΗ + adr-index + memory updated. Pending commit Phase 3 bundle. **Previous:** 2026-05-27 — **ADR-040 Phase XXIII DONE (Opus 4.7, BIM 3D rAF unification SSoT)**: Firefox profile σε 2D wheel-zoom + BIM slab visible έδειξε `Window.requestAnimationFrame` self-time 17% (27 samples / 21s). Root cause: **ΔΥΟ ανεξάρτητα persistent rAF loops** — `UnifiedFrameScheduler` (master 2D) + `ThreeJsSceneManager.startLoop()` (BIM 3D), παράλληλα, χωρίς dirty-check. Παράβαση ADR-040 Cardinal Rule #1 + half-finished ADR-366 Phase 4.2 ("single RAF coordination" comment σε 3 modules αλλά ο master scene loop ποτέ δεν αφαιρέθηκε). Industry alignment 4/4 (Forge Viewer SDK / Three.js Editor / iModel.js / AutoCAD Web — single master rAF + per-subsystem dirty-check + on-demand render). **FIX**: 1 NEW pure-function helper (`scene-dirty-state.ts` + 7-test suite) + 8 MODIFIED. `ThreeJsSceneManager`: removed `startLoop()` / `rafHandle` / `lastFrameTime`, added public `tick(now, delta)` + `isSceneDirty()` + `markSceneDirty()`, cached `RenderFrameContext` once (was rebuilt per rAF call), 11 mutation sites self-mark dirty. `scene-dispose.ts`: dropped `rafHandle` dep + cancelAnimationFrame call. `scene-setup.ts`: exposed `onRenderNeeded` (was no-op) — wired to `markSceneDirty()` so OrbitControls `dampingFactor=0.25` inertia continues to drive renders for ~300ms post-pointer-up without keeping a continuous rAF alive. `BimViewport3D.tsx`: registers `'bim-3d-scene'` system με `UnifiedFrameScheduler.register(...)` στο mount useEffect, unregister BEFORE `manager.dispose()` (race-free teardown). `.ssot-registry.json` νέο entry `frame-scheduler-master-raf` (Tier 3, forbidden pattern `this\.rafHandle\s*=\s*requestAnimationFrame`, allowlist UnifiedFrameScheduler.ts). ADR-040 §"Phase XXIII — Single rAF SSoT Consolidation (BIM 3D)" + ADR-366 Phase 4.2 closure note (cross-ref to ADR-040 XXIII). tsc PASS zero new errors. Expected profile delta: rAF self time 17% → ~8-10% (halved). **Previous:** 2026-05-27 — **ADR-380 Stair + Slab-Opening Audit Coverage DONE (Sonnet 4.6, ~1h, ADR-379 ολοκλήρωμα)**: Δύο BIM entity types εκτός ADR-379 scope: (A) `stair` — ZERO audit coverage (no client, missing από `AuditEntityType` union, missing από route `VALID_ENTITY_TYPES`). (B) `slab-opening` — partial coverage με `[{ field: 'kind' }]` placeholder pattern (χωρίς diffTrackedFields SSoT helper, χωρίς prevParams, καμία test suite). **FIX**: 4 NEW (stair-audit-client.ts + 2 test suites + ADR-380) + 6 MODIFIED (audit-trail types + route.ts + audit-tracked-fields.ts STAIR_TRACKED_FIELDS ~28 fields + SLAB_OPENING_TRACKED_FIELDS 8 fields + slab-opening-audit-client.ts refactor + use-stair-persistence.ts wiring + useSlabOpeningPersistence.ts prevParams) + `.ssot-registry.json` bim-audit-helpers +2 forbidden patterns (buildStairChanges + buildSlabOpeningChanges) + addedByAdr "ADR-379, ADR-380". 7/7 BIM entities πλέον audit-covered. Pending commit bundled με ADR-379. **Previous:** 2026-05-27 — **ADR-379 BIM Entity Audit Coverage DONE (Opus 4.7, ~3h, two root-cause fix)**: (A) `/api/audit-trail/record` route.ts ownership check έτρεχε `entityDoc.exists` ΜΕΤΑ το client-side delete → 404 → silent swallow στον client → **κανένα BIM delete audit row δεν γραφόταν** για τα 5 entity types. (B) 5 BIM audit-clients (wall/column/slab/beam/opening) δέχονταν μόνο `Pick<Entity, 'id'|'kind'>` και έγραφαν `[{ field: 'kind', oldValue, newValue }]` placeholders — zero dimensional content. Property/Building/Contact entities χρησιμοποιούν `diffTrackedFields()` SSoT σωστά εδώ και καιρό — BIM ήταν το outlier. **FIX**: 2 NEW (bim-audit-helpers.ts SSoT για create/update/delete diff builders + ADR-379) + 12 MODIFIED. Route bypass: `if (!entityDoc.exists) { if (action !== 'deleted') throw 404 }` με defense-in-depth caller-companyId tagging. 5 νέα tracked-fields registries (WALL/COLUMN/SLAB/BEAM/OPENING) στο audit-tracked-fields.ts (~70 fields total, coordinate-heavy fields εξαιρούμενα). 5 audit-clients refactored σε νέα signature `recordXxxChange(action, snapshot, { prevParams })`. 5 persistence hooks captures `prevParams` πριν το save + full snapshot σε delete. Skip-on-no-diff για updates (debounced auto-save fires σε identical params συχνά). **27/27 tests PASS** (12 helper + 15 audit-client). Route integration test skipped (Next/server Request polyfill issue στο SWC test env) — manual verify documented σε ADR-379 §4. ΕΚΚΡΕΜΟΤΗΤΕΣ ΟΜΑΔΑ ΑΥΔ + adr-index ADR-379 entry + memory updated. Pending commit. **Previous:** 2026-05-27 — **ADR-040 Phase XXII.C DONE (Opus 4.7, ~1h, TransformContext duplicate SSoT removed)**: Production-profile-driven follow-up στο XXII.A. Profile inspection αποκάλυψε ότι (a) post-XXII.A παρέμενε ~70% React reconciliation στο wheel-zoom path με 14% concentrated σε component label `RibbonGroupRoot` (name unclear από JPG — pending Giorgio clarification αν χρειαστεί), (b) `dxf-bitmap-cache.ts` είναι dead code σήμερα (instantiated αλλά rebuild()/blit() never called — `DxfRenderer.render()` καλείται direct), (c) raster < 10% frame budget. Βρέθηκε root cause #2: vestigial `contexts/TransformContext.tsx` (React Context + useState<ViewTransform>) έπεφτε χωρίς διαγραφή στο Phase XIII migration. `wrappedHandleTransformChange` έγραφε ΚΑΙ στο legacy Provider → Provider re-render + duplicate `EventBus.emit('dxf-zoom-changed')` σε κάθε wheel notch. Μοναδικός production consumer: `CoordinateDebugOverlay.tsx` (debug-only). **FIX**: 4 MODIFIED + 1 DELETED. `contexts/TransformContext.tsx` deleted via `git rm`. `useDxfViewerCallbacks.ts` `wrappedHandleTransformChange` simplified σε single `setCanvasTransform` write (writes through `updateImmediateTransform`); `contextSetTransformRef` + `handleTransformReady` removed από params + return interface + useCallback. `DxfViewerContent.tsx` drops `TransformProvider` import + wrap, drops `contextSetTransformRef` useRef, drops `handleTransformReady` destructure + param, drops unused `ViewTransform` type import. `CoordinateDebugOverlay.tsx` migrated από `contexts/TransformContext` → `systems/cursor/ImmediateTransformStore` (same `useTransformValue()` signature, useSyncExternalStore-backed — zero behavior change). `debugTools.ts` live-coords debug tool renders `CoordinateDebugOverlay` direct χωρίς runtime Provider wrap (singleton subscription); drop unused `WindowWithDxfTransform` import. **Phase XXII.B archived as obviated by profile data** (was 7h Opus για ξύλινο raster fix — wrong target). ADR-040 §Changelog Phase XXII.C populated. TS check background. Pending commit. **Previous:** Phase XXII.A — orchestrator decoupling (`useCanvasRefs()` + `getImmediateTransform()` event-time reads, `CanvasLayerStackTransformBridge.tsx` thin subscriber wrapper).
**Previous:** 2026-05-27 — **ADR-378 Phase 6 DONE — ADR-378 ALL PHASES COMPLETE (Sonnet 4.6, ~30min, SSoT registry hardening + final closure)**: `.ssot-registry.json` `snap-engine` module updated για zero-reintroduction guard. `forbiddenPatterns` 2 → 4: added `new\s+SnapEngine\s*\(` (geo-canvas legacy class deleted ADR-378 Phase 4) + `class\s+AISnappingEngine` (conference demo dead code deleted ADR-378 Phase 1). Retained `new\s+ProSnapEngineV2\s*\(` + `new\s+SnapManager\s*\(`. `description` rewritten: references ADR-378 master + 7/7 industry convergence (Revit/AutoCAD/ArchiCAD/BricsCAD/SketchUp/Vectorworks/Bentley single-engine + N-modes-via-registry) + per-pattern rationale + geo-canvas `useSnapEngine` façade + `parserResultToEntities` adapter cross-ref. `addedByAdr` ADR-040 → ADR-378 (master now owns rule). `allowlist` gained `src/subapps/dxf-viewer/snapping/__tests__/**` glob. `npm run ssot:baseline` refreshed `.ssot-violations-baseline.json` — zero new violations from any Phase 1-5 work. Grep verification: `new SnapEngine(` in `src/` → zero matches; `class AISnappingEngine` → only 1 docs analysis MD (not scanned). `local_ΕΚΚΡΕΜΟΤΗΤΕΣ.txt` X6 ✅ + header 🟢 ALL PHASES COMPLETE. ADR-378 status header 🟡 DRAFT → ✅ COMPLETE. `adr-index.md` Phases 1+2+3+4+5 → ALL Phases 1-6 COMPLETE. ADR-378 §9 Phase 6 + §15 changelog populated. Memory entry + MEMORY.md index updated. `inferred-alignment-service` entry remains standalone in this file (ADR-3XX TBD), folded into ADR-378 §11 future work cross-reference. **ADR-378 fully shipped — 5 parallel snap subsystems → 1 production singleton (ProSnapEngineV2 via getGlobalSnapEngine), 26 sub-engines via SnapEngineRegistry, DXF Viewer + geo-canvas property floorplan tab unified.** Pending commit (1 modified: `.ssot-registry.json` + 1 baseline JSON refresh + tracking files).
**Previous:** 2026-05-27 — **ADR-378 Phase 4 DONE (Opus 4.7, ~2h, Geo-canvas unification)**: Geo-canvas property floorplan tab πλέον μοιράζεται το `ProSnapEngineV2` με το DXF Viewer μέσω `getGlobalSnapEngine()` — κερδίζει 26 modes (BIM corners + dimensions + guides + text + classic CAD) αντί του προηγούμενου 6-mode parallel engine. Industry alignment 7/7 (Revit/AutoCAD/ArchiCAD: 1 engine + N modes via registry). **NEW**: `src/subapps/geo-canvas/floor-plan-system/snapping/adapter/parser-result-to-entities.ts` (108 LOC, GeoJSON LineString→LineEntity/PolylineEntity, Polygon→closed PolylineEntity, MultiLineString→multiple PolylineEntity, mirror `regionsToSnapEntities` pattern). **REFACTORED**: `hooks/useSnapEngine.ts` πλήρης rewrite (~190 LOC) — getGlobalSnapEngine() + parserResultToEntities adapter + EXTENDED_TO_GEO_MODE mapper (6 classic ExtendedSnapType → SnapMode, all other 19+ ExtendedSnapType fallback to ENDPOINT για visual consistency με existing render layer). Public API surface (`UseSnapEngineReturn`) UNCHANGED — consumers `GeoCanvasContent.tsx:93` + `FloorPlanCanvasLayer.tsx` work as-is. **DELETED**: `engine/SnapEngine.ts` (275 LOC parallel class) + `engine/endpoint-detector.ts` (202 LOC GeoJSON extraction) + `engine/snap-distance.ts` (139 LOC) + `engine/index.ts` barrel + `hooks/useSnapPoints.ts` (zero callers verified). `hooks/index.ts` + `snapping/index.ts` updated. **KEPT**: `types/snap-types.ts` (SnapPoint/SnapResult/SnapMode/SnapSettings — render layer consumers) + `config/snap-defaults.ts` (DEFAULT_SNAP_SETTINGS/SNAP_VISUAL/MODE_LABELS/PRIORITY — SnapIndicator + render utils) + `rendering/SnapIndicator.tsx`. **VERIFICATION**: tsc zero new errors σε Phase 4 files (parser-result-to-entities + useSnapEngine + geo-canvas namespace clean). Jest 188 pass / 2 pre-existing fails (bim-corner-alignment.integration + DxfGeoTransform i18n drift "At least 3 control points are required" vs Greek expected — και τα δύο unrelated to Phase 4, υπήρχαν πριν). Grep verification: zero production references `new SnapEngine` / `extractEndpoints` / `deduplicateSnapPoints` / `findNearestSnapPoint` / `useSnapPoints` / `createSnapEngine` σε geo-canvas namespace. local_ΕΚΚΡΕΜΟΤΗΤΕΣ.txt X4 ✅ + ADR-378 §3.3→§3.1 move + §9 Phase 4 DONE + §15 + adr-index ενημερωμένα. Επόμενο: Phase 6 .ssot-registry.json forbidden patterns + baseline refresh (~30min Sonnet, trivial).
**Previous:** 2026-05-27 — **ADR-378 Phase 3 DONE (Sonnet 4.6, ~2h, TextSnapEngine completion)**: TEXT/MTEXT 8-point snap pipeline ολοκληρωμένο. NEW `src/subapps/dxf-viewer/snapping/engines/TextSnapEngine.ts` (151 LOC, mirror ColumnCenterSnapEngine pattern, linear scan over text-only filtered entities, 8 points per entity = insertion + 4 corners + center + 2 edge mids, approximate bbox από fontSize/text length για TEXT ή width/height για MTEXT, rotation around insertion). NEW `engines/__tests__/TextSnapEngine.test.ts` (14 cases — empty/non-text/8-candidate emission/insertion match/type+priority/all-kinds/mtext width/radius cut/exclude/invisible/rotation 90°/multi-entity/mixed/dispose). Registry wire σε `SnapEngineRegistry.ts:101`. SnapIndicatorOverlay.tsx νέο `case 'text'` → nested square ▣ (concentric rects, Revit/AutoCAD convention distinct from generic endpoint). i18n: `snapModes.labels.text.*` 8 sub-keys (insertion/cornerTl/cornerTr/cornerBl/cornerBr/center/edgeTopMid/edgeBottomMid) + tooltips.text σε el+en. **Type system already in HEAD**: commits 7f788d8d (`ExtendedSnapType.TEXT='text'` + enabledTypes + priority position 2 after INSERTION + perModePxTolerance 10px) + aabfb04f (`SNAP_ENGINE_PRIORITIES.TEXT=2`). **Phase 3.4 N/A**: zero manual `getTextSnapPoints()`/`toSnapCandidates()` feed in `text-layout-engine.ts` — only `interaction/index.ts` re-export + tests use it. Same pattern as Phase 2 surprise. 28/28 TextSnapEngine + TextSnapProvider tests PASS. Broader snap suite 168/169 PASS (1 pre-existing fail in bim-corner-alignment.integration.test.ts not caused by Phase 3). ΕΚΚΡΕΜΟΤΗΤΕΣ X3 ✅ + ADR-378 §3.1/§3.3/§9 Phase 3/§15/§2.1 updated. Pending commit (4 new files + 4 modified). Επόμενο: Phase 4 geo-canvas unification (~3h Opus, cross-cutting) ή Phase 6 SSoT registry refresh (~30min Sonnet, trivial).
**Previous:** 2026-05-27 — **ADR-378 Phase 5 DONE (Sonnet, EXPANDED doc cleanup)**: Original plan scope ήταν 2 αρχεία (ADR-597 phantom + adr-index polish). Πραγματικό scope κατόπιν Phase 1+2 verification = **12 edits σε 11 αρχεία**: (1) ADR-597 line 12 ADR-149→ADR-378, (2) ADR-597 line 23 ιεραρχία ADR-378 §5, (3) ADR-597 line 842 references list update, (4) ADR-153 ADR-149→ADR-378, (5) ADR-359 ADR-149→ADR-378, (6) ADR-034 row AISnappingEngine.ts strikethrough+Removed, (7) ADR-092 STORAGE_KEYS.AI_SNAPPING strikethrough, (8) ADR-092 AISnappingEngine.ts file ref strikethrough, (9) ADR-362 line 451 pro-snap-engine.ts → DimDefPoint/DimLineSnapEngine, (10) ADR-362 line 621 same, (11) ARCHITECTURE.md tree pro-snap-engine.ts removed → global-snap-engine.ts added, (12) DXF_SETTINGS_PROGRESS + DXF_VIEWER_CONFERENCE_REPORT AI-Snapping marked DELETED. Plus code: storage-utils.ts AI_SNAPPING orphan const deleted, snap-engine-utils.ts:36 stale comment removed. adr-index.md ADR-378 entry polished (DRAFT→ACTIVE, Phases 1+2+5 DONE noted). Final grep: zero stale production references. Only legit remaining: historical changelog entries (ADR-314 Phase C.5.32 2026-04-19), archived ADRs (067/079), research/analysis MDs, baseline JSON (Phase 6 scope), backup files. ΕΚΚΡΕΜΟΤΗΤΕΣ X5 + ADR-378 §9 Phase 5 mark DONE + §15 changelog + memory updated. Pending commit. Επόμενο: Phase 3 TextSnapEngine completion (~2h Sonnet, νέα συνεδρία) ή Phase 4 geo-canvas unification (~3h Opus). Phase 6 SSoT registry refresh trivial μετά.
**Previous:** 2026-05-27 — **ADR-378 Phase 2 DONE (Sonnet, ghost singleton delete)**: `snapping/index.ts` έχασε το re-export γραμμή `export { snapSystem } from './pro-snap-engine'`. `git rm src/subapps/dxf-viewer/snapping/pro-snap-engine.ts`. Surprise finding: ZERO production consumers του `snapSystem` global υπήρχαν. Καμία migration χρειάστηκε — απλό delete.
**Previous:** 2026-05-27 — **ADR-378 Phase 1 DONE (Sonnet, dead code delete)**: `git rm` σε 3 αρχεία — `AISnappingEngine.ts` + `AISnappingEngine.types.ts` + `useProSnapShortcuts.ts`. Zero production references επιβεβαιωμένα με grep (μόνο 1 stale comment σε `snap-engine-utils.ts:36` + 6 doc/analysis files info-only). ai-snapping folder κενό. ΕΚΚΡΕΜΟΤΗΤΕΣ Χ1 + ADR-378 §15 changelog updated. Pending commit (Phase 1 deletes + tracking updates) μαζί με Phase 0 bundle.
**Previous:** 2026-05-27 — **ADR-378 Snap System Master DRAFT (Opus 4.7)**: Master ADR γράφτηκε για consolidation 5 παράλληλων snap συστημάτων (ProSnapEngineV2 + geo-canvas + AISnappingEngine demo + TextSnapProvider half-done + pro-snap-engine.ts ghost). Documents Orchestrator→Registry→26 engines, resurrects phantom ADR-149 priority table στο §5, defines 6-phase cleanup roadmap. Industry alignment 7/7 (Revit/AutoCAD/ArchiCAD/BricsCAD/SketchUp/Vectorworks/Bentley → all single-engine + N-modes registry). Phase 0 (ADR write) DONE. Phases 1-6 ΕΚΚΡΕΜΟΥΝ (estimate ~9h Opus+Sonnet): P1 delete dead code (AISnappingEngine + useProSnapShortcuts), P2 migrate measure-snap-bridge ghost, P3 TextSnapEngine completion, P4 geo-canvas unification, P5 doc cleanup, P6 SSoT registry. adr-index.md + local_ΕΚΚΡΕΜΟΤΗΤΕΣ.txt ΟΜΑΔΑ Χ updated. Pending commit (Phase 0 doc-only bundle: ADR-378 + adr-index + ΕΚΚΡΕΜΟΤΗΤΕΣ + pending-ratchet).
**Previous:** 2026-05-26 — **ADR-375 Phase C.7 DONE — ADR-375 ALL PHASES COMPLETE**: 3D BIM edge overlay parity. Industry stack `Line2/LineMaterial + LineSegmentsGeometry` (Autodesk Forge/Three.js Editor/Sketchfab). `EdgesGeometry(geo, 30°)` Revit silhouette default. Screen-space `linewidth × devicePixelRatio` (Revit True Line Weight pattern). 4 NEW (resolver wrapper + overlay builder + resolution store + 2 test suites = 38/38 PASS) + 5 MODIFIED (BimToThreeConverter 4 entity types via `attachEdgesProjection` helper, wall-opening-extrude per-segment, StairToThreeConverter 4 components με ADR-377 keys treads/risers/outlines, ThreeJsSceneManager.resize one-liner). Jest infra fix: pnpm-aware transformIgnorePatterns enables Three.js ESM examples. 76/76 2D regression PASS — zero impact. local_ΕΚΚΡΕΜΟΤΗΤΕΣ.txt AΛ'7 ✅ + ADR-375 v2.5 + adr-index updated. Pending commit (bundle C.6 + C.7 + ADR-376 ribbon residual).
**Previous:** 2026-05-26 — **ADR-376 Phase C.3 DONE — ADR-376 COMPLETE ALL PHASES SHIPPED**: PDF Opening Schedule Export (Πίνακας Κουφωμάτων). 1 PDF with 2 sections (Θύρες + Παράθυρα), Revit-style combined sheet. 3 NEW files (opening-schedule-pdf-exporter.ts + OpeningSchedulePdfHost.tsx + 6 tests PASS) + 9 MODIFIED (EventBus + command-keys + annotate-tab + RibbonButtonIcon + useRibbonOpeningBridge + DxfViewerContent + schedule-presets mark column + i18n el/en). ADR-376 §7 C.3 + §11 v11 changelog + adr-index updated. local_ΕΚΚΡΕΜΟΤΗΤΕΣ.txt ΑΠ7 ✅ ΥΛΟΠΟΙΗΜΕΝΟ. Pending ratchet: legacy `boq_bim_<openingId>` rows από Phase A single-entry path (αν υπάρχουν production data) — cleanup TODO παρακάτω. .ssot-registry.json `opening-boq-grouper` + `opening-tag-drag-controller` + `opening-tag-style` entries pending μέχρι να καθαρίσει working tree.
**Source of truth:** `adrs/ADR-299-ratchet-backlog-master-roadmap.md`
**Purpose:** Agent-facing live checklist. Se STATUS = ALL_DONE → salta il resto. Se STATUS = ACTIVE → leggi e ricorda a Giorgio.

---

## 🚨🚨🚨 ΥΠΟΧΡΕΩΤΙΚΗ ΑΝΑΓΝΩΣΗ — ΚΑΘΕ AGENT / DEVELOPER ΣΕ ΚΑΘΕ SESSION 🚨🚨🚨

### ADR-365 — Tailwind raw palette ratchet: τι ΠΡΕΠΕΙ να ξέρεις πριν αγγίξεις κώδικα

**Πραγματική βάση (2026-05-19): 3.659 violations σε 440 αρχεία.**
Αρχική εκτίμηση ADR-365 ήταν 249/65 — λάθος, γιατί μέτρησε **μόνο** `hover:bg-*`. Το CHECK 3.26 μετράει όλη την επιφάνεια (bg/text/border/ring/fill/stroke × 22 παλέτες × 11 αποχρώσεις × 6 state prefixes × `dark:`).

### Τι ΑΠΑΓΟΡΕΥΕΤΑΙ πλέον (zero tolerance)

Σε ΟΠΟΙΟΔΗΠΟΤΕ νέο ή αγγιγμένο αρχείο **εκτός allowlist** (`src/design-system/`, `src/styles/design-tokens/`, `tailwind.config.ts`, κλπ. — βλ. `.ssot-registry.json` → `tailwind-hardcoded-palette.allowlist`):

❌ ΟΧΙ `hover:bg-amber-100`, `text-red-600`, `dark:bg-slate-800`, `border-blue-300`, `ring-emerald-500`, κλπ.
❌ ΟΧΙ `dark:` prefix σε consumer files — semantic tokens είναι ήδη theme-aware.

### Τι ΧΡΗΣΙΜΟΠΟΙΕΙΣ αντί γι' αυτά

✅ **shadcn tokens** (παντού όπου ταιριάζει): `bg-background`, `bg-card`, `bg-muted`, `bg-accent`, `bg-destructive`, `bg-primary`, `text-foreground`, `text-muted-foreground`, `text-destructive`, `border-border`, `border-ring`, `ring-ring`.
✅ **CSS vars για status states**: `bg-[hsl(var(--bg-success))]`, `bg-[hsl(var(--bg-error))]`, `bg-[hsl(var(--bg-warning))]`, `bg-[hsl(var(--bg-info))]` (και με `/40` opacity για subtle).
✅ **COLOR_BRIDGE** (`src/design-system/color-bridge.ts`) για ώριμα mappings.
✅ **Mapping table** πλήρης: ADR-365 §3.1.

### Boy Scout Rule (ADR-365 §7.2 + N.0.2)

**ΟΤΑΝ αγγίζεις αρχείο για άλλο λόγο**: αν περιέχει raw palette utilities από τη baseline, καθάρισέ τα μαζί στο ίδιο commit. **Δεν χρειάζεται να καθαρίσεις τα πάντα — όσα μπορείς εύκολα.** Το ratchet θα ξαναπροσαρμοστεί προς τα κάτω.

### Πώς δουλεύει το ratchet (CHECK 3.26)

- **Νέο αρχείο**: zero tolerance — οποιοδήποτε raw palette utility μπλοκάρει το commit.
- **Υπάρχον αρχείο**: μπορεί να έχει ίδιο ή λιγότερο count από το baseline. Πάει πάνω = block.
- **Allowlist αρχείο**: εξαιρείται (design-system, tokens, brand-map, κλπ).
- **Εντολές**: `npm run tailwind-palette:audit` (full scan), `npm run tailwind-palette:baseline` (refresh μετά cleanup).

### Πλάνο cleanup (Phases 1-8)

Αρχικά εκτιμήθηκαν ~5h × 8 φάσεις. **Πραγματικά θα πάρει 30-50h** λόγω του 14.7x μεγαλύτερου baseline. Η στρατηγική παραμένει: per-domain atomic commits + Boy Scout. **Δεν είναι production bug** — είναι θέμα συντήρησης / theme-consistency / dark-mode.

---

## How this file is read by the agent

1. **Session start (first reply to Giorgio):** Briefly mention (2-4 lines) what is pending here, unless:
   - Giorgio gives a clear order for independent work (then 1 line: "N pending ratchet tasks in ADR-299, see `.claude-rules/pending-ratchet-work.md` when needed")
   - Giorgio's work is explicitly a **non-ratchet** topic — don't load the reply with unrelated reminders
2. **Reminder format:** One sentence for scope + hours + Giorgio's scenario. Don't write the full ADR-299 — point to it.
3. **Don't mark items as completed without order.** Giorgio decides which ratchet proceeds. If you finish an item, remove the line (no strikethrough) and write a changelog entry below.

---

## Pending tasks (priority order)

### 🔴 API CRUD routes — parking/storages/properties σε `defineRoute` + **ΚΕΝΟ ΑΣΦΑΛΕΙΑΣ storages** (priorità ΑΛΤΗ για το σκέλος ασφαλείας· 2026-07-16, N.18/ADR-584 jscpd)

**Βρέθηκε** τρέχοντας full `jscpd:check` κατ' εντολή Giorgio (11 clones / 643 tokens μεταξύ `api/parking/**` και `api/storages/**`). **Αποσύρθηκε ρητά** από τη φουρνιά de-duplication — δεν είναι ασφαλές refactor:

**1. 🔴 SECURITY — να αποφασίσει ο Giorgio ΠΡΩΤΑ (αυτό ΔΕΝ είναι refactor, είναι αλλαγή συμπεριφοράς):**
Το `storages` και το `parking` **έχουν αποκλίνει** στον έλεγχο tenant:
- `api/storages/route.ts` GET-list με `buildingId` → κάνει το Firestore query **ΠΡΩΤΑ** και φιλτράρει tenant **in-memory μετά** (`s.companyId===ctx.companyId`). Το `api/parking/route.ts` καλεί `requireBuildingInTenant` (κεντρικό, throws `TenantIsolationError`) **πριν** το query.
- `api/storages/route.ts` **POST** → **δεν ελέγχει καθόλου** tenant στο `projectId` όταν λείπει `buildingId`. Το parking κάνει ρητό fetch του `PROJECTS` doc + `companyId` check → 404/403.
- ⚠️ Ένα κοινό factory θα **«διόρθωνε» σιωπηλά** τη χαλαρότερη συμπεριφορά του storages. Αυτό είναι απόφαση ασφαλείας, **όχι** παρενέργεια de-duplication. **ΜΗΝ** το περάσεις κρυφά μέσα σε refactor.

**2. Το SSoT υπάρχει ΗΔΗ — μην φτιάξεις νέο factory:** `src/lib/api/define-route.ts` (**ADR-602**), υιοθετημένο σε **59 αρχεία** (όλα τα `accounting/*` + `procurement/*`). Συνθέτει rate-limit → `withAuth` → try/catch → `safeParseBody` → envelope. Η σωστή δουλειά = **migration**, όχι νέο abstraction.

**3. Είναι 3 οικογένειες, όχι 2:** το `api/properties/[id]/route.ts` (295 γρ.) έχει **τον ίδιο ακριβώς σκελετό** (`requirePropertyInTenantScope` + `withVersionCheck` + `linkEntity('property:buildingId')` + `softDelete` + `extractIdFromUrl`). Κεντρικοποίηση μόνο για parking+storages = **σίγουρη επανάληψη της δουλειάς**.

**4. Μηδενικά tests:** κανένα `*.test.ts` σε parking/storages/properties routes (σε αντίθεση με `accounting/__tests__/adr603-batch-*.route.test.ts` που καλύπτουν τους `defineRoute` adopters). Οποιοδήποτε shared-factory refactor είναι σήμερα **αφύλαχτο** → τα tests πάνε ΠΡΩΤΑ.

**5. Μέρος των clones ΔΕΝ αφαιρείται:** `parking|storages/[id]/showcase/email/route.ts:52-64` = το **υποχρεωτικό Next.js route-segment adapter** (`export async function POST` + `route.handle`). Ήδη πάνω στο `createShowcaseEmailRoute` factory. Ίδιες 8 γραμμές σε properties/buildings/projects. Δεν αξίζει άλλο abstraction.

**Παραμετροποίηση (αν/όταν γίνει):** collection (`PARKING_SPACES`/`STORAGE`), tenant fn (`requireParkingInTenant`/`requireStorageInTenant`), link key (`parking:buildingId`/`storage:buildingId`), softDelete/createEntity type, audit label (`parking_spot`/`storage`), display field (`number`/`name`), Zod-only πεδία (parking: `locationZone` enum+`projectId` · storage: `floorId`).

### 🪑 ADR-407 RAILING — 2D hit-testing gap (priorità bassa, Boy-Scout flag 2026-06-03)

- [ ] **`railing` λείπει από 2 hit-testing switch** (ίδιο κενό που διορθώθηκε για `furniture` στο ADR-410 v2.2). Το `RailingEntity` δεν έχει `case 'railing'` σε: (1) `services/HitTestingService.ts` → `convertToEntityModel()` (χρειάζεται geometry-recompute fallback με `computeRailingGeometry` από `../bim/railings/railing-geometry`, mirror του `furniture` case) και (2) `systems/selection/shared/selection-duplicate-utils.ts` → `calculateEntityBounds()` (πρόσθεσε `case 'railing'` στη fall-through λίστα → `calculateBimEntity2DBounds`). **Συνέπεια:** τα κάγκελα πιθανότατα δεν φωτίζονται σε hover ούτε επιλέγονται με click/marquee στο 2Δ. **Μικρό fix (~15min, 2 cases).** Επιβεβαίωσε ότι `RailingEntity` έχει `params`+`geometry` και ότι `'railing'` είναι στο `Entity` union πριν. Ο Giorgio ζήτησε μόνο έπιπλα σε αυτή τη συνεδρία → αναβλήθηκε.

### 📐 ENTITY-BOUNDS CALCULATORS — 3 παράλληλα switch (priorità media, Boy-Scout flag 2026-06-11)

- [ ] **3 ξεχωριστοί entity→2D-bounds calculators που πρέπει να μένουν συγχρονισμένοι χειροκίνητα** — κάθε νέο entity type πρέπει να προστεθεί και στους 3, αλλιώς δουλεύει σε άλλα paths και σπάει σε άλλα (το ίδιο family bug που χτύπησε `foundation` σε marquee + Home, και `railing` παραπάνω):
  1. `systems/zoom/utils/bounds.ts` → `createBoundsFromDxfScene` (Home/Shift+1 zoom-extents) — τώρα έχει `default → calculateBimEntity2DBounds` (ADR-394 2026-06-11), οπότε αυτό πλέον γενικεύει.
  2. `systems/selection/shared/selection-duplicate-utils.ts` → `calculateEntityBounds` (marquee window/crossing) — explicit per-type switch (χρειάζεται add ανά type· λείπει `railing`).
  3. `rendering/hitTesting/Bounds.ts` → `BoundsCalculator.calculateEntityBounds` (spatial-index/click) — explicit per-type switch.
  **Προτεινόμενο SSoT unification:** τα (2)+(3) να γενικευτούν όπως το (1) (BIM fall-through → `calculateBimEntity2DBounds`) ώστε ένας μόνο τόπος να ορίζει BIM bounds. ~1-2h, χρειάζεται προσοχή σε tolerance/DXF-primitive paths. Όχι τώρα (Giorgio ζήτησε μόνο τα 2 bugfixes).
- [ ] **`FitToViewService` — διπλότυπο scale/guard math σε 2 μεθόδους** (ADR-394 2026-06-11): `calculateFitToViewTransform` (scene-based) και `calculateFitToViewFromBounds` (pure-bounds) έχουν **πανομοιότυπο** boundsWidth/Height + padding + scaleX/scaleY + degenerate guard inline. Το degenerate-line fix έπρεπε να μπει **και στις δύο** (έμπειρο bug: μπήκε αρχικά μόνο στη μία). Προτεινόμενο: η `calculateFitToViewTransform` να καλεί την `calculateFitToViewFromBounds` αφού υπολογίσει bounds (DRY). ~30min.

### 🔥 CRITICAL PATH (Scenario A — ~21h expected)

- [x] **CHECK 3.17 Entity Audit Coverage** — COMPLETATO. Baseline 9→0. _(Batch 4: 4 wire-up server-side + 5 HARD_EXEMPT nel scanner.)_


### 🏗️ FEATURE PENDING — ADR-345 DXF Ribbon Interface

**ADR:** `docs/centralized-systems/reference/adrs/ADR-345-dxf-ribbon-interface.md`
**Bridge hook:** `src/subapps/dxf-viewer/ui/ribbon/hooks/useRibbonTextEditorBridge.ts`
**Data decl:** `src/subapps/dxf-viewer/ui/ribbon/data/contextual-text-editor-tab.ts`

#### Fase 5.5 — comingSoon stubs (priorità media)
- [x] **findReplace button** → ✅ WIRED 2026-05-19. `DxfFindReplaceHost.tsx` lazy wrapper + `findReplaceOpen` state in `useDxfViewerState` + action `text-find-replace`.
- [x] **spellCheck toggle** ✅ DONE 2026-05-23 — Stub `comingSoon: true` già presente in `contextual-text-editor-tab.ts:385`. Engine assente by design — nessun ulteriore lavoro necessario finché non esiste un engine.
- [x] **insert.symbol button** → ✅ WIRED 2026-05-21. `SymbolPickerDialog.tsx` (30 symbols, 3 gruppi) + `DxfSymbolPickerHost.tsx` + `symbolPickerOpen` state + action `text-insert-symbol`. `InsertTextTokenCommand` extended per raw Unicode.

#### Fasi future ADR-345 (in ordine)
- [x] **Fase 6.1: Tab SETTINGS** — DxfSettingsPanel nel ribbon, floating colors tab disabilitata ✅ 2026-05-15
- [x] **Fase 7: Panel flyout** — 4 stati minimize, pinnedPanelIds, chevron + pin UI ✅ 2026-05-15
- [x] **Fase 8: Floating panel removal** — NON da fare. Giorgio ha confermato: sidebar rimane con Επίπεδα + Ρυθμίσεις DXF. ✅ 2026-05-15

---

---

### 🪜 ADR-358 STAIR DOMAIN — pre-existing TS residual (priorità bassa)

- [x] **HitTestingService.ts:236 — DxfStair exhaustive check failure** ✅ DONE 2026-05-21 — Root cause: `entities.ts` importάρε από `'./stair'` (barrel διαγραμμένο στο ADR-363 Phase 0.5 consumer sweep). Fix: imports → `'../bim/types/stair-types'`. ADR-358 + ADR-363 changelogs ενημερώθηκαν.

---

### 📐 BIM scene-units helper — migrate-on-touch (priorità bassa, ~30min)

**Discovered 2026-05-28** (ADR-363 Phase 1C-bis HOTFIX#2 centralization audit). Δημιουργήθηκε SSoT helper `mmScaleFor(params)` + `canvasToMmScaleFor(params)` στο `src/subapps/dxf-viewer/utils/scene-units.ts` (encapsulates το `mmToSceneUnits(params.sceneUnits ?? 'mm')` + inverse `1 / mmToSceneUnits(...)` idiom). Υιοθετήθηκε ΗΔΗ στο `bim/walls/wall-grips.ts`. Τα υπόλοιπα call-sites inlinάρουν ακόμα το pattern — **migrate when you touch each file** (Boy Scout, N.0.2):

- [ ] `bim/geometry/wall-geometry.ts:57` → `mmScaleFor(params)`
- [ ] `bim/walls/wall-trims.ts:184` → `mmScaleFor(a.params)`
- [ ] `bim/geometry/beam-geometry.ts:40,76` → `mmScaleFor(params)`
- [ ] `bim/geometry/column-geometry.ts:61` → `mmScaleFor(params)`
- [ ] `bim/geometry/slab-geometry.ts:84,147` → `mmScaleFor(params)`; `:208` → `canvasToMmScaleFor(params) * MM_TO_M`
- [ ] `bim/geometry/slab-opening-geometry.ts:42` → `mmScaleFor(params)`; `:65,77` → `canvasToMmScaleFor(params)`
- [ ] `bim/validators/slab-opening-validator.ts:96` → `mmScaleFor(params)`
- [ ] `systems/cursor/wall-face-corner-snap.ts:57` → `mmScaleFor(params)`
- [ ] `rendering/entities/dimension/dim-text-renderer.ts:87` → `mmScaleFor(params)`

NOTE: `bim/stairs/stair-floor-link.ts:42` + `stair-auto-fix.ts:131` χρησιμοποιούν `1 / mmToSceneUnits(inferSceneUnitsFromWidth(width))` — διαφορετική πηγή (width-heuristic, ΟΧΙ params.sceneUnits) → ΔΕΝ migrate σε `mmScaleFor` (θα ήταν λάθος abstraction). Άσ' τα ως έχουν.

---

### 📐 Point2D→Point3D `z:0` lift — ~20 inline αντίγραφα (priorità bassa, Boy-Scout flag 2026-06-25, ADR-528)

- [ ] **Το `verts.map((p) => ({ x: p.x, y: p.y, z: 0 }))` (Point2D[]→Point3D[] z=0 lift) επαναλαμβάνεται σε ~20 σημεία** (`beam-column-cutback.ts:280` `toXY0`, `building-footprint.ts`, `column-geometry.ts`, `hatch-pattern-geometry.ts`, `polygon-interior-point.ts`, `straight-skeleton{,-faces}.ts`, `mep-*`, `column-rect-decomposition.ts`, `column-section-outline.ts`, `column-validator.ts`, …). Διαφορετικά downstream (polygonArea/triangulate/centroid) → ΟΧΙ ένα abstraction για όλα. **Πρόταση:** SSoT `liftTo3D(verts: Point2D[]): Point3D[]` στο `polygon-utils` (δίπλα στο νέο `polygon2DCentroid`) + migrate-on-touch. Μεγάλο sweep (>4 αρχεία) → ΟΧΙ inline (N.0.2). _Το centroid-specific case ΗΔΗ κεντρικοποιήθηκε στο `polygon2DCentroid` (ADR-528)._

---

### 🔧 BIM GRIP GEOMETRY — shared module extraction (priorità media, ~2-3h, cross-cutting)

**Discovered 2026-05-28** (ADR-393 stair extended grips SSoT review). Τα 3 BIM grip families (`bim/walls/wall-grips.ts`, `bim/beams/beam-grips.ts`, `bim/stairs/stair-grip-{math,transforms}.ts`) αντιγράφουν τα ίδια primitives αντί να μοιράζονται SSoT:

1. **`project2D(p3): Point2D`** (`{x:p3.x, y:p3.y}`) — αντίγραφο σε wall-grips + beam-grips + stair-grip-math. 1-liner αλλά 3×.
2. **`unitAxis`/`perpUnit` family** — wall+beam έχουν δικά τους `unitAxis(params)` + `perpUnit(u)`. Το stair πλέον καταναλώνει `directionToUnitVector`/`perp` από `stair-geometry-shared.ts` (ADR-393 fix). wall/beam ΟΧΙ ακόμη.
3. **Asymmetric corner decompose+recenter math** — `wall-grips.ts:moveCorner` και `stair-grip-transforms.ts:moveCorner` είναι ο ΙΔΙΟΣ αλγόριθμος (axial/perp split + symmetric thickness/width grow + axis recenter), σε διαφορετικά param shapes. Extractable σε generic `decomposeCornerDrag(delta, u, p) → {axial, perp}` + `applySymmetricResize(...)`.
4. **scene-floor heuristic** — `minWidthFloorFor` (stair) vs `minThicknessFloorFor`/`maxThicknessCeilingFor` (wall). Width-heuristic family (βλ. NOTE παραπάνω) — δεν είναι `mmScaleFor` αλλά μπορεί να γίνει ένα shared `physicalFloorFor(value, mm)`.

**Fix**: νέο `bim/grips/shared/bim-grip-geometry.ts` (project2D + decomposeCornerDrag + applySymmetricCornerResize + physicalFloorFor), consumed από wall/beam/stair. **Γιατί pending (όχι on-the-spot)**: >1h, 4+ files, ΚΑΙ το `wall-grips.ts` είναι σε parallel uncommitted flux (2026-05-28 positions-from-geometry fix) → extraction τώρα = merge conflict risk. Κάνε το αφού σταθεροποιηθεί το wall.

- [ ] Δημιούργησε `bim/grips/shared/bim-grip-geometry.ts` με τα 4 primitives
- [ ] Migrate wall-grips + beam-grips + stair-grip-{math,transforms} να καταναλώνουν
- [ ] `.ssot-registry.json` entry `bim-grip-geometry` (forbid re-impl `project2D`/corner-decompose εκτός module)
- [ ] Tests + metre-scene regression για κάθε family

### 📐 ADR-393 stair grip purity follow-up (priorità bassa, ~30min)

**Discovered 2026-05-28** (ίδιο review). Τα stair corner positions (G5-G8) στο `getStairGrips` derive από `params.width`/`totalRun`/`basePoint` — scene-units **by construction** (builder κλιμακώνει), άρα **όχι active bug**. Αλλά ο κανόνας [[feedback-grip-positions-read-geometry]] προτιμά read από `geometry.stringers`. Επίσης το `DIRECTION_GRIP_OFFSET_MM` στο G1 (direction) ήταν latent unit-unsafe — **fixed** μέσω `mmFactorFromWidth` (ADR-393 §8.1). Purity item:

- [ ] Corner positions G5-G8 → read από `geometry.stringers.outer/inner` endpoints (perp-sign left/right classify) αντί raw params, με raw fallback (mirror G2/G3 pattern). Verify με metre-scene test.

---

### 🎨 ADR-365 TAILWIND SEMANTIC PALETTE ENFORCEMENT (priorità alta, ~5h totali, 9 φάσεις)

Discovered 2026-05-19 (hover audit follow-up). ADR: `docs/centralized-systems/reference/adrs/ADR-365-tailwind-semantic-palette-enforcement.md`

**Status:** PHASE 8 SALES DONE 2026-05-22. Baseline: 620/167. Per-phase handoff απαιτείται πριν από κάθε νέα session.

- [x] **Phase 0 — Infrastructure** ✅ 2026-05-19 — ratchet script (`scripts/check-tailwind-palette-ratchet.js`), `.ssot-registry.json` module `tailwind-hardcoded-palette` (Tier 2, 15 allowlist entries), `.tailwind-palette-baseline.json` (3,659 violations / 440 files — actual baseline revised from estimate 249/65), CHECK 3.26 wired into `scripts/run-checks-parallel.js` (worker_thread), npm scripts `tailwind-palette:{audit,report,baseline}`. Smoke 1-5 PASS. Hook latency ~0.73s staged / ~3.4s full.
- [x] **Phase 1 — DXF Viewer** ✅ 2026-05-22 — 20 files cleaned (−254 violations). Baseline: 3,659/440 → 3,405/420.
- [x] **Phase 2 — Procurement + Vendor Portal** ✅ 2026-05-22 — 43/44 files cleaned (−373 violations). Baseline: 3,405/420 → 3,032/399. ConflictDialog migration in working tree only, blocked by CHECK 3.22 dead-code ratchet.
- [x] **Phase 3 — Accounting** ✅ 2026-05-22 — 19 files cleaned (−143 violations). Baseline: 3,032/399 → 2,889/394.
- [x] **Phase 4 — Properties + Contacts + Building Dialogs** ✅ 2026-05-22 — 23 files cleaned (−149 violations). Baseline: 2,889/394 → 2,740/371.
- [x] **Phase 5 — Shared Files + File Manager** ✅ 2026-05-22 — 9 files cleaned (−73 violations). Baseline: 2,740/371 → 2,667/362.
- [x] **Phase 6 — Dashboard + Admin + CRM + Header + Notifications** ✅ 2026-05-22 — 11 files cleaned (−137 violations). Baseline: 2,667/362 → 2,530/354.
- [x] **Phase 7 — Design System + Showcase + Sales + Geo-canvas** ✅ 2026-05-22 — 8 files cleaned (−51 violations). Added `--showcase-link` CSS var. Baseline: 2,530/354 → 2,479/346.
- [x] **Phase 8 (partial) — Admin + Accounting + Contacts + Procurement + Reports + Compositions** ✅ 2026-05-22 — 50+ files cleaned (−1,836 violations). Baseline: 2,479/346 → **643/178**. ADR changelog updated.
- [x] **Phase 8 Sales — 31 αρχεία Sales domain** ✅ 2026-05-22 — interest-cost-helpers/tabs/pricing-settings, CounterproposalTab, ForwardCurveChart, HedgingComparisonTable, DSCRStressTab, CounterproposalScenarioRow, monte-carlo-panels, DrawScheduleTab, EquityWaterfallDialog, InterestReserveChart, MonteCarloTab, SensitivityTab, ChequeDetailDialog, CreatePaymentPlanWizard, InstallmentSchedule, InterestCostSection, LoanCard, LoanStatusTimeline, PaymentPlanOverview, TransactionChainCard, SalesPropertyListCard, sales-colors.ts, AppurtenancesSection, ContractCard, ContractTimeline, PropertyHierarchyCard, SalesParkingCard, SalesStorageCard, PropertySummaryContent. Baseline: 643/178 → **620/167** (−23 violations, −11 files).
- [x] **Phase 8 — Final closure** ✅ 2026-05-22 — 36 files cleaned (−36 violations). Baseline: 620/167 → **0/0**. `npm run tailwind-palette:baseline` run. ADR-365 status: **APPROVED**. Zero-tolerance enforcement active via CHECK 3.26.

**ADR-365 FULLY COMPLETE — all 9 phases done. Baseline = 0 violations / 0 files.**

---

### 🧹 GRIP TYPES SSoT — canvas-mouse-types duplicate (priorità bassa, ~30min)

Discovered 2026-05-19 (N.0.2 Boy Scout durante ADR-183 Phase C cleanup, deprecated hook deletion).

- [ ] **`hooks/canvas/canvas-mouse-types.ts:19-89` duplicate grip types** — Local re-definitions of `VertexHoverInfo`, `EdgeHoverInfo`, `SelectedGrip`, `DraggingVertexState`, `DraggingEdgeMidpointState`, `DraggingOverlayBodyState`, `GripHoverThrottle`. Canonical SSoT lives in `hooks/grips/unified-grip-types.ts` (ADR-183). Fix: replace inline `export interface` blocks with `export type { … } from '../grips/unified-grip-types'`. Verify consumers (`useCanvasMouse.ts`, `useCanvasEffects.ts`, `useOverlayInteraction.ts`, `useLayerCanvasMouseMove.ts`, `canvas-mouse-drag-handlers.ts`, `hooks/canvas/index.ts`) still compile (`npx tsc --noEmit`). Side benefit: stale `(from useGripSystem)` JSDoc comments in `useLayerCanvasMouseMove.ts:44-62` + `canvas-mouse-types.ts:87,161-168` should be retargeted to `unified-grip-types`.

---

### 🧭 ADR-3XX (TBD) — AUTO-INFER ALIGNMENT GUIDES (priorità bassa, ~3h, discovered 2026-05-19 via SPEC-3D-004C)

**Discovered**: 2026-05-19 κατά τη διερεύνηση SPEC-3D-004C (GenArc Utils/Snap/Picking). Το GenArc `resolveSnapV2.ts:61-104` έχει αυτόματη "smart guides" λογική (Revit/AutoCAD-style) που το Nestor **δεν έχει**.

**Gap επιβεβαιωμένο** (3 engines grep):
- `GuideSnapEngine.ts` → μόνο explicit guides από `GuideStore` singleton.
- `OrthoSnapEngine.ts` → χρειάζεται explicit `referencePoint` (lastPoint draw flow).
- `ParallelSnapEngine.ts` → reference **lines**, όχι arbitrary points.

**Συμπεριφορά που λείπει**: όταν ο κέρσορας είναι ≤35cm από κάποιο wall/beam endpoint ή midpoint ή column corner/center → αυτόματη γαλάζια διακεκομμένη γραμμή στοίχισης (vertical ή horizontal), **χωρίς** χρειαζόμενο explicit guide από τον χρήστη.

**Όροι Γιώργου (επιβεβαιωμένοι 2026-05-19)**:
1. ✅ Δεν υπάρχει στο Nestor (επιβεβαιωμένο grep)
2. ✅ Θα βοηθήσει (Revit/AutoCAD industry standard)
3. ✅ Add-only — δεν σπάει υπάρχοντα systems (opt-in flag στα `SnapPresets`, lower priority από Endpoint, cap ≤2 inferred guides)
4. ✅ Πλήρως SSoT-ενσωματωμένο

**Architecture sketch (όταν προχωρήσει)**:
- Νέο module `snapping/services/InferredAlignmentService.ts` (Tier 3 SSoT) που εκθέτει `getAlignmentAnchors(scene): Point2D[]`.
- Reads BIM scene SSoT (`bim/`) — **όχι re-implementation** anchors. Anchors source: wall.start/end/midpoint + beam.start/end/midpoint + column.position + column.4 corners.
- Νέα `private addAutoInferredAlignmentCandidates()` στον `GuideSnapEngine.ts`, opt-in μέσω `SnapPresets.setArchitecturalPreset({ inferAlignment: true })`.
- Lower `priority` από `EndpointSnapEngine` (να μην κλέβει snap από explicit endpoints).
- Cap ≤2 inferred guides per frame (1 vertical + 1 horizontal).
- Tolerance 35cm (parameter στο preset).
- Tests: 2-3 jest cases (cursor near wall endpoint → guide emitted, cursor far → no guide, multiple anchors → best two selected).

**SSoT registry entry (when implemented)**:
```json
"inferred-alignment-service": {
  "tier": 3,
  "canonical": "src/subapps/dxf-viewer/snapping/services/InferredAlignmentService.ts",
  "forbiddenPatterns": ["wall\\.start.*Math\\.abs.*cursor", "alignment.*infer.*inline"]
}
```

**Effort**: ADR proposal ~1h + implementation ~2h + tests ~30min. **Total: ~3-3.5h.** Δεν blocks το ADR-366.

- [ ] Γράψε ADR proposal (επόμενος ελεύθερος αριθμός — αν ADR-366 PROPOSED παραμένει 5-19, ίσως ADR-367 ή επόμενος διαθέσιμος).
- [ ] Implementation `InferredAlignmentService` + `GuideSnapEngine` enhancement.
- [ ] Tests + SSoT registry entry.
- [ ] Boy Scout: όλα τα Architectural presets ενεργοποιούν `inferAlignment: true` by default.

---

### 🎬 ADR-366 §C.1 ANIMATION SYSTEM — ✅ FULLY CLOSED 2026-05-25

**Status (2026-05-25)**: All 3 sub-phases DONE. Animation Phase 9 ολόκληρη closed.

- ✅ **C.1.a Logic Foundation** — DONE 2026-05-25 (commit `1e393c1d`).
- ✅ **C.1.b UX / Timeline** — DONE 2026-05-25 (commit `68c473a5`). **+drag interaction DONE 2026-05-25** (uncommitted). **+§C.1.Q4 bezier 4-point editor DONE 2026-05-25** (uncommitted, FULL ENTERPRISE: 5 new + 4 modified + 23 tests). **+real scene-bbox turntable DONE 2026-05-25** (uncommitted, 2 new + 3 modified + 7 tests, SSoT REUSE computeSceneFramingBounds). Deferred resolved: ~~TimelineEditor unit tests~~ ✅ 2026-05-25, ~~snap-to-grid~~ ✅ 2026-05-25, ~~axis-constrained gizmo~~ ✅ 2026-05-25.
- ✅ **C.1.c Rendering / Queue** — DONE 2026-05-25 (8 new + 6 modified + storage rules + 3 test suites). MP4Exporter (WebCodecs H.264 + VP9 fallback inside MP4 via mp4-muxer@5.2.2 MIT) + RenderQueueStore (Zustand FIFO + AbortController Map) + RenderQueuePanel (Floating3DPanel 8th tab) + animation-queue-processor + render-checkpoint + animation-action-handlers (auto-save flow) + origin-indicator-overlay extraction (SRP cap) + Save+Export ribbon comingSoon REMOVED + Storage rules block (500MB, mp4/webm) + notification keys + i18n ~15×2. ADR drift resolved: rasterizer-not-pathtracer + project_assets DROPPED + VP9-in-MP4 + checkpoint field reuse. Cross-session subscribe DEFERRED Phase 10.

---

### 📅 ADR-034 GANTT — Phase 4 pending items (construction tracking)

**ADR:** `docs/centralized-systems/reference/adrs/ADR-034-gantt-chart-construction-tracking.md`
**ADR-266:** `docs/centralized-systems/reference/adrs/ADR-266-gantt-construction-reports.md`
**Gantt dir:** `src/components/building-management/tabs/TimelineTabContent/gantt/`
**Hooks dir:** `src/components/building-management/hooks/construction-gantt/`

**ADR-034 αλλαγμένο (χωρίς commit) — status line + Phase 4 table + ADR-266 cross-ref + changelog.**

- [x] **4.8 Dependency arrows** ✅ DONE 2026-05-21 — `GanttDependencyArrows.tsx` + `useGanttDependencyArrows.ts`, portal into `.rmg-timeline-container`, RAF-throttled bezier SVG, ADR-034 §4.8 updated
- [x] **4.5 Alert engine — deadline notifications** ✅ DONE 2026-05-21 — ADR-266 Phase D.3 (6+1 alert rules, `construction_alerts` collection, API routes, `ScheduleAlertBanner`, Telegram digest, cron bypass) + Phase D.5 (Portfolio Dashboard). ADR-034 status line stale — needs sync.
- [x] **4.6 AI integration UC-017** ✅ DONE 2026-05-23 — UC-017 module 11 source files + 4 tests (41 pass). 6 features FAST/QUALITY/VISION. `src/services/ai-pipeline/modules/uc-017-gantt-ai/`. Intent `ADMIN_GANTT_AI` + registered.
  - Τελευταίο στη σειρά — δεν μπλοκάρει 4.8/4.5

**Προτεινόμενη σειρά:** 4.8 ✅ → 4.5 ✅ → 4.6

---

### 🧹 FULL ZERO BACKLOG (Scenario B extras — +~43h expected)

- [x] **ADR-298 Phase C** — COMPLETATO. Phase C.7 DONE 2026-04-14. 11 collezioni → COVERAGE. FIRESTORE_RULES_PENDING ora VUOTA (zero entry). Coverage totale: 92 collezioni. 291 test verdi.

- [x] **ADR-298 Phase E** — COMPLETATO 2026-04-14. ADR-301 (Storage Rules Coverage SSoT). 4 path pattern, 48 celle, harness completo, CHECK 3.19 verde.

- [x] **CHECK 3.8 i18n Missing Keys** — COMPLETATO 2026-04-14. 4,750→0 violazioni. 3 fasi: (1) 730 file `useTranslation` single→array (namespace ADR-280 subs), (2) 479 chiavi genuinamente mancanti aggiunte ai locale files el+en, (3) settings.json creato. Baseline 0.

---

### 🧱 ADR-373 ISO 19650 — Phase 2 deferred items (priorità media, ~10-15h totali, discovered 2026-05-24 via Phase 1 implementation)

**ADR**: `docs/centralized-systems/reference/adrs/ADR-373-iso19650-metadata-enrichment.md`

Phase 1 ✅ IMPLEMENTED 2026-05-24 (schema + AI enricher + tests + post-finalize hook). Φύλαξη για Phase 2 — επόμενες υλοποιήσεις:

- [x] **Manual override UI** ✅ DONE 2026-05-24 — Iso19650MetadataSection.tsx + updateIso19650Metadata() + file-mutation-gateway + i18n (P2.1).
- [x] **Virtual ISO 19650 folder view** ✅ DONE 2026-05-24 — buildTreeByISO19650 + FileManagerToolbar 4th view mode + iso19650-tree ViewMode (P2.2).
- [x] **Backfill script** ✅ DONE 2026-05-24 — GET/POST /api/admin/iso19650/backfill, super_admin only, dry-run + execute, max 20/call, idempotent (P2.3).
- [x] **Distributed concurrency token bucket** ✅ DONE 2026-05-24 — enrichment-slot-service.ts Admin SDK, max 5 slots/company, TTL 5min self-heal, fail-open (P2.4).
- [x] **Monthly aggregate cost dashboard** ✅ DONE 2026-05-24 — iso19650-cost-log-service.ts + GET /api/admin/iso19650/costs, byDiscipline/byMonth aggregates, max 500 records, rules deployed (P2.5).
- [x] **`suitabilityCode` field** ✅ DONE 2026-05-24 — SUITABILITY_CODES + SUITABILITY_CODE_REGEX + SuitabilityCode type in iso19650-constants.ts. FileRecord field. isSuitabilityCode() + validateSuitabilityCode() in validators.ts. Enricher schema+prompt+buildAiResult extended. 76 tests green (73 suites / 1179 total).
- [x] **i18n keys για ISO labels** ✅ DONE 2026-05-24 — `el/iso19650.json` + `en/iso19650.json` NEW. Namespace 'iso19650' registered in SUPPORTED_NAMESPACES + namespace-loaders. 13 disciplines + 9 series + 4 CDE states + 4 suitability codes + 7 labels. i18n audit clean.
- [x] **`vision-helpers.ts` SSoT extraction** ✅ DONE (prior session) — `downloadFile` / `extractOutputText` / `isImageMime` / `VisionContent` εξαχθηκαν σε shared module. iso19650-enricher.ts + contact-document-classifier.ts imports updated.

**Effort**: ~13.5h totale per Phase 2 (additional al Phase 3 ZIP export).

---

### 🧹 ADR-370/371 DUPLICATE NUMBERING — ✅ IMPLEMENTED 2026-07-08 (Opus 4.8, **pending Giorgio commit**) — housekeeping (discovered 2026-05-24 via ADR-373 OQ8)

> **✅ RESOLVED 2026-07-08:** corner-snap doc renumbered **ADR-371 → ADR-597** (unique gap number· big-player immutable-number rule). Filesystem rename `ADR-370-bim-corner-snap-system.md` → `ADR-597-bim-corner-snap-system.md` (git rename-detection preserves blame). Redirect stub `ADR-371-bim-corner-snap-system.md` **deleted**. Refs updated same logical unit: adr-index (row + mirror + redirect row dropped), ADR-378 back-link, **42 src files** (code referenced the doc by BOTH `ADR-370` filename-number AND `ADR-371` internal-number → both → `ADR-597`· real blast radius > handoff's «24»), 9 auto-memory files + MEMORY.md index. ADR-371 now sole-owner = 3D Read-Only Viewer· ADR-370 sole-owner = Read-Only Visualization. **The proposal below (ADR-374/375) is superseded — kept as history.** Remove this whole block after Giorgio commits (N.13).

**Discovered**: 2026-05-24 durante ADR-373 Phase 1 Recognition (Glob verification del prossimo ADR libero).

**Πρόβλημα**: 4 αρχεία ADR με 2 collisione numerazione:
- `docs/centralized-systems/reference/adrs/ADR-370-bim-readonly-visualization.md`
- `docs/centralized-systems/reference/adrs/ADR-370-bim-corner-snap-system.md` ← duplicate
- `docs/centralized-systems/reference/adrs/ADR-371-bim-3d-readonly-viewer.md`
- `docs/centralized-systems/reference/adrs/ADR-371-bim-corner-snap-system.md` ← duplicate

**Effetto**: confusione nello index, ambiguità riferimenti incrociati, blocca uso di `adr-index.md` come SSoT.

**Fix proposto** (Γιώργος approval pending):
1. I due `corner-snap-system` ADRs sembrano duplicati l'uno dell'altro (stesso suffix). Verifica se sono lo stesso contenuto o due fasi diverse.
2. Se duplicati: cancella uno, l'altro rinomina come ADR-374 (next free dopo ADR-373).
3. Se distinti: rinomina ADR-370-bim-corner-snap-system.md → ADR-374, ADR-371-bim-corner-snap-system.md → ADR-375.
4. Grep tutti i riferimenti (`grep -rn "ADR-370\|ADR-371"` in docs/ + src/) e aggiorna.
5. Re-run auto-script per `adr-index.md`.

**⚠️ SCOPE CORRECTION (2026-07-08, Sonnet — blast-radius μετρήθηκε, ΟΧΙ «15min/low-risk»):**
- **Πραγματική σύγκρουση:** ΔΥΟ docs officially numbered **ADR-371** — `ADR-371-bim-3d-readonly-viewer.md` ΚΑΙ `ADR-370-bim-corner-snap-system.md` (έχει Note «officially numbered ADR-371, filename kept for git-blame»). Το `ADR-371-bim-corner-snap-system.md` = redirect stub (8γρ). Content-equal check: **DIFFERENT** (stub vs 1129γρ real doc).
- **Canonical:** `ADR-370-bim-readonly-visualization.md` = σωστό ADR-370. Το corner-snap (1129γρ, IMPLEMENTED, το ADR-398 το επεκτείνει) πρέπει να πάρει **μοναδικό ελεύθερο number**.
- **Blast radius (grep 2026-07-08):** 4 doc refs + **6 src refs** + **25 auto-memory refs** + adr-index 2 sections (γρ.362-365 + 945-947). ⚠️ **Fix proposto στάδια 2-3 STALE:** ADR-374/375 είναι ΠΙΑΣΜΕΝΑ· 600-604 πιασμένα· 605+ mint-άρονται ταυτόχρονα από άλλους agents → επιλογή number απαιτεί coordination (collision risk).
- **Ετυμηγορία:** cross-cutting docs+src+memory renumber → **dedicated planned session** (ΟΧΙ boy-scout, ΟΧΙ πάνω σε uncommitted work). Priorità bassa — δεν μπλοκάρει τίποτα (τα docs self-document το ADR-371 mapping μέσω Note+redirect).

- [x] Verifica se i 2 `corner-snap-system` ADRs sono content-equal o distinti — **DIFFERENT** (redirect stub vs 1129γρ canonical)
- [ ] Renumber corner-snap → μοναδικό ελεύθερο ADR number (dedicated session· διάλεξε number με coordination) + update note/stub
- [ ] Update 4 doc + 6 src + 25 memory refs + adr-index 2 sections
- [ ] Re-run `adr-index.md` auto-script

---

### 🪟 ~~ADR-363 Opening — scene-units thread στα edit paths~~ ✅ CLOSED 2026-05-25 (later)

Closed via `hostWall.params.sceneUnits ?? 'mm'` frozen-context pattern σε **4 πραγματικούς callers** (όχι 11 — pending list είχε 7 false positives + 1 missing caller). Files: `bim-readonly-hydration.ts:132`, `useOpeningPersistence.ts:109`, `UpdateOpeningParamsCommand.ts:71`, `WallSplitCommand.ts:149`. 3 νέα tests στο `opening-geometry.test.ts` (scene-units 'm' describe block). 27/27 PASS. Καμία API change σε commands/`OpeningParams`. Out of scope: pre-existing `wall.geometry.length * 1000` σε `applyOpeningGripDrag` — separate ratchet candidate. Βλ. ADR-363 §12 changelog 2026-05-25 (later).

---

### 🎨 ADR-375 Phase C — BIM Line Weight Advanced ✅ COMPLETE 2026-05-26 (C.1–C.7 DONE)

**ADR**: `docs/centralized-systems/reference/adrs/ADR-375-bim-entity-line-weight-semantic-system.md`
**Status**: ALL sub-phases DONE 2026-05-26. Phase C closed.

**Phase C scope** (ADR-375 §5.Phase-C):

1. **Pen Table editor** ✅ DONE 2026-05-26 — `dxf_viewer_pen_tables/{companyId}`, Zustand store, `setPenTableSource()` resolver injection, `PenTablePanel.tsx` 16×6 grid. 10 tests.
2. **Pen Sets presets** ✅ DONE 2026-05-26 — `bim-pen-sets.ts` Design/Construction/Presentation ISO tables + `penSetToOverrides()`, store `applyPreset/activePresetName`, PenTablePanel preset tabs (aria-pressed), i18n el+en. 25 tests PASS.
3. **Subcategories** — Door panel vs Door swing, Wall layers cut vs skin. Διαφορετικό pen ανά μέρος του ίδιου entity.
4. **Per-view overrides (Visibility/Graphics)** — Override styles ανά view χωρίς να αλλάζουν defaults. Επεκτείνει το ViewTemplate.
5. **Per-element overrides** — Πάχος γραμμής για ΕΝΑΝ συγκεκριμένο τοίχο. Νέο field `entity.params.lineWeightOverride?`.
6. **Layer-driven overrides** — Integration με ADR-358. Layer controls override BIM defaults (όπως Revit Layer Lineweight αν existed).
7. **3D parity (ADR-370)** — Τα ίδια SSoT tokens (PenTable + ObjectStyles + ViewRange) να εφαρμόζονται σε THREE.js line widths. Mirror του 2D Phase A pattern σε 3D viewer.

**Phase D scope** (ξεχωριστό ADR στο μέλλον — όχι μέρος C):
- Underlay rendering (όροφος από πάνω/κάτω σαν halftone reference)

**Δομικός περιορισμός εντοπίστηκε 2026-05-26 runtime verification**:
- Το σύστημα έχει **1 `dxf_viewer_levels` doc που reuses floorId** όταν αλλάζεις όροφο. Δεν υπάρχουν παράλληλα 2+ BIM Level entities με ίδιο `appliedViewTemplateId`.
- Συνέπεια: το Update propagation (`propagateToLinkedLevels`) στο `view-template.service.ts` είναι **unit-tested (28 tests PASS) αλλά runtime-unobservable**.
- Δεν είναι B.3 bug — είναι ίσως candidate για Phase D / νέο ADR (multi-level architecture extension).

**Pre-requisites για Phase C**:
- Καμία. Phase A + B είναι solid baseline.
- ⚠️ Πριν Phase C.7 (3D parity), επιβεβαίωσε ADR-370 status (3D viewer Group A/B/C state).

**Effort estimate (ψιλικά)**: C.1 ~3-5h · C.2 ~2-3h · C.3 ~5-8h · C.4 ~5-8h · C.5 ~3-5h · C.6 ~3-5h · C.7 ~5-10h. Total ~25-40h.

- [x] **C.1** Pen Table editor (UI grid + persistence) ✅ 2026-05-26
- [x] **C.2** Pen Sets presets (Design / Construction / Presentation) ✅ 2026-05-26
- [x] **C.3** Subcategories (Door panel/swing, Wall layers) ✅ 2026-05-26 (via ADR-377)
- [x] **C.4** Per-view overrides (Visibility/Graphics) ✅ 2026-05-26 — ObjectStyle +5 V/G fields, resolveIsCategoryVisible helper, full priority stack, 3 store setters, VisibilityGraphicsPanel.tsx, 24/24 tests. **v2.6 hotfix 2026-05-26**: runtime wiring across 7 BIM 2D renderers (early-return στο render() entry) + BimSceneLayer 3D category filter + BimViewport3D subscription σε objectStyles. 2 new tests (renderer skip + 3D layer filter, 18 assertions total).
- [x] **C.5** Per-element overrides (`BimElementStyleOverride` + `BimEntity.styleOverride`) ✅ 2026-05-26 — resolver C.5 priority stack, 7 renderers wired, 17/17 tests
- [x] **C.6** Layer-driven overrides (ADR-358 integration) ✅ 2026-05-26 — `BimLayerOverride` interface + `SubcategoryResolutionContext.layerOverride?` + resolver C.6 priority block (concrete mm→px, bypass pen table) + 7 renderers wired. 16/16 tests PASS.
- [x] **C.7** 3D parity (ADR-370 THREE.js line widths) ✅ 2026-05-26 — 3D BIM edge overlay via `Line2 / LineMaterial + LineSegmentsGeometry` (industry stack Autodesk Forge / Three.js Editor / Sketchfab). `EdgesGeometry(geo, 30°)` Revit silhouette default. `linewidth × devicePixelRatio` High-DPI crisp. `depthTest:true / depthWrite:false` + `alphaToCoverage:true`. 4 NEW files (`bim-3d-edge-resolver.ts` wrapper zero duplication + `bim-3d-edge-overlay-builder.ts` LineSegments2 builder + `bim-edge-resolution-store.ts` singleton + 2 tests) + 5 MODIFIED (BimToThreeConverter wall/column/beam/slab, wall-opening-extrude per-segment, StairToThreeConverter tread/riser/stringer/landing με ADR-377 keys, ThreeJsSceneManager.resize wire). Jest config infra fix για three ESM examples (pnpm-aware transformIgnorePatterns). 38/38 edge tests PASS + 76/76 2D regression PASS. TSC clean.

---

## Short sentence for session-start reminder

**Copy-paste template for the agent:**

> 📋 Pending ratchet tasks (ADR-299): **ADR-365 Tailwind Palette ✅ COMPLETE** — 0 violations / 0 files (2026-05-22). Zero-tolerance via CHECK 3.26 active. Remaining pending: Grip Types SSoT, ADR-3XX Auto-Infer Alignment. (ADR-370/371 duplicate numbering ✅ IMPLEMENTED 2026-07-08 → corner-snap = ADR-597, pending Giorgio commit.) (**ADR-375 ✅ ALL PHASES COMPLETE 2026-05-26** — A+B+C.1+C.2+C.3+C.4+C.5+C.6+C.7 DONE + v2.6 V/G visibility runtime wiring hotfix, 3D BIM edge overlay parity Line2/LineMaterial, 38/38 edge tests + 76/76 2D regression PASS. ADR-363 Opening scene-units thread ✅ CLOSED 2026-05-25 — 4 callers, 27/27 tests. ADR-366 §C.1 Animation Phase 9 ✅ FULLY CLOSED 2026-05-25; ADR-345 spellCheck ✅ DONE 2026-05-23, ADR-034 UC-017 ✅ DONE 2026-05-23; **ADR-363 Phase 2 canvas-wiring follow-up ✅ 2026-05-25**.)

---

## Changelog

| Date       | Change |
| 2026-07-16 | **✅ i18n namespace array SSoT — 137-file dedup DONE (Opus 4.8, orchestrator-approved by Giorgio).** Closes the "i18n namespace array — 143 αρχεία" entry above. Το byte-identical 10-item `common`/`common-*` array σε **137 αρχεία** → ΕΝΑ `COMMON_NAMESPACES` const (`src/i18n/namespace-bundles.ts`); όλα + τα 2 sales sidebars → `useTranslation(COMMON_NAMESPACES)` (το interim `SALES_SPACE_SIDEBAR_NAMESPACES` αφαιρέθηκε — ήταν byte-identical). **Non-obvious half (γι' αυτό ΔΕΝ ήταν "τετριμμένο find/replace"):** το CHECK 3.8 (`check-i18n-missing-keys.js`) + ο baseline generator μάθαιναν τα namespaces με regex στο **literal** `useTranslation([...])` — bare const ref → `namespaces.length===0` → **silent skip** = χάσιμο i18n key validation σε 137 αρχεία (και τα 2 sidebars ήταν ΗΔΗ τυφλά από το προηγούμενο session). Fix: shared `scripts/lib/i18n-namespace-extract.js` (SSoT — έσβησε το διπλό `extractNamespaces`) που resolve-άρει bundle identifier → array διαβάζοντας το `namespace-bundles.ts` (mirror CHECK 3.13). `useTranslation` param `string[]`→`string | readonly string[]` για `as const`. **Verified coverage-neutral:** per-file `git show HEAD` differential (identical ns set inline↔const, NotificationProvider 10↔10)· CHECK 3.8 exit 0 σε 139 αρχεία· CHECK 3.28 πράσινο· 8 νέα jest GREEN (`i18n-namespace-extract.test.js`). Μηδέν αλλαγή σε locales/`t()`/runtime. ADR-584 changelog updated. Pending commit (Giorgio· shared tree → `git commit -o <paths>`). |
| 2026-05-29 | **✅ ADR-365 green-707 TYPO INCIDENT — RESOLVED.** Closes the 2026-05-22 discovery entry below. Global rename **303 occurrences / 181 files**: `text-green-707`/`hover:text-green-707` → `text-[hsl(var(--text-success))]`; `bg-green-707`/`data-[state=checked]:bg-green-707`/`:bg-green-707` → `bg-[hsl(var(--text-success))]` (solid green-700 fill — `--text-success`=`142 76% 36%`, NOT the green-50 `--bg-success` subtle surface); `border-green-707`→`border-[hsl(var(--text-success))]`; `border-l-green-707`→`border-l-[hsl(var(--text-success))]`; `ring-green-707`/`focus:ring-green-707`→`ring-[hsl(var(--text-success))]`. Arbitrary-value classes don't match PALETTE_REGEX → baseline stays **0/0**. **Ratchet hardened** (`scripts/check-tailwind-palette-ratchet.js`): new `INVALID_SHADE_REGEX` + `countInvalidShades()` hard-block any `(bg|text|border|ring|fill|stroke)(-[lrtbxy])?-(palette)-(\d+)` whose shade ∉ `SHADES` (zero-tolerance, no baseline — catches future typos). Wired into staged-files path + `--report`/`--all` audit. Smoke-tested (green-707 incl. `data-[state=checked]:` blocks; green-700 still caught by PALETTE_REGEX; var-class passes). **Boy-scout (N.0.2)**: `ThermalEnvelopeDialog.tsx` (ADR-396) regressed 4 raw-palette violations past 0/0 baseline (`text-amber-600 dark:text-amber-400` ×2) → `text-[hsl(var(--text-warning))]`; `OrgStructureTab.tsx` 4× glued-class `text-muted-foregroundhover:` → space fix. Docs updated: ADR-365 (status APPROVED + §2.1 correction + changelog), adr-index, local_ΕΚΚΡΕΜΟΤΗΤΕΣ.txt, memory ×2. tsc clean (exit 0). grep green-707 src = 0. Pending commit. |
| 2026-05-26 | **ADR-375 v2.6 — Phase C.4 V/G Visibility Runtime Wiring HOTFIX.** Surface (Giorgio runtime report): toggling V/G eye icon στο ribbon "Ορατότητα/Γραφικά" δεν έκρυβε entities — οι κατηγορίες συνέχιζαν να εμφανίζονται 2D + 3D. Root cause: `resolveIsCategoryVisible` helper υπήρχε από v2.2 αλλά **κανείς renderer δεν τον καλούσε στο render-entry**. Ο resolver μόνο zero-ώνει lineWidthPx (stroke), ενώ fill / hatch / hover halo / hinge arcs / glazing / tread polygons / 3D mesh+edge overlay τρέχουν εκτός resolver path. Επιπλέον BimViewport3D δεν είχε subscription στο objectStyles. **Fix**: (a) 7 BIM 2D renderers (Wall/Slab/Column/Beam/Opening/SlabOpening/Stair) — one-line early-return στο top του render() μετά το type guard: `if (!resolveIsCategoryVisible('<cat>', useDrawingScaleStore.getState().objectStyles)) return;`. (b) BimSceneLayer.sync() — snapshot objectStyles once, derive 7 booleans, wrap κάθε entity loop. Wall/slab loops with own visibility=true πλέον περνάνε empty openings array όταν opening/slab-opening visibility=false (host stays solid, IFC cutout semantic preserved). (c) BimViewport3D.tsx — new useEffect subscribing σε `useBimRenderSettingsStore.subscribe((s) => s.objectStyles, ...)` που re-issues syncBimEntities. Mirror του existing entity-store subscription. **Files**: 7 renderers + BimSceneLayer + BimViewport3D + 2 new tests = 11 files (9 modified + 2 new). Tests: bim-renderers-vg-visibility (7 renderers × it.each) + BimSceneLayer-vg-visibility (10 cases). Existing 24/24 V/G resolver tests PASS unchanged. TSC clean. ADR-375 Phase C closed για 2η φορά (runtime parity τώρα 1:1 με panel). |
| 2026-05-26 | **ADR-375 v2.5 — Phase C.7 3D BIM Edge Overlay Parity IMPLEMENTED → ADR-375 ALL PHASES COMPLETE.** Mirror του 2D SSoT priority stack στο 3D BIM viewer. Industry convergence (web research Phase 1: Autodesk Forge / Three.js Editor / Sketchfab / Revit / ArchiCAD): `Line2 / LineSegments2 + LineMaterial + LineSegmentsGeometry` from `three/examples/jsm/lines` — `LineBasicMaterial.linewidth` ignored by WebGL (πάντα 1px σε όλα τα browsers εκτός Firefox), Line2 = industry standard για thick screen-space lines. `THREE.EdgesGeometry(geo, 30°)` silhouette filter (Revit/ArchiCAD default). `linewidth × devicePixelRatio` High-DPI crisp (Revit True Line Weight pattern). `depthTest:true / depthWrite:false` (edges visible χωρίς z-fighting με surface). `alphaToCoverage:true` (MSAA edge smoothing). **4 NEW files**: `bim-3d/edges/bim-3d-edge-resolver.ts` (thin wrapper γύρω από υπάρχον `resolveSubcategoryStyle` — zero duplication, default `thresholdAngle=30`, exports `Resolved3DEdgeStyle {lineWidthPx, color, visible, thresholdAngle}`), `bim-3d/edges/bim-3d-edge-overlay-builder.ts` (`buildEdgeOverlay(mesh, opts) → LineSegments2 | null` + `attachEdgeOverlay(mesh, overlay)` helpers, geometry.dispose unsubscribes resolution store → zero leaks, default fallback color `#1a1a1a` Revit edge), `bim-3d/edges/bim-edge-resolution-store.ts` (singleton με `setSize/getSize/copyInto/subscribe` API — ADR-040 compliant, fires only on real resize change), 2 test suites (`bim-3d-edge-resolver.test.ts` 18/18 + `bim-3d-edge-overlay-builder.test.ts` 20/20 = **38/38 PASS**). **5 MODIFIED**: `BimToThreeConverter.ts` (4 entity types — wall/column/beam/slab — via `attachEdgesProjection(mesh, category)` helper με `scaleDenominator=100 + dpi=96` defaults + `cutState='projection'` για 3D solids), `wall-opening-extrude.ts` (per-segment edge attach μέσα στο segment loop, mirror του ADR-363 Bug 2 pattern), `StairToThreeConverter.ts` (`attachStairEdges(mesh, subcategoryKey?)` helper με ADR-377 SUBCATEGORY_TAXONOMY keys: tread→`treads`, riser→`risers`, stringer→`outlines`, landing→parent stair; handrails skipped — round tubes σε 30° δεν παράγουν αναγνωρίσιμη σιλουέτα), `ThreeJsSceneManager.ts` (one-line wire στο `resize()` → `bimEdgeResolutionStore.setSize(width, height)`). **Jest infra fix**: added pnpm-aware `transformIgnorePatterns: ['node_modules/(?!(?:\\.pnpm/[^/]+/node_modules/)?three/)']` to root `jest.config.js` — enables Jest to transform Three.js ESM examples (LineSegments2.js/LineMaterial.js/LineSegmentsGeometry.js) που είναι pure ESM. **Lifecycle**: edge overlays = children των parent BIM meshes (`mesh.add(overlay)`), `BimSceneLayer.clearGroup()` recursive dispose τα καλύπτει automatic, raycast hits παραμένουν στο parent mesh (overlays = visual decoration, αόρατα στο raycaster). **`.ssot-registry.json` extended**: 9 new entries στο `bim-line-weight-resolver` allowlist (3 edge module files + 2 tests + 4 modified converters/scene-manager). **Outcome**: όταν αλλάζει layer override (C.6) ή element override (C.5) ή V/G visibility (C.4), οι 3D BIM ακμές αντιδρούν ζωντανά με τους ίδιους priority κανόνες όπως το 2D plan view. Pure SSoT, μηδέν resolver duplication, full Revit parity 2D↔3D. **76/76 2D regression tests PASS** (zero impact στο υπάρχον 2D pipeline). TSC clean. **Phase C ολοκληρωμένο.** |
| 2026-05-26 | **ADR-375 v2.4 — Phase C.6 Layer-Driven Lineweight Override IMPLEMENTED.** `BimLayerOverride` interface + `SubcategoryResolutionContext.layerOverride?` field. `resolveSubcategoryStyle` C.6 priority block: concrete `layerOverride.lineweightMm` → `lineweightToPx(mm, dpi)` bypass pen table (between C.5 elementOverride and subcategory pen). Color priority: elementOverride > layerOverride > subcategory/parent V/G. Pattern unaffected. 7 BIM renderers wired (`getLayer(entity.layerId)` + `isConcreteLineweight` guard → `_layerOverride`). WallRenderer passes `layerOverride` as param to `drawFootprint` + `drawMaterialHatch` (computed once in `render()`). ColumnRenderer replaces `resolveLineWeightPx` → `resolveSubcategoryStyle` to carry layerOverride context. 16/16 new tests PASS (`bim-layer-override.test.ts`). 181/181 config suite PASS (zero regression). |
| 2026-05-26 | **ADR-375 v1.9 — StairRenderer + OpeningRenderer secondary-line coverage HOTFIX.** Stairs δεν αντιδρούσαν σε DrawingScale / ViewRange Cut Plane / ObjectStyles (Giorgio runtime report). Root cause: v1.7 hotfix κάλυψε 6 BIM renderers (Wall/Slab/Column/Beam/Opening/SlabOpening) αλλά **όχι StairRenderer** (διαφορετικό pattern, υπήρχε partial migration σε 1/3 line widths) + `OpeningRenderer` είχε wired μόνο κύρια γραμμή, τα 4 secondary cues (hingeArc/leaf/sliding/glazing) hardcoded. **Fix**: (a) `StairRenderer.render()` υπολογίζει μία φορά `cutState = resolveCutState({zBottomMm: basePoint.z, zTopMm: basePoint.z+totalRise, category: 'stair'}, ds.viewRange)` + `baseLineWidth = resolveLineWeightPx({category: 'stair', cutState, scaleDenominator, dpi: 96, objectStyles})` και τα προωθεί στο `StairStyleContext` (+`baseLineWidth: number` field, required) + στα `drawHandrails/drawWalkline/drawArrow` params. Removed `RENDER_LINE_WIDTHS` import + hardcoded `cutState:'cut'` στο drawArrow. (b) `stair-render-structure-style.ts` — 8 occurrences `RENDER_LINE_WIDTHS.{THICK,NORMAL,THIN}` → `scx.baseLineWidth`. Visual hierarchy μέσω dash patterns (suspended dashed, walkline dashed, handrails dashed). Per-element hierarchy via Object Subcategories Phase C.3 (pending). (c) `OpeningRenderer.ts` — `_opBaseLineWidth` προωθούμενο από render → drawKindOverlay → drawHingeArc/drawSlidingIndicator/drawGlazing. 4 hardcoded lines 177/182/214/232 → `baseLineWidth`. Hover halo lines (StairRenderer:84, OpeningRenderer:60) εσκεμμένα διατηρούν `RENDER_LINE_WIDTHS.NORMAL` (scale-independent UX glow). **Files**: 3 modified (`StairRenderer.ts`, `stair-render-structure-style.ts`, `OpeningRenderer.ts`) + 1 test factory updated (`__tests__/stair-render-structure-style.test.ts` — `makeScx()` παίρνει optional `baseLineWidth = 1.5`). **Tests**: 16/16 stair-render-structure-style PASS. TSC clean. Pending commit (must isolate from άλλου agent ADR-376 Phase C.2 work που υπάρχει στο working tree). |
| 2026-05-26 | **ADR-376 Phase B.2 DONE — BOQ signature-group aggregation.** Single aggregated BOQ row per Mode C signature group (kind+width+height+sillHeight+openDirection). Revit Schedule pattern, 6/6 industry convergence (Revit / ArchiCAD / Tekla / Allplan / Bentley / Vectorworks). Drift fix στο ADR-376 §7 B.2 row — αρχικό "mirroring multi-layer wall pattern" ήταν λάθος γιατί openings είναι atomic (no multi-component layers); διορθώθηκε σε "single aggregated row per signature group". 2 new files + 3 modified + 29/29 tests PASS. New: `bim/services/opening-boq-grouper.ts` (pure SSoT — computeOpeningSignature + signatureKey + signatureGroupBoqId + compactMarkRange Revit-style range collapsing `Π.001..Π.150` + groupBySignature bucket + buildOpeningGroupPayload BOQItem factory με enriched title `Κούφωμα παραθύρου (BIM) — 1200×1400 (sill 900)` + description `Marks: Π.101..Π.150`; zero Firestore I/O), `bim/services/__tests__/opening-boq-grouper.test.ts` (29 assertions: signature stability 5 + group ID 2 + mark range 9 + group aggregation 4 + payload building 9). Modified: `bim/services/BimToBoqBridge.ts` (+upsertOpeningGroupForOpening με old+new signature dual recompute όταν params μεταβάλλεται · +deleteOpeningFromGroup · +private recomputeSignatureGroup με detach guard + delete-when-empty + createdAt preservation · +private fetchOpeningsForSignature με (companyId, projectId, floorplanId, kind) Firestore query + JS-side signature filter · legacy `upsertBoqItemForBim('opening', …)` πλέον warns + skips · +floorplanId? optional στο BimBoqContext), `hooks/data/useOpeningPersistence.ts` (persist callback captures prevParams από lastSavedParamsRef πριν την save για old-signature recompute · pass entity.params + floorplanId στο bridge · delete path passes lastKnownParams από ref ή deleted scene entity), `ADR-376-opening-tags.md` (§7 B.2 row drift fix + status DONE + §11 v7 changelog + status header PHASE_B2_DONE), `ADR-175-quantity-surveying-measurements-system.md` (§4.3 BIM auto-feed patterns sub-section — wall multi-layer vs opening signature aggregation distinction). Pending: Phase C (draggable tag) + `.ssot-registry.json` opening-boq-grouper entry (surgical commit όταν καθαρίσει working tree). Migration TODO: legacy `boq_bim_<openingId>` rows από Phase A single-entry path (αν υπάρχουν production data) ΔΕΝ διαγράφονται αυτόματα. |
| 2026-05-25 | **ADR-366 §C.1.b Waypoint 3D drag interaction DONE.** Closes the largest deferred item from C.1.b post-completion: waypoints are now draggable in the 3D viewport (not just visual). 3 new files + 3 modified + 17 new tests. New: `waypoint-drag-controller.ts` (pure FSM idle/hovering/dragging + camera-aligned drag plane Blender/AutoCAD pattern + Three.js Raycaster pick + ray.intersectPlane projection + exported pure helpers `setNdcFromClient` + `computeCameraAlignedPlane`), `use-waypoint-drag-interaction.ts` (React hook με DOM pointer listeners attached MONO όταν `AnimationStore.toolActive===true` + AbortController per session για clean teardown + setPointerCapture για robust drag across canvas edges + single-writer SSoT to `AnimationStore.updateWaypoint`), `__tests__/waypoint-drag-controller.test.ts` (17 tests: NDC conversion 4 + camera-plane math 1 + FSM transitions 7 + pick raycast 2 + drag projection 3). Modified: `WaypointDragHandle.ts` (+`getHandlesGroup(): Group|null` raycast exposure + `setHoverState(role|null)` με `paintSprite` helper + cold/hot palette mirror Dim3DGripsRenderer pattern, removed legacy warm/hot mix), `ThreeJsSceneManager.ts` (+`getWaypointHandlesRoot()` + `setWaypointHoverState()` getters), `BimViewport3D.tsx` (+`useWaypointDragInteraction({managerRef, canvasEl})` hook call). Tests: 50/50 PASS (33 existing C.1.c + 17 new drag). Still deferred from C.1.b: bezier 4-point editor (§C.1.Q4 «Προχωρημένα»), real scene-bbox για turntable (synthetic camera-distance still in use), TimelineEditor+WaypointDragHandle React component tests, axis-constrained gizmo arrows (X/Y/Z), snap-to-grid. |
| 2026-05-25 | **ADR-366 §C.1.c Animation Rendering / Queue DONE → Animation Phase 9 FULLY CLOSED.** 8 new + 6 modified files + Storage rules block + package.json (+mp4-muxer@5.2.2 MIT). New: MP4Exporter.ts (WebCodecs `VideoEncoder` H.264 Main L3.1 primary + VP9 Profile 0 fallback inside MP4 via mp4-muxer; dedicated offscreen WebGLRenderer + PerspectiveCamera so live viewport canvas stays untouched; rAF yield + AbortSignal), RenderQueueStore.ts (Zustand FIFO + activeJobId + module-scope `Map<jobId, AbortController>` non-serializable + idempotent enqueue + hydrateFromFirestore equality guard preserving runtime blobUrl), RenderQueuePanel.tsx (Floating3DPanel 8th tab; semantic <progress> + status badge palette via ADR-365 CSS vars + cancel/retry/download/remove + ETA), animation-queue-processor.ts (React hook glue; status-fingerprint subscription claims next queued job when idle; drives MP4Exporter + uploads to Firebase Storage + throttled progress persistence 1.5s + checkpoint serialization on AbortError → cancelled-resumable), render-checkpoint.ts (pure serialize/deserialize, semantic field reuse `lastSampleCount` → `lastFrameIndex`), animation-action-handlers.ts (extracted `handleAnimationSave` + `handleAnimationExport` keeping useDxfViewerCallbacks under 500-line cap; auto-save creates bim_animation doc when loadedDocId===null με default name `t('animation.defaultName',{time:HH:mm})`), origin-indicator-overlay.ts (SVG crosshair helper extracted from useDxfViewerCallbacks for SRP), 3 test suites under __tests__/. Modified: Floating3DPanel ('renders' tab union member + conditional via selectAnyJobs + w-72 widen), contextual-animation-tab.ts (animation.save + animation.export `comingSoon: true` REMOVED + real actions wired), useDxfViewerCallbacks.ts (+useAuth + animation.save/animation.export cases dispatching to handlers + bim3d namespace), BimViewport3D.tsx (+useAnimationQueueProcessor mount + notification callbacks wired), notification-keys.ts (+bim3d.animation.{render,save,export}.* — 8 keys), i18n el/en bim3d.json (+animation.notification.* + animation.queue.* + animation.defaultName + floatingPanel.tabs.renders, ~15 keys × 2 locales). Storage rules: new block `/companies/{companyId}/bim_animations/{animationId}/renders/{fileName}` (500MB cap, mp4/webm only). ADR drift resolved (4): (1) §C.1.Q9 standard rasterizer not path-tracer (impractical 4h+/animation); (2) §C.1.Q8 project_assets DROPPED (service does not exist; mp4 stored direct to Storage); (3) §C.1.Q7 VP9-in-MP4 fallback (separate WebM lib deferred); (4) checkpoint field reuse. Cross-session queue subscribe DEFERRED Phase 10. Auto-save policy: Giorgio chose option A (auto-create + activate Save). C.1.c checklist + ratchet entry removed. Animation Phase 9 ολόκληρη CLOSED. |
| 2026-05-25 | **ADR-366 §C.1.b Animation UX/Timeline DONE** — 10 αρχεία (4 new + 6 modified) + 100 i18n entries. New: TimelineEditor.tsx (~280 LOC vertical waypoint editor) + TimelineWaypointForm.tsx (~115 LOC properties form split για N.7.1) + WaypointDragHandle.ts (~165 LOC Three.js Sprite renderer mirror Dim3DGripsRenderer) + contextual-animation-tab.ts (~165 LOC declarative RibbonTab). Modified: AnimationStore (+toolActive + selectAnimationToolActive — SSoT deviation από brief: domain store ΟΧΙ ViewMode3DStore, mirror BimDimensions3DStore pattern), Floating3DPanel (+7th tab 'animation', conditional w-48→w-72 widen design risk resolution), ribbon-contextual-config (+ANIMATION trigger early-return), ThreeJsSceneManager (+WaypointDragHandleRenderer lifecycle), useDxfViewerCallbacks (+5 animation actions: tool-toggle/turntable/add-waypoint/delete-waypoint/reverse + syntheticBboxFromCamera helper), bim3d.json el/en (+50 keys × 2 locales pure Greek). Deferred (NOT blocker): drag interaction (raycaster + mouse handler), bezier 4-point editor, real scene-bbox, unit tests. C.1.b checklist removed. Pending: only C.1.c. |
| 2026-05-25 | **ADR-366 §C.1.a Animation Logic Foundation DONE** — 12 αρχεία (7 new + 5 SSoT extensions) + 5 test suites. C.1 split σε 3 sub-phases (C.1.a / C.1.b / C.1.c) στο ADR §C.1 Implementation Phases. Pending entry για C.1.b (UX/Timeline ~7-9h) + C.1.c (Rendering/Queue ~6-8h) added. SSoT extensions: enterprise-id `anm_bim_`/`rnj_bim_`, COLLECTIONS.BIM_ANIMATIONS + SUBCOLLECTIONS.BIM_RENDER_JOBS, RBAC `bim_animations:animations:{C,R,U,D}` × 4 roles + read viewer, AuditEntityType `'bim_animation'`, Firestore rules block. |
| 2026-05-24 | **Selection SSoT Cleanup DONE** — Removed dual-write pattern from `CanvasLayerStack.tsx`. `universalSelection` is now the ONLY write path for entity/overlay selection. `setSelectedEntityIds` removed from `entityState` type + CanvasSection prop. ADR-040 changelog updated. 3 files changed (CanvasLayerStack.tsx, canvas-layer-stack-types.ts, CanvasSection.tsx). |
| 2026-05-22 | **🚨 ADR-365 green-707 TYPO INCIDENT — DOCUMENTATION INCONSISTENCY DISCOVERED.** Phases 3-8 migration mapping `green-*` → `text-green-707` / `bg-green-707` is a TYPO (intended `green-700`, the COLOR_BRIDGE canonical at `src/design-system/color-bridge.ts:142`). `green-707` is **not** a valid Tailwind shade (`SHADES = ['50','100',…,'700',…,'950']` in `scripts/check-tailwind-palette-ratchet.js:80`) and is **not** defined anywhere in `tailwind.config.ts`, `src/styles/`, or `globals.css`. Impact: **304 occurrences across 183 files** silently emit no CSS — Tailwind JIT scans source, finds no class definition, drops the utility. Visual result: success badges / form-validation labels / terminal logs / toggles render as default text color in those files. **Why baseline reads 0/0**: the ratchet regex looks for valid shades only; `green-707` bypasses it entirely. The "complete" status of ADR-365 is **only formally complete** — semantic intent is correct but emitted CSS is broken for green success states. **Pending fix** (next phase): global rename `green-707` → semantic token (`text-[hsl(var(--text-success))]` or equivalent) + remove "WCAG exception" entry from ADR-365 §2.1 + add invalid-shade detection to ratchet script (catch `green-\d+` not in `SHADES` list). Origin commit `1788cad9` (Phase 5). |
| 2026-05-22 | **ADR-365 Phase 8 Final Closure — APPROVED.** 36 files fixed (ThemeProgressBar, ParkingGeneralTab, StorageGeneralTab, RecipientsList, OverlayListCard, PropertyGridCard, FloorPlanViewer, select-styles, TechnicalAlertConfigPanel, ProposalActionContent, EnterpriseMigrationPageContent, PropertyStatusDemoPageContent, ContactsTabContent, CompactToolbar/types, ThreadView, GenericPeriodSelector, PhotosTabBase, HeaderBar, AIQueryInput, ChequeDetailDialog, EmptyState, EntityFilesToolbar, DescriptionNotesCard, calendar, TemplateSelector, EnterprisePhotoUpload, base-tabs, PropertyStatusSelector, PageErrorState, CardIcon, AgreementListCard, LevelListCard, MaterialListCard, PurchaseOrderListCard, QuoteListCard, design-system.ts, text-utils.ts, sidebar-utils.ts, NotificationProvider, modal-layout.ts, UserTypeSelector, FloorPlanPreview, FloorPlanUploadModal, PolygonControls). Baseline: 36/36 → **0/0**. npm run tailwind-palette:baseline run. ADR-365 status: APPROVED. |
| 2026-05-22 | **ADR-365 Phase 5 DONE — Shared Files + File Manager**. 9 files: ArchiveView, TrashView, VersionHistory, UploadEntryPointSelector, HierarchicalEntryPointSelector, hierarchical-entry-cards, CameraCaptureDialog, ApprovalPanel, BatchActionsBar. Baseline: 2,740/371 → 2,667/362 (−73 violations, −9 files). Mappings: orange/amber/yellow→warning tokens; red→destructive; green→text-green-707/bg-success; violet→primary; dark: removed; eslint-disable comments removed. |
| 2026-05-22 | **ADR-365 Phase 2 PARTIAL — Procurement + Vendor Portal**. 43 files committed (VendorPortal × 5, ExtractedDataReviewPanel, SetupLockBanner, SignatoryProposalCard, SignatoryDisambiguationModal, SourcingEventSummaryCard, QuoteLineEditorTable, QuoteDetailsHeader, QuoteEditMode, ComparisonPanel, ComparisonWinnerBanner, RecommendationCard, OfflineBanner, QuoteRevisionDetectedDialog, extracted-data-review-helpers, ProcurementSubNav, VendorDetail, VendorCard, SupplierComparisonTable, SupplierMetricsCard, PurchaseOrderForm, PurchaseOrderKPIs, AgreementDetail, MaterialDetail, hub/cards × 6, ContactRfqInvitesSection, ProcurementContactTab, ProjectProcurementTabs, KpiPendingApprovalPos, VendorGridCard, VendorListCard, scan/page, RfqDetailClient, AnalyticsKpiTiles). Baseline: 3,405/420 → 3,034/400 (−371 violations, −20 files). ConflictDialog migration in working tree (−2 more), blocked by CHECK 3.22 dead-code ratchet — pending Giorgio decision. ADR-365 status + changelog + adr-index + MEMORY.md updated. |
| 2026-05-22 | **ADR-365 Phase 1 DONE — DXF Viewer subapp**. 20 files cleaned: GripContextMenu, GripHoverMenu, WallDnaSection, WallPersistenceSection, StairWarningsSection, StairPersistenceSection, StairPresetsSection, StairPerTreadOverrideSection, DimensionsTab, DraftRecoveryBanner, SpellCheckContextMenu, TextTemplateList, PlaceholderPicker, CustomDictionaryEditorDialog, MirrorConfirmOverlay, DraggableOverlayToolbar, PolygonControls, IsolateStatusIndicator, FloorplanBackgroundPanel, PromptDialog. Baseline: 3,659/440 → 3,405/420 (−254 violations, −20 files). ADR-365 status + changelog updated. |
| 2026-05-21 | ADR-363 BIM entity points SSoT migration CLOSED — `slab-grips.ts` + `slab-opening-grips.ts` + `BeamRenderer.ts` (4 methods) migrated to `getBimEntityKeyPoints2D`. Transforms/geometry/validator files intentionally skipped (need 3D `Point3D`, SSoT returns 2D only). Broken import `bim-entity-points.ts` `'../extended-types'` → `'../../types/entities'` fixed. Item removed from checklist. |
| 2026-05-19 | ADR-363 R1 DONE — `useBimCopyTool` (AutoCAD COPY FSM + full canvas wiring). ADR-363 R2 DONE — stair bridge helpers moved to `bim/hooks/bridge/`, cross-domain coupling fixed. Both removed from checklist. |
| 2026-05-15 | ADR-345 Fasi 6.1 + 7 completate. Fase 8 parziale (colors rimossa, levels rimane per ora). |
| 2026-05-12 | ADR-345 Fase 5.5 pending items aggiunti (findReplace/spellCheck/symbol comingSoon, commit chain ADR-344 Ph6+, Fasi 6/7/8). STATUS → ACTIVE. |
| 2026-05-04 | ADR-233 CLOSED — tutti e 3 gli item già implementati nel codice (POST 409 route.ts:220-233, PATCH 409 building-update.handler.ts:104-119, BuildingListCard formatBuildingLabel, unit tests entity-code-config.test.ts). Ratchet era stale. |
| 2026-05-04 | ADR-314 CLOSED — CHECK 3.18 baseline 0/0/0 ALL GREEN dal 2026-04-19 (Phase A-E DONE). `.ssot-discover-baseline.json` confirma centralizedFiles=135 protected=135 unprotected=0 duplicateExports=0 antiPatterns=0. STATUS → ALL_DONE. |
|------------|--------|
| 2026-04-11 | Initial checklist dump from ADR-299 §4/§5. Scenario A = 21h expected, Scenario B = 64h expected. No task completed yet — B.1 attendance already completed before ADR-299. |
| 2026-04-11 | CHECK 3.17 baseline ratcheted 20→19 in commit `56d95be4` (production safety hardening: deletion of `properties/admin-link` destructive chain). Was not a CHECK 3.17 cleanup target — was security deletion that reduced baseline as side-effect. Line updated. Hours estimate unchanged. |
| 2026-04-11 | CHECK 3.17 baseline 19→18 in commit `f160e750` (cluster B triage: deletion of `admin/migrate-properties` break-glass one-shot with hardcoded Greek apartment templates + tenant-specific building IDs). Expected hours updated 6→5.5 (removed 1 file from Boy Scout queue). Cluster (B) — 7 files still pending (migrate-building-features, migrate-company-id, migrate-enterprise-ids route+ops, migrations/execute-admin, migrations/normalize-floors, seed-floors, seed-parking) — deferred to focused per-file wire-up commits. |
| 2026-04-13 | ADR-298 Phase B.2 COMPLETED. accounting_audit_log + accounting_invoices + accounting_journal_entries moved to COVERAGE. New `roleDualMatrix()` + 3 seeders + 3 suites. Pending list 86→83, coverage 8→11 collections. Removed from checklist. |
| 2026-04-13 | ADR-298 Phase B.3 COMPLETED. leads + opportunities + activities moved to COVERAGE. New `crmDirectMatrix()` + 3 seeders + 3 suites. Pending list 83→80, coverage 11→14 collections. Removed from checklist. |
| 2026-04-13 | ADR-298 Phase B.4 COMPLETED. properties + storage_units + parking_spots + floors moved to COVERAGE. `adminWriteOnlyMatrix()` base + overrideCells for properties update delta. 4 seeders + 4 suites. 365 tests green. Pending list 80→76, coverage 14→18 collections. Removed from checklist. |
| 2026-04-13 | ADR-298 Phase B.5 COMPLETED. conversations + external_identities moved to COVERAGE. `crmDirectMatrix()` for both; conversations enum-validated via `isValidConversationData`. New `seed-helpers-messaging.ts` module (SRP split). 405 tests green. Pending list 76→74, coverage 18→20 collections. Removed from checklist. |
| 2026-04-13 | ADR-298 Phase B.6 COMPLETED. obligations + obligation_transmittals + obligation_templates moved to COVERAGE. All three use `crmDirectMatrix()` verbatim (no overrideCells). New `seed-helpers-compliance.ts` module (SRP — messaging+seed-helpers both at 500 lines). 3 new suites. 465 tests green (23 suites). Pending list 74→71, coverage 20→23 collections. Removed from checklist. |
| 2026-04-13 | CHECK 3.17 Batch 1 DONE. 3 admin migration files wired: execute-admin/route.ts (projects), normalize-floors/route.ts (floors), migrate-building-features/migration-operations.ts (buildings). Baseline 18→15. |
| 2026-04-13 | CHECK 3.17 Batch 2 DONE. 4 admin files wired: seed-floors.handlers.ts (floor deleted), migrate-company-id/migration-operations.ts (company created), migrate-enterprise-ids/migration-operations.ts (building updated), migrate-enterprise-ids/route.ts (building created). Baseline 15→11. |
| 2026-04-13 | CHECK 3.17 Batch 3 DONE. 2 files wired: geofence/route.ts (project updated), seed-parking/parking-seed-operations.ts (parking created). Baseline 11→9. ownership-table-service.ts deferred (client-side SDK, needs server wrapper approach). |
| 2026-04-13 | CHECK 3.17 Batch 4 DONE — ZERO BASELINE. 4 server-side wire-up (005_assign_project_codes, 006_normalize_storage, cascade-propagation, property-coverage-recalculator) + 5 HARD_EXEMPT in scanner (audit-core circular, 4 client-SDK files). Baseline 9→0. CHECK 3.17 completamente chiusa. |
| 2026-04-13 | CHECK 3.13 Phase A DONE. 14 namespace aggiunti a `SERVICE_FORM_NAMESPACES` in translate-field-value.ts. Baseline 378→214 (5 file rimasti). ADR-279 aggiornato. |
| 2026-04-13 | CHECK 3.13 Phase B DONE. 3 namespace aggiuntivi (`projects`, `properties-enums`, `storage`) + prefix-strip fallback in `translateFieldValue` e scanner `resolves()`. Baseline 214→79 (5 file rimasti). ADR-279 aggiornato. |
| 2026-04-13 | ADR-298 Phase C.1 DONE. 16 accounting collections → COVERAGE. 4 new matrix builders + coverage-matrices-accounting.ts (SRP split) + seed-helpers-accounting.ts + 16 suites. Pending 71→55, coverage 23→39. |
| 2026-04-13 | CHECK 3.13 Phase C DONE — ZERO BASELINE. 79 chiavi locale mancanti aggiunte in 9 namespace (el+en): contacts-core (company.sections/fields), storage (card.stats.level/value/stage/priority/dueDate + types.parking), building-tabs (floorplan.description + protocols.description), building (propertyTypes.*), properties-enums (status.underConstruction/blocked + saleStatus.*), dxf-viewer (steps.*), contacts-relationships (status enum + 22 types), crm (stages.*), filters (allPrices). Baseline 79→0. CHECK 3.13 completamente chiusa. ADR-279 Phase C entry aggiunto. |
| 2026-04-13 | ADR-298 Phase C.5 DONE. 11 system-global collections → COVERAGE. 4 new matrix builders (systemGlobalMatrix, systemAdminGlobalMatrix, countersMatrix, tasksMatrix) + coverage-matrices-system.ts (SRP split) + seed-helpers-system.ts + 11 suites. Pending 55→44, coverage 39→50. |
| 2026-04-14 | ADR-298 Phase C.2+C.3 DONE. 18 DXF/CAD/floorplan + file management collections → COVERAGE. 7 new matrix builders (fileTenantFullMatrix, cadFilesMatrix, fileAuditLogMatrix, fileSharesMatrix, photoSharesMatrix, fileCommentsMatrix, fileApprovalsMatrix) + coverage-matrices-dxf.ts (SRP split) + seed-helpers-dxf.ts + 18 suites. 1477 tests green (68 suites). Pending 44→26, coverage 50→68 collections. |
| 2026-04-14 | ADR-298 Phase C.4 DONE. 5 BoQ/commissions/ownership collections → COVERAGE. 4 new matrix builders (boqCategoriesMatrix, brokerageMatrix, commissionRecordsMatrix, ownershipTablesMatrix) + coverage-matrices-boq.ts (SRP split) + seed-helpers-boq.ts + 5 suites. 119 tests green (5 suites). Pending 26→21, coverage 68→73 collections. |
| 2026-04-14 | ADR-298 Phase C.6 DONE. 8 ownership-based collections → COVERAGE: companies, security_roles, users, user_notification_settings, user_2fa_settings, workspaces, teams, positions. 3 new matrix builders (companiesMatrix, usersMatrix, ownerOnlyMatrix) + coverage-matrices-users.ts (SRP) + seed-helpers-users.ts + 8 suites. 198 tests green. Pending 21→13, coverage 73→81 collections. |
| 2026-04-14 | ADR-298 Phase C.7 DONE — PHASE C COMPLETA. 11 specialized collections → COVERAGE: contact_relationships, contact_links, relationships, relationship_audit, employment_records, notifications, audit_logs, system_audit_logs, audit_log, search_documents, voice_commands. 5 new matrix builders (contactRelationshipsMatrix, employmentRecordsMatrix, notificationsMatrix, auditLogMatrix, searchDocumentsMatrix, voiceCommandsMatrix) + coverage-matrices-specialized.ts (SRP) + seed-helpers-specialized.ts + 11 suites. 291 tests green. Pending 13→0 (VUOTA). Coverage 81→92 collections. FIRESTORE_RULES_PENDING = []. |
| 2026-04-14 | ADR-301 Phase A DONE — Storage Rules Coverage SSoT. 4 path patterns → COVERAGE (canonical_with_project, canonical_no_project, cad, temp). Harness: emulator.ts + auth-contexts.ts + seed-helpers.ts + assertions.ts. Registry: personas.ts + operations.ts + coverage-manifest.ts. 4 test suites, 48 cells. CHECK 3.19 (zero-tolerance, pre-commit). jest.config.storage-rules.js + 5 npm scripts. ADR-298 Phase E chiusa. |
| 2026-04-26 | ADR-233 uniqueness validation + ADR-314 SSoT discovery aggiunti come pending. STATUS tornato ACTIVE. SSoT violations (53→0) già DONE dal 2026-04-09 — entry MEMORY.md corretta. |
| 2026-04-14 | CHECK 3.8 i18n Missing Keys DONE — ZERO BASELINE. 4,750→0 violazioni in 3 fasi: (1) 730 file single-ns→array (ADR-280 namespace split: dxf-viewer+5 subs, common+9 subs, building+5 subs, contacts+5 subs, properties+3 subs, projects+2 subs, accounting+2 subs, crm+1 sub, files+1 sub); (2) 479 chiavi genuinamente mancanti aggiunte a 30 locale files el+en; (3) settings.json creato (el+en). Baseline regenerated: 0 violations / 0 files. |
| 2026-05-19 | BIM renderGrips centralization DONE (Boy Scout N.0.2). `protected finalizeRender(entity, options)` aggiunta a `BaseEntityRenderer`. 7 BIM renderers (`WallRenderer`, `ColumnRenderer`, `BeamRenderer`, `OpeningRenderer`, `SlabOpeningRenderer`, `SlabRenderer`, `StairRenderer`) ora chiamano `this.finalizeRender()`. Bug fix bonus: `finalizeRendering` ora passa `options` a `renderGrips`. ADR-363 changelog aggiornato. |
| 2026-05-19 | ADR-183 Phase C completata. Cancellati `hooks/useDxfGripInteraction.ts` (451 righe) + `hooks/grips/useGripSystem.ts` (387 righe), entrambi `@deprecated` dal 2026-02-16 con zero function call-sites. Types migrati inline in `hooks/grips/unified-grip-types.ts` (canonical SSoT). Aggiunta nuova voce Boy Scout: `canvas-mouse-types.ts:19-89` duplicate grip types (~30min). ADR-183 changelog aggiornato. |
| 2026-05-19 | ADR-365 Tailwind Semantic Palette Enforcement created (Proposed). Hover audit revealed 249 raw palette violations σε 86 files (από τα οποία ~21 SSoT exempt → ~65 consumer files). Plan: 9 phases (Phase 0 infrastructure + Phases 1-8 per-domain migration + Phase 8 closure). Per-session handoff απαιτείται. |
| 2026-05-19 | ADR-365 Phase 0 DONE. Infrastructure deployed: ratchet script + SSoT registry module (Tier 2) + baseline + CHECK 3.26 in parallel orchestrator + npm scripts. Actual baseline 3,659/440 (revised from 249/65 — original audit was hover-only). Smoke 1-5 PASS. Hook latency ~0.73s staged. |
| 2026-05-19 | Auto-Infer Alignment Guides discovered via SPEC-3D-004C (GenArc port catalog). 3-engine grep confirmed gap (GuideSnapEngine/OrthoSnapEngine/ParallelSnapEngine — κανένα δεν κάνει auto-infer από arbitrary anchors). New pending entry added (~3h ADR proposal + implementation). 4 Giorgio conditions all ✅ satisfied (independent feature, not blocking ADR-366). |
| 2026-05-19 | **ADR-363 Phase 6 CLOSED — Multi-Layer DNA BOQ + Material→ΑΤΟΕ SSoT**. Phase 6.1 (multi-entry wall walls με `dna.layers.length > 1` → 1 parent + N children, deterministic IDs `boq_bim_${entityId}_layer_${layerId}`, per-layer detach guard ανεξάρτητο, cascade delete via `where('parentBoqItemId', '==', parentId)` query) + Phase 6.2 (material→ΑΤΟΕ catalog SSoT για 18 wall-material-catalog presets — ΟΙΚ-2/3/4/7/10/12 με quantityKind area/volume). Files: `bim/services/boq-multi-layer-builder.ts` (15 tests), `bim/config/material-to-atoe-mapping.ts` (23 tests), `bim/services/BimToBoqBridge.ts` extended (24 tests total — 12 existing single-entry preserved + 12 νέα multi-layer). BOQItem schema +4 optional fields (parentBoqItemId, isGroupParent, layerIndex, materialId) — 100% back-compat. Industry alignment 6/6 (SPEC-3D-004D §12 Q4 Revit/ArchiCAD/Bentley/Tekla/Vectorworks/Allplan Material Takeoff pattern). Ratchet entry «ADR-363 PHASE 6.x» removed από checklist. Future: user-editable bim_atoe_overrides + BOQ parent/children expandable UI (Phase 6.2+ non-blocking). |
| 2026-05-19 | **ADR-345 Fase 5.5-FR DONE** — findReplace button wired: `DxfFindReplaceHost.tsx` (NUOVO) + `findReplaceOpen` state in `useDxfViewerState` + `contextual-text-editor-tab.ts` comingSoon rimosso + `DxfViewerContent` lazy mount. spellCheck + insert.symbol rimangono comingSoon. ADR-345 changelog + Fase 5.5-FR subsection aggiunti. |
| 2026-05-19 | **ADR-363 Phase 7.2 CORE LANDED** (Mirror/Rotate/Copy BIM). Files: `bim/transforms/bim-mirror-geometry.ts` + `bim-rotate-geometry.ts` + `bim-copy-builder.ts` (SSoTs, pure functions, 7 BIM kinds each), `core/commands/entity-commands/BimCopyCommand.ts` (ICommand wrapper), MirrorEntityCommand + RotateEntityCommand extended with BIM dispatch (private `computeMirrorUpdates` / `computeRotateUpdates`). Kind-specific enterprise ID gen via `generateWallId/...`, opening↔wall + slab-opening↔slab host rewire on copy. 59 tests across 6 suites (21 mirror-geom + 12 rotate-geom + 10 copy-builder + 5 mirror dispatch + 5 rotate dispatch + 6 BimCopyCommand) all green. Ribbon buttons + MI/RO/CO shortcuts ήδη υπήρχαν; useMirrorTool + useRotationTool ήδη wired και τώρα δουλεύουν σε BIM. Ratchet block 7.2 αντικαθίσταται από smaller follow-up (~1h): `useBimCopyTool` clipboard flow. |
| 2026-05-21 | **ADR-358 stair TS gap CLOSED** — `entities.ts` consumer sweep fix: imports από `'./stair'` → `'../bim/types/stair-types'`. Root cause ήταν χαμένο αρχείο από ADR-363 Phase 0.5 sweep, όχι missing `case 'stair'` στο HitTestingService. Ratchet entry αφαιρέθηκε. |
| 2026-05-22 | **ADR-345 commit chain CLOSED (was outdated)** — Confirmed 2026-05-22 via code audit: `useTextToolbarCommandBridge` (ADR-344 Phase 6.E, 2026-05-12) is mounted in `DxfViewerContent.tsx:124` (always active). Chain ribbon→store→CommandHistory ήδη λειτουργεί: `useRibbonTextEditorBridge.setValue` → store change (isPopulating=false) → subscribe callback → `UpdateTextStyleCommand/UpdateTextGeometryCommand/UpdateTextCurrentScaleCommand/UpdateMTextParagraphCommand` → `CommandHistory`. Known gap (existing, same as floating panel): `lineSpacingFactor` + `layerId` deferred, no commands yet. Ratchet entry αφαιρέθηκε. ADR-345 §Fase-5.5 ενημερώθηκε. |
| 2026-05-19 | **ADR-363 Phase 0.5 CLOSED — Stair Migration to `bim/` ολοκληρώθηκε**. Reality-vs-ADR drift διορθώθηκε: 45 barrel stubs `systems/stairs/` αφαιρέθηκαν, 2 barrels `types/stair.ts` + `rendering/entities/StairRenderer.ts` διαγράφηκαν, 2 hooks (`useStairPersistence`, `useRibbonStairBridge`) μετακινήθηκαν σε `bim/hooks/use-stair-persistence.ts` + `use-ribbon-stair-bridge.ts` με fixed imports. `bim/renderers/StairRenderer.ts` legacy imports διορθώθηκαν. Consumer sweep: 17 × systems/stairs + 4 × useStairPersistence + 1 × useRibbonStairBridge + 65 × types/stair. SSoT registry module `bim-folder-residency` (Tier 3, baseline 0) με 5 forbidden patterns. `stair-presets-service` + `stair-firestore-service` paths ενημερώθηκαν σε bim/. Stair tests: 21 suites / 322 tests / green. Ratchet entry «ADR-363 STAIR MIGRATION — Phase 0.5 incomplete» αφαιρέθηκε. Νέα boy-scout entry προστέθηκε για bridge/* helpers (cross-domain coupling BIM→UI follow-up, ~1h). |
| 2026-05-25 | **ADR-376 Phase A — Opening Tags blank-canvas case (PENDING)**. Phase A implementation complete (per `local_ΕΚΚΡΕΜΟΤΗΤΕΣ.txt` ΟΜΑΔΑ ΑΜ): mark allocation γίνεται μόνο όταν ο user φόρτωσε κάτοψη μέσω wizard (έχει `level.floorId`). Σε blank-canvas / non-wizard placement, ο allocator skip-άρει (console.warn, `mark = undefined`, opening persists κανονικά). Pending direction Giorgio: πώς να γίνει mark allocation όταν δεν υπάρχει floor context (π.χ. default floor=0; ζήτα από user; ξεχωριστή φάση Phase B). Ratchet entry NEW — εκκρεμεί guidance. |
| 2026-05-25 | **ADR-376 v5.1 + v5.2 + v5.3 CLOSURES** — (v5.1) Layers panel UI toggle wired: `AnnotationsSection.tsx` + `LevelPanel` integration + i18n el/en + 9 SSoT tests + `.ssot-registry.json` `opening-tag-layer` Tier 3 entry. (v5.2) Cross-floor allocator bug fix στο `opening-mark-service.ts:parseMarkSeq` (seq cap [1,99] per §4.1 floor-prefix hundreds convention) — 20/20 tests PASS, previously 19/20. (v5.3) **Blank-canvas decision RESOLVED Option D** (Giorgio): silent skip παραμένει final design — §8 Out-of-scope. Previous «blank-canvas non-wizard placement» pending entry CLOSED. Zero further code work για Phase A. |
| 2026-05-25 | **ADR-376 Phase B.1 DONE** — Renumber Openings command (IMAGINiT Door Mark Update pattern). NEW (6): opening-renumber-service + tests (12/12 PASS), RenumberOpeningsCommand (ICommand + writeBatch + scene optimistic + undo), RenumberOpeningsDialog (Radix Dialog + scope radio + kind checkbox + manual-include toggle + live preview), RenumberOpeningsHost (EventBus listener + lazy load), annotate-tab-openings ribbon panel. MODIFIED (9): opening-types.ts (+markIsManual flag), opening-command-keys (+renumber action), contextual-opening-tab (+quick-action panel), ribbon-default-tabs (annotate uses new panel), useRibbonOpeningBridge (+onAction + markIsManual on Mark edit), RibbonButtonIcon (+bim-opening-renumber icon), EventBus (+bim:opening-renumber-requested event), DxfViewerContent (lazy mount Host), i18n el+en. Industry standard 5/5 convergence (IMAGINiT/ArchiCAD/Tekla/Bentley/Vectorworks): preserve manual overrides by default, opt-in to wipe. `.ssot-registry.json` νέο entry `opening-renumber-service` (Tier 3). ADR-376 §7 status table + §4.9 markIsManual semantics + §11 changelog v6. ΕΚΚΡΕΜΟΤΗΤΕΣ ΟΜΑΔΑ ΑΞ added. Pending: Phase B.2 (BOQ Schedule signature group-by Mode C) + Phase C (draggable tag). |
| 2026-06-24 | **`roundUpToModule` SSoT ΕΝΟΠΟΙΗΣΗ DONE (ADR-459 v16, Opus 4.8)** — flag (ίδιας session) → fixed. NEW pure leaf `bim/structural/sizing/module-rounding.ts` (`roundUpToModule` tolerant-ceil + `roundDownToModule`) αντικατέστησε **4 ταυτόσημα** inline `Math.ceil(value/module)*module` (slab-sizing/member-sizing/column-sizing/suggest-pad-dimensions) + το μοναδικό inline `roundDownToModule` (member-sizing). Tolerant-ceil (`- MODULE_CEIL_EPSILON`) ενσωματωμένος → ΟΛΟΙ οι callers παίρνουν dust-protection (bonus: slab/member/column πέρα από το pad που είχε το reported #3 symptom). `.ssot-registry.json` module `module-rounding` (Tier 3) + baseline. 117/117 sizing+pad jest GREEN, μηδέν regression. ΜΗΔΕΝ duplicate (grep-verified). |
| 2026-07-14 | **CHECK 3.28 in-file clone — `ThreeJsSceneManager` sync signatures DONE (ADR-584, Opus 4.8)** — εξήγαγα ΕΝΑ `FloorVisibilityScope` type + `EMPTY_FLOOR_VIS_SCOPE` const στο `scene-manager-actions.ts` (big-player option-bag: Three.js `set(options)`/Revit API). Οι 2 public wrappers (`syncBimEntities`/`syncBimEntitiesMultiFloor`) παίρνουν πλέον ΕΝΑ `scope` param (spread) αντί για 5 positional· τα 2 Args interfaces → flat intersection (`& FloorVisibilityScope`) → `scene-manager-sync.ts` internals αμετάβλητα· τα 3 call sites στο `bim3d-resync.ts` συνθέτουν μέσω `buildFloorVisibilityScope(s, floorModes)` helper. Pure internal dedup, ΜΗΔΕΝ αλλαγή συμπεριφοράς (ίδια defaults/τιμές). Αφαιρέθηκε το entry «ThreeJsSceneManager sync signatures». **ΣΤΟ ΙΔΙΟ pass καθάρισα και τα 2 προϋπάρχοντα self-clones** του `scene-manager-actions.ts` που ανέδειξε το co-staging: `rebuildBimMeshes(deps, scope, buildGeometry)` (κοινό highlighter/focus/BVH/selection/section bookkeeping — τα 2 variants διαφέρουν ΜΟΝΟ στο `bimLayer.sync` vs `.syncMultiFloor`) + `finalizeDxfOverlaySync(deps, fitDone, onFitApplied)` (κοινό invalidate/frame/section tail). Exported signatures αμετάβλητα → `scene-manager-sync.ts` + callers άθικτοι· η διαφορά `ssaoModulator.warmUp()` (conditional vs όχι) ζει στο `scene-manager-sync.ts`, ΟΧΙ σε αυτά. **✅ `jscpd:diff` = 0 clones (ήταν 2) → ΔΕΝ χρειάζεται `SKIP_JSCPD_DIFF=1`**· 126 bim-3d/scene jest GREEN. Μετά το commit: `npm run jscpd:baseline` (baseline πέφτει). Bonus: αφαιρέθηκε το stale bullet «GridSpatialIndex ↔ QuadTreeSpatialIndex» (verified λυμένο — `BaseSpatialIndex.ts` + jscpd καθαρό). |
