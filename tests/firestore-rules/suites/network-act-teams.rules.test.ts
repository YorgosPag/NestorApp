/**
 * Firestore Rules — συλλογή `network_act_teams` (ADR-867 §4.3)
 *
 * Πρότυπο: `deny_all` — **ένας** γραφέας διακομιστή (`act-team-writer.ts`), καμία πράξη πελάτη.
 *
 * ────────────────────────────────────────────────────────────────────────────
 * 🔴 ΓΙΑΤΙ ΓΡΑΦΕΤΑΙ Ο ΚΑΝΟΝΑΣ, ΑΦΟΥ Η ΠΡΟΕΠΙΛΟΓΗ ΕΙΝΑΙ ΗΔΗ `deny`
 * ────────────────────────────────────────────────────────────────────────────
 *
 * Η συλλογή γεννήθηκε στο Β3 **χωρίς κανένα `match`** και ήταν κλειστή — από την
 * **προεπιλογή**, όχι από απόφαση. Η διαφορά δεν είναι φιλοσοφική: η CHECK 3.16 σαρώνει
 * τα `match` του `firestore.rules`, άρα μια συλλογή χωρίς μπλοκ είναι **αόρατη** σε κάθε
 * πύλη κάλυψης — «κλειστή επειδή κανείς δεν την άνοιξε» δεν ξεχωρίζει από «κλειστή επειδή
 * κάποιος το αποφάσισε», και το πρώτο αλλάζει σιωπηλά την πρώτη φορά που κάποιος γράψει
 * ένα `match` για να «φτιάξει» κάτι άλλο.
 *
 * 🔑 **ΤΙ ΘΑ ΔΙΝΕ ΜΙΑ ΑΝΑΓΝΩΣΗ**: `responsibleUid` + `memberUids` ανά πράξη — δηλαδή
 * **οργανόγραμμα του γραφείου ανά πελάτη**. Ο πελάτης **δικαιούται** να ξέρει ποιοι
 * διαβάζουν (ADR-834 (ε) 🏆) — αλλά από το **ακροατήριο του νήματος**, που είναι προβολή
 * αυτής της ομάδας **για το δικό του** νήμα, όχι από τον πίνακα **όλων** των πράξεων.
 *
 * 🔴 **ΤΙ ΘΑ ΕΚΑΝΕ ΜΙΑ ΓΡΑΦΗ**: το ακροατήριο **παράγεται** από αυτό το έγγραφο. Ένας
 * πελάτης που προσθέτει το uid του στο `memberUids` **αυτοπροσκαλείται** στη συνομιλία —
 * και ο κανόνας του `network_threads` θα τον δεχόταν, σωστά, γιατί η γραμμή ακροατηρίου
 * θα ήταν **αληθινή**. Η πόρτα δεν είναι το νήμα· είναι εδώ.
 *
 * @since 2026-09-17 (ADR-867 Β4)
 */

import { FIRESTORE_RULES_COVERAGE } from '../_registry/coverage-manifest';
import { defineDenyAllCell, useDenyAllEmulator } from '../_harness/deny-all-suite';

export const COVERAGE = FIRESTORE_RULES_COVERAGE.find(
  (c) => c.collection === 'network_act_teams',
)!;

describe('network_act_teams.rules — η ομάδα της πράξης γράφεται ΜΟΝΟ από τον διακομιστή (ADR-867 §4.3)', () => {
  const env = useDenyAllEmulator();

  for (const cell of COVERAGE.matrix) {
    defineDenyAllCell(env, cell, COVERAGE.collection);
  }
});
