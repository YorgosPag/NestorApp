---
gate: "3.81"
title: "Πύλη ταυτότητας προϊόντος"
adr: "ADR-857"
summary: "λέει κάθε σημείο που ονομάζει το **προϊόν** το ΙΔΙΟ όνομα — και είναι κάθε **άλλη** χρήση **δηλωμένη, με λόγο**;"
mechanism: "⛔ ZERO TOL (Κ1+Κ2+Κ3+Κ4)"
baseline: "no baseline"
tests: "npm run test:product-identity"
escape: "SKIP_PRODUCT_IDENTITY"
---

# CHECK 3.81 — Πύλη ταυτότητας προϊόντος

**Πύλη ταυτότητας προϊόντος** (ADR-857 Φ8) — «λέει κάθε σημείο που ονομάζει το **προϊόν** το ΙΔΙΟ όνομα — και είναι κάθε **άλλη** χρήση **δηλωμένη, με λόγο**;». 🔴 **Η ΑΙΤΙΑ, ΜΕΤΡΗΜΕΝΗ 12/09**: το **ίδιο, μοναδικό email** που φεύγει κουβαλούσε **τέσσερα** ονόματα — `Nestor Construct` στη γραμμή `From:`, `— ΝΕΣΤΩΡ`/`— Nestor` στην υπογραφή θέματος, `Nestor App` στο υποσέλιδο, `Nestor Pagonis` σε ό,τι στέλνει η Firebase — και **το πρώτο δεν υπήρχε σε ΚΑΜΙΑ απογραφή**: βρέθηκε μόνο επειδή κάποιος ρώτησε «**ποιος γράφει τη γραμμή `From:`;**» αντί να ψάξει τα ονόματα που ήδη ξέραμε. 🔑 **ΚΑΙ ΚΑΝΕΝΑ ΕΡΓΑΛΕΙΟ ΔΕΝ ΤΟ ΕΠΙΑΣΕ, ΜΕ ΔΟΜΙΚΟ ΛΟΓΟ**: ο `no-hardcoded-strings` (`eslint-rules/no-hardcoded-strings.js:154`) θεωρεί «τεχνική» κάθε **λατινική** συμβολοσειρά με κενό ⇒ οι λατινικές γραφές ήταν **αόρατες**, ενώ η ελληνική θα είχε αναφερθεί. **Δεν είναι σύμπτωση ότι οι λατινικές έγιναν έξι και η ελληνική μία.** 🔴 **ΤΟ ΔΕΝΤΡΟ ΔΕΝ ΗΤΑΝ ΜΗΔΕΝΙΚΟ — Η ΠΡΟΔΙΑΓΡΑΦΗ ΤΟ ΥΠΕΘΕΤΕ ΚΑΙ ΗΤΑΝ ΨΕΥΔΕΣ** *(§5: «η πύλη μπαίνει τελευταία, σε δέντρο ήδη μηδενικό»)*: η **Φ8α** μέτρησε **~20 αδήλωτες γραφές κλάσης Α** και βρήκε **τρεις γραφές που καμία απογραφή δεν είχε δει ποτέ** — **`Nestor Enterprise`** (`enterprise-config/defaults.ts`, το `app.name` όλου του συστήματος), **`Pagonis Nestor`** (`channel-media-dispatcher.ts`, **θέμα email που φεύγει**) και **`Nestor Error Reporting System`** (`ErrorBoundary/error-helpers.ts`, **σώμα email που φεύγει**). 🏆 **Η ΠΥΛΗ ΑΠΟΔΕΙΧΘΗΚΕ ΑΥΣΤΗΡΟΤΕΡΗ ΑΠΟ ΤΟΝ ΣΧΕΔΙΑΣΤΗ ΤΗΣ, ΚΑΙ ΑΥΤΟ ΕΙΝΑΙ ΤΟ ΚΥΡΙΟ ΕΥΡΗΜΑ**: η χειρωνακτική απογραφή με `grep` σε **quoted literals** βρήκε **15**· η πρώτη εκτέλεση της πύλης βρήκε **36**. Η διαφορά ήταν **JSXText** (`<strong>Nestor Construct</strong>` σε **δύο δημόσιες σελίδες** — ακριβώς το τυφλό σημείο που δηλώνει ο **N.11**) και **template literals** (**έξι** μέσα σε HTML email). *Το «0» της απογραφής σήμαινε «δεν κοίταξα εκεί», όχι «καθαρό».* ⚠️ **AST ΚΑΙ ΠΟΤΕ ΚΕΙΜΕΝΟ — ΤΟ ΑΠΟΦΑΣΙΣΕ Η ΜΕΤΡΗΣΗ, ΟΧΙ Η ΠΡΟΤΙΜΗΣΗ**: **πέντε** σημεία του δέντρου γράφουν παλιές γραφές μέσα σε **σχόλιο** και **κανένα** δεν είναι παράβαση — το ένα είναι **το σχόλιο που τεκμηριώνει τη διόρθωση** (`app/layout.tsx`), και το `ifc-authorship.ts` κρατά `'NestorCAD'` μέσα σε **JSDoc** που εξηγεί γιατί **ΔΕΝ** πρέπει να μπει εκεί. Σαρωτής κειμένου θα κοκκίνιζε **πάνω στη ΘΕΡΑΠΕΙΑ** (σχήμα `Κ7β` του 3.50 · `Κ5` του 3.73). 🏆 **ΠΟΥ ΞΕΠΕΡΝΑΜΕ ΤΟ ΟΙΚΟΣΥΣΤΗΜΑ** *(έρευνα 13/09, με πηγές)*: το **Vale** — ο prose linter που χρησιμοποιούν **Microsoft · GitLab · Mozilla · Red Hat · Linux Foundation** — έχει **ΑΚΡΙΒΩΣ** αυτόν τον κανόνα (`substitution`: «use X instead of Y») **αλλά** ανασηκώνει με tree-sitter **μόνο σχόλια και docstrings**, αφήνοντας ρητά τα **string literals ως κώδικα** ⇒ κοιτάζει ακριβώς τα **πέντε αθώα** σημεία και είναι **δομικά τυφλό** ακριβώς εκεί όπου έζησαν **και οι επτά** λάθος γραφές· το **typescript-eslint** (100+ κανόνες) **δεν έχει κανέναν** για όρους μέσα σε literals (μόνο χειρόγραφο `no-restricted-syntax`)· το **ISO 30042 (TBX)** δίνει το πρότυπο **όνομα** του μοντέλου μας — *term status*: **preferred/admitted/deprecated** = `PRODUCT_NAME` / κλάσεις Β-Ε / `KNOWN_PAST_SPELLINGS` — αλλά είναι πρότυπο **ανταλλαγής** ορολογίας, **χωρίς κανέναν μηχανισμό επιβολής σε πηγαίο κώδικα**. 🔑 **ΠΕΝΤΕ ΚΛΑΣΕΙΣ, ΚΑΙ ΟΙ ΤΡΕΙΣ ΔΕΝ ΜΕΤΟΝΟΜΑΖΟΝΤΑΙ**: **Α** όνομα προϊόντος *(→ ρίζα)* · **Β** νομικό πρόσωπο *(το `©` ανήκει σε **πρόσωπο**, ποτέ σε λογισμικό — «© Nestor App» είναι λάθος **τύπου**)* · **Γ** δεδομένα ενοίκου *(το `email-templates.service.ts` διαφημίζει «**20+ χρόνια εμπειρίας** στη δημιουργία σπιτιών» με `info@nestorconstruct.gr`: μετονομασία θα έκανε το **λογισμικό** να ισχυρίζεται εικοσαετή εμπειρία σε οικοδομές)* · **Δ** αναγνωριστικά μηχανής *(`NestorAec` · `NestorLayerMeta` σε **XDATA ήδη γραμμένων DXF**, `NestorPagonisApp/1.0` που **απαιτεί** το Nominatim, `Nestor-Workspace` κεφαλίδα HTTP — μετονομασία **σπάει αρχεία ήδη γραμμένα στον δίσκο**)* · **Ε** παγωμένα *(`senderDisplayName` μέσα στην παγωμένη επιφάνεια της Google)*. ⛔ **ΤΕΣΣΕΡΑ ΚΡΙΤΗΡΙΑ, ΠΟΤΕ ΕΝΩΜΕΝΑ ΜΕ «Ή»** *(μάθημα 3.41)*: `Κ1` **αδήλωτη γραφή** · `Κ2` **ορφανή δήλωση** · `Κ3` **δήλωση χωρίς λόγο** (≥40 χαρ.) · `Κ4` **απόκλιση ρίζας**. 🔑 **ΔΥΟ ΚΑΝΟΝΕΣ, ΚΑΙ Η ΔΙΑΦΟΡΑ ΕΙΝΑΙ ΔΟΜΙΚΗ**: στον **κώδικα** κάθε γραφή είναι εύρημα **ακόμα και η σωστή** *(ο κώδικας **μπορεί** να εισάγει τη ρίζα — το `app/layout.tsx` έγραφε ωμό `"Nestor App"`: σωστή τιμή, **δεύτερη πηγή**)*· στα **locales** εύρημα είναι **μόνο** γραφή που **δεν** είναι η κανονική *(ένα locale **δεν μπορεί** να εισάγει σταθερά, και το ICU `{appName}` **απορρίφθηκε ρητά** στη Φ4: placeholder που δεν λύνεται φτάνει **ωμό σε PDF**)*. 🏆 **ΤΟ ΟΝΟΜΑ ΓΡΑΦΕΤΑΙ ΜΙΑ ΦΟΡΑ ΚΑΙ ΣΤΟΥΣ ΤΙΤΛΟΥΣ**: πέντε σελίδες έγραφαν μόνες τους `'Admin | Nestor'` · `'Audit Log | Nestor Admin'` · `'… | Nestor'` — **τρεις γραφές σε πέντε αρχεία**. Θεραπεία με τον **μηχανισμό του ίδιου του Next.js**: `title.template` στη **ρίζα**, που εφαρμόζεται **στα παιδιά και ΠΟΤΕ στο segment που το ορίζει** *(τεκμηριωμένο· το `title.default` είναι **υποχρεωτικό**, και τα πρότυπα **δεν αλυσιδώνονται**)* ⇒ οι πέντε σελίδες **έπαψαν να ονομάζουν το προϊόν**. ⚠️ **ZERO-TOL, ΚΑΜΙΑ BASELINE ΠΟΤΕ**: δεν υπάρχει «λιγότερες λάθος γραφές από χθες» — **μία** αρκεί για να φτάσει σε **εισερχόμενα πραγματικού ανθρώπου**. Εφικτό **επειδή μετρήθηκε** ότι η Φ8α μηδένισε την κλάση Α (**36 → 0** στην ίδια συνεδρία), όχι επειδή ελπίζουμε. 🔑 **Ο ΠΑΡΟΝΟΜΑΣΤΗΣ ΕΙΝΑΙ ΟΙ ΙΔΙΕΣ ΟΙ ΔΗΛΩΣΕΙΣ, ΚΑΙ ΕΙΝΑΙ ΙΣΧΥΡΟΤΕΡΟΣ ΑΠΟ ΑΓΚΥΡΑ ΕΝΟΣ ΑΡΧΕΙΟΥ**: **κάθε** δήλωση οφείλει να βρεθεί **ζωντανή** στο δέντρο ⇒ αν ο σαρωτής σπάσει, **δεν σιωπά** — όλες γίνονται `orphan-declaration` και η πύλη ουρλιάζει. **24 δηλώσεις · 52 δεμένες γραφές · 16.765 αρχεία**. 🔴 **ΚΑΙ Η ΠΡΩΤΗ ΓΡΑΦΗ ΤΟΥ ΜΗΤΡΩΟΥ ΗΤΑΝ ΛΑΘΟΣ — ΤΟ ΒΡΗΚΕ Η ΠΡΩΤΗ ΕΚΤΕΛΕΣΗ, ΟΧΙ Ο ΣΥΓΓΡΑΦΕΑΣ**: δηλώθηκαν `NestorPagonisApp` · `NestorAec` · `Nestor-Workspace` — ονόματα **όπως τα βλέπει ο άνθρωπος**, που όμως **δεν ανήκουν στο κλειστό σύνολο της ρίζας** ⇒ **11 ορφανές + 11 αδήλωτες για ΤΑ ΙΔΙΑ σημεία**. Θεραπεία: οι δηλωμένες γραφές είναι **υποχρεωτικά μέλη του συνόλου**, η ακρίβεια μετακομίζει στον **λόγο** *(που τον διαβάζει άνθρωπος)*, και η πύλη το λέει **ρητά** αντί να το ανακαλύπτει ως «δεν υπάρχει πια». ⚠️ **ΟΙ ΤΙΜΕΣ ΔΕΝ ΑΝΤΙΓΡΑΦΟΝΤΑΙ ΣΤΟ ΜΗΤΡΩΟ, ΕΠΙΤΗΔΕΣ** — διαβάζονται με AST **από τη ρίζα**: αντίγραφο εκεί θα ήταν η **έβδομη πηγή** του ονόματος, ακριβώς η δομή που το ADR κατήργησε *(η ασυμφωνία είναι το `Κ4`)*. ⚠️ **ΤΟ ΜΑΚΡΥΤΕΡΟ ΤΑΙΡΙΑΣΜΑ ΠΡΩΤΑ, ΜΕ ΜΑΣΚΑ**: το `Nestor` είναι **πρόθεμα** των `Nestor App`/`Nestor Pagonis` — χωρίς αυτό κάθε **σωστή** γραφή θα μετριόταν **και** ως παλιά, δηλαδή η πύλη θα κατηγορούσε τη θεραπεία. ⚠️ **ΚΑΜΙΑ `RegExp` ΧΤΙΣΜΕΝΗ ΑΠΟ ΣΥΜΒΟΛΟΣΕΙΡΑ**: το 3.73 γεννήθηκε σπασμένο ακριβώς έτσι *(`\b` μέσα σε template literal είναι **BACKSPACE**, U+0008)*, και το ίδιο τεκμηριώνει το 3.56 — εδώ γίνεται σκέτο `indexOf` σε αντίγραφο που μασκάρεται. ⚠️ **ΜΗΝ λύσεις κόκκινο με δήλωση επειδή «είναι σωστό»**: η δήλωση σημαίνει «**αυτό ΔΕΝ είναι το όνομα του προϊόντος**» — αν είναι, η θεραπεία είναι **εισαγωγή από τη ρίζα**. **Tests**: `npm run test:product-identity` — `Μ0α-δ` παρονομαστής *(συμπερ. **Μ0γ** στο ΠΡΑΓΜΑΤΙΚΟ δέντρο και **Μ0δ** «εξέτασε χιλιάδες, όχι δεκάδες»)* · `Μ1-Μ5` **Κ1 σε κάθε μορφή που κουβαλά κείμενο** *(literal · **JSXText** · template · **και οι 5 γραφές συν μία επινοημένη**)* · `Μ6-Μ9` το κλειστό σύνολο *(συμπερ. **Μ8**, που επαναφέρει το πραγματικό σφάλμα του συγγραφέα)* · `Μ10-Μ12` η ρίζα · `Μ13-Μ15` locales *(με **Μ14 υπερ-ευαισθησίας** και **Μ15** «το κλειδί δεν κρίνεται, μόνο η τιμή»)* · `Κ-Α` **τα σχόλια δεν γεννούν ευρήματα ούτε όταν γράφουν ΚΑΘΕ παλιά γραφή** · `Κ-Β…Κ-ΣΤ` συμβόλαια. **Οι μεταλλάξεις γίνονται σε μίνι-repo στον `os.tmpdir()`, ΠΟΤΕ στο κοινό working tree** *(το δέντρο μοιράζεται με πράκτορες που κάνουν commit: μετάλλαξη που δεν επαναφέρεται είναι **δοκιμή που φεύγει στην παραγωγή**)*. **Layer 2** = job στο **υπάρχον** `ssot-discover.yml`, **άνευ όρων** *(μια γραφή προσγειώνεται σε οποιοδήποτε αρχείο· φίλτρο διαδρομών θα ήταν το `blocking-path-filtered` του 3.54)*. Αναφορά: `npm run product-identity:report`. Escape: `SKIP_PRODUCT_IDENTITY=1`
