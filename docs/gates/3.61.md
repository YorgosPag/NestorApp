---
gate: "3.61"
title: "Πύλη του συνόρου πλοήγησης"
adr: "ADR-787"
summary: "ζητά αυτό το αρχείο την πλοήγηση από το **ΣΥΝΟΡΟ** (`@/lib/workspace/navigation`), και αν όχι, το είπε κάποιος **με λόγο**;"
mechanism: "⛔ ZERO TOL"
baseline: "no baseline"
tests: "npm run test:navigation-boundary"
escape: "SKIP_NAVIGATION_BOUNDARY"
---

# CHECK 3.61 — Πύλη του συνόρου πλοήγησης

**Πύλη του συνόρου πλοήγησης** (ADR-787 §5.3 ν) — «ζητά αυτό το αρχείο την πλοήγηση από το **ΣΥΝΟΡΟ** (`@/lib/workspace/navigation`), και αν όχι, το είπε κάποιος **με λόγο**;». Το Γ2 πέρασε **128 αρχεία / 141 σύμβολα**· χωρίς πύλη ο επόμενος εισάγει ξανά από `next/link` και το μαθαίνουμε **από μια οθόνη** — ή καθόλου: ωμό `prefetch` προφορτώνει **404 ΧΩΡΙΣ ΚΑΝΕΝΑ ΙΧΝΟΣ**, η πλοήγηση απλώς «είναι αργή». 🔑 **Ο ΜΗΧΑΝΙΣΜΟΣ ΑΝΑΤΡΑΠΗΚΕ ΑΠΟ ΤΗ ΜΕΤΡΗΣΗ**: ο σχεδιασμός ζητούσε **ratchet** με σωστό επιχείρημα (133 αμετανάστευτα ⇒ μονίμως κόκκινο ⇒ `SKIP_` ⇒ διακοσμητικό, η παγίδα που το **3.39** δοκίμασε και απέρριψε γραπτώς) — αλλά **το ΒΗΜΑ 1 άλλαξε την προϋπόθεση**: **μηδέν** παραβιάσεις σήμερα ⇒ zero-tol **εφικτό, μετρημένο** (πρότυπο 3.48/3.55). ⚠️ **Καμία baseline ΠΟΤΕ**: δεν υπάρχει «λιγότερες παρακάμψεις από χθες» — **μία** αρκεί. **7 ρητές καταστάσεις σε ΔΥΟ κατάστιχα** (αρχεία · δηλώσεις), κλειστή λογιστική fail-closed, άγνωστη ⇒ `throw` **με όνομα**: ⛔ `boundary-bypass` **0** · ⛔ `orphan-owner` **0** · ⛔ `reasonless-owner` **0** · ✅ `owner` 2 · `at-boundary` 23 · `unmigratable-only` 44 · `not-a-navigation-file` 15.166. 🔴 **Ο ΚΑΝΟΝΑΣ «Κ-β: ΑΔΕΣΠΟΤΗ ΔΙΕΥΘΥΝΣΗ» ΖΗΤΗΘΗΚΕ ΚΑΙ ΔΕΝ ΓΡΑΦΤΗΚΕ**, τρεις μετρημένοι λόγοι: **(1) ΥΠΑΡΧΕΙ ΗΔΗ** — η άγκυρα `Ν` του `route-catalogue-anchor.test.ts` (φρουρός #4) ρωτά **ακριβώς** αυτό (`href`·`router.push/replace`·`redirect`) με παρονομαστή/μετάλλαξη/ορφανές/υποχρεωτικό λόγο, και **έχει πύλη**: επαληθεύτηκε **ΕΚΤΕΛΩΝΤΑΣ** το 3.54 («3.394 μπλοκάρουν») ⇒ δεύτερη μηχανή = **ADR-749**· **(2) ΜΗΔΕΝ ΠΛΗΘΥΣΜΟΣ** — το «12/14 νεκρές» μετρήθηκε με AST στα 9 μητρώα και έδωσε **1**, **ψευδώς θετικό** (το `LISTING_DETAIL_ROUTE_BASE = '/listing'` είναι **ΒΑΣΗ** για το `/listing/${id}`, όχι προορισμός) ⇒ **100% FP**· **(3) ΚΑΝΕΝΑ ΚΡΙΤΗΡΙΟ ΔΕΝ ΠΕΡΑΣΕ ΤΟΝ ΠΗΧΗ <10%** — «σταθερά `key: '/…'`» ⇒ 35 ευρήματα / **86% FP** (`{id}` του **report-builder DSL**, ΟΧΙ Next `[id]` · `.md` · `.ico`/`.png`), «εξάγει `*ROUTES`» ⇒ **25% FP** (`API_ROUTES`=HTTP, `LazyRoutes`=components) **και χάνει 5 από τα 9**. *Φρουρός χωρίς πληθυσμό = προσθήκη στους **606 αδρανείς** (ADR-749 §5).* 🔴 **ΑΠΟΡΡΙΦΘΗΚΕ ΚΑΙ ΤΟ ESLint** `no-restricted-imports` — η λύση που προτείνει η κοινότητα του **next-intl**, με τον μηχανισμό **ΗΔΗ στο repo** (`check-eslint-ratchet.js` → `GATES`): (α) είναι **Layer-2 CI only, ΠΟΤΕ pre-commit** (δηλωμένο γραπτά, N.17) ⇒ η παράκαμψη θα **προσγειωνόταν**· (β) **δεν εκφράζει υποχρεωτικό λόγο** — ένα `files:` override περνά **σιωπηλά και κενό**. 🏆 Εδώ το κλειστό σύνολο απαιτεί λόγο **≥40 χαρακτήρων** ΚΑΙ **καταγγέλλει τη νεκρή δήλωση**: δύο πράγματα που **κανένας κανόνας lint δεν εκφράζει**. ⚠️ **AST, ΟΧΙ regex**: το ίδιο το σύνορο **αναφέρει** το `next/link` σε **ΠΡΟΖΑ** ⇒ regex θα κοκκίνιζε πάνω στη **ΘΕΡΑΠΕΙΑ** (`Κ7β` του 3.50)· `ts.createSourceFile` **parse-only**, N.17 ακέραιος. ⚡ Προφίλτρο κειμένου **69 από 15.235** αρχεία ⇒ **~0,7s** όταν δεν πυροδοτεί · **~4,5s** πλήρης· ⚠️ ασφαλές **ΜΟΝΟ επειδή ΑΦΑΙΡΕΙ, δεν προσθέτει**. 🔴 **ΤΡΙΑ ΕΛΑΤΤΩΜΑΤΑ ΜΕΣΑ ΣΤΗΝ ΙΔΙΑ ΤΗΝ ΠΥΛΗ**: **(α)** ένα κοινό κατάστιχο **ΔΙΠΛΟΜΕΤΡΟΥΣΕ** τον ιδιοκτήτη χωρίς λόγο (αρχείο `owner` **ΚΑΙ** δήλωση `reasonless-owner`) — το βρήκε η άγκυρα `Λ1`, όχι η ανάγνωση ⇒ **ΔΥΟ κατάστιχα** (πρότυπο 3.50)· **(β) ΔΥΟ ΑΔΡΑΝΕΙΣ ΦΡΟΥΡΟΙ** (`reasonless-owner` · η αφαίρεση `declared − flagged`) που τους αποκάλυψαν **μεταλλάξεις που ΕΜΕΙΝΑΝ ΠΡΑΣΙΝΕΣ** ⇒ **ραφή ένεσης** (`owners`, πρότυπο 3.56) + άγκυρες `Σ3β`/`Σ3γ` που τους **πυροδοτούν**· **(γ)** η άγκυρα ισοδυναμίας **γεννήθηκε λάθος ΔΥΟ φορές** — πρώτα συνέκρινε την πύλη **με τον εαυτό της** (ο παρονομαστής **μετακινούνταν μαζί με τη μετάλλαξη**, σφάλμα **ADR-790 §9.1**), μετά αντέγραψε τη **ρύθμιση** του codemod· πλέον **τρέχει το ΠΡΑΓΜΑΤΙΚΟ CLI** και **ρίχνει σε κενή απάντηση** (πρότυπο `Ν3`, 3.31). 🏆 **ΚΑΙ Η ΣΥΓΚΡΙΣΗ ΤΩΝ ΔΥΟ ΟΡΓΑΝΩΝ ΒΡΗΚΕ ΤΡΙΤΟ, ΣΤΟ CODEMOD**: το ts-morph τραβούσε **4.878 αρχεία `node_modules`** στον παρονομαστή (20.127 έναντι 15.232) — **δεν ήταν αισθητικό**: το `project.save()` γράφει ό,τι τροποποιήθηκε, οπότε αρχείο εξάρτησης που θα ταξινομούνταν `rewritten` θα **γραφόταν στο node_modules**. *Δύο όργανα στο ίδιο δέντρο **επιτρέπεται να υπάρχουν, δεν επιτρέπεται να διαφωνήσουν***; πλέον **15.235 = 15.235**, κλειδωμένο από την `Ι1`. ⚠️ **ΜΗΝ** το κάνεις ratchet· **ΜΗΝ** λύσεις κόκκινο δηλώνοντας νέο ιδιοκτήτη «για να περάσει» (το κλειστό σύνολο υπάρχει για να το δει **άνθρωπος**)· **ΜΗΝ** αφαιρέσεις το προφίλτρο. 🔶 **Δηλωμένο όριο**: κρίνει **στατικές** εισαγωγές — `await import('next/link')` θα ήταν αόρατο (μετρημένο **0**· η μορφή δεν υπάρχει στο δέντρο). **ΚΑΝΕΝΑ νέο workflow** (job στο υπάρχον `ssot-discover.yml`, άνευ όρων· μητρώο **34** πύλες, **επαληθευμένο ΕΚΤΕΛΩΝΤΑΣ** το 3.37). Layer 1 = σκανδάλη **μέσα στην πύλη** (αποφασίζει **ΑΝ** τρέχει, ποτέ **ΠΟΣΟ** σαρώνει). Tests: `npm run test:navigation-boundary` (**25**: Μ0α-γ · Κ1-Κ11 · Σ1-Σ4+Σ3β/Σ3γ · Λ1-Λ3 · Ι1-Ι2· **10/10 μεταλλάξεις κόκκινες**, `Μ0` πράσινο **πριν ΚΑΙ μετά**· ⚠️ **τέσσερις** βγήκαν αρχικά πράσινες και **κάθε φορά διορθώθηκε ο ΣΧΕΔΙΑΣΜΟΣ, ποτέ το test**). Μαζική θεραπεία: `node scripts/migrate-navigation-boundary.mjs --apply`. Αναφορά: `npm run navigation-boundary:report`. Escape: `SKIP_NAVIGATION_BOUNDARY=1`
