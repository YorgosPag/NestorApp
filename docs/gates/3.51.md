---
gate: "3.51"
title: "Πύλη ωμών i18n κλειδιών στο SSR HTML"
adr: "ADR-781"
summary: "περιέχει ωμά κλειδιά το HTML που **στέλνει ο server**;"
mechanism: "⛔ ZERO-TOL (Κ1+Κ2) + 🔴 RATCHET (Χ)"
baseline: "`.i18n-ssr-oracle-baseline.json` (**312 ευρήματα / 154 διαδρομές**, 2026-08-22 — 207 ωμά κλειδιά · 101 shell-only · 3 withheld-but-answered · 1 not-rendered· **άνοιξε το JSON**, μην αντιγράψεις τον αριθμό· τα zero-tolerance **ΔΕΝ μπαίνουν ΠΟΤΕ** εκεί)"
tests: "npm run test:i18n-ssr"
escape: "SKIP_I18N_SSR_RAW_KEYS"
---

# CHECK 3.51 — Πύλη ωμών i18n κλειδιών στο SSR HTML

**Πύλη ωμών i18n κλειδιών στο SSR HTML** (ADR-781) — «περιέχει ωμά κλειδιά το HTML που **στέλνει ο server**;». **17 ωμά κλειδιά × 141 διαδρομές**, μόνιμα, στην παραγωγή (το sidebar ζει στο **root layout**): ο `useTranslationLazy` αρχικοποιούσε την ετοιμότητα σε `useState(false)` και τη διόρθωνε **μόνο** σε `useEffect`, που **δεν τρέχει σε SSR**. 🔴 **Η ΜΕΤΑΦΡΑΣΗ ΗΤΑΝ ΗΔΗ ΕΚΕΙ** — γι' αυτό **καμία** από τις 3.8/3.13/3.33/3.34/3.36 δεν το είδε: **όλες ρωτούν «υπάρχει το κλειδί;»** και η απάντηση ήταν ναι. Το **3.25** απαγορεύει την αρχή αλλά η εμβέλειά του είναι `*PageContent.tsx` + literal `isNamespaceReady`. 🔴 **Και η μοναδική «απόδειξη χρόνου εκτέλεσης» ήταν ΤΥΦΛΗ**: `shell-slice-no-raw-keys.test.ts` γρ. **47 & 163** `if (want.whole) continue` ⇒ παρέλειπε **ακριβώς** τα 9 whole ns όπου ζούσε το `navigation`· **μετρημένο 33 κλειδιά δεν ρωτιόντουσαν ΠΟΤΕ** (143→176). Διορθώθηκε + **νέα ονομαστική άγκυρα** για κάθε `pages.*` του sidebar. **ΤΡΕΙΣ κανόνες, ΠΟΤΕ ένας με «ή»** (3.41): **Κ1** ⛔ module που παραδίδει `t` **μαζί** με ετοιμότητα που μόνο `useEffect` διορθώνει (**0**/14.751) · **Κ2** ⛔ κάθε **βάσιμο** σημείο κλήσης στην κλειστότητα **layouts** απαντιέται από το **ΑΠΟΣΤΕΛΛΟΜΕΝΟ** slice (**0**/606) · **Χ ο ΧΡΗΣΜΟΣ** 🔴 RATCHET στα κλειδιά + ⛔ `route-unreachable`/`probe-unproven` — **η αυθεντία** (δεν μπορεί να είναι πράσινος πάνω σε σπασμένη οθόνη: **είναι** η οθόνη· και ο **μόνος** που απαντά για τις **29 δυναμικές**). 🔑 **Το κριτήριο του Κ1 άλλαξε ΔΥΟ φορές από μέτρηση**: «διαβάζεται στο render» ⇒ **81** (>95% FP) · «παραδίδεται δίπλα στο `t`» ⇒ **1** (το `loadingCompanies` βάφει **μεταφρασμένο** κείμενο) · «**και το effect αγγίζει i18n**» ⇒ **0**. 🔴 **Ο per-route στατικός έλεγχος ΑΠΟΡΡΙΦΘΗΚΕ ΜΕ ΜΕΤΡΗΣΗ**: `/spaces/parking` **3.224 στατικά vs 4 ζωντανά = 99,88% FP**. Αιτίες: fan-out του `addEntry` (**27.466** ψευδή ζεύγη — το i18next δοκιμάζει ns **με σειρά**, αρκεί **ΕΝΑ**) + η κλειστότητα **σελίδας** περιέχει modals/tabs/lazy που **δεν ζωγραφίζονται**. Τα **layouts** ζωγραφίζονται **πάντα**. Βασιμότητα ns σε **τρία μετρημένα βήματα**: 211 → **−357** (κλειδί όχι όρισμα `t()` εδώ· συγκομιδή από άλλα modules) → **−30** (το `t` είναι **παράμετρος** ⇒ ns του **καλούντος**) → **0**. 🔴 **ΤΟ ΙΔΙΟ ΕΛΑΤΤΩΜΑ ΓΡΑΦΤΗΚΕ ΜΕΣΑ ΣΤΗΝ ΠΥΛΗ, ΔΥΟ ΦΟΡΕΣ**: (α) `sliceAnswers`: «`whole` ⇒ απαντά οτιδήποτε **χωρίς lookup**» = το σχήμα του `continue`· το έπιασε το `Μ8` ⇒ **καμία ειδική περίπτωση**, άγκυρα `Κ4`. (β) Η άγκυρα των 17 κλειδιών διάβαζε `Object.keys(shellSlice.navigation.pages)` — **τον ίδιο τον κριτή**: σβήνοντας το `pages.home` έμενε **ΠΡΑΣΙΝΗ**, **ο παρονομαστής μετακινούνταν με τη μετάλλαξη**. Πλέον παρονομαστής = **locale** (τι οφείλει) vs slice (τι ταξιδεύει)· **4/4 μεταλλάξεις κόκκινες**. 🏆 **ΔΥΟ ΕΠΙΦΑΝΕΙΕΣ**: κείμενο **και** `title`/`placeholder`/`aria-label`/`alt`. Στο `/spaces/parking` **4 σε κείμενο + 7 σε `aria-label`** — text-only χρησμός θα ανέφερε 4 και θα **φαινόταν σωστός**· το `aria-label` είναι η **μόνη** ετικέτα του αναγνώστη οθόνης. ⚠️ **ΤΟ ΠΛΑΣΤΟ UA ΕΙΝΑΙ ΜΕΡΟΣ ΤΗΣ ΟΡΘΟΤΗΤΑΣ**: `BLOCKED_BOT_PATTERNS` (`src/middleware.ts`) ⇒ **403 με ΚΕΝΟ σώμα** σε `curl/`·`node-fetch`·`axios/`·`headlesschrome`, **χωρίς εξαίρεση για dev** (επαληθευμένο ζωντανά) ⇒ naive probe = **«0 σε 141 διαδρομές»**. **ΜΗΝ** αφαιρέσεις pattern (κώδικας ασφαλείας, μάθημα 3.46). **Θετικό control ανά διαδρομή** = 2.003 ελληνικές τιμές **παραγόμενες από το slice** (αλλιώς ⛔ `probe-unproven`: *ο χρησμός δεν λέει «0» χωρίς να αποδείξει ότι κοίταξε*). **Κλειστό σύμπαν 30.286 κλειδιών** — **ΠΟΤΕ** ευρετικό `\w+(\.\w+)+` (πιάνει `nestorconstruct.gr`, `report.pdf`). ⚠️ **`127.0.0.1`, ΟΧΙ `localhost`**: το `fetch` του Node λύνει πρώτα `::1` ⇒ `ECONNREFUSED` = «unreachable» για λόγο που δεν είναι η εφαρμογή. ⚠️ **`next start`, ΟΧΙ `next dev`** (μετρημένο: **167s** cold compile για το `/`). ⚠️ **ΜΗΝ** κάνεις τον Χ zero-tol στα κλειδιά (υπάρχουν ζωντανά Κλάσης Β ⇒ μονίμως κόκκινο ⇒ `SKIP_`· δοκιμάστηκε και απορρίφθηκε στο 3.39)· **ΜΗΝ** διαβάσεις τη baseline ως δείκτη υγείας (θεραπεία = **per-route slices**, ADR-744 Φ4). Ταυτότητα `διαδρομή|επιφάνεια|κλειδί` — **χωρίς γραμμή** (μετακίνηση ≠ add+remove) και **ανά επιφάνεια** (διόρθωση στο κείμενο δεν κρύβει νέα στο `aria-label`). **Boy Scout**: flatten locale κλειδιών σε SSoT `lib/i18n/locale-keys.js`, με **απόδειξη ταυτότητας πριν την αλλαγή** (101 ns / 31.361 κλειδιά / **0 διαφορές**)· ⚠️ το `flattenSchema` του `_shared/i18n-governance.js` **ΔΕΝ** ενοποιείται (απαντά «τι **τύπος**» και βάζει **ενδιάμεσους** κόμβους — ADR-749 αντίστροφα). **ΚΑΜΙΑ νέα μηχανή** (`buildShellPlan` δέχεται **οποιοδήποτε** config ⇒ αυθαίρετες ρίζες με **μηδέν** αλλαγή)· Layer 1 **~1,5s** (Κ2 από το **manifest**, χωρίς γράφο) · Layer 2 `--all` (**ξαναχτίζει γράφο** ~20s: **νέο layout** αλλάζει «ό,τι ζωγραφίζει πάντα» — δηλωμένο όριο). **Κανένα νέο workflow**: 2 jobs στο `i18n-shell-slice.yml`, **όχι** στο `docker-build.yml` (Tier 1) — πύλη **ποιότητας** δεν κουμπώνει στον αγωγό **κυκλοφορίας**, αλλιώς ωμό κλειδί μπλοκάρει το deploy στο Netcup και η αποτυχία λέγεται «απέτυχε το Build & Deploy» (μάθημα ADR-775)· μητρώο **31** πύλες, **επαληθευμένο ΕΚΤΕΛΩΝΤΑΣ** το 3.37. 🔴🔴 **ADR-788 (22/08) — Ο ΧΡΗΣΜΟΣ ΔΕΝ ΕΙΧΕ ΤΡΕΞΕΙ ΠΟΤΕ, ΚΑΙ ΔΕΝ ΜΠΟΡΟΥΣΕ ΝΑ ΚΡΙΝΕΙ.** Δύο ανεξάρτητα ελαττώματα, και τα δύο μετρημένα. **(Α) ΔΕΝ ΕΚΤΕΛΟΥΝΤΑΝ**: το job έχτιζε **δεύτερο** production server με **1 από τις 20** μεταβλητές env των δύο άλλων καλούντων του `build:ci` και πέθαινε σε **OOM στα ~4,1 GB** (run `32458992910`) ⇒ **μηδέν εκτελέσεις επί 13 ημέρες**, η `.i18n-ssr-oracle-baseline.json` **δεν υπήρξε ποτέ** — δεν ήταν «0 = καθαρό», ήταν «**δεν υπάρχει καν αρχείο**». ⚠️ Το σχόλιο **μέσα** στο job έλεγε ότι έλειπε «**μία γραμμή env**» και ότι είχε διορθωθεί: **η περιγραφή της διόρθωσης ΗΤΑΝ η απόκλιση** (σχήμα 3.34/3.37). Πλέον **δεν χτίζει**: νέο `i18n-ssr-oracle.yml` (Tier 2, `workflow_run` μετά το `docker-build.yml`) που **τραβάει την εικόνα** `ghcr.io/…:main-<sha>` — το `Dockerfile` **μόνο αντιγράφει** το `.next/standalone`, άρα η εικόνα **ΕΙΝΑΙ** ο server, όχι κάτι ισοδύναμο. 🏆 «Build once, deploy many»: το ξαναχτίσιμο ανά pipeline είναι τεκμηριωμένο anti-pattern — η κατανάλωση του **ίδιου** artifact δεν **διαχειρίζεται** την απόκλιση, την κάνει **αδύνατη**. ⚠️ **ΜΗΝ** το βάλεις βήμα στο `docker-build.yml` (Tier 1): ωμό κλειδί θα σταματούσε το deploy και η αποτυχία θα λεγόταν «απέτυχε το Build & Deploy» (ADR-775/757). 🔶 **Δηλωμένο όριο**: τρέχει **μετά** το merge (η εικόνα δεν υπάρχει πριν)· `workflow_dispatch` για κατ᾽ απαίτηση. 🔶 Τα μυστικά παραγωγής λείπουν στο CI ⇒ διαδρομή που τα χρειάζεται βγαίνει ⛔, **ποτέ σιωπηλά «καθαρή»**. **(Β) ΔΕΝ ΜΠΟΡΟΥΣΕ ΝΑ ΚΡΙΝΕΙ**: το θετικό control παραγόταν **αποκλειστικά** από το κέλυφος, που ζωγραφίζεται σε **ΚΑΘΕ** διαδρομή ⇒ ο φρουρός `probe-unproven` ήταν **δομικά αδύνατο** να πυροδοτήσει (αδρανής φρουρός, ADR-749 §5). *Ο χρησμός αποδείκνυε ότι η **διαδρομή κοιτάχτηκε**, ποτέ ότι η **επιφάνεια αποδόθηκε**.* Μετρημένο: corpus **18.657** ελληνικές τιμές ≥4 σε **103** ns · κέλυφος **2.119** · **σελίδα 16.538 (88,6%)**. Πλέον **ΔΥΟ ξένα σύνολα** (`lib/i18n-ssr/controls.js`) και **ΔΥΟ νέες καταστάσεις**: 🔴 `surface-shell-only` (στατική διαδρομή που έβαψε **μόνο** το κέλυφος — ratchet) · 🔶 `surface-synthetic-id` (**33** δυναμικές με id **που δεν υπάρχει** — **μετριέται, δεν απαριθμείται**, πρότυπο `unanalyzable-heritage` του 3.44). ⚠️ **Η ΑΣΥΜΜΕΤΡΙΑ ΕΙΝΑΙ ΣΤΗ ΣΕΙΡΑ ΚΡΙΣΗΣ**: πάνω σε ακρίτη επιφάνεια το «**βρήκα** ωμό κλειδί» παραμένει αληθές, το «**δεν** βρήκα» δεν αποδεικνύει τίποτα ⇒ το `raw-key` κρίνεται **ΠΡΙΝ** (άγκυρα `Ν4`). 🔬 **Το ανά-διαδρομή control μετρήθηκε και απορρίφθηκε**: υπάρχει (`buildRouteSlice`, **2.945** τιμές για το `/properties/[id]`) αλλά κοστίζει **~25 λεπτά** ανά εκτέλεση **και ΔΕΝ λύνει την Παγίδα Α** — η δυναμική διαδρομή βάφει το «δεν βρέθηκε» με κλειδιά του **δικού της** ns, άρα θα έλεγε ✅ πάνω σε οθόνη που δεν είναι η σελίδα. ⚠️ **Ο ΔΙΑΧΩΡΙΣΤΗΣ**: το δεύτερο πέρασμα ένωνε κόμβους με `join('')` ⇒ ταιριάσματα **μέσα σε λέξη** που ποτέ δεν υπήρξαν στην οθόνη — *απόδειξη πάνω σε συμβολοσειρά που ο browser δεν ζωγράφισε δεν είναι απόδειξη*· πλέον **κενό** (άγκυρα `Ν7`). ⚠️ **154 διαδρομές, ΟΧΙ 141** (και **33** δυναμικές, όχι 29) — ο αριθμός ταξίδεψε από handoff σε ADR σε αυτό το αρχείο χωρίς να ξαναμετρηθεί· **μέτρησέ τον**. ⚠️ **ΜΙΑ πηγή** για ταυτότητες+λεπτομέρειες (`toViolations`) — τις έχτιζαν **δύο** βρόχοι και το `route-skipped` έβγαζε ταυτότητα που καμία λεπτομέρεια δεν αντιστοιχούσε (ADR-749 σε μικρογραφία). ⚠️ **`@jest-environment node`** στις άγκυρες: σε jsdom το `fetch` απαντά σφάλμα δικτύου ⇒ **όλες** έβγαιναν `route-unreachable`, σφάλμα **περιβάλλοντος** που διαβάζεται ως «σπασμένη πύλη» (3.46). Μητρώο **33 → 34**. Tests: `npm run test:i18n-ssr` (**38** στον χρησμό: 23 + Ν1-Ν8 + Δ1-Δ6· **12/12 μεταλλάξεις**, Μ0 πράσινο πριν ΚΑΙ μετά). 🔴🔴 **ADR-790 (22/08) — Η ΦΩΤΟΓΡΑΦΙΑ ΓΡΑΦΤΗΚΕ, ΚΑΙ ΧΡΕΙΑΣΤΗΚΕ ΝΑ ΑΝΑΤΡΑΠΟΥΝ ΤΕΣΣΕΡΑ.** Ο χρησμός **αρνήθηκε** baseline σε **6** διαδρομές· η άρνηση ήταν **σωστή**, έλειπε **λεξιλόγιο**. **(Α) Η ΛΙΣΤΑ ΤΟΥ `src/app/**` ΔΕΝ ΕΙΝΑΙ Η ΛΙΣΤΑ ΤΗΣ ΠΑΡΑΓΩΓΗΣ**: το `.i18n-ssr-served.json` δηλώνει **ΜΗΧΑΝΙΣΜΟΥΣ** παρακράτησης με **υποχρεωτικό λόγο** (τα `SCANNER_PATHS` του `src/middleware.ts` · ο φρουρός `if (!isTestHarnessRouteEnabled()) notFound()` **μέσα στη σελίδα**) και οι διαδρομές **ΠΑΡΑΓΟΝΤΑΙ** κάθε φορά — ⚠️ **ΠΟΤΕ χειρόγραφη λίστα διαδρομών**, το σχήμα που απέτυχε μετρημένα σε **3.34** (63) · **3.37** (18 vs 26) · **3.57** (19/20). **ΔΥΟ ανεξάρτητοι κανόνες**: δηλωμένη ⇒ ο server δεν τη σερβίρει · **αδήλωτη ⇒ 404 = ⛔** (ένας με «ή» θα ήταν πράσινος **και προς τις δύο** κατευθύνσεις). **(Β) 🔴 ΤΟ ΚΕΛΥΦΟΣ ΑΠΕΔΕΙΚΝΥΕ ΤΗ ΣΕΛΙΔΑ** — η **δεύτερη** επανάληψη του ελαττώματος του ADR-788, μία στάση κάτω: η αναζήτηση **υπο-συμβολοσειράς** έκανε τη λέξη «Ελληνικά» (κομμάτι του `aria-label="Αλλαγή γλώσσας - Ελληνικά"` **του κελύφους**) «απόδειξη σελίδας» σε **137 από 150** ζωντανές διαδρομές — **139/145 = 95,9%** των αποδείξεων ήταν τέτοιες. Η ίδια η κεφαλίδα έγραφε «**ποτέ πέρα** από κόμβο» ⇒ **ο κώδικας αντέφασκε με το συμβόλαιό του**. Πλέον **ΑΚΕΡΑΙΟΙ ΓΕΙΤΟΝΙΚΟΙ ΚΟΜΒΟΙ** (αρχή κόμβου → τέλος κόμβου)· **μηδέν κόστος**: ίδια 6 proven με το αυστηρό ταίριασμα. ⚠️ Ο «καθαρισμός» του συνόλου **δοκιμάστηκε και απέτυχε** (−341 τιμές, το «Ελληνικά» **επιβιώνει** — η ρυπαρή τιμή του κελύφους είναι **interpolation**, άρα εξαιρείται εξ ορισμού). ⚠️ **ΜΗΝ διαβάσεις το `clean` 105 → 3 ως χειροτέρευση — είναι ΟΡΑΤΟΤΗΤΑ** (σχήμα 3.8: 11→24). **(Γ) 🔴 ΤΡΙΑ δηλωμένα dev-only harness απαντούν 200 στην παραγωγή**: φυσικό πείραμα **n=5** — τα **(app)** (`loading.tsx` ⇒ Suspense ⇒ streaming) δίνουν **200**, τα **(bare)** (χωρίς) δίνουν **404**· τεκμηρίωση Next.js αυτολεξεί: «*200 for streamed responses, 404 for non-streamed*». Το σώμα **δεν** είναι το harness (καμία διαρροή) — λάθος είναι ο **κωδικός** ⇒ 🔴 `withheld-but-answered`. **(Δ) Η ΑΣΥΜΜΕΤΡΙΑ ΗΤΑΝ ΓΡΑΜΜΕΝΗ ΑΛΛΑ Η ΣΕΙΡΑ ΤΗΝ ΑΚΥΡΩΝΕ**: το `raw-key` κρίνεται πλέον **ΠΡΩΤΟ** — ένα κλειδί του **δικού μας** κλειστού σύμπαντος **είναι** η ισχυρότερη απόδειξη ότι κοιτάχτηκε δική μας επιφάνεια· το `/mandate/ssr-probe` βάφει **δύο ωμά κλειδιά και τίποτε άλλο** (ακριβώς επειδή λείπει το ns του) και **μπλόκαρε τη φωτογραφία ΟΛΟΚΛΗΡΟΥ του έργου**. ⚠️ Η άγκυρα `Ν4` **έβγαινε ΠΡΑΣΙΝΗ** πάνω σε αυτό (χρησιμοποιούσε σελίδα με **και** τα δύο controls, άρα η σειρά δεν ασκούνταν) ⇒ `Ν4β` με τη **ζωντανή** διαμόρφωση. **+ `surface-not-rendered`** (200 με **μηδέν** επιφάνειες σώματος — `/oauth/consent`, όλο το σώμα σε `<Suspense fallback={null}>`)· για να υπάρξει η τιμή «μηδέν», το **`<head>` έπαψε να μετράει** ως αποδοθείσα επιφάνεια (αλλιώς κάθε έγγραφο έχει ≥1 κόμβο, τον `<title>`, και η κατάσταση θα γεννιόταν με **μαγικό κατώφλι**)· ο τίτλος **κρίνεται** ρητά ως `document-title`, **κόστος 207→207 = μηδέν**. ⚠️ Το ⛔ `probe-unproven` **παραμένει οπλισμένο** με δική του άγκυρα (`Ρ2`) — αλλιώς θα «λύναμε» το μπλοκάρισμα σβήνοντας τον έλεγχο (ADR-749 §5). 🔬 **Η ΑΝΤΙΣΤΡΟΦΗ ΚΑΤΕΥΘΥΝΣΗ ΜΕΤΡΗΘΗΚΕ ΚΑΙ ΑΠΟΡΡΙΦΘΗΚΕ**: το `pageExtensions` + `page.dev.tsx` **υπάρχει** και θα εξαφάνιζε τις διαδρομές από την απογραφή = **σιωπηλή συρρίκνωση κάλυψης** (3.38), δεν λύνει τα `/debug*` (Edge middleware, όχι build), και θα **έκρυβε** το εύρημα Γ. 🔬 **ΒΑΘΜΟΝΟΜΗΣΗ**: η τοπική εκτέλεση με `I18N_SSR_ORACLE_BASE_URL=https://nestorconstruct.gr` δίνει **ΤΑΥΤΟΣΗΜΗ** απογραφή με το CI job πάνω στην τραβηγμένη εικόνα (105/29/12/2/4/2) ⇒ **ανεξάρτητη επιβεβαίωση του ADR-788** *και* σπορά **χωρίς Docker** (το ADR-788 §8 τη δήλωνε αδύνατη). 🔴 **ΕΥΡΗΜΑ ΓΙΑ ΤΟΝ GIORGIO**: το `/debug` είναι στα `SCANNER_PATHS` ⇒ **404 σε ΚΑΘΕ περιβάλλον, και στο dev** — δύο σελίδες (184 γρ.) που **δεν ανοίγουν ποτέ**, με **τρεις** δείκτες να τις δείχνουν (`smart-navigation-factory.ts:717` **μενού** · `jobs-registry.ts:320` · μήνυμα API). ⚠️ **ΜΗΝ** αφαιρέσεις pattern από το middleware χωρίς εντολή (κώδικας ασφαλείας, 3.46). ⚠️ **ΜΗΝ** διαβάσεις το **312** ως δείκτη υγείας: **207** είναι ωμά κλειδιά, **101** είναι `surface-shell-only` = «το περιεχόμενο δεν έφτασε ποτέ στο SSR HTML» (κανονικό για App Router με `loading.tsx`), και παραμένει **ratchet** επειδή **δεν επιβαρύνει** (νέα διαδρομή μπλοκάρει ήδη μέσω `declarations`) αλλά **φυλά την παλινδρόμηση** clean→shell-only. ⚠️ Η παρακράτηση είναι **μέρος της δήλωσης** (`(withheld: <μηχανισμός>)`) — αλλιώς σελίδα που **αποκτά** φρουρό βγαίνει σιωπηλά από την κρίση. ⚠️ **Μια μετάλλαξη βγήκε ΠΡΑΣΙΝΗ**: το `Σ9` διάβαζε την **ήδη γραμμένη** baseline (στιγμιότυπο, όχι μηχανισμό — σχήμα «Μ6» του 3.8) ⇒ εξαγωγή `declarationOf()` και η άγκυρα καλεί το **εκτελέσιμο**. **Κανένα νέο workflow**, μητρώο **34** αμετάβλητο. Tests: `npm run test:i18n-ssr` (**66 → 84**· ο χρησμός **38 → 56**: + Ν4β + Ρ1-Ρ7 + Σ1-Σ9β· **12/12 μεταλλάξεις**). Αναφορές: `npm run i18n-ssr:report` · `npm run i18n-ssr-oracle:report`. Escape: `SKIP_I18N_SSR_RAW_KEYS=1` / `SKIP_I18N_SSR_ORACLE=1`
