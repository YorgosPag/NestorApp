# ADR-750 — Διαμόρφωση Περιγραμμάτων Πίνακα (Cell Borders) — Έρευνα Αγοράς + Μοντέλο Ακμών

- **Status**: 🟡 **ΦΑΣΕΙΣ 0–6 ΚΛΕΙΣΤΕΣ ΣΕ ΚΩΔΙΚΑ· η Φ6 ΕΙΝΑΙ ΑΝΕΠΑΛΗΘΕΥΤΗ ΖΩΝΤΑΝΑ** (2026-08-05).
  🔴 **Η Φ6 δεν έχει δει οθόνη**: 30 νέα tests περνούν, αλλά κανένα δεν ζωγράφισε καμβά. Τα δύο
  σημεία που **πρέπει** να μετρηθούν πρώτα είναι στο **§20.6** (το portal-μέσα-σε-portal, και
  το κλικ στη ζώνη που το jsdom δομικά δεν μπορεί να δοκιμάσει). Ό,τι λέει αυτή η ενότητα για
  τη Φ6 προέρχεται από jest, **όχι από pixel** — μην το διαβάσεις ως «δουλεύει».
  Η **Φ5** επαληθεύτηκε ζωντανά κατά **4/6** (2026-08-04): τα βήματα **1, 2, 3, 6** μετρήθηκαν
  στην οθόνη με **δεδομένα και pixel**· το **4** είναι αποδεδειγμένο σε δεδομένα **και σε
  γεωμετρία** (4 νέα tests), με ανοιχτή **μόνο** την pixel-επιβεβαίωση· το **5** δεν δοκιμάστηκε.
  **Το κρίσιμο βήμα 3 (§19.2) πέρασε.**
  Πλήρεις μετρήσεις: **§19.8**. Οι Φ1–Φ4 επαληθεύτηκαν στην οθόνη (§18.9). Επόμενο: **ζωντανή
  επαλήθευση της Φ6** (§20.6) και μετά **Φ7** (προέλευση + μη-modal ζωγραφική, §13).
  Οι αποφάσεις Α1–Α9 του §5 ελήφθησαν από τον Giorgio μέσα στη συνεδρία της Φάσης 0, **δύο από
  αυτές αφού η έρευνα ανέτρεψε την αρχική διατύπωση** (§4). Καμία γραμμή δεν γράφτηκε πριν
  κλείσει το §6. Οι Α13–Α26 προστέθηκαν από τις Φ1–Φ5, καθώς ο κώδικας έθετε ερωτήματα που
  το σχέδιο δεν είχε δει.
- **Date**: 2026-08-03
- **Category**: DXF Viewer / Tables / Annotation / Research
- **Σχετικά**:
  - **ADR-739** (Canvas Table System) — **ο γονέας**. Το `TableStyle`, η μηχανή διάταξης
    (`measure` → `place` → `borders`), η σειριοποίηση «Λύση Α», οι ζώνες δείκτη, το mini
    toolbar μορφοποίησης (§28.7). Αυτό το ADR είναι **βήμα της Φάσης Δ** του ADR-739.
  - **ADR-642 Φ5** (Compound line styles) — `LinePatternLayer` με `offsetMm`· **η διπλή
    γραμμή του Excel υπάρχει ήδη εκεί** (§6.4). Καμία νέα μηχανική.
  - **ADR-733** (Text decorations SSoT) — το πρότυπο «ένα SSoT αναλογιών, μηδέν τοπικός
    υπολογισμός θέσης», που ακολουθεί και η διπλή γραμμή.
  - **ADR-587** (Capability anchors) — τα gates που θα ρωτήσουν όταν αλλάξει η εξαγωγή.
  - **ADR-040** (micro-leaf perf) — ο κανόνας #2 (event-time getters) και ο κανόνας #3
    (τι ΔΕΝ μπαίνει σε cache key) ισχύουν αυτούσιοι στο hover ακμής (§9.3).
  - **ADR-748 §14.7** — «η ένδειξη κατάστασης δεν είναι *μια ένδειξη κάπου*, είναι ένδειξη
    **που δεν μπορείς να μη δεις**». Εφαρμόζεται στη μη-modal ζωγραφική (§10.3).

> **Εντολή Giorgio (2026-08-03)**: *«Στο mini toolbar του πίνακα, όταν κάνω δεξί κλικ, θέλω
> να προσθέσεις την εντολή διαμόρφωσης περιγραμμάτων όπως στο Excel. Όπως το κάνουν οι
> μεγάλοι παίκτες (Revit / ArchiCAD / Maxon C4D / Figma-level), full enterprise + full SSoT,
> χωρίς εκπτώσεις. Αν για κάτι δεν είσαι σίγουρος, κάνε έρευνα. Και αν μπορείς, βρες λύσεις
> ακόμη πιο έξυπνες και προηγμένες από αυτές που χρησιμοποιούν οι μεγάλοι.»*

---

## 1. Context — τι ζητήθηκε και τι υπήρχε

Το Excel εκθέτει, σε ένα dropdown του mini toolbar, **13 προκαθορισμένες εντολές
περιγράμματος** + ένα υπομενού «Σχεδίαση περιγραμμάτων» + το dialog «Περισσότερα
περιγράμματα». Ο Νέστωρ έχει σήμερα mini toolbar μορφοποίησης (`TableFormatToolbar.tsx`,
ADR-739 §28.7) με **B / I / U / A+ / A− / ↺** και **κανένα** χειριστήριο περιγράμματος.

Το κρίσιμο δεν ήταν το κουμπί. Ήταν ότι **το μοντέλο δεν είχε πού να γράψει**.

---

## 2. SSoT Audit — τι ΥΠΑΡΧΕΙ (μετρημένο, 2026-08-03)

| Κομμάτι | Θέση | Κατάσταση |
|---|---|---|
| Μολύβι ακμής | `TableBorderSpec` — `types/table.ts:153-160` | ✅ `{visible, colorHex, widthMm, dashMm?}` — **επαναχρησιμοποιείται αυτούσιο** |
| 6 περιγράμματα ανά κλάση γραμμής | `TableBorders` — `table-style.ts:44-51` | ✅ `top/bottom/left/right/insideH/insideV` |
| Μηχανή ακμών | `table-layout-borders.ts` | ✅ ένωση συνεχόμενων, απόκρυψη εσωτερικών ακμών συγχώνευσης |
| **Η μοναδική παράκαμψη ακμής σήμερα** | `TableRow.borderTop` — `types/table.ts:179` | ⚠️ **μόνο πάνω, μόνο ολόπλατη, μόνο ανά γραμμή**· **καμία** αντίστοιχη στη στήλη |
| Επιλογή (runtime SSoT) | `state/table-cell-cursor-store.ts:143` | ✅ `selection: TableSelectionSpan = {from, to, kind}` |
| Πρόθεση επιλογής | `TableSelectionKind` — `table-cell-range.ts:174` | ✅ `'range' \| 'column' \| 'row'` — **οι ζώνες δείκτη και τα κελιά είναι ΗΔΗ το ίδιο αντικείμενο** |
| Ένας δρόμος πρόθεση→όρια | `resolveTableSelectionBounds` — `table-cell-range.ts:206` | ✅ κουμπώνει σε συγχωνεύσεις |
| Μοτίβο καθαρής πράξης | `table-axis-style-ops.ts` | ✅ `(PersistedTableModel, …) → PersistedTableModel`, ταυτοτικό no-op |
| Σειριοποίηση | `PersistedTableModel.cells: TableCellEntry[]` — `types/table.ts:343-384` | ✅ ακολουθία τριάδων (Λύση Α, ADR-739 §19.2) |
| Compound γραμμές (**διπλή**) | `config/linetype-compound-presets.ts` | ✅ `LinePatternLayer` με `offsetMm`· preset «road» = *«two parallel solid rails»* |

### 2.1 Τα τρία κενά — μετρημένα, όχι υποτιθέμενα

1. **Κανένα πεδίο border σε override.** Ούτε το `TableAxisStyleOverride`
   (`types/table.ts:288-300`) ούτε το `TableCellStyleOverride` (`:249-252`) έχουν πεδίο
   ακμής. Το μόνο σημείο εγγραφής είναι το `TableRow.borderTop`.
2. **Δεξί κλικ μέσα σε κελί δεν ανοίγει τίποτα σχετικό.** Το `useCanvasContextMenu.ts:227-258`
   ρωτά τη ζώνη δείκτη (PRIORITY 1.4)· αν αστοχήσει, πέφτει στο **γενικό μενού οντότητας
   σχεδίου** (PRIORITY 2). Δεν υπάρχει τρίτη διαδρομή.
3. **Το `lucide-react` δεν έχει κανένα border icon** (επιβεβαιώθηκε στο `node_modules`,
   `^0.562.0`). Τα εικονίδια θα γραφτούν ως custom SVG, με το μοτίβο του
   `ui/toolbar/icons/ArcIcon.tsx`.

---

## 3. Έρευνα Αγοράς — πώς το κάνουν οι μεγάλοι

### 3.1 Ποιος αποθηκεύει ΑΝΑ ΚΕΛΙ vs ΑΝΑ ΑΚΜΗ

| Σύστημα | Αποθήκευση | Απόδοση |
|---|---|---|
| OOXML SpreadsheetML (`.xlsx`) | **ανά κελί** — `<border>` με `left/right/top/bottom/diagonal` | ανά ακμή (collapsed) |
| Google Sheets API v4 | **ανά κελί** (`CellFormat.borders`) — αλλά το `updateBorders` δέχεται `innerHorizontal`/`innerVertical` | ανά ακμή |
| ODF / LibreOffice Calc | **ανά κελί** (`fo:border-*`) | **ανά ακμή** — `svx/source/dialog/framelinkarray.cxx` μετατρέπει ρητά |
| AutoCAD DXF `TABLE` | **ανά κελί** (269/275/276/278/279 κ.λπ.) + flag **178 «virtual edge»** | ανά ακμή |
| Univer (canvas, MIT) | **ανά κελί** — `IStyleData.bd = {t,b,l,r}` | ανά ακμή |
| Handsontable | **ανά κελί** (overlay DIV→SVG) | ανά ακμή, με τεκμηριωμένα bugs επικάλυψης |
| CSS `border-collapse` | δήλωση ανά κελί → **επίλυση + απόδοση ανά ακμή** | *«Borders are centered on the grid lines between the cells»* |
| Revit Schedules | ούτε — καθολικό (Appearance) + proxy widget σε επιλογή | — |
| ArchiCAD Interactive Schedule | ανά **κατηγορία στοιχείου** (None / Border / Separator Only) | — |
| Figma | ανά **πλευρά ενός node** (`strokeTopWeight` κ.λπ., μόνο Rect/Frame) | — |
| Cinema 4D | **δεν έχει καθόλου πίνακες με cell borders** | — |

**Το συμπέρασμα:** *όλοι* αποθηκεύουν ανά κελί· *όλοι* ζωγραφίζουν ανά ακμή. Η
ασυμφωνία αποθήκευσης↔απόδοσης είναι η γενεσιουργός αιτία κάθε παραπόνου του §3.3.

### 3.2 Η σύγκρουση κοινής ακμής — **πέντε ασύμβατες απαντήσεις**

| # | Σύστημα | Κανόνας | Αποφασίσιμος από τα δεδομένα; |
|---|---|---|---|
| 1 | **Excel** | «εμφανίζεται το **πιο πρόσφατα εφαρμοσμένο**» | ❌ **ΟΧΙ** — χρονικός |
| 2 | OOXML default (Word) | `βάρος = πλάτος × αριθμός στυλ` → λίστα 25 στυλ → **φωτεινότητα `R+B+2G`** → `B+2G` → `G` → σειρά ανάγνωσης | ✅ αλλά **6 επίπεδα** — απρόβλεπτο |
| 3 | OOXML **simplified** (`useSingleBorderforContiguousCells`) | cell > table· **δεξί > αριστερό**· **κάτω > πάνω** | ✅ — εξηγήσιμο σε μία πρόταση |
| 4 | CSS `collapse` | `hidden` > … > `none`· **πλατύτερο νικά** → σειρά στυλ → προέλευση | ✅ — 4 κανόνες |
| 5 | ezdxf TablePainter | **ρητός αριθμός priority** (100 ρητό / 50 default grid) | ✅ — *δηλωμένος*, όχι συναγόμενος |
| 6 | InDesign | καθολικός διακόπτης χρήστη: *Best Joins / Row Strokes in Front / Column Strokes in Front* | ✅ — **ο μόνος που το εκθέτει σε UI** |

### 3.3 Τα πραγματικά παράπονα — τεκμηριωμένα με πηγή

| # | Παράπονο | Τεκμήριο |
|---|---|---|
| **Π1** | **Ο χρήστης εγκλωβίζεται στο μολύβι** | *«Once I use a pen, I can't put it away… The only solution I have found is to hit the 'save' button»* — **60+ αναφορές** |
| **Π2** | **Δεν ξέρει ποιο κελί κατέχει τη γραμμή** | *«Deleting the cell makes no difference — whatever cell is in that position has no border»* |
| **Π3** | **«Το περίγραμμα δεν φεύγει»** | *«The border line is through my whole sheet and won't go away»* |
| **Π4** | Ασυνεπής εμφάνιση | *«my borders do not show up consistently»* · *«I have the same problem. It drives me crazy!»* |
| **Π5** | **Copy/paste + εισαγωγή γραμμής καταστρέφουν τα περιγράμματα** | MS Q&A, επιβεβαιωμένο |
| **Π6** | Τα εικονίδια δεν ξεχωρίζουν | *«it has gotten a lot harder to tell the difference between one style and the next»* — thread **χωρίς καμία απάντηση** |
| **Π7** | Το LibreOffice έχει το ίδιο από την άλλη πλευρά | *«No matter how many times I select "bottom only", Calc insists on putting the double border around all 4 sides»* |
| **Π8** | **Καμία ανάδραση τρέχουσας κατάστασης** | κανένα εργαλείο δεν δείχνει ποια περιγράμματα **ισχύουν ήδη** στην επιλογή |
| **Π9** | **Απόλυτο κενό προσβασιμότητας** | NVDA δεν ανακοινώνει border, και το ανακοινώνει **λάθος ως υπογράμμιση**· issue κλειστό 8+ χρόνια |

---

## 4. 🔴 Η ΑΝΑΤΡΟΠΗ — το «full parity ανά κελί» ήταν δομικά αδύνατο

Η αρχική εντολή ήταν ρητή: **«FULL PARITY EXCEL = NESTOR»**, και η φυσική ανάγνωσή της
είναι «αποθήκευσε ανά κελί, όπως το Excel». Η έρευνα την ανέτρεψε.

Επίσημο κείμενο Microsoft (EN + EL): *«Εάν εφαρμόσετε δύο διαφορετικούς τύπους
περιγραμμάτων σε ένα κοινόχρηστο όριο κελιού, εμφανίζεται το περίγραμμα που **εφαρμόστηκε
πιο πρόσφατα**.»*

**Ο κανόνας του Excel είναι χρονικός. Το `.xlsx` δεν αποθηκεύει σειρά εφαρμογής.**

Συνέπεια: ο κανόνας **δεν είναι αποφασίσιμος από το αρχείο** — ούτε από το ίδιο το Excel
όταν ξανανοίγει το αρχείο. Δεν είναι κανόνας απόδοσης· είναι κανόνας **αλληλεπίδρασης**
που χάνεται στο save. Ένα «full parity ανά κελί» θα αντέγραφε ένα μοντέλο του οποίου η
συμπεριφορά **δεν προκύπτει από τα ίδια του τα δεδομένα**.

Τρεις ανεξάρτητες επιβεβαιώσεις ότι η έξοδος είναι το edge-keyed μοντέλο:

1. Ο ερευνητής αγοράς **πρότεινε μόνος του** edge-keyed ως κορυφαία ευκαιρία, χωρίς να
   γνωρίζει την απόφαση.
2. Ο ερευνητής interop **κατέληξε ανεξάρτητα** στο ίδιο, από την πλευρά του DXF.
3. Το **LibreOffice ήδη μετατρέπει εσωτερικά σε per-edge** για να ζωγραφίσει· υποχρεώνεται
   να ξαναγράψει per-cell **μόνο** λόγω μορφής αρχείου.

Και μια τέταρτη, από την ίδια τη Microsoft: το ISO/IEC 29500 έχει σημαία
`useSingleBorderforContiguousCells` («Use **Simplified** Rules For Table Border Conflicts»)
επειδή ο default αλγόριθμος —με **φωτεινότητα `R+B+2G`** ως κριτήριο tie-break— είναι
απρόβλεπτος. **Υπάρχουν δύο επίσημοι, ασύμβατοι αλγόριθμοι στο ίδιο πρότυπο.**

> **Απόφαση Giorgio (2026-08-03), κατά λέξη**: *«ΚΑΝΤΟ ΑΝΑ ΓΡΑΜΜΗ … ΘΕΛΩ ΝΑ ΜΠΟΡΩ ΝΑ ΒΑΖΩ
> ΠΕΡΙΓΡΑΜΜΑ ΣΕ ΜΕΜΟΝΩΜΕΝΟ ΚΕΛΙ. ΚΑΙ ΝΑ ΑΠΟΦΥΓΟΥΜΕ ΤΟ ΠΑΡΑΠΟΝΟ ΤΩΝ ΧΡΗΣΤΩΝ ΣΤΟ ΕΞΕΛ.»*

Τα δύο **δεν** συγκρούονται: το μοντέλο ανά ακμή δεν εμποδίζει καθόλου το περίγραμμα σε
μεμονωμένο κελί — γράφει τις **4 ακμές του**. Η διαφορά εμφανίζεται **μόνο** στο σβήσιμο,
και είναι ακριβώς η ζητούμενη διόρθωση (§10.1).

---

## 5. Οι Αποφάσεις — ΕΛΗΦΘΗΣΑΝ (Giorgio, 2026-08-03)

| # | Απόφαση | Σημείωση |
|---|---|---|
| **Α1** | **Μοντέλο ανά ΑΚΜΗ πλέγματος** (edge-keyed SSoT) | αναθεωρημένο μετά το §4· η αρχική διατύπωση ήταν «ανά κελί όπως το Excel» |
| **Α2** | **Διαγώνια περιγράμματα** — ναι | ⚠️ **δεν** είναι ακμές πλέγματος (§6.3) |
| **Α3** | **Πλήρες στυλ γραμμής** — διπλή, διακεκομμένη, παχιά | μέσω ADR-642 compound (§6.4) |
| **Α4** | **Εμβέλεια: επιλογή κελιών ΚΑΙ ζώνες δείκτη** | δωρεάν — είναι ήδη το ίδιο `TableSelectionSpan` (§2) |
| **Α5** | Το UI μιλά τη **γλώσσα του Excel** (κελί/περιοχή)· το μοντέλο γράφει **ακμές** | ο χρήστης δεν μαθαίνει ποτέ τη λέξη «ακμή» |
| **Α6** | **Οι 13 εντολές του Excel αυτούσιες** + «Περισσότερα περιγράμματα» | §8 |
| **Α7** | **Μη-modal ζωγραφική ακμών** αντί για «λειτουργία μολυβιού» | §10.3 — λύνει το Π1 |
| **Α8** | **Ορατή προέλευση ακμής** (ποιος την όρισε) | §10.2 — λύνει τα Π2/Π8/Π9 |
| **Α9** | Εκτέλεση με **Orchestrator** | παράλληλοι υποπράκτορες ανά τομέα |
| **Α10** | «Εξωτερικά» σε περιοχή **δεν αγγίζει τις εσωτερικές** | Excel parity, το κρατάμε (§7) |
| **Α11** | Πολιτική εισαγωγής σε διαφωνία: **δεξί > αριστερό, κάτω > πάνω** | ISO/IEC 29500 `useSingleBorderforContiguousCells` (§14.2) |
| **Α12** | Εξαγωγή DXF: **εκπομπή edge→cell** και στα δύο γειτονικά | χωρίς απώλεια (§14.3) |
| **Α13** | Διαγραφή άξονα: **το σύνορο μετακομίζει, το τμήμα φεύγει** | ⭐ **ΝΕΑ — Φάση 1**· δεν είχε τεθεί στη Φάση 0 (§6.6) |
| **Α14** | «Χωρίς περίγραμμα» = **ρητό αόρατο μολύβι** (κανονική μορφή)· η επιστροφή στην κληρονομιά είναι **ξεχωριστή** εντολή | ⭐ **ΝΕΑ — Φάση 2** (§6.7) |
| **Α15** | Το τρέχον μολύβι είναι **παράμετρος**, ποτέ global/store | ⭐ **ΝΕΑ — Φάση 2**· ποιος το κρατά αποφασίζεται στη Φ3 |
| **Α16** | Συγχώνευση: **γράφονται όλες** οι ακμές της περιοχής· η δομή αποφασίζει τι φαίνεται | ⭐ **ΝΕΑ — Φάση 2** (§6.8) — 🔴 **η έρευνα ανέτρεψε την αρχική πρόταση** |
| **Α17** | Η **διπλή γραμμή** δηλώνεται μη διαθέσιμη μέχρι τη Φ5 — όλα ή τίποτα, με ίχνος | ⭐ **ΝΕΑ — Φάση 2** (§6.9) |
| **Α18** | Ελληνικά ονόματα: **συνεπές «παχύ»** — δεν αντιγράφουμε την ασυνέπεια «πλατύ» της Microsoft | ⭐ **ΝΕΑ — Φάση 2** (§8.4)· αφορά **μόνο** ορατό κείμενο |
| **Α19** | **Κάθε ετικέτα ονομάζει το αντικείμενό της**: «Επαναφορά μορφοποίησης» (κείμενο άξονα) εναντίον «Επαναφορά περιγραμμάτων» | ⭐ **ΝΕΑ — Φάση 3** (§18.2)· Giorgio· λύνει σύγκρουση **ταυτόσημου** κειμένου στο ίδιο toolbar |
| **Α20** | Το μολύβι **παράγεται από το ενεργό στυλ** του πίνακα (εσωτερική γραμμή της κλάσης `data`), ποτέ σταθερά και ποτέ store | ⭐ **ΝΕΑ — Φάση 3** (§18.3) — απαντά το «ποιος το κρατά» που άφησε ανοιχτό η Α15 |
| **Α21** | Το dropdown των 13 είναι `role="menu"` (**εντολές**), όχι `role="toolbar"` με `aria-pressed` | ⭐ **ΝΕΑ — Φάση 3** (§18.4) — το §9.2 αφορά το **proxy preview** της Φ6, άλλο χειριστήριο |
| **Α22** | Δεξί κλικ σε κελί: στόχος **το κελί**, εκτός αν ανήκει στην επιλογή — τότε **όλη η επιλογή** | ⭐ **ΝΕΑ — Φάση 4** (§18.5) — η σημασιολογία του Excel **χωρίς** τη μετακίνηση επιλογής |
| **Α23** | Το μολύβι = **η επιλογή του χρήστη πάνω στην Α20**, κληρονομιά **ανά πεδίο** (χρώμα · στυλ · πάχος · διπλή). Απόν πεδίο ⇒ «Αυτόματο» | ⭐ **ΝΕΑ — Φάση 5** (§19.1). Η Α20 **δεν** καταργείται: γίνεται η προεπιλογή κάθε πεδίου χωριστά |
| **Α24** | Η **διπλή γραμμή** είναι **απόσταση** (`doubleGapMm`), και **αποσυντίθεται στη διάταξη** σε δύο παράλληλα τμήματα | ⭐ **ΝΕΑ — Φάση 5** (§19.3) — 🔴 **ανατρέπει την πρόταση του §6.4** (`patternKey` προς compound catalog)· κλείνει την **Α17** |
| **Α25** | Οι **διαγώνιοι** έχουν **δικό τους μητρώο** (4 εντολές) και δικό του αρχείο πράξεων — ποτέ 14η/15η εντολή του μητρώου ακμών | ⭐ **ΝΕΑ — Φάση 5** (§19.4) — εκτελεί το §6.3 σε επίπεδο κώδικα |
| **Α26** | Το **μολύβι ρυθμίζεται σε ΕΝΑ σημείο** (γραμμή εργαλείων)· οι **εντολές** προσφέρονται και από τις δύο υποδοχές | ⭐ **ΝΕΑ — Φάση 5** (§19.6) — το μολύβι είναι εργαλείο, όχι εντολή· δύο σημεία ρύθμισης = δύο απαντήσεις στο «με τι γράφω τώρα» |

---

## 6. Το Μοντέλο Δεδομένων

### 6.1 Η ταυτότητα μιας ακμής — αγκύρωση σε **σταθερά ids**, όχι σε δείκτες

Ένας πίνακας R×C έχει ακριβώς `(R+1)×C` οριζόντιες + `R×(C+1)` κατακόρυφες ακμές. Κάθε
μία αγκυρώνεται στο κελί που βρίσκεται **κάτω/δεξιά** της — δηλαδή κάθε ακμή είναι «η
**πάνω** ή η **αριστερή** πλευρά ενός κελιού», με ένα sentinel για το τελευταίο σύνορο:

```
H:<rowId>:<colId>   →  η ΠΑΝΩ ακμή του κελιού (rowId, colId)
H:$end:<colId>      →  η κάτω ακμή του πίνακα, στη στήλη colId
V:<rowId>:<colId>   →  η ΑΡΙΣΤΕΡΗ ακμή του κελιού (rowId, colId)
V:<rowId>:$end      →  η δεξιά ακμή του πίνακα, στη γραμμή rowId
```

**Γιατί «πάνω + αριστερή» και όχι και οι τέσσερις πλευρές:** επειδή διαφορετικά κάθε
εσωτερική ακμή θα είχε **δύο** ονόματα (η κάτω του Α = η πάνω του Β) — δηλαδή θα
ξαναγεννούσαμε ακριβώς τη διφορούμενη ιδιοκτησία που το §4 απορρίπτει. Η ίδια επιλογή
έγινε ανεξάρτητα από την ομάδα του Handsontable: *«only two borders — top and left — need
to be operated on»*.

**🔑 Γιατί `rowId`/`colId` και όχι δείκτες `(r, c)`:** τα ids είναι **σταθερά** στη ζωή του
πίνακα. Εισαγωγή ή διαγραφή γραμμής **δεν μπορεί** να μετακινήσει περίγραμμα σε λάθος
θέση. Αυτό εξαφανίζει δομικά το **Π5** — ένα από τα τεκμηριωμένα παράπονα του Excel — χωρίς
καμία γραμμή κώδικα συντήρησης.

SSoT του κλειδιού: **νέα** `tableEdgeKey()` δίπλα στην υπάρχουσα `cellKey()`
(`table-model-helpers.ts`), ίδιο branded-string μοτίβο. **Ποτέ** χειροκίνητη συνένωση
string σε καλούντα.

### 6.2 Πού ζει

```ts
// PersistedTableModel — δίπλα στο υπάρχον `cells`
readonly edges?: readonly TableEdgeEntry[];
// TableEdgeEntry = readonly [TableEdgeOrientation, TableEdgeRowAnchor, TableEdgeColAnchor, TableBorderSpec]
```

Ακολουθεί **ακριβώς** τη «Λύση Α» του ADR-739 §19.2 (ακολουθία εγγραφών στο αρχείο,
ευρετήριο `Map` στη μνήμη). Το πεδίο είναι **προαιρετικό** ⇒ κάθε ήδη αποθηκευμένος
πίνακας παραμένει έγκυρος χωρίς migration — η ρητή στρατηγική του `types/table.ts:96`.
Επιπλέον **δεν γράφεται καθόλου όταν είναι κενό**, ώστε πίνακας χωρίς ρητές ακμές να
παράγει byte-ταυτόσημο JSON με πριν.

> 🔴 **ΔΙΟΡΘΩΣΗ ΤΗΣ ΦΑΣΗΣ 1 (2026-08-03).** Η Φάση 0 έγραφε εδώ
> `TableEdgeEntry = [TableEdgeKey, TableBorderSpec]`. **Λάθος**, και όχι αισθητικό: το
> κλειδί ενώνει τα σκέλη με `\u0000` — μη εκτυπώσιμο byte, επιλεγμένο ακριβώς επειδή καμία
> ταυτότητα δεν το περιέχει. Αυτό ισχύει **όσο το κλειδί μένει στη μνήμη**· γραμμένο στο
> αρχείο θα σήμαινε χαρακτήρα ελέγχου μέσα σε **κάθε αποθηκευμένο σχέδιο**, και θα έκανε τη
> μορφή του κλειδιού **δέσμευση μορφής αρχείου** (σήμερα είναι μυστικό ενός module).
> Ειρωνικά, το ίδιο το §6.2 δήλωνε ότι ακολουθεί «ακριβώς» τη Λύση Α — και η Λύση Α γράφει
> τα κελιά ως **τριάδα** `[γραμμή, στήλη, κελί]` γι' αυτόν ακριβώς τον λόγο· το
> `splitCellKey` είναι «ιδιωτικό επίτηδες». Άρα η τετράδα δεν είναι απόκλιση από τη Λύση Α:
> είναι η Λύση Α, εφαρμοσμένη σωστά.

### 6.3 ⚠️ Η διαγώνιος ΔΕΝ είναι ακμή πλέγματος

Μια διαγώνιος δεν μοιράζεται **ποτέ** με γείτονα: ανήκει αποκλειστικά σε **ένα** κελί.
Άρα ζει στο κελί, όχι στον χάρτη ακμών:

```ts
// TableCell
readonly diagonal?: {
  readonly up?: TableBorderSpec;    // ↗  (OOXML diagonalUp)
  readonly down?: TableBorderSpec;  // ↘  (OOXML diagonalDown)
};
```

Το να μπει στον ίδιο χάρτη με τις ακμές θα ήταν κατηγορικό λάθος: θα έδινε στη διαγώνιο
σημασιολογία διαμοιρασμού που δεν έχει, και θα υποχρέωνε τη μηχανή ένωσης να εξετάζει
τμήματα που δεν μπορούν ποτέ να ενωθούν.

### 6.4 Η διπλή γραμμή — **υπάρχει ήδη**, δεν τη γράφουμε

Το `TableBorderSpec.dashMm` εκφράζει **διακοπτόμενο μοτίβο κατά μήκος** της γραμμής.
Η διπλή γραμμή είναι **δύο παράλληλες γραμμές εγκάρσια** — άλλο μαθηματικό αντικείμενο,
μη εκφράσιμο με `dashMm`.

Ο μηχανισμός υπάρχει: `config/linetype-compound-presets.ts` (ADR-642 Φ5) δίνει
`LinePatternLayer` με `offsetMm`, και το preset «road» είναι κυριολεκτικά *«two parallel
solid rails»*. Το ίδιο αρχείο δηλώνει ρητά: *«FULL SSoT … No second compound mechanism.»*

~~**Πρόταση προς επικύρωση στη Φάση 1**: το `TableBorderSpec` αποκτά προαιρετικό
`patternKey?: LinePatternKey` που δείχνει στο υπάρχον catalog.~~

> 🔴 **Η ΠΡΟΤΑΣΗ ΑΝΑΤΡΑΠΗΚΕ ΣΤΗ ΦΑΣΗ 5 (Α24).** Τρεις μετρήσεις, όχι αισθητική:
> 1. **Ο τύπος `LinePatternKey` δεν υπάρχει** — η πρόταση ονόμασε τύπο που δεν γράφτηκε ποτέ.
> 2. **Το catalog έχει δύο εγγραφές**, `road` και `railway`, και είναι **κατάλογος αντικειμένων
>    του κόσμου**: το `railway` ορίζει εύρος τροχιάς **1.507 mm** και στρωτήρες **2.600 mm**
>    (πραγματικές διαστάσεις, `linetype-compound-presets.ts`). Μια ακμή κελιού που δείχνει στο
>    «road» θα ήταν σημασιολογικά ψευδής· στο «railway», γεωμετρικά παράλογη.
> 3. **Η διπλή γραμμή δεν χρειάζεται μηχανισμό.** Αποσυντίθεται **στη διάταξη** σε δύο
>    παράλληλα τμήματα (§19.3), άρα καμβάς, PDF, DXF και ΤΕΚ δεν μαθαίνουν τίποτα.
>
> Η απαγόρευση **τηρήθηκε ακέραιη**: κανένα `'double'` enum δεν γεννήθηκε. Το `doubleGapMm`
> είναι **απόσταση** — ακριβώς το «Spacing» που εκθέτει ο διάλογος *Cell Border Properties*
> του AutoCAD δίπλα στο checkbox «Double line». Δεν εφευρέθηκε σημασιολογία· δανείστηκε.

**Να μη γεννηθεί `'double'` enum** — θα ήταν δεύτερος
μηχανισμός compound μέσα σε δέκα λεπτά από τη στιγμή που ανακαλύψαμε τον πρώτο (N.0.2).

### 6.5 Σειρά προτεραιότητας — **τέσσερα επίπεδα**

Το `horizontalSpec`/`verticalSpec` (`table-layout-borders.ts:50-68`) έχει σήμερα τρία.
Το νέο επίπεδο μπαίνει **πάνω** από όλα:

```
1. ΡΗΤΗ ΑΚΜΗ            ← ΝΕΟ (ο χάρτης του §6.2)
2. TableRow.borderTop      (η υπάρχουσα παράκαμψη γραμμής-συνόλου)
3. σύνορο κλάσεων γραμμής  (bottom της πάνω κλάσης)
4. insideH / insideV της κλάσης
```

**Μηδέν tie-break.** Δεν υπάρχει «ισοπαλία» να λυθεί, γιατί το επίπεδο 1 έχει **έναν**
ιδιοκτήτη εξ ορισμού. Αυτή είναι η ολόκληρη διαφορά από τα 6 επίπεδα του §3.2/#2.

⚠️ **Η συγχώνευση προηγείται και των τεσσάρων.** Η ερώτηση «υπάρχει εδώ ακμή;» απαντιέται
**πριν** από την «με τι μολύβι;». Μια ρητή ακμή **μέσα** σε συγχώνευση δεν τη ζωγραφίζει —
αλλιώς μια πράξη *μορφοποίησης* θα ακύρωνε μια *δομική* πράξη, και το πλέγμα θα φαινόταν
μέσα από το συγχωνευμένο κελί.

### 6.6 Τι απογίνονται οι ακμές όταν σβήνεται γραμμή/στήλη (Α13 — **νέα απόφαση, Φάση 1**)

Η Φάση 0 δεν έθεσε καν το ερώτημα: η αγκύρωση σε ταυτότητες λύνει την **εισαγωγή** δωρεάν,
οπότε η διαγραφή έμοιαζε συμμετρική. Δεν είναι — η διαγραφή αφαιρεί την ίδια την άγκυρα.

> **Ο κανόνας, σε μία πρόταση:** όταν σβήνεται γραμμή, οι **πάνω** ακμές της μετακομίζουν
> στην επόμενη επιζώσα — εκτός αν εκείνη δηλώνει ήδη δική της· οι **αριστερές** της φεύγουν
> μαζί της. Συμμετρικά για στήλη.

**Γιατί μετακόμιση και όχι σβήσιμο**: το πάνω περίγραμμα του πίνακα είναι αγκυρωμένο στην
**πρώτη γραμμή**. Σκέτο σβήσιμο θα εξαφάνιζε το πλαίσιο του πίνακα επειδή ο χρήστης έσβησε
μια γραμμή δεδομένων — σιωπηλή απώλεια που δεν είχε τρόπο να προβλέψει. Είναι **ακριβώς** η
Απόφαση 4 του `table-row-column-ops.ts` («η διαγραφή άγκυρας ΜΕΤΑΦΕΡΕΙ το περιεχόμενο, δεν
το σβήνει»), εφαρμοσμένη σε γεωμετρία αντί για κείμενο — δηλαδή δεν εφευρίσκεται κανόνας,
επεκτείνεται ο υπάρχων.

**Γιατί η κάθετη φεύγει**: η οριζόντια ακμή μιας γραμμής είναι **σύνορο** — μετά τη διαγραφή
το ίδιο φυσικό σύνορο εξακολουθεί να υπάρχει, ανάμεσα σε άλλα δύο κελιά. Η κατακόρυφη είναι
**τμήμα μέσα** στο ύψος της γραμμής· μαζί με τη γραμμή παύει να υπάρχει και το τμήμα.

**Το sentinel δεν επηρεάζεται ποτέ**: το κάτω/δεξί σύνορο δεν ανήκε σε γραμμή/στήλη εξ αρχής.
Διαγραφή της τελευταίας γραμμής αφήνει το `H:$end:*` να κάτσει κάτω από τη νέα τελευταία —
δηλαδή το εξωτερικό πλαίσιο είναι **δομικά** ανθεκτικό, χωρίς κώδικα συντήρησης.

### 6.7 «Χωρίς περίγραμμα» εναντίον «Επαναφορά στο στυλ» (Α14 — **νέα απόφαση, Φάση 2**)

Το ερώτημα φαίνεται λεπτομέρεια υλοποίησης· είναι **ολόκληρο** το παράπονο Π3.

> **Ο κανόνας:** το «Χωρίς περίγραμμα» γράφει **ρητό αόρατο μολύβι**. Η διαγραφή της εγγραφής
> είναι **άλλη** εντολή, με άλλο όνομα: «Επαναφορά στο στυλ».

Το μοντέλο έχει **τρεις** καταστάσεις, και οι τρεις προσβάσιμες:

```
ρητό ορατό μολύβι   →  «θέλω ΑΥΤΗ τη γραμμή»
ρητό ΑΟΡΑΤΟ μολύβι  →  «δεν θέλω γραμμή» — νικά τα επίπεδα 2–4, μένει σβηστό
απουσία εγγραφής    →  «ό,τι λέει το στυλ» (κληρονομιά)
```

Αν το σβήσιμο υλοποιούνταν ως **διαγραφή**, τα επίπεδα 2–4 θα ξαναζωντάνευαν και το πλέγμα
της κλάσης θα επέστρεφε — δηλαδή θα αναπαράγαμε **ακριβώς** το ελάττωμα που ήρθαμε να
σκοτώσουμε, με δικά μας χέρια.

**Πού στέκονται οι μεγάλοι** (έρευνα Φ2):
- **AutoCAD**: έχει την ίδια τριάδα — η τεκμηρίωση λέει ρητά *«Use BYBLOCK to set the border
  properties to match the settings in the table style»*, δηλαδή το `ByBlock` **είναι** η
  «Επαναφορά στο στυλ». Αλλά το δικό του «No border» **δεν σβήνει στην οθόνη**: τεκμηριωμένο
  ότι *«suppressing cell borders applies only to print/plot, they are still displayed on
  screen as faded (gray)»*. Δηλαδή **το Π3 υπάρχει και στο AutoCAD, σε χειρότερη μορφή.**
- **Handsontable**: έχει **και τα δύο** μονοπάτια (`setBorders({ hide })` έναντι
  `clearBorders()`) — επιβεβαιώνει ότι η διάκριση είναι πραγματική· το `hide` όμως είναι
  ανοιχτό issue (#5240).
- **Excel**: έχει **μόνο** το ένα κουμπί, με χρονική σημασιολογία — γι' αυτό δεν μπορεί ποτέ
  να απαντήσει «ποιος όρισε αυτή τη γραμμή» (§4).

⚠️ **Το αόρατο μολύβι είναι ΚΑΝΟΝΙΚΟ** (`HIDDEN_TABLE_EDGE`): δεν κρατά χρώμα/πάχος από το
τρέχον μολύβι. Αλλιώς δύο ταυτόσημα σβησίματα με διαφορετικό επιλεγμένο μολύβι θα έδιναν
**διαφορετικές** εγγραφές — βήμα undo, diff στο αρχείο και σπασμένη ένωση τμημάτων, για
μηδενική οπτική διαφορά. Είναι η **ίδια** τιμή που χρησιμοποιούν ήδη οι κλάσεις γραμμής για
«καμία γραμμή»· η Φ2 την **κεντρικοποίησε** (ήταν ιδιωτική στο `table-style-presets.ts`).

### 6.8 Συγχώνευση: γράφονται όλες οι ακμές (Α16 — **νέα απόφαση, Φάση 2**)

> **Ο κανόνας:** η πράξη γράφει **κάθε** ακμή της περιοχής. Η συγχώνευση **κρύβει** ό,τι δεν
> υπάρχει· δεν σβήνει ό,τι ζητήθηκε. Λύνεται η συγχώνευση ⇒ η μορφοποίηση **επανεμφανίζεται**.

🔴 **Η έρευνα ανέτρεψε την αρχική πρόταση.** Το σχέδιο της Φάσης 2 υπέθετε ότι το «σωστό»
είναι να **παραλείπονται** οι εσωτερικές ακμές μιας συγχώνευσης, με το επιχείρημα «αν
αργότερα σπάσει η συγχώνευση, εμφανίζονται». Η μέτρηση της συμπεριφοράς του Excel έδειξε το
αντίθετο — *«if you apply 'All Borders' after merging cells and before unmerging them, it
will apply 'All Borders' to the range even after you unmerge»* — και, το κρίσιμο, ότι αυτή
η συμπεριφορά είναι **σωστή**:

- «Όλα τα περιγράμματα» σημαίνει «θέλω πλήρες πλέγμα **εδώ**». Η παράλειψη θα άφηνε, μετά την
  αποσυγχώνευση, **τρύπα** στο πλέγμα που ο χρήστης ρητά ζήτησε — και θα ήταν αδύνατο να
  εξηγηθεί, γιατί η αιτία (μια συγχώνευση που δεν υπάρχει πια) δεν φαίνεται πουθενά.
- **Δομή και μορφοποίηση είναι ορθογώνιες.** Είναι η ίδια αρχή με το Α13: η μορφοποίηση
  **επιβιώνει** των δομικών αλλαγών αντί να καταστρέφεται σιωπηλά από αυτές.
- Η οπτική ορθότητα **όσο** υπάρχει η συγχώνευση είναι ήδη εγγυημένη από τη μηχανή, όχι από
  τις πράξεις: το §6.5 λέει «η συγχώνευση προηγείται και των τεσσάρων επιπέδων», και τρία
  tests της Φ1 το κλειδώνουν.

**Δώρο αρχιτεκτονικής**: οι πράξεις μένουν **καθαρή γεωμετρία** — μηδέν γνώση συγχώνευσης,
καμία εξάρτηση από `buildMergeIndex`/`resolveTableModel`, κανένα νέο κοινό module, καμία
αλλαγή στον ζωγράφο. Η εναλλακτική θα απαιτούσε και τα τέσσερα.

### 6.9 Η διπλή γραμμή δεν προσποιείται (Α17 — **νέα απόφαση, Φάση 2**)

Δύο από τις 13 (`Κάτω διπλό`, `Επάνω και διπλό κάτω`) χρειάζονται μηχανισμό compound που
**υπάρχει** αλλά **δεν είναι ακόμη συνδεδεμένος** με το `TableBorderSpec` (§6.4, Φ5). Οι
τρεις δρόμοι που **δεν** ακολουθήθηκαν:

| Δρόμος | Γιατί όχι |
|---|---|
| `dashMm` για «διπλή» | μοτίβο **κατά μήκος** ≠ δύο παράλληλες **εγκάρσια** — λάθος μαθηματικό αντικείμενο |
| νέο `'double'` enum | δεύτερος μηχανισμός compound δέκα λεπτά μετά την ανακάλυψη του πρώτου (N.0.2) |
| σιωπηλό no-op | κουμπί που φαίνεται ενεργό και δεν κάνει τίποτα — χειρότερο από κουμπί που λείπει |

**Η λύση**: το μητρώο δηλώνει **και τις 13** (είναι η SSoT του dropdown), και μια ρητή
ερώτηση `isTableBorderCommandAvailable(id)` απαντά ποιες εκτελούνται σήμερα. Το UI της Φ3
**ρωτά** και δεν προσφέρει νεκρό κουμπί· αν παρ' όλα αυτά κληθεί, η πράξη επιστρέφει το
μοντέλο **αυτούσιο by-reference, με ίχνος** — και **όλα ή τίποτα**: το «Επάνω και διπλό
κάτω» δεν γράφει ούτε την επάνω, γιατί μισή εντολή που δεν ζητήθηκε είναι σιωπηλό λάθος.

### 6.10 «Παχύ» = η επόμενη πένα ISO, **παραγόμενη** (Φάση 2)

Το Excel δίνει «παχιά» ως ~3 εικονοστοιχεία — νούμερο χωρίς νόημα σε φύλλο σχεδίου. Εδώ:

> **παχύ = η πλησιέστερη πένα του καταλόγου ISO στο 2× του τρέχοντος πάχους.**

Το ISO 128-20 ορίζει τις ομάδες γραμμών ως ζεύγη **λεπτή : παχιά = 1 : 2**, και ο υπάρχων
κατάλογος (`config/lineweight-iso-catalog.ts`, με δικό του ratchet) τις περιέχει ήδη. Ο
κανόνας αναπαράγει **ακριβώς** τα πρότυπα ζεύγη — `0.13→0.25 · 0.18→0.35 · 0.25→0.50 ·
0.35→0.70 · 0.50→1.00 · 0.70→1.40 · 1.00→2.00` — **χωρίς να γραφτεί κανένα από αυτά τα
νούμερα** στον κώδικα των πράξεων. Χειρόγραφος πίνακας ζευγών θα ήταν δεύτερη κλίμακα πένας
δίπλα στην SSoT. (Το ίδιο ζεύγος 0.25/0.50 χρησιμοποιεί ήδη το preset `standard` για
πλέγμα/πλαίσιο — ανεξάρτητη επιβεβαίωση από τον ίδιο τον κώδικα.)

---

## 7. Τι σημαίνει πρακτικά — ο πίνακας συμπεριφοράς

| Ενέργεια σε **ένα** κελί | Τι γράφεται |
|---|---|
| «Όλα τα περιγράμματα» | οι **4 ακμές** του |
| «Κάτω περίγραμμα» | η **κάτω** ακμή του (= η πάνω του από κάτω) |
| «Χωρίς περίγραμμα» | οι 4 ακμές του σβήνουν — **και μένουν σβηστές** |

| Ενέργεια σε **περιοχή** R×C | Τι γράφεται |
|---|---|
| «Όλα τα περιγράμματα» | όλες οι εσωτερικές + περιμετρικές |
| «Εξωτερικά περιγράμματα» | **μόνο η περίμετρος**· οι εσωτερικές **δεν αγγίζονται** (Excel parity) |
| «Χωρίς περίγραμμα» | όλες οι ακμές της περιοχής, εσωτερικές και περιμετρικές |
| **«Επαναφορά στο στυλ»** (δική μας, Α14) | **διαγράφονται** οι ρητές ακμές — επιστρέφει η κληρονομιά |
| περιοχή που περιέχει **συγχώνευση** (Α16) | γράφονται **όλες**· η δομή κρύβει τις εσωτερικές της όσο υπάρχει |

⚠️ **Μία ρητή απόκλιση από το Excel, εσκεμμένη.** Η Microsoft τεκμηριώνει ότι *«if you
apply a box border to enclose the range B1:C5, the cells D1:D5 acquire a left border»* —
δηλαδή η ενέργεια **βγαίνει έξω από την επιλογή** και μολύνει γειτονικά κελιά. Στο
edge-keyed μοντέλο αυτό είναι **μη εκφράσιμο**: η δεξιά ακμή του C είναι η ίδια ακμή με την
αριστερή του D, μία οντότητα, και γράφεται **μία** φορά. Η οπτική συμπεριφορά είναι
ταυτόσημη· η παρενέργεια στα δεδομένα εξαφανίζεται.

---

## 8. Οι εντολές (Α6)

### 8.1 Οι 13 προκαθορισμένες — ✅ **ΕΠΙΒΕΒΑΙΩΜΕΝΕΣ** (στιγμιότυπο ελληνικού Excel, 2026-08-03)

Τα ονόματα είναι **πλήρεις φράσεις**, όχι οι συντομογραφίες που έγραφε η Φάση 0. Το μενού
έχει **τρεις ομάδες** με διαχωριστικά (καταγεγραμμένες στο μητρώο ως `group`):

| # | `id` | Επίσημο ελληνικό όνομα | Ομάδα |
|---|---|---|---|
| 1 | `bottom` | Κάτω περίγραμμα | `side` |
| 2 | `top` | Επάνω περίγραμμα | `side` |
| 3 | `left` | Αριστερό περίγραμμα | `side` |
| 4 | `right` | Δεξί περίγραμμα | `side` |
| 5 | `none` | Χωρίς περίγραμμα | `range` |
| 6 | `all` | Όλα τα περιγράμματα | `range` |
| 7 | `outside` | Εξωτερικά περιγράμματα | `range` |
| 8 | `thickOutside` | **Παχιά** εξωτερικά περιγράμματα | `range` |
| 9 | `doubleBottom` | Κάτω διπλό περίγραμμα | `accent` |
| 10 | `thickBottom` | **Παχύ** κάτω περίγραμμα | `accent` |
| 11 | `topAndBottom` | Επάνω και κάτω περίγραμμα | `accent` |
| 12 | `topAndThickBottom` | Επάνω και **πλατύ** κάτω περίγραμμα ⚠️ | `accent` |
| 13 | `topAndDoubleBottom` | Επάνω και διπλό κάτω περίγραμμα | `accent` |

…και «Περισσότερα περιγράμματα…» σε δική του ομάδα στο τέλος.

⚠️ Ο πίνακας δείχνει τι γράφει **το Excel**. Εμείς γράφουμε στο #12 «Επάνω και **παχύ** κάτω
περίγραμμα» — απόφαση **Α18** (§8.4). Σε όλα τα υπόλοιπα 12, ταυτόσημο κείμενο.

> 🔴 **Η Microsoft αποδίδει το ίδιο αγγλικό *Thick* με ΤΡΕΙΣ λέξεις**: «παχιά» (#8), «παχύ»
> (#10) και «**πλατύ**» (#12). Τα #10 και #12 απέχουν **δύο γραμμές** στο ίδιο μενού. Η Φάση 0
> είχε μαντέψει «Επάνω και **παχύ** κάτω» — δηλαδή είχε μαντέψει το *συνεπές*, όχι το
> *πραγματικό*. Είναι σφάλμα μετάφρασης, όχι σχεδιαστική επιλογή — και το **Α18** (§8.4,
> ✅ αποφασισμένο) λέει ότι εμείς γράφουμε συνεπές «παχύ».

**Ανεξάρτητη επιβεβαίωση του §9.1**: το στιγμιότυπο είναι από το **mini toolbar** του Excel
(δεξί κλικ), όχι από την κορδέλα — δηλαδή ο ίδιος ο Excel κρεμά αυτό ακριβώς το dropdown σε
mini toolbar, όπως προτείνει το §9.1 για το `TableFormatToolbar`.

### 8.2 Η σύμβαση των εικονιδίων — **μετρημένη** (δεύτερο στιγμιότυπο σε μεγέθυνση)

Και τα 13 εικονίδια είναι το **ίδιο** πλέγμα 2×2, με **δύο** επίπεδα γραμμής:

```
διάστικτη ανοιχτή γραμμή  →  το πλέγμα ως ΣΥΜΦΡΑΖΟΜΕΝΟ («εδώ είναι τα κελιά»)
συμπαγής μαύρη γραμμή     →  η ΕΝΕΡΓΕΙΑ («αυτές τις ακμές αφορά»)
```

🔑 **Το διάστικτο πλέγμα δεν είναι διακόσμηση — είναι ο μηχανισμός ανάγνωσης.** Μια μόνη
συμπαγής γραμμή σε λευκό φόντο δεν λέει «κάτω»: λέει «γραμμή». Το «κάτω» προκύπτει **μόνο**
από τη θέση της σε σχέση με ένα ορατό πλέγμα. Χωρίς αυτό, τα 13 εικονίδια θα ήταν ουσιαστικά
αδιάκριτα μεταξύ τους — και αυτό είναι το πρώτο πράγμα που πρέπει να αντιγράψει η Φ3.

Οι τρεις παραλλαγές της ενεργής γραμμής, όπως φαίνονται: **λεπτή συμπαγής** (κανονικό
μολύβι) · **παχιά συμπαγής** (#8, #10, #12) · **δύο παράλληλες** (#9, #13). Δηλαδή η ίδια
τριάδα που εκφράζει και το μοντέλο: `base` / `thick` / `double` (§6.9/§6.10).

### 8.4 Α18 — ✅ **ΑΠΟΦΑΣΙΣΤΗΚΕ (Giorgio, 2026-08-03): συνεπές «παχύ»**

> Το #12 γράφεται **«Επάνω και παχύ κάτω περίγραμμα»**. Δεν αντιγράφουμε το «πλατύ».

| Επιλογή | Υπέρ | Κατά |
|---|---|---|
| Πιστή αντιγραφή («πλατύ») | μηδενικό γνωστικό κόστος για όποιον έρχεται από Excel | το ίδιο πράγμα με δύο λέξεις σε απόσταση δύο γραμμών· «πλατύ» για γραμμή είναι **λάθος τεχνικός όρος** |
| ✅ **Συνεπές «παχύ»** | μία λέξη, ένα νόημα· **σωστός** όρος: στο ISO η γραμμή έχει **πάχος** (`lineweight`), και ο κατάλογος πένων του έργου ήδη έτσι τη λέει | μικρή απόκλιση από το Excel, σε **μία ετικέτα** |

**Γιατί αυτή η απόκλιση είναι ασφαλής**: αφορά **μόνο** ορατό κείμενο. Τα `id` του μητρώου
είναι αγγλικά και σταθερά (`topAndThickBottom`), άρα η απόφαση δεν αγγίζει **ούτε μία γραμμή
λογικής** — ούτε τώρα, ούτε αν κάποτε αλλάξει.

⚠️ **Ο κανόνας που προκύπτει, για τα υπόλοιπα locale keys της Φ3**: όπου η ελληνική
τοπικοποίηση της Microsoft είναι **εσωτερικά ασυνεπής**, ακολουθούμε τον **τεχνικό όρο του
σχεδίου** (ISO / ο ίδιος ο κώδικας του Νέστορα), όχι την ασυνέπεια. Όπου είναι απλώς
**διαφορετική** από ό,τι θα λέγαμε εμείς, ακολουθούμε τη Microsoft — parity by default,
απόκλιση **μόνο** με λόγο που γράφεται.

### 8.2 «Περισσότερα περιγράμματα»

Presets (`Κανένα`/`Περίγραμμα`/`Εσωτερικά`) · **proxy preview widget με κλικ ανά ακμή** ·
στυλ γραμμής · χρώμα · **οι δύο διαγώνιοι**. Το preview ενημερώνεται άμεσα.

### 8.3 Ζωγραφική ακμών

Αντί για τα modal `Draw Border` / `Draw Border Grid` / `Erase Border` του Excel — βλ.
§10.3, απόφαση Α7.

---

## 9. UI — πού κρέμεται

### 9.1 Το σημείο εισόδου
Νέο κουμπί dropdown στο υπάρχον `TableFormatToolbar` (μετά τον διαχωριστή μεγέθους).
Το toolbar ανοίγει σήμερα **μόνο** από ζώνη δείκτη· χρειάζεται **νέα διαδρομή** για δεξί
κλικ μέσα σε κελί με ενεργή επιλογή — νέο PRIORITY **1.45** στο `useCanvasContextMenu.ts`,
**πριν** το PRIORITY 2 (entity menu), με το ίδιο μοτίβο θύρας που χρησιμοποιεί ήδη το
`table-header-menu-port`.

### 9.2 Προσβασιμότητα — υποχρεωτικό, όχι προαιρετικό
Το APG **αποκλείει ρητά** το `role="menu"` για επιλογή κατάστασης σε οπτικό πλέγμα.
- Πλευρές που **συνδυάζονται** → `role="toolbar"` + toggle buttons με `aria-pressed`
- Στυλ γραμμής (**αμοιβαία αποκλειόμενα**) → `role="radiogroup"` + `aria-checked`
- Roving tabindex (ένα tab-stop), βέλη 2D, Home/End, **Esc**
- Κάθε εικονίδιο: `aria-label`· το SVG `aria-hidden="true"`
- ⚠️ **Το tooltip ΔΕΝ αρκεί ως accessible name** — τα tooltips δεν παίρνουν εστίαση.
  Το `TableFormatToolbar.tsx:211-222` το έχει ήδη λύσει σωστά· το μοτίβο επαναλαμβάνεται.

### 9.3 Απόδοση — οι κανόνες ADR-040 ισχύουν αυτούσιοι
Το hover ακμής είναι **υψηλής συχνότητας**. Πρέπει να ζει στο υπάρχον `HoverStore`
(zero React state), οι event handlers να διαβάζουν με **getter** (κανόνας #2), και η
ταυτότητα της υπό-hover ακμής **ΔΕΝ** μπαίνει σε cache key του bitmap (κανόνας #3 —
προκαλεί 60fps ανακατασκευή σκηνής).

⚠️ **Ακύρωση μνήμης**: κάθε πράξη ακμής **πρέπει** να επιστρέφει νέο `PersistedTableModel`
by-reference. Η αλυσίδα `WeakMap` (`RESOLVED_MODEL_CACHE` → `LAYOUT_CACHE` →
`INDEX_CACHE`) κλειδώνει σε ταυτότητα αντικειμένου· διαφορετικά **η μορφοποίηση δεν
φαίνεται μέχρι reload**. Τεκμηριωμένο ρίσκο, **έχει ήδη συμβεί μία φορά**
(`table-axis-style-ops.ts:16-19`).

---

## 10. Πού ξεπερνάμε τους μεγάλους — τέσσερα σημεία, με «γιατί δεν το κάνουν αυτοί»

### 10.1 Η σύγκρουση γίνεται **δομικά ανέκφραστη**
Δεν είναι «καλύτερη επίλυση σύγκρουσης» — είναι ότι δεν υπάρχει σύγκρουση να επιλυθεί.
Εξαφανίζονται μονομιάς τα **Π2** (ιδιοκτησία), **Π3** («δεν φεύγει»), **Π4** (ασυνέπεια).

*Γιατί δεν το κάνουν αυτοί:* συμβατότητα μορφής αρχείου 30+ ετών. `.xlsx`, `.ods`, DXF
`TABLE` είναι **όλα** per-cell· καμία εταιρεία δεν σπάει το format της. Ακόμα και το
LibreOffice, που ήδη μετατρέπει εσωτερικά, υποχρεούται να ξαναγράψει per-cell.
*Γιατί εμείς μπορούμε:* μηδέν κληρονομιά — δικός μας καμβάς, δική μας persistence.

### 10.2 **Ορατή προέλευση ακμής** (Α8)
Hover σε ακμή → υπογραμμίζεται **η ακμή** (όχι το κελί) και το toolbar δείχνει στυλ,
πάχος, χρώμα **και ποιος την όρισε** (ρητή? γραμμή-σύνολο? κλάση? στυλ σχεδίου?), με
κουμπί «καθαρισμός αυτής της ακμής». Τα εικονίδια δείχνουν **τρέχουσα κατάσταση**
(`aria-pressed`), όχι νεκρά κουμπιά — λύνει το **Π8**.

*Γιατί δεν το κάνουν αυτοί:* με per-cell αποθήκευση η ιδιοκτησία είναι **γνήσια
διφορούμενη** — δύο πιθανοί ιδιοκτήτες, και ο νικητής εξαρτάται από σειρά εφαρμογής που
δεν αποθηκεύεται (§4). **Δεν μπορούν να το δείξουν γιατί δεν το ξέρουν ούτε οι ίδιοι.**
*Γιατί εμείς μπορούμε:* με το §6.1 ο ιδιοκτήτης είναι μοναδικός εξ ορισμού — η προέλευση
βγαίνει **δωρεάν**. Το `TableFormatToolbar` έχει **ήδη** το μοτίβο (`explicit` +
τελίτσα, `:58-62, 250`).

### 10.3 **Μη-modal ζωγραφική ακμών** (Α7)
Καμία «λειτουργία μολυβιού». Ο δείκτης πλησιάζει ακμή → **ανάβει** → κλικ = toggle·
σύρσιμο = διαδοχικές ακμές· `Alt`+κλικ = σβήσιμο· **`Esc` και δεξί κλικ πάντα βγάζουν**·
καμία κατάσταση δεν επιβιώνει χωρίς **μόνιμη, αδιαμφισβήτητη ένδειξη** (ADR-748 §14.7:
όχι «μια ένδειξη κάπου», αλλά ένδειξη **που δεν μπορείς να μη δεις**).

*Γιατί δεν το κάνουν αυτοί:* το `Draw Border` είναι εργαλείο εποχής ribbon-1997 πάνω σε
DOM grid **χωρίς per-edge hit-testing** — γι' αυτό είναι modal, και γι' αυτό 60+ άνθρωποι
πατούν save για να ξεφύγουν (**Π1**).
*Γιατί εμείς μπορούμε:* το per-edge hit-test είναι **το φυσικό πρωτόγονο του καμβά μας**·
υπάρχουν ήδη `HoverStore`, `ImmediatePositionStore`, `UnifiedFrameScheduler`.

### 10.4 **Τα περιγράμματα επιβιώνουν σε εισαγωγή/διαγραφή γραμμής**
Δωρεάν από την αγκύρωση σε σταθερά ids (§6.1) — λύνει το **Π5**.

> **Bonus προς αξιολόγηση**: το proxy preview widget του InDesign με **πολλαπλό κλικ**
> (μονό = μία γραμμή, διπλό σε εξωτερική = όλη η περίμετρος, διπλό σε εσωτερική = όλες οι
> εσωτερικές). Είναι το πιο εκλεπτυσμένο border UI που βρέθηκε σε οποιοδήποτε προϊόν, και
> το Revit «Edit Borders» έχει ήδη φτωχότερο αντίγραφό του — άρα το idiom είναι **οικείο
> σε κοινό CAD**.

---

## 11. i18n (N.11)

Νέο υπο-block `table.borders.*` σε **`src/i18n/locales/el/dxf-viewer.json`** ΚΑΙ
**`src/i18n/locales/en/dxf-viewer.json`**, δίπλα στο υπάρχον `table.formatToolbar.*`
(EL: γραμμές 548-559). **Πρώτα τα κλειδιά, μετά ο κώδικας** — μηδέν `defaultValue` με
κείμενο. Μετά: `npm run generate:i18n-types` (CHECK 3.33).

---

## 12. Υποχρεωτικά Gates

| Gate | Τι θα ρωτήσει | Απάντηση |
|---|---|---|
| **CHECK 3.8 + 3.33** | i18n keys | EL+EN **πριν** τον κώδικα· `generate:i18n-types` |
| **CHECK 3.23** | native tooltip | μηδέν `title=` — Radix, όπως ήδη κάνει το toolbar |
| **CHECK 3.28 (jscpd)** | sibling clone; | οι πράξεις ακμής **μοιράζονται** πυρήνα με το `table-axis-style-ops.ts`· ποτέ αντιγραφή του `resolveAxisFormat`/`forEachAxisCellStyle` |
| **CHECK 3.29 (CI)** | DXF tsc | το subapp είναι εκτός root tsconfig — **μόνο το CI βλέπει** |
| **CHECK 5C (ADR-587)** | αλλάζει η εξαγωγή; | `entity-export-coverage.ts:99` παραμένει `decompose`· αν γίνει `native`, το anchor **θα** χρειαστεί ενημέρωση |
| **N.7.1 (500 γρ.)** | ⚠️ **κρίσιμο** | `types/table.ts` **427/500** · `table-cell-range.ts` **415/500** · `use-table-header-menu.ts` 371 · `TableHeaderContextMenu.tsx` 343 · `TableFormatToolbar.tsx` 305 — **κάθε νέα πράξη σε ΝΕΟ αδελφό αρχείο**, ποτέ επέκταση επιτόπου |
| **N.17** | tsc από πράκτορα | **ΑΠΑΓΟΡΕΥΕΤΑΙ** — jest ναι |

---

## 13. Roadmap

| Φ | Τίτλος | Παραδοτέο |
|---|---|---|
| **0** ✅ | Έρευνα + αποφάσεις | **αυτό το ADR** — μηδέν γραμμές κώδικα |
| **1** ✅ | Μοντέλο + μηχανή | **ΕΚΛΕΙΣΕ 2026-08-03** — `TableEdgeKey` + `tableEdgeKey()` + `tableEdgeKeyAt()` + `edges` στο persisted + **4ο επίπεδο** + Α13 (διαγραφή άξονα) + **47 tests**. Λεπτομέρειες: **§16** |
| **2** ✅ | Πράξεις | **ΕΚΛΕΙΣΕ 2026-08-03** — μητρώο 13 εντολών + **ένας** applier + μαζικός εγγραφέας ακμών + Α14/Α15/Α16/Α17 + **38 tests** (6/6 μεταλλάξεις). Λεπτομέρειες: **§17** |
| **3** ✅ | Εικονίδια + dropdown | **ΕΚΛΕΙΣΕ 2026-08-03** — **ΕΝΑ** εικονίδιο παραγόμενο από το μητρώο (όχι 13 SVG) + i18n EL/EN + το dropdown στο toolbar + Α19/Α20/Α21 + **51 tests**. Λεπτομέρειες: **§18** |
| **4** ✅ | Διαδρομή κελιού | **ΕΚΛΕΙΣΕ 2026-08-03** (μαζί με τη Φ3, απόφαση ιδιοκτήτη) — PRIORITY 1.45 + θύρα + **Α22** (ο στόχος είναι το κελί, εκτός αν ανήκει στην επιλογή). Λεπτομέρειες: **§18** |
| **5** ✅ | **Το μολύβι του χρήστη + διαγώνιοι** | **ΕΚΛΕΙΣΕ 2026-08-03** — χρώμα · στυλ γραμμής · **πάχος** · **διπλή** στο mini toolbar + `TableCell.diagonal` + **Α23/Α24/Α25/Α26**. 🟢 **ΕΠΑΛΗΘΕΥΤΗΚΕ ΖΩΝΤΑΝΑ 2026-08-04 κατά 4/6** — το κρίσιμο βήμα 3 (§19.2) **πέρασε σε pixel**· **+4 tests** έκλεισαν το κενό της Α24 (**1565/1565 σε 80 suites**). Δες §19.8. Λεπτομέρειες: **§19** |
| **6** 🟡 | «Περισσότερα περιγράμματα» | **ΚΛΕΙΣΤΗ ΣΕ ΚΩΔΙΚΑ 2026-08-05, ΑΝΕΠΑΛΗΘΕΥΤΗ ΖΩΝΤΑΝΑ** — κέλυφος 6 καρτελών (ενεργή μόνο η «Περίγραμμα») + listbox **14 στυλ** + χρώμα + 3 υποδείγματα + **proxy widget 8 θέσεων** + draft με ΟΚ/Άκυρο. **30 tests** (18 UI + 12 γεωμετρία) + **81** της μηχανής. 🔴 **Καμία γραμμή δεν ζωγραφίστηκε σε καμβά.** Λεπτομέρειες: **§20** |
| **7** ⭐ | **Προέλευση + μη-modal ζωγραφική** | §10.2 + §10.3 — **εδώ ξεπερνάμε τους μεγάλους** |

Οι Φ1–Φ4 είναι το «υπάρχουν περιγράμματα». Οι Φ5–Φ6 το «είναι Excel-parity». Η Φ7 το
«ξεπερνά τους μεγάλους».

⚠️ **Η Φ5 ξεπέρασε ήδη το «Excel-parity»**: το Excel δεν εκθέτει καθόλου **πάχος** γραμμής
περιγράμματος (είναι μπλεγμένο στο πλέγμα των 13 «στυλ» του) και κρύβει χρώμα + στυλ σε
**modal**. Εδώ και τα τέσσερα ζουν στο ίδιο dropdown με τις εντολές (§19.5).

---

## 14. Ρίσκα

1. ✅ **ΛΥΘΗΚΕ (Φάση 1).** Το `types/table.ts` ήταν 427/500 και ο νέος τύπος θα το έσπαγε.
   Θεραπεία: `types/table-edges.ts` από την αρχή — **και κάτι που δεν είχε προβλεφθεί**: ο
   διαχωρισμός γεννούσε **κύκλο τύπων** (`table → table-edges → table`), επειδή η ακμή
   χρειάζεται `TableRowId` ενώ το μοντέλο χρειάζεται `TableEdgeEntry`. Λύση: τρίτο, κάτω
   στρώμα `types/table-ids.ts` ⇒ **αλυσίδα** `ids → ακμές → μοντέλο`, μηδέν κύκλος. Το
   `TableBorderSpec` μετακόμισε κι αυτό στο `table-edges.ts` (είναι κυριολεκτικά «το μολύβι
   μιας ακμής») και **επανεξάγεται** — καμία υπάρχουσα διαδρομή εισαγωγής δεν άλλαξε.
   Καθαρό αποτέλεσμα: το επικίνδυνο αρχείο **μίκρυνε** σε 420 γραμμές.
   ⚠️ Ο κύκλος θα ήταν μόνο σε επίπεδο τύπων, δηλαδή **θα δούλευε** — γι' αυτό ήταν
   επικίνδυνος: θα περνούσε αθόρυβα και θα γινόταν το προηγούμενο του επόμενου (ADR-746).
2. ✅ **ΑΠΟΦΑΣΙΣΤΗΚΕ (Α11) — πολιτική εισαγωγής σε διαφωνία.** Ξένο αρχείο (`.xlsx`/DXF)
   όπου δύο κελιά όντως δηλώνουν διαφορετικό μολύβι στην κοινή ακμή: το edge-keyed μοντέλο
   δεν το εκφράζει, άρα χρειάζεται **ντετερμινιστικός** κανόνας. Ισχύει ο κανόνας #3 του
   §3.2: **δεξί > αριστερό, κάτω > πάνω**.
   *Γιατί αυτός και όχι άλλος:* (α) είναι **επίσημα τεκμηριωμένος** στο ISO/IEC 29500 ως
   `useSingleBorderforContiguousCells`, δηλαδή δεν τον εφευρίσκουμε· (β) σε αυτόν
   **συνέκλινε ανεξάρτητα** η ομάδα του Handsontable, εμπειρικά, από τη μεριά της
   υλοποίησης· (γ) εξηγείται σε **μία πρόταση** — σε αντίθεση με τα 6 επίπεδα tie-break
   του default OOXML, όπου κριτήριο είναι η **φωτεινότητα `R+B+2G`**. Ένας κανόνας που δεν
   μπορείς να πεις σε μία πρόταση δεν είναι κανόνας· είναι ατύχημα.
   ⚠️ Ισχύει **αποκλειστικά στην εισαγωγή**. Μέσα στον Νέστωρ η διαφωνία είναι
   ανέκφραστη (§6.1), άρα ο κανόνας **δεν εκτελείται ποτέ** σε δικά μας δεδομένα.
3. ✅ **ΑΠΟΦΑΣΙΣΤΗΚΕ (Α12) — εξαγωγή DXF με εκπομπή edge→cell.** Κάθε ακμή γράφεται στα
   group codes **και των δύο** γειτονικών κελιών (η κάτω του πάνω = η πάνω του κάτω,
   ταυτόσημο spec). **Χωρίς απώλεια**, επειδή η πηγή είναι ήδη μοναδική ανά ακμή — καμία
   ερώτηση σύγκρουσης δεν τίθεται ποτέ κατά την εξαγωγή. Σήμερα άλλωστε ο πίνακας εξάγεται
   `decompose` σε `LINE` (`entity-export-coverage.ts:99`), οπότε **δεν μας περιορίζει
   καθόλου**· η εκπομπή αφορά τον μελλοντικό native writer της Φάσης Ε του ADR-739.
4. **Πολλαπλασιασμός ακμών σε μεγάλο πίνακα**: ένας 8×500 έχει ~8.500 ακμές. Ο χάρτης
   είναι **αραιός** (μόνο ρητές) και η ένωση γίνεται ήδη μία φορά στο memoized
   αποτέλεσμα — το μάθημα του ADR-735. Να μη γίνει ανά καρέ.
   ✅ **Φάση 1**: προστέθηκε φράγμα `model.edges.size === 0` **πριν** τη σύνθεση κλειδιού.
   Ο συνηθέστερος πίνακας δεν έχει καμία ρητή ακμή· χωρίς το φράγμα θα συνθέταμε ~8.500
   κλειδιά μόνο για να αστοχήσουν όλα. Δεν είναι ανά καρέ (η διάταξη απομνημονεύεται) —
   είναι όμως **ανά αλλαγή μοντέλου**, δηλαδή ανά πληκτρολόγηση.

---

## 15. Τι ΔΕΝ επιβεβαιώθηκε — ρητά (100% ειλικρίνεια)

1. ✅ **ΛΥΘΗΚΕ 2026-08-03 — στιγμιότυπο ελληνικού Excel (Giorgio).** Και τα 13 ονόματα
   επιβεβαιώθηκαν· ο πίνακας είναι στο **§8.1**. Η μαντεψιά της Φάσης 0 ήταν λάθος σε **ένα**
   σημείο, και ακριβώς εκεί που κανείς δεν θα το υποψιαζόταν: το #12 λέει «Επάνω και
   **πλατύ** κάτω περίγραμμα», όχι «παχύ». Η Microsoft χρησιμοποιεί **τρεις** λέξεις για το
   ίδιο *Thick*. 🔑 **Το μάθημα**: ο πράκτορας είχε μαντέψει το **συνεπές** — και η πραγματική
   διεπαφή δεν ήταν συνεπής. Μια μετάφραση «που βγάζει νόημα» είναι ακριβώς ο τύπος εικασίας
   που δεν φαίνεται ποτέ ως εικασία. Η **Α18** (§8.4) το έκλεισε: γράφουμε συνεπές «παχύ».
2. **Αν το Excel γράφει πράγματι το border στο γειτονικό κελί** μέσα στο `styles.xml`.
   Το κείμενο της Microsoft είναι διφορούμενο. Απαιτεί πείραμα: bottom border στο A1,
   άνοιγμα του `.xlsx` ως zip, έλεγχος αν το A2 απέκτησε top border.
3. **Δεν υπάρχει τεκμηριωμένος αλγόριθμος επίλυσης σύγκρουσης για SpreadsheetML.** Ο
   πλήρης αλγόριθμος του §3.2/#2 είναι **WordprocessingML**. Για το Excel υπάρχει *μόνο*
   η πρόταση «πιο πρόσφατο».
4. **Οι DXF group codes** (69/65/66/68, 279/275/276/278, 289/285/286/288, 91/92/177/178)
   προέρχονται από δευτερογενή ανάγνωση της σελίδας Autodesk. **Πριν χρησιμοποιηθεί
   οποιοσδήποτε σε writer, επαληθεύεται στο πρωτότυπο DXF Reference.** Δεν μας μπλοκάρει
   τώρα (ο writer δεν υπάρχει).
5. **Διαγώνιο περίγραμμα στο `ACAD_TABLE`**: δεν βρέθηκε ούτε επιβεβαίωση ούτε άρνηση.
   Με `decompose` βγαίνει ως σκέτη `LINE` — δουλεύει ούτως ή άλλως.
6. **Ο κανόνας σύγκρουσης του AutoCAD** δεν βρέθηκε σε κύρια πηγή. Το group 178
   («virtual edge») είναι **έμμεση ένδειξη**, όχι απόδειξη.
7. **ArchiCAD per-cell borders**: η τεκμηρίωση δείχνει per-item-type· το community forum
   επέστρεψε 403.
8. **Cinema 4D**: η απουσία feature είναι *απόδειξη μέσω αναζήτησης*, όχι εξαντλητικός
   έλεγχος τεκμηρίωσης Maxon.
9. **APG**: δεν υπάρχει **ρητό** κριτήριο «toolbar vs radiogroup για εικονίδια σε 2D
   διάταξη». Η σύσταση του §9.2 **συνθέτει** τη σελίδα Menu (που αποκλείει ρητά το menu)
   με τη σελίδα Radio Group.

---

## 16. ΦΑΣΗ 1 — τι υλοποιήθηκε πραγματικά (2026-08-03)

**Καθαρές συναρτήσεις. Μηδέν React, μηδέν canvas, μηδέν UI, μηδέν i18n.**

### 16.1 Τα αρχεία

| Αρχείο | Τι | Γρ. |
|---|---|---|
| `types/table-ids.ts` | **ΝΕΟ** — `TableColumnId` / `TableRowId` / `CellKey` (μετακόμισαν) | 44 |
| `types/table-edges.ts` | **ΝΕΟ** — `TableBorderSpec` (μετακόμισε) + ταυτότητα ακμής + `TableEdgeEntry` / `TableEdgeIndex` | 123 |
| `bim/table/table-edge-model.ts` | **ΝΕΟ** — `tableEdgeKey()` · `tableEdgeKeyAt()` · `buildTableEdgeIndex()` · `toPersistedTableEdges()` · `rebuildTableEdgesOnDelete()` · `sameBorderSpec()` | 373 |
| `types/table.ts` | `edges` σε `TableModel` (υποχρεωτικό) + `PersistedTableModel` (προαιρετικό)· επανεξαγωγές | 427→**420** |
| `bim/table/table-model-helpers.ts` | καλωδίωση: `createTableModel` / `resolveTableModel` / `toPersistedTableModel` | 457→472 |
| `bim/table/table-layout-borders.ts` | **4ο επίπεδο**· `horizontalSpec` παίρνει πλέον **και τη στήλη** | 173→219 |
| `bim/table/table-row-column-ops.ts` | Α13 στη διαγραφή γραμμής/στήλης | 397→416 |
| `bim/table/table-cell-order.ts` | εξαγωγή `CellOrderSource` (κοινή όψη αξόνων, αποφυγή κλώνου) | 107→112 |

Κανένα αρχείο >500 γραμμές· καμία συνάρτηση >40· μηδέν `any` / `as any` / `@ts-ignore`.

### 16.2 Η αλλαγή που δεν φαινόταν στο σχέδιο

Το `horizontalSpec(model, style, r)` υπολογιζόταν **μία φορά ανά γραμμή** και εφαρμοζόταν σε
όλο το πλάτος — σωστό μέχρι τώρα, γιατί **καμία** πηγή δεν μπορούσε να πει κάτι διαφορετικό
ανά στήλη (η γραμμή και η κλάση της μιλούν πάντα ολόπλατα). Η ρητή ακμή είναι η **πρώτη**
πηγή που μπορεί. Άρα η υπογραφή έγινε `(model, style, r, c)` και η κλήση μετακινήθηκε **μέσα**
στον βρόχο των στηλών: αυτή η μία γραμμή είναι που κάνει το «περίγραμμα σε μεμονωμένο κελί»
εκφράσιμο, και ταυτόχρονα κάνει τη μηχανή **συμμετρική** (η κατακόρυφη το έκανε ήδη έτσι).
Η ένωση συνεχόμενων τμημάτων, που μέχρι τώρα δεν είχε τίποτα να ενώσει στην οριζόντια,
αποκτά επιτέλους δουλειά — και συγκρίνει **τιμή**, όχι προέλευση (test: ρητή ακμή που
συμφωνεί με το κληρονομημένο μολύβι **δεν** σπάει το τμήμα).

### 16.3 Τα tests — 47 νέα, 0 παλινδρομήσεις

`bim/table/__tests__/table-edge-model.test.ts` (**35**) · `table-edge-borders.test.ts` (**12**).
Κάθε test τρέχει τον **πραγματικό** μηχανισμό: αληθινό `JSON.parse(JSON.stringify())`, τις
ζωντανές πράξεις `insertTableRow` / `deleteTableRow` / `deleteTableColumn`, και το πλήρες
`layoutTable` — ποτέ στημένο ενδιάμεσο.

Καλύπτονται ρητά: ταυτότητα (μη μεταθετική, προσανατολισμός μέρος του κλειδιού) · **η κάτω
ακμή του (r,c) ΕΙΝΑΙ η πάνω του (r+1,c)** · sentinel · JSON round-trip · «το κλειδί δεν
ταξιδεύει» · `persisted→runtime→persisted` ταυτοτικό · **πίνακας χωρίς ακμές δεν αποκτά καν
το πεδίο** · ντετερμινιστική σειρά · άκυρο σχήμα με ίχνος · **και τα 4 επίπεδα προτεραιότητας
χωριστά** · «Χωρίς περίγραμμα» που **μένει** σβηστό (Π3) · τα 4 τμήματα γύρω από ένα κελί ·
συγχώνευση που νικά τη ρητή ακμή (και η ίδια ακμή **έξω** από τη συγχώνευση που ζωγραφίζεται) ·
Α13 σε έξι σενάρια.

**Σουίτα πίνακα**: 544/544 · **όλα τα table-related**: 1113/1113 σε 61 suites ·
`types/__tests__` + capability anchors: 925/925 · `jscpd:diff`: καθαρό.

### 16.4 🔴 Μια υπάρχουσα πύλη έπιασε την αλλαγή — και είχε δίκιο

Το `types/__tests__/entity-json-roundtrip-coverage.test.ts` κοκκίνισε: ο γενικός ανιχνευτής
βρήκε **δεύτερο** μη-σειριοποιήσιμο μονοπάτι, `table.model.edges → Map`. Σωστά — το runtime
ευρετήριο ακμών είναι `Map`, ακριβώς όπως τα κελιά, και **δεν επιτρέπεται** να μπει ποτέ σε
οντότητα σκηνής.

Δύο διορθώσεις, καμία χαλάρωση: (α) ο ισχυρισμός ονομάζει πλέον **και τα δύο** ευρετήρια
χωριστά — μια σκέτη μέτρηση («ένα ένοχο μονοπάτι») θα έμενε πράσινη αν κάποτε το `edges`
γινόταν πάλι απλό αντικείμενο, δηλαδή ο φρουρός θα σιωπούσε ακριβώς όταν χανόταν κάτι·
(β) το fixture του **πραγματικού** πίνακα απέκτησε ρητές ακμές, ώστε η persisted μορφή τους
να περνά από την ίδια δοκιμασία με τα κελιά — αλλιώς ο φρουρός θα φύλαγε τη μισή πόρτα.

### 16.5 Τι ΔΕΝ έγινε (και ανήκει στις επόμενες φάσεις)

~~Οι 13 εντολές ως πράξεις (Φ2)~~ **έγινε, §17** · εικονίδια/dropdown/i18n (Φ3) · δεξί κλικ σε κελί (Φ4) ·
διαγώνιοι + `patternKey` (Φ5) · «Περισσότερα περιγράμματα» (Φ6) · προέλευση + μη-modal
ζωγραφική (Φ7). **Καμία γραμμή UI δεν γράφτηκε.** Το `TableFormatToolbar` δεν αγγίχτηκε.

⚠️ *(Ισχύε στο κλείσιμο της Φάσης 1.)* Παρέμενε ανοιχτό το §15/#1: τα ελληνικά ονόματα των 13
εντολών. **Έκλεισε στη Φάση 2** — §8.1 + Α18 (§8.4).

---

## 17. ΦΑΣΗ 2 — τι υλοποιήθηκε πραγματικά (2026-08-03)

**Καθαρές συναρτήσεις. Μηδέν React, μηδέν canvas, μηδέν UI, μηδέν i18n.**

### 17.1 Τα αρχεία

| Αρχείο | Τι | Γρ. |
|---|---|---|
| `bim/table/table-range-border-ops.ts` | **ΝΕΟ** — μητρώο 13 εντολών · `applyTableBorderCommand()` · `clearTableRangeBorders()` · `isTableBorderCommandAvailable()` · `tableRangeSideEdges()` | 356 |
| `bim/table/table-edge-model.ts` | **μαζικός εγγραφέας** `setTableEdges()` + `TableEdgePatchMap` + `HIDDEN_TABLE_EDGE` | 373→**465** |
| `bim/table/table-style-presets.ts` | το ιδιωτικό `NO_BORDER` δείχνει πλέον στο `HIDDEN_TABLE_EDGE` (N.0.2) | 232→**239** |

Κανένα αρχείο >500· καμία συνάρτηση >40· μηδέν `any` / `as any` / `@ts-ignore`· `jscpd:diff`
καθαρό· `eslint` καθαρό.

### 17.2 Δεδομένα + ένας applier — γιατί όχι 13 συναρτήσεις

Οι 13 είναι **συνδυασμοί έξι πλευρών × μολύβι**. Το μητρώο κρατά ανά εντολή μια λίστα
**σκελών** `{ πλευρές, τροποποιητής μολυβιού }` — και όχι «σύνολο πλευρών + ένα μολύβι»,
επειδή **τρεις** από τις 13 δίνουν διαφορετικό μολύβι σε διαφορετικές πλευρές («Επάνω και
**παχύ** κάτω»). Ένα μοντέλο ενός μολυβιού δεν θα μπορούσε καν να τις εκφράσει και θα
γεννούσε ακριβώς τις τρεις χειρόγραφες εξαιρέσεις που το μητρώο ήρθε να εξαλείψει.

🔑 **Οι έξι πλευρές δεν είναι νέο λεξιλόγιο**: ο τύπος είναι `keyof TableBorders` — **οι ίδιες**
έξι λέξεις που χρησιμοποιούν ήδη οι κλάσεις γραμμής (σημασιολογία AutoCAD). Γι' αυτό ακριβώς
«Όλα τα περιγράμματα» έχει νόημα: η εντολή του χρήστη μιλά τη γλώσσα του στυλ.

### 17.3 Ο μαζικός εγγραφέας — το αρχιτεκτονικό εμπόδιο

Μια εντολή «Όλα» σε περιοχή 20×10 αγγίζει **430 ακμές**. Εγγραφέας μιας ακμής θα έδινε
O(N²) **και** θα υποχρέωνε τον καλούντα σε **δική του** γνώση της σειράς — δηλαδή δεύτερη
ταξινόμηση ακμών, η «τέταρτη μηχανή πίνακα» του ADR-739 §15 με άλλο όνομα. Το
`setTableEdges(model, patches)` εφαρμόζει τα πάντα σε **ένα** πέρασμα, ξαναπερνά από το
**ιδιωτικό** `orderTableEdges` (η σειρά μένει μία) και επιστρέφει το **ίδιο** μοντέλο
by-reference όταν καμία τιμή δεν αλλάζει. Οι 13 εντολές είναι έτσι **παραγωγοί patch map**:
μηδέν γνώση σειριοποίησης.

Ο τύπος `TableBorderSpec | null` κωδικοποιεί την Α14 **στην υπογραφή**: μολύβι (ακόμη και
αόρατο) εναντίον διαγραφής. Χωρίς αυτή τη διάκριση, «σβήσε» και «ξέχνα ό,τι όρισα» θα ήταν
η ίδια πράξη.

### 17.4 Τα tests — 50 νέα, 0 παλινδρομήσεις

`table-range-border-ops.test.ts` (**42**) · `table-edge-model.test.ts` **35→43** (+8 για τον
εγγραφέα). Κάθε συμπεριφορικό test περνά από τη **ζωντανή** διαδρομή `resolveTableModel` +
`layoutTable` — την ίδια που τροφοδοτεί καμβά/PDF/DXF — και το round-trip από αληθινό
`JSON.parse(JSON.stringify())`.

**Σουίτα πίνακα: 1185/1185 σε 63 suites.**

#### 🔴 17.4.1 Η μέτρηση μετάλλαξης βρήκε τρύπα στο ίδιο μου το δίχτυ

Έξι σκόπιμες μεταλλάξεις στις εγγυήσεις. **Πέντε** έπεσαν αμέσως· **μία επέζησε**: με την
`insideH` να φτάνει ως το κάτω σύνορο (δηλαδή να **επικαλύπτει** την `bottom`), **όλα** τα
tests έμεναν πράσινα.

Η αιτία είναι διδακτική: η επικάλυψη γράφει δύο φορές το **ίδιο κλειδί** σε `Map`, οπότε ούτε
το πλήθος ούτε το σύνολο κλειδιών αλλάζει — **η επικάλυψη δεν αφήνει ίχνος στο αποτέλεσμα**.
Ένα test «το πλήθος ισούται με τον τύπο» ανιχνεύει *έλλειψη*, ποτέ *διπλοεγγραφή*.

Η θεραπεία δεν ήταν test, ήταν **API**: η γεωμετρία μιας πλευράς εκτέθηκε ως
`tableRangeSideEdges(axes, bounds, side)` — κάτι που **ούτως ή άλλως** χρειάζεται το proxy
preview της Φ6 και το ενεργό εικονίδιο της Φ3 — και η αναλλοίωτη «οι έξι πλευρές είναι ανά
δύο ξένες» ελέγχεται πλέον **άμεσα**, σε τέσσερα σχήματα περιοχής. Και οι δύο μεταλλάξεις
επικάλυψης (οριζόντια και κατακόρυφη) πέφτουν τώρα.

⚠️ Σήμερα **καμία** εντολή δεν δίνει διαφορετικό μολύβι σε `insideH` και `bottom`, άρα η
μετάλλαξη ήταν *μη παρατηρήσιμη* — γι' αυτό ακριβώς έπρεπε να πιαστεί τώρα: **η Φ6 θα την
κάνει παρατηρήσιμη**, και τότε το ελάττωμα θα φαινόταν ως «η μία εντολή έσβησε τη δουλειά
της άλλης», χωρίς κανένα ίχνος προς την αιτία.

### 17.5 Δύο SSoT που έλειπαν από το audit της Φάσης 0

1. **`HIDDEN_TABLE_EDGE`** — το `table-style-presets.ts` είχε ήδη ιδιωτικό
   `NO_BORDER = { visible:false, colorHex:'#000000', widthMm:0 }`. Η Φ2 χρειαζόταν **την ίδια
   ακριβώς** τιμή· δύο ταυτόσημα literals θα ήταν διπλότυπο **κάτω** από το κατώφλι του jscpd
   (N.18) — δηλαδή αόρατο στην πύλη. Κεντρικοποιήθηκε: με ένα αντικείμενο, το `sameBorderSpec`
   απαντά «ίδιο» για το αόρατο της κλάσης και του χρήστη, άρα η ένωση τμημάτων δεν σπάει.
2. **`nearestIsoLineweight`** — υπήρχε ήδη ολόκληρος κατάλογος ISO με **δικό του ratchet**.
   Το «παχύ» παράγεται από εκεί (§6.10) αντί να ταμπελωθεί.

### 17.6 Τι ΔΕΝ έγινε

Εικονίδια/dropdown/i18n (Φ3) · δεξί κλικ σε κελί (Φ4) · διαγώνιοι + `patternKey` (Φ5) ·
«Περισσότερα περιγράμματα» (Φ6) · προέλευση + μη-modal ζωγραφική (Φ7). **Καμία γραμμή UI.**

✅ **Το §15/#1 έκλεισε μέσα στη Φ2**: ο Giorgio έδωσε δύο στιγμιότυπα ελληνικού Excel και τα
13 ονόματα είναι πλέον μετρημένα (§8.1), μαζί με την ομαδοποίηση του μενού (πεδίο `group` στο
μητρώο) και τη σύμβαση των εικονιδίων (§8.2) — καταγεγραμμένα **όσο υπήρχε η απόδειξη**. Η
**Α18** (§8.4) έκλεισε κι αυτή: γράφουμε συνεπές «παχύ».

**Τίποτα δεν εκκρεμεί για τη Φάση 3.**

---

## 18. ΦΑΣΕΙΣ 3+4 — τι υλοποιήθηκε πραγματικά (2026-08-03)

**Οι δύο φάσεις έγιναν μαζί, με απόφαση του ιδιοκτήτη.** Ο λόγος γράφτηκε πριν ξεκινήσουν: η Φ3
είναι **μερικώς ανεπαλήθευτη** χωρίς τη Φ4, γιατί το toolbar ανοίγει μόνο από ζώνη δείκτη — άρα
θα παραδιδόταν dropdown που δεν μπορεί να δοκιμαστεί στην **κύρια** περίπτωση χρήσης
(περίγραμμα σε ένα κελί ή σε περιοχή 3×2).

### 18.1 Τα αρχεία

| Αρχείο | Τι | Γρ. |
|---|---|---|
| `ui/…/table-format-toolbar/TableBorderIcon.tsx` | **ΝΕΟ** — το **ένα** εικονίδιο, παραγόμενο από το μητρώο | 182 |
| `ui/…/table-format-toolbar/TableBorderMenu.tsx` | **ΝΕΟ** — το dropdown μέσα στο mini toolbar | 186 |
| `ui/…/table-format-toolbar/table-border-menu-items.ts` | **ΝΕΟ** — η **κοινή** γνώση των δύο υποδοχών | 72 |
| `ui/components/TableRangeContextMenu.tsx` | **ΝΕΟ** — δεξί κλικ σε κελιά (Φ4) | 146 |
| `ui/…/dxf-context-menu/use-anchored-hidden-trigger.ts` | **ΝΕΟ** — κοινή τοποθέτηση κρυφού trigger | 68 |
| `bim/table/table-border-pencil.ts` | **ΝΕΟ** — η Α20, καθαρή συνάρτηση | 109 |
| `ui/table-cell-editor/use-table-border-actions.ts` | **ΝΕΟ** — η **μία** διαδρομή εντολής→μοντέλο | 84 |
| `ui/table-cell-editor/use-table-range-menu.ts` | **ΝΕΟ** — ο κάτοχος + η **Α22** ως καθαρή συνάρτηση | 173 |
| `ui/table-cell-editor/table-range-menu-port.ts` | **ΝΕΟ** — η θύρα (PRIORITY 1.45) | 48 |
| `ui/table-cell-editor/use-live-table.ts` | **ΝΕΟ** — ο ζωντανός πίνακας, μία φορά | 33 |
| `bim/table/table-range-border-ops.ts` | +`hasExplicitTableRangeBorders` + **δύο εξαγωγές γεωμετρίας** | 390→**416** |
| `use-roving-toolbar.ts` · `TableFormatToolbar.tsx` · `TableHeaderContextMenu.tsx` · `use-table-header-menu.ts` · `use-table-model-commit.ts` · `useCanvasContextMenu.ts` · `useCanvasSectionUI.ts` · `CanvasSection*.tsx` · `table-cell-pointer-hit.ts` | σημεία ένωσης | — |

Κανένα αρχείο >500· καμία συνάρτηση >40· μηδέν `any`/`as any`/`@ts-ignore`· μηδέν inline styles·
μηδέν `title=`· `eslint` καθαρό.

### 18.2 🔑 Τα 13 εικονίδια **παράγονται** — και αυτό ελέγχεται

Δεκατρία χειρόγραφα SVG είναι δεκατρία αδέλφια-κλώνοι (CHECK 3.28). Το χειρότερο όμως δεν είναι
το διπλότυπο: ένα χειρόγραφο εικονίδιο μπορεί να **διαφωνήσει** με τη συμπεριφορά του κουμπιού
του, και **δεν υπάρχει τίποτα να συγκριθεί** — ένα λάθος βέλος σε bitmap είναι αόρατο σε κάθε
εργαλείο. Το Excel έχει ακριβώς αυτό.

Η κίνηση δεν ήταν «ένα component που ζωγραφίζει κατά περίπτωση». Ήταν να **εξαχθούν οι δύο
ιδιωτικές συναρτήσεις γεωμετρίας** της Φ2 (`tableBorderSideOrientation`,
`tableBorderSideCrossings`) και το εικονίδιο να τις καλεί **με όρια ένα πλέγμα 2×2** αντί για τον
πίνακα του χρήστη. Δηλαδή το εικονίδιο δεν *μιμείται* την εντολή — **εκτελεί την ίδια συνάρτηση**.

Το `table-border-icon.test.tsx` το ελέγχει ως **ιδιότητα πάνω και στις 13**: οι συμπαγείς γραμμές
που βγήκαν στο DOM ισούνται με τις ακμές που απαιτεί το μητρώο. Πιάνονται και οι δύο κατευθύνσεις
(λιγότερες γραμμές· περισσότερες — η χειρότερη, γιατί υπόσχεται ό,τι δεν θα γίνει).

⚠️ Δύο πράγματα βγήκαν **δωρεάν** από τη σημασιολογία, χωρίς ειδική περίπτωση:
- Το «Χωρίς περίγραμμα» δείχνει **σκέτο** διάστικτο πλέγμα, επειδή το αόρατο μολύβι δεν έχει τι
  να δείξει — που είναι ακριβώς το εικονίδιό του στο Excel.
- Το κλειδί React κάθε γραμμής είναι **μόνο** η θέση της, επειδή οι έξι πλευρές είναι **ανά δύο
  ξένες** — η αναλλοίωτη που η Φ2 έκανε άμεσα ελέγξιμη (§17.4.1). Διπλό κλειδί δεν είναι εκφράσιμο.

### 18.3 Α20 — το μολύβι παράγεται από το στυλ· και η μέτρηση που το επικύρωσε

Η Α15 είπε «παράμετρος, ποτέ store» και άφησε ρητά ανοιχτό **ποιος** τη δίνει. Η απάντηση:
**κανείς δεν την κρατά** — παράγεται από το ενεργό στυλ, τη στιγμή της εντολής.

*Γιατί όχι «μαύρο 0,25», που είναι η απάντηση του Excel:* το Excel δεν έχει στυλ σχεδίου. Σε
**σχέδιο** όμως, ένας πίνακας με αχνό γκρι πλέγμα 0,15 mm θα αποκτούσε μαύρες γραμμές που δεν
ταιριάζουν με καμία άλλη γραμμή του φύλλου — ο χρήστης ζήτησε **παρουσία** γραμμής και θα έπαιρνε
αλλαγή **εμφάνισης**. Είναι το `ByLayer` του AutoCAD / «By Category» του Revit, στο μολύβι.

*Γιατί η **εσωτερική** γραμμή και όχι το πλαίσιο:* το `'thick'` του μητρώου είναι **σχετικό** (η
επόμενη πένα ISO, λόγος 1:2). Με βάση το ήδη παχύ πλαίσιο, το «Παχιά εξωτερικά» θα έβγαινε
**διπλάσιο** — η κλίμακα θα έσπαγε στο πρώτο πάτημα.

🔑 **Η επικύρωση ήταν μέτρηση, όχι επιχείρημα**: στο `standard` preset η εσωτερική είναι 0,25 mm
και το πλαίσιο 0,50 mm. Το `thick` της εσωτερικής δίνει **ακριβώς** 0,50 — δηλαδή «Παχιά
εξωτερικά περιγράμματα» **αναπαράγει το πλαίσιο που ορίζει το ίδιο το στυλ**, χωρίς να το ξέρει.
Κλειδωμένο ως test.

Στυλ **χωρίς καμία ορατή γραμμή** (`detailSheet`, ADR-622: και οι έξι αόρατες) παίρνει το **μελάνι
του κειμένου** του και λεπτή πένα ISO — ένα σταθερό `#000000` εκεί θα ήταν εφεύρεση σε στυλ που
δηλώνει ρητά `#222222`.

### 18.4 Α19 και Α21 — δύο αποφάσεις για το τι **λέει** και τι **είναι** το χειριστήριο

**Α19 (Giorgio).** Υπήρχε ήδη «Επαναφορά στο στυλ» στο ίδιο toolbar (στυλ κειμένου άξονα) και η
Φ2 γέννησε δεύτερη πράξη με **ταυτόσημο νόημα** για τα περιγράμματα. Δύο κουμπιά με το ίδιο
κείμενο σε απόσταση εκατοστών είναι ελάττωμα που **κανένα test δεν βλέπει και κάθε χρήστης
βλέπει**. Λύση: κάθε ετικέτα ονομάζει το **αντικείμενό** της — «Επαναφορά μορφοποίησης» εναντίον
«Επαναφορά περιγραμμάτων» (`resetToStyle` → `resetFormatting`). Κλειδώθηκε με test που απαιτεί
**μηδέν ταυτόσημα ορατά κείμενα** σε ολόκληρο το toolbar.

**Α21.** Το §9.2 αποκλείει το `role="menu"` — αλλά για **άλλο** χειριστήριο: το proxy preview της
Φ6, όπου ο χρήστης εναλλάσσει την *κατάσταση* κάθε πλευράς. Τα 13 δεν είναι καταστάσεις: είναι
**εντολές που εφαρμόζουν και κλείνουν**. Το `aria-pressed` σε εντολή χωρίς κατάσταση θα ανακοίνωνε
μονίμως «μη πατημένο» — ψέμα σε κάθε ανάγνωση. Η **τρέχουσα** κατάσταση ακμής είναι η ορατή
προέλευση της Φ7 (§10.2)· τότε το πάνελ θα αποκτήσει και δεύτερη ανάγνωση.

⚠️ Η διάταξη είναι **κατακόρυφη λίστα με ορατό κείμενο**, όχι πλέγμα εικονιδίων: είναι η μετρημένη
διάταξη του Excel (§8.1) και, ως παρενέργεια, το πάνελ δεν χρειάζεται tooltip καθόλου — το όνομα
της εντολής **είναι** το περιεχόμενο του κουμπιού.

### 18.5 Α22 — δεξί κλικ σε κελί: η σημασιολογία του Excel χωρίς την παρενέργειά του

```
επιλογή B2:D4, δεξί κλικ στο C3  ⇒  στόχος B2:D4
επιλογή B2:D4, δεξί κλικ στο E5  ⇒  στόχος E5
καμία επιλογή,  δεξί κλικ στο E5  ⇒  στόχος E5
```
Το Excel το συμπεραίνει **μετακινώντας** την επιλογή στο κελί που πατήθηκε. Εδώ το δεξί κλικ δεν
αγγίζει ποτέ την επιλογή (ADR-739 §27.14), οπότε ο ίδιος κανόνας βγαίνει από την **ερώτηση** αντί
από τη μεταβολή: ο χρήστης βλέπει την επιλογή του ανέπαφη και ξέρει τι θα βαφτεί επειδή ο τίτλος
του μενού το γράφει (`C3` / `B2:D4`).

⚠️ Η εναλλακτική «πάντα η τρέχουσα επιλογή» απορρίφθηκε ως **ψέμα**: δεξί κλικ στο E5 θα έβαφε το
B2:D4, μακριά από τον δείκτη, χωρίς καμία ένδειξη.

🔑 **Δώρο από τον ΕΝΑ δρόμο**: το μεμονωμένο κελί περνά από το `resolveTableSelectionBounds` με
είδος `'range'`, δηλαδή **κουμπώνει σε ολόκληρη συγχώνευση** (§26.5 του ADR-739) χωρίς μία γραμμή
ειδικής λογικής εδώ. Δεξί κλικ σε συγχωνευμένο κελί δίνει περίγραμμα γύρω από **όλη** τη συγχώνευση.

### 18.6 🔴 Το CHECK 3.28 έπιασε **δύο** διπλότυπα που είχα ήδη γράψει

Δεν είναι υποσημείωση — είναι ο λόγος που ο N.18 απαιτεί `jscpd:diff` **πριν** πει κανείς «έτοιμο».
Και τα δύο ήταν το ίδιο σχήμα: «ο δεύτερος καταναλωτής γεννιέται μέσα στο ίδιο commit».

1. **Η τοποθέτηση του κρυφού trigger** (31 γρ.), αντιγραμμένη από το μενού ζωνών στο νέο μενού
   περιγραμμάτων. Εξήχθη στο `use-anchored-hidden-trigger.ts` — **μαζί με το σκεπτικό της**: η
   ζωντανή μέτρηση του σφάλματος «το μενού άνοιξε στη γωνία του καμβά» ζούσε ως σχόλιο σε ένα από
   τα δύο αντίγραφα. Δύο αντίγραφα σημαίνουν ότι το σφάλμα μπορεί να ξαναγεννηθεί στο ένα, με όλη
   την εξήγηση γραμμένη στο άλλο.
2. **Η ακολουθία «ζωντανό μοντέλο → μετασχηματισμός → φύλακας ταυτότητας → commit»** (10 γρ.),
   κοινή σε μορφοποίηση άξονα και εντολές περιγράμματος. Έγινε `useLiveTableMutation` δίπλα στο
   `useTableModelCommit` — δηλαδή **ακριβώς το ίδιο σχήμα** που είχε γεννήσει το `useTableModelCommit`
   στη Φ.Δ, ένα επίπεδο ψηλότερα.

Κεντρικοποιήθηκε επίσης το `useLiveTable` (3 γραμμές × 2 καταναλωτές, **κάτω** από το κατώφλι του
jscpd — δηλαδή διπλότυπο που **καμία πύλη δεν βλέπει**, το σχήμα που περιγράφει το §17.5).

### 18.7 Τα tests — 51 νέα, 0 παλινδρομήσεις

| Αρχείο | Tests | Τι κλειδώνει |
|---|---|---|
| `table-border-icon.test.tsx` | **20** | το εικονίδιο **δεν μπορεί** να διαφωνήσει με την εντολή (ιδιότητα ×13) |
| `table-border-menu-items.test.ts` | **23** | ομάδες από το `group` · οι 2 διπλές δεν προσφέρονται · **i18n EL+EN** |
| `table-border-menu.test.tsx` | **12** | APG του dropdown: `aria-expanded`, ορατό κείμενο, `Escape` ενός επιπέδου |
| `table-border-pencil.test.ts` | **6** | Α20 + η μέτρηση «παχύ = πλαίσιο του στυλ» |
| `table-range-menu-target.test.ts` | **10** | **Α22** σε 6 σχήματα + η ετικέτα |
| `table-range-border-ops.test.ts` | **+6** | `hasExplicitTableRangeBorders` — και ότι **δεν** βλέπει άλλη περιοχή |

**Σουίτα πίνακα: 1367/1367 σε 72 suites** (από 1281/66).

#### 🔴 18.7.1 Το i18n test κλείνει κενό που **η CHECK 3.8 δεν μπορεί** να κλείσει

Τα ονόματα επιλύονται με **δυναμικό** κλειδί (``t(`table.borders.commands.${id}`)``), γιατί η
ταυτότητα **είναι** ήδη το κλειδί — χειρόγραφος πίνακας 13 γραμμών θα ήταν δεύτερη δήλωση της
ίδιας γνώσης. Η τιμή: η CHECK 3.8 ψάχνει `t('literal')` και **δεν βλέπει τίποτα εδώ** — το ίδιο
σχήμα «0 = κανείς δεν κοίταξε» που περιγράφει ο N.11 για τα ωμά ελληνικά σε JSX.

Το κενό κλείνει με τον μόνο τρόπο που κλείνει πραγματικά: το test διαβάζει τα **αληθινά** locale
JSON και το **αληθινό** μητρώο, και απαιτεί να συμφωνούν **και στις δύο** γλώσσες, **και για τα 13**
(όχι μόνο τα 11 ορατά — η Φ5 ενεργοποιεί τα υπόλοιπα δύο).

### 18.8 Τι ΔΕΝ έγινε — και μία παρατήρηση που δεν διορθώθηκε

Διαγώνιοι + `patternKey` (Φ5) · «Περισσότερα περιγράμματα» (Φ6) · προέλευση + μη-modal ζωγραφική
(Φ7). Καμία επιλογή χρώματος/πάχους από τον χρήστη: το μολύβι είναι παράγωγο (Α20) μέχρι τη Φ6.

⚠️ **Παρατήρηση, όχι διόρθωση**: το `PersistedTableModel` **δεν** είναι assignable σε `TableModel`
(τα κελιά είναι ακολουθία εναντίον `Map`), και ο υπάρχων `table-pointer-axis-selection.ts:118`
περνά `entity.model` σε συνάρτηση που δηλώνει `TableModel` — σφάλμα τύπου που ζει στο baseline του
**CHECK 3.29** (το subapp είναι εκτός root `tsconfig`, μόνο το CI το βλέπει). **Δεν αντιγράφηκε**:
ο νέος κώδικας περνά από `resolveTableModel` (cached σε `WeakMap`). Η διόρθωση του υπάρχοντος
καλούντος είναι εκτός σκοπού αυτής της φάσης.

### 18.9 ✅ Ζωντανή επαλήθευση στην οθόνη (2026-08-03)

Και οι δύο διαδρομές μετρήθηκαν στον **browser**, όχι σε jsdom — απαραίτητο, γιατί το ίδιο το
`table-format-toolbar.module.css` τεκμηριώνει σφάλμα που ήταν *πράσινο στα tests και νεκρό στον
browser* (`pointer-events` του modal Radix· σε jsdom δεν υπάρχει hit-testing, άρα κάθε
`fireEvent.click` πετυχαίνει).

| Τι | Αποτέλεσμα |
|---|---|
| **Φ4** — δεξί κλικ σε κελί | ✅ μενού «Περιγράμματα **B4**» · PRIORITY 1.45 δεν το άρπαξε το μενού οντότητας |
| **Α22** — κλικ έξω από την επιλογή | ✅ στόχος **B4** (το κελί), όχι το A3 της επιλογής |
| **Φ3** — dropdown στο mini toolbar | ✅ 11 εντολές + επαναφορά · `aria-expanded` · 3 διαχωριστικά |
| Εικονίδια (μετρημένα **από το DOM**) | ✅ και τα 11 συμφωνούν με το μητρώο· 6 διάστικτες γραμμές πλέγματος παντού |
| «Χωρίς περίγραμμα» | ✅ **καμία** συμπαγής γραμμή — σκέτο πλέγμα |
| «Εξωτερικά» vs «Όλα» | ✅ 4 έναντι 6 γραμμών — η Α10 **ορατή** στο εικονίδιο |
| #12 «Επάνω και παχύ κάτω» | ✅ `H0@1.1 + H2@2.2` — **δύο διαφορετικά μολύβια** στο ίδιο εικονίδιο |

🔑 **Η Α20 επαληθεύτηκε στα δεδομένα, όχι στο μάτι.** «Όλα τα περιγράμματα» σε **ένα** κελί έγραψε
ακριβώς τέσσερις ακμές — `H:r3:c1 · V:r3:c1 · V:r3:c2 · H:r4:c1` — με μολύβι **`#666666@0.25`**,
δηλαδή **ακριβώς** τη γραμμή πλέγματος που ορίζει το `standard` στυλ. Καμία εσωτερική ακμή σε
περιοχή 1×1, όπως ορίζει το §7.

🔑 **Και η κλίμακα ISO**: «Παχιά εξωτερικά» στο ίδιο στυλ έγραψε **`@0.5`** — η επόμενη πένα, και
ταυτόχρονα ακριβώς το πλαίσιο που δηλώνει το στυλ (§18.3). Ορατό στην οθόνη ως παχύτερο περίγραμμα.

🔑 **Το sentinel `$end` της Φ1 δούλεψε ζωντανά**: περίγραμμα στο τελευταίο κελί έγραψε
`V:r4:$end` και `H:$end:c2` — οι ακμές που δεν ανήκουν σε κανένα κελί.

⚠️ **Διόρθωση μέτρησης, καταγεγραμμένη επίτηδες.** Ενδιάμεσα αναφέρθηκε ως ελάττωμα ότι η
«Επαναφορά περιγραμμάτων» έμενε ενεργή σε ανέγγιχτο κελί. **Ήταν σφάλμα της μέτρησης, όχι του
κώδικα**: το δέντρο είχε συσσωρευμένες ακμές από προηγούμενες δοκιμές. Με probe στο ζωντανό
`canReset` επαληθεύτηκε και στις δύο κατευθύνσεις — `result:false` ⇒ `aria-disabled="true"` +
`data-disabled`, `result:true` ⇒ ενεργό. **Χωρίς probe, το συμπέρασμα θα είχε μείνει λάθος.**

⚠️ **Τι ΔΕΝ δοκιμάστηκε ζωντανά**: πλοήγηση του πάνελ με **πληκτρολόγιο**. Είναι γνωστά
απρόσιτο όσο το μενού από κάτω είναι modal — το `FocusScope` του Radix επαναφέρει κάθε εστίαση
στο `role="menu"` (τεκμηριωμένο στο `useAriaHiddenGuard`, ADR-739 §28.12). Η διαδρομή της Φ4 δεν
έχει αυτόν τον περιορισμό αλλά δεν μετρήθηκε.

---

---

## 19. ΦΑΣΗ 5 — τι υλοποιήθηκε πραγματικά (2026-08-03)

> **Εντολή Giorgio**: *«Θέλω στο mini toolbar να μπορώ να επιλέγω χρώμα περιγράμματος κελιών
> και τύπο γραμμής κελιών.»* — και, στην ερώτηση εύρους: **χρώμα + τύπος + πάχος + διαγώνιοι,
> όλα μαζί**.

### 19.1 Α23 — η Α20 δεν καταργήθηκε· έγινε το «Αυτόματο», **ανά πεδίο**

Η φαινομενική ένταση («η Α20 λέει *παράγεται*, ο χρήστης θέλει να το *ορίζει*») είναι **λάθος
διχοτόμηση**: αυτό που κρατιέται δεν είναι το μολύβι, είναι η **επιλογή**.

```
μολύβι.χρώμα  = επιλογή.χρώμα  ?? Α20(στυλ).χρώμα
μολύβι.πάχος  = επιλογή.πάχος  ?? Α20(στυλ).πάχος     ← ανεξάρτητα το ένα από το άλλο
μολύβι.μοτίβο = επιλογή.μοτίβο ?? Α20(στυλ).μοτίβο
```

Κάθε πεδίο ανεξάρτητα προαιρετικό. Ένα μολύβι που κρατούσε **ολόκληρη** την προηγούμενη
κατάσταση θα πάγωνε και τα τέσσερα με το πρώτο κλικ σε ένα από αυτά — και ο πίνακας θα έπαυε
**σιωπηλά** να ακολουθεί το στυλ του σχεδίου, που είναι ακριβώς ό,τι η Α20 υπάρχει για να
αποτρέψει. Είναι το `ByLayer` του AutoCAD και το «Αυτόματο» του Excel, εφαρμοσμένο **ανά
ιδιότητα** και όχι «όλα ή τίποτα».

**Πού ζει η επιλογή**: `state/table-border-pencil-store.ts` — module store, persisted, μηδέν
React state, ίδιο σχήμα με το χρώμα κειμένου του ADR-739. **Κρατά το ΟΝΟΜΑ** του τύπου γραμμής
(`Dashed`), όχι το μοτίβο (`[12.7, -6.35]`): το πρώτο είναι η **απόφαση**, το δεύτερο η
σημερινή της τιμή — αν ο χρήστης επεξεργαστεί τον τύπο (ADR-642 editor) ή φορτώσει άλλο `.lin`,
η επιλογή οφείλει να δείχνει στο **νέο** μοτίβο.

**Ένα** κλειδί `localStorage` με JSON, σε αντίθεση με τα δύο χωριστά του χρώματος κειμένου /
γεμίσματος — και για τον **ίδιο** λόγο που εκείνα χωρίστηκαν: εκεί είναι δύο **χειριστήρια** που
γράφονται από διαφορετικές χειρονομίες· εδώ τα τέσσερα πεδία είναι **ένα εργαλείο**, που
διαβάζεται και γράφεται μαζί σε κάθε εντολή. Και σε αλλοίωση, η ασφαλής κατάσταση είναι
**ολόκληρο** το «Αυτόματο».

### 19.2 🔴 Ένα λανθάνον ελάττωμα που έγινε ενεργό τη στιγμή που δόθηκε το χειριστήριο

Ο ζωγράφος του πλέγματος μετέτρεπε μόνος του `dashMm → px` με **σκέτο πολλαπλασιασμό**:

```ts
ctx.setLineDash(segment.spec.dashMm.map((d) => d * rc.pxPerMm));   // ← ήταν
```

Ήταν **αόρατο**, με τον ακριβή τρόπο που ο N.11/N.12 ονομάζουν «0 = κανείς δεν κοίταξε»:
**κανένα** preset δεν παρήγαγε ποτέ `dashMm` (μετρημένο — μηδέν παραγωγοί σε όλο το δέντρο),
άρα η γραμμή δεν εκτελέστηκε ούτε μία φορά σε πραγματικά δεδομένα.

Μόλις ο χρήστης απέκτησε επιλογέα στυλ γραμμής, το μοτίβο άρχισε να έρχεται από τον κατάλογο —
όπου **τα κενά είναι αρνητικά** (`[12.7, -6.35]`, σύμβαση DXF/`.lin`). Το `setLineDash` με
έστω ένα αρνητικό μήκος **αγνοεί ολόκληρη την κλήση**: η «διακεκομμένη» θα ζωγραφιζόταν
συμπαγής, **χωρίς σφάλμα πουθενά**, και ο χρήστης θα έβλεπε την επιλογή του να μην κάνει τίποτα.

Θεραπεία: `dashMmToScreenPx` — ο **ένας** μεταφραστής του έργου (διπλώνει τα αρνητικά, ανυψώνει
τις κουκκίδες), ο ίδιος που χρησιμοποιούν ο `DxfRenderer` και τα thumbnails του ribbon.

⚠️ **Ο καταγραφέας των tests έκρυβε το ελάττωμα**: το ψεύτικο `ctx` είχε
`setLineDash: () => undefined`. Ένα stub που **καταπίνει** δεν είναι ουδέτερο — είναι πράσινο
και για τις δύο υλοποιήσεις. Τώρα καταγράφει, και τρία tests το καρφώνουν.

### 19.3 Α24 — η διπλή γραμμή **αποσυντίθεται**, δεν ζωγραφίζεται

`TableBorderSpec.doubleGapMm?: number` = απόσταση **κέντρου προς κέντρο** σε sheet-mm. Το
`buildTableBorders` βγάζει **δύο** τμήματα, μετατοπισμένα κατά ±μισή απόσταση στην κάθετη, και
**χωρίς** το πεδίο.

🔑 **Γιατί στη διάταξη και όχι στον ζωγράφο**: ο ζωγράφος είναι **τέσσερις** ζωγράφοι. Μια
διπλή υλοποιημένη στον καμβά θα ήταν μία γραμμή στην οθόνη και **καμία** στην εξαγωγή —
ακριβώς η ασυμφωνία που ο ADR-739 §3 υπάρχει για να αποκλείσει, και που η **ίδια** διαδρομή
πλήρωσε ήδη μία φορά με το γερμένο κείμενο.

Η **κάθετη** έρχεται ως παράμετρος από τον παραγωγό και δεν μαντεύεται από τη γεωμετρία («ίδιο
`y` ⇒ οριζόντιο»): θα ήταν δεύτερη πηγή για τον προσανατολισμό δίπλα στην
`tableBorderSideOrientation`, και θα σιωπούσε σε τμήμα μηδενικού μήκους.

**Η απόσταση είναι `3 × πένα`**, όχι απόλυτο χιλιοστό. Με σταθερή απόσταση, μια διπλή με πένα
0,13 mm και μια με 0,70 mm θα ήταν **η ίδια**: στη μία οι γραμμές θα κολλούσαν, στην άλλη θα
φαίνονταν δύο ξένες. Το `3` δίνει κενό `2 × πένα` (το ζεύγος διαβάζεται ως **ένα** στοιχείο)
και συνολικό αποτύπωμα `4 × πένα` — δηλαδή **μία βαθμίδα πάνω από την «παχιά»**, που το ISO
128-20 ορίζει ως `2 ×`. Αυτό είναι ακριβώς ο ρόλος της στη λογιστική σύμβαση που αντιγράφουν
οι εντολές έμφασης του Excel: παχιά κάτω γραμμή = μερικό σύνολο, **διπλή** = γενικό σύνολο.

**Η Α17 έκλεισε ρητά**, όπως ζητούσε («όλα ή τίποτα, **με ίχνος**»):

- `isTableBorderCommandAvailable` **διαγράφηκε** — δεν έμεινε επιστρέφοντας μονίμως `true`.
  Μια συνάρτηση που δεν μπορεί δομικά να πει «όχι» δεν είναι φύλακας· είναι ψέμα που ο επόμενος
  θα πάρει στα σοβαρά, και νεκρός κώδικας για το CHECK 3.22.
- Το μενού πέρασε από **11 σε 13**. Το τεστ που απαιτούσε την **απουσία** των δύο διπλών
  **αντιστράφηκε** ρητά, με το σκεπτικό γραμμένο μέσα του.
- 🔑 **Τα διαχωριστικά επιβεβαίωσαν τον σχεδιασμό**: με 11 εντολές το δεύτερο έπεφτε στη θέση
  **7**, με 13 πέφτει στην **8** — και **καμία γραμμή κώδικα δεν άλλαξε**, γιατί προκύπτουν από
  το `group` του μητρώου. Μια χειρόγραφη λίστα θέσεων («μετά την 4η και την 8η», όπως λέει το
  §8.1) θα ήταν λάθος τη μία από τις δύο φορές, σιωπηλά.
- **Μηδέν νέα κλειδιά i18n**: η Φ3 είχε καταγράψει και τα 13 ονόματα «όσο υπήρχε η απόδειξη».

### 19.4 Α25 — οι διαγώνιοι, με δικό τους μητρώο

Το §6.3 το είχε πει· η Φ5 το **εκτέλεσε**: `bim/table/table-cell-diagonal-ops.ts`, αδελφό
αρχείο του `table-range-border-ops.ts` και ποτέ επέκτασή του.

```
ακμή πλέγματος  →  μοιράζεται με τον γείτονα  →  χάρτης ακμών, με ταυτότητα
διαγώνιος       →  ανήκει σε ΕΝΑ κελί, πάντα  →  ζει στο κελί, χωρίς ταυτότητα
```

Στο ίδιο μητρώο θα χρειαζόταν ψεύτικο **έβδομο «side»** που κανένα σκέλος δεν μπορεί να
χειριστεί — δηλαδή μια εξαίρεση σε κάθε `switch`.

**Τέσσερις** εντολές (↘ · ↗ · σταυρός · καμία) και όχι δύο διακόπτες: το πάνελ είναι
`role="menu"` με **εντολές** (Α21) και κάθε πάτημα εκτελείται **αμέσως**. Το Excel εκθέτει δύο
toggles επειδή ζει σε modal με «ΟΚ»· εδώ ο σταυρός θα απαιτούσε δύο ανοίγματα μενού και η
αφαίρεση θα ήταν αόρατη.

**Τι μοιράζεται σκόπιμα**: το **μολύβι** (ίδια `resolveTableBorderPencil` — ο χρήστης έχει ένα
μολύβι στο χέρι) και η **γεωμετρία** (`tableDiagonalCorners`: την ίδια συνάρτηση εκτελεί και το
εικονίδιο του μενού με άλλα όρια — άρα «το εικονίδιο δείχνει ↘ και η εντολή ζωγραφίζει ↗» δεν
είναι απίθανο, είναι **μη εκφράσιμο**).

**Δύο πράγματα που χρειάστηκαν και δεν είχαν προβλεφθεί:**

1. **Μαζικός γραφέας κελιών** (`writePersistedCells`). Ο μονός γραφέας κάνει `findIndex` **και**
   `slice()` ολόκληρου του `cells` **ανά κελί**: μια εντολή διαγωνίου σε επιλογή 500 × 8 είναι
   ~16 εκατομμύρια συγκρίσεις και άλλα τόσα αντίγραφα — **ακριβώς** το εμπόδιο που ανάγκασε τη
   Φ2 να γράψει τον μαζικό εγγραφέα ακμών (§17.3). Ίδια απάντηση: ένα πέρασμα, μία ταξινόμηση,
   μία συγχώνευση.
2. **Οι διαγώνιοι ταξιδεύουν στο `TableCellLayout`, όχι στο `TableLayout.borders`.** Δύο λόγοι,
   και ο δεύτερος είναι ελάττωμα που αποφεύχθηκε: (α) τα κελιά είναι ήδη ευρετηριασμένα και
   κόβονται δυαδικά· (β) 🔴 **ο ταξινομητής θα την έλεγε κατακόρυφη** — το `tableRenderIndex`
   χωρίζει με κριτήριο `a.y === b.y`, και μια διαγώνιος περνά τον δεύτερο κλάδο. Θα
   **ζωγραφιζόταν σωστά**, άρα κανένα test εικόνας δεν θα το έβλεπε, ενώ η αποκοπή θα είχε
   σπάσει σιωπηλά.

### 19.5 Πού ξεπερνάμε τους μεγάλους — μετρημένο

| | Χρώμα | Στυλ γραμμής | **Πάχος** | **Διπλή** | Πού |
|---|---|---|---|---|---|
| **Excel** | ✅ | ✅ | ❌ *(μπλεγμένο στα 13 «στυλ»)* | ✅ *(ένα από τα 13)* | **modal** `Ctrl+1` |
| **AutoCAD** | ✅ | ✅ | ✅ | ✅ + Spacing | **modal** *Cell Border Properties* |
| **Νέστωρ** | ✅ | ✅ | ✅ | ✅ + απόσταση παράγωγη | **στο ίδιο dropdown με τις εντολές** |

Η **θέση** είναι του Excel (το dropdown του έχει ήδη «Χρώμα γραμμής» / «Στυλ γραμμής» στο
τέλος)· το **περιεχόμενο** είναι του AutoCAD. Δεν εφευρέθηκε τρίτη διάταξη — πάρθηκε από τον
καθένα ό,τι έχει λύσει.

🔑 **Και ένα σημείο όπου η προεπισκόπηση απαντά ερώτηση που κανείς τους δεν απαντά**: κάθε
γραμμή της ζώνης δείχνει το **επιλυμένο** μολύβι — δηλαδή περνά από την ίδια
`resolveTableBorderPencil` που θα εκτελέσει η εντολή. Όποιο πεδίο λέει «Αυτόματο» δείχνει την
τιμή του **στυλ**, όχι κενό. Το Excel δείχνει την τρέχουσα τιμή μόνο **μέσα** στον modal,
δηλαδή μόνο αφού έχεις ήδη χάσει το συμφραζόμενο.

**Καμία λίστα δεν γράφτηκε**: τύποι γραμμής ← `LinetypeRegistry` (ISO 8 + ό,τι εισήχθη από
`.lin`/DXF, **μείον** το `ByLayer` — η κληρονομιά λέγεται ήδη «Αυτόματο», Α19)· πάχη ←
`LINEWEIGHT_CONCRETE_MM_VALUES` (οι 23 θετικές πένες ISO)· προεπισκόπηση μοτίβου ←
`buildLinetypeThumbnailFromPattern`, το **ίδιο** SSoT με τον renderer· παλέτα ←
`TableColorSwatchGrid` (το πλέγμα 13×6 ACI του ADR-739 Φ.Ε — **δεν** γράφτηκε δεύτερος
επιλογέας χρώματος, §19.7).

⚠️ **Ο πειρασμός που απορρίφθηκε**: «οι 6 συνηθισμένες πένες» και «οι 4 συνηθισμένοι τύποι»,
γιατί 23 + 9 φαίνονται πολλά για mini toolbar. Θα ήταν **δεύτερη κλίμακα**: ο ίδιος χρήστης που
διαλέγει 0,18 mm για μια γραμμή του σχεδίου δεν θα μπορούσε να τη δώσει σε περίγραμμα πίνακα,
χωρίς να του το λέει τίποτα. Το μέγεθος της λίστας είναι πρόβλημα **παρουσίασης** (κύλιση).

### 19.6 Α26 — το μολύβι ρυθμίζεται σε ένα σημείο, οι εντολές προσφέρονται από δύο

Η ζώνη σχεδίασης ζει **μόνο** στη γραμμή εργαλείων. Το δεξί κλικ σε επιλογή κελιών (Φ4) πήρε
τις **4 διαγώνιες εντολές** — γιατί είναι εντολές, και το `table-border-menu-items.ts` υπάρχει
ακριβώς για να μη δείχνει η μία υποδοχή κάτι που η άλλη δεν δείχνει — αλλά **όχι** το μολύβι.

Το μολύβι είναι **εργαλείο**, όχι εντολή· δύο σημεία ρύθμισής του θα ήταν δύο απαντήσεις στο
«με τι γράφω τώρα». Το ίδιο κάνουν AutoCAD και Excel: η πένα ρυθμίζεται μία φορά, εφαρμόζεται
από παντού.

### 19.7 🔴 Ο άλλος πράκτορας έγραφε στα ίδια αρχεία — τι έγινε

Το working tree μοιράζεται. Μέσα στη συνεδρία, ο άλλος πράκτορας **μετονόμασε** το
`TableTextColorMenu` σε `TableAxisColorMenu` (γενίκευση κειμένου + γεμίσματος με
`role: 'ink' | 'fill'`) και το store σε `table-toolbar-color-store` — **ενώ σχεδίαζα να
γενικεύσω το ίδιο αρχείο**.

**Δεν το άγγιξα.** Αντ' αυτού καταναλώνεται το `TableColorSwatchGrid` — το **ήδη εξαγμένο**
κοινό πλέγμα 13×6, που είναι και το ακριβό κομμάτι (78 δείγματα + roving + readout). Το
`jscpd:diff` σε **13** αρχεία βγήκε **καθαρό**: κανένα sibling clone.

🔑 Το μάθημα δεν είναι «περίμενε». Είναι ότι η **σωστή μονάδα κοινοποίησης δεν ήταν το μενού,
ήταν το πλέγμα** — και εκείνο είχε ήδη εξαχθεί από τον άλλον. Ένα δεύτερο «γενικευμένο μενού
χρώματος» θα ήταν ο τρίτος τρόπος να ρωτηθεί η ίδια ερώτηση.

Δύο ακόμη ρητές μετακινήσεις:

- `TableHeaderContextMenu.tsx` ήταν **498/500**. Τα πέντε border props έγιναν **ένα**
  (`resolveBorderMenu`, με τον τύπο του πάνελ ως συμβόλαιο) ⇒ **483** — δηλαδή η Φ5 πρόσθεσε
  τρεις ικανότητες και το αρχείο **μίκρυνε**.
- `stamp-table-layout.ts` πέρασε στις **520** από τα σχόλια του §19.2. Το πλέγμα εξήχθη σε
  `stamp-table-borders.ts` ⇒ **474**. Η τομή δεν είναι αυθαίρετη: το πλέγμα είναι το **μόνο**
  μέρος της ζωγραφικής πίνακα που ρωτά τον κατάλογο τύπων γραμμής.

### 19.8 ✅ Η ζωντανή επαλήθευση — τι μετρήθηκε πραγματικά (2026-08-04)

Έγινε σε **δικό μας πίνακα** (`ent_b220023c`, 5×3, **0 ακμές** στην αφετηρία), φτιαγμένο μέσα στη
συνεδρία: το working tree μοιράζεται με **δύο** άλλους πράκτορες που δοκίμαζαν στους δικούς τους
πίνακες, και ένας πίνακας με **16 ήδη υπάρχουσες** ακμές είναι ακριβώς η «συσσωρευμένη» αφετηρία
που στη Φ3 οδήγησε σε λάθος συμπέρασμα (§18.9).

| # | Τι | Αποτέλεσμα |
|---|---|---|
| **1** | Δομή πάνελ | ✅ **22 στοιχεία = 13 + Επαναφορά + 4 διαγώνιοι + 4 μολύβι**, 2 επικεφαλίδες, 5 διαχωριστικά |
| **2** | Πάχος | ✅ `1.00 mm` → «Όλα» ⇒ **16 ακμές, όλες `widthMm: 1`**· «Παχιά εξωτερικά» ⇒ **12 ακμές, όλες `widthMm: 2`** |
| **3** 🔴 | **Διακεκομμένη** | ✅ **ΠΕΡΑΣΕ** — δεδομένα **και** pixel. Δες παρακάτω |
| **4** | Διπλή | 🟡 δεδομένα ✅ (`doubleGapMm: 3` = 3 × πένα)· γεωμετρία ✅ (**4 νέα tests**)· **pixel ανεπιβεβαίωτο** |
| **5** | Διαγώνιος σε συγχώνευση | ❌ **δεν δοκιμάστηκε** |
| **6** | Επιβίωση σε refresh | ✅ μολύβι **και** ακμή επέζησαν πλήρους reload **και restart του dev server** |

#### 🔑 Βήμα 3 — η κρίσιμη μέτρηση του §19.2, με αριθμούς

Στα **δεδομένα** η ακμή γράφτηκε με `dashMm: [12.7, -6.35]` — δηλαδή **ακριβώς** το μοτίβο με το
**αρνητικό** κενό που θα έκανε το `setLineDash` να αγνοήσει ολόκληρη την κλήση. Στα **pixel** του
`dxf-canvas`, στις σειρές της ακμής:

```
παύλες: 90 px · 88 px        κενά: 42 px · 43 px        λόγος 90/43 ≈ 2,09
μοτίβο 12,7 / 6,35 = 2,00    ⇒ συμφωνία εντός 4,5%
```

Αν το λανθάνον ελάττωμα ζούσε ακόμα, εδώ θα υπήρχε **ένα** συνεχές run ~280 px. **Το
`dashMmToScreenPx` εκτελείται και διπλώνει σωστά το αρνητικό κενό.**

#### 🔑 Δύο φορές η μέτρηση παραλίγο να αναφέρει ελάττωμα που ΔΕΝ υπάρχει

Το §18.9 το είχε προειδοποιήσει· επαναλήφθηκε **δύο** φορές μέσα σε μία συνεδρία:

1. **Μπαγιάτικο fiber snapshot.** Το probe διάβαζε το μοντέλο από το React fiber tree, όπου
   συνυπάρχουν **επτά** αντίγραφα του ίδιου πίνακα. Το πρώτο που έβρισκε η σάρωση ήταν
   **μπαγιάτικο** και έλεγε `edges: 0` ενώ τα άλλα έξι έλεγαν `16` — δηλαδή «η εντολή δεν
   εκτελέστηκε», ενώ είχε εκτελεστεί. Θεραπεία: το probe επιστρέφει το **επικρατέστερο**
   στιγμιότυπο και **δηλώνει** τη συμφωνία (`6/7`, `7/7`).
2. **Κατώφλι χρώματος που έτρωγε το κενό.** Μια μέτρηση έδειξε «δύο μπάντες 4 px με κενό 2 px»
   σε **απλή** γραμμή. Ήταν artifact του κατωφλιού: η ίδια γραμμή, με αυστηρότερο δείγμα,
   μετρήθηκε **10 px συμπαγής** (4+2+4).

**Το μάθημα δεν είναι «μέτρα προσεκτικά». Είναι ότι η μέτρηση χρειάζεται δικό της
πείραμα ελέγχου** — εδώ: η ίδια ακμή **χωρίς** τη λειτουργία που εξετάζεται.

#### ⚠️ Γιατί το βήμα 4 έμεινε μισό, και τι μπήκε στη θέση του

Η pixel-μέτρηση της διπλής έδωσε αποτύπωμα **8 px** εκεί που το §19.3 προβλέπει `4 × πένα`. Η
μέτρηση όμως **δεν στέκει**: η κάμερα κάνει **auto-zoom κάθε φορά που ο πίνακας μπαίνει σε
επεξεργασία**, οπότε το `pxPerMm` άλλαξε ανάμεσα στο «πριν» και στο «μετά» — και σε `pxPerMm ≈ 2`
τα 8 px είναι **ακριβώς** το σωστό `4 × πένα`. Δεν αναφέρεται ελάττωμα που δεν αποδείχθηκε.

Αντ' αυτού αποκαλύφθηκε **πραγματικό κενό κάλυψης**: από τα 44 tests της Φ5, **κανένα** δεν
ρωτούσε αν το `pushBorder` βγάζει όντως **δύο** τμήματα και **πόσο** απέχουν. Καλυπτόταν **ποιο**
`doubleGapMm` παράγει το μολύβι και **ποια** ακμή το κρατά — δηλαδή η μοναδική απόφαση που κάνει
τη διπλή *διπλή* ήταν **αμέτρητη**: μια αποσύνθεση που παρήγαγε δύο **ταυτόσημα** τμήματα θα
περνούσε ολόκληρη τη σουίτα και θα ζωγράφιζε **μία** γραμμή. Προστέθηκαν **4 tests** στο
`table-edge-borders.test.ts` (ζωντανή διαδρομή μέσω `layoutTable`), που μετρούν την απόσταση σε
mm διάταξης, και στους **δύο** άξονες. **Περνούν.**

#### Τι μένει

- **Βήμα 4, pixel**: δύο ορατά παράλληλα ίχνη με κενό `2 × πένα`, σε **κλειδωμένη** κάμερα.
- **Βήμα 5**: «Διαγώνιος σταυρός» σε **συγχωνευμένο** κελί — οι δύο γραμμές να διασχίζουν **όλη**
  τη συγχώνευση, όχι το ένα υπο-κελί.
- **Βήμα 6, UI-reflect**: ότι μετά το refresh η γραμμή «Πάχος» δείχνει `1.00 mm` και **όχι**
  «Αυτόματο» (το `localStorage` επιβεβαιώθηκε· η **προβολή** του όχι).

⚠️ **Πρακτικό εμπόδιο, καταγραμμένο για τον επόμενο**: το δεξί κλικ στη ζώνη δείκτη είναι
**εύθραυστο** — αστοχία κατά λίγα pixel δεν ανοίγει το μενού κεφαλίδας αλλά **βγάζει από τη
λειτουργία επεξεργασίας**, και η επόμενη προσπάθεια ξεκινά από αλλού. Επιπλέον ο dev server
τερμάτισε μόνος του με `Server is approaching the used memory threshold, restarting…`, που
εξηγεί τα CDP timeouts της συνεδρίας — **δεν ήταν σφάλμα της σελίδας.**

### 19.9 Τα αρχεία

```
ΝΕΑ:  bim/table/table-cell-diagonal-ops.ts            (μητρώο 4 + πράξεις + γεωμετρία)
      state/table-border-pencil-store.ts              (η επιλογή, persisted)
      rendering/entities/table/stamp-table-borders.ts (εξαγωγή· το SSoT της διακεκομμένης)
      ui/…/table-format-toolbar/TableBorderPencilPanel.tsx      (η ζώνη σχεδίασης)
      ui/…/table-format-toolbar/TableDiagonalIcon.tsx           (παραγόμενο εικονίδιο)
      ui/…/table-format-toolbar/table-border-pencil-options.ts  (καθαρή παραγωγή λιστών)
      bim/table/__tests__/table-cell-diagonal-ops.test.ts       (25 tests)
ΑΛΛΑΓΜΕΝΑ: types/table-edges.ts (doubleGapMm) · types/table.ts (TableCell.diagonal) ·
      bim/table/table-border-pencil.ts (Α23) · table-range-border-ops.ts (Α24, −φίλτρο) ·
      table-edge-model.ts (sameBorderSpec) · table-layout-borders.ts (αποσύνθεση) ·
      table-layout-place.ts (διαγώνιοι) · table-layout-to-primitives.ts (εξαγωγή) ·
      table-cell-content.ts (μαζικός γραφέας) · TableRenderer.ts · TableBorderIcon.tsx ·
      TableBorderMenu.tsx · TableRangeContextMenu.tsx · TableHeaderContextMenu.tsx ·
      use-table-header-menu.ts · use-table-range-menu.ts · use-table-border-actions.ts ·
      storage-utils.ts · locales el+en · types/i18n.ts
```

---

## Changelog

- **2026-08-04 (β)** — 🔴 **ΤΟ ΠΑΧΟΣ ΤΩΝ ΑΚΜΩΝ ΔΕΝ ΚΛΙΜΑΚΩΝΕΤΑΙ ΠΙΑ ΜΕ ΤΟ ZOOM**
  ([ADR-756](ADR-756-lineweight-display-ssot.md)). Η Φ5 έδωσε στον χρήστη **μολύβι ανά ακμή** —
  δηλαδή πραγματική ιεραρχία πενών (0,13 / 0,5 / 1 / 2 mm). Ο ζωγράφος όμως τις μετέφραζε με
  `widthMm × pxPerMm`, οπότε σε μεγέθυνση **όλες** φούσκωναν μαζί: η ιεραρχία διατηρούνταν
  αναλογικά αλλά το πλέγμα γινόταν μπάρες. Πλέον `lineweightDisplayPx(widthMm)` — σταθερά px,
  ίδιος κανόνας με κάθε γραμμή σχεδίου, με τον διακόπτη «ΠΑΧΟΣ» να ισχύει **και** για τον
  πίνακα (νέο· ήταν ασύνδετος). ⚠️ **Η εκτύπωση δεν αλλάζει** — στο print pass οι δύο τύποι
  συμπίπτουν αριθμητικά (γι' αυτό και κανένα test εξαγωγής δεν είχε δει ποτέ το ελάττωμα).
- **2026-08-04** — **Ζωντανή επαλήθευση της Φ5 — 4/6 βήματα μετρημένα στην οθόνη** (§19.8).
  🔑 **Το κρίσιμο βήμα 3 πέρασε σε pixel**: η ακμή γράφτηκε με `dashMm: [12.7, -6.35]` (το μοτίβο
  με το **αρνητικό** κενό του §19.2) και ο καμβάς έδωσε **παύλες 90/88 px με κενά 42/43 px**,
  λόγος **2,09** έναντι θεωρητικού **2,00** — δηλαδή το `dashMmToScreenPx` εκτελείται. Επίσης
  μετρήθηκαν: η δομή του πάνελ (**22 = 13+1+4+4**), το πάχος (`1.00 mm` ⇒ 16 ακμές `widthMm: 1`·
  «Παχιά εξωτερικά» ⇒ 12 ακμές `widthMm: 2`) και η **επιβίωση σε refresh + restart του server**.
  🔴 **Ένα πραγματικό κενό κάλυψης βρέθηκε γράφοντας τη μέτρηση**: από τα 44 tests της Φ5,
  **κανένα** δεν ρωτούσε αν το `pushBorder` βγάζει **δύο** τμήματα και **πόσο** απέχουν — η
  μοναδική απόφαση που κάνει τη διπλή *διπλή* ήταν **αμέτρητη**, και μια αποσύνθεση με δύο
  **ταυτόσημα** τμήματα θα περνούσε ολόκληρη τη σουίτα ζωγραφίζοντας **μία** γραμμή. **+4 tests**
  (`table-edge-borders.test.ts`, ζωντανή διαδρομή, και στους δύο άξονες) ⇒ **1565/1565 σε 80
  suites**. ⚠️ **Δύο φορές η ίδια μέτρηση παραλίγο να αναφέρει ελάττωμα που δεν υπάρχει** —
  μπαγιάτικο fiber snapshot (1 στα 7) και κατώφλι χρώματος που έτρωγε το κενό· και τις δύο φορές
  το διέψευσε **πείραμα ελέγχου**, όχι προσοχή. Εκκρεμούν: pixel της διπλής, διαγώνιος σε
  συγχώνευση, UI-reflect μετά το refresh. Η **Φ6** ορίστηκε από τον Giorgio ως **full parity
  Excel**, χωρίς εκπτώσεις.
- **2026-08-03** — Φάση 0. Έρευνα αγοράς (3 παράλληλοι πράκτορες: UX/πρότυπα · SSoT audit ·
  DXF interop). Αποφάσεις Α1–Α9. 🔴 **Η έρευνα ανέτρεψε την αρχική εντολή**: το «full parity
  ανά κελί» αποδείχθηκε δομικά ανέφικτο επειδή ο κανόνας του Excel είναι **χρονικός** και
  δεν αποθηκεύεται στο αρχείο (§4). Ο Giorgio αποφάσισε **ανά ακμή**, διατηρώντας ρητά
  και τις δύο απαιτήσεις: περίγραμμα σε **μεμονωμένο κελί** ✅ και **εξάλειψη του
  παραπόνου** «η γραμμή δεν φεύγει» ✅. Δεύτερη ανατροπή: η **διπλή γραμμή δεν γράφεται** —
  υπάρχει ήδη στο ADR-642 Φ5 (§6.4).
- **2026-08-03 (β)** — Κλείδωσαν οι **Α10/Α11/Α12** (Giorgio): «Εξωτερικά» δεν αγγίζει
  εσωτερικές · πολιτική εισαγωγής **δεξί>αριστερό, κάτω>πάνω** · εξαγωγή DXF με **εκπομπή
  edge→cell**. Και τα τρία ήταν καταγεγραμμένα ως ανοιχτά ρίσκα του §14 και μεταφέρθηκαν σε
  αποφάσεις με ρητή αιτιολόγηση. **Φάση 0 ΚΛΕΙΣΤΗ.**
- **2026-08-03 (γ) — ΦΑΣΗ 1 ΕΚΛΕΙΣΕ.** Μοντέλο + μηχανή, καθαρές συναρτήσεις, **47 νέα
  tests**, μηδέν παλινδρομήσεις (544 σουίτα πίνακα / 1113 table-related / 925 types+anchors),
  `jscpd` καθαρό. Πλήρης αναφορά: **§16**. Τρεις αναθεωρήσεις του κειμένου της Φάσης 0, όλες
  γιατί ο κώδικας είπε κάτι που το σχέδιο δεν είχε δει:
  1. 🔴 **Το `TableEdgeEntry` ήταν λάθος στο §6.2.** Ως `[TableEdgeKey, TableBorderSpec]` θα
     έγραφε **μη εκτυπώσιμο byte μέσα σε κάθε αποθηκευμένο σχέδιο** και θα έκανε τη μορφή του
     κλειδιού δέσμευση μορφής αρχείου. Έγινε **τετράδα** — που είναι ό,τι κάνει ήδη η «Λύση Α»
     για τα κελιά, δηλαδή το §6.2 παραβίαζε το πρότυπο που δήλωνε ότι ακολουθεί «ακριβώς».
  2. **Ο διαχωρισμός των τύπων γεννούσε κύκλο** (`table → table-edges → table`) — απρόβλεπτο
     από τη Φάση 0. Λύθηκε με τρίτο, κάτω στρώμα `types/table-ids.ts`. Το επικίνδυνο
     `types/table.ts` **μίκρυνε** (427→420), δηλαδή το ρίσκο §14/#1 έκλεισε αντίστροφα.
  3. ⭐ **Νέα απόφαση Α13** (§6.6): η Φάση 0 δεν είχε θέσει τι απογίνονται οι ακμές στη
     **διαγραφή** άξονα. Η αγκύρωση σε ταυτότητες λύνει την εισαγωγή δωρεάν, αλλά η διαγραφή
     αφαιρεί την ίδια την άγκυρα: σκέτο σβήσιμο θα εξαφάνιζε το **πάνω πλαίσιο του πίνακα**
     επειδή ο χρήστης έσβησε μια γραμμή δεδομένων.
  🔴 **Μια υπάρχουσα πύλη κοκκίνισε και είχε δίκιο** (§16.4): ο ανιχνευτής JSON βρήκε τον νέο
  `Map` (`table.model.edges`). Ο ισχυρισμός **δυνάμωσε** αντί να χαλαρώσει — ονομάζει πλέον
  και τα δύο ευρετήρια χωριστά, και το fixture του πραγματικού πίνακα απέκτησε ρητές ακμές.
- **2026-08-03 (δ) — ΦΑΣΗ 2 ΕΚΛΕΙΣΕ.** Οι 13 εντολές ως **μητρώο δεδομένων + έναν applier**
  (όχι 13 σώματα), μαζικός εγγραφέας ακμών, **50 νέα tests**, σουίτα πίνακα **1185/1185 σε 63
  suites**, `jscpd`/`eslint` καθαρά. Πλήρης αναφορά: **§17**. Τέσσερις νέες αποφάσεις:
  1. **Α14** (§6.7) — «Χωρίς περίγραμμα» = **ρητό αόρατο μολύβι**, η επιστροφή στην κληρονομιά
     είναι **ξεχωριστή** εντολή. Η έρευνα βρήκε ότι το AutoCAD έχει την ίδια τριάδα (`ByBlock`
     = «match the table style») **αλλά** το «No border» του σβήνει μόνο στην εκτύπωση, όχι στην
     οθόνη — δηλαδή **το Π3 υπάρχει και εκεί, χειρότερο**. Το Handsontable έχει και τα δύο
     μονοπάτια, με το `hide` ανοιχτό issue. Το Excel έχει **ένα** κουμπί για δύο σημασίες.
  2. 🔴 **Α16** (§6.8) — **η έρευνα ανέτρεψε την πρόταση της Φάσης 2**: το σχέδιο ζητούσε να
     **παραλείπονται** οι εσωτερικές ακμές συγχώνευσης· η μετρημένη συμπεριφορά του Excel
     («All Borders» πριν την αποσυγχώνευση **επιβιώνει** μετά) αποδείχθηκε και η **σωστή**,
     γιατί δομή και μορφοποίηση είναι ορθογώνιες — η παράλειψη θα άφηνε **τρύπα** στο πλέγμα
     που ο χρήστης ζήτησε, χωρίς ορατή αιτία. Δώρο: οι πράξεις μένουν **καθαρή γεωμετρία**
     (μηδέν γνώση συγχώνευσης, κανένα νέο module, καμία αλλαγή στον ζωγράφο).
  3. **Α17** (§6.9) — η **διπλή γραμμή** δηλώνεται μη διαθέσιμη μέχρι τη Φ5 μέσω ρητής
     ερώτησης· **όλα ή τίποτα, με ίχνος**. Ούτε `dashMm`, ούτε `'double'` enum, ούτε σιωπηλό
     no-op.
  4. **Α15** — το τρέχον μολύβι είναι **παράμετρος**, ποτέ store.
  🔴 **Η μέτρηση μετάλλαξης βρήκε τρύπα στο ίδιο μας το δίχτυ** (§17.4.1): 5/6 μεταλλάξεις
  έπεσαν, **μία επέζησε** — επικάλυψη `insideH`/`bottom` **δεν αφήνει ίχνος** (δύο εγγραφές
  στο ίδιο κλειδί `Map` απλώς αντικαθιστούν, το πλήθος δεν αλλάζει). Η θεραπεία δεν ήταν test
  αλλά **API**: εξήχθη η `tableRangeSideEdges()` — που τη χρειάζεται ούτως ή άλλως το preview
  της Φ6 — και η ξενότητα των έξι πλευρών ελέγχεται πλέον άμεσα. Σήμερα η μετάλλαξη είναι
  **μη παρατηρήσιμη**· **η Φ6 θα την κάνει παρατηρήσιμη**.
  ⭐ Δύο SSoT που είχε χάσει το audit της Φάσης 0 (§17.5): το ιδιωτικό `NO_BORDER` του
  `table-style-presets.ts` (διπλότυπο **κάτω** από το κατώφλι του jscpd, άρα αόρατο στην πύλη)
  και ο κατάλογος ISO πένων, από τον οποίο **παράγεται** πλέον το «παχύ» (§6.10): λόγος 1:2
  του ISO 128-20 ⇒ αναπαράγονται ακριβώς τα πρότυπα ζεύγη `0.13→0.25 … 1.00→2.00` χωρίς να
  γραφτεί κανένα από αυτά τα νούμερα.
- **2026-08-03 (ε)** — ✅ **Το §15/#1 έκλεισε**: στιγμιότυπο ελληνικού Excel από τον Giorgio.
  Και τα 13 ονόματα μετρήθηκαν (**§8.1**, πλήρης πίνακας). 🔴 **Η μαντεψιά της Φάσης 0 ήταν
  λάθος σε ένα σημείο — και ακριβώς εκεί που κανείς δεν θα το υποψιαζόταν**: το #12 λέει
  «Επάνω και **πλατύ** κάτω περίγραμμα», όχι «παχύ». Η Microsoft αποδίδει το ίδιο *Thick* με
  **τρεις** λέξεις — «παχιά» (#8), «παχύ» (#10), «πλατύ» (#12) — και τα δύο τελευταία απέχουν
  **δύο γραμμές στο ίδιο μενού**. Ο πράκτορας είχε μαντέψει το **συνεπές**· η πραγματική
  διεπαφή δεν ήταν συνεπής. **Μια μετάφραση που «βγάζει νόημα» δεν φαίνεται ποτέ ως εικασία.**
  Νέα απόφαση **Α18** (§8.4): πιστή αντιγραφή ή συνεπές «παχύ»; Αφορά **μόνο** ορατό κείμενο —
  τα `id` του μητρώου είναι αγγλικά και σταθερά. **Έκλεισε την ίδια μέρα: βλ. (στ).**
  ⭐ Δύο ακόμη μετρήσεις καταγράφηκαν όσο υπήρχε η απόδειξη: (α) το μενού έχει **τρεις ομάδες**
  με διαχωριστικά (4/4/5) ⇒ το μητρώο απέκτησε πεδίο `group`, ώστε η Φ3 να μην την ξανα-βγάλει
  από στιγμιότυπο που δεν θα υπάρχει· (β) το στιγμιότυπο είναι από το **mini toolbar** του
  Excel, δηλαδή **ανεξάρτητη επιβεβαίωση του §9.1** (το dropdown κρέμεται στο mini toolbar,
  όχι στην κορδέλα). Σουίτα πίνακα **1281/1281 σε 66 suites**.
- **2026-08-03 (στ)** — ✅ **Α18 ΑΠΟΦΑΣΙΣΤΗΚΕ (Giorgio): συνεπές «παχύ»** (§8.4). Το #12
  γράφεται «Επάνω και **παχύ** κάτω περίγραμμα»· δεν αντιγράφουμε το «πλατύ» της Microsoft.
  **Ο όρος είναι και ο σωστός**: στο ISO η γραμμή έχει **πάχος** (`lineweight`) — έτσι τη λέει
  ήδη ο κατάλογος πένων του έργου. Από την απόφαση προκύπτει **κανόνας για όλα τα locale keys
  της Φ3**: όπου η ελληνική τοπικοποίηση της Microsoft είναι **εσωτερικά ασυνεπής**,
  ακολουθούμε τον τεχνικό όρο του σχεδίου· όπου είναι απλώς **διαφορετική**, ακολουθούμε τη
  Microsoft. Parity by default, απόκλιση **μόνο** με γραμμένο λόγο.
  ⭐ Δεύτερο στιγμιότυπο (μεγέθυνση) ⇒ **§8.2, η σύμβαση των εικονιδίων**: όλα τα 13 είναι το
  ίδιο πλέγμα 2×2 με **δύο** επίπεδα γραμμής — **διάστικτη** για το πλέγμα ως συμφραζόμενο,
  **συμπαγής** για την ενέργεια. 🔑 Το διάστικτο δεν είναι διακόσμηση: μια μόνη συμπαγής
  γραμμή σε λευκό φόντο λέει «γραμμή», όχι «κάτω» — το «κάτω» προκύπτει **μόνο** από τη θέση
  της σε ορατό πλέγμα. Χωρίς αυτό τα 13 εικονίδια θα ήταν αδιάκριτα. Οι τρεις παραλλαγές της
  ενεργής γραμμής (λεπτή / παχιά / δύο παράλληλες) είναι **ακριβώς** η τριάδα του μοντέλου
  `base` / `thick` / `double`.
- **2026-08-03 (ζ) — ΦΑΣΕΙΣ 3+4 ΕΚΛΕΙΣΑΝ.** Το dropdown των 13 στο mini toolbar **και** το δεξί
  κλικ σε κελιά, μαζί με απόφαση του ιδιοκτήτη: η Φ3 είναι **μερικώς ανεπαλήθευτη** χωρίς τη Φ4,
  γιατί το toolbar ανοίγει μόνο από ζώνη δείκτη — θα παραδιδόταν κώδικας που δεν μπορεί να
  δοκιμαστεί στην κύρια περίπτωση χρήσης. **51 νέα tests**, σουίτα πίνακα **1367/1367 σε 72
  suites**, `jscpd`/`eslint` καθαρά. Πλήρης αναφορά: **§18**. Τέσσερις νέες αποφάσεις:
  1. **Α19** (Giorgio) — **κάθε ετικέτα ονομάζει το αντικείμενό της**. Υπήρχε ήδη «Επαναφορά στο
     στυλ» για το στυλ κειμένου και η Φ2 γέννησε δεύτερη πράξη με **ταυτόσημο νόημα** για τα
     περιγράμματα, στο **ίδιο** toolbar. Δύο κουμπιά με ίδιο κείμενο σε απόσταση εκατοστών είναι
     ελάττωμα που **κανένα test δεν βλέπει και κάθε χρήστης βλέπει** — τώρα το βλέπει test που
     απαιτεί μηδέν ταυτόσημα ορατά κείμενα σε ολόκληρη τη γραμμή.
  2. **Α20** — το μολύβι **παράγεται από το ενεργό στυλ** (εσωτερική γραμμή της κλάσης `data`),
     απαντώντας το «ποιος το κρατά» που άφησε ρητά ανοιχτό η Α15: **κανείς**. 🔑 Η επικύρωση ήταν
     **μέτρηση**: στο `standard` η εσωτερική είναι 0,25 και το πλαίσιο 0,50 — το `thick` (επόμενη
     πένα ISO) δίνει **ακριβώς** 0,50, δηλαδή «Παχιά εξωτερικά» αναπαράγει το πλαίσιο που ορίζει
     το ίδιο το στυλ χωρίς να το ξέρει. Με βάση το πλαίσιο θα έβγαινε **διπλάσιο**.
  3. **Α21** — τα 13 είναι `role="menu"` (**εντολές**), όχι toolbar με `aria-pressed`: το §9.2
     αφορά το proxy preview της Φ6, όπου εναλλάσσεται *κατάσταση*. `aria-pressed` σε εντολή χωρίς
     κατάσταση θα ανακοίνωνε μονίμως «μη πατημένο» — ψέμα σε κάθε ανάγνωση.
  4. **Α22** — δεξί κλικ σε κελί: στόχος **το κελί**, εκτός αν ανήκει στην επιλογή. Είναι η
     σημασιολογία του Excel **χωρίς** την παρενέργειά του (εκείνο τη συμπεραίνει μετακινώντας την
     επιλογή· εδώ βγαίνει από την **ερώτηση**). Δώρο από τον ΕΝΑ δρόμο: το μεμονωμένο κελί
     κουμπώνει σε **ολόκληρη συγχώνευση** χωρίς μία γραμμή ειδικής λογικής.
  🔑 **Το μεγάλο της Φ3**: τα 13 εικονίδια **δεν σχεδιάστηκαν**. Εξήχθησαν οι δύο ιδιωτικές
  συναρτήσεις γεωμετρίας της Φ2 και το εικονίδιο τις καλεί **με όρια ένα πλέγμα 2×2** — δηλαδή
  δεν *μιμείται* την εντολή, **εκτελεί την ίδια συνάρτηση**. Το Excel έχει 13 στατικά bitmap που
  *μπορούν* να αποκλίνουν από τη συμπεριφορά και **τίποτα να τα συγκρίνει**· εδώ η απόκλιση είναι
  μη εκφράσιμη, και ελέγχεται ως ιδιότητα πάνω και στις 13.
  🔴 **Το CHECK 3.28 έπιασε ΔΥΟ διπλότυπα που είχαν ήδη γραφτεί** (§18.6) — και τα δύο «ο δεύτερος
  καταναλωτής γεννιέται μέσα στο ίδιο commit»: η τοποθέτηση κρυφού trigger (31 γρ., **μαζί με τη
  ζωντανή μέτρηση του σφάλματος που την εξηγεί** — γραμμένη μόνο στο ένα από τα δύο αντίγραφα) και
  η ακολουθία «ζωντανό μοντέλο → μετασχηματισμός → φύλακας ταυτότητας → commit» (10 γρ.). Και τα
  δύο εξήχθησαν. Ένα τρίτο (`useLiveTable`, 3 γρ. × 2) ήταν **κάτω** από το κατώφλι του jscpd,
  δηλαδή αόρατο σε κάθε πύλη — το ίδιο σχήμα με το `NO_BORDER` του §17.5.
  ⭐ Το i18n test κλείνει κενό που **η CHECK 3.8 δεν μπορεί** να κλείσει (§18.7.1): τα ονόματα
  επιλύονται με **δυναμικό** κλειδί, άρα ο σαρωτής `t('literal')` δεν βλέπει τίποτα — «0 = κανείς
  δεν κοίταξε». Το test διαβάζει τα **αληθινά** locale JSON και το **αληθινό** μητρώο, και για τα
  **13** (όχι μόνο τα 11 ορατά: η Φ5 ενεργοποιεί τα υπόλοιπα).
  ✅ **ΕΠΑΛΗΘΕΥΤΗΚΕ ΖΩΝΤΑΝΑ ΣΤΗΝ ΟΘΟΝΗ** (§18.9), και οι δύο διαδρομές. Η **Α20** μετρήθηκε στα
  **δεδομένα**: «Όλα» σε ένα κελί έγραψε ακριβώς 4 ακμές με `#666666@0.25` — τη γραμμή πλέγματος
  που ορίζει το `standard` στυλ· «Παχιά εξωτερικά» έγραψε `@0.5`, την επόμενη πένα ISO **και**
  ακριβώς το πλαίσιο του ίδιου στυλ. Το sentinel `` της Φ1 δούλεψε (`V:r4:`).
  ⚠️ **Ενδιάμεσα αναφέρθηκε ελάττωμα που ΔΕΝ υπήρχε** (η «Επαναφορά» φαινόταν μονίμως ενεργή):
  ήταν σφάλμα **της μέτρησης** — συσσωρευμένες ακμές από προηγούμενες δοκιμές. Το probe στο
  ζωντανό `canReset` το διέψευσε και στις δύο κατευθύνσεις. Χωρίς probe, το λάθος συμπέρασμα θα
  είχε μείνει γραμμένο.
- **2026-08-03 (στ) — ΦΑΣΗ 5 ΕΚΛΕΙΣΕ ΣΕ ΚΩΔΙΚΑ, ⚠️ ΑΝΕΠΑΛΗΘΕΥΤΗ ΖΩΝΤΑΝΑ.** Το **μολύβι του
  χρήστη** (χρώμα · στυλ · **πάχος** · **διπλή**) μέσα στο ίδιο dropdown με τις εντολές, +
  **διαγώνιοι**. **44 νέα tests**, σουίτα πίνακα **1487/1487 σε 75 suites**, `jscpd:diff` σε 13
  αρχεία καθαρό, `eslint` καθαρό, κανένα αρχείο >500. Πλήρης αναφορά: **§19**. Τέσσερις νέες
  αποφάσεις (**Α23–Α26**) και **δύο ανατροπές γραμμένων αποφάσεων**:
  1. 🔴 **Το §6.4 ανατράπηκε (Α24)**: η πρόταση «`patternKey` προς το compound catalog» ονόμαζε
     τύπο **που δεν υπάρχει** (`LinePatternKey`) και έδειχνε σε κατάλογο **αντικειμένων του
     κόσμου** — το `railway` ορίζει εύρος τροχιάς **1.507 mm** και στρωτήρες **2.600 mm**. Η
     διπλή γραμμή δεν χρειάζεται μηχανισμό: **αποσυντίθεται στη διάταξη** σε δύο παράλληλα
     τμήματα, οπότε καμβάς, PDF, DXF και ΤΕΚ δεν μαθαίνουν τίποτα. Η απαγόρευση τηρήθηκε —
     **κανένα `'double'` enum**· το `doubleGapMm` είναι **απόσταση**, ακριβώς το «Spacing» που
     εκθέτει ο διάλογος *Cell Border Properties* του AutoCAD.
  2. ✅ **Η Α17 έκλεισε με ίχνος**: το μενού πέρασε 11 → **13**, ο φύλακας διαθεσιμότητας
     **διαγράφηκε** (δεν έμεινε επιστρέφοντας μονίμως `true` — αυτό θα ήταν νεκρός κώδικας που
     λέει ψέματα), και το τεστ που απαιτούσε την **απουσία** των δύο διπλών αντιστράφηκε ρητά.
     🔑 Τα διαχωριστικά μετακινήθηκαν από τη θέση 7 στην 8 **χωρίς καμία αλλαγή κώδικα** — η
     απόδειξη ότι η παραγωγή τους από το `group` της Φ3 ήταν σωστή.
  3. 🔴 **Λανθάνον ελάττωμα που έγινε ενεργό** (§19.2): ο ζωγράφος του πλέγματος είχε **δεύτερο**
     `dashMm → px` (σκέτος πολλαπλασιασμός). Ήταν αόρατο επειδή **κανένα preset δεν παρήγαγε
     ποτέ `dashMm`** — «0 = κανείς δεν κοίταξε». Με τον επιλογέα στυλ, τα μοτίβα έρχονται από
     τον κατάλογο όπου **τα κενά είναι αρνητικά**, και το `setLineDash` **αγνοεί ολόκληρη την
     κλήση**: διακεκομμένη που ζωγραφίζεται συμπαγής, χωρίς σφάλμα πουθενά. ⚠️ Ο **καταγραφέας
     των tests το έκρυβε** (`setLineDash: () => undefined`) — ένα stub που καταπίνει είναι
     πράσινο και για τις δύο υλοποιήσεις.
  4. **Κοινό working tree**: ο άλλος πράκτορας γενίκευε το ίδιο μενού χρώματος **την ίδια ώρα**.
     Δεν το άγγιξα· καταναλώνεται το `TableColorSwatchGrid` που είχε ήδη εξαγάγει (§19.7). Το
     `jscpd` καθαρό. 🔑 Η σωστή μονάδα κοινοποίησης **δεν ήταν το μενού, ήταν το πλέγμα**.
  5. **Δύο αρχεία στο όριο μίκρυναν αντί να μεγαλώσουν**: `TableHeaderContextMenu` 498 → **483**
     (πέντε border props έγιναν ένα) και `stamp-table-layout` 520 → **474** (εξαγωγή του
     πλέγματος). Εξαγωγή, ποτέ trim.
  ⚠️ **ΤΙ ΔΕΝ ΕΓΙΝΕ (§19.8)**: **η ζωντανή επαλήθευση στην οθόνη**. Η καρτέλα υπό αυτοματοποίηση
  επέστρεφε συνεχώς σε `chrome://newtab`. Δεν δηλώνεται «έτοιμο» — το §19.8 έχει τα **έξι**
  βήματα που πρέπει να μετρηθούν, με **μετρήσεις στα δεδομένα** και όχι μόνο εικόνα.

- **2026-08-04 — ADR-755 (συγχώνευση κελιών): τρία πράγματα άλλαξαν εδώ, χωρίς αλλαγή αποφάσεων.**
  1. 🔑 **Η Α16 απέκτησε το αντίθετό της, και είναι σωστό.** Η Α16 λέει για περιγράμματα και
     διαγωνίους «γράφονται **όλα**, η δομή αποφασίζει τι φαίνεται». Η συγχώνευση κάνει το
     αντίστροφο — **αδειάζει** τα καλυμμένα κελιά. Η διαφορά είναι κατηγορική: εκείνα είναι
     **μορφοποίηση**, το κείμενο είναι **περιεχόμενο**, και κρυμμένο περιεχόμενο δεν βγαίνει σε
     καμία εξαγωγή ούτε το βρίσκει καμία αναζήτηση. Δες ADR-755 §4/Α2 — καταγράφεται εδώ ώστε
     να μη «διορθωθεί» πίσω από κάποιον που θα διαβάσει μόνο την Α16.
  2. ✅ **Το χρέος του `TableAxisColorMenu.module.css` πληρώθηκε**: το κοινό στιλ των πάνελ
     μετακόμισε στο `table-toolbar-panel.module.css` — **με το όνομα που το ίδιο το σχόλιο
     είχε γράψει**. Σκανδάλη ήταν ο τρίτος καταναλωτής (μενού συγχώνευσης): η επόμενη επιλογή
     ήταν «τριπλότυπο ή εξαγωγή». Μέσω `composes`, μηδέν αλλαγές σε JSX.
  3. **Ο κύκλος ζωής του πτυσσόμενου και τα δύο μισά του split button εξήχθησαν**
     (`use-toolbar-panel.ts`, `ToolbarSplitButton.tsx`) — το CHECK 3.28 τα μέτρησε ως δίδυμα
     μόλις ήρθε το τρίτο πάνελ. Το `TableBorderMenu` και το `TableAxisColorMenu` τα καταναλώνουν
     πλέον· **καμία** αλλαγή συμπεριφοράς, 145 tests πράσινα.
