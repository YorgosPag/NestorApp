# ADR-772 — Το λεξιλόγιο της διοικητικής ιεραρχίας: ένας μετατροπέας, οκτώ επίπεδα, έξι λεξιλόγια

- **Ημερομηνία**: 2026-08-08 · **Κατάσταση**: ✅ Υλοποιήθηκε· **§10 = η πύλη CHECK 3.44** (§9 = τι μένει ανοιχτό: μόνο η σύγκρουση τομέα του §5)
- **Φορέας**: κλείνει το **ADR-759 Φ3** (`.claude-rules/pending-ratchet-work.md`)
- **Σχετικά**: ADR-332 D15 (ο πρόγονος, μόνο για επαφές) · ADR-167 · ADR-186 · ADR-584 / N.18 ·
  ADR-587 §6.1 (τεστ που ρωτά τον εαυτό του) · ADR-769 (το πρότυπο `Record<K,…>`) · ADR-749 (αποδείξεις pattern)

---

## 1. Το πρόβλημα

Η οθόνη (`AddressWithHierarchy`) ρωτά **οκτώ** διοικητικά επίπεδα × (Id + Name) = **16 πεδία**,
συν 4 ταχυδρομικά. Η αποθήκευση κρατούσε **όσα ήξερε ο κάθε μετατροπέας**, **χωρίς κανένα μήνυμα**.
Ο χρήστης συμπλήρωνε, πατούσε «Αποθήκευση», έβλεπε επιτυχία — και η Δημοτική Ενότητα δεν υπήρχε πουθενά.

🔑 Το χειρότερο είδος σφάλματος: δεν σκάει, δεν κοκκινίζει, δεν αφήνει ίχνος. Εμφανίζεται μήνες
αργότερα, σε **άλλη** οθόνη, όταν τα δεδομένα έχουν ήδη χαθεί για N εγγραφές.

---

## 2. Η μέτρηση — ανά **κατεύθυνση**, όχι ανά όνομα

Το `.claude-rules/pending-ratchet-work.md` έλεγε «**έξι** επίπεδα» και ονόμαζε **τρεις** σπασμένους
μετατροπείς. Το grep ονομάτων έδειχνε **8/8** για τους τρεις. **Και τα δύο ήταν λάθος**, γιατί και
τα δύο ρωτούσαν *«αναφέρεται το όνομα;»* αντί *«μεταφέρεται και στις δύο κατευθύνσεις;»* — ο τύπος
`AddressWithHierarchyValue` εμφανίζεται σε **casts**, όχι μόνο σε αντιστοιχίσεις.

| # | Αρχείο / ζεύγος | → προς φόρμα | ← προς αποθήκευση | Ταυτότητες |
|---|---|---|---|---|
| 1 | `projects/tabs/locations/location-converters.ts` | 6/8 | 6/8 · **έχανε το `country`** | 0/8 |
| 2 | `contacts/dynamic/CompanyAddressesSection.tsx` | 8/8 | 8/8 | 2/8 |
| 3 | `building-management/.../BuildingAddressesEditor.tsx` | **5/8** | **5/8** | 0/8 |
| 4 | `contacts/dynamic/AddressesSectionWithFullscreen.tsx` | 8/8 | 8/8 (σε **δύο** δοχεία) | 2/8 |
| 5 | `projects/building-code/FrontageAddressCreateDialog.tsx` | *(δανείζεται #1)* | **2/8** | 0/8 |
| 6 | `app/demo/addresses/page.tsx` | — | **2/8** | 0/8 |

### 2.1 Τι ανέτρεψε η μέτρηση

- 🔴 Το **#3 είχε το ΑΚΡΙΒΩΣ ΙΔΙΟ σφάλμα `municipalUnit`** που η Φ3 του ADR-759 διόρθωσε στο #1 —
  **αδιόρθωτο, και στις δύο κατευθύνσεις**. Η Φ3 διόρθωσε το **δείγμα**, όχι την κλάση.
- 🔴 Το **#4 δεν ήταν σε κανέναν χάρτη** — 38 γραμμές inline, δηλαδή μετατροπέας **χωρίς όνομα**,
  αόρατος σε κάθε αναζήτηση.
- 🔴 Το **#2 ήταν το πληρέστερο**, και γι' αυτό επικίνδυνο: το grep το έβγαζε πράσινο, οπότε κανείς
  δεν ρωτούσε γιατί τα άλλα τρία δεν του έμοιαζαν. **Η ορθότητα ενός αντιγράφου δεν λέει τίποτα
  για τα υπόλοιπα** — αυτό, όχι η ορθότητα, είναι το επιχείρημα υπέρ του SSoT.

---

## 3. **Πέντε** σχήματα σιωπηλής απώλειας, όχι ένα

Η κλάση δεν ήταν «ο μετατροπέας ξέχασε». Ήταν πέντε ανεξάρτητοι μηχανισμοί με το ίδιο αποτέλεσμα:

| # | Μηχανισμός | Πού βρέθηκε |
|---|---|---|
| **Α** | Ο μετατροπέας δεν χαρτογραφεί επίπεδο | #1 (`decentAdmin`/`majorGeo` σταθερά `''`), #3 |
| **Β** | Το **δοχείο** δεν έχει καν πεδίο | `ProjectAddress`: καμία ταυτότητα, κανένα `decentAdmin`/`majorGeo` |
| **Γ** | Ο **καλών** ξαναδιαλέγει με το χέρι υποσύνολο **σωστού** αποτελέσματος | #5 — ο μετατροπέας ήταν πράσινος, η απώλεια στον καλούντα |
| **Δ** | Το **Zod** πετά τα αδήλωτα (`z.object` = `.strip()`) | `address-schemas.ts` (ADR-759 Φ3) |
| **Ε** | Το **εργοστάσιο** είναι χειρόγραφη λίστα επιτρεπτών πεδίων | 🔴 **ΝΕΟ** — `createProjectAddress` δήλωνε **11** προαιρετικά ενώ ο τύπος είχε **22** |

🔴 **Το Ε δεν ήταν σε κανέναν χάρτη και ήταν αποφασιστικό**: χωρίς αυτό, η διόρθωση του Γ **δεν θα
έκανε τίποτα** — το `createProjectAddress` θα ξαναπετούσε το `municipalUnit` στην επόμενη γραμμή.
Ίδιο σχήμα με το Δ («ό,τι δεν δηλώθηκε, εξαφανίζεται»), αλλά σε **εργοστάσιο**, όπου κανείς δεν το έψαχνε.

---

## 4. Η απόφαση

### 4.1 Δεν γράφτηκε πέμπτος μετατροπέας — **προήχθη** ο υπάρχων

Το `HIERARCHY_PROJECTION` (`utils/contacts/address-info-builder.ts`, ADR-332 D15) είχε ήδη **τις
δύο κατευθύνσεις πάνω στον ίδιο πίνακα** και ήταν ήδη στο `.ssot-registry.json`. Του έλειπαν τρία:
δεν ήξερε το λεξιλόγιο της **φόρμας**, δεν ήξερε το λεξιλόγιο **έργων**, και **δεν ήταν εξαντλητικός
στον compiler**. Μετακόμισε στο ουδέτερο `utils/address/` — **η διοικητική ιεραρχία δεν είναι έννοια
επαφών** (ίδιο πρότυπο με το WAVE 3 του `createExternalStore`).

### 4.2 Ο μηχανισμός: ο μεταγλωττιστής ως πύλη

- `Readonly<Record<AdminLevelKey, …>>` όπου `AdminLevelKey = keyof AdminPath` — **η αυθεντία των
  επιπέδων**, όχι αντίγραφό της. Ένατο επίπεδο ⇒ **δεν μεταγλωττίζεται**.
- Κάθε όνομα πεδίου τυπώνεται ως `keyof <το πραγματικό δοχείο>` ⇒ μετονομασία πεδίου σε οποιοδήποτε
  δοχείο ⇒ **δεν μεταγλωττίζεται**.
- **`NOT_STORED`**: το κελί δεν επιτρέπεται να **λείπει**. «Δεν το κρατάμε» γράφεται· η διαφορά από
  το «ξεχάστηκε» είναι ακριβώς αυτή που κόστισε τα δεδομένα.
- **Υποχρεωτικές επιλογές** (`includePostal`, `clearedIdsAsNull`) — καμία προεπιλογή. Έξι σημεία
  κλήσης, καθένα **λέει** τι εννοεί.

🏆 **Πού ξεπερνά τους μεγάλους**: Revit/ArchiCAD έχουν shared parameters που ο χρήστης ξεχνά να
χαρτογραφήσει — η απώλεια είναι **σιωπηλή by design**. Εδώ ένας μετατροπέας που ξεχνά επίπεδο **δεν
μεταγλωττίζεται**.

### 4.3 ❌ Απορρίφθηκε: `.strict()` στο Zod

Το handoff προέβλεπε ότι η απάντηση είναι `.strict()` («σκάει αντί να σβήνει»). **Δεν αντέχει
έλεγχο**: το `.strict()` κάνει το PATCH να **σκάει** σε οποιοδήποτε άγνωστο κλειδί — ένας παλιός
client θα έσπαγε **ολόκληρη** την αποθήκευση διεύθυνσης. Η αυστηρότητα είναι **χαρακτηριστικό**·
το ελάττωμα ήταν η **ελλιπής δήλωση**. Το σχήμα μένει `.strip()`, με άγκυρα `Required<ProjectAddress>`
που κάνει το ξεχασμένο πεδίο **σφάλμα μεταγλώττισης** (ADR-759 Φ3, διατηρήθηκε και επεκτάθηκε).

---

## 5. 🔴 ΑΝΟΙΧΤΟ — η σύγκρουση `community` / `neighborhood`

**Δύο τομείς διαφωνούν για το τι σημαίνει η ίδια λέξη**, και η διαφωνία είναι **δεδομένα**, όχι κώδικας:

| Τομέας | `neighborhood` σημαίνει | Κοινότητα (επίπεδο 7) |
|---|---|---|
| **Έργα** | την **Κοινότητα** | ζει στο `neighborhood` |
| **Επαφές** | ελεύθερο κείμενο, **εκτός** ιεραρχίας (`ContactFormTypes.ts`) | ζει στο `communityName` |

Το ίδιο πεδίο των έργων ζωγραφίζεται ως «Περιοχή / Συνοικία» (`LocationInlineForm`
`showNeighborhoodRegion`) **και** τροφοδοτεί τον γεωκωδικοποιητή (`geocoding-engine.ts`).

**Απόφαση: δηλώνεται, δεν λύνεται.** Ένα ξεχωριστό `ProjectAddress.community` θα ήταν αλλαγή
**ορατής συμπεριφοράς** (το πεδίο θα άδειαζε σε υπάρχουσες εγγραφές) και **ερώτημα τομέα**
(«Κοινότητα == Συνοικία;»), όχι τεχνική εκκρεμότητα. Γι' αυτό λείπει και το `communityId` από τα
νέα πεδία. Άγκυρες: `ADMIN_LEVEL_VOCABULARY.community.projectAddress` + ρητό test.

---

## 6. Τι άλλαξε

| Αρχείο | Αλλαγή |
|---|---|
| **`utils/address/administrative-hierarchy-vocabulary.ts`** 🆕 | Ο πίνακας: 8 επίπεδα × 5 λεξιλόγια + ταχυδρομικά + `neighborhood` |
| **`utils/address/administrative-hierarchy.ts`** 🆕 | `projectAddressVocabulary` (όλες οι κατευθύνσεις) · `resolveCityFromHierarchy` · `hierarchyToResolvedAddress` · `storedAddressToResolved` |
| `types/project/addresses.ts` | `ProjectAddress` **+9**: `decentAdmin`, `majorGeo`, 7 ταυτότητες (χωρίς `communityId` — §5) |
| `types/project/address-schemas.ts` | **Τα ίδια 9** στο Zod· ταυτότητες `nullable` (το `null` = «καθαρίστηκε») |
| `types/project/address-helpers.ts` | 🔴 `createProjectAddress`: η χειρόγραφη λίστα → «ό,τι έχει περιεχόμενο» |
| `utils/contacts/address-info-builder.ts` | `HIERARCHY_PROJECTION` **διαγράφηκε** → καταναλωτής· `FlatAddressFormFields` μετακόμισε (επανεξάγεται) |
| 6 καταναλωτές (§2) | Κάθε ιδιωτικό ζεύγος → **κλήση** |
| 4 δίδυμα `→ ResolvedAddressFields` | Ένα `storedAddressToResolved` (§6.2) |
| `LocationInlineForm` + `BuildingAddressesEditor` | Το διπλό `hierarchyToResolved` → ένα |
| `ProjectLocationsTab` + `addresses-section-form-mapping` | Το 3ο+4ο δίδυμο (§6.2) — αόρατα στο `--diff` |
| `.ssot-registry.json` | Νέο module `administrative-hierarchy-vocabulary` (Tier 3) + 2 patterns **με απόδειξη** |

### 6.1 Δύο κλώνοι που έπιασαν οι πύλες, όχι εγώ

- **`address-info-builder.test.ts`**: μετακίνησα το `neighborhood` στα ταχυδρομικά ⇒ η «Περιοχή /
  Συνοικία» των επαφών **εξαφανίστηκε**. Ο πρόγονος το είχε **μαζί με την ιεραρχία** («εκτός
  ιεραρχίας, αλλά ίδιο μονοπάτι»). ⇒ χωριστός πίνακας `HIERARCHY_ADJACENT_VOCABULARY`.
- **CHECK 3.28 (jscpd)**: `toResolvedFromAddr` (κτίρια) και `branchToResolvedFields` (επαφές) ήταν
  δίδυμα 50 tokens — **και είχαν ήδη αποκλίνει** στην αλυσίδα `region` και στο `country`.
  ⇒ `storedAddressToResolved(source, vocabulary)`.

### 6.2 🔴 Τα δίδυμα ήταν **τέσσερα**, και η πύλη μπορούσε να δει μόνο δύο

Μετά το «τελείωσα», ένα τελευταίο `grep "): ResolvedAddressFields {"` βρήκε **άλλα δύο**:
`toResolvedFields` (`ProjectLocationsTab.tsx`) και `formDataToResolvedFields`
(`addresses-section-form-mapping.ts`).

⚠️ **Το `jscpd --diff` δεν μπορούσε να τα δει, και αυτό είναι διδακτικό**: η πύλη εξετάζει
**μόνο τα αρχεία του diff**, και εκείνα τα δύο δεν τα είχα αγγίξει. **Μια πύλη diff δεν είναι
απογραφή** — απαντά «εισήγαγες κλώνο **τώρα**;», ποτέ «υπάρχει κλώνος;». Το Layer 2 (`jscpd-ratchet.yml`,
πλήρης σάρωση `src/`) τα μετρά, αλλά ως **αριθμό** σε ratchet, όχι ονομαστικά.

Το τέταρτο ήταν το πιο ύπουλο: διέφερε σε **δύο** σημεία (`streetNumber` αντί `number`,
`settlement || city` αντί σκέτο `city`) ⇒ *έμοιαζε* με άλλη συνάρτηση ενώ ήταν **το ίδιο ερώτημα
σε άλλο λεξιλόγιο** — ακριβώς αυτό που ο πίνακας μοντελοποιεί. Η προειδοποίηση «η προτεραιότητα
ΠΡΕΠΕΙ να ταυτίζεται με το combobox» που ζούσε εκεί ως **σχόλιο** είναι πλέον **δομή**.
Και οι τέσσερις είναι τώρα μονογραμμικοί delegates· τα τοπικά ονόματα μένουν για τους καλούντες.

Το ίδιο και για το `hierarchyToResolved`: **δύο αντίγραφα που είχαν ήδη αποκλίνει** — το ένα δεν
έπεφτε στον Δήμο όταν έλειπε ο οικισμός, οπότε **ένα χωριό έστελνε στον γεωκωδικοποιητή διεύθυνση
χωρίς πόλη** από τη μία οθόνη και με πόλη από την άλλη. Ίδιο όνομα συνάρτησης, άλλο αρχείο ⇒
αόρατο σε κάθε name-based gate. Κρατήθηκε η εκδοχή **με** το fallback.

---

## 7. Απόδειξη — όχι πράσινο

`src/utils/address/__tests__/administrative-hierarchy-round-trip.test.ts` — **29 tests**.

Το fixture είναι `Required<AddressWithHierarchyValue>`: ο compiler απαιτεί **και τα 20** πεδία.
Νέο επίπεδο στην οθόνη ⇒ **το test δεν μεταγλωττίζεται**.

### 7.1 🔴 Ο έλεγχος μετάλλαξης άλλαξε τον σχεδιασμό **δύο** φορές

Τα 23 πρώτα tests πέρασαν **με την πρώτη**. Ο έλεγχος μετάλλαξης έδειξε ότι αυτό δεν σήμαινε τίποτα:

- **Μ5 επέζησε** (`regionalUnit` → γράψε στο `label`): ο κύκλος έκλεινε **τέλεια** (γράφει `label`,
  διαβάζει `label`) και **26/26 έμεναν πράσινα**, ενώ η Περιφερειακή Ενότητα θα αποθηκευόταν στην
  ετικέτα. 🔑 **Ένας round-trip αποδεικνύει συνέπεια, όχι ορθότητα προορισμού.**
  ⇒ Προστέθηκε ρητή προσδοκία **πραγματικών ονομάτων πεδίων** ανά δοχείο.
- **Μ11 επέζησε** (`neighborhood.projectAddress`): η αλλαγή **δεν άλλαζε τίποτα** — ο ορισμός του
  **νεκρού κανόνα**. Αποκάλυψε ότι **δύο γραμμές διεκδικούσαν το ίδιο πεδίο**
  (`ProjectAddress.neighborhood`: η Κοινότητα και η Συνοικία), όπου ο νικητής θα ήταν **η σειρά του
  βρόχου**. ⇒ Ένας ιδιοκτήτης ανά πεδίο + **δομική αναλλοίωτη** «κανένα πεδίο με δύο διεκδικητές».

**Τελικό: 12/12 μεταλλάξεις κόκκινες · Μ0 πράσινο πριν και μετά.**

Επίσης: το test «ΤΙ ΟΦΕΙΛΕΙ να κρατά η αποθήκευση» είναι **χειρόγραφη λίστα εν γνώσει μου** — ο
κύκλος ρωτά «μεταφέρεται ό,τι ο πίνακας **δηλώνει**;», που αφήνει τρύπα: υποβάθμιση δήλωσης σε
`NOT_STORED` θα έκανε τον κύκλο να **αγνοήσει** το επίπεδο και να μείνει πράσινος. Η λίστα είναι
**δεύτερη, ανεξάρτητη φωνή**.

### 7.2 Παλινδρόμηση

370 tests / 28 suites πράσινα σε `utils/address`, `utils/contacts`, `types/project`,
`components/{contacts,projects,building-management}`. Οι **επαφές αμετάβλητες** — απόδειξη ότι η
προαγωγή του πίνακα δεν άλλαξε συμπεριφορά εκεί.

---

## 8. Πύλη

`.ssot-registry.json` → `administrative-hierarchy-vocabulary` (Tier 3), **2 patterns σε ορισμούς
συναρτήσεων**, και τα δύο με **απόδειξη ζωής** (`pattern-proofs.js`, ADR-749 §5) εκτελεσμένη στη
**μηχανή της πύλης**.

⚠️ **Ένα τρίτο pattern απορρίφθηκε πριν μπει**: στόχευε αναθέσεις `<επίπεδο>Name: x.<πεδίο>` και
πυροδοτούσε σε **τρία νόμιμα** αρχεία που συγχωνεύουν αποτέλεσμα γεωκωδικοποίησης. Δεν διέκρινε
χειρόγραφο μετατροπέα από νόμιμη συγχώνευση ⇒ >10% ψευδώς θετικά σε **μπλοκάρουσα** πύλη.
**Ο ορισμός συνάρτησης είναι το μονοσήμαντο σήμα· η κλήση όχι.**

---

## 9. Τι μένει ανοιχτό — ρητά

1. **§5 `community` / `neighborhood`** — απόφαση τομέα, όχι τεχνική εκκρεμότητα.
2. ✅ **ΕΚΛΕΙΣΕ 2026-08-08 — βλ. §10 (CHECK 3.44).** *(Ήταν: «δεν υπάρχει AST πύλη για δοχείο που
   δηλώνει διοικητικό πεδίο εκτός πίνακα».)*
3. **Το `ProjectAddress.neighborhood` δεν έχει `communityId`** — συνέπεια του (1).
4. **Χειροκίνητος έλεγχος οθόνης** (καμία στατική πύλη δεν τον αντικαθιστά): τοποθεσία έργου →
   Δημοτική Ενότητα + Αποκεντρωμένη Διοίκηση → αποθήκευση → **ξαναάνοιγμα**. Ομοίως καρτέλα κτιρίου
   και πρόσωπο οικοπέδου.

---

## 10. Η πύλη που έλειπε — **CHECK 3.44** (§9 σημείο 2)

### 10.1 Το ερώτημα, και γιατί **δύο** καταστάσεις και όχι εννιά

> «Ένα δοχείο διεύθυνσης απέκτησε πεδίο διοικητικής ιεραρχίας **χωρίς γραμμή στον πίνακα**;»

Τότε ο μετατροπέας δεν το μεταφέρει, **τίποτα δεν σκάει**, και η σιωπηλή απώλεια του §1 επιστρέφει
— με τη διαφορά ότι πλέον **φαίνεται λυμένη**.

Το §4.2 όμως έβαλε ήδη τον **μεταγλωττιστή** πύλη. Ό,τι πιάνει εκείνος **δεν ξαναχτίζεται**:

| Περίπτωση | Ποιος τη σταματά |
|---|---|
| Ένατο επίπεδο | `Readonly<Record<AdminLevelKey, …>>` ⇒ δεν μεταγλωττίζεται |
| Μετονομασία πεδίου σε δοχείο | `FieldChain<V> = keyof VocabularyContainers[V]` ⇒ δεν μεταγλωττίζεται |
| Ξεχασμένο πεδίο στο Zod | fixture `Required<ProjectAddress>` ⇒ δεν μεταγλωττίζεται + test κόκκινο |
| Νέο ιδιωτικό ζεύγος μετατροπέα | CHECK 3.7 (§8) |
| Δύο κανόνες στο ίδιο πεδίο | άγκυρα «κανένα πεδίο με δύο διεκδικητές» (§7.1, Μ11) |

⇒ **Δύο** καταστάσεις παράβασης: `unmapped-administrative-field` · `unregistered-vocabulary`.

⛔ **Δύο ακόμη εξετάστηκαν και απορρίφθηκαν πριν γραφτούν** — και οι δύο θα ήταν φρουροί που **δεν
μπορούν να πυροδοτήσουν**, δηλαδή προσθήκη στους **606 αδρανείς** του ADR-749 §5:
- `orphan-mapping` («ο πίνακας δείχνει σε ανύπαρκτο πεδίο») — το `keyof` το κάνει **αδύνατο**.
  Άγκυρα ότι η σιωπή είναι **σκόπιμη**: test `Κ4`.
- `orphan-not-stored` («λέει `NOT_STORED` ενώ το πεδίο υπάρχει») — **ακριβώς ισοδύναμο** με το
  πρώτο: το πεδίο θα ήταν αδιεκδίκητο, άρα ήδη παράβαση.

### 10.2 🔑 Το κριτήριο «τι είναι λεξιλόγιο» — **η μέτρηση ανέτρεψε το προφανές**

Το προφανές («τύπος με **≥3** διοικητικά πεδία») **μετρήθηκε σε ολόκληρο το `src/`** (14.708 αρχεία,
20.319 δηλώσεις τύπων) **πριν** επιλεγεί πολιτική: **12 ευρήματα, 5 ψευδώς θετικά = 41%**. Ο πήχης
της Google για **μπλοκάρουσα** πύλη είναι **<10%**.

Τα ψευδώς θετικά δεν ήταν οριακά — ήταν **κατηγορίες**:
- **τέσσερις παραγόμενοι τύποι i18n** (`I18n_Common_Audit_Fields` κ.λπ. στο `src/types/i18n.ts`):
  τα «πεδία» τους είναι **κλειδιά μετάφρασης** με ίδιο όνομα·
- **`ContactAddressMapPreviewProps`**: **δέχεται** τρία *ονόματα* ως props «for geocoding
  disambiguation» — είναι **αναγνώστης**, όχι δοχείο.

Κρατήθηκε το κριτήριο **ταυτότητας**:

> **≥3** διοικητικά πεδία, από τα οποία **≥2 ταυτότητες** (`<επίπεδο>Id`).

🔑 **Γιατί η ταυτότητα και όχι το πλήθος**: ένα **όνομα** («Θεσσαλονίκη») είναι κείμενο — το
πληκτρολογεί άνθρωπος, το επιστρέφει γεωκωδικοποιητής, το κρατά ένα prop. Μια **ταυτότητα** μπορεί
να προέλθει **μόνο** από το σύνολο δεδομένων της ιεραρχίας. Μία ταυτότητα είναι *αναφορά*· **δύο**
σημαίνει ότι ο τύπος κουβαλά **τα κλειδιά** της ιεραρχίας, δηλαδή **είναι** λεξιλόγιο.

⚠️ **Το κατώφλι δεν χαλάρωσε για να γίνει πράσινο**: δικαιολογείται από **τι είναι** μια ταυτότητα,
και το `Μ6` το δοκιμάζει **και προς τις δύο κατευθύνσεις** (1 ταυτότητα ⇒ πράσινο, 2 ⇒ κόκκινο).

🔴 **Το κριτήριο έλυσε 4 από τα 5 ψευδώς θετικά. Το πέμπτο ήταν το ενδιαφέρον**: το
`I18n_Common_Audit_Fields` **έχει** `settlementId` **και** `municipalityId` — ως κλειδιά μετάφρασης.
Έμενε **1 στα 5 = 20%**, ακόμη πάνω από τον πήχη. Λύθηκε **όχι** με εξαίρεση μονοπατιού αλλά με τη
ρητή κατάσταση `generated-artifact`: **παραγόμενο αρχείο είναι προβολή άλλου SSoT, όχι απόφαση** —
η φρεσκάδα του φρουρείται ήδη από το CHECK 3.33 και η χειροκίνητη επεξεργασία απαγορεύεται στην
επικεφαλίδα του. Ο δείκτης διαβάζεται **μόνο** από το πρώτο μπλοκ σχολίων (χαλαρό κριτήριο: **21**
αρχεία· αυστηρό: **11**, όλα όντως παραγόμενα).

**Τελικό: 4 ευρήματα, 4 πραγματικά, 0% ψευδώς θετικά.**

### 10.3 Οι επτά καταστάσεις — καμία σιωπηλή απόρριψη

| Κατάσταση | Μηχανισμός | Πλήθος (2026-08-08) |
|---|---|---|
| `unmapped-administrative-field` | ⛔ **ZERO-TOL** | **0** |
| `unanalyzable-container` | ⛔ **ZERO-TOL** (fail-closed) | **0** |
| `unregistered-vocabulary` | 🔴 **RATCHET** κατά ταυτότητα | **4** |
| `registered-vocabulary` | ✅ στήλη του πίνακα | 5 |
| `base-of-registered` | ✅ κρίνεται μέσα στο δοχείο του | 1 |
| `generated-artifact` | ✅ προβολή άλλου SSoT (§10.2) | 1 |
| `unanalyzable-heritage` | ✅ **ο παρονομαστής** — μετριέται, δεν μπλοκάρει | 213 |
| `below-vocabulary-threshold` | ✅ | 20.095 |

⚠️ **Το `unanalyzable-heritage` είναι το τυφλό σημείο ΜΕ ΑΡΙΘΜΟ.** 213 δηλώσεις έχουν βάση που δεν
επιλύεται (props που επεκτείνουν τύπους βιβλιοθηκών)· **καμία δεν φέρνει διοικητικό πεδίο σήμερα**,
αλλά η κατηγορία **δεν σιωπά**. Ίδιο πρότυπο με το `unanalyzable: 194` του CHECK 3.35. Δεν
απαριθμείται: 213 γραμμές θα έκρυβαν τα 4 πραγματικά.

### 10.4 Δύο μηχανισμοί — και γιατί **δεν** υπάρχει «κλειστό σύνολο»

1. **ZERO TOLERANCE** για τις δύο πρώτες. **Δομικά ανεπίδεκτες απορρόφησης**: το `buildPayload`
   **αρνείται** να γράψει baseline που τις περιέχει (test `Μ8`). *Ένα zero-tol που κλειδώνεται με
   ένα `--write-baseline` δεν είναι zero-tol.*
2. **RATCHET κατά ταυτότητα** για το `unregistered-vocabulary`. **Όχι** zero-tol: και τα **4** είναι
   πραγματικά και η θεραπεία τους είναι **νέα στήλη στον πίνακα** — αλλαγή που αγγίζει και τα 5
   δοχεία και είναι απόφαση τομέα (§5/§6), όχι δουλειά αυτής της πύλης.

⚠️ **Γιατί δεν υπάρχει δεύτερο «κλειστό σύνολο δηλώσεων»** όπως στο CHECK 3.39: εκεί κάθε **νέα**
δήλωση σταθερού hex είναι δομικά επικίνδυνη ακόμη κι όταν σήμερα περνά. Εδώ μια **νέα
χαρτογραφημένη** διοικητική στήλη είναι ακριβώς η **σωστή** πράξη — κλειστό σύνολο θα μπλόκαρε τη
θεραπεία. Ένα σύνολο, `declarations: []`, γραμμένο ρητά.

### 10.5 Τα τέσσερα ευρήματα — όλα πραγματικά

| Τύπος | Πεδία / ταυτότητες | Τι είναι |
|---|---|---|
| `AdministrativeAddress` (`AdministrativeAddressPicker.tsx`) | **16 / 8** | **Ακριβές δίδυμο** του `AddressWithHierarchyValue`: δεύτερος επιλογέας ιεραρχίας με δικό του σχήμα τιμής |
| `ContactFormData` (`ContactFormTypes.ts`) | 10 / 2 | Το **πραγματικό** δοχείο των επίπεδων πεδίων· το `FlatAddressFormFields` (στήλη `contactFlat`) είναι χειρόγραφο απόσπασμά του |
| `IndividualAddress` (`ContactFormTypes.ts`) | 10 / 2 | Ο **δίδυμος** του `CompanyAddress` για φυσικά πρόσωπα — ίδιες βάσεις, **καμία στήλη** |
| `DerivedWorkAddress` (`useDerivedWorkAddresses.ts`) | 10 / 2 | `extends IndividualAddress` — συνέπεια του προηγούμενου |

⚠️ **Ο αριθμός 4 ΔΕΝ είναι δείκτης υγείας.** Είναι «λεξιλόγια που ο πίνακας δεν ξέρει». **Άνοιξε το
`.address-vocabulary-baseline.json`** πριν επικαλεστείς αριθμό.

### 10.6 Δύο στρώματα, με **δηλωμένο** όριο

- **Στρώμα 1** — pre-commit, **~0,2s**: μόνο τα δοχεία του πίνακα (~9 αρχεία).
  ⚠️ **Η σκανδάλη είναι ΠΑΡΑΓΟΜΕΝΗ**: η πύλη λύνει μόνη της ποια αρχεία είναι δοχεία (και βάσεις
  τους) **από τον ίδιο τον πίνακα**, και βγαίνει αμέσως αν κανένα δεν είναι staged. Λίστα μονοπατιών
  στο `run-checks-parallel.js` θα ήταν **δεύτερη αυθεντία** που αποκλίνει σιωπηλά — το ακριβές σχήμα
  των **δύο λιστών namespace** του CHECK 3.34 (απέκλιναν κατά 63). Άγκυρες: `Κ6β`, `Κ6γ`.
  ⚠️ **ΔΕΝ βλέπει** νέο τύπο αλλού στο `src/`. Δηλωμένο, με άγκυρα `Κ5`.
  ⚠️ **ΔΕΝ αγγίζει τη baseline**: έχει δει μόνο τα δοχεία, οπότε η **απουσία** ενός
  `unregistered-vocabulary` σημαίνει «δεν σάρωσα», **όχι** «καθαρίστηκε» — σύγκριση συνόλων εκεί θα
  ανέφερε τα 4 υπαρκτά ως **πρόοδο** και θα καλούσε σε reseed (μάθημα `scope:'staged'` του 3.38).
  Άγκυρα: `Κ6`.
- **Στρώμα 2** — CI (`--all`), **~30s**: όλο το `src/`.
  ⚠️ **Χωρίς προφίλτρο κειμένου, επίτηδες.** Το «διάβασε μόνο αρχεία που αναφέρουν ρίζα επιπέδου»
  είναι **ασθενές**: το `DerivedWorkAddress` παίρνει και τα δέκα πεδία του από **άλλο αρχείο**.
  Κόστος ορθότητας: 30s αντί 7s, **μόνο στο CI**.

**ΚΑΝΕΝΑ νέο workflow**: το Στρώμα 2 είναι **job** στο υπάρχον `ssot-discover.yml`. Νέο αρχείο
workflow θα απαιτούσε εγγραφή στο `.ci-gate-tiers.json` αλλιώς **μπλοκάρει το CHECK 3.37** (ADR-757)·
το μητρώο παρακολουθεί **αρχεία**, όχι jobs ⇒ **29 πύλες, αμετάβλητο** (επαληθεύτηκε εκτελώντας το).
Η οικογένεια ταιριάζει: το 3.18 ρωτά «υπάρχει διπλότυπο SSoT;», το 3.44 «μιλά κάποιος το λεξιλόγιο
χωρίς να είναι στο SSoT;».

### 10.7 SSoT — τι **δεν** γράφτηκε

Καμία νέα μηχανή. Ο σαρωτής **δανείζεται**:

| Δανεικό | Από | Αντί για |
|---|---|---|
| `resolveSpecifier` + `readTsPathAliases` | `lib/module-graph/resolve-specifier.js` (ADR-700) | δεύτερη επίλυση ψευδωνύμων |
| `collectSourceFiles` | `lib/module-graph/scan-config.js` | δεύτερος περίπατος δέντρου (και θα σάρωνε τα backup δέντρα) |
| `runSetRatchetCli` · `compareSets` · `loadBaseline` | `lib/ratchet-baseline.js` (ADR-598/770) | έκτο control-flow ratchet |
| `ts.createSourceFile` **parse-only** | ίδιο πρότυπο με `parse-module.js` | **δεν** είναι `tsc` (N.17) |

⚠️ Το `resolveSpecifier` είναι σχεδιασμένο **injectable** («ο καλών περνά το σύνολο αρχείων»)· εδώ
περνιέται σύνολο με πλάτη τον δίσκο, γιατί οι ερωτήσεις είναι ~10, όχι 14.708.

🔴 **Το CHECK 3.28 (jscpd / N.18) έπιασε κλώνο μέσα σε αυτή τη δουλειά**: ο βρόχος «λύσε ειδικευτή →
λύσε τύπο → πάρε κλειστότητα κληρονομιάς» ήταν γραμμένος **τρεις** φορές. Εξήχθη σε
`resolveContainerDeclarations` (ένα, τρεις καταναλωτές). **Η πύλη το είπε πριν γραφτεί το «done».**
Ολικό `jscpd` σε `scripts/`: **204 κλώνοι, 0 δικοί μου** — γιατί μια πύλη diff **δεν είναι
απογραφή** (§0.2 του handoff· κόστισε ήδη μία φορά σε αυτή την αλυσίδα).

### 10.8 Απόδειξη

- **31 tests**, `npm run test:address-vocabulary` — Μ0 (5) · Μ1–Μ8 · Π1–Π7 · Κ1–Κ7 · πολιτική (2).
- **12/12 μεταλλάξεις κόκκινες**, **Μ0 πράσινο πριν ΚΑΙ μετά** (κατώφλια ×2 · δείκτης παραγωγής ×2 ·
  διεκδίκηση · αλυσίδα · κληρονομιά · `export *` · fail-closed δοχείο · απορρόφηση baseline ·
  σκανδάλη Στρώματος 1 · ένωση πινάκων).
- ⚠️ **Οι μεταλλάξεις είναι στις ΕΙΣΟΔΟΥΣ, όχι στην πύλη**: κάθε Μ αντιγράφει τα **πραγματικά**
  αρχεία σε μίνι-repo και αλλάζει **μία** γραμμή — ό,τι θα έκανε ένας άνθρωπος αύριο. Το `miniRepo`
  **ουρλιάζει** αν μια μετάλλαξη δεν άλλαξε τίποτα (μάθημα Μ11 του §7.1).
- ⚠️ **Τα Π είναι χειρόγραφα, επίτηδες** (ADR-587 §6.1): ο σαρωτής παίρνει επίπεδα και δοχεία **από
  τον πίνακα**· τα Π τα ξαναγράφουν με το χέρι, ώστε αθόρυβη αφαίρεση γραμμής να έχει **δεύτερη
  φωνή** να τη διαψεύσει.
- ⚠️ **Το μίνι-repo δεν είναι καθαρό, και αυτό είναι το νόημα**: κουβαλά τα πραγματικά αρχεία, άρα
  και 2 από τα 4 ζωντανά ευρήματα. Οι μεταλλάξεις κρίνονται στη **διαφορά** — σύγκριση με κενό
  σύνολο θα οδηγούσε σε προσαρμογή του κριτηρίου μέχρι να «βγει» ο αριθμός.

### 10.9 ⚠️ Παγίδα που πληρώθηκε: **το CHECK 3.44 δεν ήταν το 3.42**

Η πύλη γράφτηκε ως **3.42**. Στο **τέλος** της υλοποίησης, ο επανέλεγχος έδειξε ότι το 3.42
(ADR-773 §8) **και** το 3.43 (ADR-774) είχαν καταληφθεί από **άλλον πράκτορα στο ίδιο δέντρο, ενώ
έτρεχα** — ακριβώς όπως το ADR-771 είχε αρπάξει τον αριθμό αυτού του ADR. **Επαλήθευσε τον επόμενο
ελεύθερο τη στιγμή που τον χρειάζεσαι, όχι στην αρχή της συνεδρίας**, και ξανά πριν το commit.

---

## 11. Changelog

- **2026-08-08** — Δημιουργία. Κλείνει το ADR-759 Φ3. Μέτρηση ανά κατεύθυνση (6 σημεία,
  5 σχήματα απώλειας), προαγωγή του `HIERARCHY_PROJECTION` σε `Record<AdminLevelKey,…>` με 5
  λεξιλόγια, `ProjectAddress` +9 πεδία + Zod, `createProjectAddress` χωρίς χειρόγραφη λίστα,
  6 καταναλωτές → κλήσεις, 3 κλώνοι εξαλείφθηκαν, 29 tests / 12 μεταλλάξεις, registry + αποδείξεις.
  **Ανοιχτά: §9.**
- **2026-08-08** — **§10: η πύλη που έλειπε (CHECK 3.44)**. Κλείνει το **§9 σημείο 2**· τα σημεία
  1/3 (η σύγκρουση τομέα του §5) **μένουν ανοιχτά κατ' απόφαση**.
  · **Δύο** καταστάσεις παράβασης, όχι εννιά — ο μεταγλωττιστής καλύπτει ήδη τις υπόλοιπες (§10.1)·
  δύο ακόμη (`orphan-mapping`, `orphan-not-stored`) **απορρίφθηκαν πριν γραφτούν** ως αδρανείς.
  · **Το κριτήριο άλλαξε από τη μέτρηση, όχι από τον χάρτη**: το προφανές «≥3 διοικητικά πεδία»
  μετρήθηκε σε όλο το `src/` (20.319 δηλώσεις) και έδωσε **41% ψευδώς θετικά**· το κριτήριο
  **ταυτότητας** (≥3 πεδία, **≥2 `*Id`**) + η ρητή κατάσταση `generated-artifact` το κατέβασαν σε
  **0%** — **4 ευρήματα, 4 πραγματικά** (§10.2, §10.5).
  · **Δύο μηχανισμοί**: zero-tolerance (2 καταστάσεις, **δομικά ανεπίδεκτο απορρόφησης** — το
  `buildPayload` αρνείται) + ratchet κατά ταυτότητα (`.address-vocabulary-baseline.json`, **4**).
  **Κανένα δεύτερο «κλειστό σύνολο»**, με γραπτό λόγο (§10.4).
  · **Δύο στρώματα**: pre-commit ~0,2s με **παραγόμενη** σκανδάλη (καμία χειρόγραφη λίστα) ·
  CI ~30s **χωρίς προφίλτρο κειμένου, επίτηδες**. **ΚΑΝΕΝΑ νέο workflow** — job στο υπάρχον
  `ssot-discover.yml`· μητρώο **29** πύλες, επαληθευμένο εκτελώντας το CHECK 3.37 (§10.6).
  · **Καμία νέα μηχανή**: δανείζεται `resolve-specifier` (ADR-700), `scan-config`,
  `runSetRatchetCli` (ADR-598/770). Το **CHECK 3.28 έπιασε κλώνο** και εξήχθη το
  `resolveContainerDeclarations`· ολικό `jscpd` σε `scripts/`: **0 δικοί μου** (§10.7).
  · **31 tests, 12/12 μεταλλάξεις κόκκινες, Μ0 πράσινο πριν & μετά** — μεταλλάξεις στις **εισόδους**
  (μίνι-repo από τα πραγματικά αρχεία), Π χειρόγραφα ως **δεύτερη φωνή** (§10.8).
  · ⚠️ Ο αριθμός της πύλης άλλαξε **3.42 → 3.44** στο τέλος: άλλος πράκτορας πήρε τα 3.42/3.43 ενώ
  έτρεχε (§10.9). Ίδιο σχήμα με την αρπαγή του ADR-771.
