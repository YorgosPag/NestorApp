# ADR-724 — Ο **Χώρος Εργασίας** του DXF Viewer: αγκύρωση, αλλαγή μεγέθους, αιώρηση της κύριας παλέτας

| Πεδίο | Τιμή |
|---|---|
| **Status** | 🟢 **Φ0 + Φ1 + Φ2 + Φ3 + Φ4/§9.1 IMPLEMENTED — ΚΑΙ ΟΙ ΤΡΕΙΣ ΛΕΙΤΟΥΡΓΙΕΣ ΤΟΥ §13 ΕΙΝΑΙ ΖΩΝΤΑΝΕΣ**: αγκύρωση **αριστερά** · αγκύρωση **δεξιά** · **αιώρηση** με σύρσιμο, ζώνες απόθεσης 64px και περίγραμμα-προεπισκόπηση (§14.8). Αλλαγή πλάτους με δείκτη **και** πληκτρολόγιο· οι δύο καμβάδες ακολουθούν· το περιεχόμενο προσαρμόζει την πυκνότητά του στο πλάτος της παλέτας (§14.6). Απόδοση **μετρήθηκε** (§4.3.1): το σύρσιμο κοστίζει ~0. **Φ4/§9.3 + §9.2: NOT IMPLEMENTED** (βέλτιστο πλάτος · view transitions). Ανέλεγκτο ζωντανά: **mobile drawer** (§14.2) |
| **Date** | 2026-07-28 (Φ0/Φ1) · 2026-07-29 (Φ2, Φ4 §9.1, **Φ3**) |
| **Category** | DXF Viewer — Layout / Workspace· Shared UI primitives (`components/ui/floating`, `components/ui/resizable`) |
| **Author** | Claude Opus 5 + Γιώργος Παγώνης |
| **Trigger** | Giorgio: «ΤΟ CONTAINER ΝΑ ΜΕΓΑΛΩΝΕΙ/ΜΙΚΡΑΙΝΕΙ ΔΕΞΙΑ-ΑΡΙΣΤΕΡΑ ΚΑΙ ΝΑ ΑΚΟΛΟΥΘΟΥΝ ΟΙ ΚΑΜΒΑΔΕΣ 2Δ ΚΑΙ 3Δ· ΝΑ ΜΠΟΡΕΙ ΝΑ ΕΙΝΑΙ FLOATING Ή ΝΑ ΑΓΚΥΡΩΝΕΙ ΑΡΙΣΤΕΡΑ Ή ΔΕΞΙΑ» |
| **Συνεχίζει** | **ADR-723** — που δήλωσε ρητά *«⛔ εκτός σκοπού: docking, keyboard resize»*. Αυτό το ADR είναι ακριβώς αυτό το εκτός-σκοπού |
| **Companions** | ADR-040 (micro-leaf· ο καμβάς)· ADR-176 (responsive + pointer events)· ADR-241 (fullscreen)· ADR-418 (πραγματική κλίμακα 1:N)· ADR-549 (dpr)· ADR-092 (storage SSoT)· ADR-013 (panel tokens)· ADR-002 (z-index) |
| **Industry alignment** | Revit *Dockable Windows* · ArchiCAD palettes · Cinema 4D Layout Manager · Figma right panel · VS Code sash · WAI-ARIA `separator` (window splitter) |
| **Risk** | **High** — αγγίζει τη ρίζα διάταξης του viewer **και** τον κανόνα resize του καμβά (ADR-040 CHECK 6B/6D). Μετριάζεται με φασεολόγηση: Φ1 δίνει το 80% της αξίας αγγίζοντας 3 αρχεία |

---

## 0. Πώς διαβάζεται αυτό το έγγραφο

Είναι **ερευνητικό ADR**: γράφτηκε πριν από τον κώδικα, όπως ορίζει ο κανόνας N.0.1 Φάση 1.
Τα §1–§4 είναι **μετρημένα ευρήματα** στον σημερινό κώδικα (με αρχείο:γραμμή — επαληθεύσιμα).
Τα §5–§9 είναι **αποφάσεις** και §10 το σχέδιο υλοποίησης. Όπου δεν είμαι σίγουρος, το λέω ρητά
αντί να το κρύψω πίσω από βεβαιότητα (§11 «Ανοιχτά ερωτήματα»).

---

## 1. Τι ζητήθηκε

Η κύρια παλέτα του viewer (καρτέλες *Επίπεδα / Ρυθμίσεις DXF / Ιδιότητες / Διαστάσεις / Υλικά /
BIM 3D / Διατομές Κάσας / Εισαγόμενα*) — σήμερα καρφωμένη αριστερά σε **384px** — πρέπει να αποκτήσει:

1. **Αλλαγή πλάτους** με σύρσιμο, και οι δύο καμβάδες (2D & 3D) να προσαρμόζονται ταυτόχρονα.
2. **Τρεις τρόπους**: αγκυρωμένη αριστερά (σημερινή θέση), αγκυρωμένη **δεξιά**, ή **αιωρούμενη**
   πάνω από τους καμβάδες.

---

## 2. SSoT AUDIT — τι **υπάρχει ήδη** (grep, όχι εικασία)

### 2.1 Ώριμη υποδομή που **πρέπει** να χρησιμοποιηθεί

| Υπάρχον SSoT | Αρχείο | Τι λύνει ήδη |
|---|---|---|
| **Floating γεωμετρία** (ADR-723) | `src/components/ui/floating/*` | σύρσιμο, **8 λαβές** αλλαγής μεγέθους, κανόνας ορίων, μνήμη ανά χρήστη, διάσωση από «χαμένη εκτός οθόνης» |
| `useFloatingPanelGeometry` | `…/floating/useFloatingPanelGeometry.ts` | ο **ένας** κάτοχος θέσης+μεγέθους μιας παλέτας· rAF-coalesced `window.resize`· persist στο **τέλος** χειρονομίας |
| `useResizable` / `useDraggable` | `src/hooks/` | pointer events (ADR-176), pointer capture, 8 ακμές, ελεγχόμενα (controlled) — **αποθήκη = ο καλών** |
| `floating-panel-persistence` | `…/floating/` | `nestor:floating-panel-geometry:v1:` πάνω από `@/lib/storage` (SSR/quota-safe) |
| **Split panes** | `src/components/ui/resizable.tsx` | shadcn wrapper του **`react-resizable-panels@4.7.2`** (MIT) — ήδη σε **3** σελίδες |
| Καμβάς 2D | `hooks/canvas/useCanvasSizeObserver.ts:83`, `useCanvasResize.ts` | `ResizeObserver` + dpr sync (ADR-549) |
| Καμβάς 3D | `bim-3d/scene/scene-manager-resize.ts:40` | `applyViewportResize()` → aspect + `setSize` + SSAO + ViewCube + `bimEdgeResolutionStore` + invalidate caches |
| Pointer rect | `rendering/core/pointer-rect-cache.ts:58` | `ResizeObserver` στο ίδιο το canvas ⇒ **αυτο-ακυρώνεται** σε κάθε αλλαγή πλάτους |
| Z-index | `styles/DxfZIndexSystem.styles.ts:113` | `dxfZIndex.overlays.sidebar` = `sticky + 10` |
| Responsive | `useResponsiveLayout` + `constants/layout.ts` | `desktop ≥ 1024` / `tablet ≥ 768` / `mobile` |

**Συμπέρασμα Α:** το ~80% υπάρχει. Το ζητούμενο είναι **σύνθεση**, όχι νέος μηχανισμός.

### 2.2 ~~Το **μοναδικό** εμπόδιο σήμερα~~ → 🔴 **ΗΤΑΝ ΔΥΟ** (διορθώθηκε 2026-07-29, §14.6)

> ⚠️ **Η επικεφαλίδα αυτής της ενότητας ήταν λάθος και κόστισε μια ολόκληρη φάση.** Η έρευνα
> σταμάτησε στο πρώτο εύρημα και δήλωσε «δεν υπάρχει άλλη σκληρή παραδοχή πλάτους **πουθενά**».
> Υπήρχε: το `FloatingPanelContainer.tsx:135` κρατούσε **δεύτερο** `w-96` (384px), ένα επίπεδο
> **πιο μέσα** από το `SidebarSection`. Η Φ1 ξεκλείδωσε το εξωτερικό, η παλέτα μεγάλωνε, και το
> περιεχόμενο έμενε στα 384px αφήνοντας κενή λωρίδα δεξιά — ορατό στην οθόνη του Giorgio επί
> **δύο φάσεις**, ενώ όλα τα tests ήταν πράσινα.
>
> **Γιατί δεν φάνηκε:** το grep έψαξε στη διαδρομή *διάταξης* (`layout/`) και το βρήκε εκεί. Το
> δεύτερο κλείδωμα ζούσε στη διαδρομή *περιεχομένου* (`ui/`) — άλλος φάκελος, ίδιο token.
> **Μάθημα:** το «δεν υπάρχει αλλού» απαιτεί grep του **token** σε όλο το subapp, όχι του φακέλου
> που ερευνάς. Ο έλεγχος τώρα είναι μηχανικός (`workspace-palette-density.test.tsx` §2).

Το **πρώτο** από τα δύο εμπόδια:

`src/subapps/dxf-viewer/layout/SidebarSection.tsx:56-60`

```ts
WIDTH:     PANEL_LAYOUT.WIDTH.PANEL_LG                       // w-96      = 384px
MIN_WIDTH: PANEL_LAYOUT.LAYOUT_DIMENSIONS.SIDEBAR_MIN_WIDTH  // min-w-[384px]
MAX_WIDTH: PANEL_LAYOUT.LAYOUT_DIMENSIONS.SIDEBAR_MAX_WIDTH  // max-w-[384px]
```

`min = max = 384` + `flex-shrink-0`. **Δεν υπάρχει άλλη σκληρή παραδοχή πλάτους** πουθενά στη
διάταξη: το `MainContentSection.tsx:124` έχει ήδη `min-w-0 overflow-hidden` και
`getMainContentSectionStyles()` έχει `flex: 1`. Δηλαδή **η flexbox είναι ήδη σωστή** — το
`min-w-0` (η κλασική αιτία του «δεν συρρικνώνεται») υπάρχει.

### 2.3 ⚠️ Δύο **νεκρά** συστήματα docking — ΜΗΝ τα αναστήσεις

| Αρχείο | Κατάσταση | Απόδειξη |
|---|---|---|
| `layout/CadDock.tsx` | **ΝΕΚΡΟ + ΣΠΑΣΜΕΝΟ** | κάνει `import { DockviewReact } from 'dockview'` — το **`dockview` ΔΕΝ είναι στο `package.json` και ΔΕΝ είναι εγκατεστημένο**. Δεν το εισάγει κανείς. Περνά απαρατήρητο επειδή το subapp εξαιρείται από το root `tsconfig` (ADR-663/ADR-719) |
| `systems/toolbars/ToolbarsContext.types.ts:33` | **ΝΕΚΡΟ** | έχει `docked: boolean` + `isToolbarDocked()`, αλλά `grep -rn "ToolbarsProvider" src` → **0 αποτελέσματα** |

> **Κανόνας μνήμης**: «feature σε unmounted container = ΝΕΚΡΟ». Και τα δύο είναι *σχόλια που
> μοιάζουν με αρχιτεκτονική*. Η ύπαρξή τους **δεν** αποτελεί επιχείρημα υπέρ του `dockview`.
> **Ενέργεια**: το `CadDock.tsx` προτείνεται προς **διαγραφή** (ξεχωριστό commit, εκτός αυτής της
> εργασίας — το working tree μοιράζεται με άλλον πράκτορα).

---

## 3. Τι κάνουν οι μεγάλοι παίχτες (τεκμηριωμένο)

| Εφαρμογή | Αγκύρωση | Αιώρηση | Πλάτος | Ιδιαιτερότητα |
|---|---|---|---|---|
| **Revit** | Project Browser + Properties σε **οποιαδήποτε άκρη**, στοιβαζόμενες ή δίπλα-δίπλα | ναι, ακόμη και **σε δεύτερη οθόνη** | ελεύθερο | **διπλό κλικ στη γραμμή τίτλου = dock/undock**· περίγραμμα-προεπισκόπηση κατά το σύρσιμο |
| **ArchiCAD** | παλέτες σε dock zones | ναι | ελεύθερο | *Work Environment* profiles = ονομασμένες διατάξεις |
| **Cinema 4D** | κάθε manager σε οποιαδήποτε άκρη ή ξεχωριστό παράθυρο | ναι | ελεύθερο | ονομασμένα **Layouts** με εναλλαγή |
| **Figma** | δεξί panel **σταθερό** (canvas-first) | όχι | πρόσφατα resizable | συνειδητή **αντίθετη** επιλογή |
| **VS Code** | primary/secondary sidebar | όχι | sash | **διπλό κλικ στο sash → «optimal width»** (όπως Sublime/Atom)· **δεν** έχει keyboard resize (ανοιχτό issue) |

**Συμπέρασμα Β:** το «resizable + dockable» **είναι** η πρακτική του κλάδου για CAD/BIM — δεν είναι
προσωπική προτίμηση. Το «σκέτο floating» **δεν** είναι: είναι επιλογή του χρήστη *πάνω* σε ένα
dock model. Άρα υλοποιούμε **και τα τρία**, με το docked ως προεπιλογή.

**Συμπέρασμα Γ (πού τους ξεπερνάμε):** το VS Code **δεν** έχει keyboard resize· εμείς το έχουμε
(§5.2). Καμία CAD εφαρμογή δεν προσαρμόζει το *περιεχόμενο* της παλέτας στο πλάτος της — εμείς
μπορούμε (§9.1).

> ⚠️ **ΔΙΟΡΘΩΣΗ (2026-07-28, μετρημένο ζωντανά).** Η αρχική διατύπωση εδώ έλεγε ότι το keyboard
> resize «το παίρνουμε **δωρεάν**». **Ήταν ψευδής.** Η βιβλιοθήκη όντως το υλοποιεί, αλλά στην
> **εφαρμογή μας δεν λειτουργούσε καθόλου**: με εστίαση στο διαχωριστικό, τα βέλη άφηναν το πλάτος
> **670 → 670** και αντ' αυτού **πάναραν το σχέδιο** ~80px/πάτημα.
>
> Αιτία (επαληθευμένη στο bundle, handler `Te`): ο listener του splitter είναι **element-level,
> φάση bubble**, και ξεκινά με `if (e.defaultPrevented) return;` — ενώ οι global accelerators του
> viewer τρέχουν σε **window capture**, δηλαδή **πρώτοι**. Έκαναν το pan και `preventDefault()`,
> οπότε το splitter δεν έβλεπε ποτέ το συμβάν.
>
> **Το μάθημα**: «η βιβλιοθήκη το υποστηρίζει» **δεν** σημαίνει «η εφαρμογή το έχει». Ανάμεσά τους
> στέκεται η ιδιοκτησία του πλήκτρου, και αυτή είναι **δική μας** ευθύνη (ADR-711). Διορθώθηκε με
> **τρίτη ερώτηση** στον υπάρχοντα φύλακα — βλ. §5.2 και ADR-711 §5.7.

---

## 4. Τι θα σπάσει αν το κάνουμε αφελώς (τα δύσκολα)

Το σύρσιμο είναι η εύκολη μισή ώρα. Αυτά είναι που ξεχωρίζουν το επαγγελματικό εργαλείο:

### 4.1 Ο κανόνας αγκύρωσης του 2D — υπάρχει για το **ύψος**, λείπει για το **πλάτος**

`hooks/canvas/useViewportManager.ts:134-142`:

```ts
const deltaHeight = height - oldHeight;
if (oldHeight > 0 && Math.abs(deltaHeight) > 0.5) {
  const newOffsetY = currentTransform.offsetY + deltaHeight;   // ⇐ αγκύρωση ΚΑΤΩ ακμής
}
```

Δηλαδή σε αλλαγή ύψους το σχέδιο μένει κολλημένο στην **κάτω** ακμή — συνεπές με τον χάρακα του
app (0.000m κάτω-αριστερά). Για το **πλάτος δεν υπάρχει αντίστοιχη γραμμή** — γιατί μέχρι σήμερα το
πλάτος **δεν άλλαζε ποτέ**. Συνέπεια, ακριβώς:

- **Αγκύρωση ΔΕΞΙΑ**: η αριστερή ακμή του καμβά δεν κουνιέται ⇒ το σχέδιο μένει **απόλυτα ακίνητο**. ✅
- **Αγκύρωση ΑΡΙΣΤΕΡΑ**: η αριστερή ακμή του καμβά μετακινείται κατά Δ ⇒ **το σχέδιο σέρνεται μαζί
  με την παλέτα**. Οπτικά λάθος για CAD.

**Απόφαση**: αντιστάθμιση με βάση τη μετατόπιση της **οθονο-χωρικής** αριστερής ακμής του
container (`offsetX -= Δ(rect.left)`), όχι με βάση το Δ πλάτους. Έτσι και οι δύο πλευρές
συμπεριφέρονται ίδια, και ο κανόνας παραμένει **ένας**. Αγγίζει `useViewportManager` ⇒
**ADR-040 CHECK 6B/6D: το ADR-040 πρέπει να μπει staged στο ίδιο commit.**

### 4.2 Η κλίμακα **απαγορεύεται** να αλλάξει

Το app εμφανίζει πραγματική κλίμακα σχεδίου `1:N` (ADR-418, `SidebarZoomLeaf`). Οποιοδήποτε
zoom-to-fit κατά το resize θα άλλαζε **εμφανιζόμενο αριθμό** χωρίς εντολή χρήστη. Ο κανόνας είναι
απόλυτος: **resize ⇒ αλλάζει μόνο η ορατή περιοχή, ποτέ το `scale`.**

### 4.3 Καταιγίδα resize κατά το σύρσιμο

Κάθε pixel κίνησης → `ResizeObserver` → (2D) πιθανό full-scene bitmap rebuild + (3D)
`renderer.setSize` + SSAO resize + `invalidateFrameCaches`. Με **3.107 στοιχεία** αυτό είναι το #1
ρίσκο αίσθησης. Τρεις στρατηγικές, κατά σειρά προτίμησης:

1. **Live + rAF coalescing** (Figma/C4D): μία ενημέρωση ανά frame. Το `ResizeObserver` *ήδη*
   παραδίδει μία φορά ανά frame — άρα ίσως αρκεί. **Μέτρησέ το πρώτα** με 3.107 οντότητες.
2. **Cheap-path κατά τη χειρονομία**: όσο `[data-resize-handle-active]`, ο 2D blit-άρει το
   υπάρχον bitmap τεντωμένο και κάνει **ένα** full redraw στο `pointerup`.
3. **Ghost splitter** (γραμμή κατά το σύρσιμο, ένα resize στο τέλος) — σίγουρο αλλά λιγότερο
   «ζωντανό». Εφεδρεία, όχι πρώτη επιλογή.

⛔ **ΜΗΝ** επιλέξεις (2) ή (3) «προληπτικά». Χωρίς μέτρηση είναι πρόωρη βελτιστοποίηση που
θυσιάζει την αίσθηση.

#### 4.3.1 ✅ ΜΕΤΡΗΘΗΚΕ (2026-07-28, ζωντανά, 3.107 οντότητες) — **η στρατηγική (1) ΑΡΚΕΙ**

Η πρώτη ζωντανή μέτρηση ανέφερε «σύρσιμο στα 10 FPS» και οδήγησε στο συμπέρασμα ότι το resize
είναι ακριβό. **Η μέτρηση ήταν σωστή· η απόδοση της αιτίας λάθος.** Έλειπε η **ομάδα ελέγχου**:

| Σενάριο | Διάμεσος καρέ | FPS |
|---|---|---|
| Σύρσιμο διαχωριστικού (90 `pointermove`) | **70,8 ms** | 14,1 |
| **Ηρεμία — κανένα input** | **71,1 ms** | 14,1 |
| Ίδιο app, **απλή σελίδα** (χωρίς viewer) | 17,3 ms | 57,8 |
| Viewer με **κρυμμένους** τους καμβάδες | 16,8 ms | 59,5 |

**Το σύρσιμο κοστίζει ~0.** Είναι ταυτόσημο με την ηρεμία. Ο viewer τρέχει στα ~14 FPS **ούτως ή
άλλως**, και το resize δεν προσθέτει μετρήσιμο φορτίο.

Επιπλέον, σε **4 δευτερόλεπτα ηρεμίας**: **μηδέν** `longtask` — το main thread είναι εντελώς
αδρανές. Το κόστος **δεν είναι JS**. Είναι η **σύνθεση 13–15 στοιβαγμένων καμβάδων πλήρους
viewport** (1286×697 ο καθένας): κρύβοντας οποιουσδήποτε ~μισούς τα καρέ πέφτουν 71 → ~42 ms,
δηλαδή το κόστος είναι **γραμμικό στο πλήθος** (~5 ms ανά καμβά ανά καρέ), χωρίς μεμονωμένο ένοχο.

**Απόφαση: η στρατηγική (2) *cheap-path* ΔΕΝ υλοποιείται.** Θα ήταν ακριβώς η πρόωρη
βελτιστοποίηση που απαγορεύει η παράγραφος από πάνω: θα πρόσθετε πολυπλοκότητα και θα θυσίαζε τη
ζωντανή αίσθηση, για να «διορθώσει» κόστος **που δεν υπάρχει**.

> 📌 **Το ζήτημα των ~14 FPS είναι υπαρκτό αλλά ΕΚΤΟΣ ΣΚΟΠΟΥ αυτού του ADR**: προϋπάρχει του dock
> (μετριέται με μηδέν αλληλεπίδραση) και ανήκει στο **ADR-040** (αρχιτεκτονική στοίβας καμβάδων).
> Μετρήθηκε σε **dev build**· το πόσο επιμένει σε production παραμένει ανοιχτό. **Το συμπέρασμα του
> §4.3 δεν εξαρτάται από αυτό**: είναι **σύγκριση μέσα στο ίδιο build** (70,8 vs 71,1), οπότε ο
> dev overhead ακυρώνεται στη διαφορά.

### 4.4 ADR-040 — πού **δεν** επιτρέπεται να ζήσει το πλάτος

Το πλάτος αλλάζει ~60 φορές/δευτ. κατά το σύρσιμο. **Απαγορεύεται** να μπει σε store στον οποίο
συνδρομούν οι orchestrators (`CanvasSection`, `CanvasLayerStack`) — θα ξαναρενδάριζε ~426 fibers ανά
pixel (τεκμηριωμένο ADR-040 Φ XXII.B). Το πλάτος ζει στο DOM (η βιβλιοθήκη γράφει `flex-grow`) και
στο store **μόνο** στο τέλος της χειρονομίας.

### 4.5 Το mobile drawer δεν αγγίζεται

`DxfViewerContent.tsx:373` → `layoutMode === 'desktop' ? <SidebarSection/> : <MobileSidebarDrawer/>`.
Το dock system είναι **desktop-only** (`≥1024px`). Σε tablet/mobile τίποτα δεν αλλάζει.

### 4.6 Fullscreen (ADR-241)

Ο `FullscreenOverlay` τυλίγει **μόνο** το `MainContentSection` — άρα σε fullscreen η παλέτα μένει
εκτός. Πρέπει να επιβεβαιωθεί ότι το `Group`/`Panel` δεν καταρρέει όταν το ένα του παιδί φεύγει σε
`position: fixed`. **Δοκίμασέ το ρητά** (§10 Φ1 checklist).

### 4.7 Δομικός περιορισμός της βιβλιοθήκης

> *«Separator elements must be direct DOM children of their parent Group elements.»*

Το σημερινό `<section className="flex flex-1 min-h-0">` (`DxfViewerContent.tsx:368`) γίνεται το
`Group`. Παιδιά του πρέπει να είναι **ακριβώς**: `Panel`(sidebar) · `Separator` · `Panel`(canvas).
Το `FullscreenOverlay` + `Suspense` μπαίνουν **μέσα** στο δεύτερο `Panel`.

---

## 5. Η βιβλιοθήκη: `react-resizable-panels@4.7.2` — τι μας δίνει **δωρεάν**

Ήδη εξάρτηση (MIT ✅ κανόνας N.5 δεν ενεργοποιείται), ήδη σε 3 σελίδες μέσω
`src/components/ui/resizable.tsx`.

### 5.1 ⚠️ API v4 ≠ API v2 — μην αντιγράψεις παλιά docs

| v2 (παλιά docs, παντού στο διαδίκτυο) | **v4.7.2 (αυτό που έχουμε)** |
|---|---|
| `PanelGroup` / `PanelResizeHandle` | **`Group`** / **`Separator`** |
| `direction="horizontal"` | **`orientation="horizontal"`** |
| ποσοστά μόνο | **αριθμός = pixels**, string χωρίς μονάδα = ποσοστό |

### 5.2 Οι ιδιότητες που κάνουν τη διαφορά

| Ιδιότητα | Γιατί την **χρειαζόμαστε** |
|---|---|
| `minSize={280}` / `maxSize={720}` (px) | CAD θέλει **pixel** όρια, όχι ποσοστά |
| `groupResizeBehavior="preserve-pixel-size"` | μεγαλώνει το παράθυρο ⇒ **ο καμβάς** παίρνει τον χώρο, η παλέτα κρατά τα px της. Ακριβώς Revit/VS Code |
| `collapsible` + `collapsedSize` | σύμπτυξη σε λωρίδα με ένα σύρσιμο (VS Code) |
| `Separator` | `role="separator"` + `aria-valuenow/min/max` + **πληκτρολόγιο** (`ArrowLeft/Right`, `Home`, `End`) — επαληθευμένο στο bundle. ⚠️ **ΔΕΝ αρκεί μόνο του** — δες §5.2.1 |
| `onLayoutChanged` | καλείται **μετά** την απελευθέρωση του δείκτη ⇒ **μία** εγγραφή ανά χειρονομία. Ίδιο δόγμα με το `onResizeEnd` του ADR-723 |
| `Panel.onResize(size)` | δίνει **και px και %** ⇒ αποθηκεύουμε px |

### 5.2.1 ⚠️ ΤΟ ΠΛΗΚΤΡΟΛΟΓΙΟ ΔΕΝ ΗΤΑΝ ΔΩΡΕΑΝ — η ιδιοκτησία του πλήκτρου είναι δική μας

Μετρημένο ζωντανά (Φ1, 3.107 οντότητες), με **πραγματικά** πλήκτρα και `document.activeElement`
επιβεβαιωμένα στο `DIV[role=separator]`:

| | Πριν τη διόρθωση | Μετά |
|---|---|---|
| Πλάτος μετά από `ArrowLeft` | **670 → 670** (τίποτα) | **604 → 487** ✅ |
| `offsetX` του καμβά | **+80px/πάτημα** (πάναρε το σχέδιο) | αλλάζει **μόνο** ως αντιστάθμιση §4.1 ✅ |

**Η αιτία δεν ήταν το splitter — ήταν η σειρά των φάσεων του DOM.** Ο handler της βιβλιοθήκης
(`Te`) είναι **element-level, φάση bubble**, και ξεκινά με `if (e.defaultPrevented) return;`. Οι
global accelerators του viewer τρέχουν σε **window capture** — πρώτοι. Ο ένας τους
(`useKeyboardShortcuts`, `PAN_STEP = 80`) έκανε pan **και** `preventDefault()`. Άρα **ένα** σφάλμα
παρήγαγε **δύο** συμπτώματα: το πλάτος δεν άλλαζε *και* το σχέδιο έφευγε.

**Η διόρθωση ζει στον ΕΝΑΝ φύλακα ιδιοκτησίας πλήκτρων** (`src/lib/a11y/keyboard-scope.ts`,
ADR-711/ADR-364), **όχι** εδώ. Το ADR-711 είχε **δύο** ονομασμένες ερωτήσεις — «γράφει κείμενο;»
και «θα καταναλώσει τον **χαρακτήρα**;». Καμία δεν κάλυπτε τα **βέλη**: το ίδιο του το σχόλιο
σημείωνε ρητά ότι `slider`/`radio`/`tab`/`grid` «πλοηγούνται με βέλη» αλλά τα **εξαιρούσε**, γιατί
δεν υπήρχε ερώτηση να τα στεγάσει. Προστέθηκε η **τρίτη**:

```
3. «Θα καταναλώσει το ΠΛΗΚΤΡΟ ΠΛΟΗΓΗΣΗΣ;» → consumesDirectionalKeys()
   = ερώτηση 2  +  ARROW_NAVIGATION_ROLES (separator, slider, scrollbar, radio*, tab*, grid*)
```

Το **πλήκτρο** επιλέγει την ερώτηση: πλοηγικό ⇒ ερώτηση 3· οτιδήποτε άλλο ⇒ ερώτηση 2. Έτσι με
εστίαση στο διαχωριστικό τα **βέλη** ανήκουν στο splitter, ενώ το «Z»/«L» φτάνουν κανονικά στον
viewer. Ένα ενιαίο πλατύτερο predicate θα σκότωνε τους accelerators γραμμάτων — παλινδρόμηση, όχι
διόρθωση.

⛔ **Καμία τοπική άμυνα στο `WorkspaceSplitLayout`.** Ένα `stopPropagation` στο διαχωριστικό δεν θα
έλυνε τίποτα: ο accelerator έχει **ήδη τρέξει** (capture) πριν το συμβάν φτάσει στο element.

### 5.3 ⛔ ΜΗΝ χρησιμοποιήσεις `useDefaultLayout`

Η βιβλιοθήκη προσφέρει `useDefaultLayout({ storage })` για persistence. **Το απορρίπτουμε**: θα
γινόταν **δεύτερος ιδιοκτήτης** του πλάτους, δίπλα στο δικό μας store — δηλαδή δύο αλήθειες τη
στιγμή της επαναφοράς. Ακριβώς το σχήμα που παράγει «η παλέτα πήδηξε πίσω» (το ίδιο λάθος που
απέφυγε ρητά το ADR-723 §useFloatingPanelGeometry). **Ένας** κάτοχος: το δικό μας store, μέσω
`defaultSize` (ανάγνωση) + `onResize`/`onLayoutChanged` (εγγραφή).

Επιπλέον, το `Layout` της βιβλιοθήκης είναι `{ [panelId]: number }` σε **ποσοστά** — μια
αποθηκευμένη διάταξη 25% δίνει άλλο πλάτος σε άλλη οθόνη. Τα CAD θυμούνται **px**.

---

## 6. Η αρχιτεκτονική

### 6.1 Δύο μηχανισμοί, **ένα** SSoT κατάστασης

Αγκυρωμένο και αιωρούμενο είναι **γεωμετρικά διαφορετικά προβλήματα** (μέσα στη ροή vs
`position: fixed`). Ένα ενιαίο engine θα ήταν χειρότερο και από τα δύο. Άρα:

```
                    ┌──────────────────────────────────────┐
                    │   workspace-dock-store  (ΤΟ SSoT)    │
                    │   { mode, dockedWidth }              │
                    └───────────┬──────────────┬───────────┘
                     mode=docked│              │mode=floating
                                ▼              ▼
              react-resizable-panels    ADR-723 FloatingPanel
              Group/Panel/Separator     useFloatingPanelGeometry
              (px, a11y, keyboard)      (drag, 8 λαβές, clamp)
              ↑ πλάτος                  ↑ {x,y,w,h}
              persist: το store μας     persist: ADR-723 v1 key
```

**Γιατί δύο κλειδιά persistence και όχι ένα**: τα πεδία **δεν επικαλύπτονται**. Το store μας κατέχει
«σε ποια λειτουργία είμαι + πόσο πλατιά είμαι όταν είμαι αγκυρωμένη». Το ADR-723 κατέχει «πού και
πόσο μεγάλη είμαι όταν αιωρούμαι» — και το κάνει ήδη σωστά (clamp σε κάθε ανάγνωση, διάσωση από
χαμένη-εκτός-οθόνης). Δεύτερη υλοποίηση του ίδιου clamp θα ήταν **διπλότυπο** (N.18/jscpd).

### 6.2 Νέα αρχεία

| Αρχείο | Ρόλος | Όριο |
|---|---|---|
| `systems/workspace/workspace-dock-geometry.ts` | **Καθαρές συναρτήσεις**, μηδέν React/DOM: `clampDockWidth`, `dockToFloatGeometry`, `floatToDockWidth`, `resolveDropTarget`. Πλήρως ελέγξιμο με jest χωρίς jsdom (καθρέφτης του `floating-panel-geometry.ts`) | ~150 |
| `systems/workspace/workspace-dock-store.ts` | Imperative store (zero React state) + persistence πάνω από `@/lib/storage`· `subscribe`/`getSnapshot` για **leaf** χρήση | ~120 |
| `systems/workspace/useWorkspaceDock.ts` | Το leaf hook (`useSyncExternalStore`) — **ΜΟΝΟ** σε components που επιτρέπεται να ξαναρενδάρουν | ~40 |
| `layout/WorkspaceSplitLayout.tsx` | Ο **μοναδικός** τόπος στο subapp που γνωρίζει `react-resizable-panels`. Σειρά παιδιών ανά `mode` | ~150 |
| `layout/WorkspaceDockMenu.tsx` | «Αγκύρωση αριστερά / δεξιά / Αιωρούμενο / Επαναφορά» | ~90 |

### 6.3 Τροποποιούμενα

| Αρχείο | Αλλαγή | Προσοχή |
|---|---|---|
| `layout/SidebarSection.tsx` | αφαίρεση `MIN/MAX_WIDTH`· `variant: 'inline' \| 'drawer' \| 'floating'` | το `w-96` γίνεται προεπιλογή του Panel, όχι κλείδωμα |
| `app/DxfViewerContent.tsx:368-399` | το `<section>` → `WorkspaceSplitLayout` | §4.7 direct-child |
| `hooks/canvas/useViewportManager.ts` | αντιστάθμιση `offsetX` (§4.1) | **ADR-040 staged** (CHECK 6B) |
| `config/panel-tokens.ts` | νέα `WORKSPACE_DOCK` όρια | τα `SIDEBAR_MIN/MAX_WIDTH` **μένουν** (τα χρησιμοποιεί το drawer) |
| `i18n/locales/{el,en}/dxf-viewer-shell.json` | `workspaceDock.*` | **πρώτα τα κλειδιά, μετά ο κώδικας** (N.11) |

### 6.4 Οι τιμές

| Σταθερά | Τιμή | Αιτιολόγηση |
|---|---|---|
| `DOCK_WIDTH_DEFAULT` | **384** | το σημερινό ⇒ **μηδενική οπτική αλλαγή** στην πρώτη φόρτωση |
| `DOCK_WIDTH_MIN` | **280** | ίδιο με `DEFAULT_MIN_PANEL_SIZE.width` του ADR-723 — **μία** έννοια «στενότερο λειτουργικό πλάτος» σε όλη την εφαρμογή |
| `DOCK_WIDTH_MAX` | **720** | πάνω από αυτό ο καμβάς παύει να είναι ο πρωταγωνιστής |
| `CANVAS_MIN_WIDTH` | **320** | ο καμβάς **ποτέ** κάτω από αυτό· υπερισχύει του `DOCK_WIDTH_MAX` σε στενές οθόνες |

---

## 7. Ο κανόνας μετάβασης (το κομμάτι που ξεχνιέται)

Ζει **αποκλειστικά** στο `workspace-dock-geometry.ts`, ως καθαρές συναρτήσεις:

| Μετάβαση | Κανόνας |
|---|---|
| **docked → floating** | Αν υπάρχει αποθηκευμένη floating γεωμετρία (ADR-723) ⇒ **αυτή νικά** (επιστρέφει εκεί που την άφησες — συμπεριφορά Revit). Αλλιώς: `{x,y,w,h}` = το **τρέχον** rect της αγκυρωμένης παλέτας, ελαφρώς μετατοπισμένο ⇒ η μετάβαση φαίνεται **φυσική**, όχι τηλεμεταφορά |
| **floating → docked** | `dockedWidth = clampDockWidth(floatingWidth)`· η πλευρά προκύπτει από το **πού αφέθηκε** (§7.1) |
| **αλλαγή πλευράς** | το `dockedWidth` **δεν** αλλάζει |
| **επαναφορά** | `clearPanelGeometry()` (ADR-723) + `DOCK_WIDTH_DEFAULT` + `docked-left` = «Reset palette locations» του AutoCAD |

### 7.1 Πού «πέφτει» η παλέτα (κανόνας Revit)

Κατά το σύρσιμο μιας **αιωρούμενης** παλέτας, αν ο δείκτης μπει σε ζώνη **64px** από την αριστερή ή
δεξιά ακμή του χώρου εργασίας ⇒ εμφανίζεται **περίγραμμα-προεπισκόπηση** της αγκύρωσης· στο
`pointerup` αγκυρώνει. Αλλιώς μένει αιωρούμενη. Καθαρή συνάρτηση: `resolveDropTarget(pointerX, rect)`.

---

## 8. Χειρονομίες & προσβασιμότητα

| Χειρονομία | Αποτέλεσμα | Πηγή |
|---|---|---|
| Σύρσιμο διαχωριστικού | αλλαγή πλάτους, live | κλάδος |
| **Διπλό κλικ διαχωριστικού** | επαναφορά στο **`defaultSize`** του panel — δηλαδή στο πλάτος με το οποίο **άνοιξε η συνεδρία** (το αποθηκευμένο), όχι στο `DOCK_WIDTH_DEFAULT` | VS Code / Sublime / Atom |
| **Διπλό κλικ επικεφαλίδας** | dock ⇄ float | **Revit** |
| `ArrowLeft/Right`, `Home`, `End` στο διαχωριστικό | αλλαγή πλάτους από πληκτρολόγιο | βιβλιοθήκη **+ ο φύλακας ιδιοκτησίας πλήκτρων** (§5.2.1)· το VS Code ΔΕΝ το έχει |
| Δεξί κλικ επικεφαλίδας | μενού: αριστερά / δεξιά / αιωρούμενο / επαναφορά | Revit / C4D |

⚠️ Το πληκτρολόγιο **δεν** πρέπει να συγκρουστεί με τον Escape bus / `keyboard-scope` (ADR-364 /
ADR-711): το διαχωριστικό είναι εστιάσιμο στοιχείο — τα βελάκια το αφορούν **μόνο όσο έχει εστίαση**.

### 8.1 ⚠️ ΔΙΟΡΘΩΣΗ ΤΗΣ ΕΡΕΥΝΑΣ (Φ1) — το διπλό κλικ **ανήκει ήδη στη βιβλιοθήκη**

Η αρχική γραφή αυτού του πίνακα υπέθετε ότι το διπλό κλικ θα το υλοποιούσαμε εμείς. **Δεν
ισχύει.** Το `react-resizable-panels@4` εγγράφει δικό του listener `dblclick` σε **capture**
φάση στο `document`, που επαναφέρει το panel στο `defaultSize` του και κάνει `preventDefault()`.

Συνέπειες που **δεν** ήταν προφανείς πριν από τον κώδικα:

1. **Δεν μπορεί να ακυρωθεί από εμάς.** Capture στο `document` σημαίνει ότι τρέχει **πριν** από
   οποιονδήποτε React handler (bubble στο root). Ένα δικό μας `resize()` θα ήταν **δεύτερος
   ιδιοκτήτης**: το panel θα άλλαζε μέγεθος **δύο φορές** ανά διπλό κλικ.
2. **Το `defaultSize` έχει διπλή σημασία by design**: αρχικό πλάτος **και** στόχος του διπλού
   κλικ. Επειδή του δίνουμε το αποθηκευμένο πλάτος, η χειρονομία σημαίνει «επαναφορά στο
   αποθηκευμένο μου» (*revert to saved*, τύπου Photoshop) — **όχι** «επιστροφή στο 384».
3. Άρα ο ρόλος μας περιορίζεται στο να **ακολουθήσει** το store ό,τι εφάρμοσε η βιβλιοθήκη
   (σημείωση πρόθεσης χρήστη ⇒ εγγραφή στο `onLayoutChanged`). Κανένας δεύτερος μηχανισμός.

Το «επαναφορά στο `DOCK_WIDTH_DEFAULT`» παραμένει επιθυμητό ως **ρητή εντολή μενού**
(«Επαναφορά», §7) — δηλαδή **Φ2**, μαζί με τον πρώτο πραγματικό καλούντα της.

---

## 9. Πάνω από τον κλάδο (τι **δεν** κάνει κανείς τους)

### 9.1 Container queries στο περιεχόμενο της παλέτας — **η πραγματικά προηγμένη ιδέα**

Σε Revit/ArchiCAD/C4D, όταν στενεύεις μια παλέτα το περιεχόμενο απλώς **κόβεται ή κυλά**. Με
`container-type: inline-size` στο `<aside>` και `@container` κανόνες, το περιεχόμενο **προσαρμόζεται
στο δικό του πλάτος** (όχι του viewport):

- `< 320px` → οι καρτέλες γίνονται **μόνο εικονίδια**
- `320–520px` → η σημερινή μονόστηλη μορφή
- `> 520px` → οι λίστες ιδιοτήτων σε **δύο στήλες**

Αυτό είναι το αναγνωρισμένο killer use case των container queries και έχει **καθολική** υποστήριξη
browser από το 2026. ⚠️ Tailwind **v3.4** εδώ ⇒ δεν υπάρχει built-in `@container`. **Πρώτη επιλογή:
σκέτο CSS** στο υπάρχον stylesheet του subapp — **μηδέν νέα εξάρτηση** (ο N.5 δεν ενεργοποιείται).
Το `@tailwindcss/container-queries` (MIT) είναι εφεδρεία, όχι προτίμηση.

### 9.2 View Transitions για τη μετάβαση dock ⇄ float

`document.startViewTransition()` με κοινό `view-transition-name` ⇒ η παλέτα **μεταμορφώνεται**
αντί να εξαφανίζεται και να εμφανίζεται αλλού. Καμία CAD εφαρμογή δεν το κάνει (είναι native
δυνατότητα browser). Progressive enhancement: όπου δεν υποστηρίζεται, απλή εναλλαγή.
⚠️ **Υποχρεωτικός** φύλακας `prefers-reduced-motion` — υπάρχει ήδη πρότυπο στο
`bim-3d/accessibility/use-reduced-motion.ts` (αν χρειαστεί σε κοινό επίπεδο, **μετακόμισε** το, μην
το αντιγράψεις — N.18).

### 9.3 Διπλό κλικ = «βέλτιστο πλάτος»

Το VS Code κάνει reset· Sublime/Atom υπολογίζουν το **ελάχιστο πλάτος που χωρά όλο το περιεχόμενο**
(`scrollWidth` του περιεχομένου, clamped). Το δεύτερο είναι σαφώς καλύτερο και μετρήσιμο.

**Ιεράρχηση:** το §9 είναι **Φάση 4**. Δεν μπαίνει τίποτα από αυτά πριν δουλέψουν σωστά τα §5–§7.

---

## 10. Σχέδιο υλοποίησης (φάσεις — κάθε μία αποστέλλεται μόνη της)

### Φ0 — Θεμέλια (καμία οπτική αλλαγή)
1. i18n κλειδιά `workspaceDock.*` σε **el + en** (N.11: πρώτα τα κλειδιά).
2. `workspace-dock-geometry.ts` + jest suite (καθαρές συναρτήσεις, χωρίς jsdom).
3. `workspace-dock-store.ts` + persistence + jest.
4. Σταθερές στο `panel-tokens.ts`.
> **Έξοδος**: πράσινα tests, μηδέν αλλαγή στο UI.

### Φ1 — Αγκυρωμένη αριστερά, **με αλλαγή πλάτους** ← *το 80% της αξίας*
1. `WorkspaceSplitLayout.tsx` (Group / Panel / Separator, px όρια, `preserve-pixel-size`).
2. Ξεκλείδωμα `SidebarSection` (αφαίρεση `MIN/MAX`).
3. Σύνδεση στο `DxfViewerContent` (§4.7 direct-child).
4. Αντιστάθμιση `offsetX` στο `useViewportManager` (§4.1) — **ADR-040 staged**.
5. ✅ **Μέτρηση** FPS κατά το σύρσιμο με `47_ergasia.dxf` (3.107 στοιχεία) ⇒ απόφαση §4.3 —
   **έγινε**, βλ. **§4.3.1**: το σύρσιμο κοστίζει ~0 ⇒ **καμία** cheap-path.
6. ✅ Έλεγχος: fullscreen (§4.6) · 3D viewport · χάρακες · crosshair · hit-test — **όλα πέρασαν**
   ζωντανά (§14.2). ⚠️ **mobile drawer (§4.5): ΑΝΕΛΕΓΚΤΟ ζωντανά** — τα εργαλεία δεν μεταβάλλουν
   το `innerWidth`· καλύπτεται από 2 jest tests + τον ρητό κλάδο `split={layoutMode==='desktop'}`.
7. ✅ **Πληκτρολόγιο** (δεν ήταν στο αρχικό σχέδιο — αποδείχθηκε ότι *δεν* ερχόταν δωρεάν): §5.2.1.
> **Έξοδος**: ο Giorgio σέρνει το διαχωριστικό και οι δύο καμβάδες ακολουθούν. ✅ **ΕΠΙΤΕΥΧΘΗΚΕ**
> (και με τα βέλη του πληκτρολογίου).

### Φ2 — Αγκύρωση δεξιά ✅ **ΑΠΕΣΤΑΛΗ (2026-07-29)**
~~Αντιστροφή σειράς παιδιών + μενού + persistence. Μικρή, χαμηλού ρίσκου.~~

🔴 **Η πρόβλεψη ήταν λάθος — δες §14.5.1.** Η αντιστροφή σειράς είναι όντως μία γραμμή, αλλά
κρύβει ότι **η αλλαγή πλευράς μετακινεί τον καμβά χωρίς να τον αλλάζει σε μέγεθος**, και ο
`ResizeObserver` σιωπά σε καθαρή μετατόπιση. Τρία σιωπηλά ελαττώματα: άλμα σχεδίου 392px στο
επόμενο σύρσιμο, hit-test με λάθος origin επί 5s, και **απώλεια WebGL context** αν λείπουν τα
`key`. Παραδόθηκε με: `mode` στο store, γραμμή τίτλου + μενού, «Επαναφορά» όλης της διάταξης.

### Φ3 — Αιωρούμενη ✅ **ΑΠΕΣΤΑΛΗ (2026-07-29)**
~~`FloatingPanel` (ADR-723) με `resizable` + `persistenceKey="dxf.workspace-sidebar"` + κανόνες
μετάβασης §7 + drop zones §7.1 + z-index `dxfZIndex.overlays.sidebar`.~~

Παραδόθηκε όπως σχεδιάστηκε, με **δύο διορθώσεις του ίδιου του σχεδίου** (§14.8):

1. 🔴 **Το `dxfZIndex.overlays.sidebar` ΔΕΝ ΥΠΑΡΧΕΙ** — και δεν χρειαζόταν. Το `overlays` δεν
   έχει `sidebar` (μόνο `selection/crosshair/snap/cursor/zoom`)· το σωστό όνομα είναι
   `dxfZIndex.ui.sidebar` = 1110. Αλλά η **ίδια η παλέτα** δεν το χρησιμοποιεί: το
   `FloatingPanel` του ADR-723 φέρει ήδη `zIndex: 1700` από τα design tokens του overlay. Ένα
   δεύτερο z-index εδώ θα ήταν δεύτερος ιδιοκτήτης. Το `ui.sidebar` το χρησιμοποιεί **μόνο** το
   περίγραμμα-προεπισκόπηση, που *πρέπει* να είναι κάτω από την παλέτα που σέρνεται.
2. 🔴 **Το `floatToDockWidth` του §6.2 δεν γράφτηκε ποτέ** — κατέρρευσε στο `clampDockWidth`.
   Ήταν, κυριολεκτικά, το ίδιο κατώφλι με άλλο όνομα· δύο ονόματα για έναν κανόνα αποκλίνουν.

### Φ4 — Πάνω από τον κλάδο
~~§9.1 container queries~~ ✅ **ΑΠΕΣΤΑΛΗ (2026-07-29, §14.6)** → §9.3 βέλτιστο πλάτος → §9.2 view
transitions (**και τα δύο NOT IMPLEMENTED**).

🔴 **Η ιεράρχηση «το §9 είναι τελευταίο» ήταν λάθος.** Το §9.1 δεν ήταν στολίδι: χωρίς αυτό η
Φ1/Φ2 παρέδιδαν πλάτος **που δεν αξιοποιούνταν**. Ο Giorgio το διατύπωσε ακριβώς: *«αν δεν
ξεδιπλώνονται οι καρτέλες, είναι άσκοπο να μεγαλώνει το πάνελ»*. Μια δυνατότητα αλλαγής μεγέθους
χωρίς προσαρμογή περιεχομένου είναι **μισή δυνατότητα**.

---

## 11. Ανοιχτά ερωτήματα (ειλικρινώς: δεν τα ξέρω ακόμη)

1. **Απόδοση §4.3 — ✅ ΕΚΛΕΙΣΕ (2026-07-28), με μέτρηση.** Η στρατηγική **(1) live + rAF
   coalescing** επαληθεύτηκε **επαρκής**: το σύρσιμο κοστίζει ~0 (70,8 ms vs 71,1 ms σε ηρεμία).
   Η (2)/(3) **δεν** υλοποιείται. Πλήρη νούμερα και μεθοδολογία: **§4.3.1**.
   ➜ Αυτό που **έμεινε ανοιχτό** είναι άλλο ερώτημα, **εκτός ADR-724**: ο viewer τρέχει στα ~14 FPS
   ακόμη και σε πλήρη ηρεμία (μηδέν `longtask`· κόστος γραμμικό στο πλήθος των στοιβαγμένων
   καμβάδων). Ανήκει στο **ADR-040**, μετρήθηκε σε dev build.
2. **Fullscreen × Group** (§4.6) — ✅ **ΑΠΑΝΤΗΘΗΚΕ ΣΤΗ Φ1, δομικά.** Ο `FullscreenOverlay`
   (`src/core/containers/FullscreenOverlay.tsx:149`) κάνει `createPortal` στο `document.body`:
   μετακινείται το **περιεχόμενο** του δεύτερου panel, **όχι** το panel. Άρα τα άμεσα παιδιά του
   `Group` παραμένουν `Panel · Separator · Panel` σε **κάθε** κατάσταση — ο περιορισμός §4.7 δεν
   μπορεί να παραβιαστεί από το fullscreen. Καλύπτεται από test (§14). Παραμένει σκόπιμο ένα
   οπτικό πέρασμα, αλλά το δομικό ρίσκο έκλεισε.
3. **Πλάτος ανά λειτουργία;** Ο χρήστης ίσως θέλει διαφορετικό πλάτος ανά καρτέλα (Επίπεδα στενή,
   Ιδιότητες πλατιά). Το Revit **δεν** το κάνει. Προτείνω **όχι** — αλλά είναι απόφαση του Giorgio.
4. **Ανά χρήστη ή ανά έργο;** Το ADR-723 απάντησε **ανά χρήστη** (localStorage) με τεκμηρίωση
   (Revit/AutoCAD/Photoshop αποθηκεύουν διάταξη στο προφίλ, ποτέ στο αρχείο). Το ίδιο εδώ, εκτός αν
   ο Giorgio θέλει «Work Environment profiles» τύπου ArchiCAD (μελλοντικό ADR).
5. **Σχόλιο τεκμηρίωσης προς έλεγχο**: το `floating-panel-geometry.ts:59` αποδίδει το «Minimum
   Visible Header» στο **ADR-030**, αλλά το `ADR-030-unified-frame-scheduler.md` αφορά άλλο θέμα.
   Πιθανό λάθος παραπομπής του ADR-723 — να επαληθευτεί και να διορθωθεί (χαμηλή προτεραιότητα).

---

## 12. Πύλες (gates) που πρέπει να περάσουν

| Πύλη | Τι απαιτεί |
|---|---|
| **ADR-040 CHECK 6B/6D** | αγγίζοντας `useViewportManager` / canvas ⇒ **υποχρεωτικά staged το ADR-040** αλλιώς μπλοκάρει το commit |
| **N.11 / CHECK 3.8** | κάθε νέο `t('…')` έχει κλειδί σε **el + en** πριν τον κώδικα |
| **N.7.1** | κανένα αρχείο >500 γρ., καμία συνάρτηση >40 γρ. |
| **N.3** | καμία inline style — **εξαίρεση** η δυναμική γεωμετρία (προηγούμενο ADR-723) |
| **N.18 / CHECK 3.28** | `npm run jscpd:diff <staged>` πριν πεις «τελείωσα». Ο κίνδυνος εδώ είναι **αντιγραφή του clamp** του ADR-723 |
| **N.17** | ❌ **κανένα `tsc`** από τον πράκτορα |
| **N.(-1)** | ❌ **κανένα commit/push** χωρίς εντολή Giorgio |
| **N.2 / N.5** | μηδέν `any`· καμία νέα εξάρτηση (η μόνη υποψήφια, `@tailwindcss/container-queries`, είναι MIT αλλά **αποφεύγεται**) |

---

## 13. Απόφαση

**Υιοθετούμε το μοντέλο τριών λειτουργιών** (`docked-left` / `docked-right` / `floating`) με
**δύο μηχανισμούς και ένα SSoT κατάστασης**:

- **αγκυρωμένη** → `react-resizable-panels@4.7.2` (υπάρχουσα MIT εξάρτηση· px όρια· WAI-ARIA
  splitter με πληκτρολόγιο· `preserve-pixel-size`),
- **αιωρούμενη** → `FloatingPanel` / `useFloatingPanelGeometry` (ADR-723, αναλλοίωτο),
- **κατάσταση** → `workspace-dock-store` (ένας κάτοχος· `useDefaultLayout` απορρίπτεται ρητά).

Οι καμβάδες **δεν χρειάζονται νέα υποδομή** — ο `ResizeObserver` τους είναι ήδη σωστός. Χρειάζονται
**έναν** νέο κανόνα: αντιστάθμιση `offsetX` (§4.1), ώστε η αριστερή αγκύρωση να είναι οπτικά τόσο
σταθερή όσο η δεξιά.

---

## 14. Changelog

| Ημ/νία | Αλλαγή |
|---|---|
| 2026-07-28 (γ) | **Φ1 ΔΙΟΡΘΩΣΕΙΣ μετά από ζωντανή επαλήθευση.** Βλ. §14.2 |
| 2026-07-28 (β) | **Φ0 + Φ1 IMPLEMENTED.** Βλ. αναλυτικά §14.1 παρακάτω |
| 2026-07-28 | **Δημιουργία** — SSoT audit (grep) + έρευνα κλάδου. Κατάσταση: **RESEARCHED, NOT IMPLEMENTED**. Ευρήματα: (α) το μόνο εμπόδιο είναι `min=max=384` στο `SidebarSection.tsx:56-60`· (β) και οι δύο καμβάδες ακολουθούν **ήδη** αυτόματα· (γ) δύο **νεκρά** συστήματα docking (`CadDock`+ακατάστατο `dockview`, `ToolbarsContext`) — να μην αναστηθούν· (δ) το `react-resizable-panels` v4 δίνει px όρια + keyboard splitter δωρεάν· (ε) ο κανόνας αγκύρωσης του 2D υπάρχει για το **ύψος** και λείπει για το **πλάτος** |

### 14.1 Φ0 + Φ1 — τι απεστάλη (2026-07-28)

**Νέα αρχεία**

| Αρχείο | Ρόλος | Γρ. |
|---|---|---|
| `systems/workspace/workspace-dock-geometry.ts` | `clampDockWidth` / `parseDockWidth` — καθαρές, μηδέν React/DOM | 55 |
| `systems/workspace/workspace-dock-store.ts` | Ο **ένας** κάτοχος του αποθηκευμένου πλάτους | 82 |
| `layout/WorkspaceSplitLayout.tsx` | Ο μοναδικός τόπος του subapp που ξέρει από split panes | ~175 |
| `systems/workspace/__tests__/*` (2) | 39 tests | — |
| `layout/__tests__/WorkspaceSplitLayout.test.tsx` | 8 tests (δομή §4.7, a11y, split=false passthrough) | — |
| `hooks/canvas/__tests__/anchor-transform-on-resize.test.ts` | 15 tests (§4.1) | — |

**Τροποποιημένα**: `SidebarSection.tsx` (ξεκλείδωμα πλάτους) · `MobileSidebarDrawer.tsx` ·
`app/DxfViewerContent.tsx` (σύνδεση) · `hooks/canvas/useViewportManager.ts` (§4.1 + ενοποίηση
3 μετρήσεων) · `config/panel-tokens.ts` (`WORKSPACE_DOCK`) · `utils/storage-utils.ts` (κλειδί) ·
`components/ui/resizable.tsx` (re-export τύπου) · `i18n/locales/{el,en}/dxf-viewer-shell.json`
(`workspaceDock.*`) · **`ADR-040` changelog** (CHECK 6B/6D).
**Σύνολο: 62 tests πράσινα. jscpd (N.18): καθαρό στα 10 αρχεία.**

**Πέντε σημεία όπου η υλοποίηση ΔΙΑΦΩΝΗΣΕ με την έρευνα** (ο κώδικας είναι η αλήθεια, N.0.1):

1. **Το `mode` ΔΕΝ μπήκε στο store** (§6.1 το προέβλεπε). Στη Φ1 έχει **μία** δυνατή τιμή
   (`docked-left`) — πεδίο με μία τιμή είναι νεκρός κώδικας, όχι αρχιτεκτονική. Μπαίνει στη Φ2
   μαζί με τον πρώτο αναγνώστη του. Το store της Φ1 είναι **σκέτο πλάτος**.
2. **Το διπλό κλικ ανήκει στη βιβλιοθήκη** — βλ. §8.1. Ακυρώνει τη γραμμή «επαναφορά στο
   `DOCK_WIDTH_DEFAULT`» του §8 **και** το `resetDockedWidth` που είχε προγραμματιστεί.
3. **Το `CANVAS_MIN_WIDTH` ΔΕΝ επιβάλλεται στο `workspace-dock-geometry`** (§6.2 το υπαινισσόταν)
   αλλά ως `minSize` στο **panel του καμβά**: εκεί είναι το μόνο σημείο που γνωρίζει το
   διαθέσιμο πλάτος τη στιγμή του συρσίματος. Το store δεν βλέπει viewport — αν το αντέγραφε,
   θα διαφωνούσαν σε κάθε αλλαγή μεγέθους παραθύρου.
4. **Το `SidebarSection.variant` ΑΦΑΙΡΕΘΗΚΕ** (§6.3 προέβλεπε επέκτασή του σε `'floating'`).
   Μετά την απελευθέρωση του πλάτους, οι κλάδοι `inline`/`drawer` παρήγαγαν **ταυτόσημο**
   markup ⇒ διακόπτης που δεν ανάβει τίποτα. Επιστρέφει στη Φ3, όταν αποκτήσει νόημα.
5. **Το subapp ΔΕΝ εισάγει το `react-resizable-panels`** (§6.2 έλεγε «ο μοναδικός τόπος που
   γνωρίζει τη βιβλιοθήκη»). Περνά από το **υπάρχον** shadcn wrapper
   `@/components/ui/resizable` — ήδη σε 3 σελίδες. Ένα σημείο επαφής σε **ολόκληρη** την
   εφαρμογή αντί για δύο.

**Δύο ευρήματα προς μελλοντική εργασία** — ✅ **και τα δύο έκλεισαν στο §14.4 (2026-07-29)**:

- `src/components/ui/resizable.tsx` περιέχει **νεκρούς v2 selectors**: το
  `[&[data-resize-handle-active]]:bg-ring` και τα `data-[panel-group-direction=vertical]:*`
  **δεν αντιστοιχούν σε τίποτα στην v4** — η v4 εκπέμπει `data-separator` με τιμές
  `inactive` / `hover` / `active` / `disabled` και `data-group`. Δηλαδή οι 3 υπάρχουσες σελίδες
  **δεν έχουν οπτική ένδειξη ενεργού συρσίματος** και το νομίζουν. Το `WorkspaceSplitLayout`
  παρακάμπτει με σωστούς v4 selectors στο δικό του className.
- `layout/CadDock.tsx` παραμένει **νεκρό + σπασμένο** (`import 'dockview'`, μη εγκατεστημένο) —
  §2.3. Προτείνεται διαγραφή σε **ξεχωριστό** commit.

---

### 14.2 Φ1 — διορθώσεις μετά τη ζωντανή επαλήθευση (2026-07-28 γ)

Η Φ1 απεστάλη με **62 πράσινα tests** και **δύο πραγματικά ελαττώματα** που κανένα test δεν έπιασε.
Και τα δύο ήταν αόρατα σε jsdom (δεν κάνει διάταξη, δεν εκτελεί φάσεις πληκτρολογίου).

| # | Ελάττωμα | Ρίζα | Διόρθωση |
|---|---|---|---|
| **Ε1** | «σύρσιμο στα 10 FPS» | **δεν ήταν ελάττωμα** — έλειπε η ομάδα ελέγχου· η ηρεμία μετρά τα ίδια | καμία· §4.3.1 τεκμηριώνει γιατί η (2) **δεν** μπαίνει |
| **Ε2** | βέλη: πλάτος 670→670 **και** το σχέδιο πάναρε 80px | window **capture** accelerator πριν τον **bubble** handler της βιβλιοθήκης (`defaultPrevented`) | **τρίτη ερώτηση** στον φύλακα ADR-711 (§5.2.1) |
| **Ε3** *(νέο — βρέθηκε κατά την επαλήθευση του Ε2)* | το αποθηκευμένο πλάτος ήταν **ένα βήμα πίσω** (DOM 487,2 · store 604) | το `onLayoutChanged` καλείται **πριν** εφαρμοστεί η διάταξη ⇒ **και** το `onResize` ref **και** το DOM είναι μπαγιάτικα | η εγγραφή αναβάλλεται **ένα καρέ** (`requestAnimationFrame`), μετά διαβάζεται το DOM |
| **Ε4** *(νέο — βρέθηκε κατά την επαλήθευση του Ε3)* ✅ **επαληθευμένο ζωντανά 2026-07-28 δ** | με **πλήκτρο** το πλάτος άλλαζε (531,1 → 647,5) αλλά **καμία εγγραφή δεν γινόταν ποτέ** ⇒ το keyboard resize ξεχνιόταν στο επόμενο άνοιγμα· με **σύρσιμο** έγραφε κανονικά | το `onLayoutChanged` τεκμηριώνεται ως «**μετά την απελευθέρωση του δείκτη**» — η χειρονομία του πληκτρολογίου **δεν έχει** απελευθέρωση δείκτη | η εγγραφή σκανδαλίζεται **και** από το `onKeyDown`: **μία** συνάρτηση (`schedulePersist`), **δύο** σκανδάλες. Το rAF μέσα της ακυρώνει την προηγούμενη ⇒ αν πυροδοτηθούν και τα δύο μονοπάτια, η εγγραφή γίνεται **μία** φορά |

> **Γιατί το Ε3 κρυβόταν**: στο **σύρσιμο** το `onResize` έχει ήδη τρέξει ~60 φορές πριν σηκωθεί ο
> δείκτης, οπότε η «προηγούμενη» τιμή τύχαινε να είναι η σωστή. Μόνο το **πληκτρολόγιο** — μία
> διακριτή αλλαγή ανά πάτημα — το εμφάνισε γυμνό. Δηλαδή **η διόρθωση του Ε2 αποκάλυψε το Ε3**, και
> η επαλήθευση του Ε3 αποκάλυψε το **Ε4**. Τρία διαδοχικά ελαττώματα στο **ίδιο** μονοπάτι, κανένα
> ορατό χωρίς το προηγούμενο.

> ⚠️ **ΤΟ Ε4 ΔΕΝ ΕΙΝΑΙ ΕΛΕΓΞΙΜΟ ΣΕ JSDOM — και δεν γράφτηκε ψεύτικο test γι' αυτό.** Ο **native**
> keydown listener της βιβλιοθήκης τρέχει πριν από τον δικό μας (React delegated) και σε jsdom πετά
> «Previous layout not found»: δεν υπάρχει διάταξη να μεταβάλει. Μετρημένο: όταν πετάει, ο handler
> μας **δεν τρέχει καθόλου**, ούτε με `try/catch`. Κάθε assertion εκεί θα μετρούσε **το jsdom**.
> Ελέγχεται το συμπληρωματικό («`Tab` δεν γράφει») + **ζωντανά** σε **ενεργή** καρτέλα.
>
> 📌 **Παγίδα μέτρησης (ίδια με ADR-711 §7.2):** σε καρτέλα με `visibilityState: 'hidden'` το
> `requestAnimationFrame` **παγώνει**, άρα η αναβεβλημένη εγγραφή δεν τρέχει ποτέ και η μέτρηση
> δείχνει ψευδώς «δεν αποθηκεύει». **Έλεγξε `document.visibilityState` πριν πιστέψεις τη μέτρηση.**

**Τι επαληθεύτηκε ζωντανά** (Chrome, `47_ergasia.dxf`, 3.107 οντότητες, πραγματικά πλήκτρα):

| Έλεγχος (§10 Φ1 checklist) | Απόδειξη |
|---|---|
| Πλήκτρα αλλάζουν πλάτος | 408→280, 604→487, 487→604, 720→604 ✅ |
| Το σχέδιο **δεν** πανάρει | `canvasLeft + offsetX` = **1114,58 → 1114,17** (μόνο στρογγυλοποίηση) |
| **§4.1 + hit-test + crosshair + χάρακες** | ίδιο σημείο **οθόνης** πριν/μετά resize ⇒ world **ταυτόσημο**: `X: 240,3927, Y: 293,2950 m` |
| Αποθήκευση — **σύρσιμο** | store == DOM (531,1 == 531,11) ✅ |
| Αποθήκευση — **πλήκτρο** | ✅ **επαληθευμένο 2026-07-28 δ** (δες §14.2.1) |
| **Διπλό κλικ** | 720 → **487,2** = το πλάτος έναρξης συνεδρίας ⇒ επιβεβαιώνει το §8.1 *revert-to-saved* |
| **3D viewport** | 13 → **15** καμβάδες· όλοι ακολουθούν το νέο πλάτος (`1841`)· το ViewCube 160×160 μένει σταθερό **by design** |
| **Fullscreen** (§4.6) | παιδιά του `Group` παραμένουν `panel·separator·panel`· η παλέτα μένει εκτός overlay· οπτικά καθαρό |
| **Mobile drawer** (§4.5) | ⚠️ **ΔΕΝ επαληθεύτηκε ζωντανά** — το `resize_window` δεν μεταβάλλει το `innerWidth` (μένει 2400 ενώ `outerWidth`=1920). Καλύπτεται από 2 jest tests (`split={false}` ⇒ fragment passthrough, μηδέν separator) και από τον ρητό κλάδο `split={layoutMode === 'desktop'}` |

**Τι άλλαξε στον κώδικα**

| Αρχείο | Αλλαγή |
|---|---|
| `src/lib/a11y/keyboard-scope.ts` | **+ ερώτηση 3**: `ARROW_NAVIGATION_ROLES`, `DIRECTIONAL_KEYS`, `isDirectionalKey`, `consumesDirectionalKeys`, `focusConsumesDirectionalKeys`· το `shouldGlobalShortcutYield` δέχεται πλέον **και** το `key` |
| `layout/WorkspaceSplitLayout.tsx` | αναβολή εγγραφής ένα καρέ + `elementRef` + cleanup· τεκμηρίωση της εξάρτησης από τον φύλακα |
| `__tests__/keyboard-scope.test.ts` | **40 → 81** tests |
| `__tests__/WorkspaceSplitLayout.test.tsx` | **8 → 11**· το παραπλανητικό test ξαναγράφτηκε (βλ. κάτω) |

**Το test που ήταν ψέμα.** Υπήρχε test με όνομα *«αλλαγή πλάτους από πληκτρολόγιο (το VS Code ΔΕΝ
το έχει)»* που έλεγχε **μόνο** `tabIndex === 0`. Ήταν **πράσινο πάνω σε εντελώς νεκρή δυνατότητα**.
Η εστιασιμότητα δεν αποδεικνύει ιδιοκτησία πλήκτρου. Αντικαταστάθηκε από τρία tests που ελέγχουν το
**πραγματικό** συμβόλαιο: ο accelerator **παραιτείται** στα βέλη, **δεν** παραιτείται στα γράμματα,
και το διαχωριστικό είναι εστιάσιμο (ξεχωριστός, ειλικρινής ισχυρισμός).

### 14.2.1 ✅ Ε4 — ζωντανή επαλήθευση (2026-07-28 δ)

Chrome, `47_ergasia.dxf`, **πραγματικά** πλήκτρα μέσω CDP, `document.activeElement` επιβεβαιωμένο
στο `DIV[role=separator]` με το σωστό `aria-label`. Παγίδα στο `Storage.prototype.setItem`
φιλτραρισμένη στο κλειδί `workspace-dock`.

| Πέρασμα | Χειρονομία | DOM πριν → μετά | Εγγραφές | store | Ετυμηγορία |
|---|---|---|---|---|---|
| **1** | `ArrowLeft` ×2 | 647,266 → **414,453** | `["414.453125"]` — **μία** | 414,453 == DOM | ✅ |
| **reload** | — | — | — | — | **το πλάτος κρατήθηκε: 414,453** ✅ |
| **2** | `ArrowRight` ×2 | 414,453 → **647,266** | `["647.265625"]` — **μία** | 647,266 == DOM | ✅ |

**Το κρίσιμο**: πριν το πέρασμα 1 το αποθηκευμένο ήταν **647,246** (από προηγούμενο σύρσιμο). Μετά
από **μόνο** πληκτρολόγιο + reload ήρθε **414,453**. Δηλαδή η εγγραφή προήλθε αποδεδειγμένα από το
μονοπάτι του πλήκτρου — όχι από σύρσιμο, όχι από την προϋπάρχουσα τιμή.

**Δύο πατήματα ⇒ μία εγγραφή**: το `cancelAnimationFrame` μέσα στο `schedulePersist` συγχώνευσε τη
διπλή χειρονομία, και η τιμή που γράφτηκε είναι η **τελική** — όχι η ενδιάμεση. Αυτό είναι η
idempotency του N.7.2 #3 μετρημένη, όχι δηλωμένη. Ταυτόχρονα ξεχωρίζει το «καμία εγγραφή» από το
«έγραψε την ίδια τιμή» (η παγίδα του §7.3 του handoff): και τα δύο περάσματα άλλαξαν **πραγματικά**
την τιμή.

> 📌 **Η παγίδα του `visibilityState` επιβεβαιώθηκε ως πλατφόρμα, όχι ως ελάττωμα.** Το CDP στέλνει
> πλήκτρα **χωρίς** να φέρνει το παράθυρο μπροστά, οπότε η καρτέλα έμενε occluded ⇒ `hidden` ⇒ το
> `requestAnimationFrame` **παγωμένο** ⇒ `writes: []`, ακριβώς η ψευδώς αρνητική μέτρηση που κόλλησε
> την προηγούμενη προσπάθεια. Μόλις η καρτέλα έγινε ορατή, το **εκκρεμές** rAF εκτελέστηκε και η
> εγγραφή έπεσε αμέσως. Καμία απώλεια δεδομένων: το rAF **αναβάλλεται**, δεν ακυρώνεται — και σε
> πραγματική χρήση ο χρήστης βλέπει την καρτέλα που ρυθμίζει.

### 14.3 Google-level declaration (N.7.2) — Φ1 μετά τις διορθώσεις

✅ **Google-level: ΝΑΙ** για τη Φ1 (αγκύρωση αριστερά + αλλαγή πλάτους με δείκτη **και**
πληκτρολόγιο).

| # | Ερώτηση | Απάντηση |
|---|---|---|
| 1 | Proactive ή reactive; | **Proactive** — το πλάτος γράφεται στο **τέλος της χειρονομίας**, όχι ως παρενέργεια κάθε pixel |
| 2 | Race condition; | **Όχι** — το Ε3 ήταν ακριβώς αυτό (`onLayoutChanged` πριν το `onResize`) και έκλεισε αναβάλλοντας ένα καρέ αντί να στοιχηματίσουμε στη σειρά κλήσεων |
| 3 | Idempotent; | **Ναι** — `setDockedWidth` με ίδια τιμή = πλήρες no-op (ούτε notify ούτε εγγραφή) |
| 4 | Belt-and-suspenders; | **Ναι** — DOM read **+** `onResize` ref· ο φύλακας πλήκτρων ρωτά **target ΚΑΙ** `activeElement` |
| 5 | Single Source of Truth; | **Ναι** — **ένα** store για το πλάτος, **ένας** φύλακας ιδιοκτησίας πλήκτρων (ADR-711), **ένας** κανόνας αγκύρωσης (§4.1). Καμία τοπική άμυνα στο layout component |
| 6 | Await ή fire-and-forget; | Η εγγραφή είναι **αναβεβλημένη κατά ένα καρέ** — συνειδητά, γιατί η σωστή τιμή **δεν υπάρχει** νωρίτερα· ακυρώνεται στο unmount |
| 7 | Ποιος κατέχει τον κύκλο ζωής; | **Ρητά**: το `workspace-dock-store` κατέχει το πλάτος· το `keyboard-scope` την ιδιοκτησία πλήκτρου· το `useViewportManager` την αγκύρωση του σχεδίου |

⚠️ **Δύο ειλικρινή κενά** (δηλωμένα, όχι κρυμμένα) — το τρίτο (Ε4 ανεπαλήθευτο) **έκλεισε** στο
§14.2.1:
1. **Mobile drawer ανέλεγκτο ζωντανά** (§14.2) — δομικά ασφαλές, αλλά «καλυμμένο από tests» **δεν**
   είναι «είδα ότι δουλεύει».
2. **Ο viewer τρέχει στα ~14 FPS σε ηρεμία** (§4.3.1). **Δεν** το προκάλεσε αυτό το ADR και **δεν**
   το επιδεινώνει το σύρσιμο — αλλά είναι το πραγματικό ταβάνι αίσθησης του εργαλείου και ανήκει
   στο **ADR-040**.

### 14.4 Καθάρισμα — τα δύο ευρήματα του §14.1 (2026-07-29)

#### (α) Νεκροί selectors στο `src/components/ui/resizable.tsx`

Το wrapper έστυλιζε με ονόματα **v2/v3** πάνω σε εγκατεστημένη **v4.7.2**. Επαληθεύτηκε **δύο
φορές** — στο ίδιο το bundle (`dist/react-resizable-panels.js`) **και** σε ζωντανό DOM — ότι η v4
εκπέμπει **μόνο** `data-group`, `data-panel`, `data-separator`, `data-testid`:

| Παλιό (νεκρό) | Πού ζει τώρα | Διόρθωση |
|---|---|---|
| `data-[panel-group-direction=vertical]:flex-col` στο `Group` | **πουθενά** — το `Group` δεν εκπέμπει κατεύθυνση | διαβάζεται από το **prop** `orientation` (προαιρετικό, προεπιλογή `"horizontal"`) |
| `data-[panel-group-direction=vertical]:*` ×6 στον handle | στο `aria-orientation` του **ίδιου του separator** | `[&[aria-orientation=horizontal]]:*` |
| `[&[data-resize-handle-active]]:bg-ring` | `data-separator="inactive\|hover\|active\|disabled"` | `data-[separator=active]:bg-ring` |

⚠️ **Η κατεύθυνση περιγράφει τη ΓΡΑΜΜΗ, όχι την ομάδα**: ομάδα `horizontal` (panels δίπλα-δίπλα)
⇒ separator με `aria-orientation="vertical"`. Το να αντιγράψει κανείς μηχανικά το `vertical` του
παλιού ονόματος θα παρήγαγε τον **αντίστροφο** κανόνα.

**Ζωντανή επίπτωση**: και οι 3 καταναλωτές (`FileManagerPageContent`, `EntityFilesContent`,
`ReconciliationPageContent`) χρησιμοποιούν `orientation="horizontal"` + `withHandle` — άρα το μόνο
που τους **έλειπε πραγματικά** ήταν η ένδειξη ενεργού συρσίματος, και αυτό διορθώθηκε. Οι κανόνες
κατεύθυνσης είναι σήμερα λανθάνοντες (κανείς δεν είναι `vertical`) αλλά πλέον **σωστοί**.

#### (β) Το test που θα το είχε πιάσει — και που ΠΡΩΤΑ ΑΠΕΤΥΧΕ ΝΑ ΤΟ ΠΙΑΣΕΙ

`src/components/ui/__tests__/resizable-dom-contract.test.tsx` (10 tests). Δεν ελέγχει
συμβολοσειρές· είναι **διασταυρωτικό**: εξάγει ποια attributes **στοχεύει** το className μας και
απαιτεί να **υπάρχουν** στο DOM που παράγει η βιβλιοθήκη. Μετονομασία σε επόμενη major ⇒ κόκκινο,
χωρίς να ξέρουμε εκ των προτέρων το νέο όνομα.

> 🔴 **Η πρώτη γραφή του ήταν χάρτινη — και το ανακάλυψε μόνο η δοκιμή falsifiability.** Ο parser
> απαιτούσε `=`, οπότε η μορφή **παρουσίας** (`data-[resize-handle-active]`, χωρίς τιμή) περνούσε
> αόρατη· και ο έλεγχος «αποσυρμένων» συνέκρινε **ωμή συμβολοσειρά**, που δεν ταιριάζει ποτέ
> επειδή το Tailwind γράφει `data-[…]` **με αγκύλη**. Με την παλινδρόμηση επανεισαγμένη τεχνητά,
> το test έμεινε **10/10 πράσινο**. Μετά τη διόρθωση του parser (4 μορφές αντί για 2): **2 κόκκινα**
> στην ίδια ακριβώς παλινδρόμηση, **10/10 πράσινα** στον σωστό κώδικα.
>
> **Το δίδαγμα είναι το ίδιο με το §14.2**: ένα πράσινο test δεν αποδεικνύει τίποτα αν δεν έχει
> αποδειχθεί ότι **μπορεί** να κοκκινίσει. Επανεισήγαγε το ελάττωμα πριν εμπιστευτείς τη δικλείδα.

#### (γ) Διαγραφή `layout/CadDock.tsx` (231 γρ.)

Νεκρό **και** σπασμένο: `import { DockviewReact } from 'dockview'` με το `dockview` **μη
εγκατεστημένο** (επιβεβαιώθηκε από `require`). Μηδέν εισαγωγείς. Ήταν νάρκη — όποιος το άγγιζε
έσπαγε το build.

Παρασυρόμενα i18n κλειδιά: τα `dxfViewerPanels.cadDock.{panels,snapModes,layers,properties,
history,accessibility}` έμειναν **ορφανά** (0 χρήσεις, μετρημένο) και αφαιρέθηκαν από `el` **και**
`en`. ⚠️ Το `cadDock.statusBar.*` **ΔΕΝ** είναι ορφανό — το χρησιμοποιεί ζωντανά ολόκληρη η
`CadStatusBar` + 4 ακόμη αρχεία· η ονομασία είναι απλώς κληρονομιά. `validate:i18n`: **30017/30017
κλειδιά, 100% και στα δύο**.

#### (δ) Το τρίτο νεκρό — ο master SNAP toggle του `ProSnapToolbar`

Η διαγραφή του CadDock **παρήγαγε** νέο νεκρό κώδικα, και αυτό φάνηκε μόνο επειδή μετρήθηκαν οι
καταναλωτές αντί να θεωρηθεί δεδομένο ότι «το αρχείο έφυγε, τελείωσε»:

Το `ProSnapToolbar` είχε **προαιρετικό** κλάδο `{onToggleSnap !== undefined && (…)}` — κουμπί
«SNAP» με μετρητή. Ο μοναδικός εναπομείνας καταναλωτής (`CadStatusBar.tsx:474`) **δεν** περνά
`onToggleSnap`, άρα ο κλάδος έγινε **απροσπέλαστος** τη στιγμή που έφυγε το CadDock. Αφαιρέθηκαν:
τα props `snapEnabled`/`onToggleSnap`, ο `handleMasterToggle`, ο `enabledCount`, το εικονίδιο
`Target` από τα imports, και το ορφανό κλειδί `overlayToolbar.objectSnap` (el+en).

Το ίδιο το σχόλιο του prop **ήδη έλεγε** «CadStatusBar OSNAP is the canonical toggle» — δηλαδή ο
δεύτερος toggle ήταν ανεκτός μόνο όσο ζούσε το CadDock. Στη θέση του μπήκε ρητό σχόλιο ώστε να μην
ξαναφυτρώσει δεύτερος ιδιοκτήτης της ίδιας κατάστασης (ADR-341 SSoT, `cadToggles.osnap`).
`validate:i18n`: **30016/30016, 100%**.

> 🟠 **ΕΥΡΗΜΑ ΕΚΤΟΣ ΣΚΟΠΟΥ — το `src/types/i18n.ts` είναι μπαγιάτικο κατά ~4 μήνες.** Το header του
> γράφει `Generated: 2026-04-03`. Η αναπαραγωγή του (`npm run generate:i18n-types`) βγάζει
> **+39.924 / −16.377 γραμμές** — δηλαδή τέσσερις μήνες αλλαγών σε locales όλης της εφαρμογής που
> **ποτέ δεν πέρασαν στους τύπους**. Το αρχείο **επαναφέρθηκε στο HEAD** και δεν περιλαμβάνεται
> εδώ: μια τέτοια αλλαγή δεν έχει θέση σε commit καθαρισμού, πολύ περισσότερο σε **κοινό working
> tree**. Χρειάζεται δικό της πέρασμα, με δικό της έλεγχο — και προφανώς **gate**, γιατί κανένα
> δεν το έπιασε επί 4 μήνες.

---

### 14.5 Φ2 — αγκύρωση δεξιά + `mode` + «Επαναφορά» (2026-07-29)

#### 14.5.1 🔴 ΔΙΟΡΘΩΣΗ ΤΟΥ §10 — η Φ2 **ΔΕΝ** ήταν «μικρή, χαμηλού ρίσκου»

Το §10 έγραφε: «**Φ2 — Αγκύρωση δεξιά.** Αντιστροφή σειράς παιδιών + μενού + persistence. Μικρή,
χαμηλού ρίσκου». Ο κώδικας διέψευσε και τα δύο επίθετα. Η αιτία είναι **μία** πρόταση που κανείς
δεν είχε γράψει:

> **Η αλλαγή πλευράς ΜΕΤΑΚΙΝΕΙ τον καμβά χωρίς να τον ΑΛΛΑΖΕΙ ΣΕ ΜΕΓΕΘΟΣ.**

Το panel του καμβά κρατά ταυτόσημο πλάτος· απλώς βρίσκεται αλλού. Και το `ResizeObserver`
πυροδοτείται **μόνο** σε αλλαγή μεγέθους — σε καθαρή μετατόπιση **σιωπά**. Από αυτό προκύπτουν
τρία ανεξάρτητα ελαττώματα, **κανένα** ορατό τη στιγμή της ενέργειας:

| # | Ελάττωμα | Συνέπεια |
|---|---|---|
| **1** | Το `containerLeftRef` (`useViewportManager.ts:139`) μένει στην παλιά ακμή | Το **επόμενο** σύρσιμο υπολογίζει `Δleft = 0 − 392` ⇒ **το σχέδιο πετάγεται 392px**. Μετρημένο σε test: `offsetX` 100 → **492** |
| **2** | Το `CanvasBoundsService` ακυρώνει cache μόνο σε window-resize/scroll/λήξη 5s (`CanvasBoundsService.ts:83-116`) | **hit-test και snap με λάθος origin** για έως 5 δευτερόλεπτα μετά την αλλαγή πλευράς |
| **3** | Χωρίς σταθερά `key`, ο React ταιριάζει τα παιδιά **κατά θέση** | Η αντιστροφή σειράς **ξαναφτιάχνει** το panel του καμβά ⇒ **απώλεια WebGL context**, ξαναχτίσιμο σκηνής, ακύρωση bitmap cache |

Το #3 είναι το ακριβότερο και το πιο αόρατο: μια εντολή μενού που οφείλει να είναι ακαριαία θα
πάγωνε δευτερόλεπτα, και τίποτα στον κώδικα δεν θα το φώναζε.

**Το κοινό σχήμα και των τριών**: *γεγονός διάταξης που δεν παράγει `ResizeObserver`*. Υπήρχε ήδη
ακριβώς ένα προηγούμενο στο ίδιο αρχείο — το **ADR-549 Φ7** (αλλαγή DPR). Η λύση είναι η ίδια και
είναι αυτή που εφαρμόζουν οι docking managers των **Revit/C4D**: ο διαχειριστής διάταξης
**ειδοποιεί** τα viewports μετά από κάθε πράξη, δεν περιμένει observer.

#### 14.5.2 Η θεραπεία: **ξαναμέτρηση**, όχι αντιστάθμιση

```
setDockMode()  →  dockModeStore.subscribe
                   ├─ canvasBoundsService.clearCache()          (άμεσα)
                   └─ UnifiedFrameScheduler.scheduleOnce(…)     (επόμενο καρέ, μετά το flush)
                        ├─ canvasBoundsService.clearCache()
                        └─ measureAndApply()   ← ξανασπέρνει το containerLeftRef
```

Το `measureAndApply` **προϋπήρχε** και κάνει ήδη ακριβώς το σωστό (`:168`): ξανασπέρνει την ακμή
**χωρίς** να αγγίξει το `transform`. Άρα το σχέδιο μένει ακίνητο **μέσα** στον καμβά — η
συμπεριφορά Revit/AutoCAD: μια παλέτα που μετακόμισε δεν είναι λόγος να αλλάξει η κάμερα.
Καμία νέα λογική αγκύρωσης· **μηδέν** νέος `requestAnimationFrame` (ο SSoT `UnifiedFrameScheduler`,
ADR-030). Η συνδρομή είναι **imperative** — ο καμβάς δεν μαθαίνει *ποια* είναι η πλευρά, μόνο ότι
κάτι μετακινήθηκε, ώστε να μην υπάρχει `if (mode === …)` στο μονοπάτι απόδοσης.

⚠️ **Το §2.1 του handoff («δεξιά αγκύρωση ⇒ Δleft = 0 ⇒ μηδέν νέος κώδικας») ήταν σωστό αλλά
μισό**: ισχύει για τη *σταθερή κατάσταση*, όχι για τη *μετάβαση*.

#### 14.5.3 Τι απεστάλη

| Αρχείο | Ρόλος |
|---|---|
| `systems/workspace/workspace-dock-mode.ts` **(νέο)** | `WorkspaceDockMode`, `DOCK_MODES`, `parseDockMode`, `isDockedRight`. Καθαρό |
| `systems/workspace/useWorkspaceDock.ts` **(νέο)** | `useDockMode()` — leaf hook |
| `layout/workspace-dock-menu.tsx` **(νέο)** | Μοντέλο εντολών + **ένας** renderer |
| `layout/WorkspacePaletteHeader.tsx` **(νέο)** | Γραμμή τίτλου + μενού |
| `systems/workspace/workspace-dock-store.ts` | `getDockMode`/`setDockMode`/`subscribeDockMode`/`resetDockLayout` |
| `layout/WorkspaceSplitLayout.tsx` | Σειρά ανά `mode` + **σταθερά `key`** |
| `hooks/canvas/useViewportManager.ts` | §14.5.2 — **ADR-040 staged** |
| `layout/SidebarSection.tsx` | Προσάρτηση επικεφαλίδας· i18n του status |
| `components/ui/floating/floating-panel-persistence.ts` | `clearAllPanelGeometry()` — ο **πρώτος** καλών του reset (είχε 0) |
| `lib/storage/safe-storage.ts` | `safeRemoveItemsByPrefix()` + εξαγωγή του `isStorageAvailable` |
| `utils/storage-utils.ts` | `WORKSPACE_DOCK_MODE` + κατανάλωση του κοινού `isStorageAvailable` |
| locales `el`+`en` | `workspaceDock.{title,menuLabel,dockLeft,dockRight,resetLayout,statusLabel}` |

**Αποφάσεις σχεδιασμού**

1. **`mode` = δύο τιμές, όχι τρεις.** Το `'floating'` (Φ3) θα ήταν νεκρός κλάδος — ο ίδιος
   λόγος που το `mode` δεν μπήκε στη Φ1. Το `parseDockMode` απορρίπτει άγνωστες τιμές, άρα η Φ3
   επεκτείνει **χωρίς migration** και χωρίς αλλαγή του `:v1` κλειδιού.
2. **Δύο κλειδιά, όχι ένα record `{mode,width}`.** Τα δύο πεδία έχουν αντίθετα προφίλ: το πλάτος
   γράφεται ~60×/δευτ. και **δεν** έχει συνδρομητές· η πλευρά γράφεται μία φορά στους μήνες και
   **πρέπει** να έχει. Κοινό record ⇒ κάθε σύρσιμο ειδοποιεί τους συνδρομητές της πλευράς =
   ακριβώς το πρόβλημα ADR-040.
3. **«Επαναφορά» = ΟΛΗ η διάταξη** (απόφαση Giorgio): αγκυρωμένη (πλάτος+πλευρά) **και** κάθε
   αιωρούμενη (ADR-723). Μερική επαναφορά είναι χειρότερη από καμία — ο χρήστης δεν ξέρει *ποιο*
   από τα δύο συστήματα του χάλασε τη διάταξη. Η απαρίθμηση γίνεται με το **πρόθεμα** του
   ADR-723, όχι με χειρόγραφο μητρώο ids: 17 καταναλωτές, ένας εκτός subapp — κάθε λίστα θα
   ξεχνούσε σιωπηλά την επόμενη παλέτα.
4. **Γραμμή τίτλου** (απόφαση Giorgio) αντί για κουμπί στο footer: είναι **και** η λαβή που
   απαιτεί η Φ3 για dock⇄float. Χτίζεται μία φορά. Δύο επιφάνειες για το ίδιο μενού (δεξί κλικ =
   ταχύτητα, «⋮» = **ανακαλυψιμότητα**), **ένας** renderer με ενθετόμενα primitives — τα δύο
   μενού του Radix έχουν διαφορετικά components, οπότε ο αφελής δρόμος θα ήταν sibling clone.

#### 14.5.4 Τι αποδεικνύουν τα tests (και ότι **μπορούν** να κοκκινίσουν)

**102 tests πράσινα** σε 6 suites. Κάθε νέα δικλείδα δοκιμάστηκε με **τεχνητή επανεισαγωγή του
ελαττώματος** (§14.2 δίδαγμα — πράσινο test δεν αποδεικνύει τίποτα μέχρι να το δεις κόκκινο):

| Μετάλλαξη | Αποτέλεσμα |
|---|---|
| Αφαίρεση `clearAllPanelGeometry()` από το `resetDockLayout` | 1 κόκκινο ✅ |
| `parseDockMode` δέχεται κάθε string | 4 κόκκινα ✅ |
| Αφαίρεση των `key` από τα Panel | **1 κόκκινο** — ακριβώς το test ταυτότητας ✅ |
| Αφαίρεση της συνδρομής `subscribeDockMode` | **1 κόκκινο**: `offsetX` Expected 100 / **Received 492** ✅ |

Το τελευταίο είναι η απόδειξη ότι το ελάττωμα #1 του §14.5.1 ήταν **πραγματικό** και όχι θεωρητικό:
άλμα ακριβώς 392px = το πλάτος της παλέτας.

⚠️ Το test ταυτότητας χρησιμοποιεί `toBe` (ταυτότητα αντικειμένου), **όχι** `toBeInTheDocument`:
ένας νέος κόμβος με ίδιο περιεχόμενο θα περνούσε το δεύτερο και θα έκρυβε ακριβώς την παλινδρόμηση.

#### 14.5.5 Παράπλευρο εύρημα — δύο storage layers (N.18 / CHECK 3.28)

Το `jscpd:diff` φώτισε **προϋπάρχον** clone 28 γρ./54 tokens ανάμεσα σε `lib/storage/safe-storage.ts`
και `dxf-viewer/utils/storage-utils.ts` (το Φ2 απλώς έφερε τα δύο αρχεία στο ίδιο diff).
Διαχωρίστηκε σε δύο πράγματα:

- **Διορθώθηκε επιτόπου** (N.0.2 «μικρό ⇒ τώρα»): το `isStorageAvailable()` ήταν byte-identical.
  Πλέον εξάγεται από το κοινό και το subapp το εισάγει. `jscpd:diff` **πράσινο** στα 13 αρχεία.
- **Δεν διορθώθηκε, και σκοπίμως**: το `storageSet` κάνει **πάντα** `JSON.stringify`, το
  `safeSetItem` αποθηκεύει τα strings **ωμά**. Δεν είναι διπλότυπο — είναι **ασύμβατα συμβόλαια**.
  Ενοποίηση χωρίς dual-read migration θα έκανε `JSON.parse('mm')` → throw → σιωπηλή προεπιλογή,
  δηλαδή **απώλεια κάθε αποθηκευμένης προτίμησης-string** σε 32 αρχεία. Καταγράφηκε στο
  `.claude-rules/pending-ratchet-work.md`.

#### 14.5.6 Τι **δεν** καλύπτεται από jsdom — μένει για ζωντανή επαλήθευση

1. Σύρσιμο διαχωριστικού **αμέσως μετά** από αλλαγή πλευράς (το κρίσιμο σενάριο του #1 —
   ελέγχεται αριθμητικά σε test, όχι με πραγματικά pixels).
2. Κλικ σε οντότητα μέσα στο παράθυρο των 5s (ελάττωμα #2 — hit-test).
3. Ότι ο `<canvas>` κρατά όντως το WebGL context του (στο jsdom δεν υπάρχει WebGL· ελέγχεται
   μόνο η ταυτότητα κόμβου).
4. Η οπτική εμφάνιση της νέας γραμμής τίτλου.

---

### 14.6 Φ4 §9.1 — η πυκνότητα απαντά στο πλάτος **της παλέτας** (2026-07-29)

**Αφορμή (Giorgio):** *«όταν ανοίγω προς τα δεξιά το πάνελ, οι καρτέλες και τα ταβ δεν
ξεδιπλώνονται ώστε να γίνονται μία σειρά και να κερδίζουμε χώρο. Αν δεν συμβαίνει αυτό είναι
άσκοπο να μεγαλώνει το πάνελ.»*

#### 14.6.1 🔴 Η αιτία **δεν** ήταν η απουσία container queries

Η προφανής ανάγνωση («λείπει το §9.1») ήταν λάθος και θα οδηγούσε σε **νεκρό CSS**:

1. **Το trigger strip είχε ήδη `flex flex-wrap`** (`navigation/tabs-types.ts:40`). Τα tab
   **ήξεραν** να ξεδιπλώνονται· δεν τους δινόταν ποτέ πλάτος.
2. **Δεύτερο κλείδωμα 384px** στο `FloatingPanelContainer.tsx:135` (§2.2). Ένα `@container` πάνω
   από αυτό θα μετρούσε **πάντα 384px** ⇒ το κατώφλι των 520px **δεν θα ενεργοποιούνταν ποτέ**.

**Άρα η σειρά αντιστράφηκε**: πρώτα η αφαίρεση του clamp (η *προϋπόθεση*), μετά τα κατώφλια.
Μόνο το βήμα 1 λύνει το παράπονο· τα κατώφλια είναι το επιπλέον.

#### 14.6.2 Τι απεστάλη

| Αρχείο | Αλλαγή |
|---|---|
| `ui/workspace-palette-density.module.css` **(νέο)** | ο container + τα δύο `@container` κατώφλια |
| `ui/FloatingPanelContainer.tsx` | `WIDTH.PANEL_LG` → `WIDTH.FULL`· εμβέλειες `palette`/`tabStrip`/`content` |
| `ui/entity-properties/EntityPropertyRow.tsx` | `data-palette-rows` στο `EntityPropertySection` |
| `ui/beam-advanced-panel/BeamAdvancedPanel.tsx` | `data-palette-rows` στο `BeamAdvancedSection` |
| `ui/__tests__/workspace-palette-density.test.tsx` **(νέο)** | 13 tests |

**Σκέτο CSS, μηδέν εξάρτηση** — το `@tailwindcss/container-queries` (MIT) θα ήταν νέα εξάρτηση
για τρεις κανόνες· ο N.5 δεν χρειάστηκε καν να ενεργοποιηθεί.

#### 14.6.3 Δύο αποφάσεις που **δεν** ήταν προφανείς

**(α) Οι δύο στήλες είναι opt-in (`data-palette-rows`), όχι καθολικός κανόνας.** Εξετάστηκαν και
τα **6** group sections της παλέτας· **μόνο 2** πληρούν το κριτήριο «ομοιόμορφες γραμμές
ετικέτα/χειριστήριο». Τα υπόλοιπα 4 θα **χαλούσαν**: το `MaterialSection` έχει swatch 64px, το
`PartsSection` έχει ένα `<ul>` (θα πήγαινε ολόκληρο στη 2η στήλη), το `BoqSection` μία παράγραφο,
το `SlabOpening` έχει **ήδη** `<dl class="grid-cols-2">`. Και η καρτέλα «Επίπεδα» — δέντρο 54
στρώσεων — θα τεμαχιζόταν κατά ύψος με κάθε άνοιγμα κόμβου. **Ένας καθολικός κανόνας θα
«κέρδιζε χώρο» καταστρέφοντας τη λειτουργία.** Το χαρακτηριστικό είναι **αδρανές εκτός παλέτας**
(κανένας container να το απαντήσει), άρα τα ίδια components μέσα σε διαλόγους μένουν ανέπαφα —
μηδέν δεύτερος κλάδος κώδικα.

**(β) Η ετικέτα κρύβεται ΟΠΤΙΚΑ, όχι με `display: none`.** Το εικονίδιο είναι `<svg>` χωρίς
`<title>` ⇒ η ετικέτα είναι το **μοναδικό** προσβάσιμο όνομα της καρτέλας. Ένα `display:none`
θα άφηνε **οκτώ ανώνυμα tab** στον αναγνώστη οθόνης — ανταλλαγή 40 pixels με τη χρηστικότητα.

⚠️ **Παρενέργεια που ελέγχθηκε:** `container-type: inline-size` συνεπάγεται
`contain: layout style inline-size`· το **layout** containment κάνει το στοιχείο containing block
για `position: fixed` απογόνους. Επαληθεύτηκε ότι **κανένα** panel της παλέτας δεν έχει
μη-portaled `fixed` (τα Radix Select/Dialog/Popover κάνουν portal στο `body`).

#### 14.6.4 Τι αποδεικνύουν τα tests — **και ότι μπορούν να κοκκινίσουν**

Το jsdom **δεν υλοποιεί `@container`** και το jest αντικαθιστά το CSS module με stub ⇒ ένα
render-test θα ήταν πράσινο **ακόμη κι αν το `.module.css` ήταν άδειο**. Γι' αυτό η §1 των tests
διαβάζει το CSS **ως κείμενο** (αφαιρώντας τα σχόλια — κατώφλι που ζει μόνο σε σχόλιο δεν ισχύει)
και η §2 φυλάει την **προϋπόθεση**, όχι το αποτέλεσμα.

**Mutation-verified 4/4** (2026-07-29) — κάθε μετάλλαξη κοκκίνισε tests:

| Μετάλλαξη | Αποτέλεσμα |
|---|---|
| επαναφορά `w-96` στη ρίζα | **2 failed** |
| κατώφλι `520px` → `900px` | **4 failed** |
| `clip-path` → `display: none` | **1 failed** |
| αφαίρεση `data-palette-rows` | **1 failed** |

**13 νέα tests· 30/30 πράσινα** μαζί με τα Φ1 (καμία παλινδρόμηση)· `jscpd:diff` **καθαρό**.

⚠️ Το test §2 ρωτά **τη γραμμή της ρίζας**, όχι όλο το αρχείο: η πρώτη γραφή του έψαχνε σε όλο το
`FloatingPanelContainer.tsx` και κοκκίνιζε στο **ίδιο του το σχόλιο τεκμηρίωσης** (που αναφέρει
το `PANEL_LG` για να εξηγήσει γιατί έφυγε) **και** στο `PANEL_SM` του «Loading translations…» —
ένα `fixed` overlay που επιστρέφει **πριν** τη ρίζα, άρα δεν κλειδώνει κανέναν container.

#### 14.6.5 Τι **δεν** έγινε — ρητά

- **§9.3** (διπλό κλικ = βέλτιστο πλάτος) και **§9.2** (view transitions): NOT IMPLEMENTED.
- **Τα 4 μη-συμμετέχοντα group** (§14.6.3α) παραμένουν μονόστηλα **σκοπίμως**. Αν κάποιο τους
  αναδιαρθρωθεί σε ομοιόμορφες γραμμές, αρκεί να προσθέσει `data-palette-rows`.
- ~~**Οπτικό πέρασμα ζωντανά**~~ → ✅ **ΕΓΙΝΕ 2026-07-29**, δες §14.6.7.

#### 14.6.6 Google-level declaration (N.7.2)

✅ **Google-level: ΝΑΙ** — μία πηγή αλήθειας για τα κατώφλια (ένα `.module.css`), opt-in αντί για
καθολικό κανόνα που θα έσπαγε 4 από τα 6 group, προσβασιμότητα διατηρημένη (οπτική απόκρυψη), και
ο φύλακας ελέγχει την **αιτία** (το clamp) όχι το σύμπτωμα — mutation-verified 4/4.

#### 14.6.7 ✅ Ζωντανή επαλήθευση στον browser (2026-07-29) — **μετρημένη, όχι «με το μάτι»**

Chrome, `localhost:3000/dxf/viewer`, viewport **2400×1121 CSS px**, DPR 0.8, επιλεγμένη
οντότητα = γραμμοσκίαση (`ΑΠΟΤ_HATCH`) ⇒ η καρτέλα «Ιδιότητες» με **3** group
`data-palette-rows`. Οι τιμές διαβάστηκαν από `getComputedStyle`, όχι από στιγμιότυπο.

| # | Κριτήριο | Μέτρηση | Ετυμηγορία |
|---|---|---|---|
| i | >520px → δύο στήλες | 718px ⇒ `display:grid`, `grid-template-columns: 334.785px 334.785px` και στα 3 group· 450px ⇒ `display:flex` | ✅ |
| ii | <320px → μόνο εικονίδια | 278px ⇒ ετικέτα `position:absolute; clip-path:inset(50%)`, πλαίσιο **1×1px**, tab πλάτος **32px**· το `textContent` **ακέραιο** και στα 8 tab (το προσβάσιμο όνομα επιβιώνει) | ✅ |
| iii | λιγότερες σειρές όσο πλαταίνει | 382px ⇒ **3** σειρές · 699px ⇒ **2** · 718px ⇒ **2** (τα `rowTops` μετρήθηκαν ως διακριτά `getBoundingClientRect().top`) | ✅ |

**Τα κατώφλια είναι ακριβώς εκεί που τα ορίζει η προδιαγραφή** — δυαδική αναζήτηση με άμεσο
πλάτος στον container: 318/319 ⇒ ετικέτα κρυφή, **320** ⇒ ορατή· 518/519 ⇒ μονόστηλο,
**520** ⇒ δίστηλο. Καμία απόκλιση ενός pixel, κανένα «περίπου». Μηδέν σφάλματα κονσόλας.

##### 🔴 14.6.7.α Εύρημα: η **μία** σειρά είναι δομικά ανέφικτη όσο η παλέτα είναι αγκυρωμένη

Η αφορμή του §14.6 ήταν κατά λέξη *«να ξεδιπλώνονται ώστε να γίνονται **μία σειρά**»*. Το
παραδοθέν πετυχαίνει 3→2 σειρές, **όχι** 1. Μετρήθηκε γιατί:

| Μέγεθος | Τιμή |
|---|---|
| Άθροισμα πλατών των 8 tab | **750px** |
| + 7 κενά × 4px + padding λωρίδας 2×8px | **794px** |
| Ελάχιστο πλάτος παλέτας για **1** σειρά (δυαδική αναζήτηση) | **795px** |
| `PANEL_LAYOUT.WORKSPACE_DOCK.WIDTH_MAX` | **720px** |

Λείπουν **75px** — και το `WIDTH_MAX: 720` δεν είναι τυχαίο: το σχόλιό του λέει *«πάνω από αυτό
ο καμβάς παύει να είναι ο πρωταγωνιστής»*. Το κενό **δεν** κλείνει με ρύθμιση padding: τα tab
έχουν ήδη μόλις `8px` οριζόντιο padding· μηδενίζοντάς το εξ ολοκλήρου κερδίζονται 128px αλλά τα
tab κολλάνε μεταξύ τους — ανταλλαγή μιας σειράς με τη στοχευσιμότητα (Fitts).

**Δεν άλλαξε τίποτα, σκοπίμως.** Η επιλογή είναι του Giorgio και είναι **προϊοντική**:

1. **Άφησέ το** — 3→2 σειρές είναι ήδη το κέρδος· 1 σειρά δεν είναι αυτοσκοπός.
2. **Αύξησε το `WIDTH_MAX` σε ~800px** — 33% ενός οθόνης 2400px· ο καμβάς πέφτει στο 67%.
3. **Η Φ3 το λύνει μόνη της**: η **αιωρούμενη** παλέτα δεν δεσμεύεται από το `WIDTH_MAX` (έχει
   δικά της όρια, ADR-723) ⇒ όποιος θέλει τα 8 tab σε μία σειρά, την αιωρεί. Αυτή είναι και η
   απάντηση του Revit στο ίδιο ερώτημα.

⚠️ Το νούμερο **795px είναι συνάρτηση των ελληνικών ετικετών και της γραμματοσειράς** — μια
μετάφραση ή μια ένατη καρτέλα το μετακινεί. Μην το κωδικοποιήσεις ως σταθερά πουθενά.

---

### 14.7 Κεντρικοποίηση του δομικού λεξιλογίου της παλέτας (2026-07-29, εντολή Giorgio)

**Αφορμή:** κατά τη Φ4 καταγράφηκε διπλότυπο στο `.claude-rules/pending-ratchet-work.md` (N.0.2).
Ο Giorgio το ανέτρεψε επιτόπου: *«ΝΑ ΤΟ ΚΕΝΤΡΙΚΟΠΟΙΗΣΕΙΣ ΤΩΡΑ»*.

#### 14.7.1 Τι ήταν αντιγραμμένο

| Σχήμα | Εμφανίσεις | Πού |
|---|---|---|
| group (`<section class="flex flex-col gap-1">` + επικεφαλίδα) | **6** | `EntityPropertyRow` · `BeamAdvancedPanel` · `ImportedMeshAdvancedPanel` **×3** · `SlabOpeningAdvancedPanel` |
| γραμμή (`<div class="flex items-center justify-between gap-2 py-0.5">` + ετικέτα) | **6** | `BimPropertyRow` **×2** · `EntityPropertyRow` **×4** |

**Νέο SSoT:** `ui/components/palette-primitives.tsx` → `PaletteGroupSection` + `PaletteFieldRow`.
**12 σημεία → 2.** Και τα 12 μεταναστεύτηκαν· `grep` επιβεβαιώνει **μηδέν** υπόλειμμα στην παλέτα.

#### 14.7.2 🔴 Κανένα εργαλείο δεν τα έβλεπε — και ήταν **δομικά** αδύνατο

- **CHECK 3.28 (jscpd)**: κατώφλι **50 tokens**, κάθε εμφάνιση ~20 ⇒ **μονίμως πράσινο**. Το
  επιβεβαίωσε και μετά: `jscpd:diff` καθαρό στα 7 αρχεία **και πριν και μετά** το refactor.
- **CHECK 3.18 (ssot:discover)**: σαρώνει `src/config|utils|lib` σε `-maxdepth 1` ⇒ **δεν ανοίγει
  ποτέ** το `src/subapps/dxf-viewer/ui/`.

Βρέθηκαν με **ανάγνωση**, ψάχνοντας κάτι άλλο (πού να μπει το `data-palette-rows`). Άλλη μια
επιβεβαίωση του μοτίβου των N.11/N.12: **το «0» σήμαινε «κανείς δεν κοίταξε».**

#### 14.7.3 Η παγίδα: τι **δεν** ισοπεδώθηκε

Τα 6 group **έμοιαζαν** ίδια στο markup και **δεν ήταν**. Τρεις πραγματικές διαφορές έγιναν props
αντί να εξαφανιστούν — κάθε μία με δικό της test (§4 της suite):

1. **`headingLevel` (3 vs 4)** — δομή εγγράφου, όχι στυλ. Το `EntityPropertySection` ζει στη ρίζα
   καρτέλας· τα advanced panels φωλιάζουν. Σταθερό επίπεδο = χαλασμένο περίγραμμα a11y.
2. **`tone` (`plain` vs `overline`)** — το `ImportedMeshAdvancedPanel` (ADR-683) χρησιμοποιεί
   κεφαλαία/αραιά/muted. **Επιλογή σχεδιασμού του**, όχι απόκλιση προς διόρθωση.
3. **`uniformRows`** — **μόνο 2 από τα 6** το δικαιούνται (§14.6.3α). Η προεπιλογή είναι `false`
   και **φυλάσσεται από test**: αν γυρίσει σε `true`, το `<ul>` του `PartsSection` εκτοξεύεται
   ολόκληρο στη δεύτερη στήλη.

**Μία σκόπιμη απλοποίηση:** το `<header>` που τύλιγε τη μοναδική επικεφαλίδα σε 3 σημεία
**αφαιρέθηκε**. Περιτύλιγμα γύρω από μία επικεφαλίδα δεν προσθέτει σημασιολογία, **και** σπάει τον
κανόνα `[data-palette-rows] > h3, > h4` του §9.1, που απαιτεί σχέση **άμεσου παιδιού**. Οπτικά
ταυτόσημο (`<header>` και `<h4>` είναι και τα δύο block σε flex column). Φυλάσσεται από test.

#### 14.7.4 ⛔ Κανένα registry module — τεκμηριωμένα

Το N.12 ζητά καταχώριση στο `.ssot-registry.json` όταν κεντρικοποιείται module. **Δεν έγινε, και
είναι σωστό**: ο μόνος υποψήφιος `forbiddenPattern` θα ήταν το `className="flex flex-col gap-1"`,
που χτυπά **νόμιμα** 5 διαλόγους (`CreateBlockDialog`, `TitleBlockLocalizeDialog` ×2,
`TitleBlockLibraryDialog`, `AiTitleBlockDialog`) — φόρμες, όχι group ιδιοτήτων παλέτας. Ένα
pattern με ≥50% false positives δεν είναι φύλακας· είναι θόρυβος που εκπαιδεύει σε παράκαμψη.
Ίδιο σκεπτικό με το ADR-584 (`usePropertyEditor`: «candidate patterns grep-verified και
**απορρίφθηκαν**»). Η προστασία εδώ είναι τα **tests**, όχι το registry.

#### 14.7.5 Επαλήθευση

**131 suites / 1349 tests πράσινα** (όλο το `ui/` + `layout/` του subapp) · `jscpd:diff` **καθαρό**
στα 7 αρχεία · **18 tests** στη suite της Φ4 (13 §9.1 + 5 §14.7) · **mutation-verified 4/4**
(§14.6.4). ❌ **Κανένα `tsc`** (N.17).

⚠️ **Ζωντανά ανέλεγκτο**: η οπτική ταυτότητα των 6 group μετά τη μετανάστευση. Τα tests ελέγχουν
επίπεδο επικεφαλίδας, κλάσεις τόνου και τη σχέση άμεσου παιδιού — **όχι pixels**.

---

### 14.8 Φ3 — η αιωρούμενη παλέτα (2026-07-29)

**Η τρίτη και τελευταία λειτουργία του §13.** Μετά από αυτή, η εντολή του Giorgio
(*«ΝΑ ΜΠΟΡΕΙ ΝΑ ΕΙΝΑΙ FLOATING Ή ΝΑ ΑΓΚΥΡΩΝΕΙ ΑΡΙΣΤΕΡΑ Ή ΔΕΞΙΑ»*) είναι ολόκληρη υλοποιημένη.

#### 14.8.1 🔴 Η δυαδική ερώτηση σε τριμερή ένωση — το σφάλμα που **δεν** έγινε

Το handoff προειδοποιούσε: `isDockedRight(mode) ? [καμβάς,δ,παλέτα] : [παλέτα,δ,καμβάς]`. Με
`mode === 'floating'` το **else** τρέχει ⇒ η αιωρούμενη παλέτα αποδίδεται **αγκυρωμένη αριστερά**.
Ο compiler δεν βλέπει τίποτα (ο κώδικας είναι έγκυρος), κανένα test δεν βλέπει τίποτα (δεν
υπήρχε τίποτα λάθος να δει) — ο κώδικας απλώς **απαντά σε λάθος ερώτηση**.

**Η θεραπεία δεν ήταν ένα δεύτερο predicate.** Ένα `isFloating()` δίπλα στο `isDockedRight()`
θα άφηνε την **τέταρτη** τιμή να ξεχαστεί με τον ίδιο ακριβώς τρόπο. Το `isDockedRight`
**διαγράφηκε** και αντικαταστάθηκε από το `resolveWorkspaceLayout(mode)`: εξαντλητικό `switch`
με φύλακα `never`, όπου μια νέα κατάσταση χωρίς `case` είναι **σφάλμα μεταγλώττισης**.

Επιπλέον, ο τύπος **χωρίστηκε στα δύο**: `WorkspaceDockedSide` (πάντα πλευρά) ⊂
`WorkspaceDockMode` (πλευρά **ή** αιώρηση). Χωρίς αυτό, το πεδίο «τελευταία πλευρά» θα μπορούσε
να πάρει τιμή `'floating'` — «βγες από την αιώρηση προς την αιώρηση», δηλαδή ένα διπλό κλικ που
δεν κάνει τίποτα. Ο τύπος το κάνει **αδύνατο**, ο `parseDockedSide` το φυλάει και στο storage.

#### 14.8.2 Τι απεστάλη

| Αρχείο | Αλλαγή |
|---|---|
| `layout/WorkspaceFloatingPalette.tsx` **(νέο, 286 γρ.)** | `FloatingPanel` + παρατηρητής συρσίματος + περίγραμμα-προεπισκόπηση |
| `layout/__tests__/WorkspaceFloatingPalette.test.tsx` **(νέο)** | 13 tests, **αληθινό** σύρσιμο |
| `layout/__tests__/WorkspacePaletteHeader.test.tsx` **(νέο)** | 11 tests |
| `layout/__tests__/SidebarSection.variant.test.tsx` **(νέο)** | 8 tests — γράφτηκε **επειδή μια μετάλλαξη έμεινε πράσινη** (§14.8.5) |
| `systems/workspace/workspace-dock-mode.ts` | `'floating'`· `WorkspaceDockedSide`· `resolveWorkspaceLayout`· `parseDockedSide`· `toDockedSide`· **−`isDockedRight`** |
| `systems/workspace/workspace-dock-geometry.ts` | `dockToFloatGeometry`· `resolveDropTarget`· `WorkspaceRect`· `FLOAT_SPAWN_OFFSET`· `DOCK_DROP_ZONE_WIDTH` |
| `systems/workspace/workspace-dock-store.ts` | `lastDockedSide` (3ο κλειδί)· `getLastDockedSide`· `toggleDockFloat`· `resetDockLayout` επαναφέρει και τα τρία |
| `layout/WorkspaceSplitLayout.tsx` | **τριμερής** διακλάδωση· `sidebar` → render prop· `workspaceRef` |
| `layout/WorkspacePaletteHeader.tsx` | λαβή συρσίματος· διπλό κλικ· φύλακας μη-συρόμενων στόχων· περιγραφή a11y |
| `layout/SidebarSection.tsx` | `variant: 'inline' \| 'drawer' \| 'floating'` |
| `layout/workspace-dock-menu.tsx` | `MODE_PRESENTATION` ως **ρητό** `Record<WorkspaceDockMode, …>` |
| `layout/MobileSidebarDrawer.tsx` · `app/DxfViewerContent.tsx` · `utils/storage-utils.ts` | προσαρμογή στα παραπάνω |
| `components/ui/floating/FloatingPanel.tsx` + `index.ts` | **προσθετικό**: `useFloatingPanelContextOptional()` |

**Μηδέν νέα εξάρτηση** (N.5). **Κανένα αρχείο >500 γρ.** (N.7.1· το μεγαλύτερο: 366).
**Κανένα αρχείο ADR-040** δεν αγγίχτηκε ⇒ το CHECK 6B δεν ενεργοποιείται (δες §14.8.4).

#### 14.8.3 Πέντε αποφάσεις που **δεν** ήταν προφανείς

**(α) Ένα hook που επιστρέφει `null` αντί για δεύτερη επικεφαλίδα.** Η γραμμή τίτλου πρέπει να
λειτουργεί **και** αγκυρωμένη **και** μέσα σε `FloatingPanel`. Το `useFloatingPanelContext`
**πετά** εκτός panel. Η αφελής λύση —«κάλεσε το hook μόνο όταν αιωρείται»— παραβιάζει τους
κανόνες των hooks· η δεύτερη αφελής λύση —«γράψε μια floating επικεφαλίδα»— είναι sibling clone
(τίτλος + μενού + δεξί κλικ, ξανά· CHECK 3.28). Προστέθηκε **προσθετικά** στο ADR-723 μια
μη-πετώσα παραλλαγή. Μηδέν ρίσκο για τους 17 υπάρχοντες καταναλωτές.

**(β) Ο φύλακας «μη-συρόμενος στόχος» ζει στον ΔΙΚΟ μας handler, όχι στο `useDraggable`.**
Το `useDraggable` έχει ήδη «smart interaction exclusion» για κουμπιά — **αλλά** θεωρεί λαβή
οτιδήποτε ταιριάζει σε `.cursor-grab` και τότε **παρακάμπτει** αυτόν τον αποκλεισμό. Η
επικεφαλίδα φέρει `cursor-grab` σε όλο το πλάτος της (σωστή ένδειξη πρόθεσης), οπότε ένα
`mousedown` στο «⋮» θα περνούσε ως σύρσιμο, θα καλούσε `preventDefault()`, και **το μενού δεν θα
άνοιγε ποτέ με ποντίκι**. Φιλτράρουμε **πριν** την ανάθεση: μόνος τρόπος να έχεις σωστό δείκτη
**και** λειτουργικό κουμπί. (Μετάλλαξη M7 — κοκκινίζει.)

**(γ) Η δέσμευση της αγκύρωσης γίνεται σε `pointerup`, όχι στο cleanup του effect.** Το cleanup
τρέχει επίσης σε κάθε αλλαγή εξάρτησης και στην αποπροσάρτηση — ένα resize παραθύρου στη μέση
του συρσίματος θα «άφηνε» την παλέτα μόνο του. Ο κύκλος ζωής του React **δεν** είναι συνώνυμο
της χειρονομίας του χρήστη.

**(δ) Η «πλησιέστερη ακμή» αντί για σειρά προτεραιότητας.** Όταν ο χώρος εργασίας είναι
στενότερος από `2 × 64px` οι ζώνες **επικαλύπτονται**· ένα `if (αριστερή) … else if (δεξιά)` θα
έδινε «αριστερά» σε όλο το πλάτος ⇒ η δεξιά αγκύρωση θα γινόταν σιωπηλά **απροσπέλαστη** σε
στενή οθόνη, και μόνο εκεί.

**(ε) Η καταγραφή της τελευταίας πλευράς ζει **μέσα** στο `setDockMode`.** Υπάρχουν **τέσσερις**
ανεξάρτητοι καλούντες (μενού «⋮», δεξί κλικ, απόθεση σε ζώνη, διπλό κλικ). Αν η καταγραφή ζούσε
στον καλούντα, θα αρκούσε **ένας** να την ξεχάσει ώστε η παλέτα να «γυρίζει αριστερά» ανεξήγητα
— και μόνο στη διαδρομή που κανείς δεν δοκίμασε. Ένας γράφων, μία εγγύηση.

#### 14.8.4 Γιατί ο καμβάς δεν χρειάστηκε **καμία** αλλαγή

Η μετάβαση αγκύρωση⇄αιώρηση **αλλάζει το πλάτος** του καμβά (ξυπνά ο `ResizeObserver`) **και**
αλλάζει το `mode` (το `useViewportManager` είναι ήδη συνδρομητής του `subscribeDockMode` από τη
Φ2). Δύο ανεξάρτητα μονοπάτια για το ίδιο γεγονός (N.7.2 #4) — και η υποδομή της Φ2 αποδείχθηκε
επαρκής χωρίς να το ξέρει. Συνέπεια: **κανένα αρχείο ADR-040 δεν αγγίχτηκε**, άρα το CHECK 6B
δεν ενεργοποιείται και δεν χρειάστηκε staged ADR-040.

#### 14.8.5 🔴 Mutation testing: **12 μεταλλάξεις, 11 κόκκινες, 1 ισοδύναμη — και 2 πραγματικά κενά**

Κάθε κρίσιμος φύλακας μεταλλάχθηκε επίτηδες και ελέγχθηκε αν τα tests κοκκινίζουν.

| # | Μετάλλαξη | Αποτέλεσμα |
|---|---|---|
| M1 | `resolveWorkspaceLayout('floating')` → `'sidebar-first'` (**η ακριβής παλινδρόμηση §4β**) | **7 failed** |
| M2 | `resolveDropTarget`: προτεραιότητα αριστερά αντί για πλησιέστερη ακμή | ⚠️ **πράσινο → κενό test** (βλ. κάτω) |
| M3 | `dockToFloatGeometry`: σταθερή μετατόπιση `+24` και στις δύο πλευρές | **2 failed** |
| M4 | `parseDockedSide` δέχεται `'floating'` | **2 failed** |
| M5 | `setDockMode` δεν καταγράφει την τελευταία πλευρά | **5 failed** |
| M6 | `resetDockLayout` δεν επαναφέρει τη μνήμη πλευράς | ⚠️ **ισοδύναμη μετάλλαξη** (βλ. κάτω) |
| M7 | Αφαίρεση φύλακα «μη-συρόμενος στόχος» (το «⋮» γίνεται λαβή) | **1 failed** |
| M8 | Διπλό κλικ χωρίς φύλακα (το «⋮» εναλλάσσει κατάσταση) | **1 failed** |
| M9 | Ο παρατηρητής ακούει **πάντα**, όχι μόνο όσο σέρνεται | **1 failed** |
| M10 | Το `pointercancel` αγκυρώνει κι αυτό | **1 failed** |
| M11 | Το `'floating'` ξαναφοράει το περίγραμμα της κάρτας | ⚠️ **πράσινο → κενό test** (βλ. κάτω) |
| M12 | Το αιωρούμενο `<aside>` ξαναπαίρνει `h-full` (ξεχειλίζει αντί να κυλά) | **1 failed** *(μετά την κάλυψη)* |

**🔴 M2 — το test ΔΕΝ ΜΠΟΡΟΥΣΕ να κοκκινίσει.** Έλεγχε «στενή οθόνη» με x=10 και x=90 σε πλάτος
100. Το x=90 έχει `distanceToLeft = 90 > 64`, άρα **δεν** ήταν στην αριστερή ζώνη: ο κλάδος της
επικάλυψης **δεν εκτελούνταν ποτέ**. Πράσινο test που επιβεβαίωνε άλλον κλάδο. Διορθώθηκε με
σημεία **μέσα** στην πραγματική επικάλυψη ([36, 64]) ⇒ M2 τώρα **1 failed**.

**🔴 M11 — κανένα από τα 177 tests δεν έβλεπε το «ένδυμα» της παλέτας.** Το
`WorkspaceSplitLayout.test.tsx` αντικαθιστά την αιωρούμενη παλέτα με δείκτη· το
`WorkspaceFloatingPalette.test.tsx` δεν αποδίδει `SidebarSection`. Άρα το διπλό περίγραμμα/σκιά
μέσα στο `FloatingPanel` ήταν **εντελώς αφύλακτο**. Γράφτηκε το `SidebarSection.variant.test.tsx`
(8 tests) ⇒ M11 **2 failed**, και προέκυψε το M12 από πάνω.

**⚪ M6 — ισοδύναμη μετάλλαξη, όχι κενό.** Το `resetDockLayout` καλεί ούτως ή άλλως
`setDockMode(DOCK_MODE_DEFAULT)`, που καταγράφει `'docked-left'` ως τελευταία πλευρά. Οι δύο
προεπιλογές **συμπίπτουν σήμερα**, άρα η ρητή γραμμή είναι σήμερα no-op. Δεν διαγράφηκε και δεν
«καλύφθηκε» με ψεύτικο test: υπάρχει για την ημέρα που το `DOCK_MODE_DEFAULT` θα γίνει
`'floating'` — τότε η σιωπηρή εξάρτηση θα άφηνε την «Επαναφορά» **μερική**. Καταγράφεται εδώ
ρητά ώστε να μη «βελτιστοποιηθεί» ως νεκρός κώδικας.

**Σύνολο: 185 tests πράσινα** (8 suites: workspace + layout + ui) · `jscpd:diff` **καθαρό** στα
13 αρχεία · ❌ **κανένα `tsc`** (N.17).

#### 14.8.6 ✅ Ζωντανή επαλήθευση στον browser (2026-07-29)

Chrome, viewport 2400×1121, από **καθαρό** localStorage. Οι τιμές διαβάστηκαν από το DOM.

| Έλεγχος | Μέτρηση |
|---|---|
| Αγκυρωμένη ⇒ διπλό κλικ επικεφαλίδας | `mode: floating`, διαχωριστικό **εξαφανίστηκε**, παλέτα στα `x=80, y=169` = χώρος εργασίας (56,145) **+24** ✅ |
| Δείκτης λαβής | αγκυρωμένη `cursor-default` · αιωρούμενη `cursor-grab` · κατά το σύρσιμο `cursor-grabbing` ✅ |
| Σύρσιμο στην **αριστερή** ζώνη | περίγραμμα «Αγκύρωση αριστερά κατά την απόθεση»· στο `pointerup` **αγκύρωσε αριστερά** ✅ |
| Σύρσιμο στη **δεξιά** ζώνη | περίγραμμα «Αγκύρωση δεξιά κατά την απόθεση» στα `x=2008, w=384` (= 56+2336−384) ✅ |
| Απόθεση στη **μέση** (x=1200) | **έμεινε αιωρούμενη** ✅ |
| Επιστροφή «εκεί που ήταν» | αγκύρωση δεξιά → αιώρηση → διπλό κλικ ⇒ **`docked-right`**, όχι η προεπιλογή ✅ (κανόνας Revit) |
| Αποθηκευμένη γεωμετρία νικά | η επανα-αιώρηση επέστρεψε στο αποθηκευμένο `{x,y}`, όχι στη γεωμετρία εκκίνησης ✅ |
| **Το κέρδος του §14.6.7.α** | αιωρούμενη στα **898px** ⇒ οι 8 καρτέλες σε **ΜΙΑ σειρά** (αγκυρωμένη: αδύνατο, μέγιστο 720 < 795) ✅ |

⚠️ **Παρατήρηση εργαλείου, όχι ελάττωμα προϊόντος.** Το «στιγμιαίο» σύρσιμο του αυτοματισμού
(pointerdown → moves → pointerup **στην ίδια εργασία**) **δεν** αγκυρώνει. Μετρήθηκε ότι τότε
δεν προλαβαίνει **ούτε** το `useDraggable` του ADR-723: η παλέτα μένει σε `isDragging`. Και οι
δύο προσαρτούν ακροατές σε effect **μετά** το commit του `isDragging` — μια χειρονομία που
τελειώνει πριν από το πρώτο καρέ είναι αόρατη και στους δύο. **Ανθρώπινο σύρσιμο διαρκεί
τουλάχιστον ένα καρέ**· με σωστά χρονισμένα συμβάντα (≥60ms) όλα λειτουργούν, όπως ο πίνακας
παραπάνω. Προϋπάρχον χαρακτηριστικό του ADR-723, όχι της Φ3.

#### 14.8.7 Τι **δεν** έγινε — ρητά

- **§9.2 view transitions** και **§9.3 βέλτιστο πλάτος**: NOT IMPLEMENTED (Φ4, εκτός σκοπού).
  Η μετάβαση dock⇄float είναι σήμερα **στιγμιαία** — καθαρή, αλλά χωρίς μεταμόρφωση.
- **§11.3 πλάτος ανά καρτέλα**: όχι· εκκρεμεί απόφαση Giorgio (το Revit δεν το κάνει).
- **§11.5** — η λάθος παραπομπή `floating-panel-geometry.ts:59 → ADR-030`: **δεν** διορθώθηκε,
  γιατί το αρχείο δεν χρειάστηκε να αλλάξει (ο κανόνας του χρησιμοποιήθηκε **αυτούσιος**).
- **Πλήκτρο για dock⇄float**: δεν υπάρχει. Το Revit επίσης δεν έχει. Η χειρονομία είναι
  προσβάσιμη μέσω του **μενού** (πληκτρολογήσιμο) — γι' αυτό το μενού δεν είναι διακοσμητικό.

#### 14.8.8 Google-level declaration (N.7.2)

| # | Ερώτηση | Απάντηση |
|---|---|---|
| 1 | Proactive ή reactive; | **Proactive** — η γεωμετρία εκκίνησης υπολογίζεται από τιμές που **ήδη** έχουν ιδιοκτήτη, χωρίς μεταβατική κατάσταση ανάμεσα σε δύο renders |
| 2 | Race condition; | **Όχι** — η αγκύρωση δεσμεύεται στο `pointerup` (γεγονός), όχι σε cleanup effect (κύκλος ζωής) |
| 3 | Ιδεμποτεντικό; | **Ναι** — `toggleDockFloat` ×2 = καμία αλλαγή· `resetDockLayout` ×2 = ίδιο αποτέλεσμα |
| 4 | Belt-and-suspenders; | **Ναι** — ο καμβάς μαθαίνει τη μετάβαση από **δύο** ανεξάρτητα μονοπάτια (§14.8.4) |
| 5 | SSoT; | **Ναι** — η αιωρούμενη γεωμετρία ανήκει **ολόκληρη** στο ADR-723· εδώ μηδέν clamp, μηδέν persistence, μηδέν σύρσιμο |
| 6 | Await ή fire-and-forget; | Ά/Α — σύγχρονες μεταβάσεις κατάστασης |
| 7 | Ποιος κατέχει τον κύκλο ζωής; | **Ρητά**: `WorkspaceSplitLayout` κατέχει «ποια διάταξη»· το store κατέχει «ποια κατάσταση»· το ADR-723 κατέχει «πού και πόσο μεγάλη» |

✅ **Google-level: ΝΑΙ** — η δυαδική παγίδα **εξαλείφθηκε δομικά** αντί να μπαλωθεί (εξαντλητικό
`switch` + διαχωρισμός τύπων ⇒ η επόμενη κατάσταση δεν *μπορεί* να ξεχαστεί σιωπηλά), μηδέν
διπλότυπο έναντι του ADR-723, και το mutation testing **βρήκε δύο πραγματικά κενά** που
καλύφθηκαν αντί να δικαιολογηθούν.

---

## 15. Πηγές

- [Revit — Dockable Windows (Autodesk Help)](https://help.autodesk.com/cloudhelp/2023/ENU/Revit-GetStarted/files/GUID-2FCA3097-36CC-4EED-B6BB-BAF431EC9475.htm)
- [Revit — Video: Dockable Windows](https://help.autodesk.com/view/RVT/2024/ENU/?guid=GUID-DAEAAE9B-EEC0-4948-9E42-C573B6DFDE18)
- [VS Code — Custom Layout](https://code.visualstudio.com/docs/configure/custom-layout)
- [VS Code issue #4660 — double-click border to optimal width](https://github.com/Microsoft/vscode/issues/4660)
- [VS Code issue #300121 — keyboard shortcut to resize secondary sidebar](https://github.com/microsoft/vscode/issues/300121)
- [react-resizable-panels (MIT)](https://github.com/bvaughn/react-resizable-panels)
- [MDN — CSS container queries](https://developer.mozilla.org/en-US/docs/Web/CSS/Guides/Containment/Container_queries)
- [Container queries in 2026: powerful, but not a silver bullet — LogRocket](https://blog.logrocket.com/container-queries-2026/)
- [MDN — View Transition API](https://developer.mozilla.org/en-US/docs/Web/API/View_Transition_API)
