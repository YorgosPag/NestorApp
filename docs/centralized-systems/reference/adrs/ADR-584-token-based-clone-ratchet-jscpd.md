# ADR-584: Token-based Copy/Paste Clone Ratchet (jscpd) — CHECK 3.28

## Status
✅ **ACTIVE — 2026-07-08 — BASELINE 4548 clones (3.92%) over src/** — Layer 1 (pre-commit diff gate) + Layer 2 (CI full-repo ratchet) live. Baseline: `.jscpd-baseline.json`. Config SSoT: `.jscpdrc.json`. Engine: `scripts/check-jscpd-ratchet.js` (+ 39 Jest tests). CI: `.github/workflows/jscpd-ratchet.yml`.

**Related:**
- **ADR-314** (SSoT Discover Ratchet, CHECK 3.18) — the name/regex-based detector this ADR complements. CHECK 3.18 finds duplicate *names* and registry gaps; CHECK 3.28 finds duplicate *token sequences* regardless of name. Both run; neither replaces the other.
- **ADR-294** (SSoT Ratchet Enforcement) — the ratchet philosophy (violations only decrease) that this check mirrors.
- **ADR-299** (Ratchet Backlog Master Roadmap) — CHECK 3.28 is a new ratchet candidate; baseline drift >10% updates §2 there.
- **CHECK 3.22** (Dead-code Ratchet) — the "fast local smart-skip + authoritative CI full scan" pattern reused here.

---

## Context

Giorgio's recurring failure mode, confirmed 2026-07-08: **when centralizing code, new duplicates are born in the same commit.** The pattern is "centralize A, then write B and C as parallel twins" — e.g. `clipHatch` copy-pasted as `clipHatchByPoly` (identical body, different name).

The existing gate is structurally blind to this:
- **CHECK 3.7** (`.ssot-registry.json` ratchet) only blocks regressions of *already-registered* patterns.
- **CHECK 3.18** (`ssot:discover`) discovers *new* duplicate **names** and registry gaps — but it is **name/regex-based**. A clone with a **different name** is invisible to it. `grep -i jscpd package.json` = empty → no token-based detector existed.

Big-player practice for exactly this problem:
- **Google**: Tricorder static analysis + automated clone detection + canonical libs (Abseil/Guava).
- **Meta/Figma**: CI clone-detection gates + shared design-system packages.
- **Universal tool**: a **token-based Copy/Paste Detector** with a **ratchet baseline** — catches clones regardless of name, even within a single diff.

The chosen tool is **jscpd** (v5.0.11, **MIT** — verified via `npm view jscpd license` and the installed `package.json`; a `devDependency`, so it is excluded from CHECK 12's `--production` license scan). It is a token-based detector with a JSON reporter.

---

## Decision

Add **CHECK 3.28 — token-based clone ratchet** as a **two-layer** gate that mirrors CHECK 3.18 / CHECK 3.22, **reusing the existing ratchet framework** (baseline-load → scan → compare → smoke/full modes → Jest suite) rather than inventing a new one.

### Layer 1 — pre-commit diff gate (`--diff`)
`scripts/git-hooks/pre-commit` Phase 0.6 runs jscpd over **only the staged src files**. Because the scan set *is* the diff, every clone jscpd reports is a **same-commit sibling clone** → **BLOCK**, listing the offending file pairs and line ranges. This is the direct guarantee against Giorgio's failure mode (α).

Runs **sequentially in bash**, NOT inside `run-checks-parallel.js`, because jscpd spawns child processes which deadlock inside the Node `worker_threads` pool — the same reason Phase 0.5 (hardcoded-strings) runs sequentially.

Zero-tolerance on new intra-diff clones (Giorgio's choice: *hybrid* gate). Escape hatch: `SKIP_JSCPD_DIFF=1 git commit …` (must be justified to Giorgio).

### Layer 2 — CI full-repo ratchet (`--full`)
`.github/workflows/jscpd-ratchet.yml` re-runs a **full `src/` scan** on every PR / push to `main`, reads `statistics.total.clones` from the JSON report, and compares it to `.jscpd-baseline.json`. **Total rose → BLOCK.** Ratchet-down only: duplication may only decrease (β). This is the authoritative gate and also catches a new file cloning **already-committed** code, which a staged-only scan cannot see. Also runnable locally: `JSCPD_FULL=1` or `npm run jscpd:check`.

### Config SSoT
`.jscpdrc.json` is the single source of truth for `minTokens`, formats, and ignore globs. The engine, the npm scripts, and CI all defer to it — no second threshold is hardcoded anywhere.

### Resolved open decisions (Giorgio, 2026-07-08)
| Decision | Choice | Rationale |
|----------|--------|-----------|
| **Threshold** (`minTokens`) | **50** (strict, ~5–8 lines) | Catches small clones like `clipHatch`/`clipHatchByPoly`. Maximum guarantee. |
| **Scope** | **All of `src/`** | Big-player standard: lock the whole app's current state, ratchet down from there. |
| **Gate strictness** | **Hybrid** | Pre-commit blocks any NEW intra-diff clone (α) + CI ratchets the total (β). |
| **Local cost** | staged-only fast local + full authoritative in CI | Mirrors CHECK 3.22; full scan is ~10s and stays in CI. |

### AI-workflow self-diff guard
Before declaring any centralization "done", the agent runs `npm run jscpd:diff <its staged files>` (same engine as the hook) so it cannot ship sibling clones and call it centralized. Encoded as CLAUDE.md rule **N.18** + memory `reference_jscpd_clone_ratchet_ssot`.

---

## Architecture

```
                      .jscpdrc.json  (SSoT: minTokens=50, formats, ignores)
                             │
          ┌──────────────────┴───────────────────┐
          │                                       │
  Layer 1 (pre-commit)                    Layer 2 (CI, authoritative)
  scripts/git-hooks/pre-commit            .github/workflows/jscpd-ratchet.yml
  Phase 0.6  → --diff <staged src>        → --full  (whole src/)
  blocks same-commit sibling clones       ratchets statistics.total.clones
          │                                       │      vs .jscpd-baseline.json
          └───────────────┬───────────────────────┘
                          │
             scripts/check-jscpd-ratchet.js
             parseArgs · runScanner(jscpd) · parseSummary
             loadBaseline · compare · writeBaseline
             runDiff · runFull · runSmoke
                          │
             scripts/__tests__/check-jscpd-ratchet.test.js  (39 tests)
```

**npm scripts:**
- `jscpd:scan` — human HTML/console report (diagnose where clones are)
- `jscpd:baseline` — lock the current total into `.jscpd-baseline.json`
- `jscpd:check` — full scan + ratchet compare (what CI runs)
- `jscpd:diff <files>` — the pre-commit / self-guard diff gate
- `test:jscpd` — the Jest suite

---

## Baseline & maintenance

- **Baseline**: 3059 clones / 36135 duplicated lines (2.31%) over `src/`, min-tokens 50, 2026-07-16 (`.jscpd-baseline.json` — SSoT). Ξεκίνησε στα 4548 / 58309 (3.92%) στις 2026-07-08.
- After legitimate de-duplication, run `npm run jscpd:baseline` to lock progress downward.
- The baseline **never rises** without an explicit, justified refresh.

---

## Changelog

- **2026-07-17** — Clone dedup — **`WallDna`: η αριθμητική μπήκε στο `LayeredBuildup` SSoT, το σχήμα ΟΧΙ (σκόπιμα)**. **UNCOMMITTED.** 2 modules + 1 suite / 1 domain (`bim/types`). Ο τοίχος ήταν ο **μόνος** από τα τρία compound-structure entities εκτός SSoT (slab + roof ήδη μέσα).
  - **Τι ήταν διπλό.** Το `computeTotalThickness` (`wall-dna-types.ts`) είχε σώμα **byte-identical** με το `computeBuildupTotalThickness` (`layered-buildup.ts`) — `layers.reduce((sum, layer) => sum + layer.thickness, 0)`. **jscpd-ΑΟΡΑΤΟ**: 3 γραμμές, πολύ κάτω από τα `min-tokens: 50` του `.jscpdrc.json` — γι' αυτό επέζησε ενώ ο slab είχε ήδη μεταναστεύσει. **Το κατώφλι tokens είναι ο μηχανισμός, όχι η αλήθεια** — τα μικρά διπλότυπα θέλουν μάτι, όχι εργαλείο.
  - **Fix — thin wrapper, public API αμετάβλητο** (το archetype της family-types συνεδρίας). Το `computeTotalThickness` κρατά όνομα + υπογραφή + export, delegate-άρει στο generic — **ακριβώς** το σχήμα που ο slab έχει ήδη στο `computeSlabTotalThickness` (`slab-dna-types.ts:49-53`). Δεν εφευρέθηκε μοτίβο· αντιγράφηκε το υπάρχον. **Blast radius: 1 αρχείο πηγής** — οι **6** καταναλωτές του `computeTotalThickness` δεν αγγίχτηκαν.
  - **🔴 Το `WallDna = LayeredBuildup<WallLayerSide>` ΔΕΝ έγινε — απόφαση, όχι παράλειψη.** Τα wall layers κουβαλάνε `side`, το SSoT λέει `zone`. Το `dna` είναι **persisted σε ΔΥΟ επίπεδα**: `WallParams.dna` (`wall-types.ts:131`, entity docs) **και** `WallTypeParams.dna` (`bim-family-type.ts:99` → `typeParams` → Firestore, ADR-412). Άρα `side`→`zone` = **migration υπαρχόντων εγγράφων**, όχι rename — **75 σημεία / 22 αρχεία** (grep-μετρημένα, όχι εκτίμηση backlog).
  - **Πρακτική μεγάλων παιχτών (απόφαση Giorgio 2026-07-17).** Revit («Compound Structure») και IFC (`IfcMaterialLayerSet`) έχουν **ένα** ενιαίο concept → το (β) είναι εννοιολογικά σωστό. **ΑΛΛΑ κανένας τους δεν κάνει schema migration για κοσμητική μετονομασία πεδίου** — versioned schema + reader που δέχεται και τα δύο. Εφόσον το `BuildupThicknessSource` **ήδη** γεφυρώνει τη διαφορά δομικά, το (β) πληρώνει migration ρίσκο σε δύο persisted collections για ένα όνομα. **Αναβλήθηκε ρητά.** Αν ποτέ γίνει: ADR + **reader-tolerant migration** (δέχεται `side` ΚΑΙ `zone` στο load, γράφει πάντα `zone`), **ΟΧΙ** big-bang backfill. Το «παραμετρικό όνομα πεδίου στο `LayeredBuildupLayer`» **απορρίφθηκε** — ρητή απαγόρευση αυτού του ADR.
  - **Η απόκλιση ήταν ΣΙΩΠΗΛΗ — τώρα είναι τεκμηριωμένη**, στα headers **και των δύο** modules (ο επόμενος που θα δει `side` vs `zone` θα ξέρει *γιατί*, χωρίς να ξανα-ανακαλύψει το persistence).
  - **⚠️ Το ίδιο το SSoT έλεγε ψέματα.** Το `layered-buildup.ts` ισχυριζόταν ότι *«per-entity modules (`wall-dna-types.ts`, `slab-dna-types.ts`) parametrise it»* — **ο τοίχος ΔΕΝ το παραμετροποιεί**. Αντικαταστάθηκε με ρητό **adoption status** (πλήρες: slab/roof · μερικό: wall). Τρίτο περιστατικό σχολίου που ισχυρίζεται κάλυψη ανύπαρκτη — **τα σχόλια δεν επαληθεύονται από κανέναν έλεγχο**, όπως τα `@enterprise` tags.
  - **⚠️ Εύρημα εκτός στόχου: το `layered-buildup` ΔΕΝ έχει module στο `.ssot-registry.json`** (grep → κενό). Το SSoT υπάρχει στον κώδικα αλλά **κανένα ratchet δεν το προστατεύει** — γι' αυτό ο τοίχος μπόρεσε να ξεφύγει σιωπηλά και να επιβιώσει. **Δεν προστέθηκε** (N.12 θέλει εντολή + baseline, που τα κάνει ο Giorgio).
  - **Tests: +2 στο υπάρχον `wall-dna-types.test.ts`** — wiring, όχι συμπεριφορά: (1) parity `computeTotalThickness` ↔ `computeBuildupTotalThickness` σε **και τα 7 seeds**, (2) το `WallDna` ικανοποιεί δομικά το `BuildupThicknessSource` (compile-time) + το generic `buildupBoundaryFractions` δουλεύει πάνω του. **11/11 GREEN**· τα προϋπάρχοντα **αμετάβλητα** (η απόδειξη ότι το API δεν έγειρε). Ευρύτερο σάρωμα: **79 suites / 1039 tests GREEN**.
  - **Mutation-verified ×2**: (1) ο τοίχος ξανα-inline-άρει σώμα που **αποκλίνει** → 2 tests έσπασαν· (2) το **generic** αποκλίνει (τα δύο κινούνται μαζί → το parity test είναι **τυφλό** εξ ορισμού) → το έπιασαν τα hardcoded seed thicknesses (210/310/160). **Η δεύτερη mutation είναι το ουσιώδες**: δείχνει ότι το parity test **μόνο του** δεν αρκεί — η κάλυψη προκύπτει από τον συνδυασμό. Revert → 11/11. `jscpd:diff` καθαρό.
- **2026-07-16** — Clone dedup — **`useCameraCapture` ↔ `useVideoRecorder`: η κοινή `getUserMedia` συνεδρία**. **UNCOMMITTED.** 3 hooks + 2 νέα SSoT modules / 1 domain. **Μετρημένο: 4 clones / 64 διπλές γραμμές / 369 διπλά tokens → 0 / 0 / 0.** Τα συνολικά tokens **έπεσαν** (3474 → 3396) **παρότι** τα 2 νέα modules μπήκαν στο σάρωμα — δηλαδή δεν μετακινήθηκε κώδικας, **αφαιρέθηκε**.
  - **Τι ήταν διπλό.** Τα δύο hooks κάνουν **γνήσια διαφορετικά πράγματα** (canvas frame-grab → JPEG vs MediaRecorder → webm/mp4) πάνω σε **γνήσια ταυτόσημη προϋπόθεση**: μια ζωντανή κάμερα δεμένη σε `<video>`, με τα σφάλματα άδειας χαρτογραφημένα σε σταθερό κωδικό και κάθε track σταματημένο στο teardown. Το `mapErrorToCode` ήταν **byte-identical** στα δύο αρχεία — **μια διόρθωση στο ένα δεν θα έφτανε ΠΟΤΕ στο άλλο**. Ίδια ιστορία σε `startCamera` / `stopCamera` / cleanup effect / error-code union.
  - **Fix — «shared primitive + per-instance binding»** (το archetype των ADR-591 / ADR-664). **ΝΕΟ `src/hooks/media/useMediaStreamSession.ts`**: κατέχει status/error state, `videoRef`, `streamRef`, `startCamera`, `stopCamera`, το unmount cleanup και το **μοναδικό** `mapMediaErrorToCode`. Τα δύο hooks δίνουν **μόνο** ό,τι πραγματικά διαφέρει: `audio` (false/true), `logger` (ώστε το telemetry να κρατά το όνομα του καθενός), `onStreamReady` (device enumeration — μόνο η κάμερα), `onStopped`.
  - **Τα extended statuses ΔΕΝ ισοπεδώθηκαν.** Type parameter `TExtra`: `CameraCaptureStatus = MediaSessionStatus | 'capturing'`, `VideoRecorderStatus = MediaSessionStatus | 'recording' | 'finalizing'` → **ταυτόσημα unions με πριν**, χωρίς lowest-common-denominator status. **Public API αμετάβλητο** — ο μοναδικός καταναλωτής (`CameraCaptureDialog.tsx`, εισάγει και το `CameraCaptureErrorCode`) **δεν αγγίχτηκε**.
  - **2ο μικρό SSoT (N.0.2) — ΝΕΟ `src/lib/media/media-mime.ts`**: `pickSupportedMime()` + `getExtensionFromMime()`, κοινά με το **`useVoiceRecorder`** που είχε δικό του αντίγραφο του ίδιου probe loop + extension switch. **Οι candidate lists ΕΜΕΙΝΑΝ στους καλούντες** — είναι domain data (vp9/opus vs opus), δεν ανήκουν σε generic helper.
  - **⚠️ Το `useVoiceRecorder` ΔΕΝ μπήκε στη session — ΔΕΝ είναι δίδυμο.** Χωρίς `videoRef`/live preview/`stopCamera`, **string-matching** αντί για `mapErrorToCode`, άλλο status union (`transcribing`/`done`), επιστρέφει κείμενο όχι `File`, και είναι δεμένο με το Whisper gateway. Θα ήταν god-shell. Μοιράζεται **μόνο** τους MIME helpers.
  - **⚠️ Το `ADR-311` που επικαλούνται ΚΑΙ ΤΑ ΔΥΟ hooks (`@enterprise ADR-311 - Desktop Camera Capture via WebRTC`) ΔΕΝ ΥΠΑΡΧΕΙ ως αρχείο** — phantom reference, **ακριβώς όπως το ADR-307 στο ADR-664**. Τα `@enterprise` tags **δεν επαληθεύονται** από κανέναν έλεγχο· δεύτερο περιστατικό → μοτίβο, όχι σύμπτωση.
  - **Registry**: νέο module **`media-stream-session`**. Pattern = **`OverconstrainedError`** — εμφανίζεται **πουθενά αλλού** παρά σε αντιγραμμένο getUserMedia error mapper (grep-verified: μόνο SSoT + test, **και τα δύο allowlisted → 0 παραβιάσεις → κανένα `ssot:baseline`**). Το `facingMode` **ΑΠΟΡΡΙΦΘΗΚΕ** ως pattern: υπάρχει και σε **σχόλιο** του `useCameraCapture` → false positive. **86/86 registry golden tests GREEN.**
  - **Tests: ΚΑΙ ΤΑ ΔΥΟ hooks ήταν ΑΤΕΣΤΩΤΑ** παρότι shippάρουν στον camera dialog. **3 ΝΕΑ suites / 49 tests**: `useMediaStreamSession.test.tsx` (session contract + και οι 8 κλάδοι του error mapper), `media-hook-bindings.test.tsx` (τι κατέχει ο καθένας πάνω από τη session), `media-mime.test.ts` (probe + extensions). **49/49 GREEN.**
  - **Mutation-verified**: (1) `audio: audioRef.current` → `false` (ο recorder χάνει ήχο **σιωπηλά**), (2) το `onStopped` δεν καλείται (stale `activeDeviceId`) → **4 tests έσπασαν σε ΔΥΟ επίπεδα** (core + bindings) → revert → 49/49.
  - **Ένα test μου ήταν λάθος, όχι ο κώδικας**: περίμενα `Camera 3` για τη 2η κάμερα μετά από ένα audioinput· το `filter` τρέχει **πριν** το `map`, άρα ο δείκτης μετρά **κάμερες** → `Camera 2`. Σωστό για τον χρήστη· διορθώθηκε η προσδοκία.
  - **Καταγράφηκε, ΔΕΝ πιάστηκε**: υπάρχουν **ΔΥΟ** `useVoiceRecorder` (`src/hooks/` + `src/subapps/dxf-viewer/text-engine/ai/` — ADR-344 Φ12, δικό του `DXF_TIMING` config). Άλλο domain (subapp άλλου πράκτορα) → backlog.
  - **⚠️ Προϋπάρχον latent bug, ΔΕΝ διορθώθηκε (εκτός σκοπού)**: το `stopCamera` του recorder **δεν** σταματά το tick ούτε τον MediaRecorder — stopCamera την ώρα της εγγραφής αφήνει το `durationMs` να τρέχει με status `idle`. Θέλει δική του απόφαση συμπεριφοράς.
  - **Verification**: `jscpd:diff` καθαρό σε **8** αρχεία (ΧΩΡΙΣ `SKIP_JSCPD_DIFF`)· 0 ορφανά imports (grep ανά σύμβολο και στα 4 αρχεία). **ΟΧΙ tsc (N.17).** 🔴 Giorgio commit: `useMediaStreamSession.ts` + `media-mime.ts` + 3 test suites + `useCameraCapture.ts` + `useVideoRecorder.ts` + `useVoiceRecorder.ts` + `.ssot-registry.json`.

- **2026-07-16** — Clone dedup — **`family-type-ui-helpers.ts`: το ΤΕΤΡΑΠΛΟ copy-paste → generic factory**. **UNCOMMITTED.** 2 αρχεία / 1 domain (Plan Mode, εγκεκριμένο από Giorgio μετά το N.8 gate). **Η ίδια πεντάδα helpers ήταν γραμμένη 4 φορές** (wall/slab/roof/opening = 20 συναρτήσεις): `asXFamilyType` · `listXTypes` · `getOverriddenXParamKeys` · `resolveXTypeAssignment` · `normaliseXOverrides`. Τα σώματα διέφεραν **μόνο** σε 3 σημεία: category string, `X_OVERRIDABLE_KEYS`, `resolveEffectiveXParams`.
  - **🔴 jscpd-ΑΟΡΑΤΟ — γι' αυτό έχει σημασία.** Το CHECK 3.28 **δεν** το σήμανε ποτέ: κάθε block πέφτει **κάτω** από το κατώφλι των 50 tokens του `.jscpdrc.json`. Βρέθηκε **μόνο διαβάζοντας** το αρχείο. Επιβεβαιώνει ότι το token-based ratchet είναι **δίχτυ, όχι εγγύηση** — τα «πολλά μικρά δίδυμα» περνούν από κάτω και θέλουν ανθρώπινο/agent μάτι.
  - **SSoT audit ΠΡΙΝ** (εντολή Giorgio): μηδέν hits για `makeFamilyType*` → δεν υπήρχε διπλότυπος μηχανισμός. Το `useFamilyTypeEditor` + `family-type-properties-parts` είναι **άλλο επίπεδο** (UI widget chrome) — δεν καλύπτει αυτούς τους helpers. **Το αδελφό `resolve-effective-params.ts` είχε ΗΔΗ το σχήμα-στόχο** (generic `resolveEffectiveParams<P,TP>` + 4 thin named wrappers) → επεκτάθηκε καθιερωμένη πρακτική **του ίδιου φακέλου**, δεν εφευρέθηκε νέα.
  - **Fix**: module-private `makeFamilyTypeHelpers<C, P>(category, overridableKeys, resolveEffective)` — **3 params** (κάτω από το όριο-4 του ADR-584, άρα **δεν** χρειάστηκε spec object) + `normaliseTypeOverrides<TP>` (category-agnostic, εκτός factory). Κάθε κατηγορία = ~5 γρ. config + named re-exports. **Μετρημένο: 261 → 169 γραμμές κώδικα (-35%)**· σύνολο 396 → 378 γρ. — **το σύνολο έπεσε μόνο 18 γρ. επειδή το JSDoc μεγάλωσε σκόπιμα** (module header + factory contract). Ο στόχος ήταν η εξάλειψη του διπλότυπου, όχι το line count.
  - **Το `C` παράγει το `TP`** μέσω `BimTypeParamsByCategory[C]` → ο compiler **απαγορεύει** το λάθος ζευγάρωμα (`'slab'` + `WALL_OVERRIDABLE_KEYS` + `resolveEffectiveRoofParams` δεν χτίζει). Ασφάλεια που τα hand-copied blocks **δεν μπορούσαν** να έχουν. Νέα 5η κατηγορία (`stair` — έχει ήδη `StairTypeParams`, δεν έχει helpers) = ένα config block.
  - **Public API ΑΜΕΤΑΒΛΗΤΟ** (προτίμηση Giorgio): και οι 20 εξαγωγές μένουν με ίδιες υπογραφές → **οι 14 καταναλωτές δεν αγγίχτηκαν** (widgets/dialogs/controllers/stores). Τα 4 `*TypeAssignment` interfaces είναι δομικά ταυτόσημα → structural assignability, **μηδέν cast**. Διατηρήθηκε και η **ασύμμετρη ονοματολογία** (ο wall δεν έχει infix: `getOverriddenParamKeys`/`normaliseOverrides`) — μετονομασία = API break για μηδέν όφελος.
  - **ΟΧΙ νέο cast**: το μόνο `as` είναι το narrowing `type as BimFamilyType<C>`, που **προϋπήρχε τετραπλασιασμένο** (`as BimFamilyType<'wall'>` κ.λπ.) — γενικεύτηκε, δεν προστέθηκε. Μηδέν `as any`.
  - **Tests: slab/roof/opening ήταν ΑΤΕΣΤΩΤΑ** — το `family-type-ui-helpers.test.ts` (10 tests) κάλυπτε **μόνο wall**. Τα 10 έμειναν **αμετάβλητα** (= η απόδειξη ότι το API δεν έγειρε) και περνούν. **ΝΕΟ `__tests__/family-type-helpers-parity.test.ts` (26 tests)** — στοχεύει το **wiring**, όχι τη συμπεριφορά: το σώμα αποδεικνύεται **μία φορά** από τα wall tests, και τα λάθος triples τα κόβει ο compiler· ο **μόνος** εναπομείνας κίνδυνος είναι copy-paste slip στις 20 αναθέσεις (`asRoofFamilyType = slabHelpers.asFamilyType` **συμπεραίνεται καθαρά** — ο compiler δεν το πιάνει).
  - **Mutation-verified ×2, και το 2ο διόρθωσε λάθος ΔΙΚΟ μου σχόλιο**: (1) `asRoofFamilyType → slabHelpers` → 2 roof narrowing tests έσπασαν ✓ — **αλλά ο disjoint έλεγχος ΔΕΝ έσπασε**, ενώ το σχόλιό μου ισχυριζόταν ότι τον καλύπτει· το `listTypes` κλείνει πάνω στο **εσωτερικό** narrower της factory, όχι στο export. Το σχόλιο διορθώθηκε ώστε να λέει τι πραγματικά πιάνει. (2) `listRoofTypes → slabHelpers.listTypes` → disjoint + roof listTypes έσπασαν ✓ → revert → **205/205 GREEN (17 suites)**.
  - **Registry (N.12): ΔΕΝ προστέθηκε module — σκόπιμα.** Το `makeFamilyTypeHelpers` είναι **module-private** (δεν εξάγεται) → δεν υπάρχει επιφάνεια να το παρακάμψει άλλο αρχείο· ένα `forbiddenPattern` θα ήταν θόρυβος χωρίς αντικείμενο προστασίας. Ο φύλακας εδώ είναι το parity test.
  - **Verification**: `npx jest src/subapps/dxf-viewer/bim/family-types` → **205/205**· `jscpd:diff` καθαρό (ΧΩΡΙΣ `SKIP_JSCPD_DIFF`). **ΟΧΙ tsc (N.17).** 🔴 Giorgio commit: `src/subapps/dxf-viewer/bim/family-types/family-type-ui-helpers.ts` + `src/subapps/dxf-viewer/bim/family-types/__tests__/family-type-helpers-parity.test.ts`.

- **2026-07-16** — Clone dedup — **`useBorderTokens` self-clones** (`src/hooks/useBorderTokens.ts`). **UNCOMMITTED.** 1 αρχείο / 1 domain. Ο επόμενος στόχος της λίστας του `src/hooks/` cluster μετά το ADR-664 + το ADR-195 Phase 12. **Μετρημένο αποτέλεσμα: 3 clones / 112 διπλές γραμμές (22.63% του αρχείου) → 0 / 0** (jscpd απευθείας στο αρχείο, HEAD vs refactored). 495 → 467 γρ.
  - **Τι ήταν διπλό.** (α) Το **`quick` object γραμμένο ΔΥΟ φορές** — μία μέσα στο hook και μία στο static export `borderTokens.quick` (26 κλειδιά, byte-identical). Δύο ορισμοί της **ίδιας** οπτικής γλώσσας = εγγυημένο drift: κάθε αλλαγή έπρεπε να γίνει σε 2 σημεία, με **μηδέν** μηχανισμό να το επιβάλλει. (β) Το ζεύγος `colorMap` + `directionMap` γραμμένο **ΤΡΕΙΣ** φορές (`getDirectionalBorder` / `getMultiDirectionalBorder` / `getCombinedBorder`).
  - **Fix**: module-level `QUICK_BORDERS` (μία φορά, `as const`) που τροφοδοτεί **και** το `useBorderTokens().quick` **και** το `borderTokens.quick` → **δεν μπορούν πια να αποκλίνουν**· `STATUS_BORDER_COLOR` + `BORDER_DIRECTION` + `STATUS_BASE_BORDER` hoisted· pure helper `directionalBorder(status, directions)` = η **μοναδική** υλοποίηση πίσω και από τις 3 μεθόδους (`getDirectionalBorder` = `directionalBorder(status, [direction])`). Νέοι τύποι `BorderStatus`/`BorderDirection` αντικαθιστούν 6 inline union επαναλήψεις.
  - **Public API αμετάβλητο** — 30+ καταναλωτές του hook + 20 του static export **δεν αγγίχτηκαν**.
  - **⚠️ ΔΕΝ ενοποιήθηκε το `getStatusBorder` με το `STATUS_BASE_BORDER`, παρότι μοιάζουν.** **Αποκλίνουν πραγματικά**: το `getStatusBorder('default')` επιστρέφει `border-[1px] border-[rgb(229,231,235)]` (**interpolated design tokens**), ενώ το `getCombinedBorder('default')` επιστρέφει `border border-border` (**Tailwind/CSS-var**). Ενοποίηση = σιωπηλή αλλαγή οπτικού αποτελέσματος σε **δύο** διαφορετικά σύνολα καταναλωτών. Η ομοιότητα είναι **επιφανειακή** — το άφησα.
  - **Tests: το αρχείο ήταν ΑΤΕΣΤΩΤΟ** παρά τους 50+ καταναλωτές. **ΝΕΟ `src/hooks/__tests__/useBorderTokens.test.ts` (41 tests)**, γραμμένο **ΠΡΙΝ** το refactor ως characterization suite → **41/41 GREEN στον HEAD κώδικα**, μετά **41/41 GREEN στον κεντρικοποιημένο**. Το test `quick` ↔ `borderTokens.quick` **απέδειξε** την ταυτότητα που δικαιολογεί το collapse — δεν την υπέθεσα.
  - **Mutation-verified**: (1) `subtle: 'border'` → `'gray-500'` στον κοινό χάρτη, (2) `directionalBorder` αντιστρέφει τη σειρά των directions → **3 tests έσπασαν** → revert → 41/41.
  - **Boy Scout (N.0.2)**: αφαιρέθηκε **προϋπάρχον νεκρό import** `borders` (μόνο στη γραμμή import· οι άλλες 3 εμφανίσεις ήταν σχόλια).
  - **Verification**: `jscpd:diff` καθαρό (ΧΩΡΙΣ `SKIP_JSCPD_DIFF`)· 0 ορφανά imports (grep και στα 8 σύμβολα). **ΟΧΙ tsc (N.17).** 🔴 Giorgio commit: `src/hooks/useBorderTokens.ts` + `src/hooks/__tests__/useBorderTokens.test.ts`.

- **2026-07-16** — Clone dedup — **`RibbonRoofTypePropertiesWidget`: ο ΤΡΙΤΟΣ family-type αδελφός**. **UNCOMMITTED.** Τα wall/opening είχαν ήδη κεντρικοποιηθεί σε `useFamilyTypeEditor` + `family-type-properties-parts`· το roof έκανε την ίδια δουλειά με **δικό του αντίγραφο** (rename draft + `useEffect` re-sync + `commitRename` + `onNameKeyDown` + header markup). Πλέον καταναλώνει το SSoT: **159 → 105 γρ.**, μηδέν δική του rename λογική.
  - **⚠️ Από τα «2 πιθανά bugs» της καταγραφής, ΤΟ ΕΝΑ ΔΕΝ ΕΙΝΑΙ BUG — επαληθεύτηκε πριν γραφτεί κώδικας.** Η καταγραφή σήμαινε 🔴 το `commitRename` που συγκρίνει `next === currentType.name` (stored) αντί για `typeName` (display). **Για το roof είναι ισοδύναμο σήμερα**: το `resolveTypeDisplayName` αποκλίνει από το `type.name` **μόνο** για built-in (→ `t(key)`) και για auto **wall** types (`AUTO_WALL_TYPE_NAME_KEY = 'auto.wall.generic'`, wall-specific)· τα built-in κόβονται νωρίτερα από το `isBuiltInType` guard του ίδιου `commitRename`, και **`origin: 'auto'` υπάρχει ΜΟΝΟ στο `auto-wall-type.ts`** → δεν υπάρχουν auto roof types. Άρα ήταν **latent/εύθραυστο**, όχι ενεργό bug. Το merge το κάνει σωστό ούτως ή άλλως (+ το `typeName` μπαίνει στα deps, που έλειπε).
  - **Το ΑΛΛΟ είναι πραγματικό**: το roof έδειχνε `{typeName} · builtinBadge` σε **ΚΑΘΕ** μη-editable κλάδο, ενώ `editable = !isBuiltIn && canWrite`. Με **`canWrite === false`** (`create-family-type-controller.ts:352` → `canWrite: !!service`, δηλαδή service μη διαθέσιμο) ένας **user type εμφανιζόταν ως «built-in»**. Το SSoT header ρωτά `editor.isBuiltIn`, όχι «δεν είναι editable» → σωστό. Σπάνιο μονοπάτι, αλλά υπαρκτό.
  - **ΟΧΙ `FamilyTypeActions` εδώ** (σε αντίθεση με το wall): Edit/Delete για roofs ζουν στο sibling `RibbonRoofFamilyTypeWidget` → θα διπλασιάζονταν τα κουμπιά. Το `openEditRoofType` περνιέται στον editor ώστε το clone-to-edit να μένει καλωδιωμένο.
  - **i18n**: **καμία νέα key** — όλα τα κοινά (`properties`/`rename`/`renameTooltip`/`builtinBadge`/`resetToType*`/`override*`/`paramMaterial`/`paramThickness`/`thicknessUnit`/`materialNone`) υπάρχουν ήδη στο `bimFamilyType`. Ορατή αλλαγή: το roof διαβάζει πλέον από `bimFamilyType.*` αντί `roofFamilyType.*` (ίδια labels — ήταν διπλό block)· **ΚΡΑΤΗΘΗΚΕ** το `roofFamilyType.material.{rc,tile,wood}` (roof-specific τιμές). Το rename input αποκτά `<Tooltip>` + focus ring (styling του SSoT).
  - **⚠️ Dead locale keys ΔΕΝ σβήστηκαν**: τα `roofFamilyType.{properties,rename,builtinBadge,resetToType,resetToTypeTooltip,override,overrideTooltip,paramMaterial,paramThickness,thicknessUnit,materialNone}` μπορεί να χρησιμοποιούνται ακόμα από `EditRoofTypeDialog`/`RibbonRoofFamilyTypeWidget` → θέλει grep ανά key, χωριστό βήμα.
  - **Verification**: `jscpd:diff` ✅ 0 clones· **18/18 `useFamilyTypeEditor` tests GREEN**· ο `useRoofFamilyTypeController` ταιριάζει δομικά στο `FamilyTypeEditorController` (ίδιο σχήμα με τον wall, που ήδη το καταναλώνει). **ΟΧΙ tsc (N.17).** ⚠️ **browser-verify pending**.

- **2026-07-16** — Clone dedup — **`FitToViewService`: τα δύο fit-to-view δίδυμα** (backlog item ADR-394, flagged 2026-06-11). **UNCOMMITTED.** 1 αρχείο / 1 domain.
  - **Ο ίδιος ο κώδικας το παραδεχόταν** (`NOTE: duplicated guard logic also lives in calculateFitToViewTransform above — flagged for SSoT unification`): `calculateFitToViewTransform` (scene-based) και `calculateFitToViewFromBounds` (pure-bounds) είχαν **~60 γραμμές ταυτόσημα** — padding clamp, degenerate-axis substitution, scaleX/scaleY, NaN guards, offset (align/center).
  - **Το κόστος ΔΕΝ ήταν θεωρητικό**: το ADR-394 degenerate-line fix («Z σε μονή αξονική γραμμή») μπήκε αρχικά **μόνο στο ένα** δίδυμο → το ένα fit-to-view path έφτιαχνε τη γραμμή, το άλλο την απέρριπτε. Ακριβώς το failure mode που υπάρχει ο N.18 για να πιάνει.
  - **Fix**: το `calculateFitToViewTransform` κατέχει πλέον **μόνο** το bounds resolution (`createCombinedBounds` + το δικό του `null` συμβόλαιο) και **delegate-άρει**. Τα μαθηματικά ζουν **μία φορά** στο `calculateFitToViewFromBounds` = το SSoT. **334 → 237 γραμμές.** Public API αμετάβλητο (και οι 5 καταναλωτές άθικτοι: `useFitToView`/`useZoomWindowTool`/`useViewState`/`capture-2d`/`performFitToView`).
  - **Tests — το δίδυμο ήταν ΑΤΕΣΤΩΤΟ, γι' αυτό πέρασε απαρατήρητο.** Τα υπάρχοντα 4 (`FitToViewService-degenerate`) χτυπούσαν **μόνο** το pure-bounds. **ΝΕΟ `FitToViewService-twin-parity.test.ts`** (16 tests): `describe.each` σε 6 σχήματα bounds (κανονικό box / arbitrary origin / οριζόντια / κατακόρυφη / αρνητικές / true point) × 2 (default + `alignToOrigin`+padding) → **τα δύο μονοπάτια πρέπει να συμφωνούν σε transform/success/reason** + options pass-through (maxScale clamp) + τα δύο συμβόλαια σφάλματος (bounds null vs viewport άκυρο) + `bounds` στο result. Ο resolver mock-άρεται → κλειδώνει τη **σχέση**, όχι τα bounds math.
  - **Mutation-verified**: αφαίρεσα επίτηδες το `options` από το delegate (το ακριβές bug που ρισκάρει το refactor) → **6 tests έσπασαν** → revert + επιβεβαίωση.
  - **Verification: 430/430 GREEN (`services/__tests__`, 28 suites)· `jscpd:diff` καθαρό (ΧΩΡΙΣ `SKIP_JSCPD_DIFF`)· 0 ορφανά imports.** **ΟΧΙ tsc (N.17).** 🔴 Giorgio commit: `FitToViewService.ts` + `FitToViewService-twin-parity.test.ts` NEW + ADR-584 + `.claude-rules/pending-ratchet-work.md`.

- **2026-07-16** — Clone dedup — **mutation impact guard hooks** (`src/hooks/`), the core of the **repo's largest directory pair**. **UNCOMMITTED. → new ADR-664.** A full `jscpd:check` sweep ranked `src/hooks/ ↔ src/hooks/` first (1331 lines / 82 clones / 32 files); grouping the report by file-pair confirmed the prior handoff's guess: **6 sibling guard hooks** (broker-terminate / engineer-remove / landowners-save / ownership-mutation / project-mutation / ika-labor-compliance) each re-declared the same preview state machine — **24 clones / 428 duplicated lines between them**. Collapsed onto `src/hooks/impact-guard/useProjectImpactGuard.tsx`; each sibling is now a ~24-line binding (**704 → 334 lines**). **Public API unchanged** → the six `useGuarded*` consumers untouched. This is the **client-side mirror of ADR-591**, which de-duplicated the *server* services of the same family — same archetype (shared primitive + per-instance binding). The audit also surfaced two defects, fixed here: dead `previewRef` in 3 of 6 (written, never read), and an **unstable `reset`** in the two option-taking guards (`options = {}` literal + `[options]` dep re-created `reset` and the dialog `useMemo` on every render; `useGuardedOwnershipTableMutation` passes an inline literal, so it fired in production). `usePropertyMutationImpactGuard` / `useContactMutationImpactGuard` deliberately out — different preview type, dialog, and message namespace. New registry module `impact-guard-hook` uses patterns that match **no pre-existing file**, so **no `ssot:baseline` refresh was needed** — deliberate, since the tree is shared with other agents' uncommitted work and a baseline refresh would sweep it in. **Verified: `jscpd:diff` on 7 files → 0 clones (without `SKIP_JSCPD_DIFF`); 31 new tests GREEN (13 state machine + 18 bindings); 49/49 across `src/hooks`; 86/86 registry golden regex; mutation-verified ×2** — pointing a guard at the wrong endpoint broke the binding test, and replacing the confirm `setTimeout(…, 0)` with a synchronous call broke the INP test; both reverted. **ΟΧΙ tsc (N.17).** `jscpd:check` reads **2926/3059**, but the tree carries other agents' uncommitted work — the isolated, measured contribution of this pass is **−24 clones**. Baseline **not** refreshed (Giorgio, same session).

- **2026-07-16** — Clone dedup — **entity list-page headers, Ομάδα Β** (`projects/ProjectsHeader` · `properties/page/PropertiesHeader` · `property-management/page/PropertiesHeader` · `sales/page/SalesAvailableHeader`). **UNCOMMITTED.** Κλείνει το ανοιχτό σημείο της Ομάδας Α (commit `d42dabd7`). Απόφαση Giorgio: «όπως οι μεγάλοι παίχτες (Revit/ArchiCAD/Figma) — full enterprise + full SSoT· αν δεν το προτείνουν, ακολούθησε την πρακτική τους».
  - **⚠️ Ο καταγεγραμμένος πίνακας διαφορών ήταν ΕΝ ΜΕΡΕΙ ΛΑΘΟΣ — επαληθεύτηκε token προς token πριν γραφτεί κώδικας.** (α) **`spacing.padding.sm` ΕΙΝΑΙ κυριολεκτικά `p-2`** (`useSpacingTokens.ts:211`) → η γραμμή «Padding» δεν ήταν διαφορά, ήταν το ίδιο πράγμα με δύο ονόματα. (β) **Ο `ProjectsHeader` ΔΕΝ ανήκε στη διάλεκτο Β**: το trash toggle του ήταν σχεδόν byte-identical με το `HeaderTrashToggle` (ίδιο `p-2`/`quick.button`/`ACCENT_HOVER`/`getStatusBorder`/badge cap· **είχε ήδη `aria-pressed`**) — διέφερε μόνο σε έναν `<Tooltip>` wrapper· το filter button του ήταν **τρίτη** παραλλαγή. Αφαιρώντας τα φαντάσματα, η πραγματική διαφορά ήταν **μόνο hover + radius**.
  - **BUG (προϋπάρχον, ΟΧΙ regression της Ομάδας Α) — το mobile filter button δεν είχε ΚΑΘΟΛΟΥ border-radius.** Το `useBorderTokens().radius` είναι **raw design values** (`radius.md === '0.5rem'`), όχι Tailwind κλάσεις — αυτές ζουν στο `radiusClass` (`radiusClass.md === 'rounded-md'`). Το `${radius.md}` παρήγαγε `className="md:hidden p-2 0.5rem …"` → **άκυρη κλάση που το Tailwind αγνοεί** → τετράγωνο κουμπί σε Parkings/Storages. **`git show d42dabd7` επιβεβαίωσε ότι το `radius.md` προϋπήρχε** του refactor (η Ομάδα Α το μετέφερε πιστά → ήταν όντως behavior-preserving). **Ειρωνικά η Ομάδα Β είχε το ΣΩΣΤΟ στυλ** (`rounded-md` κυριολεκτικά). Fix: `radiusClass.md` στο SSoT → η Β δεν αλλάζει, η Α **διορθώνεται**.
  - **ΜΙΑ οπτική γλώσσα, ΟΧΙ `variant: 'accent' | 'subtle'`.** Ένα variant prop θα κωδικοποιούσε **μόνιμα** την ασυνέπεια αντί να τη λύσει — δεν είναι SSoT, είναι ασυνέπεια με τύπο· Figma/Revit/C4D έχουν **ΕΝΑ** hover state ανά icon-button. Κρατήθηκε το `ACCENT_HOVER`: είναι το canonical hover του design system για ghost/icon buttons (βλ. `BUTTON_GHOST`), αλλάζει και το text color (contrast), και ήταν ήδη σε 4 από τα 7 headers. **Ορατή αλλαγή**: hover `bg-muted/50` 150ms → `bg-accent` 200ms σε 4 σελίδες.
  - **Tooltip: ΠΑΝΤΑ στο trash, ΠΟΤΕ στο filter.** Icon-only κουμπί χωρίς tooltip = affordance gap (Figma/Revit standard) → το `HeaderTrashToggle` τυλίγεται πάντα, με `trashTooltip ?? trashAriaLabel` (ο tooltip λέει ό,τι ακούει ο screen reader· ρητό μόνο όταν το κουμπί εναλλάσσει νόημα — `ProjectsHeader`: «Κάδος» ↔ «Πίσω στη λίστα»). **Το filter είναι `md:hidden` (mobile-only) και το tooltip θέλει hover που δεν υπάρχει σε touch → θα ήταν νεκρός κώδικας**· εκεί αρκεί το `aria-label`. **Επαληθεύτηκε ότι δεν σπάει**: ο `TooltipProvider` τυλίγει τα πάντα στο top level (`ConditionalAppShell.tsx:200`, με ρητό σχόλιο) → Buildings/Parkings/Storages αποκτούν tooltip χωρίς αλλαγή στους καλούντες.
  - **a11y κέρδη (ήταν το πραγματικό στοίχημα, όχι το στυλ)**: `aria-pressed` στο trash (+3 σελίδες) **και στο filter** (+7 — δεν το είχε κανείς)· badge cap `99+` στο `properties/page` (4ψήφιος μετρητής έσπαγε τον `h-4 w-4` κύκλο).
  - **N.11 (Boy Scout)**: το `properties/page/PropertiesHeader` είχε hardcoded `aria-label="Toggle filters"` → νέο κλειδί **`actions.toggleFilters`** σε el+en `common-actions.json`. ⚠️ Το `tCommon('actions.toggleFilters')` του `property-management` **δεν resolve-άριζε ποτέ** (το κλειδί δεν υπήρχε) — τώρα υπάρχει. Επίσης `sales.available.filters.toggle`: «Εναλλαγή» → «Εναλλαγή φίλτρων» (σκέτο «Εναλλαγή» = φτωχό aria-label).
  - **Το self-guard ξαναχτύπησε — και είχε δίκιο.** Ο 1ος `jscpd:diff` βρήκε **δικό μου νέο δίδυμο** (14 γρ. / 70 tokens): τα δύο `PropertiesHeader` κατέληξαν με ταυτόσημο type+props block. **Νέο SSoT στο `list-page-header-props.ts`**: `ListGridViewMode = Extract<ViewMode,'list'|'grid'>` (ΟΧΙ ξεχωριστό union → αποδεδειγμένα υποσύνολο, **έφυγαν τα `as CoreViewMode` casts**), `LIST_GRID_VIEW_MODES`, `LIST_PAGE_VIEW_MODES` (μεταφέρθηκε από το `ListPageHeader`), το `ListPageHeaderProps` έγινε **generic** `<TViewMode extends ViewMode = ViewMode>`, και `ListGridHeaderProps = Omit<…,'showTrash'|'onToggleTrash'|'trashCount'>`. Τα 3 headers κάνουν πλέον `extends` αντί να επαναλαμβάνουν.
  - **`trashAriaLabel` έγινε optional με τεκμηριωμένο λόγο**: γράφτηκε πρώτα `trashAriaLabel: ''` στους καλούντες χωρίς κάδο — **ψεύτικη τιμή για να ικανοποιηθεί ο τύπος** → διορθώθηκε στο SSoT. Discriminated union **δεν** μπορεί να το επιβάλει: οι καλούντες περνούν `onToggleTrash` τύπου `(() => void) | undefined` κατευθείαν από props, οπότε **κανένα** union member δεν θα ταίριαζε.
  - **Αφαιρέθηκαν τα deprecated aliases που έφτιαξα ο ίδιος** (`PropertiesViewMode`/`SalesViewMode`): grep-verified ότι **κανείς δεν τα εισάγει από τα headers** (το εν χρήσει `SalesViewMode` έρχεται από `@/types/sales-shared`) → σκουπίδια, όχι backward compat.
  - **Σκόπιμα ΕΚΤΟΣ**: (α) το `SalesViewMode` ορίζεται σε **3 σημεία** (`sales-shared.ts` · `useSalesPropertiesViewerState.ts` · πρώην header) — άλλο domain (hooks/types), θέλει δικό του πέρασμα· (β) τα 4 headers **δεν** έγιναν `ListPageHeader` wrappers: `viewModes`/`addButton`/`availableCount`/overrides θα το έκαναν το parameter-soup god-shell που αυτό το ADR αποφεύγει. Το πραγματικό clone ήταν τα κουμπιά + το contract, και αυτά κεντρικοποιήθηκαν.
  - **Verification**: `jscpd:diff` στα 8 αρχεία **1 → ✅ 0 clones** (ΧΩΡΙΣ `SKIP_JSCPD_DIFF`)· full ratchet **2906/3059 (−153)**· `grep -rln 'key="trash-toggle"' src/` **και** `key="mobile-filter"` → **μόνο το SSoT** (0 inline επιβιώσαντες σε 7 headers)· 0 hardcoded «Toggle filters»· call sites επαληθεύτηκαν (`viewMode as 'list'|'grid'` → συμβατό). **ΟΧΙ tsc (N.17).** ⚠️ **browser-verify pending** — δεν υπάρχει ΚΑΝΕΝΑ test που να αποδίδει αυτά τα headers.
  - ⚠️ **Baseline ΔΕΝ κλειδώθηκε**: το δέντρο περιέχει ταυτόχρονη uncommitted δουλειά άλλου agent → το `−153` δεν είναι όλο δικό μου. Ο Giorgio τρέχει `jscpd:baseline` μετά το commit.

- **2026-07-16** — Clone dedup — **BIM renderers, Ομάδα Β (finish/analytical overlays)** + **grip-type pass-through**. **UNCOMMITTED.** Κλείνει τα δύο ανοιχτά σημεία του `HANDOFF-jscpd-bim-renderer-cluster-CLOSED-ADR584.md` (απόφαση Giorgio: «επίλεξε εσύ»).
  - **Το halo έγινε ΠΑΡΑΓΩΓΟ, όχι ανεξάρτητο**: `paintHoverHalo`/`paintPolygonHoverHalo` δέχονται optional `bodyLineWidth` (default `NORMAL`) και υπολογίζουν `body + glowExtraWidth`. Οι 4 της Ομάδας Β (`FloorFinish`/`SpaceSeparator`/`ThermalSpace`/`WallCovering`) hand-approximate-άριζαν το άθροισμα με magic `+ 1.5` ενώ το σώμα τους ήταν `1.2` → το halo είχε ήδη ξεφύγει από αυτό που παρακολουθούσε. **Η Ομάδα Β ΔΕΝ ήταν κακό αντίγραφο της Α — ανήκει στη Γ** (παραμετρικό `entityLineWidth + glow`) με λεπτό σώμα· ισοπέδωση στο `NORMAL` θα ήταν regression.
  - **ΝΕΟ token `RENDER_LINE_WIDTHS.BIM_FINISH_BOUNDARY = 1.2`** — το `1.2` ήταν hardcoded σε 4 σημεία. Πλέον ΕΝΑ σημείο κινεί και το σώμα και το halo. Οπτικό αποτέλεσμα: halo **7.5 → 7.2px** σε 4 τύπους (αόρατο· glow, όχι γεωμετρία).
  - **ΝΕΟ `paintSelectionHalo`** (N.0.2 boy-scout) — το 8γραμμο «selected» μπλοκ ήταν ταυτόσημο σε `SpaceSeparator`+`ThermalSpace`. Σκόπιμα **πλατύτερο + αδιαφανές** έναντι του hover glow: καμία από τις δύο οντότητες δεν εκπέμπει grips (ADR-422 L0 / ADR-437 D-F), άρα το «επιλεγμένο» το κουβαλά μόνο το περίγραμμα και πρέπει να ξεχωρίζει από το hover. Σταθερές `SELECTION_HALO_BODY_WIDTH`/`SELECTION_HALO_OPACITY` στο SSoT.
  - **`mapBimGrips` — το `type` περνά ΑΥΤΟΥΣΙΟ** (`type: g.type`, το τριπλό ternary έφυγε). Ο τύπος είναι **load-bearing**: το `isGripTypeVisible` (SSoT) γκειτάρει `center→showCenters` / `edge|midpoint→showMidpoints`, και ο `GripColorManager` βάφει τα cold `edge` πράσινα. Το flattening σε `'vertex'` δήλωνε «δομικό endpoint, πάντα ορατό» για grips που δεν είναι. **Cast-free**: ο parametric `GripType = Exclude<GripKind,'control'|'close'>` είναι γνήσιο υποσύνολο του render `GripInfo['type'] = Exclude<GripKind,'close'>`.
  - **BUG FIX — `FoundationRenderer.getGrips`**: hardcode-άριζε `type: 'vertex' as const` σε ΟΛΑ τα grips ενώ ο generator εκπέμπει `center` (ADR-517) + `edge`. **Η θεμελίωση ήταν η μόνη οντότητα όπου οι 3 διακόπτες «Grip Types» δεν έκαναν τίποτα.** Πλέον → `mapBimGrips` + `resolveShape`. Αφαιρέθηκε και το παρωχημένο σχόλιο «central MOVE grip not emitted» (το ADR-517 το ξαναπρόσθεσε).
  - ⚠️ **Ορατή αλλαγή, γι' αυτό ΞΕΧΩΡΙΣΤΟ commit από το μηχανικό de-dup**: με το pass-through, τα midpoint grips σε `Foundation`/`Beam`/`MepFixture`/`MepManifold`/`MepRadiator` γίνονται πράσινα + κρύβονται με τον διακόπτη «Midpoints» — δηλαδή αποκτούν τη συμπεριφορά που έχουν ΗΔΗ όλες οι άλλες οντότητες.
  - **Self-guard (N.18)**: `jscpd:diff` **5 → 3**. Το 1 από τα 5 ήταν **δικό μου νέο δίδυμο** (έφτιαξα το ίδιο ιδιωτικό `tracePolygon` bind σε 2 αρχεία) → inline-αρίστηκε στο `tracePolygonScreenPath`, ιδίωμα ίδιο με τα 10 migrated. Τα 3 υπόλοιπα **δεν εξάγονται χωρίς base class**: render-prologue (guard+`determinePhase`+halo → θέλει `beginPhasedBodyRender`) · υπογραφές `getGrips`/`hitTest` · οι horizontal/vertical κλάδοι του `drawParallelLines` (προϋπάρχον, δεν αγγίχθηκε).
  - **Tests**: 443/443 GREEN (`bim/renderers` + `bim/visibility` + `hooks/grips`).

- **2026-07-16** — Clone dedup — **entity list-page headers** (`BuildingsHeader` / `ParkingsHeader` / `StoragesHeader`). **UNCOMMITTED.** Βρέθηκε τρέχοντας full `jscpd:check` κατόπιν εντολής Giorgio· ο ίδιος ενέκρινε ρητά το cluster (N.8: 6 αρχεία / 2 domains).
  - **Τρία SSoT, γιατί το self-guard ξεφλούδισε το κρεμμύδι σε τρεις γύρους** — κάθε γύρος του `jscpd:diff` αποκάλυπτε το επόμενο στρώμα κάτω από το προηγούμενο:
    1. **`core/headers/header-custom-actions.tsx`** (ΝΕΟ) — `buildHeaderCustomActions()`: το mobile-filter + trash-toggle ζευγάρι που ήταν **byte-identical** σε 3 headers. Κρατά κλειδιά (`mobile-filter`/`trash-toggle`) και σειρά (φίλτρα→κάδος) ακριβώς όπως τα inline αντίγραφα.
    2. **`core/headers/list-page-header-props.ts`** (ΝΕΟ) — `ListPageHeaderProps`: το props contract, ταυτόσημο σε Parkings/Storages (16 γρ. / 92 tokens).
    3. **`core/headers/ListPageHeader.tsx`** (ΝΕΟ) — το ίδιο το header. Η αλήθεια που αποκάλυψε ο 2ος γύρος: `ParkingsHeader` και `StoragesHeader` **ήταν το ίδιο component** — διέφεραν μόνο σε εικονίδιο, i18n namespaces και prefix κλειδιών. Πλέον είναι wrappers 43/37 γρ. (από 126/114).
  - **Ο καλών κάνει το i18n resolve** (`labels` prop έτοιμων strings), ΟΧΙ το SSoT: κάθε οντότητα έχει δικά της namespaces (`storage` vs `building*`) και το **CHECK 3.8 πρέπει να βλέπει τα `t()` στο αρχείο που τα κατέχει**.
  - **`BuildingsHeader` σκόπιμα ΔΕΝ χρησιμοποιεί το `ListPageHeader`**: έχει `addButton`, δεν έχει αναζήτηση → άλλο contract. Μοιράζεται μόνο το `buildHeaderCustomActions` (το μέρος που ήταν όντως διπλό).
  - **Αφαιρέθηκε διπλότυπος τύπος που έφτιαξα ο ίδιος**: το `ListPageHeaderViewMode` ήταν αντίγραφο του υπάρχοντος `ViewMode` (`'list'|'grid'|'byType'|'byStatus'`). Χρησιμοποιείται πλέον το `ViewMode` → **έφυγαν και τα `as ViewMode` casts**, που υπήρχαν μόνο για να γεφυρώσουν δύο ταυτόσημα unions.
  - **N.11 (Boy Scout)**: το `BuildingsHeader` είχε hardcoded `aria-label="Toggle filters"` → νέο κλειδί `accessibility.toggleFilters` σε **el + en** `building.json`. ⚠️ Ορατή αλλαγή: το aria-label γίνεται «Εναλλαγή φίλτρων» στο ελληνικό locale (ήταν αγγλικό ανεξαρτήτως γλώσσας).
  - **⚠️ ΔΕΥΤΕΡΗ ΟΠΤΙΚΗ ΔΙΑΛΕΚΤΟΣ — ΤΟΤΕ ΕΚΤΟΣ, ΠΛΕΟΝ ΕΝΟΠΟΙΗΜΕΝΗ** (Ομάδα Β· βλ. εγγραφή «Ομάδα Β» παρακάτω): `projects/ProjectsHeader`, `properties/page/PropertiesHeader`, `property-management/page/PropertiesHeader`, `sales/page/SalesAvailableHeader`. ⚠️ Ο πίνακας διαφορών αυτής της εγγραφής **αποδείχθηκε εν μέρει λανθασμένος** στον επόμενο γύρο (το `spacing.padding.sm` **είναι** `p-2`· ο `ProjectsHeader` **δεν** ανήκε στη διάλεκτο Β) — βλ. την εγγραφή της Ομάδας Β για τα επαληθευμένα δεδομένα.
  - **Verification**: `jscpd:diff` στα 7 αρχεία **3 γύροι → ✅ 0 clones** (χωρίς `SKIP_JSCPD_DIFF`)· full ratchet **3012/3059**· grep-verified 0 ορφανά imports / 0 ορφανά type refs· μεγέθη εντός N.7.1. **ΟΧΙ tsc (N.17).** ⚠️ **browser-verify pending** — δεν υπάρχει ΚΑΝΕΝΑ test που να αποδίδει αυτά τα headers.
  - ⚠️ **Baseline ΔΕΝ κλειδώθηκε**: το δέντρο περιέχει ταυτόχρονη uncommitted δουλειά άλλου agent (Wall/Slab preview SSoT) → το `−47` δεν είναι όλο δικό μου· `jscpd:baseline` θα κλείδωνε ξένα κέρδη. Ο Giorgio το τρέχει μετά το commit.

- **2026-07-16** — Clone dedup (cluster B follow-up δ — the BIM renderer **hover-halo** bodies). Closes the *second half* of the 51-clone backlog (the `hitTest`/`getGrips` half is the entry below). **The SSoT already existed** — `paintPolygonHoverHalo` in `bim-polygon-render.ts` — and, per the entry below, **no base-class change was needed**: it is a free function, callable from any `BaseEntityRenderer` subclass as `paintPolygonHoverHalo(this.ctx, (p) => this.worldToScreen(p), verts, phaseState.phase === 'highlighted')`. **Migrated 12 renderers / 12 halo sites.** **10 are plain closed footprints** (`MepBoiler`/`MepFitting`/`MepFixture`/`MepManifold`/`MepRadiator`/`MepSegment`/`MepUnderfloor`/`MepWaterHeater`/`Roof`/`SlabOpening`) — each inlined `save → glowColor → NORMAL+glowExtraWidth → glowOpacity → setLineDash([]) → drawPolygonPath → stroke → restore`, byte-identical to the SSoT (their private `drawPolygonPath` is itself a byte-copy of `tracePolygonScreenPath`: `<3` verts guard, `beginPath`, `moveTo`, `lineTo`×n, `closePath`) ⇒ **zero visual change**. **2 renderers do NOT trace a closed polygon**, and this is the finding the backlog note had wrong: `BeamRenderer` traces the **multi-subpath** ADR-458 cut-back pieces (`buildPiecesPath`), and `RailingRenderer` traces an **OPEN centreline** polyline — routing either through `paintPolygonHoverHalo` would have `closePath`'d it (a phantom end→start leg on the railing). So the glow **style** (the part that was actually duplicated) was hoisted into a **new primitive `paintHoverHalo(ctx, highlighted, trace)`**: it owns `save`/glow style/`stroke`/`restore` and takes the caller's path-only `trace` callback; `paintPolygonHoverHalo` is now a 1-line wrapper over it (`() => tracePolygonScreenPath(…)`), so the glow constants are stated **once** in the codebase and serve every halo shape. Beam passes `() => this.buildPiecesPath(drawable)`; Railing passes `() => this.tracePolyline(symbol.pathStroke)` (its existing trace/stroke split — `strokePolyline` would have double-stroked). Dropped the now-orphan `HOVER_HIGHLIGHT` import from 9 renderers (+ `RENDER_LINE_WIDTHS` from `SlabOpeningRenderer`, `HOVER_HIGHLIGHT` only from `RoofRenderer`'s shared `color-config` import — `UI_COLORS_BASE` kept). **2 sites deliberately NOT migrated, both re-classified from the backlog's assumptions:** **(a) `MepSegmentRenderer` riser halo (:299)** — a **screen-space `arc()`**, not a polygon; more importantly it runs *inside* the caller's `save`/`restore` and ends with a hardcoded `globalAlpha = 1` rather than a restore, so wrapping it in `paintHoverHalo`'s own save/restore would restore the *inherited* alpha instead — a real (if unlikely) behaviour delta inside a de-dup commit. Left inline. **(b) group Β (`FloorFinish`/`SpaceSeparator`/`ThermalSpace`/`WallCovering`, `glowExtraWidth + 1.5`)** — the backlog framed the fix as "alignment = +0.5px". **That framing is wrong and would have been a regression:** all four stroke their *body* at `lineWidth = 1.2` (not `RENDER_LINE_WIDTHS.NORMAL = 2`), so `glowExtraWidth + 1.5` is not a mis-copy of group A's formula — it is the **parametric** form (group Γ's `entityLineWidth + glowExtraWidth`) with a thin-body width, i.e. group Β belongs with **Γ, not A**. Flattening it to `NORMAL + glowExtraWidth` would widen a deliberately thin annotation halo by 0.5px on 4 entity types. The genuine defect there is narrower — the magic `1.5` doesn't match the `1.2` body it tracks — and is logged for Giorgio (see `pending-ratchet-work.md`); the right shape is an optional `lineWidth` param on the SSoT, not a flatten. **Verified: `jscpd:diff` on the 13 files 40 → 26 clones; 248/248 renderer + visibility tests (23 suites) GREEN; `grep -rn glowExtraWidth src/subapps/dxf-viewer/bim/renderers/*.ts` leaves only the SSoT + group Β (4) + group Γ (Stair/Wall) + the riser — zero group-A survivors; zero orphan imports.** **ΟΧΙ tsc (N.17).** Pure SSoT adoption — no ADR-040 subscription change (documented here for the CHECK 6D entity-renderer gate). **The remaining 26 clones are a different family, pre-existing and untouched by this pass:** the private `drawPolygonPath` helper is still re-declared in **12 renderers** despite being byte-identical to `tracePolygonScreenPath` (plus the `drawStroke`/`drawTag` appliance bodies). 12 files ⇒ N.0.2 routes it to the backlog, not an on-the-spot fix; logged in `pending-ratchet-work.md`. Commit path: `SKIP_JSCPD_DIFF=1` (justified — clone-*reducing* vs HEAD; see the `--diff` whole-file limitation noted below). Baseline drops → run `npm run jscpd:baseline` after commit.

  **Same pass, second wave (26 → 13) — the three remaining mechanical families, all onto SSoTs that already existed.** **(α) private `drawPolygonPath` → `tracePolygonScreenPath`** in **10 renderers** (20 call sites): all 12 copies in the folder were byte-identical to each other *and* to the SSoT (same `<3` guard, `beginPath`, `moveTo`, `lineTo`×n, `closePath`), so each private helper was deleted outright. *(`FloorFinish`/`ThermalSpace` hold the other 2 copies but are group Β — out of scope this pass, see below.)* **(β) private `drawStroke`(×4: MepBoiler/MepManifold/MepRadiator/MepWaterHeater) + `drawPolyline`(×2: Beam/MepSegment) → `strokePolylinePaths`** — six byte-identical helpers, each exactly the SSoT's per-line body. Where the caller's loop changed style **per item** (MepBoiler's `strokes`/`ventStrokes`, coloured by System Classification) the loop stays and passes `[line]`; where it did **not**, the whole array goes in one call (`symbol.fuelStrokes`/`glyphStrokes`/`finStrokes`/`gratingStrokes`) — `strokePolylinePaths` *is* that loop. `MepUnderfloor.drawLoopPath` deliberately kept: it sets its own style and has no `<2` guard. **(γ) `getGrips` `.map()` → `mapBimGrips`** for `MepUnderfloor`/`Roof`/`SlabOpening`. **Safety-verified the same way the entry below did, not assumed:** `mapBimGrips` maps `center → 'center'` whereas these inline maps collapsed everything-but-`midpoint` to `'vertex'` — divergent *iff* a generator emits `center`. All three delegate to the shared `buildPolygonOutlineGrips` emitter (`bim/grips/polygon-outline-grip-core.ts`), which emits **only** `'vertex'` and `'midpoint'` → identical output. **Where this pass STOPS, and why (the line is deliberate, not fatigue):** the last **13** clones are the **fill+outline body sequence** (`fillStyle → trace → fill → strokeStyle → lineWidth → [setLineDash] → trace → stroke`). It is **not** mechanical: the renderers diverge on lineWidth token (`NORMAL` vs `THIN`), on dash (MepSegment/MepFitting/MepUnderfloor dash, the appliances don't), and on fill source (`adaptFillTintForCanvas` vs `resolveBimBodyFill`) — a single helper would take ~5 params (the parameter-soup god-helper this ADR warns against). Worse, the inline blocks **deliberately leak ctx state**: `MepBoiler` sets `lineWidth = NORMAL` for the outline and the *symbol strokes below rely on inheriting it* until an explicit `THIN`. A `strokePolygonOutline`-style helper (which `save`/`restore`s) would silently revert that — a real render change inside a de-dup commit. Logged in `pending-ratchet-work.md` as needing a design decision, not a sweep. **Verified after both waves: `jscpd:diff` on the 13 files 40 → 13 clones; 248/248 renderer + visibility tests (23 suites) GREEN; −325 net lines (94+/419−); zero orphan helpers/imports (`grep` for `private drawPolygonPath|drawStroke|drawPolyline` in the touched files = empty).** **ΟΧΙ tsc (N.17).**
- **2026-07-16** — Clone dedup (cluster — «Edit … Type» dialog live-preview twins). Driven by a full `jscpd:check` sweep: the report's **largest single clone in the whole repo** (101 lines / 627 tokens) was `SlabTypePreviewRenderer` ↔ `WallTypePreviewRenderer`, plus a 45L/425T `fitCamera` pair and a 60L/370T pair between `SlabTypePreviewPanel` ↔ `WallTypePreviewPanel` — i.e. the slab preview had been built by copying the wall preview wholesale (the N.18 failure mode, at file scale: the two renderers were ~95% byte-identical and the two panels differed only in their DNA type, renderer class, and two i18n keys). Extracted **2 new SSoTs**: **`bim-3d/preview/band-stack-preview-renderer.ts`** — `BandStackPreviewRenderer<TDna, TBand>`, owning the entire mini THREE viewport (WebGL context, lights, 3/4 camera, the 8-corner exact-fit `fitCamera` solve, the `EdgesGeometry` highlight outline, band raycast picking, alt+click pivot, resize, dispose) — and **`ui/ribbon/components/BandStackPreviewPanel.tsx`**, owning the whole React shell (mount/dispose, prop→renderer effects, texture-version swap, pointer→NDC→pick de-dup, markup). **Not a god-shell:** everything that genuinely differs is declared in a small **`BandStackPreviewSpec`** — `viewDir`, `fallbackThicknessM`, `buildBands`, `boxOf` (the band's box), `halfExtents` — which is exactly the 4-point delta the two previews actually have (slab layers stack along **Y** across a 1.4×0.9 m patch with view dir `(0.85,1.05,1.5)` and a 0.2 m fallback; wall layers stack along **Z** across a 1.2×1.0 m slice with view dir `(1.5,1.05,0.85)` and a 0.25 m fallback). Generalization was cheap because the two band types (`SlabPreviewBand`/`WallPreviewBand`) already share `layerId`/`materialId` field *names* and both DNAs structurally satisfy `{layers:[{id,materialId}], totalThickness}` → the base needs only `PreviewDnaLike`/`PreviewBandLike` structural bounds, no converter change. Panels depend on a **`BandStackPreviewHandle<TDna>`** interface rather than a concrete class, so the generic panel stays free of the band type parameter. **Public API byte-identical** — `SlabTypePreviewRenderer`/`WallTypePreviewRenderer` remain constructible classes (`new X(container)`) that now just bind their spec, and both panels keep their exact props contract → **none of the 3 consuming dialogs (`EditWallTypeDialog` / `EditSlabTypeDialog` / `EditRoofTypeDialog`) were touched**. (`EditRoofTypeDialog` already reused `SlabTypePreviewPanel` — roof DNA *is* `SlabDna` — so it inherits the dedup for free.) One deliberate improvement over the originals: the panel reads `createRenderer` through a ref, so a caller passing an inline arrow can never force a remount and drop the WebGL context.

  **Testing — the refactor was made testable rather than declared untestable (Giorgio, same session).** The first pass shipped with "WebGL renderers have no tests, behaviour preservation rests on a line-by-line audit", which is exactly the hand-wave the big players don't accept. The fix is the standard move: **get the logic out of the GPU object**. (a) The 8-corner frustum solve — the hardest maths here, and now shared by BOTH previews — was extracted to **`bim-3d/preview/band-stack-fit.ts`** (`solveFitDistance(halfExtents, viewDir, fovDeg, aspect)` + `FIT_MARGIN`), pure, no WebGL. (b) `SLAB_PREVIEW_SPEC` / `WALL_PREVIEW_SPEC` are now **exported** — the spec *is* the per-entity behaviour that survived the merge, so it is the refactor's actual risk surface, and it is pure data + pure functions. What remains untestable in jsdom is only the `WebGLRenderer` shell (context, lights, GPU dispose) — which is THREE's code, not ours. **`__tests__/band-stack-fit.test.ts` (24 tests)**: property tests, NOT golden numbers — each case places a real `PerspectiveCamera` at the solved distance and projects all 8 corners, asserting `|ndc| ≤ 1` (nothing clipped — the ADR-414 (b) regression), all corners in front of the camera, and **tightness** (`max|ndc| > 1/FIT_MARGIN − 0.05`, so `return Infinity` cannot pass), over the real wall/slab stubs plus degenerate (0.001 m), very thick (1 m) and extreme-aspect (0.4 / 4.0) cases; plus monotonicity (2× box → exactly 2× distance) and narrower-FOV-backs-off. **`__tests__/band-stack-preview-spec.test.ts` (27 tests)**: a regression lock transcribed from the *pre-refactor* renderers — the shared contract via `describe.each` (unit `viewDir`, one band per layer, positive finite boxes, **bands sum to the DNA total thickness**, stack centred on the origin) + each spec's exact deltas (wall `(1.5,1.05,0.85)` / 1.2×1.0 m / Z-stacked / 0.25 m fallback; slab `(0.85,1.05,1.5)` / 1.4×0.9 m / Y-stacked / 0.2 m fallback) + an explicit *"the merge did not collapse them"* guard (wall stacks on Z, slab on Y). **Mutation-verified, not just green:** injecting the exact bug this refactor risks (wall `boxOf` stacking along Y like the slab) fails 2 tests; reverted and re-confirmed. Note the suite's own first draft had a real bug — it located the stack axis by searching for the value `0.5`, but the wall's `halfExtents(1)` is `[0.6, 0.5, 0.5]` (the 1.0 m stub height collides with the thickness) → ambiguous; replaced with differencing (`halfExtents(0.2)` vs `halfExtents(0.6)` — the stack axis is the only extent that moves with thickness).

  **Verified: `jscpd:diff` on all 7 files = 0 clones (was 3 pairs, incl. the repo's largest); `jscpd:check` (--full, authoritative Layer 2) 3041 → 3035; 56/56 GREEN (24 fit + 27 spec + the 5 existing `wall-type-preview-geometry`); the 2 shared i18n keys (`ribbon.commands.{slabFamilyType,bimFamilyType}.preview.{title,emptyHint}`) verified present in the `el` locale.** **ΟΧΙ tsc (N.17).** Both renderers sit **outside** the ADR-040 high-frequency canvas path (standalone render-on-demand scenes) → no subscription/architecture change; documented here for the CHECK 6D gate. ⚠️ **Still worth one browser look** — the tests now cover the geometry/fit contract, but nothing asserts pixels (material resolution, outline crispness, texture UVs are visual). Behaviour ADRs **ADR-412 / ADR-414** unchanged. Baseline drops → run `npm run jscpd:baseline` after commit.
- **2026-07-16** — Clone dedup (cluster B follow-up γ — the BIM renderer `hitTest` + `getGrips` bodies). Closes the *first half* of the 51-clone backlog logged by the visibility-SSoT sweep (entry below). **No new SSoT was needed — both already existed** in `bim-polygon-render.ts` (`polygonBboxHitTest`, `mapBimGrips`), but only the `BimFootprintRenderer` family (Column/ElectricalPanel/FloorplanSymbol/Furniture/Slab) had been migrated onto them; the `BaseEntityRenderer` family (all MEP + Beam/Roof/Railing/SlabOpening/FloorFinish/ThermalSpace) still carried inline copies — the textbook N.18 failure mode (centralize A, leave B+C as siblings). **Migrated 15 renderers.** `hitTest` → `polygonBboxHitTest(bb, verts, point, tolerance)` for the 11 whose detailed test is a plain footprint ray-cast (`MepBoiler`/`MepFixture`/`MepManifold`/`MepRadiator`/`MepWaterHeater`/`MepUnderfloor`/`MepSegment`/`Roof`/`SlabOpening`/`FloorFinish`/`ThermalSpace` — note the polygon source legitimately varies: `geometry.footprint` / `params.footprint` / `geometry.polygon` / `geometry.outline`, which is exactly why it is a *parameter*, not a base-class hook). **New primitive `bboxRejectsPoint(bbox, point, tolerance)`** added to `bim-polygon-render.ts` for the 3 whose tail is genuinely entity-specific — `BeamRenderer` (ADR-458 cut-back `pieces.some(pointInPolygon)`), `MepFittingRenderer` (degenerate footprint `< 3` verts → bbox *is* the target → `true`), `RailingRenderer` (`distanceToSegment` along `resolvedPath`); `polygonBboxHitTest` now calls it internally, so the padded-bbox rule is stated **once** in the codebase. `getGrips` → `mapBimGrips` for the 4 stragglers (`Beam`/`MepFixture`/`MepManifold`/`MepRadiator`). **Behaviour-preserving — verified, not assumed:** `mapBimGrips` maps `midpoint → 'midpoint'` whereas the inline `.map()`s collapsed everything-but-`center` to `'vertex'`, so this is only safe if no generator emits `midpoint`; grepped every generator (`beam-grips`, `mep-fixture-grips`, `mep-manifold-grips`, `mep-radiator-grips` → `centred-box-grips` via the ADR-602 adapter) → they emit only `center`/`vertex`/`edge`/`corner` → identical output. **`FoundationRenderer.getGrips` deliberately NOT migrated:** `getFoundationGrips` *does* emit `type: 'center'` (foundation-grips.ts:152) while the renderer hardcodes `type: 'vertex' as const` for **all** grips — migrating would silently flip that grip's render type inside a de-dup commit. Logged as an open question for Giorgio (possible bug) in `.claude-rules/pending-ratchet-work.md`. **Verified: `jscpd:diff` 51 → 40 clones; 248/248 renderer + visibility tests (23 suites) GREEN; grep-verified zero orphan `pointInPolygon` imports and zero leftover inline bbox blocks.** **ΟΧΙ tsc (N.17).** Pure SSoT adoption — no ADR-040 subscription change (documented here for the CHECK 6D entity-renderer gate). **Remaining 40 clones = the second half of the backlog** (render() phase-preamble → `beginPhasedBodyRender`; `drawPolyline` → `tracePolygonScreenPath`/`strokePolylinePaths`) and are **NOT** mechanical: the holders extend `BaseEntityRenderer` (base-class change needed) and `FloorFinishRenderer`'s inline halo uses `glowExtraWidth + 1.5` vs the SSoT's `RENDER_LINE_WIDTHS.NORMAL + glowExtraWidth` (**visual** change on hover) → needs a decision, logged in `pending-ratchet-work.md`. Commit path: `SKIP_JSCPD_DIFF=1` (justified — clone-reducing vs HEAD; see the `--diff` whole-file limitation noted below). Baseline drops → run `npm run jscpd:baseline` after commit.
- **2026-07-16** — Clone dedup (cluster 5 — CRM task/calendar dialogs). **Completes a centralization that had been left half-finished:** an earlier pass extracted the shared *atoms* (`ui/date-picker-field.tsx`, `ui/enum-select.tsx`, `crm/shared/TaskDialogFormFooter.tsx`, `constants/crm-task-enums.ts`, `i18n/date-fns-locale.ts`, plus `combineDateAndTime`/`splitDateAndTime` in `lib/date-local.ts`) but left `CalendarCreateDialog` ↔ `TaskEditDialog` as twins in their *shell*, so `jscpd:diff` still reported **7 clone pairs** (the exact N.18 failure mode: centralize A, ship B+C as siblings). Extracted the two remaining SSoTs: **`crm/shared/TaskDialogShell.tsx`** (Dialog → header with icon+title → `sr-only` description → `form onSubmit` → children → footer; owns the `useIconSizes`/`useSpacingTokens`/`useLayoutClasses` triple and renders `TaskDialogFormFooter` itself, since `onCancel` was `() => onOpenChange(false)` in both) and **`crm/shared/TaskFormField.tsx`** (`fieldset` + `Label` + control, with an optional `action` slot for the create dialog's `VoiceMicButton` — the only place the label row diverged). **Not a god-shell:** the shell owns only what was byte-identical; each dialog still passes its own fields as children (create = contact/project/reminder/endDate, edit = status/priority), and `TaskDetailPanel` stays deliberately outside (typeable date field + `PopoverAnchor` = genuinely different UX, documented in `date-picker-field.tsx`). Also killed the last competing enum declaration: **`types/crm-extra.ts` deleted** — it re-declared `CrmTaskType`/`CrmTaskPriority`/`CrmTaskStatus` (identical unions) next to the new `constants/crm-task-enums.ts` SSoT; its single consumer (`crm/tasks/task-activity.ts`) now imports them from `@/types/crm`, which re-exports the SSoT. **Verified: `jscpd:diff` on the 16-file CRM set = 7 → 0 clones (no `SKIP_JSCPD_DIFF` needed); 13 NEW `date-local` SSoT tests (`combineDateAndTime`/`splitDateAndTime` round-trip, no-mutation, invalid-time → midnight instead of Invalid Date, JSON-serialised Timestamp, fallback) → 27/27 GREEN in `date-local.test.ts`; grep-verified zero orphan references after the import removals.** Both dialogs shrank well under N.7.1 (398→303, 332→209 lines). **ΟΧΙ tsc (N.17).** ⚠️ **browser-verify pending** — no test renders either dialog; the shell refactor is structural (same components, same props, same order) but unexercised end-to-end.
- **2026-07-16** — Clone dedup (cluster B follow-up β — repo-wide visibility-SSoT sweep): the 2Δ-plan visibility-gate preamble `const layer = e.layerId ? getLayer(e.layerId) : null; if (!resolveIsEntityVisible({category,layerId,discipline}, {objectStyles: store.getState().objectStyles, disciplineVisibility: store.getState().disciplineVisibility, layer})) return;` was copy-pasted **byte-near-identical in 21 BIM 2D renderers** (Beam/Column/ElectricalPanel/FloorplanSymbol/Foundation/Furniture/MepBoiler/MepFitting/MepFixture/MepManifold/MepRadiator/MepSegment/MepUnderfloor/MepWaterHeater/Opening/Railing/Roof/SlabOpening/Slab/Stair/Wall). Extracted **new SSoT** `bim/visibility/bim-plan-visibility.ts` → `resolveBimPlanVisibility(entity, layer)`: reads `objectStyles` + `disciplineVisibility` from the render-settings store once (event-time `getState()`, ADR-040-compliant — no subscription) and delegates to the pure `resolveIsEntityVisible`. **Deliberately a NEW file, not folded into `visibility-resolver.ts`** — the pure resolver stays store-free/unit-testable (adding a Zustand import would drag the store into its isolated test module graph); the wrapper keeps the same module path (`drawing-scale-store` shim) the renderers used, so every existing renderer test mock (`jest.mock('…/drawing-scale-store')`, incl. `bim-renderers-vg-visibility.test.ts`) still applies unchanged. The 9-line preamble collapses to a 1-line call per renderer (−190/+67 lines); `RoofRenderer` also drops its now-dead `useDrawingScaleStore` import. **3D/overlay render paths NOT touched** — they consult extra floor/building/isolate sources and call the pure resolver directly with a full `VisibilityContext`. Public API (class + `render()`) byte-identical. **Verified: 7 NEW SSoT jest tests (`bim-plan-visibility.test.ts`) + 248 renderer/visibility tests (23 suites) GREEN; `jscpd:check` (--full, authoritative Layer 2) = clones 3066/3494, −428 below baseline.** ⚠️ **Layer 1 (`jscpd:diff`) note:** co-staging all 21 renderers surfaces **51 intra-diff clones**, but these are **pre-existing body-debt** (grips/hover/Mep-appliance bodies at lines 200-360), verified **clone-neutral vs HEAD** (reconstructing the same 21 files at HEAD yields the identical 51) — my preamble change adds zero and appears in none of them. Commit path: `SKIP_JSCPD_DIFF=1` (justified — clone-neutral vs HEAD, Layer 2 passes −428) or split the commit. Baseline drops → run `npm run jscpd:baseline` after commit. New backlog logged: the 51 pre-existing Mep-appliance renderer-body clones (separate sweep).
- **2026-07-08** — ADR created. CHECK 3.28 implemented end-to-end: `.jscpdrc.json`, `scripts/check-jscpd-ratchet.js` (diff/full/smoke/write-baseline), `.jscpd-baseline.json` (4548), pre-commit Phase 0.6 diff gate, `.github/workflows/jscpd-ratchet.yml` CI ratchet, 39 Jest tests (all green), npm scripts `jscpd:*` + `test:jscpd`. jscpd@5.0.11 (MIT) added as devDependency via pnpm. Open decisions resolved with Giorgio (min-tokens 50, whole src/, hybrid gate).
- **2026-07-08** — Clone dedup pass (CHECK 3.28 diff-gate driven). Extracted `utils/scalar-math.ts` as the neutral, dependency-free SSoT for the `clamp` / `clamp01` / `clamp255` family + arc-length/circular sampling helpers (`path-sampler-strategies/path-sample-math.ts`), plus `raySegmentsIntersection`, `useGripMovement` scene-entity helpers, `grip-types` `export type *`, `DxfProjection` alias and `readParamsKind`. Canvas/micro-leaf consumers (`guide-click-handlers.ts`, `rendering/entities/shared/geometry-rendering-utils.ts`, `geometry-utils.ts` — the latter re-exports the clamp family for backward compat) now import from `scalar-math` instead of inlining `Math.max/min` clamps. Pure SSoT adoption — **no behavioural or ADR-040 architecture change** (documented here to satisfy the CHECK 6B/6D ADR gate). Split `grip-commit-adapters.ts` (→ `grip-parametric-dispatch.ts`) and `useRibbonCommands.ts` (→ `useRibbonToggleCommands.ts`) under the 500-line limit (N.7.1).
- **2026-07-14** — Clone dedup: `ThreeJsSceneManager` sync signatures → `FloorVisibilityScope` options object (in-file clone pair, `syncBimEntities` 293-298 ~ `syncBimEntitiesMultiFloor` 322-327: same 5 positional params — `floors`/`buildings`/`activeBuildingId`/`buildingVisModes`/`floorVisModes` — with identical defaults). Extracted ONE named `FloorVisibilityScope` type + `EMPTY_FLOOR_VIS_SCOPE` const in `scene-manager-actions.ts` (big-player option-bag convention: Three.js `set(options)` / Revit API option bags). Both `ThreeJsSceneManager` public wrappers now take a single `scope: FloorVisibilityScope = EMPTY_FLOOR_VIS_SCOPE` param and spread it; both `SyncBimEntitiesArgs`/`SyncMultiFloorBimEntitiesArgs` flat-intersect it (`… & FloorVisibilityScope`) so `scene-manager-sync.ts` internals (reading `args.floors`/… directly) stay byte-identical; the 3 `bim3d-resync.ts` call sites compose via a `buildFloorVisibilityScope(s, floorModes)` helper (external site = `{ ...EMPTY_FLOOR_VIS_SCOPE, floorVisModes }`). **Pure internal SSoT dedup — identical defaults & call-site values, no behavioural change** (documented here to satisfy the CHECK 6D ADR gate; behaviour ADRs ADR-399 Phase B / ADR-382 unchanged).

  **Same pass — the 2 pre-existing `scene-manager-actions.ts` self-clones that co-staging surfaced were also cleared** (no `SKIP_JSCPD_DIFF` needed): (a) `rebuildBimMeshes(deps, scope, buildGeometry)` now owns the highlighter-clear → focus-clear → **build** → BVH-rearm → building-visibility → selection-reapply → path-tracer → section sequence shared by `syncBimEntitiesIntoScene` / `syncMultiFloorBimEntitiesIntoScene` (they differ ONLY in the `BimSceneLayer.sync` vs `.syncMultiFloor` call, injected as `buildGeometry`); (b) `finalizeDxfOverlaySync(deps, fitDone, onFitApplied)` owns the invalidate → frame → section tail shared by `syncDxfOverlayIntoScene` / `syncDxfOverlayMultiFloorIntoScene`. Exported signatures unchanged → `scene-manager-sync.ts` and all callers untouched. The conditional-vs-unconditional `ssaoModulator.warmUp()` difference lives in `scene-manager-sync.ts` (NOT these functions), so both extractions are behaviour-identical. **Verified: `jscpd:diff` on all 3 files = 0 clones (was 2); 126 jest GREEN across `bim-3d/scene`.** Baseline drops → run `npm run jscpd:baseline` after commit.
- **2026-07-16** — Clone dedup (cluster B — BIM footprint-renderer boilerplate): `jscpd --diff` across the 5 `BaseEntityRenderer`-subclass renderers (`ColumnRenderer` — already migrated — + `ElectricalPanelRenderer` / `FloorplanSymbolRenderer` / `FoundationRenderer` / `FurnitureRenderer`) found **12 clone pairs** (22% dup) of the 3 patterns each re-inlined: the hover-halo glow block, the private `drawPolygonPath` screen-trace, and the bbox+point-in-polygon `hitTest`. **No new SSoT needed — it already existed:** `BimFootprintRenderer` (`bim-footprint-renderer.ts`, `protected drawPolygonPath` + `paintHoverHalo`) backed by `bim-polygon-render.ts` (`tracePolygonScreenPath` / `paintPolygonHoverHalo` / `polygonBboxHitTest`), which `Column`/`Slab` already extend. Migrated the **4 stragglers** onto it: `extends BaseEntityRenderer` → `extends BimFootprintRenderer`; inline hover-halo → `this.paintHoverHalo(verts, phaseState.phase === 'highlighted')`; inline `hitTest` bbox+`pointInPolygon` → `polygonBboxHitTest(bb, …footprint.vertices, point, tolerance)`; deleted each private `drawPolygonPath` (now inherited). Removed the now-dead `HOVER_HIGHLIGHT` / `pointInPolygon` / (where unused) `RENDER_LINE_WIDTHS` imports. Pure behaviour-preserving consolidation onto the tested SSoT — no ADR-040 subscription change (documented here for the CHECK 6D entity-renderer ADR gate). **Verified: `jscpd` on the 5 files = 6 clones (was 12, 22%→7.4%); 158 renderer-suite tests (18 suites) + 28 VG-visibility/tool-coverage GREEN.** The remaining 6 are OTHER patterns out of this cluster's 3-pattern scope — getGrips `.map`, the `resolveIsEntityVisible` render preamble, and the symbol-stroke draw loop — logged as a cluster-B follow-up. Baseline drops → run `npm run jscpd:baseline` after commit.

  **Cluster B follow-up (same session, 2026-07-16):** cleared 3 of the 6 remaining pairs. **(α) getGrips → `mapBimGrips`** for furniture/electrical-panel/floorplan-symbol (the 3 sharing the `g.type === 'center' ? 'center' : 'vertex'` map, 167-token clone). Safety-verified the blocker first: the centred-box grip adapter's `type` union is `'center' | 'corner' | 'vertex'` — **never `'midpoint'`** — so `mapBimGrips`' extra `midpoint→'midpoint'` arm never fires ⇒ behaviour-identical (`corner`/`vertex`→`vertex`, `center`→`center`). Foundation's `getGrips` (unconditional `type:'vertex'`, no center grip emitted) is different text, not part of the clone → left as-is. **(γ) symbol-stroke loop → new `strokePolylinePaths(ctx, toScreen, polylines)`** in `bim-polygon-render.ts` (skip <2-pt sub-arrays; caller owns style), adopted by ElectricalPanel (breaker rows) + FloorplanSymbol (WC strokes) — byte-identical to the inlined `for…{beginPath;moveTo;…lineTo;stroke}`. **(β) the `resolveIsEntityVisible` visibility-gate preamble was deliberately NOT touched** — it recurs across ~20+ renderers (not just these 5), so it belongs to a dedicated repo-wide visibility-SSoT sweep, not this cluster. **Verified: `jscpd` on the 5 files = 3 clones (12→3, 22%→3.4%); 171 renderer-suite tests (19 suites) + tool-coverage GREEN; 0 orphan refs.**

  **Cluster B tail (same day, surfaced co-staging all 4 stragglers for one commit):** committing the 4 renderers *together* tripped CHECK 3.28 on a 4th shared block the previous per-renderer commits never co-staged to see — the phase-body render preamble `determinePhase → paintHoverHalo → applyPhaseStyle → ctx.save() → setLineDash([])`, byte-identical in Electrical/Furniture/Floorplan (+ the `determinePhase…save()` head in Foundation): 3 flagged pairs (50–77 tokens). Extracted **`beginPhasedBodyRender(entity, verts, options): PhaseRenderingState`** onto `BimFootprintRenderer` (returns the phase state for the renderers that branch on it afterwards — Foundation's hover/select dim-pill). The now-dead `import type { Entity }` dropped from all 4. **Verified: `jscpd` on the 5 files = 0 clones (3→0); 158 renderer-suite tests (18 suites) GREEN.** The visibility-gate preamble (β) stays open as a repo-wide sweep — it is below the token threshold at this 5-file scope (0 flagged).
- **2026-07-16** — Clone dedup (cluster A — parametric grip-commit preamble + rotate-pivot): `jscpd --diff` across `grip-parametric-commits.ts` / `grip-parametric-copy.ts` / `grip-parametric-centred-box-commits.ts` found **7 clone pairs** (4× 8-line/65-token + 3× 15-line/79-token), the two blocks copy-pasted once per entity type: (1) the `createSceneManagerAdapter → getEntity → cast → type/params guard` entity-resolution preamble, and (2) the `BimRotateHotGripStore.getSnapshot() → useRotatePivot → anchor → currentPos` rotate-pivot-vs-plain-anchor resolution. Extracted **ONE SSoT** `hooks/grips/grip-commit-resolve.ts` with **2 helpers**: `resolveParametricGripEntity<T>(deps, entityId, entityType) → { sceneManager, entity } | null` (generic over the concrete entity type, discriminant literal guard, `candidate as T` narrowing declared-not-enforced — mirror `gripKindOf`), and `resolveGripCommitAnchor(isRotationGrip, gripPosition, delta) → { anchor, currentPos, pivotPatch }` (`pivotPatch` = `{ pivot }` for a rotate drag / `{}` otherwise, spread straight into the `apply*GripDrag` input). Migrated **20 call sites** across the 3 files (5 parametric commits + 11 hot-grip copies + 4 centred-box commits + the local `resolveMepManifoldGripContext` now delegates to the shared helper). Public commit API byte-identical (every `commit*GripDrag`/`commit*Copy` signature + output unchanged). **Verified: `jscpd:diff` on all 4 files = 0 clones (was 7); 8 NEW SSoT jest tests (`grip-commit-resolve.test.ts`) + 195 existing grip tests (20 suites) GREEN.** Baseline drops → run `npm run jscpd:baseline` after commit.
- **2026-07-16** — Clone dedup: the 10-item `common` + `common-*` namespace array passed to `useTranslation([...])` was **byte-identical in 137 files** (the CHECK 3.28 diff-gate tripped when the two sales sidebars — ADR-199 — co-staged; the array is invisible to the full-repo ratchet because a single scattered ~30-token line/file never reaches min-tokens 50 pairwise). Centralized to **one SSoT const** `COMMON_NAMESPACES` in `src/i18n/namespace-bundles.ts`; all 137 files + the 2 sidebars now `useTranslation(COMMON_NAMESPACES)` (the redundant `SALES_SPACE_SIDEBAR_NAMESPACES`, added the prior session, was byte-identical → removed). **Tooling contract fix (the non-obvious half):** CHECK 3.8 (`check-i18n-missing-keys.js`) and the baseline generator learn a file's namespaces by regex-matching the *literal* `useTranslation([...])` array — a bare const reference would match nothing → `namespaces.length === 0` → the file is silently **skipped**, dropping `t()` key validation for it. So both scripts now share `scripts/lib/i18n-namespace-extract.js` (SSoT — the two previously carried a duplicate `extractNamespaces`), which resolves a bundle identifier back to its list by statically reading `namespace-bundles.ts` (mirrors CHECK 3.13's `readServiceFormNamespaces`). `useTranslation`'s param widened `string[]` → `string | readonly string[]` so `as const` bundles type-check. **Verified coverage-neutral:** per-file `git show HEAD` differential — every touched file extracts the *identical* namespace set inline-vs-const (NotificationProvider 10↔10, still fully checked); CHECK 3.8 exit 0 on sample; 8 new jest tests GREEN (`scripts/__tests__/i18n-namespace-extract.test.js`). No locale keys, no `t()` calls, no runtime behaviour changed.
- **2026-07-16** — Clone dedup (cluster — design-system card shells): `GridCard.tsx` ↔ `ListCard.tsx` carried **8 clone pairs / 151 duplicated lines (21.3%)** — the two shells had been copy-evolved from each other, so the hover toolbar, the header, the badge rows, the stats/children tail, the event handlers and the selection rail existed twice. **Not a God-shell merge**: grid and list are genuinely different layouts (ADR-588 / Revit-Figma "keep per-type surfaces, share primitives"), so both shells stay, rebuilt on **6 new primitives** (`CardHeaderBlock` / `CardTitleBlock` / `CardBadges` / `CardBody` / `CardActionsToolbar` / `CardSelectionIndicator`), **2 hooks** (`useCardShell` = shell tokens+handlers, `useCardInteraction` = the click/Enter/Space activation contract) and `pickCardIdentity` (the one place that knows the identity prop set — otherwise each shell forwards it by hand and they drift). Deltas are props, not copies: `density`/`overlay`/`withTooltips` on the toolbar, `badgeRowClassName` (`flex-wrap` for tiles vs `overflow-hidden` for rows), `statsLayout`. **Public API byte-identical** (`GridCardProps`/`ListCardProps` + `@/design-system` exports) → **none of the 112 consumer files touched**; the `GridCardBadgeVariant ⊂ ListCardBadgeVariant` invariant that `domain/cards/**` relies on is preserved and now documented in both `.types.ts`.
  **The lead the handoff flagged resolved as dead code, not unadopted SSoT:** `ListCardBaseProps`/`ListCardSelectionState`/`ListCardBadge` (`primitives/Card/types.ts`, 2026-01-08, "for future ListCard component") had **zero importers** — the ListCard shipped the same day and ignored them — and had diverged from reality (`selection?: {isSelected,onSelect}` vs the actual flat `isSelected`+`onClick`; badge union missing `info`). Adopting them = a worse API forced onto 112 files; **deleted**, which also resolves the collision behind the `ListCardBadge as ListCardBadgeConfig` alias. The real shared contract (`CardBadge`/`CardAction`/`CardIdentityProps`/`CardBaseProps`) was instead derived from the two live shells, with `CardBadgeVariant` sourced from the centralized Badge — killing the `as …|'error'` cast both shells carried (`'error'` was in *neither* union). Both barrels now `export *` from the layer below (the re-listed export block was itself a clone).
  **Verified: `jscpd:diff` on all 18 files = 0 clones (was 8); ESLint 0 errors; 34 NEW jest tests (`design-system/__tests__/card-shells.test.tsx` — the design-system had none) GREEN**, covering the shared contract against both shells via `describe.each` + each shell's genuine differences. Full ADR: **ADR-013**. Baseline drops → run `npm run jscpd:baseline` after commit.
- **2026-07-16** — Clone dedup (cluster — entity trash-state hooks): the `src/hooks/` quartet `useBuildingsTrashState` ↔ `useParkingTrashState` ↔ `useProjectsTrashState` ↔ `useStoragesTrashState` carried **12 clones / 122 duplicated lines (21.5%)** — one trash state machine, copied four times. **This was not an unowned duplicate: ADR-281 §2 had already decided "one centralised mechanism, not copy-paste per entity"** and even listed `useSoftDelete.ts` + `useTrashView.ts` as deliverables; they were never written, so the copies filled the gap for ~3 months. New engine `hooks/trash/useEntityTrashState.ts` (fetch / toggle / restore / staged permanent-delete + confirm/cancel / auth-gated initial load); each sibling is a ~25-line binding holding an `EntityTrashSpec` (kind, route, response-key selector, optional realtime `refreshOn`). **Not a God-shell** — what genuinely differs stays a spec field or an option: Projects notify through their own notification SSoT, Storages subscribe to `STORAGE_DELETED`, Buildings keep `onBuildingMovedToTrash`. **Scope decided by measurement, not by name**: `usePropertiesTrashState` (0 clones vs the quartet — `Promise.allSettled` + ADR-226 guard + no auto-fetch) and `useContactsTrashState` (no fetch at all) share the suffix and nothing else; both left alone and allowlisted/documented. Four prop names for one idea (`onBeforeToggle`/`onRestoreComplete`/`onItemDeleted`/`setSelectedProject`) → `clearSelection`, following the headers precedent (one language, not a `variant` that would encode the drift permanently). **Verified: `jscpd:diff` clean without `SKIP_JSCPD_DIFF`; 12 clones → 0 (measured, quartet + engine); 55 NEW tests — written PRE-merge against untested hooks, unchanged across it, GREEN on both sides; mutation-verified ×2** (wrong route/kind binding → 3 red; engine idempotency guard dropped → 1 red; reverted). Registry `entity-trash-state` chosen so it matches **only** the copied machine (`setPendingPermanentDeleteIds`) → 0 pre-existing violations, **no `ssot:baseline` needed** (shared tree). Golden 90/90. Full ADR: **ADR-281**. Baseline drops → run `npm run jscpd:baseline` after commit.
- **2026-07-17** — Clone dedup (cluster — client mutation actions over a policy gateway): the backlog said "`usePaymentPlan` self-clones (~69 lines)", i.e. **one file**. Measuring said otherwise, in both directions: the same archetype ran through **`useChequeRegistry` + `useLoanTracking`** (16 clones with `usePaymentPlan`) **and `useLegalContracts`** (a different gateway and a different ADR — 230 — yet +3 clones), for **19 clones / 229 duplicated lines (28.17%) / 23 mutation callbacks**; while **`useInterestCalculator` measured +0 against the trio** and stayed out — it shares only the *name* `fetchJson`, and its copy is a genuine variant (default Content-Type, `res.text()` on error → different failure semantics). Scope set by measurement, not by the noun in the hook name.
  **NEW** `src/lib/mutations/gateway-action.ts` — `runGatewayAction(invoke, { run, background?, blocked? })` owns the four decisions that were retyped 23 times: scope guard → gateway → refetch → map onto `ActionResult` so a throw never escapes into a click handler. `background` is the fire-and-forget **label**, not a mode flag, so "backgrounded but unlabelled" is unrepresentable. Bindings keep their own `useCallback`: action identities stay stable, which consumers depend on. **NEW** `src/lib/api/fetch-json.ts` — `fetchJson`/`jsonRequest` were **5 byte-identical copies** (4 hooks + `mutateJson` in the finance gateway); semantics preserved exactly. It does **not** replace `apiClient`: that one is Bearer-authenticated, retries, and unwraps `{success,data}` → these callers are session-cookie same-origin and read the envelope themselves. The `financial-intelligence` / `interest-calculator` / `filesystem` gateways keep their own variants — each differs (error fallback text, `res.text()`, `apiClient` delegation), so merging them would be a behaviour change outside the measured cluster. **NEW** `src/hooks/api/useApiList.ts` — cheque+loan only; `usePaymentPlan` (two routes in one `Promise.all`) and `useLegalContracts` (REST + Firestore subscription) keep their own fetchers rather than bending it into a god-hook.
  **🔴 One deliberate behaviour change:** `deletePlan` was the only one of nine actions **without a guard** — it forwarded `propertyId!` and reached the gateway with a literal null. Its eight siblings, including `updatePlan` written directly above it, all refuse first; no ADR or caller wants a null-scoped delete. Copy-paste omission → **closed**, not encoded as an option (same call as the trash-hooks `clearSelection` merge). `useLoanTracking`'s `'No unit selected'` kept as spec — wording differs, the machine does not.
  **Verified: 19→0 clones** measured; `jscpd:diff` clean **without** `SKIP_JSCPD_DIFF`; **182 tests** (`hooks/__tests__/gateway-action-bindings.test.tsx` 170 + `lib/mutations/__tests__/gateway-action.test.ts` 12) written **before** the merge onto untested hooks, **GREEN on HEAD (169) and after**, same assertions but the one deliberate change; **mutation-verified ×2** (wrong gateway in a binding = 5 red; guard removed from the engine = 21 red; both reverted); registry module `gateway-action` — pattern grep-verified to hit **only** the SSoT (**0 pre-existing violations → no `ssot:baseline` needed**, which matters in a shared tree); golden **90/90**; `ssot:flat` 357 modules. No tsc (N.17).
- **2026-07-17** — Clone dedup (cluster — property viewer hook composition): the backlog said "`usePropertyEditor` ↔ `usePropertyViewer` (61 lines)", i.e. one pair, one kind of duplicate. Measuring said **three unrelated duplicates across four files** — and, unusually, none of them was a copied *engine*. Sibling-by-sibling jscpd: the pair = 2 clones/59 lines; **+`usePublicPropertyViewer` +1** (its `DEFAULT_PUBLIC_FILTERS` is a byte-identical copy of `DEFAULT_FILTERS`); **+`usePropertyFilters` +1** (its inline `emptyStats` is a byte-identical copy of `DEFAULT_STATS`); **`usePropertyState` +0 and `usePropertyDescriptionGenerator` +0 → out**. `usePropertyViewer` also carries a **self-clone** (destructure ↔ re-export), which is why every pairing looked "+1" until attributed by subsetting. **4 clones / 4 files → 0.**
  **`usePropertiesViewerState` measured +0 and grep confirmed it rather than expanding scope** (the handoff's standing warning is the reverse — that jscpd under-reports extent — so it was checked both ways): its `totalStorageUnits: 0` sits inside a *computed* stats object, and `user-preferences-types`' zeroed block is a **different shape** (`soldStorageUnits`/`reserved`, no operational metrics). Both are genuinely different constructs, not copies. It is touched only because its `DEFAULT_FILTERS` import had to move.
  **The audit changed the shape of the fix — the duplicates were a symptom of a circular import.** `usePropertyViewer` imports `usePropertyEditor`, which imported `DEFAULT_FILTERS` **back from `usePropertyViewer`**. The zero values had been parked in a *hook* module, so the second copy in `usePublicPropertyViewer` was the cheaper option for whoever wrote it. Fix follows the repo's own precedent (`EMPTY_STATS: PropertyStats` sits beside its interface in `project-status-types.ts`): **the zero value of a type lives beside the type.** `DEFAULT_FILTERS` → `src/types/property-viewer.ts` (beside `FilterState`), `DEFAULT_PROPERTY_STATS` → `src/types/property.ts` (beside `PropertyStats`, renamed from `DEFAULT_STATS`, which had **no external consumer**). The cycle is gone; both consumers repointed; no back-compat re-export was left behind — two canonical homes for one constant is what produced this in the first place.
  **The field-list duplicate is not extractable, it is deletable.** `usePropertyEditor`'s 15 fields + 15 setters were written **three times**: its own return, the viewer's destructure, the viewer's re-export. Replaced by `const editor = usePropertyEditor()` + `...editor` in the return — the list now lives **only** where the `useState` calls are (a new field was 3 edits, now 1). `usePolygonHandlers` is **not** spread: it returns its own `handleUpdateProperty` that the viewer deliberately drops in favour of the guarded one, so a blind spread would silently regress the mutation guard — locked by a test.
  **Boy Scout (N.0.2):** `FALLBACK_FLOOR_ID` was dead at HEAD while the line below re-typed its exact expression inline (`process.env.NEXT_PUBLIC_DEFAULT_FLOOR_ID || 'floor-1'`) — the const now feeds that call site. **Lines: viewer 292→226, filters 154→143, public 287→275 (−89 total); editor unchanged (it is the source).**
  **No registry module — deliberate, and the honest reason:** the candidate patterns were grep-verified first and **rejected**. `priceRange: { min: undefined, max: undefined }` legitimately appears in `defaultPropertyFilters`/`defaultUnitFilters`/parking/storage configs, which are **different filter types** (no `project`/`building`; `type` vs `propertyType`). No single line lives *only* in this SSoT — the distinguishing signal is the multi-line field combination, which a line-based ERE cannot express. A false-positive-prone pattern is worse than none; the jscpd ratchet (CHECK 3.28) is already the correct gate for a re-duplicated data literal.
  **Verified: 4→0 clones** measured (`jscpd --min-tokens 50` on the 5-file family); **85 tests** (`hooks/__tests__/property-viewer-composition.test.tsx`) written **before** the refactor onto **four untested hooks**, **GREEN on HEAD and after, assertions unchanged** (the expected empty-FilterState and zero-PropertyStats values are typed out **independently** in the test, so a drifted SSoT fails rather than agrees with itself); **mutation-verified ×2** (viewer not forwarding `editor.filters` = 1 red; `availableProperties: 0→1` in the zero-value SSoT = 2 red; both reverted); ESLint 0. No tsc (N.17).
  ⚠️ **`jscpd:diff` flags one pair that is NOT mine and must not be "fixed" blind:** `src/types/property.ts:423-456 ↔ 530-550` (34 lines/89 tokens) — two interfaces restating the same `condition`/`renovationYear`/`deliveryDate`/`systemsOverride` block. **Proven pre-existing**: my only edit to that file is `@@ -667,0 +668,24 @@` (a pure append), and running jscpd on `git show HEAD:src/types/property.ts` reports the **identical** range 423:5-456:5. The diff gate is baseline-unaware, so it surfaces because the file is in the staged set → **`SKIP_JSCPD_DIFF=1` is justified for this commit, with that evidence**. Merging those two interfaces is a type change across unmeasured consumers — recorded, not smuggled in.
