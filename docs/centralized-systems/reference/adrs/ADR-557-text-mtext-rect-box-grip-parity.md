# ADR-557: Text / MText λαβές με ΠΛΗΡΗ parity ορθογώνιου τοίχου/κολόνας (SSoT)

| Πεδίο | Τιμή |
|---|---|
| **Status** | 🟢 Slices 1-5 + **Φ-attachment** (H+V real-metrics) IMPLEMENTED (UNCOMMITTED) — grips + οριζόντια browser-verified· **κάθετο (visual/em split) 🔴 εκκρεμεί browser-verify** (Giorgio 2026-07-07) |
| **Date** | 2026-06-30 |
| **Last Updated** | 2026-07-07 |
| **Category** | Canvas & Rendering / Grips |
| **Location** | `src/subapps/dxf-viewer/` (`bim/text/`, `hooks/grips/`, `rendering/{entities,ghost}/`, `core/commands/text/`, `bim-3d/{converters,grips}/`, `canvas-v2/dxf-canvas/`) |
| **Author** | Claude (κατόπιν εντολής Giorgio) |
| **Related ADRs** | **ADR-363** (rect-grip-engine + column adapter = το πρότυπο), ADR-436 (pad adapter), ADR-397 (glyph registry SSoT), ADR-507 §8 (`MergeableUpdateCommand`), ADR-040 (preview canvas), ADR-537 (3D raw-dxf grips), ADR-530 (glyph text render), ADR-108 (text-metrics ratios) |

---

## Summary

Όταν επιλέγεις **ορθογώνιο τοίχο/κολόνα** → 8 λαβές (4 γωνίες + 4 μέσα-πλευράς) + 1 move + 1 rotation. Ο Giorgio ζήτησε **τον ΙΔΙΟ ΑΚΡΙΒΩΣ κώδικα** στα **ΚΕΙΜΕΝΑ** (TEXT + MTEXT): ίδιες λαβές, ίδιο resize/move/rotate, **μία πηγή αλήθειας, όχι διπλότυπο**.

Λύση: ΝΕΟ entity-agnostic adapter `bim/text/text-grips.ts` που **καλεί** τον κοινό πυρήνα `rect-grip-engine`/`rect-frame` (ίδιος κώδικας με τοίχο/κολόνα/πέδιλο), αντικαθιστώντας την παλιά μονή «position grip» του κειμένου. Εκπέμπει **10 grips** και στα 3 paths (2D interaction, 2D visual render, 3D viewport) + live ghost (preview ≡ commit) + minimal commit (`UpdateTextTransformCommand`).

- **MTEXT**: πλάτος = το πραγματικό `MTextEntity.width`.
- **TEXT (απλό)**: πλάτος = ΝΕΟ `widthFactor` (AutoCAD TEXT X-scale) + οριζόντιο `ctx.scale(widthFactor,1)` στον renderer.

---

## Context — δύο κρίσιμα ευρήματα audit (το αρχικό πλάνο τα έχασε)

### 1. ΔΥΟ ξεχωριστά grip συστήματα (όχι ένα)
Το αρχικό πλάνο υπέθεσε ότι το emission ζει μόνο στο `computeDxfEntityGrips` (`hooks/grip-computation.ts`). Στην πραγματικότητα:

| Σύστημα | Συνάρτηση | Τι τροφοδοτεί |
|---|---|---|
| Interaction (hit-test) + **3D render** | `computeDxfEntityGrips` → `getTextGrips` | `GripRegistryPublisher`→`AllGripsStore`· `grip-3d-dxf-raw-grips` |
| **2D VISUAL render** (ζωγραφική τετραγώνων) | `BaseEntityRenderer.getGrips()` **per-entity** | `renderGrips`→`PhaseManager.renderPhaseGrips` |

Αν αλλάξεις μόνο το πρώτο, το 3D δείχνει 10 λαβές αλλά ο 2D καμβάς δείχνει την παλιά **1**. **Και τα δύο** πρέπει να επιστρέφουν το ίδιο grip set → και τα δύο καλούν `getTextGrips`. (Mirror `ColumnRenderer.getGrips` → `getColumnGrips`.)

### 2. Λάθος σύμβαση άγκυρας bbox (`entity-bounds.ts`)
Το αρχικό πλάνο §7 παρέπεμψε στο `entity-bounds.ts` που τοποθετεί `position` = **πάνω-αριστερά** (box προς τα **κάτω**, `minY = position.y − h`). Η **πραγματική** τοποθέτηση κειμένου της εφαρμογής (επιβεβαιωμένη από τον 3D text converter `dxf-text-3d.ts`: *«baseline-left, matching the 2D anchor + getEntityBBox lower-left corner»*, `mesh.center = position + (w/2, +h/2)`) είναι `position` = **κάτω-αριστερά**, box προς τα **ΠΑΝΩ**. Η λάθος σύμβαση έβαζε τις λαβές **κάτω από** το κείμενο (ίδια λάθος θέση 2D **και** 3D, αφού κοινό `getTextGrips`). Διόρθωση: `textToRectFrame` centre = `position + R(θ)·(w/2, +h/2)`.

---

## Decision — αρχιτεκτονική (μηδέν διπλότυπο)

**Κοινός πυρήνας (reuse, ΟΧΙ αντιγραφή math):** `bim/grips/rect-frame.ts` + `rect-grip-engine.ts` (opposite-corner/edge fixed) — ο ίδιος που χρησιμοποιούν τοίχος/κολόνα/πέδιλο.

**ΝΕΟ adapter `bim/text/text-grips.ts`** (πρότυπο: `column-rect-adapter.ts`):
- `textToRectFrame(dxfText)` — `position` (κάτω-αριστερά) + `widthFactor`/`width` + `rotation` → centroid `RectFrame`. Defensive: `resolveBoxHeight` (fallback 2.5) + `naturalTextWidth` ανέχεται undefined `text` (pure, δεν σκάει).
- `getTextGrips(entity)` — 10 `GripInfo` (mirror `rectColumnGrips`): center MOVE (0) + rotation (1) + 4 edges (2,3,8,9) + 4 corners (4..7). Rotation handle = `rotationHandleMidwayOffset` (ίδια πολιτική με κολόνα).
- `applyTextGripDrag(kind, {entity,delta,currentPos?,ortho?,pivot?})` → `TextTransformPatch {position?,rotation?,height?,width?,widthFactor?}`. ΕΝΑ pure transform· pivot περιστροφής = bbox-centre + re-home `position` (Figma-like).

**Διάκριση MTEXT vs TEXT:** `width != null` ⇒ MTEXT (patch `width`)· αλλιώς TEXT (patch `widthFactor`). Ο converter (`dxf-text-entity-converter.ts`) κουβαλά `width` μόνο σε MTEXT, `widthFactor` μόνο σε TEXT.

**Commit:** ΝΕΟ minimal `UpdateTextTransformCommand` (extends `MergeableUpdateCommand`) που γράφει **top-level** πεδία (`position/rotation/height/fontSize/width|widthFactor`) — ΟΧΙ το `UpdateTextGeometryCommand` (κενά: γράφει `textNode.rotation` ενώ ο renderer διαβάζει flat `rotation`· αγνοεί `width` χωρίς `columns`). Drag-merge → ένα undo.

**Glyph render (TEXT X-scale):** `TextRenderer` προσθέτει **μόνο** οριζόντιο `ctx.scale(widthFactor,1)` γύρω από το text origin, ΜΕΤΑ την υπάρχουσα rotation (μηδέν αλλαγή rotation/zoom math)· `widthFactor === 1` → byte-identical legacy path. `scaleText` (toolbar Scale) τιμά `widthFactor *= |sx|` (Boy-Scout consistency με `scaleMText`).

**Glyph icons:** `grip-glyph-registry` → `text-move`='move', `text-rotation`='rotation' (4-βέλη / καμπύλο βέλος, ίδιο SSoT με κολόνα).

### ΧΩΡΙΣ mirror grip (δεν ζητήθηκε).

---

## Files (Slices)

- **Slice 1 (foundation):** `grip-kinds.ts` (`TextGripKind`), `grip-types.ts`, `grip-computation-types.ts`, `useGripMovement.ts`, `dxf-types.ts` (`DxfText.width?/widthFactor?`), `entities.ts` (`TextEntity.widthFactor?`), ΝΕΟ `bim/text/text-grips.ts` + tests.
- **Slice 2 (emission):** `dxf-text-entity-converter.ts` (carry width/widthFactor), `grip-computation.ts` (`case 'text'` → `getTextGrips`).
- **Slice 3 (commit+ghost):** ΝΕΟ `UpdateTextTransformCommand.ts`, ΝΕΟ `grip-parametric-text-commits.ts` (+re-export), `grip-commit-adapters.ts` (dispatch), `grip-projections.ts`, `grip-drag-preview-transform.ts`, `apply-entity-preview.ts`, `entity-preview-types.ts`, `unified-grip-types.ts`, `grip-registry.ts` (forward `textGripKind`).
- **Slice 4 (2D render parity + widthFactor):** `TextRenderer.getGrips` (10 grips, mirror Column) + horizontal `widthFactor` scale + hitTest width, `grip-glyph-registry.ts`, `entity-bounds.ts`, `scale-entity-transform.ts`.
- **Slice 5:** αυτό το ADR.

**Tests:** `bim/text/__tests__/text-grips.test.ts` (geometry/resize/rotation/round-trip), `hooks/__tests__/grip-computation-text.test.ts` (emission=10).

---

## Φ-attachment — Attachment-aware Text-Box SSoT (2026-06-30)

### Πρόβλημα (Giorgio)
«ΚΕΙΜΕΝΑ, ΦΩΤΕΙΝΟ ΠΛΑΙΣΙΟ HOVER ΚΑΙ ΛΑΒΕΣ δεν συμπίπτουν, ούτε 2Δ ούτε 3Δ· και είναι σε **διαφορετικές θέσεις** 2Δ vs 3Δ (όχι full parity)· το hover frame δεν εμφανίζεται καθόλου στο 2Δ.»

### Root cause (διόρθωση του ευρήματος #2 πιο πάνω)
Η `position` ενός κειμένου είναι το **attachment point** (9-point grid TL/TC/.../BR, MTEXT group 71) — **ΟΧΙ** πάντα κάτω-αριστερά. Το Slice-1 finding #2 κωδικοποίησε σταθερά **BL** (lower-left, box προς τα πάνω) βασισμένο στο σχόλιο του `dxf-text-3d.ts`. Αυτό είναι σωστό **μόνο** για baseline-left κείμενο· για κάθε άλλο attachment (π.χ. **BR** στο screenshot) η BL σύμβαση τοποθετεί το box σε λάθος μεριά. Επιπλέον υπήρχαν **4 ασύμφωνες πηγές** box, όλες attachment-blind:

| Σημείο | Σύμβαση πριν | Σύμπτωμα |
|---|---|---|
| `textToRectFrame` (grips) | BL (lower-left, +x/+y) | λαβές σε λάθος μεριά |
| `dxf-text-3d.ts` (3D mesh) | BL (lower-left, +x/+y) | 3D κείμενο σε λάθος θέση |
| `TextRenderer.hitTest` | TL (top-left, +x/−y) | άλλη σύμβαση |
| `getEntityBBox` (cull + 3D hover frame) | συμμετρικό ±h | λάθος, αλλά ορατό frame |

Ο **2D renderer** είναι η μόνη σωστή πηγή: διαβάζει `textStyle.textAlign`(L/C/R) + `textStyle.textBaseline`(T/M/B) (παράγωγα attachment, `extractFirstRunStyle`). Default renderer = `top/left` (**TL**) → κείμενο **κάτω-δεξιά**, ενώ grips+3D (BL) → **πάνω-δεξιά** → εξ ου 2Δ(κάτω)≠3Δ(πάνω) **και** λαβές≠κείμενο ακόμη και χωρίς attachment.

### Λύση — ΕΝΑ box SSoT, N consumers (reuse, μηδέν διπλότυπο)
ΝΕΟ `bim/text/text-box.ts` (pure):
- **HORIZONTAL** — `x` κέντρο από το `offsetForJustification` (`text-engine/layout/attachment-point.ts`, ADR-344 Φ3· μόνο η στήλη L/C/R) μέσω `horizontalCenterOffset` πάνω στο **πραγματικό glyph advance** (`measureTextAdvanceWorld`). Το **VISUAL** box μετά **inset-άρει** το advance box κατά τα glyph **side bearings** (`horizontalInkFractions`: leading = `inkLeft/advance`, trailing = `(advance−inkRight)/advance`) → αγκαλιάζει τα γράμματα ΚΑΙ αριστερά/δεξιά (Giorgio 2026-07-07: «επεκτείνεται προς τα έξω»). ZERO inset για MTEXT (πλάτος = explicit frame) + no-font.
- **VERTICAL — ΔΥΟ boxes** (η σύμβαση «visual bounds vs edit box» των μεγάλων editors):
  - `resolveTextBox` (**VISUAL**) — αγκαλιάζει τα ΖΩΓΡΑΦΙΣΜΕΝΑ glyphs: baseline εκεί που τον τοποθετεί ο renderer (**font** ascent/descent ανά attachment row, mirror `TextRenderer.fillGlyphRun`) + extent = πραγματικό glyph **INK** (cap height για κεφαλαία, +descenders για g/p/y), μέσω `measureTextVerticalRatios` (`text-vertical-metrics.ts`). Είναι το box για **2D grips + hover frame + hitTest** → λαβές/πλαίσιο ≡ γράμματα (Giorgio 2026-07-07: το em-box ήταν ~0.19·em ψηλότερο πάνω· μετρημένο 93 units πάνω από το cap-top).
  - `resolveTextEmBox` (**NOMINAL**) — το προ-metrics em box (`emVerticalRatios`), για το **3D textured plane** (`dxf-text-3d`) + **culling**, που είναι em-based (το 3D canvas ζωγραφίζει το glyph κεντραρισμένο σε em cell) και ΔΕΝ πρέπει να ακολουθήσει το cap box.
  ```
  center = position + R(rotationDeg)·{ horizontalCenterOffset, ((top+bottom)/2)·h }
  halfLength = ((top−bottom)/2)·h      // visual: top−bottom = inkAscent+inkDescent
  ```
- `textBoxToPosition(frame, entity)` — inverse του **VISUAL** box (ίδια ratios) → resize/rotate κρατά καρφωμένο το attachment point (Revit/AutoCAD).
- `textVisualExtentRatio(entity)` — visual extent ÷ em (= inkAscent+inkDescent) — ο διαιρέτης που το resize χρησιμοποιεί για να ανακτήσει το ονομαστικό `height` από το τραβηγμένο box height (χωρίς jump στο release· em-path → 1.0).
- `textBoxCornersWorld` (visual) / `textEmBoxCornersWorld` (em) / `textBoxAABB` (em) — rotation-aware γωνίες + AABB.

**Consumers:**
- `text-grips.ts` — `textToRectFrame` = re-export του `resolveTextBox` (visual)· `framePatch` → `height = boxHeight / textVisualExtentRatio` (visual→nominal), `position` → `textBoxToPosition`.
- `dxf-text-3d.ts` — anchor mesh στο `resolveTextEmBox().center` (em· ώστε το 3D κείμενο να ΜΗΝ μετακινηθεί ~53 units σε 277-unit τίτλο).
- `TextRenderer.hitTest` — rotation-aware test στο VISUAL box· 2D hover frame stroke `textBoxCornersWorld` (visual) → φωτεινό πλαίσιο ≡ γράμματα.
- `getEntityBBox` (`dxf-viewport-culling.ts`) → `textBoxAABB` (em· γενναιόδωρο cull, δεν pop-άρει το κείμενο στην άκρη).
- `dxf-entity-outline.ts` (3D hover halo) → `textEmBoxCornersWorld` (em· ταιριάζει με το 3D plane).

**ΟΧΙ αλλαγή** στη rotation/zoom/scale math του `renderTextContent` (guard αρχείου τηρήθηκε — μόνο anchor/box/hitTest + additive hover frame).

### Drag-response fix (Giorgio: «πιάνω τις λαβές και τις μετακινώ, το κείμενο ΔΕΝ ανταποκρίνεται»)
Εμπειρικό trace ΟΛΟΥ του pipeline (όχι μεμονωμένα) βρήκε **2 κενά** στο live ghost / commit:
1. **MTEXT αγνοούνταν στο live ghost.** Ο `applyEntityPreview` λαμβάνει το **scene entity**, του οποίου ο τύπος είναι `'text'` **Ή** `'mtext'`. Το branch έλεγχε μόνο `entity.type === 'text'` → για MTEXT επέστρεφε το ίδιο ref → `transformed === entity` → ο ghost παρακάμπτεται → **καμία ζωντανή απόκριση** (το commit όμως ήδη δεχόταν `'mtext'` → ασυμφωνία). FIX: `entity.type === 'text' || entity.type === 'mtext'` (mirror του commit).
2. **Το box έχανε το attachment στο drag.** Το scene entity κουβαλά το attachment στο `textNode.attachment` (όχι στο `textStyle`), και το `projectSceneTextToDxf` (commit) δεν το περνούσε → `resolveTextBox` έπεφτε σε TL. FIX: ο `resolveTextBox` διαβάζει `textNode.attachment` πρώτα (scene path), μετά `textStyle` (flat path)· το commit `projectSceneTextToDxf` κουβαλά `textStyle` μέσω του υπάρχοντος `extractFirstRunStyle`. Έτσι το box στο ghost + commit ταυτίζεται με τις λαβές που έπιασε ο χρήστης (TL..BR).

**Tests:** ΝΕΟ `bim/text/__tests__/text-box.test.ts` (9-point grid centre, default=TL, corners pin anchor, 2Δ≡3Δ move-grip===centre, inverse round-trip incl. rotation, AABB, `textNode.attachment`). ΝΕΟ `rendering/ghost/__tests__/apply-entity-preview-text.test.ts` (TEXT+**MTEXT** move/corner ghost regression). Updated: `text-grips.test.ts` (`text()` helper ρητά BL), `dxf-wireframe-hit-test.test.ts` (νέο attachment-aware bbox). 87 jest GREEN.

---

## Multi-line — Enter → πραγματική αλλαγή γραμμής (2026-07-07)

### Πρόβλημα (Giorgio)
Γράφοντας κείμενο σε **πολλαπλές γραμμές** (Enter μέσα στη φόρμα), στον καμβά το κείμενο **δεν έσπαγε**: όλες οι γραμμές σε **μία σειρά** και στα σημεία των Enter ζωγραφίζονταν **«tofu» κουτάκια (□)**.

### Root cause (SSoT audit)
Το flat `text` κουβαλά ήδη `\n` ανάμεσα στις paragraphs (το `extractFlatText` τις κάνει `join('\n')`), αλλά **κανένας** consumer δεν έκανε split: renderer, box, 3D περνούσαν **ΟΛΟΚΛΗΡΗ** τη `\n`-joined συμβολοσειρά στον glyph shaper → ο shaper δεν έχει glyph για `\n` → notdef «□», και όλες οι paragraphs κατέρρεαν σε ένα baseline. Το `text-engine/layout` (`layoutTextNode`, word-wrap/columns) υπάρχει αλλά τροφοδοτεί **μόνο** το `TextSnapProvider` — όχι το render path (ζωγραφική flat string).

### Λύση — ΕΝΑ split/line-metrics SSoT, 3 consumers (μηδέν διπλότυπο)
ΝΕΟ `bim/text/text-lines.ts`:
- `splitTextLines(text)` — split σε `\r\n|\r|\n`, εγγύηση ≥1 γραμμή.
- `resolveLineSpacingRatio(text)` = **υπάρχουσα** `CHARACTER_METRICS.LINE_HEIGHT_RATIO` (1.2, «for multi-line text spacing») × `textNode.lineSpacing.factor` (default 1.0) — **καμία νέα magic σταθερά**.
- `resolveMultilineExtents(row, lineCount, ratio)` — κατανομή του extra ύψους `(L−1)·ratio` ανά attachment row ώστε το `position` να μένει η άγκυρα: **T** μεγαλώνει προς τα **κάτω**, **B** προς τα **πάνω**, **M** συμμετρικά. **Ένας** κανόνας που μοιράζονται renderer + box.

Consumers (multi-line-aware μέσα στις **υπάρχουσες** συναρτήσεις — grips/hover/hitTest/3D/culling/resize έρχονται δωρεάν από το box SSoT):
1. **`rendering/entities/TextRenderer.ts`** — `renderTextContent` κάνει split· ΝΕΟ `paintTextLines` ζωγραφίζει κάθε γραμμή σε `origin + firstOffset + i·advance` (`advance = screenHeight·ratio`, `firstOffset = −topAdd·screenHeight` — ίδιος κανόνας attachment με το box → render ≡ box). Καλύπτει και τα rotated / `widthFactor` paths + per-line decorations.
2. **`bim/text/text-box.ts`** — `effectiveTextWidth`/`glyphInkOf`/`baseTextAdvanceWorld` μετρούν τη **widest γραμμή** (όχι το `\n`-joined string)· `emVerticalRatios`/`visualVerticalRatios` επεκτείνουν top/bottom κατά τα `extents`. Το resize inverse (`textVisualExtentRatio`/`textBoxToPosition`) μένει consistent (το line count είναι σταθερό στο drag → recover του nominal `height`/`widthFactor` αμετάβλητο).
3. **`bim-3d/converters/dxf-text-3d.ts`** — το CanvasTexture στοιβάζει τις γραμμές (width = max γραμμή, block height = `fontPx + (L−1)·advance`)· anchor = το **ήδη multi-line-aware** `resolveTextEmBox` centre → 2Δ ≡ 3Δ.

**Εκτός scope (σκόπιμα):** word-wrap εντός MTEXT `width` frame (χωριστό feature· το `layoutTextNode` engine υπάρχει γι' αυτό). Εδώ καλύπτονται τα **hard `\n` breaks** (Enter) που αναφέρθηκαν.

**Tests:** ΝΕΟ `bim/text/__tests__/text-box-multiline.test.ts` (split edge cases· line-spacing factor· attachment T/M/B distribution· box height=Σ γραμμών + width=max· em box· resize inverse round-trip σε rotated 3-line). Default stub (ink==em) → όλα τα προϋπάρχοντα geometry tests αμετάβλητα. **63 GREEN** σε `bim/text/__tests__` + **11 GREEN** (3D-text + ghost-preview-text) = zero regression. 🔴 εκκρεμεί browser-verify.

---

## Boy-Scout / γνωστά εκκρεμή
- `text-engine/interaction/TextGripGeometry.ts` + `TextGripHandler.ts` = ασύνδετο παλιό σύστημα (4 γωνίες+move+rotation+mirror, δική του math). **Μερικώς αντικαθιστούμενο** — να αποσυρθεί όταν επιβεβαιωθεί zero-usage.
- ✅ **ΛΥΘΗΚΕ (Φ-attachment):** `TextRenderer.hitTest` + `getEntityBBox` ενοποιήθηκαν στο attachment-aware `text-box.ts` SSoT (τέλος της down/lower-left/±h ασυμφωνίας· hover frame πλέον ορατό στο 2Δ).

---

## Changelog
- **2026-07-07 (browser-verify #4 — ΤΟ κυανό ήταν clearance dims, ΟΧΙ AutoAlign· ρίζα = `getEntityBounds` χωρίς `mtext` case· ΔΙΟΡΘΩΣΗ του #3)** — Το draw-order fix του #3 **ΑΠΕΤΥΧΕ** στο browser (Giorgio): MTEXT εξακολουθούσε να μη δείχνει κυανό. **Νέα διάγνωση με runtime διαγνωστικά (`[MTDIAG]` στο whole-move branch του `paintGripActionAlignmentTraces`):** το published `path.origin` **ΚΑΙ** το projected screen ήταν **πεπερασμένα + on-screen** και για TEXT και για MTEXT (`activePaths:1`, `dx/dy` σταθερή γωνία → ποτέ NaN)· `globalAlpha:1`, `composite:'source-over'` (κανένα leaked ctx-state). Ένα προσωρινό **κόκκινο marker** με την ΙΔΙΑ γεωμετρία φάνηκε **και στα δύο** → το paint των AutoAlign traces φτάνει κανονικά στην οθόνη ΚΑΙ για MTEXT. **Κρίσιμη διαπίστωση:** το `paintActionAlignmentTracking` **δεν παίρνει entity** → αδύνατο να διαφέρει ανά τύπο· άρα το «κυανό» που λείπει **ΔΕΝ είναι** οι AutoAlign traces. **Παλέτα (`overlay-line-style.ts`):** `alignment = #CCCCCC` (**γκρι**), `listeningDim = #29B6F6` (**κυανό**). Το ορατό «κυανό» = οι **neighbor-clearance/listening dims** (`paintGripMoveClearanceDims` → `resolveMoveClearanceDims` → `resolveEntityFootprintForDims`), όχι τα (γκρι) alignment traces — όλο το #1/#2/#3 investigation κυνηγούσε **λάθος overlay**. **ΡΙΖΑ:** `resolveEntityFootprintForDims` → generic fallback `getEntityBounds` (`systems/zoom/utils/bounds-entity.ts`) που έχει `case 'text'` (single-line char-count bbox) **αλλά ΚΑΝΕΝΑ `case 'mtext'`** → `null` → `undefined` footprint → `resolveMoveClearanceDims` = `null` → **καμία κυανή clearance dim για MTEXT**. TEXT δούλευε λόγω του `case 'text'`. **FIX (1 αρχείο κώδικα, FULL SSoT):** `resolveEntityFootprintForDims` — branch για `isTextEntity || isMTextEntity` που επιστρέφει το **attachment/rotation/MULTI-LINE-aware** VISUAL box (`textBoxCornersWorld`, ΤΟ ΙΔΙΟ box SSoT με grips/hover/hitTest, γρ.360), με height fallback ίδιο με το hit-test (`Bounds.calculateTextBounds`: `height→fontSize→2.5`). Καλύπτει **και TEXT** (ακριβές glyph box αντί crude char-count) **και MTEXT** + περιστροφή + πολλαπλές γραμμές — μηδέν νέα γεωμετρία. **Finite-guard** (SSoT `isFinitePoint`): position-less / degenerate text → fall-through στο generic fallback (καμία NaN corner στο clearance aggregate). **+4 jest** στο `entity-footprint-for-dims.test.ts` (TEXT box· MTEXT box — ΤΟ regression· χωρίς-position → undefined)· **19 GREEN** (footprint 9 + arc + neighbor-clearance 10). ⚠️ Το draw-order reorder του #3 (`useGripGhostPreview.ts`) **παραμένει** ως legit βελτίωση (traces πάνω από το opaque ghost) — απλώς δεν ήταν αυτό το bug. ✅ **BROWSER-VERIFIED (Giorgio 2026-07-07): Alt+drag MTEXT → κυανές clearance dims εμφανίζονται, όπως TEXT.** **Open follow-up (εκτός scope):** `paintAlignmentPaths` hardcoded `#CCCCCC` (§6 handoff).
- **2026-07-07 (browser-verify #3 — MTEXT whole-move: κυανά AutoAlign ίχνη αόρατα· draw-order fix, 3-agent deep-dive)** [⚠️ ΑΝΑΘΕΩΡΗΘΗΚΕ από #4 — το «κυανό» ήταν clearance dims, ΟΧΙ AutoAlign traces· το draw-order δεν ήταν η αιτία] — Giorgio: με Alt+drag, κείμενο **μίας γραμμής (TEXT)** δείχνει κυανά, **πολλαπλών γραμμών (MTEXT)** ΟΧΙ. **Runtime diagnostics (πολλοί γύροι):** το resolve είναι ΣΩΣΤΟ — το published `GripAlignmentTrackingStore` δίνει **HIT** και το paint καλείται με **ΠΑΝΟΜΟΙΟΤΥΠΑ** δεδομένα (`activePaths.length===1`) και για τα δύο· άρα η ρίζα ήταν **RENDERING/draw-order**, όχι resolve. **Deep-dive με 3 παράλληλους Explore agents (ΟΜΟΦΩΝΟ):** το `paintGripActionAlignmentTraces` ζωγραφιζόταν στο `useGripGhostPreview.ts:~350` **ΠΡΙΝ** το moving-ghost body block, που ζωγραφίζει το κινούμενο κείμενο **ΠΛΗΡΩΣ OPAQUE** (`drawRealEntityPreview`→`bimPreview.render(model, {})`→`BaseEntityRenderer.setupStyle` `globalAlpha=OPAQUE`· άδειο `{}`) στο **ΙΔΙΟ** canvas → σκέπαζε τα ίχνη. ΟΛΑ τα άλλα post-ghost overlays (HUD/clearance/gradient) ζωγραφίζονται σκόπιμα **ΜΕΤΑ** το ghost· τα alignment traces ήταν η μόνη εξαίρεση. Ο move-anchor = `frame.center` (`text-grips.ts:133`)· για MTEXT στη μέση του **ψηλού multi-line stack** → οι opaque γλυφές σκέπαζαν τα ίχνη· TEXT (μικρό footprint) → ξέφευγαν. **FIX (1 αρχείο, καθαρό reorder):** το block `paintGripActionAlignmentTraces` μετακινήθηκε **ΜΕΤΑ** το ghost body block (`useGripGhostPreview.ts`), δίπλα στα «paint LAST» HUD/clearance/gradient → τα ίχνη ζωγραφίζονται πλέον **πάνω** από το opaque ghost, για ΚΑΘΕ τύπο. Μηδέν αλλαγή paint/logic/perf (καμία νέα subscription — συμβατό με ADR-040· CHECK 6B/6D → staged αυτό το ADR). Αφαιρέθηκαν όλα τα temp `[MLDIAG*]` probes. **243 jest GREEN.** 🔴 εκκρεμεί browser-verify (Alt+drag MTEXT → κυανά ορατά όπως TEXT/γραμμή). **Open follow-ups (εκτός scope):** `paintAlignmentPaths` (tracking-paint.ts) αγνοεί το tracking palette (hardcoded `#CCCCCC`) + λείπει `Number.isFinite` guard στο origin/dir (silent-invisible protection).
- **2026-07-07 (browser-verify #2 — drag-and-drop κειμένου: ΚΑΝΕΝΑ κυανό/AutoAlign/Polar ίχνος· entity-agnostic base-point AutoAlign σε ΟΛΟΥΣ τους movers)** — Giorgio (μετά το προηγούμενο fix): μετακινώντας κείμενο (μονής/πολλαπλής γραμμής) με drag-and-drop, **δεν άναβαν** ούτε κυανές ενδείξεις γειτόνων ούτε ίχνη ευθυγράμμισης AutoAlign/Polar — ενώ στη **γραμμή** ανάβουν. **Ρίζα (SSoT audit + trace event→arm→dp→draw→commit):** το `text-move` είναι **hot-grip 'move'** (`HOT_GRIP_OP_REGISTRY`), όχι plain-drag. Το `applyGripDragAlignmentTracking` (το brain που κάνει snap+publish των AutoAlign ιχνών στο grip-drag) κάλυπτε ΜΟΝΟ: dimension grips, **Alt-move** (`isActiveGripAltMove()`), ή **LINE** grips (`isLineEntity`). Η γραμμή δείχνει ίχνη γιατί το midpoint/MOVE-cross της περνά από το **line branch** (`getLineGripAlignmentAnchors`→base). Το **text-move** (hot-grip, μη-γραμμή, χωρίς Alt, **χωρίς `movesEntity` στο `ActiveDragGripInfo`**) έπεφτε στο `clearGripAlignmentTracking()` → μηδέν snap → μηδέν ίχνος (και το τοπικό `paintGripActionAlignmentTraces` δεν κούμπωνε αξιόπιστα αφού ο cursor δεν γινόταν snap). **FIX (4 αρχεία, ΕΝΑ base-point brain, μηδέν διπλότυπο):** (α) `GripDragStore.ts` — νέο πεδίο `ActiveDragGripInfo.movesEntity`· (β) `grip-mouse-handlers.ts` — δημοσίευση `movesEntity: nearGrip.movesEntity === true` **και** στο hot-grip enter **και** στο plain-drag `setActiveDragGrip` (ο base point έρχεται μετά, `setActiveDragGripAnchor` στο 2ο κλικ)· (γ) `grip-drag-alignment-tracking.ts` — η base-point συνθήκη γίνεται `(isActiveGripAltMove() || dimGrip.movesEntity === true) && dragAnchor` → **ΚΑΘΕ** whole-entity move (text/mtext/column/group/line-MOVE) τρέχει το ΙΔΙΟ `resolveBasePointTracking([anchor])` (κοινό με body-drag)· (δ) `mouse-handler-up.ts` — συμμετρικό base-point branch στο commit πριν το line branch → **preview ≡ commit** (το κείμενο κουμπώνει όπου έδειξε το ίχνος). Entity-agnostic — line MOVE grips παίρνουν το ίδιο `[dragAnchor]` (μηδέν regression)· line ENDPOINT (movesEntity:false) κρατά τα fixed-endpoint anchors. **+ΝΕΟ** `grip-drag-move-alignment-tracking.test.ts` (text-move base-point track· «πριν το base pick» no-track· non-mover no-track). **201 GREEN** (cursor 57 + grips 144). 🔴 εκκρεμεί browser-verify (drag-and-drop κειμένου → κυανά+AutoAlign+Polar όπως η γραμμή).
- **2026-07-07 (text/mtext Alt-move — ΙΔΙΑ πορτοκαλί POLAR ακτίνα με τη γραμμή· ΕΝΑ entity-agnostic SSoT)** — Giorgio: επιλέγω **γραμμή** → κλικ σε **λαβή** → **Alt** → drag (whole-entity «move-from-point») → εμφανίζονται **και τα τρία**: κυανά ενδείξεις γειτόνων, ίχνη AutoAlign, ίχνη Polar. **Θέλω ΑΚΡΙΒΩΣ το ίδιο κάνοντας Alt+drag κείμενο (TEXT/MTEXT).** **SSoT audit + πλήρης ιχνηλάτηση pipeline (event→arm→dp→draw→commit):** (1) τα text grips περνούν στο **ίδιο** unified system (`computeDxfEntityGrips` case `'text'`→`getTextGrips`, καλύπτει TEXT+MTEXT)· (2) το arm (`grip-mouse-handlers`) + το dp (`resolveDxfDragPreview`→`buildDxfDragPreview` altMove branch → `{movesEntity:true, anchorPos}`) είναι **entity-agnostic**· (3) ο draw callback φτάνει στο `paintGripActionAlignmentTraces` για κείμενο (`normalizePreviewEntity` δεν απορρίπτει text· `applyEntityPreview` §251/271 επιστρέφει μετακινημένο text)· (4) τα **κυανά + AutoAlign + Polar-increment paths** έρχονται ΟΛΑ από το **ΕΝΑ** `resolveActionAlignmentTracking` (entity-agnostic — ίδιο SSoT με 2-click MOVE + body-drag) → **ήδη ανάβουν** στο κείμενο. **Το μόνο line-gated στοιχείο = η πορτοκαλί POLAR ακτίνα** (`resolveEndpointReshapePolarLock`), gated σε ΕΝΑ σημείο (`resolveEndpointReshapeAnchor` επιστρέφει null για ό,τι δεν είναι line/polyline-άκρο ή BIM-footprint). **FIX (1 αρχείο κώδικα, ~7 γρ.):** στο `resolveEndpointReshapePolarLock` (`hooks/grips/grip-endpoint-polar-lock.ts`), όταν δεν υπάρχει reshape anchor **ΚΑΙ** ο drag είναι whole-entity Alt-move (`isActiveGripAltMove()`), η polar περιστρέφεται γύρω από το **σημείο βάσης** (`anchorPos`) → entity-agnostic ακτίνα για TEXT/MTEXT/**οποιαδήποτε** οντότητα, ίδιος `resolveOrthoPolarStep`+`paintPolarTrackingLine` SSoT. **Μηδέν αλλαγή στα δύο seams:** `useGripGhostPreview` (223-228/360-366) **και** `grip-mouseup-handler` (77-88/207) καλούν **ήδη** αυτόν τον resolver → **preview ≡ commit** αυτόματα. **Μηδέν regression:** line/polyline **άκρο** alt-move κρατά far-end origin (`fixed` non-null → fallback skip)· non-alt reshape → null (καμία ακτίνα)· BIM footprint αμετάβλητο. **MTEXT/multi-line:** base-point polar είναι geometry-free (`anchorPos`+cursor) → πανομοιότυπο. Cross-ref ADR-357/513 (polar SSoT), ADR-560/562 (alignment SSoT), ADR-363/560 (Alt move-from-point). **+ΝΕΟ** `grip-endpoint-polar-lock-whole-move.test.ts` (text/mtext base-point lock· non-alt→null· line-endpoint far-end guard· polar-off/not-snapped → **6 GREEN**). 🔴 εκκρεμεί browser-verify (Alt+drag κείμενο → πορτοκαλί ακτίνα+γωνία, όπως η γραμμή).
- **2026-07-07 (hover HIT-zone ≠ ζωγραφισμένο πλαίσιο — ο hover δεν ακολουθούσε τον κέρσορα)** — Giorgio: κάνοντας hover σε κείμενο, **το φωτεινό περίγραμμα δεν αντιστοιχούσε με την κίνηση του ποντικιού** — από κάποιες πλευρές άναβε **πριν** ο κέρσορας φτάσει στα γράμματα, από άλλες ο κέρσορας είχε **ήδη μπει** και δεν άναβε. **Ρίζα (trace ολόκληρου του hover pipeline `HitTestingService → HitTester.hitTestPoint → performDetailedHitTest → hitTestText`):** το ζωγραφισμένο πλαίσιο (`TextRenderer.renderHoverFrame` → `textBoxCornersWorld` → `resolveTextBox`, attachment-aware VISUAL box) και το **hit-test** χρησιμοποιούσαν **ΔΙΑΦΟΡΕΤΙΚΑ box**: το `hitTestText` έφτιαχνε δικό του **hardcoded** κουτί (monospace πλάτος `len·h·0.6`, baseline-bottom-left `position.x→+width` / `position.y-height→position.y`) που αγνοούσε attachment / `widthFactor` / πραγματικό glyph advance / cap-height ink → οι δύο περιοχές δεν συνέπιπταν (ασύμμετρη διαφορά σε κάθε πλευρά). Δευτερεύον: ο **spatial-index EntityModel** (`hit-test-entity-model.ts` case `'text'`) κρατούσε μόνο `position/text/height/rotation` — έχανε `textStyle/widthFactor/width`, οπότε ακόμα κι αν το hit-test καλούσε το `resolveTextBox` θα υπολόγιζε διαφορετικό κουτί από τον renderer. **FIX (2 αρχεία, ΕΝΑ box SSoT):** (α) `hit-test-annotations.ts` `hitTestText` → test κατά του `resolveTextBox` + `projectToLocalFrame` (mirror `TextRenderer.hitTest`· το `tolerance` είναι ήδη σε world units, προστίθεται απευθείας στο half-extent)· αφαιρέθηκαν τα πλέον αχρησιμοποίητα imports (`degToRad`, `TEXT_METRICS_RATIOS`). (β) `hit-test-entity-model.ts` case `'text'` → μεταφέρει `textStyle/widthFactor/width` (καθρέφτης του render converter `dxf-renderer-entity-model.ts`) ώστε το hit-box να είναι **πανομοιότυπο** με το ζωγραφισμένο frame. **+ΝΕΟ** `hit-test-text-box-parity.test.ts` (grid parity hit-test≡`resolveTextBox`, rotation). **Browser-verify #1 (Giorgio):** μονογραμμικά **OK** — φως κουμπώνει ακριβώς στα γράμματα· **αλλά πολυγραμμικά ΔΕΝ φωτίζονταν** (ο κέρσορας πάνω από τις κάτω γραμμές δεν ανιχνευόταν). **Ρίζα #2 (broad phase):** το spatial-index bbox `Bounds.ts calculateTextBounds` υπολόγιζε **ύψος μίας γραμμής** (`estimatedHeight = textHeight`, monospace width, baseline-top-left) → οι γραμμές 2..N έπεφταν **ΕΞΩ** από το bbox → το entity δεν επιστρεφόταν καν ως candidate → το narrow-phase `hitTestText` δεν έτρεχε ποτέ. **FIX #2 (1 αρχείο):** `calculateTextBounds` → `textBoxAABB` (το ήδη multi-line/attachment/rotation-aware em-box AABB SSoT, γενναιόδωρο superset του VISUAL box) → το broad phase περικλείει **κάθε** γραμμή· αφαιρέθηκαν τα πλέον αχρησιμοποίητα `resolveEntityText`/`TEXT_METRICS_RATIOS` imports. Το parity test επεκτάθηκε με **multi-line case** (broad-phase bounds περικλείουν όλες τις γωνίες + narrow-phase hit πάνω από την τελευταία γραμμή). **100 GREEN** (hitTesting + bim/text). 🔴 εκκρεμεί browser-verify #2 (πολυγραμμικά → φως σε όλες τις γραμμές).
- **2026-07-07 (multi-line follow-up — MTEXT box να ΑΓΚΑΛΙΑΖΕΙ το κείμενο, όχι το overlay frame)** — Browser-verify (Giorgio): το πολυγραμμικό δούλεψε, αλλά οι **λαβές + το hover πλαίσιο ήταν ~5–6× πιο φαρδιά** από τα γράμματα. **Ρίζα:** MTEXT (εργαλείο πολυγραμμικού) κουβαλά `width` = πλάτος του **overlay επεξεργασίας** (~40% καμβά, ADR-344)· το `effectiveTextWidth` επέστρεφε αυτό το frame για κάθε MTEXT (+ μηδέν side-bearing inset) → box = column frame, όχι glyphs. Επειδή grips/hover/hitTest/3D διαβάζουν το ΙΔΙΟ box SSoT, και τα δύο βγήκαν φαρδιά (μία αιτία). **Απόφαση (Giorgio):** hug. **FIX (2 αρχεία):** (α) `text-box.ts` — `effectiveTextWidth(MTEXT)` = **`min(frame, contentAdvance)`** (widest γραμμή)· ΝΕΟ `isTextBoxFrameConstrained` (frame `<` content → text wraps, frame wins· αλλιώς hug)· το `horizontalInkFractions` εφαρμόζει side-bearings στο hug. (β) `text-grips.ts` `framePatch` — resize γράφει `width` **ΜΟΝΟ** σε frame-constrained MTEXT· αλλιώς `widthFactor` (όπως TEXT) → το hug ακολουθεί το drag **χωρίς jump/snap-back** (corner + E/W). Αφαιρέθηκε το πλέον αχρησιμοποίητο `isMTextBox`. Όταν μπει word-wrap, το `min` δίνει φυσικά το frame — μηδέν regression. **+5 tests** (wide-frame hug, narrow-frame constrained, E-resize hold)· 3 προϋπάρχοντα MTEXT tests επικαιροποιήθηκαν στη νέα συμπεριφορά. **83 GREEN**. 🔴 εκκρεμεί browser-verify (λαβές/hover κουμπώνουν στα γράμματα).
- **2026-07-07 (multi-line — Enter → πραγματική αλλαγή γραμμής, τέλος στα «tofu» □)** — Giorgio: πολυγραμμικό κείμενο (Enter στη φόρμα) εμφανιζόταν **σε μία σειρά** με **□** στα σημεία των Enter. **Ρίζα (SSoT audit):** το flat `text` κουβαλά ήδη `\n` (paragraphs `join('\n')`), αλλά renderer/box/3D περνούσαν ΟΛΟΚΛΗΡΗ τη `\n`-string στον glyph shaper → notdef «□» + κατάρρευση σε ένα baseline (το `layoutTextNode` engine τροφοδοτεί μόνο το `TextSnapProvider`, όχι το render). **FIX — ΕΝΑ split/line-metrics SSoT, 3 consumers:** ΝΕΟ `bim/text/text-lines.ts` (`splitTextLines` + `resolveLineSpacingRatio` = **υπάρχον** `CHARACTER_METRICS.LINE_HEIGHT_RATIO` 1.2 × node factor, καμία νέα σταθερά + `resolveMultilineExtents` κατανομή extra ύψους ανά attachment: T→κάτω/B→πάνω/M→συμμετρικά). (1) `TextRenderer.renderTextContent`→ΝΕΟ `paintTextLines` (κάθε γραμμή σε `firstOffset + i·advance`, ίδιος κανόνας attachment με το box· καλύπτει rotated/`widthFactor`/decorations). (2) `text-box.ts` — width=**widest** γραμμή (`effectiveTextWidth`/`glyphInkOf`/`baseTextAdvanceWorld`), height=Σ γραμμών (`em`/`visualVerticalRatios` + extents)· resize inverse consistent (line count σταθερό στο drag). (3) `dxf-text-3d.ts` — CanvasTexture στοιβάζει γραμμές, anchor το multi-line-aware `resolveTextEmBox` → 2Δ≡3Δ. Grips/hover/hitTest/culling **δωρεάν** από το box SSoT. **Εκτός scope:** word-wrap εντός MTEXT `width` (χωριστό feature). ΝΕΟ `text-box-multiline.test.ts` (**63 GREEN** bim/text + **11 GREEN** 3D/ghost, zero regression). 🔴 εκκρεμεί browser-verify (Enter → πολλαπλές γραμμές· πλαίσιο/λαβές/3D καλύπτουν ΟΛΕΣ).
- **2026-07-07 (body-drag MOVE ghost — αόρατο «φάντασμα» κειμένου κατά τη μετακίνηση)** — Giorgio repro: επιλέγω κείμενο (λαβές ορατές) → κλικ **μέσα στο σώμα** → drag+drop· κατά τη μεταφορά **δεν εμφανιζόταν το φάντασμα** του κειμένου (για γραμμές/BIM εμφανίζεται). **Ρίζα (trace ολόκληρου του body-drag pipeline):** το body-drag οπλίζεται σωστά για κείμενα (`EntityBodyDragStore.arm` — καμία text-specific εξαίρεση) και το `makeTranslationPreview` παράγει preview `{ movesEntity:true }` **ΧΩΡΙΣ** `textGripKind`. Το text branch στο `apply-entity-preview.ts` όμως ήταν gated **μόνο** σε `textGripKind`, οπότε το body-drag έπεφτε στο generic `movesEntity` path (`calculateMovedGeometry`) που ενημερώνει **μόνο** το `position` — αφήνοντας τα flat `text`/`height` undefined για in-app κείμενα (περιεχόμενο ζει στο `textNode`). Έτσι ο render EntityModel (`buildEntityModelFromDxf` case `'text'`, διαβάζει raw `te.text`) → `TextRenderer.render` early-return (`if (!text) return`) → **τίποτα δεν ζωγραφιζόταν**. (Grip-drag δούλευε γιατί το `textGripKind` branch έκανε ήδη `projectSceneTextToDxf`.) **FIX (1 αρχείο, ίδιο proven SSoT pattern):** το text branch (`apply-entity-preview.ts`) καλύπτει πλέον `textGripKind || movesEntity`· για whole-entity move (body-drag / Move tool, χωρίς grip kind) κάνει `projectSceneTextToDxf` + inject flat `text`/`height`/`textStyle` + `position += delta`. Έτσι το φάντασμα ζωγραφίζεται πανομοιότυπα με το grip-drag (preview ≡ commit). Καλύπτει text **και** mtext. **+3 jest** (`apply-entity-preview-text.test.ts` — textNode-only TEXT + flat TEXT + textNode-only MTEXT body-drag· σύνολο 9· **40 GREEN** σε όλο το `rendering/ghost/__tests__`). 🔴 εκκρεμεί browser-verify (κλικ μέσα σε κείμενο → drag → φάντασμα ορατό).
- **2026-07-07 (ΠΛΗΡΗΣ rotation/move/alignment parity με την ΚΟΛΟΝΑ — hot-grip opt-in)** — Το κείμενο είχε ήδη κοινό `rect-grip-engine` (resize) + σωστά glyph icons, αλλά η **μετακίνηση/περιστροφή** δεν είχαν την εμπειρία της κολόνας (ορισμός pivot με κλικ, πράσινα/κόκκινα βελάκια, λευκές+POLAR γραμμές, pivot ⊙, κυανά ίχνη). **Ρίζα (SSoT audit + 3 traces):** το hot-grip σύστημα (ADR-397, «opt-in by adding rows, never fork the FSM») + όλα τα overlays (`useGripGhostPreview` → `paintDirectionArc`/`paintRotationTracking`/`paintGripActionAlignmentTraces`) + το `BimRotateHotGripStore` είναι **entity-agnostic**, gated μόνο σε `phase==='hotGrip'`/`movesEntity`/`rotatePivot` — ΟΧΙ σε entity type. Το κείμενο απλώς **δεν είχε καταχωρηθεί**. **FIX (6 additive, μηδέν νέα μηχανή):** (A) `wall-hot-grip-fsm.ts` — `hotGripKindOf` διαβάζει `textGripKind` + `HOT_GRIP_OP_REGISTRY` παίρνει `'text-move':'move'`, `'text-rotation':'rotate'`· (B) `move-glyph-frame.ts` — branch `text`/`mtext` (top-level `rotation` → local axes) ώστε το centre-move grip να πάρει `moveGlyphFrame` (4-βέλο directional· **bonus:** διορθώνει και το deterministic rotation reference baseline μέσω `resolveRotateReferenceAnchor`)· (C) `grip-projections.ts` — `buildRotateReferencePreview` προωθεί `textGripKind` στο rotate-free ghost· (D) `apply-entity-preview.ts` — το text branch περνά `pivot: rotatePivot` στο `applyTextGripDrag` (preview ≡ commit)· (E) `grip-parametric-text-commits.ts` — `commitTextGripDrag` διαβάζει `BimRotateHotGripStore` (picked pivot + reference anchor· mirror `commitColumnGripDrag`)· (F) `ambient-alignment-source.ts` — text/mtext insertion point ως πηγή ευθυγράμμισης (O(1), ADR-040 perf· `boundsFromPoints` fallback για <3-vert members) ώστε γειτονικό κείμενο να ανάβει κυανές γραμμές. **Έρχονται δωρεάν (entity-agnostic):** pivot ⊙, πράσινο/κόκκινο τόξο, λευκές+POLAR στην περιστροφή, grip temperature, «R» 6-click reference, typed-angle Δαχτυλίδι Εντολών, κυανά base-point ίχνη. **Εκτός:** `setActiveDragGrip.gripKind` forward (το hot-grip branch ήδη περνά `hotGripKindOf`· περιττό για text) — αποφυγή ρίσκου σε shared αρχείο. **164 jest GREEN** (5 touched suites 109 + 12 consumer suites 55). 🔴 εκκρεμεί browser-verify.
- **2026-07-07 (render↔hover grip mismatch μετά resize — `widthFactor` dropped στον render EntityModel)** — Μετά από resize, οι λαβές **ζωγραφίζονταν** σωστά αλλά το **hover δεν φώτιζε** τις δεξιές/κεντρικές λαβές + τα σημάδια move/rotation (Giorgio: «αριστερές OK, κέντρο+δεξιά+σημάδια χάνονται μετά την πρώτη αλλαγή»). **Διάγνωση (temp `[GRIP-PARITY]` render + `[GRIP-REGISTRY]` interaction, τέλεια συσχέτιση):** το `ratioW` render/hover = **ΑΚΡΙΒΩΣ** το `widthFactor` του hover entity. **ΔΥΟ converters:** ο hover path διαβάζει το raw scene entity (`layer,lineWidth,widthFactor`) → εφαρμόζει το `widthFactor` που έγραψε το resize → στενό (σωστό)· ο render path περνά από `canvas-v2/dxf-canvas/dxf-renderer-entity-model.ts` (`layerId,lineType,lineweight`) που **δεν** μετέφερε `widthFactor`/`width` → πλατύ → οι δύο διίστανται (αγκυρωμένα αριστερά, στενό δεξιά). **FIX:** ο render EntityModel converter (`buildEntityModel` case `'text'`) μεταφέρει πλέον `widthFactor` + `width` (mirror του `dxf-text-entity-converter`) → ζωγραφισμένες λαβές/κείμενο = hit-test. **+** `dxf-text-entity-converter` καθρεφτίζει τον `isMTextBox` discriminator (width-less MTEXT κουβαλά `widthFactor`, §6 open item). Temp διαγνωστικά αφαιρέθηκαν. **665+ jest GREEN.** 🔴 εκκρεμεί browser-verify.
- **2026-07-07 (Φ-attachment ΟΡΙΖΟΝΤΙΟ — glyph ink side-bearing inset, «επεκτείνεται προς τα έξω»)** — Μετά το κάθετο fix, browser-verify: πάνω/κάτω κουμπώνει τέλεια αλλά **δεξιά/αριστερά το box επεκτεινόταν έξω** από τα γράμματα (glyph side bearings — το πλάτος ήταν το pen **advance**, όχι το ink). **FIX:** το vertical-metrics SSoT γενικεύτηκε σε **`measureTextGlyphInk`** (full glyph ink box H+V: font metrics + ink extent + `inkLeft`/`inkRight`/`advance`, μία λήψη `getBoundingBox()`). Το VISUAL box inset-άρει το advance box κατά τα side bearings (`horizontalInkFractions`)· ΝΕΟ `textVisualWidthRatio` (= 1 − bearings) οδηγεί το resize inverse (`framePatch.widthFactor = newVisualWidth / (baseAdvance · widthRatio)` → box holds, no jump). ZERO inset για MTEXT/no-font (byte-identical). Consumers/tests αμετάβλητα (default stub: ink spans full advance → visual==advance). ΝΕΟ `text-box-horizontal.test.ts` (side-bearing stub → box hugs left+right, E-resize round-trip). Stub επεκτάθηκε (`inkLeftEm`/`inkRightEm`). **665 jest GREEN.** 🔴 εκκρεμεί browser-verify.
- **2026-07-07 (Φ-attachment ΚΑΘΕΤΟ — visual glyph-ink box, «μεγάλο κενό πάνω»)** — Το box ύψος χρησιμοποιούσε το ονομαστικό em, ενώ ο renderer ζωγραφίζει τα caps στο ~0.71·em πάνω στο baseline (baseline τοποθετημένο κατά το **font ascent** ~0.905·em) → το πλαίσιο/λαβές ~0.19·em ψηλότερα, μεγάλο κενό πάνω (μετρημένο με temp `[VBOX-DIAG]`: για «ΤΕΣΤ» Liberation Sans, box top **93 units** πάνω από το cap-top, inkDescent=0). **FIX (real metrics, όπως Revit/Figma):** ΝΕΟ SSoT `text-engine/fonts/text-vertical-metrics.ts` `measureTextVerticalRatios` (font ascent/descent για baseline anchor + glyph **INK** bbox `getPath().getBoundingBox()` για extent· nominal fallback από `TEXT_METRICS_RATIOS`, +ΝΕΟ `CAP_HEIGHT_RATIO=0.7`· ο flaky CSS `actualBoundingBox*` tier παραλείφθηκε σκόπιμα — μετρήθηκε `-17` στη μηχανή Giorgio). **«Visual bounds vs edit box» split:** `resolveTextBox` = VISUAL cap/ink box (2D grips/hover/hitTest ≡ γράμματα)· ΝΕΟ `resolveTextEmBox` = nominal em box (3D plane + culling, μηδέν 3D regression — αλλιώς το 3D κείμενο μετακινούνταν ~53 units). Resize inverse μέσω `textVisualExtentRatio` (visual→nominal· `framePatch` διαιρεί το box height, ώστε το resize να ΜΗΝ σπάσει — box holds, no jump). Consumers repointed: `dxf-text-3d`→`resolveTextEmBox`, `dxf-entity-outline`→`textEmBoxCornersWorld`, `textBoxAABB`→em. ΝΕΑ tests: `text-vertical-metrics.test.ts` + `text-box-vertical.test.ts` (cap stub 0.7/0 → box αγκαλιάζει caps + resize round-trip no-jump). Stub επεκτάθηκε (`getBoundingBox`· default ink=metrics → VISUAL≡em → **όλα τα προϋπάρχοντα geometry tests πράσινα αμετάβλητα**). **628 jest GREEN.** 🔴 εκκρεμεί browser-verify.
- **2026-07-07 (TEMP diagnostics αφαιρέθηκαν)** — Αφαιρέθηκαν ΟΛΑ τα προσωρινά διαγνωστικά (`[TEXTBOX-DIAG]`/`logTextBoxDiag`/throttle map στον `TextRenderer`, `[GRIP-HIT-DIAG]` στον `grip-mouse-handlers` +orphaned `UnifiedGripInfo` import, `[TEXT-COMMIT-DIAG]`/`[TEXT-COMMIT-AFTER]` στον `grip-parametric-text-commits`, το νέο `[VBOX-DIAG]`) πριν το commit. Grep `TEMP-DIAG|*-DIAG|logTextBoxDiag` → 0 hits.
- **2026-07-06/07 (Φ-attachment ΟΡΙΖΟΝΤΙΟ — real glyph advance + durable height + projection SSoT)** — (α) Το πλάτος του box χρησιμοποιούσε monospace προσέγγιση (`len·h·0.6`) ενώ ο renderer ζωγραφίζει με πραγματικό proportional advance → box off οριζόντια. FIX: ΝΕΟ `text-engine/fonts/text-advance.ts` `measureTextAdvanceWorld` (3-tier: opentype `getGlyphRun`→CSS `measureText`→monospace)· `effectiveTextWidth`+`baseTextAdvanceWorld` το χρησιμοποιούν· `framePatch` inverse μέσω `baseTextAdvanceWorld` (deltaW=0 verified). (β) **Durable resize height:** το ύψος ζει στο `textNode.runs[].style.height` (`resolveTextHeight` το διαβάζει πρώτο) → flat `height` write σκιαζόταν same-tick· FIX `scaleTextNodeRunHeights` (`utils/text-node-utils.ts`) γράφει scaled textNode + `UpdateTextTransformCommand.textNode`. (γ) **Projection SSoT:** ΝΕΟ `bim/text/project-scene-text.ts` `projectSceneTextToDxf` (scene→DxfText, preview≡commit)· ο ghost (`apply-entity-preview.ts`) inject-άρει flat text/height ώστε ο `TextRenderer` να μη κάνει early-return. ΝΕΑ tests `text-advance.test.ts` + `_stub-font.ts` (deterministic 0.6 ratio) + `text-node-utils.test.ts`.
- **2026-07-06 (TEMP diagnostic — box-vs-glyph offset instrumentation)** — Προσωρινό (`REMOVE BEFORE COMMIT`) διαγνωστικό logging στον `TextRenderer.render` (`logTextBoxDiag` + throttle map, 1 log/400ms/entity) που μετρά την οριζόντια απόκλιση ανάμεσα στο **geometry box** (grips/hover/hit-test, `resolveTextBox` → monospace προσέγγιση) και τα **πραγματικά ζωγραφισμένα glyphs** (real font metrics `getGlyphRun`, else `ctx.measureText`) — για να εντοπιστεί γιατί οι λαβές/hover frame κάθονται μετατοπισμένα σε σχέση με το κείμενο (ADR-557 Φ-attachment follow-up). **Καθαρά instrumentation**: `console.log('[TEXTBOX-DIAG]', …)` πριν το `ctx.restore()`, καμία αλλαγή στη ζωγραφική/geometry/commit. Committed κατόπιν ρητής εντολής Giorgio (2026-07-06) ώστε να διατηρηθεί το working tree καθαρό· να **αφαιρεθεί** πριν το production polish του box-vs-glyph fix. Co-staged: ADR-557 (CHECK 6D). 🟡 TEMP.
- **2026-07-06 (MTEXT ghost regression re-fix — triage)** — Το `apply-entity-preview.ts:237` guard είχε **αναιρεθεί** από `'text'||'mtext'` πίσω σε μόνο `'text'` (commit `0878ed54`, 30/6) με σχόλιο-αιτιολογία «MTEXT normalised to 'text' at scene→Dxf conversion» → **λάθος γι' αυτό το pipeline**: το mtext→text normalize (`dxf-text-entity-converter.ts:49`) συμβαίνει ΜΟΝΟ στο render/hit-test pipeline (`dxf-scene-entity-converter`)· το ghost-preview παίρνει το **raw scene entity** (`useGripGhostPreview.getEntity`) με `type==='mtext'`, και το `normalizePreviewEntity` μαπάρει μόνο `lwpolyline→polyline`. Αποτέλεσμα: το ζωντανό ghost χανόταν ξανά σε MTEXT grip-drag (regression του ba33b0c2). **FIX:** επαναφορά `entity.type === 'text' || entity.type === 'mtext'` + διόρθωση του παραπλανητικού σχολίου. Το `apply-entity-preview-text.test.ts` (regression guard) ήταν pre-existing failing → τώρα GREEN (move + MTEXT corner resize width 800→860). Triage 16 pre-existing tests.
- **2026-06-30 (Φ-attachment, drag-response fix)** — «το κείμενο δεν ανταποκρίνεται στις λαβές»: το live ghost (`applyEntityPreview`) αγνοούσε MTEXT (έλεγχε μόνο `type==='text'`, ενώ το commit δεχόταν `'mtext'`) → καμία ζωντανή απόκριση· + το box έχανε το attachment στο drag (scene entity → `textNode.attachment`, commit δεν το περνούσε). FIX: ghost δέχεται `'text'|'mtext'`· `resolveTextBox` διαβάζει `textNode.attachment` → `textStyle` → TL· commit `projectSceneTextToDxf` κουβαλά `textStyle` (`extractFirstRunStyle`). ΝΕΟ `apply-entity-preview-text.test.ts`. 87 jest GREEN.
- **2026-06-30 (Φ-attachment)** — Attachment-aware text-box SSoT. ΝΕΟ `bim/text/text-box.ts` (`resolveTextBox`/`textBoxToPosition`/`textBoxCornersWorld`/`textBoxAABB`) reuse `offsetForJustification` (ADR-344). Ενοποίησε 4 ασύμφωνες box πηγές (grips/3D mesh/hitTest/getEntityBBox) σε ΕΝΑ· νέο 2D hover frame· grip box + 3D mesh + hitTest + cull + 3D outline όλα attachment-aware → λαβές=κείμενο=hover frame, 2Δ≡3Δ. 🔴 εκκρεμεί browser-verify (BR/TL/MC/rotation) + commit (Giorgio).
- **2026-06-30** — Slices 1-5 υλοποιήθηκαν (UNCOMMITTED). Browser-verified: 10 λαβές σωστά τοποθετημένες 2D + 3D (Giorgio). Renumber ADR-551→**557** (collision με census ADR στο shared tree). Εκκρεμεί commit (Giorgio).
