# ADR-401 — BIM Wall Top/Base Constraints + Attach-to-Structural (associative auto-height)

- **Status**: 🟢 ACCEPTED — **Phase A + B1 + B2 + B3a + B3b + B3c + C + D + E2 DONE** (A: 2026-05-30 resolver SSoT + τύποι + Zod· B1: 2026-05-31 host-plan builder SSoT + 2D section full-profile· B2: 2026-05-31 stepped/sloped 3D solid + syncWalls wiring + tests· B3a: 2026-05-31 profile-aware `computeWallGeometry` area/volume/bbox + BOQ feed· **B3b: 2026-05-31 ETICS Z1 ΠΛΗΡΕΣ ΣΚΑΛΩΤΟ ΚΕΛΥΦΟΣ (per-edge variable top 3D + profile-aware BOQ area + `buildWallHostInputs` SSoT extract)· B3c: 2026-05-31 2D cut-state = no-op confirmed + doc**· **C: 2026-05-31 associative attach via fresh-recompute (αναθεωρημένο scope μετά code-audit) + detach-on-host-delete warning**· **D: 2026-05-31 auto-attach UX — Z-gated detection + undoable batch command**· **E2: 2026-05-31 κεκλιμένη στέγη/πλάκα host adapter — slope-plane SSoT + sloped underside· **E.1: 2026-05-31 ribbon manual attach/detach top+base — pick-host tool (`useWallAttachTool`) + γενικό `DetachWallsCommand`**· **E.3: 2026-05-31 wall top/base vertical grip 3D — split του axis-Y resize σε δύο octahedra (πάνω=height, κάτω=baseOffset) + detach-on-drag SSoT `detachWallSide`**· **E.2: 2026-05-31 ribbon numeric input `topElevationEnd` (κεκλιμένη δοκός) στο contextual beam tab**· **E.4: 2026-05-31 manual-edit-breaks-attach — ribbon/panel height/baseOffset edit ενώ attached → detach affected side (νέο pure `detachSidesAffectedByVerticalEdit`, reuse `detachWallSide`)**· **F.1: 2026-05-31 column attach core engine (params+Zod+per-corner profile resolver SSoT)**· **F.2: 2026-05-31 column consumers — 3D per-corner prism + profile-aware BOQ + 2D no-op + ETICS out-of-scope**). Phase F.3 (column auto-attach+ribbon+3D grip+edit-break) εκκρεμεί.
- **Date**: 2026-05-30
- **Author**: Giorgio Pagonis + Claude (Opus 4.8)
- **Scope**: BIM `wall` (primary) — `column` mirror σε επόμενη φάση. Καταναλώνει `beam` + `slab` ως «structural hosts».
- **Builds on**: ADR-369 (Elevation Convention — datum), ADR-363 §5.4 (derived-geometry central cascade), ADR-399 (Levels/Floors SSoT)
- **Related**: ADR-396 (ETICS — assembly/ύψη τοίχου), ADR-358 (Stair↔Floor), ADR-366 (3D Viewer)
- **Impact**: 🟡 ADDITIVE σε τύπους· 🔴 αλλάζει το **πού** ορίζεται το κατακόρυφο ύψος τοίχου (από static scalar → resolved-from-context). Wipe & Reseed (ADR-369 Q10) — μηδέν data migration.

---

## 1. Context — Το πρόβλημα (από Giorgio)

Οι τοίχοι BIM είναι συνήθως τοίχοι **πλήρωσης**: μπαίνουν αφού στηθεί ο στατικός σκελετός. Ροή που θέλει ο Giorgio:

1. Φτιάχνω τοίχο BIM με προκαθορισμένο ύψος (π.χ. 2,85 μ).
2. Τοποθετώ **πάνω** του δοκάρι (π.χ. 0,50 μ).
3. Ο τοίχος **μικραίνει αυτόματα** ώστε να χωρά το δοκάρι — χωρίς χειροκίνητη επέμβαση.

**Αριθμητικό παράδειγμα** (ADR-369 datum):
```
Ύψος ορόφου (πάτωμα→πάτωμα)         = 3,00 μ   (FFL επόμενου ορόφου = 3,00)
Πλάκα οροφής (πάχος)                = 0,15 μ   → κάτω παρειά πλάκας = 2,85
Δοκάρι κάτω από πλάκα (ύψος)        = 0,50 μ   → κάτω παρειά δοκαριού = 2,50
─────────────────────────────────────────────
Τελικό ύψος τοίχου (κάτω από δοκάρι) = 2,50 μ   (αντί 2,85 χωρίς δοκάρι)
```

### 1.1 Γιατί ΟΧΙ «στατική αφαίρεση ενός αριθμού»

Το «αφαιρώ 0,50 μία φορά» είναι **εύθραυστο**: σπάει αν μετακινηθεί/σβηστεί το δοκάρι, αν αλλάξει το ύψος ορόφου, αν υπάρχουν 2 δοκάρια, αν το δοκάρι καλύπτει μέρος του τοίχου. Η σωστή λύση είναι **συσχετιστική** (associative), όπως το Revit «Attach Top/Base».

### 1.2 Industry research — Revit «Attach Top/Base»

- Μετά την τοποθέτηση, η κορυφή/βάση τοίχου **«κολλάει»** (attach) σε άλλο στοιχείο στο ίδιο κατακόρυφο επίπεδο: **πλάκα, στέγη, ταβάνι, δοκάρι** (πάνω/κάτω).
- Παραμετρική σχέση: το ύψος **μεγαλώνει/μικραίνει αυτόματα** ώστε να ακολουθεί το όριο του στοιχείου. Αλλαγή κλίσης/υψομέτρου του host → ο τοίχος ακολουθεί ζωντανά.
- Σε δοκάρι → ο τοίχος κολλάει στην **κάτω παρειά** του δοκαριού.
- Στο Revit το attach είναι **χειροκίνητο** (pick wall + pick element). Edit profile → **σπάει** το attach.
- 🟢 **Δική μας βελτίωση**: auto-attach κατά την τοποθέτηση του δοκαριού (εντοπισμός τοίχων από κάτω + attach με undo).

**Πηγές**: Autodesk «Attach Walls to Other Elements» (Revit 2020), Revit Structure «Attaching Structural Objects Together», Autodesk User's Guide «Attaching Walls».

### 1.3 Current State (ο κώδικας που τρέχει — code wins)

| Τι | Πού | Κατάσταση |
|----|-----|-----------|
| **Datum ορόφου** | ADR-369 (✅ IMPLEMENTED) | `Floor.elevation` = **πάνω επιφάνεια πλάκας** = FFL (πάτωμα→πάτωμα). Πλάκα/δοκάρι κρέμονται προς τα κάτω. **ΑΠΟΦΑΣΙΣΜΕΝΟ.** |
| **Wall vertical binding** | `bim/types/bim-binding.ts`, `wall-types.ts` | Υπάρχουν `baseBinding` (`storey-floor`\|`absolute`), `topBinding` (`storey-ceiling`\|`absolute`\|`unconnected`), `baseOffset`/`topOffset`/`unconnectedHeight`. |
| **Πραγματικό ύψος γεωμετρίας** | `bim/geometry/wall-geometry.ts:117`, `bim-3d/converters/wall-opening-extrude.ts:64` | ⚠️ Χρησιμοποιούν **απευθείας `params.height`**. Τα bindings είναι **σχεδόν διακοσμητικά** — δεν παράγουν resolved height. |
| **Vertical-extent resolver (μερικός)** | `bim-3d/2d-section/section-intersect.ts:96` `toWallPlan()` | Μόνο εδώ resolved-από-binding (baseY/topY). Κοιτάζει `unconnected`/`absolute`, αλλιώς `baseY + height`. **De-facto SSoT υποψήφιος προς γενίκευση.** |
| **Floor-height cascade** | `src/app/api/floors/floor-height-cascade.service.ts` | Όταν αλλάζει `floor.height` → recompute `params.height` για `topBinding='storey-ceiling'` τοίχους/κολώνες. Τύπος: `floor.height·1000 + topOffset − baseOffset`. ⚠️ **Αγνοεί πλάκα/δοκάρι** (δίνει 3000, όχι 2850). Server batch + EntityAudit. |
| **Hosted-opening cascade** | ADR-363 §5.4 `cascadeHostedOpeningsForWalls` | Pattern «το παιδί ακολουθεί τον γονιό σε ΟΛΑ τα transform paths». **Mirror για attach.** |
| **Plan-overlap utils** | `bim/geometry/shared/polygon-utils.ts` (`polygonIntersectionAreaMm2`, `clipPolygonBySH`) | Reuse για «ποιοι τοίχοι είναι κάτω από το δοκάρι/πλάκα». |
| **Beam→slab deduction** | `bim/geometry/slab-geometry.ts` + test | Πρότυπο plan-intersection + depth clamp — ίδια μαθηματικά με coverage detection. |

**Συμπέρασμα Phase 1 (Recognition)**: η υποδομή υπάρχει αλλά **ημιτελής** — τα top/base bindings ΔΕΝ καταναλώνονται από τη γεωμετρία, και δεν υπάρχει καθόλου «attach σε συγκεκριμένο structural element». Το ADR-369 §9 Q5 το είχε προβλέψει ως «auto-stretch cascade» αλλά υλοποιήθηκε μόνο floor-driven (storey-ceiling), όχι element-driven.

---

## 2. Decision

**Συσχετιστικά (associative) Top/Base Constraints για τοίχους BIM, με δυνατότητα «Attach to Structural» σε δοκάρι/πλάκα, ευθυγραμμισμένα με Revit + το datum του ADR-369.**

### 2.1 Δύο επίπεδα δέσμευσης (constraint)

1. **Storey-level constraints** (ήδη υπάρχουν, ενεργοποιούνται σωστά):
   - `baseBinding`/`topBinding` με σημασία ADR-369. Το `topBinding='storey-ceiling'` διορθώνεται ώστε να **αφαιρεί την πλάκα οροφής** (top = επόμενο FFL − πάχος πλάκας), όχι σκέτο floor-to-floor.
2. **Element-level attach** (νέο — η καρδιά του ADR):
   - Νέα τιμή `topBinding = 'attached'` + νέο πεδίο `attachTopToId?: string` (FK → beam ή slab entity).
   - Το πραγματικό ύψος **υπολογίζεται ζωντανά** = κάτω παρειά του host − βάση τοίχου. **Δεν αποθηκεύεται** ως `height`.
   - Mirror για βάση: `baseBinding='attached'` + `attachBaseToId?` (π.χ. πάνω σε δοκό θεμελίωσης) — Phase 2.

### 2.2 SSoT — `resolveWallTopProfile(wall, ctx)` (νέο, centralized) — FULL ENTERPRISE

ΕΝΑΣ pure resolver, **μοναδική** πηγή για το κατακόρυφο εύρος τοίχου. Επειδή επιλέξαμε FULL (όχι single-scalar height), ο resolver επιστρέφει **προφίλ κορυφής** κατά μήκος του άξονα του τοίχου — υποστηρίζει **σκαλωτή** (πολλαπλά δοκάρια/μερική κάλυψη) και **κεκλιμένη** (κεκλιμένο δοκάρι/στέγη) κορυφή. Καταναλώνεται από **ΟΛΑ** τα paths (3D extrude, section, BOQ, grips, dimensions, 2D annotation):

```ts
interface WallVerticalContext {
  readonly floorElevationMm: number;            // FFL του ορόφου του τοίχου (ADR-369)
  readonly nextFloorElevationMm?: number;       // FFL επόμενου ορόφου (storey-ceiling)
  readonly ceilingSlabThicknessMm?: number;     // πάχος πλάκας οροφής (storey-ceiling − slab)
  /** Επιστρέφει την κάτω-παρειά ενός host ως συνάρτηση κατά μήκος άξονα (mm)
   *  + plan-overlap span [t0,t1] (0..1) πάνω στον τοίχο. null = δεν βρέθηκε. */
  readonly resolveHost?: (id: string) => HostUndersidePlan | null;
  /** Όλοι οι structural hosts του ορόφου — για auto-detect κάλυψης κατά μήκος. */
  readonly candidateHosts?: readonly HostUndersidePlan[];
}
/** Γραμμικό προφίλ κάτω-παρειάς host προβαλλόμενο στον άξονα τοίχου. */
interface HostUndersidePlan {
  readonly hostId: string;
  readonly hostType: 'beam' | 'slab' | 'roof' | 'wall';
  readonly t0: number; readonly t1: number;     // span κάλυψης (0..1) πάνω στον άξονα
  readonly z0mm: number; readonly z1mm: number;  // underside στα t0, t1 (γραμμικό → κεκλιμένο)
}
/** Ένα τμήμα κορυφής: από t0 έως t1, top πηγαίνει γραμμικά z0→z1. */
interface WallTopSegment {
  readonly t0: number; readonly t1: number;
  readonly z0mm: number; readonly z1mm: number;
  readonly source: 'attached' | 'storey-ceiling' | 'absolute' | 'unconnected' | 'fallback';
  readonly hostId?: string;
}
interface WallTopProfile {
  readonly baseZmm: number;
  readonly segments: readonly WallTopSegment[];  // ordered, καλύπτουν [0,1] χωρίς κενά
  readonly maxTopZmm: number;                    // για bbox
  readonly hasAttach: boolean;
  readonly missingHostIds: readonly string[];    // hosts που λείπουν → fallback + warning
}
```

**Κανόνας σύνθεσης (lower-envelope)**: για κάθε σημείο `t` του άξονα, η κορυφή = το **χαμηλότερο** όριο μεταξύ όλων των hosts που καλύπτουν το `t` + του storey-ceiling. Όπου δεν καλύπτει κανείς host → storey-ceiling (ή nominal fallback). Έτσι βγαίνει αυτόματα και η σκαλωτή (διαφορετικά δοκάρια) και η μερική κάλυψη (το ακάλυπτο μέρος μένει ψηλά).

- `params.height` υποβιβάζεται σε **nominal/unattached fallback** (όπως το Revit «Unconnected Height»): ισχύει μόνο όπου δεν υπάρχει host ούτε storey-ceiling. Host λείπει (σβήστηκε) → segment πέφτει σε fallback + `missingHostIds`.
- Το υπάρχον `toWallPlan()` (`section-intersect.ts`) γίνεται **consumer** του resolver (παράγει το single-segment ή stepped προφίλ), όχι παράλληλη λογική. Single host οριζόντιος → ένα segment με z0=z1 (back-compat με ίσιο τοίχο).

### 2.3 Host underside formula (ADR-369 datum)

```
beam.underside(t)  = beam.topElevation(t) − beam.depth      (✅ Phase E (β) — tilted beam, topElevationEnd → z0≠z1)
slab.underside     = slab.levelElevation + heightOffset − slab.thickness
roof.underside(t)  = slab.underside + slope(pt(t))   (✅ Phase E2 — tilted slab/roof, z0≠z1)
wall.top(t)        = min{ host.underside(t) : host καλύπτει t } ⊓ storeyCeiling   (lower envelope)
beam.topside(t)    = beam.topElevation(t) + zOffset                  (✅ (γ) base-attach — άνω παρειά)
slab.topside       = slab.levelElevation + heightOffset             (✅ (γ) — άνω παρειά πλάκας/θεμελίου)
wall.base(t)       = max{ host.topside(t) : host καλύπτει t }                     (✅ (γ) UPPER envelope)
```

### 2.4 Γεωμετρία στερεού (stepped/sloped prism) — 3D + section

Το στερεό του τοίχου παύει να είναι απλό box-extrude. Γίνεται **prism με επίπεδη βάση και πολυγραμμική/κεκλιμένη κορυφή** που ακολουθεί το `WallTopProfile`: ανά segment, η κορυφή είναι quad/τραπέζιο (z0→z1). 3D: build top faces per segment (mirror `wall-opening-extrude` piece-wise). Section/2D height annotation: ίδιο προφίλ. BOQ volume: Σ (segment area × thickness), όχι `length×height`.

### 2.5 Attach UX — auto + manual (FULL Revit parity + βελτίωση)

**Auto-attach** κατά το **commit τοποθέτησης δοκαριού/πλάκας**:
1. Plan-overlap detection (reuse `polygonIntersectionAreaMm2`) → ποιοι τοίχοι του ίδιου ορόφου περνούν από κάτω (+ span t0..t1).
2. Για κάθε τέτοιον τοίχο **με `topBinding='storey-ceiling'`** (default «ελεύθερη» κορυφή): καταχωρείται attach στον host. Τοίχοι ήδη `absolute`/`unconnected`/manual → **δεν** πειράζονται.
3. **Undoable** ενιαία command (`AttachWallsTopCommand`) — ένα undo αναιρεί όλο το auto-attach.

**Manual attach/detach** (Revit parity, ✅ **Phase E.1 DONE 2026-05-31**): contextual wall ribbon panel «Σύνδεση δομικού» με 4 κουμπιά. **Attach** («Σύνδεση Κορυφής/Βάσης») → ενεργοποιεί pick-host tool (`wall-attach-top`/`wall-attach-base` ToolType· `useWallAttachTool` snapshot-άρει τους επιλεγμένους τοίχους στην ενεργοποίηση, μετά click στο δοκάρι/πλάκα = host) → `AttachWalls{Top|Base}Command` (ΥΠΗΡΧΑΝ). Host pick = unit-safe μέσω `getHoveredEntity()` (HoverStore SSoT) + mm-space geometry fallback. **Detach** («Αποσύνδεση Κορυφής/Βάσης») → bridge action σε ΟΛΟΥΣ τους επιλεγμένους τοίχους → νέο γενικό `DetachWallsCommand(side)` (επαναφορά `storey-ceiling`/`storey-floor` + clear `attach{Top|Base}ToIds`, ένα undo). Πολλαπλά attach σε διαφορετικά spans → σκαλωτό προφίλ (§2.2). FULL: ο τοίχος κρατά **λίστα** `attachTopToIds: string[]` (όχι μόνο ένα) ώστε να δένει σε πολλά δοκάρια ταυτόχρονα.

### 2.6 Associative cascade (mirror ADR-363 §5.4) — FULL SSoT

ΕΝΑ SSoT `cascadeStructuralAttachForHosts(hostIds, scene)` καλείται σε **ΟΛΑ** τα transform paths του host (move/rotate/scale/grip/depth-edit/delete) **και** του τοίχου (ώστε να ξαναϋπολογιστεί η κάλυψη/προφίλ αν μετακινηθεί ο τοίχος). Επιστρέφει τους τοίχους με αλλαγμένο προφίλ για re-render/persist. Client (live command) + server batch (mirror `floor-height-cascade.service.ts`) με EntityAudit. Idempotent· belt-and-suspenders no-op όταν μηδέν επηρεαζόμενοι.

### 2.7 Base-attach (Phase γ) — μεταβλητή βάση + ETICS dual-band

Καθρέφτης του top-attach για τη **βάση** του τοίχου, όταν `baseBinding='attached'` (ο τοίχος «κατεβαίνει» σε θεμέλιο/πεδιλοδοκό). Συμπεριφορά = **Revit «Attach Base» — bidirectional/target-driven** (Autodesk «increases OR decreases to conform»): η βάση πάει στην **άνω-παρειά** (topside) host(s), πάνω **ή** κάτω από το nominal.

- **SSoT engine** `bim/geometry/wall-base-profile.ts` — `resolveWallBaseProfile` + `evaluateWallBaseAt`. **Upper-envelope** (`highestAt` = MAX, αντίστροφο του top `lowestAt` = MIN): πολλά hosts → ο τοίχος πατά στο **ΨΗΛΟΤΕΡΟ** στήριγμα (δεν διαπερνά κανένα). Ακάλυπτο span → nominal base ρητά (ΟΧΙ baseline-στο-envelope, αφού bidirectional).
- **Παράδειγμα** (mirror §1, αντεστραμμένα): nominal βάση τοίχου = 0,00 (FFL)· πεδιλοδοκός με άνω-παρειά στο −0,40 → η βάση **κατεβαίνει** στο −0,40 (ο τοίχος μεγαλώνει 0,40 προς τα κάτω, πατά στο θεμέλιο). Δύο θεμέλια διαφορετικού βάθους → σκαλωτή βάση (πατά στο ψηλότερο ανά span).
- **Consumers (γ2 — όλα ζωντανά):** 3D κάτω-έδρα (`zBotAM/zBotBM` per-boundary, jamb/ποδιά ακολουθούν τη βάση, **πρέκι ΟΧΙ** — μένει floor-relative)· 2D τομή (`wallSection` yMin@cut)· BOQ (ύψος/area/volume = top − base· η αρνητική βάση **μεγαλώνει** το εμβαδό).
- **ETICS dual-band (γ3 — §B3b sibling):** `bim/geometry/envelope-wall-base.ts` `resolveEnvelopeEdgeBases` (per-edge μεταβλητή βάση, **ΧΩΡΙΣ** clamp στο 0 — η βάση μένει αρνητική) + `chainBaseAreaM2`. Το Z1 κέλυφος ντύνει ΚΑΙ το κατέβασμα του τοίχου στο θεμέλιο: `EnvelopeToThree` band builder γενικεύτηκε σε **dual-band** (top profile **και** bottom profile, split στο union των breakpoints, interior-biased). BOQ Z1 area = `chainProfileAreaM2 − chainBaseAreaM2`.
- **Auto-attach (γ1):** `findWallsToAutoAttachBaseToHost` — **inverted Z-gate** (host topside **κάτω** από τη βάση = θεμέλιο, αντίθετο του top) + `AttachWallsBaseCommand` (batch undoable, mirror του top).

---

## 3. Datum Convention (ΑΠΟΦΑΣΙΣΜΕΝΟ — ADR-369, εδώ μόνο επανάληψη)

`Floor.elevation` = **πάνω επιφάνεια τελειωμένης πλάκας (FFL)** = το πάτωμα που πατάς. Ύψος ορόφου = πάτωμα→πάτωμα. Πλάκα & δοκάρι κρέμονται προς τα κάτω. Top-of-structural = `FFL − finishThickness` (default 80mm). **Επιλογή Α — δεν ξανανοίγει.**

---

## 4. SSoT & επαναχρησιμοποίηση (no duplicates)

| Ανάγκη | Υπάρχον SSoT προς reuse |
|--------|-------------------------|
| Datum/elevation | `bim/utils/bim-floor-utils.ts` `getEntityAbsoluteElevation`, ADR-369 |
| Vertical-extent | επέκταση `section-intersect.ts toWallPlan` → νέο `resolveWallTopProfile` (stepped/sloped) |
| Coverage detection | `shared/polygon-utils.ts` `polygonIntersectionAreaMm2`, `clipPolygonBySH` |
| Cascade pattern | ADR-363 §5.4 `cascadeHostedOpeningsForWalls` (client) + `floor-height-cascade.service.ts` (server) |
| Floors SSoT | `useFloorsByBuilding` (FLOORS doc), ADR-399 |
| Audit | `EntityAuditService.recordChange` (ADR-195) |

---

## 5. Φάσεις υλοποίησης (FULL — όλες εντός scope)

> **Re-scope 2026-05-30 (υλοποίηση):** Το Phase A περιοριστηκε στον **πυρήνα**
> (resolver + τύποι + Zod + section consumer + tests) ώστε να είναι αυτοτελές,
> testable και low-risk (κανόνας «phase per session»). Η ενσωμάτωση στο **3D
> extrude / bbox / BOQ** μετακινήθηκε στο Phase B (απαιτεί το stepped-solid),
> γιατί χρειάζονται variable-height geometry, όχι μόνο τον resolver.

- **Phase A — Resolver SSoT ✅ DONE (2026-05-30)**: NEW `bim/geometry/wall-top-profile.ts` — `resolveWallTopProfile` (stepped/sloped lower-envelope) + `resolveWallBaseZmm` + `resolveWallNominalTopZmm` (shared nominal scalar) + types (`HostUndersidePlan`/`WallVerticalContext`/`WallTopSegment`/`WallTopProfile`). `topBinding='attached'` + `attachTopToIds: string[]` στους τύπους (`bim-binding.ts`, `wall-types.ts`) + Zod (`wall.schemas.ts`, attach refinement). `section-intersect.toWallPlan` → consumer του shared nominal resolver (μηδέν behavior change· `storey-ceiling` αφαίρεση πλάκας ενεργοποιείται όταν δοθεί `nextFloorElevationMm`/`ceilingSlabThicknessMm` context). Tests: `wall-top-profile.test.ts` (παράδειγμα 3.00→2.50, σκαλωτή, μερική κάλυψη, κεκλιμένη + τομή baseline, missing-host).
- **Phase B — Stepped/sloped solid + downstream** (σπασμένο σε B1/B2/B3, «phase per session»):
  - **B1 — Host-plan builder + 2D section ✅ DONE (2026-05-31)**: NEW `bim/geometry/wall-host-plan-builder.ts` (SSoT) — `buildHostUndersidePlans` / `makeResolveHost` / `makeWallTopContext` (plan-overlap = **segment ∩ polygon** t-intervals, robust convex+concave μέσω midpoint-inside ray-cast reuse `isPointInPolygon`) + `beamHostInput`/`slabHostInput` adapters (§2.3 underside formulas σε mm). NEW `evaluateWallTopAt(profile,t)` SSoT helper στο `wall-top-profile.ts`. **2D section full-profile**: `section-intersect.toWallPlan` δέχεται `resolveHost` + carry-άρει `WallTopProfile`· `wallSection` αποτιμά το προφίλ στο σημείο της εγκάρσιας τομής (single-point top, σκαλωτή κορυφή)· `section-scene-sync` χτίζει per-wall `resolveHost` από beams+slabs. Beam/slab = οριζόντια (z0=z1)· concave multi-span host → largest-span (Phase E refinement). 37/37 tests (18 builder + 14 resolver + 5 wallSection). tsc clean.
  - **B2 — Stepped/sloped 3D solid ✅ DONE (2026-05-31)**: host-plan wiring στο `BimSceneLayer.syncWalls` (beams+slabs → `makeWallTopContext`/`resolveWallTopProfile`, μόνο όταν υπάρχει attached τοίχος) + piece-wise prism σε **3 converter paths**. NEW `wall-piece-geometry.ts` (`buildSlopedWallPieceGeometry` — custom 8-vertex wedge για κεκλιμένη κορυφή). `WallOpeningPiece.zTopM` → `zTopAM`/`zTopBM` (per-boundary top → flat ⇒ ίσα, sloped ⇒ διαφορετικά). `computeWallOpeningPieces(wall, openings, wallTop?)` — jambs/πρέκια ακολουθούν το προφίλ + **split στα profile breakpoints** (interior-biased eval για σκαλωτό/ασυνεχές προφίλ)· ποδιά=επίπεδη. `BimToThreeConverter.wallToMesh(..., profile?)` + `makeWallTopLocalFn` (mm→local m) + flat=`ExtrudeGeometry`/sloped=`buildSlopedWallPieceGeometry` routing· straight piece-path ενεργό ΚΑΙ χωρίς ανοίγματα όταν `hasAttach`. `wall-opening-extrude` (curved/polyline): top ακμή front-face = polyline προφίλ (stepped+sloped ενιαία). **Flat τοίχος = αμετάβλητο fast path (μηδέν regression).** 61/61 tests (16 stepped-solid + back-compat). bbox → B3 (ο `computeWallGeometry` δεν δέχεται resolver context).
  - **B3a — BOQ profile-aware geometry ✅ DONE (2026-05-31)**: `computeWallGeometry(params, kind, openings?, profile?)` — νέα optional `profile?: WallTopProfile` → gross area = **Σ(segment length × avg segment height)** (νέος helper `profileGrossAreaM2`, σκαλωτή+κεκλιμένη) + bbox top = `maxTopZmm − baseZmm` αντί nominal `params.height`. Flat/χωρίς profile = byte-for-byte fast path. `wall-boq-feed.ts`: νέος `resolveAttachedWallProfile(entity, scene)` (beams+slabs → `beamHostInput`/`slabHostInput`/`makeWallTopContext`/`resolveWallTopProfile`, `floorElevationMm=0` mirror του section path) → `wallBoqEntity` recompute όταν `attached || openings`. 6 νέα tests (attached/stepped/sloped/back-compat/openings/bbox baseOffset) → 81/81 regression PASS, tsc clean. **Persisted bbox σε όλα τα transform paths = Phase C cascade** (ο resolver context δεν φτάνει στα `add-wall-to-scene`/`UpdateWallParamsCommand`).
  - **B3b — ETICS Z1 ✅ DONE (2026-05-31, ΠΛΗΡΕΣ ΣΚΑΛΩΤΟ ΚΕΛΥΦΟΣ)**: per-edge μεταβλητή κορυφή. **Mapping perimeter-edge → wall-profile:** το `EnvelopeChain` += `edgeWallIds?: (string|null)[]` (ευθυγραμμισμένο 1:1 με `envelopeFaceEdges`)· `envelope-shell.buildChain`/`orphanWrap` το γεμίζουν από `ShellEdge.sourceEntityId` (το legacy `computeEnvelopePerimeter` το αφήνει undefined → flat fallback). NEW SSoT `envelope-wall-top.ts`: `resolveEnvelopeEdgeTops` (προβολή face-endpoints στον άξονα τοίχου → `tA,tB`· split στα profile breakpoints → **ανεξάρτητα sub-segments** ανά ακμή, interior-biased segment-eval → σκαλοπάτι = καθαρή ασυνέχεια) + `chainProfileAreaM2` (Σ μήκος×μέσο ύψος, mirror `profileGrossAreaM2`) + `projectTOnAxis`. `EnvelopeToThree.envelopeChainToMesh(..., edgeTops?)` + `addVariableTopBand` (clip sub-segments στο span· flat→`ExtrudeGeometry`, sloped→**reuse `buildSlopedWallPieceGeometry`** wedge· sill/head ανέπαφα). `BimSceneLayer.addEnvelopeShell` χτίζει per-attached-wall `WallTopRef` (`buildEnvelopeWallTopRefs`) + `resolveEnvelopeEdgeTops` ανά chain (μηδέν κόστος όταν κανένας attached). `envelope-boq-sync.computeZ1FacadeArea` profile-aware όταν `walls.some(attached)`, αλλιώς `Σ perimeterM × maxWallHeightM` (ΑΜΕΤΑΒΛΗΤΟ). **SSoT extract** `buildWallHostInputs(beams, slabs)` (4 sites: syncWalls/section-scene-sync/wall-boq-feed/addEnvelopeShell — Boy Scout N.0.2). **Flat/μη-attached = byte-for-byte fast path** (edgeTops undefined → flat heightM). 24 νέα tests (`envelope-wall-top.test.ts` 12 + `buildWallHostInputs` 2 + regression).
  - **B3c — 2D plan cut-state ✅ DONE (2026-05-31, NO-OP confirmed)**: `WallRenderer:189` είναι render leaf (ADR-040 — δεν σκανάρει hosts). Cut plane (~1.2 m) πέφτει πάντα μεταξύ base και attached top (~2.5 m) όσο και του nominal → cut-state = `'cut'` και στις δύο περιπτώσεις = **no-op**. Doc σχόλιο προστέθηκε (cut-state = nominal σκόπιμα). Θα διέφερε μόνο αν δοκάρι κατέβαζε την κορυφή κάτω από το cut plane (extreme edge case → host scan σε leaf· εκτός scope).
- **Phase C — Associative attach**: ✅ DONE (2026-05-31). **Αναθεωρήθηκε μετά από code-audit (code = source of truth):** η οπτική συσχέτιση host→attached-wall ΗΔΗ λειτουργεί μέσω **fresh-recompute** — `BimSceneLayer.syncWalls` (3D), `section-intersect` (2D τομή) και `wall-boq-feed` (BOQ) ξαναχτίζουν το `WallTopProfile` από τη ζωντανή σκηνή σε ΚΑΘΕ resync, και το `use-bim3d-sync` re-runs σε κάθε entity change. Άρα host move/rotate/resize ακολουθείται αυτόματα (Revit/ArchiCAD parity: ο client είναι ο geometry engine, το client persistence με geometry+bbox είναι ο «server»). Ένα persisted `cascadeStructuralAttachForHosts` θα έγραφε γεωμετρία που κανείς δεν διαβάζει (≈ dead code, αντι-SSoT) + ένα server batch δεν έχει trigger (beam/slab μετακινούνται μόνο client-side) → **απορρίφθηκαν**. Υλοποιήθηκε το πραγματικό κενό = **detach-on-host-delete warning**: NEW `bim/walls/wall-structural-attach-coordinator.ts` (`notifyWallsOnHostDeletion`) + `findAttachedWalls` reverse-lookup SSoT στο `bim-cascade-resolver` (mirror του `findHostedOpenings`, beam/slab→wall) + decoupled `bim:wall-attach-host-missing` (EventBus) + thin subscriber `useDxfViewerNotifications` (sonner toast, mounted στο `DxfViewerContent`) + i18n `attachToStructural.hostMissing` (el+en, ns `dxf-viewer-shell`). Wired σε `DeleteEntityCommand` + `DeleteMultipleEntitiesCommand`. **ΟΧΙ** mutation του `attachTopToIds` (ο resolver εξουδετερώνει gracefully τα missing → baseline· κρατώντας το ref, undo της διαγραφής ξανα-attach-άρει αυτόματα). Tests 10/10, tsc clean, regression 42/42.
- **Phase D — Auto-attach UX**: ✅ DONE (2026-05-31). Όταν δημιουργείται structural host (δοκάρι/πλάκα), οι `storey-ceiling` τοίχοι από κάτω κολλάνε αυτόματα την κορυφή τους (Revit auto-attach, με undo). **Detection SSoT** `findWallsToAutoAttachToHost(host, entities)` στο `wall-structural-attach-coordinator.ts`: (1) μόνο `topBinding='storey-ceiling'` (δεν πειράζει attached/unconnected/absolute)· (2) **κάτοψη** = `buildHostUndersidePlans` (ο ΙΔΙΟΣ projector με τον resolver, αντί για ξεχωριστό `polygonIntersectionAreaMm2` — πιο SSoT)· (3) **Z gate** (επιλογή Giorgio): `hostInput.undersideZmm > resolveWallBaseZmm + 1mm` → ο host είναι πάνω από τη βάση του τοίχου (οροφή/δοκάρι), ΟΧΙ η πλάκα-πάτωμα από κάτω. `floorElevationMm=0` (active level σύμβαση, mirror `wall-boq-feed`/`envelope-boq-sync`). **NEW `AttachWallsTopCommand`** (batch, undoable, ΕΝΑ undo entry): patch `topBinding='attached'` + append host στο `attachTopToIds` + recompute geometry/validation + cascade hosted openings (mirror `UpdateWallParamsCommand`). **NEW thin hook `useStructuralAutoAttach`** (listener στο `drawing:entity-created` → detect → `execute(AttachWallsTopCommand)`, mounted στο `DxfViewerContent`) + decoupled `bim:walls-auto-attached` event + info toast (`attachToStructural.autoAttached`, el+en). **Undo σημείωση:** η δημιουργία beam/slab ΔΕΝ είναι command (direct `appendEntityToScene`)· το undo αποκολλάει τους τοίχους και αφήνει τον host (το attach = δικό του undo step, Revit parity). Tests 12/12 (6 detection + 6 command), tsc clean. 🔴 browser verify εκκρεμεί.
- **Phase E2 — Κεκλιμένη στέγη/πλάκα host adapter**: ✅ DONE (2026-05-31). Όταν η πλάκα/στέγη είναι `geometryType='tilted'`, ο attached τοίχος ακολουθεί την **κεκλιμένη** κάτω-παρειά (z0mm ≠ z1mm). **NEW SSoT `bim/geometry/slab-slope.ts`** (`slabUndersideZmmAt`/`slabTopZmmAt`/`slabSlopeOffsetZmm`) — καθιερώνει την κανονική ερμηνεία του `SlabSlope` (single-plane tilt: `direction`° CCW from +X = ανηφόρα, `angle`% = mm/mm, `pivotEdge` = γραμμή που μένει στη nominal στάθμη, default κέντρο AABB). `HostFootprintInput` += optional `undersideZmmAt?: (pt)=>number`· `buildHostUndersidePlans` αποτιμά την παρειά στα **plan-points των άκρων του covered span** (flat host = scalar, byte-for-byte back-compat). `slabHostInput`: tilted → attach `undersideZmmAt` + `hostType='roof'` όταν `kind='roof'`. **Αυτόματη διάχυση**: 3D wedge (B2 `buildSlopedWallPieceGeometry` ήδη έτοιμο), 2D τομή, BOQ ακολουθούν χωρίς αλλαγή — όλα χτίζουν τους hosts μέσω του κοινού `buildWallHostInputs`/`slabHostInput`. Tests: NEW `slab-slope.test.ts` (10) + `wall-host-plan-builder.test.ts` Phase-E2 integration (4) → 31/31 + regression 75/75, tsc clean. **⚠️ Out of scope (flagged):** (α) το ίδιο το tilted slab render-άρει ακόμη επίπεδο box (pre-existing ADR-369 §9 Q7 κενό — το `slab-slope.ts` SSoT είναι έτοιμο να το καταναλώσει)· (β) κεκλιμένο **δοκάρι** (δεν έχει slope param στο `BeamParams`).
- **Phase E.1 — Manual attach/detach ribbon ✅ DONE (2026-05-31)** (Revit parity: «Κόλλησε κορυφή σε…» / «Αποκόλληση», multi-host).
- **Phase E.3 — Wall top/base vertical grip (3D) ✅ DONE (2026-05-31)**: το axis-Y resize του gizmo (ADR-402) έσπασε σε **δύο** grips. **NEW SSoT `bim/walls/wall-attach-detach.ts`** (`detachWallSide` unconditional reset + `isWallSideAttached` guard) — εξήχθη από το `DetachWallsCommand.buildDetachedParams` (όχι διπλότυπο· κοινό από E.1+E.3, έτοιμο για E.4). `gizmo-geometry.ts`: για τον άξονα Y, **δεύτερο** octahedron στο `−RESIZE_HANDLE_OFFSET` (id `resize-m-y`) — το πάνω handle δρομολογεί ΟΛΑ τα hitboxes (κέντρο+γωνίες) στο `resize-y`, το κάτω στο `resize-m-y` (X/Z αμετάβλητα — shared mirror). `bim-gizmo-overlay.RESIZE_HANDLES_BY_TYPE.wall += 'resize-m-y'` (μόνο ο τοίχος· beam/column/slab μένουν single vertical). `bim3d-resize-bridge.computeWallResizeParams` axis-Y branch ανά `mode`: `normal` (TOP)→`height += Δ` (βάση σταθερή)· `mirror` (BASE)→`baseOffset += Δ` & `height −= Δ` (κορυφή σταθερή)· **detach-on-drag** (Giorgio: Revit «edit breaks attach») όταν η πλευρά attached, μέσω `detachWallSide`. Το `mode:'normal'|'mirror'` ρέει ήδη end-to-end (gizmo-types→parseHandleId→handleToConstraint→drag-bridge→`ResizeDragMm`) — μηδέν νέο taxonomy. Tests: NEW `wall-attach-detach.test.ts` (8) + `bim3d-resize-bridge.test.ts` += 7 (top/base/down/up/zero/detach-top/detach-base/non-attached) + `bim-gizmo-overlay.test.ts` += 2 (wall has `resize-m-y`, others don't) → 55/55 + gizmo-hit-test 3/3, tsc clean. 🔴 browser verify εκκρεμεί.
- **Phase E.2 — Ribbon input `topElevationEnd` (κεκλιμένη δοκός) ✅ DONE (2026-05-31)**: το param + η πλήρης διάχυση (3D `applyBeamSlope` / 2D `beamSection` / BOQ / wall-attach `beamHostInput.undersideZmmAt`) ήταν ήδη έτοιμα από Phase E(β) — έλειπε **μόνο** το UI widget. Νέο combobox (mm, free entry, ίδια elevation presets) στο **geometry panel** του contextual beam ribbon, δίπλα στο `topElevation`. Registry: `BEAM_RIBBON_KEYS.params.topElevationEnd` + numeric-key type union + `BEAM_RIBBON_NUMBER_KEYS`. Bridge: `NUMBER_KEY_TO_FIELD += topElevationEnd` (generic numeric path το dispatch-άρει μέσω `UpdateBeamParamsCommand`)· `getComboboxState` fallback — `topElevationEnd` απών (flat δοκός) → δείχνει το `topElevation` ώστε ο χρήστης να βλέπει την flat στάθμη και να την αλλάζει για κλίση. **Pure-mm combobox** (ΟΧΙ `RibbonWallDimensionWidget` → αποφεύγει το `/1000`-vs-`mmScaleFor` latent issue). i18n: `beamEditor.topElevationEnd` + `...EndTooltip` (el+en, ns `dxf-viewer-shell`). Tests: NEW `beam-command-keys.test.ts` (3 — registry consistency) PASS, tsc clean. 🔴 browser verify εκκρεμεί.
- **Phase E.4 — Manual-edit-breaks-attach ✅ DONE (2026-05-31)** (Revit «edit breaks attach» για ribbon/panel numeric edits, mirror του E.3 grip): όταν ένα χειροκίνητο UI patch αλλάζει `height` (top driver) ή `baseOffset` (base driver) ενώ η αντίστοιχη πλευρά είναι `attached` → σπάει πρώτα το binding ώστε η ρητή τιμή να νικά τον host-follow. **NEW pure SSoT `detachSidesAffectedByVerticalEdit(params, patch)` στο `bim/walls/wall-attach-detach.ts`** (reuse `detachWallSide` + `isWallSideAttached` — μηδέν διπλότυπο). Wiring στον **κεντρικό** `useWallParamsDispatcher`/`dispatchWallParamPatch` (ΕΝΑΣ SSoT writer για ΟΛΑ τα ribbon/panel wall edits: length/height/thickness widget + DNA panel) — **όχι** μέσα στο `UpdateWallParamsCommand`, ώστε το cascade/host-follow recompute (που περνά απευθείας από το command, ΟΧΙ από αυτόν τον hook) να **μην** σπάει λάθος το attach. Detach + edit = **ένα** undo step (το `prevParams` του command επαναφέρει και τα δύο). Tests: `wall-attach-detach.test.ts` += 6 (height→top / baseOffset→base / both / unchanged-noop / thickness-noop / non-attached-noop) → 11/11, tsc clean. 🔴 browser verify εκκρεμεί.
- **Phase F — Column mirror** (γενίκευση top/base attach τοίχου → κολώνα· Giorgio 2026-05-31: **σύνθετο profile** + **πλήρες mirror**). Σπασμένο σε F.1/F.2/F.3 («phase per session»):
  - **F.1 — Core engine + params + Zod ✅ DONE (2026-05-31)**: `ColumnParams += attachTopToIds?/attachBaseToIds?: readonly string[]` (`column-types.ts`) + Zod 4 refinements `attached ⇔ ≥1 id` (`column.schemas.ts`, mirror wall). **NEW SSoT `bim/geometry/column-vertical-profile.ts`** — `resolveColumnBaseZmm`/`resolveColumnNominalTopZmm` (scalar, mirror wall) + `resolveColumnTopProfile`/`resolveColumnBaseProfile` (**per-corner** envelope: η κολώνα δεν έχει άξονα → το προφίλ αποτιμάται σε κάθε γωνία του footprint· top=lower-envelope `min{nominal, καλύπτουσες κάτω-παρειές}`, base=upper-envelope bidirectional `max{καλύπτουσες άνω-παρειές}` ⇒ διαφορετικές γωνίες/κεκλιμένα hosts → κεκλιμένη/στρεβλή κορυφή) + `makeColumnHostResolver`. **REUSE** αυτούσια τα `HostFootprintInput` + `beamHostInput`/`slabHostInput`/`buildWallHostInputs` (μηδέν διπλή formula παρειάς) + `isPointInPolygon` (point-in-host coverage). Tests: NEW `column-vertical-profile.test.ts` (19 — scalars/lower-envelope/μερική κάλυψη/κεκλιμένο/cap-nominal/missing/upper-envelope bidirectional/legacy-no-topside). tsc 0 errors.
  - **F.2 — Consumers ✅ DONE (2026-05-31, Opus 4.8)**: η attached κολώνα ψηλώνει/χαμηλώνει/γέρνει πραγματικά. **(1) 3D `columnToMesh`:** NEW SSoT `bim-3d/converters/column-piece-geometry.ts` (`buildColumnPrismGeometry`) — **per-corner N-vertex prism** (κάτω/πάνω δακτύλιος ανά footprint-corner στο `cornerBaseLocalM`/`cornerTopLocalM`, concave-safe καπάκια via `THREE.ShapeUtils.triangulateShape`, quad πλευρές). Γενίκευση του 8-vertex `buildSlopedWallPieceGeometry` σε footprint polygon (η κολώνα δεν έχει άξονα). `columnToMesh(..., topProfile?, baseProfile?)` routing: `hasAttach` → prism, αλλιώς ίσιο `ExtrudeGeometry` fast-path (byte-for-byte). `BimSceneLayer.syncColumns` χτίζει context (`buildWallHostInputs` + `makeColumnHostResolver` + resolvers, guarded — μηδέν geometry access για μη-attached). Units: footprint x,y σε scene-m· Z absolute mm → local m `(z−FFL)·0.001` (mirror `makeWallTopLocalFn`). **(2) BOQ `computeColumnGeometry(params, topProfile?, baseProfile?)`:** profile-aware effective ύψος = `avg(cornerTopZmm) − avg(cornerBaseZmm)` (mirror wall B3a `profileGrossAreaM2`)· volume = area × effHeight· flat = `params.height` fast-path. NEW `hooks/data/column-boq-feed.ts` (`columnBoqEntity`, mirror `wall-boq-feed`) — recompute profile-aware από scene beams+slabs· wired σε `useColumnPersistence` (persist + persistRestore). **(3) 2D `ColumnRenderer` cut-state = NO-OP** (render leaf, cut plane ~1.2m πάντα μεταξύ base & attached top → ίδια cut-state· doc μόνο, mirror WallRenderer B3c). **(4) ETICS = out-of-scope/no-op:** οι κολώνες συνεισφέρουν μόνο το **2D footprint** στο envelope perimeter (`envelope-column-bridge`/`columnExteriorArc`)· το Z-band της θερμοπρόσοψης οδηγείται από τους **τοίχους** (perimeter extrusion), όχι από column-specific profile — η column-attach δεν αλλάζει το ύψος του κελύφους. Tests: NEW `column-piece-geometry.test.ts` (12 — flat/per-corner κεκλιμένο/concave L/εκφυλισμός/guards/winding-normals)· `column-geometry.test.ts` += 5 (profile-aware effective ύψος)· NEW `column-boq-feed.test.ts` (4 — flat/missing-scene/underside/ghost-host). tsc 0 errors (αρχεία μου)· μηδέν regression (scene baseline 25/49 pre-existing test-lag ταυτόσημο με/χωρίς τις αλλαγές).
  - **F.3 — Auto-attach + ribbon + 3D grip + edit-break** (❌ TODO): `findColumnsToAutoAttachToHost`/`...BaseToHost` (coordinator) · `AttachColumns{Top|Base}Command`/`DetachColumnsCommand` · column branch στο `useStructuralAutoAttach` · `useColumnAttachTool` + ToolTypes + `contextual-column-tab` + `useRibbonColumnBridge` · `RESIZE_HANDLES_BY_TYPE.column += 'resize-m-y'` + `computeColumnResizeParams` axis-Y detach-guard · **γενίκευση** `wall-attach-detach.ts` → shared `entity-attach-detach.ts` (Boy Scout N.0.2).

### 5.1 Consumer map (σημεία που διαβάζουν scalar `wall.params.height` — Phase B/C targets)
| Site | Αρχείο | Φάση |
|------|--------|------|
| Geometry SSoT (area/volume/bbox) | `bim/geometry/wall-geometry.ts` (✅ B3a: `profile?` param → `profileGrossAreaM2` + bbox `maxTopZmm−baseZmm`) | B3a✅ |
| BOQ feed (net area/volume) | `hooks/data/wall-boq-feed.ts` (✅ B3a: `resolveAttachedWallProfile` → profile-aware `computeWallGeometry`) | B3a✅ |
| 2D section topY | `bim-3d/2d-section/section-intersect.ts` (✅ B1: full-profile via `resolveHost` + `wallSection` profile-at-cut) | A✅/B1✅ |
| 3D extrude (solid) | `bim-3d/converters/BimToThreeConverter.ts` (✅ B2: `wallToMesh(profile?)` + `makeWallTopLocalFn` + flat/sloped routing) | B2✅ |
| 3D extrude (openings, curved) | `wall-opening-extrude.ts` (✅ B2: front-face top polyline), `wall-opening-pieces.ts` (✅ B2: `zTopAM`/`zTopBM` + `wallTop?` split) | B2✅ |
| 3D wedge (κεκλιμένη κορυφή) | `bim-3d/converters/wall-piece-geometry.ts` (NEW B2: `buildSlopedWallPieceGeometry`) | B2✅ |
| 3D scene wiring | `bim-3d/scene/BimSceneLayer.ts` `syncWalls` (✅ B2: hostInputs + `resolveWallTopProfile`) | B2✅ |
| ETICS Z1 BOQ area | `bim/services/envelope-boq-sync.ts` (✅ B3b: `computeZ1FacadeArea` → `chainProfileAreaM2`· flat = `Σ perimeterM × maxWallHeightM`) | B3b✅ |
| ETICS Z1 3D band | `bim-3d/scene/BimSceneLayer.ts` `addEnvelopeShell` (✅ B3b: `buildEnvelopeWallTopRefs` + `resolveEnvelopeEdgeTops` → `envelopeChainToMesh(edgeTops)`) | B3b✅ |
| ETICS edge-top SSoT | `bim/geometry/envelope-wall-top.ts` (NEW B3b: `resolveEnvelopeEdgeTops` + `chainProfileAreaM2` + `projectTOnAxis`) | B3b✅ |
| ETICS chain provenance | `bim/geometry/envelope-perimeter.ts` (`EnvelopeChain.edgeWallIds?`) + `envelope-shell.ts` (`buildChain`/`orphanWrap` populate) | B3b✅ |
| ETICS 3D variable band | `bim-3d/converters/EnvelopeToThree.ts` (✅ B3b: `addVariableTopBand` + `envelopeChainToMesh(edgeTops?)`, reuse `buildSlopedWallPieceGeometry`) | B3b✅ |
| Host inputs SSoT | `bim/geometry/wall-host-plan-builder.ts` (`buildWallHostInputs` — 4 consumers) | B3b✅ |
| 2D plan cut-state | `bim/renderers/WallRenderer.ts:189` (✅ B3c: no-op confirmed + doc) | B3c✅ |

---

## 6. Edge cases — ΑΠΟΦΑΣΙΣΜΕΝΑ (FULL ENTERPRISE + FULL SSOT, Giorgio 2026-05-30)

| Θέμα | Απόφαση |
|------|---------|
| **Πολλαπλά δοκάρια / μερική κάλυψη** | **Σκαλωτή κορυφή** — ο τοίχος ακολουθεί το **χαμηλότερο όριο** ανά τμήμα (lower-envelope, §2.2). Ακάλυπτο μέρος → storey-ceiling. Όχι single-scalar height. |
| **Trigger** | **Auto + manual** — auto-attach (undoable) κατά την τοποθέτηση host + χειροκίνητο attach/detach ribbon (Revit parity). |
| **Ποια στοιχεία «κολλάνε»** | **Όλα τα valid**: δοκάρι, πλάκα οροφής, στέγη (κεκλιμένη), τοίχος από πάνω. `attachTopToIds` λίστα. |
| **Σβήσιμο host** | Ο τοίχος επιστρέφει αυτόματα στο nominal/storey-ceiling + `missingHostIds` warning flag (associative cascade). |
| **Χειροκίνητη αλλαγή ύψους attached τοίχου** | **Σπάει το attach** → γίνεται `unconnected` με το νέο ύψος (Revit parity). |
| **Κεκλιμένο δοκάρι/στέγη** | **Εντός scope** — γραμμικό underside z0→z1 ανά segment (Phase B/E2). |

---

## 7. Tests (προβλεπόμενα)

- `resolve-wall-top-profile.test.ts` — attached/storey-ceiling(−slab)/absolute/unconnected/fallback· **lower-envelope** σκαλωτό (2 δοκάρια)· μερική κάλυψη· κεκλιμένο (z0≠z1)· παράδειγμα 3,00→2,85→2,50.
- `structural-attach-cascade.test.ts` — host move/scale/depth-edit/delete → wall προφίλ· πολλαπλά δοκάρια· idempotent· detach-on-delete + warning.
- `auto-attach-detection.test.ts` — plan-overlap, μερική κάλυψη (span t0..t1), μόνο storey-ceiling τοίχοι attach.
- `wall-stepped-solid.test.ts` — piece-wise prism: 3D top faces per segment + BOQ volume integration.
- Regression: `wall-geometry`, `wall-opening-extrude`, `section-intersect` με νέο resolver.

---

## 8. Changelog

| Date | Author | Change |
|------|--------|--------|
| 2026-05-31 | Giorgio + Claude (Opus 4.8) | **Phase F.2 — COLUMN ATTACH CONSUMERS IMPLEMENTED** (pending commit, 🔴 browser verify). Η attached κολώνα ψηλώνει/χαμηλώνει/γέρνει πραγματικά σε 3Δ + μετριέται σωστά στο BOQ. **(1) 3D — NEW SSoT `bim-3d/converters/column-piece-geometry.ts` (`buildColumnPrismGeometry`):** per-corner N-vertex prism (κάτω/πάνω δακτύλιος ανά footprint-corner στο `cornerBaseLocalM`/`cornerTopLocalM`· concave-safe καπάκια via `THREE.ShapeUtils.triangulateShape`· quad πλευρές). Γενίκευση του 8-vertex `buildSlopedWallPieceGeometry` σε footprint polygon (η κολώνα δεν έχει άξονα — per-corner shear αντί 1D wedge, ίδιο μοντέλο με `applySlabSlope`/`applyBeamSlope`). **Cap winding gotcha:** ο μετασχηματισμός plan(x,y)→world(x,·,−y) είναι κατοπτρισμός → CCW-plan τρίγωνο στον πάνω δακτύλιο δίνει +Y (κοιτά πάνω), οπότε bottom cap = αντίστροφη φορά. `columnToMesh(..., topProfile?, baseProfile?)`: `hasAttach` → prism, αλλιώς ίσιο `ExtrudeGeometry` fast-path (byte-for-byte μη-attached, μηδέν regression). `BimSceneLayer.syncColumns` χτίζει context (`buildWallHostInputs` + `makeColumnHostResolver` + resolvers, guarded — μηδέν geometry access για μη-attached). Units boundary: footprint x,y σε scene-m· profile Z absolute mm → local m `(z−FFL)·0.001` (mirror `makeWallTopLocalFn`). **(2) BOQ — `computeColumnGeometry(params, topProfile?, baseProfile?)`** profile-aware: effective ύψος = `avg(cornerTopZmm) − avg(cornerBaseZmm)` (mirror wall B3a)· volume = area × effHeight· flat = `params.height`. NEW `hooks/data/column-boq-feed.ts` (`columnBoqEntity`, mirror `wall-boq-feed`) recompute από scene beams+slabs· wired σε `useColumnPersistence` (persist + persistRestore, `levelManager` scene). **(3) 2D `ColumnRenderer` cut-state = NO-OP** (render leaf, cut plane ~1.2m πάντα μεταξύ base & attached top· doc μόνο, mirror WallRenderer B3c). **(4) ETICS = out-of-scope/no-op** — η κολώνα συνεισφέρει μόνο το **2D footprint** στο envelope perimeter (`columnExteriorArc`)· το Z-band οδηγείται από τους **τοίχους** (perimeter extrusion), όχι από column profile. Tests: NEW `column-piece-geometry.test.ts` (**12** — flat/per-corner κεκλιμένο/μεταβλητή βάση/concave-L/εκφυλισμός/guards/winding face-normals)· `column-geometry.test.ts` **+5** (profile-aware effective ύψος top/base/both/clamp)· NEW `column-boq-feed.test.ts` (**4** — flat/missing-scene/underside-2300/ghost-host). bim-3d converters **69/69** PASS· **tsc 0 errors** (filtered touched files)· **μηδέν regression** (scene baseline 25/49 pre-existing wall test-lag ταυτόσημο με/χωρίς τις αλλαγές μου). **❌ Εκκρεμεί:** F.3 (column auto-attach + ribbon + 3D grip + edit-break + Boy Scout γενίκευση `wall-attach-detach`→`entity-attach-detach`). ⚠️ Pre-existing: `BimSceneLayer` scene tests (wall fixtures χωρίς `params.start`) — test-lag, χωριστό από F.2. |
| 2026-05-31 | Giorgio + Claude (Opus 4.8) | **Phase F.1 — COLUMN ATTACH CORE ENGINE IMPLEMENTED** (pending commit). Ξεκίνησε το Phase F (γενίκευση top/base attach τοίχου → κολώνα). Giorgio decisions: **(α) σύνθετο profile** (η κολώνα ακολουθεί κεκλιμένη κορυφή/βάση, όχι μόνο scalar)· **(β) πλήρες mirror** (core + auto-attach + ribbon + 3D grip + edit-break). **F.1 = ο πυρήνας:** `ColumnParams += attachTopToIds?/attachBaseToIds?: readonly string[]` (`column-types.ts`) + Zod 4 refinements `attached ⇔ ≥1 id` (`column.schemas.ts`, mirror `wall.schemas.ts`). **NEW SSoT `bim/geometry/column-vertical-profile.ts`**: scalars `resolveColumnBaseZmm`/`resolveColumnNominalTopZmm` (mirror wall) + **per-corner** `resolveColumnTopProfile`/`resolveColumnBaseProfile`. **Αρχιτεκτονικό σημείο:** η κολώνα έχει σημειακό footprint (όχι άξονα όπως ο τοίχος) → δεν αντιγράφεται ο 1D `t`-resolver· αντί αυτού το προφίλ αποτιμάται **ανά γωνία του footprint** (top = lower-envelope `min{nominal, καλύπτουσες κάτω-παρειές}`· base = upper-envelope bidirectional `max{καλύπτουσες άνω-παρειές}`, ακάλυπτη γωνία → nominal). Διαφορετικές γωνίες / κεκλιμένα hosts ⇒ `cornerTopZmm` διαφέρουν ⇒ κεκλιμένη/στρεβλή κορυφή (το «σύνθετο profile»). **REUSE** αυτούσια `HostFootprintInput` + `beamHostInput`/`slabHostInput`/`buildWallHostInputs` (`wall-host-plan-builder.ts`) + `isPointInPolygon` (host coverage) — μηδέν διπλή formula κάτω/άνω-παρειάς. `makeColumnHostResolver` lookup. Tests: NEW `column-vertical-profile.test.ts` (**19/19** — scalars/single-host/cap-nominal/μερική-κάλυψη-σκαλωτό/κεκλιμένο-host/missing/2-host lower-envelope/base bidirectional πάνω+κάτω/μερική/legacy-no-topside/resolver). **tsc 0 errors** (αρχεία μου). **❌ Εκκρεμεί:** F.2 (consumers 3D/2D/BOQ/ETICS) · F.3 (auto-attach+ribbon+grip+edit-break). 🔴 browser verify ολόκληρου του ADR-401. |
| 2026-05-31 | Giorgio + Claude (Opus 4.8) | **Phase E.2 + E.4 — RIBBON `topElevationEnd` INPUT + MANUAL-EDIT-BREAKS-ATTACH IMPLEMENTED** (pending commit, 🔴 browser verify). Έκλεισαν οι δύο μικρές υπο-εργασίες του «E». **E.2 (κεκλιμένη δοκός ribbon input):** το `BeamParams.topElevationEnd?` + όλη η διάχυση (3D `applyBeamSlope` / 2D `beamSection.slopeYAt` / BOQ / wall-attach `beamHostInput.undersideZmmAt`) ήταν ΗΔΗ έτοιμα από Phase E(β) — έλειπε **μόνο** το UI. Νέο combobox (mm, free entry, ίδια `ELEVATION_MM_OPTIONS`) στο **geometry panel** του `contextual-beam-tab.ts`, δίπλα στο `topElevation`. Registry `beam-command-keys.ts`: `params.topElevationEnd` + `BeamRibbonNumberCommandKey` union + `BEAM_RIBBON_NUMBER_KEYS`. Bridge `useRibbonBeamBridge`: `NUMBER_KEY_TO_FIELD += topElevationEnd` (generic numeric path → `UpdateBeamParamsCommand`, undoable, atomic recompute)· `getComboboxState` fallback — `topElevationEnd` απών (flat) → surface `topElevation` ώστε το πεδίο να δείχνει την τρέχουσα flat στάθμη. **Pure-mm combobox** (ΟΧΙ `RibbonWallDimensionWidget` → αποφεύγεται το flagged `/1000`-vs-`mmScaleFor` latent issue σε meter scenes). i18n `beamEditor.topElevationEnd` + `...EndTooltip` (el+en). Tests: NEW `beam-command-keys.test.ts` (3). **E.4 (manual-edit-breaks-attach, mirror E.3 grip):** χειροκίνητο UI patch που αλλάζει `height` (top driver) ή `baseOffset` (base driver) ενώ η πλευρά είναι `attached` → σπάει το binding πρώτα (η ρητή τιμή νικά τον host-follow). **NEW pure SSoT `detachSidesAffectedByVerticalEdit(params, patch)` στο `bim/walls/wall-attach-detach.ts`** (reuse `detachWallSide`+`isWallSideAttached`, μηδέν διπλότυπο). Wiring στον **κεντρικό** `dispatchWallParamPatch`/`useWallParamsDispatcher` (ΕΝΑΣ SSoT writer για ΟΛΑ τα ribbon/panel wall edits) — **όχι** μέσα στο `UpdateWallParamsCommand`, ώστε το cascade/host-follow recompute (περνά απευθείας από το command, ΟΧΙ από αυτόν τον hook — επιβεβαιωμένο με grep) να **μην** σπάει λάθος το attach. Detach+edit = **ένα** undo step (`prevParams` επαναφέρει και τα δύο). Tests: `wall-attach-detach.test.ts` += 6 → **11/11**. **tsc 0 errors.** **❌ Εκκρεμεί:** Phase F (column mirror). 🔴 browser verify A→E.4. |
| 2026-05-31 | Giorgio + Claude (Opus 4.8) | **Phase E.3 — WALL TOP/BASE VERTICAL GRIP (3D) IMPLEMENTED** (pending commit, 🔴 browser verify). Ο τοίχος αποκτά **δύο** κάθετα grips στο 3D gizmo (ADR-402) αντί ενός συμμετρικού: **πάνω** (height, top face) + **κάτω** (baseOffset, bottom face) — Revit/ArchiCAD/SketchUp move-face. **Εύρημα:** το gizmo είχε ΗΔΗ axis-Y resize (single, centroid-anchored, → `height`)· αξιοποιήθηκε η αχρησιμοποίητη `mode:'normal'|'mirror'` υποδομή (ρέει ήδη end-to-end gizmo-types→parseHandleId→handleToConstraint→drag-bridge→`ResizeDragMm`) — **μηδέν νέο handle taxonomy**. **NEW SSoT `bim/walls/wall-attach-detach.ts`**: `detachWallSide(params, side)` (unconditional binding→DEFAULT + clear `attach{Top\|Base}ToIds`) + `isWallSideAttached`. Εξήχθη από το `DetachWallsCommand.buildDetachedParams` (refactor → κοινό SSoT, ίδια συμπεριφορά, 7/7 E.1 tests intact)· καταναλώνεται από E.1 (manual detach) + E.3 (grip detach-on-drag) + έτοιμο για E.4 (ribbon edit). **`gizmo-geometry.ts`:** για τον άξονα Y, **δεύτερο** octahedron @`−RESIZE_HANDLE_OFFSET` (`resize-m-y`)· το πάνω handle δρομολογεί ΟΛΑ τα hitboxes (center+4 corners) στο `resize-y`, το κάτω στο `resize-m-y`· X/Z αμετάβλητα (shared in-plane mirror corners). **`bim-gizmo-overlay.ts`:** `RESIZE_HANDLES_BY_TYPE.wall += 'resize-m-y'` (μόνο ο τοίχος — beam/column/slab κρατούν single vertical). **`bim3d-resize-bridge.computeWallResizeParams`** axis-Y branch ανά `drag.mode`: `normal`(TOP)→`height = clampMin(height + ΔupMm)` (βάση σταθερή)· `mirror`(BASE)→`baseOffset += ΔupMm` & `height = clampMin(height − ΔupMm)` (κορυφή σταθερή στις height-driven περιπτώσεις· resolver-driven αλλιώς → συνεπές)· **detach-on-drag** (Giorgio-confirmed Revit «edit breaks attach») όταν `isWallSideAttached` → `detachWallSide` πριν το scalar. Dispatch = υπάρχον `UpdateWallParamsCommand` (geometry recompute + hosted-opening cascade). Tests: NEW `wall-attach-detach.test.ts` (8) + `bim3d-resize-bridge.test.ts` += 7 + `bim-gizmo-overlay.test.ts` += 2 → **55/55** (+ `gizmo-hit-test` 3/3 αμετάβλητο), **tsc 0 errors**. Edge (follow-up): `topBinding='unconnected'` με ρητό `unconnectedHeight` + κάτω grip δεν κρατά τέλεια σταθερή κορυφή (σπάνιο). **❌ Εκκρεμεί:** E.2 (topElevationEnd ribbon input) · E.4 (manual-edit-breaks-attach για ribbon/height-length paths — reuse `detachWallSide`) → F (column mirror). |
| 2026-05-31 | Giorgio + Claude (Opus 4.8) | **Phase E.1 — MANUAL ATTACH/DETACH RIBBON (top+base) IMPLEMENTED** (pending commit, 🔴 browser verify). Έκλεισε το χειροκίνητο κομμάτι του §2.5 (Revit «Attach/Detach Top/Base»). **Attach (pick-host):** 2 νέα ToolTypes `wall-attach-top`/`wall-attach-base` (`ToolStateManager` `editing`, non-continuous) + NEW hook `hooks/tools/useWallAttachTool.ts` (mirror `useBimCopyTool` activation snapshot + `useWallSplitTool` click): στην ενεργοποίηση snapshot-άρει τους επιλεγμένους τοίχους ως targets (helper), στο click βρίσκει host (δοκάρι/πλάκα) και χτίζει `AttachWalls{Top|Base}Command` (ΥΠΗΡΧΑΝ). **Host pick unit-safe:** πρωτεύον `getHoveredEntity()` (HoverStore SSoT, μηδέν unit math) + belt-and-suspenders mm-space geometry fallback (slab→`pointInPolygon`, beam→axis distance· ο caller μετατρέπει scene-units→mm στο boundary, αποφεύγει την παγίδα meter-scene). **Detach:** NEW γενικό `core/commands/entity-commands/DetachWallsCommand.ts` (`side:'top'|'base'`, ΕΝΑ αρχείο αντί 2 — SSoT· mirror `AttachWallsBaseCommand`: prev/next snapshot once, recompute geometry/validation, cascade openings· επαναφορά `DEFAULT_WALL_{TOP|BASE}_BINDING` + clear ids)· bridge action σε ΟΛΟΥΣ τους επιλεγμένους τοίχους (`useRibbonWallBridge.onAction` + selection widen `getSelectedEntityIds`, reuse `resolveWallAttachTargets`). **NEW SSoT helper** `bim/walls/wall-attach-pick.ts` (`resolveWallAttachTargets`/`resolveStructuralHostId`/`findStructuralHostAtPoint`, pure/testable). **Ribbon:** νέο panel `wall-structural-attach` (4 simple buttons· attach = `commandKey=ToolType` no-action αυτο-ενεργοποίηση tool, detach = `action`)· `WALL_RIBBON_KEYS_ACTIONS += detachTop/detachBase`· 3 νέα lucide icons (`ArrowUpToLine`/`ArrowDownToLine`/`Unlink2`). **Wiring (ADR-040-safe pass-through):** `useModifyTools`+`canvas-click-types`+`useCanvasClickHandler` (PRIORITY 1.615)+`useCanvasEscapeRegistrations`+`CanvasSection`. **EventBus:** `bim:walls-attached-manual`/`bim:walls-detached` + toasts (`attachToStructural.attachedManual/detached/pickHostPrompt`, el+en, ns `dxf-viewer-shell`). Tests: NEW `DetachWallsCommand.test.ts` (7) + `wall-attach-pick.test.ts` (6) → 13/13 + ribbon-bridge regression PASS, tsc clean. **❌ Εκκρεμεί E-rest:** topElevationEnd ribbon input (E.2) · vertical grip 3D (E.3) · manual-edit-breaks-attach (E.4) → F (column mirror). |
| 2026-05-31 | Giorgio + Claude (Opus 4.8) | **Phase γ3 — ETICS dual-band + docs + rest tests IMPLEMENTED** (pending commit, 🔴 browser verify). Έκλεισε το base-attach: το Z1 κέλυφος ντύνει πλέον ΚΑΙ το κατέβασμα του τοίχου στο θεμέλιο. **(α) ETICS dual-band:** NEW SSoT `bim/geometry/envelope-wall-base.ts` (`resolveEnvelopeEdgeBases` per-edge μεταβλητή βάση — sibling του `resolveEnvelopeEdgeTops`, **ΧΩΡΙΣ** clamp στο 0 αφού η βάση είναι αρνητική σε θεμέλιο· `chainBaseAreaM2` — null edge → 0 συνεισφορά· reuse `projectTOnAxis`/`AxisPoint` SSoT). `EnvelopeToThree`: `addVariableTopBand` → γενικό **`addProfiledBand`** (top profile + bottom = profile (base-attach) ή scalar floor (0/πρέκι head), split στο **union** top+base breakpoints, interior-biased segment-eval· flat-prism ή `buildSlopedWallPieceGeometry` με 4 ανεξάρτητες γωνίες)· νέο `addFlatEdge` fast path (byte-for-byte μη-attached)· `envelopeChainToMesh(..., edgeBases?)`· στα ανοίγματα η **ποδιά** ακολουθεί τη βάση [base..sill], το **πρέκι** μένει [head..top]. `bim-envelope-scene-builder.addEnvelopeShell` `buildEnvelopeWallBaseRefs` + `resolveEnvelopeEdgeBases` ανά chain (guarded). `envelope-boq-sync.computeZ1FacadeArea` → **area = topArea − baseArea** (`chainProfileAreaM2 − chainBaseAreaM2`· η αρνητική βάση μεγαλώνει σωστά το κέλυφος). **(β) docs N.15:** §2.3 `wall.base(t)` UPPER-envelope formula + NEW §2.7 base-attach prose + αυτό το changelog. **(γ) rest tests:** NEW `envelope-wall-base.test.ts` (11 — σκαλωτή/κεκλιμένη/**αρνητική-no-clamp**/partial/floorElev + chainBaseAreaM2)· NEW `AttachWallsBaseCommand.test.ts` (6, mirror top)· `wall-structural-attach-coordinator.test.ts` += `findWallsToAutoAttachBaseToHost` describe (6 — foundation-below→attach / ceiling-above→skip inverted gate)· NEW `wall.schemas.test.ts` (12 — top+base attach refinement, Boy Scout: δεν υπήρχε κανένα). **tsc 0 errors** (νέα αρχεία), 75/75 (νέα + commands/coordinator/schema/boq/envelope-top regression) + **618/618** regression (converters+2d-section+bim/geometry) PASS. Flat/μη-attached = byte-for-byte fast path παντού. **❌ Εκκρεμεί:** E-rest (ribbon manual attach/detach top+base + vertical grip + manual-edit-breaks-attach) → F (column mirror)· Sub-Phase 1 stair. |
| 2026-05-31 | Giorgio + Claude (Opus 4.8) | **Phase (γ) base-attach — γ1 ENGINE + γ2 CONSUMERS IMPLEMENTED** (pending commit, 🔴 browser verify). Ο **πάτος** του τοίχου κολλάει στην **άνω-παρειά** structural host(s) — **bidirectional/Revit** (Autodesk «increases OR decreases to conform»): πολλά hosts → **upper-envelope** (πατάει στο ΨΗΛΟΤΕΡΟ στήριγμα, καθρέφτης του top lower-envelope). **γ1 (μηχανή):** NEW SSoT `bim/geometry/wall-base-profile.ts` (`resolveWallBaseProfile` + `evaluateWallBaseAt`· `highestAt`=MAX· covered span→host topside, uncovered→nominal ρητά — ΟΧΙ baseline-στο-envelope, αφού bidirectional)· `WallBaseBinding += 'attached'` (+ VALUES + Zod)· `WallParams.attachBaseToIds?`· `wall.schemas.ts` base superRefine· `wall-top-profile.ts` export shared helpers + `HostTopsidePlan` + `resolveHostTopside?`· `wall-host-plan-builder.ts` `topsideZmm/topsideZmmAt` + `buildHostTopsidePlans`/`makeResolveHostTopside`/`makeWallBaseContext` (beam topside=`topElev+zOff`, slab=`levelElev+offset`· tilted→`beamTopZmmAt`/`slabTopZmmAt`)· NEW `AttachWallsBaseCommand` (mirror top)· `findWallsToAutoAttachBaseToHost` (inverted Z-gate, host topside κάτω από base — foundation)· `useStructuralAutoAttach` dispatch top+base + EventBus `bim:walls-auto-attached-base`. **γ2 (consumers — mirror του top wiring):** **(1) 3D κάτω-έδρα** — `wall-opening-pieces.ts` `WallOpeningPiece.zBotM` → **`zBotAM`/`zBotBM`** (per-boundary base, flat⇒ίσα/sloped⇒διαφορετικά) + NEW `WallBaseLocalFn` + `computeWallOpeningPieces(wall, openings, wallTop?, wallBase?)` (union top+base breakpoints· jamb/ποδιά πάτος = base profile, **πρέκι ΟΧΙ** = σταθερό lintel floor-relative)· `wall-piece-geometry.buildSlopedWallPieceGeometry` wedge bottom verts @zBotA/@zBotB (κεκλιμένος/σκαλωτός πάτος)· `BimToThreeConverter` NEW `makeWallBaseLocalFn` + `wallToMesh(..., baseProfile?)` (flat-extrude όταν top **&** base flat, αλλιώς wedge· piece-path ενεργό όταν `openings ‖ wallTop ‖ wallBase`)· `wall-opening-extrude` (curved/polyline) bottom edge = polyline base profile· `BimSceneLayer.syncWalls` `resolveWallBaseProfile`+`makeWallBaseContext` ανά `baseBinding='attached'` (hostInputs guard = top **‖** base). **(2) 2D τομή** — `WallPlan.baseProfile?` + `toWallPlan(..., resolveHostTopside?)` (baseY=`minBaseZmm`) + `wallSection` yMin αποτιμάται στο cut· `section-scene-sync` `makeResolveHostTopside`. **(3) BOQ** — `computeWallGeometry(..., baseProfile?)`: gross area = **trapezoidal ∫(top(t)−base(t)) σε union breakpoints** (νέο `profileGrossAreaM2(lengthM, heightMm, top?, base?)` + `evalProfileSegAt` interior-biased· top-only flat = byte-for-byte)· bbox bottom = `minBaseZmm` (θεμέλιο κάτω από τη στάθμη)· `wall-boq-feed.resolveAttachedWallProfiles` → {top, base}. **`EnvelopeToThree`** piece literal → `zBotAM/zBotBM` (επίπεδος band πάτος). Flat/μη-attached = byte-for-byte fast path παντού. Tests: NEW `wall-base-profile.test.ts` (8, γ1) + NEW `wall-base-attach-consumers.test.ts` (8: 3D flat/stepped/sloped/πρέκι-σταθερό, 2D yMin@cut, BOQ top−base 17.5m²) → **tsc 0 errors**, 8/8 νέα + **607/607 regression** (converters+2d-section+bim/geometry) PASS. **❌ Εκκρεμεί γ3:** ETICS dual-band (`envelope-wall-top.ts` base sibling) + docs N.15 (§2.x base prose) + rest tests (AttachWallsBaseCommand / coordinator base-gate matrix / schema). E-rest (ribbon/grip) + F (column) εκκρεμούν. |
| 2026-05-31 | Giorgio + Claude (Opus 4.8) | **Phase E (β) — TILTED BEAM (κεκλιμένη δοκός) IMPLEMENTED** (pending commit, 🔴 browser verify). Η δοκός γέρνει **γραμμικά κατά μήκος του άξονά της** (Revit sloped beam): `BeamParams += topElevationEnd?` (mm, πάνω παρειά στο `endPoint`)· απών/ίσο με `topElevation` = οριζόντια δοκός (flat fast-path, byte-for-byte back-compat). **NEW SSoT `bim/geometry/beam-slope.ts`**: `isBeamTilted`/`beamSlopeOffsetZmm`/`beamTopZmmAt`/`beamUndersideZmmAt` — offset = **αδιάστατο axis fraction** `f` (προβολή pt στον άξονα start→end / μήκος²) × `(topElevationEnd − topElevation)`. **Unit-safe by construction:** το magnitude (Δmm) είναι σωστό σε mm-scene **ΚΑΙ** meter-scene (σε αντίθεση με το slab slope §E2 που κληρονομεί το `mmScaleFor` latent issue — η δοκός ΔΕΝ το κληρονομεί γιατί το `f` είναι ratio). **Αυτόματη διάχυση μέσω των ΙΔΙΩΝ SSoT consumers με την tilted slab:** (1) **3D** `BimToThreeConverter.applyBeamSlope` (per-vertex world-Y shear, mirror `applySlabSlope`· `mesh.position.y` = top-at-start nominal → f=0 αμετάβλητο, f=1 → +Δ)· (2) **wall-attach** `beamHostInput` += `undersideZmmAt` όταν tilted → ο attached τοίχος ακολουθεί την κεκλιμένη κάτω-παρειά (z0mm≠z1mm, ΗΔΗ έτοιμο το §2.3 `beam.underside(t)`)· (3) **2D section** `BeamPlan += slopeYAt?`, `toBeamPlan` το γεμίζει όταν tilted, `beamSection` αποτιμά στο μέσο της τομής (mirror `slabSection`)· (4) **bbox** `computeBeamGeometry.computeBbox` κρατά `[min,max]` top range για fit-to-view (ADR-394)+culling. Schema: `beam.schemas.ts += topElevationEnd` (optional finite). Tests: NEW `beam-slope.test.ts` (πλήρες SSoT: flat/start/end/mid/off-axis/down/unit-safety/degenerate/top/underside) + `beam-slope-mesh.test.ts` (4 — flat box, shear===slope plane, σταθερό βάθος, wall-consistency===`beamUndersideZmmAt`) + `section-intersect-beam-slope.test.ts` (9) → **84/84** (νέα + regression slab-slope+host-plan+beam-geometry+section) PASS, tsc clean. **❌ Εκκρεμούν: (γ) base-attach· E-rest (ribbon για set του `topElevationEnd`/grip/manual-edit-breaks-attach)· F (column).** 2D section παραλληλόγραμμο cross-section = follow-up. |
| 2026-05-31 | Giorgio + Claude (Opus 4.8) | **Phase E2 follow-up (α) — TILTED SLAB 3D RENDERING + 2D SECTION IMPLEMENTED** (pending commit, 🔴 browser verify). Έκλεισε το flagged κενό του E2 + το ADR-369 §9 Q7 «tilted extrude path»: η ίδια η πλάκα/στέγη render-άρεται **κεκλιμένη** (πριν: πάντα επίπεδο box). Η κλίση είναι **ένα affine επίπεδο** → `BimToThreeConverter.slabToMesh` περνά το flat extruded box από `applySlabSlope` (per-vertex shear στο world-Y, καταναλώνει το `slabSlopeOffsetZmm` SSoT — την **ΙΔΙΑ** ποσότητα με τον `wall-top-profile` resolver/τοίχο → ο attached τοίχος **εφάπτεται**). Top & bottom face γέρνουν ίσα → σταθερό πάχος· holes/slab-openings διατηρούνται· `geometryType!=='tilted'` = no-op fast-path. Coords μετα-rotation (`ROT_X_NEG_90`): shape(sx,sy)→world(sx,z,−sy)· plan-point = `{x:worldX, y:−worldZ}`· offset(mm)×MM_TO_M = world m· pivot offset=0 → η πλάκα γέρνει γύρω από το pivot, `mesh.position.y` (nominal) αμετάβλητη. **2D section**: `toSlabPlan` += `slopeYAt?(pt)` (top/bottom m στο plan-point)· `slabSection` αποτιμά την παρειά στο **μέσο της τομής** (single-point rect, mirror `wallSection`). **Units note (pre-existing, flagged):** το `params.outline` είναι σε **canvas units** (mm σε mm-scene, m σε meter-scene· `buildDefaultSlabParams` σηκώνει 2D canvas vertices χωρίς scaling)· το slope offset×MM_TO_M είναι σωστό σε mm-scenes και **συνεπές** με τοίχο/resolver σε ΟΛΑ τα scenes (εφάπτονται), αλλά απόλυτα 1000× υποτιμημένο σε meter-scenes — ίδιο latent issue με Phase E2/ADR-398 grip· δεν διορθώνεται εδώ (out of scope, χρειάζεται `mmScaleFor` σε resolver+όλους τους consumers = ξεχωριστό ratchet). Tests: NEW `slab-slope-mesh.test.ts` (4 — flat box, shear===slope plane vs flat baseline, σταθερό πάχος, wall-consistency underside===`slabUndersideZmmAt`) + `section-intersect-slab-slope.test.ts` (7 — slopeYAt, flat back-compat, pivot/up/down cut) → 11 νέα· **54/54** (slab-slope+host-plan+stepped-solid+wall-profile+2 νέα) + **39/39** regression (BimSceneLayer multifloor+visibility+envelope-to-three) PASS, tsc clean. **2D section παραλληλόγραμμο cross-section** (true sloped top/bottom αντί single-point rect) = follow-up αν χρειαστεί `SectionRect` επέκταση. **❌ Εκκρεμούν: (β) κεκλιμένο δοκάρι (νέο slope param στο `BeamParams`)· (γ) base-attach· E-rest (ribbon/grip)· F (column).** |
| 2026-05-31 | Giorgio + Claude (Opus 4.8) | **Phase E2 IMPLEMENTED** (κεκλιμένη στέγη/πλάκα host adapter, pending commit, 🔴 browser verify). Όταν η πλάκα/στέγη είναι `geometryType='tilted'`, ο attached τοίχος ακολουθεί την **κεκλιμένη** κάτω-παρειά (z0mm ≠ z1mm). **NEW SSoT `bim/geometry/slab-slope.ts`**: `slabUndersideZmmAt`/`slabTopZmmAt`/`slabSlopeOffsetZmm` — καθιερώνει την κανονική ερμηνεία του `SlabSlope` (single-plane tilt: `direction`° CCW from +X = ανηφόρα· `angle`% = mm/mm· `pivotEdge` = γραμμή/σημείο που μένει στη nominal στάθμη, default κέντρο AABB· N/S/E/W = μέσο ακμής AABB). `HostFootprintInput` += optional `undersideZmmAt?: (pt)=>number`· `buildHostUndersidePlans` αποτιμά την παρειά στα **plan-points των άκρων του covered span** (flat host = scalar → z0=z1, byte-for-byte back-compat). `slabHostInput`: tilted → attach `undersideZmmAt` (SSoT) + `hostType='roof'` όταν `kind='roof'`. **Αυτόματη διάχυση** (μηδέν επιπλέον κώδικας): 3D wedge (B2 `buildSlopedWallPieceGeometry` ήδη έτοιμο), 2D τομή, BOQ ακολουθούν — όλα χτίζουν hosts μέσω του κοινού `buildWallHostInputs`/`slabHostInput`. Tests: NEW `slab-slope.test.ts` (10 — flat/pivot center+N/S/E/W/W-edge/direction+Y/top-underside-parallel) + `wall-host-plan-builder.test.ts` Phase-E2 integration (4 — roof hostType, box no-slope, sloped plan z0≠z1, full chain `evaluateWallTopAt` 0≠1) → 31/31 + regression 75/75 (wall-top-profile+stepped-solid+section+wall-geometry+envelope-wall-top) PASS, tsc clean. **⚠️ Out of scope (flagged):** (α) το ίδιο το tilted slab render-άρει ακόμη επίπεδο box (pre-existing ADR-369 §9 Q7 κενό· `slab-slope.ts` έτοιμο να το καταναλώσει)· (β) κεκλιμένο **δοκάρι** (δεν έχει slope param στο `BeamParams`)· (γ) base-attach. **E (ribbon/grip/base) + F (column) εκκρεμούν.** |
| 2026-05-30 | Giorgio + Claude (Opus 4.8) | Initial PROPOSED — έρευνα (ADR-369 datum ήδη αποφασισμένο = Επιλογή Α· current code: bindings μη-καταναλωμένα από geometry· floor-height-cascade υπάρχει· hosted-opening cascade pattern). Industry Revit Attach Top/Base. Decision: associative resolver + element attach + auto-attach UX. Q&A §6 ανοιχτό. |
| 2026-05-30 | Giorgio + Claude (Opus 4.8) | **Giorgio: «ΘΕΛΩ ΛΟΓΙΚΗ ΚΑΙ ΥΛΟΠΟΙΗΣΗ FULL ENTERPRISE + FULL SSOT».** Όλα τα §6 ανοιχτά → ΑΠΟΦΑΣΙΣΜΕΝΑ στην πλήρη μορφή: σκαλωτή/κεκλιμένη κορυφή (lower-envelope `resolveWallTopProfile` αντί single-scalar), `attachTopToIds[]` multi-host (δοκάρι/πλάκα/στέγη/τοίχος), auto+manual attach, detach-on-delete+warning, manual-edit-breaks-attach. §2.2 resolver → top-profile· §2.4 stepped/sloped solid· §5 φάσεις A-F (όλες in-scope). |
| 2026-05-30 | Claude (Opus 4.8) | **Phase A IMPLEMENTED** (pending commit). NEW `bim/geometry/wall-top-profile.ts`: `resolveWallTopProfile` (exact piecewise-linear lower-envelope με pairwise-intersection splitting → σκαλωτή + κεκλιμένη + τομή baseline) + `resolveWallBaseZmm` + `resolveWallNominalTopZmm` (shared nominal scalar SSoT) + 5 types. `WallTopBinding` += `'attached'` (+ values + Zod), `WallParams.attachTopToIds?: readonly string[]`, Zod attach refinement (attached⇔≥1 id). `section-intersect.toWallPlan` → consumer (μηδέν behavior change· `storey-ceiling` slab-subtraction ενεργό με context). NEW `__tests__/wall-top-profile.test.ts` (15 cases incl. ADR §1 3.00→2.50). §5 re-scope: 3D/bbox/BOQ → Phase B. |
| 2026-05-31 | Claude (Opus 4.8) | **Phase B2 IMPLEMENTED** (pending commit). Stepped/sloped 3D wall solid σε **3 converter paths** + scene wiring. NEW `wall-piece-geometry.ts` `buildSlopedWallPieceGeometry` (8-vertex wedge, κεκλιμένη κορυφή). `WallOpeningPiece.zTopM` → `zTopAM`/`zTopBM` (per-boundary, §2.4)· `computeWallOpeningPieces(wall, openings, wallTop?)` (jambs/πρέκια ακολουθούν προφίλ + split στα breakpoints, interior-biased eval για σκαλωτό ασυνεχές)· `BimToThreeConverter.wallToMesh(..., profile?)` + `makeWallTopLocalFn` (mm→local m) + flat=ExtrudeGeometry/sloped=wedge routing (piece-path ενεργό ΚΑΙ χωρίς ανοίγματα όταν `hasAttach`)· `wall-opening-extrude` front-face top = polyline προφίλ (curved/polyline). `BimSceneLayer.syncWalls` χτίζει hostInputs (beams+slabs) + `resolveWallTopProfile` ανά attached τοίχο (guarded — μηδέν κόστος όταν κανένας attached). Flat τοίχος = αμετάβλητο fast path. Tests: NEW `wall-stepped-solid.test.ts` (16: flat back-compat, οριζόντιο σκαλωτό, breakpoint split, sloped wedge, ADR §1 3.00→2.50, opening+stepped) → 61/61 + BimSceneLayer 32/32. tsc clean. bbox → B3. |
| 2026-05-31 | Claude (Opus 4.8) | **Phase B3a IMPLEMENTED** (pending commit). Profile-aware BOQ geometry. `computeWallGeometry` += optional `profile?: WallTopProfile` (4η param) → gross area = `profileGrossAreaM2` (Σ segment length × avg segment height, σκαλωτή+κεκλιμένη) + bbox top = `maxTopZmm − baseZmm` (αντί nominal `params.height`)· flat/χωρίς profile = byte-for-byte fast path (computeBbox υπογραφή αμετάβλητη — περνά `topExtentMm`). `wall-boq-feed.ts`: NEW `resolveAttachedWallProfile(entity, scene)` (filter beams+slabs → `beamHostInput`/`slabHostInput`/`makeWallTopContext`/`resolveWallTopProfile`, `floorElevationMm=0` mirror `section-scene-sync`)· `wallBoqEntity` recompute όταν `openings \|\| attached`. Tests: 6 νέα στο `wall-geometry.test.ts` (single-lowered/stepped/sloped/back-compat-flat/openings/bbox-baseOffset) → 81/81 (wall-geometry+top-profile+host-plan-builder+stepped-solid+section-intersect) PASS, tsc clean. **Giorgio ΑΠΟΦΑΣΗ:** B3b ETICS Z1 → ΠΛΗΡΕΣ ΣΚΑΛΩΤΟ ΚΕΛΥΦΟΣ (handoff). Scope boundary: persisted bbox σε transform paths = Phase C cascade (resolver context δεν φτάνει εκεί). |
| 2026-05-31 | Claude (Opus 4.8) | **Phase B3b + B3c IMPLEMENTED** (pending commit). **B3b — ETICS Z1 ΠΛΗΡΕΣ ΣΚΑΛΩΤΟ ΚΕΛΥΦΟΣ** (απόφαση Giorgio §6). Per-edge variable top: `EnvelopeChain.edgeWallIds?: (string\|null)[]` (1:1 με `envelopeFaceEdges`)· `envelope-shell.buildChain`/`orphanWrap` populate από `ShellEdge.sourceEntityId` (legacy `computeEnvelopePerimeter` → undefined → flat). NEW SSoT `bim/geometry/envelope-wall-top.ts`: `resolveEnvelopeEdgeTops` (προβολή face→άξονας `tA,tB` + split στα profile breakpoints → **ανεξάρτητα sub-segments**, interior-biased segment-eval → σκαλοπάτι = καθαρή ασυνέχεια, ΟΧΙ λοξό γεφύρωμα) + `chainProfileAreaM2` (Σ μήκος×μέσο ύψος) + `projectTOnAxis`. `EnvelopeToThree.envelopeChainToMesh(..., edgeTops?)` + `addVariableTopBand` (clip sub-segments· flat→`ExtrudeGeometry`, sloped→reuse `buildSlopedWallPieceGeometry` wedge· sill/head ανέπαφα). `BimSceneLayer.addEnvelopeShell` `buildEnvelopeWallTopRefs` (per-attached profile) + `resolveEnvelopeEdgeTops` ανά chain (guarded — μηδέν κόστος όταν κανένας attached). `envelope-boq-sync.computeZ1FacadeArea` profile-aware όταν attached, αλλιώς `Σ perimeterM × maxWallHeightM` (ΑΜΕΤΑΒΛΗΤΟ). **SSoT extract** `buildWallHostInputs(beams,slabs)` (4 sites — Boy Scout N.0.2). **Flat/μη-attached = byte-for-byte fast path.** **B3c**: `WallRenderer:189` cut-state = no-op confirmed + doc (render leaf, ADR-040 — cut plane πάντα μεταξύ base/top). Tests: NEW `envelope-wall-top.test.ts` (12) + `buildWallHostInputs` (2) → 151/151 (envelope-wall-top+host-plan+shell+perimeter+boq+envelope-to-three+BimSceneLayer) + 63/63 (wall regression) PASS, tsc clean. 🔴 browser verify. C–F εκκρεμούν. |
| 2026-05-31 | Claude (Opus 4.8) | **Phase B1 IMPLEMENTED** (pending commit). NEW `bim/geometry/wall-host-plan-builder.ts` (SSoT): `buildHostUndersidePlans`/`makeResolveHost`/`makeWallTopContext` — plan-overlap = **segment ∩ polygon** t-intervals (robust convex+concave, midpoint-inside via `isPointInPolygon` SSoT) + `beamHostInput`/`slabHostInput` (§2.3 underside, reuse `computeBeamGeometry.outline`). NEW `evaluateWallTopAt(profile,t)` SSoT στο `wall-top-profile.ts`. **2D section full-profile**: `WallPlan.topProfile?`, `toWallPlan(wall, floorElevationM, resolveHost?)`, `wallSection` profile-at-cut (single-point top στην εγκάρσια τομή), `section-scene-sync` per-wall `resolveHost` από beams+slabs. Beam/slab οριζόντια (z0=z1)· concave multi-span→largest. Tests: `wall-host-plan-builder.test.ts` (18) + `section-intersect-wall-profile.test.ts` (5) + resolver 14 = **37 PASS**. tsc clean. Phase B split → B1✅/B2(3D solid)/B3(BOQ+ETICS). |
| 2026-05-31 | Giorgio + Claude (Opus 4.8) | **Phase C DONE** (associative attach) — pending commit, 🔴 browser verify. **Code-audit αναθεώρησε το scope (code = source of truth):** η οπτική συσχέτιση host→attached-wall ΗΔΗ λειτουργεί μέσω **fresh-recompute** — `BimSceneLayer.syncWalls` (3D) + `section-intersect` (2D τομή) + `wall-boq-feed` (BOQ) ξαναχτίζουν το `WallTopProfile` από τη ζωντανή σκηνή σε ΚΑΘΕ resync, και το `use-bim3d-sync` re-runs σε κάθε entity change. Host move/rotate/resize ακολουθείται αυτόματα (Revit/ArchiCAD parity: client = geometry engine, client persistence με geometry+bbox = «server»). Persisted `cascadeStructuralAttachForHosts` = dead-code (γράφει γεωμετρία που κανείς δεν διαβάζει) + server batch χωρίς trigger (beam/slab μετακινούνται μόνο client-side) → **απορρίφθηκαν** (αντι-SSoT). Υλοποιήθηκε το πραγματικό κενό = **detach-on-host-delete warning**: NEW `bim/walls/wall-structural-attach-coordinator.ts` (`notifyWallsOnHostDeletion`) + `findAttachedWalls` reverse-lookup SSoT στο `bim-cascade-resolver` (mirror `findHostedOpenings`, beam/slab→wall) + decoupled `bim:wall-attach-host-missing` (EventBus) + thin subscriber `useDxfViewerNotifications` (sonner toast, mounted `DxfViewerContent`) + i18n `attachToStructural.hostMissing` (el+en, ns `dxf-viewer-shell`). Wired `DeleteEntityCommand` + `DeleteMultipleEntitiesCommand`. **ΟΧΙ** mutation `attachTopToIds` (resolver gracefully ignores missing→baseline· undo re-attaches). NEW `wall-structural-attach-coordinator.test.ts` 9/9 + regression 42/42 (cascade-resolver+delete+opening-coordinator+structural-attach). tsc clean (0 errors). |
| 2026-05-31 | Claude (Opus 4.8) | **Phase C COMMITTED** — ο κώδικας Phase C (host-deletion warning) committed σε δική του ομάδα μαζί με αυτή την εγγραφή changelog (9 αρχεία: i18n el/en + `DxfViewerContent` + `bim-cascade-resolver` + `DeleteEntityCommand` + `EventBus` + NEW `wall-structural-attach-coordinator.ts` + test + `useDxfViewerNotifications.ts`). 🔴 browser verify εκκρεμεί. |
| 2026-05-31 | Giorgio + Claude (Opus 4.8) | **Phase D IMPLEMENTED** (auto-attach UX, pending commit, 🔴 browser verify). Δοκάρι/πλάκα πάνω από `storey-ceiling` τοίχους → auto-attach κορυφής με undo (Revit). **Giorgio απόφαση: δοκάρι+πλάκα ΜΕ έλεγχο ύψους (Z gate)** — η πλάκα-πάτωμα από κάτω ΔΕΝ προσελκύει την κορυφή. Detection SSoT `findWallsToAutoAttachToHost(host, entities)` στο `wall-structural-attach-coordinator.ts`: (1) μόνο `topBinding='storey-ceiling'`· (2) plan overlap = `buildHostUndersidePlans` (ο ΙΔΙΟΣ projector με τον resolver — προτιμήθηκε αντί `polygonIntersectionAreaMm2`, πιο SSoT)· (3) Z gate `undersideZmm > resolveWallBaseZmm + 1mm` (`floorElevationMm=0` active-level σύμβαση, mirror `wall-boq-feed`/`envelope-boq-sync`). NEW `core/commands/entity-commands/AttachWallsTopCommand.ts` (batch undoable, ΕΝΑ undo entry· patch `topBinding='attached'`+append `attachTopToIds`+recompute geometry/validation+cascade openings, mirror `UpdateWallParamsCommand`· prev/next snapshot once → idempotent undo/redo). NEW thin hook `hooks/useStructuralAutoAttach.ts` (listener `drawing:entity-created`→detect→`execute`, mounted `DxfViewerContent`). NEW EventBus event `bim:walls-auto-attached` + info toast `attachToStructural.autoAttached` (el+en). **Undo:** beam/slab creation δεν είναι command (direct `appendEntityToScene`)→undo αποκολλάει τοίχους, host μένει (attach = δικό του undo step, Revit parity). Tests: `AttachWallsTopCommand.test.ts` 6/6 + `findWallsToAutoAttachToHost` 6/6 → coordinator 15/15 total, tsc clean. **E–F εκκρεμούν.** |
