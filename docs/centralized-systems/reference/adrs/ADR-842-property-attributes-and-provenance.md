# ADR-842 — ΤΑ ΧΑΡΑΚΤΗΡΙΣΤΙΚΑ ΤΟΥ ΑΚΙΝΗΤΟΥ: ο κανόνας μετρά **ερωτήματα**, όχι **δεδομένα**

> **Κατάσταση**: 🟢 Σε εκτέλεση — **Φ0+Φ1+Φ2+Φ3 έκλεισαν** *(2026-09-02)*· επόμενο **Φ5**
> **Πεδίο**: τα **χαρακτηριστικά** ενός ακινήτου — ποιος τα ρωτά, ποιος τα υπολογίζει, ποια φεύγουν δημόσια
> **Γεννήθηκε από**: ερώτηση Giorgio 2026-09-02 — *«γιατί η καταχώρηση ζητά λίγα και η οθόνη δείχνει πολλά; μήπως για να μην τρομάξουμε τον χρήστη; τι κάνουν οι μεγάλοι παίκτες;»*
> **Αναφορές**: **ADR-777** *(§25.6 · Α14 §17.2 — ο κανόνας των 8 πεδίων)* · **ADR-839** *(versioning δημόσιου σχήματος)* ·
> **ADR-841** *(το σώμα της δημόσιας αγγελίας · Φ8 «ο απλός δημιουργός»)* · **ADR-287** *(Batch 28 — ο μετρητής πληρότητας)* ·
> **ADR-749** *(μία μηχανή, μία αλήθεια)* · **ADR-759** *(§5.3 — πεδίο χωρίς προέλευση)* · **ADR-145** *(τύποι ακινήτου SSoT)*

---

## 1. Το ερώτημα

Ο Giorgio μέτρησε **δύο οθόνες του ίδιου έργου** και βρήκε ασυμμετρία:

> *«Τι πρέπει να κάνουμε όταν γίνεται μια νέα καταχώρηση ακινήτου στο `/offers/new` σχετικά με τις
> παροχές και τα επιμέρους χαρακτηριστικά; Η σελίδα `/o/comp_…/spaces/properties` εμφανίζει πολύ
> περισσότερα χαρακτηριστικά. Γιατί συμβαίνει αυτό; Επειδή δεν θέλουμε να τρομάξουμε τον χρήστη
> ζητώντας του πολλά στοιχεία; Τι κάνουν οι μεγάλοι παίκτες; Μήπως σε δεύτερο χρόνο τους
> παροτρύνουν να τα συμπληρώσουν όλα;»*

Και έθεσε τον πήχη:

> *«Θέλω υλοποίηση με σύστημα FULL ENTERPRISE + FULL SSOT … θέλω να ξεπεράσει και τον
> επαγγελματισμό και την ποιότητα των μεγάλων παικτών. Δεν με απασχολεί πόσος χρόνος θα απαιτηθεί.»*

---

## 2. Η μέτρηση — **τρία** σύνολα πεδίων, και το φράγμα είναι το **τρίτο**

| Πού | Τύπος | Πόσα |
|---|---|---|
| `/offers/new` *(ιδιώτης)* | `OwnerPropertyDraft` — `types/owner-property.ts:497` | **8**: `type · areaSqm · offers · place · floor · bedrooms · media · title` |
| `/o/<comp>/spaces/properties` *(εταιρεία)* | `Property` — `types/property.ts` | **~60**: `areas{gross,net,balcony,terrace,garden}` · `layout{bedrooms,bathrooms,wc,totalRooms,levels,balconies}` · `orientations` · `views` · `condition` · `energy{class,certificateId,…}` · `heating/cooling/waterHeating` · `finishes{flooring,windowFrames,glazing}` · `interiorFeatures` · `securityFeatures` · `millesimalShares` · `linkedSpaces` · `levels/levelData` |
| **η δημόσια αγγελία** | `PublicListing` — `types/public-listing.ts:224-447` | **18 πεδία, εκ των οποίων 4 ιδιότητες**: `type · areaSqm · floor · bedrooms` *(`lib/listings/listing-disclosure.ts:167-170`)* |

🔴 **ΤΟ ΕΥΡΗΜΑ ΠΟΥ ΑΝΑΤΡΕΠΕΙ ΤΗΝ ΥΠΟΘΕΣΗ ΤΗΣ ΕΡΩΤΗΣΗΣ.** Η ασυμμετρία **δεν** είναι «ο ιδιώτης
ρωτιέται λίγα». Είναι ότι **η εταιρεία κατέχει ήδη τα πλούσια δεδομένα και ΚΑΝΕΝΑ δεν φεύγει
δημόσια**. Τα `energyClass`, `interiorFeatures`, `securityFeatures`, `bathrooms` γράφονται σήμερα,
αποθηκεύονται σήμερα, και **σταματούν στο σύνορο της προβολής**.

⇒ **Επέκταση της φόρμας χωρίς επέκταση της προβολής θα έγραφε πεδία που κανείς δεν βλέπει ποτέ.**
Γι' αυτό το πρώτο εκτελέσιμο στάδιο είναι η **προβολή** (Φ3), όχι η φόρμα.

### 2.1 🔑 Και οι δύο οικογένειες περνούν ήδη από **μία** μηχανή

`projectableFromOwnerProperty()` *(`lib/owner-property/owner-property-projection.ts:71-173`)* και το
`Property` της εταιρείας τροφοδοτούν **αμφότερα** το `projectListingShape()`
*(`services/listings/public-listing-projection.ts:293-351`)* μέσω του **δομικού** `ProjectableProperty`.

Αυτό δεν είναι λεπτομέρεια — είναι ο λόγος που η Φ3 κοστίζει **μία** επέκταση αντί για δύο, και ο
λόγος που το ADR-749 («μία μηχανή») δεν παραβιάζεται.

---

## 3. 🔴 Η φαινομενική σύγκρουση με το §25.6 — και γιατί **δεν** είναι σύγκρουση

Το `types/owner-property.ts:35-53` δηλώνει **δεσμευτικό κανόνα**:

> *«5 βασικά + 3 ειδικά, **ΠΟΤΕ περισσότερα**»* — και η **Α14 §17.2** τον δένει με τρεις όρους:
> *«ρωτάμε **μόνο** ό,τι χρειάζεται το ταίριασμα · **ποτέ ό,τι μπορούμε να υπολογίσουμε** · η φόρμα
> **μικραίνει** όσο δίνεις»*

Μια πρόχειρη ανάγνωση λέει «άρα απαγορεύονται τα χαρακτηριστικά». **Η μέτρηση του ίδιου του κανόνα
λέει το αντίθετο**, και το αποδεικνύει η **τρίτη** του πρόταση:

> ⚠️ *«ποτέ ό,τι μπορούμε να **υπολογίσουμε**»* — μια απαγόρευση που **προϋποθέτει** ότι υπάρχουν
> δεδομένα τα οποία το σύστημα **κατέχει χωρίς να ρωτήσει**. Αν ο κανόνας απαγόρευε **δεδομένα**,
> αυτή η πρόταση δεν θα είχε νόημα: δεν χρειάζεται να απαγορεύσεις να ρωτήσεις κάτι που δεν
> επιτρέπεται καν να υπάρχει.

🔑 **Ο κανόνας μετρά ΕΡΩΤΗΜΑΤΑ ΤΗΣ ΦΟΡΜΑΣ ΕΙΣΟΔΟΥ, όχι ΠΕΔΙΑ ΤΟΥ ΜΟΝΤΕΛΟΥ.** Και η τρίτη πρόταση
(*«η φόρμα μικραίνει όσο δίνεις»*) περιγράφει **δυναμική**, δηλαδή ρητά **προβλέπει** ότι το σύνολο
των γνωστών χαρακτηριστικών μεγαλώνει ενώ το σύνολο των ερωτήσεων μικραίνει.

⇒ **Το ADR-842 δεν ανατρέπει το §25.6. Το εκπληρώνει.** Η `/offers/new` μένει **ακριβώς 8 πεδία**,
για πάντα.

---

## 4. Η έρευνα — τι κάνουν πράγματι οι μεγάλοι παίκτες

Ζητήθηκε ρητά από τον Giorgio. Ευρήματα *(2026-09-02)*:

| Παίκτης | Πρακτική |
|---|---|
| **Airbnb** | *«publish first, then improve»* — μετά τη δημιουργία ο οικοδεσπότης οδηγείται στο **Today tab** να προσθέσει/επιβεβαιώσει ό,τι λείπει· ο εμπλουτισμός (κανόνες σπιτιού, ημερολόγιο, πολιτική ακύρωσης) γίνεται **μετά** τη δημοσίευση |
| **Zillow** | **Content Completion Score** στο Builder Dashboard — μετρά πληρότητα και επιστρέφει *insights* για βελτίωση |
| **Zillow / Trulia** | **Computer vision** που αναγνωρίζει χαρακτηριστικά **από φωτογραφίες** (ξύλινα δάπεδα, γαλλικά παράθυρα, πισίνες, θέα) και τα δένει με το Zestimate |
| **Spitogatos** | Εργαλεία **προβολής** (featured listings, visibility boost) — το κίνητρο είναι **κατάταξη**, όχι δικαίωμα ύπαρξης |
| **LinkedIn / Google Business Profile** | Το ίδιο μοτίβο έξω από τα ακίνητα: σταθερά βάρη ανά πεδίο, τα ελλείποντα εμφανίζονται ως **προτροπές**, ποτέ ως φράγμα |

📊 Ακαδημαϊκή επιβεβαίωση ότι τα χαρακτηριστικά **μετράνε**: κατά NBER w25174, η οπτική πληροφορία
εξηγεί **11,7%** της διακύμανσης τιμών (location-residualized).

### 4.1 🏆 Πού ο Νέστωρ μπορεί να τους **ξεπεράσει** — και δεν είναι φιλοδοξία, είναι **περιουσιακό στοιχείο**

Οι μεγάλοι **μαντεύουν** χαρακτηριστικά από **εικόνες**. Πιθανολογικά, με θόρυβο, χωρίς εγγύηση.

**Ο Νέστωρ έχει DXF/BIM/3D — γεωμετρία, όχι εικόνα.** Από μια κάτοψη τα εξής **υπολογίζονται με
βεβαιότητα**: εμβαδά (μικτό/καθαρό/μπαλκόνι/βεράντα), αριθμός δωματίων, μπάνια/WC, προσανατολισμός
από τον βορρά του σχεδίου, επίπεδα μεζονέτας.

⇒ Ένας ιδιώτης που ανεβάζει κάτοψη **κερδίζει χαρακτηριστικά χωρίς να απαντήσει καμία ερώτηση**.
Αυτό είναι **κυριολεκτικά** το *«ποτέ ό,τι μπορούμε να υπολογίσουμε»* της Α14 §17.2 — ο κανόνας του
έργου, εκτελεσμένος αντί για δηλωμένος.

---

## 5. Η απόφαση

**Α1 — Η αρχή.** *Κάθε χαρακτηριστικό κουβαλά την **προέλευσή** του, και η φόρμα εισόδου ρωτά **μόνο**
ό,τι καμία άλλη πηγή δεν μπορεί να απαντήσει.*

**Α2 — Η `/offers/new` δεν αλλάζει.** Μένει **ακριβώς 8 πεδία** (§3). Τα χαρακτηριστικά ζουν σε
**ξεχωριστή διαδρομή** («Βελτίωσε την αγγελία σου»), προσβάσιμη μόνο **μετά** τη δημοσίευση.
*(Απόφαση Giorgio: ο δείκτης εμφανίζεται μετά τη δημοσίευση, όχι μέσα στη φόρμα.)*

**Α3 — Η προβολή πρώτα, η φόρμα μετά.** Πρώτα φεύγουν δημόσια τα **ήδη υπάρχοντα** δεδομένα των
εταιρειών (§2), ώστε ο ιδιώτης να **βλέπει τι κερδίζει** πριν του ζητηθεί οτιδήποτε.
*(Απόφαση Giorgio: «ναι — αυτό είναι το ζητούμενο».)*

**Α4 — Κανένα νέο λεξιλόγιο τιμών.** Οι κατάλογοι **υπάρχουν ήδη**
*(`features/property-details/components/property-fields-constants.ts:28-68`)* — προσανατολισμοί (8),
κατάσταση (4), ενεργειακή (8), θέρμανση (5), ψύξη (4), δάπεδα (5), κουφώματα (3), υαλοπίνακες (4),
εσωτερικά (7), ασφάλεια (6). **Προάγονται** σε `constants/`· δεν ξαναγράφονται.

**Α5 — Καμία δεύτερη μηχανή πληρότητας.** Το `constants/property-completion.ts` (ADR-287) είναι ήδη
καθαρή, ντετερμινιστική μηχανή με βάρη ανά τύπο και buckets. Τα **17** `FIELD_KEYS` του
*(`constants/field-completion-weights.ts:47-65`)* ταιριάζουν σχεδόν 1:1 με τις τέσσερις οικογένειες
που επέλεξε ο Giorgio. Ο ιδιώτης αποκτά **adapter**, όχι μηχανή.

**Α6 — Πέμπτο λεξιλόγιο προέλευσης, τεκμηριωμένο με το κριτήριο του `location-provenance.ts`.**
Το έργο έχει **τέσσερα**, καθένα για δικό του ερώτημα *(και το `lib/location/location-provenance.ts:28-40`
δηλώνει ρητά γιατί δεν επεκτείνονται μεταξύ τους — «το σχήμα του ADR-749»)*:

| Υπάρχον | Ερώτημα που απαντά |
|---|---|
| `FieldProvenance` *(`types/project-building-code.ts:30`)* | από πού ήρθε ένας **όρος δόμησης** |
| `SurveyFieldProvenance` *(`types/project-survey-record.ts:45`)* | ποιος έγραψε ένα **πεδίο καρτέλας** |
| `ProvenanceActivity` *(`types/vqe/envelope.ts`)* | ποιος **υπολογισμός** παρήγαγε έναν αριθμό |
| `LocationProvenance` *(`lib/location/location-provenance.ts`)* | από πού ήρθε η **θέση** |

Κανένα δεν απαντά *«από πού ξέρουμε αυτό το **χαρακτηριστικό**;»*. Άρα:

```
AttributeProvenance = 'measured' | 'declared' | 'inferred'
```

- `measured` — υπολογισμένο από DXF/BIM. **Βεβαιότητα**, όχι πρόβλεψη.
- `declared` — το είπε άνθρωπος.
- `inferred` — προτάθηκε από συμφραζόμενα ή μοντέλο.

🔑 **Το σχήμα ΔΕΝ είναι νέο**: το `Sourced<T>` *(`types/project-survey-record.ts:64-69`)* είναι ήδη
generic *«τιμή που κουβαλά την προέλευσή της»*, γεννημένο από το ADR-759 §5.3 — *«πεδίο χωρίς
προέλευση είναι πεδίο όπου, σε έξι μήνες, κανείς δεν ξέρει αν το έγραψε άνθρωπος ή αρχείο»*.
Προάγεται· δεν αντιγράφεται.

**Α7 — Το `inferred` δεν γίνεται ΠΟΤΕ δημόσιο χωρίς επιβεβαίωση ανθρώπου.**
Εδώ ο Νέστωρ είναι **αυστηρότερος** από τη Zillow: ένα χαρακτηριστικό που μάντεψε μοντέλο μπορεί να
**προταθεί** στον κάτοχο, αλλά δεν φτάνει στον αγοραστή ως γεγονός. Η διαφορά `measured` vs
`inferred` υπάρχει ακριβώς για να είναι αυτός ο κανόνας **εκφράσιμος**.

---

## 6. ⚠️ ΜΗΝ

1. **ΜΗΝ** προσθέσεις χαρακτηριστικά στη φόρμα `/offers/new`. Είναι το χωνί εισόδου (Α2).
2. **ΜΗΝ** γράψεις δεύτερη λίστα τιμών για τον ιδιώτη (Α4, κανόνας N.0.2).
3. **ΜΗΝ** γράψεις δεύτερη μηχανή πληρότητας ή δεύτερο σύνολο βαρών (Α5).
4. **ΜΗΝ** χειρογράψεις το `LISTING_ATTRIBUTE_KEYS` — **παράγεται** από το `LISTING_DISCLOSURE`
   *(`lib/listings/listing-disclosure.ts:213-226`)*.
5. **ΜΗΝ** ανεβάσεις το `PUBLIC_LISTING_SCHEMA_VERSION` χωρίς **νέο, αμετάβλητο** κρίκο
   `LISTING_MIGRATIONS` στο ίδιο commit (ADR-839 §5). Και ποτέ επεξεργασία παλιού κρίκου.
6. **ΜΗΝ** χρησιμοποιήσεις έλεγχο αληθοφάνειας σε αριθμητικό χαρακτηριστικό: **`0` είναι ΤΙΜΗ** —
   `bedrooms: 0` = γκαρσονιέρα, `floor: 0` = ισόγειο *(`listing-disclosure.ts:244-260`)*.
7. **ΜΗΝ** δείξεις όλες τις ελλείψεις μαζί. Το `FormIssues` *(`DraftFormShell.tsx:175`)* δείχνει
   **τα πάντα πάντα** — σωστό για **φράγματα**, λάθος για **coaching**. Προτάσεις **3 τη φορά**.
8. **ΜΗΝ** τρέξεις rebuild δημόσιων προβολών από πράκτορα. Είναι **λειτουργική πράξη ανθρώπου**
   (ADR-839 §7): `/api/admin/rebuild-public-listings`.
9. **ΜΗΝ** επεκτείνεις το `OwnerPropertyFields.tsx` (ήδη 18KB) — νέα αρχεία στο `form/` (N.7.1).

---

## 7. Το σχέδιο, σε φάσεις

**Σειρά: Φ0 → Φ1 → Φ2 → Φ3 → Φ5 → Φ4 → Φ6.**

| Φάση | Τι | Κατάσταση |
|---|---|---|
| **Φ0** | Αυτό το έγγραφο + η γραμμή στο `owner-property.ts` + γραμμή στο `adr-index.md` | ✅ **2026-09-02** |
| **Φ1** | Η αυθεντία του λεξιλογίου + άγκυρα + φρουρός στο `.ssot-registry.json` | ✅ **2026-09-02** *(§7.2)* |
| **Φ2** | `AttributeProvenance` + `SourcedAttribute<T>` + ο κανόνας του Α7 σε κώδικα | ✅ **2026-09-02** *(§7.3)* |
| **Φ3** | `PublicListing` + `LISTING_DISCLOSURE` + migration + `ListingAttributeList` | ✅ **2026-09-02** *(§7.4)* |
| **Φ5** | Μεταφραστής **`PublicListing` → `CompletionFormSlice`** + δείκτης στο `OwnerPropertyView` | ✅ **2026-09-02** *(§7.5)* |
| **Φ4** | Η οθόνη «Βελτίωσε την αγγελία σου» + τα πεδία στο `OwnerProperty` | ⏳ |
| **Φ6** | `measured` — DXF/BIM → χαρακτηριστικά *(δένει με ADR-841 Φ8)* | ⏳ |

**Γιατί η Φ3 πριν τη Φ4**: ξεκλειδώνει τα **ήδη υπάρχοντα** δεδομένα (§2) — αξία από την πρώτη μέρα,
μηδέν νέα φόρμα. **Γιατί η Φ5 πριν τη Φ4**: πρώτα το κίνητρο, μετά η φόρμα (§4, μοτίβο Airbnb).

### 7.2 ✅ Φ1 — τι έγινε πραγματικά *(2026-09-02)*

**Το σχέδιο έλεγε «μετακίνησε τους καταλόγους σε `constants/`». Η μέτρηση βρήκε ότι το μισό
ήταν ήδη εκεί — και ότι το πραγματικό πρόβλημα ήταν άλλο.**

Οι **τύποι** ζούσαν ήδη κεντρικά (`constants/property-features-enterprise.ts`). Αυτό που
ζούσε feature-local ήταν οι **λίστες τιμών** — και ήταν **χειρόγραφη επανάληψη** των ίδιων
τιμών: `type HeatingType = 'central' | …` εδώ, `HEATING_OPTIONS: HeatingType[] = ['central', …]`
εκεί. **Δύο τόποι, ένα λεξιλόγιο.**

🔴 **Μετρήθηκε ότι συμφωνούσαν, 10 στα 10.** Η διόρθωση δεν έγινε επειδή κάτι έσπασε — έγινε
επειδή **δεν μπορούσε να σπάσει θορυβωδώς**: ο τύπος `XType[]` δέχεται **υποσύνολο**, άρα
τιμή που προστίθεται στο λεξιλόγιο **δεν φτάνει ποτέ στο dropdown** και **κανένας
μεταγλωττιστής δεν παραπονιέται**. Ο χρήστης απλώς δεν βλέπει την επιλογή. *(Ίδιο σχήμα με
τις «δύο ελλιπείς λίστες που συμφωνούν μεταξύ τους» του `listing-disclosure.ts`.)*

**Η λύση αντιστρέφει την κατεύθυνση**: η **λίστα** έγινε η πηγή, ο **τύπος** παράγεται
(`type X = (typeof XS)[number]`) — το ιδίωμα που το έργο ήδη χρησιμοποιεί στο
`LISTING_DISCLOSURE → ListingAttributeKey`. Οι δέκα `*_OPTIONS` έγιναν **όψη**. Η απόκλιση
είναι πλέον **δομικά αδύνατη**, όχι απλώς ελεγμένη.

**Δεύτερο εύρημα, μέσα στην ίδια εργασία**: η πρώτη γραφή έδινε στις όψεις **την ίδια
αναφορά** με το λεξιλόγιο (`= ORIENTATIONS`). Επειδή το `Object.values()` τρέχει **μία φορά**
ανά module, ένα `.sort()` σε **οποιοδήποτε** dropdown θα μετέβαλλε **το ίδιο το λεξιλόγιο**
για ολόκληρη την εφαρμογή, με την αιτία αόρατη στο σημείο του σφάλματος. Διορθώθηκε με
`readonly` στην αυθεντία + `[...X]` στις όψεις ⇒ **σφάλμα μεταγλώττισης**, όχι περιστατικό.

| Απόδειξη | Μέτρηση |
|---|---|
| Άγκυρα `constants/__tests__/property-vocabulary-authority.test.ts` | **79 tests** |
| **Μετάλλαξη** *(η άγκυρα ΜΠΟΡΕΙ να κοκκινίσει — CHECK 3.54)* | **2/2 πιάστηκαν**: λίστα που χάνει τιμή **και** λίστα ίδιου μήκους με **αλλαγμένη σειρά** *(τη δεύτερη ένας έλεγχος συνόλου θα την έχανε)* |
| Παλινδρόμηση σε όλο το `src/constants/__tests__/` | **272/272 πράσινα**, 12 suites |
| **Φρουρός pre-commit** — νέο `forbiddenPattern` στο module `property-features-enterprise` | **απόδειξη ζωής 8/8** στη **μηχανή της πύλης** (ECMAScript, όχι `grep` — ADR-749): 4 μορφές παλινδρόμησης πιάνονται, 4 νόμιμες μορφές περνούν |
| Σάρωση υπάρχοντος κώδικα με τον νέο φρουρό | **15.789 αρχεία, 0 παραβιάσεις** — δεν σπάει τίποτα |
| `npm run jscpd:diff` στα 4 αρχεία (N.18) | **0 νέοι κλώνοι** |

⚠️ **Το `X_OPTIONS` κράτησε το ΟΝΟΜΑ του επίτηδες**: πέντε καταναλωτές το εισάγουν
*(`PropertyFieldsEditForm`, `PropertyFieldsDetailCards`, `…Row2`, `LinkedSpacesCard`,
`PropertyDetailsContent`)*. Έφυγε η **αυθεντία**, όχι το όνομα — μηδέν αλλαγή στα σημεία κλήσης.

⛔ **Τι ΔΕΝ έγινε, δηλωμένα**: τα `COMMERCIAL_STATUS_OPTIONS` και `OPERATIONAL_STATUS_OPTIONS`
στο ίδιο αρχείο παραμένουν χειρόγραφα. Είναι **άλλο** λεξιλόγιο με **δικό του** module
(`commercial-statuses`), και η ένωσή τους εδώ θα ήταν το λάθος που το ADR-806 §7 #2 ονομάζει
**ομωνυμία, όχι διπλότυπο**.

### 7.3 ✅ Φ2 — τι έγινε πραγματικά, και **πού έπεσε έξω το σχέδιο** *(2026-09-02)*

🔴 **Το σχέδιο έλεγε «προάγεται το `Sourced<T>`». Η μέτρηση είπε ΟΧΙ, και ο κώδικας νίκησε**
*(N.0.1: «CODE = SOURCE OF TRUTH»)*.

Δύο ευρήματα το ανέτρεψαν:

1. **Το `Sourced<T>` δεν είναι καθολικό — είναι survey-specific.** 17 αρχεία, ~150 χρήσεις,
   **όλες** στον κόσμο του τοπογραφικού. Το `provenance` του είναι σκληρά δεμένο στο
   `SurveyFieldProvenance`, και το `rawText` («το κείμενο όπως εμφανίστηκε») δεν έχει
   αντίστοιχο σε μετρημένο χαρακτηριστικό.
2. **Υπάρχει ΗΔΗ δεύτερο δοχείο, και δεν επαναχρησιμοποίησε το πρώτο.** Το
   `lib/location/location-provenance.ts:247` έχει δικό του `Attested<T>`
   (`value`/`source`/`attestedAt`). Δηλαδή **το precedent του έργου είναι ότι κάθε τομέας
   προέλευσης κρατά δικό του σχήμα** — δεν είναι παράλειψη, είναι η υπάρχουσα πρακτική.

**Και ο τρίτος λόγος είναι ο αποφασιστικός**: ένα κοινό δοχείο θα έκανε όλα τα ειδικά πεδία
**προαιρετικά** — δηλαδή θα επέτρεπε `inferred` **χωρίς** `confirmedAt` να περάσει τη
μεταγλώττιση. **Αυτό ακριβώς απαγορεύει το Α7.** Γι' αυτό: **διακριτή ένωση**, όπου ο τύπος
**επιβάλλει** τα πεδία κάθε σκέλους (`measured` → `sourceRef`· `inferred` → `confidence` +
`confirmedAt: string | null`, **ποτέ optional**).

**Νέο**: `src/lib/property/attribute-provenance.ts` — λεξιλόγιο, κατάταξη, δοχείο, και ο
κανόνας του Α7 σε **μία** εκτελέσιμη θέση (`isPubliclyPresentable`).

🔑 **Η κατάταξη ΕΙΝΑΙ ο ορισμός**: `AttributeProvenance = keyof typeof ATTRIBUTE_PROVENANCE_RANK`
⇒ **δεν μπορεί να υπάρξει προέλευση χωρίς δηλωμένη βαθμίδα**. Ίδιο ιδίωμα με το
`LOCATION_PROVENANCE_RANK` και συνεπές με το Α4 της Φ1 (η λίστα είναι η πηγή).

🔴 **`inferred` **κάτω** από `declared`, και είναι απόφαση**: ένα μοντέλο που «βλέπει» τζάκι
σε φωτογραφία δεν σβήνει τη δήλωση του ιδιοκτήτη ότι δεν υπάρχει. Ίδιο σκεπτικό με το
`geocoded` της θέσης, που μπήκε χαμηλότερα από το `manual` ώστε αυτόματη γεωκωδικοποίηση να
μη σβήνει πινέζα ανθρώπου.

| Απόδειξη | Μέτρηση |
|---|---|
| Άγκυρα `lib/property/__tests__/attribute-provenance.test.ts` | **17 tests** |
| **Μετάλλαξη** | **3/3 πιάστηκαν** — Μ1 *το Α7 αγνοεί την έγκριση* (2 κόκκινα) · Μ2 *ισοβαθμία περνά* (2) · Μ3 *το μάντεμα νικά τη δήλωση* (4) |
| `npm run jscpd:diff` (N.18) | **0 νέοι κλώνοι** |

⚠️ **Το `confidence: 0.999` ΔΕΝ φεύγει δημόσια** — υπάρχει ρητό test. «Σχεδόν σίγουρο» δεν
είναι «κάποιος το είδε», και εδώ ο Νέστωρ είναι **αυστηρότερος** από Zillow/Trulia που
δημοσιεύουν συμπεράσματα computer-vision αυτούσια.

⛔ **Τι ΔΕΝ έγινε, δηλωμένα**: κανένα υπάρχον πεδίο δεν τυλίχθηκε ακόμη σε `SourcedAttribute`.
Το δοχείο **υπάρχει και ελέγχεται**, αλλά αποκτά καταναλωτές στη **Φ4**. *(Δηλώνεται επειδή
«υπάρχει τύπος» ≠ «χρησιμοποιείται» — φρουρός χωρίς απόδειξη ζωής, ADR-749 §5.)*

### 7.4 ✅ Φ3 — τι έγινε πραγματικά *(2026-09-02)*

**Το φράγμα του §2 έσπασε: η δημόσια αγγελία πέρασε από 4 σε 27 στοιχεία, χωρίς να ρωτηθεί
κανένας άνθρωπος τίποτα.** Η `/offers/new` έμεινε **ακριβώς 8 πεδία** (Α2), όπως υποσχέθηκε.

#### Η απόφαση του §8 #4 — πάρθηκε από τον Giorgio, με μέτρηση από πίσω

Επιλέχθηκε **(β)+(γ) μαζί**: νέος ρόλος αποκάλυψης **`'feature-set'`** για τα **σύνολα**, και
**ομαδοποίηση** των μονών πεδίων στην οθόνη. Ο λόγος **δεν** είναι διάταξη — είναι ότι το
`'attribute'` ξέρει **δύο** καταστάσεις και τα σύνολα έχουν **τρεις**:

| Τιμή | Σημασία | Τι λέει η οθόνη |
|---|---|---|
| `null` | **κανείς δεν ρώτησε** — δικό μας χρέος | «Δεν έχει δηλωθεί» |
| `[]` | ο κάτοχος **απάντησε «καμία»** | «Δηλώθηκε ότι δεν υπάρχουν» |
| `[…]` | υπάρχουν | οι ίδιες οι τιμές |

🏆 **Η μεσαία γραμμή είναι δομικά ανέκφραστη σε Zillow · idealista · Spitogatos**: εκεί το
«δήλωσα ότι δεν έχω παροχές» και το «δεν με ρώτησε κανείς» καταλήγουν στην **ίδια σιωπή**. Η
διαφορά είναι **εμπορική**, όχι φιλολογική: *«καμία παροχή»* είναι πληροφορία τιμής,
*«άγνωστο»* δεν είναι. Ίδιο σχήμα με το `never-asked` vs `owner-declined` της θέσης (Α5 §3) —
δηλαδή ο κανόνας του έργου, εφαρμοσμένος μία βαθμίδα πιο βαθιά.

#### Η παρουσίαση — και γιατί **δεν** είναι συμβιβασμός

Ο κανόνας της οθόνης 3 (*«κάθε στοιχείο φαίνεται, με τιμή ή με **ονομασμένη απουσία**»*) είναι
ό,τι την κάνει καλύτερη από κάθε portal. **Σε 27 πεδία όμως γίνεται το αντίθετό του**: ~21
συνεχόμενες γραμμές «δεν έχει δηλωθεί» θάβουν τα γεγονότα και διαβάζονται ως **κατηγορητήριο
κατά του κατόχου**.

Η λύση: **το ισοζύγιο ταξιδεύει ΜΑΖΙ με την ομάδα.** Κάθε ομάδα τυπώνει **πάντα** το δικό της
κλειστό «2 από 3», δείχνει τα δηλωμένα, και κρατά τα κενά πίσω από **μία** ενέργεια που τα
**μετρά στην ετικέτα της** (*«5 δεν έχουν δηλωθεί»*), με το περιεχόμενο **παρόν στο DOM** πίσω
από `hidden`.

| | Zillow / idealista | Επίπεδη λίστα 27 γραμμών | **Νέστωρ** |
|---|---|---|---|
| Ξέρω **πόσα** λείπουν; | ❌ ποτέ | ✅ | ✅ **πάντα ορατό** |
| Ξέρω **ποια** λείπουν; | ❌ ποτέ | ✅ | ✅ με **ένα** κλικ |
| Διαβάζονται τα γεγονότα; | ✅ | ❌ θάβονται | ✅ |

📐 **Έρευνα, όχι γούστο**: Nielsen Norman Group — *«designs that go beyond 2 disclosure levels
typically have low usability»* (άρα **ακριβώς δύο**, και η κατάσταση είναι **ανά ομάδα**, ποτέ
καθολικός διακόπτης) και *«the progression must have strong information scent»* (άρα το κουμπί
λέει **πόσα** και **τι κατάστασης**, ποτέ «Περισσότερα»). Το ίδιο ιδίωμα έχουν τα εργαλεία που
ο Giorgio έθεσε ως πήχη: η παλέτα του **Revit** ομαδοποιεί σε *Constraints · Dimensions ·
Identity Data* και **δεν εξαφανίζει ποτέ** τη γραμμή κενής παραμέτρου· ο Attribute Manager του
**Cinema 4D** χωρίζει σε καρτέλες ανά ομάδα.

#### 🔴 Τρία σημεία όπου το σχέδιο έπεσε έξω, και ο κώδικας νίκησε

**1. Η «αντιγραφή πεδίων» ήταν στην πραγματικότητα ΚΡΙΣΗ.** Το `ProjectableProperty` είναι
**δομικός** τύπος πάνω σε **ωμό έγγραφο Firestore**: τα λεξιλογικά πεδία είναι `string`, όχι
`ConditionType`. Ένα `property.condition as ConditionType` θα έστελνε ένα παλιό `'Καλή'`
αυτούσιο στο κλειστό σχήμα, η οθόνη θα ζητούσε `properties-enums:condition.Καλή`, και θα
ζωγράφιζε **ωμό κλειδί στον ανώνυμο επισκέπτη** — η οικογένεια που το repo έχει πληρώσει
τέσσερις φορές. ⇒ **κάθε** λεξιλογική τιμή επαληθεύεται έναντι της αυθεντίας της **Φ1**, και
ό,τι δεν αναγνωρίζεται γίνεται `null`. Έτσι η Φ1 απέκτησε τον **πρώτο πραγματικό καταναλωτή
της**: οι κατάλογοι έπαψαν να είναι μόνο dropdown — είναι το **φίλτρο ανάμεσα στη βάση και
στον κόσμο**.

**2. Το `[]` δεν είναι «άδειο», είναι ΙΣΧΥΡΙΣΜΟΣ.** Ο κρίκος μετανάστευσης γράφει **`null`**
στα σύνολα, ποτέ `[]` — αλλιώς θα δήλωνε *«ο κάτοχος είπε: καμία παροχή»* για **κάθε παλιό
έγγραφο ταυτόχρονα**. Η ίδια απόφαση και στην προβολή: ένα `['ξύλο']` (μη κενό, κανένα μέλος
ονομάσιμο) γίνεται **`null`** και όχι `[]`.

**3. Η πύλη είχε δίκιο για το i18n, και η θεραπεία ήταν ΜΕΤΑΚΟΜΙΣΗ.** Η πρώτη γραφή έβαλε τις
ετικέτες στο `search-results` — που είναι **εγγυημένο namespace του κελύφους**, δηλαδή
ταξιδεύει **ολόκληρο σε 141 διαδρομές**. Μετρήθηκε **13.095 → 14.670 bytes**, όριο **14.400**.
Το μητρώο απαγορεύει ρητά την αύξηση αριθμού (*«η θεραπεία είναι μετακόμιση»*). ⇒ ολόκληρο το
`detail.attributes.*` μετακόμισε σε **νέο per-route namespace `listing-detail`** με **έναν**
καταναλωτή. **Το κέλυφος έπεσε στα 12.801** — δηλαδή η Φ3 άφησε κάθε διαδρομή της εφαρμογής
**294 bytes ελαφρύτερη απ' ό,τι τη βρήκε**.

#### ⚠️ Ένα εύρημα που **δεν** προκάλεσε η Φ3, και δηλώνεται

Το ταβάνι της `/listing/[id]` ήταν **ήδη σπασμένο**: μετρήθηκε 2026-09-02, με **όλες** τις
αλλαγές της Φ3 αφαιρεμένες, στα **6.664 bytes** έναντι ταβανιού **5.935**. Η σφράγιση της
31/08 είχε παλιώσει σιωπηλά (δεν ξαναπαράχθηκε το slice μετά την ADR-841). Από τα **+4.433**
της νέας σφράγισης, τα **1.916 δεν ανήκουν στη Φ3**. Γραμμένο στο `why` της σφράγισης.

#### 🔬 ΖΩΝΤΑΝΗ ΕΠΑΛΗΘΕΥΣΗ — και τι βρήκε

Τα tests της οθόνης 3 ζωγραφίζουν με **mock** του `useTranslation` (το `t` επιστρέφει το
κλειδί). Αυτό είναι σωστό για *«ποια ερωτήματα κάνει η οθόνη;»* και **τυφλό** για *«τα
βρίσκει ο runtime;»* — ανάμεσα στο κλειδί και στο locale στέκονται το `Namespace` του
`lazy-config`, το `case` του `namespace-loaders` και το πρόθεμα `ns:` της κλήσης, και
**κανένα δεν εκτελείται**. Η Φ3 έκανε ακριβώς την κίνηση που το γεννά: μετακίνησε κλειδιά
από **εγγυημένο** namespace κελύφους σε ολοκαίνουριο **lazy-loaded** namespace, στη
**δημόσια πρώτη επαφή**.

**Τι έγινε πραγματικά** *(2026-09-02, dev server σε απομονωμένη θύρα)*:

| Έλεγχος | Αποτέλεσμα |
|---|---|
| Χρησμός **CHECK 3.51** στις διαδρομές `listing` | **0 ωμά κλειδιά** · `/test-harness/listing-shapes` **clean** |
| `GET /listing/<πραγματικό id>` | **200**, SSR ζωγραφίζει «Φόρτωση αγγελίας…» **μεταφρασμένο** |
| Firestore `public_listings` | 🔴 **4 στα 5 έγγραφα είναι `schemaVersion: 3`** — δεν έχουν ούτε το `gallery` της ADR-841. Η ανάγνωση τρέχει **δύο** κρίκους (4 **και** 5) |

🔴 **Και ένα ψευδώς θετικό που έπρεπε να διαλυθεί**: ο server που έτρεχε ήδη στη θύρα 3000
επέστρεφε **500** για κάθε `/listing/*`. Δεν ήταν ο κώδικας — ήταν **πεσμένος render worker**
(*«Jest worker encountered 2 child process exceptions»*): σε καθαρή θύρα η ίδια διαδρομή
απαντά **200**. Η μεταγλώττιση της σελίδας θέλει **149 δευτερόλεπτα**, που εξηγεί γιατί ο
worker πέθανε σε μηχάνημα 4 πυρήνων με δύο συνεδρίες. *Καταγράφεται επειδή ένας 500 σε
δημόσια σελίδα είναι ακριβώς αυτό που κυνηγούσε η επαλήθευση, και η αιτία **δεν** ήταν εκεί
που φαινόταν.*

⛔ **Η οθόνη ΔΕΝ φωτογραφήθηκε σε browser, και το λέω**: η σελίδα δεν φτάνει ποτέ σε
`window.load` στο dev (ο χάρτης κρατά ανοιχτό αίτημα), οπότε ο αυτοματισμός δεν μπορεί να
τραβήξει στιγμιότυπο. Αντί για μια στιγμιαία εικόνα, γράφτηκε **μόνιμη άγκυρα** που ζωγραφίζει
την κάρτα με τους **πραγματικούς loaders** και το **πραγματικό ICU**
(`listing-attributes-runtime.test.tsx`, **9 tests**) και ρωτά *«φαίνονται ελληνικά;»*:
«Στοιχεία ακινήτου» · «8 από 27 στοιχεία δηλωμένα» · «Καλή» · «Αυτόνομη» · «Τζάκι» ·
**«Ισόγειο»** *(το `floor: 0`)* · **«Δηλώθηκε ότι δεν υπάρχουν»** · «9 δεν έχουν δηλωθεί».

🔑 **Και είναι ισχυρότερη από στιγμιότυπο**: η μετάλλαξη **Μ8** *(σβήσε το `case` του loader)*
κάνει **και τις 9 κόκκινες** — δηλαδή η άγκυρα **εκτελεί** το wiring που το CHECK 3.36 ελέγχει
μόνο στατικά, και θα το κάνει σε κάθε επόμενη αλλαγή, όχι μία φορά.

#### 📐 Η ΧΑΡΤΟΓΡΑΦΗΣΗ ΕΠΑΛΗΘΕΥΤΗΚΕ ΣΕ **ΠΡΑΓΜΑΤΙΚΑ** ΕΓΓΡΑΦΑ — όχι σε fixtures

Ο αναγνώστης του `public-listing-attributes.ts` **υποθέτει σχήμα** (`layout.bathrooms` ·
`areas.net` · `systemsOverride.heatingType` · `energy.class` · `finishes.flooring` ·
`propertyAmenities`). Αν τα ζωντανά έγγραφα τα κρατούσαν αλλού, η προβολή θα έβγαζε
**σιωπηλά μηδέν** με **όλα τα tests πράσινα** — το σχήμα *«0 = κανείς δεν κοίταξε»*, στην πιο
ύπουλη μορφή του. Μετρήθηκαν **8 έγγραφα `properties`** *(2026-09-02)*:

| Πεδίο | Ζωντανή πραγματικότητα |
|---|---|
| `layout.{bedrooms,bathrooms,wc}` | ✅ **ακριβώς όπως διαβάζεται**, και στα 8 |
| `areas.{gross,net,balcony,terrace,garden}` | ✅ **ακριβώς όπως διαβάζεται** — και το `garden: 0` **υπάρχει ζωντανά**, δηλαδή η διαδρομή *«το `0` είναι ΤΙΜΗ»* δεν είναι θεωρητική |
| `orientations` | ✅ πίνακας στη **ρίζα**, όπως διαβάζεται |
| `condition` · `renovationYear` · `energy` · `systemsOverride` · `finishes` · `interiorFeatures` · `securityFeatures` · `propertyAmenities` | ❌ **απόντα σε ΟΛΑ τα 8** |

⇒ **Η χαρτογράφηση είναι σωστή** — και ταυτόχρονα **12 από τα 23** πεδία θα μείνουν `null`
**ακόμη και μετά** την ανακατασκευή, επειδή κανείς δεν τα συμπλήρωσε ποτέ. Αυτό **δεν** είναι
βλάβη της Φ3· είναι η **μέτρηση του πραγματικού αποθέματος**, και είναι ακριβώς η πληροφορία
που χρειάζεται η **Φ5** (δείκτης πληρότητας) για να μην υποσχεθεί κάτι που δεν υπάρχει.

| Απόδειξη | Μέτρηση |
|---|---|
| Νέες άγκυρες | `listing-attribute-groups.test.ts` (**13**) · `public-listing-attributes.test.ts` (**14**) · `listing-attribute-value.test.ts` (**127**) · `listing-attributes-runtime.test.tsx` (**9**, πραγματικός i18n) · Κ7γ · Κ8 σχήματος (**3**) |
| **Μεταλλάξεις** *(η άγκυρα ΜΠΟΡΕΙ να κοκκινίσει — CHECK 3.54)* | **10/10 πιάστηκαν** — Μ1 *το ασαφές σύνολο βαφτίζεται «καμία»* (1 κόκκινο) · Μ2 *το `[]` γίνεται «κανείς δεν ρώτησε»* (2) · Μ3 *ομάδα χάνει τα σύνολά της* (5) · Μ4 *ο κριτής λεξιλογίου δέχεται κάθε συμβολοσειρά* (3) · Μ5 *το locale χάνει τις παροχές* (7) · Μ6 *ο κρίκος γράφει `[]` αντί για `null`* (1) · Μ7 *η ενέργεια αποκάλυψης μετρά ΟΛΑ αντί για τα κενά* (1) · **Μ8** *σβήνει το `case` του loader* (**9/9**) · Μ9 *η ετικέτα ζητιέται από λάθος namespace* (1) · Μ10 *σπάει η παράμετρος ICU* (2) |
| Παλινδρόμηση | **1.647/1.647 πράσινα** (+ **690/690** μετά τη ζωντανή επαλήθευση), 85 suites (listings · services · οθόνη 3 · ζήτηση · εντολές · constants · i18n · διευθύνσεις) |
| Πύλες | **3.74** ✅ · **3.8** ✅ · **3.33** ✅ · **3.34** ✅ *(Κ2 25 εντός / 0 εκτός)* · **3.36** ✅ · **3.71** ✅ · **3.28** ✅ *(0 νέοι κλώνοι, 16 αρχεία)* |
| N.7.1 | κάθε νέο αρχείο **≤246 γραμμές**, κάθε συνάρτηση **≤40** |

⛔ **Τι ΔΕΝ έγινε, δηλωμένα**:
- **Ο ιδιώτης δίνει `null` σε όλα τα 23** — ο `OwnerProperty` δεν έχει τα πεδία, και **δεν
  πρέπει** να τα αποκτήσει στη φόρμα (Α2). Έρχονται στη **Φ4**. Γραμμένο ρητά στο
  `owner-property-projection.ts` αντί να υπονοηθεί.
- **Κανένα `SourcedAttribute` δεν χρησιμοποιήθηκε** — τα δεδομένα των εταιρειών δεν κουβαλούν
  προέλευση σήμερα. Το δοχείο της Φ2 αποκτά καταναλωτές στη Φ4/Φ6, όπως δηλώθηκε εκεί.
- **Καμία ανακατασκευή προβολών** — είναι **πράξη ανθρώπου** (ADR-839 §7). Μέχρι να τρέξει το
  `/api/admin/rebuild-public-listings`, τα ζωντανά έγγραφα ανεβαίνουν **στην ανάγνωση** (lazy
  migration) και δείχνουν «δεν έχει δηλωθεί» — που είναι η **αλήθεια** μέχρι την ανακατασκευή.
- **Δύο ευρήματα N.0.2 καταγράφηκαν αντί να διορθωθούν** *(`.claude-rules/pending-ratchet-work.md`)*:
  **13 χειρόγραφοι type guards** για το ίδιο ερώτημα «ανήκει στο λεξιλόγιο;» *(η Φ3 δεν πρόσθεσε
  14ο — έγραψε **έναν** γενικό)*, και οι **παροχές που φεύγουν ωμές σε PDF · email · Telegram**
  *(η μισή θεραπεία — τα κλειδιά — μπήκε δωρεάν σε αυτή τη φάση)*.

### 7.5 ✅ Φ5 — τι έγινε πραγματικά *(2026-09-02)*

**Ο ιδιώτης βλέπει πλέον πόσο πλήρης είναι η αγγελία του και τι να κάνει μετά — με
μηδέν νέα μηχανή, μηδέν νέα βάρη και μηδέν νέα κλειδιά i18n.**

#### 🔴 Το σχέδιο έλεγε λάθος είσοδο, και η μέτρηση το ανέτρεψε **προς τα πάνω**

Το §7 υποσχόταν *«Adapter **`OwnerProperty`** → `CompletionFormSlice`»*. Ο `OwnerProperty`
έχει **8** πεδία· μετά τη Φ3 το **`PublicListing` έχει 27**, και **και οι δύο** οικογένειες
περνούν ήδη από **μία** προβολή. Μετρήθηκε ότι από τα **17** `FIELD_KEYS` της μηχανής:

| | Πλήθος | Ποια |
|---|---|---|
| ταυτόσημο όνομα στο `PublicListing` | **13** | `type` `bedrooms` `bathrooms` `orientations` `condition` `energyClass` `heatingType` `coolingType` `windowFrames` `glazing` `flooring` `interiorFeatures` `securityFeatures` |
| μετονομασία | **3** | `areaGross`→`areaSqm` · `areaNet`→`netAreaSqm` · `photos`→`gallery.length` |
| **δεν υπάρχει** | **1** | `floorplan` *(§8 #2)* |

⇒ Ο μεταφραστής είναι **σχεδόν ταυτοτικός**, και η είσοδος καλύπτει **και** τον ιδιώτη
**και** την εταιρεία με **μία** γραφή. Η Φ3 έκανε τη Φ5 φθηνή χωρίς να το ξέρει.

#### ⛔ Η δύσκολη απόφαση: ο μεταφραστής **ΔΕΝ ξαναερμηνεύει**

Η Φ3 έδωσε στα σύνολα **τρεις** καταστάσεις· η μηχανή του ADR-287 ξέρει **δύο**. Άρα
`securityFeatures: []` και `wc: 0` βαθμολογούνται ως **έλλειψη**, ενώ η **λογιστική** τα
μετρά ως **δηλωμένα**. Ο πειρασμός ήταν να σταλεί **δείκτης-φάντασμα** στην είσοδο.
**Απορρίφθηκε**, με τρεις λόγους:

1. θα ήταν **δεύτερη σημασιολογία πληρότητας από την πίσω πόρτα** — το **Α5** παραβιασμένο
   μέσω της **εισόδου** αντί μέσω νέου αρχείου·
2. **δεν γενικεύεται**: για το `wc: 0` δεν υπάρχει τίμιος δείκτης, και θεραπεία της μισής
   κλάσης αφήνει την άλλη μισή **αόρατη**·
3. η **ρίζα** είναι το `isArrayComplete`/`isPositiveNumber` της μηχανής, και το ίδιο ισχύει
   **εξίσου για την εταιρική διαδρομή**, από το ADR-287 και πριν.

⚠️ Και η προφανής διόρθωση **στη μηχανή** είναι επικίνδυνη, μετρημένα: `isArrayComplete([])`
θα μετρούσε ως πλήρες κάθε **άθικτο** multi-select της εταιρικής φόρμας (αρχικοποιείται σε
`[]`) ⇒ φούσκωμα του ποσοστού **κάθε** ακινήτου, αναδρομικά. ⇒ **ξεχωριστό ADR**,
καταγεγραμμένο· η απόκλιση **δηλώνεται σε άγκυρα** ώστε η μέρα που θα αλλάξει να
**κοκκινίσει** αντί να περάσει σιωπηλά.

🔑 **Και οι δύο αριθμοί είναι σωστοί, γιατί απαντούν ΔΙΑΦΟΡΕΤΙΚΑ ΕΡΩΤΗΜΑΤΑ**: η λογιστική
λέει στον **αγοραστή** *«πόσα στοιχεία έχουν απάντηση»*· ο δείκτης λέει στον **κάτοχο**
*«πόσο εμπορεύσιμη είναι η αγγελία σου»*.

#### 📐 Η κάτοψη είναι πάντα μηδέν — και το ταβάνι **μετρήθηκε** πριν γίνει δεκτό

Το `ListingImage` δεν έχει **είδος** (§8 #2), άρα ο τίμιος αριθμός κατόψεων είναι **0**.
Η κρίσιμη ερώτηση δεν είναι *«πειράζει;»* αλλά *«μένει ο δείκτης κερδίσιμος;»*:

| Τύπος | Παρονομαστής | Μέγιστο χωρίς κάτοψη | Πράσινο (≥71%); |
|---|---|---|---|
| διαμέρισμα | 23,0 | `21/23` = **91%** | ✅ |
| αποθήκη | 9,0 | `7/9` = **78%** | ✅ |

⇒ Κανείς δεν κλειδώνεται έξω. *(Αν δεν ίσχυε, η σωστή κίνηση θα ήταν **εξαίρεση** της
κάτοψης — όχι σιωπή.)* Και οι δύο αριθμοί είναι **εκτελέσιμοι** σε άγκυρα, όχι γραμμένοι.

#### 🔴 Τρία ανοιχτά θέματα έκλεισαν, και το ένα με **ανατροπή της υπόθεσής του**

**§8 #1 — ο ιδιώτης δεν έχει `operationalStatus`.** Νέο
`lib/property/property-completion-stance.ts`: εξαντλητικός
`Record<PropertyStatus, CompletionStance>`, **μία** γραμμή `pre-completion` *(«σύντομα
διαθέσιμο»)*, καμία ποτέ σε `'draft'`. Το επιχείρημα είναι **ασύμμετρο κόστος**: η εξαίρεση
αφαιρεί μόνο την **ποινή** (τα πεδία μένουν ορατά και δηλώσιμα), ενώ η μη-εξαίρεση ζητά
**ΠΕΑ που ο νόμος δεν εκδίδει** πριν την αποπεράτωση — coaching που δεν μπορεί να εκτελεστεί.

🔴 **Και εδώ ο κώδικας ανέτρεψε το ερώτημα**: το σχέδιο έλεγε *«το `commercialStatus:
'coming-soon'`»* — αλλά το `CommercialStatus` έχει **επτά** τιμές και το «coming-soon» **δεν
είναι** μία από αυτές· ζει στο **`PropertyStatus`** (δώδεκα τιμές). Και **όμως φτάνει** στη
δημόσια αγγελία, γιατί το `public-listing-projection.ts:307` το λαθρεμπορεύει με ωμό
`as CommercialStatus` πάνω στο `property.status`. ⇒ Ο πίνακας κλειδώθηκε στο **υπερσύνολο**,
γιατί ένας πίνακας πάνω στο `CommercialStatus` θα ήταν **φρουρός χωρίς απόδειξη ζωής**
(ADR-749 §5): γραμμή που ο μεταγλωττιστής απορρίπτει και `if` που δεν πυροδοτεί ποτέ. Το ίδιο
το `as` καταγράφηκε ως χρέος — η διόρθωσή του αγγίζει το `listing-filters.ts`.

**§8 #3 — ταίριασμα `PropertyType ⊆ PropertyTypeCanonical`.** Επαληθεύτηκε: το
`getFieldWeightsForType` έκανε **ακριβή** ταύτιση, οπότε **τέσσερις από τις εννέα** μη
κανονικές τιμές έπαιρναν **λάθος** πίνακα:

| Αποθηκευμένο | Σωστός | Έδινε | Συνέπεια |
|---|---|---|---|
| `'Αποθήκη'` | **9,0** | **23,0** *(+156%)* | ζητούσε **υπνοδωμάτια** από αποθήκη, ως **κρίσιμα** |
| `'Κατάστημα'` | **14,0** | **23,0** *(+64%)* | — |
| `'Στούντιο'` | 21,5 | 23,0 | υπνοδωμάτια `0,5` μη κρίσιμα → `2` **κρίσιμα** |
| `'Γκαρσονιέρα'` | 22,0 | 23,0 | — |

Θεραπεία: **ρωτά την υπάρχουσα αυθεντία** `normalizePropertyType` (κανένα βάρος δεν άλλαξε,
Α5). ⚠️ **Μηδέν αλλαγή για τα σημερινά δεδομένα, μετρημένη**: και τα **8** ζωντανά έγγραφα
έχουν κανονικό είδος ⇒ **λανθάνον, όχι ενεργό**.

**§8 #7 — τα επίπεδα της μεζονέτας.** Απόφαση Giorgio: **παράγωγο με προέλευση**. Το
`PublicListing.levels` έγινε **`SourcedAttribute<number> | null`** — το **πρώτο** πεδίο του
σχήματος που κουβαλά προέλευση, και ο **πρώτος πραγματικός καταναλωτής** του δοχείου της Φ2.

- **δήλωση** (`layout.levels`) ⇒ `declared` · **δομή** (`levels[]`) ⇒ `measured` με δείκτη πηγής·
- ⚠️ η **σειρά είναι αντίστροφη της κατάταξης** επίτηδες: η δήλωση είναι **ρητή απάντηση**, η
  δομή **παρενέργεια** της σύνδεσης με ορόφους. Το `preferStrongerAttribute` απαντά *«ποια
  νικά όταν ΚΡΑΤΑΜΕ και τις δύο»*· εδώ **παράγουμε** μία, και το ερώτημα είναι *«ποια ρωτάμε
  πρώτη»* — άλλο ερώτημα, γι' αυτό δεν καλείται εκείνη η συνάρτηση·
- **σχήμα 5 → 6** με **νέο, αμετάβλητο** κρίκο (ADR-839 §5)· το `at` είναι το `projectedAt`
  **του ίδιου του εγγράφου** *(μια μετανάστευση δεν έχει καλούντα να της δώσει ρολόι, και
  `new Date()` θα έλεγε «το μάθαμε τη στιγμή που το διαβάσαμε»)*· χωρίς σφραγίδα ⇒ **`null`**,
  δηλωμένη απώλεια μέχρι την ανακατασκευή· και ο κρίκος είναι **ιδιοδύναμος**·
- 🔴 **το Α7 απέκτησε δεύτερη εκτελέσιμη θέση** — το `isAttributeDeclared` για το `levels`
  **ρωτά** το `isPubliclyPresentable`, ώστε ανεπιβεβαίωτο `inferred` να μη φτάνει ποτέ στον
  αγοραστή. Καταναλωτής του κανόνα, **όχι** δεύτερη υλοποίηση.

⚠️ **Γιατί ΜΟΝΟ αυτό το πεδίο από τα 27**: είναι το **μόνο** με **δύο πηγές που μπορούν να
διαφωνήσουν**. Δοχείο προέλευσης πάνω σε πεδίο μίας πηγής θα απαντούσε πάντα το ίδιο —
τελετουργία, όχι εγγύηση. Τα υπόλοιπα το αποκτούν στη **Φ6**.

#### 🔑 Ο τύπος απαγορεύει πλέον το `[object Object]`

Το `'verbatim'` του `listing-attribute-value.ts` κάνει `String(listing[key])` — ασφαλές όσο
**κάθε** ιδιότητα ήταν βαθμωτή. Με το `levels` σε δοχείο, ένα `verbatim` **μεταγλωττίζεται
μια χαρά** και ζωγραφίζει `[object Object]` στον ανώνυμο επισκέπτη: η οικογένεια του ωμού
κλειδιού, από **άλλη πόρτα**. ⇒ Ο πίνακας έγινε **key-aware** (`AttributeValueKindFor`): μη
βαθμωτό κλειδί δέχεται **μόνο** `'custom'`, δηλαδή μόνο αν κάποιος απαντήσει *«πώς γίνεται
κείμενο;»*. Συν **εκτελέσιμη** άγκυρα σε **όλα** τα κλειδιά, γιατί ο μεταγλωττιστής δεν
τρέχει ως πύλη για πράκτορα (N.17).

#### 🎯 Η οθόνη: coaching, όχι κατηγορητήριο

`OwnerListingCompletion` — μπάρα, ποσοστό, βαθμίδα, και **τρεις** προτάσεις τη φορά (§6 #7),
τα **βαρύτερα** που λείπουν. Στέκεται **μετά** το δόλωμα του §12.6 και **πριν** το κουμπί
επεξεργασίας: το δόλωμα λέει *«N άνθρωποι ψάχνουν κάτι σαν το δικό σας»*, ο δείκτης λέει
*«να τι λείπει για να το βρουν»*, και μετά υπάρχει λόγος να πατηθεί το κουμπί.

- 🔑 **Είσοδος η ΔΗΜΟΣΙΑ αγγελία** μέσω `projectListingShape` *(το σχήμα **χωρίς** την πύλη)*
  — άρα ο δείκτης υπάρχει **και πριν** τη δημοσίευση, εκεί που το κίνητρο αξίζει περισσότερο.
  Δεύτερος καταναλωτής αυτής ακριβώς της διάσπασης· ο πρώτος ήταν το δόλωμα του §12.6.
- ⚠️ **Μηδέν νέα κλειδιά i18n, μετρημένο**: όλα τα κείμενα — τίτλος, οι τρεις βαθμίδες, **και
  τα 17** ονόματα πεδίων — ζουν ήδη στο `properties:completion.*` από το ADR-287.
- 🔑 **Δεν είναι το `PropertyCompletionBreakdown`**, που δείχνει **όλες** τις ελλείψεις: εκείνο
  είναι σωστό για τον **επαγγελματία** που ελέγχει καρτέλα, και λάθος για coaching.
- 🧹 **N.0.2**: οι δύο επιλυτές χρώματος βαθμίδας ήταν **ιδιωτικοί** στο
  `PropertyCompletionMeter`· **εξήχθησαν** σε μία αρχή αντί να αντιγραφούν.
- 🧹 Και το `jscpd` βρήκε **προϋπάρχοντες** κλώνους στο `field-completion-weights.ts`
  (`shop`⇄`office`, `hall`⇄`storage`) — καθαρίστηκαν με το **ιδίωμα του ίδιου του αρχείου**
  (`COMMERCIAL_FINISHES_AND_MEDIA` · `AUXILIARY_MATRIX`), με τα βάρη **αμετάβλητα**.

| Απόδειξη | Μέτρηση |
|---|---|
| Νέες άγκυρες | `property-completion-stance.test.ts` (**19**) · `listing-completion-slice.test.ts` (**22**) · `listing-levels-provenance.test.ts` (**16**) · `field-completion-weights.test.ts` (**26**) · `owner-listing-completion.test.tsx` (**9**, πραγματικοί loaders + ICU) · Τ4 στο `listing-attribute-value.test.ts` |
| **Μεταλλάξεις** *(CHECK 3.54)* | **19/19 πιάστηκαν** |
| 🔴 …και **μία δραπέτευσε πρώτα**, με μάθημα | Η μετάλλαξη του §8 #3 βγήκε **πράσινη** από τη διαδρομή της Φ5: ο μεταφραστής στέλνει **ήδη κανονικοποιημένο** είδος, άρα η γραμμή **δεν εκτελούνταν ποτέ** από εκεί — **φρουρός χωρίς απόδειξη ζωής**. Η άγκυρα μετακόμισε **δίπλα στη γραμμή** (`constants/__tests__/`), όπου ζει η **εταιρική** διαδρομή· εκεί κοκκινίζει **7 tests**. |
| Παλινδρόμηση | **10.976 πράσινα / 629 suites** *(4 suites κόκκινες, **προϋπάρχουσες και εκτός του diff** — δες παρακάτω)* |
| Πύλες | **3.34** ✅ · **3.36** ✅ · **3.71** ✅ · **3.33** ✅ · **3.8** ✅ · **3.74** ✅ · **3.28** ✅ *(0 νέοι κλώνοι σε 13 αρχεία, **και −2 προϋπάρχοντες**)* |
| N.7.1 | κάθε νέο αρχείο **≤253 γραμμές**, κάθε συνάρτηση **≤40** |
| i18n | το `/offers/[offerId]` πήγε **16.257 → 17.429** bytes, ταβάνι **18.370** ⇒ **καμία** νέα σφράγιση |

⛔ **Τι ΔΕΝ έγινε, δηλωμένα**:
- **Η προέλευση ΔΕΝ ζωγραφίζεται ακόμη.** Ένα σήμα *«μετρημένο από το σχέδιο»* δίπλα στην
  τιμή είναι πραγματικό πλεονέκτημα έναντι κάθε portal — και θέλει **νέα κλειδιά** σε
  διαδρομή με **σφραγισμένο ταβάνι**, δηλαδή **πράξη ανθρώπου**. Ανήκει στη **Φ6**, όπου το
  `measured` παύει να είναι μία περίπτωση και γίνεται ο κανόνας.
- **Καμία ανακατασκευή προβολών** — πράξη ανθρώπου (ADR-839 §7). Μέχρι τότε τα ζωντανά
  έγγραφα ανεβαίνουν **στην ανάγνωση** και ο δείκτης δείχνει χαμηλά ποσοστά, που είναι η
  **αλήθεια**.
- **Κανένας `tsc`** (N.17) — τον τρέχει ο Giorgio.
- 🔴 **Τέσσερις κόκκινες suites στο main, προϋπάρχουσες και εκτός του diff της Φ5**:
  `hooks/notifications/registry-exhaustiveness` · `components/shared/desktop-only-exit`
  *(`fetch is not defined`)* · `components/shared/files/useFileListActions` · και
  `services/demand/place-interest-custody`, που σκάει σε `owner-media-publication.ts:55`
  επειδή το fixture του **δεν έχει `media`** — ενώ ο τύπος το απαιτεί. Είναι το **τρίτο**
  ελλιπές fixture της ίδιας οικογένειας με τα δύο που βρήκε η Φ3, και όλα δείχνουν στο ίδιο:
  **κανείς δεν έτρεξε `tsc`** μετά την ADR-841.

---

### 7.1 🔴 Το μεγαλύτερο ρίσκο, ονομαστικά

Το `.i18n-shell-slice.json` ορίζει **σφραγισμένα όρια bytes ανά διαδρομή** (ADR-744, CHECK 3.34).
Η `/offers/new` είναι σφραγισμένη στα **10.324** bytes *(2026-08-30)*. Πολλά νέα κλειδιά σπάνε το
όριο, και το **ξανασφράγισμα είναι πράξη ανθρώπου** με γραπτό `why`.

⇒ Αυτός είναι **δεύτερος, ανεξάρτητος** λόγος που η Φ4 είναι **ξεχωριστή διαδρομή**: κρατά το βάρος
των i18n κλειδιών **έξω** από το χωνί εισόδου. Η αρχιτεκτονική απόφαση και η πύλη συμφωνούν.

---

### 7.6 🔴 Α6 — Η ΓΗ ΔΕΝ ΕΙΧΕ ΓΡΑΜΜΗ ΠΟΥΘΕΝΑ, ΚΑΙ Η Φ5 ΤΟ ΕΚΑΝΕ ΚΑΤΑΡΡΕΥΣΗ *(2026-09-03)*

#### 7.6.1 Το εύρημα — **ζωντανή** λευκή οθόνη, με γραμμή

Αναφορά από την παραγωγή, `https://www.nestorconstruct.gr/offers/ownp_330a5a4b-…`:

```
TypeError: Cannot read properties of undefined (reading 'map')
  OwnerListingCompletion:94                       useMemo
    → assessPropertyCompleteness                  (constants/property-completion.ts:283)
      → weightEntries.map(…)                      ← weightEntries === undefined
        ↑ getFieldWeightsForType('plot')
          → FIELD_WEIGHTS['plot']                 ← ΔΕΝ ΥΠΑΡΧΕΙ ΓΡΑΜΜΗ
```

**Δεν ήταν ακραία περίπτωση.** Ζωντανή μέτρηση στα `owner_properties`: **5 στα 5** έγγραφα
γης είναι `plot` ⇒ **κάθε** οικόπεδο έριχνε ολόκληρη τη σελίδα λεπτομέρειας.

#### 7.6.2 🔑 Η ρίζα: ο φρουρός **υπήρχε** και ήταν **σβηστός**

Το `plot`/`parcel` μπήκε στο λεξιλόγιο στις **2026-08-20** (`716b2390`, ADR-777 §8.32). Το
`property-types.ts` διαφημίζει ρητά τον μηχανισμό ασφαλείας:

> *«Ο `Record<PropertyTypeCanonical, PropertyClass>` κάνει τον **μεταγλωττιστή** φρουρό: νέο
> είδος **δεν μεταγλωττίζεται** μέχρι κάποιος να απαντήσει τι είναι.»*

Και είναι **αλήθεια** — ο μεταγλωττιστής θα κοκκίνιζε. Απλώς **κανείς δεν τον τρέχει**: ο
`npm run typecheck` πεθαίνει σε `JavaScript heap out of memory` μετά από ~290s στα 4 GB
*(μετρημένο 2026-09-02, `.claude-rules/pending-ratchet-work.md`)*. Δηλαδή **έξι** πίνακες
δηλωμένοι εξαντλητικοί έμειναν ελλιπείς για **δεκατέσσερις ημέρες**, με τον φρουρό να
υπάρχει στον τύπο και να μην εκτελείται πουθενά.

🔴 **Και τη ρίψη την πυροδότησε η ίδια η Φ5.** Ως τις 2026-09-02 ο επιλυτής ήταν
`if (type in FIELD_WEIGHTS) … else FIELD_WEIGHTS.apartment` — το `plot` **δεν** ήταν στον
πίνακα, έπεφτε στο `apartment`, **λάθος παρονομαστής αλλά ζωντανή οθόνη**. Η διόρθωση του
**§8 #3** μετακίνησε την ερώτηση από *«υπάρχει γραμμή;»* σε *«αναγνωρίζεται το είδος;»* —
σωστή κίνηση, που όμως μετέτρεψε ένα **σιωπηλό** ελάττωμα σε **κατάρρευση**, επειδή η
απάντηση στη δεύτερη ερώτηση ήταν «ναι» και στην πρώτη «όχι».

⚠️ **Το μάθημα δεν είναι «μη διορθώνεις παρονομαστές».** Είναι ότι μια διόρθωση που
**στενεύει** ένα fallback πρέπει να ρωτά *«τι έπεφτε εκεί μέσα;»* — και η απάντηση ήταν
μετρήσιμη σε μία γραμμή grep.

#### 7.6.3 Η κλάση — **έντεκα** τόποι, όχι ένας

| Τόπος | Πριν | Συνέπεια για γη |
|---|---|---|
| `FIELD_WEIGHTS` | 12 γραμμές | 🔴 **TypeError** — η σελίδα κάτω |
| `FLOOR_TYPE_MATRIX` | 10 γραμμές | 🔴 **TypeError** με δηλωμένο όροφο *(βρέθηκε από μετάλλαξη, όχι από την αναφορά)* |
| `AREA_RULES` | 12 γραμμές | 🔶 σιωπηλό `insufficientData` |
| `LAYOUT_RULES` | 12 γραμμές | 🔶 σιωπηλό |
| `ORIENTATION_RULES` | 12 γραμμές | 🔶 σιωπηλό |
| `MEDIA_THRESHOLDS` | 12 γραμμές | 🔶 σιωπηλό *(σώθηκε από `in`-έλεγχο)* |
| **6 × `KNOWN_PROPERTY_TYPES`** | χειρόγραφο Set των 12, ×6 | 🔶 **ο λόγος που μόνο ΕΝΑ έσπασε** |

🔑 Τα έξι αντίγραφα ήταν ταυτόχρονα **η αιτία** *(κανένα δεν έμαθε τη γη)* και **η ασπίδα**
*(απέρριπταν το `plot` πριν φτάσει στον πίνακα)*. Και τα έξι ήταν κατά λέξη η SSoT
`isCanonicalPropertyType`, που **υπήρχε ήδη** στο `property-types.ts` — με σχόλιο που
προειδοποιούσε ακριβώς γι' αυτό: *«δύο οθόνες με χειρόγραφο `includes(...)` θα ήταν δύο
αυθεντίες»*. Ήταν **έξι**.

#### 7.6.4 📐 Τα βάρη της γης — **έρευνα**, όχι γνώμη

Απόφαση Giorgio: *«όπως το κάνουν οι μεγάλοι παίκτες»*. Τρεις ανεξάρτητες πηγές:

| Πηγή | Τι ζητά για γη |
|---|---|
| **CRMLS Land form** *(τροφοδοτεί Zillow)* | `Lot Size` · `Lot Features` · **`Survey`** · `Road Frontage` · `Utilities` · `Zoning` — και ρητά *«bedrooms/bathrooms **when applicable**»* |
| **idealista** *(suelo/terreno)* | superficie · **calificación urbanística** · servicios *(agua · luz · saneamiento)* · acceso |
| **Spitogatos** *(οικόπεδο)* | εμβαδόν · **Σ.Δ. / κάλυψη** · άρτιο-οικοδομήσιμο · εντός/εκτός σχεδίου |

⇒ Από όσα **έχει σήμερα το μοντέλο**, τα ζητούμενα είναι **τέσσερα**: ταυτότητα · εμβαδόν ·
**τοπογραφικό** · φωτογραφίες. Παρονομαστής **8,0** έναντι **23,0** του διαμερίσματος. Τα
υπόλοιπα **13** κλειδιά γίνονται `exempt` — ο ιδιοκτήτης χωραφιού **δεν** μαθαίνει ποτέ ότι
«του λείπει η ενεργειακή κλάση».

🔴 **Το `floorplan` για τη γη ΕΙΝΑΙ το τοπογραφικό, και είναι κρίσιμο** — το `Survey` του
CRMLS. Ίδιο επιχείρημα με την αποθήκη *(«σε κενό χώρο η φωτογραφία δείχνει έναν τοίχο, ενώ
το σχήμα είναι όλη η πληροφορία»)*, μόνο εντονότερο: για οικόπεδο το διάγραμμα είναι το
έγγραφο **από το οποίο κρίνεται η αγορά**.

⛔ **Το `?? FIELD_WEIGHTS.apartment` απορρίφθηκε ρητά**: είναι η αστοχία του **§8 #3** με άλλο
όνομα — θα ζητούσε **υπνοδωμάτια ως κρίσιμα** από οικόπεδο.

#### 7.6.5 Η στάση του επιλυτή: **fail-fast ⊕ graceful degradation**

Ο `getFieldWeightsForType` δεν μπορεί πλέον να επιστρέψει `undefined`:

- **ανάπτυξη/δοκιμές** ⇒ `throw` με μήνυμα που λέει τι να κάνεις·
- **παραγωγή** ⇒ {@link CRITICAL_IDENTITY} *(«τι είναι» + «πόσο μεγάλο»)*, τα δύο που ισχύουν
  για **κάθε** ακίνητο που μπορεί να υπάρξει.

🔑 Η ασυμμετρία είναι σκόπιμη: ο μηχανικός το μαθαίνει **θορυβωδώς**· ο άνθρωπος που κοιτάζει
το ακίνητό του **δεν πληρώνει** το κενό μας με λευκή οθόνη.

#### 7.6.6 🔬 Η άγκυρα, και τι έμαθε από τις μεταλλάξεις

`constants/__tests__/property-type-coverage.test.ts` — **39 δοκιμασίες**, τρεις ομάδες:
**Ε** *(απόδειξη ζωής)* · **Α** *(κάθε πίνακας έχει γραμμή για κάθε είδος, **και καμία
ορφανή**)* · **Β** *(κάθε μηχανή **εκτελείται**)* · **Γ** *(η γη ρωτιέται **για γη**)*.

**Μεταλλάξεις: 4/4 κόκκινες** — αλλά η τέταρτη μόνο **μετά** από διόρθωση της ίδιας της
άγκυρας, και αυτό είναι το χρήσιμο μέρος:

| # | Μετάλλαξη | Αποτέλεσμα |
|---|---|---|
| Μ1 | σβήσιμο γραμμής `plot` από `FIELD_WEIGHTS` | **5 κόκκινες** |
| Μ2 | `plot: [...RESIDENTIAL_CORE]` *(το fallback που απορρίφθηκε)* | **1 κόκκινη** (Γ1) |
| Μ3 | σβήσιμο `parcel` από `AREA_RULES` | **2 κόκκινες** |
| Μ4 | σβήσιμο του κλάδου γης στον όροφο | ⚠️ **0 κόκκινες** → μετά τη διόρθωση: **2** |

🔴 **Η Μ4 απέδειξε ότι η πρώτη γραφή της άγκυρας ήταν ρηχή.** Καλούσε κάθε μηχανή με **κενά**
ορίσματα (`floor: null`, `bedrooms: null`), οπότε οι μισές έβγαιναν σε `insufficientData`
**πριν** αγγίξουν τον πίνακά τους — δηλαδή δοκίμαζαν την **έξοδο**, όχι τη μηχανή. Η
διόρθωση καλεί κάθε μηχανή **δύο φορές** (`SPARSE` ⊕ `DENSE`), και με τη δεύτερη ήρθε στο φως
**δεύτερη ζωντανή κατάρρευση** που καμία αναφορά δεν είχε δείξει:
`FLOOR_TYPE_MATRIX['plot'][band]` ⇒ *«Cannot read properties of undefined (reading 'upper')»*
για οικόπεδο με δηλωμένο όροφο.

⇒ Η γη έφυγε και από τον **τύπο** του `FLOOR_TYPE_MATRIX` (`Exclude`) με ρητό κλάδο: `null`
όροφος ⇒ `ok`, οτιδήποτε άλλο ⇒ `implausible`. **Η γη δεν έχει όροφο** — και αυτό δεν είναι
το ίδιο με «ισόγειο», που είναι η απάντηση της βίλας.


#### 7.6.7 🧹 Boy Scout: **έξι** αντίγραφα της πόρτας εισόδου *(N.0.2 · N.18)*

Το `jscpd:diff` στα αγγιγμένα αρχεία κοκκίνισε — και η μέτρηση στο HEAD έδειξε ότι οι κλώνοι
ήταν **προϋπάρχοντες**, όχι δικοί μου. Δεν έμειναν γι' αυτό: το `plot` δεν είχε γραμμή σε
έξι πίνακες επειδή ακριβώς αυτό το σχήμα *(«κάθε αρχείο λύνει μόνο του το κοινό»)* είναι που
γεννά τα κενά.

| Πού | Συνάρτηση | Εύρος |
|---|---|---|
| `area` · `interior-features` · `systems` | `toNonNegativeNumber` **×3** — *byte-για-byte ταυτόσημες, ίδιο sha256* | `≥ 0`, δεκαδικοί |
| `layout` | `toNonNegativeInt` | `≥ 0`, ακέραιοι *(δωμάτια)* |
| `floor-type` | `toFiniteNumber` | κάθε πεπερασμένος *(**υπόγειο = −1**)* |
| `price` | `toPositiveNumber` | `> 0` *(τιμή μηδέν δεν είναι τιμή)* |

⇒ `constants/plausibility-input.ts`: **μία** μηχανή, **τέσσερις ονομασμένες πύλες**.

⛔ **ΟΧΙ `toNumber(value, { min, integer })`** — μια γενική συνάρτηση με σημαίες μεταφέρει την
**απόφαση εύρους** στο σημείο κλήσης, δηλαδή επιτρέπει σε δύο καλούντες του **ίδιου** πεδίου
να διαφωνήσουν για το τι είναι έγκυρο. Οι πύλες είναι **ονόματα του τομέα**.

🔬 Άγκυρα `plausibility-input.test.ts` — φυλάει τις **τρεις διαφορές** που δικαιολογούν τις
τέσσερις πύλες *(αρνητικό ⇒ μόνο ο όροφος· μηδέν ⇒ δήλωση για μεγέθη, απουσία για τιμή·
δεκαδικοί ⇒ τα δωμάτια κόβονται προς τα κάτω)* και, **εκτελεσμένη**, την παγίδα
`Number('') === 0`: «δεν συμπλήρωσε τίποτα» που γίνεται «δήλωσε μηδέν».


#### 7.6.8 🖥️ Η ΕΠΑΛΗΘΕΥΣΗ ΣΤΗΝ ΟΘΟΝΗ ΒΡΗΚΕ ΕΛΑΤΤΩΜΑ ΠΟΥ **ΚΑΜΙΑ ΜΗΧΑΝΗ ΔΕΝ ΜΠΟΡΟΥΣΕ ΝΑ ΠΙΑΣΕΙ**

Με καθαρό dev server *(`kill-port 3000` · `rm -rf .next`)*, η
`/offers/ownp_330a5a4b-…` απέδωσε **πλήρως**. Και η οθόνη συμφώνησε με τη μέτρηση
**γραμμή προς γραμμή**:

| Μετρημένο *(εκτέλεση της αλυσίδας με το **ζωντανό** έγγραφο)* | Στην οθόνη |
|---|---|
| `type: "plot"` · `areaGross: 500` | «Οικόπεδο · 500 τ.μ.» |
| `percent: 0.5` · `weightTotal: 8` | «Πληρότητα καταχώρησης **50%**» |
| `floorplan:missing` · `photos:missing` | «Τι λείπει: …» |
| `exempt: 13` | καμία ερώτηση για υπνοδωμάτια / ενεργειακή κλάση / θέρμανση |

🔴 **ΚΑΙ ΕΛΕΓΕ «Τι λείπει: ΚΑΤΟΨΗ».**

Η βαθμολόγηση ήταν **σωστή** *(το `floorplan` για τη γη **είναι** το τοπογραφικό —
§7.6.4)*, ο δείκτης σωστός, **685 tests πράσινα** — και η **λέξη** ζητούσε από
ιδιοκτήτη οικοπέδου **σχέδιο ορόφου κτιρίου που δεν υπάρχει**. Το ίδιο και το
`areaGross` = *«Μικτή επιφάνεια»*: μικτό ⇄ καθαρό είναι διάκριση **κτίσματος**.

⚠️ **Καμία μηχανή δεν μπορούσε να το πιάσει**: όλα τα αριθμητικά ήταν σωστά. Μόνο η
**ανάγνωση** ήταν λάθος. Αυτό είναι το επιχείρημα υπέρ του περπατήματος, σε μία γραμμή.

**Η θεραπεία — SSoT, γιατί οι ετικέτες έχουν ΔΥΟ αναγνώστες:**

- `completionFieldLabelKey(fieldKey, propertyType)` *(`field-completion-weights.ts`)* —
  επιστρέφει `completion.fields.land.<key>` όταν η **κλάση** είναι `land` και το πεδίο
  δηλώνεται ως «λέγεται αλλιώς», αλλιώς το γενικό. **Μερική** υπερκάλυψη επίτηδες: νέο
  `FIELD_KEY` δεν χρειάζεται γραμμή σε κάθε κλάση για να έχει όνομα.
- `el`: «Τοπογραφικό διάγραμμα» · «Εμβαδόν γης» — `en`: «Survey plan» · «Land area».
- 🔑 **Το `CompletionAssessment` κουβαλά πλέον `propertyType`** *(κανονικοποιημένο)*: ο
  `PropertyCompletionBreakdown` δέχεται **μόνο** το `assessment`, και ένα δεύτερο prop θα
  επέτρεπε να **ονομάζονται** τα πεδία με άλλο είδος από αυτό με το οποίο **κρίθηκαν**.

⛔ **Γιατί όχι διόρθωση μόνο στην οθόνη του ιδιώτη**: τις ίδιες ετικέτες τις διαβάζει και
η εταιρική `PropertyCompletionBreakdown`. Θα έλεγε «Κάτοψη» για το **ίδιο** οικόπεδο —
δεύτερη αυθεντία για το ίδιο όνομα, η κλάση που αυτό ακριβώς το §7.6 καθάρισε.

🔬 **Ομάδα Λ** *(4 δοκιμασίες × κάθε είδος × κάθε πεδίο)*: κάθε κλειδί που **παράγει** η
SSoT **λύνεται σε κείμενο, σε el ΚΑΙ en** — ένα κλειδί χωρίς μετάφραση δεν κοκκινίζει
πουθενά, **τυπώνεται αυτούσιο στην οθόνη**. Και το `Λ4` κλειδώνει ότι η ωμή ελληνική τιμή
`'Οικόπεδο'` παίρνει την **ίδια** ετικέτα με το `plot`.

✅ **Επαναλήφθηκε στην οθόνη**: «Τι λείπει: **Τοπογραφικό διάγραμμα** · Φωτογραφίες».

---

#### 7.6.9 🔴 Η ΙΔΙΑ ΛΕΞΗ, ΔΕΥΤΕΡΗ ΕΠΙΦΑΝΕΙΑ — Η ΘΕΡΑΠΕΙΑ ΤΟΥ §7.6.8 ΕΛΥΣΕ **ΤΟΝ ΕΝΑ ΑΝΑΓΝΩΣΤΗ**

Νέα αναφορά **από την οθόνη**, ίδια μέρα, ίδια διεύθυνση *(`/offers/ownp_330a5a4b-…`,
είδος **Οικόπεδο**)*. Η **ίδια σελίδα** έλεγε δύο πράγματα:

| Επιφάνεια | Τι έλεγε |
|---|---|
| **Πεδίο ανεβάσματος** *(`OwnerPropertyMediaField`)* | *«Ανέβασε ό,τι έχεις — φωτογραφίες, **κάτοψη**, PDF»* |
| Δείκτης πληρότητας, **δύο κάρτες παρακάτω** | *«Τι λείπει: **Τοπογραφικό διάγραμμα**»* ✅ |

🔑 **ΤΟ ΕΥΡΗΜΑ ΔΕΝ ΕΙΝΑΙ ΤΟ ΚΕΙΜΕΝΟ — ΕΙΝΑΙ ΠΟΙΟΣ ΡΩΤΗΣΕ.** Η απόφαση **υπήρχε** και
ήταν σωστή *(`LAND_LABELLED_FIELDS` + `completionFieldLabelKey`, §7.6.8)*, με **δική της
άγκυρα** *(ομάδα Λ, «η γη ΔΕΝ λέει Κάτοψη»)*. Το ανέβασμα ήταν το **μόνο** σημείο που
**δεν ρώτησε κανέναν** — σταθερό `property-market:offer.media.label`.

⚠️ **Και το §7.6.8 δεν μπορούσε να το προβλέψει**: απαρίθμησε τους αναγνώστες των
**ετικετών πεδίων** *(δείκτης ιδιώτη + εταιρική ανάλυση)* και τους κάλυψε και τους δύο.
Το uploader **δεν είναι αναγνώστης ετικέτας** — είναι δική του πρόταση προς τον άνθρωπο,
σε **άλλο namespace** *(`property-market`)*. Δηλαδή η ερώτηση *«πώς λέγεται το σχέδιο
αυτού του είδους;»* είχε **τρίτο** ρωτούντα, που δεν φαινόταν από την απογραφή.

**Η θεραπεία — Ο ΙΔΙΟΣ ΚΡΙΤΗΣ ΜΕ ΤΟΝ ΑΔΕΛΦΟ, ΟΧΙ ΤΡΙΤΟ ΜΗΤΡΩΟ:**

- `isLandPropertyType(form.watch('type'))` στο `OwnerPropertyMediaField` — **αυτό ακριβώς**
  το κατηγόρημα ρωτά το `OwnerBasicsFields` **δύο πεδία πιο πάνω** για το «Εμβαδόν
  **οικοπέδου**». Ένα χειρόγραφο `type === 'plot' || 'parcel'` εδώ θα ήταν η **τρίτη**
  αυθεντία για το «τι είναι γη».
- `offer.media.landLabel` / `landHelp` σε **el + en** — «Φωτογραφίες και τοπογραφικό» ·
  «Photos and survey plan» *(το `en` ακολουθεί το «Survey plan» του §7.6.8, όχι δεύτερη
  απόδοση του ίδιου όρου)*.
- ⛔ **Όχι μετονομασία του κοινού κλειδιού**: θα ζητούσε τοπογραφικό διάγραμμα για
  **διαμέρισμα**. Το φυλά το `Κ3`.

🔬 **Άγκυρα με ΑΠΟΔΟΣΗ, όχι με έλεγχο κλειδιών** *(`OwnerPropertyMediaField.land-label.test.tsx`,
10 δοκιμασίες)*. Ένα test που ρωτά *«λύνεται το `landLabel`;»* θα ήταν **πράσινο και με το
ελάττωμα ζωντανό** — τα κλειδιά υπήρχαν ήδη στον δείκτη πληρότητας. Η ερώτηση του ανθρώπου
δεν ήταν «υπάρχει μετάφραση;», ήταν **«τι λέει η οθόνη μου;»**. Ομάδες: **Ε** *(απόδειξη
ζωής)* · **Κ1–Κ4** *(γη ⇒ τοπογραφικό · και τα δύο είδη γης · διαμέρισμα ⇒ **κάτοψη** ·
κενό είδος ⇒ δεν μαντεύει)* · **Λ1–Λ3** *(κείμενο σε δύο locales · καμία «κάτοψη»/«floor
plan» στη γη · **διαφέρει** από το γενικό — αλλιώς είναι νεκρό κλειδί)*.
**Μετάλλαξη 3/3 κόκκινες** *(επαναφορά στα σταθερά κλειδιά ⇒ Κ1 + Κ2×2, με τα Κ3/Κ4 σωστά
πράσινα)*.

🔴 **ΑΝΟΙΧΤΟ, ΕΠΑΛΗΘΕΥΜΕΝΟ ΣΤΗ ΔΙΑΔΡΟΜΗ — ΔΥΟ ΚΡΙΤΕΣ ΓΙΑ ΤΟ «ΕΙΝΑΙ ΓΗ;», ΚΑΙ Ο ΕΝΑΣ
ΞΕΡΕΙ ΛΙΓΟΤΕΡΑ** *(→ §8 #9)*: το `isLandPropertyType` ρωτά `isCanonicalPropertyType`
**χωρίς** `normalizePropertyType` *(`property-types.ts:155`)*, ενώ το
`completionFieldLabelKey` **κανονικοποιεί** *(`field-completion-weights.ts:432`)*. Άρα ωμή
παλαιά τιμή `type: 'Οικόπεδο'` — που ο τύπος `PropertyType` **επιτρέπει** και το
`ownerPropertyFormFrom` περνά **αυτούσια** — δεν είναι γη για τη **φόρμα** και είναι γη για
τον **δείκτη**. ⛔ **Δεν διορθώθηκε εδώ επίτηδες**: δυνατός κριτής **μόνο** στο uploader θα
έκανε την **ίδια οθόνη** να αυτο-αντικρούεται *(«Εμβαδόν» πάνω, «τοπογραφικό» κάτω)* —
χειρότερο από το να λένε και τα δύο το ίδιο λάθος. Η θεραπεία είναι **στο κατηγόρημα** και
αφορά ~~8 καταναλωτές σε 3 τομείς~~ **6 καταναλωτές (+1)** *(N.8 ⇒ χωριστή εντολή)*. Δεν
χτυπά σήμερα: **5 στα 5** ζωντανά `owner_properties` με γη είναι `plot`. ⚠️ Και η
**υπάρχουσα** άγκυρα *(`property-type-classes.test.ts` Κ2)* είναι **πράσινη είτε
κανονικοποιεί είτε όχι** — δοκιμάζει το `'Αποθήκη'`, που δεν είναι γη **ούτε μετά** από
κανονικοποίηση.

> ✅ **ΕΚΛΕΙΣΕ ΤΗΝ ΙΔΙΑ ΜΕΡΑ — §7.6.11.** 🔴 Και **δύο** ισχυρισμοί αυτής της παραγράφου
> ήταν **λάθος**, διορθωμένοι εκεί: *(α)* οι καταναλωτές είναι **6**, όχι 8 — ο αριθμός
> βγήκε από `grep` που μετρούσε test αρχεία, εσωτερική χρήση και **σχόλιο** *(ακριβώς το
> μοτίβο «αριθμός που ταξιδεύει χωρίς να ανοίξει κανείς το αρχείο» που καταγγέλλει το
> `CLAUDE.md`)*· *(β)* η θεραπεία **δεν** μπορούσε να μπει στο `propertyClassOf` — θα
> έφτιαχνε **κύκλο εισαγωγών με TDZ κατάρρευση** εξαρτώμενη από τη σειρά φόρτωσης. Μπήκε
> σε **τρίτο leaf**, και ο αδύναμος κριτής **έπαψε να εξάγεται**.

---

#### 7.6.10 🔴 Η ΦΟΡΜΑ ΡΩΤΟΥΣΕ «ΤΙ ΘΕΛΕΙΣ ΝΑ ΚΑΝΕΙΣ;» ΧΩΡΙΣ ΝΑ ΞΕΡΕΙ «ΤΙ ΕΙΝΑΙ»

**Τρίτο** εύρημα από την ίδια οθόνη, ίδια μέρα. Είδος **Οικόπεδο**, τσεκ **«Βραχυχρόνια
μίσθωση»** — και από κάτω τρία πεδία:

> *«Τιμή ανά **διανυκτέρευση** (€)»* · *«Ελάχιστες **διανυκτερεύσεις**»* ·
> *«Μέγιστος αριθμός **επισκεπτών**»*

Ένα οικόπεδο δεν έχει διανυκτερεύσεις.

🔑 **ΤΟ ΚΕΝΟ ΕΙΝΑΙ ΔΟΜΙΚΟ, ΟΧΙ ΚΕΙΜΕΝΟΥ** — και είναι η **ίδια κλάση** με τα §7.6.8 /
§7.6.9: ο πίνακας που τροφοδοτεί εκείνη την οθόνη *(`OFFER_NUMBER_FIELDS`)* είναι
`Record<OfferKind, …>`, δηλαδή κλειδωμένος στη **διάθεση** και **τυφλός στο είδος**. Και
οι τέσσερις επιλογές προσφέρονταν σε κάθε ακίνητο.

**Και ο μισός κανόνας ΥΠΗΡΧΕ ΗΔΗ, κρυμμένος σε `if`:**

```ts
if (draft.offers.some((o) => isOpenOffer(o) && o.kind === 'exchange')) {
  if (!isLandPropertyType(draft.type)) found.push('exchange-requires-land');
}
```

⚠️ *«Η αντιπαροχή αφορά **ΜΟΝΟ** τη γη»* ζούσε **στο σώμα** του
`ownerPropertyInvariantViolations`. Ένας κανόνας γραμμένος σε `if` **δεν δείχνει σε
κανέναν ότι λείπει ο διπλανός του** — γι' αυτό ο συμμετρικός του δεν γράφτηκε ποτέ.

**🏆 ΤΙ ΚΑΝΕΙ Η ΑΓΟΡΑ — Η ΔΙΑΚΡΙΣΗ ΔΕΝ ΕΙΝΑΙ «ΒΡΑΧΥΧΡΟΝΙΑ Ή ΟΧΙ», ΕΙΝΑΙ Η ΜΟΝΑΔΑ:**

| Πλατφόρμα | Γη | Μονάδα |
|---|---|---|
| **Airbnb** | **δεν τη δέχεται** — απαιτεί κατάλυμα με κρεβάτια | ανά διανυκτέρευση |
| **Hipcamp** | ✅ αλλά ρωτά **πρώτα** τύπο θέσης + υποδομή *(νερό · WC · πρόσβαση)* | ανά διανυκτέρευση |
| **Peerspace · Giggster** | ✅ γυρίσματα & εκδηλώσεις | **ανά ώρα** |
| **Neighbor.com** | ✅ στάθμευση & αποθήκευση | **ανά μήνα** |
| **LoopNet · Crexi** | ✅ *ground lease* | **ανά 25–99 έτη** |
| **Zillow · Spitogatos** | πώληση ή μακροχρόνια | ποτέ ανά διανυκτέρευση |

⇒ **Η διανυκτέρευση εμφανίζεται μόνο όταν υπάρχει κάτι που κοιμάσαι μέσα.** Το ίδιο λέει
και ο νόμος: το Μητρώο Ακινήτων Βραχυχρόνιας Διαμονής *(ν. 4446/2016)* δέχεται
**κατοικίες** — οικόπεδο δεν εγγράφεται.

**Η θεραπεία — ΕΝΑΣ κριτής, ΔΥΟ μηνύματα** *(απόφαση Giorgio 2026-09-03: «μία γραμμή
ποιες διαθέσεις έχουν νόημα»)*:

- **`constants/offer-kind-eligibility.ts`** — `OFFER_KIND_CLASSES`,
  `Record<OfferKind, readonly PropertyClass[]>`: `sell` και `leaseOut` **όλες** *(η γη
  πωλείται και εκμισθώνεται κανονικά — ground lease, χωράφι, υπαίθρια στάθμευση)*,
  `exchange: ['land']`, `leaseShort: ['residential']`.
- 🔑 **Η ερώτηση είναι ανά ΚΑΤΗΓΟΡΙΑ, ποτέ ανά είδος**: μια λίστα ειδών θα έπρεπε να
  θυμηθεί το «Αγροτεμάχιο» δίπλα στο «Οικόπεδο», και θα ξεχνούσε το τρίτο είδος γης την
  ημέρα που θα προστεθεί. Η κατηγορία είναι ήδη ο **δεύτερος άξονας** του
  `property-types.ts` και υπάρχει ακριβώς γι' αυτό.
- **Δύο κωδικοί, όχι ένας**: `exchange-requires-land` *(υπάρχων)* +
  `short-lease-requires-dwelling` *(νέος)*. Η κίνηση που ζητείται από τον άνθρωπο είναι
  **αντίθετη** — *«διάλεξε γη»* έναντι *«διάλεξε κατοικία»* — και ένα κοινό «αυτή η
  διάθεση δεν ταιριάζει» θα τον άφηνε να μαντέψει προς τα πού.
- ⚠️ **Άγνωστο/κενό είδος ⇒ επιτρεπτό, ποτέ σφάλμα**: η φόρμα ξεκινά με `type: ''` και ο
  άνθρωπος **δεν έχει απαντήσει ακόμη**. Το κενό το πιάνει ο **δικός του** κωδικός
  *(`type-missing`)*, που ξέρει να πει «διάλεξε είδος». Το φυλά το **Κ22**.

⛔ **ΓΙΑΤΙ ΟΧΙ ΑΠΟΚΡΥΨΗ ΤΟΥ ΤΣΕΚ ΚΑΙ ΟΧΙ ΣΙΩΠΗΛΟ ΦΙΛΤΡΑΡΙΣΜΑ.** Και τα δύο είναι παγίδα:
μια επιλογή που **εξαφανίζεται αφού τσεκαριστεί** δεν μπορεί να **ξε**-τσεκαριστεί, και
μια διάθεση που φιλτράρεται σιωπηλά στην αποθήκευση είναι το **ίδιο** σιωπηλό ψέμα με το
«οικόπεδο στον 3ο όροφο» που ο `ownerPropertyDraftFrom` υπάρχει για να αποτρέψει. Ο
πίνακας τροφοδοτεί **invariants** — η οθόνη λέει **τι** φταίει και **τι να κάνεις**,
όπως ήδη κάνει το `exchange-requires-land` από τις 2026-08-20. Ο ίδιος runner τρέχει
**και στον διακομιστή** *(`owner-property.service.ts`)*.

🔬 **ΜΕΤΑΛΛΑΞΕΙΣ 6/6 — ΚΑΙ Η ΜΙΑ ΒΡΗΚΕ ΚΕΝΟ ΠΟΥ Η ΣΧΕΔΙΑΣΗ ΔΕΝ ΕΙΔΕ.** Η πρώτη γραφή των
αγκυρών *(Κ18–Κ22)* δοκίμαζε **μόνο γη**. Η μετάλλαξη
`leaseShort: ['residential','commercial']` έμεινε **ΠΡΑΣΙΝΗ** ⇒ ο αποκλεισμός του
εμπορικού ήταν **δηλωμένη απόφαση χωρίς φρουρό**: σκέλος που μοιάζει με απόδειξη και
είναι **εμφάνιση**. ⇒ **Κ23** *(γραφείο · κατάστημα · αίθουσα · αποθήκη ⇒ **ακριβώς**
αυτή η μία παραβίαση)*. Οι έξι: `leaseShort` δέχεται γη · δέχεται εμπορικό · δέχεται
**μόνο** εμπορικό · `isOpenOffer`→`isLiveOffer` *(σπάει Κ21 + Κ17)* · άγνωστο είδος ⇒
`false` *(σπάει το Κ22)* · `exchange` δέχεται τα πάντα *(παλινδρόμηση του **υπάρχοντος**
κανόνα)*.

📊 **998 πράσινα** σε `types` + `lib/owner-property` + `components/owner-property` +
`constants` *(37 σουίτες)*. Το `src/types/i18n.ts` αναγεννήθηκε — **CHECK 3.33 OK, 107
locale αρχεία**.

🔴 **Νέο ανοιχτό θέμα §8 #10**: η βραχυχρόνια χρήση **γης και εμπορικού** είναι υπαρκτή
αγορά· αυτό που λείπει είναι η **μονάδα**, όχι η αγορά.

**Μεταλλάξεις 6/6 κόκκινες** *(Μ5: η γη σταματά να μετονομάζει το `floorplan` · Μ6:
λείπει η αγγλική ετικέτα)*.

---

---

#### 7.6.11 🔴 ΔΥΟ ΚΡΙΤΕΣ ΓΙΑ ΤΟ «ΕΙΝΑΙ ΓΗ;» — ΚΑΙ Η ΘΕΡΑΠΕΙΑ ΗΤΑΝ ΝΑ ΣΒΗΣΕΙ Ο ΕΝΑΣ

**Τέταρτο** εύρημα της ίδιας αλυσίδας, και το **μόνο** που δεν το ανέφερε η οθόνη: το
βρήκε το §7.6.9 γράφοντας τον δικό του uploader, και το άφησε ανοιχτό ως **§8 #9**.

Η ίδια ερώτηση —*«είναι γη αυτό το ακίνητο;»*— απαντιόταν από **δύο** συναρτήσεις με
**διαφορετική δύναμη**: το `isLandPropertyType` ρωτούσε `isCanonicalPropertyType`
**σκέτο**, ενώ το `completionFieldLabelKey` περνούσε πρώτα από `normalizePropertyType`.
Άρα ωμή παλαιά τιμή `type: 'Οικόπεδο'` **δεν ήταν γη** για τη φόρμα και **ήταν γη** για
τον δείκτη πληρότητας — στην **ίδια σελίδα**.

---

##### 🔴 Η ΥΠΟΨΙΑ ΠΟΥ ΕΠΑΛΗΘΕΥΤΗΚΕ ΠΡΩΤΗ, ΚΑΙ ΑΛΛΑΞΕ ΟΛΟΚΛΗΡΗ ΤΗ ΛΥΣΗ

Η προφανής θεραπεία —*«βάλε το `normalize` μέσα στο `propertyClassOf`»*— είναι
**αδύνατη**. Το `constants/property-types.ts` έχει **μηδέν imports**: είναι αληθινό leaf,
και το `property-type-aliases` **εισάγει από αυτό** *(γρ. 25, με γραμμένη τη δήλωση «leaf
module — εξαρτάται μόνο από το property-types»)*.

```
  property-types  ──import──▶  property-type-aliases
         ▲                              │
         └──────────────import──────────┘        ⇐ ΚΥΚΛΟΣ
```

⚠️ **ΚΑΙ ΔΕΝ ΕΙΝΑΙ «ΑΠΛΩΣ ΚΥΚΛΟΣ» — ΕΙΝΑΙ ΚΑΤΑΡΡΕΥΣΗ ΣΕ ΧΡΟΝΟ ΕΚΤΕΛΕΣΗΣ, ΕΞΑΡΤΩΜΕΝΗ ΑΠΟ
ΤΗ ΣΕΙΡΑ ΦΟΡΤΩΣΗΣ.** Το `property-types.ts` υπολογίζει **στο module-eval** το
`CREATABLE_PROPERTY_TYPES = PROPERTY_TYPES.filter(… !isLandPropertyType(t))`. Αν το πρώτο
module του γράφου που αποτιμηθεί είναι το `property-type-aliases` — και **8 παραγωγικά
αρχεία το εισάγουν απευθείας** — τότε το `PROPERTY_TYPE_ALIASES` βρίσκεται σε **TDZ** τη
στιγμή εκείνου του `filter` ⇒ `ReferenceError: Cannot access 'PROPERTY_TYPE_ALIASES'
before initialization`. Δηλαδή **δουλεύει σε dev και σκάει σε prod**, ανάλογα με τη σειρά
του bundler.

🔑 **Άρα η απουσία κανονικοποίησης ΔΕΝ ΗΤΑΝ ΠΑΡΑΛΕΙΨΗ** — ήταν η μόνη διαθέσιμη έξοδος
από κύκλο **που κανείς δεν είχε γράψει ότι υπάρχει**. Το §8 #9 υπέθετε ότι *«το διπλό
`normalize` είναι ταυτοτικό, άρα η αλλαγή δεν χαλά κανέναν»*: η υπόθεση ήταν σωστή για τη
**σημασιολογία** και άσχετη με το **εμπόδιο**.

---

##### 🏆 ΤΟ ΠΡΟΤΥΠΟ, ΚΑΙ ΠΟΥ ΤΟ ΞΕΠΕΡΝΑΜΕ

Το σχήμα έχει όνομα σε τρία επίπεδα. Στον **τομέα**: *anti-corruption layer* — η μετάφραση
ζει στο **σύνορο**, γιατί *«η διαφθορά συμβαίνει όταν πιάνεις τον εαυτό σου να σκορπίζει
`if (theirType == LEGACY)` μέσα στη λογική· **αυτό** το `if` είναι η διαφθορά, ήδη μέσα»*.
Στον **τύπο**: *parse, don't validate* — παρσάρεις **μία φορά** στην περιφέρεια, σε μορφή
που κάνει τις άκυρες καταστάσεις **μη-αναπαραστάσιμες**. Στη **μηχανική των modules**:
*«pull the shared contract up»* — δύο modules που χρειάζονται το ένα το άλλο λύνονται με
**τρίτο**.

🏆 **Πού πάμε παραπέρα**: το τρίτο module δεν **προσθέτει** δυνατό κριτή — **σβήνει τον
αδύναμο**. Τα `propertyClassOf` και `isLandPropertyType` **έπαψαν να εξάγονται** και
έγιναν ιδιωτικά, όπου τρέχουν αποκλειστικά πάνω σε τιμές του ίδιου του `PROPERTY_TYPES`
*(κανονικές εξ ορισμού, άρα η κανονικοποίηση εκεί θα ήταν ταυτοτική)*.

**Η διαφορά είναι ουσιώδης, όχι στιλιστική.** Ένα τρίτο αρχείο που απλώς πρόσθετε τον
δυνατό θα άφηνε **δύο** εξαγόμενους κριτές για την ίδια ερώτηση — δηλαδή θα αναπαρήγαγε
το ελάττωμα **ένα επίπεδο πιο πάνω**, με τον επόμενο καταναλωτή να διαλέγει σωστά **κατά
τύχη**. Πλέον ο αδύναμος κριτής **δεν είναι λάθος επιλογή· δεν είναι επιλογή**. Αυτό
είναι *parse, don't validate* εφαρμοσμένο στην **επιφάνεια των συναρτήσεων** αντί στα
δεδομένα: η άκυρη κλήση γίνεται **μη-αναπαραστάσιμη**, όχι «φυλαγμένη από test».

---

##### 🔢 Η ΔΙΟΡΘΩΣΗ ΤΟΥ ΑΡΙΘΜΟΥ: **6**, ΟΧΙ 8

🔴 **Το §7.6.9 και το §8 #9 έγραφαν «8 καταναλωτές σε 3 τομείς». Ήταν λάθος.** Ο αριθμός
βγήκε από `grep` που μετρούσε **και test αρχεία**. Το `grep` δίνει 8 γραμμές, από τις
οποίες **δύο δεν είναι καταναλωτές**: το `property-types.ts:200` είναι **εσωτερική** χρήση
πάνω σε κανονικές τιμές, και το `owner-property-invariants.ts:87` είναι **σχόλιο**.

Οι **παραγωγικοί** καλούντες είναι **έξι**:

| # | Αρχείο | Έβλεπε ωμή τιμή; |
|---|---|---|
| 1 | `components/building-management/tabs/property-tab-constants.ts` | ❌ όχι *(τρέχει πάνω στο `PROPERTY_TYPES`)* |
| 2 | `components/owner-property/form/OwnerPropertyFields.tsx` | ✅ **ναι** — τιμή φόρμας |
| 3 | `components/owner-property/form/OwnerPropertyMediaField.tsx` | ✅ **ναι** — τιμή φόρμας |
| 4 | `constants/floor-type-plausibility.ts` | ❌ όχι *(φρουρός `isCanonicalPropertyType` στη :133)* |
| 5 | `lib/listings/listing-filters.ts` | ✅ **ναι** — `listing.type` από τη βάση |
| 6 | `lib/owner-property/owner-property-form-values.ts` | ✅ **ναι** — και το **χειρότερο**, γιατί **γράφει** |

➕ **Έβδομο σημείο, που το §8 #9 δεν είχε δει καθόλου**: το `constants/offer-kind-eligibility.ts`
*(§7.6.10, γραμμένο την **ίδια μέρα**)* καλεί `propertyClassOf` **απευθείας**.

🔴 **Και εκεί το ελάττωμα ήταν ΕΠΙΤΡΕΠΤΙΚΟ, όχι ετικέτα.** Το `isOfferKindEligible` λέει
*«άγνωστο είδος ⇒ `true`»* — σκόπιμα, γιατί η φόρμα ξεκινά με `type: ''` και ο άνθρωπος
δεν έχει απαντήσει ακόμη. Αλλά με τον ασθενή κριτή η ωμή τιμή `'Οικόπεδο'` **έμοιαζε με
«δεν απάντησε»** ενώ ο άνθρωπος **είχε απαντήσει** ⇒ η **βραχυχρόνια μίσθωση περνούσε σε
οικόπεδο**, με «τιμή ανά διανυκτέρευση» σε γυμνή γη — δηλαδή **ακριβώς το περιστατικό που
το §7.6.10 μόλις είχε κλείσει**, ξανα-ανοιγμένο από την πλαϊνή πόρτα. ⚠️ Το ίδιο και ο
συμμετρικός: αντιπαροχή σε ωμό `'Διαμέρισμα'` **δεν** κατηγορούνταν.

⚠️ **Το μοτίβο είναι αυτό που το `CLAUDE.md` καταγγέλλει επανειλημμένα**: αριθμός που
ταξιδεύει από ανάλυση σε ADR σε handoff **χωρίς να ανοίξει κανείς το αρχείο**. Και οι δύο
θέσεις του «8» *(§7.6.9 και §8 #9)* διορθώθηκαν σε **6 (+1)**.

---

##### 🔬 ΟΙ ΑΓΚΥΡΕΣ — ΚΑΘΕ ΣΚΕΛΟΣ ΠΟΥ ΔΙΑΦΟΡΟΠΟΙΕΙ

⚠️ **Το προηγούμενο σκέλος ήταν εμφάνιση, όχι απόδειξη**, και μένει επίτηδες: το
`isLandPropertyType('Αποθήκη') === false` *(Κ2)* ήταν **πράσινο και στις δύο εκδοχές** —
το `'Αποθήκη'` λύνεται σε `storage`, δηλαδή εμπορικό, άρα «όχι γη» **πριν και μετά**.
Κρίνει **άλλο** πράγμα *(ότι το εμπορικό δεν βαφτίζεται γη)* και γι' αυτό δεν αφαιρέθηκε
— αλλά **δεν ήταν ποτέ φρουρός αυτού** του ελαττώματος.

| Άγκυρα | Τι εκτελεί |
|---|---|
| **Κ2β** | ωμή `'Οικόπεδο'`/`'Αγροτεμάχιο'` ⇒ **γη**· και οι μορφές που υπόσχεται ο πίνακας αναγνώρισης *(πεζά, άτονα, με κενά γύρω)* |
| **Κ2γ** | 🔴 **ο αδύναμος κριτής ΔΕΝ ΕΙΝΑΙ ΕΙΣΑΓΩΓΙΜΟΣ** — `'isLandPropertyType' in propertyTypesModule === false`. Κοκκινίζει αν κάποιος ξαναβάλει `export`, που **είναι** η επαναφορά του ελαττώματος |
| **Μ2β2** | `ownerPropertyDraftFrom` με ωμή τιμή ⇒ `floor`/`bedrooms` **μηδενίζονται** *(το Μ2β από πάνω περνά `'plot'`, άρα ήταν πράσινο και πριν)* |
| **Κ18β** | βραχυχρόνια σε ωμό `'Οικόπεδο'` ⇒ `short-lease-requires-dwelling` |
| **Κ18γ** | αντιπαροχή σε ωμό `'Διαμέρισμα'` ⇒ `exchange-requires-land` *(ο συμμετρικός: η κανονικοποίηση **δεν** κάνει «τα πάντα γη»)* |

---

##### ⛔ ΤΙ ΔΕΝ ΕΚΑΝΕ ΑΥΤΗ Η ΑΛΛΑΓΗ

**Δεν άγγιξε το σύνορο ανάγνωσης** — και το μέτρησε πριν το αποφασίσει. **Και οι δύο πύλες
γραφής είναι ήδη κλειστές**: το `owner-property-draft-schema.ts` δηλώνει
`z.enum(PROPERTY_TYPES)` *(μόνο κανονικές)* και το `property-mutation-gateway.ts:164`
κανονικοποιεί **και πετά** στην αποτυχία. Άρα **καμία νέα εγγραφή δεν μπορεί να γεννήσει
legacy τιμή**· το παλαιό λεξιλόγιο είναι **ιστορικό και δεν μεγαλώνει**. Αυτό είναι που
ακυρώνει το γνωστό κόστος της «κανονικοποίησης στην ανάγνωση» *(«ο κώδικας μετανάστευσης
πρέπει να ζει στην εφαρμογή επ' αόριστον»)*: εδώ το σύνολο είναι **κλειστό**.

🔴 **Αυτό που μένει είναι ότι Ο ΤΥΠΟΣ ΛΕΕΙ ΨΕΜΑΤΑ**: τα `OwnerProperty.type` και
`PublicListing.type` δηλώνονται `PropertyType` *(η ευρεία ένωση)*, και το
`public-listing-projection.ts:328` γράφει `(property.type ?? 'apartment') as PropertyType`
— **δεύτερο `as`**, ήδη καταγγελμένο γραπτώς στο `owner-property-draft-schema.ts`. Όσο ο
τύπος είναι ευρύς, **κάθε** καταναλωτής οφείλει να θυμηθεί τον σωστό κριτή και **τίποτε
δεν ελέγχει ότι το έκανε**. ⇒ **νέο §8 #11**.

📊 Νέο αρχείο: `src/constants/property-classification.ts` *(leaf: εισάγει και τα δύο,
κανένα δεν το εισάγει ⇒ ακυκλικό **εξ ορισμού**, όχι κατά σύμβαση)*. Μητρώο:
`property-classification` *(tier 3)*, με pattern που **αποδεδειγμένα ταιριάζει** τις
δηλώσεις του SSoT και **δεν** ταιριάζει καταναλωτές ή imports — όχι αδρανής φρουρός
*(ADR-749: **606 από 671** patterns χωρίς απόδειξη ζωής)*.

---

## 8. Δηλωμένα ανοιχτά θέματα

1. ~~**Ο ιδιώτης δεν έχει `operationalStatus`.**~~ ✅ **ΕΚΛΕΙΣΕ 2026-09-02 (Φ5, §7.5)**.
   Απόφαση Giorgio: το «σύντομα διαθέσιμο» βαθμολογείται **σαν `under-construction`**, με
   επιχείρημα το **ασύμμετρο κόστος** (η εξαίρεση αφαιρεί μόνο την ποινή· η μη-εξαίρεση ζητά
   **ΠΕΑ που ο νόμος δεν εκδίδει**). Ζει σε **εξαντλητικό πίνακα**
   (`lib/property/property-completion-stance.ts`), όχι σε `if` — και **πάνω στο `PropertyStatus`**,
   γιατί το «coming-soon» **δεν είναι** `CommercialStatus`: φτάνει στη δημόσια αγγελία μόνο μέσω
   ενός ωμού `as` στο `public-listing-projection.ts:307`. ⛔ Καμία τιμή δεν χαρτογραφείται ποτέ σε
   `'draft'`. 🔴 **Νέο χρέος που γεννήθηκε από το εύρημα**: εκείνο το `as` — καταγράφηκε στο
   `.claude-rules/pending-ratchet-work.md`, γιατί η διόρθωσή του αγγίζει το `listing-filters.ts`.
2. **Το `OwnerPropertyMedia` δεν ξεχωρίζει φωτογραφία από κάτοψη**
   *(`types/owner-property.ts:162-187`)*, ενώ η μηχανή απαιτεί `{photos, floorplan}` και δίνει στην
   κάτοψη **πλήρες** βάρος. Χωρίς τη διάκριση, η Φ6 (`measured`) **δεν έχει είσοδο**.

   🔴 **Και ούτε το `ListingImage` το ξεχωρίζει** *(μετρημένο στη Φ5)*: `url` · `width` · `height` ·
   `altKey` · `sources` — **κανένα είδος**. Άρα ο δείκτης της Φ5 στέλνει **πάντα `floorplan: 0`**,
   που είναι η αλήθεια και όχι εικασία. 📐 **Το κόστος μετρήθηκε πριν γίνει δεκτό**: μέγιστο
   `21/23 = 91%` για διαμέρισμα και `7/9 = 78%` για αποθήκη — **και τα δύο πάνω** από το κατώφλι
   του πράσινου (71%), άρα κανείς δεν κλειδώνεται έξω. *(Αν δεν ίσχυε, η σωστή κίνηση θα ήταν
   **εξαίρεση** της κάτοψης, όχι σιωπή.)*
3. ~~**Ταίριασμα `PropertyType` ⊆ `PropertyTypeCanonical`**~~ ✅ **ΕΚΛΕΙΣΕ 2026-09-02 (Φ5, §7.5)**.
   **Επαληθεύτηκε ότι ΠΑΡΗΓΕ σιωπηλά λάθος παρονομαστή**, σε **4 από τις 9** μη κανονικές τιμές —
   χειρότερη η `'Αποθήκη'`, που βαθμολογούνταν με **23,0** αντί για **9,0** *(+156%)* και της
   ζητούνταν **υπνοδωμάτια ως κρίσιμα**. Θεραπεία: το `getFieldWeightsForType` ρωτά την
   **υπάρχουσα** αυθεντία `normalizePropertyType` — κανένα βάρος δεν άλλαξε (Α5). ⚠️ Μετρήθηκε ότι
   και τα **8** ζωντανά έγγραφα έχουν κανονικό είδος ⇒ ήταν **λανθάνον, όχι ενεργό**, και η
   θεραπεία αλλάζει **μηδέν** σημερινές βαθμολογίες.
4. ~~**Ποια χαρακτηριστικά είναι `'attribute'` και ποια όχι**~~ ✅ **ΕΚΛΕΙΣΕ 2026-09-02 (Φ3, §7.4)**.
   Απόφαση Giorgio: **(β)+(γ) μαζί** — νέος ρόλος **`'feature-set'`** για τα σύνολα *(γιατί έχουν
   **τρεις** καταστάσεις και το `'attribute'` ξέρει δύο)*, **ομαδοποίηση** στην οθόνη για τα μονά
   πεδία, και **ισοζύγιο ανά ομάδα** με τα κενά πίσω από **ένα** επίπεδο αποκάλυψης.
5. **Η ταυτότητα του σχεδίου στο `measured`** *(άνοιξε στη Φ2)*. Το `sourceRef` είναι σήμερα
   `string` **επίτηδες**: ποιο σχέδιο, ποια έκδοση, και **πότε παλιώνει** μια μέτρηση όταν το
   σχέδιο αλλάξει, ανήκουν στη **Φ6**. Ορισμός δομημένης ταυτότητας **πριν** υπάρξει ο
   παραγωγός θα ήταν επινόηση — και θα ήταν το τρίτο λεξιλόγιο ταυτότητας σχεδίου.
   ⚠️ **Ανοιχτό μαζί του**: μια `measured` τιμή από **παλιό** σχέδιο εξακολουθεί να νικά μια
   φρέσκια `declared`. Η κατάταξη ρωτά **πηγή**, όχι **φρεσκάδα**.
6. **Το κίνητρο κατάταξης** (§4, μοτίβο Spitogatos) δεν αποφασίστηκε: η πληρότητα επηρεάζει τη σειρά
   στα αποτελέσματα ή μόνο ένα σήμα; Αγγίζει το ADR-777 και **δεν** αποφασίζεται εδώ.

7. ~~🔴 **ΤΟ `levels` ΤΗΣ ΜΕΖΟΝΕΤΑΣ ΕΙΝΑΙ ΓΝΩΣΤΟ ΚΑΙ ΔΗΜΟΣΙΕΥΕΤΑΙ ΩΣ «ΔΕΝ ΕΧΕΙ ΔΗΛΩΘΕΙ»**~~ ✅ **ΕΚΛΕΙΣΕ 2026-09-02 (Φ5, §7.5)**
   *(μετρημένο σε ζωντανό έγγραφο 2026-09-02, Φ3)*. Το `prop_2d612992` έχει
   `isMultiLevel: true` και `levels[]` με **δύο** εγγραφές — αλλά το `layout.levels`, που
   διαβάζει η προβολή, είναι **απόν**. Άρα η δημόσια αγγελία λέει *«Επίπεδα: δεν έχει
   δηλωθεί»* για ακίνητο του οποίου την πολυεπίπεδη φύση **το σύστημα ήδη κατέχει**.

   ⚠️ **Είναι παραβίαση του ίδιου του κανόνα του έργου** — *«ποτέ ό,τι μπορούμε να
   **υπολογίσουμε**»* (Α14 §17.2 · Α1 αυτού του ADR): δείχνουμε κενό εκεί που έχουμε γνώση.

   ⛔ **ΔΕΝ διορθώθηκε στη Φ3, και ο λόγος είναι ότι είναι ΑΠΟΦΑΣΗ, όχι πέρασμα**: αλλάζει τη
   σημασία δημόσιου πεδίου από **δηλωμένο** σε **παραγόμενο**, και ανοίγει το ερώτημα *«με
   ποια προέλευση;»* — που είναι ακριβώς το `AttributeProvenance` της Φ2 (`declared` vs
   `measured`) και ανήκει στη **Φ4/Φ6**. Ένα σιωπηλό `?? levels.length` εδώ θα δημοσίευε
   **παράγωγο ως δήλωση κατόχου**.

   ✅ **Η ΑΠΟΦΑΣΗ ΠΟΥ ΠΑΡΘΗΚΕ (Giorgio, 2026-09-02): παράγωγο ΜΕ ΠΡΟΕΛΕΥΣΗ.** Το
   `PublicListing.levels` έγινε **`SourcedAttribute<number> | null`** — δήλωση ⇒ `declared`, δομή
   ⇒ `measured`· σχήμα **5 → 6** με νέο **αμετάβλητο** κρίκο· και το `isAttributeDeclared` ρωτά
   πλέον το `isPubliclyPresentable`, δηλαδή το **Α7 απέκτησε δεύτερη εκτελέσιμη θέση**. Δες §7.5.

   📌 Το ίδιο ερώτημα σηκώνει και το `areaSqm` της **μεζονέτας**: η ρίζα καθρεφτίζει το
   **πρωτεύον** επίπεδο (`gross: 95`), όχι το άθροισμα — συμπεριφορά **προγενέστερη** της Φ3,
   που όμως γίνεται ορατή τώρα που δημοσιεύεται και το `netAreaSqm` δίπλα της.

8. 🔴 **Η ΓΗ ΔΕΝ ΜΠΟΡΕΙ ΝΑ ΠΕΙ ΤΟ ΣΗΜΑΝΤΙΚΟΤΕΡΟ ΠΟΥ ΕΧΕΙ** *(άνοιξε στο §7.6, 2026-09-03)*.
   Και στις **τρεις** πηγές της έρευνας, τα κορυφαία πεδία μιας αγγελίας γης είναι το
   **δικαίωμα δόμησης** *(Σ.Δ. · συντελεστής κάλυψης · άρτιο/οικοδομήσιμο · εντός/εκτός σχεδίου
   — `calificación urbanística` · `Zoning`)* και οι **παροχές & πρόσβαση** *(ρεύμα · νερό ·
   αποχέτευση · πρόσοψη σε δρόμο — `Utilities` · `Road Frontage Type` · `servicios` · `acceso`)*.

   ⛔ **Κανένα από αυτά δεν υπάρχει στο μοντέλο**: ούτε στα 8 πεδία του `OwnerProperty`, ούτε
   στα **27** του `PublicListing` μετά τη Φ3. Δηλαδή σήμερα μια αγγελία οικοπέδου **δεν έχει
   πού να τα γράψει**, και ο αγοραστής δεν έχει πού να τα διαβάσει.

   🔑 **Γι' αυτό ο πίνακας του §7.6.4 έχει τέσσερα πεδία και όχι οκτώ**: βάρος σε κλειδί που
   κανείς δεν μπορεί να συμπληρώσει είναι μετρητής που **δεν φτάνει ποτέ στο 100%** — και
   είναι ακριβώς η αστοχία που το **Α5** απαγορεύει *(«η μηχανή δεν ξαναερμηνεύει την είσοδο»)*
   από την άλλη πλευρά.

9. ~~🔴 **ΔΥΟ ΚΡΙΤΕΣ ΓΙΑ ΤΟ «ΕΙΝΑΙ ΓΗ;», ΚΑΙ Ο ΕΝΑΣ ΞΕΡΕΙ ΛΙΓΟΤΕΡΑ**~~ ✅ **ΕΚΛΕΙΣΕ
   2026-09-03 (§7.6.11)** *(άνοιξε στο §7.6.9, ίδια μέρα)*.

   **Τι ήταν**: το `isLandPropertyType` ρωτούσε `isCanonicalPropertyType` **σκέτο**, ενώ το
   `completionFieldLabelKey` περνούσε πρώτα από `normalizePropertyType`. Ωμή παλαιά τιμή
   `type: 'Οικόπεδο'` — που ο τύπος `PropertyType` **επιτρέπει ρητά** και που το
   `ownerPropertyFormFrom` περνά **αυτούσια** στη φόρμα — **δεν ήταν γη** για τη φόρμα και
   **ήταν γη** για τον δείκτη. Σοβαρότερο σημείο: το `ownerPropertyDraftFrom` **δεν**
   μηδένιζε `floor`/`bedrooms`, δηλαδή **γραφόταν** «οικόπεδο στον 3ο όροφο».

   🔴 **ΔΥΟ ΠΡΑΓΜΑΤΑ ΠΟΥ ΑΥΤΗ Η ΕΓΓΡΑΦΗ ΕΛΕΓΕ ΛΑΘΟΣ, ΚΑΙ ΔΙΟΡΘΩΘΗΚΑΝ**:

   1. **«8 καταναλωτές σε 3 τομείς» ⇒ ΕΙΝΑΙ 6 (+1).** Ο αριθμός βγήκε από `grep` που
      περιλάμβανε **test αρχεία**· από τις 8 γραμμές, το `property-types.ts:200` είναι
      **εσωτερική** χρήση και το `owner-property-invariants.ts:87` είναι **σχόλιο**.
      Ονομαστικά στο §7.6.11 — μαζί με **έβδομο** σημείο που αυτή η εγγραφή δεν είχε δει
      καθόλου *(`offer-kind-eligibility.ts`, γραμμένο την ίδια μέρα)*, όπου το ελάττωμα
      ήταν **επιτρεπτικό**: βραχυχρόνια μίσθωση **περνούσε σε οικόπεδο**.
   2. **«Η αλλαγή δεν χαλά κανέναν, το διπλό `normalize` είναι ταυτοτικό» ⇒ σωστό αλλά
      ΑΣΧΕΤΟ ΜΕ ΤΟ ΕΜΠΟΔΙΟ.** Το `normalize` **δεν μπορούσε** να μπει στο `propertyClassOf`:
      το `property-type-aliases` **εισάγει** το `property-types`, και το
      `CREATABLE_PROPERTY_TYPES` καλεί το κατηγόρημα **στο module-eval** ⇒ **TDZ
      `ReferenceError`** εξαρτώμενο από τη σειρά φόρτωσης. Η απουσία κανονικοποίησης **δεν
      ήταν παράλειψη· ήταν έξοδος από κύκλο που κανείς δεν είχε γράψει**.

   ✅ **Πώς έκλεισε**: τρίτο leaf module *(`constants/property-classification.ts`)* που
   εισάγει και τα δύο και **κανένα δεν το εισάγει** ⇒ ακυκλικό εξ ορισμού· και —το ουσιώδες—
   τα `propertyClassOf`/`isLandPropertyType` **έπαψαν να εξάγονται**, ώστε ο αδύναμος κριτής
   να **μην είναι επιλογή**. Άγκυρα που το εκτελεί: **Κ2γ**.

   ⚠️ **Το μάθημα του φρουρού μένει γραμμένο**: το Κ2 δοκίμαζε `'Αποθήκη'` — τιμή που δεν
   είναι γη **ούτε μετά** από κανονικοποίηση, άρα ήταν **πράσινο και στις δύο** εκδοχές.
   Σκέλος που μοιάζει με απόδειξη και είναι **εμφάνιση**. Το σκέλος που διαφοροποιεί είναι
   `'Οικόπεδο'` **(Κ2β)**, και μπήκε.

10. 🟡 **ΛΕΙΠΕΙ Η ΜΟΝΑΔΑ, ΟΧΙ Η ΑΓΟΡΑ — ΒΡΑΧΥΧΡΟΝΙΑ ΧΡΗΣΗ ΓΗΣ ΚΑΙ ΕΜΠΟΡΙΚΟΥ** *(άνοιξε στο
    §7.6.10, 2026-09-03)*. Το `leaseShort` δέχεται πλέον **μόνο κατοικία**, και ο αποκλεισμός
    του **εμπορικού** δεν έχει τον ίδιο λόγο με της γης: ένα γραφείο, μια αίθουσα ή ένα
    κατάστημα **όντως** νοικιάζονται βραχυχρόνια *(co-working, pop-up, εκδήλωση)* — αλλά **ανά
    ώρα ή ανά ημέρα**, και το «μέγιστος αριθμός επισκεπτών» εκεί σημαίνει **χωρητικότητα**, όχι
    πόσοι κοιμούνται. Το ίδιο και η γη: κατασκήνωση *(Hipcamp)*, στάθμευση & αποθήκευση
    *(Neighbor.com, **ανά μήνα**)*, γυρίσματα & εκδηλώσεις *(Peerspace · Giggster, **ανά ώρα**)*.

    ⇒ **Αν κάποτε θέλουμε αυτή την αγορά, ΔΕΝ είναι χαλάρωση του πίνακα — είναι ΠΕΜΠΤΟ είδος
    διάθεσης με ΔΙΚΗ ΤΟΥ μονάδα** *(ανά ώρα / ανά ημέρα)* και δικό του λεξιλόγιο χρήσης
    *(κατασκήνωση · στάθμευση · γύρισμα · εκδήλωση · βοσκή)*. Το `OFFER_KIND_CLASSES` θα του
    δώσει γραμμή `['land', 'commercial']` και **δεν θα αγγίξει** το `leaseShort`.

    ⛔ **Η εύκολη κίνηση είναι λάθος**: να μπει απλώς το `commercial` στο `leaseShort`. Τότε η
    οθόνη θα ζητούσε *«Τιμή ανά **διανυκτέρευση**»* και *«Μέγιστος αριθμός **επισκεπτών**»* για
    **αίθουσα εκδηλώσεων** — το **ίδιο** ελάττωμα με το οικόπεδο, μετακινημένο μία κλάση
    παρακάτω. Το φυλά το **Κ23**, που μπήκε **επειδή το βρήκε μετάλλαξη** *(δες §7.6.10)*.

   ⚠️ **Το κόστος δεν είναι νέα κλειδιά βαρών — είναι νέα ΠΕΔΙΑ**: μοντέλο (`OwnerProperty` +
   `PublicListing` + κρίκος σχήματος) · φόρμα · i18n · προβολή · δημόσια οθόνη. Και σηκώνει
   δική του απόφαση προϊόντος: *«ποια από αυτά είναι **δήλωση κατόχου** και ποια **έγγραφο**;»*
   — το «άρτιο και οικοδομήσιμο» σε βεβαίωση μηχανικού **δεν** είναι το ίδιο πράγμα με το
   «άρτιο και οικοδομήσιμο» που λέει ο πωλητής. Είναι ακριβώς η διάκριση `declared` ⇄
   `measured` της **Φ2**, στην πιο εμπορικά κρίσιμη μορφή της.

   📌 Καταγράφηκε επίσης στο `.claude-rules/pending-ratchet-work.md`.

11. 🔴 **Ο ΤΥΠΟΣ ΛΕΕΙ ΨΕΜΑΤΑ — Η ΚΑΝΟΝΙΚΟΠΟΙΗΣΗ ΔΕΝ ΕΧΕΙ ΦΤΑΣΕΙ ΑΚΟΜΗ ΣΤΟ ΣΥΝΟΡΟ
    ΑΝΑΓΝΩΣΗΣ** *(άνοιξε στο §7.6.11, 2026-09-03)*. Το §7.6.11 έκανε τον κριτή **έναν και
    δυνατό**. Δεν άγγιξε αυτό που **γεννά** την ανάγκη του: τα `OwnerProperty.type` και
    `PublicListing.type` δηλώνονται `PropertyType` — την **ευρεία** ένωση, που περιλαμβάνει
    `DEPRECATED_PROPERTY_TYPES` και `LegacyGreekPropertyType`.

    ⚠️ **Συνέπεια, δομική**: κάθε καταναλωτής οφείλει να **θυμηθεί** τον σωστό κριτή, και
    **τίποτε δεν ελέγχει ότι το έκανε**. Ένα σκέτο `type === 'plot'` κάπου αύριο είναι πάλι
    το ίδιο ελάττωμα, και ο μεταγλωττιστής **δεν** έχει λόγο να διαμαρτυρηθεί. Το ίδιο
    δείχνει και το `public-listing-projection.ts:328`: `(property.type ?? 'apartment') as
    PropertyType` — **δεύτερο `as`**, ήδη καταγγελμένο γραπτώς στο
    `owner-property-draft-schema.ts`, που βαφτίζει **διαμέρισμα** κάθε τιμή που λείπει.

    🏆 **Η ώριμη κίνηση είναι ΓΝΩΣΤΗ και έχει όνομα**: *anti-corruption layer* — το `type`
    κανονικοποιείται **μία φορά**, όταν το έγγραφο μπαίνει στον τομέα, και οι τύποι
    οντοτήτων στενεύουν σε `PropertyTypeCanonical`. Τότε **ο μεταγλωττιστής**, όχι μια
    άγκυρα, αποδεικνύει ότι καμία legacy τιμή δεν φτάνει πουθενά — και ο κριτής του §7.6.11
    γίνεται **περιττός**, που είναι το σωστό τέλος για έναν κριτή.

    ✅ **ΚΑΙ ΕΙΝΑΙ ΕΦΙΚΤΟ, ΜΕΤΡΗΜΕΝΟ**: **και οι δύο** πύλες γραφής είναι **ήδη κλειστές**
    — `owner-property-draft-schema.ts` *(`z.enum(PROPERTY_TYPES)`)* και
    `property-mutation-gateway.ts:164` *(`normalizePropertyType` + **throw**)*. Άρα καμία
    νέα εγγραφή δεν γεννά legacy τιμή, και το παλαιό λεξιλόγιο είναι **ιστορικό και δεν
    μεγαλώνει**. Αυτό ακυρώνει το κλασικό κόστος της lazy κανονικοποίησης *(«ο κώδικας
    μετανάστευσης ζει στην εφαρμογή επ' αόριστον»)*: εδώ το σύνολο είναι **κλειστό**, άρα
    ο δρόμος τελειώνει — με backfill και **αφαίρεση** του legacy λεξιλογίου από την ένωση.

    ⚠️ **Μέγεθος**: αγγίζει `types/owner-property.ts` · `types/public-listing.ts` *(τύποι
    που διαβάζουν ~46 αρχεία)* · τη δημόσια προβολή · τις διαδρομές ανάγνωσης ⇒ **2+ τομείς,
    N.8, χωριστή εντολή**. ⛔ **Μην το ξεκινήσεις μαζί με άλλη εργασία σε μοιρασμένο δέντρο.**

---

## 9. Changelog

| Ημερομηνία | Τι |
|---|---|
| **2026-09-03** | 🔴 **ΔΥΟ ΚΡΙΤΕΣ ΓΙΑ ΤΟ «ΕΙΝΑΙ ΓΗ;» — ΚΑΙ Η ΘΕΡΑΠΕΙΑ ΗΤΑΝ ΝΑ ΣΒΗΣΕΙ Ο ΕΝΑΣ (§7.6.11, κλείνει το §8 #9).** **Τέταρτο** εύρημα της ίδιας αλυσίδας, και το μόνο που **δεν** το ανέφερε η οθόνη: το βρήκε το §7.6.9 γράφοντας τον uploader του. Το `isLandPropertyType` ρωτούσε `isCanonicalPropertyType` **σκέτο**· το `completionFieldLabelKey` κανονικοποιούσε ⇒ ωμή `type: 'Οικόπεδο'` **δεν ήταν γη** για τη φόρμα και **ήταν** για τον δείκτη, στην **ίδια σελίδα**. 🔴 **Η ΠΡΟΦΑΝΗΣ ΘΕΡΑΠΕΙΑ ΗΤΑΝ ΑΔΥΝΑΤΗ, ΚΑΙ ΑΥΤΟ ΗΤΑΝ ΤΟ ΕΥΡΗΜΑ**: βάζοντας `normalize` στο `propertyClassOf` φτιάχνεται **κύκλος** (`property-type-aliases` **εισάγει** το `property-types`), και επειδή το `CREATABLE_PROPERTY_TYPES` καλεί το κατηγόρημα **στο module-eval**, το αποτέλεσμα είναι **TDZ `ReferenceError` εξαρτώμενο από τη σειρά φόρτωσης του bundler** — *δουλεύει σε dev, σκάει σε prod*. ⇒ **η σημερινή απουσία κανονικοποίησης ΔΕΝ ήταν παράλειψη, ήταν έξοδος από κύκλο που κανείς δεν είχε γράψει**. 🏆 **Πρότυπο σε τρία επίπεδα** (*anti-corruption layer* · *parse, don't validate* · *«pull the shared contract up»*) — **και το ξεπερνάμε**: το νέο leaf `constants/property-classification.ts` (`resolvedPropertyClassOf` · `isLandProperty`) δεν **προσθέτει** δυνατό κριτή, **σβήνει τον αδύναμο**: τα `propertyClassOf`/`isLandPropertyType` **έπαψαν να εξάγονται** και έμειναν ιδιωτικά, όπου τρέχουν μόνο πάνω στο `PROPERTY_TYPES` (κανονικές εξ ορισμού). ⇒ **ο αδύναμος κριτής δεν είναι λάθος επιλογή· δεν είναι επιλογή** — *parse, don't validate* στην **επιφάνεια των συναρτήσεων**, όχι στα δεδομένα. 🔢 **ΔΙΟΡΘΩΣΗ ΑΡΙΘΜΟΥ: 6, ΟΧΙ 8.** Το §7.6.9 και το §8 #9 έγραφαν «8 καταναλωτές σε 3 τομείς» — `grep` που μετρούσε test αρχεία· από τις 8 γραμμές, μία είναι **εσωτερική χρήση** και μία **σχόλιο**. Ονομαστικά οι 6, με **4 από αυτούς να βλέπουν όντως ωμή τιμή** (οι άλλοι 2 έχουν φρουρό ή τρέχουν πάνω στο `PROPERTY_TYPES`). ➕ **Έβδομο σημείο που το §8 #9 δεν είχε δει**: `offer-kind-eligibility.ts` (γραμμένο την **ίδια μέρα**), όπου το ελάττωμα ήταν **ΕΠΙΤΡΕΠΤΙΚΟ** — το *«άγνωστο είδος ⇒ `true`»* έκανε την ωμή `'Οικόπεδο'` να **μοιάζει με «δεν απάντησε»** ⇒ **βραχυχρόνια μίσθωση περνούσε σε οικόπεδο**, δηλαδή το περιστατικό του §7.6.10 **ξανα-ανοιγμένο από την πλαϊνή πόρτα**· ίδιο και ο συμμετρικός (αντιπαροχή σε ωμό `'Διαμέρισμα'`). ⚠️ *Το μοτίβο «αριθμός που ταξιδεύει χωρίς να ανοίξει κανείς το αρχείο» είναι ακριβώς αυτό που καταγγέλλει το `CLAUDE.md` — και συνέβη εδώ, μέσα σε μία μέρα.* 🔬 **Άγκυρες που ΔΙΑΦΟΡΟΠΟΙΟΥΝ**: **Κ2β** (ωμή γη + πεζά/άτονα/με κενά) · **Κ2γ** (🔴 ο αδύναμος **δεν είναι εισαγώγιμος**: `'isLandPropertyType' in module === false` — κοκκινίζει αν ξαναμπεί το `export`) · **Μ2β2** (`ownerPropertyDraftFrom` με ωμή τιμή ⇒ `floor`/`bedrooms` μηδενίζονται — το Μ2β περνούσε `'plot'`, άρα ήταν πράσινο και πριν) · **Κ18β** + **Κ18γ**. ⚠️ Το **Κ2** (`'Αποθήκη'`) **μένει** αλλά καταγράφεται ρητά ως **εμφάνιση, όχι απόδειξη**: ήταν πράσινο **και στις δύο** εκδοχές. 📊 **73/73 πράσινα** σε 4 σουίτες. Μητρώο SSoT: `property-classification` (tier 3), με pattern **αποδεδειγμένα ζωντανό** (ταιριάζει τις δηλώσεις του SSoT, **όχι** καταναλωτές/imports) — όχι αδρανής φρουρός (ADR-749). 🔴 **Νέο ανοιχτό θέμα §8 #11**: **ο τύπος λέει ψέματα** — `OwnerProperty.type`/`PublicListing.type` είναι η **ευρεία** ένωση, οπότε κάθε καταναλωτής οφείλει να *θυμηθεί* τον σωστό κριτή και τίποτε δεν το ελέγχει· η ώριμη κίνηση είναι *canonicalization at the read boundary* + στένεμα σε `PropertyTypeCanonical`, που κάνει τον κριτή **περιττό**. ✅ **Και είναι εφικτή, μετρημένο**: **και οι δύο** πύλες γραφής είναι ήδη κλειστές (`z.enum(PROPERTY_TYPES)` · `normalizePropertyType`+throw) ⇒ το legacy λεξιλόγιο είναι **ιστορικό και δεν μεγαλώνει**, άρα ο δρόμος **τελειώνει** (backfill + αφαίρεση από την ένωση) αντί να ζει «επ' αόριστον». |
| **2026-09-03** | 🔴 **Α6 — Η ΓΗ ΔΕΝ ΕΙΧΕ ΓΡΑΜΜΗ ΠΟΥΘΕΝΑ, ΚΑΙ Η Φ5 ΤΟ ΕΚΑΝΕ ΚΑΤΑΡΡΕΥΣΗ** (§7.6). Αναφορά **από την παραγωγή**: `TypeError: Cannot read properties of undefined (reading 'map')` — `FIELD_WEIGHTS['plot']` δεν υπήρχε, και **5 στα 5** ζωντανά έγγραφα γης είναι `plot` ⇒ **κάθε** οικόπεδο έριχνε τη σελίδα λεπτομέρειας. 🔑 **Η ρίζα δεν ήταν η γη — ήταν ότι ο φρουρός υπήρχε και ήταν ΣΒΗΣΤΟΣ**: το `plot` μπήκε στο λεξιλόγιο **14 ημέρες** νωρίτερα (ADR-777 §8.32) και ο `Record<PropertyTypeCanonical, …>` **θα** το είχε πιάσει, αλλά ο `npm run typecheck` πεθαίνει σε **OOM**. 🔴 **Και τη ρίψη την πυροδότησε η ίδια η Φ5**: η διόρθωση του §8 #3 μετακίνησε την ερώτηση από *«υπάρχει γραμμή;»* σε *«αναγνωρίζεται το είδος;»* — σωστή κίνηση που μετέτρεψε **σιωπηλό** ελάττωμα σε **κατάρρευση**. 📐 **Έντεκα τόποι, όχι ένας**: 6 πίνακες + **6 χειρόγραφα αντίγραφα** του `KNOWN_PROPERTY_TYPES` — που ήταν ταυτόχρονα **η αιτία** και **η ασπίδα** (απέρριπταν τη γη πριν φτάσει στον πίνακα), και ήταν κατά λέξη η SSoT `isCanonicalPropertyType` **που ήδη υπήρχε**. 🏆 **Τα βάρη της γης από ΕΡΕΥΝΑ** (CRMLS Land form/Zillow · idealista · Spitogatos): από όσα έχει το μοντέλο, τα ζητούμενα είναι **τέσσερα** — ταυτότητα · εμβαδόν · **τοπογραφικό** *(το `Survey`, κρίσιμο)* · φωτογραφίες. Παρονομαστής **8,0** έναντι 23,0· τα 13 κλειδιά κτίσματος **exempt**. ⛔ Το `?? apartment` απορρίφθηκε ρητά — είναι το §8 #3 με άλλο όνομα. 🔒 Ο επιλυτής: **fail-fast σε dev, `CRITICAL_IDENTITY` σε production** — ποτέ λευκή οθόνη σε άνθρωπο, ποτέ σιωπή σε μηχανικό. 🔬 **39 tests · μεταλλάξεις 4/4 κόκκινες — και η 4η ΜΕΤΑ από διόρθωση της ίδιας της άγκυρας**: η πρώτη γραφή καλούσε τις μηχανές με **κενά** ορίσματα, δηλαδή δοκίμαζε την **έξοδο** και όχι τη μηχανή· η `SPARSE ⊕ DENSE` κλήση αποκάλυψε **δεύτερη ζωντανή κατάρρευση** (`FLOOR_TYPE_MATRIX['plot'][band]` για οικόπεδο **με δηλωμένο όροφο**) που **καμία αναφορά δεν είχε δείξει**. **328 → 366 πράσινα στα `src/constants` · 791/792 στους καταναλωτές** *(η μία κόκκινη είναι **ξένη και προϋπάρχουσα**: το `2d7f481c` ανέβασε το σχήμα σε 7 χωρίς να ενημερώσει το `listing-levels-provenance.test.ts`)*. 🖥️ **Η ΕΠΑΛΗΘΕΥΣΗ ΣΤΗΝ ΟΘΟΝΗ ΒΡΗΚΕ ΔΕΥΤΕΡΟ ΕΛΑΤΤΩΜΑ (§7.6.8)**: η σελίδα απέδωσε πλήρως και η οθόνη συμφώνησε με τη μέτρηση **γραμμή προς γραμμή** *(50% · 8 · 13 exempt)* — και έλεγε **«Τι λείπει: ΚΑΤΟΨΗ»** σε ιδιοκτήτη οικοπέδου. Η βαθμολόγηση σωστή, **685 tests πράσινα**, μόνο η **λέξη** λάθος: καμία μηχανή δεν μπορούσε να το πιάσει. ⇒ `completionFieldLabelKey` **SSoT** *(οι ετικέτες έχουν **δύο** αναγνώστες — ο δείκτης του ιδιώτη και η εταιρική ανάλυση)*, `completion.fields.land.*` σε el+en *(«Τοπογραφικό διάγραμμα» · «Εμβαδόν γης»)*, και το `CompletionAssessment` κουβαλά πλέον **κανονικοποιημένο** `propertyType` ώστε να μην ονομάζεται με άλλο είδος από αυτό με το οποίο κρίθηκε. **Ομάδα Λ**: κάθε παραγόμενο κλειδί **λύνεται σε κείμενο σε el ΚΑΙ en** — αλλιώς τυπώνεται αυτούσιο. **Μεταλλάξεις 6/6**. 🧹 **Boy Scout (§7.6.7)**: το `jscpd` κοκκίνισε στα αγγιγμένα αρχεία — κλώνοι **προϋπάρχοντες** *(επαληθευμένο στο HEAD)*, που όμως **δεν έμειναν**: **έξι** αντίγραφα της πόρτας εισόδου *(τρία byte-για-byte ταυτόσημα)* έγιναν `constants/plausibility-input.ts` — **μία** μηχανή, **τέσσερις ονομασμένες πύλες**, ⛔ ποτέ `toNumber(value, {min, integer})` *(σημαίες = η απόφαση εύρους φεύγει στον καλούντα)*. **391 πράσινα στα `src/constants` · 888/889 στους καταναλωτές · jscpd καθαρό στα 12 αρχεία**. 🔴 **Νέο ανοιχτό θέμα §8 #8**: το **δικαίωμα δόμησης** και οι **παροχές/πρόσβαση** — τα κορυφαία πεδία γης και στις τρεις πηγές — **δεν υπάρχουν στο μοντέλο**. |
| **2026-09-03** | 🔴 **Η ΦΟΡΜΑ ΡΩΤΟΥΣΕ «ΤΙ ΘΕΛΕΙΣ ΝΑ ΚΑΝΕΙΣ;» ΧΩΡΙΣ ΝΑ ΞΕΡΕΙ «ΤΙ ΕΙΝΑΙ» (§7.6.10).** **Τρίτο** εύρημα από την ίδια οθόνη, ίδια μέρα: είδος **Οικόπεδο**, τσεκ **«Βραχυχρόνια μίσθωση»**, και από κάτω *«Τιμή ανά **διανυκτέρευση**»* · *«Ελάχιστες **διανυκτερεύσεις**»* · *«Μέγιστος αριθμός **επισκεπτών**»*. 🔑 **Ίδια κλάση με τα §7.6.8/§7.6.9**: ο `OFFER_NUMBER_FIELDS` είναι `Record<OfferKind, …>` — κλειδωμένος στη **διάθεση**, **τυφλός στο είδος**· και οι τέσσερις επιλογές προσφέρονταν σε κάθε ακίνητο. ⚠️ **Και ο μισός κανόνας ΥΠΗΡΧΕ, κρυμμένος σε `if`**: το *«η αντιπαροχή αφορά ΜΟΝΟ τη γη»* ζούσε **στο σώμα** του `ownerPropertyInvariantViolations` — και **ένας κανόνας σε `if` δεν δείχνει σε κανέναν ότι λείπει ο διπλανός του**, γι' αυτό ο συμμετρικός δεν γράφτηκε ποτέ. 🏆 **Η αγορά είναι ομόφωνη, και η διάκριση είναι η ΜΟΝΑΔΑ** *(Airbnb: δεν δέχεται γη · Hipcamp: ναι, αλλά ρωτά **πρώτα** υποδομή · Peerspace/Giggster: **ανά ώρα** · Neighbor.com: **ανά μήνα** · LoopNet ground lease: **ανά 25–99 έτη**)* — **η διανυκτέρευση εμφανίζεται μόνο όταν υπάρχει κάτι που κοιμάσαι μέσα**· το ίδιο λέει και το Μητρώο Βραχυχρόνιας Διαμονής (ν. 4446/2016), που δέχεται **κατοικίες**. ⇒ **ΕΝΑΣ κριτής, ΔΥΟ μηνύματα** *(απόφαση Giorgio: «μία γραμμή ποιες διαθέσεις έχουν νόημα»)*: `constants/offer-kind-eligibility.ts` → `OFFER_KIND_CLASSES` *(`sell`/`leaseOut` **όλες** — η γη πωλείται και εκμισθώνεται κανονικά· `exchange: [land]`· `leaseShort: [residential]`)*, **ανά ΚΑΤΗΓΟΡΙΑ ποτέ ανά είδος** *(λίστα ειδών θα ξεχνούσε το τρίτο είδος γης)*, και **δύο** κωδικοί γιατί η κίνηση του ανθρώπου είναι **αντίθετη** *(«διάλεξε γη» ⇄ «διάλεξε κατοικία»)*. ⛔ **Όχι απόκρυψη τσεκ, όχι σιωπηλό φιλτράρισμα**: επιλογή που εξαφανίζεται αφού τσεκαριστεί δεν **ξε**-τσεκάρεται, και σιωπηλό φιλτράρισμα είναι το ίδιο ψέμα με το «οικόπεδο στον 3ο όροφο». 🔬 **Μεταλλάξεις 6/6 — και η μία ΒΡΗΚΕ ΚΕΝΟ ΠΟΥ Η ΣΧΕΔΙΑΣΗ ΔΕΝ ΕΙΔΕ**: με άγκυρες **μόνο σε γη**, η μετάλλαξη `leaseShort: [residential, commercial]` έμενε **ΠΡΑΣΙΝΗ** ⇒ ο αποκλεισμός του εμπορικού ήταν **δηλωμένη απόφαση χωρίς φρουρό** ⇒ **Κ23**. **998 πράσινα / 37 σουίτες**· `i18n.ts` αναγεννήθηκε (**CHECK 3.33 OK, 107 locale**). 🔴 **Νέο ανοιχτό θέμα §8 #10**: η βραχυχρόνια χρήση **γης και εμπορικού** είναι υπαρκτή αγορά — λείπει η **μονάδα**, όχι η αγορά· είναι **πέμπτο είδος διάθεσης** *(ανά ώρα/ημέρα)*, ⛔ **ποτέ** χαλάρωση του `leaseShort`. |
| **2026-09-03** | 🖥️ **Η ΙΔΙΑ ΛΕΞΗ, ΔΕΥΤΕΡΗ ΕΠΙΦΑΝΕΙΑ — Η ΘΕΡΑΠΕΙΑ ΤΟΥ §7.6.8 ΕΛΥΣΕ **ΤΟΝ ΕΝΑ ΑΝΑΓΝΩΣΤΗ** (§7.6.9).** Νέα αναφορά **από την οθόνη**, ίδια μέρα, ίδια διεύθυνση: με είδος **Οικόπεδο**, το πεδίο ανεβάσματος ζητούσε *«φωτογραφίες, **κάτοψη**, PDF»* ενώ ο δείκτης πληρότητας **δύο κάρτες παρακάτω** έλεγε σωστά *«λείπει: Τοπογραφικό διάγραμμα»* — **η ίδια σελίδα, δύο απαντήσεις**. 🔑 **Το εύρημα δεν είναι το κείμενο, είναι ΠΟΙΟΣ ΡΩΤΗΣΕ**: η απόφαση υπήρχε και είχε **δική της άγκυρα** (ομάδα Λ, *«η γη ΔΕΝ λέει Κάτοψη»*)· το uploader ήταν το **μόνο** σημείο που δεν ρώτησε κανέναν. ⚠️ **Και το §7.6.8 δεν μπορούσε να το προβλέψει**: απαρίθμησε τους αναγνώστες των **ετικετών πεδίων** και τους κάλυψε και τους δύο — το uploader δεν είναι αναγνώστης ετικέτας, είναι **δική του πρόταση** σε **άλλο namespace** (`property-market`). ⇒ `isLandPropertyType(form.watch('type'))` — **αυτό ακριβώς** το κατηγόρημα ρωτά ο αδελφός `OwnerBasicsFields` **δύο πεδία πιο πάνω** για το «Εμβαδόν οικοπέδου»· `offer.media.landLabel`/`landHelp` σε **el + en** («Φωτογραφίες και τοπογραφικό» · «Photos and survey plan», ακολουθώντας το `Survey plan` του §7.6.8). ⛔ **Όχι μετονομασία του κοινού κλειδιού** — θα ζητούσε τοπογραφικό για **διαμέρισμα** (το φυλά το `Κ3`). 🔬 **Άγκυρα με ΑΠΟΔΟΣΗ, όχι με έλεγχο κλειδιών** (10 δοκιμασίες): ένα test *«λύνεται το landLabel;»* θα ήταν **πράσινο με το ελάττωμα ζωντανό** — τα κλειδιά υπήρχαν ήδη αλλού. Η ερώτηση του ανθρώπου ήταν **«τι λέει η οθόνη μου;»**. **Μεταλλάξεις 3/3 κόκκινες**, με τα Κ3/Κ4 σωστά πράσινα. 🔴 **Νέο ανοιχτό θέμα §8 #9**: **δύο κριτές για το «είναι γη;»** — το `isLandPropertyType` **δεν** κανονικοποιεί, το `completionFieldLabelKey` **ναι**, άρα ωμή τιμή `'Οικόπεδο'` δεν είναι γη για τη φόρμα και είναι για τον δείκτη. ⛔ **Δεν διορθώθηκε εδώ επίτηδες**: δυνατός κριτής μόνο στο uploader θα έκανε την **ίδια οθόνη** να αυτο-αντικρούεται. 8 καταναλωτές / 3 τομείς ⇒ N.8. ⚠️ Και ο **υπάρχων** φρουρός (`property-type-classes` Κ2) είναι **πράσινος και στις δύο** εκδοχές: δοκιμάζει `'Αποθήκη'`, που δεν είναι γη **ούτε μετά** από κανονικοποίηση. |
| **2026-09-02** | ✅ **Φ5 εκτελέστηκε** (§7.5): **ο ιδιώτης βλέπει πόσο πλήρης είναι η αγγελία του και τι να κάνει μετά** — με **μηδέν** νέα μηχανή, **μηδέν** νέα βάρη, **μηδέν** νέα κλειδιά i18n. 🔴 **Το σχέδιο έλεγε λάθος είσοδο**: υποσχόταν adapter από τον `OwnerProperty` (**8** πεδία)· η μέτρηση βρήκε ότι το `PublicListing` έχει **27** και καλύπτει **και τις δύο** οικογένειες — **13** πεδία ταυτόσημα, **3** μετονομασίες, **1** ανύπαρκτο (`floorplan`). ⛔ **Η δύσκολη απόφαση**: ο μεταφραστής **ΔΕΝ ξαναερμηνεύει** — το `[]` («απάντησε: καμία») και το `wc: 0` βαθμολογούνται ως έλλειψη, γιατί ο **δείκτης-φάντασμα** θα ήταν το **Α5 παραβιασμένο από την είσοδο**, δεν γενικεύεται στους αριθμούς, και η ρίζα είναι στη μηχανή του ADR-287 — **ξεχωριστό ADR**, με την απόκλιση **δηλωμένη σε άγκυρα**. **Τρία ανοιχτά θέματα έκλεισαν**: **#1** (στάση ανά διάθεση, εξαντλητικός πίνακας, καμία τιμή ποτέ σε `draft`), **#3** (*«σιωπηλά λάθος παρονομαστής» ΕΠΑΛΗΘΕΥΤΗΚΕ* — `'Αποθήκη'` έπαιρνε **23,0** αντί για **9,0**, +156%, με **υπνοδωμάτια ως κρίσιμα**· θεραπεία = ρωτά την **υπάρχουσα** αυθεντία), **#7** (τα επίπεδα = **παράγωγο με προέλευση**: `SourcedAttribute<number>`, σχήμα **5 → 6** με νέο αμετάβλητο κρίκο, **πρώτος πραγματικός καταναλωτής** του δοχείου της Φ2, και **δεύτερη εκτελέσιμη θέση για το Α7**). 🔴 **Ο κώδικας ανέτρεψε το ερώτημα του #1**: το «coming-soon» **δεν είναι** `CommercialStatus` (επτά τιμές) — ζει στο `PropertyStatus` (δώδεκα) και φτάνει δημόσια **μόνο μέσω ωμού `as`** στο `public-listing-projection.ts:307`· γι' αυτό ο πίνακας κλειδώθηκε στο **υπερσύνολο**, αλλιώς θα ήταν **φρουρός χωρίς απόδειξη ζωής**. 🔑 **Ο τύπος απαγορεύει πλέον το `[object Object]`** (key-aware `AttributeValueKindFor`) — η οικογένεια του ωμού κλειδιού από άλλη πόρτα. 🎯 **Η οθόνη**: **τρεις** προτάσεις τη φορά (§6 #7), μετά το δόλωμα και **πριν** το κουμπί — coaching, όχι κατηγορητήριο. **92 νέα tests · μετάλλαξη 19/19 · 10.976 πράσινα / 629 suites · 7 πύλες ✅ · jscpd −2 προϋπάρχοντες κλώνοι**. 🔴 **Και μία μετάλλαξη ΔΡΑΠΕΤΕΥΣΕ πρώτα, με μάθημα**: ο φρουρός του #3 ήταν αδοκίμαστος από τη διαδρομή της Φ5 (ο μεταφραστής στέλνει ήδη κανονικοποιημένο είδος) — η άγκυρα μετακόμισε **δίπλα στη γραμμή**, όπου ζει η **εταιρική** διαδρομή. ⛔ Δηλωμένα: η **προέλευση δεν ζωγραφίζεται** ακόμη (νέα κλειδιά σε σφραγισμένη διαδρομή = πράξη ανθρώπου· ανήκει στη Φ6)· **καμία ανακατασκευή**· **κανένας `tsc`** (N.17)· και **4 κόκκινες suites στο main, προϋπάρχουσες και εκτός του diff** — η μία (`place-interest-custody`) είναι το **τρίτο** ελλιπές fixture της ADR-841. |
| **2026-09-02** | ✅ **Φ3 εκτελέστηκε** (§7.4): **η δημόσια αγγελία πέρασε από 4 σε 27 στοιχεία**, με **μηδέν** νέα ερώτηση προς άνθρωπο — το φράγμα του §2 ήταν η **προβολή**, και έσπασε. Το ανοιχτό θέμα **§8 #4 έκλεισε** με απόφαση Giorgio: ρόλος **`'feature-set'`** + ομάδες με **δικό τους** ισοζύγιο. 🏆 **Η τρίτη κατάσταση**: `null` (κανείς δεν ρώτησε) ≠ `[]` (ο κάτοχος είπε «καμία») — **δομικά ανέκφραστη** σε Zillow · idealista · Spitogatos. 🔴 **Το σχέδιο έπεσε έξω σε τρία σημεία και ο κώδικας νίκησε**: (α) η «αντιγραφή πεδίων» ήταν **κρίση** *«είναι ονομάσιμη αυτή η τιμή;»* έναντι της αυθεντίας της Φ1 — που έτσι απέκτησε τον **πρώτο πραγματικό καταναλωτή** της· (β) ο κρίκος μετανάστευσης γράφει **`null`** στα σύνολα, γιατί `[]` θα ήταν **ισχυρισμός για λογαριασμό** κάθε παλιού κατόχου· (γ) η πύλη 3.34 είχε δίκιο — οι ετικέτες μετακόμισαν από το **εγγυημένο** `search-results` (141 διαδρομές) σε νέο per-route **`listing-detail`**, και **το κέλυφος έπεσε 13.095 → 12.801**, δηλαδή η Φ3 άφησε την εφαρμογή **ελαφρύτερη απ' ό,τι τη βρήκε**. ⚠️ Δηλωμένο: το ταβάνι της `/listing/[id]` ήταν **ήδη σπασμένο** πριν από τη Φ3 (6.664 > 5.935, μετρημένο με τις αλλαγές αφαιρεμένες) — **1.916 από τα 4.433** της νέας σφράγισης **δεν** ανήκουν εδώ. **166 νέα tests · μετάλλαξη 10/10 · 1.647/1.647 πράσινα · 7 πύλες ✅ · jscpd 0**. 🔬 **ΖΩΝΤΑΝΗ ΕΠΑΛΗΘΕΥΣΗ**: χρησμός 3.51 **0 ωμά κλειδιά**, `/listing/<πραγματικό id>` **200**, και **μόνιμη** άγκυρα με τους **πραγματικούς loaders + ICU** αντί για στιγμιότυπο (Μ8: σβήσε το `case` ⇒ **9/9 κόκκινες**). Δύο ευρήματα στην πορεία: τα ζωντανά έγγραφα είναι **`schemaVersion: 3`** *(εκκρεμεί ανακατασκευή από την ADR-841 κιόλας)*, και ο 500 του server στη θύρα 3000 ήταν **πεσμένος render worker**, όχι ο κώδικας. 📐 **Η χαρτογράφηση επαληθεύτηκε σε 8 ΠΡΑΓΜΑΤΙΚΑ έγγραφα**: `layout`/`areas`/`orientations` διαβάζονται **ακριβώς** όπως αποθηκεύονται (και το `garden: 0` υπάρχει ζωντανά), αλλά **12 από τα 23** πεδία είναι απόντα παντού. 🔴 **Νέο ανοιχτό θέμα §8 #7**: η μεζονέτα δημοσιεύει «Επίπεδα: δεν έχει δηλωθεί» ενώ `isMultiLevel: true` — γνώση που έχουμε και δεν λέμε. |
| **2026-09-02** | ✅ **Φ2 εκτελέστηκε** (§7.3): `AttributeProvenance` + `SourcedAttribute<T>` + ο κανόνας του **Α7** σε **μία** εκτελέσιμη θέση. 🔴 **Το σχέδιο έπεσε έξω και ο κώδικας νίκησε**: το `Sourced<T>` **δεν** είναι καθολικό (17 αρχεία, όλα survey· `rawText` δεμένο εκεί) και το `location-provenance.ts` **ήδη** δεν το επαναχρησιμοποίησε — έχει δικό του `Attested<T>`. Αποφασιστικός λόγος: κοινό δοχείο ⇒ όλα τα ειδικά πεδία optional ⇒ **`inferred` χωρίς `confirmedAt` θα περνούσε τη μεταγλώττιση**, ακριβώς ό,τι απαγορεύει το Α7 ⇒ **διακριτή ένωση**. **17 tests · μετάλλαξη 3/3 · jscpd 0**. ⛔ Δηλωμένο: το δοχείο **δεν έχει ακόμη καταναλωτές** — τους αποκτά στη Φ4. |
| **2026-09-02** | ✅ **Φ1 εκτελέστηκε** (§7.2): η **λίστα** έγινε η πηγή και ο **τύπος** παράγεται — η απόκλιση λεξιλογίου/dropdown είναι πλέον **δομικά αδύνατη**. Το σχέδιο έλεγε «μετακίνησε τους καταλόγους»· **η μέτρηση βρήκε ότι οι τύποι ήταν ήδη κεντρικοί και το πραγματικό πρόβλημα ήταν η χειρόγραφη επανάληψη των ΤΙΜΩΝ** — 10 στα 10 συμφωνούσαν, δηλαδή **τύχη, όχι δομή**. 🔴 Δεύτερο εύρημα μέσα στην εργασία: η πρώτη γραφή μοιραζόταν **αναφορά** ⇒ ένα `.sort()` dropdown θα μετέβαλλε τη SSoT για όλη την εφαρμογή· διορθώθηκε με `readonly` + `[...X]`. **79 tests · μετάλλαξη 2/2 · 272/272 πράσινα · φρουρός με απόδειξη ζωής 8/8 · 15.789 αρχεία σαρωμένα, 0 παραβιάσεις · jscpd 0**. |
| **2026-09-02** | Γέννηση. Φάση 1 (αναγνώριση) του N.0.1. Μετρήθηκαν **τρία** σύνολα πεδίων (8 / ~60 / 4 δημόσιες ιδιότητες) και βρέθηκε ότι το φράγμα είναι η **προβολή**, όχι η φόρμα (§2). Η φαινομενική σύγκρουση με το §25.6 **διαλύθηκε από την ίδια του την τρίτη πρόταση** (§3). Έρευνα αγοράς: Airbnb «publish first», Zillow Content Completion, CV amenity detection (§4). Εντοπίστηκε το **περιουσιακό στοιχείο DXF/BIM** ως ο δρόμος υπεροχής (§4.1). Αποφάσεις **Α1-Α7**· **9 «ΜΗΝ»**· **5 ανοιχτά θέματα**. Καμία αλλαγή κώδικα. |
