# ADR-773 — Ο χάρτης των **επτά** αρχών χρώματος

**Κατάσταση**: 📍 **ΧΑΡΤΗΣ — μετρημένος, χωρίς υλοποίηση.** Καμία τιμή δεν άλλαξε.
**Ημ/νία**: 2026-08-07 · **Προέκυψε από**: ADR-770 Στρώμα 2β (§13) · **Σχετικά**: ADR-759, ADR-365, ADR-682, ADR-749

> 🔑 **Γιατί υπάρχει ξεχωριστά.** Το ADR-770 §12.2 έγραψε «η εφαρμογή έχει **δύο** συστήματα
> χρωμάτων», και το §12.2 πρόσθεσε ένα **τρίτο, εγκαταλελειμμένο**. Το Στρώμα 2β μέτρησε και
> βρήκε **επτά** — και το ένα από αυτά κρύβει βλάβη **χειρότερη** από εκείνη που ξεκίνησε
> ολόκληρη την εκστρατεία. Ένας χάρτης με λάθος πλήθος δεν είναι ανακρίβεια· είναι ο λόγος
> που κάθε πύλη «κάλυπτε το πρόβλημα» και το πρόβλημα έμενε.

---

## 1. Τα επτά, μετρημένα

| # | Αρχή | Μέσο | Μέγεθος (μετρημένο 2026-08-07) | Θεματικό; |
|---|---|---|---|---|
| 1 | `src/app/globals.css` + `generated/variables.css` | CSS custom properties | **230** μοναδικά ονόματα· 23 επιφάνειες × 15 foregrounds × 2 θέματα | ✅ **ναι, εξ ορισμού** |
| 2 | `design-tokens/modules/foundations.ts` → `colors.*` | σταθερά hex σε TS | **66** hex / **46** μοναδικά · **745** αρχεία αναφέρουν `colors.text.*` | ❌ **σκληρό hex φωτεινού θέματος** |
| 3 | style-object factories στα ίδια modules | αντικείμενα στυλ σε TS | **169** σημεία χρήσης (`style={X.y.z}`) · **159** συναρτήσεις · **27** `rgba()` | ❌ κληρονομούν το #2 |
| 4 | `build:tokens` → `design-system/generated/variables.css` + `design-tokens/generated/tokens.ts` | παραγόμενο CSS + TS | **128** ονόματα· `tokens.ts` = **1** καταναλωτής | ⚠️ **ονόματα αποκλίνοντα** — βλ. §2 |
| 5 | **`src/design-system/` + `src/ui-adapters/react/useSemanticColors`** | **Tailwind κλάσεις** | **110** ωμές κλίμακες σε 2 αρχεία → **875** αρχεία καταναλωτές | ❌ **βλ. §3 — το εύρημα** |
| 6 | `const XXX = '#hex'` σε κώδικα προϊόντος | σταθερές σε TS | **193** σταθερές σε **108** αρχεία | ❌ |
| 7 | Χρώματα από **δεδομένα** (`entry.color`, `layer.color`, `brandColor`) | runtime τιμές | **~6** σημεία κειμένου · **~25** swatch/preview | ❌ **άλυτο με token** |

---

## 2. Το #4 **δεν** είναι εγκαταλελειμμένο — αποτυγχάνει σιωπηλά σε 18 σημεία

Το ADR-770 §12.2 το χαρακτήρισε «εγκαταλελειμμένο, 1 καταναλωτής». Λάθος — ή μάλλον, λάθος
ερώτηση: το `tokens.ts` έχει 1 καταναλωτή, αλλά το `variables.css` είναι **εισαγμένο στη
γραμμή 3** του `globals.css` και **128 ονόματα** από αυτό ζουν στη σελίδα.

Το CHECK 3.40 (`dangling-var`) μέτρησε: **18 δηλώσεις** του `layoutUtilities.cssVars.*`
δείχνουν σε **11 ονόματα που δεν ορίζονται πουθενά**:

```
ΖΗΤΑ                          ΥΠΑΡΧΕΙ
--color-text-tertiary    →    --color-text-muted
--color-bg-primary       →    (κανένα αντίστοιχο)
--radius-sm              →    --border-radius-sm
--spacing-4              →    (άλλη σύμβαση)
```

⇒ Τα declarations είναι *invalid at computed-value time*, οπότε το στοιχείο **κληρονομεί**:
βάφεται με χρώμα που **δεν είναι λάθος — είναι αυθαίρετο**. Το TS ζητά τη σύμβαση ονομασίας
μιας **παλαιότερης** έκδοσης του generator.

⚠️ **Αόρατο σε κάθε στατικό εργαλείο εξ ορισμού**: το ερώτημα «υπάρχει αυτό το var;»
απαντιέται **μόνο** από το φορτωμένο CSS. Το βρήκε το Στρώμα 2β με τον μηχανισμό των **δύο
sentinels** (ADR-770 §13.3), όχι με ανάγνωση κώδικα.

---

## 3. 🔴 Το εύρημα: το #5 κρύβει **1,02:1** — χειρότερο από το 1,01:1 που ξεκίνησε τα πάντα

`src/design-system/tokens/colors.ts:76-78` δηλώνει:

```ts
primary:   'text-slate-900',
secondary: 'text-slate-600',
muted:     'text-slate-400',
```

Υπολογισμένο με το **ίδιο** βαθμονομημένο όργανο (`wcag-contrast.js`) πάνω στα **πραγματικά**
tokens του `globals.css`:

| Κλάση | φωτεινό `--card` / `--background` | **σκοτεινό (ΠΡΟΕΠΙΛΕΓΜΕΝΟ) `--card` / `--background`** |
|---|---|---|
| `text-slate-900` | 15,65:1 · 14,76:1 | 🔴 **1,20:1** · 🔴 **1,02:1** |
| `text-slate-600` | 6,64:1 · 6,26:1 | 🔴 **1,96:1** · 🔴 **2,30:1** |
| `text-slate-400` | 🔴 **2,25:1** · 🔴 **2,12:1** | 5,78:1 · 6,80:1 |

**Και τα τρία είναι μονοθεματικά**, σε εφαρμογή δύο θεμάτων — **ακριβώς η ίδια βλάβη με το
`foundations.ts`, σε άλλο μέσο** (Tailwind κλάση αντί hex). Και **875 αρχεία** καταναλώνουν
τον hook που τα σερβίρει.

Το ADR-759 ξεκίνησε από **1,01:1**. Εδώ είναι **1,02:1**, στο **προεπιλεγμένο** θέμα.

### Γιατί καμία πύλη δεν το ρωτά — και **δεν** είναι κενό καμίας από αυτές

| Πύλη | Τι ρωτά | Γιατί δεν βλέπει αυτό |
|---|---|---|
| **CHECK 3.26** (ADR-365) | «παρακάμπτεις το SSoT με ωμή κλίμακα;» | Τα δύο αρχεία είναι **ρητά στην allowlist** — **ορθά**: *είναι* το SSoT. Η πύλη φρουρεί την **παράκαμψη**, όχι την **ποιότητα** του SSoT. (Baseline σήμερα: **0/0** — η εκστρατεία ADR-365 **ολοκληρώθηκε**. ⚠️ Το `CLAUDE.md` λέει «ratchet 249/86» — **μπαγιάτικο**, είναι ο **αρχικός** αριθμός του ADR-365.) |
| **CHECK 3.38** (ADR-770 Στρ. 1) | «γράφεις `text-primary`;» | Εδώ γράφεται `text-slate-900`. Άλλη κλάση. |
| **CHECK 3.39 / 3.40** | «αυτή η **τιμή** αλλάζει ετυμηγορία;» | Εδώ δεν υπάρχει **τιμή** — υπάρχει **κλάση**. Η τιμή ζει στο Tailwind. |
| **CHECK 3.32** (ADR-710) | παλέτα **γραφημάτων** | άλλο υποσύστημα |

🔑 **Το κενό είναι η ερώτηση που δεν διατυπώθηκε ποτέ**: *«οι κλάσεις που παράγει η κεντρική
αρχή χρώματος είναι θεματικές;»*. Το `color-bridge.ts` **ξέρει** την απάντηση — το σχόλιό του
στη γραμμή 19-22 γράφει κατά λέξη `bg-[hsl(var(--bg-primary))]` ❌ → `bg-background` ✅ — και
τη εφαρμόζει σωστά για τις **επιφάνειες** (`bg-background`, `bg-muted`, `bg-card`). Οι **110**
ωμές είναι αλλού: **49** στο `tokens/colors.ts` (μεταξύ τους τα τρία παραπάνω) και **61** στο
`color-bridge.ts`, όπου είναι **σκόπιμα κατηγορικά** (debug overlays, constraint χρώματα).

⇒ Δηλαδή **ο σωστός τρόπος είναι γνωστός, γραμμένος και μισο-εφαρμοσμένος μέσα στο ίδιο
αρχείο** — το ίδιο σχήμα με το `foundations.ts`/`semanticColors` του ADR-770 §12.2. **Τρίτη
φορά** που το ίδιο μοτίβο εμφανίζεται σε αυτό το έργο.

---

## 4. Ποιο ερώτημα φρουρείται σήμερα, ανά αρχή

| # | Φρουρείται από | Κατάσταση |
|---|---|---|
| 1 | CHECK 3.32 (μόνο παλέτα γραφημάτων) · 3.38/3.39/3.40 το **καταναλώνουν** ως αυθεντία | ✅ είναι η πηγή αλήθειας |
| 2 | **CHECK 3.39** (ρίζα: 43 δηλώσεις) + **CHECK 3.40** (διάδοση: 159) | ✅ φρουρούμενο, ratchet |
| 3 | **CHECK 3.40** (ο browser λύνει τις τιμές) · τα `rgba()` του 3.39 = δηλωμένο όριο Κ5 | ⚠️ **μερικώς** |
| 4 | **CHECK 3.40** (`dangling-var`, 18) | ✅ **νέο** |
| 5 | **ΚΑΝΕΝΑ** ως προς αντίθεση (3.26 = allowlist by design) | 🔴 **ΑΚΑΛΥΠΤΟ** |
| 6 | CHECK 3.38 όταν καταλήγουν σε κλάση· αλλιώς κανένα | ⚠️ **μερικώς** |
| 7 | **ΚΑΝΕΝΑ** — και **δεν λύνεται με token** | 🔴 **ΑΚΑΛΥΠΤΟ, ΑΛΛΟ ΕΡΩΤΗΜΑ** |

---

## 5. Τι **δεν** αποφασίζει αυτό το ADR

Αυτό είναι **χάρτης**, όχι σχέδιο. Ρητά **δεν** αποφασίζει:

- ❌ **Καμία τιμή δεν άλλαξε** σε κανένα από τα επτά. Το #2 έχει **745** καταναλωτές, το #5
  **875** — μετανάστευση με αυτό το μέγεθος θέλει ρητή εντολή, όχι πρωτοβουλία.
- ❌ **Δεν προτείνει αλλαγή του `--primary`** (ADR-682 §5.5, γραπτή απόρριψη).
- ❌ Δεν αποφασίζει αν τα επτά θα γίνουν **ένα**. Το προφανές («όλα να δείχνουν στο #1») είναι
  σωστό ως κατεύθυνση αλλά **δεν** έχει μετρηθεί ως εφικτό: το #7 δεν *μπορεί* να δείξει σε
  token (το χρώμα έρχεται από τον χρήστη) και το #5 παράγει **κλάσεις**, όχι τιμές.

## 6. Τι προτείνεται, με σειρά — και γιατί αυτή

1. **Πύλη για το #5**, ίδιο σχήμα με το 3.39: το `tokens/colors.ts` δηλώνει Tailwind κλάσεις σε
   **σημασιολογικούς ρόλους** (`text.primary`), οπότε το ερώτημα «αλλάζει η ετυμηγορία ανάμεσα
   στα δύο θέματα;» εφαρμόζεται **αυτολεξεί** — αρκεί μια χαρτογράφηση κλίμακας Tailwind → hex.
   Είναι το **φθηνότερο** από όλα και κλείνει το **μεγαλύτερο** ακάλυπτο (875 καταναλωτές).
2. **Το codemod του #2** (ADR-770 §14) — η θεραπεία της μάζας.
3. **Επέκταση `rgba()` στο 3.39** (δηλωμένο όριο Κ5, 27 σημεία): φθηνή, αλλά αγγίζει τη
   baseline του 2α, οπότε **μετά** το commit του.
4. **Διόρθωση των 11 dangling ονομάτων** του #4 — μετονομασία με 18 καταναλωτές, μικρή αλλά
   θέλει εντολή.
5. **on-color για το #7** — άλλο ερώτημα, άλλο ADR. Το χρώμα από δεδομένα δεν διορθώνεται με
   token: ή υπολογίζεται αντίθετο κείμενο σε χρόνο εκτέλεσης (Material 3 `on-*`), ή δεν
   χρησιμοποιείται για κείμενο.

⚠️ **Η σειρά δεν είναι κατά μέγεθος** — είναι κατά **λόγο κάλυψης προς κόστος**. Το #5 είναι
πρώτο γιατί η μηχανή υπάρχει ήδη ολόκληρη και το ακάλυπτο είναι το μεγαλύτερο.

---

## 7. Changelog

| Ημ/νία | Αλλαγή |
|---|---|
| 2026-08-07 | **Δημιουργία.** Προέκυψε από το ADR-770 Στρώμα 2β: το §12.2 έγραφε «**δύο** συστήματα χρωμάτων» (+ ένα τρίτο «εγκαταλελειμμένο»)· η μέτρηση βρήκε **επτά**. 🔴 **Το εύρημα**: `src/design-system/tokens/colors.ts:76-78` δηλώνει `text.primary='text-slate-900'`, `secondary='text-slate-600'`, `muted='text-slate-400'` — **και τα τρία μονοθεματικά**, με **875** αρχεία καταναλωτές. Υπολογισμένο στα πραγματικά tokens: το `text-slate-900` δίνει **1,20:1 σε `--card`** και **1,02:1 σε `--background`** στο **προεπιλεγμένο (σκοτεινό)** θέμα — **χειρότερο** από το 1,01:1 του ADR-759 που ξεκίνησε την εκστρατεία· και το `text-slate-400` σπάει στο **φωτεινό** (2,12:1), ίδιο σχήμα με το `#94a3b8` του §12.3. **Καμία πύλη δεν το ρωτά, και δεν είναι κενό καμίας**: το 3.26 έχει τα αρχεία **ρητά στην allowlist** (ορθά — *είναι* το SSoT· φρουρεί την **παράκαμψη**, όχι την **ποιότητα**), το 3.38 ψάχνει `text-primary`, τα 3.39/3.40 διαβάζουν **τιμές** και εδώ υπάρχει **κλάση**. Η ερώτηση «οι κλάσεις που παράγει η κεντρική αρχή είναι θεματικές;» **δεν διατυπώθηκε ποτέ**. ⚠️ Ο σωστός τρόπος είναι **γνωστός και μισο-εφαρμοσμένος στο ίδιο αρχείο**: το `color-bridge.ts:19-22` γράφει κατά λέξη `bg-[hsl(var(--bg-primary))]` ❌ → `bg-background` ✅ και το εφαρμόζει σωστά για τις **επιφάνειες** — **τρίτη** εμφάνιση αυτού του μοτίβου στο έργο. ⚠️ **Διορθώσεις σε προηγούμενες δηλώσεις**: (α) το #4 (`build:tokens`) **δεν** είναι εγκαταλελειμμένο — έχει **18 ζωντανούς καταναλωτές που όλοι αποτυγχάνουν σιωπηλά** σε **11 ανύπαρκτα** custom properties (απόκλιση ονοματοδοσίας generator), εύρημα του `dangling-var`· (β) η baseline του **CHECK 3.26 είναι σήμερα 0/0** — η εκστρατεία ADR-365 **ολοκληρώθηκε**, ενώ το `CLAUDE.md` επικαλείται ακόμα «249/86», τον **αρχικό** αριθμό. **Καμία τιμή δεν άλλαξε σε κανένα από τα επτά** (745 + 875 καταναλωτές). |
