# ADR-408 — MEP Connectors & Systems (Connectivity Backbone)

| Field | Value |
|---|---|
| Status | 🟢 **Φ1 + Φ2 + Φ3 + Φ4 DONE** — κορμός + πρώτη «πηγή» + ακεραιότητα δικτύου (2026-06-02, Opus 4.8). Φ1+Φ2: connector model (embedded) + MepSystem (persisted, geometry-less) + coordinator. **Φ3: ηλεκτρικός πίνακας** = full point-based BIM element (mirror ADR-406 fixture pipeline· IfcElectricDistributionBoard· outgoing power connector· units-safe panelToMesh) ✅ **BROWSER-VERIFIED 2026-06-02**. **Φ4: cascade/integrity** — delete πίνακα-πηγής→διαλύει τα κυκλώματά του· delete μέλους→βγαίνει από το κύκλωμα· **coherent single-undo** (CompoundCommand bundle) + 2 SSoT commands (Update/Dissolve) μέσω mutator port. **Φ5 DONE + ✅ BROWSER-VERIFIED + COMMITTED** (Circuit UI + colour-by-system + reconciliation). **Φ6 DONE** (2026-06-02, Opus 4.8): Circuit-Management Panel — rename/colour (κεντρικός ColorDialogTrigger)/add-remove member μέσω έτοιμου `UpdateMepSystemParamsCommand`· panel-centric trigger· 144/144 MEP PASS, tsc 0. **Φ7 DONE** (2026-06-03, Opus 4.8): ορατά καλώδια / home-run wires — παράγωγη γεωμετρία (ΟΧΙ persisted), 2D annotation overlay (ADR-040 micro-leaf) + 3D conduit (units-safe TubeGeometry)· daisy-chain + home-run· routing SSoT `computeCircuitWirePaths` (κοινό 2D+3D)· νέα κατηγορία `mep-wire` + View toggle· 115/115 MEP PASS, tsc 0. 🔴 Εκκρεμεί commit (Giorgio) + browser verify Φ6/Φ7 |
| Date | 2026-06-02 |
| Owner | Giorgio / Claude (Opus 4.8) |
| Related | ADR-405 (discipline taxonomy & MEP foundation — **θεμέλιο**), ADR-406 (point-based MEP fixture — **πρότυπο pipeline + ο πρώτος connector host**), ADR-401 (wall attach-to-structural — **πρότυπο coordinator: reference-list + warning event, no mutation**), ADR-195 (entity audit), ADR-355/361 (Firestore service SSoT) |

---

## Context

Το ADR-405 έστησε τη **Discipline** ως πρώτης τάξης διάσταση και το ADR-406 το **πρώτο MEP
στοιχείο** (σημειακό φωτιστικό). Το επόμενο επίπεδο της Revit είναι ο **κορμός
συνδεσιμότητας**: typed σημεία σύνδεσης (**Connectors**) + λογικά δίκτυα (**Systems**). Χωρίς
αυτόν, τα MEP στοιχεία είναι «βουβά σύμβολα» — δεν συμμετέχουν σε κύκλωμα/δίκτυο.

**Greenfield**: δεν υπήρχε καμία προηγούμενη έννοια connector/system/circuit/network (zero SSoT
conflict). Αυτό το ADR υλοποιεί **μόνο τον κορμό** (Φ1 + Φ2)· ο ηλεκτρικός πίνακας (Φ3), η
ακεραιότητα cascade (Φ4) και το UI ανάθεσης (Φ5) είναι roadmap.

### Πώς το λύνουν οι μεγάλοι (industry convergence)

| Παίκτης | Connector | System |
|---|---|---|
| **Revit** | Connector ⊂ Family (domain electrical/duct/pipe, flow, system classification) | Electrical Circuit / Duct System / Piping System — first-class, no geometry, source + members |
| **ArchiCAD MEP** | MEP port στο object | Logical MEP system |
| **IFC** | `IfcDistributionPort` | `IfcDistributionSystem` / `IfcDistributionCircuit` |

**Κοινός παρονομαστής:** ο connector ανήκει στο στοιχείο (embedded)· το System είναι λογικό
δίκτυο (χωρίς γεωμετρία) που κατέχει τη λίστα μελών.

---

## Decision

### 1. Connector = embedded sub-object (όχι entity)
Ο `MepConnector` ζει στα `params` του host στοιχείου (`MepConnectorHostParams.connectors`) —
ποτέ standalone entity / Firestore doc / enterprise-id. `connectorId` host-local· παγκόσμια
ταυτότητα = `(entityId, connectorId)`. Παγκόσμια θέση **παράγεται** από `position`+`rotation`
του host (`connectorWorldPosition`, mirror του `transformFootprint`) — καμία persisted γεωμετρία.
Domain electrical πρώτα· duct/pipe reserved.

### 2. MepConnectorHostParams interface
Mixed στα `params` κάθε MEP στοιχείου (additive/optional → zero migration). Το φωτιστικό
(ADR-406) έγινε retrofit· ο πίνακας (Φ3) θα το επεκτείνει ομοιόμορφα.

### 3. MepSystem = first-class persisted doc, χωρίς geometry, εκτός `Entity` union
Collection `floorplan_mep_systems`, enterprise-id `mepsys_*`. Δεν είναι σχήμα στον καμβά →
φορτώνεται σε δικό του store (`MepSystemStore`), όχι στο scene.

### 4. SSoT ownership — το System κατέχει την αλήθεια

| Concept | Owner of truth | Derived/cache |
|---|---|---|
| Connector definition | host `params.connectors` (embedded) | connector world position (από host `position`+`rotation`) |
| Circuit membership | `MepSystem.params.members` (το System doc) | `MepConnector.systemId` back-reference |
| Circuit source | `MepSystem.params.sourceEntityId/sourceConnectorId` | — |
| Reconciliation | **System → connector** (System always wins) | stale `systemId` ανεκτό (όπως dangling `attachTopToIds`) |

Ο `mep-system-coordinator` είναι το ΜΟΝΟ σημείο που reconcile-άρει το cache + τρέχει τα
integrity reverse-lookups + εκπέμπει `bim:mep-system-member-missing` (mirror του
`wall-structural-attach-coordinator`, pattern ADR-401 C: pure detection + signal, no mutation).

---

## Implementation (Φ1 + Φ2)

### Φ1 — Connector model
- **NEW** `bim/types/mep-connector-types.ts` — `MepConnectorDomain`/`MepFlowDirection`/
  `ElectricalSystemClassification`/`MepElectricalConnectorParams`/`MepConnector` +
  `connectorWorldPosition` (pure) + `buildDefaultLightingConnector`.
- **NEW** `bim/types/mep-component-types.ts` — `MepConnectorHostParams`.
- **NEW** `bim/types/mep-connector.schemas.ts` — Zod (reused από fixture + system).
- **Retrofit** `MepFixtureParams extends MepConnectorHostParams` + schema optional `connectors`
  + `buildDefaultMepFixtureParams` δίνει default lighting connector.

### Φ2 — System model + persistence + coordinator
- **NEW** `bim/types/mep-system-types.ts` (`MepSystemEntity`/`Params`/`Member`,
  `buildDefaultCircuitParams`) + `mep-system.schemas.ts`.
- **NEW** `bim/mep-systems/`: `mep-system-firestore-service.ts` (setDoc N.6 + subscribe +
  update/delete, no geometry), `mep-system-audit-client.ts` (ADR-195), `mep-system-coordinator.ts`
  (`buildConnectorSystemIndex`/`reconcileEntityConnectors`/`findSystemsBySource`/
  `findSystemMembershipsByEntity`/`detectMissingSystemMembers`/`notifyMissingSystemMembers`),
  `mep-system-store.ts` (zustand), `connector-access.ts` (`getEntityConnectors`/`isMepConnectorHost`).
- **NEW** `hooks/data/useMepSystemPersistence.ts` (subscribe→store + imperative
  create/update/delete) + `app/MepSystemPersistenceHost.tsx` (mounted στο `DxfViewerTopBar`).
- **Registrations:** enterprise-id `MEP_SYSTEM='mepsys'` (4 αρχεία)· `FLOORPLAN_MEP_SYSTEMS`
  collection· `MEP_SYSTEM_TRACKED_FIELDS` + switch· `AuditEntityType 'mep-system'`· route
  `VALID_ENTITY_TYPES` + `ENTITY_COLLECTION_MAP` (**+ Boy-Scout fix: `mep-fixture` έλειπε από το
  map → fixture audit 400-άριζε σιωπηλά**)· EventBus `bim:mep-system-changed` +
  `bim:mep-system-member-missing`· `firestore.rules floorplan_mep_systems` + coverage-manifest
  (CHECK 3.16)· exhaustive maps σε `propagate-entity-rename`/`incremental-backup`.

**Tests:** 25 νέα (connector world-pos 8, coordinator 12, system schemas 5) PASS· 38/38 fixture
regression PASS· tsc clean.

---

## Implementation (Φ3 — Ηλεκτρικός Πίνακας)

Το πρώτο MEP **«πηγή»** στοιχείο — full point-based BIM element, **mirror 1:1 του ADR-406
fixture pipeline**, με δύο εσκεμμένες διαφορές: (1) ο connector ρέει **out** (`flow:'out'`,
classification `power`) — τροφοδοτεί κύκλωμα· (2) είναι **wall-mounted** (το 3D box κεντράρεται
κατακόρυφα στο `mountingElevationMm`, αντί να κρέμεται από οροφή).

- **NEW types:** `bim/types/electrical-panel-types.ts` (`ElectricalPanelKind='distribution-board'`,
  `ElectricalPanelParams extends MepConnectorHostParams`, geometry cache, `ElectricalPanelEntity`
  με `type:'electrical-panel'` + `ifcType:'IfcElectricDistributionBoard'`) + `.schemas.ts` (Zod).
- **NEW** `bim/types/mep-connector-types.ts` → `buildDefaultPanelOutgoingConnector` (+ `connector-access`).
- **NEW** `bim/electrical-panels/`: `electrical-panel-geometry` (compute+validate), `-symbol`
  (panelboard divider strokes SSoT), `-firestore-service` (setDoc N.6 + subscribe/update/delete),
  `-audit-client` (ADR-195), `add-electrical-panel-to-scene`, `ElectricalPanelGhostRenderer`.
- **NEW** `hooks/drawing/electrical-panel-completion` + `useElectricalPanelTool` (FSM) +
  `ui/ribbon/.../electrical-panel-tool-bridge-store` + `UpdateElectricalPanelParamsCommand` +
  `services/factories/electrical-panel.factory`.
- **NEW 2D:** `bim/renderers/ElectricalPanelRenderer` (ADR-040 leaf) + `useElectricalPanelGhostPreview`
  + `canvas-layer-stack-electrical-panel-ghost` (+ CanvasSection/CanvasLayerStack/leaves wiring).
- **NEW 3D:** `panelToMesh` (BimToThreeConverter — **units-safe `sceneUnitsToMeters` stair pattern**,
  ΟΧΙ buggy fixtureToMesh) + `MaterialCatalog3D 'elem-electrical-panel'` + `BimSceneLayer.syncPanels`
  + `Bim3DEntitiesStore.panels[]` + `use-bim3d-electrical-panel-placement` + `ElectricalPanelPlacementGhost`.
- **NEW persistence:** `hooks/data/useElectricalPanelPersistence` + `app/ElectricalPanelPersistenceHost`
  (mounted στο `DxfViewerTopBar`).
- **Registrations:** EntityType/BimElementType/`Entity`+`isElectricalPanelEntity`· BimCategory
  `'electrical-panel'`→`DISCIPLINE_BY_CATEGORY=electrical` (+object-styles/subcategories)· enterprise-id
  `ELECTRICAL_PANEL='elecpnl'`· `FLOORPLAN_ELECTRICAL_PANELS`· `ELECTRICAL_PANEL_TRACKED_FIELDS`+switch·
  `AuditEntityType`+route `VALID_ENTITY_TYPES`/`ENTITY_COLLECTION_MAP`· EventBus (place-3d/params/delete/restore)·
  ToolType+tool-definitions+useSpecialTools+useCanvasClickHandler· dxf-types `DxfElectricalPanel`+2 converters·
  hit-test bounds ×4· DeleteEntityCommand+restore effect+useSmartDelete· useFloors3DAggregator·
  backup/propagate/dxf-export maps· ribbon (Server icon, shortcut EP)+i18n el/en· firestore.rules+coverage-manifest+indexes.
- **Tests:** geometry/symbol/schemas/completion/panel-mesh (units-safe assertion) PASS.

## Implementation (Φ4 — Cascade / Integrity)

Κλείδωμα ακεραιότητας του δικτύου **πριν** το UI ανάθεσης (Φ5). Δύο κανόνες «σαν Revit»:
**delete πίνακα-πηγής → διαλύει τα κυκλώματά του**· **delete μέλους → βγαίνει από το κύκλωμα**
(αφαίρεση από `MepSystem.params.members`). Απόφαση Giorgio: **coherent single-undo** (ένα Ctrl+Z
επαναφέρει ΚΑΙ entity ΚΑΙ κυκλώματα) + **full enterprise / full SSoT** (command-based, όχι
side-effects).

- **Detection (pure SSoT)** — **NEW** `resolveMepCascadeOnDelete(deletedIds, systems): MepCascadePlan`
  στον `mep-system-coordinator.ts` (reuse `findSystemsBySource`). Επιστρέφει `{ dissolve[],
  memberRemovals[] }`· system που διαλύεται εξαιρείται από member-edits (δεν πειράζουμε doomed doc).
  Καμία side effect.
- **Mutator port (SSoT bridge)** — **NEW** `bim/mep-systems/mep-system-mutator.ts`
  (`setMepSystemMutator`/`getMepSystemMutator` + `MepSystemMutator` interface). Module-level bridge
  hook↔command (mirror `wall-cascade-delete-store`). Τα commands ΔΕΝ γράφουν Firestore — καλούν τον
  port, που προωθεί στο `useMepSystemPersistence` (sole writer). Null-safe → headless no-op.
- **Commands (undoable, SSoT primitives)** — **NEW** `UpdateMepSystemParamsCommand`
  (member-removal τώρα· rename/assign Φ5· drag-merge) + `DissolveMepSystemCommand` (κρατά πλήρες
  `MepSystemEntity` snapshot· undo = **id-preserving** `restoreSystem` αφού `saveSystem` δέχεται `id`).
  Target = mutator port, ΟΧΙ `ISceneManager` (το System δεν είναι scene entity).
- **Coherent undo (CompoundCommand)** — στο `useSmartDelete` η διαγραφή MEP entity + τα cascade
  commands τυλίγονται σε ΕΝΑ `CompoundCommand('Delete MEP', …)` → ένα atomic undo unit. Τα
  υπάρχοντα `bim:*-delete-requested` emits (Firestore delete panel/fixture) μένουν ανέπαφα·
  `bim:entity-restore-requested` (undo) επαναφέρει το entity, η `CompoundCommand.undo` επαναφέρει τα
  systems — coherent.
- **Persistence** — `useMepSystemPersistence` + `restoreSystem` (id-preserving `saveSystem` + audit
  `'restored'` + un-suppress deletedIds) + register/unregister του mutator port (useEffect).
- **Safety net (belt-and-suspenders)** — το resync-time `notifyMissingSystemMembers` παραμένει για
  τυχόν dangling references εκτός cascade path.
- **Tests:** coordinator cascade-plan (5) + commands/compound integration (10) PASS· `tsc` 0·
  zero νέα Firestore query (καμία αλλαγή rules/indexes/coverage).

## Implementation (Φ5 — Circuit UI + colour-by-system + reconciliation)

Εδώ «καταναλώνεται» ο κορμός: ο χρήστης φτιάχνει κυκλώματα από το UI, τα βλέπει χρωματισμένα (2D+3D),
και το `connector.systemId` cache συγχρονίζεται («System always wins»). Απόφαση Giorgio:
**full enterprise / full SSoT, Revit-grade** → το **System κατέχει το χρώμα του** (persisted, editable).

### Φ5.A — Create-circuit UI από selection
- **Colour SSoT** — `MepSystemParams.color?` (persisted hex· `.strict()` Zod schema + `regex`). **NEW**
  `bim/mep-systems/mep-system-color.ts`: `MEP_SYSTEM_PALETTE` + `pickNextSystemColor` (least-used,
  deterministic) + `buildEntitySystemColorIndex`/`resolveEntitySystemColor` (entityId→colour, source+
  members) + `hexToThreeInt`/`buildEntitySystemColorIntIndex`/`hexToRgba` + reference-memo
  `getEntitySystemColorIndexCached`. Firestore rules valid-άρουν μόνο top-level keys → **zero** αλλαγή
  rules/indexes.
- **Resolution (pure SSoT)** — **NEW** `bim/mep-systems/mep-circuit-from-selection.ts`:
  `resolveCircuitFromSelection(selected, systems)` → source (πίνακας `flow:'out'`) + members (φωτιστικά
  `flow:'in'`) + **reassignRemovals** (Revit single-circuit rule: μέλος ήδη σε κύκλωμα → **μεταφέρεται**,
  βγαίνει από το παλιό). Typed errors (`no-source`/`multiple-sources`/`no-members`).
- **Command (undoable)** — **NEW** `CreateMepSystemCommand` (inverse του `DissolveMepSystemCommand`:
  pre-minted enterprise id· `execute/redo → mutator.createSystem`· `undo → dissolveSystem`).
  `MepSystemMutator += createSystem(entity)`· `useMepSystemPersistence` shared `persistSystemEntity`
  (id-preserving, audit `'created'`/`'restored'`) + `createSystemEntity` + port register.
- **Ribbon UI (clone beam pattern)** — **NEW** contextual tab `contextual-mep-circuit-tab.ts`
  (trigger όταν selection έχει ≥1 πίνακα ΚΑΙ ≥1 φωτιστικό, `ribbon-contextual-config.ts`) +
  `mep-circuit-command-keys.ts` + `useRibbonMepCircuitBridge` (resolve→`CompoundCommand(create +
  reassign UpdateMepSystemParamsCommand)`→`executeCommand`)· wired σε `useDxfBimBridges`/
  `useDxfViewerRibbon`/`useRibbonCommands`. Feedback = EventBus → `useDxfViewerNotifications` toasts
  (decoupled). i18n el+en.

### Φ5.B — Scene-time reconciliation
- **NEW** `hooks/data/useMepConnectorReconciliation.ts`: subscribe `useMepSystemStore` + scene change →
  `buildConnectorSystemIndex` → `reconcileEntityConnectors` ανά fixture/panel → ΕΝΑ `setLevelScene` μόνο
  σε πραγματική αλλαγή (idempotent, referential-stable → κανένα render loop). **Scene-only (όχι
  Firestore)** — derived cache, ξαναχτίζεται από truth σε κάθε load. Mount στο `MepSystemPersistenceHost`.

### Φ5.C — Colour-by-system (2D + 3D)
- **Industry-faithful rule (research-backed):** χρωματίζονται **ΜΟΝΟ τα μέλη/φορτία (φωτιστικά)**· ο
  **πίνακας-πηγή ΔΕΝ** παίρνει χρώμα κυκλώματος (κοινή πηγή πολλών — Revit: το Electrical Equipment δεν
  φέρει Circuit Number/Panel parameter, τα filters το πιάνουν με `Supply From`). SSoT:
  `buildEntitySystemColorIndex` χαρτογραφεί **μόνο** `members[].entityId` (ΟΧΙ `sourceEntityId`).
- **2D (ADR-040 micro-leaf)** — `MepFixtureRenderer`: μετά το visibility guard, read
  `useMepSystemStore.getState()` (zero-subscription), override stroke/fill με το χρώμα του system
  (`getEntitySystemColorIndexCached` + `hexToRgba`)· unassigned → amber default. `ElectricalPanelRenderer`
  ΔΕΝ χρωματίζεται (μένει equipment teal).
- **3D** — `SyncContext += systemColorIndex` (entityId→THREE int)· `BimSceneLayer.buildContext` το χτίζει
  (`buildEntitySystemColorIntIndex`)· `syncFixtures` το περνά στον `fixtureToMesh += systemColor?` →
  **NEW** `getSystemTintedMaterial3D` (cache per `${type}:${colorInt}`, **ΠΟΤΕ** mutate singleton)·
  `syncPanels`/`panelToMesh` ΔΕΝ χρωματίζονται· `use-bim3d-vg-resync` subscription (d) → resync σε αλλαγή
  systems.
- **Always-on** (assigned vs unassigned)· toggle «colour by system» = roadmap.
- **Tests:** `mep-circuit-from-selection` (6) + `mep-system-color` (8) + `CreateMepSystemCommand` (3) +
  `useMepConnectorReconciliation` (3) + `MaterialCatalog3D-system-tint` (2)· `tsc` 0· 124/124 MEP
  regression PASS. (25 pre-existing wall-fixture 3D failures = ΟΧΙ regression.)

## Implementation (Φ6 — Circuit-Management Panel)

Διαχείριση **υπάρχοντος** κυκλώματος (Revit "Electrical Circuits" properties) — όλα μέσω του ΕΤΟΙΜΟΥ
undoable `UpdateMepSystemParamsCommand` (καμία νέα command/mutator/rules αλλαγή). UI = επέκταση του Φ5
contextual ribbon tab (απόφαση Giorgio), **panel-centric** (Revit-faithful: το φωτιστικό κρατά το δικό
του fixture tab· το κύκλωμα διαχειρίζεται από τον πίνακα/system browser).

- **Active managed circuit (SSoT):** NEW `mep-circuit-editor-store.ts` (zustand `activeSystemId`) — γνήσια
  ephemeral UI state (δεν παράγεται όταν ένας πίνακας τροφοδοτεί >1 κύκλωμα → ο χρήστης διαλέγει).
  NEW `useMepCircuitEditorSync` (always-on, mount στο `MepSystemPersistenceHost`): από το primary selection
  → `resolveManagedCircuits` → reconcile `activeSystemId` (κρατά έγκυρη επιλογή, αλλιώς πρώτος candidate).
- **Pure SSoT:** NEW `mep-circuit-editor.ts` — `resolveManagedCircuits` (member-of ∪ source-of, reuse
  `findSystemMembershipsByEntity` + `findSystemsBySource`)· `buildAddMembersUpdate` (union + Revit reassign)
  · `buildRemoveMembersUpdate` (idempotent). **Boy-scout:** το `computeReassignRemovals` εξήχθη στον
  `mep-system-coordinator` (SSoT) και το `mep-circuit-from-selection` το κάνει reuse (dedupe).
- **UI:** 3 leaf widgets (ADR-040) — `RibbonMepCircuitPickerWidget` (1 candidate → read-only label· >1 →
  dropdown → `setActiveSystemId`)· `RibbonMepCircuitNameWidget` (rename, debounced `isDragging` merge σε 1
  undo, blur/Enter flush, ESC revert· μοτίβο `RibbonWallDimensionWidget`)· `RibbonMepCircuitColorWidget`
  (**κεντρικός** `ColorDialogTrigger`/`EnterpriseColorDialog` hex in/out, ίδιος picker με opening-tag/grid).
  Νέο panel `mep-circuit-properties` στο tab + actions `addMembers`/`removeMembers` (bridge handlers,
  selection-driven, CompoundCommand update+reassign). EventBus `bim:mep-circuit-members-added/-removed/
  -edit-failed` → toasts.
- **Trigger:** `useActiveContextualTrigger` subscribe `useMepSystemStore`· surfaces το tab όταν primary =
  electrical-panel που τροφοδοτεί ≥1 κύκλωμα (πέρα από το Φ5 create-case πίνακας+φωτιστικά). Colour edit →
  αυτόματο resync (2D leaf store-read· 3D `use-bim3d-vg-resync`), μηδέν νέο render code.
- **SSoT διατηρείται:** System κατέχει `members[]`+`color`· πίνακας-πηγή ΔΕΝ χρωματίζεται· colour resolver =
  ένα SSoT· connector cache διορθώνεται από το Φ5 reconciliation.
- **Tests:** `mep-circuit-editor` (15) + `mep-circuit-editor-store` (3)· `tsc` 0· 144/144 MEP regression
  PASS. (25 pre-existing wall-fixture 3D failures = ΟΧΙ regression.)

## Implementation (Φ7 — Ορατά καλώδια / Home-Run Wires)

Η **οπτική** αναπαράσταση του κυκλώματος (Revit home-run wiring) — ο χρήστης «βλέπει» ποιο φωτιστικό
τροφοδοτεί ποιος πίνακας. **Παράγωγη γεωμετρία, ΟΧΙ persisted** (το `MepSystem` παραμένει geometry-less):
υπολογίζεται render-time από τα live host transforms, οπότε ακολουθεί move/rotate πίνακα/φωτιστικών δωρεάν.
Scope = 2D annotation + 3D conduit· τοπολογία = **daisy-chain + home-run** (nearest-neighbor, πίνακας πρώτος).

- **Routing SSoT (καρδιά):** NEW `bim/mep-systems/mep-wire-routing.ts` — `computeCircuitWirePaths(systems,
  resolve)` (greedy nearest-neighbor αλυσίδα από τον πίνακα, ντετερμινιστικό tie-break = σειρά members,
  skip off-scene hosts)· `WireHostPoint {x,y (canvas units), zMm (mm above FFL)}` ώστε **ΜΙΑ** υπολογιστική
  διαδρομή να τροφοδοτεί ΚΑΙ το 2D (x/y) ΚΑΙ το 3D (x/y/zMm)· `WireStyle` **per-circuit** (Φ7 follow-up #1:
  και τα τρία `'straight'`/`'orthogonal'`/`'arc'` ship· το `'arc'` = quadratic-Bézier sampled-σε-polyline
  στο `expandSegment`, άρα 2D `lineTo` & 3D `LineCurve3` το παίρνουν ίδιο, μηδέν curve maths στους renderers).
  Το style ζει στο `MepSystemParams.wireStyle` (SSoT), διαχέεται ως `CircuitWirePath.style`, διαβάζεται στο
  `buildWirePolyline(path)` — οι renderers δεν κρατούν default. Χρώμα =
  `systemColor` (το System κατέχει το χρώμα). Pure (no store/React/Date/Math.random).
- **2D micro-leaf (ADR-040):** NEW `components/dxf-layout/HomeRunWiresOverlay.tsx` (clone `EnvelopeOverlay`) +
  NEW pure `bim/renderers/MepWireRenderer.ts` (`drawCircuitWires`: polyline σε system colour + **home-run
  arrowhead** στον πίνακα). Leaf subscriptions ΜΟΝΟ εδώ (`useMepSystemStore` + objectStyles slice)· ο shell
  `CanvasLayerStack` δεν αποκτά νέο `useSyncExternalStore` (CHECK 6C safe)· mount inline (όπως EnvelopeOverlay).
- **3D conduit (units-safe):** NEW `bim-3d/converters/mep-wire-to-three.ts` (`wirePathToMesh`: `CurvePath` of
  `LineCurve3` → `TubeGeometry`, **stair/railing pattern** `sceneUnitsToMeters` — ΟΧΙ το buggy fixture path·
  χρώμα = `getSystemTintedMaterial3D('mep-wire', int)` no-singleton-mutation) + `BimSceneLayer.syncWires`
  (μετά το `syncPanels`· resolver από **ορατούς** hosts μόνο → hide φωτιστικού κόβει το leg, hide πίνακα κόβει
  όλο το κύκλωμα) + `MaterialCatalog3D` `elem-mep-wire`. Αυτόματο resync: το Φ5 `use-bim3d-vg-resync`
  (systems + V/G) ξαναχτίζει και τα wires — μηδέν νέα subscription.
- **Visibility (reuse SSoT):** NEW `BimCategory 'mep-wire'` (`bim-object-styles` + `MODEL_BIM_CATEGORIES` →
  το «Μόνο DXF» το κρύβει) + `DISCIPLINE_BY_CATEGORY['mep-wire']='electrical'` (το electrical toggle το κρύβει).
  NEW View-tab toggle `MepWireToggle` (mirror `HideBimToggle`, flips `objectStyles['mep-wire'].visible` μέσω
  του έτοιμου `setObjectStyleVisibility` — κανένας bespoke flag). i18n `ribbon.commands.mepWire.*`.
- **SSoT διατηρείται:** καμία νέα persisted οντότητα/command/rule/index· το χρώμα ανήκει στο System·
  ο πίνακας-πηγή ΔΕΝ χρωματίζεται (μόνο τα καλώδια+μέλη).
- **Tests:** `mep-wire-routing` (15) + `mep-wire-to-three` (5, units-safe mm/m parity)· `tsc` 0· 115/115 MEP
  PASS. (25 pre-existing wall-fixture 3D failures = ΟΧΙ regression.)

## Roadmap (επόμενο push)

- ✅ «colour by system» view toggle **DONE** (2026-06-03)· ✅ per-circuit edit από φωτιστικό **DONE** (2026-06-03)· ✅ seed legacy connectors **DONE** (2026-06-03) — **ο κορμός ADR-408 «MEP σαν Revit» (electrical) πλήρης.**
- Φ7 follow-ups: ✅ `orthogonal`/`arc` wire styles **DONE** (#1, 2026-06-03)· ✅ waypoints **DONE** (#3,
  2026-06-03)· ✅ conductor-count ticks **DONE** (2026-06-03)· ✅ colour-by-system toggle **DONE** (2026-06-03)· ✅ per-circuit edit από φωτιστικό **DONE** (2026-06-03)· ✅ seed legacy connectors **DONE** (2026-06-03).
- ✅ duct/pipe **element pipeline** (Φ8) **DONE** (2026-06-03) — ΕΝΑ ενοποιημένο `mep-segment` entity (domain `duct|pipe` + sectionKind `rectangular|round`), placement/γεωμετρία/2D/3D/persistence/BOQ/grips, mirror του δοκαριού. **❌ υπόλοιπο:** duct/pipe **systems** (grouping segments σε δίκτυα + routing — αντίστοιχο των Φ2/Φ7 electrical· επόμενο frontier)· contextual props tab (edit size/elevation)· 3D 2-click placement· connectors στο segment (forward hook, empty).

---

## Changelog
- **2026-06-09 (Opus 4.8, Plan εγκεκριμένο — «σαν Revit, FULL ENTERPRISE + FULL SSOT»)** — **BOILER 2D PLAN TAG DONE** (Revit «Mechanical Equipment Tag»· jest 8 νέα tag + 12 symbol regression πράσινα· tsc N.17· 🔴 browser-verify + commit). **Τι:** ετικέτα-leader στην κάτοψη του λέβητα που δείχνει **μοντέλο** (Type Catalog literal label / fallback «Λέβητας»), **ισχύ** (`thermalOutputW`→ακέραια kW), **καύσιμο** (`fuelType` enum→i18n) και **Ø καπναγωγού** (`flueDiameterMm`→`Ø DNxxx`, **μόνο** gas/oil — electric/heat-pump δεν έχουν καπναγωγό). Always-on auto-tag (όχι toggle v1, σαν Revit)· gated από το ΙΔΙΟ `resolveIsEntityVisible` που ήδη τρέχει (tag κρύβεται με το entity) + min-zoom anti-clutter (`TAG_MIN_ZOOM=0.1`, mirror `OPENING_TAG_MIN_ZOOM`). **Λύση (FULL SSOT, pure core):** NEW `bim/mep-boilers/mep-boiler-tag.ts` — `buildBoilerTagLines(params, t): string[]` (η ΜΟΝΗ πηγή «τι γράφει το tag»· formats kW/DN, fuel enum→labelKey, model literal· translator-injected → pure & unit-testable) + thin i18n-bound `resolveBoilerTagLines(params)` wrapper (binds `i18n.t` ns `dxf-viewer-shell`). Ο `MepBoilerRenderer` απέκτησε private `drawTag()` (screen-space fixed-pixel, zoom-invariant, μοτίβο `MepSegmentRenderer.drawSlopeIndicatorScreen`: leader από bbox top-right γωνία → boxed left-aligned stack· warm-red border `#dc2626` + ουδέτερο translucent bg + dark text), κλήση μετά το symbol pass / πριν το `finalizeRender`. **Αρχεία (boiler-isolated, καθαρά ADDITIVE):** NEW `bim/mep-boilers/mep-boiler-tag.ts` · `bim/renderers/MepBoilerRenderer.ts` (import + tag consts + `drawTag()` + κλήση) · i18n `ribbon.commands.mepBoilerTag.*` (el+en: power/fuel/flue/modelFallback/kWUnit/dnPrefix + fuelTypes.{gas,oil,electric,heat-pump}) · NEW test `bim/mep-boilers/__tests__/mep-boiler-tag.test.ts` (8: gas combi πλήρες· explicit flue DN· electric/heat-pump χωρίς flue· parametric fallback· kW rounding· absent power/fuel omits). **ADR-040 staged** (CHECK 6D — `MepBoilerRenderer.ts` drawing file· μόνο additive screen-space annotation, μηδέν subscription/cache-key). git add ΜΟΝΟ δικά μου· **ΜΗΝ adr-index**. **ΜΑΘΗΜΑ:** το tag content ζει σε pure translator-injected builder (SSoT + testable) — ο renderer κρατά ΜΟΝΟ layout/styling· i18n στον renderer μέσω του non-hook `i18n` singleton (ns-bound wrapper).
- **2026-06-09 (Opus 4.8, Plan Mode εγκεκριμένο — «σαν Revit, FULL ENTERPRISE + FULL SSOT»)** — **WYSIWYG PLACEMENT GHOST ΛΕΒΗΤΑ DONE** (το 2D placement preview δείχνει πλέον ΟΛΟ το σύμβολο, jest 40 boiler πράσινα· tsc background N.17· 🔴 browser-verify + commit). **Πρόβλημα:** ο placement ghost (`MepBoilerGhostRenderer`, οδηγούμενος από `useMepBoilerGhostPreview`) έδειχνε ΜΟΝΟ footprint fill + outline + anchor marker — ΟΧΙ το σύμβολο (divider/flame glyph) ούτε τους connector stubs/flue vent· όμως ο placed `MepBoilerRenderer` δείχνει πλέον ΟΛΟ το σύμβολο (connector-driven), άρα το preview ΔΕΝ ήταν WYSIWYG (ο χρήστης δεν έβλεπε πού πέφτουν συνδέσεις/καπναγωγός πριν το κλικ· στη Revit το placement preview δείχνει ολόκληρη την οικογένεια). **Λύση (FULL SSOT reuse, μηδέν νέα γεωμετρία):** νέος getter `useMepBoilerTool.getGhostSymbol(cursor): BoilerSymbolGeometry | null` που καλεί το ΙΔΙΟ `buildMepBoilerSymbol(params, computeMepBoilerGeometry(params))` SSoT που χρησιμοποιεί ο placed renderer → byte-for-byte WYSIWYG. Boy-scout extract κοινού `resolveGhostParams(cursor)` ώστε `getGhostFootprint` + `getGhostSymbol` να μοιράζονται ΕΝΑΝ params builder (μηδέν duplication/drift). Ο ghost renderer δέχεται optional `symbol?` και σχεδιάζει `strokes`+`ventStrokes` (GHOST_LINE_WIDTH) + `glyphStrokes` (thinner ~1.25) σε ίδιο warm-red palette = ίδιο read με τον placed. **Αρχεία (boiler-isolated, καθαρά ADDITIVE):** `hooks/drawing/useMepBoilerTool.ts` (resolveGhostParams + getGhostSymbol) · `bim/mep-boilers/MepBoilerGhostRenderer.ts` (optional `symbol` input + `drawSymbol`/`traceStroke`) · `hooks/tools/useMepBoilerGhostPreview.ts` (νέο prop `getGhostSymbol` → drawFrame) · `components/dxf-layout/canvas-layer-stack-mep-boiler-ghost.tsx` (mount prop) · `components/dxf-layout/canvas-layer-stack-types.ts` (payload type) · `components/dxf-layout/CanvasSection.tsx:465` (additive `getGhostSymbol: mepBoilerTool.getGhostSymbol`· καμία αλλαγή σε radiator/heating/sizing wiring) · NEW test `hooks/drawing/__tests__/useMepBoilerTool.test.tsx` (4: idle/null null· full symbol glyph=4 + stubs≥2· outline===footprint WYSIWYG). **ADR-040 staged** (CHECK 6B/6D — CanvasSection orchestrator + mount/hook architecture-critical· αλλά μόνο pass-through ενός pure getter, μηδέν νέα subscription). git add ΜΟΝΟ δικά μου· **ΜΗΝ adr-index**. **ΜΑΘΗΜΑ:** το placement ghost γίνεται WYSIWYG δωρεάν όταν ο tool εκθέτει τον ΙΔΙΟ symbol-builder SSoT που χρησιμοποιεί ο placed renderer — μηδέν διπλή γεωμετρία.
- **2026-06-09 (Opus 4.8, Plan Mode εγκεκριμένο — «σαν Revit, FULL ENTERPRISE + FULL SSOT»)** — **2D ΣΥΜΒΟΛΟ ΛΕΒΗΤΑ ΟΔΗΓΟΥΜΕΝΟ ΑΠΟ ΤΟΥΣ CONNECTORS (FULL SSOT) + flue vent glyph DONE** (jest 37 boiler + 209 renderers/mep-boilers πράσινα· tsc background N.17· 🔴 browser-verify + commit). **Πρόβλημα:** το `mep-boiler-symbol.ts` `buildMepBoilerSymbol` hardcode-άρε ΑΚΡΙΒΩΣ 2 stubs (supply +X / return −X) → οι combi DHW (hot/cold/recirc) **και** ο νέος καπναγωγός (`boiler-flue`) **δεν φαίνονταν στην κάτοψη** (στη Revit όλοι οι connectors είναι ορατοί στην οικογένεια). **Λύση (connector-driven, μηδέν drift ποτέ ξανά):** το symbol κάνει πλέον loop πάνω στο `buildBoilerConnectors(params)` — τη ΜΟΝΗ SSoT πηγή των connectors — και για κάθε connector λύνει world θέση μέσω `connectorWorldPosition(c, params.position, params.rotation)` (το shared rotation SSoT), σχεδιάζοντας 1 stub ανά πραγματικό port. Κατεύθυνση stub = `normalize(worldPos − params.position)` (= `R·`normalised local offset). **Regression-free:** supply localPos `{hw,0}` → world = ΑΚΡΙΒΩΣ το midpoint του +X edge (= το παλιό `supplyRoot`) με κατεύθυνση +X· return ομοίως → πανομοιότυπη γεωμετρία με πριν. **Flue distinct glyph:** ο `boiler-flue` (`domain:'duct'`) παίρνει διακριτό **vent glyph** (stub + chevron «^» arrowhead προς τα έξω = exhaust flow) μέσω νέου pure SSoT helper `buildFlueVentStroke`, σε νέο πεδίο `BoilerSymbolGeometry.ventStrokes` (κρατά τους duct εξαερισμούς ξεχωριστά από τους σωλήνες νερού). **Αρχεία (boiler-isolated, καθαρά ADDITIVE):** `bim/mep-boilers/mep-boiler-symbol.ts` (connector-driven loop + `ventStrokes` πεδίο + `buildFlueVentStroke` helper· imports `buildBoilerConnectors`+`connectorWorldPosition`) · `bim/renderers/MepBoilerRenderer.ts` (σχεδίαση `symbol.ventStrokes`, NORMAL weight) · NEW test `bim/mep-boilers/__tests__/mep-boiler-symbol.test.ts` (12: regression supply/return + combi 4/5 stubs + flue vent gas/oil/electric/heat-pump + rotation 90°). **ΕΚΤΟΣ ADR-040** (το symbol είναι pure, ο renderer είναι zero-subscription leaf). git add ΜΟΝΟ δικά μου· **ΜΗΝ adr-index**. **ΜΑΘΗΜΑ:** όταν ένα 2D σύμβολο πρέπει να δείχνει connectors, οδήγησέ το από τον connector-builder SSoT + `connectorWorldPosition` — ΠΟΤΕ hardcoded stubs· νέοι connectors (DHW/flue/μελλοντικοί) εμφανίζονται αυτόματα.
- **2026-06-09 (Opus 4.8 — iterative 3D gizmo session, «όπως η Revit»)** — **Φ-D/Φ-E (3D GIZMO) — LIVE FITTING FOLLOW (η τάπα/μούφα/ταυ ακολουθεί real-time) + endpoint handle = δαχτυλίδι DONE** (jest 49 affected πράσινα· tsc 🔴 background· 🔴 browser-verify + commit). **Στόχος (Giorgio):** (a) τα endpoint handles (σφαίρες) ήταν «πολύ μεγάλα και κρύβουν τις τάπες» → μικρά **camera-facing δαχτυλίδια** (torus, κούφια → η τάπα φαίνεται μέσα από την τρύπα· `buildEndpointHandle` + `billboardEndpointRings` στο overlay `updateScale`)· (b) όταν σέρνεις άκρο σωλήνα, **η ΤΑΠΑ (`mep-fitting` cap) δεν ακολουθούσε** — μετακινούνταν μόνο στο release· ο Giorgio θέλει live. **Λύση (mirror pipe-follow, reuse pure reconciler → ghost===commit):** ανά frame, ο ΥΠΑΡΧΩΝ `resolveDesiredFittings(transient)` τρέχει σε **transient entity list** όπου ο δραγμένος σωλήνας + οι followers φέρουν τα LIVE next-params → επιστρέφει τα fittings (cap/elbow/tee) στα μετακινούμενα junctions → ξαναχτίζονται με `mepFittingToMesh` (μέσω `createMepFitting` από draft). Τα παλιά fitting GROUPS (tagged `userData['entityId']`) κρύβονται. **Αρχεία:** `bim3d-pipe-follow-preview-rebuild.ts` (NEW `incidentFittingIds` + `buildFittingFollowPreviewObjects` + `affectedFittingSegmentIds`· reuse `liveNextParams`/`followerPatches`/`connectedPipeSegmentIds`) · `bim3d-edit-live-preview.ts` (NEW `captureFittings`/`applyFittings` + state + `incidentFittingIds` getter, match `entityId` αφού τα fittings είναι GROUPS) · `bim3d-edit-interaction-handlers.ts` (`captureIncidentFittings` σε pointerdown endpoint+else· `applyFittings` σε move/rotate/endpoint applyLivePreview) · `gizmo-constants.ts`/`gizmo-handle-builders.ts`/`gizmo-geometry.ts`/`bim-gizmo-overlay.ts` (sphere→torus ring + billboard). Wired σε ΟΛΑ τα drag kinds (endpoint/move/rotate/vertical) που ήδη κάνουν pipe-follow. **ΕΚΤΟΣ ADR-040**. git add ΜΟΝΟ δικά μου· **ΜΗΝ adr-index**. ⚠️ ο reconciler τρέχει ανά frame (όπως ο pipe-follow resolver)· acceptable για single-entity drag. **ΜΑΘΗΜΑ:** derived entities (fittings) ακολουθούν live = τρέξε τον commit-time pure reconciler σε transient list με τα live next-params + rebuild via converter (ghost===commit)· τα fittings είναι GROUPS → capture match `entityId` (όχι `bimId`).
- **2026-06-09 (Opus 4.8, Plan Mode εγκεκριμένο — «σαν Revit, FULL ENTERPRISE + FULL SSOT»)** — **ΚΑΠΝΑΓΩΓΟΣ ΛΕΒΗΤΑ (FLUE) — duct domain foundation + combustion exhaust connector DONE** (jest: boiler-geometry **+6 νέα** [gas flue back-centre/exhaust/up · oil flue · electric/heat-pump/unspecified → όχι flue · DN100 default vs override · combi+recirc+gas → 6 distinct connectors] = **30/30 mep-boilers** [+ boiler-model-catalog]· 495 MEP regression + 24 boiler bridge πράσινα· tsc 🔴 pending [N.17 slot]· 🔴 browser-verify + commit). **Στόχος (Revit «Mechanical Equipment → Flue/Vent connector», καπναγωγός):** ένας λέβητας **αερίου/πετρελαίου** καίει καύσιμο → εξάγει **καυσαέρια** μέσω καπναγωγού· μοντελοποιήθηκαν όλες οι συνδέσεις (θέρμανση supply/return, DHW hot/cold/recirc) **ΕΚΤΟΣ** του καπναγωγού· ηλεκτρικός/αντλία θερμότητας = **καμία καύση → κανένας καπναγωγός**. **🔑 Σχεδιαστική απόφαση (locked): θεμελίωση `duct` domain** — ο `MepConnectorDomain` είχε `'duct'` **reserved** (χωρίς params interface)· αυτό το slice το **υλοποιεί** (πρότυπο «reserve→implement» όπως το pipe slice): NEW `DuctSystemClassification = 'exhaust'` (επεκτάσιμο: μελλοντικά supply-air/return-air) + NEW `MepDuctConnectorParams { systemClassification, diameterMm? }` + `duct?` optional στο `MepConnector` (mirror του `pipe?`, additive — μηδέν αλλαγή σε pipe/electrical unions). **ΟΧΙ** νέο `PlumbingSystemClassification` member (ο flue είναι **duct**, όχι σωλήνας νερού). **🔑 Gating:** seeded **μόνο** όταν `fuelType ∈ {gas, oil}` (`buildBoilerConnectors`, ανεξάρτητα του combi/DHW gate — απλός λέβητας αερίου χωρίς combi έχει καπναγωγό)· `fuelType` υπάρχει ήδη από τον Type Catalog. **Connector:** id `boiler-flue`, `domain:'duct'`, `flow:'out'`, classification `exhaust`, **θέση back-centre `{0,-hl}`** (διακριτό από supply/return στο y=0 + 4 DHW γωνίες· `{0,-hl}` ελεύθερο), `localDirection {0,0,1}` (προς καμινάδα). Διάμετρος = `flueDiameterMm ?? DEFAULT_BOILER_FLUE_DIAMETER_MM` (DN100, range 80/100/130). **UI = «Καπναγωγός» panel** (`visibilityKey: combustion` → bridge.getPanelVisibility → `fuelType gas/oil`, mirror του `combi` gate αλλά οδηγείται από fuelType) με flue diameter combobox DN80/100/130. **Re-seed ΔΩΡΕΑΝ:** `UpdateMepBoilerParamsCommand` + reconciliation ήδη καλούν `buildBoilerConnectors`· `MepConnectorSchema.duct` zod branch (`.strict()` → υποχρεωτικό) ώστε ο flue να επιβιώνει persist/load. **Αρχεία (boiler-isolated):** `mep-connector-types.ts` (`DuctSystemClassification`+`MepDuctConnectorParams`+`duct?`+`BOILER_FLUE_CONNECTOR_ID`+`buildBoilerFlueConnector`) · `mep-connector.schemas.ts` (`DuctSystemClassificationSchema`+`MepDuctConnectorParamsSchema`+`.duct` optional) · `mep-boiler-geometry.ts` (`buildBoilerConnectors` → flue gated-by-fuelType) · `mep-boiler-types.ts`/`.schemas.ts` (+`flueDiameterMm?` additive + `DEFAULT_BOILER_FLUE_DIAMETER_MM`) · `mep-boiler-command-keys.ts` (`params.flueDiameter` number key + `combustion` visibility key) · `useRibbonMepBoilerBridge.ts` (`flueDiameter`→`flueDiameterMm` + combustion visibility branch — **καθαρά ADDITIVE, sizing readouts ανέγγιχτα**) · `contextual-mep-boiler-tab.ts` (NEW «Καπναγωγός» panel) · i18n el+en. **ΕΚΤΟΣ ADR-040** (pure geometry/ribbon). git add ΜΟΝΟ δικά μου· **ΜΗΝ adr-index** (shared tree). **Next:** fuel/efficiency ΚΕΝΑΚ· flue 2D/3D glyph· vent terminal. **ΜΑΘΗΜΑ:** ο καπναγωγός = πρώτος `duct`-domain connector → θεμελιώνει το domain (params interface + zod branch) χωρίς να αγγίξει pipe/electrical· gated by fuelType (όχι producesDhw — καύση ≠ ΖΝΧ)· `.strict()` schema → νέο domain payload ΥΠΟΧΡΕΩΤΙΚΑ +zod branch αλλιώς σπάει στο load.
- **2026-06-09 (Opus 4.8, Plan Mode εγκεκριμένο — «προχώρα όπως το κάνει η Revit»)** — **Φ-E (3D GIZMO) — ENTITY-AWARE DOF (2-άξονες planar vs 3-άξονες ελεύθερες) + polish DONE** (jest **71 affected πράσινα**· tsc 🔴 background· 🔴 browser-verify + commit). **Στόχος (Giorgio, iterative gizmo session):** (a) κάθετος άξονας Y λαχανί→**σκούρο πράσινο**· (b) **απόκρυψη** κεντρικής πορτοκαλί πυραμίδας (free-move center)· (c) **το gizmo να αντικατοπτρίζει τους βαθμούς ελευθερίας ανά οντότητα** — άλλες 3 άξονες, άλλες μόνο 2 (κάτοψη), «όπως η Revit». **Λύση (Revit DOF):** **structural+μη-MEP** (`wall`/`column`/`beam`/`slab`/`stair`/`furniture`/`opening`/`roof`/`railing`/`electrical-panel`/`floor-finish`) = **planar (2-άξ.)** → `axis-x`/`axis-z`+`plane-xz`+`rotate-y`· **`axis-y` ΑΦΑΙΡΕΘΗΚΕ** από base (υψόμετρο/base-offset = contextual tab, Revit-correct, όχι free-drag). **Free-3D MEP** (`mep-segment`/`mep-fixture`/`mep-manifold`/`mep-radiator`/`mep-boiler`/`mep-water-heater`) = **3 άξ.** → +`axis-y`+**`plane-xy`**+**`plane-yz`** (πλήρες 3D σαν Revit move tool). **Αρχεία:** `bim-gizmo-overlay.ts` (`BASE_HANDLES` planar baseline + `FREE_3D_MOVE_HANDLES` + `FREE_3D_MOVE_TYPES` set· `activeHandlesFor` προσθέτει 3D μόνο σε free-3D)· `gizmo-constants.ts` (`GIZMO_COLOR_Y` `0x2de52d`→`0x1f8a1f`). **Combined H+V fix (κρίσιμο):** κατακόρυφο plane handle σε free-3D MEP → ταυτόχρονα οριζόντιο+κατακόρυφο· το `buildEditCommand` move έκανε `if(deltaUpMm)→vertical-only` **πετώντας** το οριζόντιο, και MoveEntityCommand+Update*Params στην ΙΔΙΑ οντότητα συγκρούονται. FIX: NEW `buildMepCombinedMoveCommand` → **ΕΝΑ** `Update*ParamsCommand` με combined next params (`calculateBimMovedGeometry` XY → `mepVerticalNextParams` Z) + NEW `mepUpdateCommandFromNext` (per-type) + `withConnectedPipeFollow` (followers από combined pose)· live `liveNextParams` move tail εφαρμόζει vertical στα moved params (ghost===commit). **ΕΚΤΟΣ ADR-040** (`bim-3d/gizmo`+`animation`). git add ΜΟΝΟ δικά μου· **ΜΗΝ adr-index**. **ΜΑΘΗΜΑ:** Revit DOF = gizmo δείχνει μόνο τους βαθμούς ελευθερίας (planar 2-άξ. vs free-3D)· resize-y (διάσταση ύψους) ≠ axis-y move (θέση) — το πρώτο μένει στα δομικά, το δεύτερο όχι· combined H+V = ΕΝΑ params update.
- **2026-06-09 (Opus 4.8, Plan Mode εγκεκριμένο — «σαν Revit, FULL ENTERPRISE + FULL SSOT»)** — **DHW RECIRCULATION (DHWR) — recirculation return inlet στον combi λέβητα DONE** (jest: boiler-geometry **+4 νέα** [recirc 5ος connector / gated-by-combi / 4-connector regression / diameter fallback] = **47/47 boiler suites**· 226 MEP+bridge regression πράσινα· tsc background· 🔴 browser-verify + commit). **Στόχος (Revit «Domestic Hot Water + Recirculation»):** σε πολυώροφα/μεγάλα κτίρια το ΖΝΧ **ανακυκλοφορεί** (recirculation loop) ώστε να φτάνει άμεσα στις βρύσες χωρίς αναμονή/σπατάλη· ο combi αποκτά **recirculation return inlet** — το ζεστό επιστρέφει στον λέβητα και ξαναθερμαίνεται. Με `producesDhw + dhwRecirculation` ο combi αποκτά **5ο** connector: recirc inlet (`flow:'in'`, id `boiler-dhw-recirc`, γωνία `{-hw,-hl}` back-left → διακριτό από τα 4). **🔑 Σχεδιαστική απόφαση (locked):** **REUSE** classification `domestic-hot-water` με `flow:'in'` — **ΟΧΙ** νέο `PlumbingSystemClassification` union member (αποφυγή αλλαγής shared union/schemas/resolvers)· ο recirc inlet γίνεται **μέλος του ΙΔΙΟΥ DHW δικτύου** που πηγάζει ο hot outlet, κλείνοντας το loop ΔΩΡΕΑΝ (μηδέν αλλαγή σε source/resolvers). **🔑 Gating:** seeded **μόνο** όταν `producesDhw && dhwRecirculation` (plain/non-combi λέβητας → όχι recirc ακόμη κι αν το flag set). Διάμετρος = `dhwConnectorDiameterMm ?? connectorDiameterMm`. **UI = proper Revit Yes/No toggle** «Ανακυκλοφορία ΖΝΧ» στο υπάρχον contextual **«ΖΝΧ» panel** (combi-only, `visibilityKey: combi`) δίπλα στη DHW διάμετρο. **Re-seed ΔΩΡΕΑΝ:** `UpdateMepBoilerParamsCommand` + reconciliation ήδη καλούν `buildBoilerConnectors`. **Αρχεία (boiler-isolated):** `mep-connector-types.ts` (`BOILER_DHW_RECIRC_CONNECTOR_ID` + `buildBoilerDhwRecircInletConnector`) · `mep-boiler-geometry.ts` (`buildBoilerConnectors` → recirc 5ος, gated) · `mep-boiler-types.ts`/`.schemas.ts` (+`dhwRecirculation?` additive) · `mep-boiler-command-keys.ts` (`toggles.dhwRecirculation` + union/set) · `useRibbonMepBoilerBridge.ts` (`TOGGLE_KEY_TO_FIELD` map → `onToggle`/`getToggleState` by commandKey) · `contextual-mep-boiler-tab.ts` (toggle button στο «ΖΝΧ» panel) · i18n el+en. **ΕΚΤΟΣ ADR-040** (pure geometry/ribbon). git add ΜΟΝΟ δικά μου· **ΜΗΝ adr-index** (shared tree). **Next:** fuel/efficiency ΚΕΝΑΚ· DHWR pump/flow sizing. **ΜΑΘΗΜΑ:** recirculation = +1 connector με REUSE της hot-water classification (flow:in) → μέλος του ίδιου δικτύου, μηδέν αλλαγή σε union/resolvers· gated behind combi· toggle by-commandKey map γενικεύει το single-toggle pattern χωρίς fork.
- **2026-06-09 (Opus 4.8, Plan Mode εγκεκριμένο — «σαν Revit, FULL ENTERPRISE + FULL SSOT»)** — **Φ-D (3D GIZMO) — PER-ENDPOINT SHAPE HANDLES (μετακίνηση ΑΚΡΟΥ σωλήνα σε κάτοψη+υψόμετρο) DONE** (jest **30 νέα** [`bim3d-endpoint-move` + endpoint cases στο `bim-gizmo-drag-bridge`]· γειτονικά suites 116/116 πράσινα· tsc 🔴 background· 🔴 browser-verify + commit). **Στόχος (Giorgio):** επιλογή σωλήνα στο 3D → shape-handles στα **δύο άκρα**· σέρνεις ένα → ο σωλήνας **τεντώνει από εκείνο το άκρο** (το άλλο μένει), σε **κάτοψη ΚΑΙ υψόμετρο**, με τους συνδεδεμένους σωλήνες/εξαρτήματα να ακολουθούν (Revit). Το κεντρικό move/rotate gizmo παραμένει· τα endpoint handles είναι ΠΡΟΣΘΗΚΗ, **single-select `mep-segment` μόνο**. **🔑 Revit-grade απόφαση (camera-facing projection):** ο resize/axis projection δίνει ΕΙΤΕ οριζόντια ΕΙΤΕ κάθετα — όχι και τα δύο σε ένα drag. Νέο constraint `endpoint` → `projectOntoPlane(ray, endpointWorld, **cameraDir**)` = free move ενός node στο επίπεδο της κάμερας (SketchUp/Revit «move in view plane»): top view → καθαρή κάτοψη· πλάγια όψη → υψόμετρο· ένα handle καλύπτει και τα δύο. Τιμά axis-lock + «Connect To» snap (`makeResizeSnapFn`). **🔑 z normalization (αποφυγή bug):** το `applyMepSegmentGripDrag` κρατά z=`startPoint.z ?? 0` → legacy οριζόντιος run (z=0, πραγματικό ύψος=`centerlineElevationMm`) θα έπεφτε στο 0. NEW pure SSoT **`bim3d-endpoint-move.ts` `computeMepSegmentEndpointMove`** → plan XY μέσω grip SSoT + z μέσω `resolveSegmentEndpointElevationsMm` (γράφει **και τα δύο** z κανονικοποιημένα + re-derive `centerlineElevationMm`, mirror του `computeMepSegmentVerticalMove`). **Αρχιτεκτονική (mirror resize, max reuse):** (1) `gizmo-types` — `endpoint-start/-end` ids + `{kind:'endpoint',endpoint}` constraint + `parseHandleId`/`handleToConstraint`. (2) **SSoT `segmentAxisEndpointsWorld(params, baseElevationM)`** εξάχθηκε στον `mep-segment-to-mesh` (verbatim η endpoint-world formula, **κοινή converter↔gizmo**· ο converter την καλεί τώρα). (3) `gizmo-geometry` — 2 endpoint handles (reuse `buildResizeHandle` square). (4) `bim-gizmo-overlay` — `ENDPOINT_HANDLES_BY_TYPE` + **νέα μόνη υποδομή** `setEndpointHandles`/`setDraggedEndpoint`/`refreshEndpointOffsets` (τα children τοποθετούνται σε `(world−anchor)/rootScale` σε κάθε position/scale refresh → world-locked, screen-constant μέγεθος). (5) `gizmo-hit-test` priority 4. (6) `bim-gizmo-drag-bridge` — `endpoint-move` outcome (deltaMm+deltaUpMm)· `gizmo-projection` camera-plane branch. (7) `bim-gizmo-controller` — endpoint live preview· anchor=endpoint world· dragged handle follows (centre μένει). (8) `bim3d-edit-command-builders` — `buildEndpointMoveCommand` → `UpdateMepSegmentParamsCommand` + **`withConnectedPipeFollow`** (reuse Φ-C). (9) `bim3d-preview-rebuild` — `buildEndpointMovePreviewObject` (δραγμένος σωλήνας via `mepSegmentToMesh`)· `bim3d-pipe-follow-preview-rebuild` — `PipeDragXform += endpoint` (followers reuse resolver). (10) handlers — endpoint capture (`captureResize`+`capturePipes`) + `applyLivePreview` branch + endpoint snapFn· hook wiring `refreshSegmentEndpointHandles`. **ΕΚΤΟΣ ADR-040** (`bim/`+`bim-3d/gizmo`+`bim-3d/animation`+`bim-3d/converters` — όχι canvas micro-leaf· CHECK 6B/6D δεν εφαρμόζονται). git add ΜΟΝΟ δικά μου· **ΜΗΝ adr-index** (shared tree). **🟡 Γενίκευση (μελλοντικό):** ίδιο `endpoint` kind → beam/wall/duct length editing (registry-driven). **ΜΑΘΗΜΑ:** per-endpoint 3D drag = camera-facing projection (όχι axis-constrained) για κάτοψη+υψόμετρο μαζί· endpoint world = κοινή SSoT με τον converter· z ΠΑΝΤΑ μέσω `resolveSegmentEndpointElevationsMm` (legacy-safe), ποτέ raw `point.z`.
- **2026-06-09 (Opus 4.8, Plan Mode εγκεκριμένο — «σαν Revit, FULL ENTERPRISE + FULL SSOT»)** — **COMBI ΛΕΒΗΤΑΣ (θέρμανση + ΖΝΧ) — FULL water path DONE** (jest: boiler-geometry +4 / NEW `pipe-network-source` 9 / `mep-pipe-network-from-selection` +3 = **40 νέα**· **78 MEP suites / 748 tests πράσινα** + boiler bridge 63· tsc καθαρό δικά μου· 🔴 browser-verify + commit). **Στόχος:** ο `mep-boiler` ήταν θέρμανση-μόνο· **πραγματικός Revit combi** (Mechanical Equipment) **παίρνει κρύο νερό και το θερμαίνει** — όχι ζεστό «από το πουθενά». Με `producesDhw` ο λέβητας αποκτά **ΔΥΟ** DHW connectors που κλείνουν το water path: **hot outlet** (`domestic-hot-water`, `flow:'out'`, id `boiler-dhw-hot`, `{+hw,+hl}` → **πηγάζει** το DHW δίκτυο) + **cold inlet** (`domestic-cold-water`, `flow:'in'`, id `boiler-dhw-cold`, `{-hw,+hl}` → **μέλος** του cold δικτύου), mirror θερμοσίφωνα 1:1. Combi = **4 connectors** (supply/return/dhw-hot/dhw-cold, 4 distinct γωνίες), plain = 2. **Dedicated DHW διάμετρος** `dhwConnectorDiameterMm` (default DN15· fallback `connectorDiameterMm`). **🔑 Ο κόμπος (Revit-grade):** combi = **ΔΥΟ** `flow:'out'` (hydronic-supply + domestic-hot-water) → ο `findPipeNetworkSourceConnectorId` («πρώτο out») διάλεγε λάθος· ο `resolvePipeNetworkFromSelection` παρήγαγε classification μόνο από single `source.params.systemClassification`. **FIX (2 σημεία):** (1) `findPipeNetworkSourceConnectorId(source, classification?)` — προτιμά out-connector **αυτής** της classification· αλλιώς legacy first-out (**backward compatible**) + NEW `sourceOutConnectorClassifications`. (2) `resolvePipeNetworkFromSelection` συμπεραίνει το target classification **ΓΕΩΜΕΤΡΙΚΑ** — του out-connector που **αγγίζει** ο επιλεγμένος σωλήνας (`connectorWorldPosition` + `resolvePipeJoinTolerance`)· **ΜΟΝΟ** όταν `sourceOutConnectorClassifications(source).size > 1` (combi)· μονο-out πηγές → ίδιο code path → **regression-free**. Το cold-in είναι `flow:'in'` → δεν αγγίζει καθόλου την out-logic. **UI = proper Revit Yes/No toggle button** (`type:'toggle'`, mirror roof `slopeUnitPercent` — όχι combobox) + **contextual «ΖΝΧ» panel** (DHW διάμετρος) που εμφανίζεται **μόνο όταν combi** (`visibilityKey: combi` → bridge.getPanelVisibility, Revit «params appear by type»). **Re-seed connectors ΔΩΡΕΑΝ:** `UpdateMepBoilerParamsCommand.applyPatch` + `seedDefaultConnectors` + `useMepConnectorReconciliation` boiler branch ήδη καλούν `buildBoilerConnectors` → toggle + load self-heal + per-connector systemId cache (cold-in παίρνει snap+colour-by-system δωρεάν, όπως ο θερμοσίφωνας). **Αρχεία (boiler+connector+bridge+resolver isolated):** `mep-connector-types.ts` (`BOILER_DHW_HOT_CONNECTOR_ID`+`BOILER_DHW_COLD_CONNECTOR_ID`+`buildBoilerDhwHotOutletConnector`/`buildBoilerDhwColdInletConnector`) · `mep-boiler-geometry.ts` (`buildBoilerConnectors` → 2 DHW connectors + dedicated diameter) · `mep-boiler-types.ts`/`.schemas.ts` (+`producesDhw?`/`dhwConnectorDiameterMm?` + default 15) · `pipe-network-source.ts` (classification-aware + `sourceOutConnectorClassifications`) · `mep-pipe-network-from-selection.ts` (`inferTargetClassificationByGeometry`) · `mep-boiler-command-keys.ts` (`toggles.producesDhw`+`isMepBoilerToggleKey` + DHW-diameter number key + `combi` visibility) · `useRibbonMepBoilerBridge.ts` (`onToggle`/`getToggleState` + DHW-diameter field + combi visibility) · `useRibbonCommands.ts` (additive boiler toggle routing) · `contextual-mep-boiler-tab.ts` (toggle button + «ΖΝΧ» panel) · i18n el+en. **ΕΚΤΟΣ ADR-040** (pure geometry/resolver/ribbon). git add ΜΟΝΟ δικά μου· **ΜΗΝ adr-index** (shared tree). **Next:** DHW recirculation return (DHWR)· fuel/efficiency ΚΕΝΑΚ. **ΜΑΘΗΜΑ:** full Revit combi = πλήρες water path (cold-in + hot-out, mirror θερμοσίφωνα), όχι hot «από το πουθενά»· proper Yes/No = `type:'toggle'` (όχι combobox)· contextual params εμφανίζονται ανά type μέσω visibilityKey· όταν source έχει >1 out-classifications η disambiguation είναι γεωμετρική (ποιον connector αγγίζει ο σωλήνας).
- **2026-06-09 (Opus 4.8 — «σαν Revit, FULL ENTERPRISE + FULL SSOT»)** — **Φ-C (3D GIZMO) — MEP ROTATE/VERTICAL PERSISTENCE + CONNECTIVITY + LIVE PREVIEW DONE** (jest **80/80**· tsc καθαρό· 🔴 browser-verify + commit). **Πρόβλημα (Giorgio, 3D browser-verify):** περιστροφή σωλήνα μέσω 3D gizmo → preview περιστρέφει ΑΛΛΑ στο release **επανέρχεται στην αρχική**· ίδιο για **κάθετη (axis-Y) μετακίνηση** ειδών/συλλέκτη· και οι **συνδεδεμένοι σωλήνες δεν ακολουθούσαν** (ούτε live ούτε commit). **Root cause (3 παράλληλα κενά στο 3D edit system, ΟΧΙ στον Φ-C resolver):** (1) `calculateBimRotatedGeometry` (`bim/transforms/bim-rotate-geometry.ts`) χωρίς MEP `case` → `{}` no-op → re-sync revert· (2) `verticalCommandForEntity` (`bim3d-edit-command-builders.ts`) χωρίς MEP cases → no command → revert· (3) move/rotate/vertical commands δεν παρέσερναν connected pipes. **Λύση (FULL SSOT, reuse Φ-C resolvers):** (1) NEW `rotateMepSegment` (mirror `rotateBeam`) + γενικό `rotateMepPointHost` (position rotate + rotation accumulate· connectors host-local→follow free) + 7 cases (segment/fixture/panel/manifold/radiator/boiler/water-heater). (2) NEW `computeMepHostVerticalMove` (`mountingElevationMm+=Δ`) + `computeMepSegmentVerticalMove` (endpoint z+=Δ) + MEP branch στο `verticalCommandForEntity`. (3) NEW `withConnectedPipeFollow` wrapper → τυλίγει move/rotate/vertical base + `UpdateMepSegmentParamsCommand[]` σε CompoundCommand (ΕΝΑ undo). **(4) LIVE PREVIEW (Revit «connected ends stretch live»):** NEW `bim3d-pipe-follow-preview-rebuild.ts` (mirror `bim3d-wire-preview-rebuild`) + `capturePipes`/`applyPipes` στο `Bim3DEditLivePreview` + wiring στους handlers (move+rotate+vertical)· ανά frame: live next-params dragged (reuse calculateBimMoved/Rotated + vertical SSoT) → resolver → `mepSegmentToMesh` per follower (ghost===commit· unit-safe worldToDxfPlan*mmScale). +2 rotate-geometry tests· EditCommand union += 6 MEP Update commands. **ΕΚΤΟΣ ADR-040** (`bim/`+`bim-3d/`). git add ΜΟΝΟ δικά μου· **ΜΗΝ adr-index**. **ΜΑΘΗΜΑ:** το 3D gizmo edit χρειάζεται per-type case ΣΕ ΚΑΘΕ άξονα (rotate-geometry/vertical-move/move-geometry)· missing case = σιωπηλό `{}`/null → re-sync revert· live preview = mirror του wire-follow (capture/apply + per-frame converter rebuild).
- **2026-06-08 (Opus 4.8, Plan Mode εγκεκριμένο + 1 subagent — «σαν Revit, FULL ENTERPRISE + FULL SSOT»)** — **ΚΑΤΑΛΟΓΟΣ ΜΟΝΤΕΛΩΝ ΛΕΒΗΤΑ (Boiler Type Catalog, Revit «Mechanical Equipment Type») DONE** (jest **47/47**: catalog 23 + bridge 24· tsc: βλ. session note· 🔴 browser-verify + commit). **Στόχος:** το `thermalOutputW` ήταν ελεύθερο νούμερο — τώρα dropdown «Μοντέλο» στον boiler tab με 7 πραγματικά μοντέλα (αερίου συμπύκνωσης/πετρελαίου/αντλία θερμότητας/ηλεκτρικός)· επιλογή → **auto-fill** thermalOutputW+διαστάσεις+DN+fuelType+modelId → η «Εγκατεστημένη» του ADR-422 L2 boiler-sizing γίνεται πραγματική. «Παραμετρικό» (SELECT_CLEAR_VALUE) → καθαρίζει modelId. **Πρότυπο 1:1 = `useRibbonMepFixtureBridge` assetId picker** (string-key combobox· tab `options:[]`, ο bridge δίνει options δυναμικά στο `getComboboxState`· `onComboboxChange` → catalog lookup → γράφει πολλά params σε ΕΝΑ `UpdateMepBoilerParamsCommand`). **Αρχεία (boiler-isolated):** NEW `bim/mep-boilers/boiler-model-catalog.ts` (pure: `BoilerModelPreset`/`BOILER_MODEL_CATALOG` as const/`listBoilerModels`/`resolveBoilerModel`/`applyBoilerModelToParams`/`clearBoilerModel`) · `mep-boiler-types.ts` (+`modelId?`/`fuelType?` additive) · `mep-boiler.schemas.ts` · `mep-boiler-command-keys.ts` (+`stringParams.modelId`/`isMepBoilerRibbonStringKey`) · `contextual-mep-boiler-tab.ts` (panel «Μοντέλο» πρώτο) · `useRibbonMepBoilerBridge.ts` (string-key branches· readout/number branches αμετάβλητα) · `useRibbonCommands.ts` (3 additive routing) · i18n el+en (panel «Μοντέλο» + «Παραμετρικό»· product names = `isLiteralLabel:true`, μηδέν i18n). **ΜΑΘΗΜΑ:** catalog picker = ο `assetId` pattern (tab `options:[]` + bridge-dynamic options + multi-param write σε ΕΝΑ command)· `isLiteralLabel` για product names· νέο boiler string-key = guard + routing σε ΔΥΟ σημεία (onComboboxChange+getComboboxState). **ΕΚΤΟΣ ADR-040** (ribbon/data). git add ΜΟΝΟ δικά μου· **ΜΗΝ adr-index** (shared tree). **Next:** combi DHW· fuel/efficiency για ΚΕΝΑΚ.
- **2026-06-08 (Opus 4.8, Plan Mode εγκεκριμένο — «σαν Revit, FULL ENTERPRISE + FULL SSOT»)** — **Φ-C — CONNECTIVITY-PRESERVING MOVE (host grips XY+rotation + pipe grips) DONE** (jest **22/22** [+11 νέα `mep-move-propagation` · 11 `mep-elevation-propagation` αμετάβλητα]· tsc background· 🔴 browser-verify + commit). **Πρόβλημα (Giorgio, handoff):** όταν μετακινείς host (συλλέκτη/είδος υγιεινής/λέβητα/καλοριφέρ/θερμοσίφωνα) ή σωλήνα, τα **συνδεδεμένα άκρα σωλήνων αποκολλώνται** — δεν ακολουθούν/τεντώνουν. **Revit-στόχος:** το συνδεδεμένο άκρο ακολουθεί (ο σωλήνας αλλάζει ΜΗΚΟΣ, το άλλο άκρο μένει)· host=anchor. **Λύση (FULL SSOT, pure, ΕΝΑΣ resolver):** NEW `bim/mep-segments/mep-move-propagation.ts` — μοντέλο «anchors»: ένα κινούμενο στοιχείο εκθέτει world poses (host → pipe connectors μέσω `connectorWorldPosition` + datum `pointHostMountingElevationMm`· segment → 2 endpoints), ζευγαρωμένα OLD→NEW· κάθε pipe endpoint coincident με OLD anchor → retarget σε NEW (XY **και** Z· match-on-OLD = ακριβές tracking, η περιστροφή σαρώνει τα connector XY → καλύπτεται ΔΩΡΕΑΝ). `resolveHostMoveConnectedPipePatches` + `resolveSegmentMoveConnectedPipePatches`. **Drag-merge stability:** patch εκπέμπεται για ΚΑΘΕ coincident endpoint (όχι μόνο όταν κινήθηκε) → σταθερό child-shape του `CompoundCommand` ώστε τα διαδοχικά drag samples να γίνονται **ΕΝΑ undo** (`canMergeWith` απαιτεί ίσο πλήθος). NEW SSOT accessor `getConnectorHostPlanTransform` (`connector-access.ts`· point hosts → position/rotation· underfloor → identity, connectors ήδη σε world coords). NEW command builder `build-connectivity-host-update.ts` (`buildConnectivityHostUpdate` + executors host & segment· πρότυπο `buildManifoldParamUpdate`· pipe sub-cmds `isDragging=true`). **Wiring:** Φ-C1 — 5 host grip commits (`commitMepFixtureGripDrag`/manifold/radiator/boiler/water-heater) → `executeHostMoveWithConnectedPipes` (XY translate + rotation + resize, driven από connector world-pose delta). Φ-C2 — `commitMepSegmentGripDrag` → `executeSegmentMoveWithConnectedPipes` (translate/endpoints/rotation· coincident γείτονες ακολουθούν). **Boy-scout split (N.7.1):** 3 heating-host commits → NEW `grip-parametric-heating-host-commits.ts` (centred-box 489→359 γρ.· re-export ανέπαφο). **ΕΚΤΟΣ ADR-040** (`bim/` + `hooks/grips` — όχι canvas micro-leaf· CHECK 6B/6D δεν εφαρμόζονται). git add ΜΟΝΟ δικά μου· **ΜΗΝ adr-index** (shared tree). **🟡 PENDING (επόμενα slices):** Φ-C3 generic move command (move tool/arrow-keys/drag-select → `MoveEntityCommand`/`bim-move-geometry`)· host Z-μέσω-contextual-tab follow για non-manifold hosts (σήμερα μόνο grips XY/rotation + manifold Z)· Φ-C2b body-tap follow («main=anchor» στο pipe move, mirror του Φ-B2a tee-fix). **ΜΑΘΗΜΑ:** ένας agnostic resolver driven από connector world-poses (OLD→NEW) καλύπτει XY+Z+rotation ομοιόμορφα· coincidence-based patch emission = κλειδί για σταθερό CompoundCommand drag-merge. **+BUGFIX (browser-verify Giorgio «περιστρέφω σωλήνα → preview OK αλλά στο release επανέρχεται στην αρχική»):** PRE-EXISTING bug, ανεξάρτητο του Φ-C resolver. Root cause: το `hotGripKindOf` (`hooks/grips/wall-hot-grip-fsm.ts`) ΔΕΝ επέστρεφε το `grip.mepSegmentGripKind` → το `mep-segment-rotation`/`-midpoint` δεν έμπαινε ΠΟΤΕ στο 6-/3-click hot-grip FSM (αν και το `HOT_GRIP_OP_REGISTRY` + `apply-entity-preview` τα υποστήριζαν ήδη)· έπεφτε σε press-drag με λάθος anchor (axis-midpoint αντί picked centre) → incoherent angle → revert. Fix (3 σημεία, opt-in row που έλειπε — ακριβώς ό,τι προειδοποιεί το ADR-397): (1) `hotGripKindOf` += `mepSegmentGripKind`· (2) `buildDxfDragPreview` += `mepSegmentGripKind`+anchorPos (3-click move ghost)· (3) `buildRotateReferencePreview` += `mepSegmentGripKind` (6-click rotate ghost). +4 regression tests `wall-hot-grip-fsm.test.ts` (41/41). tsc καθαρό. ΕΚΤΟΣ ADR-040.
- **2026-06-08 (Opus 4.8, Plan Mode εγκεκριμένο — «σαν Revit, FULL ENTERPRISE + FULL SSOT»)** — **Φ-B2a EXT — TEE / BODY ELEVATION PROPAGATION DONE** (jest **12/12** `mep-elevation-propagation` [+2 νέα tee cases]· tsc: μηδέν diagnostics στο αρχείο· 🔴 browser-verified [Giorgio «ΛΕΙΤΟΥΡΓΕΙ»] + pending commit). **Πρόβλημα (Giorgio, κατά browser-verify ADR-426):** όταν αλλάζεις υψόμετρο άκρου σωλήνα, οι **end-to-end** & **3-way endpoint** συνδέσεις ακολουθούσαν (Φ-B2a), ΑΛΛΑ ένας κλάδος που ακουμπά στη **ΜΕΣΗ (body)** ενός περαστού σωλήνα (ταυ) **έμενε πίσω** → η σύνδεση έσπαγε. **Root cause:** το `resolveConnectedElevationPatches` ταίριαζε **μόνο endpoints** (`patchCoincidentEndpoints` έλεγχε `startPoint`/`endPoint`) — ένας κόμβος στο σώμα τμήματος δεν βρισκόταν. **Revit-correct κανόνας:** ο **περαστός σωλήνας (main) = anchor**· ο κλάδος κρέμεται απ' αυτόν, δεν τον σκίζει. **Λύση (FULL SSOT, pure, αμφίδρομη):** στο `bim/mep-segments/mep-elevation-propagation.ts` — (1) NEW `projectOnSegmentBodyParam(p,a,b,tol2)` (reuse SSoT `getNearestPointOnLine`· επιστρέφει `t∈(0,1)` αν το σημείο πέφτει στο **σώμα**, εξαιρώντας τις endpoint-ζώνες που τις πιάνει ήδη το coincidence) + `interpolateSegmentZMm` (γραμμικό z κατά μήκος — ακριβές flat **και** sloped). (2) **Edit κλάδου:** NEW `resolveThroughPipeAnchorAt` → ο κόμβος του edited κλάδου **κουμπώνει** στο interpolated z του main (νέο tier στο `resolveNodeAnchorMm`: host outlet > through-pipe body > raw value). (3) **Edit main:** το `patchCoincidentEndpoints` έγινε `patchOtherSegment` — κάθε endpoint άλλου σωλήνα είτε **coincides** με changed node (όπως πριν) είτε **ακουμπά στο σώμα** του edited main → ακολουθεί στο interpolated z (coincidence wins). **Συνέπειες ασφαλείς:** τα 10 υπάρχοντα tests (end-to-end / 3-way / anchor / duct-skip / idempotent) **αμετάβλητα πράσινα**· auto δίκτυο (router σπάει τον κορμό σε tap points → όλα endpoint joins) δουλεύει όπως πριν. **Tests (+2):** «editing the MAIN drags a branch tapping its middle (interpolated 1400)» + «editing the BRANCH tap-end snaps back to the main (anchor)». **ΕΚΤΟΣ ADR-040** (`bim/` pure). git add ΜΟΝΟ δικά μου· **ΜΗΝ adr-index** (shared tree). **🟡 ΕΠΟΜΕΝΟ (handoff `2026-06-08_adr408-connectivity-preserving-move`):** «connectivity-preserving MOVE» — μετακίνηση host/σωλήνα (XY+Z+rotation) → τα συνδεδεμένα άκρα ακολουθούν/τεντώνουν (σήμερα δεν ακολουθούν· μόνο manifold Z-param έχει follow). **ΜΑΘΗΜΑ:** το ταυ-στη-μέση δεν είναι endpoint coincidence → χρειάζεται point-on-body projection + «main=anchor» με γραμμική παρεμβολή z.
- **2026-06-08 (Opus 4.8, Plan Mode εγκεκριμένο + 10 subagents — «όπως οι μεγάλοι, σαν Revit — FULL ENTERPRISE + FULL SSOT»)** — **DHW — ΘΕΡΜΟΣΙΦΩΝΑΣ (`mep-water-heater`) νέο point-based BIM entity = ΠΗΓΗ ΖΕΣΤΟΥ ΝΕΡΟΥ ΧΡΗΣΗΣ DONE** (jest **94/94**: 24 νέα [geometry 11 + bridge 10 + extras] + 70 MEP regression μηδέν regression· tsc: βλ. session note· 🔴 browser verify + commit + firebase deploy [rules/indexes]). **Πρόβλημα (code=SoT):** οι υδραυλικές υποδοχές απέκτησαν `domestic-hot-water` inlets, ΑΛΛΑ ο λέβητας (`mep-boiler`) είναι **hydronic θέρμανση χώρου** — όχι ΖΝΧ· το δίκτυο ζεστού νερού χρήσης είχε **τερματικά χωρίς πηγή**. **Λύση (Revit «Mechanical Equipment»/`IfcUnitaryEquipment`):** νέο dedicated entity (καθαρός διαχωρισμός θέρμανσης χώρου ↔ ΖΝΧ). **Template = ΛΕΒΗΤΑΣ 1:1.** **Δύο deltas vs λέβητα:** (1) connectors = **cold inlet** (`domestic-cold-water`, `flow:'in'`, −X → μέλος cold network) + **hot outlet** (`domestic-hot-water`, `flow:'out'`, +X → **πηγάζει** το ΖΝΧ δίκτυο)· ids `wh-cold`/`wh-hot`. (2) νέο optional param `tankCapacityL` (χωρητικότητα δεξαμενής). prefix `wht`, collection `floorplan_mep_water_heaters`, discipline `plumbing`, χρώμα **μπλε** (`#1d4ed8`/`0x2563eb`· DHW=νερό, διακριτό από κόκκινο boiler), icon Droplet, defaults 500×500×900mm @ elev 1500. **FULL SSOT γενίκευση network-source:** ο θερμοσίφωνας μπήκε στον υπάρχοντα guard `pipe-network-source.ts` `isPipeNetworkSourceEntity` (manifold|boiler|**water-heater**)· `findPipeNetworkSourceConnectorId` (`flow==='out'`) επιστρέφει το hot outlet → δίκτυο κληρονομεί `domestic-hot-water` ΔΩΡΕΑΝ (Revit «source owns System Classification»). **ΟΛΟΚΛΗΡΩΜΕΝΑ (~80 αρχεία):** data-model (types/schemas/geometry+`buildWaterHeaterConnectors`/factory/completion — προϋπήρχαν)· 2D (renderer+**διακριτό tank glyph**: εγγεγραμμένος κύκλος δεξαμενής + στάθμη νερού + zigzag αντίσταση/symbol/grips+commit-copy/audit/add-to-scene)· silent-drop dispatch (entities-union+`isMepWaterHeaterEntity`/bim-base/dxf-types+`DxfMepWaterHeater`/scene-converter/renderer-model/object-styles/subcategories/discipline/atoe/EntityRendererComposite/Bounds/hit-test/bim-bounds/selection-duplicate/grip-computation+glyph-registry+grip-kind union ×5)· tool (completion+hook+ghost+bridge-store+command-keys[+`tankCapacityL`]+useSpecialTools+useCanvasClickHandler+tool-definitions+ToolType+home-tab-draw+RibbonButtonIcon Droplet)· factory+enterprise-id(`wht`)· DeleteEntityCommand/useSmartDelete/restore+`UpdateMepWaterHeaterParamsCommand`+events(`bim:place/params-updated/delete-requested`)· 3D (`waterHeaterToMesh` blue+`syncWaterHeaters`+store `waterHeaters` slice+`elem-mep-water-heater`+aggregator+placement ghost/hook)· connectivity additive (connector-access/seed[kind-aware self-heal]/reconciliation/SnapEngine/elevation/water-source-resolve/connector-resolve/recognizer[`MepSourceKind+='water-heater'`])· **persistence ΠΛΗΡΗΣ** (firestore-service+hook+host mounted DxfViewerTopBar+collections+`FLOOR_SCOPED_BIM_COLLECTIONS`+audit-tracked[+`tankCapacityL`]+rules+indexes×4)· **contextual tab «Ιδιότητες Θερμοσίφωνα»** (Geometry/Thermal[+tankCapacityL]/**Δίκτυο fold-in**/Actions + bridge + 10 tests)· i18n el+en parity (21 keys + `tankCapacityL`). **ΜΑΘΗΜΑ:** νέο point-based source = mirror λέβητα 1:1· source-vs-source διαφέρει μόνο στο connector classification (hot-out αντί hydronic-supply)· ο network-source guard γενικεύεται (όχι fork)· νέο entity = registration ΚΑΙ στο `base-entity.ts` EntityType ΚΑΙ στο grip-kind union (×5 sites). **PENDING:** 3D-viewport click-placement verify· recirculation return (DHWR)· BOQ (BLOCKED ΗΛΜ code). **ΕΚΤΟΣ ADR-040** (πλην ghost/canvas-leaf mount — CHECK 6B/6D STAGE). git add ΜΟΝΟ δικά μου· **ΜΗΝ adr-index** (SHARED tree).
- **2026-06-08 (Opus 4.8, Plan Mode — handoff CODE, «FULL ENTERPRISE + FULL SSOT, σαν Revit»)** — **DRAW-TIME «SYSTEM TYPE» PICKER ΣΤΟΝ ΣΩΛΗΝΑ (Revit Type Selector, color-by-system) DONE** (jest 23/23 `useRibbonMepSegmentBridge`· tsc 0 δικά μου· ✅ **BROWSER-VERIFIED** 2026-06-08 Giorgio: χρώμα σωστό μπλε/κόκκινο/καφέ· 🔴 pending commit Giorgio). _Σημ.: στο verify παρατηρήθηκε ΑΣΧΕΤΟ προϋπάρχον Firebase SDK `b815/ca9 INTERNAL ASSERTION` από `useWallPersistence` listener teardown (`firestore-query.service.ts:313`) — γνωστό (ADR-367), console-spam μόνο, write/render OK· δεν αφορά αυτό το feature._ **Πρόβλημα (Giorgio):** «όταν τοποθετώ σωλήνες δεν ξέρω τι βάζω — όλοι ίδιο χρώμα». Ο γενικός `mep-pipe` σχεδιαζόταν `classification: undefined` → default χρώμα, άγνωστο σύστημα. Το classification picker ζούσε σε panel `visibilityKey: pipeDomain` που ο bridge έκρυβε draw-time (`if(!segment) return false`). **Λύση (Revit «πάντα υπάρχει System Type», mirror του draw-time `centerlineElevation` Φ8 #2b — μηδέν fork):** (1) **NEW visibility key `pipeClassification`** (draw-or-select) στο `mep-segment-command-keys.ts` (union + set). (2) **Split panel** στο `contextual-mep-segment-tab.ts`: NEW `mep-segment-classification` panel (μόνο classification combobox· `visibilityKey: pipeClassification`· label NEW `ribbon.panels.mepSegmentClassification` «Σύστημα»/«System») + κρατήθηκε `mep-segment-plumbing` panel μόνο με slope (`pipeDomain`, selection-only — slope=derived από endpoints, άνευ νοήματος draw-time· relabel «Κλίση»/«Slope»). (3) **Dual-mode bridge** `useRibbonMepSegmentBridge.ts`: `getComboboxState` draw-time → `toolHandle.overrides.classification ?? ''`· `onComboboxChange` draw-time → `setParamOverrides({classification})` (τύπος `PlumbingSystemClassification`, μηδέν `any`)· `getPanelVisibility(pipeClassification)` resolved **ΠΡΙΝ** το `!segment` early-return (draw-time: `toolHandle.isActive && domain==='pipe'` → καλύπτει pipe+drain-pipe, εξαιρεί duct· selection: `seg.params.domain==='pipe'`)· `toolHandle` στο deps. (4) **Default System Type** `mep-pipe` `useSpecialTools-placement-tools.ts`: `classification: undefined` → **`'domestic-cold-water'`** (μπλε· `mep-duct` μένει unclassified, `mep-drain-pipe` μένει `sanitary-drainage`). **Χρώμα ΔΩΡΕΑΝ:** το committed σωλήνα ήδη χρωματίζεται από το `classification` param μέσω `resolveSegmentClassificationColor` (το `mep-drain-pipe`=καφέ το αποδείκνυε) → draw-time override → κρύο μπλε / ζεστό κόκκινο / αποχέτευση καφέ αμέσως. i18n el+en parity (1 νέο panel key· τα `mepClassification.*` option labels υπήρχαν). **ΕΚΤΟΣ ADR-040** (ribbon-level subscription, όχι canvas leaf· **ΜΗΝ adr-index**). ⚠️ SHARED tree (παράλληλο pending «Πλυντήριο»)· git add ΜΟΝΟ δικά μου. **🟡 deferred (nice-to-have):** per-option χρωματιστό swatch στο dropdown· ghost-preview χρώμα draw-time (WYSIWYG). **ΜΑΘΗΜΑ:** draw-time picker = mirror του υπάρχοντος dual-mode (`!segment` branch + visibility ΠΡΙΝ το early-return + `toolHandle` deps)· νέο panel αντί επέκταση υπάρχοντος ώστε slope να μείνει selection-only.
- **2026-06-08 (Opus 4.8, handoff CODE slices — «FULL ENTERPRISE + FULL SSOT, σαν Revit»)** — **ΔΡΟΜΟΣ B — CONNECTABLE ΠΛΥΝΤΗΡΙΟ / ΟΙΚΟΓΕΝΕΙΑ APPLIANCE (Revit IfcElectricAppliance) DONE** (jest 40/40 appliance+sanitary· tsc: βλ. session note· 🔴 browser verify + commit). **Στόχος (Giorgio):** πλυντήριο ως **πλήρες Revit appliance** — τοποθετείται 2D/3D **ΚΑΙ συνδέεται** (παροχή κρύου νερού + αποχέτευση). **Αρχιτεκτονική (εγκεκριμένο plan):** (1) ΟΧΙ νέο entity type — **νέο `kind` του `mep-fixture`** `'washing-machine'` (παίρνει connectors/placement/renderer/bounds/hit-test/persistence/selection/copy ΔΩΡΕΑΝ· ~18 αρχεία vs 60). (2) **Διακριτή «οικογένεια Appliance»** — NEW `bim/appliances/appliance-symbol-spec.ts` (`APPLIANCE_KINDS`/`isApplianceKind`/`APPLIANCE_SPEC`/`APPLIANCE_DRAWERS`/`applianceFixtureToolKind`) **ΟΧΙ** μέσα στο `SANITARY_KINDS` (αλλιώς ψεύδεται το `isSanitaryKind` + θα έμπαινε στα «Είδη Υγιεινής»)· NEW unifying SSoT `bim/mep-fixtures/plumbing-fixture-spec.ts` (`PlumbingFixtureKind = SanitaryKind ∪ ApplianceKind`, `isPlumbingFixtureKind`, `resolvePlumbingFixtureSpec/Drawer`, `plumbingFixtureToolKind`, `fixtureMeshPresetsForKind`/`resolveFixtureMeshPreset` dispatchers· μηδέν cycle). (3) **Connectors** cold inlet (`domestic-cold-water`, in, DN15) + drain outlet (`sanitary-drainage`, out, DN50), **cold-only** (EU)· reuse generic builders μέσω του υπάρχοντος `buildSanitaryFixtureConnectors` (γενικεύτηκε σε `PlumbingFixtureKind` + `resolvePlumbingFixtureSpec`). (4) **UI** νέα ribbon ομάδα **«Συσκευές»**→«Πλυντήριο» (commandKey `mep-washing-machine`) + contextual tab **«Ιδιότητες Συσκευής»** (NEW `contextual-mep-appliance-fixture-tab.ts`, thin copy· trigger `mep-appliance-fixture-selected` **ΠΡΙΝ** το sanitary branch) + 3D picker «Πλυντήριο (3Δ)» (NEW `appliance-fixture-mesh-catalog.ts`, `washing_machine_01` CC-BY 597×587×850 base anchor). (5) **IFC `IfcElectricAppliance`** (νέο· `resolveFixtureIfcType` appliance branch)· **BimCategory reuse `'sanitary'`**· **mesh folder `'appliance'`** (`resolveFixtureMeshCategory`)· schemas += `'washing-machine'` + `'IfcElectricAppliance'` (αλλιώς silent Zod drop). **Generalize (isSanitaryKind→isPlumbingFixtureKind):** `mep-connector-seed`, `mep-fixture-completion`, `mep-fixture-symbol` (2D drawer dispatch)· `mep-fixture-to-mesh` appliance routing (category 'appliance', base anchor)· `useRibbonMepFixtureBridge` (mesh picker dispatcher + appliance delete-confirm)· tool registration (`ToolType`+`tool-definitions`+`useSpecialTools-placement`+`useCanvasClickHandler` via `plumbingFixtureToolKind`). **i18n** el+en (`mepFixture.appliance.washingMachine`, `catalog.washingMachine01`, `ribbon.commands.bim.mepAppliance.*`, `tabs/panels.mepApplianceFixture*`, `mepApplianceFixtureEditor.deleteConfirm`)· **asset-credits** διαβάζει `APPLIANCE_MESH_CATALOG` (CC-BY nikita.bulgakov). **Slice 0 mesh** ήδη στο Storage (`bim-mesh-library/appliance/washing_machine_01.glb`, asset surgery 290k→105k tris). **BOQ deferred** (κανένα mep-fixture δεν τροφοδοτεί BOQ σήμερα — ξεχωριστό task). **Tests (3 NEW):** `appliance-symbol-spec` (kinds/tool-id/spec/drawer)· `plumbing-fixture-spec` (dispatch)· `appliance-fixture` (washing-machine→cold+drain, mesh catalog, IFC/category/mesh resolvers)· 40/40 PASS. **ΕΚΤΟΣ ADR-040.** git add ΜΟΝΟ δικά μου· **ΜΗΝ adr-index** (shared tree). **ΜΑΘΗΜΑ:** νέα connectable «οικογένεια» = νέο `kind` + distinct family registry + ΕΝΑ unifying dispatch SSoT (sanitary ∪ appliance)· τα callers γενικεύονται σε `isPlumbingFixtureKind`· connectors/snap/placement/persistence δωρεάν.
- **2026-06-08 (Opus 4.8, Plan Mode εγκεκριμένο — «σαν Revit, FULL ENTERPRISE + FULL SSOT»)** — **ΥΔΡΑΥΛΙΚΕΣ ΥΠΟΔΟΧΕΣ → ΣΥΝΔΕΣΗ ΣΤΟ ΔΙΚΤΥΟ ΥΔΡΕΥΣΗΣ (Revit Plumbing Fixture water connectors + System membership) DONE** (jest 143/143 MEP regression· tsc: βλ. session note· 🔴 browser verify + commit). **Στόχος (Giorgio, AskUserQuestion):** νιπτήρας/WC/ντουζιέρα/μπανιέρα/μπιντές (`mep-fixture`, SanitaryKind) να αποκτούν **water-supply connectors** ώστε σωλήνες ύδρευσης να **κουμπώνουν** (Revit «Connect To») + να **εντάσσονται** στο plumbing `MepSystem`. **Κρίσιμη διαπίστωση (code=SoT):** η υποδομή connector/system είναι **domain-agnostic** → **snap** (`MepConnectorSnapEngine` ήδη `isMepFixtureEntity`), **elevation-στο-snap** (`resolveMepConnectorElevationMmAt`/`pointHostMountingElevationMm` ήδη fixture), **cap-suppression** (`collectHostConnectorEndpoints`), **reconciliation** (`reconcileEntityConnectors`) + **`resolveManagedSystems`** δουλεύουν ΗΔΗ generic — **μηδέν αλλαγή**. Σήμερα οι sanitary fixtures είχαν ΜΟΝΟ `sanitary-drainage` outlet (αποχέτευση). **2 εστιασμένα κενά καλύφθηκαν:** **(Φάση 1 — connectors)** SSoT spec `SANITARY_SPEC += supply{cold,hot,diameterMm}` (wc=cold-only DN15· basin/shower/bidet=cold+hot DN15· bath cold+hot DN20)· NEW builders `buildSanitaryColdWaterConnector`/`buildSanitaryHotWaterConnector` (`domain:'pipe'`,`flow:'in'`,classification `domestic-cold/hot-water`, mirror `buildRadiatorSupplyConnector`) + ids `san-cold`/`san-hot`· **NEW SSoT factory** `bim/mep-fixtures/sanitary-fixture-connectors.ts` `buildSanitaryFixtureConnectors(kind, sceneUnits)` → `[drain, cold?, hot?]` (drain στο origin αμετάβλητο· cold/hot σε **unit-safe** back-edge offset μέσω `mmToSceneUnits`, distinct positions· z=0 floor stubs)· wired σε **completion** (`mep-fixture-completion` sanitary branch, +sceneUnits param) **και** **seed** (`mep-connector-seed` πλέον **kind-aware**: sanitary→water set, floor-drain→drain [Boy-Scout fix· ήταν lighting], light→lighting → legacy self-heal on load). **(Φάση 2 — membership)** NEW `fixtureMembersForClassification(fixture, classification)` (classification-aware: η υποδοχή μπαίνει μέσω του connector ΑΥΤΗΣ της classification — cold inlet→cold network, hot→hot, drain→sanitary· Revit per-(entity,connector))· `resolvePipeNetworkFromSelection` συμπεριλαμβάνει επιλεγμένες υποδοχές ως μέλη (classification=source's)· `buildAddPipeMembersUpdate` δέχεται fixtures (πέρα από segments)· `useRibbonMepPipeNetworkBridge.handleAddMembers` περνά ΟΛΕΣ τις επιλεγμένες οντότητες. Reuse create-from-selection (multi-select manifold+σωλήνες+υποδοχές) + add-members flow (μηδέν νέο trigger/command). **ΜΗΔΕΝ νέα persisted πεδία/rules/indexes/i18n** (additive στο `params.connectors`). **Tests:** NEW `sanitary-fixture-connectors.test` (per-kind, distinct/unit-safe positions, WC cold-only)· EXT `mep-fixture-sanitary` (drain+cold+hot counts)· EXT `mep-connector-seed` (sanitary self-heal)· EXT `mep-pipe-network-from-selection` (fixture admitted via matching classification)· 143/143 MEP PASS. **ΕΚΤΟΣ ADR-040.** git add ΜΟΝΟ δικά μου· **ΜΗΝ adr-index** (shared tree). **🟡 follow-up (deferred):** read-only «Δίκτυο» indicator panel στο sanitary fixture tab· πραγματικό supply-stub elevation (τώρα z=0 floor). **ΜΑΘΗΜΑ:** η connector/system υποδομή είναι entity-agnostic → «σύνδεση νέου host στο δίκτυο» = ΜΟΝΟ (1) seed connectors per type + (2) classification-aware member admission· snap/elevation/reconciliation δωρεάν.
- **2026-06-08 (Opus 4.8, Plan Mode — «όπως οι μεγάλοι παίχτες, σαν Revit — FULL ENTERPRISE + FULL SSOT»)** — **Φ15 Phase-2 Task B — CROSS-FLOOR «RISER THROUGH» 2D ΣΥΜΒΟΛΟ DONE** (tsc 0 δικά μου· jest **68/68** [mep-segments + useFloors2DUnderlay regression + νέο derive]· 🔴 browser verify + commit). Συνέχεια του Task A (height/floor UI). **Στόχος (Revit «cut plane» annotation):** στον **ενεργό** όροφο ζωγραφίζεται **derived** riser glyph (κύκλος + up/down βέλος) για κάθε κατακόρυφη στήλη **άλλου** ορόφου της οποίας το z-span διαπερνά το FFL του ενεργού — ώστε ο μηχανικός να βλέπει ότι στήλη περνά από το επίπεδό του. **Single source = οι base-floor risers** (zero duplicate persistence — τα σύμβολα παράγονται). **FULL SSOT, 4 αποφάσεις locked:** (1) **Πηγή = raw `SceneModel` BIM entities** (όχι converted DxfScene που χάνει τα mep-segment z/params). (2) **Boy-Scout extract `useBuildingFloorScenes`** — το cross-floor sourcing (visited→`getLevelScene`, unvisited→`loadFileV2` cached, visibility `floorVisibilityModes==='hide'`) ζούσε inline στο `useFloors2DUnderlay`· εξάχθηκε σε **NEW `hooks/data/useBuildingFloorScenes.ts`** (raw models + floorId)· **2 consumers**: ο 2D underlay (convert) + ο riser overlay (filter mep-segments). `useFloors2DUnderlay` τώρα = thin convert wrapper (zero behaviour change· tests πράσινα). (3) **Glyph draw SSoT** — NEW `drawRiserSymbol(ctx, symbol, color, lineWidth)` στο `mep-riser-symbol.ts`· καλείται **και** από `MepSegmentRenderer.renderRiser` (owner, visual αμετάβλητο) **και** από το overlay → ταυτόσημο σύμβολο, μηδέν drift. (4) **Overlay = custom screen-space canvas leaf** (mirror `FloorUnderlayOverlay`), `pointer-events-none`, world→screen via `CoordinateTransforms.worldToScreen`. **Αρχεία (4 NEW + 4 MOD):** NEW `useBuildingFloorScenes.ts`, `bim/mep-segments/derive-risers-through-floor.ts` (pure: `deriveRisersThroughFloor(verticalSegments, currentFloorElevMm)` → marks όπου `min(z)≤FFL≤max(z)` ΚΑΙ όχι ο owner [base `startPoint` floor — εκεί ζωγραφίζει το `renderRiser`]· κρατά classification για χρώμα), `components/dxf-layout/RiserThroughOverlay.tsx` (active σε `mode==='2d'` any scope· FFL = datum-relative του ενεργού μέσω `resolveBuildingDatumElevationM`/`resolveFloorDatumRelativeElevationMm` + `useFloorsByBuilding`), NEW test `derive-risers-through-floor.test.ts` (boundary FFL/owner-exclusion/up+down/non-vertical/tolerance). MOD `mep-riser-symbol.ts` (+drawRiserSymbol), `MepSegmentRenderer.ts` (wire drawRiserSymbol), `useFloors2DUnderlay.ts` (consume extract), `CanvasLayerStack.tsx` (mount). **Convention:** `startPoint.z`/`endPoint.z` = datum-relative mm = ίδιος χώρος με το floor FFL → direct compare. **STAGE ADR-040** (νέο overlay leaf + MepSegmentRenderer touch — CHECK 6B/6D). git add ΜΟΝΟ δικά μου· **ΜΗΝ adr-index** (shared tree). **ΜΑΘΗΜΑ:** cross-floor annotation = derive από single source (base-floor risers) + owner-exclusion γεωμετρικά (FFL==base floor)· glyph draw SSoT μοιράζεται owner-renderer + overlay για μηδέν drift· το cross-floor sourcing είναι κοινό SSoT (underlay + riser overlay), όχι duplicate.
- **2026-06-08 (Opus 4.8, Plan Mode εγκεκριμένο — «ΟΠΩΣ ΟΙ ΜΕΓΑΛΟΙ, σαν Revit/4M — FULL ENTERPRISE + FULL SSOT»)** — **Εύρος Β #3 follow-up — ΕΝΔΟΔΑΠΕΔΙΑ: SPIRAL/SNAIL ΜΟΤΙΒΟ + ΣΤΡΟΓΓΥΛΕΜΕΝΕΣ ΚΑΜΨΕΙΣ (arc-fillet pipe bends) DONE** (tsc 0 δικά μου· jest **30/30** underfloor [+9 νέα]· 🔴 pending browser verify + commit). **Δύο βελτιώσεις μετά από browser feedback Giorgio (3D screenshot: οι αλλαγές διεύθυνσης έδειχναν οξείες μύτες):** **(Α) ΝΕΟ μοτίβο `'spiral'` (αρχιμήδειος κοχλίας / counterflow snail)** — σπειροειδώς περίμετρος→κέντρο σε ομόκεντρα δαχτυλίδια (pitch `2·spacing`) + επιστροφή→έξω interleaved (supply δίπλα σε return παντού· ομοιόμορφη θ., το πιο διαδεδομένο residential). NEW `buildSpiralField` μέσω επαναλαμβανόμενου `insetClosedPolygon` (concentric rings, counterflow ordering even-inward/odd-outward, `reorderRingFromNearest` για σύντομα inter-ring jumps, CAP 200)· concave/εκφυλισμός → graceful fallback στο snake. **(Β) ΣΤΡΟΓΓΥΛΕΜΕΝΕΣ ΚΑΜΨΕΙΣ — αυτό κάνουν οι μεγάλοι (Revit/MagiCAD/4M):** το PEX δεν τσακίζει· min bend radius ~5×διάμετρος, στις 180° U-turns rounding ≤ spacing/2 (αλλιώς overlap σειρών). NEW `resolveUnderfloorBendRadiusScene(params)` = `min(5·diameter, spacing/2)` σε scene units (**παράγεται αυτόματα, ΧΩΡΙΣ νέο UI param**) + NEW pure `buildFilletedUnderfloorPath(path, radius)` (tangent circular arc ανά γωνία, clamp στο μισό κάθε γειτονικού segment, endpoints preserved, sampleArc ≤12 σημεία/καμπή). **SSoT απόφαση (Revit-grade):** το persisted `loopPath` μένει **lean corner polyline** (μικρό Firestore doc, SSoT τοπολογίας)· οι κάμψεις re-derived **on-demand** από **ΕΝΑ shared helper** που καλούν **2D renderer + 3D converter** (μηδέν απόκλιση 2D/3D)· το `totalLengthM` (BOQ) υπολογίζεται **filleted-accurate** στο compute (Revit material-takeoff parity). **3D converter (ΚΡΙΣΙΜΟ — 2ο browser feedback «2D τέλειο, 3D ακόμα γωνίες»):** το `THREE.TubeGeometry`/`CatmullRom` **resample-άρει** την curve ομοιόμορφα κατά arc-length → με path «σύντομα arc U-turns + μακριές ευθείες σειρές» όλα τα δείγματα πάνε στις ευθείες και **παρακάμπτουν τις μικρές κάμψεις** → γωνιώδες 3D (το 2D canvas σχεδιάζει ακριβώς τα σημεία → τέλειο). Fix: **custom `buildUnderfloorTubeGeometry` με ΕΝΑ ring ανά vertex (parallel-transport frames)** — τιμά τα σημεία 1:1 (πυκνά arcs=καμπύλα, αραιές ευθείες=φθηνά), WYSIWYG με 2D· φθηνότερο από over-tessellated TubeGeometry· επιστρέφει `THREE.BufferGeometry` (position+normal+index). **8 αρχεία:** `mep-underfloor-geometry.ts` (engine: `buildPatternField` branch + spiral + fillet + radius resolver)· `mep-underfloor-types.ts` + `mep-underfloor.schemas.ts` (`MepUnderfloorPattern/z.enum += 'spiral'`)· `MepUnderfloorRenderer.ts` (draw filleted path)· `mep-underfloor-to-three.ts` (fillet-first + LineCurve3)· `contextual-mep-underfloor-tab.ts` (option «Σπιράλ (κοχλίας)»)· `useRibbonMepUnderfloorBridge.ts` (PATTERN_VALUES += spiral)· i18n el+en `mepUnderfloorEditor.patternSpiral`. **Tests (+9):** spiral στα 3 it.each· spiral multi-ring inside· `resolveUnderfloorBendRadiusScene` (mm/m+clamp)· `buildFilletedUnderfloorPath` (collinear/right-angle/endpoints/radius0/short). **ΕΚΤΟΣ ADR-040** (engine + pure renderer zero-subscriptions + 3D converter· κανένα canvas micro-leaf). git add ΜΟΝΟ δικά μου· **ΜΗΝ adr-index** (shared tree). **ΜΑΘΗΜΑ:** οι μεγάλοι σχεδιάζουν πραγματικές κάμψεις σταθερής ακτίνας (fillet/bend-radius), ΟΧΙ spline — fillet ΜΙΑ φορά σε shared SSoT helper (corners persisted, bends on-demand) → 2D+3D+BOQ συνεπή· radiant snail = concentric counterflow rings.
- **2026-06-08 (Opus 4.8, Plan Mode εγκεκριμένο — «σαν Revit, FULL ENTERPRISE + FULL SSOT»)** — **Φ8 EXT #2b — PURE-FREEHAND RISER σε 3D (αριθμητικό υψόμετρο, Revit Options Bar "Offset") DONE** (tsc: βλ. session note· jest 42/42 targeted· 🔴 browser verify + commit). **Στόχος (Giorgio, AskUserQuestion → Οδός A):** τοποθέτηση κατακόρυφου/κεκλιμένου riser στο **γενικό** σωλήνα/αεραγωγό (`mep-pipe`/`mep-duct`/`mep-drain-pipe`) **χωρίς** connector snap — ρητή authoring υψομέτρου 2ου άκρου κατά το draw. **Κρίσιμη διαπίστωση (code=SoT):** το **z-pipeline + entity + geometry (Φ15) + ghost υποστηρίζουν ΗΔΗ πλήρως risers** — ο 3D hook διαβάζει `z = connectorZ(snap) ?? centerlineMmNow()` σε **κάθε** click, όπου `centerlineMmNow()=overrides.centerlineElevationMm`. Άρα αν το draw-time override **αλλάξει μεταξύ των 2 clicks** → riser **δωρεάν** (click1 z=E1, click2 z=E2). **ΚΑΜΙΑ αλλαγή** σε hook/ghost/completion/geometry/FSM — η δουλειά είναι **καθαρό draw-time ribbon plumbing** (πρότυπο 1:1 column tool). **Σχεδιασμός «ΕΝΑ tab, dual-mode»:** το ίδιο contextual tab εμφανίζεται και στο selection (επεξεργασία) και στο draw-time (ενεργό εργαλείο, καμία επιλογή)· ο bridge διαχωρίζει με `resolveSegment()==null`. **Αρχεία (6 code + 2 test):** (1) `mep-segment-tool-bridge-store.ts` — handle += `setParamOverrides` (mirror manifold/radiator). (2) `useMepSegmentTool.ts` — δημοσιεύει το ήδη υπάρχον `setParamOverrides` στο bridge handle (+dep). (3) `app/ribbon-contextual-config.ts` [shared] — `useActiveContextualTrigger` += branch `activeTool ∈ {mep-pipe,mep-duct,mep-drain-pipe} → MEP_SEGMENT_CONTEXTUAL_TRIGGER`. (4) `useRibbonMepSegmentBridge.ts` — dual-mode: `mepSegmentToolBridgeStore.use()` + draw-time branches σε `getComboboxState`/`onComboboxChange` (centerlineElevation → read/write override) + `getPanelVisibility(selectionOnly)`. (5) `mep-segment-command-keys.ts` — νέο visibility key `selectionOnly`. (6) `contextual-mep-segment-tab.ts` — split geometry panel: centreline (dual-mode, always) | start/end σε νέο `mep-segment-endpoints` panel (selectionOnly) | actions panel += selectionOnly· +preset `3000`. **ΜΗΔΕΝ νέα i18n** (reuse `centerlineElevation` label — αποφεύγει το σπασμένο `en/dxf-viewer-shell.json` άλλου agent). **Tests:** EXT `useRibbonMepSegmentBridge.test` (draw-time read/write/visibility) + EXT `use-bim3d-mep-segment-placement.test` (override αλλάζει μεταξύ 2 clicks → z 0→3000 riser)· 42/42 PASS. **Διακριτό** από το dedicated `mep-drain-riser` tool (Φ15, point-based «έως όροφο»). **ΕΚΤΟΣ ADR-040** (ribbon + bim-3d/placement, κανένα canvas micro-leaf). git add ΜΟΝΟ δικά μου· **ΜΗΝ adr-index** (shared tree). **ΜΑΘΗΜΑ:** όταν το z-pipeline είναι ήδη per-click, ο riser authoring = ΜΟΝΟ ένα draw-time UI που αλλάζει το override ζωντανά (dual-mode bridge, πρότυπο column)· μηδέν fork.
- **2026-06-08 (Opus 4.8, handoff — «ΟΠΩΣ Η REVIT/4M — FULL ENTERPRISE + FULL SSOT»)** — **Εύρος Β #3 follow-up — ΕΝΔΟΔΑΠΕΔΙΑ 3D: ΠΡΑΓΜΑΤΙΚΕΣ ΣΠΕΙΡΕΣ ΩΣ 3D ΣΩΛΗΝΕΣ (TubeGeometry) DONE** (tsc 0 δικά μου· jest 21/21 = 14 geometry + **7 NEW** converter· 🔴 pending browser verify + commit). **Στόχος (Giorgio):** το 3D της ενδοδαπέδιας να σχεδιάζει τις **πραγματικές σπείρες ως 3D σωλήνες** (όπως Revit/4M-FineHEAT radiant floor), αντί για το σημερινό επίπεδο έγχρωμο layer. **Αρχιτεκτονική απόφαση (Revit-grade, κλειδωμένη):** ο converter επιστρέφει πλέον **`THREE.Group`** = (α) faint translucent **screed band** (context, η παλιά λεπτή πλάκα) **+** (β) **swept `TubeGeometry`** σερπαντίνα = ο γνήσιος βρόχος. **FULL SSOT zero-recompute:** οι σπείρες ΔΕΝ ξαναϋπολογίζονται — reuse του params-derived `entity.geometry.loopPath` (`Point3D[]`, scene-units, ήδη units-correct μετά το ADR-422 unit-fix). **Units-safe** (mirror `mep-wire-to-three`/`mep-segment-to-mesh`): XY scene-units → μέτρα με `sceneUnitsToMeters`, screedOffset/διάμετρος mm → μέτρα με `MM_TO_M`. **Axis mapping explicit:** `world = (x·sceneToM, screedY, -y·sceneToM)` (plan Y=North → world -Z). **Στάθμη:** band bottom + pipe centreline στο `FFL + screedOffsetMm`· ο σωλήνας centreline = screed + radius (κάθεται μέσα στο screed). **Καμπύλη:** `CatmullRomCurve3(..., 'centripetal')` → τα 180° U-turns διαβάζονται ως αληθινές κάμψεις σωλήνα χωρίς overshoot/cusp· `radialSegments=6`, `tubularSegments=clamp(loopPath.length·6, 64, 4000)` (triangle budget). **3 αρχεία (+1 NEW test):** (1) `bim-3d/converters/mep-underfloor-to-three.ts` — ΞΑΝΑΓΡΑΦΤΗΚΕ: `underfloorToMesh`→**`underfloorToObject3D`** (return `THREE.Object3D | null`)· 2 NEW exported pure helpers `buildUnderfloorLoopPoints` (loopPath→world Vector3[]) + `buildUnderfloorTubeGeometry` (centripetal sweep, <2 pts→null)· degenerate loop → band-only fallback· group tagged (bimId/bimType/levelId) + κάθε mesh `tagMesh`. (2) `bim-3d/scene/BimSceneLayer.ts` — `syncUnderfloors` import/κλήση/τοπική μεταβλητή (`obj`)· **dispose ήδη OK** (το `clearGroup` traverse-άρει recursively Groups μετά το ADR-363 wall-Group fix)· `_hasMesh` traverse βρίσκει τα nested meshes. (3) `bim/materials/material-catalog-defs.ts` — **NEW `elem-mep-underfloor-pipe`** (solid warm-red `0xdc2626` PEX, rough 0.45/metal 0.10)· **ΚΡΙΣΙΜΟ:** μπήκε **ΠΡΙΝ** το `elem-mep-underfloor` αφού το `resolveMaterialKey` είναι prefix `startsWith` first-wins (`elem-mep-underfloor-pipe`.startsWith(`elem-mep-underfloor`) ⇒ θα έπεφτε στο band· η πιο ειδική σειρά πρώτη). (4) NEW `__tests__/mep-underfloor-to-three.test.ts` (7: helper axis/elevation, tube null<2, Group+tube, screed-elevation Y, units-safe mm==m, degenerate band-only). **ΕΚΤΟΣ ADR-040** (3D pure converter, κανένα 2D canvas micro-leaf· κανένα CHECK 6B/6C/6D αρχείο). git add ΜΟΝΟ δικά μου· **ΜΗΝ adr-index** (shared tree). **ΠΑΛΙΕΣ ενδοδαπέδιες** = re-create/reload (cached geometry). **ΜΑΘΗΜΑ:** Revit-grade radiant floor 3D = sweep `TubeGeometry` κατά μήκος του params-derived loopPath (μηδέν recompute)· prefix-resolver material keys = πιο ειδικό πρώτο.
- **2026-06-08 (Opus 4.8, Plan Mode — «FULL ENTERPRISE + FULL SSOT, σαν Revit»)** — **Φ15 Phase-2 Task A — RISER HEIGHT/FLOOR UI (Revit base/top constraint) DONE** (tsc 0 δικά μου· 55/55 mep-segments jest· 🔴 browser verify + commit). Συνέχεια του Φ15 v1 (engine+tool, vertical `mep-segment` = drain riser). **Στόχος:** contextual tab όταν το riser tool είναι active → ο χρήστης διαλέγει «Έως όροφο» + διάμετρο· το riser χτίζεται με σωστά datum-relative base/top αντί για το v1 fixed default 3m. **Revit μοντέλο:** base = ο **τρέχων** όροφος (όπου σχεδιάζεις), combobox «Έως όροφο» = ο top όροφος· και τα δύο ελευθερώνονται σε **datum-relative mm** μέσω του ίδιου SSoT που στοιβάζει το 3D (`resolveBuildingDatumElevationM`/`resolveFloorDatumRelativeElevationMm`, `bim-3d/scene/floor-stack-elevation.ts`). **FULL SSOT μηδέν fork** — reuse `useFloorsByBuilding`/`useLevelsOptional` + mirror του light-fixture-library tool-active pattern (bridge/tab/command-keys). **Αλλαγές:** (1) `mep-riser-tool-bridge-store.ts` handle `heightMm/setHeight` → `baseElevationMm/topElevationMm/setSpanMm` (κανείς δεν το διάβαζε — clean refactor)· (2) `useMepRiserTool.ts` state base/top + `setSpan`, το click χτίζει `completeMepSegmentFromTwoClicks(..., baseElevationMm, topElevationMm)`· (3) **NEW** `ui/ribbon/hooks/bridge/mep-riser-command-keys.ts` (`MEP_RISER_RIBBON_KEYS {toFloor:string, diameter:num}` + guards)· (4) **NEW** `ui/ribbon/data/contextual-mep-riser-tab.ts` (tab «Κατακόρυφη Στήλη», trigger `mep-riser-tool-active`· «Έως όροφο» dynamic options από bridge + «Διάμετρος» DN drainage 50–160)· (5) **NEW** `ui/ribbon/hooks/useRibbonMepRiserBridge.ts` (floors logic: `floorsAbove` = όροφοι πάνω από τον τρέχοντα· init-once-per-activation effect ευθυγραμμίζει το span στον τρέχοντα όροφο [base] + nearest-above [top]· `getComboboxState` value = ο όροφος όπου `topMm === topElevationMm`· `onComboboxChange` → `setSpanMm`/`setDiameter`)· (6) wiring `useRibbonCommands.ts` (import/type/prop/destructure/2 dispatch branches/3 dep arrays) + `useDxfBimBridges.ts` + `useDxfViewerRibbon.ts` (construct+pass) + `app/ribbon-contextual-config.ts` [shared] (register `CONTEXTUAL_MEP_RISER_TAB` + trigger branch `activeTool === 'mep-drain-riser'`)· (7) i18n el+en `ribbon.tabs/panels/commands.mepRiser.*` (parity). **ΕΚΤΟΣ ADR-040** — pure ribbon/bridge, κανένα canvas renderer/overlay/CanvasSection (όχι CHECK 6B/6D). git add ΜΟΝΟ δικά μου· **ΜΗΝ adr-index** (shared codex tree). **🔴 ΕΠΟΜΕΝΟ Task B:** cross-floor «riser through» 2D σύμβολο σε passed floors (NEW `derive-risers-through-floor.ts` + read-only overlay leaf mirror `FloorUnderlayOverlay` ADR-399 Phase D — STAGE ADR-040). **ΜΑΘΗΜΑ:** tool-active ribbon constraint = mirror του fixture-library (bridge-store + bridge hook + command-keys + contextual tab + composer wiring)· το «ύψος» γίνεται Revit base/top με τα floor elevations να περνούν από το ίδιο datum SSoT με το 3D stack.
- **2026-06-08 (Opus 4.8, συνέχεια handoff — «FULL ENTERPRISE + FULL SSOT, σαν Revit»)** — **Φ8 EXT — 3D ΣΩΛΗΝΑΣ Revit-grade: connector-Z mate («Connect To») + κλίση/offset authoring (risers).** Στόχος (Giorgio): ο σωλήνας σε 3D να συμπεριφέρεται «όπως η Revit». Μέχρι τώρα τα 3D clicks έβγαιναν **χωρίς z** → πάντα οριζόντιος στο default centreline. **Κρίσιμη διαπίστωση (honesty / code=SoT):** ΟΛΟΚΛΗΡΟ το z-pipeline υπήρχε ήδη και **δεν άλλαξε** — `MepSegmentClickPoint` δέχεται `z?`, `useMepSegmentTool.onCanvasClick` αποθηκεύει `startElevationMm` + περνά `endPoint.z`, `completeMepSegmentFromTwoClicks` κάνει per-endpoint z (Φ-A) + drain slope. Έλειπε **ΜΟΝΟ** να φτάσει το z στο emitted event από τον 3D hook. **Σχεδιασμός:** σε κάθε move+click ο 3D hook υπολογίζει `elevationMm = connectorZ(snap) ?? centerlineOffset@clickTime` και το εκπέμπει **πάντα** ως `point.z`· διαφορετικό start/end ⇒ κεκλιμένος σωλήνας/riser (Revit per-click offset)· ίδιο ⇒ οριζόντιος (καμία οπισθοδρόμηση). **1 NEW SSoT helper** `bim/mep-segments/mep-snap-connector-elevation.ts` (`resolveSnapConnectorElevationMm` — candidate→host→`resolveMepConnectorElevationMmAt`)· **Boy-Scout:** ο 2D `mouse-handler-up.ts` (γρ. 226-233) ΞΑΝΑΓΡΑΦΤΗΚΕ να καλεί τον ΙΔΙΟ helper → 2D+3D μοιράζονται ΕΝΑ SSoT (−2 imports). **MOD:** `placement-snap.ts` (επιφανειακό `snapEntityId`/`snapType` — geometry-only, καμία MEP/scene εξάρτηση)· `use-bim3d-mep-segment-placement.ts` (host lookup στο `Bim3DEntitiesStore` [mepSegments/manifolds/radiators/boilers/fixtures/underfloors]· **μονάδες:** το nearest-connector pick γίνεται σε SCENE units μέσω `planMmToScenePoint`, ΟΧΙ plan-mm — το z μένει mm Φ-A· marker + ghost στο σωστό riser ύψος)· `MepSegmentPlacementGhost.update(+endElevationMm)` (WYSIWYG κλίση)· `drawing-event-map.ts` (`bim:place-mep-segment-3d` payload += `z?`). **ΧΩΡΙΣ αλλαγή (ready):** useMepSegmentTool/mep-segment-completion/bridge-store/mep-connector-elevation. **Drain slope ΔΕΝ σπάει** (επαληθευμένο: ο guard `bothSnappedDistinct` ελέγχει *distinct* όχι *non-null* → οριζόντιο drain start==end → slopePercent ισχύει). **Tests:** NEW `mep-snap-connector-elevation.test` (5) + EXT `placement-snap.test` (candidate surfacing) + EXT `use-bim3d-mep-segment-placement.test` (free→centreline z, override z, connector z)· 33/33 targeted + 138/138 MEP regression PASS, tsc 0 δικά μου. **ΕΚΤΟΣ ADR-040** (`bim-3d/placement/` pure-Three· `mouse-handler-up` = καθαρό SSoT refactor). git add ΜΟΝΟ δικά μου· **ΜΗΝ adr-index** (shared tree). **ΜΑΘΗΜΑ:** Revit-grade 3D linear placement = ο hook απλώς διοχετεύει per-click elevation (connector ?? offset)· το FSM/completion (Φ-A per-endpoint z) ήταν ήδη η SSoT — μηδέν fork. 🔴 browser verify + commit (Giorgio).
- **2026-06-08 (Opus 4.8, handoff — «FULL ENTERPRISE + FULL SSOT, σαν Revit»)** — **Εύρος Β BOQ AUTO-FEED: MEP → Προμετρήσεις με ΗΛΜ άρθρα (Revit Material/System Takeoff).** Τα 5 θερμαντικά/MEP entities (καλοριφέρ/λέβητας/συλλέκτης/σωλήνας/ενδοδαπέδια) τροφοδοτούν πλέον αυτόματα το BOQ όπως ήδη τα δομικά (OIK). **FULL SSOT zero-fork:** μόνο επέκταση `bim-to-atoe-mapping.ts` (`BimEntityType += mep-*`· νέοι πίνακες `MEP_*_MAPPING` με **placeholder ΗΛΜ codes** ανά ΑΤΗΕ group [ΗΛΜ-5 ύδρευση / 6 αποχέτευση / 7 θέρμανση / 8 HVAC]· resolver `+classification` param + `resolveMepSegmentMapping`) + wiring των 5 MEP persistence hooks στον `BimToBoqBridge` (upsert στο persist, delete στο delete-path· νέο `buildingId` param σε hooks+hosts→DxfViewerTopBar). **Μονάδες:** καλοριφέρ/λέβητας/συλλέκτης = `pcs` (qty 1)· σωλήνας/ενδοδαπέδια = `m` (segment `geometry.length`→`lengthM`· underfloor `totalLengthM`→`lengthM`). **Per-System takeoff (Revit):** ο σωλήνας μετριέται ΑΝΑ plumbing classification (hydronic-supply ΗΛΜ-7.10 / return 7.11 / cold 5.01 / hot 5.02 / drainage 6.01 / generic 5.00· duct HVAC 8.01) → χωριστές γραμμές BOQ· φρεάτιο `drainage-collector` = ΗΛΜ-6.02. `deriveAtoeQuantity` αμετάβλητο (pcs/m ήδη supported). auto-fittings DEFERRED v1 (mapping προβλέπει χώρο). **Tests:** +mep cases στο bim-to-atoe-mapping (+ segment classification override) + mep upsert/delete στο BimToBoqBridge = 86/86 PASS· Boy-Scout fix stale sliding-door assertion (OIK-5.01). tsc 0 δικά μου. ⚠️ Placeholder ΗΛΜ codes → αντικατάσταση με πραγματικά άρθρα ΣΕ ΕΝΑ αρχείο. ΕΚΤΟΣ ADR-040. 🔴 browser verify + commit (Giorgio).
- **2026-06-08 (Opus 4.8, συνέχεια handoff — «FULL ENTERPRISE + FULL SSOT, σαν Revit»)** — **Εύρος Β #1+#2 follow-up — ΚΑΛΟΡΙΦΕΡ (`mep-radiator`) + ΛΕΒΗΤΑΣ (`mep-boiler`): 3D-VIEWPORT CLICK-PLACEMENT DONE** (tsc 0 δικά μου — τα εναπομείναντα errors είναι pre-existing `mesh-to-object3d:124`/`apply-entity-preview:316`/`DeleteEntityCommand:54` roof + `useOpeningFamilyTypeController` WIP άλλου agent στο shared tree που ΔΕΝ άγγιξα· 🔴 pending browser verify + commit). **Στόχος (Giorgio):** τοποθέτηση καλοριφέρ + λέβητα με **κλικ μέσα στο 3D viewport** (single-click, point-based), όχι μόνο στην 2D κάτοψη — συμπληρώνει το pattern που ήδη υπήρχε για συλλέκτη (Φ12) + σωλήνα (Φ8). **Διαπίστωση (honesty / code = source of truth):** όλο το «δύσκολο» scaffolding (events `bim:place-mep-{radiator,boiler}-3d`, 2D listeners `useMep{Radiator,Boiler}Tool.ts:141`, bridge-stores `mep-{radiator,boiler}-tool-bridge-store`, converters `radiatorToMesh`/`boilerToMesh`, completion builders, DEFAULT mounting elevations 450/1200mm) είχε ΗΔΗ μπει όταν φτιάχτηκαν τα entities — έλειπε **ΜΟΝΟ** ο 3D-side emitter + ghost. **Template = `useBim3DMepManifoldPlacement` (point-based single-click).** **4 NEW + 2 mount γραμμές (όλα clone manifold):** (1) `bim-3d/placement/use-bim3d-mep-radiator-placement.ts` (gate `activeTool==='mep-radiator'` && `selectIs3D`· work-plane raycast στο `floorElev + mountingElevationMm`· orbit-drag guard 5px· OSNAP `resolvePlacementSnap`· emit `bim:place-mep-radiator-3d` → 2D `onCanvasClick` = commit path **ΕΝΑ** με το 2D, FSM=SSoT). (2) `bim-3d/placement/MepRadiatorPlacementGhost.ts` (pure-Three· `buildDefaultMepRadiatorParams`→`computeMepRadiatorGeometry`→`radiatorToMesh`· warm-red `0xdc2626` translucent· WYSIWYG ghost==cursor αφού ΙΔΙΑ elevation τροφοδοτεί raycast + mesh). (3)+(4) λέβητα ζεύγος (`use-bim3d-mep-boiler-placement.ts` + `MepBoilerPlacementGhost.ts`· `boilerToMesh`· DEFAULT 1200mm). (5) `BimViewport3D.tsx` — 2 imports + 2 κλήσεις δίπλα στο manifold/segment placement. **FULL SSOT μηδέν fork:** ο 3D hook ΜΟΝΟ υπολογίζει world point· κανένα διπλό commit logic. **v1 (mirror manifold):** default rotation/anchor (όχι Tab-anchor στο 3D placement — documented follow-up). **ΕΚΤΟΣ ADR-040** (`bim-3d/placement/` pure-Three, κανένα 2D canvas micro-leaf· κανένα CHECK 6B/6C/6D αρχείο). git add ΜΟΝΟ δικά μου· **ΜΗΝ adr-index** (shared tree). **ΜΑΘΗΜΑ:** όταν το entity scaffolding (events/2D listener/bridge-store/converter) μπει σωστά εξαρχής, το 3D point-based placement = pure clone του manifold (emitter + ghost), μηδέν αλλαγή στο commit path. Με αυτό **κλείνει το Εύρος Β θέρμανσης** (δίκτυο + καλοριφέρ + λέβητας + ενδοδαπέδια, 3D placement σε όλα τα point-based)· απομένει μόνο BOQ θέρμανσης (🔴 BLOCKED — χρειάζεται κωδικό άρθρου ΗΛΜ).
- **2026-06-07 (Opus 4.8, συνέχεια handoff — «FULL ENTERPRISE + FULL SSOT, σαν Revit»)** — **Σύστημα Θέρμανσης Εύρος Β #3 — ΕΝΔΟΔΑΠΕΔΙΑ ΘΕΡΜΑΝΣΗ (`mep-underfloor`) νέο AREA-based BIM entity — Waves A–F DONE** (tsc 0 δικά μου — τα εναπομείναντα errors είναι pre-existing `mesh-to-object3d:124`/`apply-entity-preview:316` + roof/`useOpeningFamilyTypeController` WIP άλλου agent στο shared tree που ΔΕΝ άγγιξα· underfloor geometry 13/13 + beam-hatch 23/23 PASS· 🔴 pending browser verify + commit + `firebase deploy --only firestore:indexes,firestore:rules`). **Απόφαση αρχιτεκτονικής (Revit-true, κλειδωμένη Plan Mode):** η ενδοδαπέδια είναι **AREA-based τερματικό** (`IfcSpaceHeater` radiant) — clone του **FloorFinish** (ADR-419 «ένα polygon ανά δωμάτιο»), ΟΧΙ point-based σαν καλοριφέρ/λέβητας. Έχει δικό του `footprint: Polygon3D` (world mm, CCW)· μέσα του υπολογίζεται **παραμετρική σερπαντίνα σωλήνων** (`computeMepUnderfloorGeometry` SSoT). **2 connectors στην είσοδο** supply (`uf-supply`, `flow:in`, hydronic-supply) + return (`uf-return`, `flow:out`, hydronic-return) → member supply+return δικτύου ταυτόχρονα σαν καλοριφέρ (per-(entity,connector), μηδέν special logic)· **ΟΧΙ** network source, **ΟΧΙ** auto-fittings (ένας συνεχής βρόχος). **IDENTITY host transform** (όχι `position`/`rotation`)· τα connectors αποθηκεύουν `localPosition` σε WORLD coords (τα entry points). prefix `uhf`, collection `floorplan_mep_underfloors`, discipline `plumbing`, χρώμα κόκκινο `#dc2626`. **2 patterns** `boustrophedon` (μπρος-πίσω) + `counterflow-spiral` (διπλή σπείρα/αντιρροή), default boustrophedon· `totalLengthM` → BOQ-ready. **Wave 0 Foundation** (types/schemas/serpentine geometry+13 tests/enterprise-id/collection/connector builders· SSoT promotion `buildAxisAlignedHatch`+`clipLineToBbox` στο polygon-utils — beam re-imports). **Wave A 2D rendering** (`MepUnderfloorRenderer` clone FloorFinish: translucent fill + σερπαντίνα polyline + 2 ◇ connectors· + 7 registration silent-drop points). **Wave B grips** (polygon vertex/edge grips clone FloorFinish + `UpdateMepUnderfloorParamsCommand`). **Wave C creation** (NEW completion+preview-store+`useMepUnderfloorTool`+factory· additive canvas-click-types/useCanvasClickHandler/useSpecialTools/drawing-preview-tool-points/drawing-preview-generator/bim-ortho-reference/home-tab-draw[«Ενδοδαπέδια» στο Heating group]/i18n· reuse `SlabToolLike`+`appendEntityToScene`· **Boy-Scout fix:** `'floor-finish'` έλειπε από το `DrawingTool` union — προστέθηκε μαζί με `'mep-underfloor'`). **Wave D 3D** (`underfloorToMesh` = thin screed band @ `screedOffsetMm`+`elem-mep-underfloor` material· store `underfloors` slice+`setUnderfloors`+`syncUnderfloors`+resync+aggregator+ReadOnlyOverlay). **Wave E persistence+connectivity** (NEW firestore-service+audit-client+`useMepUnderfloorPersistence`[connector systemId projection guard]+`MepUnderfloorPersistenceHost` mounted DxfViewerTopBar· additive audit-tracked-fields+connector-seed`buildUnderfloorConnectors`+reconciliation+**connector-elevation special branch** [underfloor ΔΕΝ έχει position → datum = `FFL + screedOffsetMm`]+useSmartDelete+restore-effect union+**firestore.rules** block+**firestore.indexes ×4**). **Wave F contextual tab «Ιδιότητες Ενδοδαπέδιας»** (Geometry [spacing/clearance/patternType selector/totalLength readonly readout] / Thermal [screedOffset/connectorDiameter/thermalOutput] / **Δίκτυο fold-in** [reuse `resolveManagedSystems` + mep-circuit/pipe-network widgets] / Actions· NEW command-keys+bridge+tab· additive ribbon-contextual-config/useDxfBimBridges/useRibbonCommands/useDxfViewerRibbon/i18n· bridge ξαναχτίζει connectors `buildUnderfloorConnectors` πριν dispatch αφού το command recompute-άρει μόνο geometry). **ΜΑΘΗΜΑΤΑ:** (1) το handoff ήταν out-of-date — Wave A ήταν ΗΔΗ ολοκληρωμένο στον κώδικα (code = source of truth, N.0.1)· (2) area entity = clone FloorFinish/slab παντού (bim-bounds/grips/ghost/hit-test/renderer/3D thin solid), ΟΧΙ boiler centred-box· (3) connector-elevation για identity-transform host = ξεχωριστό branch (datum=screed offset)· (4) `UpdateMepUnderfloorParamsCommand` recompute-άρει μόνο geometry → ο tab bridge ξαναχτίζει connectors. **PENDING:** BOQ (BLOCKED ΗΛΜ code)· 3D-viewport click-placement (area = δύσκολο, deferred). **ΕΚΤΟΣ ADR-040** (κανένα canvas micro-leaf αρχείο — area entity, χωρίς ghost-leaf· ο renderer είναι pure). git add ΜΟΝΟ δικά μου· **ΜΗΝ adr-index** (SHARED tree).
- **2026-06-07 (Opus 4.8, Plan Mode εγκεκριμένο — «FULL ENTERPRISE + FULL SSOT, σαν Revit»)** — **Φ8 follow-up — ΣΩΛΗΝΑΣ/ΑΕΡΑΓΩΓΟΣ: 3D 2-CLICK PLACEMENT DONE** (tsc 0 δικά μου· 24 νέα tests + 107 mep-segment regression PASS· 🔴 pending browser verify + commit). **Στόχος (Giorgio):** τοποθέτηση σωλήνα ύδρευσης (`mep-segment`, domain `pipe`) με κλικ **μέσα στο 3D viewport**, όχι μόνο στην 2D κάτοψη — «σαν Revit». **Διαπίστωση (honesty):** ο **ΣΥΛΛΕΚΤΗΣ** (`mep-manifold`) είχε **ΗΔΗ** πλήρες 3D placement (`useBim3DMepManifoldPlacement` mounted, ADR-403/Φ12) → καμία αλλαγή, μόνο verify. Έλειπε **μόνο ο σωλήνας**. **Αρχιτεκτονική (ADR-403 framework, πρώτη LINEAR οντότητα):** το event `bim:place-mep-segment-3d` + ο listener στο `useMepSegmentTool` υπήρχαν ήδη («reserved for 3D tool») — έλειπε ο **emitter** + το rubber-band ghost. **3 NEW:** (1) `bim-3d/placement/use-bim3d-mep-segment-placement.ts` (mirror του manifold hook· gate σε `mep-pipe`/`mep-duct`/`mep-drain-pipe` + 3D· raycast στο centreline work-plane· orbit-drag guard· OSNAP reuse). (2) `bim-3d/placement/MepSegmentPlacementGhost.ts` (pure-Three rubber-band axis start→cursor μέσω `completeMepSegmentFromTwoClicks`→`mepSegmentToMesh`· ορατό ΜΟΝΟ σε `awaitingEnd`· χρώμα classification/domain SSoT). (3) `ui/ribbon/hooks/bridge/mep-segment-tool-bridge-store.ts` (read-only projection του FSM — εκτός domain/overrides/units **και** `phase`+`startPoint`+`startElevationMm`, ώστε το pure-Three ghost να ξέρει πότε/από πού να σχεδιάσει· διαφορά από τον point-based manifold bridge). **2 MODIFIED:** `useMepSegmentTool.ts` (single-writer bridge publish effect, mirror manifold· **μηδέν** αλλαγή στο FSM/commit), `BimViewport3D.tsx` (mount, 1 γραμμή + import δίπλα στο manifold). **FULL SSOT:** το commit path (`completeMepSegmentFromTwoClicks`→`addMepSegmentToScene`) μένει **ΕΝΑ** με το 2D — το 3D αλλάζει μόνο το «πώς παίρνω world point». **v1:** οριζόντιος σωλήνας στο default centreline — τα clicks **ΔΕΝ** φέρουν z (free-point convention, ίδιο με 2D), η completion βάζει default centreline, το sync ξαναπροσθέτει το floor base (WYSIWYG). **Follow-up (documented):** connector-Z mate σε 3D (recovery z από MEP connector, mirror 2D Φ-B1) + sloped 3D runs. **ΕΚΤΟΣ ADR-040** (`bim-3d/` placement, όχι 2D canvas micro-leaf· κανένα CHECK 6B/6C/6D αρχείο, όπως και το manifold 3D placement). git add ΜΟΝΟ δικά μου· **ΜΗΝ adr-index** (shared tree). **ΜΑΘΗΜΑ:** για linear 2-click στο 3D placement framework, ο bridge store πρέπει να κατοπτρίζει `phase`+`startPoint` (όχι μόνο overrides όπως στα point-based) ώστε το pure-Three ghost να σχεδιάζει το rubber-band — το FSM μένει single source of truth, ο bridge είναι read-only projection.
- **2026-06-07 (Opus 4.8, Plan Mode εγκεκριμένο — «FULL ENTERPRISE + FULL SSOT, όπως η Revit»)** — **Φ12 follow-up — ΣΥΛΛΕΚΤΗΣ ΥΔΡΕΥΣΗΣ: GRIPS ADD/REMOVE OUTLET (Revit "array control" ▲/▼) DONE** (tsc 0 δικά μου — τα 4 εναπομείναντα project errors είναι pre-existing `mesh-to-object3d:124` + WIP άλλου agent σε `floor-finish`/`double-door`/`apply-entity-preview` που ΔΕΝ άγγιξα· jest 63/63 manifold + 7/7 glyph-registry + 898 grip/mep regression PASS· 🔴 pending browser verify + commit). **Στόχος (Giorgio):** on-canvas affordance «όπως οι μεγάλοι παίκτες, σαν Revit» για αλλαγή `outletCount` (πριν: μόνο από το tab «Έξοδοι»). **Αρχιτεκτονική (Revit-true):** η Revit ελέγχει τον αριθμό ports ενός array/multi-port στοιχείου με το **array control** = αριθμός + βελάκια ▲/▼ (single-click), όχι «εφεύρημα» +/− κουμπιά ούτε drag-stretch (τα outlets ξαναμοιράζονται evenly σε σταθερή μπάρα → stretch δεν έχει γεωμετρικό νόημα). **2 single-click ACTION grips** `'mep-manifold-outlet-add'` (▲) / `'mep-manifold-outlet-remove'` (▼) στο +X άκρο της μπάρας (αντίθετα από το −X inlet), **mirror του `opening-rotation`** pattern: ο `commitDxfGripDragModeAware` τα δρομολογεί **ΠΡΙΝ** το zero-delta guard (η λαβή ΕΙΝΑΙ το κουμπί). **Disabled-at-bound (Revit):** το ▲ κρύβεται στο `MAX_MANIFOLD_OUTLET_COUNT` (12), το ▼ στο `MIN` (1) → κανένα no-op click. **FULL SSOT (το γνήσια enterprise κομμάτι):** η λογική «re-seed connectors (`buildMepManifoldConnectors`) → `UpdateMepManifoldParamsCommand` → `resolveManifoldConnectedPipePatches` → `CompoundCommand` (host moves, connectors follow)» ζούσε **ΑΠΟΚΛΕΙΣΤΙΚΑ** στο `dispatchParams` του `useRibbonMepManifoldBridge` (το `commitMepManifoldGripDrag` ΔΕΝ έκανε re-seed). **Boy-scout extract (N.0.2)** → NEW pure `bim/mep-manifolds/mep-manifold-param-update.ts` `buildManifoldParamUpdate(entities, manifold, nextParams, sceneManager) → { command, segmentIds }` που καλούν **και** ο tab bridge **και** το νέο grip commit → μηδέν copy-paste, tab+grip δεν αποκλίνουν ποτέ. **9 αρχεία (2 NEW):** (1) NEW `mep-manifold-param-update.ts` (SSoT builder). (2) `useRibbonMepManifoldBridge.ts` — `dispatchParams` καλεί τον helper (−5 imports). (3) `hooks/grip-kinds.ts` — `MepManifoldGripKind` +2 literals. (4) `mep-manifold-grips.ts` — `getMepManifoldGrips` εκπέμπει τα 2 action grips (θέσεις στο +X μέσω `rotateVector`/`mmScaleFor`· hidden-at-bound· stable index 6/7· `KIND_TO_ROLE` → `Partial` αφού τα action kinds δεν έχουν box role). (5) `rendering/grips/types.ts` — `GripShape` +`'triangle-up'`/`'triangle-down'`. (6) `rendering/grips/GripShapeRenderer.ts` — 2 cases + `renderTriangleGlyph` (filled ▲/▼). (7) `bim/grips/grip-glyph-registry.ts` — 2 kinds → triangle glyphs. (8) `grip-parametric-centred-box-commits.ts` — NEW `commitMepManifoldOutletCountGrip` (single-click, `outletCount ±1` clamp, καλεί SSoT helper, `getEntities?.()` optional-guard για το connected-pipe set) + re-export μέσω `grip-parametric-commits.ts`. (9) `grip-commit-adapters.ts` — intercept πριν το zero-delta guard. **Tests:** grips (+4: both action grips στο mid count → 8 grips total· θέσεις +X· hidden-at-MAX/MIN)· NEW `mep-manifold-param-update.test.ts` (re-seed N+1· plain vs `CompoundCommand`+segmentIds με mocked patches)· glyph-registry (+1 manifold ▲/▼ + επεκτάθηκε το «every entry move/rotation» → +triangle). **Λειτουργεί και για `drainage-collector`** (το `outletCount` εκεί = branch-inlet count· ίδια γεωμετρία). **ΕΚΤΟΣ ADR-040** (το `GripShapeRenderer`/glyph registry είναι pure rendering primitives, όχι canvas micro-leaf subscription· κανένα CHECK 6B/6C/6D αρχείο). git add ΜΟΝΟ δικά μου· **ΜΗΝ adr-index** (shared tree). **ΜΑΘΗΜΑ:** Revit "array control" = single-click ▲/▼ action grips (mirror `opening-rotation` zero-delta intercept), ΟΧΙ drag-to-count· όταν δύο entry points (tab + grip) γράφουν το ίδιο πεδίο με μη-τετριμμένα side-effects (re-seed + connected-pipe follow), extract σε ΕΝΑ pure command-builder αντί να αντιγραφεί.
- **2026-06-05 (Opus 4.8, Plan Mode εγκεκριμένο + 6 subagents — «FULL ENTERPRISE + FULL SSOT, σαν Revit»)** — **Σύστημα Θέρμανσης Εύρος Β #2 — ΛΕΒΗΤΑΣ (`mep-boiler`) νέο point-based BIM entity DONE** (tsc production 0 boiler errors — τα 5 εναπομείναντα είναι pre-existing σε useRoofFamilyTypeController/StandaloneStatusBar που ΔΕΝ άγγιξα· geometry 12/12 + bridge 10/10 PASS· 🔴 pending browser verify + commit + firebase deploy· SHARED tree). **Απόφαση αρχιτεκτονικής (Revit-true):** ο λέβητας είναι **Mechanical Equipment / `IfcBoiler`** — ΞΕΧΩΡΙΣΤΟ entity (όχι `MepManifoldKind` hack που θα έδινε λάθος `IfcPipeFitting`). Template = **καλοριφέρ** (point-based 2-connector). **Δύο deltas vs καλοριφέρ:** (1) ο λέβητας είναι **ΠΗΓΗ** — οι 2 connectors έχουν ΑΝΤΙΣΤΡΟΦΗ ροή: supply outlet (`boiler-supply`, `flow:'out'`, `hydronic-supply`, +X → **πηγάζει** το supply δίκτυο) + return inlet (`boiler-return`, `flow:'in'`, `hydronic-return`, −X). (2) **owns `systemClassification`** (default `hydronic-supply`· το δίκτυο που πηγάζει το κληρονομεί — Revit «source owns System Classification»). prefix `blr`, collection `floorplan_mep_boilers`, discipline `plumbing`, χρώμα κόκκινο `#dc2626`, icon Flame, defaults 450×350×700mm @ elev 1200. **FULL SSOT γενίκευση network-source (το γνήσια νέο):** NEW canonical guard `bim/mep-systems/pipe-network-source.ts` `isPipeNetworkSourceEntity(entity)` (manifold | boiler) + `findPipeNetworkSourceConnectorId` = **η ΜΙΑ πηγή** για «τι πηγάζει δίκτυο σωλήνων»· ο `resolvePipeNetworkFromSelection` (`isMepManifoldEntity` filter → `isPipeNetworkSourceEntity`· local `findManifoldSourceConnectorId` → SSoT) + ο contextual-trigger branch (`hasManifold` → `hasNetworkSource`) ΔΡΟΜΟΛΟΓΟΥΝ μέσω του guard → επιλογή λέβητα+σωλήνες δημιουργεί hydronic-supply δίκτυο ΔΩΡΕΑΝ. **ΟΛΟΚΛΗΡΩΜΕΝΑ (11 slices, ~55 αρχεία):** types/schemas/entity-union(+`IfcBoiler` ×3 ifc-entity-mixin + `mep-boiler` σε base-entity `EntityType` + bim-base `BimElementType`)· geometry+`buildBoilerConnectors`(αντίστροφη ροή)+12 tests· 2D renderer+symbol(burner/flame glyph)+silent-drop (scene-converter/renderer-model/dxf-types/object-styles/subcategories/discipline/EntityRendererComposite)· grips(centred-box SSoT, `MepBoilerGripKind`)· ghost(STAGE ADR-040)· tool(completion+hook+bridge-store+add-to-scene+useSpecialTools+useCanvasClickHandler+`MepBoilerToolLike`+field UseCanvasClickHandlerParams+tool-definitions+home-tab-draw+ToolType+events+i18n)· factory+enterprise-id(blr)· hit-test/bounds/selection-duplicate/useSmartDelete/DeleteEntityCommand/restore+`UpdateMepBoilerParamsCommand`· 3D(`boilerToMesh`+`syncBoilers`+store `boilers` slice+`elem-mep-boiler`+aggregator+resync+ReadOnlyOverlay)· **connectivity additive** (connector-access/seed/reconciliation/SnapEngine/elevation → πιπες snap+inherit elevation δωρεάν)· **persistence ΠΛΗΡΗΣ** (firestore-service+audit+hook+host mounted+collections+audit-tracked[+systemClassification]+rules+indexes×2)· **contextual tab «Ιδιότητες Λέβητα»** (Geometry/Thermal/**Δίκτυο fold-in** [reuse `resolveManagedSystems` + mep-circuit/pipe-network widgets — ο λέβητας πηγάζει, ΟΧΙ classification picker αφού fixed hydronic-supply]/Actions + bridge `getPanelVisibility` hasNetwork + 10 tests). **ΜΑΘΗΜΑ:** νέο BIM entity = registration ΚΑΙ στο `base-entity.ts` `EntityType` union (αλλιώς «incorrectly extends BimEntity» — ξεχωριστό από το bim-base `BimElementType`)· source-vs-terminal = αντίστροφη connector flow + το network-source guard ως SSoT γενικεύεται, δεν fork-άρεται. **PENDING:** 3D-viewport click-placement· BOQ (BLOCKED ΗΛΜ code). **Εύρος Β #3+:** ενδοδαπέδια (area-based). ΕΚΤΟΣ ADR-040 (πλην ghost). ΜΗΝ adr-index (SHARED tree).
- **2026-06-05 (Opus 4.8 — εγκεκριμένο, «FULL ENTERPRISE + FULL SSOT, σαν Revit»)** — **Σύστημα Θέρμανσης Εύρος Β #1 — SLICE 11: CONTEXTUAL TAB «Ιδιότητες Καλοριφέρ» DONE** (tsc production 0 δικά μου — μόνο pre-existing `mesh-to-object3d:124`· bridge test **10/10 PASS**· firebase indexes+rules του entity ήδη deployed νωρίτερα στη session· 🔴 pending browser verify + commit). **Στόχος (Giorgio):** το placed καλοριφέρ να επεξεργάζεται από contextual ribbon tab (όχι μόνο grips). **Αρχιτεκτονική (mirror «Ιδιότητες Συλλέκτη» Φ12 αλλά ΛΙΤΟΤΕΡΟ):** το καλοριφέρ είναι **τερματικό** → **ΟΧΙ** system classification (σταθερή ροή supply/return), **ΟΧΙ** outlet count (2 σταθεροί connectors), **ΟΧΙ** network-management panel (καταναλωτής, όχι πηγή — δεν sourcing δίκτυο). **3 panels:** Geometry (width/length/bodyHeight/mountingElevation) · Thermal (connectorDiameter/thermalOutput W) · Actions (close/delete). **3 νέα αρχεία:** `ui/ribbon/hooks/bridge/mep-radiator-command-keys.ts` (numeric key registry + action keys + type guards· χωρίς classification/visibility keys) · `ui/ribbon/data/contextual-mep-radiator-tab.ts` (`CONTEXTUAL_MEP_RADIATOR_TAB` + trigger `mep-radiator-selected` + mm/W presets) · `ui/ribbon/hooks/useRibbonMepRadiatorBridge.ts` (selected-entity bridge· `getComboboxState`/`onComboboxChange`/`onAction`). **⚠️ Διαφορές από manifold bridge:** (α) ο bridge **ΔΕΝ** προ-υπολογίζει connectors — το `UpdateMepRadiatorParamsCommand` ΗΔΗ τους re-seeds από `width` μέσα στο `applyPatch`· (β) **ΚΑΜΙΑ** pipe-elevation propagation στο mountingElevation change — **συνεπές με το radiator grip path** (`commitMepRadiatorGripDrag` ομοίως δεν propagate-άρει· τυχόν host-follow θα μπει ενιαία σε grip+tab follow-up). **4 wiring edits:** `app/ribbon-contextual-config.ts` (import + `RIBBON_CONTEXTUAL_TABS` + `resolveContextualTrigger` branch `mep-radiator`) · `app/useDxfBimBridges.ts` (instantiate + props type + return) · `app/useDxfViewerRibbon.ts` (destructure + pass) · `ui/ribbon/hooks/useRibbonCommands.ts` (import + prop + 3 branches: onComboboxChange/getComboboxState/onAction + deps). **i18n el+en parity:** `ribbon.tabs.mepRadiatorProperties` + `ribbon.panels.mepRadiator{Geometry,Thermal,Actions}` + `ribbon.commands.mepRadiatorEditor.{width,length,bodyHeight,mountingElevation,connectorDiameter,thermalOutput,close,delete,deleteConfirm}`. **delete/params events** (`bim:mep-radiator-delete-requested`/`-params-updated`) προϋπήρχαν (drawing-event-map + useSmartDelete). **ΕΚΤΟΣ ADR-040** (καθαρά ribbon UI logic, κανένα canvas micro-leaf). git add ΜΟΝΟ δικά μου· **ΜΗΝ adr-index** (shared tree). **ΜΑΘΗΜΑ:** terminal entity tab = manifold tab μείον classification/outlets/network· ο radiator command αυτο-re-seeds connectors → ο bridge απλό dispatch. **PENDING follow-up:** 3D-viewport click-placement· BOQ (BLOCKED — ΗΛΜ code).
- **2026-06-05 (Opus 4.8, Plan Mode incremental — εγκεκριμένο, «FULL ENTERPRISE + FULL SSOT, σαν Revit»)** — **Σύστημα Θέρμανσης Εύρος Β #1 — ΚΑΛΟΡΙΦΕΡ (`mep-radiator`) νέο point-based BIM entity DONE** (tsc production exit 0 — μηδέν δικά μου production λάθη· τα εναπομείναντα tsc λάθη είναι pre-existing test fixtures· geometry 12/12 PASS· 🔴 pending browser verify + commit· SHARED tree με uncommitted Εύρος Α + Φ14). **Στόχος (Giorgio):** μετά την ενεργοποίηση δικτύου θέρμανσης (Εύρος Α), προσθήκη του πρώτου τερματικού στοιχείου θέρμανσης. **Αρχιτεκτονική (Revit-grade):** το καλοριφέρ είναι **τερματικό (όχι πηγή)** — point-based entity με **ακριβώς 2 pipe connectors σταθερής classification**: supply inlet (`rad-supply`, `flow:'in'`, `hydronic-supply`) στο −X άκρο + return outlet (`rad-return`, `flow:'out'`, `hydronic-return`) στο +X άκρο. Έτσι γίνεται **member ΔΥΟ δικτύων ταυτόχρονα** (ένα supply, ένα return) — λειτουργεί χωρίς special handling επειδή η συμμετοχή είναι per-`(entity, connector)` (`MepSystemMember`). **Template = `mep-manifold` (Φ12)** — ~50 αρχεία (νέο entity + registrations). **IFC:** `IfcSpaceHeater` (νέο στο `ifc-entity-mixin`: union + values + zod). **Enterprise ID:** prefix `rad`, collection `floorplan_mep_radiators`. **Discipline:** `plumbing` (συνέπεια με hydronic pipework). **Params** (mirror manifold για SSoT grip/geometry reuse): `width`/`length`/`bodyHeightMm`/`mountingElevationMm`(default 450, wall-mounted)/`connectorDiameterMm`(15)/`thermalOutputW?`. Χρώμα warm-red `#dc2626`. **ΟΛΟΚΛΗΡΩΜΕΝΑ:** types/schemas/entity-union· geometry (`computeMepRadiatorGeometry`/`buildRadiatorConnectors`/`validateMepRadiatorParams`)+tests· 2D renderer+symbol (fin bars + 2 stubs)+**3 silent-drop cases** (dxf-types/dxf-scene-entity-converter/dxf-renderer-entity-model)+object-styles/subcategories/discipline· grips (centred-box SSoT· `MepRadiatorGripKind` σε grip-kinds/grip-types/unified/glyph/computation/registry)· ghost (renderer+hook+leaf mount → CanvasSection/CanvasLayerStack/leaves/types — **STAGE ADR-040** CHECK 6B)· tool (completion+`useMepRadiatorTool`+bridge-store+add-to-scene+useSpecialTools+useCanvasClickHandler+`MepRadiatorToolLike`+tool-definitions+home-tab-draw[icon `bim-mep-radiator`=Thermometer]+3 events+i18n el/en)· factory+enterprise-id(prefix/class/convenience)· hit-test/bounds (**silent-drop #5** HitTestingService geometry-recompute + Bounds/entity-bounds/bim-bounds/selection-duplicate + useSmartDelete + DeleteEntityCommand)· grip-commits (`UpdateMepRadiatorParamsCommand` undoable + re-seeds connectors + commit dispatch + Ctrl-copy)· **connectivity additive** (connector-access getEntityConnectors/isMepConnectorHost· mep-connector-seed `buildRadiatorConnectors`· useMepConnectorReconciliation `reconcileHost`· MepConnectorSnapEngine extractMepConnectorPoints· mep-connector-elevation `pointHostMountingElevationMm`+resolve → πιπες snap+inherit elevation δωρεάν Φ-B1)· 3D (`radiatorToMesh`+`syncRadiators`+Bim3DEntitiesStore `radiators` slice+`elem-mep-radiator` material+useFloors3DAggregator)· **persistence ΠΛΗΡΗΣ** (firestore-service+audit-client+`useMepRadiatorPersistence`+`MepRadiatorPersistenceHost` mounted DxfViewerTopBar+COLLECTIONS+audit-tracked-fields+**firestore.rules** block+**firestore.indexes** ×2). **ΜΑΘΗΜΑ:** νέος IFC type = 3 σημεία στο ifc-entity-mixin· νέο tool στο click pipeline = νέο `*ToolLike` type στο canvas-click-tool-types + field στο UseCanvasClickHandlerParams. **PENDING follow-up:** contextual tab «Ιδιότητες Καλοριφέρ» (grip-resize ήδη καλύπτει διαστάσεις)· 3D-viewport click-placement (το 3D DISPLAY έγινε)· BOQ (BLOCKED — χρειάζεται ΗΛΜ article code· τα ΑΤΟΕ έχουν μόνο ΟΙΚ). **Εύρος Β #2+:** λέβητας (source· ίσως `MepManifoldKind 'heating-boiler'`)· ενδοδαπέδια (area-based). ΕΚΤΟΣ ADR-040 (πλην ghost mount). ⚠️ additive-only στα shared (mep-connector-types).
- **2026-06-05 (Opus 4.8, Plan Mode — εγκεκριμένο, «FULL ENTERPRISE + FULL SSOT σαν Revit»)** — **Φ14 follow-up #3 — ΔΙΑΚΡΙΤΟ 2D ΣΥΜΒΟΛΟ ΦΡΕΑΤΙΟΥ (grating / catch basin) DONE** (7/7 PASS, tsc 0 δικά μου· 🔴 pending browser verify + commit). **Πρόβλημα:** το φρεάτιο (`mep-manifold` kind `'drainage-collector'`) ξεχώριζε από τον συλλέκτη ύδρευσης **μόνο στο χρώμα** (καφέ vs cyan) — το ίδιο το 2D σύμβολο (`buildMepManifoldSymbol`) σχεδίαζε **πανομοιότυπη μπάρα + stubs**. **Στόχος (Giorgio): «όπως οι μεγάλοι παίκτες, Revit-grade, FULL ENTERPRISE + FULL SSOT»** — το φρεάτιο να διαβάζεται με μια ματιά. **Λύση (Revit/CIBSE catch-basin σύμβαση = σχάρα/grating):** η αλλαγή ζει **εξ ολοκλήρου στο SSoT σύμβολο** + τον μοναδικό consumer του (επιβεβαιωμένο grep: 1 renderer + 1 test). **2 αρχεία κώδικα + 1 test:** (1) `bim/mep-manifolds/mep-manifold-symbol.ts` — `ManifoldSymbolGeometry` += optional `gratingStrokes?: readonly ManifoldStroke[]` (ξεχωριστό από τα stubs → λεπτότερη γραμμή· `undefined` για water manifold = back-compat)· NEW pure `buildDrainageGratingStrokes(v0..v3)` = `GRATING_BAR_COUNT`(6) παράλληλες ράβδοι κατά τη μικρή διάσταση (κάτω edge `v0→v1` → πάνω edge `v3→v2`), κατανεμημένες κατά width, `GRATING_INSET`(0.15) από τις short edges· **rotation-aware δωρεάν** (verts ήδη rotated)· reuse `lerp`· `buildMepManifoldSymbol` gate `isDrainageCollectorKind(params.kind)` → προσθέτει grating, τα stubs (1+N στις θέσεις connectors) **μένουν** (δείχνουν N inlets + 1 outlet). (2) `bim/renderers/MepManifoldRenderer.ts` — draw των `gratingStrokes` με `RENDER_LINE_WIDTHS.THIN`(1px) ίδιο καφέ· boy-scout DRY NEW `drawStroke()` helper (το stub loop επαναχρησιμοποιείται). **Καμία αλλαγή footprint/geometry/connectors/grips/hit-test/persistence** → grips/bounds/select ανέπαφα. **Tests:** `mep-manifold-symbol.test.ts` +4 (6 grating bars 2-point· water manifold no grating· keeps outline+stubs· bars inside footprint bbox) → 7/7 PASS. **STAGE ADR-040** (`MepManifoldRenderer` = 2D leaf CHECK 6D· καμία αλλαγή subscription architecture). git add ΜΟΝΟ δικά μου· **ΜΗΝ adr-index**. **ΜΑΘΗΜΑ:** διακριτό σύμβολο = προσθήκη kind-gated strokes στο SSoT symbol generator + thinner render style, μηδέν άγγιγμα του footprint που τρέφει grips/hit-test. **+ GHOST PREVIEW kind-aware fix (browser-diagnosed με temp logging — Giorgio «το φάντασμα είναι γαλάζιο του συλλέκτη, το τοποθετημένο σωστά καφέ»):** το committed entity ήταν σωστό (καφέ+σχάρα 2D+3D), αλλά τα placement ghosts ήταν **hardcoded cyan** (2D `MepManifoldGhostRenderer` + 3D `MepManifoldPlacementGhost` material `0x14b8a6`) + το `mepManifoldToolBridgeStore` δημοσίευε **hardcoded `kind:'floor-manifold'`** (latent bug). Fix (FULL SSOT): **NEW `resolveManifoldPalette(kind)`** στο `mep-manifold-symbol.ts` (`{strokeHex, fillRgb}`· water cyan-teal / drainage brown) = η ΜΙΑ πηγή για renderer + 2 ghosts· `MepManifoldRenderer` boy-scout κεντρικοποίηση (αφαιρέθηκαν 4 hardcoded constants)· `MepManifoldGhostRenderer` += `kind` input → palette + **grating preview** (export `buildDrainageGratingStrokes`, WYSIWYG)· `useMepManifoldGhostPreview` διαβάζει `kind` από bridge imperatively (ADR-040-safe, μηδέν props-threading μέσω CanvasSection)· `MepManifoldPlacementGhost.update` recolour material από palette· bridge `kind` = `state.overrides.kind ?? 'floor-manifold'`. +2 palette tests → **9/9 PASS**. 6 αρχεία. STAGE ADR-040 (ghost leaves CHECK 6D). **+ SQUARE FOOTPRINT fix (Giorgio «βλέπω συλλέκτη, καφέ αλλά καμία σχάρα/φρεάτιο»):** το φρεάτιο κληρονομούσε τις **μπάρα-διαστάσεις** του συλλέκτη (400×80mm) → (α) το σχήμα διαβαζόταν ως συλλέκτης, (β) σε στενή μπάρα η σχάρα (6 κάθετες ~56mm) ήταν μικροσκοπική/αόρατη. Revit catch basin = **τετράγωνο**. NEW `DEFAULT_DRAINAGE_COLLECTOR_SIZE_MM=450`· `buildDefaultMepManifoldParams` gate `kind==='drainage-collector'` → `width=length=450` (override-able)· water manifold αμετάβλητο. Τετράγωνο → η σχάρα (~340mm) διαβάζεται ως φρεάτιο. +3 completion tests → **12/12 PASS**. 7 αρχεία συνολικά. **ΜΑΘΗΜΑ: ένα διακριτό σύμβολο χρειάζεται ΚΑΙ διακριτό σχήμα — η σχάρα πάνω σε ίδια μπάρα-διάσταση = ακόμα «συλλέκτης».**
- **2026-06-05 (Opus 4.8, Plan Mode — εγκεκριμένο, «FULL ENTERPRISE + FULL SSOT σαν Revit»)** — **Φ-B2b EXT #2 — CONNECTOR-HOST INCIDENTS → NO SPURIOUS CAP DONE + ✅ BROWSER-VERIFIED** (Giorgio «ΛΕΙΤΟΥΡΓΕΙ» — σωλήνας σε outlet συλλέκτη = καμία τάπα· tsc 0 δικά μου, 1α 57+208/208 + 1β 134/134 PASS· 🔴 pending commit). **Πρόβλημα:** το `derivePipeJunctions` μάζευε **μόνο `mep-segment` endpoints** → όταν ένα άκρο σωλήνα κουμπώνει σε **outlet συλλέκτη**, ο κόμβος έβλεπε 1 incident → `classifyJunction` → `cap` → **ψεύτικη τάπα** εκεί που ο σωλήνας πιάνει τον συλλέκτη. **Revit-correct («the equipment is the fitting»):** ο host καλύπτει το άκρο — καμία cap. **Λύση (FULL SSOT, pure):** (1) **Γενίκευση `MepFittingIncident`** — canonical `entityId` (segment **ή** host) + `incidentEntityId(inc)` SSoT accessor (fallback σε legacy `segmentId`)· `segmentId` → **optional** (back-compat ανάγνωσης παλιών persisted docs)· NEW transient `host?: boolean`. (2) **NEW `bim/mep-systems/mep-host-connector-endpoints.ts`** `collectHostConnectorEndpoints(entities)` — pure collector: για κάθε point-host (manifold/fixture) κάθε **pipe-domain** connector → world position (`connectorWorldPosition`) + unit-aware `zScene` (`elevationMm·mmToScene`, ίδιο 3D pattern με EXT #1)· reuse NEW exported SSoT `pointHostMountingElevationMm` (boy-scout: το πρώην private `hostMountingElevationMm` στο `mep-connector-elevation.ts`, τώρα κοινό snap+collector). (3) `mep-pipe-junctions.ts` — internal `SegmentEndpoint`→γενικό `JunctionEndpoint` (host-agnostic, precomputed directionUnit/diameter)· host endpoints μπαίνουν στο **ίδιο union-find** → ο σωλήνας ταυτίζεται (xyz) με τον connector → host incident στον κόμβο· `centerlineElevationMm` avg **μόνο segment ends** (lone-host fallback)· sort by `entityId`. (4) `mep-fitting-classify.ts` — **short-circuit:** `incidents.some(host)` → `kind: null` (πριν το count switch, ώστε 1-pipe-at-host να ΜΗΝ θεωρηθεί dead-end cap). (5) `mep-segment-trim.ts` — skip host + `incidentEntityId`. (6) **Color consumers** (folded εδώ για tsc 0 αφού `segmentId` έγινε optional): `MepFittingRenderer.ts` + `sync-mep-elements.ts` → `.filter(!host).map(incidentEntityId)`. **Migration-safe:** schema `entityId`/`segmentId` και τα δύο optional + `.refine` (≥1 present) → παλιά docs (μόνο segmentId) ΚΑΙ νέα (entityId) περνούν Zod· ο reconciler forward-migrate-άρει με μία ενημέρωση. **Self-heal:** ήδη-persisted ψεύτικες caps σε host nodes → εκτός desired set → ο reconciler τις **σβήνει**. **Tests:** classify (+3 host→null cases), junctions (+manifold fixture: 2-incident host node / classify null vs free-end cap / diff-elevation no-merge / no-pipes→[]), NEW host-connector-endpoints (6: world+rotation+metre-scale+domain-filter+determinism). **8 αρχεία (2 NEW).** **ΕΚΤΟΣ ADR-040** (`bim/` pure + `bim-3d/converters/`). git add ΜΟΝΟ δικά μου· ΜΗΝ adr-index. **✅ ΦΑΣΗ 1β DONE (ίδια session):** ο reconciler `buildPipeTopologySignature` (`useMepFittingAutoReconciliation.ts`) hash-άρει πλέον ΚΑΙ τα host connector endpoints — reuse του **ίδιου SSoT** `collectHostConnectorEndpoints` (μηδέν διπλή λογική) → μετακίνηση/υψομέτρωση συλλέκτη re-triggers reconcile (cap εμφανίζεται/σβήνει χωρίς αλλαγή σωλήνα)· hash by world pos+`zScene`+diameter ώστε να πυροδοτεί ΜΟΝΟ όταν ένας connector όντως μετακινείται, όχι σε κάθε host param tweak. +1 αρχείο (σύνολο #2 = **9 αρχεία, 2 NEW**)· tsc 0 δικά μου· 134/134 MEP+reconciliation regression PASS. **#2 ΟΛΟΚΛΗΡΩΘΗΚΕ.** ΕΚΤΟΣ ADR-040 (`hooks/data/`, όχι canvas micro-leaf).
- **2026-06-05 (Opus 4.8, Plan Mode — εγκεκριμένο)** — **Φ-B2b EXT #1 — JUNCTION xyz-MATCHING + z-AWARE junctionKey DONE** (tsc 0 δικά μου, 95/95 PASS· 🔴 pending browser verify + commit). **Πρόβλημα:** το `derivePipeJunctions` (`bim/mep-systems/mep-pipe-junctions.ts`) ταίριαζε endpoints **μόνο σε κάτοψη** (`dist2 = dx²+dy²`, z αγνοείτο) → δύο σωλήνες που **διασταυρώνονται σε κάτοψη αλλά διαφορετικό ύψος** (ο ένας περνά πάνω από τον άλλο, ΧΩΡΙΣ σύνδεση) **συγχωνεύονταν ψευδώς** σε έναν κόμβο → λάθος cross/tee + ψεύτικη μούφα. **Revit-correct:** ίδιος κόμβος μόνο αν συμπίπτουν και στα **τρία** (xyz). **Λύση (FULL SSOT, pure):** (1) NEW `SegmentEndpoint.zScene` = `elevationMm · mmToSceneUnits(sceneUnits)` (ομοιογενής μονάδα με x/y — ίδιο pattern με το Φ-B2b `directionUnit` 3D)· (2) `dist2(Point3D)` → NEW `endpointDist2(a,b)` = `dx²+dy²+dz²` (3D **σφαίρα** αντί 2D δίσκου· tolerance ίδιο `resolvePipeJoinTolerance` 25mm unit-aware)· (3) **ΚΡΙΣΙΜΟ:** το `junctionKey` ήταν κι αυτό planar (`qx:qy`) — ο reconciler κάνει create/update/**delete diff BY junctionKey** → δύο διακριτοί κόμβοι ίδιου xy/διαφορετικού z θα έπαιρναν **ίδιο key** → ο ένας θα έσβηνε τον άλλον. Fix: `junctionKey(position, zScene, tol)` με `qz` cell **conditional** (`qz===0 → "qx:qy"`, αλλιώς `"qx:qy:qz"`) → οριζόντια δίκτυα (z=0) **κρατούν ΑΚΡΙΒΩΣ το παλιό key** → μηδέν reconcile churn / re-creation υπαρχόντων fittings· διασταυρούμενοι σε διαφορετικό z → διακριτά keys → κανένα false delete. **Συνέπειες ασφαλείς:** `position.z` μένει 0 (κανείς consumer δεν το διαβάζει — converter/renderer/trim χρησιμοποιούν `centerlineElevationMm`/`incidents`)· `centerlineElevationMm = avg(elevMm)` τώρα ακριβές αφού τα coincident μοιράζονται z· classify/resolve/trim/schema/types **αμετάβλητα**. **Φ-B2a propagation** ευθυγραμμίζει τα συνδεδεμένα endpoints στο ίδιο z → εντός tol → συγχωνεύονται σωστά· ασύνδετα → εκτός tol → χωρίζουν. **Tests:** NEW `describe('xyz-matching (3D junction)')` (5: no-merge diff-z / merge same-xyz / distinct keys crossing / back-compat z=0 key `'37:0'` / z-cell append)· όλα τα horizontal/riser cases αμετάβλητα. **2 αρχεία:** `mep-pipe-junctions.ts` + `__tests__/mep-pipe-junctions.test.ts`. **ΕΚΤΟΣ ADR-040** (`bim/` pure). git add ΜΟΝΟ δικά μου· ΜΗΝ adr-index. **🟡 ΕΠΟΜΕΝΟ Φ-B2b EXT #2:** connector-host incidents (σωλήνας→outlet συλλέκτη/φωτιστικού → καμία ψεύτικη cap· γενίκευση `MepFittingIncident.segmentId`→`entityId`, migration-safe).
- **2026-06-05 (Opus 4.8, Plan Mode — εγκεκριμένο)** — **Φ-B2b — 3D-AWARE FITTINGS (tilted, Revit-grade) DONE + ✅ BROWSER-VERIFIED** (Giorgio «τώρα είναι σωστή η σύνδεση»· pending commit). **Πρόβλημα (browser, screenshot `012647.jpg`):** μετά το reducing elbow, ο **2ος σωλήνας δεν ταυτιζόταν** με την κωνική μούφα. **Root cause (στον κώδικα):** το `mep-fitting-to-mesh.ts` `planDirToWorld` (γρ.64) **μηδένιζε την κατακόρυφη συνιστώσα** (`new THREE.Vector3(dir.x, 0, -dir.y)`, σχόλιο «pipes run flat») → κάθε μούφα χτιζόταν **επίπεδη** στο `centerlineElevationMm`. Η παραδοχή έσπασε με το **Φ-A (per-endpoint z)**: οι σωλήνες έχουν κλίση (riser/sloped) → σε κεκλιμένο κόμβο η οριζόντια μούφα δεν συναντά το κεκλιμένο κομμένο άκρο. Αφορά **ΟΛΕΣ** τις γωνιακές μούφες σε κεκλιμένους σωλήνες· ο αιχμηρός reducing κώνος το έκανε ολοφάνερο. **ΑΡΧΙΤΕΚΤΟΝΙΚΗ (ΑΚΡΙΒΗΣ, όχι προσέγγιση):** ο σωλήνας ήδη κόβεται κατά τον **3D άξονα** κατά `trimMm` (= το `tangentLen` της μούφας). Άρα αν η μούφα τοποθετεί το tangent της στο **ίδιο** `node + dir3D·tangentLen`, τα πρόσωπα ταυτίζονται απόλυτα → **το trim ΔΕΝ αλλάζει** (μένει 2D `tangentLen`)· μόνο δίνουμε στη μούφα τις 3D κατευθύνσεις. **3 αρχεία:** (1) `mep-pipe-junctions.ts` — `directionUnit` πλέον **πραγματικό 3D** (NEW `SegmentEndpoint.otherElevationMm`· `dz = (otherElev−thisElev)·mmToSceneUnits` → unit 3D με σωστές αναλογίες προς τον world-metre άξονα)· junction matching μένει planar (xy)· schema z **optional** → μηδέν migration. (2) **NEW `bim/geometry/mep-fitting-bend-3d.ts`** `computeBend3DArcPoints(dirA,dirB,tangentLen,segments)` — pure SSoT 3D τόξο στο επίπεδο των δύο σωλήνων (`R=tangentLen·tan(φ/2)`, center στη bisector, **slerp** των radius-vectors)· εφαπτόμενο στα δύο πόδια στο `dir·tangentLen`· collinear→2-point stub. (3) `mep-fitting-to-mesh.ts` — `planDirToWorld((x,y,z))→(x,z,−y)` (κατακόρυφη=world-up· z=0 legacy→ίδιο)· `buildBendTube` παίρνει 3D world dirs→3D arc→TubeGeometry+`applyBendTaper` (reuse, +tubularSegments param)· **inline/legs/cap** προσανατολίζονται κατά τις 3D incident κατευθύνσεις (μεγέθη από body SSoT)· mitered/fallback 3D δωρεάν. **Συνέπειες ασφαλείς:** 2D footprint + trim πετάνε το z (`tangentLen` 2D αμετάβλητο)· classify `dot` 3D (πιο σωστό — vertical elbow). **Tests:** junctions (riser z>0.9 unit / horizontal z≈0) + NEW bend-3d (tangent στα πόδια / planar z≈0 / sloped lift / collinear stub)· **90/90 mep-fitting+junctions+trim PASS**. tsc 0 δικά μου. **ΕΚΤΟΣ ADR-040** (`bim/` pure + `bim-3d/converters/`). git add ΜΟΝΟ δικά μου· ΜΗΝ adr-index. **🟡 Εκτός scope:** junction xyz-matching + junction z όταν τα endpoints διαφέρουν σε elevation (Φ-B2a propagation το χειρίζεται).
- **2026-06-05 (Opus 4.8, Plan Mode — εγκεκριμένο)** — **Φ14 — ΣΥΣΤΗΜΑ ΑΠΟΧΕΤΕΥΣΗΣ (Sanitary Drainage, Revit-grade, FULL SSOT) DONE** (pending commit, 🔴 browser verify). **Στόχος (Giorgio): σύστημα αποχέτευσης «σαν Revit, FULL ENTERPRISE + FULL SSOT».** **Αρχιτεκτονική (Revit: η αποχέτευση ΔΕΝ είναι νέο entity — είναι Pipe με Pipe System Type = Sanitary):** μηδέν fork, μηδέν νέο EntityType, reuse όλης της υποδομής ύδρευσης (Φ8–Φ13). **(A) Αγωγός αποχέτευσης** = υπάρχον `mep-segment` (`domain:'pipe'`) + **2 νέα optional πεδία** `MepSegmentParams.classification?: PlumbingSystemClassification` (instance hint / derived cache — «System always wins» όταν μπει σε δίκτυο, καθρέφτης του `connector.systemId`) + `slopePercent?` (κλίση βαρύτητας, instance property)· schema `.optional()`, completion overrides (pipe-only). NEW dedicated tool `'mep-drain-pipe'` (reuse `useMepSegmentTool`, preset `classification:'sanitary-drainage'` + `DEFAULT_DRAINAGE_SLOPE_PERCENT=1.5`· clear on tool-switch ώστε νερό≠αποχέτευση) + ribbon button «Αγωγός Αποχέτευσης» (DP) + contextual panel «Δίκτυο/Αποχέτευση» (classification picker + slope %, visible iff `domain==='pipe'`). **Χρώμα (1 SSoT 2D+3D):** NEW `resolveSegmentClassificationColor` στο `mep-system-color.ts` — system colour wins → classification hint (drainage=καφέ `#b45309`) → per-domain default· wired 2D `MepSegmentRenderer` + 3D `mep-segment-to-mesh` (`getSystemTintedMaterial3D`). **(B) Φρεάτιο/συλλέκτης αποχέτευσης** = reuse `mep-manifold` με **νέο kind `'drainage-collector'`** (όχι νέο entity → μηδέν 84-file break· και τα δύο `IfcPipeFitting`)· `isDrainageCollectorKind` SSoT· `buildMepManifoldConnectors` branch: ο collector είναι ο **καθρέφτης** του distributor — **N gravity inlets + 1 sewer outlet** (ρόλοι αντεστραμμένοι, ίδια γεωμετρία) via NEW `buildManifoldBranchInletConnector` (`m-in-0…`)· default `systemClassification:'sanitary-drainage'`· 2D+3D καφέ (kind gate). NEW tool `'mep-drainage-collector'` (reuse `useMepManifoldTool`, kind preset) + ribbon button «Φρεάτιο Αποχέτευσης». entity.type μένει `'mep-manifold'` → persistence/bounds/hit-test/3D-sync/selection/contextual-tab/firestore **όλα δουλεύουν αυτόματα** (μόνο 2 activeTool gates ενημερώθηκαν: click-routing + 3D placement). **(C) Δίκτυο αποχέτευσης** = υπάρχον `MepSystem` (`pipe-network` + `'sanitary-drainage'`)· κληρονομείται αυτόματα από `params.systemClassification` του φρεατίου στο `resolvePipeNetworkFromSelection` → καφέ color-by-system δωρεάν (+ classification picker: ήδη υλοποιημένο από το Hydronic Εύρος-Α work, ίδια kebab i18n keys `mepClassification.*` — αφαιρέθηκε διπλό camelCase block). **i18n** `bim.mepDrainPipe.*`/`bim.mepDrainageCollector.*` + `mepSegmentEditor.classification/slopePercent` + `panels.mepSegmentPlumbing` el+en parity. **Firestore:** ΜΗΔΕΝ νέα rules/indexes/collections (reuse `floorplan_mep_segments`/`_systems`/`_manifolds`). **Tests:** colour resolver + drainage preset (classification/slope propagate pipe, ignore duct) + drainage-collector connectors (N inlets+1 outlet, sanitary default) + isDefaultClassificationColor — **PASS** (mep-system-color 18, mep-segment-completion 9, mep-manifold-geometry +3). tsc 0 δικά μου (μόνο γνωστό pre-existing `mesh-to-object3d:124` ADR-411). **STAGE ADR-040** (`MepSegmentRenderer`/`MepManifoldRenderer` = 2D leaves CHECK 6D· δεν άλλαξε αρχιτεκτονική subscription). ⚠️ SHARED tree (Hydronic Εύρος-Α agent co-edits `mep-manifold-types`/`-geometry`/`mep-system-color`/`mep-connector-types` — η classification υποδομή συγχωνεύτηκε καθαρά)· git add ΜΟΝΟ δικά μου· **ΜΗΝ adr-index**. **ΜΑΘΗΜΑ:** νέο **kind** (όχι νέο entity type) για παραλλαγή point-based fitting = 12 αρχεία αντί 84· το flow-direction διαφέρει μόνο στους connector ρόλους, η γεωμετρία είναι κοινή. **🟡 follow-ups:** BOQ MEP σωλήνων/φρεατίων (το `mep-segment` δεν είναι ακόμα στο BOQ union — ευρύτερη φάση)· ξεχωριστό V/G toggle «Αποχέτευση» (νέα `BimCategory:'drain-pipe'`)· ✅ **φρεάτιο 2D σύμβολο διακριτό DONE 2026-06-05** (grating/σχάρα — βλ. επόμενο changelog entry)· contextual labels kind-aware («Είσοδοι» αντί «Έξοδοι»).
- **2026-06-05 (Opus 4.8, Plan Mode — εγκεκριμένο)** — **ΣΥΣΤΗΜΑ ΘΕΡΜΑΝΣΗΣ Εύρος Α — HYDRONIC NETWORK ACTIVATION (Revit-grade, FULL SSOT) DONE** (pending commit, 🔴 browser verify). **Πρόβλημα:** ο MEP κορμός υποστήριζε ήδη θέρμανση στο επίπεδο τύπων (`PlumbingSystemClassification` `hydronic-supply`/`hydronic-return` + `classificationDefaultColor` κόκκινο/μπλε) αλλά **ΚΑΘΕ** δίκτυο + συλλέκτης δημιουργούνταν **hardcoded `domestic-cold-water`** (`buildManifold*Connector` literal + `buildCreateCommand` → `DEFAULT_DERIVED_PIPE_CLASSIFICATION`) → κανείς δεν μπορούσε να φτιάξει δίκτυο θέρμανσης από το UI. **Αρχιτεκτονική (Revit «source equipment owns System Classification, System inherits»):** (1) ο **συλλέκτης κατέχει** την classification — NEW optional `MepManifoldParams.systemClassification` (+schema `.optional()`, back-compat default cold-water)· οι connector builders `buildManifoldInletConnector`/`buildManifoldOutletConnector` πήραν 3η param `classification` (default cold-water → όλοι οι callers άθικτοι)· `buildMepManifoldConnectors` περνά `params.systemClassification ?? 'domestic-cold-water'`. (2) Το **δίκτυο κληρονομεί** — `PipeNetworkFromSelectionDraft.systemClassification` populated από `manifold.params.systemClassification` στο `resolvePipeNetworkFromSelection`· `useRibbonMepPipeNetworkBridge.buildCreateCommand` χρησιμοποιεί `draft.systemClassification` (+ `classificationDefaultColor`) αντί hardcoded. (3) **Generic picker 5 τιμών** — ⚠️ δύο πηγές → δύο UI adapters (όχι ένα widget): **συλλέκτης** = combobox στο manifold tab (NEW key `MEP_MANIFOLD_RIBBON_KEYS.params.classification` + `isMepManifoldClassificationKey`· bridge string-enum branch reuse `dispatchParams` → re-seed connectors δωρεάν· **+ router `useRibbonCommands` routάρει το key στον manifold bridge μέσω του NEW guard** — 🐛 fix runtime crash: χωρίς το guard το `getComboboxState`/`onComboboxChange` δεν έφταναν στον bridge → value `''` → Radix «Empty value forbidden by Select»· μάθημα: string-enum combobox key χρειάζεται ΚΑΙ router guard, όπως `isMepFixtureRibbonStringKey`)· **δίκτυο** = NEW `RibbonMepNetworkClassificationWidget` (clone WireStyle, `isPipeSystemParams`, `UpdateMepSystemParamsCommand` + colour follows classification μόνο όταν `isDefaultClassificationColor` — NEW helper στο `mep-system-color.ts`, custom colour preserved). **i18n** `ribbon.commands.mepClassification.*` (label+5) + `ribbon.panels.mepManifoldSystem` el+en parity + **γενίκευση labels** που έλεγαν «ύδρευσης» (`mepPipeNetwork.create` «Δημιουργία δικτύου ύδρευσης»→«Δημιουργία δικτύου», `createTooltip`/`created` toast, tab «Δίκτυο Ύδρευσης»→«Δίκτυο Σωληνώσεων», manifold tooltip «ύδρευσης»→«ύδρευσης/θέρμανσης») → αφού η ΙΔΙΑ ροή φτιάχνει πλέον και θέρμανση. **Revit-grade UI confirm:** ΕΝΑ κουμπί «Δημιουργία δικτύου» + ΕΝΑ tool «Συλλέκτης» + ΕΝΑ tool «Σωλήνας» (κοινά) = σωστό — ο τύπος προκύπτει από τη source classification, ΟΧΙ από ξεχωριστά κουμπιά ανά domain. **Tests:** inheritance (manifold hydronic→draft hydronic· absent→cold-water)· manifold connector builders με/χωρίς classification· isDefaultClassificationColor· **58/58 affected PASS** (jest). **ΕΚΤΟΣ ADR-040** (καθαρά bim/ui logic). ⚠️ SHARED tree (Φ14 drainage-collector agent co-edits `mep-manifold-types`/`-geometry`/`mep-system-color`/`mep-connector-types` — η classification αλλαγή συγχωνεύτηκε καθαρά· git add ΜΟΝΟ δικά μου· **ΜΗΝ adr-index**). **ΜΑΘΗΜΑ:** όταν το ΙΔΙΟ εννοιολογικό πεδίο επεξεργάζεται από 2 πηγές (entity params vs System store) χρειάζεται 2 UI adapters με κοινά i18n labels, ΟΧΙ ένα κοινό widget. **Εκτός εύρους (Β — επόμενο):** θερμαντικά σώματα/καλοριφέρ, λέβητας, ενδοδαπέδια (νέα BIM entities).
- **2026-06-05 (Opus 4.8, Plan Mode — εγκεκριμένο)** — **REDUCING ELBOW (Revit-grade, FULL SSOT) DONE + ✅ BROWSER-VERIFIED** (Giorgio «τώρα η μούφα είναι κωνική»· pending commit). **Πρόβλημα (Firestore-verified, fitting `mepfit_bb4e8bde`: incidents Ø250+Ø50, dot≈−0.71 angled):** όταν ενώνονταν σωλήνες **διαφορετικής Ø υπό γωνία**, το auto-fitting (Φ11) έβγαζε `elbow` με **ΜΙΑ** διάμετρο (`primaryDiameterMm`=η μεγάλη) → ο μικρός σωλήνας κούμπωνε σε γωνία μεγάλης Θ (στραβό). Ευθύγραμμοι διαφορετικής Ø → ήδη σωστά `reducer` (κώνος). **Έρευνα Revit/IFC:** το default της Revit για διαφορετική Ø υπό γωνία είναι **ΕΝΑ «Reducing Elbow»** (single component, `NewElbowFitting` auto-select· «Elbow+Reducer» = routing-preference fallback)· IFC `IfcPipeFitting` PredefinedType **BEND** = 2 ports αλλαγής κατεύθυνσης με **διαφορετική nominal Ø ανά port**. → **Προσέγγιση Α: ένα κωνικό reducing elbow** = Revit-correct **και** FULL SSOT (διατηρεί 1 fitting ανά `junctionKey`, μηδέν αλλαγή idempotency). **ΑΡΧΙΤΕΚΤΟΝΙΚΗ — μηδέν νέα persisted πεδία:** τα per-end diameters υπάρχουν ήδη (`MepFittingIncident.diameterMm` + `MepFittingParams.secondaryDiameterMm`). Καθαρά γεωμετρική αλλαγή στο ΕΝΑ body SSoT (`mep-fitting-body.ts` → 2D + 3D + trim). Το `kind` μένει `'elbow'`. **4 αρχεία:** (1) `mep-fitting-classify.ts` — angled + `|dA−dB|>ε` → emit `secondaryDiameterMm` (reducing-aware)· (2) `mep-fitting-bend.ts` — `computeElbowBend(...,diameterB=diameter)` (optional 6η param, back-compat) → `R = bendFactor·max(dA,dB)` (η μεγάλη οδηγεί το centreline, Revit long-radius) + NEW `ElbowBend.radiusStart/radiusEnd`· `tessellateBendFootprint` lerp ακτίνα κατά μήκος τόξου (uniform όταν start==end)· (3) `mep-fitting-body.ts` `buildBendBody` → πέρασμα incident[0]/incident[1] diameters· (4) `mep-fitting-to-mesh.ts` `buildBendTube` → TubeGeometry χτισμένο στη max ακτίνα + NEW `applyBendTaper` (scale ανά ring radial offset = `lerp(rStart,rEnd)/tubeRadius`, ring center = `curve.getPointAt(i/segs)`· no-op όταν ίσες). **Μηδέν αλλαγή 2D renderer/types/schemas/persistence** (όλα διαβάζουν το body SSoT). Mitered elbow μένει uniform (converter special-case). **Tests:** classify (reducing vs plain elbow) + bend (radiusStart/End, R=max, footprint taper) + body (bend end radii + footprint narrows tangentA→tangentB)· **80/80 mep-fitting+junctions+trim PASS** (jest). tsc 0 δικά μου. **ΕΚΤΟΣ ADR-040** (`bim/` pure + `bim-3d/converters/` — εκτός CHECK 6B/6D). git add ΜΟΝΟ δικά μου· ΜΗΝ adr-index. **ΜΑΘΗΜΑ:** reducing elbow = ίδιο `kind:'elbow'` με 2 διαμέτρους (όχι νέο entity)· reconciler δεν κάνει HMR → restart dev + hard refresh. **🟡 Επόμενο (deferred):** manifold sizing (Revit-correct = parametric/manual, ξεχωριστή απόφαση).
- **2026-06-05 (Opus 4.8, Plan Mode — εγκεκριμένο)** — **Manifold HOST-FOLLOW (Revit «host moves, connectors follow») DONE + ✅ BROWSER-VERIFIED** (pending commit). Αλλαγή υψομέτρου συλλέκτη (`mep-manifold`) → οι σωλήνες με άκρο κουμπωμένο στα outlets **ακολουθούν** (πριν: ο συλλέκτης ανεβοκατέβαινε αλλά τα συνδεδεμένα άκρα έμεναν πίσω, η ένωση έσπαγε — όπως το Φ-B2a αλλά με πηγή τον συλλέκτη). **Λύση (FULL SSOT, pure):** NEW `resolveManifoldConnectedPipePatches(entities, manifoldId, nextManifoldParams, tolerance?)` στο `bim/mep-segments/mep-elevation-propagation.ts` — υπολογίζει τη νέα world θέση + στάθμη κάθε outlet από τις `nextManifoldParams` (outlet local z + `mountingElevationMm`, reuse `withEndpointZ`/`dist2`), βρίσκει τα coincident pipe endpoints (εντός `resolvePipeJoinTolerance(entities)`, unit-aware) και επιστρέφει `SegmentElevationPatch[]` → ο κόμβος ακολουθεί τον συλλέκτη. **Wiring (`useRibbonMepManifoldBridge.ts`):** το `dispatchParams` packs τον `UpdateMepManifoldParamsCommand` + τα per-segment `UpdateMepSegmentParamsCommand` σε **ΕΝΑ `CompoundCommand`** («Update manifold + connected pipes», single undo) + per-segment `EventBus.emit('bim:mep-segment-params-updated')` ώστε ο `useMepSegmentPersistence` να σώσει ΟΛΟΥΣ· ο συλλέκτης σώζεται scene-diff. **Tests:** +3 manifold cases (`mep-elevation-propagation.test.ts`). tsc 0 δικά μου. ΕΚΤΟΣ ADR-040. git add ΜΟΝΟ δικά μου· ΜΗΝ adr-index. **ΜΑΘΗΜΑ:** ο reconciler/bridge hook **δεν κάνει HMR** → restart dev server + hard refresh για να ζήσει η νέα λογική.
- **2026-06-05 (Opus 4.8, Plan Mode — εγκεκριμένο)** — **Φ11 hotfix — UNIT-AWARE JOIN TOLERANCE + ORPHAN SELF-HEAL DONE + ✅ BROWSER-VERIFIED** («fine keys, χωρίς ψεύτικους σταυρούς»). **Root cause (διαγνώστηκε με Firestore data):** `DEFAULT_PIPE_JOIN_TOLERANCE = 1` ήταν **raw scene-units** → σε σκηνή σε **μέτρα** = ανοχή **1 ΜΕΤΡΟ**. Συνέπειες: (1) κοντοί σωλήνες → τα δύο τους άκρα συγχωνεύονταν → ψεύτικος **cross**· (2) ο `junctionKey` κβαντιζόταν σε κελιά 1m → **collisions** → πολλά fittings στο ίδιο key (π.χ. `"10:1"`) → αδιάγραφτα orphans (`persistedByKey` έχανε τα collided docs). **Fix (FULL SSOT, unit-aware):** NEW `PIPE_JOIN_TOLERANCE_MM = 25` + `resolvePipeJoinTolerance(entities)` (= `25mm × mmToSceneUnits(units)`, units από το 1ο `mep-segment`) στο `bim/mep-systems/mep-pipe-network-derive.ts`· `derivePipeNetworks` default → unit-aware. `mep-pipe-junctions.ts` (`derivePipeJunctions`) + `mep-elevation-propagation.ts` default tolerance → `resolvePipeJoinTolerance(entities)`. **Orphan self-heal (`useMepFittingAutoReconciliation.ts`):** το delete branch έγινε **delete-by-id, collision-proof** — iterate ΟΛΑ τα scene fittings, σβήσε όποιο `junctionKey` ∉ desired **by `entity.id`** (NEW `deleteSceneFitting`· το `persistedByKey` key-lookup άφηνε collided docs πίσω). **Tests:** `mep-pipe-junctions.test.ts` → meter-scene cases (κοντοί σωλήνες ΔΕΝ συγχωνεύονται· angled→elbow όχι cross· distinct keys). tsc 0 δικά μου. **STAGE ADR-408.** ΕΚΤΟΣ ADR-040. git add ΜΟΝΟ δικά μου· ΜΗΝ adr-index. **ΜΑΘΗΜΑ:** ΟΛΑ τα MEP geometry tolerances πρέπει να είναι unit-aware (η σκηνή του Giorgio = ΜΕΤΡΑ)· ο reconciler hook **δεν κάνει HMR** → restart dev + hard refresh + (αν η βάση γέμισε orphans) wipe.
- **2026-06-04 (Opus 4.8, Plan Mode — εγκεκριμένο)** — **Φ-B2a — CONNECTED ELEVATION PROPAGATION (Revit-style network move) DONE** (pending commit, 🔴 browser verify). **Λύνει το screenshot:** συλλέκτης→σωλήνας Α→μούφα→σωλήνας Β (ενωμένα 3D χάρη στη Φ-B1)· ο χρήστης άλλαξε το «Υψόμετρο τέλους» του σωλήνα Α (το άκρο στη μούφα) 400→0 → ο Α έγειρε σωστά (Φ-A) ΑΛΛΑ η μούφα + η αρχή του Β έμειναν στο παλιό ύψος (η ένωση έσπασε). **Root cause:** κάθε σωλήνας = ανεξάρτητη οντότητα· αλλαγή z ενός άκρου ΔΕΝ προπαγανδιζόταν στο coincident άκρο του διπλανού (στο Revit τα συνδεδεμένα κουνιούνται μαζί). **Λύση (FULL SSOT, pure):** **NEW `bim/mep-segments/mep-elevation-propagation.ts` `resolveConnectedElevationPatches(entities, edited, editedNext, tolerance?)`** — όταν αλλάζει το z άκρου σωλήνα, προπαγανδίζεται σε **όλα τα coincident pipe endpoints** στον ίδιο κόμβο (xy match εντός `DEFAULT_PIPE_JOIN_TOLERANCE`) → ο κόμβος ανεβοκατεβαίνει μαζί· επιστρέφει `SegmentElevationPatch[]` (edited πρώτο + κάθε επηρεαζόμενος σωλήνας). **Άγκυρα:** αν ο κόμβος είναι coincident με outlet συλλέκτη/φωτιστικού, το z **κλειδώνει στη στάθμη της άγκυρας** (source wins — άκρο σωλήνα δεν φεύγει από τον host του)· reuse Φ-B1 `resolveMepConnectorElevationMmAt` (host datum=`mountingElevationMm`, ΟΧΙ `position.z`). Junction matching μένει planar — μετά την propagation τα coincident endpoints έχουν ίδιο z → η planar fitting reconciliation βγάζει τη μούφα στο συνεπές z → κολλάει (3D-junction matching = Φ-B2b). Scope: **pipe μόνο** (ducts future, mirror `derivePipeJunctions`). **Wiring (`useRibbonMepSegmentBridge.ts`):** NEW `dispatchElevationEdit` (δίπλα στην `dispatchParams`)· διαβάζει `scene.entities`, καλεί τον resolver, packs ΟΛΑ τα patches σε **ΕΝΑ `CompoundCommand`** («Update connected MEP elevations», single undo)· per-segment `EventBus.emit('bim:mep-segment-params-updated')` ώστε ο `useMepSegmentPersistence` να σώσει ΟΛΟΥΣ. 1 patch → απλό `UpdateMepSegmentParamsCommand` (clean merge). **Boy-Scout (N.0.2):** ο Φ-B1 `mep-connector-elevation.ts` broad `Extract` cast έσπασε από νέο `FloorplanSymbolParams` (ADR-415 agent· έχει `position` όχι `rotation`) → αντικαταστάθηκε με type-guard narrowing (`isMepManifoldEntity||isMepFixtureEntity`, ανθεκτικό σε μελλοντικά param types) → −1 project tsc error. **Tests:** NEW `mep-elevation-propagation.test.ts` (8: propagate/centreline-midpoint/non-coincident/anchor/3-way/duct-skip/idempotent) — **40/40 affected PASS**. tsc 0 δικά μου (12 project errors = WIP άλλων agents roof/family-types/useArray/trim/mesh-to-object3d — ΟΧΙ δικά μου). ΕΚΤΟΣ ADR-040 (καθαρά bim/ui logic). git add ΜΟΝΟ δικά μου· ΜΗΝ adr-index. **❌ ΕΠΟΜΕΝΟ Φ-B2b:** 3D junctions (xyz matching + junction z) + γενίκευση `MepFittingIncident` `segmentId`→`entityId+connectorId` (συλλέκτης/φωτιστικό=incidents → καμία ψεύτικη cap). Μετά Φ-C (risers/fittings 3D), Φ-D (polish).
- **2026-06-04 (Opus 4.8, Plan Mode — εγκεκριμένο)** — **Φ-B1 — CONNECTOR-MATE SNAP (inherit xyz, στρώμα 2/4) DONE** (pending commit, 🔴 browser verify). Όταν άκρο σωλήνα/αεραγωγού **κουμπώνει** σε MEP connector (outlet συλλέκτη / άλλο σωλήνα / φωτιστικό), το άκρο **κληρονομεί την πλήρη 3D θέση** του connector (Revit «Connect To») → ο σωλήνας πιάνει φυσικά το δίκτυο σε 3D, όχι μόνο σε κάτοψη. **ΚΡΙΣΙΜΟ εύρημα:** το πραγματικό 3D z ενός connector διαφέρει ανά host — ο συλλέκτης έχει `position.z=0` αλλά το outlet ζει στο `mountingElevationMm` (400)· ο σωλήνας έχει per-endpoint z (Φ-A). Ο snap κουβαλά μόνο plan (x,y)· το z ανακτάται type-aware. **NEW pure SSoT `resolveMepConnectorElevationMmAt(entity, x, y)`** (`bim/mep-segments/mep-connector-elevation.ts`): segment→matched endpoint own z (`segmentConnectorWorldPosition`)· manifold/fixture→`mountingElevationMm` + connector local z (ΟΧΙ `connectorWorldPosition().z` που διαβάζει το μηδενικό `position.z`)· panel/non-host→null. **Wiring:** `mouse-handler-up` — μετά το snap, αν `snapPoint.type===BIM_MEP_CONNECTOR` resolve z από `scene`+`entityId`, attach `worldPoint.z` (`connectorZmm`)· `useCanvasClickHandler` — για το segment tool, αν το click έχει z (connector snap) χρησιμοποιεί το **ακριβές** snapped worldPoint (το snap **υπερισχύει** του ORTHO/POLAR `bimPoint` που θα μετατόπιζε το x,y εκτός connector)· `useMepSegmentTool` — state κρατά `startElevationMm` (NEW `MepSegmentClickPoint` type με optional z)· `mep-segment-completion` — **Revit cascade**: `startZ = snappedStart ?? snappedEnd ?? centreline`, `endZ = snappedEnd ?? snappedStart ?? centreline` (start@συλλέκτη(400)+free end ⇒ **οριζόντιος** @400 συνέχεια στάθμης· start@συλλέκτη(400)+end@σωλήνα(2800) ⇒ **λοξός riser**)· `centerlineElevationMm` = derived midpoint. **Tests:** NEW `mep-connector-elevation.test.ts` (segment/manifold-mountingElevation/fixture/panel-null) + `mep-segment-completion.test.ts` (cascade: neither/one/both snapped + override) — 32/32 affected PASS. tsc 0 δικά μου. ΕΚΤΟΣ ADR-040 (`mouse-handler-up` = cursor system, ΟΧΙ canvas-drawing leaf — όχι CHECK 6B/6D). git add μόνο δικά μου· ΜΗΝ adr-index. **❌ ΕΠΟΜΕΝΟ Φ-B2:** 3D junctions (dist matching σε xyz + junction z) + γενίκευση `MepFittingIncident` `segmentId`→`entityId+connectorId` (ο συλλέκτης/φωτιστικό γίνονται incidents → **καμία ψεύτικη «τάπα» (cap)** όπου ο σωλήνας πιάνει συλλέκτη). Μετά Φ-C (risers/fittings 3D), Φ-D (polish).
- **2026-06-04 (Opus 4.8, Plan Mode — εγκεκριμένο)** — **Φ-A — PER-ENDPOINT Z FOUNDATION (πραγματική 3D σύνδεση «σαν Revit», στρώμα 1/4) DONE** (pending commit, 🔴 browser verify). Θεμέλιο για συνεχές 3D δίκτυο συλλέκτης→σωλήνας→μούφα→σωλήνας (το screenshot έδειχνε σκόρπια στοιχεία σε διαφορετικά ύψη: συλλέκτης floor-mounted 400mm vs σωλήνας default 2800mm, snap μόνο σε κάτοψη, junctions επίπεδα, σωλήνες αυστηρά οριζόντιοι). **Απόφαση Giorgio: «σαν Revit, FULL ENTERPRISE + FULL SSOT, τέλειο».** **Αρχιτεκτονική:** τα `startPoint.z`/`endPoint.z` (mm) γίνονται η **αυθεντική** στάθμη κάθε άκρου — ο σωλήνας μπορεί να **ανεβαίνει/κλίνει** (riser/sloped, αληθινό 3D τμήμα μεταξύ 2 σημείων). Το `centerlineElevationMm` → **derived/back-compat** (= midpoint). **SSoT resolver `resolveSegmentEndpointElevationsMm(params)`** (+`deriveCenterlineElevationMm`/`isSegmentInclined`): self-healing migration **χωρίς destructive Firestore migration** — «both ends z=0 ⇒ ανέβα στο centreline» (legacy/οριζόντιο default) αλλιώς «κάθε z με fallback centreline»· **idempotent & ασφαλές** (νέο doc με δύο μηδενικά z έχει πάντα centreline=0 ⇒ no-op· ένας riser με start στο δάπεδο z=0/end≠0 ΔΕΝ διαβρώνεται). **Consumers (όλοι μέσω resolver):** `mep-segment-geometry` (bbox z-range = span(start,end)±h/2)· `mep-segment-connectors` (κάθε connector z = το **δικό του** άκρο, όχι κοινό centreline)· `mep-segment-to-mesh` (**η ουσία:** swept solid μεταξύ 2 αληθινών 3D σημείων — cylinder/extrude κατά 3D άξονα + stable horizontal-width basis για near-vertical risers + trim κατά 3D άξονα)· `mep-pipe-junctions` (incident/junction elevation από per-endpoint z· z-matching παραμένει planar — 3D matching = Φ-B)· `mep-segment-completion` (νέο segment `start.z=end.z=centerlineElevationMm` authoritative). **UI:** segment tab — «Ύψος άξονα» ανεβάζει **και τα δύο** άκρα (whole-run lift) + **2 νέα πεδία «Υψόμετρο αρχής»/«Υψόμετρο τέλους»** (per-endpoint riser/slope, Revit-style) με derived centreline sync (`buildElevationParams`)· keys/tab/bridge + i18n `mepSegmentEditor.startElevation/endElevation` el+en. **Tests:** NEW `mep-segment-elevation.test.ts` (resolver migration cases/derive/inclined) + riser cases σε connectors/geometry/junctions/bridge — **81/81 affected PASS** (18 νέα). tsc 0 δικά μου. ΕΚΤΟΣ ADR-040 (`mep-segment-to-mesh` ζει στο `bim-3d/converters/` — εκτός CHECK 6B/6D pattern). ⚠️ SHARED tree (ADR-415 floorplan-symbol agent· git add ΜΟΝΟ δικά μου· **ΜΗΝ adr-index**)· τα contextual tabs της προηγ. συνεδρίας μένουν uncommitted (Giorgio τα κάνει commit). **❌ ΕΠΟΜΕΝΑ:** Φ-B (connector-mate snap inherit xyz + 3D junctions + γενίκευση `MepFittingIncident` `segmentId`→`entityId+connectorId` ώστε ο συλλέκτης/φωτιστικό να γίνονται incidents)· Φ-C (risers/fittings 3D)· Φ-D (polish).
- **2026-06-04 (Opus 4.8, Plan Mode — εγκεκριμένο)** — **CONTEXTUAL PROPERTY TABS «Ιδιότητες Συλλέκτη» (Φ12) + «Ιδιότητες Σωλήνα/Αεραγωγού» (Φ8) DONE** (pending commit, 🔴 browser verify). Οι δύο deferred contextual tabs ώστε ο χρήστης να επεξεργάζεται παραμέτρους **μετά** την τοποθέτηση (πρότυπο 1:1 = «Ιδιότητες Φωτιστικού» ADR-406 v0.7). **FULL ENTERPRISE + FULL SSOT — ΜΗΔΕΝ νέο command** (reuse `UpdateMepManifoldParamsCommand`/`UpdateMepSegmentParamsCommand`). **(A) Συλλέκτης (`mep-manifold`):** panels Geometry (width/length/bodyHeight/mountingElevation) + Έξοδοι (outletCount 1-12/inletDiameter/outletDiameter) + **«Δίκτυο» (fold-in)** + Actions. **Decision Giorgio (AskUserQuestion): fold-in «σαν Revit»** — ο επιλεγμένος συλλέκτης δείχνει ΠΑΝΤΑ «Ιδιότητες Συλλέκτη», με το pipe-network management **folded** ως self-hiding panel (visibilityKey `hasNetwork` → `resolveManagedSystems([manifold]).length>0`), reuse των ίδιων domain-agnostic `mep-circuit-picker/name/color` widgets + `MEP_PIPE_NETWORK_RIBBON_ACTIONS` (καθρέφτης του panel «Κύκλωμα» μέσα στο fixture tab). Ο **manifold→network-tab manage branch** στο `ribbon-contextual-config` **αφαιρέθηκε** (αντικαθίσταται από το fold-in)· ο multi-select **create** branch (manifold+pipes) **μένει**. ⚠️ **Re-seed connectors:** ο `UpdateMepManifoldParamsCommand` δεν ξανακάνει seed connectors → ο bridge ξαναχτίζει `connectors: buildMepManifoldConnectors(nextParams)` (idempotent SSoT) μέσα στο patch ΠΡΙΝ το dispatch, ώστε outlets↔connectors να μένουν σε συγχρονισμό (outletCount clamp [1,12]). **(B) Σωλήνας/Αεραγωγός (`mep-segment`, ΕΝΑ tab duct+pipe):** panels Διατομή (sectionKind — self-hide για pipe, πάντα round) + Διαστάσεις (width+height rect / diameter round, gated) + Γεωμετρία (centerlineElevation) + Actions. `domain` **ΟΧΙ editable** (αλλάζει discipline/IFC/BOQ) — απλώς gate-άρει το section choice. Ο `UpdateMepSegmentParamsCommand` δεν emit-άρει → ο bridge κάνει emit `bim:mep-segment-params-updated` (το `useMepSegmentPersistence` auto-save ακούει το event)· ο manifold auto-save είναι scene-diff (το command emit-άρει ήδη). **6 NEW:** `mep-manifold-command-keys.ts`/`contextual-mep-manifold-tab.ts`/`useRibbonMepManifoldBridge.ts` + `mep-segment-command-keys.ts`/`contextual-mep-segment-tab.ts`/`useRibbonMepSegmentBridge.ts` + 2 test suites (21/21 PASS). **MOD wiring:** `ribbon-contextual-config` (register 2 tabs + 2 `resolveContextualTrigger` cases + αφαίρεση manifold manage branch)· `useRibbonCommands`/`useDxfBimBridges`/`useDxfViewerRibbon` (compose 2 bridges: combobox/state/action/visibility routing)· i18n `mepManifoldEditor.*`/`mepSegmentEditor.*` + tabs/panels el+en parity. ΕΚΤΟΣ ADR-040 (καθαρά ui/bim logic, κανένα canvas-drawing αρχείο). tsc 0 δικά μου. ⚠️ SHARED tree (ADR-415 floorplan-symbol agent· git add ΜΟΝΟ δικά μου· **ΜΗΝ adr-index**). 🟡 follow-ups: manifold grips add/remove outlet· duct (air) systems· Φ14 system browser/sizing.
- **2026-06-04 (Opus 4.8, Plan Mode — εγκεκριμένο)** — **Φ13 — ΔΙΚΤΥΟ ΥΔΡΕΥΣΗΣ ΑΠΟ ΣΥΛΛΕΚΤΗ (Pipe network from manifold) DONE + ✅ BROWSER-VERIFIED** (pending commit· Giorgio: «Δημιουργήθηκε δίκτυο ύδρευσης — σωλήνες: 2. OLA OK»). Η **explicit, «σαν Revit»** διαδρομή: επιλέγεις **συλλέκτη (`mep-manifold`) + σωλήνες (`mep-segment` domain `pipe`)** → δημιουργείται plumbing `MepSystem` (`systemType:'pipe-network'`) με **source = ο συλλέκτης** (outlet connector, flow `out`, fallback `m-out-0`) και **members = οι σωλήνες** (καθρέφτης του ηλεκτρικού circuit-from-selection Φ5.A + management panel Φ6). Συμπληρώνει το `derivePipeNetworks` (Φ10, implicit topology): εδώ η πηγή είναι ρητά ο συλλέκτης, όχι το lex-smallest segment. **FULL ENTERPRISE + FULL SSOT — ΜΗΔΕΝ fork** (απόφαση Giorgio). **Reuse ως έχει:** `CreateMepSystemCommand`/`UpdateMepSystemParamsCommand`/`mep-system-coordinator` (computeReassignRemovals/findSystemsBySource/findSystemMembershipsByEntity/memberKey)/`mep-system-color` (`classificationDefaultColor`/`buildDefaultPipeNetworkParams`)/`useMepConnectorReconciliation`/`mep-circuit-editor-store` (`activeSystemId` κοινό)/`useMepCircuitEditorSync` (κοινό) **και τα widgets picker/name/color** (ήδη system-agnostic — διαβάζουν `activeSystemId`+`UpdateMepSystemParamsCommand`, ΟΧΙ electrical params). **Γενίκευση (1 pure fn, χωρίς fork):** `resolveManagedCircuits` → **`resolveManagedSystems`** (entity-type-agnostic 2-pass: όλα member-of, μετά όλα source-of — **διατηρεί ΑΚΡΙΒΩΣ την electrical σειρά**· καλύπτει electrical [panel/fixture] + plumbing [manifold/pipe] με ΕΝΑ SSoT· members resolve BY id όχι kind). Picker label = systemType-aware (`networkPicker`=«Δίκτυο» για pipe, `circuitPicker`=«Κύκλωμα» για electrical). **5 NEW:** `mep-pipe-network-from-selection.ts` (`resolvePipeNetworkFromSelection` source=manifold+members=pipes· **κάθε σωλήνας=2 endpoint members** seg-start+seg-end, καθρέφτης derive· `buildAddPipeMembersUpdate`· `pipeSegmentMembers`)· `contextual-mep-pipe-network-tab.ts` (reuse widgetIds `mep-circuit-picker/name/color`, ΟΧΙ wireStyle/conductors)· `mep-pipe-network-command-keys.ts`· `useRibbonMepPipeNetworkBridge.ts` (create→`buildDefaultPipeNetworkParams`+`classificationDefaultColor`+reassigns· add/remove· close· toasts αναφέρουν **σωλήνες** distinct-host όχι endpoints)· `__tests__/mep-pipe-network-from-selection.test.ts`. **MOD wiring:** `ribbon-contextual-config` (register tab + 2 triggers: create=manifold+pipe· manage=manifold sources network)· `useRibbonCommands`+`useDxfBimBridges`+`useDxfViewerRibbon` (compose bridge)· `drawing-event-map`+`useDxfViewerNotifications` (5 `bim:mep-network-*` events + pipe toasts)· i18n `mepPipeNetwork.*` el+en (notifications + tab/panels/commands + `mepCircuit.networkPicker`). **Source ΔΕΝ tint-άρεται** (members-only color index — ο συλλέκτης δεν μπαίνει στα members). **Επαλήθευση:** tsc 0 δικά μου· **62/62 MEP pure tests PASS** (νέο pipe-network suite + γενικευμένο editor/coordinator/from-selection regression). ΕΚΤΟΣ ADR-040 (καθαρά bim/ui logic, κανένα canvas-drawing αρχείο). ⚠️ SHARED tree (ADR-415 floorplan-symbol agent· git add ΜΟΝΟ δικά μου· **ΜΗΝ adr-index**). **ΕΚΤΟΣ (επόμενα):** contextual «Ιδιότητες Συλλέκτη»/«Ιδιότητες Σωλήνα» tabs (deferred Φ12/Φ8)· duct (air) systems· Φ14 system browser/sizing.
- **2026-06-04 (Opus 4.8, Plan Mode — εγκεκριμένο + 8 subagents)** — **Φ12 — ΣΥΛΛΕΚΤΗΣ ΥΔΡΕΥΣΗΣ (Plumbing Manifold) DONE** (pending commit, 🔴 browser verify + firebase deploy rules+indexes). Νέο point-based BIM entity «σαν Revit Plumbing Equipment» = η **πηγή διανομής ύδρευσης**, καθρέφτης 1:1 του Ηλεκτρικού Πίνακα (Φ3). EntityType `'mep-manifold'`, kind `'floor-manifold'`, shape rectangular (μπάρα), BimCategory `'mep-manifold'`→**plumbing**, IfcType **`IfcPipeFitting`** (reuse — multi-branch JUNCTION fitting, −1 registration vs νέο type), prefix `'mfld'`, collection `FLOORPLAN_MEP_MANIFOLDS`. **Connectors: 1 inlet (`flow:'in'`, domain pipe, `domestic-cold-water`) στη −X άκρη + N outlets (`flow:'out'`) κατανεμημένοι κατά μήκος της +Y front edge** (default `outletCount=4`, MAX 12)· παράγονται από `buildMepManifoldConnectors(params)` (SSoT scene-unit offsets) — καλείται ΚΑΙ στο completion ΚΑΙ στο `seedDefaultConnectors`. Pipes κουμπώνουν μέσω `MepConnectorSnapEngine` (προστέθηκε `isMepManifoldEntity` στον point-host κλάδο — connectorWorldPosition χειρίζεται ΟΛΑ τα outlets αυτόματα). **floor-mounted**: 3D box κεντράρεται κατακόρυφα στο `mountingElevationMm` default 400· `manifoldToMesh` = **units-safe stair `sceneUnitsToMeters` pattern** (ΟΧΙ buggy fixtureToMesh — mesh test pins 0.4m σε mm==m scene). Grips = reuse `centred-box-grips.ts` SSoT (thin adapter, μηδέν fork — move 3-click/rotate 6-click/4 corners· εγγραφή στις ΙΔΙΕΣ entity-agnostic πύλες με `mepManifoldGripKind`). Manifold = source → **ΔΕΝ tint-άρεται** από color-by-system (members-only index, precedent πίνακα) → μόνο 2D renderer + 3D material `elem-mep-manifold` (cyan 0x0891b2). **~22 NEW files** (`bim/mep-manifolds/*`, `bim/types/mep-manifold-types`+`.schemas`, completion/tool/command/bridge/persistence/host/ghost-hook/ghost-leaf, factory, 3D placement-ghost+hook, 4 test files) **+ ~40 registrations σε 15 κατηγορίες** (entity union+guard+isBimEntity, base-entity/bim-base, BimCategory ×4 + subcategories + discipline, enterprise-id ×4, audit ×3 + MEP_MANIFOLD_TRACKED_FIELDS, eventbus ×4, tool ×5, dxf-types+converters ×3, **bounds/hit-test ×5 — silent-drop μάθημα**, delete/restore/smart-delete, aggregator+Bim3DEntitiesStore(`manifolds`/`setManifolds`)+bim-move-geometry, BimSceneLayer.syncManifolds+MaterialCatalog+bim3d-edit-math, grips ~15 gates, connector-access/seed/snap ×3, ribbon `Συλλέκτης`/Split icon/i18n el+en, firestore rules+indexes ×2+collections+coverage-manifest, EntityRendererComposite, CanvasSection/CanvasLayerStack/leaves ghost threading, DxfViewerTopBar host, BimViewport3D). **Επαλήθευση:** tsc 0 δικά μου (μόνο γνωστό `mesh-to-object3d:124` non-mine)· 39 νέα tests PASS (geometry/connectors/symbol/grips/mesh-units) + 178 MEP regression + 487 grip/ghost/bounds PASS. **Boy-Scout:** `bim-discipline.test.ts` ήταν stale (mechanical/plumbing `toEqual([])` ενώ duct/pipe/sanitary ήδη mapped) → διορθώθηκε στις πραγματικές τιμές + `mep-manifold`. **STAGE ADR-040** (BimSceneLayer CHECK 6B + ghost leaves 6D). ⚠️ SHARED tree (ADR-415 floorplan-symbol agent· git add ΜΟΝΟ δικά μου· ΜΗΝ adr-index). **ΕΚΤΟΣ (επόμενο Φ):** «δίκτυο ύδρευσης από συλλέκτη» UI (mirror `mep-circuit-from-selection`)· grips add/remove outlet· hosted wall-attach· catalog/sizing.
- **2026-06-04 (Opus 4.8, orchestrator — 8 agents)** — **Φ11 — AUTO-FITTINGS «ΣΑΝ REVIT» (persisted) DONE** (pending commit, 🔴 browser verify). Όταν ενώνονται σωλήνες (`mep-segment` domain `pipe`), μπαίνει αυτόματα **persisted εξάρτημα** (`mep-fitting` entity) στον κόμβο — κλικάρεται/μετριέται/BOQ, όπως Revit Routing Preferences. **Απόφαση Giorgio (AskUserQuestion):** lifecycle = πλήρως αυτόματα & ζωντανά· scope = **και τα 6** (elbow/coupling/reducer/tee/cross/cap)· elbow = παραμετρικό (`elbowStyle: 'radiused'|'mitered'`, default radiused). **3 στρώματα:** (A) `mep-pipe-junctions.ts` `derivePipeJunctions` (επέκταση union-find του `mep-pipe-network-derive`: junction nodes = {key, position, incidents[]{segmentId, connectorId, directionUnit, diameterMm}})· (B) `mep-fitting-classify.ts` τοπολογία→kind (1=cap· 2 collinear same-Ø=coupling/diff-Ø=reducer· 2 angled=elbow· 3=tee· 4=cross· ≥5=null) + `mep-fitting-geometry.ts` + `mep-fitting-resolve.ts` (pure, deterministic, idempotent· `MepFittingDraft` χωρίς id)· (Γ) `useMepFittingAutoReconciliation.ts` host: subscribe `floorplan_mep_fittings`→scene + debounced reconcile (topology→desired set→**diff BY `junctionKey`**: create/update/delete). **Idempotency = `junctionKey`** (quantized node position)· echo-loop defences mirror `useMepSegmentPersistence` (pending/deleted guards + 500ms debounce + hydration gate + no-op `desiredSignature` short-circuit — **μάθημα snap-fix**). Undo: auto-fittings = derived state, ΟΧΙ στο user undo stack (self-heal on next topology change). **~35 αρχεία** (πρότυπο `mep-segment`): types/schemas/junctions/classify/geometry/resolve/factory + 2D `MepFittingRenderer` (EntityRendererComposite) + 3D units-safe `mep-fitting-to-mesh` (stair `sceneUnitsToMeters`, radiused=TubeGeometry/arc) + firestore-service/audit/add-to-scene + reconciliation host + `MepFittingPersistenceHost` + registrations (entities union+`isMepFittingEntity`+`isPersistedBimEntity`, enterprise-id `mepfit`, `FLOORPLAN_MEP_FITTINGS`, rules, indexes, audit-tracked-fields, audit-trail union, backup, i18n `mepFitting.*` el+en, `elem-mep-fitting` material, **3 σημεία bounds** Bounds/HitTestingService/selection-duplicate-utils — μάθημα furniture, `BimSceneLayer.syncFittings`). 32/32 pure tests PASS (junctions/classify/resolve-idempotency), tsc 0 δικά μου (4 project errors = άλλου agent floorplan-symbol/sanitary + 1 γνωστό mesh-to-object3d:124). **STAGE ADR-040** (BimSceneLayer CHECK 6B). ⚠️ SHARED tree (enterprise-id/collections/rules co-edited με floorplan-symbol agent· git add ΜΟΝΟ δικά μου). 🟡 Known follow-up (Φ12): transient orphan fitting σε πολύ γρήγορη μετακίνηση σωλήνα μέσα στο async-write window (self-heal επόμενη αλλαγή)· manual grips/delete fitting· duct fittings· catalog/sizing.
- **2026-06-04 (Opus 4.8)** — **🐛 FIX Φ9 — MEP connector snap «δεν κουμπώνει» → ✅ browser-verified.** Ο snap engine έπαιρνε scene **χωρίς** τα mep-segments (`initialize segments=0`) και δεν ξανα-αρχικοποιούνταν ποτέ μετά το σχεδίασμα. **Root cause ήταν στο ADR-040 SSoT `useGlobalSnapSceneSync`** (όχι στον MEP κώδικα): το deferred `requestIdleCallback` re-init ακυρωνόταν από το per-run effect cleanup σε κάθε benign no-op re-render (Firestore `subscribeSegments` echo). Fix: cleanup μόνο σε unmount· superseding μέσω cancel-before-schedule. **Γενικό fix — αφορά όλα τα BIM snap.** Λεπτομέρειες: ADR-040 changelog 2026-06-04. Τα Φ9 leaves (connector resolver/engine/registry/types/tolerances) ήταν σωστά εξ αρχής. Snap τώρα κουμπώνει (◇), endpoints ενώνονται, «Δίκτυα σωλήνων» → δίκτυα:N. 8/8 MepConnectorSnap PASS, tsc 0. ❌ ΕΠΟΜΕΝΟ (απόφαση Giorgio): Φ11 auto-fittings **σαν Revit** (persisted elements — elbow/coupling/tee/reducer) ώστε οι ενώσεις να μη φαίνονται «άσχημες».
- **2026-06-04 (Opus 4.8)** — **Φ9/Φ10 ΣΤΡΩΜΑ Β — Plumbing network leaves (connectors + snap + auto-system + colour + UI)** (pending commit, 🔴 browser verify).
  Ολοκλήρωση του φυσικού δικτύου πάνω στο θεμέλιο τύπων του Στρώματος Α. **5 leaves, full SSOT, 196/196 MEP PASS, tsc 0 νέα.** ΕΚΤΟΣ ADR-040
  ΕΚΤΟΣ τα 3D-scene αρχεία (BimSceneLayer = CHECK 6B → STAGE ADR-040· SnapIndicatorOverlay = CHECK 6D → STAGE ADR-040).
  - **Leaf 1 — Segment endpoint connectors:** NEW `bim/mep-segments/mep-segment-connectors.ts` `segmentConnectorWorldPosition(connectorId, params)`
    (seg-start→`startPoint`, seg-end→`endPoint`, z=`centerlineElevationMm`· ΟΧΙ point-host `connectorWorldPosition` — ο σωλήνας ΔΕΝ έχει position+rotation).
    `connector-access.ts` (getEntityConnectors/isMepConnectorHost) + `mep-connector-seed.ts` (2 connectors start+end, domain mirror duct/pipe) +
    `useMepConnectorReconciliation.ts` (widen generic + segment arm) — όλα +`isMepSegmentEntity` arm.
  - **Leaf 2 — Snap-to-connect:** NEW `snapping/engines/MepConnectorSnapEngine.ts` (πρότυπο WallCornerSnapEngine· segment→2 endpoints direct,
    fixture/panel→`connectorWorldPosition`). `ExtendedSnapType.BIM_MEP_CONNECTOR` (priority **-1.5**: πάνω από endpoint/column-centre, κάτω από face corners) +
    enabledTypes/priority/tolerance + register SnapEngineRegistry + ◇ SnapIndicatorOverlay symbol + `'mep_connector'` στο querySnap union (ISpatialIndex/QuadTree/Placeholder) +
    ProSnapToolbar Record/BIM_MODES + i18n `snapModes.labels.bim.mepConnector`. Το snap εφαρμόζεται κεντρικά (mouse-handler-up) → start/end click του segment tool κουμπώνουν δωρεάν.
  - **Leaf 3 — Φ10 auto-system (pure):** NEW `bim/mep-systems/mep-pipe-network-derive.ts` `derivePipeNetworks(entities, tol, defaultClass)` — union-find connected
    components στα **pipe** endpoints (≤1 scene-unit tolerance), 1 network/component, deterministic source = lexικά-μικρότερο segment seg-start, members = όλοι οι
    endpoint connectors. Default classification = `domestic-cold-water` (re-classify στο UI). Πρότυπο `mep-circuit-from-selection.ts` αλλά topology αντί selection.
  - **Leaf 4 — Colour-by-classification:** NEW `classificationDefaultColor(PlumbingSystemClassification)` (cold=#2563eb, hot=#dc2626, drainage=#b45309,
    hydronic-supply=#dc2626, hydronic-return=#2563eb — CIBSE/Revit convention). 2D gate στον `MepSegmentRenderer` (resolveEntitySystemColor + colorBySystem toggle,
    segment.id = colour-index key). 3D: `getSystemTintedMaterial3D` += duct/pipe, `mepSegmentToMesh` += `systemColor` param, `BimSceneLayer.syncMepSegments` passthrough.
  - **Leaf 5 — UI «Δίκτυα σωλήνων»:** action key `mepCircuit.actions.deriveNetworks` (folded στο υπάρχον `useRibbonMepCircuitBridge` → μηδέν νέο prop-threading στο
    useRibbonCommands) + `handleDeriveNetworks` (derivePipeNetworks → 1 CompoundCommand CreateMepSystemCommand/draft, single undo) + home-tab-draw button +
    EventBus `bim:mep-networks-derived` + toast + i18n el/en (button label/tooltip, networkDefaultName, networksDerived). Reuse buildDefaultPipeNetworkParams + classificationDefaultColor.
  - **Roadmap:** Φ11 auto-fittings (elbow/tee/reducer)· Φ12 inline valves· Φ13 manifold/water-heater (multi-connector equipment)· Φ14 system browser/sizing· duct (air) systems.
- **2026-06-03 (Opus 4.8)** — **Φ9 ΣΤΡΩΜΑ Α — Θεμέλιο τύπων plumbing (discriminated union)** (pending commit, 🔴 browser verify· Στρώμα Β leaves εκκρεμούν).
  Πρώτο βήμα του υδραυλικού δικτύου (ύδρευση/αποχέτευση/θέρμανση — επιλογή Giorgio). Recognition: 4 παράλληλοι χαρτογράφοι →
  ο πυρήνας (commands/coordinator/mutator/color) είναι ΗΔΗ domain-agnostic· μόνο οι **τύποι** ήταν hard-typed στο ηλεκτρολογικό.
  **Αλλαγή:** `MepSystemParams` έγινε **discriminated union** σε `systemType` (`MepElectricalSystemParams | MepPipeSystemParams`):
  τα electrical-only πεδία (`wireStyle`/`wireWaypoints`/`conductors`/`ratedVoltage`/`poles`) ζουν ΜΟΝΟ στο electrical arm →
  ο compiler αναγκάζει guards ώστε pipe-network να μην μπει ποτέ στο `computeCircuitWirePaths`. ΝΕΑ: `PlumbingSystemClassification`
  (cold/hot-water, sanitary-drainage, hydronic-supply/return), `PipeFluid`, `MepPipeConnectorParams` (diameter/fluid/**slopePercent** για
  αποχέτευση/flowLps), `MepConnector.pipe?`, `MepSystemType += 'pipe-network'`, `buildDefaultPipeNetworkParams`, type-guards
  `isElectricalSystemParams`/`isPipeSystemParams`, segment endpoint connector builders (`buildSegmentEndpointConnector` start/end,
  `flow:'bidirectional'`, ΧΩΡΙΣ classification στο seed — η κλάση ανήκει στο System). Zod: `MepSystemParamsSchema` = `z.discriminatedUnion`,
  `MepPipeConnectorParamsSchema`. Narrowing guards σε 6 consumers (mep-wire-routing filter· waypoint gesture/2D+3D interaction → narrowed
  `params`· HomeRunWiresOverlay· 2 ribbon widgets). tsc 0 νέα, **151/151 MEP PASS** (ηλεκτρολογικό ανέπαφο). ΕΚΤΟΣ ADR-040.
  **Στρώμα Β (επόμενο, παραλληλοποιήσιμο):** segment endpoint connectors wiring (seed/connector-access/reconciliation + `segmentConnectorWorldPosition`)·
  `MepConnectorSnapEngine` (snap-to-connect)· `mep-pipe-network-derive.ts` (connected-component auto-system, Φ10)· `classificationDefaultColor` +
  color-by-system στον MepSegmentRenderer (2D+3D)· UI «Δίκτυο σωλήνων».
- **2026-06-03 (Opus 4.8)** — **Φ8 FIX — pipe midpoint tick units bug** (pending commit, 🔴 browser verify).
  Σύμπτωμα Giorgio: σχεδιάζοντας **σωλήνα**, τεράστια κάθετη γραμμή στο μέσον. Root: το pipe symbol tick στο
  `mep-segment-symbol.ts` υπολόγιζε `tickHalf` σε **world units** με absolute clamp `[4, 20]` → σε σχέδιο **μέτρων**
  το `MIN_TICK_HALF=4` σήμαινε **4 μέτρα** (ίδια κλάση με τα meter-scale bugs). **Fix (screen-constant, «σαν Revit»):**
  το tick βγήκε από το pure SSoT (`buildSegmentSymbol` πλέον επιστρέφει ΜΟΝΟ centerline, domain-agnostic) και
  ζωγραφίζεται render-time στον `MepSegmentRenderer` με **σταθερό μήκος pixels** (`PIPE_TICK_HALF_PX=7`) μέσω ΝΕΟΥ pure
  `buildPipeTickScreen(startScreen, endScreen)` — ίδιο μοτίβο με `mep-wire-conductor-ticks.ts` (zoom- & scene-independent).
  NEW `mep-segment-symbol.test.ts` (unit-parity mm vs m + screen-constant + perpendicular· 6 tests). 21/21 mep-segment
  PASS, tsc 0 (δικά μου). MepSegmentRenderer ζει στο `bim/renderers/` → ΕΚΤΟΣ CHECK 6D → ΟΧΙ ADR-040 staging.
- **2026-06-03 (Opus 4.8, orchestrator)** — **Φ8 — DUCT/PIPE ELEMENT PIPELINE DONE** (pending commit, 🔴 browser verify).
  Πρώτο **μηχανολογικό/υδραυλικό** BIM στοιχείο — ο counterpart του point-based electrical fixture/panel. **Απόφαση
  Giorgio (AskUserQuestion):** ΕΝΑ **ενοποιημένο** linear MEP entity (`type: 'mep-segment'`) με δύο ορθογώνιους
  discriminators — `domain` (`'duct'` μηχανολογικά / `'pipe'` υδραυλικά → discipline + BimCategory + IFC class + BOQ) και
  `sectionKind` (`'rectangular'` duct / `'round'` round-duct/pipe → swept διατομή) — ακριβώς όπως το δοκάρι ενοποίησε
  rectangular+I-shape κάτω από ΕΝΑ `sectionKind` (data, όχι 2ο entity type). Κόβει στη μέση τα ~34 vs 68 registration
  points, καθαρό SSoT, ικανοποιεί «όλο μαζί» (ΕΝΑ pipeline καλύπτει ΚΑΙ duct ΚΑΙ pipe). **Template = ΔΟΚΑΡΙ** (όχι
  fixture): γραμμικό 2-click με διατομή σαρωμένη κατά άξονα· units-safe `MM_TO_M` (basis-matrix sweep όπως
  `buildSweptIBeamGeometry`, ΟΧΙ buggy `fixtureToMesh`). Elevation = **centreline** (Revit «Middle Elevation», η διατομή
  κεντράρεται κάθετα — όχι top-face). **Υλοποίηση (ΜΙΑ orchestrator συνεδρία, 4 parallel subagents + main):**
  Foundation NEW `mep-segment-types`/`.schemas`/`mep-segment-geometry`/`shared/round-profile`/`mep-segment-completion`/
  `mep-segment.factory`. 3D NEW `mep-segment-to-mesh` (rect+round sweep) + `MaterialCatalog3D` elem-mep-duct/pipe +
  `BimSceneLayer.syncMepSegments` + `Bim3DEntitiesStore.mepSegments`. 2D NEW `MepSegmentRenderer` + `mep-segment-symbol`
  + `EntityRendererComposite`. Ghost+grips NEW `mep-segment-grips`/`MepSegmentGhostRenderer`/ghost-preview/mount + grip
  registrations. Data NEW `mep-segment-firestore-service` (setDoc + `generateMepSegmentId`, N.6) + `useMepSegmentPersistence`
  + `MepSegmentPersistenceHost` + `mep-segment-audit-client` + `add-mep-segment-to-scene` + `UpdateMepSegmentParamsCommand`
  + `useMepSegmentTool` (2-click FSM). **~34 global registrations** (EntityType/BimElementType/entities union+guard·
  BimCategory `duct`/`pipe`+styles+subcat+discipline mechanical/plumbing· audit type+tracked-fields+route maps· IFC
  `IfcDuctSegment`/`IfcPipeSegment`· EventBus· ToolType `mep-duct`/`mep-pipe`+tool-defs· dxf-types+2 converters· hit-test ×4·
  delete/restore/smart-delete· aggregator· backup×3· move/selection/edit-math· enterprise-id `mepseg`+collection·
  firestore.rules+indexes×2). Ribbon: 2 home-tab buttons (Αεραγωγός/Σωλήνας)+icons+i18n el/en. **Connectivity:** το
  segment κρατά το `MepConnectorHostParams` mixin ως **forward hook** (empty) — duct/pipe **systems** = επόμενο Φ.
  Tests: NEW `mep-segment-geometry.test` 15/15 PASS (geometry + round-profile + unit-parity + validation), tsc 0 (δικά
  μου αρχεία). **STAGE ADR-040** (CHECK 6B `BimSceneLayer`/`canvas-layer-stack-leaves` + 6D ghost renderers). ⚠️ SHARED
  TREE με ADR-412 (enterprise-id/collections/rules co-edited)· `git add` ΜΟΝΟ specific, ΠΟΤΕ -A. **❌ DEFERRED:**
  contextual props tab· 3D 2-click placement· duct/pipe systems+routing.
- **2026-06-03 (Opus 4.8)** — **Φ5 roadmap — SEED LEGACY CONNECTORS DONE** (pending commit, 🔴 browser verify).
  Κλείνει το τελευταίο Φ5 caveat: φωτιστικά/πίνακες που φτιάχτηκαν **πριν** το connector model (Φ1/Φ2) δεν είχαν
  `params.connectors`, οπότε δεν συμμετείχαν πλήρως σε reconciliation / wire-routing / `connector.systemId` cache.
  **Απόφαση Giorgio:** «όπως οι μεγάλοι παίκτες (Revit), FULL ENTERPRISE + FULL SSOT» → **scene-only seed**: σε Revit
  οι connectors είναι μέρος του **family definition** — υλοποιούνται ντετερμινιστικά από τον τύπο κατά το load, **δεν**
  αποθηκεύονται ως per-instance mutable data. Persisted backfill θα δημιουργούσε **δεύτερο αντίγραφο** στο Firestore
  που μπορεί να αποκλίνει (drift) → παραβίαση SSoT. **Υλοποίηση:** NEW pure SSoT `bim/mep-systems/mep-connector-seed.ts`
  → `seedDefaultConnectors(entity)` re-materialises τον default connector του host type από τους builders
  (`buildDefaultLightingConnector` / `buildDefaultPanelOutgoingConnector` — η ΜΟΝΑΔΙΚΗ πηγή του default connector
  shape)· same ref όταν ο host έχει ήδη connectors ή δεν είναι connector host (idempotent, pure). **Folded σε ΕΝΑ
  scene pass** μέσα στο `useMepConnectorReconciliation` (seed-then-reconcile → ένα `setLevelScene` diff): κάθε host
  πρώτα seed-άρεται, μετά reconcile-άρεται το `systemId` cache του. Scene-only — μηδέν Firestore write / migration /
  companyId-rules / idle ping-pong κίνδυνος, ακριβώς όπως το `systemId` cache που τροφοδοτεί. **ΕΚΤΟΣ ADR-040** (state
  hook + pure helper, όχι canvas micro-leaf). Tests: NEW `mep-connector-seed.test` (5: fixture→c1 in· panel→c1 out·
  host-με-connector→same ref· non-host→same ref· no-mutation) + integration `useMepConnectorReconciliation.test` (+4:
  seed-then-reconcile σε ένα pass· seed χωρίς systems· idempotent μετά το seed) → 11/11 νέα, 322/322 MEP+types+data
  PASS, tsc 0. ⚠️ SHARED TREE — `git add` ΜΟΝΟ specific, ΠΟΤΕ -A.
- **2026-06-03 (Opus 4.8, Plan Mode)** — **Φ7 roadmap — PER-CIRCUIT EDIT ΑΠΟ ΦΩΤΙΣΤΙΚΟ DONE** (pending commit, 🔴
  browser verify). Revit «device → Electrical Circuits»: επιλέγεις φωτιστικό → βλέπεις σε ποιο κύκλωμα ανήκει + jump
  στη διαχείρισή του. Giorgio (AskUserQuestion): **«Select Panel → jump»** (όχι in-place tab switch) → σέβεται την
  panel-centric αρχή του Φ6. **FULL SSOT, μηδέν νέα command/routing.** Νέο panel **«Κύκλωμα»** στο
  `contextual-mep-fixture-tab` (self-hides μέσω `visibilityKey hasCircuit`): (1) read-only info widget NEW
  `RibbonMepFixtureCircuitWidget` (όνομα κυκλώματος + colour swatch· διαβάζει το **ήδη συγχρονισμένο** `activeSystemId`
  + `resolveManagedCircuits([fixture])`· self-hide null)· (2) action button «Επεξεργασία Κυκλώματος» → bridge
  `editCircuit` → `setActiveSystemId(circuit.id)` + `universalSelection.select(circuit.params.sourceEntityId,
  'dxf-entity')` → ο πίνακας-πηγή επιλέγεται → το panel-centric circuit tab (Φ6) εμφανίζεται σε manage mode με ενεργό
  το κύκλωμα του φωτιστικού (το `useMepCircuitEditorSync` κρατά το `activeSystemId` έγκυρο). **Reuse:** το
  `resolveManagedCircuits` ήδη χειρίζεται member-of (fixture → το ένα του κύκλωμα, Revit single-circuit)· `select`
  ήδη στο `useUniversalSelection`· `sourceEntityId` ήδη στα `MepSystemParams`. Bridge: widen `universalSelection`
  slice +`select`· νέο action `editCircuit`· `getPanelVisibility('hasCircuit')`. i18n el/en
  `ribbon.panels.mepFixtureCircuit` + `ribbon.commands.mepFixtureEditor.{circuit.label,editCircuit,editCircuitTooltip}`.
  **ΕΚΤΟΣ ADR-040** (ribbon data/components/bridge — όχι CHECK 6B/6D micro-leaf). Tests: +4 στο
  `useRibbonMepFixtureBridge.test` (hasCircuit visible/hidden· editCircuit selects source panel + sets active· no-op
  χωρίς κύκλωμα) → 17/17 + 128/128 MEP regression PASS, tsc 0. **🐛 BROWSER-VERIFY FIX (Giorgio): το "Edit Circuit"
  πήγαινε στην «Αρχική» αντί στο circuit tab.** ROOT = **γενικό ribbon bug** στο `RibbonRoot` auto-activation effect
  (ADR-345 §5.4): χειριζόταν μόνο persistent→contextual & contextual→none· σε **contextual→ΑΛΛΟ contextual** (fixture
  tab → circuit tab) καμία branch δεν έτρεχε → το `activeTabId` έμενε στο εξαφανισμένο `mep-fixture-editor` →
  `findRibbonTabById`→undefined→`orderedTabs[0]`=home. FIX: όταν το visible contextual set αλλάζει σε μη-κενό,
  activate το πρώτο visible contextual tab εκτός αν είναι ήδη ενεργό (καλύπτει ΚΑΙ persistent→contextual ΚΑΙ
  contextual→contextual)· διορθώνει επίσης κάθε entity→entity contextual μετάβαση (π.χ. wall→column). ΕΚΤΟΣ ADR-040
  (ribbon shell, όχι canvas micro-leaf). ⚠️ SHARED TREE — `git add` ΜΟΝΟ specific, ΠΟΤΕ -A.
  Αρχεία: NEW `ui/ribbon/components/RibbonMepFixtureCircuitWidget.tsx`· MOD `ui/ribbon/hooks/bridge/mep-fixture-command-keys.ts`
  + `ui/ribbon/data/contextual-mep-fixture-tab.ts` + `ui/ribbon/hooks/useRibbonMepFixtureBridge.ts`
  + `ui/ribbon/components/RibbonPanel.tsx` + `ui/ribbon/components/RibbonRoot.tsx` + i18n el/en + `useRibbonMepFixtureBridge.test.tsx`.
- **2026-06-03 (Opus 4.8, Plan Mode)** — **Φ7 roadmap — COLOUR-BY-SYSTEM TOGGLE DONE** (pending commit, 🔴 browser
  verify). View-tab ON/OFF master switch για το colour-by-system (Revit "Color circuits by system") — μέχρι τώρα
  **πάντα ON, χωρίς flag**. OFF → φωτιστικά/πίνακες/καλώδια πέφτουν στο default χρώμα. **FULL ENTERPRISE + FULL SSOT.**
  **ΜΙΑ SSoT πηγή:** νέο `colorBySystem: boolean` (default `true`) στο `ResolvedBimSettings`/`BimRenderSettings`
  (`config/bim-render-settings-types.ts`, resolver `?? true` → legacy views παραμένουν χρωματισμένα) + στο
  `useBimRenderSettingsStore` (state + `buildRaw` persist + idempotent `setColorBySystem` setter, mirror
  `setDisciplineVisibility`· Firestore-persisted per-view· `commitObjectStyles` helper +`colorBySystem` ώστε
  subcategory writes να μην το χάνουν). **4 gate points διαβάζουν το ΙΔΙΟ flag** (τα 3 του handoff + το 3D-wire που
  προστέθηκε για πλήρες 2D/3D parity): (1) **2D φωτιστικό** `MepFixtureRenderer` — gate του `resolveEntitySystemColor`
  (OFF → amber default)· (2) **2D καλώδια** `MepWireRenderer.drawCircuitWires(..., colorBySystem=true)` + νέα export
  `DEFAULT_WIRE_COLOR='#b45309'` (mirror του 3D `elem-mep-wire` material)· `HomeRunWiresOverlay` leaf-subscribe το flag
  & το περνά (+waypoint handle colour)· **το pure routing SSoT `mep-wire-routing.ts` ΔΕΝ άλλαξε** (gate στον renderer)·
  (3) **3D φωτιστικά/πίνακες** `BimSceneLayer.buildContext` — OFF → **κενό** `systemColorIndex` (converters πέφτουν σε
  default material, μηδέν αλλαγή converter)· (4) **3D καλώδια** `SyncContext.colorBySystem` → `sync-circuit-wires` →
  `wirePathToMesh(..., colorBySystem)` → OFF: `getElementMaterial3D('mep-wire')` αντί system-tinted. **UI:** NEW
  `ColorBySystemToggle.tsx` (1:1 mirror `MepWireToggle`, Palette/Ban icon) + `view-tab-bim-settings.ts`
  (`COLOR_BY_SYSTEM_BUTTON` στο `BIM_GRAPHICS_PANEL`) + `RibbonPanel.tsx` dispatch. i18n el/en
  `ribbon.commands.colorBySystem.{label,enable,disable,tooltipEnable,tooltipDisable}`. **STAGE ADR-040**
  (`HomeRunWiresOverlay` overlay CHECK 6D + `BimSceneLayer` critical CHECK 6B). Tests: store default/toggle/idempotent/
  rehydrate (5 νέα) + `mep-wire-to-three` OFF→default-material + νέο `MepWireRenderer.color` suite (4) → 41/41 PASS,
  tsc 0 (δικά μου). ⚠️ 25 pre-existing wall-attach 3D failures (`syncWalls` topBinding, ADR-401/404 άλλου agent) =
  ΟΧΙ regression. Αρχεία: NEW `ui/ribbon/components/ColorBySystemToggle.tsx` + `MepWireRenderer.color.test.ts`· MOD
  `config/bim-render-settings-types.ts` + `state/bim-render-settings-store.ts` + `bim/renderers/MepFixtureRenderer.ts`
  + `bim/renderers/MepWireRenderer.ts` + `components/dxf-layout/HomeRunWiresOverlay.tsx` + `bim-3d/scene/bim-scene-context.ts`
  + `bim-3d/scene/BimSceneLayer.ts` + `bim-3d/scene/sync-circuit-wires.ts` + `bim-3d/converters/mep-wire-to-three.ts`
  + `ui/ribbon/data/view-tab-bim-settings.ts` + `ui/ribbon/components/RibbonPanel.tsx` + i18n el/en + 2 test files.
- **2026-06-03 (Opus 4.8, Plan Mode)** — **Φ7 roadmap — CONDUCTOR-COUNT TICKS DONE** (pending commit, 🔴 browser
  verify). Giorgio (AskUserQuestion): πλήρης Revit προσέγγιση — ξεχωριστά hot/neutral/ground με long/short ticks,
  FULL ENTERPRISE + FULL SSOT. Τα home-run «tick marks» του Revit: λοξές γραμμές που διασχίζουν το home-run leg
  κοντά στο βελάκι, μία ανά αγωγό — **μακριά** ανά φάση (hot/ungrounded), **κοντή** ανά ουδέτερο (neutral/grounded),
  **κοντή + κουκκίδα** ανά γείωση (equipment ground). SSoT ροή: `MepSystemParams.conductors` (persisted, +Zod
  0–12 strict· ΑΝΕΞΑΡΤΗΤΟ από το `poles` rollup — annotation, όχι load calc· `DEFAULT_CONDUCTORS={hot:1,neutral:1,
  ground:1}`) → `computeCircuitWirePaths` το βάζει στο `CircuitWirePath.conductors` (mirror `colorHex`/`style`) →
  NEW pure SSoT `mep-wire-conductor-ticks.ts` (`buildConductorTicks(tip,from,conductors)` → screen-space tick
  segments + kind, zoom-independent όπως το βελάκι· ticks march panel→fixture, hots→neutrals→grounds) → ο
  `MepWireRenderer` μόνο στρώνει (+ κουκκίδα ground). Επεξεργασία: NEW `RibbonMepCircuitConductorsWidget`
  (hot/neutral/ground number inputs, mirror name-widget lifecycle: debounced isDragging merge / blur-Enter flush /
  Esc revert) → υπάρχον undoable `UpdateMepSystemParamsCommand` (μηδέν νέο command). **2D annotation μόνο** (όπως
  το home-run βελάκι — Revit τα δείχνει σε κάτοψη). **Εκτός ADR-040 scope** (`bim/renderers/`, όχι CHECK 6B/6D).
  +1 NEW test suite (conductor ticks) + extend routing/schema tests → 76/76 MEP-wire + 4/4 overlay PASS, tsc 0.
  Αρχεία: NEW `bim/mep-systems/mep-wire-conductor-ticks.ts` + `ui/ribbon/components/RibbonMepCircuitConductorsWidget.tsx`
  + tick test· MOD `bim/types/mep-system-types.ts` (ConductorBreakdown+DEFAULT_CONDUCTORS) + `mep-system.schemas.ts`
  + `bim/mep-systems/mep-wire-routing.ts` (path.conductors) + `bim/renderers/MepWireRenderer.ts` (draw) +
  `ui/ribbon/components/RibbonPanel.tsx` (wiring) + `ui/ribbon/data/contextual-mep-circuit-tab.ts` (Row 5) + i18n
  el/en (`ribbon.commands.mepConductors.*`) + routing/schema tests.
- **2026-06-03 (Opus 4.8)** — **🐛 Φ7 FU#3 3D — waypoint handle ξεκολλά από το καλώδιο FIX** (pending commit,
  🔴 browser verify). Bug (Giorgio): η λευκή σφαίρα-λαβή ενός waypoint, όταν περιστρέφεις τη θέα 3D, σε πολλές
  περιπτώσεις δεν κάθεται πάνω στη γραμμή του καλωδίου. ROOT CAUSE: **δύο διαφορετικές z-interpolations** για το
  ΙΔΙΟ σημείο. Ο conduit (`splicedSegmentInterior`) δίνει το `zMm` του waypoint με **arc-length** fraction
  (`cum[i+1]/total` = πραγματικό μήκος polyline)· ο handle (`collectHandleNodes` στο 3D hook) χρησιμοποιούσε
  **ομοιόμορφο index fraction** `(i+1)/(N+1)`. Όταν τα άκρα του segment διαφέρουν σε ύψος (φωτιστικό@οροφή vs
  πίνακας@τοίχο), τα δύο z αποκλίνουν → η σφαίρα επιπλέει πάνω/κάτω· το orbit αποκαλύπτει το κάθετο offset.
  FIX (FULL SSOT): εξαγωγή `splicedSegmentInterior` (τώρα δέχεται `WireHostPoint` αντί `RoutedHost`) από το
  `mep-wire-routing.ts`· το `collectHandleNodes` καλεί ΤΗΝ ΙΔΙΑ συνάρτηση → handle z === wire z πάντα, για όλα
  τα styles (straight/orthogonal/arc — η σφαίρα κάθεται σε vertex της polyline). **Εκτός ADR-040 scope.** +3
  routing regression tests (arc-length vs index-fraction) → 27/27 routing + 62/62 MEP-wire PASS, tsc 0. Αρχεία:
  MOD `bim/mep-systems/mep-wire-routing.ts` (export + signature) + `bim-3d/animation/use-bim3d-wire-waypoint-interaction-3d.ts`
  (reuse) + `mep-wire-routing.test.ts`.
- **2026-06-03 (Opus 4.8)** — **🐛 Φ7 P2 live-move — meter-scene 1000× fly-off FIX** (pending commit, 🔴 browser
  verify). Bug (Giorgio): σε live drag του 3D gizmo πάνω σε φωτιστικό/πίνακα, το καλώδιο του κυκλώματος
  «πετάγεται στο άπειρο» κατά το drag και επανέρχεται σωστά στο release. ROOT CAUSE = ίδια κλάση με το
  ADR-402/404 meter-scale fix: στο `bim3d-wire-preview-rebuild.applyDragXform` η live μετατόπιση (`worldToDxfPlan`
  → **mm**) προστίθεται κατευθείαν στα plan points που είναι σε **scene units** (mm/cm/m, ό,τι σκαλώνει το
  `wirePathToMesh` με `sceneToM`). Σε σχέδιο μέτρων → +5m έδινε +5000 → εκτόξευση ×1000· το release ήταν σωστό
  γιατί το committed `sync-circuit-wires` διαβάζει consistent units. FIX: μετατροπή του X/Y delta + του rotate
  pivot mm→scene units μέσω του SSoT `mmToSceneUnits(units)` (`mmScale`· mm→1/cm→0.1/m→0.001) πριν αγγίξουν το
  point· το `zMm` μένει πάντα mm (καμία μετατροπή). Καλύπτει ΚΑΙ move ΚΑΙ plan-rotate (ίδιος root). **Εκτός
  ADR-040 scope** (bim-3d/animation, όχι CHECK 6B/6D). +1 meter-scene regression test → 10/10 PASS, tsc 0.
  Αρχείο: MOD `bim-3d/animation/bim3d-wire-preview-rebuild.ts` (+test).
- **2026-06-03 (Opus 4.8)** — **Φ7 follow-up #3 — 3D EDITING DONE** (pending commit, 🔴 browser verify). Giorgio
  (AskUserQuestion): πρόσθεσε editing και μέσα στο 3D viewport (όχι μόνο κάτοψη). Πλήρης επαναχρησιμοποίηση
  του 2D plan-space SSoT — **μηδέν** νέα routing/persistence/command λογική: NEW hook
  `use-bim3d-wire-waypoint-interaction-3d.ts` (mirror των placement hooks: AbortController listeners στο renderer
  canvas, store reads at event time, OrbitControls off μόνο κατά το drag, armed σε 3D + `select` tool) + NEW
  `WireWaypointHandles3D.ts` (σφαίρες-λαβές screen-constant + insert «+» ghost, mirror `PlacementSnapMarker`,
  added στο `manager.scene` ΕΚΤΟΣ bimLayer group). Raycast: σφαίρες για node grab/delete· conduit tube για insert.
  Μετατροπή world↔plan με τα ADR-403 SSoT (`worldToPlanMm`/`planMmToScenePoint`/`resolveActiveFloorElevationMm`)
  → reuse `computeCircuitHostSegments`/`hitTestInsertion`/`deleteWaypointOriented`/`applyWaypointGesture` +
  optimistic `upsertSystem` (live resync μέσω `use-bim3d-vg-resync`) + undoable `UpdateMepSystemParamsCommand`.
  **Boy-Scout (N.0.2):** εξαγωγή `applyWaypointGesture` σε NEW `mep-wire-waypoint-gesture.ts` (κοινό 2D+3D)· και
  ενοποίηση του resolver — `resolverFromHosts` πήρε optional `zMm` ώστε ΕΝΑ SSoT να σερβίρει 2D overlay + 3D
  conduit sync + 3D editor (το `sync-circuit-wires` σταμάτησε να διπλασιάζει το resolver lambda). Mount στο
  `BimViewport3D`. **Εκτός ADR-040 scope** (bim-3d/ files, όχι CHECK 6B/6D). 123/123 MEP + 13/13 overlay/preview
  regression PASS, tsc 0. Αρχεία: NEW `bim-3d/animation/use-bim3d-wire-waypoint-interaction-3d.ts` +
  `bim-3d/animation/WireWaypointHandles3D.ts` + `bim/mep-systems/mep-wire-waypoint-gesture.ts` + gesture test· MOD
  `bim/mep-systems/mep-wire-resolver.ts` (+zMm) + `bim-3d/scene/sync-circuit-wires.ts` (reuse resolver) +
  `bim-3d/viewport/BimViewport3D.tsx` (mount) + `hooks/canvas/use-mep-wire-waypoint-interaction.ts` (reuse gesture).
- **2026-06-03 (Opus 4.8, Plan Mode)** — **Φ7 follow-up #3: WAYPOINTS DONE** (pending commit, 🔴 browser verify).
  Χειροκίνητα ενδιάμεσα σημεία στη διαδρομή ενός κυκλώματος (Revit «Wire Vertex») — direct-manipulation:
  **drag** πάνω σε segment → γεννά + σέρνει νέο vertex· **drag** υπάρχον vertex → move· **right-click** vertex →
  delete. **FULL SSoT, Revit-grade:** τα waypoints persist-άρουν ως `MepSystemParams.wireWaypoints` (+ Zod, mirror
  του `wireStyle`/`color`), **per-segment topology με order-independent key** (sorted host-pair `entityId:connectorId`)
  → επιβιώνουν όταν η nearest-neighbour daisy-chain ξανα-σειροθετείται. **Μία ένεση στο routing:** το
  `computeCircuitWirePaths` κάνει splice τα oriented waypoints (+ **linear zMm interpolate** κατά μήκος του
  σπασμένου polyline) στα `points[]`, ώστε `buildWirePolyline`/`expandSegment` εφαρμόζουν το `wireStyle` **per
  sub-segment** δωρεάν — **2D + 3D αμετάβλητα** (`mep-wire-to-three` δεν αγγίχτηκε· διαβάζει `buildWirePolyline`).
  Interaction = mirror του ADR-376 C.1 opening-tag drag: null-render leaf `MepWireWaypointDragMount` +
  `useMepWireWaypointInteraction` (raw pointer listeners στο viewport, capture + setPointerCapture + RAF), pure FSM
  `MepWireWaypointDragController`· **optimistic** `upsertSystem` κατά το drag (overlay re-routes) + undoable
  `UpdateMepSystemParamsCommand` στο release (μηδέν νέο command). Επεξεργάσιμο **μόνο το ενεργό κύκλωμα**
  (`activeSystemId`). NEW pure SSoT: `mep-wire-waypoints.ts` (key/orientation/builders) + `mep-wire-waypoint-hit.ts`
  + `mep-wire-resolver.ts` (Boy-Scout: εξαγωγή του host-resolver glue, κοινό overlay + interaction) +
  `mep-wire-waypoint-ui-store.ts` (hover highlight leaf). Handles+hover ζωγραφίζονται στο `MepWireRenderer`
  (`drawWaypointHandles`). **STAGE ADR-040** (αγγίχτηκαν `HomeRunWiresOverlay` 6D + `canvas-layer-stack-leaves` 6B·
  μηδέν `useSyncExternalStore` σε orchestrator). 132/132 MEP+overlay+preview regression PASS (incl. 3 νέα suites),
  tsc 0. Αρχεία: NEW `mep-wire-waypoints.ts`/`mep-wire-waypoint-hit.ts`/`mep-wire-resolver.ts`/
  `mep-wire-waypoint-ui-store.ts`/`mep-wire-waypoint-drag-controller.ts`/`use-mep-wire-waypoint-interaction.ts`/
  `canvas-layer-stack-mep-wire-waypoint.tsx` + 3 test files· MOD `mep-wire-routing.ts`/`mep-system-types.ts`/
  `mep-system.schemas.ts`/`MepWireRenderer.ts`/`HomeRunWiresOverlay.tsx`/`canvas-layer-stack-leaves.tsx`/
  `mep-wire-routing.test.ts`.
- **2026-06-03 (Opus 4.8, Plan Mode)** — **Φ7 follow-up #1: orthogonal/arc wire styles DONE** (pending commit,
  🔴 browser verify). Per-circuit «Wiring Type» (Revit) — ο χρήστης επιλέγει `straight`/`orthogonal`/`arc`
  ανά κύκλωμα, εφαρμόζεται ταυτόχρονα 2D + 3D. **FULL SSoT, μηδέν διπλασιασμός geometry:** το style ζει ως
  `MepSystemParams.wireStyle` (+ Zod enum, persisted), διαχέεται ως `CircuitWirePath.style` στο
  `computeCircuitWirePaths` (αδελφός του `colorHex`), και διαβάζεται **σε ΕΝΑ σημείο** στο `buildWirePolyline(path)`
  — αφαιρέθηκε το dead `style` param από `drawCircuitWires`/`wirePathToMesh`, οπότε **κανένα call-site δεν
  άλλαξε** (overlay/3D περνούν ήδη systems). Το `'arc'` έγινε **πραγματική καμπύλη**: quadratic-Bézier
  sampled-σε-polyline στο `expandSegment` (control = midpoint + κάθετο bulge), άρα 2D (`lineTo`) & 3D
  (`LineCurve3` tube) ζωγραφίζουν ίδια καμπύλη χωρίς curve maths στους renderers. UI: NEW leaf widget
  `RibbonMepCircuitWireStyleWidget` (canonical Radix `@/components/ui/select`, ADR-001· mirror color widget) στο
  contextual circuit tab (Row 4)· dispatch μέσω **έτοιμου** `UpdateMepSystemParamsCommand` (generic patch,
  undoable — μηδέν νέο command). i18n `ribbon.commands.mepWireStyle.*` (el+en). Live-follow P2/P2b ανέπαφα
  (το style ρέει μέσω `path.style`). **Δεν αγγίχτηκαν** `HomeRunWiresOverlay`/`CanvasLayerStack`/
  `sync-circuit-wires`/`bim3d-wire-preview-rebuild` → ADR-040 6B/6C καθαρά· `MepWireRenderer` (canvas) → stage
  ADR-040 (6D). 95/95 MEP + 118/118 fixture/panel/3D-preview regression PASS, tsc 0. Αρχεία: `mep-wire-routing.ts`
  + `MepWireRenderer.ts` + `mep-wire-to-three.ts` + `mep-system-types.ts` + `mep-system.schemas.ts` +
  `RibbonMepCircuitWireStyleWidget.tsx` (NEW) + `RibbonPanel.tsx` + `contextual-mep-circuit-tab.ts` + i18n el/en
  + `mep-wire-routing.test.ts`.
- **2026-06-03 (Opus 4.8, Plan Mode)** — **🐛 IDLE PING-PONG FIX DONE** (pending commit, 🔴 browser verify).
  Σε ηρεμία ο viewer εκτελούσε ~2490 React fibers/idle commit. **Root cause:** ping-pong δύο writers που
  διαφωνούσαν για το derived `MepConnector.systemId`. Ο `useMepConnectorReconciliation` (Φ5) σφραγίζει
  `systemId` στα scene fixtures/panels («System wins»)· το `useMepFixturePersistence` diff-merge έκανε
  `dequal(existing.params, doc.params)` — το Firestore doc **δεν** φέρει authoritative `systemId` (type
  contract `mep-connector-types.ts`), άρα existing(με)≠doc(χωρίς) → `docToEntity` **έσβηνε** το systemId →
  `setLevelScene` → νέα `LevelsContext` identity → re-subscribe ΟΛΩΝ των listeners → re-deliver → ∞ (+δεύτερη
  ακμή: stale persisted systemId). Ο τοίχος δεν loop-άρει γιατί δεν έχει derived-field divergence (ο
  `mutated`/`dequal` guard επιστρέφει σωστά `false` στο echo). **Fix (System-wins, persistence ΠΟΤΕ δεν
  κατέχει το cache):** NEW pure SSoT `projectConnectorSystemIds(fresh, live)` στον `mep-system-coordinator`
  (συμμετρικός αδελφός του `reconcileEntityConnectors` — οδηγείται από το **live scene cache** αντί system
  index· αγνοεί τελείως το doc's systemId· referential-stable). Καλείται στο diff-merge των fixture + panel
  persistence hooks πριν το `dequal` → echo γίνεται no-op → ο loop σπάει. **Defense-in-depth:** idempotent
  `setSystems` στο `mep-system-store` (dequal bail σε identical Firestore re-deliveries → δεν ειδοποιεί
  reconcile subscribers). 7 νέα coordinator tests / 66 MEP PASS, tsc 0. ΟΧΙ ADR-040 staging (κανένα micro-leaf
  αρχείο). RC-1/RC-3 (re-subscribe identity churn / per-render reconcile) = out-of-scope pre-existing,
  αβλαβή με σωστό guard. 5 αρχεία: `mep-system-coordinator.ts` + `useMepFixturePersistence.ts` +
  `useElectricalPanelPersistence.ts` + `mep-system-store.ts` + coordinator test.
- **2026-06-03 (Opus 4.8, Plan Mode)** — **Φ7 P2b DONE** (3D **rotate**-follow — κλείνει την ιστορία του
  live-follow). Το καλώδιο ακολουθούσε live μόνο στο 3D **move** gizmo· τώρα ακολουθεί και στο **plan-rotate**
  (Y-ring). Generalize NEW `bim3d-wire-preview-rebuild.ts`: `buildCircuitWirePreviewObjects(draggedIds, xform)`
  με discriminated `WireDragXform` (`move`{translation} | `rotate`{pivot, angleRad})· για rotate, η resolved
  connector plan-θέση **orbit-άρει** το pivot μέσω canonical `rotatePoint` (geometry-vector-utils, ADR-188,
  μηδέν raw cos/sin)· world +Y ↔ DXF-plan CCW 1:1 (επαληθεύτηκε με `applyRotate`)· `worldToDxfPlan` linear
  (μηδέν offset) → valid και σε point pivot. `interaction-handlers`: `captureCircuitWires` (πρώην
  `captureMoveWires`) καλείται τώρα **και** στο rotate· νέο rotate re-route block στο `applyLivePreview`. +1
  rotate test (orbit bbox). 28/28 (rebuild+live-preview) + 141/141 regression PASS, `tsc` 0. 🔴 Pending commit
  (Giorgio) + browser verify (rotate φωτιστικό/πίνακα gizmo 3D → καλώδιο ακολουθεί live).
- **2026-06-03 (Opus 4.8, Plan Mode)** — **Φ7 P2 DONE** (το καλώδιο ακολουθεί **LIVE** το drag, 2D + 3D —
  πριν ενημερωνόταν μόνο στο release). Root: το live move ζωγραφιζόταν μέσω preview/ghost ξεχωριστά από το
  committed scene· η λύση ξαναϋπολογίζει τη διαδρομή ανά frame με resolver που, για τους dragged hosts,
  επιστρέφει τη **live preview** θέση (override) — **πλήρες SSoT, μηδέν διπλασιασμός routing**
  (`computeCircuitWirePaths`/`wirePathToMesh` αμετάβλητα). **2D (store-free):** ο `HomeRunWiresOverlay` παίρνει
  νέο prop `gripDragPreview` (ήδη διαθέσιμο στο `CanvasLayerStack` γρ.410 → μηδέν νέο subscription, CHECK 6C
  safe)· ο resolver λύνει τον dragged host από το **previewed entity** μέσω `applyEntityPreview` (ΙΔΙΑ SSoT με
  το ghost → wire endpoint === ghost, **move + rotation + corner** ομοιόμορφα). Boy-Scout: εξαγωγή NEW
  `hooks/tools/grip-drag-preview-transform.ts` (`toEntityPreviewTransform`) — το snapshot→transform mapping που
  διπλασιαζόταν inline στο `useGripGhostPreview`, τώρα κοινό SSoT ghost+wire. **3D (mirror ADR-401 dependent
  re-clip, move-first):** NEW `bim-3d/animation/bim3d-wire-preview-rebuild.ts` (`affectedWireSystemIds` +
  `buildCircuitWirePreviewObjects`: resolver με `worldToDxfPlan` delta override μόνο στους dragged → reuse
  routing+converter SSoT· rebuild ΜΟΝΟ τα affected circuits· 'all' scope → commit-on-release) + νέο wire
  channel στο `Bim3DEditLivePreview` (`captureWires` by `mepWireSystemId` / `applyWires` swap, mirror του
  dependent μηχανισμού· reset/commit/isActive) + `bim3d-edit-interaction-handlers` (`captureMoveWires` στο
  pointerDown move branch + `applyWires` per-frame στο `applyLivePreview`). **Stage ADR-040** (CHECK 6B/6D —
  `HomeRunWiresOverlay`/`CanvasLayerStack`). Tests: NEW `bim3d-wire-preview-rebuild` (9) + `grip-drag-preview-
  transform` (4) + `HomeRunWiresOverlay.resolver` (4) + wire cases στο `bim3d-edit-live-preview` (5)·
  116/116 MEP + 130/130 move/grip regression PASS, `tsc` 0. **3D rotate-follow = P2b** (βλ. entry παραπάνω·
  το 2D καλύπτει rotate δωρεάν μέσω `applyEntityPreview`). 🔴 Pending commit (Giorgio) + browser verify (drag
  φωτιστικό/πίνακα gizmo 3D + 2D → καλώδιο ακολουθεί live).
- **2026-06-03 (Opus 4.8, Giorgio review)** — **Φ3 grip SSoT κεντρικοποίηση** (follow-up στο «wall-parity»).
  Ο Giorgio εντόπισε σε review ότι το `electrical-panel-grips.ts` ήταν ~90% διπλότυπο του
  `mep-fixture-grips.ts`. **Boy-Scout N.0.2:** εξαγωγή NEW `bim/grips/centred-box-grips.ts` (entity-agnostic
  centred rotatable-box grip SSoT — `getCentredBoxGrips` + `applyCentredBoxGripDrag`, role-based, παραμετρικό
  `minDimensionMm`)· **ΚΑΙ** fixture (ADR-406) **ΚΑΙ** panel γίνανε thin role↔kind adapters (το fixture κρατά
  μόνο το `circular`/`diameter` extension που δεν έχει box equivalent). ~200 γρ. διπλότυπο → ΕΝΑΣ SSoT. Βλ.
  ADR-397 §D3.1. 11 νέα SSoT tests + 118 PASS regression (fixture circular+rectangular / panel / hot-grip /
  ghost / glyph), tsc 0, μηδέν raw cos/sin. Pending commit (Giorgio).
- **2026-06-03 (Opus 4.8)** — **Φ3 grip UX «wall-parity» DONE** (Ηλεκτρικός Πίνακας — πλήρες 2D grip UX
  «όπως ο τοίχος/φωτιστικό/κολώνα»). Ο πίνακας απέκτησε παραμετρικές λαβές (move-κέντρο + rotation-handle +
  4 γωνίες resize opposite-corner-anchored) + hot-grip UX (move 3-click, rotation 6-click ROTATE→Reference,
  γωνίες 2-click) + status-bar μηνύματα + οδηγητικές/rubber-band γραμμές + live ghost + Ctrl-copy.
  **ΚΡΙΣΙΜΗ ΑΡΧΗ:** μηδέν fork — ΕΓΓΡΑΦΗ στις ίδιες entity-agnostic πύλες grip (κανένα παράλληλο pipeline).
  **FULL SSOT, μηδέν raw cos/sin:** reuse `grip-math` (`rotateVector`/`projectToLocalFrame`/
  `sweptAngleDegAboutPivot`) + canonical `rotatePoint` (ADR-188). 1:1 mirror του `mep-fixture-grips`
  ΜΕΙΟΝ circular (ο πίνακας είναι **μόνο rectangular** → ΟΧΙ diameter grip). **NEW:**
  `bim/electrical-panels/electrical-panel-grips.ts` (`getElectricalPanelGrips` + `applyElectricalPanelGripDrag`) +
  `commitElectricalPanelGripDrag` (grip-parametric-commits, `UpdateElectricalPanelParamsCommand` + emit
  `bim:electrical-panel-params-updated`) + `commitElectricalPanelCopy` (grip-parametric-copy, fresh
  enterprise id N.6 via `buildElectricalPanelEntity`+`addElectricalPanelToScene`) + 2 test files. **MOD
  (πρόσθεσε panel row, δεν έσβησε άλλες):** `grip-types` (`ElectricalPanelGripKind` + field) +
  `useGripMovement` re-export + `unified-grip-types` + `grip-computation` (case + DxfGripDragPreview field) +
  `grip-registry` (forward) + `grip-projections` (buildDxfDragPreview + buildRotateReferencePreview) +
  `useGripGhostPreview` + `apply-entity-preview` (interface + grip branch + movesEntity case) +
  `draw-ghost-entity` (footprint polygon) + `grip-glyph-registry` (move/rotation rows) +
  `ElectricalPanelRenderer.getGrips` (rewrite mirror MepFixtureRenderer) + `grip-commit-adapters` (branch) +
  `wall-hot-grip-fsm` (6 rows + `?? electricalPanelGripKind`). 58/58 grip/ghost tests PASS, `tsc` 0, grep
  cos/sin = 0. **Stage ADR-408 (+ ADR-397)** — CHECK 6D (ghost renderers). 🔴 Pending commit (Giorgio) +
  browser verify (move/rotate/corners/copy/ghost).
- **2026-06-03 (Opus 4.8)** — **Φ7 DONE** (ορατά καλώδια / home-run wires, 2D + 3D). Παράγωγη γεωμετρία
  (ΟΧΙ persisted· το `MepSystem` μένει geometry-less). Routing SSoT NEW `mep-wire-routing.ts`
  (`computeCircuitWirePaths` daisy-chain + home-run, nearest-neighbor· `WireHostPoint` x/y/zMm κοινό 2D+3D·
  `WireStyle` seam straight/orthogonal/arc). 2D: NEW `HomeRunWiresOverlay` (ADR-040 micro-leaf, clone
  EnvelopeOverlay) + NEW `MepWireRenderer` (polyline + home-run arrow), mount στο `CanvasLayerStack`
  (**stage ADR-040**, CHECK 6B/6D). 3D: NEW `mep-wire-to-three.ts` (`CurvePath`→`TubeGeometry`, units-safe
  `sceneUnitsToMeters`) + `BimSceneLayer.syncWires` (ορατοί hosts μόνο) + `MaterialCatalog3D` `elem-mep-wire`·
  resync μέσω του Φ5 `use-bim3d-vg-resync`. Visibility: NEW `BimCategory 'mep-wire'` (+MODEL/DISCIPLINE) +
  View-tab `MepWireToggle` (reuse `setObjectStyleVisibility`). 115/115 MEP PASS, tsc 0. Pending commit (Giorgio)
  + browser verify. Follow-ups: orthogonal/arc styles (seam), conductor ticks, waypoints.
- **2026-06-02 (Opus 4.8)** — **Φ6 DONE** (Circuit-Management Panel — rename / colour / add-remove member).
  Διαχείριση υπάρχοντος κυκλώματος μέσω του ΕΤΟΙΜΟΥ `UpdateMepSystemParamsCommand` (zero νέα command/
  mutator/rules). NEW `mep-circuit-editor` (pure: `resolveManagedCircuits` + add/remove builders) +
  `mep-circuit-editor-store` (active circuit) + `useMepCircuitEditorSync` (selection→active, mount στο
  `MepSystemPersistenceHost`). Boy-scout: `computeReassignRemovals` → SSoT στον coordinator (reuse από
  `mep-circuit-from-selection`). 3 leaf widgets (picker/name/**κεντρικός** `ColorDialogTrigger`/color) +
  properties panel + addMembers/removeMembers bridge actions (CompoundCommand) + EventBus toasts + i18n
  el+en. Trigger panel-centric (electrical-panel που τροφοδοτεί ≥1 κύκλωμα). `tsc` 0· 144/144 MEP PASS
  (20 νέα). 🔴 Pending commit (Giorgio) + browser verify.
- **2026-06-02 (Opus 4.8)** — **Φ5 DONE** (Circuit UI + colour-by-system + scene-time reconciliation).
  «Καταναλώνεται» ο κορμός. **Φ5.A:** persisted `MepSystemParams.color` (Revit System Colour, zero αλλαγή
  rules)· NEW `mep-system-color` (palette/pick/index SSoT) + `mep-circuit-from-selection` (source+members+
  Revit single-circuit reassign) + `CreateMepSystemCommand` (mutator `createSystem`, undoable) +
  contextual ribbon tab «Δημιουργία κυκλώματος» (clone beam, trigger=πίνακας+φωτιστικά) + EventBus toasts.
  **Φ5.B:** NEW `useMepConnectorReconciliation` (scene-only, idempotent, System→connector «System wins»),
  mount στο `MepSystemPersistenceHost`. **Φ5.C:** colour-by-system 2D (Mep/Panel renderers, ADR-040
  leaves) + 3D (`SyncContext.systemColorIndex` + `getSystemTintedMaterial3D` no-singleton-mutation +
  resync sub). 22 νέα tests, tsc 0, 124/124 MEP regression PASS. Pending commit· browser verify. **Stage
  ADR-040 (CHECK 6B/6D).** Roadmap: per-circuit edit panel + colour-by-system toggle.
  **Post-verify διορθώσεις (browser):** (1) **legacy-safe create** — `resolveCircuitFromSelection`
  canonical-id fallback (φωτιστικό/πίνακας χωρίς embedded connector γίνεται member/source με
  `FIXTURE_POWER_CONNECTOR_ID`/`PANEL_OUT_CONNECTOR_ID`)· (2) **toast i18n** — το `created` έγινε απλό
  key (το plural `_one`/`_other` δεν resolve-άρισε runtime)· (3) **panel ΟΧΙ χρωματισμένος** — research
  (Revit Electrical Equipment δεν φέρει circuit colour): `buildEntitySystemColorIndex` χαρτογραφεί μόνο
  members· καθαρίστηκαν `ElectricalPanelRenderer`/`panelToMesh`/`syncPanels`.
- **2026-06-02 (Opus 4.8)** — **Φ4 DONE** (cascade / integrity). Delete πίνακα-πηγής→διαλύει τα
  κυκλώματά του· delete μέλους→βγαίνει από το κύκλωμα. **Coherent single-undo** μέσω `CompoundCommand`
  bundle (entity delete + system commands). NEW pure `resolveMepCascadeOnDelete` (coordinator, reuse
  `findSystemsBySource`)· NEW `mep-system-mutator` port (hook↔command bridge, SSoT-preserving)· NEW
  `UpdateMepSystemParamsCommand` + `DissolveMepSystemCommand` (target mutator port, id-preserving
  restore)· `useMepSystemPersistence.restoreSystem` + mutator registration· `useSmartDelete` compound
  wiring. 15 νέα tests (cascade-plan 5 + commands/compound 10), tsc 0, zero νέα Firestore query.
  Pending commit. Φ5 roadmap.
- **2026-06-02 (Opus 4.8)** — **Φ3 DONE** (ηλεκτρικός πίνακας — η πρώτη circuit «πηγή»). Full
  point-based BIM element mirror του ADR-406 fixture pipeline: ~20 νέα αρχεία + ~40 registration
  touch-points (EntityType→rules→ribbon→3D). `IfcElectricDistributionBoard`, outgoing power
  connector, wall-mounted units-safe `panelToMesh` (stair `sceneUnitsToMeters` pattern). geometry/
  symbol/schemas/completion/panel-mesh tests. ✅ browser-verified (place 2D/3D, persistence/refresh,
  delete/refresh, click-select, ribbon/discipline visibility). Pending commit. Φ4–Φ5 roadmap.
- **2026-06-02 (Opus 4.8)** — Φ1 + Φ2 DONE (αρχιτεκτονικός κορμός). Connector embedded model +
  MepSystem persisted abstraction + coordinator reconciliation + πλήρεις registrations + rules.
  Boy-Scout: διορθώθηκε latent `mep-fixture` ENTITY_COLLECTION_MAP gap. 25 νέα tests, tsc 0.
  Pending commit + browser verify. Φ3–Φ5 roadmap.
