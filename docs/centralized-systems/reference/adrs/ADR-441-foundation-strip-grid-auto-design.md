# ADR-441 — Αυτόματη Εσχάρα Πεδιλοδοκών / Ενοποιημένο Πέδιλο (Foundation Strip-Grid Auto-Design)

**Status**: 🟢 IN PROGRESS — αποφάσεις LOCKED (2026-06-11), υλοποίηση σε slices. **Slice 0+1+2+3+JOIN+4+5a+5a-control+5a-grid+6+6b DONE** (Slice 1 rules+indexes DEPLOYED· Slice 3+JOIN+4+5a+5a-control+5a-grid+6+6b εκκρεμούν browser-verify+commit). v1 (associative grid hosting + εσχάρα + follow-on-move + BOQ net-volume + justification mechanism + **justification UI control** + **auto-inward περίμετρος managed regeneration: self-heal + χειροκίνητη υπεροχή** [αντικατέστησε το corner-fill] + **reconciling regeneration: split-safe + idempotent** + **re-host legacy ορφανών**) ΟΛΟΚΛΗΡΩΘΗΚΕ. Σχέδιο: §10. DEFER: Slice 5 (5b pad anchor/5c tie-beams)· Slice 6c (reconcile-with-confirm)· ATOE bridge.
**Date**: 2026-06-11
**Author**: Opus (συνομιλία Giorgio)
**Σχέση**: επεκτείνει ADR-436 (BIM Foundation Discipline — pad/strip/tie-beam). Δες επίσης ADR-363 (wall region/perimeter tools), ADR-423 (MEP auto-design pattern).

---

## 1. Πλαίσιο / Πρόβλημα

Ο Giorgio θέλει να προσθέσουμε στο auto-design τον τύπο **«εσχάρα / ενοποιημένο πέδιλο»** (strip footing grid). Αφορμή: σχέδιο θεμελίωσης κτιρίου (2 στιγμιότυπα που έστειλε):

- **Μπλε** = πέδιλα/πεδιλοδοκοί στο κατώτερο επίπεδο — **συνεχείς ορθογώνιες λωρίδες διατομής ~1.0 m (πλάτος) × 0.50 m (ύψος)**, ενωμένες σε **εσχάρα** (grid) που σχηματίζει 4 φατνώματα.
- **Πράσινο** = συνδετήριες δοκοί / κορμός πεδιλοδοκού (πατάνε πάνω στα μπλε).
- **Κόκκινο** = τοιχία + κολώνες (πατάνε πάνω στα μπλε, στους κόμβους & περίμετρο).

Στη 2η εικόνα (μόνο τα μπλε) φαίνεται **ενιαία συνεχής μάζα με 4 ορθογώνια ανοίγματα** → οι μεσαίες/κοινές λωρίδες είναι **ΜΙΑ ενιαία** (όχι δύο παράλληλες), δηλαδή το boolean union στις διασταυρώσεις γίνεται σωστά (καμία διπλομέτρηση όγκου).

Μαζί, μπλε (φαρδιά βάση) + πράσινο (στενός κορμός) = κλασική διατομή **ανεστραμμένου-Τ πεδιλοδοκού**.

---

## 2. Δομική θεωρία (γιατί είναι σωστό)

| Τύπος | IFC / Revit family | Σχήμα | Πότε |
|---|---|---|---|
| Μεμονωμένο πέδιλο (spread/isolated) | `IfcFooting PAD_FOOTING` / Isolated Foundation | Ορθογώνιο/τετράγωνο, παραμετρικό W×L×T, **διακριτό** | Κάτω από μία κολώνα |
| Συνεχές πέδιλο / πεδιλοδοκός (strip) | `IfcFooting STRIP_FOOTING` / Wall Foundation | Γραμμική λωρίδα | Κάτω από φέρον τοίχο |
| Εσχάρα πεδιλοδοκών (grillage / σταυρωτοί) | πλέγμα STRIP_FOOTING | Λωρίδες σε 2 διευθύνσεις, ενωμένες | Πλαισιωτός φορέας, μέτριο έδαφος, αντισεισμική συν-λειτουργία, πέδιλα που θα επικαλύπτονταν |
| Κοιτόστρωση (mat/raft) | `IfcSlab` (foundation) | Ενιαία πλάκα | Κακό έδαφος / πολύ πυκνά πέδιλα |

- **Τετράγωνο** πέδιλο → κεντρικό/συμμετρικό φορτίο. **Ορθογώνιο** → ροπή σε μία διεύθυνση ή περιορισμός χώρου (περιμετρικό/γωνιακό κοντά σε όριο).
- Όταν μεμονωμένα πέδιλα **εφάπτονται/επικαλύπτονται** → ενοποιούνται σε **κοινό πέδιλο (combined)** ή **πεδιλοδοκό/εσχάρα**.
- **Το ορθογώνιο σχήμα/διατομή είναι το standard** — το επιβεβαίωσε ο Giorgio με την εικόνα.

**Συμπέρασμα:** η εσχάρα πεδιλοδοκών (ορθογώνιες λωρίδες ενωμένες) είναι απολύτως ορθό, πολύ συνηθισμένο σύστημα.

---

## 3. Τι κάνει η Revit (απάντηση: ένα-ένα ανά κατηγορία, ΟΧΙ ταυτόχρονα)

Η Revit είναι **authoring**, όχι structural auto-designer. Τα στοιχεία τοποθετούνται **ξεχωριστά, ανά κατηγορία** (με batch):

- **Wall Foundation** (πεδιλοδοκός/strip) → host από τοίχο· επιλέγεις **πολλούς τοίχους μαζί** → μπαίνει σε όλους· διασταυρώσεις → **auto-join/miter**.
- **Isolated Foundation** (πέδιλο) → batch «at columns».
- **Foundation Slab** (κοιτόστρωση) → σχεδιάζεις boundary.
- **Grade beam / συνδετήρια δοκός** → είναι **Structural Framing** (άλλη κατηγορία) → μπαίνει **χωριστά**.

➡️ **Πεδιλοδοκός και συνδετήρια = διαφορετικές κατηγορίες → η Revit ΔΕΝ τα βάζει σε μία εντολή.** Το true auto-design ολόκληρου συστήματος το κάνουν analysis εργαλεία/add-ins (Robot, Tekla, Dynamo), όχι το core της Revit.

**Ευκαιρία (value-add πάνω από Revit):** εμείς ΜΠΟΡΟΥΜΕ να παράγουμε όλο το σύστημα ταυτόχρονα (εσχάρα + προαιρετικά συνδετήριες) σε μία εντολή / ένα undo, κρατώντας **διακριτά entities** για σωστό BOQ/IFC/audit.

---

## 4. Έρευνα κώδικα (2026-06-11) — ΤΙ ΥΠΑΡΧΕΙ / ΤΙ ΛΕΙΠΕΙ

Όλα στο `src/subapps/dxf-viewer/`.

### 4.1 Υπάρχει (απευθείας reusable — SEARCH FIRST, μην ξαναφτιάξεις)

| Κομμάτι | Αρχείο / signature | Ρόλος |
|---|---|---|
| **Boolean union πολυγώνων** | `bim/geometry/shared/safe-polygon-boolean.ts` → `safeUnion/safeIntersection/safeDifference` | Wrapper polygon-clipping@0.15.7 (MIT), crash-proof, meter-scale robust. Ήδη ενώνει επικαλυπτόμενα αποτυπώματα (`building-footprint.ts`). **Άμεσα reusable για εσχάρα.** |
| **Ανίχνευση περιοχών/ορθογωνίων** | `bim/walls/perimeter-from-faces.ts` (`extractClosedPolygons`, `perimeterFacesToRects`, `unionTouchingPolygons` via safeUnion, `getCachedRegionPerimeters` WeakMap), `bim/walls/wall-in-region.ts` (`extractLineSegments`, `findRectanglesFromSegments`, `findEnclosingRectangle`, `buildWallFillingRect`) | Corner-graph rectangle detector + κλειστά polygons από DXF lines. Κοινό SSoT wall+column region. |
| **Strip από τοίχο (1)** | `bim/foundations/foundation-from-wall.ts` → `buildStripFromWall(wall, overrides, levelId, sceneUnits)` | Εξάγει start/end + width=wall.thickness → `completeFoundationFromTwoClicks(...,'strip',...)`. Re-export `pickWallEntityAt` (κοινό hit-test). |
| **Foundation geometry/entity** | `bim/geometry/foundation-geometry.ts` → `computeFoundationGeometry` (strip/tie-beam = `buildBandFootprint` band start→end× width· pad = rect+anchor+rotation), `hooks/drawing/foundation-completion.ts` → `buildFoundationEntity`, `buildDefaultFoundationParams`, `completeFoundationFromTwoClicks` | Pure builders. strip & tie-beam = ΙΔΙΟ geometry, διαφορά IFC + default width. |
| **Batch pattern (N entities σε loop)** | `hooks/drawing/use-wall-commit.ts` → `buildFillingWalls`, `commitInRegionRects`, `commitPerimeterFaces` | Πρότυπο για `buildFoundationsFromAllWalls`. Κάθε entity → `onCreated` ένα-ένα + EventBus toast (`bim:walls-from-perimeter`). |
| **Wall auto-join / miter** | `bim/walls/wall-region-autojoin.ts` (`extendFillingWallToNeighbors`, ray-extend endpoints), `bim/walls/wall-trims.ts` (miter trim engine bevel/butt + `applyTrimPatches`), `addWallToScene` (recompute trims όλων) | Πρότυπο join στις διασταυρώσεις (γεωμετρικό extension, ΟΧΙ boolean). |
| **CompoundCommand (atomic undo)** | `core/commands/CompoundCommand.ts` | Atomic execute/undo, nested, serializable. (Το wall batch ΔΕΝ το χρησιμοποιεί — undo ανά entity.) |
| **Insert foundation** | `bim/foundations/add-foundation-to-scene.ts` → `addFoundationToScene` | Thin wrapper `appendEntityToScene(...,'foundation')`. Χωρίς miter (foundations δεν join-αρονται σήμερα). |
| **Tool / ribbon** | `hooks/drawing/useFoundationTool.ts` (FSM: pad single-click, strip/tie-beam 2-click, from-wall 1-click· `placementMode: 'freehand'\|'from-wall'`), `bim/foundations/foundation-preview-store.ts`, `systems/tools/tool-definitions.ts` (4 ids), `ui/ribbon/data/home-tab-draw.ts`, `ui/ribbon/data/contextual-foundation-tab.ts` | Entry points + contextual panels (pad-only / line-only / elevation). |
| **Region foundation = slab** | (ADR-436 §3.6) εδαφόπλακα/κοιτόστρωση = REUSE `SlabEntity` (kinds ground/foundation) | Για «ενιαίο σώμα» αναπαράσταση, μία επιλογή = slab με openings. |

### 4.2 Λείπει (νέος κώδικας)

1. **`buildStripGridFromWalls(walls[], overrides, levelId, sceneUnits)`** — batch loop (mirror `buildFillingWalls`): κάθε φέρων τοίχος → `buildStripFromWall` → array `FoundationEntity`.
2. **Ένωση στις διασταυρώσεις** — είτε (α) **trim/miter** σαν τους τοίχους (διακριτά entities, χωρίς overlap), είτε (β) **safeUnion** των band footprints σε ΕΝΑ σώμα (polygon-with-holes). Χωρίς αυτό → διπλό υλικό/όγκος στις διασταυρώσεις, λάθος BOQ.
3. **Dedup overlapping strips** — παράλληλοι κοντινοί τοίχοι (διαχωριστικός + φέρων) → bands επικαλύπτονται· dedup (ίδιος άξονας ± tol) ή merge.
4. **Νέο placement mode + FSM branch** στο `useFoundationTool` (π.χ. `'grid'` / `'from-all-walls'`) + commit (mirror `commitPerimeterFaces`).
5. **Atomic undo** για όλη την εσχάρα (10-50 strips) → `CompoundCommand` (το wall batch δεν το κάνει· για εσχάρα ο χρήστης περιμένει 1 undo).
6. **Ribbon entry** — νέο subvariant/tool id + EventBus toast («Δημιουργήθηκαν N πεδιλοδοκοί»).
7. **(Αν «ενιαίο σώμα»)** νέα αναπαράσταση: είτε νέο foundation kind με polygon-with-holes geometry, είτε reuse SlabEntity (foundation kind) με slab-openings.

---

## 5. Ανοιχτές αποφάσεις (ΕΚΚΡΕΜΟΥΝ — συνέχεια συζήτησης με Giorgio)

1. **Πηγή / trigger της εσχάρας:**
   - (Α) Από τους **τοίχους** (batch· πιο κοντά στην υποδομή + στην εικόνα). ← πρόταση Opus
   - (Β) Από **περίγραμμα/region** (σχεδιάζεις γραμμές → ανίχνευση φατνωμάτων).
   - (Γ) Από **κολώνες** (πέδιλα στις κολώνες + συνδετήριες/grid lines).

2. **Αναπαράσταση αποτελέσματος:**
   - (Α) **Διακριτοί πεδιλοδοκοί + join** στις διασταυρώσεις (Revit-like· 1 entity ανά γραμμή· BOQ/IFC ανά στοιχείο· φαίνεται ενιαίο· θέλει trim engine).
   - (Β) **Ένα ενοποιημένο σώμα** (safeUnion → 1 entity polygon-με-τρύπες· όπως η εικόνα· απλούστερο visual + BOQ· χάνεις ανά-στοιχείο ανάλυση· νέο kind ή reuse slab).

3. **Συνδετήριες δοκοί ταυτόχρονα;**
   - (Α) Όχι, μόνο πεδιλοδοκοί (v1 εστιασμένο). ← πρόταση Opus για v1
   - (Β) Ναι, πεδιλοδοκοί + συνδετήριες σε ένα undo (value-add, μεγαλύτερο scope).

---

## 6. Πρόταση Opus (draft — προς συζήτηση)

**v1 = «Εσχάρα πεδιλοδοκών από τοίχους», διακριτά entities με join, χωρίς συνδετήριες:**
- Trigger: μία εντολή (ribbon button) που παίρνει όλους τους φέροντες τοίχους της σκηνής → `buildStripGridFromWalls`.
- Join: reuse pattern `extendFillingWallToNeighbors` (extend endpoints) ή trim· για σωστή BOQ υπολογίζουμε **unioned area/volume** με `safeUnion` (χωρίς να merge-άρουμε τα entities).
- Entities: διακριτό `FoundationEntity` (strip) ανά τοίχο → σωστό BOQ/IFC/audit (ADR-436).
- Undo: `CompoundCommand` (atomic για όλη την εσχάρα).
- Εκτέλεση: **Plan Mode** (cohesive foundation domain) ή **Orchestrator** (5+ αρχεία, geometry/batch/ribbon/command) — απόφαση Giorgio (N.8). Μοντέλο: **Opus**.

**Εναλλακτική (αν ο Giorgio θέλει ακριβώς την εικόνα):** «ενιαίο σώμα» via `safeUnion` → 1 entity (slab-foundation με openings ή νέο kind) — απλούστερο, αλλά χάνει την ανά-πεδιλοδοκό ανάλυση.

---

## 7. Επόμενα βήματα

1. **[ΕΚΚΡΕΜΕΙ — Giorgio]** Απαντήσεις στις 3 ανοιχτές αποφάσεις (§5).
2. Μετά: Plan Mode (ή Orchestrator) με ακριβές file plan βάσει §4.2.
3. Υλοποίηση reusing §4.1 (safeUnion, buildStripFromWall, batch pattern, CompoundCommand).
4. Tests (geometry union, batch, FSM) + N.15 docs.

---

## 8. Ευρύτερη φιλοσοφία & στρατηγική (καταγραφή συζήτησης Giorgio, 2026-06-11)

Ο Giorgio εξήγησε τη συνολική φιλοσοφία — η εσχάρα πεδιλοδοκών είναι **το πρώτο κομμάτι** ενός μεγαλύτερου στόχου. Καταγράφεται εδώ ως κατεύθυνση.

### 8.1 Διπλό mode εφαρμογής
1. **Design from scratch** — ο χρήστης σχεδιάζει 2D/3D οντότητες στον καμβά (το σημερινό).
2. **Import-driven erection** — εισάγεις **DXF σχέδια μηχανικών** (στατικά / αρχιτεκτονικά / Η/Μ, π.χ. πλήρη σχέδια αδείας) και **ανεγείρεις ημι-αυτόματα το 3D μοντέλο**, **bottom-up**: θεμελίωση → υπόγειο/-α → ισόγειο → όροφοι. Εστίαση: **οπλισμένο σκυρόδεμα** (όχι μεταλλικές, προς το παρόν).

**Στόχος (LOCKED ως αρχή):** δραστική **επιτάχυνση του 3D στησίματος** — ο μηχανικός δίνει περίγραμμα/εντολές, το σύστημα γεμίζει — **ημι-αυτόματο, παραμετρικό, πολυπαραγοντικό**.

### 8.2 Παραμετρική θεμελίωση (το αίτημα του Giorgio)
Δίνεις **περίγραμμα** → εντολή «φτιάξε εσχάρα πεδίλων **N×M φατνώματα**», με:
- αυξομείωση πλήθους/διαστάσεων φατνωμάτων,
- **κεντρικά ή έκκεντρα** πέδιλα,
- **μετακίνηση πέδιλου χωρίς να «σπάει»** η εσχάρα (μη-ομοιόμορφα φατνώματα),
- **διαφορετικά πάχη/ύψη** ανά πέδιλο/φάτνωμα → **πολυπαραμετρικό**.
Μετά: **συνδετήριες** (κεντρικές/έκκεντρες, με πάχη/ύψη), μετά **τοιχία/κολώνες**.

### 8.3 Πώς το κάνουν ΟΝΤΩΣ οι μεγάλοι — **GRID-FIRST (κάναβος αξόνων)**
**Όλα** τα σοβαρά εργαλεία σκυροδέματος (Revit, Tekla, CSI ETABS/SAFE, **ProtaStructure**, Allplan, SCIA, Graitec) ξεκινούν από **κάναβο αξόνων** (Α/Β/Γ… × 1/2/3…) με **μεταβλητές αποστάσεις**:
- οι **τομές** = κόμβοι (κολώνες), τα **κουτιά** = φατνώματα → ο «πίνακας N×M» = παραμετρικός κάναβος·
- **associative**: μετακινείς άξονα → **όλα τα hosted στοιχεία ακολουθούν** (= το «move χωρίς να σπάει»)· τα στοιχεία είναι **constrained στο grid**, όχι ανεξάρτητα.

➡️ **Σωστή αρχιτεκτονική: ο κάναβος = SSoT· πέδιλα/συνδετήριες/κολώνες/δοκοί = «παιδιά» του.** Λύνει ταυτόχρονα: πίνακας, αυξομείωση, move-no-break, παραμετρικότητα — σε **όλους** τους ορόφους. Η «εσχάρα από περίγραμμα» = **ειδική περίπτωση** (grid → strips στις γραμμές).

### 8.4 Ο πιο κοντινός «μεγάλος»: **ProtaStructure** (+ CSI SAFE, Graitec, Allplan)
Κάνει **ακριβώς** τον workflow του Giorgio για σκυρόδεμα: import αρχιτεκτονικό DWG ως υπόβαθρο → κολώνες/δοκοί στον κάναβο (snap στις γραμμές) → **auto-generate foundations** (isolated/strip/raft, αυτόματη διαστασιολόγηση από φορτία) → storey propagation → analysis → σχέδια.

### 8.5 Η αλήθεια για «αυτόματο 2D → 3D» (honesty)
- **Καθαρή Revit**: imported CAD = **underlay**, χειροκίνητο trace (ή Dynamo). Δεν «διαβάζει» την κάτοψη.
- **Ημι-αυτόματα** (ProtaStructure/SAFE): αναγνώριση **γραμμές → άξονες/μέλη**, με επιβεβαίωση χρήστη.
- **AI «κάτοψη → 3D»** (Snaptrude, TestFit, Hypar, ARKio, startups): **emerging, human-in-the-loop**.
- ➡️ **Το πλήρως αυτόματο «DXF → έτοιμο 3D» ΔΕΝ είναι λυμένο πουθενά.** Όλοι κάνουν **ημι-αυτόματο, grid-driven, με τον μηχανικό στο τιμόνι** — δηλαδή **ακριβώς** τον στόχο του Giorgio. Ρεαλιστικό & state-of-the-art.

### 8.6 Κεντρικά vs έκκεντρα πέδιλα (διευκρίνιση)
- **Κεντρικό**: κολώνα στο κέντρο (συμμετρικό φορτίο).
- **Έκκεντρο**: κολώνα στην άκρη (περιμετρικά/όρια οικοπέδου) → **ροπή** → λύση με **συνδετήρια δοκό ισορρόπησης (strap/balanced beam)** ή **κοινό πέδιλο**. Οι συνδετήριες παραλαμβάνουν διαφορικές καθιζήσεις + ροπές εκκεντρότητας + σεισμικά. Στα εργαλεία = **offset/eccentricity παράμετροι**.

### 8.7 Σύσταση κατεύθυνσης (προς συζήτηση)
Πριν την «εσχάρα» ως μεμονωμένο feature, η θεμελιώδης απόφαση = **structural grid (κάναβος) ως SSoT**, με τα δομικά στοιχεία constrained σε αυτόν. **ΑΝΟΙΧΤΟ προς διερεύνηση:** υπάρχει ήδη concept «οδηγών/guides» στη 2D σχεδίαση (`useGuideState`/`useGuideActions`/`guide-click-handlers`) — **να ελεγχθεί αν οι guides = structural grid ή κάτι διαφορετικό** (named/associative/host-bearing vs απλές βοηθητικές γραμμές). Αυτό καθορίζει αν χτίζουμε grid πάνω στο υπάρχον ή από την αρχή.

### 8.8 ΕΥΡΗΜΑ (έρευνα 2026-06-11): οι υπάρχοντες «οδηγοί/guides» ΕΙΝΑΙ ήδη structural grid (ADR-189) — λείπουν 2 κρίσιμα

Διερευνήθηκε το ερώτημα της §8.7. **Καλό νέο: ΔΕΝ ξεκινάμε από το μηδέν.** Το σύστημα `systems/guides/` (ADR-189 «Construction Grid / Guide System», εμπνευσμένο Fespa/Tekton) είναι **ήδη πλούσιος κάναβος αξόνων**, ΟΧΙ απλές γραμμές Illustrator.

**Έχει ήδη (Revit/Tekla-style):**
- Ονοματισμένους άξονες **Α/Β/Γ… × 1/2/3…** (auto ή user label) + **bubbles** στο περιθώριο (`guide-annotations-renderer.ts`).
- **Διαστάσεις (bay) μεταξύ αξόνων** (B3), **groups** («Δομικός Κάνναβος»/MEP), **structural presets** (bay 4/5/6/8m, `CreateGridFromPresetCommand`).
- **Snap engine** (`GuideSnapEngine`, intersection/midpoint/fractal), τύποι X (κατακόρυφος) / Y (οριζόντιος) / XZ (διαγώνιος).
- **IFC4 export** ως `IFCGRID`/`IFCGRIDAXIS` (Revit-importable).
- Πλήρες command set (create/move/parallel/perpendicular/rotate/scale/mirror/equalize/polar-array/from-entity) με **undo/redo**, lock, visibility.
- Store: `systems/guides/guide-store.ts` (in-memory singleton, observer pattern, max 500), `Guide` interface (`id/axis/offset/label/style/groupId/parentId/startPoint/endPoint`).

**ΛΕΙΠΟΥΝ 2 κρίσιμα για πλήρες Revit-grade grid που οδηγεί αυτόματη ανέγερση:**
1. **Persistence** — οι guides είναι **session-only** (χάνονται σε reload· καμία Firestore/localStorage). Τα BIM entities persist, οι guides όχι.
2. **Associative hosting (το κρισιμότερο κενό)** — **καμία** BIM entity (wall/column/beam/foundation/slab schema) δεν φέρει `guideId`/`gridAxisId`. Μετακινείς άξονα → **τίποτα δεν ακολουθεί**. Στη Revit, στοιχείο hosted σε grid axis ακολουθεί αυτόματα. Αυτό είναι το «move-no-break» που ζητά ο Giorgio (§8.2).

Δευτερεύοντα κενά: per-floor/per-level scope (τώρα global singleton), bubble extents, per-view crop.

**ΣΥΜΠΕΡΑΣΜΑ:** ο «κάναβος» της §8.3 **υπάρχει ήδη ως guides (ADR-189)** — είναι ~80% του δρόμου. Για να γίνει το SSoT της αυτόματης θεμελίωσης/ανέγερσης χρειάζεται: **(α) persistence** + **(β) associative hosting** (BIM entities → constrained σε guide axes, follow-on-move). Τότε: εσχάρα πεδίλων = «πέδιλα στις τομές/γραμμές του grid», και το «N×M φατνώματα / αυξομείωση / move-no-break / παραμετρικό» βγαίνουν φυσικά. **Σχέση: ADR-189 (grid) + ADR-436 (foundation) + ADR-441 (αυτό).**

## 10. Σχέδιο υλοποίησης (LOCKED 2026-06-11) — 4 slices

**Αποφάσεις Giorgio:** (1) αφετηρία = **associative grid hosting** (Revit way)· (2) πηγή εσχάρας = **ο κάναβος (grid)**· (3) αναπαράσταση = **διακριτοί πεδιλοδοκοί + join**· (4) **ΟΧΙ** συνδετήριες v1· scope = **όλα τα slices 0→3** (έγκριση ανά slice)· grid = **per-όροφο** (Revit-grade).

**Canonical hosting model (SSoT, generic):** slot-based `GuideBinding { guideId, slot }` όπου `slot ∈ {start-x,start-y,end-x,end-y,center-x,center-y}`, σε `BimEntity.guideBindings?` (base). Υπερκαλύπτει foundation-specific tags· επιτρέπει αύριο wall/column/beam χωρίς re-write.

- **Slice 0 — Hosting types (✅ DONE):** `bim/hosting/guide-binding-types.ts` (GuideBinding/GuideBindingSlot/HostedEntityMixin + extractBoundGuideIds/hasGuideBindings)· `BimEntity.guideBindings?` (`bim/types/bim-base.ts`)· Zod `bim/types/guide-binding.schemas.ts` + foundation entity schema. Backward-compatible (optional· entity schemas `.passthrough()`). 11 jest + 383 regression PASS.
- **Slice 1 — Grid persistence per-όροφο (✅ DONE + DEPLOYED):** mirror foundation Firestore· 1 doc/floor (`floorplan_grid_guides`, grd_* enterprise-id)· `GridGuideFirestoreService` (createGrid setDoc / updateGrid)· `useGridGuidePersistence` (hydrate `clear`+`restoreGroup`/`restoreGuide`· debounced 1000ms save με anti-echo signature guard· per-floor reset `store.clear()` on scope change)· `GridGuidePersistenceHost` mounted στο DxfViewerTopBar· serialization `guideToSnapshot` (strips temporary + undefined keys, Firestore-safe). + firestore.rules match block + 4 indexes **DEPLOYED στο pagonis-87766**. 14 jest PASS.
- **Slice 2 — Εσχάρα από grid (1ο ορατό) (✅ DONE):** `bim/foundations/foundation-from-grid.ts` (`buildStripGridFromGuides`· intersection-to-intersection segments → zero-overlap join· strips born-hosted με slot-based `guideBindings`· dedup σχεδόν-ταυτόσημων offsets· invisible-skip· `<2 άξονες/διεύθυνση`→`insufficient-guides`)· orchestrator `bim/foundations/foundation-grid-commit.ts` (`commitFoundationGridFromGuides` → ΕΝΑ atomic `CreateFoundationsCommand`, 1 undo)· batch command `core/commands/entity-commands/CreateFoundationsCommand.ts` (πιστό mirror `CreateMepSegmentsCommand`: scene via `ISceneManager`, Firestore via deferred `drawing:entity-created`/`bim:foundation-delete-requested` σε microtask — persistence-correct create **και** undo). **ΔΙΟΡΘΩΣΗ σχεδίου vs draft:** (α) **ribbon `action` (`foundation.actions.fromGrid`), ΟΧΙ canvas tool** — η εσχάρα διαβάζει υπάρχοντα κάναβο, καμία canvas αλληλεπίδραση → ταιριάζει στο one-shot πρότυπο των MEP auto-design bridges, όχι σε fake tool· (β) **`CreateFoundationsCommand` αντί generic `CompoundCommand<CreateEntityCommand>`** — το `CreateEntityCommand` δεν εκπέμπει `drawing:entity-created` → strips δεν θα persist-άρανε ποτέ. Wiring: `useRibbonFoundationBridge.onAction` (`handleFromGrid`)· ribbon subVariant στο `foundationGroup`· events `bim:foundations-from-grid{built,ignored}` / `bim:foundations-from-grid-failed{reason}`· toast μέσω `useDxfViewerNotifications` (ICU plurals)· i18n el/en. 16 jest (7 grid builder + 3 commit + 6 command). ΕΚΤΟΣ ADR-040 (one-shot batch, καμία preview-store/canvas/renderer/guide-render αλλαγή· grid read μέσω `getGlobalGuideStore()` στο event-time, χωρίς high-freq subscription). BOQ overlap στους κόμβους = `safeUnion` offline (DEFER Slice 4).
- **Slice 3 — Follow-on-move (✅ DONE):** μετακινείς άξονα κανάβου → όλα τα hosted strips ακολουθούν live (60fps) + persist on drag-complete (Revit associative grid). Pure-first αρχιτεκτονική:
  - NEW `bim/hosting/derive-params-from-guides.ts` — `deriveFoundationParamsFromGuides(params, bindings, getOffset)`: slot→coordinate writes (start-x→start.x… center-x→position.x), immutable, idempotent (no-change→`null`). XZ/διαγραμμένος άξονας (offset undefined) → slot αγνοείται.
  - NEW `bim/hosting/guide-hosting-reconciler.ts` — pure `buildHostingIndex` (inverted index `Map<guideId,Set<entityId>>`) + `reconcileHostedFoundations` (only-changed updates· geometry+validation re-derived από SSoT `computeFoundationGeometry`/`validateFoundationParams`).
  - NEW `hooks/data/useHostingReconciler.ts` + `app/HostingReconcilerHost.tsx` — **το μόνο stateful κομμάτι (ADR-040)**: imperative `guide-store.subscribe`, **RAF-throttled 1×/frame**, per-tick diff των bound-axis offsets (skip unrelated notifies), only-changed `setLevelScene`. Loop-free (ακούει μόνο guide-store). Mount στο `DxfViewerTopBar` δίπλα στα persistence hosts.
  - **Persist on drag-complete:** settle-debounce (350ms) → `EventBus.emit('bim:entities-moved')` με τις moved strips → reuse `useBimEntityMovedPersistEffect` (ADR-436), **κανένα νέο persist path**.
  - **Persist bindings (ΚΡΙΣΙΜΟ):** MOD `FoundationDoc` + `entityToSaveInput` (`foundation-firestore-service.ts`) + `docToEntity` (`useFoundationPersistence.ts`) → `guideBindings` round-trip· **καμία αλλαγή firestore.rules** (create rule = `hasAll` allowlist, επιτρέπει το nested field· update rule δεν το απαγορεύει).
  - Stage ADR-040 (changelog entry· νέα αρχεία ΕΚΤΟΣ CHECK 6B/6D paths). 22 jest (8 derive + 6 reconciler + 4 firestore-service guideBindings + 4 υπάρχοντα regression) + tsc καθαρό δικά μου. DEFER: wall/column/beam hosting (derive→strategy registry ανά kind), XZ/διαγώνιοι άξονες, per-tick guide-offset snapshot persistence για undo-after-reload.
- **Slice JOIN — Corner-fill γωνιών (✅ DONE):** έκλεισε τα ακάλυπτα `w/2 × w/2` τεταρτημόρια στις **4 εξωτερικές γωνίες** της εσχάρας (οι λωρίδες σταματούσαν στο centerline → ορατά κενά γωνιών· εσωτερικοί κόμβοι ήδη πλήρως καλυμμένοι από επικάλυψη). **Follow-move-safe** (κρίσιμο): η επέκταση δεν είναι σκέτη αλλαγή start/end (το Slice 3 derive θα την ακύρωνε σε κάθε move) αλλά **νέο optional `extend?: number` (mm, signed) στο `GuideBinding` endpoint** = σταθερή απόσταση *σχετικά με τον (μετακινούμενο) άξονα*, άρα επιβιώνει του re-derive. MOD: `bim/hosting/guide-binding-types.ts` (`GuideBinding.extend?`) + `bim/types/guide-binding.schemas.ts` (`extend: z.number().optional()` — `.strict()` schema, αλλιώς restore σκάει) + `bim/hosting/derive-params-from-guides.ts` (honor `extend` με mm→scene conversion μέσω SSoT `mmScaleFor`, ΜΟΝΟ στο extend term· idempotent διατηρείται) + `bim/foundations/foundation-from-grid.ts` (extend `±width/2` ΜΟΝΟ σε **γωνιακά** endpoints = extreme-parallel-axis × extreme-perpendicular-axis· refactor σε `emitVerticalStrips`/`emitHorizontalStrips` helpers για ≤40γρ· geometry baked στο build + binding tag, ίδια φόρμουλα με derive → consistent). **ΟΧΙ** επέκταση μη-γωνιακών περιμετρικών άκρων (θα προεξείχαν «δόντια»). 25 jest (10 derive incl. 4 extend + 13 grid builder incl. 6 corner-fill + 2 existing updated) + tsc καθαρό. ΕΚΤΟΣ ADR-040 (όλα pure type/schema/build/derive — καμία scene-write/renderer/guide-render). DEFER Slice 4 BOQ: η επικάλυψη/extend κάνει double-count όγκου στους κόμβους → `safeUnion` offline.

**Επόμενα slices (σχεδιασμός — LOCKED 2026-06-11):**
- **Slice 4 — BOQ θεμελίωσης + net volume (safeUnion) (✅ DONE):** (Α) σύνδεση θεμελίωσης στο schedule (preset/columns/registry/UI toggle/i18n — δεν υπήρχε καθόλου foundation στο BOQ) + (Β) καθαρός (de-duplicated) όγκος εσχάρας μέσω boolean **union**. Η επικάλυψη στους κόμβους (`w × w` block ανά διασταύρωση, εγγενής στη μονολιθική αναπαράσταση) διπλομετρά τον όγκο· λύση χωρίς αλλαγή γεωμετρίας/όψης (Revit/Tekla way). NEW `bim/geometry/foundation-grid-boq.ts` (`computeFoundationGridNet` thickness-bucketed `safeUnion`· `foundationStripNetGeometry` per-strip net = gross − ½·Σ επικαλύψεων μέσω `safeIntersection`· Σ μεριδίων == union, exact για uniform πάχος) + NEW `hooks/data/foundation-boq-feed.ts` (`applyFoundationGridNet` pre-pass μόνο σε hosted grid strips). Boy-Scout SSoT: **export** `multiPolygonArea` από `polygon-utils.ts` (ήταν private duplicate ×2). Schedule: `ScheduleEntityType`+`AnyBimEntity`+`mapFoundation`+`PRESET_REGISTRY`+`FOUNDATION_COLUMNS`+`ScheduleEntityToggle`+`BimScheduleDialog` (pre-pass στο `useMemo`)+i18n el/en. Η στήλη όγκου δείχνει **net** ανά λωρίδα → άθροισμα = καθαρός όγκος, χωρίς νέα totals-infra. ΕΚΤΟΣ ADR-040 (καμία canvas/renderer αλλαγή). DEFER: ATOE/accounting bridge (`bimToBoqBridge` σε `useFoundationPersistence`)· μεταβλητό πάχος ανά grid (uniform=exact). 24 net+preset jest + 238 regression + tsc καθαρό.
- **Slice 5 — Foundation Justification + έκκεντρα πέδιλα + συνδετήριες (tie-beams):** σήμερα οι λωρίδες είναι **κεντρικές (concentric)** πάνω στον άξονα (`w/2` εκατέρωθεν) = δομικά ιδανικό default (φορτίο από κεντροβαρικό, μηδενική εκκεντρότητα). ΑΛΛΑ σε **όριο οικοπέδου / υπάρχον κτίριο** ο μηχανικός χρειάζεται **έκκεντρο** πέδιλο (ανάπτυξη μόνο προς τα μέσα, αλλιώς το `w/2` overhang περνά το όριο). Revit-grade λύση = παράμετρος **Justification (Location Line)** ανά πέδιλο.
  - **ΑΝΑΘΕΩΡΗΣΗ ΜΗΧΑΝΙΣΜΟΥ (2026-06-11, Giorgio «όπως η Revit, full SSoT»):** η αρχική draft πρότεινε `GuideBinding.extend`· μετά από Recognition στον κώδικα (code=SoT) επιλέχθηκε **καθαρότερη geometry-param λύση** (= η Revit «Location Line»: type/instance param, ΟΧΙ coordinate hack). Το justification είναι **semantic πεδίο στα `params`** (`'center'|'left'|'right'`, relative στη φορά start→end), honored στο `buildBandFootprint` ως **κάθετο shift `sign·w/2`** του centerline κατά τον CCW normal. Πλεονεκτήματα vs extend: **follow-move-safe αυτόματα** (το param επιβιώνει του `{...params}` spread στο `deriveFoundationParamsFromGuides` — δεν χρειάζεται binding μηχανισμός)· **tracks width** (offset από τρέχον width στο geometry-time, όχι fixed mm)· **single-source**· **persist ΔΩΡΕΑΝ** (round-trips μέσα στο `params`, μηδέν Firestore αλλαγή)· δουλεύει & σε **χειροκίνητες** λωρίδες (όχι μόνο grid-hosted).
  - **Slice 5a — Justification mechanism (✅ DONE 2026-06-11 Opus):** NEW `StripJustification` type + `justification?` πεδίο σε `StripFootingParams`/`TieBeamParams` (`bim/types/foundation-types.ts`, optional → backward-compatible) + SSoT `JUSTIFICATION_NORMAL_SIGN` map (`{center:0,left:1,right:-1}`) + `DEFAULT_STRIP_JUSTIFICATION='center'`. Honored στο `buildBandFootprint` (`bim/geometry/foundation-geometry.ts`, κάθετη μετατόπιση centerline κατά `sign·hw` πριν το band· `center`→0→**identical footprint, zero regression**). Schema `StripJustificationSchema` + `.optional()` σε strip/tie-beam (`foundation.schemas.ts`, `.strict()`-safe). Override plumbing στο `FoundationParamOverrides` + `buildDefaultFoundationParams` line branch (`foundation-completion.ts`, conditional spread → Firestore-safe undefined). **Default `center` → καμία οπτική/συμπεριφορική αλλαγή.** 54 jest (geometry left/right/center + area/volume invariance + idempotent· derive follow-move-safe preserve· schema accept/reject) + 190/192 foundation regression (2 fails = pre-existing stale `foundation-preview-helpers.test.ts`, WYSIWYG refactor — ΟΧΙ δικά μου). ΕΚΤΟΣ ADR-040 (pure type/schema/geometry/builder — καμία scene-write/renderer/grip). Μηδέν persistence/rules αλλαγή.
  - **Slice 5a-control — UI set justification (✅ DONE 2026-06-11 Opus):** ribbon combobox 3 επιλογών (Κεντρικά/Αριστερά/Δεξιά — Revit «Location Line») στο **line-only panel** (strip/tie-beam· pad → anchor) που γεμίζει το ΗΔΗ-υπάρχον `FoundationParamOverrides.justification` (placement-time, drawing-mode branch) **ΚΑΙ** κάνει undoable mutation σε επιλεγμένη λωρίδα μέσω του υπάρχοντος `UpdateFoundationParamsCommand` (edit-time, selected-entity branch) — **ΕΝΑ combobox, mirror του `anchor`**. NEW `FOUNDATION_RIBBON_KEYS.stringParams.justification` + array entry (`foundation-command-keys.ts`)· `JUSTIFICATION_OPTIONS` + combobox μετά το thickness στο line panel (`contextual-foundation-tab.ts`)· bridge wiring και στα 4 σημεία (`useRibbonFoundationBridge.ts`). **Boy-Scout refactor:** εξαγωγή `readSelectedStringField`/`readToolStringField`/`nextParamsForStringChange`/`applyStringChangeToHandle` (τα `getComboboxState`/`onComboboxChange` ήταν ήδη στο όριο 40γρ· τώρα DRY + string-key-generic). i18n `foundationEditor.justification.*` (el/en ICU). 7 jest (drawing-mode default/write· selected strip write→param· selected pad → null/no-op) + 266 foundation regression PASS (2 fails = pre-existing stale `foundation-preview-helpers.test.ts`, ΟΧΙ δικά μου). ΕΚΤΟΣ ADR-040 (ribbon-data + bridge + i18n· καμία scene-write/renderer/grip). Μηδέν persistence/rules (round-trips μέσα στα `params`). **Default `center` → καμία ορατή αλλαγή· σε pad το combobox δεν εμφανίζεται.**
  - **Slice 5a-grid — Auto-inward περίμετρος + managed regeneration (✅ DONE 2026-06-11 Opus):** η «Εσχάρα από κάναβο» παράγει πλέον τις **περιμετρικές** λωρίδες **έκκεντρες προς τα μέσα** (εξωτερική παρειά ΠΑΝΩ στον άξονα → μηδέν overhang έξω από το περίγραμμα/όριο), **αντικαθιστώντας το corner-fill** (όταν 2 περιμετρικές σε γωνία αναπτύσσονται inward, η γωνία κλείνει φυσικά). **Πλήρης αυτοματοποίηση + χειροκίνητη υπεροχή (Revit managed regeneration), Giorgio «full enterprise»:** NEW pure SSoT `bim/foundations/foundation-grid-justification.ts` `gridStripJustification(orient, idx, count)` (περιμετρική→inward σήμανση μέσω CCW normal `buildBandFootprint`: V xi=0→right/last→left, H yi=0→left/last→right· εσωτερική→center). `foundation-from-grid.ts`: αφαιρέθηκε το corner-fill `extend` (coords+bindings καθαρά), per-strip justification override. **NEW `justificationManual?: boolean`** στα strip/tie-beam params (`foundation-types.ts`+`foundation.schemas.ts` `.strict()`): ο μηχανικός που αλλάζει έδραση μέσω 5a-control το θέτει `true` (`useRibbonFoundationBridge` selected-entity branch) → το managed reconcile ΔΕΝ το επαναφέρει. **Reconciler reflow** (`foundation-grid-reconcile.ts` +`toReJustify`): matched λωρίδες ίδιας topology με διαφορετική έδραση → αν **auto** (`!justificationManual`) ευθυγραμμίζονται στον κανόνα (self-heal όταν άξονας αλλάζει ρόλο περιμετρικός↔εσωτερικός, **κρατά id**, re-derived geometry)· αν **χειροκίνητη** → preserve. Το justification ΔΕΝ μπαίνει στο `gridStripSignature`. Orchestrator (`foundation-grid-commit.ts`): οι reflows = in-place updates → folded στο **υπάρχον `RehostFoundationsCommand`** (μηδέν νέο command)· result +`reJustified`· event/toast +`reJustified` (folded στο «reconciled»). **Boy-Scout:** ο rehost (6b) `adoptTarget` υιοθετεί πλέον & την έδραση κανόνα του target (έκλεισε ο περιορισμός «rehosted κρατά center»). **BOQ** geometry-driven (`safeUnion`) → καθαρός όγκος σωστός για inward (επαληθεύτηκε, 2×2 union 8.16 m²). ~9 αρχεία + tests: foundation-from-grid (inward+καθαρά bindings), foundation-grid-justification (νέο rule), foundation-grid-reconcile (reflow+manual-preserve), foundation-grid-commit (5a-grid self-heal μέτρα), foundation-grid-boq+foundation-preset (inward net-volume). 74 grid jest + 289 foundation/hosting regression (2 fails = pre-existing stale preview-helpers, ΟΧΙ δικά μου) + tsc. ΕΚΤΟΣ ADR-040 (pure builder/geometry/reconcile + ribbon-data· καμία scene-write/renderer/grip). Μηδέν firestore.rules (round-trips στα `params`). **Default: fresh κάναβος → inward περίμετρος αυτόματα· μηχανικός υπερισχύει per-strip & επιβιώνει αναγέννησης· axis-role-change → auto self-heal.**
  - **DEFER (επόμενες εγκρίσεις):** **5b** έκκεντρα pad μέσω `anchor` (υπάρχει)· **5c** strap/συνδετήριες ισορρόπησης (`tie-beam` kind· §8.6 ροπή εκκεντρότητας). Σχέση §8 (κεντρικά vs έκκεντρα + strap beams).

- **Slice 6 — Idempotent / reconciling «Εσχάρα από κάναβο» + migration ορφανών:** root cause (Giorgio 2026-06-11): παλιοί πεδιλοδοκοί δεν ακολουθούσαν τη μετακίνηση άξονα (legacy χωρίς `guideBindings`, προ-Slice-3) **και** κάθε «Εσχάρα από κάναβο» έκανε **τυφλή** παραγωγή (πλήρες νέο set → διπλοί). Revit/Tekla way = grid=SSoT, members hosted+follow, **generation idempotent/managed** (ξέρει τα existing, δεν διπλασιάζει· νέος άξονας → σκόπιμο re-run, ΟΧΙ μαγική auto-create).
  - **Slice 6 (full reconcile) — managed regeneration via signature-set diff (✅ DONE 2026-06-11 Opus):** το αρχικό 6a (exact-key skip / add-only) **αναθεωρήθηκε**: έλυνε το re-run ίδιου κανάβου αλλά **έσπαγε σε αλλαγή τοπολογίας** (Giorgio repro): (α) **ενδιάμεσος** οδηγός υποδιαιρεί bays → split λωρίδες με νέα keys → δημιουργούνται **πάνω** στις παλιές whole· (β) **εξωτερικός** οδηγός → παλιά περιμετρική γίνεται εσωτερική αλλά **κρατά** το corner-fill `extend` (προεξέχει). Κοινή ρίζα: incremental-add ≠ managed-reconcile. **ΛΥΣΗ = signature-set diff** (Revit/Tekla managed regeneration): NEW pure `gridStripSignature(strip) → string|null` = `segmentKey | rounded(start) | rounded(end)` (grid-ταυτότητα + γεωμετρία· το `extend` αλλάζει τα coords → ξεχωρίζει περιμετρική vs εσωτερική· μη-grid → `null`). NEW pure `foundation-grid-reconcile.ts` `reconcileGridStrips(target, existing) → {toCreate, toDelete, unchanged}`: target-sig ∉ existing → create· existing-sig ∉ target → delete· σε αμφότερα → αμετάβλητη (**κρατά id, μηδέν write**)· null-sig (legacy ορφανές/χειροκίνητες) **ΠΟΤΕ** delete. NEW `core/commands/entity-commands/DeleteFoundationsCommand.ts` = inverse του `CreateFoundationsCommand` (scene remove + deferred `bim:foundation-delete-requested`· undo = re-add + `drawing:entity-created` re-persist αρχικά ids). Orchestrator `commitFoundationGridFromGuides`: build full target → reconcile vs existing grid-managed → εκτελεί **ΕΝΑ** `CompoundCommand([Delete?, Create?])` (1 undo)· no-op → `reason:'up-to-date'`· result `{created, deleted, unchanged}`. **REVERT** η 6a προσθήκη `coveredKeys`/`skippedCount` (ο builder ξαναγίνεται full-target). UI: event `bim:foundations-from-grid {created, deleted}`· toast `built`/`reconciled`/`upToDate` (ICU el/en). 48 jest (13 segment+signature, 6 reconcile, 6 delete-cmd, 6 create-cmd, 11 builder, 5 orchestrator + reconcile scenarios) + tsc. ΕΚΤΟΣ ADR-040 (pure build/reconcile + commands· grid read event-time). **Αποτέλεσμα: split-safe + corner-fill-role-safe + idempotent· τέρμα οι διπλοί, η παλιά ακραία επανέρχεται στο centerline· 1 Ctrl+Z αναιρεί όλο το reconcile· minimal mutation, stable ids.**
  - **Slice 6b (re-host / migration legacy ορφανών) — Option A (✅ DONE 2026-06-11 Opus):** οι παλιοί πεδιλοδοκοί (προ-Slice-3, `guideBindings`=undefined → ορφανοί → δεν ακολουθούν) **ξανα-κρεμιούνται κατά γεωμετρία** στον τρέχοντα κάναβο, **ΧΩΡΙΣ διαγραφή**, μέσα στο ίδιο κουμπί «Εσχάρα από κάναβο» (πριν το reconcile). **Κλειδί SSoT:** ο rehosted **υιοθετεί τα bindings + start/end του αντίστοιχου target φατνώματος** (κρατά id+διατομή) → το `gridStripSignature` του γίνεται ταυτόσημο με του target → ο υπάρχων `reconcileGridStrips` τον βλέπει ως `unchanged` (**μηδέν διπλοί, μηδέν διαγραφή, μηδέν αλλαγή στη λογική Slice 6**). Matching κατά **grid-topology** (segmentKey από nearest-guide, όχι raw coords → δουλεύει & για περιμετρικές: το corner-fill overhang έρχεται δωρεάν με την υιοθέτηση target coords). NEW pure `bim/foundations/foundation-grid-rehost.ts` (`rehostOrphanStrips(orphans, target, xGuides, yGuides)` → `{original, rehosted}[]`· reuse `segmentKeyFromBindings`/`computeFoundationGeometry`/`hasGuideBindings`· **tol scale-aware** = `REHOST_MATCH_FRACTION 0.25 × τοπικό bay-spacing`, μάθημα Slice 6· deterministic πρώτος-ανά-segment). **Multi-bay (2026-06-11 follow-up):** χειροκίνητη λωρίδα σε όλο το ύψος ενός άξονα (>1 φάτνωμα) ΔΕΝ αγνοείται πλέον — η `orphanSegmentKey` ταιριάζει στο **πρώτο (χαμηλότερο) φάτνωμα** της διαδρομής (`firstBay` με sorted-index adjacency, NEW `nearestIndex`/`sortedByOffset`), ο ορφανός υιοθετεί εκείνο το ένα segment (κρατά id) και τα υπόλοιπα φατνώματα του άξονα τα δημιουργεί κανονικά ο reconciler (target ∉ existing → create) → πλήρης κάλυψη άξονα, **μηδέν διπλό** (Giorgio repro: νέος άξονας + χειροκίνητη πεδιλοδοκός σε όλο το ύψος → πριν αγνοούνταν + επικάλυψη με grid). NEW `core/commands/entity-commands/RehostFoundationsCommand.ts` (update-semantics: `updateEntity` merge bindings+params+geometry· undo→original· deferred `bim:entities-attached` persist, mirror Delete/CreateFoundationsCommand). **Persistence gap fix (ΚΡΙΣΙΜΟ):** `FoundationUpdateInput` + `updateFoundation` (`foundation-firestore-service.ts`) + `persist()` update-branch (`useFoundationPersistence.ts`) → **+`guideBindings`** (πριν δεν persist-άρανε σε update → re-host θα χανόταν μετά reload). **Καμία αλλαγή firestore.rules** (verified: update block χωρίς field-allowlist). Orchestrator `commitFoundationGridFromGuides`: compute rehosts → fold-in στο existing → reconcile → ΕΝΑ `CompoundCommand([Rehost?, Delete?, Create?])` (1 undo)· result +`rehosted`. UI: event `bim:foundations-from-grid` +`rehosted`· toast `foundationGrid.rehosted` (ICU el/en). 34 jest (6 rehost pure + 6 command + 3 commit scenarios + 1 firestore-service + 18 regression) + 394/395 foundation/hosting/command regression (1 fail = pre-existing `AssignWallTypeCommand`, ADR-412, ΟΧΙ δικό μου). ΕΚΤΟΣ ADR-040 (pure rehost + command + persistence· grid read event-time). **DEFER:** undo-after-reload bindings clear (in-session undo πλήρως σωστό)· corner-fill width edge (rehosted κρατά δικό του width, υιοθετεί target overhang)· ορφανός με άκρα εκτός tol από οδηγούς (freehand μη ευθυγραμμισμένος → μένει ελεύθερος, by design).
  - **DEFER:** **6c** = «Ενημέρωση εσχάρας» ξεχωριστή action με confirm (αν χρειαστεί reconcile χωρίς το auto-replace του main click).

- **Slice 7 — Live re-split/reflow στο follow-move (auto, χωρίς κουμπί) (✅ DONE 2026-06-12 Opus):** Giorgio (DB version 50, follow-move role-swap): μετά τη μετακίνηση άξονα οι λωρίδες ακολουθούσαν σε **συντεταγμένες** αλλά (α) η κάθετη που τη διέσχιζε νέο σταυρόδρομο **δεν έσπαγε** (επικάλυψη) και (β) οι περιμετρικές στον νέο ακραίο άξονα έμεναν **center** (προεξοχή ~50%) — το split-on-cross + reflow γινόταν **μόνο** με χειροκίνητο «Εσχάρα». Giorgio επέλεξε **(B) live κατά το drag + auto release** (Revit associative grid). **Αρχιτεκτονική (2 tiers, μηδέν αλλαγή στον ADR-040-critical `useHostingReconciler`):**
  - **Ταυτότητα — γιατί 2 μηχανισμοί σειριακά:** `reconcileHostedFoundations` (coordinate-follow) = **id-preserving** χωρίς τοπολογία· `commitFoundationGridFromGuides` (signature-reconcile) = τοπολογικά σωστό αλλά οι coords είναι ΜΕΣΑ στο `gridStripSignature` → σκέτη μετακίνηση θα γινόταν delete+create (νέα ids). **Λύση:** στο release τρέχει **πρώτα** το υπάρχον coordinate-follow (κρατά ids, γράφει νέες coords) και **μετά** (settle) αυτόματα το ίδιο managed reconcile με το κουμπί → existing coords == target coords → απλές μετακινήσεις `unchanged` (same id), **μόνο** τα γνήσια split → create/delete, τα role-changes → reJustify (same id, Step 4/5 διατηρούνται).
  - **Tier 2 (auto «Εσχάρα» στο drag-complete):** NEW `hooks/data/useGridGuideSettleEmitter.ts` (imperative guide-store subscribe, debounce 400ms, εκπέμπει `bim:grid-guides-settled` ΜΟΝΟ όταν το pure `gridOffsetSignature` του συνόλου ορατών offsets όντως αλλάξει ΚΑΙ `getDraggingGuideId()===null`· **baseline-first** → μηδέν emit στο αρχικό load· loop-free) mounted στο `HostingReconcilerHost`. NEW event `bim:grid-guides-settled` (`drawing-event-map.ts`). Consumer `useRibbonFoundationBridge`: extract `runFoundationGridCommit()` (SSoT, κοινό button+auto) + `emitFromGridToast()`· auto-listen **gated** σε «υπάρχει ήδη grid-managed εσχάρα» (`isFoundationEntity && hasGuideBindings` — **ΜΗΝ** auto-create στο αρχικό draw)· toast **μόνο** αν `created+deleted+reJustified+rehosted>0` (μηδέν spam).
  - **Tier 1 (split-aware live ghost):** όσο σύρεται οδηγός, το ghost δείχνει το **live target grid** (split + inward γωνίες) από τα ζωντανά offsets, αντί coordinate-follow only. NEW pure `bim/foundations/foundation-grid-ghost.ts` (`deriveGridFollowGhostFootprints` = thin wrapper πάνω στον SSoT `buildStripGridFromGuides` → footprints **pixel-identical** με το commit → seamless handoff, μηδέν flash). MOD `GuideFollowGhostOverlay.tsx` (grid-target ghost αν υπάρχει εσχάρα· coordinate-follow για non-grid hosted· **ADR-040-critical**, stage). **SSoT refactor (Boy-Scout):** NEW exports `enumerateGridStrips`/`gridAxesFromReader`/`GridStripSpec`/`GridAxes` στο `foundation-from-grid.ts` — κοινός segment enumerator builder+ghost, μηδέν duplication των loops/justification math.
  - 17 νέα jest (4 grid-ghost + 5 settle-signature + 2 enumerator + 4 bridge auto-listen + 2 ghost) + 72 foundation/grid regression + tsc. 🔴 browser-verify (drag άξονα → live split ghost + auto-reconcile στο release χωρίς κουμπί· απλή μετακίνηση → same-id, μηδέν toast) + commit. **DEFER:** χειροκίνητα-justified λωρίδες δείχνουν auto-έδραση στο ghost (commit διατηρεί manual, Step 5)· only-changed ghost subset (v1 = full target)· wall/column follow.

- **Slice 8 — Revit-grade auto-junction-join (γωνίες/κόμβοι για ΟΠΟΙΑΔΗΠΟΤΕ έδραση) (✅ DONE 2026-06-12 Opus):** Giorgio (screenshot 011341): άλλαξε «Έδραση Άξονα» εξωτερικής πεδιλοδοκού από inward (Δεξιά) σε **outward** (Αριστερά) → **κενά στις γωνίες** + οι γείτονες δεν προσαρμόζονται. Εντολή «όπως οι μεγάλοι/Revit, full enterprise + full SSoT». **Ρίζα:** οι λωρίδες έχουν άκρα ΑΚΡΙΒΩΣ στους άξονες (μηδέν extend)· η γωνία κλείνει σήμερα **μόνο** χάρη στην παραδοχή «περιμετρική=inward» (5a-grid)· outward → footprint έξω → η κάθετη φτάνει μόνο μέχρι τον άξονα → ακάλυπτο γωνιακό τετράγωνο.
  - **Revit απόφαση = miter / extend-to-join** μέσω του ΥΠΑΡΧΟΝΤΟΣ `GuideBinding.extend` (per-endpoint, follow-move-safe): κάθε άκρο λωρίδας που είναι **terminus** (γραμμή τελειώνει — όχι εσωτερικός κόμβος με collinear συνέχεια) επεκτείνεται κατά μήκος του άξονά της μέχρι την **ΑΠΕΝΑΝΤΙ (μακρινή) παρειά** των κάθετων λωρίδων στον κόμβο (clamp ≥0). **Inward → far face = ο άξονας → extend=0 → ZERO regression.** Outward → η κάθετη επεκτείνεται όσο χρειάζεται → γωνία κλείνει, μηδέν νέο overhang. Justification-agnostic (διαβάζει την πραγματική παρειά από `geometry.bbox`).
  - **SSoT downstream**: το miter ζει στο `geometry.footprint` (μέσω extend → `deriveFoundationParamsFromGuides` → `computeFoundationGeometry`) → **render 2D + 3D + BOQ αυτόματα σωστά** (μηδέν αλλαγή σε FoundationRenderer/foundation-to-three/foundation-grid-boq)· follow-move-safe + persist-free (bindings round-trip).
  - **NEW pure** `bim/foundations/foundation-grid-junctions.ts` `computeGridJunctionExtends(strips) → RehostedStrip[]` (terminus-gated miter· idempotent· reuse RehostedStrip/extend/geometry). **Trigger 1** (`foundation-grid-commit.ts`): post-reconcile junction pass folded στο ΙΔΙΟ CompoundCommand (fresh «Εσχάρα» + Slice 7 follow-move). **Trigger 2** (`useRibbonFoundationBridge.ts` `dispatchParams`): αλλαγή έδρασης/πλάτους grid λωρίδας → ανα-υπολογισμός junctions → **οι γείτονες ενημερώνονται live** ως `CompoundCommand([Update, Rehost])` (1 undo) = το «οι γωνίες κλείνουν μόνες τους» που έλειπε.
  - **`gridStripSignature` → bare coords** (`foundation-grid-segments.ts`): αφαιρεί το junction `extend` πριν το rounding → το miter δεν αλλάζει την ταυτότητα reconcile (όπως ήδη το justification) → fresh target (μηδέν extend) ταιριάζει existing (με miter) → μηδέν spurious delete/create. (Το extend δεν είναι πια corner-fill identity — 5a-grid το αντικατέστησε με justification.)
  - 12 νέα jest (5 junctions zero-regression/outward/idempotent + 2 bridge live-recompute + regression) + 115 foundation/grid total + tsc. ΕΚΤΟΣ ADR-040 (pure geometry/reconcile/bridge· render αμετάβλητο). 🔴 browser-verify (εξωτερική→outward→γωνία κλείνει live· →inward→μαζεύεται· fresh inward→καμία αλλαγή) + commit. **DEFER:** edited strip own-miter (σπάνιο· το «Εσχάρα»/follow-move το διορθώνει)· μεταβλητό πάχος miter.

**Hook point (επιβεβαιωμένο):** `guide-store.ts` `moveGuideById`/`moveDiagonalGuideById` → `notify()`· `subscribe()`+`getVersion()` καλύπτουν όλα τα move paths.

## 9. Changelog

| Date | Author | Change |
|------|--------|--------|
| 2026-06-14 | Opus | **Soft-warning ασυνεπούς έδρασης «από κάναβο» (Revit-style, μη-blocking).** Σενάριο Giorgio: κολόνες inner → δοκάρια center → τοίχοι outer στον ΙΔΙΟ κάναβο → οι παρειές κολόνας/τοίχου δεν ευθυγραμμίζονται. Απόφαση: **ΟΧΙ hard block** (legit μικτές περιπτώσεις υπάρχουν, π.χ. κολόνα center + τοίχος finish-face)· **ΟΧΙ single-mode** (χάνει ανά-τύπο ευελιξία)· **ΝΑΙ soft warning** μόνο σε ΠΡΑΓΜΑΤΙΚΗ ασυνέπεια (mirror `bim:foundation-on-upper-storey`). NEW `bim/grid/grid-justification-consistency.ts → detectGridJustificationConflicts` (pure): perpendicular offset sign ανά άξονα — γραμμικά από `extend` στα shared-axis bindings, κολόνα από `anchor` (−sign(dx/dy))· conflict = ίδιος guideId με `+1` ΚΑΙ `−1`. **Κριτήριο = αντίθετη φορά, ΟΧΙ απλώς διαφορετικό mode**: col-inner+beam-center ΔΕΝ τριγκάρει (center=0)· col-inner+wall-outer τριγκάρει. `warnIfGridJustificationConflict(scene)` καλείται από τα 3 bridges μετά το commit → EventBus `bim:grid-justification-conflict` → toast.warning (i18n `gridJustification.conflict` el+en). 7 jest (end-to-end μέσω builders). ΕΚΤΟΣ ADR-040. 🔴 browser-verify + commit. |
| 2026-06-14 | Opus | **3-mode justification «Εσχάρα από κάναβο» γενικεύτηκε σε ΚΟΛΟΝΕΣ + ΔΟΚΑΡΙΑ + ΤΟΙΧΟΥΣ (mirror πεδιλοδοκών, FULL SSoT).** Ο `gridStripJustification` κανόνας (περιμετρικές inner/outer, εσωτερικές πάντα center) είναι πλέον η ΕΝΙΑΙΑ πηγή για ΟΛΑ τα grid-managed δομικά. **Αρχιτεκτονική:** κάθε οντότητα υλοποιεί τον κανόνα με τον **φυσικό της μηχανισμό** (όπως ήδη foundation=param vs wall=extend): **γραμμικά (δοκάρι/τοίχος) = extend-baked** (NEW `bim/grid/grid-segment-justification.ts → justifyGridSegment`: κάθετη μετατόπιση start/end κατά ±width/2 + `extend` mm στα perpendicular bindings· follow-move-safe μέσω `deriveLineSlots` [coord=axisOffset+extend·scale], μηδέν αλλαγή geometry/type)· **κολόνα (σημειακό) = anchor** (NEW `bim/grid/grid-column-justification.ts → gridColumnJustification`: σύνθεση `gridStripJustification` ×2 [X&Y] → 9-position `ColumnAnchor`· anchor ήδη εφαρμόζεται από geometry + επιβιώνει follow-move → ΚΑΙ width-edit-safe). **Slice 0 relocate (Boy Scout):** `GridPerimeterMode`/`gridStripJustification`/`DEFAULT_GRID_PERIMETER_MODE`/`GridStripOrientation` → neutral `bim/grid/grid-justification.ts` (foundation module = re-export, back-compat)· `canonicalAxisNormal` extract → `bim/grid/axis-normal.ts` (shared). **UI:** factory `createGridPerimeterModeStore` (`bim/grid/`) → 4 instances (foundation/beam/wall/column)· structural-tab τα 3 «από κάναβο» (wall/col/beam) έγιναν split-buttons με 3 variants (Inner/Center/Outer, mirror foundation)· +6 action keys (`*.fromGridCenter/Outer`)· 3 bridges `handle*FromGrid(mode)` set store + commit· i18n 9 keys el+en. **Default = `inner`** για col/beam/wall (πριν: center-only· τώρα consistent με foundations — το ζητούμενο «ΤΟ ΙΔΙΟ»)· commit/builder threading `perimeterMode`. **Idempotency ανέπαφη** (segment/intersection keys = binding guideIds, ΟΧΙ coords/extend). **Γνωστό DEFER gap (γραμμικά extend-baked):** αλλαγή πλάτους *μετά* τη γένεση δεν ξανα-κλιμακώνει το frozen extend → ξανα-τρέξε εσχάρα (κολόνες δεν έχουν το gap). **Tests:** NEW grid-segment-justification (8) + grid-column-justification (9-case mapping)· beam/wall tests: centered-contract → explicit `'center'` + νέα inner/outer κάλυψη· structural-tab updated (4 splits) + Boy-Scout fix slab grid keys στο EXPECTED. 188+ foundation/geometry/bridge regression GREEN. ΕΚΤΟΣ ADR-040. ΜΑΘΗΜΑ: το follow-move offset ΔΕΝ θέλει νέο param σε γραμμικά — ο υπάρχων `extend` μηχανισμός (Finish-Face wall) το καλύπτει· point entity = anchor (ήδη width-edit-safe). 🔴 tsc (PowerShell denied → Giorgio) + browser-verify (3 modes live στη σκηνή §3) + commit. |
| 2026-06-13 | Opus | **Grid-guides persistence flush-on-ready fix (οδηγοί που μπαίνουν πριν ετοιμαστεί το floor scope δεν χάνονται πλέον στο reload).** Σταδιακός read-only έλεγχος αυτόματης δημιουργίας σε άδεια βάση (Giorgio: τοποθέτησε 4 οδηγούς → φάνηκαν στον κάμβο → hard-refresh → **χάθηκαν**· δεύτερη φορά → persistάρησαν `grd_e11944a1…`, 4 guides, scope `flr_4e7868ba…`). Root cause (`useGridGuidePersistence.ts`): το `serviceRef` είναι `null` ώσπου να γίνει έτοιμο **ολόκληρο** το scope (`companyId/projectId/floorplanId/userId`)· το `floorplanId` = `levelManager.fileRecordId` λύνεται **αργά** (αφού δέσει το DXF), ενώ το `floorId` είναι έτοιμο νωρίς. Δύο συνδεδεμένα κενά: **(Α)** ο debounced `doSave` έκανε silent `return` σε null service → οι οδηγοί έμεναν στον κάμβο αλλά **δεν γράφονταν ποτέ**. **(Β)** το subscribe effect εξαρτιόταν **μόνο** από `scopeKey` (= `floorId`-based, έτοιμο νωρίς) → δεν ξανατρέχει όταν αργήσει το `floorplanId` → ούτε subscription/save ποτέ. **FIX:** NEW `serviceReady` state (reactive mirror του `serviceRef`, set στο instantiation effect) → προστέθηκε στα deps του subscribe effect ώστε να ξανατρέχει όταν δέσει αργά το scope· + flush-on-ready στο subscribe callback (capture pending οδηγών του store **πριν** το per-floor reset· αν το remote doc είναι κενό → `restoreSnapshotsToStore` once → ο debounced save τα persistάρει τώρα που το service είναι έτοιμο· `pendingSettled` guard κατά διπλο-flush· remote-wins αν υπάρχει doc). Boy-Scout: εξαγωγή `restoreSnapshotsToStore` SSoT (DRY με το `hydrate`). Anti-echo `lastSavedSigRef` ανέγγιχτο. Idempotent, zero-regression στο happy path (scope έτοιμο πριν την τοποθέτηση). IDE diagnostics καθαρά (1 αρχείο). **4 jest** (NEW `useGridGuidePersistence.test.tsx` — renderHook + fake timers + mock store/service, ΠΡΑΓΜΑΤΙΚΑ `guidesToSnapshots`/`snapshotToGuide`: flush-on-ready empty-remote→create· remote-wins→hydrate χωρίς create· no-empty-doc· happy-path normal save) PASS. ΕΚΤΟΣ ADR-040. 🔴 browser-verify (βάλε οδηγούς ΑΜΕΣΩΣ με το άνοιγμα της κάτοψης, πριν δέσει το scene → hard-refresh → επιβιώνουν) + commit (Giorgio· shared tree· git add ΜΟΝΟ δικά μου). |
| 2026-06-13 | Opus | **Storey-aware beams + framing-based column→beam attach (πλαίσιο «από κάναβο» κλείνει Revit-grade).** Σταδιακός read-only έλεγχος δημιουργίας (όροφος `height:5m`) αποκάλυψε 2 συνδεδεμένα κενά: **(Α)** τα δοκάρια «από κάναβο» γεννιόνταν με `topElevation=3000` (legacy `DEFAULT_BEAM_TOP_ELEVATION_MM`) αγνοώντας το storey height — το `buildDefaultBeamParams` δεν είχε μπει στο ADR-448 Φ2 storey seam. **(Β)** 0 κολώνες έκαναν attach στα δοκάρια: το reverse-attach τρέχει, αλλά τα δοκάρια **frame-into** (`trimSegmentEndpointsToColumns` τα κόβει στην παρειά) → footprint δεν ΚΑΛΥΠΤΕΙ την κολώνα → το slab-style `hostCoversColumn` + per-corner soffit resolver αδρανή. **FIX (FULL SSoT):** **(Α)** `beam-completion.buildDefaultBeamParams` → `topElevation = resolveStoreyCeilingElevationMm(override, DEFAULT)` (REUSE ADR-448 Φ2 resolver· top-of-beam = floor-relative storey ceiling· πιάνει manual+grid+sloped· safe-by-construction fallback 3000). **(Β1)** NEW `findColumnsFramedByBeam` (`column-structural-attach-coordinator`): κολώνα framed όταν το κέντρο της κάθεται στον άξονα δοκαριού (perp≈0) εντός span+support — REUSE exported `columnSupportAlong` (`column-face-trim`, ΙΔΙΟ κριτήριο με το frame-into trim)· Z-gate beam-top > max(base,FFL). **(Β2)** `useStructuralAutoAttach.attachEntitiesUnderHost`: ένωση (dedup) covering+framing κολωνών → `AttachColumnsCommand('top')` (υπάρχον· `topBinding='attached'`+`attachTopToIds`). **(Β3)** `resolveColumnTopProfile` NEW framing branch (`classifyTopHosts`): attached host που δεν καλύπτει γωνία → η κορυφή ανεβαίνει στο beam-top (flat, `topsideZmm`, max αν πολλά framing)· covering πλάκες κλιπάρουν per-corner κάτω (ΑΜΕΤΑΒΛΗΤΟ). Associativity δωρεάν (live resync, μηδέν persisted cascade). Render lower-envelope (slab) byte-for-byte ανέπαφο. 10 νέα jest (beam-storey ×3· findColumnsFramedByBeam ×7· framing top ×4) + 42 regression PASS, IDE clean (5 files). ΕΚΤΟΣ ADR-040. cross-ref ADR-401 §F (column attach), ADR-448 Φ2/Φ4b (storey seam + cascade). 🔴 browser-verify (δοκάρια top=storey ceiling· κολώνες attached σε framing beams· πλαίσιο κλείνει· beam/storey move→κολώνες ακολουθούν) + commit (Giorgio· shared tree· git add ΜΟΝΟ δικά μου). **DEFER:** drop-beam soffit cut· per-column footing-aware framing. |
| 2026-06-12 | Opus | **GEN-COL static continuity — κολώνες από κάναβο φτάνουν στη θεμελίωση (Giorgio browser-feedback «θέλω στατική συνέχεια — οι κολώνες να φτάνουν στη θεμελίωση»).** Οι κολώνες ισογείου ήταν [0,3000] (base storey-floor)· σε πραγματικό πλαίσιο Ο/Σ είναι **συνεχείς** μέχρι το footing. FIX: το `buildColumnGridFromGuides` +optional `foundationBaseLevelMm` → όταν υπάρχουν footings στη σκηνή, η κολώνα κατεβάζει τη βάση εκεί (`baseOffset = foundationTop`) και **επιμηκύνεται ισόποσα** ώστε η κορυφή να μείνει στην οροφή ορόφου (`top = base + height` → `height += baseDrop`· π.χ. base −1000, height 3000→4000, top μένει 3000). **SSoT:** NEW `bim/foundations/foundation-level.ts` `sceneFoundationTopMm(entities)` = min `topElevationMm` των strip/pad (συνδετήριες tie-beam εξαιρούνται — κάθονται ψηλότερα, δεν είναι το footing έδρασης)· **reuse ΚΑΙ από την εδαφόπλακα** (`deriveFoundationMatLevelMm` → delegate, μηδέν duplication). `ColumnParamOverrides` +`baseOffset?` (το `buildDefaultColumnParams` το διοχετεύει· default 0 = zero regression). `column-grid-commit` διαβάζει scene footings → περνά στο builder. v1: global min footing top για ΟΛΕΣ τις grid κολώνες (per-column footing detection = DEFER). create-only → υπάρχουσες κολώνες θέλουν delete+regenerate. 12 jest (foundation-level ×4 + column-from-grid foundation-base ×4 + regression) PASS, IDE type-clean. ΕΚΤΟΣ ADR-040. 🔴 Giorgio: delete 9 κολώνες + re-run «Κολώνες από κάναβο» → [−1000,3000] + commit (shared tree· git add ΜΟΝΟ δικά μου). cross-ref ADR-401 (column base constraint). |
| 2026-06-12 | Opus | **Slice GEN-SLAB DONE — «Πλάκες από κάναβο» (εδαφόπλακα + δάπεδα + οροφές, `SlabEntity`).** Πρώτη **area-hosted** οικογένεια «από κάναβο» (οι προηγούμενες: γραμμικές strip/wall/beam/tie + σημειακές column). Τρία υπο-εργαλεία με κλειδωμένες Revit-grade αποφάσεις (Giorgio): **(1) Εδαφόπλακα** = ΕΝΑ ενιαίο `SlabEntity kind='foundation'` ανά building component (slab-on-grade, ΔΕΝ υποδιαιρείται από τον κάναβο)· outline = `computeBuildingFootprint(walls,columns,beams).outerRings` (REUSE ADR-396 boolean-union footprint). **(2) Δάπεδο** = ΠΟΛΛΑ `kind='floor'`, ΕΝΑ ανά φάτνωμα, clipped στις εσωτερικές παρειές των δοκαριών + notched γύρω από κολώνες/τοιχία. **(3) Οροφή** = ίδιο με `kind='roof'`. **Η ΜΟΝΗ αρχιτεκτονική διαφορά από τα δοκάρια:** οι πλάκες δένουν σε **φάτνωμα (2D cell)** όχι ακμή → NEW `enumerateGridBays` (mirror `enumerateGridStrips`, `(nX-1)·(nY-1)` cells, 4-axis bindings start-x=αριστερά/end-x=δεξιά/start-y=κάτω/end-y=πάνω) + NEW `bayKeyFromBindings` (idempotent cell key, διακριτοί 4 άξονες· `segmentKeyFromBindings` επιστρέφει null για bays → μηδέν collision). **Generation (NEW `bim/slabs/slab-from-grid.ts`):** `buildFoundationMatSlabs` (footprint→ενιαία, holes=DEFER αφού `Polygon3D` δεν κρατά τρύπες — αίθρια→slab-openings)· `buildSlabBaysFromGuides` (ανά bay: rect → **`safeDifference(rect, ...beamFootprints, ...columnFootprints)`** = το ΚΛΕΙΔΙ· τα δοκάρια straddle τον άξονα → η αφαίρεση κόβει στην εσωτερική παρειά· οι κολώνες/shear-wall notch-άρουν· οι κανονικοί τοίχοι ΔΕΝ κόβουν, κάθονται πάνω στην πλάκα — Revit-grade· strip closing-duplicate vertex αλλιώς slab validator reject· largest outer ring=μία πλάκα, διάσπαση/holes=DEFER slab-openings). **Hosting (NEW `bim/hosting/slab-hosting-strategy.ts` + `deriveRectBaySlots` στο `derive-slots.ts` + `slab: slabHostingStrategy` στο `STRATEGIES`):** area-deriver ξαναχτίζει το ΟΡΘΟΓΩΝΙΟ outline από 4 άξονες (χρειάζεται ΟΛΟΥΣ — η πλάκα κινείται ως σύνολο)· v1 follow-move = ως ορθογώνιο (live re-clip κατά το drag χρειάζεται τα μετακινούμενα δοκάρια, μη διαθέσιμα στο pure reconcile → **DEFER regenerate**)· ο reconciler αγγίζει μόνο πλάκες του μετακινούμενου άξονα → οι υπόλοιπες κρατούν το clip. **Command (NEW `CreateSlabsCommand`):** batch, deferred-microtask `drawing:entity-created` tool='slab' (το ακούει `useSlabPersistence` γρ.399), undo→`bim:slab-delete-requested`· mirror `CreateBeamsCommand`. **Orchestrator (NEW `bim/slabs/slab-grid-commit.ts`):** `commitFoundationMatFromGuides` (idempotent: υπάρχει `kind='foundation'`→up-to-date) + `commitSlabBaysFromGuides(kind)` (idempotent skip ανά `bayKeyFromBindings`, floor & roof συνυπάρχουν στο ίδιο φάτνωμα→όχι cross-skip). **Persistence guideBindings round-trip (αλλιώς floor/roof bays χάνουν hosting μετά reload — ίδιο gap με foundation):** `slab-firestore-service` +`guideBindings?` σε `SlabDoc`/`SlabSaveInput`/`SlabUpdateInput` + write `saveSlab`/`updateSlab` + pass-through `entityToSaveInput`· `slab-persistence-helpers.docToEntity` re-hydrate. **Per-bay πάχος (Q3):** δωρεάν — κάθε φάτνωμα=ξεχωριστή οντότητα, ο στατικός αλλάζει `thickness` ανά πλάκα από το ΥΠΑΡΧΟΝ slab properties bridge (reconcile/idempotent διατηρούν per-bay thickness). **Wiring:** `slab-command-keys` +3 actions (`fromGridMat`/`Floor`/`Roof`, το `isSlabActionKey` τα πιάνει μέσω `Object.values`)· `useRibbonSlabBridge` +2 handlers (mat + bays-kind) + onAction (πριν `resolveSlab` — δεν θέλουν επιλογή) + toast emitter· `structural-tab` +3 `actionBtn` (panel `structural-floors`)· `drawing-event-map` +2 events· `useDxfViewerNotifications` +2 toasts (ICU)· i18n el/en (`slabGrid.*` + 3 labels). **SSoT:** `guideBindings` ήδη στο `BimEntity` base (`bim-base.ts:151`) → `SlabEntity` το κληρονομεί. 24 jest (slab-from-grid ×11 [MAT 5 + FLOOR clip/notch 6] + slab-grid-commit idempotent ×8 + slab-hosting-strategy ×5) + 304/305 hosting/foundations/beams regression (1 pre-existing beam-grips rotation fail, shared-tree, όχι δικό μου) PASS· IDE type-diagnostics καθαρά σε ΟΛΑ τα touched (N.17 — δεύτερος tsc έτρεχε, χρήση language server αντί νέου process). **DB baseline (read-only):** `floorplan_slabs`=0 → μετά «Εδαφόπλακα» αναμένεται 1 (`kind:'foundation'`)· μετά «Δάπεδα» 4 (`kind:'floor'`, 4-axis guideBindings, clipped). ΕΚΤΟΣ ADR-040 (pure builders/commands/ribbon). cross-ref ADR-436 (slab reuse εδαφόπλακα), ADR-369 (slab elevation top-face FFL), ADR-401 (auto-attach: πλάκα-οροφής→host κορυφών τοίχων δωρεάν). 🔴 browser-verify (3 κουμπιά → εδαφόπλακα ενιαία/δάπεδα ανά φάτνωμα κομμένα στα δοκάρια+notch κολώνες/οροφές· reload→bays ακολουθούν· re-run→idempotent) + commit (Giorgio· shared tree icon-agent structural-tab· git add ΜΟΝΟ δικά μου). **ELEVATION FIX (Giorgio browser-feedback «η εδαφόπλακα & τα δάπεδα σχεδιάζονται στο ίδιο υψόμετρο»):** το default `SLAB_KIND_DEFAULT_LEVEL_ELEVATION_MM['foundation']=0` έκανε την εδαφόπλακα coplanar με τα δάπεδα ισογείου (top 0 → [−200,0] και τα δύο). Δομικά λάθος: η εδαφόπλακα=**γενική κοιτόστρωση στη ΣΤΑΘΜΗ ΘΕΜΕΛΙΩΣΗΣ**, όχι δάπεδο ισογείου. FIX (SSoT, ΟΧΙ hardcode): NEW `deriveFoundationMatLevelMm(entities)` στο `slab-grid-commit` → `levelElevation` της εδαφόπλακας = **min `topElevationMm` των footings (strip/pad) της σκηνής** (συνδετήριες tie-beam εξαιρούνται, κάθονται ψηλότερα)· fallback SSoT `defaultFoundationTopElevationMm('strip')` (−1000). Έτσι προσαρμόζεται αυτόματα σε custom βάθος θεμελίωσης· override μέσω `deps.overrides.levelElevation` υπερισχύει. DB-verified: strips top −1000 → εδαφόπλακα [−1200,−1000] (στη θεμελίωση), συνδετήριες [−1000,−500], δάπεδα [−200,0] → καθαρός κατακόρυφος stack. +5 jest (builder override + derive ×4). **AUTO-ATTACH FIX (Giorgio browser-feedback «η εδαφόπλακα τράβηξε τις βάσεις τοίχων ΚΑΙ κολωνών πάνω της»):** μόλις η εδαφόπλακα μπήκε στο −1000 (κάτω από τις βάσεις@0), πέρασε το inverted Z-gate του ADR-401 base auto-attach (`findWallsToAutoAttachBaseToHost` + column/stair analogues: host topside < wall base → attach) → τράβηξε ΟΛΑ τα storey-floor στοιχεία κάτω. Δομικά λάθος: η εδαφόπλακα-θεμελίωσης (raft/κοιτόστρωση) ΔΕΝ είναι auto-attach host (attach σε αυτήν = explicit, Revit-grade). FIX 2 σημεία: (α) `useStructuralAutoAttach` skip `attachEntitiesUnderHost` όταν created=`kind='foundation'` slab → καλύπτει FORWARD για τοίχους+κολώνες+σκάλες με ΕΝΑ gate· (β) `wall-structural-attach-coordinator.baseHostInputFor` εξαιρεί `kind='foundation'` → καλύπτει REVERSE (τοίχος-μετά-εδαφόπλακα). +1 jest (foundation raft → μηδέν base attach). Recovery (ήδη-attached walls/columns): undo πριν reload, ή delete εδαφόπλακα→resolver neutralize→storey-floor→re-run με fixed code. **GROUND-SLAB CORRECTION (Giorgio «η εδαφόπλακα ΠΑΤΑΕΙ στο έδαφος — όχι στη στάθμη θεμελίωσης» + «FULL ENTERPRISE+SSoT όπως Revit»):** το ELEVATION FIX (εδαφόπλακα@−1000) ήταν over-correction — το −1000 είναι radier/θεμελιόπλακα, ΟΧΙ «εδαφόπλακα/δάπεδο επί εδάφους». Revit-grade: «Εδαφόπλακα» = **ground-bearing slab `kind='ground'`** (όχι `foundation`), άνω παρειά στο **FFL (0)**, με **SSoT layered build-up** `getDefaultSlabBuildupForKind('ground')` (= ΥΠΑΡΧΟΝ `createDefaultGroundBuildup`: tile+screed+XPS επίστρωση → RC core → στεγάνωση+blinding που πατά στο μπάζωμα, 405mm) → το **φέρον σκυρόδεμα κάθεται κάτω από το FFL** (η επίστρωση το φέρνει στο 0) **χωρίς magic offset** — οι στρώσεις (SSoT) το ορίζουν. `buildFoundationMatSlabs`→rename `buildGroundBearingSlabs` (kind='ground'+ground DNA). Αναιρέθηκε ΟΛΟ το `deriveFoundationMatLevelMm` (−1000 derive) — `ground` default 0. Idempotency check `kind='ground'`. **Το `sceneFoundationTopMm` ΠΑΡΑΜΕΝΕΙ** (το χρησιμοποιεί η GEN-COL continuity, μόνο η slab usage αναιρέθηκε). +1 jest (ground DNA), 35 jest PASS. Συνέπεια: αποτύπωμα geometry — εδαφόπλακα [−405,0] κάθεται στο μπάζωμα πάνω από συνδετήριες [−1000,−500]· οι «Πλάκες δαπέδου» (`kind='floor'`) = αναρτημένοι/άνω όροφοι. **TOP-HOST FIX (Giorgio «η εδαφόπλακα ΑΛΛΟΙΩΣΕ την κορυφή των κολωνών»):** συνέπεια GEN-COL continuity — οι κολώνες φτάνουν στο −1000, οπότε ΚΑΘΕ floor-type πλάκα (ground/floor, underside −405/−200 > base −1000) περνούσε το ADR-401 top Z-gate (`undersideZmm > baseZmm`) → η κορυφή κολώνας/τοίχου attach-άρε στην πλάκα-δάπεδο (την νόμιζε ταβάνι). FIX (principled, και τους 2 coordinators `column`+`wall`): το top-host gate συγκρίνει με τη **ΣΤΑΘΜΗ ΙΣΟΓΕΙΟΥ** `Math.max(baseZmm, ACTIVE_LEVEL_FLOOR_MM)` αντί μόνο `baseZmm` → δοκάρια/οροφές (underside>FFL)=top hosts· εδαφόπλακα/δάπεδα (underside<FFL) ΟΧΙ· podium-safe + multi-storey-safe. `findColumnsToAutoAttachToHost`+`findWallsToAutoAttachToHost`+`findHostsToAttachWallTop`. +4 jest, 52/52 coordinator PASS. **Other (αναμενόμενα, ΟΧΙ bugs):** «πάνω από συνδετήρες»=slab-on-grade στο μπάζωμα· «διαπέρασε κολώνες»=συνεχείς κολώνες περνούν ΜΕΣΑ από το δάπεδο (Revit-correct· slab notch interior columns=DEFER). DB-verified συνδετήριες [−1000,−500]/πεδιλοδοκοί [−1400,−1000]. **DEFER:** holes/αίθρια→slab-openings· live re-clip κατά το axis-drag· μη-ορθογώνια/διαγώνια φατνώματα· sloped roof· generation-time per-bay thickness UI. |
| 2026-06-12 | Opus | **Slice GEN-BEAM DONE — «Δοκάρια από κάναβο» (structural `beam`, born-bound, idempotent create-only).** Τέταρτη γραμμική οικογένεια «από κάναβο» μετά πεδιλοδοκούς/τοίχους/συνδετήριες — η πρώτη **εκτός** `FoundationEntity`. **Το κρίσιμο κενό που λύθηκε πρώτα:** το beam hosting (follow-move) ΔΕΝ υπήρχε. NEW `bim/hosting/beam-hosting-strategy.ts` = ακριβές mirror του `wallHostingStrategy` (line slots → `deriveLineSlots`· `computeBeamGeometry`· `validateBeamParams`· outline από beam footprint) με τη μόνη διαφορά ότι ο `BeamParams` κρατά τον άξονα ως `startPoint`/`endPoint` (Point3D) → write-back **διατηρεί το z** (`{ ...startPoint, x, y }`)· `beam: beamHostingStrategy` στο `STRATEGIES`. **Persistence guideBindings round-trip (αλλιώς ο born-bound δοκός χάνει τα bindings μετά reload → δεν ακολουθεί):** `beam-firestore-service` +`guideBindings?` σε `BeamDoc`/`BeamSaveInput`/`BeamUpdateInput` + write σε `saveBeam`/`updateBeam` + pass-through στο `entityToSaveInput`· `useBeamPersistence` restore στο `docToEntity` + round-trip στο `updateBeam` path (mirror wall/column `f992df62`). **Generation:** NEW `bim/beams/beam-from-grid.ts` (`buildBeamGridFromGuides` — **REUSE αυτούσιο** `enumerateGridStrips`/`gridAxesFromReader`· μία born-bound δοκός ανά segment άξονα, centerline ΠΑΝΩ στον άξονα μηδέν extend, `completeBeamFromTwoClicks` builder SSoT) + `beam-grid-commit.ts` (create-only orchestrator, idempotent skip ανά **`segmentKeyFromBindings`** — γενικό SSoT key, coordinate-free) + NEW `core/commands/entity-commands/CreateBeamsCommand.ts` (deferred-microtask `drawing:entity-created` tool='beam', undo→`bim:beam-delete-requested`· mirror `CreateWallsCommand`). **Σχεδιαστική απόφαση (Revit-grade, εγκρίθηκε Giorgio):** v1 = create-only **ΧΩΡΙΣ auto-miter** στις γωνίες (parity GEN-WALL). beam-join (corner-miter) = **DEFER**. Follow-move δωρεάν μέσω hosting strategy. **+FRAME-INTO κολωνών (Giorgio browser-feedback «τα δοκάρια φτάνουν στα ΚΕΝΤΡΑ των κολωνών, όχι στις παρειές»):** στη Revit τα δοκάρια καρφώνουν στην **παρειά** της στήριξης (clear span). FIX = **γενίκευση SSoT**: το `wall-column-trim.ts` (GEN-WALL) ήταν ήδη kind-agnostic → μετακινήθηκε σε `bim/columns/column-face-trim.ts` (`trimSegmentEndpointsToColumns`), ο τοίχος έγινε thin re-export shim (byte-identical, μηδέν αλλαγή wall path/tests), και ο `beam-from-grid` το καλεί: `buildBeamGridFromGuides` +optional `columns`, `beam-grid-commit` διαβάζει `isColumnEntity` της σκηνής. Άκρο σε κολώνα → `GuideBinding.extend = ±column-support-along-beam` → δοκός σταματά στην παρειά (follow-move-safe· `segmentKeyFromBindings` αγνοεί extend → idempotency ανέπαφη). **Migration gap (create-only):** προϋπάρχουσες untrimmed δοκοί δεν αυτο-διορθώνονται σε re-run (idempotent skip) → delete+re-create (όπως walls· full reconcile=DEFER). +2 jest frame-into (with/without columns). **Wiring:** `beam-command-keys` +`fromGrid` action (το `isBeamActionKey` το αναγνωρίζει αυτόματα μέσω `Object.values`)· `useRibbonBeamBridge` +`handleBeamsFromGrid`+onAction+toast emitter· `structural-tab` +1 `actionBtn` (panel `structural-beams`, icon `struct-beam-from-grid`)· `drawing-event-map` +2 events (`bim:beams-from-grid`/`-failed`)· `useDxfViewerNotifications` +2 toasts (ICU)· i18n el/en (`beamGrid.*` + label `beamsFromGrid`). **ΣΗΜΕΙΩΣΗ SSoT:** το `guideBindings` υπάρχει ήδη στο `BimEntity` base (`bim-base.ts:151`) → ο `BeamEntity` το κληρονομεί, μηδέν αλλαγή στα beam-types. 34 jest (beam-from-grid ×9 [+2 frame-into] + beam-grid-commit idempotent ×4 + beam-hosting-strategy ×4 + wall-from-grid/wall-grid-commit regression μέσω shim + structural-tab) PASS. **DB-verified (read-only baseline→generate):** κάναβος 3×3 → 9 columns (center-x/y) + 12 beams (4 bindings, scope `flr_161aa890…`, born-bound persisted). ΕΚΤΟΣ ADR-040 (pure builders/commands/ribbon). 🔴 browser-verify (κουμπί «Δοκάρια από κάναβο» → δοκός ανά segment, **σταματά στις παρειές κολωνών**· σύρε άξονα→ακολουθούν· reload→ΑΚΟΜΗ ακολουθούν [bindings persisted]· re-run→idempotent skip) + commit (Giorgio· shared tree icon-agent structural-tab· git add ΜΟΝΟ δικά μου). **DEFER:** beam-join corner-miter· GEN-SLAB· full reconcile (untrimmed migration). |
| 2026-06-12 | Opus | **Slice GEN-TIE DONE — «Συνδετήριες δοκοί από κάναβο» (born-bound, idempotent create-only).** Τρίτη γραμμική οικογένεια «από κάναβο» μετά τις πεδιλοδοκούς (εσχάρα) & τους τοίχους. **Πλήρες SSoT reuse:** ο builder `buildStripGridFromGuides` πήρε param `kind: 'strip'\|'tie-beam' = 'strip'` (threaded στο `buildBoundStrip` → `completeFoundationFromTwoClicks` — τα tie-beam defaults πλάτους/βάθους υπάρχουν ήδη στο `getKindDimensionDefaults`)· ίδιος `enumerateGridStrips`/ίδια bindings/ίδιο `CreateFoundationsCommand`. NEW `bim/foundations/tie-beam-grid-commit.ts` = create-only orchestrator (mirror `wall-grid-commit`, idempotent skip ανά `segmentKeyFromBindings`, `mode='center'` — κεντραρισμένες, χωρίς overhang). **Kind-partition (ΚΡΙΣΙΜΟ — οι συνδετήριες συνυπάρχουν με πεδιλοδοκούς στον ίδιο άξονα):** ο segmentKey είναι kind-agnostic, άρα η «Εσχάρα» reconcile (`foundation-grid-commit.ts`) τώρα φιλτράρει `existingFoundations`+`computeRehosts` σε `kind='strip'` ΜΟΝΟ, και το auto-settle gate (`useRibbonFoundationBridge`) απαιτεί grid-managed **strip** → μηδέν cross-delete/cross-trigger ανάμεσα στα δύο overlays. **Follow-move δωρεάν** μέσω `foundationHostingStrategy` (κάθε foundation με bindings ακολουθεί). **Wiring:** `foundation-command-keys` +`tieBeamsFromGrid` action+`isFoundationActionKey`· `useRibbonFoundationBridge` +`handleTieBeamsFromGrid`+onAction+toast· `structural-tab` +1 `actionBtn` (panel foundation, icon `struct-found-tie-from-grid`)· `drawing-event-map` +2 events· `useDxfViewerNotifications` +2 toasts (ICU)· i18n el/en (`tieBeamGrid.*` + label `tieBeamsFromGrid`). 50 jest (tie-beam-grid-commit idempotent ×4 + kind-partition cross ×2 + builder kind-param ×2 + foundation-from-grid/grid-commit/structural-tab regression) + 549 foundations/walls regression PASS. **ELEVATION FIX (Giorgio browser-feedback «οι συνδετήρες έχουν ίδιο υψόμετρο εκκίνησης με τις πεδιλοδοκούς → χάνονται μέσα τους»):** δομικά λάθος (Eurocode 8 §5.4.1.2 — η συνδετήρια συνδέει κεφαλές θεμελίων ΨΗΛΟΤΕΡΑ). Το geometry «κρεμάει το στερεό ΚΑΤΩ» από το `topElevationMm`· strip top −1000 βάθος 400 → [−1400,−1000]· tie-beam είχε ΙΔΙΟ top −1000 βάθος 500 → [−1500,−1000] θαμμένη. FIX: NEW `DEFAULT_TIE_BEAM_TOP_ELEVATION_MM=-500` + SSoT `defaultFoundationTopElevationMm(kind)` (tie-beam→−500, pad/strip→−1000) στους ΔΥΟ default builders (`foundation-types.buildDefaultFoundationParams` + `foundation-completion.buildDefaultFoundationParams`) → συνδετήρια [−1000,−500] κάθεται ΠΑΝΩ στην πεδιλοδοκό (κάτω παρειά της = άνω παρειά strip). Ισχύει & για το **manual** εργαλείο (συνέπεια). +2 jest elevation-stacking. **CORNER FIX (Giorgio DB-baseline review «στις 4 άκρες οι συνδετήρες αφήνουν κενά»):** read-only DB ανάλυση (12 strips + 12 tie-beams) απέδειξε — οι πεδιλοδοκοί έχουν `justification`+`extend:±600` (Slice 8 miter→γωνίες κλειστές)· οι συνδετήριες create-only ήταν κεντραρισμένες ΧΩΡΙΣ extend → ακάλυπτο w/2×w/2 τετράγωνο σε κάθε εξωτερική γωνία. FIX: ο `tie-beam-grid-commit` περνά πλέον το τελικό σύνολο (existing+new) από το **ΙΔΙΟ SSoT** `computeGridJunctionExtends` (υποστηρίζει ήδη `kind='tie-beam'`, γρ.77) → terminus άκρα προεκτείνονται στη μακρινή παρειά των κάθετων (miter), γωνίες κλείνουν, idempotent, follow-move-safe. Create-only → `CompoundCommand([Rehost(existing-corner-updates)?, Create(mitered-new)?])` 1 undo. **Migration:** re-run πάνω σε προϋπάρχουσες non-mitered συνδετήριες → `created=0, jointed>0` (toast «έκλεισαν γωνίες»). +event `jointed` + toast `tieBeamGrid.jointed` ICU el/en + 2 jest (miter-extend + migration). 152 foundations jest PASS. ΕΚΤΟΣ ADR-040. 🔴 browser-verify (κουμπί «Συνδετήριες από κάναβο» → συνδετήρια ανά segment· συνύπαρξη με εσχάρα χωρίς αλληλοδιαγραφή· follow-move μετά reload) + commit (Giorgio· shared tree icon-agent structural-tab· git add ΜΟΝΟ δικά μου). **DEFER:** GEN-BEAM (NEW beam-hosting-strategy πρώτα)· GEN-SLAB (πολύγωνο — σχεδιαστική απόφαση)· full reconcile/auto-resplit συνδετήριων. |
| 2026-06-12 | Opus | **Περιμετρικό mode «Έδραση εσχάρας» (Giorgio: «δημιουργούνται πάντα έκκεντροι — θέλω επιλογή κεντρικά/εξωτερικά/εσωτερικά»).** Το 5a-grid hardcode-άρε τις περιμετρικές inward· τώρα 3-way επιλογή. **Core:** `gridStripJustification` +param `mode: GridPerimeterMode = 'center'\|'inner'\|'outer'` (center→όλες center· inner→τωρινό inward default· outer→flipped, περιμετρικές προεξέχουν· εσωτερικές ΠΑΝΤΑ center) + `DEFAULT_GRID_PERIMETER_MODE='inner'`. Threading: `enumerateGridStrips`/`buildStripGridFromGuides`/`deriveGridFollowGhostFootprints` → `commitFoundationGridFromGuides` (deps.perimeterMode). **SSoT store:** NEW `foundation-grid-settings-store.ts` (module-level mode cell) → το διαβάζει ΚΑΙ το κουμπί (`runFoundationGridCommit`) ΚΑΙ το auto-settle reconcile (Slice 7) ΚΑΙ ο live ghost (`GuideFollowGhostOverlay` draw-time getter, ADR-040-safe) → μετακίνηση άξονα δεν επαναφέρει σιωπηλά mode. **UI:** `structural-tab` «Εσχάρα από κάναβο» = **split-button** (main=last-used· dropdown 3 variants Εσωτερικά/Κεντρικά/Εξωτερικά → 2 νέα action keys `fromGridCenter`/`fromGridOuter` + `isFoundationActionKey`)· `useRibbonFoundationBridge.handleFromGrid(mode)` set store→run. i18n el/en ×3 labels+tooltips. 12 νέα jest (justification mode ×4 + builder mode ×3 + structural split ×2 + 3 regression) + 201 foundation/hosting regression PASS. ΕΚΤΟΣ ADR-040 πλην του overlay getter (stage ADR-040). 🔴 browser-verify (βελάκι κουμπιού → 3 modes· κεντρικά=concentric· εξωτερικά=προεξέχουν· μετακίνηση άξονα κρατά το mode) + commit. shared-tree: structural-tab/.test πειράζονται κι από icon-agent → git add ΜΟΝΟ δικά μου hunks. |
| 2026-06-12 | Opus | **Slice GEN-COL + GEN-WALL DONE (generation «Κολώνες/Τοίχοι από κάναβο» — born-bound, idempotent).** Mirror της «Εσχάρα από κάναβο» για ΚΟΛΩΝΕΣ & ΤΟΙΧΟΥΣ: το follow-on-move (Slice COL/WALL, `f992df62`) ήταν host-on-snap-only· τώρα υπάρχει και η **generation** πάνω στην ίδια υποδομή. **GEN-COL:** NEW `bim/columns/column-from-grid.ts` (`buildColumnGridFromGuides` → μία born-bound κολώνα ανά τομή X×Y, `center-x`/`center-y` bindings, reuse `buildColumnEntity`· νέος `enumerateGridIntersections`) + `column-grid-commit.ts` orchestrator (idempotent skip ανά ζεύγος guide-id) + NEW `CreateColumnsCommand` (deferred `drawing:entity-created` tool='column', mirror `CreateFoundationsCommand`). **GEN-WALL:** NEW `bim/walls/wall-from-grid.ts` (`buildWallGridFromGuides` — **REUSE αυτούσιο** `enumerateGridStrips`· τα strip bindings = τα wall slots, centerline ΠΑΝΩ στον άξονα μηδέν extend) + `wall-grid-commit.ts` (idempotent skip ανά segment key, direction-agnostic) + NEW `CreateWallsCommand`. **Wall→column face-trim (Revit face-to-face, Giorgio browser-feedback):** NEW pure `bim/walls/wall-column-trim.ts` — όταν κάθεται κολώνα σε άκρο τοίχου, το άκρο τραβιέται από το **κέντρο** στην **παρειά** της κολώνας μέσω `GuideBinding.extend` (= support distance του column footprint στη διεύθυνση του τοίχου· follow-move-safe + persist δωρεάν, ίδιος μηχανισμός με Finish-Face/corner-fill)· `wall-grid-commit` διαβάζει τις κολώνες της σκηνής και τις περνά στον builder (columns-first workflow· re-trim μετά από column-add = DEFER). **SSoT:** `gridAxesFromReader` +optional `minPerAxis` (default 2· κολώνες=1, μία τομή=μία κολώνα). **Wiring:** `{column,wall}-command-keys.ts` +`fromGrid` action· `useRibbon{Column,Wall}Bridge` +handler+toast emitter· `structural-tab.ts` 2 νέα `actionBtn` (panels walls/columns)· `drawing-event-map` 4 νέα events· `useDxfViewerNotifications` 4 toast handlers (ICU)· i18n el/en (`columnGrid.*`/`wallGrid.*` + 2 labels). 30 jest (2 builders + 2 commits + structural-tab + 2 wall-column-trim) + 25 foundation regression PASS. ΕΚΤΟΣ ADR-040 (pure builders/commands/ribbon — μηδέν ghost/outline/micro-leaf). 🔴 browser-verify + commit + deploy (Giorgio· shared tree· git add ΜΟΝΟ δικά μου). **DEFER:** Slice DIAG-HOST διαγώνιο wall hosting (το slot/offset model είναι 1D-axis-only· διαγώνιος → νέο slot type: projection σημείου→γραμμή άξονα + t-along-axis)· miter-join εσχάρας τοίχων· full reconcile/delete-obsolete· follow-ghost generation. |
| 2026-06-12 | Opus | **Slice GEN+COL+WALL — Associative grid hosting γενικεύεται σε ΤΟΙΧΟΥΣ & ΚΟΛΩΝΕΣ (Revit grid-driven structure).** Το follow-on-move ήταν foundation-only· τώρα ο κάναβος φιλοξενεί όλη τη δομή (§8 GRID-FIRST όραμα). **Slice GEN (zero behavior change):** εξήχθη `HostingStrategy` registry (`hosting-strategy.ts` + `-types.ts`) + shared slot-writers (`derive-slots.ts` — `deriveLineSlots`/`derivePointSlots`, το πρώην inline foundation derive· `derive-params-from-guides` έγινε thin caller)· ο `guide-hosting-reconciler` γενικεύτηκε (`reconcileHostedEntities` dispatch ανά `entity.type`· `reconcileHostedFoundations` alias)· `useHostingReconciler` φίλτρο `isFoundationEntity`→`isGridHosted` (bindings + registered strategy)· `guide-follow-ghost` + `GuideFollowGhostOverlay` (ADR-040) ζωγραφίζουν per-kind outline (foundations εσχάρα-aware· τοίχοι/κολώνες generic zero-lag). 134 foundation regression πράσινα = απόδειξη zero-change. **Slice COL:** `column-hosting-strategy` (point slots → `position`)· host-on-snap (`resolve-axis-bindings.ts`: γεωμετρική σύμπτωση με άξονα, ΟΧΙ snap-event) → auto-bind center-x/center-y στο `useColumnTool` commit (Revit «Column → At Grids»)· persist guideBindings (column-firestore-service + docToEntity restore). **Slice WALL:** `wall-hosting-strategy` (line slots → `start/end`)· host-on-snap στο `use-wall-commit` straight path με **lookup από location-line + `extend` residual** ώστε η Finish-Face παρειά να μένει στον άξονα· axis-aligned-only (διαγώνιοι → καμία δέσμευση)· persist guideBindings (wall-firestore-service + wall-persistence-helpers restore). Firestore rules `hasAll` (όχι `hasOnly`) → guideBindings χωρίς rules change. 60 hosting jest (derive-slots/registry/strategies/resolver) + 134 foundation + 410 wall + 582/586 column+drawing (4 fails = άλλος agent: foundation-preview/floorplan-symbol). **DEFER:** Slice GRID-GEN «Κολώνες/Τοίχοι από κάναβο» generators (born-bound, mirror εσχάρας)· διαγώνιο wall hosting. ✅ **BROWSER-VERIFIED + COMMITTED `f992df62`** (Giorgio live 2026-06-12: κολώνα follow+reload ✓· τοίχος follow+Finish-Face ✓· μη-bound ανεπηρέαστα ✓). ⚠️ mixed commit (Haiku agent bulk-add — συμπεριέλαβε ORTHO-F8/snap-step/grip-move άλλων πρακτόρων). |
| 2026-06-12 | Opus | **Slice 10 — crossing-via-drag auto-trigger VERIFIED (✅ DB-confirmed, καμία νέα αλλαγή κώδικα).** Διερεύνηση (Phase A διαγνωστικό + read-only DB) του φόβου ότι το crossing **μέσω drag** (όχι κουμπί) άφηνε stale full-span (το coordinate-follow τέντωνε λωρίδες με stale bindings). **Αποτέλεσμα:** ο auto-trigger δουλεύει end-to-end. Runtime logs: `bim:grid-guides-settled` fire-άρει (→ **H1 emitter-gap απορρίπτεται**), ο auto-listener τρέχει `runFoundationGridCommit`, reconcile `created:4 / deleted:4 / reJustified:9` — **ταυτόσημο delta με το κουμπί**. DB (17 strips): καθαρή τοπολογία 9 κάθετες (3 στήλες × 3 Y-bays) + 8 οριζόντιες (4 Y × 2 X-bays), **μηδέν full-span, μηδέν overlap**· το crossed `be38435f` πέρασε στη μεσαία θέση· το **MANUAL `fnd_0f5bef39` επέζησε** με ίδιο id + `justificationManual:true` + center + width 600 πάνω στον crossed άξονα → **H3 (moved-persist overwrite) απορρίπτεται**. **Αιτία που «δουλεύει τώρα»: το ίδιο το Slice 9** (segmentKey identity + `toUpdate`) εξάλειψε το delete+create-on-coords race → ο settle-commit υπολογίζει καθαρά μόνο τα γνήσια topology deltas. Δεν χρειάστηκε Phase B fix. Αφαιρέθηκαν τα προσωρινά `[ADR441-DIAG]` console.debug (`useRibbonFoundationBridge.ts` ×3 + `foundation-grid-commit.ts` ×1 + TEMP import). **DEFER (latent, H1):** pure-swap δύο αξόνων σε ακριβώς αντεστραμμένες θέσεις → `gridOffsetSignature` με `sort()` δεν θα fire-άρει (θεωρητικό· εδώ το set άλλαξε)· fix = signature = ακολουθία axis-ids ταξινομημένων κατά offset. 🔴 commit (Giorgio· μαζί με Slice 9). |
| 2026-06-12 | Opus | **Slice 9 DONE (binding-aware managed identity — id + instance overrides επιβιώνουν μετακίνησης άξονα, Revit datum-move).** Live verify (Test 1, read-only DB σύγκριση φρέσκου baseline): απλή μετακίνηση άξονα C (χωρίς crossing) → τοπολογία καθαρή ΑΛΛΑ **9/17 strips delete+recreate με νέα id** (όσα ακουμπούν τον άξονα) → manual override (justification+width) χανόταν **ακόμη και χωρίς crossing**. Root cause: ο `reconcileGridStrips` ταυτοποιούσε existing↔target με `gridStripSignature` (= `segmentKey` **+ bare-coords**) αντί σκέτο `segmentKey` (coordinate-free, binding-aware, σταθερό σε μετακίνηση). **FIX:** match στο `segmentKeyFromBindings` → matched-by-segmentKey με αλλαγή coords/έδρασης = **in-place managed update** (νέο `toUpdate: RehostedStrip[]`, κρατά id· coords←target· διατομή width/thickness←existing· χειροκίνητη έδραση preserved, auto→re-justify κανόνα) αντί delete+create. Το `gridStripSignature` πλέον = change-detector («άλλαξαν coords;»), όχι identity. Λύνει ΚΑΙ simple-move ΚΑΙ crossing carry-over (όσο το ζεύγος γειτονικών αξόνων υπάρχει· `pairKey` order-independent). Dedup διπλών segmentKeys → delete. SSoT reuse: `segmentKeyFromBindings`/`computeFoundationGeometry`/`RehostFoundationsCommand` — μηδέν νέα μηχανή/command. MOD `foundation-grid-reconcile.ts` (ταυτότητα+managed-update, `toReJustify`→`toUpdate`) + `foundation-grid-commit.ts` (wire `toUpdate`). `gridStripSignature`/`GuideFollowGhostOverlay` (ADR-040) ανέγγιχτα. 16 reconcile jest (5 νέα: coordinate-follow, simple-move manual, simple-move auto, crossing carry-over, dedup) + 146 grid/consumers PASS, tsc 0 δικά μου. **DEFER:** crossing όπου το ζεύγος αξόνων όντως αλλάζει = legit recreate (Revit-honest — instance override σε φάτνωμα που έπαψε χάνεται)· width-override UI. 🔴 browser-verify Test 1 (same-id) + Test 2 (crossing) + commit (Giorgio· shared tree). |
| 2026-06-12 | Opus | **Slice 8 DONE (Revit-grade auto-junction-join — γωνίες/κόμβοι για ΟΠΟΙΑΔΗΠΟΤΕ έδραση).** Giorgio (screenshot 011341): εξωτερική πεδιλοδοκός σε outward έδραση → κενά στις γωνίες, οι γείτονες δεν προσαρμόζονται («όπως οι μεγάλοι/Revit, full enterprise+SSoT»). Λύση = miter/extend-to-join μέσω του υπάρχοντος `GuideBinding.extend`: κάθε **terminus** άκρο επεκτείνεται στη **μακρινή παρειά** των κάθετων στον κόμβο (clamp ≥0)· inward → far face=άξονας → extend=0 (**zero-regression**)· outward → η κάθετη επεκτείνεται → γωνία κλείνει. SSoT: το miter ζει στο `geometry.footprint` → 2D+3D+BOQ αυτόματα (μηδέν αλλαγή renderers). NEW pure `foundation-grid-junctions.ts` `computeGridJunctionExtends` (terminus-gated, idempotent, reuse RehostedStrip/extend). **Trigger 1** post-reconcile pass folded στο CompoundCommand (`foundation-grid-commit.ts`, fresh «Εσχάρα»+Slice 7). **Trigger 2** `useRibbonFoundationBridge.dispatchParams` → αλλαγή έδρασης/πλάτους → junction recompute → **γείτονες live** ως `CompoundCommand([Update,Rehost])` (1 undo). `gridStripSignature` → **bare coords** (extend-invariant, όπως justification) → fresh target ταιριάζει existing-with-miter (μηδέν spurious delete/create). 12 jest + 115 foundation/grid total + tsc. ΕΚΤΟΣ ADR-040. 🔴 browser-verify (outward→γωνία κλείνει live· inward→καμία αλλαγή)+commit. DEFER: edited own-miter (rare· «Εσχάρα» το διορθώνει). |
| 2026-06-12 | Opus | **Slice 7 DONE (live re-split/reflow στο follow-move — auto, χωρίς κουμπί).** Giorgio (B): μετακίνηση άξονα → οι λωρίδες σπάνε στα νέα σταυρόδρομα & οι γωνίες ευθυγραμμίζονται **ζωντανά** + κλειδώνουν **αυτόματα** στο release (Revit associative grid), χωρίς να πατηθεί «Εσχάρα». 2 tiers, **μηδέν αλλαγή στον ADR-040-critical `useHostingReconciler`**: στο release τρέχει πρώτα το υπάρχον coordinate-follow (id-preserving) και μετά (settle 400ms) αυτόματα το ΙΔΙΟ managed reconcile με το κουμπί → απλές μετακινήσεις `unchanged` (same id), γνήσια split→create/delete, role-changes→reJustify (same id). **Tier 2:** NEW `useGridGuideSettleEmitter` (guide-store subscribe, debounce, εκπέμπει `bim:grid-guides-settled` σε αλλαγή του pure `gridOffsetSignature`, baseline-first, loop-free) mounted στο `HostingReconcilerHost` + NEW event· `useRibbonFoundationBridge` extract `runFoundationGridCommit`/`emitFromGridToast` (SSoT button+auto) + auto-listen **gated** σε «υπάρχει grid εσχάρα» (μην auto-create στο draw) + toast μόνο αν delta>0. **Tier 1:** NEW pure `foundation-grid-ghost.ts` (`deriveGridFollowGhostFootprints` wrapper πάνω στον SSoT `buildStripGridFromGuides` → pixel-identical με commit) + MOD `GuideFollowGhostOverlay.tsx` (grid-target ghost split-aware, ADR-040-critical stage). **Boy-Scout SSoT:** NEW exports `enumerateGridStrips`/`gridAxesFromReader` (κοινός enumerator builder+ghost). 17 νέα jest + 72 regression + tsc. ΕΚΤΟΣ ADR-040 (pure/event) εκτός του overlay (stage ADR-040). 🔴 browser-verify+commit. DEFER: manual-justified ghost δείχνει auto-έδραση (commit κρατά manual)· only-changed ghost subset· wall/column follow. |
| 2026-06-12 | Opus | **Slice 5a-grid fix (follow-move orientation-invariant justification).** DB-verify (Giorgio screenshot): μετακίνηση περιμετρικού Y-άξονα κάτω από άλλον → οι ακραίες **κάθετες** λωρίδες προεξείχαν προς τα **έξω** από τους ακραίους οδηγούς (αντί inward). ΡΙΖΑ (code=SoT): το `buildBandFootprint` (`foundation-geometry.ts`) υπολόγιζε το CCW normal από `dx,dy = end−start` → η inward justification εξαρτιόταν από τη **φορά start→end**· το `deriveFoundationParamsFromGuides` (follow-on-move) γράφει σκέτα τα slots **χωρίς κανονικοποίηση φοράς** → όταν ο guide του `start-y` προσπερνά τον `end-y` (άξονας κατεβαίνει), η φορά αντιστρέφεται (−Y) → το normal γυρίζει → `'right'` (inward +X) δίνει **outward −X**. FIX (1 σημείο SSoT): canonical tangent στο `buildBandFootprint` (V→+Y, H→+X: `if (uy<−ε || (|uy|≤ε && ux<0)) flip`) πριν το normal → **geometry orientation-invariant** (Revit Location Line). Καλύπτει build-time (baked) + re-derive (follow-move) + 5a-control ταυτόχρονα· δεν αγγίζει params/bindings/derive. Zero-regression: `center`→sign 0 μηδέν διαφορά· κανονικές φορές→no-op. 49 jest (geometry/from-grid/justification/derive) + **3 νέα orientation-invariance** (reversed V 'right'/'left' → ίδιο inward bbox + area/volume invariant), 19 geometry total PASS. ΕΚΤΟΣ ADR-040 (pure geometry). 🔴 browser-verify (μετακίνηση άξονα → λωρίδες μένουν inward)+commit. |
| 2026-06-11 | Opus | **Slice 5a-grid fix (reconciled toast δείχνει reJustified).** DB-verify (Giorgio, self-heal test): +1 οριζόντιος οδηγός πάνω από τον τελευταίο → 3×3→3×4 → η πρώην περιμετρική (y-last 'right', έκκεντρη) έγινε εσωτερική → **reflow σε center** (reJustified=2, ίδιο id, concentric footprint — επιβεβαιωμένο read-only στη βάση: `fnd_bb3b8c0c`/`fnd_c00b653f` createdAt παλιό + updatedAt νέο, justification field αφαιρέθηκε). **ΟΜΩΣ** το toast έδειχνε μόνο «Προστέθηκαν 5· αντικαταστάθηκαν 0» — το `reJustified` ΔΕΝ φαινόταν. ΡΙΖΑ: `useDxfViewerNotifications.ts` reconciled branch δεν περνούσε `reJustified` param + το ICU string εμφάνιζε πάντα «αντικαταστάθηκαν {deleted}» (ακόμα & =0). FIX (3 αρχεία): `t('foundationGrid.reconciled', {created, deleted, reJustified})` + el/en reconciled ICU `=0 {}` hide → δείχνει μόνο ό,τι όντως συνέβη + «ευθυγραμμίστηκαν #» / «re-aligned #». Αποτέλεσμα: «Προστέθηκαν 5 πεδιλοδοκοί· ευθυγραμμίστηκαν 2.» Καμία αλλαγή i18n key (υπάρχουσα τιμή), μηδέν νέα baseline. ΕΚΤΟΣ ADR-040. **✅ BROWSER-VERIFIED** (Giorgio, refresh+ξανα-Εσχάρα). 🔴 commit. |
| 2026-06-11 | Opus | **Slice 5a-grid DONE (auto-inward περίμετρος + managed regeneration, Revit-grade).** Giorgio «full enterprise, πλήρης αυτοματοποίηση + χειροκίνητη». Η «Εσχάρα από κάναβο» παράγει περιμετρικές **inward** (εξωτ. παρειά στον άξονα, μηδέν overhang), **αντικαθιστώντας το corner-fill** (inward γωνία κλείνει φυσικά). NEW pure `foundation-grid-justification.ts` `gridStripJustification` (SSoT κανόνας: V xi=0→right/last→left, H yi=0→left/last→right, εσωτ.→center). `foundation-from-grid.ts`: -corner-fill extend (καθαρά coords/bindings) + per-strip justification override. NEW `justificationManual?` flag (types+schemas `.strict()`): 5a-control selected-entity το θέτει `true` → reconcile δεν το επαναφέρει. `foundation-grid-reconcile.ts` +`toReJustify`: auto λωρίδες reflow στον κανόνα (self-heal axis-role-change, κρατά id+geometry), χειροκίνητες preserve· justification ΕΚΤΟΣ signature. Orchestrator: reflows folded στο **υπάρχον RehostFoundationsCommand** (μηδέν νέο command)· +`reJustified` event/toast. Boy-Scout: rehost `adoptTarget` υιοθετεί & την έδραση κανόνα target. BOQ geometry-driven (safeUnion) verify inward (2×2 union 8.16 m²). 74 grid jest + 289 regression (2 fails pre-existing preview-helpers, ΟΧΙ δικά μου) + tsc. ΕΚΤΟΣ ADR-040. Μηδέν rules. 🔴 browser-verify+commit. DEFER: 5b pad anchor, 5c strap/tie-beams. |
| 2026-06-11 | Opus | **Slice 5a-control DONE (UI set justification — Revit «Location Line»).** Το justification mechanism (5a) ήταν DONE αλλά **καμία UI δεν το έθετε** → πρακτικά αόρατο. ΛΥΣΗ = ribbon combobox 3 επιλογών (Κεντρικά/Αριστερά/Δεξιά) στο **line-only panel** (strip/tie-beam· pad→anchor), mirror του `anchor`: drawing-mode branch → γεμίζει το ΗΔΗ-υπάρχον `FoundationParamOverrides.justification` (placement-time)· selected-entity branch → undoable mutation μέσω υπάρχοντος `UpdateFoundationParamsCommand` (edit-time). NEW `FOUNDATION_RIBBON_KEYS.stringParams.justification` (+array) + `JUSTIFICATION_OPTIONS` combobox + bridge wiring ×4. **Boy-Scout:** εξαγωγή `readSelectedStringField`/`readToolStringField`/`nextParamsForStringChange`/`applyStringChangeToHandle` (τα callbacks ήταν στο όριο 40γρ → DRY + string-generic). i18n `foundationEditor.justification.*` el/en ICU. 7 jest + 266 foundation regression (2 fails pre-existing stale preview-helpers, ΟΧΙ δικά μου) + tsc. ΕΚΤΟΣ ADR-040 (ribbon-data+bridge+i18n). Μηδέν persistence/rules. Default `center` → καμία ορατή αλλαγή· pad → combobox κρυφό. 🔴 browser-verify+commit. DEFER: 5a-grid (auto perimeter inward + corner-fill reconcile + BOQ verify), 5b pad anchor, 5c strap/tie-beams. |
| 2026-06-11 | Opus | **Slice 6b fix (multi-bay ορφανός re-host).** Browser-verify (Giorgio): νέος άξονας + χειροκίνητη πεδιλοδοκός σε όλο το ύψος του → «Εσχάρα» → η χειροκίνητη **αγνοούνταν** + η εσχάρα έφτιαχνε per-bay strips από πάνω = **επικάλυψη**. ΡΙΖΑ: `orphanSegmentKey` υπολόγιζε signature **ολόκληρου του ανοίγματος** (`V\|gx\|y0\|y2`) ενώ τα target είναι **per-bay** (`V\|gx\|y0\|y1`, …) → κανένα match → ignored (το γνωστό DEFER «multi-bay»). ΛΥΣΗ: η `orphanSegmentKey` ταιριάζει πλέον στο **πρώτο (χαμηλότερο) φάτνωμα** της διαδρομής (NEW `firstBay` + `nearestIndex` + `sortedByOffset`, sorted-index adjacency)· ο ορφανός υιοθετεί εκείνο το ένα segment (κρατά id+διατομή) και τα υπόλοιπα φατνώματα του άξονα τα δημιουργεί κανονικά ο reconciler (target ∉ existing → create) → πλήρης κάλυψη, **μηδέν διπλό**. Single-bay συμπεριφορά αμετάβλητη (first-bay = το ίδιο bay). 4 νέα jest (2 pure multi-bay V/H + 2 commit-level Giorgio scenario) + 103 foundations regression + tsc. ΕΚΤΟΣ ADR-040. 🔴 browser-verify+commit. DEFER αφαιρέθηκε (multi-bay). |
| 2026-06-11 | Opus | **Slice 3-perf DONE (zero-lag associative follow ghost).** Giorgio: ορατό lag οδηγού↔πεδιλοδοκού στο follow-on-move. ΡΙΖΑ: ο reconciler ακολουθεί μέσω `setLevelScene` (React)→re-render→bitmap rebuild (1-3 frames)· ο οδηγός ζωγραφίζεται imperative (μηδέν lag)→strips μένουν πίσω. ΛΥΣΗ (mirror ProposalGhostOverlay/grip-ghost): όσο σύρεται οδηγός, οι hosted strips ζωγραφίζονται **imperative σε αποκλειστικό canvas** στη live θέση, frame-synced. NEW `GuideFollowGhostOverlay.tsx` (dedicated canvas· repaint=guide-store notify RAF-coalesced + transform-frame· `getImmediateTransform()` draw-time) + NEW pure `bim/hosting/guide-follow-ghost.ts` (`deriveFollowGhostFootprints`→reuse `reconcileHostedFoundations`) + NEW `systems/guides/guide-drag-store.ts` (imperative drag-state SSoT). MOD `useCanvasContainerHandlers` (set/clear drag-store) + `useHostingReconciler` (suppress per-frame `setLevelScene` όσο σύρεται→μηδέν bitmap rebuild· release→ΕΝΑ commit+persist) + mount στο `canvas-layer-stack-leaves`. Per-strip auto-handoff (ghost δείχνει committed≠live-derived→συγκλίνουν στο commit)· linger 140ms→μηδέν flash. 6 jest (drag-store+follow-ghost)+33 hosting regression+tsc. ADR-040-critical (stage)· leaf-only subscription, CHECK 6B/6D safe. 🔴 browser-verify+commit. DEFER: wall/column/beam follow (per-kind strategy). |
| 2026-06-11 | Opus | **Slice 6b DONE (re-host / migration legacy ορφανών — Option A).** Λύνει το σκέλος (α) του αρχικού προβλήματος: παλιοί πεδιλοδοκοί (προ-Slice-3, χωρίς `guideBindings`) δεν ακολουθούσαν τη μετακίνηση αξόνων. Τώρα ξανα-κρεμιούνται κατά γεωμετρία στον τρέχοντα κάναβο, **χωρίς διαγραφή**, μέσα στο «Εσχάρα από κάναβο» (πριν το reconcile). **SSoT κλειδί:** ο rehosted υιοθετεί bindings+coords του target φατνώματος (κρατά id+διατομή) → ίδιο `gridStripSignature` → ο reconciler τον βλέπει ως `unchanged` (μηδέν διπλοί/διαγραφή, **μηδέν αλλαγή στη λογική Slice 6**). Matching κατά grid-topology (segmentKey από nearest-guide· δουλεύει & για περιμετρικές). NEW pure `foundation-grid-rehost.ts` (`rehostOrphanStrips`· reuse `segmentKeyFromBindings`/`computeFoundationGeometry`/`hasGuideBindings`· **tol scale-aware** `0.25×bay-spacing`, μάθημα Slice 6· πρώτος-ανά-segment) + NEW `RehostFoundationsCommand` (update-semantics `updateEntity` merge, undo→original, deferred `bim:entities-attached` persist). **Persistence gap fix:** `FoundationUpdateInput`+`updateFoundation`+`persist()` update-branch → **+`guideBindings`** (πριν δεν persist-άρανε σε update· καμία αλλαγή firestore.rules). Orchestrator → ΕΝΑ `CompoundCommand([Rehost?,Delete?,Create?])` (1 undo)· result +`rehosted`· event/toast +`rehosted` (ICU el/en). 34 jest + 394/395 regression (1 fail pre-existing `AssignWallTypeCommand` ADR-412, ΟΧΙ δικό μου) + tsc. ΕΚΤΟΣ ADR-040. 🔴 browser-verify+commit. DEFER: multi-bay ορφανός, undo-after-reload bindings clear, 6c reconcile-with-confirm. |
| 2026-06-11 | Opus | **Slice 6 fix (scale-aware signature tolerance).** Browser-verify (Giorgio screenshot): σε σκηνή ΜΕΤΡΩΝ η πρώην ακραία κάθετη εισχωρούσε `w/2` μέσα στη νέα. ΡΙΖΑ: `SIGNATURE_COORD_TOL=1` σκην. μονάδα «έτρωγε» το corner-fill overhang (`width/2 × 0.001 ≈ 0.25` σε μέτρα) → περιμετρική (με overhang) & εσωτερική (χωρίς) έπαιρναν ΙΔΙΟ signature → ο reconciler δεν αντικαθιστούσε τη stale περιμετρική. FIX: tol `1→0.001` (sub-mm σε κάθε κλίμακα· διακρίνει overhang ≥~0.1, απορροφά float-noise <<0.001) + ακέραιος `coordBucket` (καθαρό deterministic token). +2 segment regression (μέτρα overhang 0.25 ξεχωρίζει· sub-tol noise ίδιο) +1 orchestrator regression (μέτρα εξωτερικός οδηγός→deleted>0). 27 foundation-grid jest πράσινα. |
| 2026-06-11 | Opus | **Slice 6 DONE (Reconciling «Εσχάρα από κάναβο» — signature-set diff).** Το 6a (exact-key skip) **αναθεωρήθηκε**: έσπαγε σε αλλαγή τοπολογίας (Giorgio repro): ενδιάμεσος οδηγός → split πάνω σε whole· εξωτερικός → παλιά περιμετρική κρατά corner-fill. ΛΥΣΗ = managed regeneration (Revit/Tekla). NEW pure `gridStripSignature` (=`segmentKey\|round(start)\|round(end)`· extend ξεχωρίζει περιμετρική/εσωτερική· μη-grid→null) + NEW pure `foundation-grid-reconcile.ts` `reconcileGridStrips(target,existing)→{toCreate,toDelete,unchanged}` (sig-set diff· null-sig legacy/manual ΠΟΤΕ delete) + NEW `DeleteFoundationsCommand` (inverse CreateFoundationsCommand, deferred persistence events). Orchestrator → ΕΝΑ `CompoundCommand([Delete?,Create?])` (1 undo)· no-op→`reason:'up-to-date'`· result `{created,deleted,unchanged}`. REVERT 6a `coveredKeys`/`skippedCount`. UI: event `{created,deleted}`· toast `built`/`reconciled`/`upToDate` (ICU el/en). 48 jest + tsc. ΕΚΤΟΣ ADR-040. Μηδέν rules. split-safe + corner-fill-role-safe + idempotent + minimal-mutation/stable-ids. 🔴 browser-verify+commit. DEFER: 6b re-bind ορφανών· 6c reconcile-with-confirm UI. |
| 2026-06-11 | Opus | **Slice 5a DONE (Foundation Justification — geometry-param mechanism) + ΑΝΑΘΕΩΡΗΣΗ μηχανισμού.** Giorgio «όπως η Revit, full enterprise + full SSoT» → αναθεωρήθηκε η κλειδωμένη draft (`GuideBinding.extend`) σε **geometry-param** (Revit «Location Line»: semantic param στα `params`, honored στο geometry· follow-move-safe αυτόματα μέσω του `{...params}` spread στο derive· tracks width· single-source· persist δωρεάν· δουλεύει & σε χειροκίνητες λωρίδες). NEW `StripJustification` type + `justification?` σε strip/tie-beam params + SSoT `JUSTIFICATION_NORMAL_SIGN` map + `DEFAULT_STRIP_JUSTIFICATION` (foundation-types.ts)· κάθετο shift `sign·hw` του centerline στο `buildBandFootprint` (foundation-geometry.ts, center→0→identical/zero-regression)· `StripJustificationSchema.optional()` σε strip/tie-beam (foundation.schemas.ts)· override plumbing (foundation-completion.ts conditional spread). Default `center` → καμία ορατή αλλαγή. 54 jest (geometry/derive-follow-move-safe/schema) + 190/192 foundation regression (2 fails pre-existing stale preview-helpers, ΟΧΙ δικά μου). ΕΚΤΟΣ ADR-040. Μηδέν persistence/rules. DEFER: 5a-control (UI set), 5a-grid (auto perimeter + corner-fill reconcile + BOQ verify), 5b pad anchor, 5c strap/tie-beams. |
| 2026-06-11 | Opus | **Slice 4 DONE (BOQ θεμελίωσης + net volume safeUnion) + §10 +Slice 5 σχεδιασμός.** Εύρημα: η θεμελίωση ΔΕΝ ήταν καθόλου στο schedule/BOQ → Slice 4 = (Α) σύνδεση + (Β) anti-double-count. NEW `bim/geometry/foundation-grid-boq.ts` (computeFoundationGridNet thickness-bucketed safeUnion + foundationStripNetGeometry per-strip net via safeIntersection, Σ μεριδίων==union exact uniform) + NEW `hooks/data/foundation-boq-feed.ts` (applyFoundationGridNet pre-pass hosted grid strips). Boy-Scout: export `multiPolygonArea` από polygon-utils (private duplicate ×2 → SSoT· refactor footprint-region-classifier). Schedule wiring: types ScheduleEntityType +foundation, schedule-preset-columns FOUNDATION_COLUMNS, schedule-presets AnyBimEntity+mapFoundation+PRESET_REGISTRY, ScheduleEntityToggle, BimScheduleDialog (pre-pass useMemo), i18n el/en entityType.foundation. Στήλη όγκου = net ανά λωρίδα → άθροισμα=καθαρός όγκος (χωρίς totals-infra). +§10 Slice 5 (Justification/έκκεντρα πέδιλα + tie-beams μέσω GuideBinding.extend). 24 net+preset jest + 238 regression + tsc καθαρό. ΕΚΤΟΣ ADR-040. DEFER ATOE bridge + μεταβλητό πάχος. 🔴 browser-verify+commit. |
| 2026-06-11 | Opus | **Slice JOIN DONE (corner-fill γωνιών εσχάρας, follow-move-safe).** Έκλεισε τα ακάλυπτα `w/2 × w/2` τεταρτημόρια στις 4 εξωτερικές γωνίες (λωρίδες σταματούσαν στο centerline). Λύση = optional `extend?: number` (mm, signed) στο `GuideBinding` endpoint (σταθερή απόσταση σχετικά με τον άξονα → επιβιώνει του Slice 3 re-derive· σκέτη αλλαγή start/end θα ακυρωνόταν σε κάθε follow-move). MOD: `guide-binding-types.ts` (`extend?`) + `guide-binding.schemas.ts` (`.strict()` +`extend` optional) + `derive-params-from-guides.ts` (honor extend, mm→scene μέσω SSoT `mmScaleFor` ΜΟΝΟ στο extend term, idempotent ok) + `foundation-from-grid.ts` (extend `±width/2` ΜΟΝΟ σε γωνιακά endpoints = extreme-parallel × extreme-perpendicular· refactor `emitVerticalStrips`/`emitHorizontalStrips` ≤40γρ· geometry baked στο build ίδια φόρμουλα με derive). ΟΧΙ επέκταση μη-γωνιακών (αποφυγή «δοντιών»). 25 jest (4 extend derive + 6 corner-fill grid + updates) + tsc καθαρό. ΕΚΤΟΣ ADR-040. DEFER Slice 4 BOQ double-count→safeUnion. |
| 2026-06-11 | Opus | Αρχική σύνταξη (DRAFT): καταγραφή συζήτησης Giorgio (εικόνες εσχάρας πεδιλοδοκών, δομική θεωρία, Revit comparison «ένα-ένα ανά κατηγορία») + ευρήματα έρευνας κώδικα (τι υπάρχει/τι λείπει: safeUnion + region detection + buildStripFromWall + batch pattern + CompoundCommand υπάρχουν· batch-from-walls + crossing-join/union + grid FSM/ribbon λείπουν) + 3 ανοιχτές αποφάσεις (trigger / αναπαράσταση / συνδετήριες) + πρόταση v1. Status PROPOSED — συνέχεια συζήτησης. ΟΧΙ κώδικας ακόμη. |
| 2026-06-11 | Opus | +§8 Ευρύτερη φιλοσοφία & στρατηγική (καταγραφή 2ης συζήτησης Giorgio): διπλό mode (design-from-scratch + import-driven bottom-up erection θεμελίωση→όροφοι, σκυρόδεμα)· παραμετρική θεμελίωση N×M φατνώματα (αυξομείωση/κεντρικά-έκκεντρα/move-no-break/variable πάχη-ύψη)· **GRID-FIRST** = πώς το κάνουν όλοι οι μεγάλοι (Revit/Tekla/ETABS-SAFE/ProtaStructure/Allplan/SCIA/Graitec· associative grid→hosted follow)· ProtaStructure=πιο κοντινός workflow· honesty: πλήρως-αυτόματο 2D→3D ΔΕΝ λύθηκε πουθενά, όλοι ημι-αυτόματο human-in-the-loop· κεντρικά vs έκκεντρα + strap beams· σύσταση=structural grid ως SSoT. ΑΝΟΙΧΤΟ: υπάρχοντες guides (useGuideState/Actions) == grid ή διαφορετικό (υπό διερεύνηση). |
| 2026-06-11 | Opus | **Slice 1 DONE + DEPLOYED.** Grid persistence per-όροφο (mirror foundation Firestore): NEW `systems/guides/guide-persistence-types.ts` (GridGuideDoc/GuideSnapshot + guideToSnapshot/snapshotToGuide serialization, strips temporary + undefined-keys) + `guide-firestore-service.ts` (GridGuideFirestoreService createGrid/updateGrid/subscribeGrid, 1 doc/floor) + `hooks/data/useGridGuidePersistence.ts` (hydrate clear+restore· debounced 1000ms save + anti-echo signature guard· per-floor reset on scope change) + `app/GridGuidePersistenceHost.tsx`. MOD: enterprise-id (GRID_GUIDE 'grd' + generateGridGuideDocId ×3 αρχεία)· firestore-collections (FLOORPLAN_GRID_GUIDES + FLOOR_SCOPED)· DxfViewerTopBar mount· firestore.rules match block· firestore.indexes 4 composite. **DEPLOYED rules+indexes → pagonis-87766.** 14 jest PASS. ΜΑΘΗΜΑ: entity schemas `.passthrough()` → guideBindings backward-compat χωρίς schema bloat. |
| 2026-06-11 | Opus | **Αποφάσεις LOCKED + Slice 0 DONE.** Giorgio: (1) associative grid hosting πρώτα· (2) πηγή=κάναβος· (3) διακριτοί+join· (4) ΟΧΙ συνδετήριες v1· scope=όλα 0→3 έγκριση ανά slice· grid per-όροφο. +§10 σχέδιο 4 slices + canonical slot-based hosting model. Status PROPOSED→IN PROGRESS. **Slice 0** (hosting types): NEW `bim/hosting/guide-binding-types.ts` + `bim/types/guide-binding.schemas.ts`· MOD `bim/types/bim-base.ts` (`BimEntity.guideBindings?`) + `foundation.schemas.ts`. Backward-compatible. 11 jest + 383 regression PASS. Recognition: επιβεβαιώθηκε hook point `guide-store.moveGuideById/moveDiagonalGuideById→notify`, `BimEntity` base, foundation persistence mirror checklist. |
| 2026-06-11 | Opus | **Slice 3 DONE (follow-on-move / associative grid hosting).** Μετακίνηση άξονα κανάβου → όλα τα hosted strips ακολουθούν live (60fps) + persist on settle (Revit/Tekla). NEW `bim/hosting/derive-params-from-guides.ts` (pure slot→coordinate, immutable, idempotent→null) + `bim/hosting/guide-hosting-reconciler.ts` (pure `buildHostingIndex` inverted index + `reconcileHostedFoundations` only-changed, geometry+validation re-derived από SSoT) + `hooks/data/useHostingReconciler.ts` + `app/HostingReconcilerHost.tsx` (ADR-040: imperative guide-store subscribe, RAF-throttled 1×/frame, per-tick bound-axis offset diff, only-changed setLevelScene, loop-free). Persist: settle-debounce 350ms → `bim:entities-moved` → reuse `useBimEntityMovedPersistEffect` (κανένα νέο path). Persist bindings: MOD `FoundationDoc`+`entityToSaveInput`+`docToEntity` (guideBindings round-trip· καμία αλλαγή firestore.rules — `hasAll` allowlist). Stage ADR-040 (changelog). 22 jest + tsc καθαρό. ΕΚΤΟΣ CHECK 6B/6D paths. DEFER: wall/column hosting (strategy registry), XZ άξονες, undo-after-reload offset snapshot. |
| 2026-06-11 | Opus | **Slice 2 DONE.** «Εσχάρα πεδιλοδοκών από κάναβο»: NEW `bim/foundations/foundation-from-grid.ts` (pure `buildStripGridFromGuides` — intersection-to-intersection zero-overlap segments, born-hosted slot-based guideBindings, dedup, invisible-skip, `<2 άξονες`→insufficient-guides) + `bim/foundations/foundation-grid-commit.ts` (orchestrator → 1 atomic command) + `core/commands/entity-commands/CreateFoundationsCommand.ts` (batch, mirror CreateMepSegmentsCommand: deferred-microtask `drawing:entity-created`/`bim:foundation-delete-requested` → persistence-correct create+undo). MOD: `useRibbonFoundationBridge.onAction` (handleFromGrid) + `foundation-command-keys` (fromGrid action) + `home-tab-draw` (subVariant) + `drawing-event-map` (2 events) + `useDxfViewerNotifications` (toast) + i18n el/en (ICU plurals). **ΔΙΟΡΘΩΣΗ σχεδίου:** ribbon `action` ΟΧΙ canvas tool (one-shot, mirror MEP auto-design)· `CreateFoundationsCommand` ΟΧΙ `CompoundCommand<CreateEntityCommand>` (το τελευταίο δεν εκπέμπει `drawing:entity-created` → δεν θα persist-άρανε). 16 jest PASS, tsc καθαρό δικά μου. ΕΚΤΟΣ ADR-040. Εκκρεμεί Slice 3 (follow-on-move). |
| 2026-06-11 | Opus | +§8.8 ΕΥΡΗΜΑ (έρευνα guides): το `systems/guides/` (ADR-189) ΕΙΝΑΙ ήδη πλούσιος structural grid — named άξονες Α/Β/Γ×1/2/3 + bubbles + bay dimensions + groups + presets 4/5/6/8m + GuideSnapEngine + IFC4 IFCGRID/IFCGRIDAXIS + full command set/undo. **ΛΕΙΠΟΥΝ 2 κρίσιμα** για Revit-grade grid που οδηγεί ανέγερση: (1) **persistence** (guides=session-only, καμία Firestore) (2) **associative hosting** (καμία BIM entity δεν φέρει guideId/gridAxisId → move άξονα δεν παρασύρει στοιχεία = το «move-no-break» που ζητά ο Giorgio). Δευτερεύοντα: per-floor scope, bubble extents, crop. Συμπέρασμα: ~80% έτοιμο· grid=SSoT ανέγερσης χρειάζεται persistence+associative-hosting. Σχέση ADR-189+436+441. |
