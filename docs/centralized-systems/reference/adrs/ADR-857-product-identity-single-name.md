# ADR-857: **Μία ταυτότητα παντού** — το όνομα του προϊόντος αποκτά ρίζα, και η πέμπτη γραφή γίνεται αδύνατη

| Metadata | Value |
|----------|-------|
| **Status** | ✅ **ΟΛΟΚΛΗΡΩΜΕΝΟ** — Φ1-Φ7 (2026-09-12) · **Φ8 = Φ8α μηδενισμός + Φ8β πύλη `CHECK 3.81`** (2026-09-13). 🔴 **Η προϋπόθεση της §5 ήταν ΨΕΥΔΗΣ**: το δέντρο **ΔΕΝ ήταν μηδενικό** — η Φ8α βρήκε **36** αδήλωτες γραφές και **τρεις ολόκληρες γραφές που καμία απογραφή δεν είχε δει** (§7 #12) |
| **Date** | 2026-09-12 |
| **Category** | Architecture & SSoT |
| **Canonical Location** | `src/constants/product-identity.ts` |
| **Author** | Γιώργος Παγώνης + Claude Code (Anthropic AI) |
| **Σχετικά** | ADR-777 §8.29 / §8.54 (`email-texts.ts` · `brandedSubject`) · ADR-851 §3 Ε (παγωμένο `senderDisplayName`) · ADR-856 (το email αλλαγής διεύθυνσης) · ADR-812 / CHECK 3.73 (λεξιλόγιο τομέα — **το πρότυπο της πύλης**) · ADR-792 / CHECK 3.59 (ενικό λεξιλόγιο) · ADR-666 (pseudo locale) |

---

## 1. Το εύρημα — **τέσσερα ονόματα σε ΕΝΑ μήνυμα**

Η εντολή ήταν «μία ταυτότητα παντού → `Nestor App`». Ο έλεγχος βρήκε ότι το ίδιο, μοναδικό email
που φεύγει σήμερα κουβαλά:

| Θέση | Τιμή | Πηγή |
|---|---|---|
| **Αποστολέας** (`From:`) | `Nestor Construct` | `src/server/comms/email-providers.ts:123` |
| **Θέμα** (υπογραφή) | `— ΝΕΣΤΩΡ` / `— Nestor` | `src/server/comms/email-texts.ts:105` / `:121` |
| **Υποσέλιδο** | `Nestor App` | `src/services/email-templates/base-email-template.ts:155` |
| **Όσα στέλνει η Firebase** | `Nestor Pagonis` | `src/config/firebase-auth-config.ts:57` |

🔴 **Το `'Nestor Construct'` δεν υπήρχε σε καμία απογραφή** — ούτε στο handoff, ούτε στην πρώτη
δική μας. Βρέθηκε μόνο επειδή ο έλεγχος ρώτησε «**ποιος γράφει τη γραμμή `From:`;**» αντί να
ψάξει τα ονόματα που ήδη ξέραμε.

## 2. Γιατί **κανένα εργαλείο δεν το έπιασε** — μετρημένο

`eslint-rules/no-hardcoded-strings.js:154` (διέξοδος για κλάσεις Tailwind):

```js
if (/^[\w\-\s[\]/.:()'=,>&*#~+@!%]+$/.test(value) && !greekPattern.test(value)
    && (/[:[(\]]/.test(value) || value.includes(' '))) return true;   // ⇒ «τεχνική», δεν αναφέρεται
```

με `greekPattern = /[Α-Ωα-ωάέήίόύώ]/` (`:53`). Άρα:

- `'ΝΕΣΤΩΡ'` → **έχει ελληνικά** ⇒ αναφέρεται·
- `'Nestor App'` · `'Nestor Pagonis'` · `'Nestor Construct'` → **λατινικά με κενό** ⇒ **αόρατες**.

⇒ **Δεν είναι σύμπτωση ότι οι λατινικές γραφές έγιναν τέσσερις και η ελληνική μία.** Ο κανόνας
δεν είναι χαλασμένος — είναι **δομικά τυφλός** σε αυτή την κλάση. Γι' αυτό η απάντηση είναι
**πύλη με μητρώο**, όχι ρύθμιση του lint.

## 3. Οι αποφάσεις

### Α. **Πέντε κλάσεις** — και μόνο η πρώτη μετονομάζεται

Το λάθος που θα κατέστρεφε δεδομένα θα ήταν «βρες-και-αντικατέστησε». Κάθε εμφάνιση κατατάσσεται:

| Κλάση | Τι είναι | Απόφαση |
|---|---|---|
| **Α — όνομα προϊόντος** | υπογραφή θέματος, υποσέλιδο, TOTP issuer, `workbook.creator`, `<title>`, `poweredBy` | → **`Nestor App`** |
| **Β — νομικό πρόσωπο** | `asset-pack-registry.ts` `holder`, `© 2026 …` | **μένει `Nestor Pagonis`** — τα πνευματικά δικαιώματα ανήκουν σε **πρόσωπο**, όχι σε προϊόν |
| **Γ — δεδομένα πελάτη** | `companyName` σε PDF, `og:site_name`, `senderName`, `FROM_NAME` | **ΔΕΝ μετονομάζεται** — είναι δεδομένα ενοίκου· αλλάζει **μόνο η εφεδρική** τιμή |
| **Δ — αναγνωριστικά μηχανής** | `NestorPagonisApp/1.0` (User-Agent Nominatim/Overpass) · `Nestor-Workspace` · IFC `NestorBIM` · XDATA `NestorAec`/`NestorLayerMeta`/`NestorGrid`… | ⛔ **ΠΟΤΕ** — είναι **συμβόλαια μορφής**· μετονομασία σπάει αρχεία **ήδη γραμμένα στον δίσκο** |
| **Ε — παγωμένα** | `firebase-auth-config.ts:57` `senderDisplayName` · το fixture απόκλισης `auth-config-state.test.ts:124` | **μένουν** (§7 #1) |

### Β. Η ρίζα: `src/constants/product-identity.ts`, **μηδέν εισαγωγές**

**Δεν υπάρχει** υπάρχον SSoT να επεκταθεί — ελέγχθηκαν τέσσερις υποψήφιοι: το `BRAND` του
`base-email-template.ts` είναι **παλέτα χρωμάτων** (και `server-only`)· το `BOT_IDENTITY` /
`SYSTEM_IDENTITY` είναι **ηθοποιοί συνομιλίας**· το `tenant.ts` κρατά **μόνο id**· το i18n κρατά
**τέσσερα διαφορετικά κλειδιά** για το ίδιο ουσιαστικό.

Η ρίζα ακολουθεί **ακριβώς** το πρότυπο του `src/i18n/languages.ts`: **καμία εισαγωγή**, ώστε να
τη διαβάζουν διακομιστής, πελάτης, **cron**, και τα δύο αρχεία καθαρών δεδομένων που
**επίτηδες** αποφεύγουν το i18next (`email-texts.ts` · `auth-action-email-texts.ts`).

### Γ. Το `brand` **διαγράφεται** — δεν αλλάζει τιμή

Ένα `Record<HumanLanguage, …>` του οποίου **κάθε στήλη** οφείλει να έχει την **ίδια** τιμή είναι
δομή που **επιτρέπει** απόκλιση — δηλαδή **η γεννήτρια της επόμενης γραφής**. Και είχε ήδη
αποκλίνει: `ΝΕΣΤΩΡ` στα ελληνικά, `Nestor` στα αγγλικά.

Μαζί φεύγει και η παράμετρος: `brandedSubject(language, subject)` → **`brandedSubject(subject)`**.
Μια υπογραφή που ζητά γλώσσα **υπόσχεται** ότι το όνομα εξαρτάται από γλώσσα. Δεν εξαρτάται.

### Δ. 🔴 `KNOWN_PAST_SPELLINGS` — ο φρουρός της **ουράς**

Το `brandedSubject` κρίνει αν ένα θέμα υπογράφει ήδη με `endsWith`. Το **ίδιο το αρχείο**
προειδοποιεί (ADR-777 §8.54): *«τα ήδη γραμμένα `pending` έγγραφα της ουράς κουβαλούν το παλιό,
χειρόγραφο επίθεμα **για πάντα**»*.

⇒ Αν ο έλεγχος γινόταν **μόνο** στο νέο όνομα, κάθε ουραγμένο θέμα `«Νέα αγγελία — ΝΕΣΤΩΡ»` θα
έφευγε ως **`«Νέα αγγελία — ΝΕΣΤΩΡ — Nestor App»`** — στα εισερχόμενα πραγματικού ανθρώπου.

Γι' αυτό η ρίζα δηλώνει **και τις παλιές γραφές**, και ο έλεγχος τις τιμά όλες. ⚠️ Το σύνολο
**συρρικνώνεται μόνο** αφού αδειάσει η ουρά — και αυτό γράφεται, δεν υπονοείται.

### Ε. Η ελληνική πρόζα **ξαναγράφεται**, δεν αντικαθίσταται

Τα ελληνικά **κλίνουν** το όνομα: *«Άνοιγμα στον **Νέστορα**»*, *«…ειδοποιήσεις email στον
**Νέστορα**»*. Το `Nestor App` είναι **άκλιτο** ⇒ οι προτάσεις αναδιατυπώνονται
(*«Άνοιγμα στο Nestor App»*). Μια μηχανική αντικατάσταση θα παρήγε αγράμματα ελληνικά.

---

## 4. 🏆 Πού ξεπερνά την πρακτική

1. **Η ταξινόμηση προηγείται της μετονομασίας.** Οι περισσότερες «ενοποιήσεις μάρκας» είναι
   find-and-replace· εδώ **τρεις** κλάσεις (Β, Γ, Δ) **δεν** μετονομάζονται, και η Δ θα έσπαγε
   αρχεία **ήδη γραμμένα στον δίσκο**.
2. **Η αλλαγή φυλάει την ουρά.** Το `KNOWN_PAST_SPELLINGS` αντιμετωπίζει τα **ήδη γραμμένα**
   μηνύματα — το σημείο όπου μια «αθώα» μετονομασία γίνεται ορατή σε παραλήπτη.
3. **Η πύλη δεν εμπιστεύεται τον εαυτό της**: **0 ευρήματα = ΑΠΟΤΥΧΙΑ** (§5), επειδή το repo έχει
   **τέσσερις** τεκμηριωμένες πύλες όπου το «0» σήμαινε «κανείς δεν κοίταξε».

---

## 5. Η πύλη ταυτότητας — **CHECK 3.81, ΥΛΟΠΟΙΗΜΕΝΗ 2026-09-13**

📘 **Πλήρες ιστορικό, μετρήσεις και «⚠️ ΜΗΝ»**: `docs/gates/3.81.md` (ή `npm run gate:explain 3.81`).

### 5.0 Τα δύο πράγματα που ΔΕΝ ζούσαν σε αυτό το έγγραφο, και τώρα ζουν

**(α) ΤΟ ΠΡΟΤΥΠΟ ΠΟΥ ΜΙΜΕΙΤΑΙ.** Ίδιο καθεστώς (ZERO-TOL, **καμία baseline**), τρία αρχεία:
`.domain-vocabulary.json` + `scripts/check-domain-vocabulary.js` +
`scripts/__tests__/domain-vocabulary.test.js` (CHECK 3.73 / ADR-812). Από εκεί δανείζονται
**αυτούσια**: το κλειστό μητρώο με **υποχρεωτικό λόγο ≥40 χαρακτήρων**, η **κλειστή λογιστική
fail-closed** (άγνωστη κατάσταση ⇒ `throw` **με όνομα**), η εκτύπωση **κάθε** μπλοκάρουσας
**ακόμα και στο μηδέν** (μάθημα 3.48), το `collectSourceFiles`, και το μίνι-repo από
**πραγματικά** αρχεία. **Καμία νέα μηχανή.**

**(β) ΤΟ ΚΑΒΕΑΤ ΤΟΥ ΑΡΙΘΜΟΥ — ΚΑΙ ΓΙΑΤΙ ΜΕΝΕΙ ΓΡΑΜΜΕΝΟ ΑΦΟΥ ΛΥΘΗΚΕ.** Αυτή η ενότητα
προδιαγράφει αρχικά τη πύλη ως «CHECK 3.79»· στο **ίδιο 24ωρο**, άλλος agent στο **ίδιο
δέντρο** υλοποίησε και **έτρεξε** το CHECK 3.79 = «Πύλη του σκιασμένου αρχείου» (ADR-858).
Ένας αριθμός **δεσμευμένος σε προδιαγραφή πριν την υλοποίηση** είναι υπόσχεση που **κανείς
δεν επιβάλλει** — και σε κοινό δέντρο με παράλληλους agents η σύγκρουση είναι **αναμενόμενη,
όχι ατύχημα**, ακριβώς όπως με τους αριθμούς ADR.

⚠️ **ΠΩΣ ΒΡΙΣΚΕΤΑΙ Ο ΣΩΣΤΟΣ — ΡΩΤΑ ΤΙΣ ΤΡΕΙΣ ΑΥΘΕΝΤΙΕΣ, ΟΧΙ ΤΟ `ls`.** Η αυθεντία του
CHECK 3.66 είναι η **ΕΝΩΣΗ**: δρομολογητής (`scripts/run-checks-parallel.js`, μοτίβο
`add<Οτιδήποτε>('3.NN'`) **+** hook (`scripts/git-hooks/pre-commit`, `CHECK 3.NN`) **+**
`.gate-inventory.json`. 🔴 **Και το `grep 3.81` έδωσε ΨΕΥΔΩΣ ΘΕΤΙΚΟ στην πρώτη αναζήτηση**:
ταίριαξε μέσα στο «63.**817**» ενός σχολίου. Μετρήθηκε 2026-09-13: δρομολογητής **3.80** ·
hook **3.79** · `docs/gates` **3.80** · `.gate-inventory` μόνο 3.40/3.77 ⇒ **3.81 ελεύθερο**.

⚠️ **ΤΟ `.gate-inventory.json` ΕΙΝΑΙ ΜΟΝΟ ΓΙΑ CI-ONLY ΠΥΛΕΣ** (`ciOnlyGates`, fail-closed
κατεύθυνση). Η 3.81 τρέχει σε pre-commit ⇒ **ΔΕΝ μπαίνει εκεί**. Η εγγραφή γίνεται στον
**δρομολογητή** + `docs/gates/3.81.md` + `npm run gate-index:generate`.

⚠️ **ΤΟ ΜΑΘΗΜΑ ΕΙΝΑΙ ΤΟ ΙΔΙΟ ΜΕ ΤΟΥΣ ΑΡΙΘΜΟΥΣ ADR** (CLAUDE.md: *«ΜΗΝ εμπιστεύεσαι αριθμό
γραμμένο εδώ· ΡΩΤΑ ΤΗΝ ΠΥΛΗ»*): ένας αριθμός πύλης **δεσμευμένος σε προδιαγραφή** πριν την
υλοποίηση είναι υπόσχεση που **κανείς δεν επιβάλλει** — και σε κοινό δέντρο με παράλληλους
agents, η σύγκρουση είναι **αναμενόμενη, όχι ατύχημα**. **Πώς βρίσκεις τον σωστό**:
`ls docs/gates/` **τη στιγμή της υλοποίησης**, και **ξανά πριν το commit**· το **CHECK 3.66**
(μητρώο πυλών) μπλοκάρει την ασυμφωνία.

**Ερώτηση**: «λέει κάθε σημείο που ονομάζει το **προϊόν** το ΙΔΙΟ όνομα — και είναι κάθε **άλλη**
χρήση **δηλωμένη, με λόγο**;»

**Αυθεντία**: κλειστό μητρώο `.product-identity.json` (ιδίωμα `.domain-vocabulary.json`):
`canonical` · `root` · `declarations[]` με `file`, `symbol`, **`class`** (Α-Ε) και **λόγο ≥40
χαρακτήρων**. **Νέα δήλωση μπλοκάρει ακόμη κι όταν είναι σωστή** — ώστε τον δεύτερο «αθώο» να τον
δει άνθρωπος.

**Τέσσερα κριτήρια, ποτέ ενωμένα με «ή»** (μάθημα CHECK 3.41), όλα **⛔ ZERO-TOL**:
`Κ1` αδήλωτη γραφή · `Κ2` ορφανή δήλωση · `Κ3` δήλωση χωρίς λόγο · `Κ4` απόκλιση ρίζας.

**ZERO-TOL και ΟΧΙ ratchet**: δεν υπάρχει «λιγότερες λάθος γραφές από χθες» — **μία** αρκεί για να
φτάσει σε εισερχόμενα.

🔴 **ΚΑΙ Η ΑΙΤΙΟΛΟΓΗΣΗ ΠΟΥ ΕΓΡΑΦΕ ΕΔΩ ΗΤΑΝ ΨΕΥΔΗΣ — ΜΕΤΡΗΜΕΝΟ 2026-09-13.** Η πρόταση έλεγε
*«εφικτό **μόνο** επειδή η πύλη μπαίνει τελευταία, σε δέντρο **ήδη μηδενικό**»*. Το δέντρο **δεν
ήταν μηδενικό**: η πρώτη εκτέλεση της πύλης βρήκε **36 αδήλωτες γραφές**, ανάμεσά τους **τρεις
ολόκληρες γραφές που δεν υπήρχαν σε καμία απογραφή** (§7 #12) — δύο εκ των οποίων **έφευγαν σε
email**. Το ZERO-TOL είναι εφικτό επειδή η **Φ8α το μηδένισε** (36 → 0 στην ίδια συνεδρία),
**όχι** επειδή ήταν ήδη μηδέν. *Μια προδιαγραφή που υποθέτει την κατάσταση του δέντρου αντί να
τη μετρήσει είναι ευχή.*

**Ψευδώς θετικά — μετρημένα**: **58 από τις 59** εμφανίσεις `'ΝΕΣΤΩΡ'` στο `src/` είναι **fixtures
δοκιμών** ⇒ αποκλεισμός `__tests__/` τις σβήνει **χωρίς κόστος**. Τα κλιτά **προσωπικά** ονόματα
(«Νέστορας Παγώνης» σε παραδείγματα του AI pipeline, «Νέστορα»→«Νέστορας» σε κανόνα γραμματικής)
είναι **δομικά αόρατα**: δεν ανήκουν στο κλειστό σύνολο γραφών, που έρχεται **από τη ρίζα**.

🔑 **ΚΑΙ ΤΟ «ΜΙΑ ΔΗΛΩΣΗ ΑΝΑ ΣΥΜΒΟΛΟ» ΗΤΑΝ ΛΑΘΟΣ ΣΧΗΜΑ — ΤΟ ΑΠΕΔΕΙΞΕ Η ΠΡΩΤΗ ΕΚΤΕΛΕΣΗ.** Η
προδιαγραφή ζητούσε `symbol`· το μητρώο γράφτηκε με `NestorPagonisApp` · `NestorAec` ·
`Nestor-Workspace` — ονόματα **όπως τα βλέπει ο άνθρωπος**, που όμως **δεν ανήκουν στο κλειστό
σύνολο** ⇒ **11 ορφανές δηλώσεις + 11 αδήλωτες γραφές για ΤΑ ΙΔΙΑ σημεία**. Το κλειδί είναι
**(αρχείο, γραφή)**, οι δηλωμένες γραφές είναι **μέλη του συνόλου**, και η ακρίβεια μετακομίζει
στον **λόγο** — που τον διαβάζει άνθρωπος. Το ίδιο το §7 #9 το είχε ήδη γράψει: *«απογραφή
ταυτότητας ψάχνει **ΤΗΝ ΤΙΜΗ**, ποτέ μόνο τον δρόμο που την γράφει»*.

**Άμυνες κατά του «0 = κανείς δεν κοίταξε»**: (α) ο **παρονομαστής τυπώνεται και ελέγχεται** —
**0 ευρήματα = αποτυχία**, η ρίζα οφείλει πάντα να βρεθεί· (β) **άγκυρα βαθμονόμησης στο αληθινό
δέντρο** (το `email-texts.ts` πρέπει να είναι στα ευρήματα)· (γ) **σουίτα μεταλλάξεων**: οι
τέσσερις γνωστές γραφές + μία επινοημένη (`NestorApp`) ⇒ **5/5 κόκκινα**.

---

## 6. Σειρά — ποτέ σπασμένο δέντρο

| Φάση | Τι |
|---|---|
| **Φ1** | η ρίζα, **χωρίς καταναλωτές** (μηδέν αλλαγή συμπεριφοράς) |
| **Φ2** | η ραχοκοκαλιά των email: `brand` φεύγει · πρόζα ξαναγράφεται · `KNOWN_PAST_SPELLINGS` **στο ίδιο commit** με τις άγκυρες |
| **Φ3** | υποσέλιδο + `©` (το `©` παίρνει το **νομικό πρόσωπο**, κλάση Β) |
| **Φ4** | κέλυφος + **ένωση των 4 κλειδιών μάρκας σε ΕΝΑ**· μετά **αναγέννηση** τύπων (3.33) + shell slice (3.34) |
| **Φ5** | εξαγωγείς PDF/Excel |
| **Φ6** | 2FA (`APP_NAME`) |
| **Φ7** | εφεδρικές τιμές ενοίκου (κλάση Γ) |
| **Φ8α** | **ο μηδενισμός** — 36 αδήλωτες γραφές σε 22 αρχεία → **0**. 🔴 Χρειάστηκε επειδή η υπόθεση «δέντρο ήδη μηδενικό» ήταν **ψευδής** (§5) |
| **Φ8β** | **η πύλη** (`CHECK 3.81`), σε δέντρο που **μετρήθηκε** μηδενικό — όχι που υποτέθηκε |

---

## 7. 🔶 Δηλωμένα όρια

1. ⛔ **Το `senderDisplayName` ΔΕΝ αλλάζει** (`firebase-auth-config.ts:57`): ζει **μέσα στην
   παγωμένη επιφάνεια** της Google (ADR-851 §7 #1). Αλλαγή στο git **τώρα** θα δημιουργούσε
   **τρίτη μόνιμη απόκλιση** που **δεν διορθώνεται**. Αλλάζει **μετά** το ξεπάγωμα.
2. **Το `Nestor App CRM` στο `legal.json` μένει** — είναι **δημοσιευμένο νομικό κείμενο** (Όροι ·
   Απόρρητο · Διαγραφή δεδομένων, και σε **παραγόμενα** route slices). Αλλαγή δημοσιευμένων όρων
   έχει διάσταση **έκδοσης**, όχι κώδικα. Δηλώνεται, δεν γίνεται σιωπηλά.
3. ✅ **ΛΥΘΗΚΕ 2026-09-12 — ΤΟ `FROM_NAME` ΔΕΝ ΟΡΙΖΕΤΑΙ ΣΤΗΝ ΠΑΡΑΓΩΓΗ.** Το όριο έλεγε «δεν
   μπορεί να μετρηθεί από εδώ»· **μετρήθηκε από τον άνθρωπο**, στον πίνακα Coolify
   (`159.195.44.221:8000` → εφαρμογή `f661kuiq901llma0o8unhvpi` → Environment Variables).
   Απογραφή **55** μεταβλητών παραγωγής: **ούτε `FROM_NAME` ούτε `FROM_EMAIL`**.

   ⇒ **Η εφεδρεία του κώδικα ΕΙΝΑΙ ό,τι φεύγει**, άρα η αλλαγή της Φ7 είναι **άμεσα ορατή**:
   ο αποστολέας λέει πλέον `Nestor App <info@nestorconstruct.gr>` αντί `Nestor Construct`.
   *(Και η διεύθυνση είναι επίσης εφεδρεία κώδικα — `FROM_EMAIL` άθετο.)*

   🔑 **ΚΑΙ Η ΣΥΣΤΑΣΗ ΕΙΝΑΙ «ΜΗΝ ΤΗΝ ΠΡΟΣΘΕΣΕΙΣ»**, όσο ο ένοικος είναι ένας: ορίζοντας
   `FROM_NAME` στο Coolify δημιουργείς **δεύτερη πηγή** για το όνομα του προϊόντος — ακριβώς
   τη δομή που αυτό το ADR κατήργησε, μόνο που αυτή τη φορά ζει **έξω από το git**, όπου
   κανένα grep και καμία πύλη δεν τη βλέπει. Η μεταβλητή αποκτά νόημα **μόνο** όταν
   υπάρξει ένοικος με **δικό του** όνομα αποστολέα (κλάση Γ, BYOD per tenant).

   ⚠️ **Σημείωση πεδίου, όχι εύρημα**: η ίδια απογραφή έδειξε ότι λείπει και το
   `RESEND_API_KEY` ⇒ η αλυσίδα έχει **έναν** κρίκο (Mailgun). Αυτό **ήταν ήδη δηλωμένο και
   μετρημένο** στο `server/comms/email-provider-chain.ts` (2026-08-19) και **δεν** είναι
   ελάττωμα: οι μη ρυθμισμένοι κρίκοι **παραλείπονται πριν την κλήση**, δεν μπαίνουν στις
   `attempts`, και το `failedOver` **δεν** ανάβει ψευδώς. Δεν ξαναγράφεται εδώ — η αυθεντία
   είναι εκείνο το module.
4. **Το 2FA μένει σε μικτή κατάσταση για πάντα**: αλλαγή του `issuer` επηρεάζει **μόνο νέες**
   εγγραφές — όποιος έχει ήδη σαρώσει QR κρατά `Nestor Pagonis` στο τηλέφωνό του. **Κανένα ενεργό
   TOTP δεν σπάει** (Key Uri Format· το μυστικό και η ετικέτα είναι ήδη αποθηκευμένα).
5. **Εύρος που ΔΕΝ σαρώθηκε**: `functions/` · `packages/` · `mcp-server/` · `public/` · e2e.
   Γράφεται ρητά, ώστε κανείς να μην πει «μία ταυτότητα **παντού**» για κάτι που μετρήθηκε **σε
   ένα μόνο δέντρο**.
6. ✅ **ΛΥΘΗΚΕ 2026-09-12 — ΚΑΙ Η ΠΑΡΑΚΑΤΩ ΠΡΟΒΛΕΨΗ ΕΙΝΑΙ ΠΛΕΟΝ ΨΕΥΔΗΣ.** Το CHECK 3.28
   **δεν** μπλοκάρει: μετρήθηκε **πράσινο, 0 κλώνοι σε 9 αρχεία** (οι 8 εξαγωγείς **μαζί** με
   το `src/lib/export/excel-workbook.ts`). Άλλος agent εξήγαγε το κοινό — τον μετατροπέα
   `#RRGGBB → FFRRGGBB` (**επτά** αντίγραφα, **δύο** ονόματα), το `HEADER_FILL`/`HEADER_FONT`
   και το `writeBuffer → Blob → trigger` με το μακρύ MIME του OOXML.
   🔴 **ΚΑΙ ΤΟ ΠΡΑΣΙΝΟ ΕΞΑΡΤΑΤΑΙ ΑΠΟ ΤΟ ΝΑ ΜΠΕΙ ΕΚΕΙΝΟ ΤΟ ΑΡΧΕΙΟ ΣΤΟ ΙΔΙΟ COMMIT**: είναι
   `A ` (δεν υπάρχει στο `HEAD`) και **τρεις** σταδιοποιημένοι εξαγωγείς το **εισάγουν** ⇒
   commit χωρίς αυτό = **σπασμένο HEAD**.
   ⚠️ **Η πρόβλεψη ήταν λάθος με διδακτικό τρόπο, και μένει γραμμένη**: η μέτρηση που την
   στήριζε έτρεξε πάνω σε **περιεχόμενο που άλλαζε ταυτόχρονα** από άλλον agent. Ο δείκτης
   που το αποκάλυψε: το `jscpd` ανέφερε κλώνο στο `gantt-excel-exporter.ts:108-118` ενώ το
   αρχείο έχει **106 γραμμές** — ανύπαρκτη γραμμή. **Σε κοινό δέντρο, δύο μετρήσεις σε
   διαφορετικές στιγμές δεν είναι μία μέτρηση.** (Ιστορικό της λανθασμένης πρόβλεψης:)

   **ΤΟ CHECK 3.28 ΘΑ ΜΠΛΟΚΑΡΕΙ ΤΟ COMMIT ΤΩΝ ΕΞΑΓΩΓΕΩΝ — ΚΑΙ ΔΕΝ ΤΟ ΠΡΟΚΑΛΕΣΕ Η Φ5.**
   Το άγγιγμα των εξαγωγέων τους φέρνει **μαζί** στο σύνολο που συγκρίνει το `jscpd --diff`, και
   αποκαλύπτεται **προϋπάρχον, δομικό** διπλότυπο σε **δύο** οικογένειες:
   - **PDF**: `builder-pdf-exporter.ts` **με τον εαυτό του** (283-291 ↔ 326-334 · 293-302 ↔
     336-345 — δύο σχεδόν ταυτόσημα μπλοκ `autoTable` για επίπεδα ομαδοποίησης L1/L2) και με
     `report-pdf-exporter.ts` (292-302).
   - **Excel**: `report-excel` ↔ `gantt-excel` ↔ `milestone-excel` ↔ `payment-excel`
     (51-59 · 74-91 · 106-114 · 108-118 · 127-139 · 164-174 · 168-179).

   **Αποδείχθηκε με `git diff --unified=0`** ότι οι αλλαγές της Φ5 είναι **2 γραμμές ανά αρχείο**
   (μία εισαγωγή + μία τιμή) και έχουν **μηδενική τομή** με **κάθε** εύρος κλώνου. Άλλωστε οι
   **ίδιες οι κεφαλίδες** των αρχείων το δηλώνουν: *«Pattern: payment-excel-exporter.ts»*,
   *«same pattern as gantt-excel-exporter.ts»*, *«REUSES: report-pdf-exporter.ts»*.

   ⚠️ **Δεν παρακάμπτεται με `SKIP_JSCPD_DIFF=1` από τον πράκτορα** — είναι απόφαση του Giorgio.
   Ο N.0.2 θα το έστελνε στο `.claude-rules/pending-ratchet-work.md`, αλλά εκείνο το αρχείο είναι
   **πιασμένο από άλλον agent** στο κοινό δέντρο· γι' αυτό καταγράφεται **εδώ**.

7. 🔴 **ΟΡΑΤΗ ΑΛΛΑΓΗ ΔΗΜΟΣΙΟΥ ΚΕΙΜΕΝΟΥ, ΚΑΙ ΚΑΜΙΑ ΠΥΛΗ ΔΕΝ ΤΗΝ ΠΙΑΝΕΙ.** Το
   `search-results.site.brand` έλεγε **«Nestor»** — σκέτο, σε **δύο** γλώσσες — ενώ τα τρία
   αδέλφια του έλεγαν «Nestor App». Η δημόσια κεφαλίδα (`PublicSiteHeader.tsx`) γράφει πλέον
   **`Nestor App`**. Είναι ο **σκοπός** της ADR, αλλά δηλώνεται: οι πύλες κρίνουν **ταυτότητα
   κλειδιών**, ποτέ **μετατόπιση κειμένου**. Κανένα αυτόματο δικτυωτό δεν θα το έλεγε.

8. 🔴 **ΤΑ ΗΔΗ ΠΑΡΑΧΘΕΝΤΑ PDF ΚΡΑΤΟΥΝ ΤΟ ΠΑΛΙΟ ΟΝΟΜΑ — ΚΑΙ ΓΙΑ ΤΑ ΚΤΙΡΙΑ/ΕΡΓΑ ΓΙΑ ΠΑΝΤΑ.**
   Τα labels **δεν** αποθηκεύονται στο snapshot (λύονται φρέσκα), **αλλά** το PDF ανεβαίνει
   στο Storage (`pdfStoragePath`) και η δημόσια διαδρομή **σερβίρει το αποθηκευμένο αρχείο
   χωρίς καμία επανα-απόδοση** (`api/showcase/[token]/pdf/route.ts`). Αναγέννηση υπάρχει
   **μόνο για ακίνητα** (`POST /api/properties/[id]/showcase/regenerate` →
   `regeneratePdfForShare`). **Κτίρια/έργα: καμία διαδρομή. Bulk/admin: καμία.**
   ⚠️ Το σημείο όπου γίνεται ορατό: ο σύνδεσμος «Λήψη PDF» αποδίδεται **ακριβώς πάνω** από το
   υποσέλιδο (`ShowcaseClient.tsx`) ⇒ φρέσκο κείμενο στην οθόνη, παγωμένο μέσα στο PDF.

9. ⚠️ **ΟΙ ΕΦΕΔΡΕΙΕΣ ΗΤΑΝ ΕΠΤΑ, ΟΧΙ ΠΕΝΤΕ.** Η απογραφή της Φ7 είχε μετρήσει 5· το grep
   βρήκε **δύο ακόμη**, σε **client** κώδικα: `useEmailForm.tsx:295` (`senderName`) και
   `PhotoSharePageContent.tsx:79` (`og:site_name`). Και οι δύο κλάση Γ, και οι δύο έλεγαν
   «Nestor Construct». **Μια απογραφή που δεν τρέχει ξανά το grep είναι υπόθεση.**

10. ⚠️ **ΔΥΟ ΣΤΕΝΩΣΕΙΣ ΓΛΩΣΣΑΣ ΠΟΥ ΔΙΑΦΩΝΟΥΝ — ΚΑΙ ΕΠΙΤΗΔΕΣ ΔΕΝ ΕΝΩΘΗΚΑΝ.** Το
    `ShowcaseClient.tsx` στενεύει τη γλώσσα **στο χέρι** (`startsWith('el') ? 'el' : 'en'`),
    ενώ υπάρχει ρίζα `showcaseLocale` (`lib/agency/showcase-filter.ts`). Φαινομενικά
    κεντρικοποίηση ενός `?:` (N.0.2) — **αλλά η σουίτα της ρίζας ΕΚΤΕΛΕΙ ότι
    `showcaseLocale('el-GR') === 'en'`**, ενώ η χειρόγραφη δίνει `'el'`. Η «κεντρικοποίηση»
    θα γύριζε κάθε `el-GR` χρήστη **στα αγγλικά**. 🔑 **Δύο σώματα που απαντούν στο ίδιο
    ερώτημα με ΔΙΑΦΟΡΕΤΙΚΟ κριτήριο νικητή δεν είναι διπλότυπο — είναι δύο ερωτήματα**, και
    η ένωσή τους είναι σφάλμα συμπεριφοράς, όχι καθαριότητα.

11. 🔶 **ΧΡΕΟΣ SSoT ΠΟΥ ΚΑΤΑΓΡΑΦΕΤΑΙ ΕΔΩ** (το `.claude-rules/pending-ratchet-work.md` είναι
    **ξένο/πιασμένο**, 779 KB, `STATUS: ACTIVE`):
    - **Τρεις** ανεξάρτητες αναγνώσεις `FROM_NAME`/`FROM_EMAIL` με **τρεις** εφεδρείες
      (`server/comms/email-providers.ts` · `services/email.service.ts` ·
      `subapps/procurement/.../email-channel.ts`). Αγγίζει **παραγωγική αλληλογραφία** ⇒ δεν
      ενοποιείται μέσα σε δουλειά ταυτότητας.
    - **Πέντε** δομικά διπλότυπα interfaces με `poweredBy: string`, **κανένα** `extends`:
      `labels-shared.ts:45` · `property-showcase/labels.ts:142` · `project-showcase/labels.ts:126`
      · `building-showcase/labels.ts:144` · `pdf-renderer-base.ts:74`.
    - **Σκληρή ελληνική πρόζα** στα θέματα του `email.service.ts` (`generateSubject`):
      **προϋπάρχον** χρέος i18n (N.11). Η Φ7 διόρθωσε την **ταυτότητα**, όχι τη μετάφραση —
      και δεν πρόσθεσε νέα παράβαση.

12. 🔴 **ΤΡΕΙΣ ΓΡΑΦΕΣ ΠΟΥ ΔΕΝ ΥΠΗΡΧΑΝ ΣΕ ΚΑΜΙΑ ΑΠΟΓΡΑΦΗ — ΚΑΙ ΟΙ ΔΥΟ ΕΦΕΥΓΑΝ ΣΕ EMAIL.**
    Το §1 μέτρησε **τέσσερα** ονόματα. Ήταν **επτά**:

    | Γραφή | Πού | Τι είναι |
    |---|---|---|
    | **`Nestor Enterprise`** | `core/configuration/enterprise-config/defaults.ts` | το `app.name` **όλου του συστήματος** |
    | **`Pagonis Nestor`** | `services/ai-pipeline/shared/channel-media-dispatcher.ts` | **θέμα email** με συνημμένο |
    | **`Nestor Error Reporting System`** | `components/ui/ErrorBoundary/error-helpers.ts` | **σώμα email** αναφοράς σφάλματος |

    ⚠️ **Καμία δεν ήταν ορατή στη μέθοδο που τις έψαχνε.** Οι δύο πρώτες επειδή η απογραφή
    έψαξε **γνωστά** ονόματα· η τρίτη επειδή ζει σε **template literal**. Το μάθημα του §7 #9
    («ψάξε **την τιμή**, όχι τον δρόμο») ήταν **σωστό και ανεπαρκές**: χρειάζεται να ψάξεις
    την τιμή **σε κάθε μορφή που κουβαλά κείμενο**.

13. 🔴 **Η ΧΕΙΡΩΝΑΚΤΙΚΗ ΑΠΟΓΡΑΦΗ ΒΡΗΚΕ 15, Η ΠΥΛΗ ΒΡΗΚΕ 36 — ΚΑΙ Η ΔΙΑΦΟΡΑ ΕΧΕΙ ΟΝΟΜΑ.**
    Το `grep` έψαχνε γραφές μέσα σε **quoted literals**. Δεν έβλεπε **JSXText**
    (`<strong>Nestor Construct</strong>` σε **δύο δημόσιες σελίδες**: `PhotoSharePageContent`
    και `VendorPortalErrorState`) ούτε **template literals** (**έξι** μέσα σε HTML email).
    🔑 Είναι **ακριβώς** το τυφλό σημείο που δηλώνει ο **N.11** για τον `no-hardcoded-strings`
    (*«καμία `JSXExpressionContainer` στον visitor map»*) — δηλαδή **δύο ανεξάρτητα όργανα**
    ήταν τυφλά στην **ίδια** κλάση. **Το «0» της απογραφής σήμαινε «δεν κοίταξα εκεί».**
    ⚠️ Και το αντίστροφο, που δικαιολογεί το AST: **πέντε** σημεία γράφουν παλιές γραφές μέσα
    σε **σχόλιο** και **κανένα** δεν είναι παράβαση — το ένα είναι **το σχόλιο που τεκμηριώνει
    τη διόρθωση**, και το `ifc-authorship.ts` κρατά `'NestorCAD'` σε JSDoc που εξηγεί γιατί
    **ΔΕΝ** πρέπει να μπει εκεί. **Σαρωτής κειμένου θα κοκκίνιζε πάνω στη θεραπεία.**

14. 🔶 **ΤΟ `email-templates.service.ts` ΔΗΛΩΝΕΤΑΙ, ΔΕΝ ΔΙΟΡΘΩΝΕΤΑΙ — ΚΑΙ ΕΙΝΑΙ ΑΠΟΦΑΣΗ.**
    Έξι γραφές «Nestor Construct» μέσα σε HTML πρότυπα που διαφημίζουν **κατασκευαστική
    εταιρεία**: *«20+ χρόνια εμπειρίας στη δημιουργία σπιτιών»*, `info@nestorconstruct.gr`,
    «Commercial Division». Είναι **ζωντανό** (3 καταναλωτές, στέλνει αληθινά email).
    Μετονομασία σε «Nestor App» θα έκανε το **λογισμικό** να ισχυρίζεται εικοσαετή εμπειρία σε
    οικοδομές — **λάθος τύπου**, ίδιας οικογένειας με το «© Nestor App». Είναι **κλάση Γ**
    σκληρά γραμμένη· η σωστή θεραπεία (παραμετροποίηση ανά ένοικο) είναι **δική της φέτα**,
    αδελφή του χρέους `FROM_NAME` του #11.

15. 🔴 **ΤΟ ΟΝΟΜΑ ΤΟΥ ΕΞΑΓΟΜΕΝΟΥ ΑΡΧΕΙΟΥ ΜΕΝΕΙ `Nestor_` — ΚΑΙ ΤΟ ΠΕΡΙΣΤΑΤΙΚΟ ΕΙΝΑΙ ΤΟ
    ΧΡΗΣΙΜΟ ΜΕΡΟΣ.** Η Φ8α πρόσθεσε `productFilenameToken()` στη ρίζα ώστε το
    `Nestor_<τομέας>_Report_<ημ>.xlsx` να παράγεται από το `PRODUCT_NAME`. Αποτέλεσμα:
    **`NestorApp_…`** — γραφή που **κανείς δεν επέλεξε ποτέ**, σε αρχείο που **κατεβαίνει
    στον δίσκο του πελάτη**. **Αναιρέθηκε**, και το `productFilenameToken()` διαγράφηκε.

    🔑 **Το ADR ζήτησε ΜΙΑ ταυτότητα — δεν ζήτησε ΝΕΟ όνομα.** Η αλλαγή ονόματος
    παραδοτέου είναι απόφαση **προϊόντος**· δηλώνεται και παρκάρεται, όπως το δημοσιευμένο
    νομικό κείμενο του #2. *Μια δουλειά ταυτότητας που αλλάζει ό,τι βρει μπροστά της παύει
    να είναι ταυτότητα και γίνεται μετονομασία — ακριβώς αυτό που το §3.Α απαγορεύει.*

    🔴 **ΚΑΙ ΤΟ ΚΕΝΟ ΕΠΑΛΗΘΕΥΣΗΣ, ΓΡΑΜΜΕΝΟ ΓΙΑ ΝΑ ΜΗΝ ΞΑΝΑΣΥΜΒΕΙ**: το έπιασε **σουίτα που
    καμία από τις λίστες επαλήθευσής μου δεν έτρεχε** (`report-engine`). Το `test:product-identity`
    έδινε **29/29** και το `test:ai-pipeline` **1235/1235** — και **τα δύο ήταν αληθινά και
    ανεπαρκή**: ο hook τρέχει **ευρύτερη** περιοχή (87 σουίτες έναντι 78).
    ⚠️ **Και η ίδια η CHECK 3.81 ΔΕΝ μπορούσε να το δει, δομικά**: τα `__tests__/`
    εξαιρούνται —**σωστά**, είναι fixtures— άρα ένα test που **κατοχυρώνει παλιά γραφή**
    είναι **αόρατο** στην πύλη. Δηλωμένο όριο, όχι ελάττωμα προς διόρθωση: πύλη που κρίνει
    τα fixtures θα κοκκίνιζε στις 58 από τις 59 εμφανίσεις που το §5 μέτρησε ως θόρυβο.

---

## 8. Πύλες

**Φ1-Φ2**: 3.28 (`jscpd:diff`) · 3.49/3.53 (ταυτότητα ADR) · N.17 (χωρίς `tsc`).
**Φ4**: 3.8 · 3.33 (`generate:i18n-types`) · 3.34 (shell slice) · 3.51 (ωμά κλειδιά στο SSR).
**Φ8**: **CHECK 3.81** — η ίδια η πύλη ταυτότητας (`docs/gates/3.81.md` ·
`npm run product-identity:report` · `npm run test:product-identity` · `SKIP_PRODUCT_IDENTITY=1`)
+ **CHECK 3.66** (μητρώο πυλών· η 3.81 φτάνει **δηλωμένη**, αλλιώς `undocumented-gate`) +
`gate-index:generate` + **CHECK 3.33/3.34** (άλλαξαν **τιμές** locale, όχι κλειδιά).

---

## Changelog

| Ημερομηνία | Τι |
|---|---|
| **2026-09-13** | ✅ **Φ8 ΕΚΛΕΙΣΕ — Φ8α (μηδενισμός) + Φ8β (CHECK 3.81)**. 🔴 **Η ΠΡΟΫΠΟΘΕΣΗ ΤΗΣ §5 ΗΤΑΝ ΨΕΥΔΗΣ ΚΑΙ ΤΟ ΑΠΕΔΕΙΞΕ Η ΙΔΙΑ Η ΠΥΛΗ**: το δέντρο **δεν ήταν μηδενικό** — πρώτη εκτέλεση **36 αδήλωτες γραφές**, με **τρεις ολόκληρες γραφές άγνωστες σε κάθε απογραφή** (§7 #12), **δύο από τις οποίες έφευγαν σε email**. 🏆 **ΤΟ ΚΥΡΙΟ ΕΥΡΗΜΑ ΕΙΝΑΙ ΜΕΘΟΔΟΛΟΓΙΚΟ**: η χειρωνακτική απογραφή με `grep` σε quoted literals βρήκε **15**· η πύλη **36**. Η διαφορά ήταν **JSXText** (δύο **δημόσιες** σελίδες) και **template literals** (έξι σε HTML email) — **ακριβώς** το τυφλό σημείο που δηλώνει ο N.11 (§7 #13). **Η πύλη αποδείχθηκε αυστηρότερη από τον σχεδιαστή της.** 🔑 **Δύο δικά μου σφάλματα που τα βρήκε η ΕΚΤΕΛΕΣΗ, όχι η ανάγνωση**: (α) το μητρώο δήλωνε `NestorPagonisApp`/`NestorAec` — γραφές **εκτός** του κλειστού συνόλου ⇒ **11 ορφανές + 11 αδήλωτες για τα ΙΔΙΑ σημεία**· θεραπεία: κλειδί **(αρχείο, γραφή)** με γραφές **μέλη του συνόλου**, ακρίβεια στον **λόγο**· (β) δήλωσα το `ifc-authorship.ts` ενώ το `'NestorCAD'` του ζει σε **JSDoc** — η πύλη το κατήγγειλε ως ορφανή, **σωστά**. 🏆 **ΤΟ ΟΝΟΜΑ ΓΡΑΦΕΤΑΙ ΜΙΑ ΦΟΡΑ ΚΑΙ ΣΤΟΥΣ ΤΙΤΛΟΥΣ**: πέντε σελίδες έγραφαν μόνες τους τρεις διαφορετικές γραφές· θεραπεία με τον **μηχανισμό του ίδιου του Next.js** (`title.template` στη ρίζα — εφαρμόζεται στα **παιδιά** και **ποτέ** στο segment που το ορίζει) ⇒ **καμία σελίδα δεν ονομάζει πια το προϊόν**. Νέο `productFilenameToken()` στη ρίζα (**παράγωγο**, όχι δεύτερη σταθερά) για το `Nestor_…_Report.pdf`. 🏆 **ΠΟΥ ΞΕΠΕΡΝΑΜΕ, ΜΕ ΠΗΓΕΣ** (έρευνα 13/09): το **Vale** (Microsoft · GitLab · Mozilla · Red Hat · Linux Foundation) έχει **ακριβώς** τον κανόνα `substitution` αλλά ανασηκώνει με tree-sitter **μόνο σχόλια/docstrings**, αφήνοντας ρητά τα literals ως κώδικα — **δομικά τυφλό** εκεί όπου έζησαν και οι επτά γραφές, και **βλέπον** ακριβώς τα πέντε αθώα σχόλια· το **typescript-eslint** (100+ κανόνες) δεν έχει κανέναν για όρους σε literals· το **ISO 30042 TBX** δίνει το **όνομα** του μοντέλου (*term status*: preferred/admitted/deprecated) χωρίς **κανέναν** μηχανισμό επιβολής σε κώδικα. **Μετρήσεις**: 16.765 αρχεία · 5 γραφές · **24 δηλώσεις / 52 δεμένες** · ⛔ **0/0/0/0**. CHECK 3.66: `documented-gate` 61→**63**, `ghost-row` **0**, αδήλωτες **11** (baseline **ανέπαφη**). |
| **2026-09-12 (γ)** | ✅ **Φ4 + Φ7 προσγειώθηκαν** — 31 αλλαγές σε 17 αρχεία. **Πύλες, εκτελεσμένες**: CHECK 3.34 **331/331** (8 σουίτες) · CHECK 3.51 **84/84** · CHECK 3.28 **0 κλώνοι σε 13 αρχεία** · στοχευμένα **32/32**. **Φ4**: τα **8** locale declarations των κλειδιών κλάσης Α **διαγράφηκαν** (`auth.brand.name` · `navigation.user.name` · `search-results.site.brand` · `showcase.brand.poweredBy`) και οι καταναλωτές διαβάζουν τη **ρίζα**. 🏆 **Η απόφαση είναι τεκμηριωμένη, όχι προτίμηση**: τα ονόματα προϊόντων είναι «do not translate» (Mozilla l10n style guide · Lokalise · Smartling · Phrase) και οι μεταφράσιμες συμβολοσειρές μένουν **χωριστά** από όσες δεν μεταφράζονται (Microsoft globalization) ⇒ η απόκλιση γίνεται **δομικά αδύνατη** αντί «φυλασσόμενη» (N.7.2 #5), και το απορριφθέν ICU `{appName}` διαλύεται (κανένα placeholder ⇒ τίποτα ωμό σε PDF). Επαληθεύτηκε ότι ο `no-hardcoded-strings` είναι **δομικά τυφλός** σε `{PRODUCT_NAME}`: ο visitor map (`:233-238`) έχει μόνο `Literal`/`TemplateLiteral`/`JSXText`/`JSXAttribute`, **καμία** `JSXExpressionContainer`. **Δύο μηχανισμοί ενώθηκαν**: το `showcasePoweredByDefault` συνθέτει πλέον πρόζα + ρίζα και το **React υποσέλιδο καταναλώνει την ΙΔΙΑ συνάρτηση** με το PDF — ήταν δύο πηγές που αποδίδονταν **στην ίδια σελίδα**. Το `©` πήρε **`LEGAL_ENTITY_NAME`** (έγραφε «© 2026 Nestor App» ⇒ απέδιδε δικαιώματα σε **λογισμικό**: λάθος **τύπου**). Αναγέννηση 3.33 + 3.34 **με απόδειξη** (`brand.name`/`appName`/`poweredBy`/`"brand": "Nestor"` = **0** στα τρία παραγόμενα· το `I18n_Auth_Brand` συρρικνώθηκε σε `{ footer }`). **Φ7**: **7** εφεδρείες (§7 #9) και τα **5** χειρόγραφα θέματα του `email.service.ts` περνούν πλέον από τον SSoT `brandedSubject()` — ήταν η **έκτη, κρυφή** διαδρομή υπογραφής, **ζωντανή** μέσω του property-share API, με **άλλο** όνομα. 🔴 **Τρεις παγίδες που μόνο το grep έπιασε** (N.17: απαγορευμένο το `tsc`): (α) το `ShowcaseFooter` απέκτησε **υποχρεωτικό** prop — grep απέδειξε **έναν** καλούντα· (β) αφαιρέθηκε η δέσμευση `t` από **τρία** components — grep απέδειξε **καμία** ορφανή κλήση (στο `AuthScreenChrome` το `useTranslation` **μένει** σωστά για το `AuthToolbar`, άρα το namespace παραμένει δηλωμένο όπως απαιτεί η ADR-744 §18)· (γ) δικό μου σχόλιο έμεινε να παραπέμπει σε κλειδί **που διέγραψα** — ψευδής τεκμηρίωση, διορθώθηκε. 🔴 **ΚΑΙ ΒΡΕΘΗΚΕ ΕΝΑΤΟΣ ΕΞΑΓΩΓΕΑΣ ΠΟΥ Η Φ5 ΕΧΑΣΕ**: `utils/contacts/contact-data-exchange.ts` έγραφε `company: 'Nestor Construct'` στα **metadata εξαγόμενου αρχείου** επαφών — ίδια ερώτηση με το `workbook.creator` («ποιο πρόγραμμα παρήγαγε αυτό το αρχείο;» ⇒ κλάση **Α**), σκληρό, χωρίς env. **Ήταν αόρατο επειδή η απογραφή έψαξε ΟΝΟΜΑ ΣΥΜΒΟΛΟΥ** (`workbook.creator`) και αυτό περνά από το γενικό `exportService.exportData({ metadata })`. 🔑 **Το μάθημα**: απογραφή ταυτότητας ψάχνει **την ΤΙΜΗ**, ποτέ μόνο τον δρόμο που την γράφει. 🔴 **ΣΥΓΚΡΟΥΣΗ ΑΡΙΘΜΟΥ ΠΥΛΗΣ, ΜΕΤΡΗΜΕΝΗ**: το «CHECK 3.79» της §5 **πιάστηκε στο ίδιο 24ωρο** από άλλον agent (ADR-858, «Πύλη του σκιασμένου αρχείου», τρέχει με baseline + 11 άγκυρες) ⇒ ο καρφωμένος αριθμός **αφαιρέθηκε** από **τρία** σημεία· ο αριθμός βρίσκεται **τη στιγμή της υλοποίησης**, όπως οι αριθμοί ADR. ⚠️ Νέα δηλωμένα όρια: **§7 #7-#11**. |
| **2026-09-12 (β)** | ✅ **Φ1 · Φ2 · Φ3 · Φ5 · Φ6 προσγειώθηκαν** — **38 σουίτες / 559 tests πράσινα**. Ρίζα `constants/product-identity.ts`· το `brand` **και** η παράμετρος γλώσσας του `brandedSubject` **έφυγαν**· έξι προτάσεις πρόζας **ξαναγράφτηκαν** (τα ελληνικά έκλιναν το όνομα)· υποσέλιδο + `©` (το `©` πήρε το **νομικό πρόσωπο**)· **11** εξαγωγείς PDF/Excel· TOTP issuer + αρχείο εφεδρικών κωδικών. 🔴 **Δύο ευρήματα που τα tests ΔΕΝ θα έπιαναν**: (α) η διαγραφή του `brand` άφησε **δύο σιωπηλές αναγνώσεις** στο `app-message-email.ts` που επέστρεφαν `undefined` — **292 tests ήταν πράσινα από πάνω τους**, και τις βρήκε **μόνο** το grep (N.17: απαγορευμένο το `tsc`)· (β) ξεχάστηκε μια **εισαγωγή** στο `base-email-template.ts` ⇒ 17 κόκκινα, που **τα έπιασε η δοκιμή**. Δύο άγκυρες στράφηκαν σε **ζωντανή** ερώτηση: το `Open in Nestor` ήταν **υποσυμβολοσειρά** του νέου ονόματος (ψευδώς πράσινο) και το `not.toContain('ΝΕΣΤΩΡ')` θα γινόταν **νεκρός φρουρός**. Νέα άγκυρα **Β4** εκτελεί τον φρουρό της **ουράς**. |
| **2026-09-12** | 🔵 **Γέννηση.** Η εντολή «μία ταυτότητα παντού» αποκάλυψε **τέσσερα** ονόματα στο **ίδιο** email — και το ένα (`Nestor Construct`, η γραμμή `From:`) **δεν υπήρχε σε καμία απογραφή**. Μετρήθηκε **γιατί** κανείς δεν το είδε: ο κανόνας `no-hardcoded-strings` θεωρεί «τεχνική» κάθε **λατινική** συμβολοσειρά με κενό ⇒ οι τέσσερις λατινικές γραφές ήταν **αόρατες**, ενώ η ελληνική θα είχε αναφερθεί. Απόφαση: **ταξινόμηση σε πέντε κλάσεις πριν από κάθε μετονομασία** (τρεις δεν μετονομάζονται — η κλάση Δ θα έσπαγε αρχεία ήδη γραμμένα στον δίσκο)· ρίζα **χωρίς εισαγωγές** στο πρότυπο του `languages.ts`· **διαγραφή** του `brand` και της παραμέτρου γλώσσας του `brandedSubject`· και `KNOWN_PAST_SPELLINGS` για να μη **διπλοϋπογραφούν** τα ήδη ουραγμένα θέματα. |
