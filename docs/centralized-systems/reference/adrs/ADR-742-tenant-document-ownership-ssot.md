# ADR-742 — «Ανήκει ΑΥΤΟ το έγγραφο;»: μία ερώτηση, ένα δόγμα αποκάλυψης

- **Κατάσταση:** Φάση Α ✅ υλοποιημένη (commit `01f8dcbc`) · Φάση Β ✅ υλοποιημένη · Φάσεις Γ+Δ ⏸️
- **Ημερομηνία:** 2026-07-31
- **Σχετικά:** ADR-255 (per-document ownership — απορροφάται), ADR-232 (έγγραφα υπεργραφείου),
  ADR-702 (tenant query scope — **ΔΕΝ** ενοποιείται), ADR-356 (super-admin scope — **ΔΕΝ** ενοποιείται),
  ADR-699 (share resolvers), ADR-734 §7.1 (το ίδιο δόγμα, ήδη γραμμένο για το στρώμα του πράκτορα)

---

## 1. Το πρόβλημα

Η ερώτηση **«ανήκει αυτό το έγγραφο στον πελάτη που ρωτάει;»** ήταν γραμμένη σε
**τέσσερα** ανεξάρτητα συστήματα, από πέντε διαφορετικές στιγμές:

| # | Πού | Τι ήταν |
|---|---|---|
| 1 | `lib/auth/tenant-isolation.ts` (ADR-255, Ιαν 2026) | 6 φύλακες — **το ίδιο 20γραμμο block αντιγραμμένο 6 φορές μέσα στο ίδιο αρχείο** |
| 2 | `lib/auth/tenant-ownership.ts` (2026-07-30) | καθαρό module, γραμμένο από κάποιον που **δεν έβλεπε** το #1 |
| 3 | `services/sharing/resolver-core/share-entity-access.ts:78` | `=== user.companyId`, client SDK |
| 4 | `lib/firestore/entity-creation.service.ts:132` | ίδιος έλεγχος για ήδη διαβασμένο γονέα |

Συν `filterSnapshotsByTenant` που την ξανάγραφε για παρτίδες, και **46 αρχεία**
με χειρόγραφο `x.companyId !== ctx.companyId`.

### 🔴 Η αιτία — και είναι η μόνη που έχει σημασία

**Το `tenant-isolation.ts` δεν είχε ποτέ καταχώρηση στο `.ssot-registry.json`.**

Κανένα gate δεν φύλαγε την περιοχή· κανένα `ssot:discover` δεν το ανέδειξε. Έτσι,
τον Ιούλιο, κάποιος έγραψε καλόπιστα ένα δεύτερο καθαρό module για το ίδιο
πράγμα — δεν υπήρχε τρόπος να το βρει.

> **Ένα SSoT χωρίς εγγραφή στο μητρώο δεν είναι SSoT — είναι απλώς ένα αρχείο.**

Ίδιο σχήμα με το «0 = κανείς δεν κοίταξε» των N.11/N.12. Αν αυτό το ADR κάνει
μόνο ένα πράγμα σωστά, είναι **η καταχώρηση στο μητρώο**, όχι η ενοποίηση.

---

## 2. ⛔ Τι ΔΕΝ ενοποιείται

| Αρχείο | Ερώτηση | Πότε |
|---|---|---|
| `tenant-scope.ts` (ADR-702) | «ποιανού εταιρείας γραμμές να **λιστάρω**;» | **πριν** το read |
| `super-admin-scope.ts` (ADR-356) | ο διακόπτης εταιρείας στο **header** | **πριν** το read |
| `tenant-ownership.ts` (**εδώ**) | «ανήκει **ΑΥΤΟ**;» | **μετά** το read |

Διαφορετική είσοδος, διαφορετική στιγμή. Η ενοποίησή τους αλλάζει **ποιος βλέπει τι**.

---

## 3. Η απόφαση

### 3.1 Ο πυρήνας — καθαρός επίτηδες

`lib/auth/tenant-ownership.ts`: **μηδέν Firebase, μηδέν `server-only`.**

Αυτό δεν είναι αισθητική. Είναι η προϋπόθεση για να μοιράζονται **την ίδια
σύγκριση** ο browser (share resolvers, `boq-repository`) και το Admin SDK.

> Το σχόλιο του ADR-699 έλεγε ότι η ενοποίηση «θα έσερνε το `firebase-admin` σε
> client bundle». Ήταν **σωστό για τα περιβλήματα** και **λάθος για τη σύγκριση
> δύο strings** — και ακριβώς η σύγκριση ήταν αυτή που είχε διπλασιαστεί.
> Το επιχείρημα του bundle κάλυπτε, χωρίς να το θέλει, ένα χειρόγραφο αντίγραφο.

### 3.2 Τρεις **ονομασμένες** έννοιες, όχι σημαίες

Μια boolean σε κλήση ασφαλείας είναι αυτό που ρυθμίζεται λάθος στο review:

| Συνάρτηση | Τι κάνει |
|---|---|
| `isOwnedByCompany` / `isPayloadOwnedByCompany` | **η ερώτηση**, χωρίς παρενέργειες |
| `ownedOrNull` | **σιωπηλή** πολιτική — ξένο ≡ ανύπαρκτο· **δεν μαρτυρά ύπαρξη** |
| `assertOwnedByCompany` | **ρητή άρνηση** — **μαρτυρά ύπαρξη**, συνειδητή ανταλλαγή |
| `concealCrossTenant` | **η απόφαση αποκάλυψης** στο σύνορο HTTP |

### 3.3 Ο κανόνας αποκάλυψης

> **«Θα μπορούσε ο καλών να το μάθει νόμιμα με άλλον τρόπο;»**
> Ναι → πες την αλήθεια (403). Όχι → σιώπησε (404).

**ΟΧΙ** «διάλεξε κωδικό». **ΟΧΙ** «να είμαστε ομοιόμορφοι».

Στον Νέστορα το `list` είναι πάντα tenant-scoped → κανονικός χρήστης δεν μπορεί
ποτέ να μάθει νόμιμα ότι υπάρχει ξένο id → **σιωπή**. Ο bypass ρόλος έχει ήδη
cross-tenant ορατότητα → **ειλικρινές 403**, που του σώζει τη διάγνωση.

⚠️ Η εξαίρεση super-admin **δεν είναι νέο δόγμα** — είναι ήδη αυτό που κάνει το
`tenant-isolation.ts` στις έξι `require*InTenant`. **Ευθυγραμμίζεται, δεν εισάγεται.**

#### Πηγές (έρευνα 2026-07-31)

| Πλατφόρμα | Γραμμή |
|---|---|
| **GitHub** | 404 — ρητά «to avoid confirming the existence of private repositories» |
| **Google Drive** | 404 — το `notFound` καλύπτει σκόπιμα και τα δύο |
| **Figma** | 404 — άλλαξε **από** 403 και δεν το γύρισε πίσω παρά τις διαμαρτυρίες |
| **Autodesk APS** | 403/404 διακριτά — το πλησιέστερο CAD/BIM **δεν** ακολουθεί (B2B με ρητό project membership) |
| **OWASP / IDOR** | 404 για μη-δημόσιους πόρους |
| 🏆 **AWS S3** | **παράγει** τον κανόνα: έχεις `s3:ListBucket` → 404· δεν έχεις → 403 ακόμη κι όταν λείπει |

### 3.4 🔑 Η κρίσιμη σχεδιαστική απόφαση (Φάση Β)

> Η **υπηρεσία** ρίχνει **πάντα** τυποποιημένο `CrossTenantAccessError` (αλήθεια).
> Το **route** αποφασίζει τι φεύγει στο σύρμα.

- Μηδέν αλλαγή υπογραφών στα services
- Η απόφαση ζει στο **σύνορο αποκάλυψης** — το μόνο στρώμα που ξέρει *ποιος* ρωτάει
- Εσωτερικοί καλούντες και logs κρατούν πλήρη εικόνα

⚠️ **Το μεταμφιεσμένο 404 πρέπει να είναι ΠΑΝΟΜΟΙΟΤΥΠΟ με το γνήσιο.** Αν το
γνήσιο λέει `Text template not found: tpl_x` και το ψεύτικο σκέτο `Not found`,
**το ίδιο το κείμενο γίνεται μαντείο**. Παράγεται από τον **ίδιο constructor**.

---

## 4. Ένα πραγματικό σφάλμα που διορθώθηκε στην πορεία

Και οι τέσσερις χειρόγραφες μορφές απαντούσαν **`true`** όταν το έγγραφο **και**
ο καλών είχαν κενό `companyId`:

```ts
snap.data().companyId === user.companyId   // '' === ''  ⇒  true
```

Καλών με χαλασμένο token (`companyId: ''`) έβλεπε κάθε έγγραφο με κενό
`companyId`. Το `isPayloadOwnedByCompany` αντιμετωπίζει το κενό ως **απουσία**
tenant, ποτέ ως tenant που τυχαίνει να ταιριάζει.

---

## 5. Τι υλοποιήθηκε (Φάση Α)

| Αρχείο | Αλλαγή |
|---|---|
| `lib/auth/tenant-ownership.ts` | + `isPayloadOwnedByCompany`, + `concealCrossTenant` |
| `lib/auth/tenant-isolation.ts` | 6 αντίγραφα → **1** `requireDocInTenant` + 6 δηλώσεις· `filterSnapshotsByTenant` καλεί το SSoT |
| `services/sharing/resolver-core/share-entity-access.ts` | `canShare` καλεί το SSoT· διορθώθηκε το σχόλιο που κάλυπτε το αντίγραφο |
| `lib/firestore/entity-creation.service.ts` | `verifyTenantAccess` καλεί το SSoT |
| `.ssot-registry.json` | **νέο module `tenant-document-ownership`** ← το σημαντικότερο |
| `lib/auth/__tests__/tenant-isolation.test.ts` | **νέο** — οι 6 φύλακες είχαν **μηδέν** κάλυψη |

**Συμβόλαιο αμετάβλητο:** ονόματα, υπογραφές, μηνύματα, status, codes και
εγγραφές audit αναπαράγονται αυτούσια — **41 αρχεία καταναλωτές δεν αγγίχτηκαν**.

### Επαλήθευση

- 70/70 tests πράσινα (`tenant-ownership` + `tenant-isolation`)
- **Mutation-verified 4/4**: αφαίρεση κανόνα κενού · λάθος συλλογή σε δήλωση ·
  αφαίρεση bypass · αλλαγή μηνύματος 404 — **όλα κοκκίνισαν**
- `jscpd:diff` καθαρό (N.18)
- Τα 3 `forbiddenPatterns` **μετρημένα στο μηδέν** εκτός allowlist

---

## 6. Παράπλευρο εύρημα — ένα ψευδές κόκκινο που κόστισε 16 ημέρες

Το `tenant-scope-anchor.test.ts` (ADR-702) ήταν κόκκινο στο CI από **2026-07-15**
και είχε καταγραφεί σε handoffs ως «προϋπάρχον, μην το αγγίξεις».

**Ο κώδικας ήταν σωστός.** Το `buildings/route.ts` είχε εξαγάγει το `GET` του στο
`buildings-list.handler.ts` για SRP — σωστό refactor που **κράτησε** το
`resolveTenantListScopeFromUrl`. Το anchor έψαχνε το string στο `route.ts` και
δεν το έβρισκε πια.

Το anchor **δεν είχε άδικο για το δόγμα — είχε άδικο για τη διεύθυνση.**

Διόρθωση: το anchor ακολουθεί πλέον τα re-exports (`export { GET } from './x'`)
και ρωτά ό,τι ρωτά και το ADR — *περνά αυτό το endpoint από το δόγμα;* —
αφήνοντας το SRP να μετακινεί κώδικα ελεύθερα. Προστέθηκαν δύο **self-tests** του
ίδιου του follower (πιάνουν και «σταμάτησε να ακολουθεί» και «μάζεψε τα πάντα»).
Mutation-verified 3/3.

> **Μάθημα:** ένα anchor που κοκκινίζει για λάθος λόγο είναι χειρότερο από
> ανύπαρκτο — εκπαιδεύει τους πάντες να το αγνοούν **και** πιάνει τη θέση όπου
> θα έμπαινε ένα που δουλεύει.

---

## 7. Τι υλοποιήθηκε στη Φάση Β (2026-07-31)

Και τα τέσσερα endpoints περνούν πλέον από `concealCrossTenant`.

### 7.1 Το σχήμα που εφαρμόστηκε — και γιατί αυτό

> Η ταυτότητα γνήσιου και μεταμφιεσμένου 404 δεν είναι **θέμα προσοχής**· είναι **δομική**.

Κάθε διαδρομή δίνει **ένα** `notFound` callback που εξυπηρετεί **και τους δύο** κλάδους:

```ts
backgroundErrorResponse({ err, ctx, notFound: () => backgroundNotFoundResponse(id) })
// conceal: notFound   ← το ΙΔΙΟ callback, όχι δεύτερη γραφή του ίδιου σώματος
```

Δεν υπάρχει τρόπος να αποκλίνουν χωρίς να αλλάξουν **και τα δύο**. Αυτό κατοχυρώνεται με `toEqual` —
όχι «μοιάζει», **ίσο**.

Παραμετροποιείται επειδή το `DELETE` έχει δικό του σχήμα (`{ deleted: false }`): η μεταμφίεση ακολουθεί
**το σχήμα της διαδρομής**, αλλιώς το σχήμα και μόνο θα πρόδιδε τη διαφορά.

### 7.2 Τα αρχεία

| Αρχείο | Αλλαγή |
|---|---|
| `api/dxf/_domain-error-mapping.ts` | **νέο** — ο κανόνας αποκάλυψης, μία φορά για πρότυπα + λεξικό |
| `api/dxf/_serialize-audit-fields.ts` | **νέο** — τα 6 πεδία audit σε μορφή σύρματος, μία φορά |
| `api/dxf/_domain-route.ts` | **νέο** — η **σύνδεση** ενός route (ρυθμός + ταυτότητα + δικαιώματα + `try/catch`), μία φορά για **12** endpoints (§7.8) |
| `api/dxf/text-templates/ai/_ai-route-helpers.ts` | το 12ο αντίγραφο του κελύφους → **εξειδίκευση** του κοινού runner |
| `api/dxf/{text-templates,custom-dictionary}/_helpers.ts` | `mapServiceError(err, **ctx**)` — `ctx` **υποχρεωτικό** |
| 6 routes DXF (12 κλήσεις `errorResponse`) | περνούν `ctx` |
| `api/floorplan-backgrounds/_background-error-response.ts` | **νέο** — το σύνορο των routes υποβάθρου |
| `services/floorplan-background/background-ownership.ts` | **νέο** — «δικαιούμαι να γράψω σε ΑΥΤΟ;», 4 καλούντες |
| `floorplan-scale.service.ts` | **9ο σημείο**: σκέτο `Error` → `CrossTenantAccessError` μέσω SSoT |
| `floorplan-backgrounds/[id]/route.ts` · `.../calibrate/route.ts` | `instanceof` αντί `msg.includes(...)` |
| `lib/auth/tenant-ownership.ts` | 🔴 `assertOwnedByCompany` → `isPayloadOwnedByCompany` (§7.5) |

### 7.3 Λάθος #2: «cross-tenant» και «locked» στον ίδιο κλάδο

Το `[id]/route.ts:154` έγραφε `msg.includes('Cross-tenant') || msg.includes('locked')` και έβγαζε **και τα
δύο** ως `409 FORBIDDEN`. Είναι **αντίθετα**:

- «locked» μαρτυρά ότι ο πόρος **υπάρχει και σου ανήκει** — πληροφορία που ο χρήστης δικαιούται
- «cross-tenant» είναι **ακριβώς** αυτό που δεν επιτρέπεται να μαρτυρηθεί

Πλέον `BackgroundLockedError` → ξεχωριστό `409 LOCKED`, **χωρίς** μεταμφίεση.

### 7.4 Το string-matching ως έλεγχος πρόσβασης

`msg.includes('Cross-tenant')` ήταν **δύο** ζωντανά σημεία — όχι τρία· το `floor-wipe-queries.ts` απλώς
logger-άρει. Μια αθώα αλλαγή διατύπωσης σε μήνυμα υπηρεσίας θα μετέτρεπε **σιωπηλά** την άρνηση ασφαλείας
σε `500`. Τώρα είναι **μηδέν**, και το `includes\('Cross-tenant'\)` μπήκε **4ο `forbiddenPattern`** στο
module `tenant-document-ownership`.

⚠️ Το `isRoleBypass(` **δεν** μπήκε: έχει ~80 νόμιμες χρήσεις RBAC — θα ήταν θόρυβος, όχι σήμα.

### 7.5 🔴 Τρύπα που η Φάση Α είχε αφήσει ανοιχτή — τη βρήκε test

Η §4 περιγράφει το σφάλμα του κενού `companyId`. Η Φάση Α το έκλεισε **μόνο στο σιωπηλό μονοπάτι**
(`isPayloadOwnedByCompany` → οι έξι `require*InTenant`). Η `assertOwnedByCompany` συνέχιζε να ρωτά με
σκέτο `===`, ενώ **και οι τέσσερις** καλούντες της της δίνουν `snap.data() as XDoc`:

> ο **τύπος υπόσχεται** `companyId: string`· η **βάση δεν το εγγυάται**. Το `as` ήταν το ψέμα.

Καλών με χαλασμένο token (`companyId: ''`) περνούσε σε κάθε έγγραφο με κενό `companyId`. Η υπογραφή
δέχεται πλέον `MaybeTenantOwned` και ρωτά `isPayloadOwnedByCompany` — μία αλλαγή, τέσσερα σημεία κλήσης
διορθωμένα, μηδέν αλλαγή στους καλούντες.

**Το βρήκε ένα test της Φάσης Β, όχι ανάγνωση κώδικα.** Δύο συνεδρίες είχαν διαβάσει αυτή τη συνάρτηση.

### 7.6 Επαλήθευση

- **386/386 πράσινα** σε 30 suites
- **15/15 μεταλλάξεις σκοτωμένες** — μεταξύ άλλων: χειρόγραφο `'Not found'` αντί ίδιου constructor·
  αγνόηση ρόλου· απόκλιση γνήσιου/μεταμφιεσμένου· «locked» που μεταμφιέζεται· επιστροφή στο `===`·
  υπηρεσία που παύει να καλεί τον SSoT· αντιμετάθεση `createdBy`/`updatedBy`· **καρφωμένος ρόλος στον
  εκτελεστή**· log που χάνει την αλήθεια
- **N.18: 6 clones → 0** (2 δικά μου, 4 προϋπάρχοντα — Boy Scout) σε **18** αρχεία
- `test:registry-golden` **102/102**· τα 4 `forbiddenPatterns` μετρημένα **στο μηδέν** εκτός allowlist
- **ΟΧΙ tsc (N.17)**

### 7.8 Ο εκτελεστής route — γιατί έγινε κι αυτός ένας

Δώδεκα endpoints έγραφαν το **ίδιο** δεκάγραμμο κέλυφος: ρυθμιστής ρυθμού → ταυτότητα → δικαιώματα →
`try/catch` → καταγραφή → χαρτογράφηση. Το `jscpd` το μετρούσε ως clones **ήδη στο HEAD**, οπότε
οτιδήποτε άγγιζε κανείς εκεί έκοβε το CHECK 3.28.

Δεν είναι «λίγος κώδικας που μοιάζει» — είναι η **αλυσίδα ασφαλείας** κάθε endpoint. Αν ένα αντίγραφο
ξεχάσει τον ρυθμιστή ρυθμού ή τυλίξει το `try/catch` λίγο πιο μέσα, η διαφορά **δεν φαίνεται πουθενά**:
το endpoint απαντά κανονικά, απλώς είναι λιγότερο προστατευμένο από τα αδέλφια του.

⚠️ Ο ρυθμιστής είναι σκόπιμα **καρφωμένος** σε `STANDARD` και δεν παραμετροποιείται: και τα δώδεκα
endpoints είχαν `withStandardRateLimit`. Παράμετρος με μία μόνο τιμή σε όλους τους καλούντες δεν είναι
ευελιξία — είναι πρόσκληση να διαφοροποιηθεί κάποιος αθόρυβα.

🔴 **Ο εκτελεστής είναι το σημείο όπου όλη η απόφαση αποκάλυψης κρέμεται από μία μεταβλητή** — το
`ctx.globalRole` που παραδίδει στη χαρτογράφηση. Ένα καρφωμένο `'super_admin'` εκεί θα άνοιγε ξανά το
μαντείο για **όλο** το πεδίο ορισμού, με κάθε άλλο test πράσινο. Γι' αυτό υπάρχει
`__tests__/domain-route-ctx.test.ts`: μοναδική δουλειά του να αποδεικνύει ότι φτάνει ο **σωστός**
καλών, όχι ένας ρόλος.

### 7.7 ⚠️ Γνωστή ασυμμετρία — δηλωμένη, όχι κρυμμένη

Η εξαίρεση super-admin ισχύει όπου η **υπηρεσία ρίχνει**. Το `FloorplanBackgroundService.getById` είναι
**σιωπηλό για όλους** (συνειδητή απόφαση της Φάσης Α: ξένο ≡ ανύπαρκτο), άρα από εκείνο το μονοπάτι
ούτε ο bypass ρόλος παίρνει `403`. Το να αλλάξει θα απαιτούσε αλλαγή υπογραφής στην υπηρεσία — που το
§3.4 ρητά αποκλείει.

---

## 7ter. Η σάρωση Γ+Δ — η **αναγνώριση** (2026-07-31)

Ο Γιώργος ενέκρινε **ενιαία** σάρωση Γ+Δ (όχι σε δόσεις ανά domain): το
`forbiddenPattern` μπαίνει μόνο όταν μεταναστεύσουν **όλα**, οπότε οι δόσεις
κυνηγούν κινούμενο στόχο.

### 7ter.1 Τα μετρημένα — **65 σημεία σε 45 αρχεία**

| Κατηγορία | Σημεία | Κριτήριο |
|---|---:|---|
| **Γ** | **52** | ρωτά «ανήκει ΑΥΤΟ;» μετά το read, αρνείται με 403 |
| **Δ** | **10** | ίδια ερώτηση, **ήδη** σιωπά |
| **⛔ ΕΞΩ** | **3** | άλλη ερώτηση μεταμφιεσμένη — δεν αγγίζεται |

🔴 **Το χονδρικό φίλτρο «έχει 403 στο αρχείο» ήταν λάθος και προς τις δύο
κατευθύνσεις.** Από τα 9 «υποψήφια Δ» μόνο **3** ήταν όντως Δ· και βρέθηκαν **6
Δ κρυμμένα μέσα σε αρχεία που είχαν 403** (τα procurement services: το `getX`
σιωπά, το `updateX` ρίχνει, **ίδιο αρχείο**). ⇒ **Η ταξινόμηση απαιτεί ανάγνωση,
όχι grep** — αυτός είναι όλος ο λόγος ύπαρξης του Βήματος 1.

Τα 3 ⛔ ΕΞΩ: δύο **log-only** κλάδοι super-admin (`projects/[projectId]/route.ts`,
`project-mutations.service.ts` — γράφουν log, δεν αποφασίζουν) + το φίλτρο λίστας
του `floorplan-overlays.handlers.ts` πάνω σε **ήδη** tenant-scoped query (ADR-702).
⚠️ Η πρόβλεψη ότι «πολλά projects θα είναι ΕΞΩ λόγω ADR-356» **δεν επιβεβαιώθηκε**:
κανένα `superAdminOverride`, κανένα `resolveSuperAdminProjectScope` — είναι η ίδια
bypass εξαίρεση που κάνει ήδη το `tenant-isolation.ts`.

### 7ter.2 🔴 Ο κλάδος `reveal` είναι **απρόσιτος** στη Γ — δομική διαφορά από τη Β

Στη Φάση Β η υπηρεσία έριχνε **πάντα** και ο bypass ρόλος έπαιρνε ειλικρινές 403.
Τα 52 Γ γράφουν `!isSuperAdmin && companyId !== …`: ο bypass **παίρνει το
έγγραφο**, δεν φτάνει ποτέ σε άρνηση. Αφελής αντικατάσταση με
`assertOwnedByCompany` (που **δεν έχει εξαίρεση super-admin by design**) θα έδινε
403 στον υπεργραφέα ⇒ **παλινδρόμηση**. Το `!isSuperAdmin &&` μένει **έξω** από
την κλήση SSoT· πρακτικά η Γ εδώ είναι «403 → 404 **για τους μη-bypass**».

### 7ter.3 Τι λέει η βιομηχανία για την ασυμμετρία bypass (έρευνα 2026-07-31)

Πέντε σημεία μπλοκάρουν **και** τον super-admin ενώ τα αδέρφια τους όχι
(`contacts/[contactId]/route.ts`, `contacts/…/properties`, `ika/…/insurance-class`,
`parking/route.ts:214` **ενώ το `:98` στο ίδιο αρχείο** έχει bypass,
`admin/role-management/…`). Το ερώτημα «bypass παντού ή πουθενά;» είναι **λάθος
ερώτημα**:

> **Zero Standing Privilege**: καμία μόνιμη, always-on προνομιακή πρόσβαση.
> Cross-tenant admin ενέργειες με **just-in-time elevation, χρονικά φραγμένη,
> ελεγχόμενη** — **ποτέ standing cross-tenant access**.
> Η **Google** το εφαρμόζει στον εαυτό της: «by default, Google does not have
> access to customer data»· το **Access Transparency** καταγράφει ποιος, πότε,
> σε τι και **γιατί**· το **Access Approval** δίνει βέτο στον πελάτη.

⇒ Το `isRoleBypass(ctx.globalRole)` σε **47 από τα 52** σημεία **είναι** standing
cross-tenant privilege, σκόρπιο σε 45 αρχεία, χωρίς λόγο/όριο/audit. Τα 5
«αυστηρά» δεν είναι το σφάλμα — είναι τα **μόνα** που κάνουν το σωστό.

**Απόφαση**: η Γ+Δ **δεν αλλάζει ποιος βλέπει τι** (αλλάζει *τι λέμε* στην
άρνηση)· η ασυμμετρία **δηλώνεται** αντί να «διορθωθεί» σιωπηλά. Η μετάβαση σε
JIT elevation είναι **ξεχωριστή δουλειά** — αλλά η ραφή προετοιμάζεται: η bypass
απόφαση παύει να είναι σκόρπιο `!isSuperAdmin &&` και γίνεται ονομασμένη έννοια,
ώστε το JIT να μπει σε **ένα** σημείο αντί για 47.

### 7ter.4 Αρχιτεκτονική: PEP/PDP, όχι «ένας φύλακας»

> **OWASP Multi-Tenant**: *«Implement authorization checks at the data access
> layer, not just API layer»* — **layered**.
> **PDP/PEP**: κάθε υπηρεσία είναι **PEP** (εφαρμόζει)· η πολιτική ζει σε **PDP**
> (αποφασίζει)· το **embedded PDP** (in-memory) αποφεύγει το network hop.

Αυτό **είναι** ήδη το σχήμα της Φάσης Β: `tenant-ownership.ts` = PDP, route = PEP.
Το `requireProjectInTenant` **δεν** ταιριάζει επειδή είναι PDP+PEP+**PIP** μαζί
(κάνει το ίδιο το read) ενώ τα 45 αρχεία έχουν **ήδη διαβάσει** — θα έκανε δεύτερο
`get()`. ⇒ **λεπτές κλήσεις PEP + ανά-domain adapter** όπου το σχήμα
επαναλαμβάνεται πανομοιότυπα· το CHECK 3.28 **επιβάλλει** τον adapter εκεί που
υπάρχει επανάληψη και αφήνει ήσυχα τα ιδιόμορφα.

### 7ter.5 Δύο σημεία που **δεν** αντικαθίστανται 1-προς-1

- `messages/delete/route.ts` — **bulk**, απαντά `200` με
  `errors[{reason:'belongs to different company'}]`: per-item soft-fail, όχι throw.
- `rfq-service.getRfq` — καλείται **και** από public vendor portal (HMAC token)
  όπου **δεν υπάρχει `ctx.globalRole`** ⇒ σκέτο `ownedOrNull`, χωρίς
  `concealCrossTenant` (δεν υπάρχει ρόλος να ρωτηθεί).

---

## 7quater. Φάση Δ — το στρώμα του πράκτορα ΤΝ **DONE** (2026-07-31)

Τα 5 σημεία του `services/ai-pipeline/tools/handlers/**` μεταναστεύθηκαν πρώτα,
γιατί εκεί το μαντείο ύπαρξης δεν ήταν θεωρητικό: **δύο handlers έλεγαν αυτολεξεί
την αλήθεια στο μοντέλο**.

| Handler | Τι έφτανε στο μοντέλο **πριν** |
|---|---|
| `document-reader` | `Access denied: file belongs to a different company.` |
| `file-lifecycle` | `Access denied: file belongs to another company.` |
| `banking` | `Access denied` — **διακριτό** από το «Contact not found» |
| `relationship` | `Access denied — source contact.` |
| `firestore` | `{success:false}` ενώ το γνήσιο κενό είναι `{success:true,data:null}` |

**Νέο**: `services/ai-pipeline/tools/tool-tenant-guard.ts` — `resolveOwnedToolDoc`,
διακριτή ένωση ώστε ο καλών **να μην μπορεί** να ξεχάσει την άρνηση (το `data` δεν
υπάρχει στον αρνητικό κλάδο). Ένα `notFound` callback εξυπηρετεί **και** «δεν
υπάρχει» **και** «ανήκει αλλού» (§7.1).

🔴 **Εδώ η σιωπή δεν είναι επιλογή, είναι το μόνο σωστό**: το `AgenticContext`
φέρει `companyId` + `isAdmin` — και το `isAdmin` είναι ρόλος **εντός** εταιρείας,
**όχι** ο καθολικός bypass. Δεν υπάρχει ρόλος να ρωτηθεί ⇒ ο κλάδος `reveal` δεν
έχει νόημα σε αυτό το στρώμα. Αν ποτέ το `AgenticContext` αποκτήσει `globalRole`,
**αυτό** είναι το ένα σημείο που θα το ρωτήσει, όχι πέντε handlers.

🔴 **Το `firestore-handler` ήταν σπασμένο στο ΣΧΗΜΑ, όχι στο κείμενο.** Το κείμενο
έλεγε ήδη «Document not found» — αλλά το γνήσιο κενό απαντούσε
`{success:true,data:null,count:0}` και το ξένο `{success:false,…}`. **Αρκούσε το
`success` flag** για να ξεχωρίσει τα δύο, χωρίς να διαβαστεί λέξη. ⇒ **Η
μεταμφίεση κρίνεται στο ολόκληρο σχήμα της απόκρισης, όχι στο μήνυμα.**

🔴 **Η σουίτα τεκμηρίωνε το ίδιο το σφάλμα, πράσινη.** Δύο tests στέκονταν
δίπλα-δίπλα: το ένα απαιτούσε `success:true` για «δεν υπάρχει», το άλλο
`success:false` για «ξένο». Ξαναγράφτηκε ώστε να ελέγχει **ταυτότητα** (`toEqual`
μεταξύ των δύο κλάδων) — που είναι το πραγματικό συμβόλαιο.

🐛 **Κενό ασφαλείας που έκλεισε στην πορεία**: το `relationship-handler` έλεγχε
**μόνο την πηγή**. Ο πράκτορας μπορούσε να συνδέσει δική του επαφή με **ξένη**,
και η επιτυχία επιβεβαίωνε ότι το ξένο id υπάρχει. **Η σχέση αγγίζει δύο έγγραφα,
άρα ρωτάει δύο φορές.** ⚠️ Η αφαίρεση του νέου φύλακα άφηνε **12/12 πράσινα** —
γράφτηκαν 4 tests που τη σκοτώνουν.

⚠️ **ΑΛΛΑΓΗ ΣΥΜΠΕΡΙΦΟΡΑΣ (εγκεκριμένη)**: `banking` και `relationship` έγραφαν
`data?.companyId && data.companyId !== ctx.companyId` — άφηναν να περάσει έγγραφο
**χωρίς** `companyId`. Ο SSoT ρωτά `isPayloadOwnedByCompany` ⇒ **έγγραφο χωρίς
tenant δεν ανήκει σε κανέναν** (§4).

### 7quater.1 Boy Scout: ο **εκτελεστής ανάγνωσης** του πράκτορα

Το CHECK 3.28 ανέδειξε **5 προϋπάρχοντες κλώνους** στα ίδια αρχεία (μετρημένους
**ήδη στο HEAD**, άρα «χωριστά commits δεν τα λύνουν»). Καθαρίστηκαν όλοι:

| Κλώνος | Λύση |
|---|---|
| `nullableString` ×2 handlers | → `executor-shared` (κενό ≡ απόν· αλλιώς άδειο πεδίο **σβήνει** τιμή) |
| `accountId` boilerplate ×2 | → `requireAccountId` |
| ο βρόχος «filters→where→orderBy→limit» ×4 | → **νέο** `handlers/firestore-query-plan.ts` |
| η αλυσίδα «επιτρεπτή συλλογή → RBAC → tenant scope» ×2 | → `planScopedRead` |
| το άνοιγμα των δύο read paths | → **`withScopedRead`** (ο εκτελεστής) |

🔴 **Η παγίδα που πληρώθηκε στην εξαγωγή — μην την «απλοποιήσεις» πίσω:** το
τελευταίο καταφύγιο του fallback έγραφε **σκληρά** `where('companyId','==',…)`,
αγνοώντας τον operator. **Δεν ήταν αμέλεια**: το `enforceCompanyScope`, όταν βρει
**υπάρχον** φίλτρο `companyId`, αντικαθιστά **μόνο την τιμή** (`{...f, value}`) και
**κρατά τον operator που έστειλε το μοντέλο** — και το `mapOperator` δέχεται `!=`
και `not-in`. Αφελής χρήση του κοινού βρόχου θα μετέτρεπε το καταφύγιο σε **«όλες
οι ΑΛΛΕΣ εταιρείες»**. Καρφώθηκε με `tenantEqualityFilter` + 4 tests
(`it.each(['!=','not-in','in','>='])`).

Ο **`withScopedRead`** υπάρχει για τον ίδιο λόγο με τον `_domain-route.ts` της
Φάσης Β (§7.8): κάνει το «διάβασε χωρίς tenant scope» **μη εκφράσιμο** — δεν
υπάρχει μονοπάτι προς το `db` που να μην έχει περάσει από την αλυσίδα.

**Επαλήθευση**: **1220/1220** σε 76 suites (`npm run test:ai-pipeline:all`, N.10)·
**10/10 μεταλλάξεις** (σκέτο `===`· φύλακας που παύει να ρωτά· απόκλιση
γνήσιου/μεταμφιεσμένου· αφαίρεση φύλακα πηγής· αφαίρεση φύλακα στόχου· καταφύγιο
που σέβεται τον operator· tenant που χάνεται στο τελευταίο attempt· tenant scope
εκτός αλυσίδας· αντιστροφή σειράς attempts· εκτελεστής που παρακάμπτει)·
**N.18 καθαρό**· N.7.1 όλα τα αρχεία <500 γρ.

---

## 7quinquies. Ομάδα 2 — procurement services **DONE** (2026-08-01)

Επτά υπηρεσίες, **20** σημεία (τα 19 της §7ter **+1 που η σάρωση έχασε**), δύο
σύνορα HTTP, μηδέν χειρόγραφη σύγκριση `companyId` που να έχει απομείνει.

### 7quinquies.1 🔴 Το 20ό σημείο — γιατί το έχασε η σάρωση

`rfq-line-service.ts:45` έγραφε:

```ts
const rfqCompanyId = (snap.data() as { companyId: string }).companyId;
if (rfqCompanyId !== companyId) throw new Error('Forbidden');
```

Η σύγκριση είναι σε **δύο** εντολές, με **ενδιάμεση μεταβλητή** — το grep της
§7ter έψαχνε `\.companyId !== ctx\.companyId` και δεν την είδε. Και έφερε **και**
το `as` ψέμα της §7.5: ο τύπος υπόσχεται `companyId: string`, η βάση δεν το
εγγυάται.

⇒ **Ο αριθμός «65» της §7ter είναι κάτω όριο, όχι σύνολο.** Το ίδιο σχήμα με τα
`0` του N.11/N.12: το grep μετρά **τη μορφή που έψαξες**, όχι το φαινόμενο. Οι
επόμενες ομάδες να διαβάζουν το αρχείο, όχι μόνο τα hits.

### 7quinquies.2 Το δομικό σφάλμα στο σύνορο: **δύο ανεξάρτητοι υπολογισμοί**

Κάθε route προμηθειών υπολόγιζε **χωριστά** τον κωδικό και το μήνυμα:

```ts
const message = getErrorMessage(error, 'Failed to update material');
const status  = resolveProcurementErrorStatus(error, { …, mode: 'mutation' });
```

Δύο ανεξάρτητοι υπολογισμοί πάνω στο ίδιο σφάλμα **δεν μπορούν να
μεταμφιεστούν μαζί**: μεταμφίεση που γυρίζει `403`→`404` αλλά αφήνει
`'Forbidden'` στο σώμα δεν κρύβει τίποτα, και τίποτα δεν τους κρατούσε
συγχρονισμένους. Το `resolveProcurementErrorOutcome` επιστρέφει **ένα ζεύγος**
`(status, message)` + `logMessage` που **δεν** μεταμφιέζεται ποτέ.

### 7quinquies.3 Το `msg.includes('Forbidden')` → **0** (η §7.4 για τις προμήθειες)

Ο κλάδος `msg.includes('Forbidden') → 403` ήταν **έλεγχος πρόσβασης γραμμένος σε
πεζά γράμματα**. Μετά τη μετανάστευση ο κώδικας έχει **μηδέν** παραγωγούς σκέτου
`throw new Error('Forbidden')`, οπότε αφαιρέθηκε: ήταν νεκρός **και** επικίνδυνος
(όποιος ξαναέγραφε τη λέξη σε οποιοδήποτε μήνυμα ξανάνοιγε το μαντείο). Ο κλάδος
`not found`→404 έμεινε — το 404 **είναι** η σιωπηλή απάντηση.

⚠️ Τρία contract tests της ADR-603 **κάρφωναν** αυτό ακριβώς (`'Forbidden'`→403).
Ξαναγράφτηκαν ώστε να απαιτούν το **αντίθετο** + την ταυτότητα γνήσιου/
μεταμφιεσμένου. **Το κόκκινο ήταν το σωστό αποτέλεσμα** (μάθημα #2 της Ομάδας 1).

### 7quinquies.4 Τα δύο ιδιόμορφα

- **`getRfq`** — καλείται και από **public vendor portal** (HMAC token, μηδέν
  `ctx`, μηδέν `globalRole`) με `invite.companyId`, τιμή από **άλλο έγγραφο**.
  Εκεί το `concealCrossTenant` δεν έχει τι να ρωτήσει ⇒ σκέτο `ownedOrNull`.
  Τα δύο μονοπάτια **δεν** τυποποιήθηκαν σε ένα (§7ter.5).
- **`rfq-service:58`, φίλτρο επαφών στο email fan-out** — έμεινε σιωπηλή
  παράλειψη παραλήπτη, **δεν** προήχθη σε κύριο φύλακα. 🐛 Αλλά έγραφε
  `data.companyId && data.companyId !== ctx.companyId`: επαφή **χωρίς**
  `companyId` περνούσε. Εδώ η ζημιά δεν είναι διαρροή ονόματος — το μήνυμα
  μεταφέρει **portal token** που ανοίγει το RFQ. ⚠️ Η μετάλλαξη που το
  επανέφερε άφηνε **282/282 πράσινα**· γράφτηκε ξεχωριστή σουίτα (5 tests).

### 7quinquies.5 Τα αρχεία

| Αρχείο | Αλλαγή |
|---|---|
| `services/procurement-ownership.ts` | **νέο** — `PROCUREMENT_RESOURCE`, `ProcurementNotFoundError` + **εργοστάσιο**, `ProcurementCrossTenantError`, `assertOwnedProcurementDoc`. Καθαρό module (το μοιράζεται το σύνορο HTTP) |
| `services/procurement-owned-doc.ts` | **νέο** — ο ένας φορτωτής: `loadOwnedProcurementDoc` (Γ) · `readOwnedProcurementDoc` (Δ) · `requireOwnedSnapshot` (**συναλλαγές**) · `softDeleteOwnedProcurementDoc` |
| `services/rfq-ownership.ts` | **νέο** — ο πόρος «RFQ» + φορτωτής, κοινός σε 3 υπηρεσίες (7 σημεία) |
| `api/procurement/_shared/procurement-error-outcome.ts` | **νέο** — `(status, message, logMessage)` ως **ένα** ζεύγος |
| `api/procurement/_shared/procurement-mutation.ts` | `auth` **υποχρεωτικό** + `conflictName`/`validationName`/`mode` → **ένας** εκτελεστής για **15** μεταβολές |
| `api/procurement/_shared/error-status.ts` | ο κλάδος `Forbidden`→403 **αφαιρέθηκε** |
| `api/rfqs/_shared/rfq-error-response.ts` | **νέο** — το σύνορο της **παλιάς** οικογένειας (άλλο σχήμα σύρματος) |
| `api/rfqs/_shared/rfq-id-route.ts` | **νέο** — ο εκτελεστής των 5 χειριστών `api/rfqs/[id]/**` (§7.8) |
| 7 services · 11 routes | μεταναστευμένα |

### 7quinquies.6 ⚠️ Αλλαγές συμπεριφοράς — δηλωμένες

1. **`api/rfqs/[id]` PATCH/DELETE, `cancel`, `reopen`**: «δεν υπάρχει» ήταν
   `400`, τώρα `404`· «ανήκει αλλού» ήταν **`400 Forbidden`** (ρητή αποκάλυψη με
   λάθος κωδικό), τώρα **μεταμφιεσμένο 404** για τους μη-bypass. Η §3.2 του
   handoff ζητούσε να **δηλωθεί** — δηλώνεται *και* κλείνει: η αφή του αφήνει
   το κενό ορθάνοιχτο στο νέο σχήμα.
2. **`GET /api/rfqs/[id]`** απέκτησε `try/catch` μέσω του κοινού κελύφους
   (υπερσύνολο· στην πράξη αδρανές, το `getRfq` δίνει `null`).
3. **`getQuote`** έχανε τον τοπικό `logger.warn` με `actualCompanyId`: πλέον
   καταγράφει ο SSoT (`Cross-tenant access blocked` με resource/id/caller).
   Συνειδητή στένωση — ο ιδιοκτήτης ενός id προκύπτει από το id.
4. **Fan-out προσκλήσεων**: επαφή **χωρίς/κενό** `companyId` δεν λαμβάνει πια
   (ίδια εγκεκριμένη αλλαγή με `banking`/`relationship`, §7quater).

### 7quinquies.7 ⚠️ Δεύτερος μηχανισμός σιωπής — μετρημένος, μη προφανής

Το `safeFirestoreOperation(op, fallback)` **καταπίνει** το σφάλμα όταν του δοθεί
`fallback`. Άρα οι Δ διαδρομές (`getX(..., null)`) σιωπούν από **δύο**
ανεξάρτητους μηχανισμούς, και το `listRfqLines(..., [])` επιστρέφει `[]` αντί να
ρίξει σε ξένο RFQ. Δεν είναι κενό ασφαλείας (σιωπή = η σωστή απάντηση), **αλλά
δεν είναι απόφαση** — είναι γενικό `catch`. Οι σουίτες χρησιμοποιούν διαφανές
mock (τα σφάλματα ανεβαίνουν), όπως και οι αδελφές τους· η απόκλιση δηλώνεται.

### 7quinquies.8 Boy Scout — N.18: **10 προϋπάρχοντες κλώνοι → 0**

Μετρημένοι **στο HEAD** (5 στις 3 υπηρεσίες + 5 στα 3 legacy routes), άρα κάθε
αφή τους έκοβε το CHECK 3.28 (ίδιο σχήμα με §7.8). Εξήχθησαν:

| Κλώνος | Λύση |
|---|---|
| φόρτωση+φύλακας ×7 υπηρεσίες | `loadOwnedProcurementDoc` |
| διαδικασία soft-delete ×2 | `softDeleteOwnedProcurementDoc` (ιδεμποτεντικό + `updatedAt` + ίχνος) |
| φόρτωση RFQ ×3 αρχεία | `rfq-ownership` |
| αντικείμενο `RfqLine` ×3 | `buildRfqLine` — **η γραμμή 3 είναι `companyId`** (CHECK 3.10) |
| προοίμιο μαζικής εισαγωγής ×2 · batch εγγραφή ×2 | `beginBulkInsert` · `writeLinesBatch` |
| οι δύο δίδυμες συναλλαγές σύνδεσης RFQ | `mutateRfqLink(link: boolean)` |
| κέλυφος `withAuth` ×5 χειριστές | `rfqIdRoute` |

### 7quinquies.9 Επαλήθευση

- **292/292** πράσινα σε **15** suites (`src/subapps/procurement` + `api/procurement` + `api/rfqs`)
- **22/22 μεταλλάξεις σκοτωμένες** — μεταξύ άλλων: επιστροφή στο σκέτο `===`·
  υπηρεσία/συναλλαγή/φορτωτής που παύει να ρωτά· χειρόγραφο `'Not found'` στη
  μεταμφίεση· **καρφωμένο `'super_admin'` στον εκτελεστή**· εκτελεστής χωρίς
  `try`· soft-delete χωρίς ιδεμποτεντικότητα και χωρίς `updatedAt`· μεταμφίεση
  του `PO_EXISTS`· επαναφορά του κενού `companyId` στο fan-out
- 🐛 **13 tests που δεν είχαν τρέξει ΠΟΤΕ**: το `rfq-service.test.ts` έσκαγε στο
  φόρτωμα (`admin.firestore is not a function` — το mock όριζε **μόνο** τον χώρο
  ονομάτων, όχι τη συνάρτηση). «Test suite failed to run», όχι κόκκινος
  ισχυρισμός — γι' αυτό δεν φαινόταν πουθενά ως σπασμένο test
- **N.18: 10 → 0**· N.7.1 όλα τα αρχεία <500 γρ. (max 444)
- **ΟΧΙ tsc (N.17)**

---

## 7sexies. Ομάδα 3 — projects **DONE** (2026-08-01)

Δέκα σημεία απόφασης σε εννιά αρχεία, **δύο** σχήματα σύρματος, μηδέν χειρόγραφη
σύγκριση `companyId` που να έχει απομείνει στο πεδίο ορισμού.

### 7sexies.1 🔴 Η μεταμφίεση ήταν **ΜΙΣΟΓΡΑΜΜΕΝΗ** — και γι' αυτό χειρότερη από καμία

Κάποιος **είχε ήδη καταλάβει** τον κανόνα αποκάλυψης και έβαλε το «Project not
found» μέσα στο κείμενο της άρνησης. Αλλά κράτησε τον **κωδικό 403** *και*
πρόσθεσε το πρόθεμα `'Access denied - '`:

| | γνήσιο «δεν βρέθηκε» | άρνηση ιδιοκτησίας |
|---|---|---|
| κωδικός | `404` | **`403`** ← προδίδει |
| μήνυμα | `Project not found` | **`Access denied - Project not found`** ← προδίδει |

Είναι η **§7quater ανάποδα**: εκεί το κείμενο έλεγε «not found» ενώ το *σχήμα*
πρόδιδε· εδώ το κείμενο *προσποιείται* ενώ ο *κωδικός* προδίδει.
⇒ **Η μεταμφίεση κρίνεται στο ΟΛΟΚΛΗΡΟ ΣΧΗΜΑ — κωδικός + σώμα + μήνυμα.**

### 7sexies.2 🔑 Ρητή απόφαση: **ΟΧΙ** `concealCrossTenant` εδώ

Το handoff ζητούσε ρητή επιλογή (§7ter.2: ο κλάδος `reveal` είναι **απρόσιτος**
στη Γ, γιατί ο bypass **παίρνει το έγγραφο**). Επιλέχθηκε **σκέτο εργοστάσιο
`projectNotFound()`**, όχι `concealCrossTenant` με νεκρό `reveal`:

1. **Κώδικας που δεν εκτελείται δεν δοκιμάζεται — άρα σαπίζει.** Θα τεκμηρίωνε
   την πολιτική με γραμμή που **κανένα** test δεν μπορεί να πιάσει.
2. **Θα έλεγε ψέματα για το πού ζει η απόφαση.** Η απόφαση bypass λαμβάνεται
   στη `checkProjectAccess`· *εκεί* πρέπει να φαίνεται, ώστε η μετάβαση σε JIT
   elevation (§7ter.3) να έχει **ένα** σημείο να αλλάξει, όχι δύο συνεννοημένα.

Ο κανόνας §3.3 τηρείται **αυστηρότερα**, όχι χαλαρότερα: όποιος θα δικαιούταν
την αλήθεια την έχει ήδη πάρει ολόκληρη — με τη μορφή του ίδιου του εγγράφου.

### 7sexies.3 🔴 Οι δύο «log-only» κλάδοι **απορροφήθηκαν** — αλλιώς το Βήμα 7 ήταν αδύνατο

Η §7ter.1 τους έβγαλε **έξω** από τα σημεία απόφασης, και σωστά: *γράφουν log,
δεν αποφασίζουν*. Όμως το Βήμα 7 βάζει το `\.companyId !== ctx\.companyId` στα
`forbiddenPatterns`, και **αυτό είναι έλεγχος κειμένου** — οι δύο κλάδοι το
γράφουν αυτολεξεί. Αν έμεναν, **το κλείδωμα του πεδίου ορισμού θα ήταν αδύνατο**.

Απορροφήθηκαν ως **παρατήρηση**: η ίδια συνάρτηση που κρίνει την πρόσβαση είναι
και η μόνη που ξέρει ότι ο υπεργραφέας κοιτάζει ξένο έργο — γιατί είναι **η ίδια
σύγκριση**, όχι δύο ανεξάρτητες. Ετυμηγορία με **τρεις ονομασμένες καταστάσεις**
(`owned` / `cross-tenant-bypass` / `denied`), όχι boolean (§3.2).

### 7sexies.4 Δύο σχήματα σύρματος — και γιατί **δεν** ενοποιήθηκαν

- **Εννιά σημεία** → `ApiError` ⇒ `projectNotFound()`, **μηδέν ορίσματα** (δεν
  υπάρχει τιμή που να μπορεί να αποκλίνει ανάμεσα στους δύο κλάδους).
- **`structure/[projectId]`** → `NextResponse.json({success,error,projectId})`.
  Κοινό εργοστάσιο εδώ θα πρόδιδε τη διαφορά **με το ίδιο το σχήμα** (§7.1).
  Κοινό μένει **μόνο το κείμενο** (`PROJECT_NOT_FOUND_MESSAGE`) — το μόνο που ο
  πελάτης μπορεί να συγκρίνει *μεταξύ* διαδρομών.

Γι' αυτό ο φύλακας εκτίθεται **δύο φορές**: `checkProjectAccess` (PDP, ολική,
χωρίς ρίψη) και `requireProjectAccess` (PEP για την οικογένεια `ApiError`). Το
`structure` καλεί το πρώτο και επιβάλλει μόνο του. Φορτωτής «που ρίχνει ή
επιστρέφει ανάλογα με σημαία» θα ήταν ακριβώς η boolean που απαγορεύει η §3.2.

### 7sexies.5 Τα αρχεία

| Αρχείο | Αλλαγή |
|---|---|
| `api/projects/_shared/project-ownership.ts` | **νέο** — καθαρό module: `PROJECT_NOT_FOUND_MESSAGE`, εργοστάσιο `projectNotFound()`, `checkProjectAccess` (PDP), `requireProjectAccess` (PEP) |
| `api/projects/_shared/project-owned-doc.ts` | **νέο** — ο **ένας** φορτωτής: φόρτωσε → «υπάρχει;» → «δικό μου;», ως μία πράξη |
| `api/projects/_shared/project-preview-route.ts` | **νέο** — ο εκτελεστής των **6** preview· ρυθμιστής + δικαίωμα **μέσα**, όχι έξω |
| `api/projects/_shared/project-cache.ts` | **νέο** (Boy Scout) — `PROJECT_LIST_CACHE_PREFIX` + `invalidateProjectCaches`, 4 σημεία |
| 6 preview routes | 49–58 γρ. → **13–22 γρ.** το καθένα |
| `[projectId]/route.ts` (GET) · `project-mutations.service.ts` ×2 · `structure/[projectId]/route.ts` | μεταναστευμένα |

### 7sexies.6 ⚠️ Αλλαγές συμπεριφοράς — δηλωμένες

1. **`403 'Access denied - Project not found'` → μεταμφιεσμένο `404 'Project not found'`**
   σε **και τα 10** σημεία, για τους μη-bypass.
2. **Η παγίδα του κενού έκλεισε σε 9 από τα 10.** Μόνο το `handleUpdateProject`
   ρωτούσε ήδη σωστά (`!projectData?.companyId || …`). Πλέον όλα ρωτούν
   `isPayloadOwnedByCompany`: **καλών με χαλασμένο token (`companyId: ''`) δεν
   ταιριάζει πια με έργο που έχει κενό `companyId`** (§4).
3. **Τα 6 preview απέκτησαν καταγραφή άρνησης** (πριν: καμία). Υπερσύνολο.
4. **Ο ρυθμιστής ρυθμού μπήκε ΜΕΣΑ στον εκτελεστή** — μια νέα preview διαδρομή
   δεν *μπορεί* πια να τον ξεχάσει.

### 7sexies.7 Boy Scout — N.18

- **Sibling clone που έφτιαξα και διόρθωσα**: η κεντρικοποίηση της *ερώτησης*
  άφησε την *τετράδα βημάτων* (ref → get → «υπάρχει;» → «δικό μου;»)
  αντιγραμμένη· το `jscpd` τη μέτρησε **αμέσως** ανάμεσα σε `route.ts` και
  `project-mutations.service.ts`. ⇒ `project-owned-doc.ts`. *Ακριβώς η αστοχία
  που προβλέπει ο N.18: κεντρικοποιείς το Α, γράφεις Β+Γ ως δίδυμα.*
- **Προϋπάρχων κλώνος στο HEAD** (επαληθευμένος με `git show HEAD:… | jscpd`):
  η τετράδα `cache.delete(...)` σε **4** σημεία, με το πρόθεμα δηλωμένο ως
  literal σε **3** και **χειρόγραφο** σε ένα τέταρτο (`trash/…/permanent-delete`).
  ⇒ `project-cache.ts`· το νεκρό `CACHE_KEY_PREFIX` αφαιρέθηκε.
- ⚠️ **Ένα «εύρημα» που ήταν αστοχία δικού μου grep**, καταγράφεται επειδή
  παραλίγο να μπει στο ADR ως γεγονός: μέτρηση έδειξε ότι 2 από τα 4 σημεία
  σβήνουν 3 αντί για 4 κλειδιά. **Το μοτίβο αναζήτησης δεν περιλάμβανε το
  `bootstrap:tenant`.** Και τα 4 ήταν πλήρη. *Το grep μετρά τη μορφή που έψαξες*
  (§7quinquies.1) — **και ισχύει και για τον έλεγχο, όχι μόνο για τη σάρωση.**

### 7sexies.8 Επαλήθευση

- **55/55** πράσινα σε 4 νέες σουίτες· **829/829 σε 59 suites** (`api` + `lib/auth` + `lib/api`)
- **18/18 μεταλλάξεις σκοτωμένες** στο τελικό σχήμα (+ 21 σε ενδιάμεσα)
- 🐛 **Μια μετάλλαξη επέζησε και αποκάλυψε πραγματικό κενό**: αφαίρεση του
  ελέγχου ύπαρξης από τον φορτωτή. Για μη-bypass καλούντα το αποτέλεσμα είναι
  ταυτόσημο (έγγραφο χωρίς `companyId` δεν ανήκει σε κανέναν ⇒ άρνηση) — αλλά
  για τον **υπεργραφέα** ο φύλακας λέει «πέρνα», οπότε θα κατέβαινε στο
  επιχειρησιακό βήμα **φάντασμα έργου** με `data: undefined`. **Ο έλεγχος
  ύπαρξης δεν είναι πλεονασμός του φύλακα ιδιοκτησίας· είναι το μόνο πράγμα που
  σταματά τον bypass.** Γράφτηκαν 2 tests· η μετάλλαξη σκοτώθηκε.
- **N.18 καθαρό** σε 18 αρχεία· N.7.1 όλα <500 γρ.· **ΟΧΙ tsc (N.17)**

### 7sexies.9 ⚠️ Παρατήρηση χωρίς αλλαγή

Το `broker-terminate-preview` περνά στον υπολογισμό τον tenant του **καλούντα**
(`ctx.companyId`), όχι του **έργου** — μοναδικό στα έξι. Για bypass καλούντα σε
ξένο έργο τα δύο διαφέρουν. Διατηρήθηκε αυτούσιο: η Ομάδα 3 αλλάζει *τι λέμε
στην άρνηση*, όχι *ποιος βλέπει τι* (§7ter.3).

---

## 7septies. 🔴 Το εύρημα που ακύρωνε την Ομάδα 3 — **η ομοιομορφία ανά ΠΟΡΟ** (2026-08-01)

### 7septies.1 Το μαντείο δεν είναι ιδιότητα διαδρομής· είναι ιδιότητα **πόρου**

Με την Ομάδα 3 κλειστή, μέτρηση έδειξε ότι η μεταμφίεση **ακυρωνόταν ολόκληρη**:

| Διαδρομή | Ξένο `projects/{id}` |
|---|---|
| `GET /api/projects/{id}` | **404** (Ομάδα 3) |
| `/api/projects/{id}/customers` | **403 `Access denied`** |
| `/api/projects/{id}/efka-declaration` | **403** |
| `/api/v2/projects/{id}/customers` | **403** |
| `/api/floors` (μέσω `floors.shared`) | **403** |

Οι τέσσερις περνούσαν από το `requireProjectInTenant` → `requireDocInTenant`,
που έριχνε `TenantIsolationError('Access denied', 403, 'FORBIDDEN')`.

> **Αν δεκαπέντε διαδρομές δέχονται το ίδιο id και μία απαντά διαφορετικά, ο
> πόρος είναι αποκαλυμμένος — και κάθε unit test καθεμιάς μένει πράσινο, γιατί
> καθεμιά είναι εσωτερικά συνεπής.**

### 7septies.2 Έρευνα: **δύο** έγκυρα δόγματα, και το λάθος είναι η **ανάμειξη**

| Δόγμα | Ποιος | Μηχανισμός |
|---|---|---|
| **404-ομοιόμορφο** | GitHub · Google Drive · Figma | η **άρνηση** μεταμφιέζεται σε απουσία |
| **403-ομοιόμορφο ασαφές** | **Google Cloud AIP-193/211** | η **απουσία** μεταμφιέζεται σε άρνηση: *«Permission '{p}' denied on resource '{r}' (or it might not exist)»*, με τον έλεγχο **δικαιώματος πριν** τον έλεγχο ύπαρξης |

🔴 **Και τα δύο κλείνουν το μαντείο.** Αυτό που το ανοίγει είναι η ανάμειξη.
Η AIP-193 το λέει ρητά: *«Permission **must** be checked prior to checking if the
resource or parent exists»* — δηλαδή η απάντηση **δεν επιτρέπεται να εξαρτάται**
από την ύπαρξη. Ίδιο περιεχόμενο με τον κανόνα `s3:ListBucket` του S3 (§3.3).

**Γιατί ο Νέστωρ διαλέγει το 404** και όχι το δόγμα της Google: το κριτήριο της
§3.3 είναι «θα μπορούσε ο καλών να το μάθει νόμιμα αλλιώς;». Στο GCP η απάντηση
είναι συχνά **ναι** (δικαιώματα κληρονομούνται από γονικούς πόρους, και ο χρήστης
δικαιούται να μάθει *ποιο* δικαίωμα του λείπει) ⇒ το ασαφές 403 είναι εκεί πιο
χρήσιμο. Στον Νέστορα κάθε `list` είναι tenant-scoped (ADR-702) ⇒ κανονικός
χρήστης **δεν μπορεί ποτέ** να μάθει νόμιμα ότι υπάρχει ξένο id ⇒ σιωπή.

### 7septies.3 Η διόρθωση: **δύο γραμμές** στον έναν πυρήνα

Το `requireDocInTenant` δίνει πλέον **ένα** `notFound()` closure που καλούν
**και οι δύο** κλάδοι άρνησης — μηδέν ορίσματα, άρα καμία δυνατότητα απόκλισης.

🔴 **Η μεταμφίεση διαδόθηκε δομικά σε 41 καταναλωτές χωρίς να αγγιχτεί κανείς
τους**: όλοι έγραφαν ήδη `error.code === 'NOT_FOUND' ? notFoundMessage : 'Access denied'`,
και το `code` είναι πλέον `'NOT_FOUND'` **και στις δύο** περιπτώσεις.

Καλύπτει **έξι** οντότητες μαζί: `project` · `building` · `property` · `storage`
· `parking` · `opportunity` — δηλαδή προκαταβάλλει μέρος των Ομάδων 4 και 6.

⛔ **Το `requireTenantScope` (ADR-702) κρατά το 403 του** — και δεν είναι
ασυνέπεια: απαντά σε **άλλη ερώτηση** (§2), και ο καλών ονομάζει **ρητά** ξένη
εταιρεία, οπότε δεν μαθαίνει τίποτα που δεν έγραψε ο ίδιος. Άρνηση **παραμέτρου**,
όχι **εγγράφου**. Ομοίως τα 401/403 ταυτότητας του `require-project-for-page`.

⚠️ **Το ίχνος ελέγχου κρατά την αλήθεια**: `reason` παραμένει
`'Tenant isolation violation - companyId mismatch'` για την άρνηση και το μήνυμα
της οντότητας για την απουσία. Η μεταμφίεση αφορά **μόνο** το σύρμα (§3.4) —
κατοχυρωμένο με ξεχωριστό test.

### 7septies.4 🔴 Το test που κάρφωνε το **αντίθετο** — και ζητούσε ρητή απόφαση

Το `tenant-isolation.test.ts` είχε test με τίτλο *«συμβόλαιο που ΔΕΝ επιτρέπεται
να μετακινηθεί»* και σχόλιο:

> «Είναι **συνειδητή** επιλογή του ADR-255 … Καρφώνεται ώστε να μην "διορθωθεί"
> παρεμπιπτόντως: η αλλαγή της είναι **αλλαγή δόγματος και θέλει ρητή απόφαση**,
> όχι refactor.»

Ο συγγραφέας είχε **δίκιο να το καρφώσει**. Η ρητή απόφαση λήφθηκε (Giorgio,
2026-08-01, μετά από έρευνα). Το test **αντιστράφηκε**: απαιτεί πλέον τα δύο
σφάλματα να είναι **ίσα** (`toEqual` σε ολόκληρο το σχήμα), και κρατά το ιστορικό
στο σώμα του ώστε να μη διαβαστεί ποτέ ως αυθαίρετο. **13 tests κοκκίνισαν· ήταν
το σωστό αποτέλεσμα** (μάθημα #5 της Ομάδας 2).

### 7septies.5 🏆 Το anchor — αυτό που η βιομηχανία **δεν** κάνει

`lib/auth/__tests__/resource-concealment-anchor.test.ts`

GitHub, Google (AIP-193/211), AWS S3 και Figma **δηλώνουν** τον κανόνα σε πεζό
κείμενο και βασίζονται στον έλεγχο κώδικα για την ομοιομορφία. **Κανένας δεν
δημοσιεύει αυτόματο έλεγχο ότι όλες οι διαδρομές ενός πόρου συμφωνούν.** Το
κείμενο όμως δεν σπάει ποτέ — σπάει η **δέκατη έκτη διαδρομή**, που γράφεται έξι
μήνες αργότερα από κάποιον που δεν διάβασε το ADR. Ακριβώς αυτό συνέβη εδώ.

Το anchor διαβάζει τα αρχεία από τον δίσκο και ελέγχει, **ανά πόρο**:

1. καμία διαδρομή του δεν γράφει τη μισογραμμένη μεταμφίεση (`'Access denied - '`)
2. καμία δεν ξαναγράφει τη σύγκριση ιδιοκτησίας με το χέρι
3. **πουθενά** στα σαρωμένα δέντρα το «δεν βρέθηκε» του πόρου δεν συνοδεύεται
   από `403` — **αυτό** πιάνει την αδελφική διαδρομή σε **άλλο** φάκελο
4. ο κοινός `requireDocInTenant` δεν φτιάχνει ποτέ ξανά `TenantIsolationError`
   με 403

**Δεν είναι ratchet και δεν έχει baseline**: ένας μεταναστευμένος πόρος είναι
**μηδέν ή τίποτα** — «λιγότερες διαρροές» δεν κρύβει τίποτα.

⚠️ **Δηλωμένα τυφλά σημεία** (γιατί «0» χωρίς δήλωση σημαίνει «κανείς δεν
κοίταξε» — N.11/N.12): σαρώνει `src/app/api`, `src/lib`, `src/server`,
`src/services` (τα δέντρα όπου γεννιέται άρνηση HTTP)· δεν βλέπει δυναμικά
συντιθέμενα μηνύματα· και **μετρά ρητά** τους μη-μεταναστευμένους πόρους:
`contact` = **4** σημεία (Ομάδα 4), με **ισότητα** — αύξηση είναι νέα διαρροή,
μείωση σημαίνει ότι η ομάδα προχώρησε και ο κατάλογος θέλει ενημέρωση.

**4/4 μεταλλάξεις του ίδιου του anchor σκοτωμένες** (403 στον πυρήνα ·
μισογραμμένη μεταμφίεση σε διαδρομή έργου · χειρόγραφη σύγκριση · **αδελφική
διαδρομή σε άλλο φάκελο** που απαντά 403 για «Project not found»).

---

## 7octies. Ομάδα 4 — contacts / buildings / ika **DONE** + 🔴 **η διαρροή που δεν ήταν μαντείο** (2026-08-01)

### 7octies.1 🔴🔴 Το εύρημα: **διαρροή περιεχομένου**, όχι ύπαρξης

Η Ομάδα 4 ξεκίνησε ως μεταμφίεση 12 σημείων. Η ανάγνωση αποκάλυψε κάτι
**διαφορετικού είδους** — και σοβαρότερο από ό,τι ολόκληρο το ADR-742 μέχρι τότε.

Ο πόρος `contact` έχει **έξι** διαδρομές «προεπισκόπηση επίπτωσης». Το handoff
της Ομάδας 3 τις περιέγραφε ως «δύο δίδυμα». Ήταν **έξι**, και **τέσσερις** από
αυτές έτρεχαν:

| διαδρομή | δικαίωμα | φύλακας ιδιοκτησίας |
|---|---|---|
| `address-impact-preview` | **κανένα** | **κανένας** |
| `communication-impact-preview` | **κανένα** | **κανένας** |
| `company-identity-impact-preview` | **κανένα** | **κανένας** |
| `name-cascade-preview` | **κανένα** | **κανένας** |
| `identity-impact-preview` | `crm:contacts:update` | ναι (403) |
| `service-identity-impact-preview` | `crm:contacts:update` | ναι (403) |

Το `withAuth` ελέγχει δικαίωμα **μόνο αν του δοθεί** (`middleware.ts:223`)· το
`_ctx` των τεσσάρων ήταν **αχρησιμοποίητο**, δηλαδή ο tenant δεν συμμετείχε
καθόλου στην απάντηση.

**Και η δεύτερη μισή αιτία ζούσε στη μηχανή.** Ο `contact-impact-engine`
ρωτούσε, σε **τρεις** εκτελεστές:

```ts
if (!query.skipCompanyFilter && companyId) {   // ← η δεύτερη συνθήκη
  q = q.where('companyId', '==', companyId);
}
```

Η παράμετρος ήταν `companyId?` και **κανένας από τους έξι καλούντες δεν την
περνούσε** — ένας μάλιστα (`contact-identity-impact-preview.service:90`) έγραφε
ρητά `undefined` για να φτάσει στην πέμπτη θέση. ⇒ **Το φίλτρο tenant δεν
ενεργοποιήθηκε ποτέ, σε κανένα μονοπάτι.**

Αποτέλεσμα: **οποιοσδήποτε συνδεδεμένος χρήστης**, με id ξένης επαφής, μάθαινε
πόσα ακίνητα, τιμολόγια, συμβόλαια, σχέσεις και πιστοποιητικά ΑΠΥ την αφορούν —
**από όλους τους πελάτες μαζί**. Δεν είναι μαντείο ύπαρξης (§3.3): είναι
**διαρροή περιεχομένου**.

> 🔑 **Η απόδειξη ότι είναι σφάλμα, όχι σχεδιαστική επιλογή**: η **αδελφή μηχανή**
> πάνω στο **ίδιο** `contact-dependency-registry` το έκανε ήδη σωστά.
> `deletion-guard.ts` δηλώνει `companyId: string` (**υποχρεωτικό**) και ρωτά
> **μόνο** `if (!dep.skipCompanyFilter)`. **Ένα registry, δύο μηχανές, δύο
> δόγματα — και κανένα gate δεν τις συνέκρινε.** Ίδιο σχήμα με το CHECK 3.34
> (δύο χειρόγραφες λίστες namespace που είχαν αποκλίνει κατά 63).

**Διόρθωση**: το `companyId` έγινε **υποχρεωτικό** σε ολόκληρη την αλυσίδα (5
υπογραφές + 6 services + 6 routes), και η δεύτερη συνθήκη έφυγε από τους τρεις
εκτελεστές. Το `skipCompanyFilter: true` **παραμένει** νόμιμο για τις 6 συλλογές
που δεν φέρουν `companyId` — η διαφορά είναι ότι η παράλειψη είναι πλέον
**δηλωμένη ανά εξάρτηση**, όχι **παρενέργεια** του τι ξέχασε ο καλών.

### 7octies.2 Τι απέτυχε στη μέτρηση — τρεις φορές

**(α) Το grep του handoff έχασε 2 σημεία.** Τα `channels/route.ts:167` και
`link-channel/route.ts:134` γράφουν `[FIELDS.COMPANY_ID] !== ctx.companyId` —
με **σταθερά** αντί για κυριολεκτικό `.companyId`. Το μοτίβο
`\.companyId !== ctx\.companyId` είναι **τυφλό** σε αυτή τη μορφή. ⚠️ **Αυτό
αφορά άμεσα το Βήμα 7**: το `forbiddenPattern` που προβλέπει το σχέδιο θα ήταν
διάτρητο με τον ίδιο τρόπο.

**(β) Το anchor έμεινε πράσινο ενώ ο αριθμός άλλαζε.** Μετά τη μετανάστευση δύο
διαδρομών, το `NOT_YET_MIGRATED` για `contact` **παρέμεινε ακριβώς 4** — όχι
επειδή δεν άλλαξε τίποτα, αλλά επειδή έφυγαν **δύο πραγματικές** παραβάσεις και
μπήκαν **δύο σχόλια** που τεκμηριώνουν το παλιό σχήμα. **Η ισότητα κρατήθηκε
κατά τύχη: ο μετρητής μετρούσε πρόζα.** Διορθώθηκε με καθολικό `stripComments`
πριν από **κάθε** μέτρηση + 3 self-tests. Ως συνέπεια, η εξαίρεση `ssot:` ανά
πόρο **καταργήθηκε** — ήταν workaround για ακριβώς αυτό, και μια εξαίρεση που
δεν χρειάζεται είναι τρύπα που περιμένει.

**(γ) Το `jscpd` βρήκε τρία αντίγραφα ΚΑΘΑΡΑ.** Οι φύλακες
`project-`/`contact-`/`building-ownership` είχαν **πανομοιότυπη** λογική με
αλλαγμένα μόνο τα ονόματα — και `projectData`/`contactData`/`buildingData` είναι
**διαφορετικά tokens**. Είναι το τυφλό σημείο του ADR-584 **ανάποδα**: εκεί το
`ssot:discover` (name-based) δεν έβλεπε structural clones με άλλο όνομα· εδώ το
token-based δεν βλέπει αντίγραφο **που κρύβεται πίσω από συνεπή μετονομασία**.
**Δύο ανιχνευτές, ίδιο κενό.**

### 7octies.3 Το δόγμα bypass — ρητή απόφαση με έρευνα

Ο πόρος `contact` είχε **δύο** δόγματα για το **ίδιο** `contacts/{id}`:
`GET` και `properties` **χωρίς** bypass· τα δύο preview και το `DELETE` (μέσω
`softDelete`) **με** bypass.

🔴 Η ασυμμετρία ήταν προς τη **λάθος** κατεύθυνση: ο υπεργραφέας **δεν** μπορούσε
να διαβάσει ξένη επαφή, αλλά **μπορούσε** να τρέξει πάνω της προεπισκόπηση
ταυτότητας — που διαβάζει **σχέσεις ολόκληρης** της επαφής. Το πιο διεισδυτικό
μονοπάτι ήταν το πιο ανοιχτό.

**Έρευνα βιομηχανίας (2026-08-01)** — ομόφωνη, και **δεν δικαιώνει καμία από τις
δύο υπάρχουσες συμπεριφορές**:

- **Google Cloud** — *zero standing privilege*: «access to Customer Data is
  **denied by default** for all Google personnel»· όταν δίνεται είναι
  **προσωρινή**, απαιτεί **έγκυρη αιτιολόγηση** (Access Transparency), και το
  Access Approval απαιτεί **έγκριση του ίδιου του πελάτη**.
- **Atlassian** — Tenant Context Service: το tenant επιβάλλεται «from the cloud
  edge through backend services»· πρόσβαση «only for the time required».
- **Γενική πρακτική SaaS** — break-glass με **JIT elevation**, χρονικά φραγμένη,
  με καταγεγραμμένο λόγο: *«never grant standing cross-tenant access»*.

⇒ **Το σημερινό `isRoleBypass` σε 47 σημεία ΕΙΝΑΙ το anti-pattern.** Η Ομάδα 4
**δεν** υλοποιεί JIT (αλλαγή δόγματος σε 47 σημεία, απαιτεί απόφαση Giorgio
κατά N.8). Ενοποιεί **με** bypass — ευθυγράμμιση με `checkProjectAccess`,
`requireDocInTenant` (§7septies) και `softDelete` — και **καθιστά τη μετάβαση
εφικτή**: η bypass απόφαση είναι πλέον **ονομασμένη κατάσταση**
(`'cross-tenant-bypass'`) σε **ένα** σημείο (`createOwnershipDecision`), όχι 47
σκόρπια `!isSuperAdmin &&`. Όταν έρθει το JIT, αλλάζει **μία** συνάρτηση.

⚠️ **Δηλωμένη διεύρυνση**: `GET /contacts/{id}` και `/properties` δίνουν πλέον
πρόσβαση στον υπεργραφέα εκεί που πριν την αρνούνταν. Η προηγούμενη στένωση ήταν
**ψευδής ασφάλεια** — τα preview routes του έδιναν ήδη τα ίδια δεδομένα σε πιο
επεξεργασμένη μορφή.

### 7octies.4 Τι υλοποιήθηκε

**Κοινός πυρήνας (νέα, `lib/auth/`)**

| Module | Τι λύνει |
|---|---|
| `resource-ownership-guard.ts` | **Η απόφαση**, μία φορά για κάθε πόρο (`createOwnershipDecision`). Εξάλειψε τα 3 αντίγραφα που το `jscpd` δεν έβλεπε. |
| `owned-doc-loader.ts` | **Η αλυσίδα** φόρτωσε→υπάρχει;→δικό μου; Η Ομάδα 3 την είχε λύσει **για τα έργα**· η Ομάδα 4 θα έγραφε δίδυμο **κεντρικοποιητή**. |

**Ανά πόρο (λεπτά δεσίματα — λεξιλόγιο, όχι λογική)**

`contacts/_shared/`: `contact-ownership` · `contact-not-found-response`
(ξεχωριστό: το `next/server` **έσπασε** τη σουίτα με `ReferenceError: Request is
not defined` ⇒ ο φύλακας θα έμενε αδοκίμαστος) · `contact-owned-doc` ·
`contact-preview-route` (**6 διαδρομές**, δικαίωμα + ρυθμιστής + φύλακας
**μέσα**· 💡 ο τύπος επαφής έρχεται από το φορτίο του φύλακα ⇒ **μία** ανάγνωση
αντί για δύο).
`buildings/_shared/building-ownership`. Το `project-ownership`/`project-owned-doc`
**ξαναδέθηκαν** στον κοινό πυρήνα με **αμετάβλητη** δημόσια υπογραφή.

**Σημεία που έκλεισαν (πέρα από τα 12 του handoff)**

`bank-accounts-server.service.ts:88` — **εκτός φακέλου, δύο σφάλματα μαζί**:
(1) `if (contactData?.companyId && …)` ⇒ επαφή **χωρίς** `companyId` περνούσε ως
`success: true` για **οποιονδήποτε**, και από εκεί περνούν και οι **τρεις
μεταλλάξεις** τραπεζικών λογαριασμών — αυτολεξεί το σφάλμα που η §7quinquies
είχε κλείσει στο `rfq-service.ts`, **ζωντανό σε δεύτερο αρχείο**·
(2) `'Contact not found'`→404 vs `'Access denied'`→403 = μαντείο.
`ika/workers/…/insurance-class` — ο πόρος είναι **`contacts/{id}`** σε άλλο
δέντρο, με **τρίτο** σχήμα άρνησης (`403 'Forbidden'`).
`admin/role-management/project-members` — κωδικός ήδη 404, αλλά **μήνυμα**
`'Project not found or access denied'`: ένα μήνυμα που **απαριθμεί** τους λόγους
της άρνησης είναι το ίδιο μαντείο, απλώς πιο ευγενικό.
`resolve-helpers` ×3 — σιωπηλά (**Δ**), αλλά με την παγίδα του κενού σε
**εγγραφές** (email, λογότυπο, ρόλος προμηθευτή σε ξένο έγγραφο).

### 7octies.5 Πύλες

**419/419 σε 28 suites** · **11/11 μεταλλάξεις σκοτωμένες** · N.18 καθαρό σε 13
αρχεία · N.7.1 όλα <500 · **ΟΧΙ tsc** (N.17).

🐛 **Μια μετάλλαξη επέζησε και αποκάλυψε ανεπαρκές test — δικό μου.** Το πρώτο
test της μηχανής έδινε πάντα **υπαρκτό** tenant, οπότε η επαναφορά του
`&& companyId` άφηνε **5/5 πράσινα**. Το κενό `companyId` είναι η **μόνη** είσοδος
που ξεχωρίζει τις δύο εκδοχές (fail-closed vs φίλτρο που εξαφανίζεται) — και
είναι ακριβώς το χαλασμένο token του §4. Προστέθηκε· η μετάλλαξη πεθαίνει.

🐛 **Το `jscpd` έπιασε κλώνο που γέννησε η ίδια η διόρθωση**: μόλις το άνοιγμα
των `resolve-helpers` ενοποιήθηκε, οι δύο ουρές `EntityAuditService.recordChange`
έγιναν **πανομοιότυπες** (10 γρ. / 57 tokens). Εξήχθη `recordContactChange`.
**Η κεντρικοποίηση μπορεί να δημιουργήσει τον κλώνο που ο N.18 απαγορεύει.**

---

## 7novies. Βήμα Α — **η διπλοτυπία του ΚΑΝΟΝΑ tenant filter** (2026-08-01)

### 7novies.1 Τι ακριβώς ήταν διπλό

**Όχι κώδικας — κανόνας.** Οι δύο μηχανές κάνουν διαφορετική δουλειά και
**δεν** ενοποιούνται: `deletion-guard` = «τι θα **σπάσει** αν σβήσω;» ·
`contact-impact-engine` = «τι θα **επηρεαστεί** αν αλλάξω;».

Διαβάζουν όμως τον **ίδιο** κατάλογο, όπου κάθε εξάρτηση δηλώνει
`skipCompanyFilter` = «αυτή η **συλλογή** φέρει `companyId`;». Ο κανόνας
**εφαρμογής** αυτής της δήλωσης ήταν γραμμένος **έξι φορές σε τρία αρχεία** —
και είχε ήδη **αποκλίνει σε δύο δόγματα** (§7octies): τα τρία του impact-engine
έγραφαν `if (!query.skipCompanyFilter && companyId)`, τα άλλα πέντε
`if (!dep.skipCompanyFilter)`.

Η §7octies διόρθωσε την **απόκλιση**. Η **δομή** που την επέτρεψε έμενε: έξι
σημεία όπου ο έβδομος συγγραφέας μπορούσε να αποκλίνει ξανά, χωρίς κανένα gate
να τα συγκρίνει.

### 7novies.2 🔴 Ο SSoT υπήρχε ήδη — και το audit τον βρήκε **πριν** γραφτεί διπλότυπο

Το SSOT audit (grep πριν από κάθε γραμμή, N.0) βρήκε `lib/firestore/tenant-scoped-query.ts`
(ADR-702, `scopeQueryToTenant`). **Απαντά όμως άλλη ερώτηση**, και η σύγχυση των
δύο ήταν το πραγματικό ρίσκο του βήματος:

| | ερώτηση | «χωρίς φίλτρο» σημαίνει |
|---|---|---|
| `scopeQueryToTenant` (ADR-702) | «ποιον tenant **δικαιούται ο καλών**;» | **πολιτική**: super-admin list scope |
| `skipCompanyFilter` (μητρώο) | «αυτή η **συλλογή** φέρει `companyId`;» | **δομικό**: το πεδίο δεν υπάρχει καν |

🔑 **Η λύση: σύνθεση στο μισό που ΔΕΝ παραλείπει ποτέ.** Το `scopeQueryToTenant`
έχει **δικό του, νόμιμο** κλάδο παράλειψης (`companyId === null`). Αν ο κανόνας
του μητρώου έμπαινε μέσα του, οι **δύο λόγοι** παράλειψης θα μοιράζονταν κλάδο
και κανείς δεν θα ξεχώριζε **πολιτική** από **σφάλμα** — ακριβώς η αστοχία που
γέννησε το §7octies.

Άρα εξήχθη από το ADR-702 module ο **αδιαπραγμάτευτος** πυρήνας:

```ts
scopeQueryToCompany(query, companyId)   // βάζει ΠΑΝΤΑ where(companyId,'==',x)
scopeQueryToTenant(query, scope)        // ← τώρα τον καλεί· κρατά τον δικό του κλάδο
tenantScopedDependencyQuery(...)        // ← τον καλεί· κρατά τον ΔΙΚΟ ΤΟΥ κλάδο
```

Δύο πολιτικές, **ένας** μηχανισμός, **καμία** κοινή διακλάδωση.

⚠️ **Και δεν κατασκευάστηκε ψεύτικο `TenantScope`.** Ο σκελετός του handoff
πρότεινε `scopeQueryToTenant(query, { companyId })` — **δεν τυπώνει**: ο τύπος
απαιτεί `isSuperAdmin` / `isCrossTenant`, ισχυρισμούς **για τον καλούντα** που
το στρώμα εξαρτήσεων δεν γνωρίζει. Ένα `isSuperAdmin: false` της στιγμής θα ήταν
ψέμα που θα γινόταν αληθινό μόνο κατά τύχη — και θα έσπαγε την ημέρα που ο
ADR-702 αρχίσει να διαβάζει τα πεδία (π.χ. audit σε cross-tenant ανάγνωση).

### 7novies.3 Η υλοποίηση

`lib/firestore/dependency-tenant-scope.ts` — **μία** εξαγόμενη συνάρτηση:

```ts
tenantScopedDependencyQuery(db, collectionPath, dep, companyId): Query
```

Δεν είναι «εφάρμοσε φίλτρο σε υπάρχον query» αλλά **«άνοιξε τη συλλογή ήδη
περιορισμένη»** — ίδιο δόγμα με το `tenantScopedCollection` του ADR-702: το
αφιλτράριστο ενδιάμεσο **δεν υπάρχει ως έκφραση**. Το λάθος δεν πιάνεται, είναι
**μη-εκφράσιμο**.

- **`useCollectionGroup` ΜΕΣΑ** (ερώτημα §3.3.4 του handoff): είναι το «**πώς**
  ανοίγει η συλλογή». Αν έμενε έξω, ο μοναδικός καλών του (`deletion-guard`
  cascade) θα ξανάφτιαχνε αφιλτράριστο ενδιάμεσο — δηλαδή ακριβώς αυτό που το
  module καθιστά αδύνατο.
- **`dep` περνιέται ΟΛΟΚΛΗΡΟ**, ποτέ ανασυντεθειμένο literal: αν κάποιος ξεχάσει
  να αντιγράψει το `skipCompanyFilter`, η μέτρηση **μηδενίζεται σιωπηλά** (φίλτρο
  σε ανύπαρκτο πεδίο ⇒ 0 έγγραφα ⇒ «καμία εξάρτηση»).
- **`companyId` υποχρεωτικό** — ο τύπος είναι ο φύλακας που δεν ξεχνιέται
  (μάθημα #6). Κενό `companyId` **δεν** ακυρώνει το φίλτρο: αποτυγχάνει
  **κλειστά** (§4).

**Έξι σημεία → μηδέν αντίγραφα**: `contact-impact-engine` ×3 ·
`deletion-guard` ×2 · `deletion-link-guard` ×1.

### 7novies.4 🐛 Boy Scout: η κεντρικοποίηση **γέννησε τον κλώνο** — δεύτερη φορά

Μόλις έφυγε το τετράγραμμο `if` από τα δύο σώματα, το `jscpd` κοκκίνισε με
**τρεις** κλώνους (16 + 13 + 15 γραμμές) ανάμεσα σε `deletion-guard` και
`deletion-link-guard`: η ουρά συνάθροισης και το ζεύγος «αναφορά εξάρτησης /
αναφορά αποτυχίας» ήταν **πανομοιότυπα**, και τα κρατούσαν χωριστά μόνο οι
τέσσερις γραμμές που μόλις είχαν φύγει. **Το ίδιο σχήμα με τις ουρές
`EntityAuditService` του §7octies** — μάθημα #3, δεύτερη επιβεβαίωση:
**`jscpd:diff` και ΜΕΤΑ την κεντρικοποίηση, όχι μόνο πριν.**

Εξήχθησαν στο υπάρχον `deletion-common.ts` (ο ήδη κοινός φάκελος των δύο
φυλάκων): `toDependencyOutcome` · `unavailableDependencyOutcome` ·
`summarizeDependencyCheck(results, copy)`. Το `getDefaultRemediation` έγινε
**εσωτερικό** — μοναδικός καλών του είναι πλέον ο κοινός συνθέτης.

🔑 **Ενοποιείται ο μηχανισμός, όχι το λεξιλόγιο** (ίδιο δόγμα με §7octies): τα
μηνύματα («η διαγραφή αποκλείεται» ≠ «ο συνεργάτης δεν μπορεί να αφαιρεθεί»)
μένουν στον κάθε φύλακα ως `DependencyCheckCopy`.

🔴 **Και αποκάλυψε πραγματικό κενό κάλυψης**: η **μπλοκαρισμένη** διαδρομή των
δύο φυλάκων **δεν είχε κανένα test** — δοκιμάζονταν μόνο με κενά αποτελέσματα.
Ο κανόνας `count !== 0` (που κάνει το **σφάλμα ελέγχου** να μπλοκάρει) και το
`Math.max(0, count)` (που εμποδίζει το `-1` να αφαιρέσει από το σύνολο) ήταν
**αόρατα**: μετάλλαξη σε `count > 0` θα επέτρεπε διαγραφή οντότητας της οποίας
τις εξαρτήσεις δεν καταφέραμε καν να δούμε. 8 νέα tests.

### 7novies.5 Το anchor — ισότητα, όχι ανισότητα

`lib/firestore/__tests__/dependency-tenant-scope.test.ts` ελέγχει **τρία
ανεξάρτητα** πράγματα, γιατί το καθένα σπάει χωρίς το άλλο:

| | ερώτηση |
|---|---|
| **Α** | ο κανόνας είναι **σωστός**; (ο SSoT μόνος του) |
| **Β** | οι έξι εκτελεστές τον **καλούν**; (σωστός SSoT που δεν καλείται δεν φυλάει τίποτα) |
| **Γ** | υπάρχει **έβδομο** αντίγραφο; (η **δομή**) |

Το Γ σαρώνει `src/app`, `src/config`, `src/lib`, `src/hooks`, `src/services` με
**`stripComments` πριν από κάθε μέτρηση** (§7octies.2β) και απαιτεί ότι τη
σημαία ονομάζουν σε **εκτελέσιμο** κώδικα **ακριβώς τρία** αρχεία: τα δύο μητρώα
που τη **δηλώνουν** και ο ένας που την **εφαρμόζει**. **Ισότητα** — τέταρτο
αρχείο σημαίνει ότι κάποιος ξαναέγραψε τον κανόνα.

⚠️ **Δηλωμένα τυφλά**: δεν σαρώνονται `src/components` / `src/subapps` (το
δεύτερο έχει δικό του `node_modules` με σπασμένους συνδέσμους — ίδιος
περιορισμός με το `resource-concealment-anchor`)· και οι δύο κατάλογοι είναι
`server-only`, οπότε καταναλωτής εκεί θα ήταν ήδη σφάλμα άλλου είδους.

### 7novies.6 ⚠️ Παρατήρηση **χωρίς αλλαγή** — το κενό `companyId` στον φύλακα διαγραφής

Στον `contact-impact-engine` το κενό `companyId` αποτυγχάνει **κλειστά** (0
μετρήσεις σε προεπισκόπηση = ακίνδυνο, και είναι καρφωμένο με test, §7octies).
Στον **φύλακα διαγραφής** η ίδια είσοδος δίνει 0 εξαρτήσεις ⇒ `allowed: true`
⇒ **fail-open**: η οντότητα σβήνεται ενώ οι εξαρτήσεις της δεν κοιτάχτηκαν.

**Δεν αλλάχθηκε εδώ**, και σκόπιμα: μια ρίψη σε κενό tenant είναι **αλλαγή
δόγματος** στη διαδρομή διαγραφής (N.8 — απόφαση Giorgio), και το ίδιο test που
καρφώνει το «fail closed» της μηχανής επιπτώσεων θα κοκκίνιζε. Καταγράφεται ως
ρητό εύρημα, όχι ως σιωπηλή αλλαγή — μάθημα #9. Η προϋπόθεση για να συμβεί είναι
καλών με χαλασμένο token, δηλαδή το `as` ψέμα του §7.5.

### 7novies.7 Πύλες

**460/460 σε 28 suites** (`lib/firestore` · `lib/auth` · `api/contacts` ·
`api/projects` · `api/floors` · `hooks`) · **11/11 μεταλλάξεις σκοτωμένες**:

| # | μετάλλαξη | πού πέθανε |
|---|---|---|
| α | `skipCompanyFilter` αγνοείται | φίλτρο σε συλλογή που δεν το φέρει |
| β | επαναφορά του `&& companyId` | **μόνο** με κενό `companyId` |
| γ | το φίλτρο εξαφανίζεται | 14 tests |
| δ | καρφωμένος tenant | «ο μισθωτής είναι ΑΥΤΟΣ που δόθηκε» |
| ε | `useCollectionGroup` αγνοείται | cascade του `property` |
| ζ, η | **σημείο κλήσης** παρακάμπτει τον SSoT (×2 μηχανές) | οι έλεγχοι Β |
| θ | έβδομο χειρόγραφο αντίγραφο | το anchor Γ |
| ι, κ, λ | `count !== 0`→`> 0` · `Math.max(0,…)` · όριο προεπισκόπησης | `deletion-common` |

**N.18**: 3 κλώνοι → **0** (7 αρχεία καθαρά) · **N.7.1** όλα <500 · **ΟΧΙ tsc** (N.17).

---

## 7decies. Ομάδα 5 — messages / conversations **DONE** + 🔴 **η μνήμη που παρέκαμπτε τον φύλακα** (2026-08-01)

### 7decies.1 Τα επτά σημεία — και τι πραγματικά έλεγαν

Δύο πόροι, **επτά** σημεία, **μηδέν** μεταμφίεση: όχι μισογραμμένη (§7sexies),
**καμία**.

| Σημείο | γνήσιο «λείπει» | ξένο |
|---|---|---|
| `messages/edit:103` · `pin:104` · `[messageId]/reactions:190` (POST) · `:328` (GET) | `404 'Message not found'` | `403 'Access denied'` |
| `messages/delete:129` (μαζικό) | `reason: 'Message not found'` | `reason: 'Unauthorized - message belongs to different company'` |
| `conversations/{id}/messages:178` | `404 'Conversation {id} not found'` | `403 'Unauthorized: You can only access conversations from your company'` |
| `conversations/{id}/send:250` | `404 'Conversation {id} not found'` | `403 'Unauthorized: …send messages to conversations from your company'` |

🔴 Τα τρία τελευταία δεν είναι απλώς «λάθος κωδικός»: το μήνυμα **κατονομάζει
τον λόγο**, δηλαδή λέει ρητά ότι το έγγραφο **υπάρχει και ανήκει αλλού**. Ο
κωδικός γίνεται δευτερεύων — **το κείμενο είναι το μαντείο**.

**Το ιδιόμορφο (§7ter.5) τηρήθηκε**: το `messages/delete` απαντά `200` με
`errors[]`. Η μεταμφίεση ακολούθησε **εκείνο** το σχήμα — ο `reason` γίνεται ο
**ίδιος** με του ανύπαρκτου μηνύματος — αντί να μετατραπεί σε 404. Γι' αυτό ο
πόρος εκθέτει **και** τη σκέτη ετυμηγορία (`checkMessageAccess`, PDP) πέρα από
τον εκτελεστή που ρίχνει: μια ρίψη εκεί θα ακύρωνε τη διαγραφή **όλης** της
παρτίδας.

⚠️ **Δηλωμένη διεύρυνση**: κανένα από τα επτά δεν είχε κλάδο bypass. Η ενοποίηση
γίνεται **με** bypass (ADR-232 · §7septies · §7octies.3) ώστε τα μηνύματα να μη
γίνουν η μοναδική εξαίρεση· παραμένει **γνωστό χρέος** μέχρι το JIT.

### 7decies.2 🔴🔴 Η μνήμη που παρέκαμπτε τον φύλακα — **διαρροή περιεχομένου**

Η ανάγνωση του `GET /api/conversations/{id}/messages` βρήκε κάτι
**διαφορετικού είδους** από μεταμφίεση:

1. ο έλεγχος ιδιοκτησίας γινόταν **ΜΕΤΑ** την ανάγνωση της μνήμης, και
2. το κλειδί ήταν `api:conversations:messages:{id}:p{page}:s{size}:{order}` —
   **χωρίς μισθωτή**.

⇒ Χρήστης **άλλης εταιρείας** που ζητούσε το ίδιο `conversationId` με τις ίδιες
παραμέτρους σελίδας έπαιρνε **CACHE HIT** με τα μηνύματα της πρώτης, και ο
φύλακας **δεν έτρεχε ποτέ**.

**Και δεύτερο σημείο, άλλου σχήματος**: στο `GET /api/conversations` (λίστα) το
**ερώτημα** ήταν σωστά tenant-scoped (`where(companyId)`), αλλά το κλειδί της
μνήμης όχι ⇒ το φίλτρο ακυρωνόταν **πριν καν εκτελεστεί**. *Ένα φίλτρο μισθωτή
που το προσπερνά η μνήμη δεν είναι φίλτρο μισθωτή.*

🔴 **Αόρατο σε ολόκληρο το ADR-742 μέχρι τώρα**: κάθε έλεγχος ρωτούσε «**τι
απαντά** ο φύλακας;» — κανείς «**τρέχει** ο φύλακας;».

**Διόρθωση, δύο ζώνες** (N.7.2 #4): ο φύλακας μετακινήθηκε **πριν** τη μνήμη,
**και** ο μισθωτής μπήκε στο κλειδί. Το δεύτερο μόνο του δεν αρκεί (αύριο
κάποιος βάζει στο κλειδί τιμή που ελέγχει ο καλών)· το πρώτο μόνο του αφήνει
ζωντανή τη μοιρασμένη θέση.

**Νέο anchor** `lib/auth/__tests__/tenant-cache-isolation-anchor.test.ts`:
σαρώνει `src/app/api` και απαιτεί (α) **κάθε** έκφραση κλειδιού μνήμης να
περιέχει διακριτή μισθωτή (`companyId` ή το `cacheSlot` των έργων), (β) όπου
συνυπάρχουν φύλακας και μνήμη, ο φύλακας να **προηγείται**.

🐛 **Και το ίδιο το anchor γεννήθηκε κενό.** Η πρώτη γραφή τερμάτιζε το regex σε
`;\n` ενώ το repo είναι **CRLF** ⇒ **μηδέν** εκφράσεις βρέθηκαν στα πραγματικά
αρχεία και ο έλεγχος περνούσε **κενός**. Φάνηκε **μόνο** επειδή η μετάλλαξη
«κλειδί χωρίς μισθωτή» **επέζησε**. Προστέθηκε `\r?` **και** ρητή φρουρά «ο
ανιχνευτής βρίσκει όντως ≥4 κλειδιά»: *«κανένας παραβάτης» και «καμία μέτρηση»
δίνουν το ίδιο πράσινο.* Ίδιο σχήμα με το CHECK 3.33 του CLAUDE.md.

### 7decies.3 🔴 Ο ανιχνευτής του anchor ήταν **στενός** — και το gate αναπαρήγαγε το λάθος του §7septies

Η επαναμέτρηση (πάντα πρώτο βήμα, §8) έδειξε **όγδοο** σημείο που καμία σάρωση
δεν είχε δει: `communications/share-to-channel:153` — πόρος **`contacts/{id}`**,
σε **τρίτο** δέντρο, με `404 'Contact not found'` vs `403 'Access denied'`.

**Δύο ανεξάρτητοι λόγοι που επέζησε ολόκληρης της Ομάδας 4:**

1. Ήταν γραμμένο `[FIELDS.COMPANY_ID] !== ctx.companyId` — μορφή που το
   `HAND_ROLLED_OWNERSHIP` **δεν κάλυπτε** (η §7octies.2α το είχε **προβλέψει**
   ρητά για το Βήμα 7· εδώ αποδείχθηκε ότι κόστιζε **ήδη**).
2. Ο έλεγχος «καμία διαδρομή **του φακέλου**» αναπαρήγαγε, **μέσα στο ίδιο το
   gate**, το λάθος που κλείνει η §7septies: *το μαντείο είναι ιδιότητα ΠΟΡΟΥ,
   όχι φακέλου*.

**Διορθώσεις στο anchor:**

- ο ανιχνευτής καλύπτει πλέον **και τις δύο μορφές** της σύγκρισης·
- προστέθηκε **ΚΑΘΟΛΙΚΗ απογραφή με ισότητα** (`HAND_ROLLED_INVENTORY`): η
  πλήρης λίστα αρχείων που επιτρέπεται να την περιέχουν, οπουδήποτε στα
  σαρωμένα δέντρα — **9 αρχεία, όλα Ομάδα 6**·
- `message` και `conversation` μετακόμισαν στο `CONCEALED_RESOURCES`, με
  **μετρημένο ανά πόρο** κατώφλι αρχείων (`minFiles`) αντί για ένα αυθαίρετο
  «>5» που κοκκίνιζε στις 5 διαδρομές των συνομιλιών.

### 7decies.4 🔴 Δύο **κοινοί** μηχανισμοί ακύρωναν τη μεταμφίεση ήδη μεταναστευμένων πόρων

Νέο σήμα στο anchor — **άρνηση που κατονομάζει τον λόγο**
(`belongs to a different company` / `from your company` σε `ApiError(...)`,
`error: '…'`, `reason: '…'`) — καθολικά **μηδέν, χωρίς εξαιρέσεις**. Βρήκε δύο
ζωντανά σημεία **εκτός** κάθε φακέλου πόρου:

| Αρχείο | Τι έκανε | Ποιους πόρους αφορούσε |
|---|---|---|
| `lib/firestore/soft-delete-engine.ts:376` | `403 'Unauthorized: {X} belongs to different company'` | **έξι** οντότητες — `contact`, `property`, `building`, `project`, `parking`, `storage` |
| `app/api/audit-trail/record/route.ts:116` | `403 'Access denied: entity belongs to a different company'` | **κάθε** οντότητα του `ENTITY_COLLECTION_MAP` |

⇒ Τρεις πόροι δηλώνονταν μεταμφιεσμένοι από τις Ομάδες 3 και 4 ενώ **μία**
διαδρομή διαγραφής και **μία** διαδρομή audit απαντούσαν διαφορετικά για το ίδιο
id. Και οι δύο διορθώθηκαν με **ένα** `notFound()` closure, μηδέν ορίσματα
(§7.1).

🔴 **Και οι δύο έφεραν μαζί την παγίδα του κενού** (§4): ρωτούσαν
`data?.companyId && data.companyId !== ctx.companyId` ⇒ έγγραφο **χωρίς**
`companyId` περνούσε για **οποιονδήποτε**. Αυτολεξεί το σφάλμα που έκλεισε στο
`rfq-service` (§7quinquies) και ξανά στο `bank-accounts-server` (§7octies) —
**ζωντανό σε τρίτο και τέταρτο αρχείο**. Χρησιμοποιούν πλέον
`isPayloadOwnedByCompany`· το `audit-trail` πέρασε επίσης από `globalRole !==
'super_admin'` στον SSoT `isRoleBypass` (§7.4).

⚠️ **Δηλωμένη αυστηροποίηση**: έγγραφα χωρίς `companyId` παύουν να είναι
προσβάσιμα από όλους. Καλύπτεται από 5 νέα tests.

### 7decies.5 Τι υλοποιήθηκε

**Ανά πόρο (λεπτά δεσίματα — λεξιλόγιο, όχι λογική)**

`messages/_shared/`: `message-ownership` (απόφαση + κείμενο + εργοστάσιο· εκθέτει
**και** την ετυμηγορία για το μαζικό delete) · `message-owned-doc`
(`loadOwnedMessage` + `readMessageDoc`, το ωμό snapshot για τη **μία** διαδρομή
που δεν επιτρέπεται να ρίξει).
`conversations/_shared/`: `conversation-ownership` (το εργοστάσιο δέχεται
`conversationId` επειδή ο **γνήσιος** κλάδος γράφει το id μέσα στο μήνυμα —
μεταμφίεση χωρίς αυτό θα ξεχώριζε **στο κείμενο**) · `conversation-owned-doc`.

**Boy Scout (N.7.1 / N.18)**: `send/store-outbound-message.ts` και
`messages/conversation-messages-page.ts` — η **εμμονή** και η **χαρτογράφηση**
βγήκαν από τους εκτελεστές HTTP (403→309 και 260→184 γραμμές).

🐛 **Και εδώ η κεντρικοποίηση γέννησε κλώνο** (μάθημα #3, **τρίτη** φορά): τα
imports που πρόσθεσα έσπρωξαν τα προοίμια δύο ζευγαριών διαδρομών πάνω από το
κατώφλι των 50 tokens του `jscpd`. Δεν «παρακάμφθηκε» ούτε χαλαρώθηκε το gate:
οι δύο εξαγωγές παραπάνω **μείωσαν πραγματικά** τα προοίμια (το ένα ζευγάρι
έπεσε στα 6 imports, το άλλο έχασε 7). **CHECK 3.28 καθαρό σε 16 αρχεία.**

### 7decies.6 Πύλες

**478/478 σε 31 suites** · **8/8 μεταλλάξεις σκοτωμένες**:

| # | μετάλλαξη | πού πέθανε |
|---|---|---|
| ν1 | ο εκτελεστής δεν ρίχνει σε `denied` | ταυτότητα σύρματος |
| ν2 | μεταμφίεση → `403 'Access denied'` | `toEqual` στο **ζεύγος** |
| ν3 | `conversationNotFound` χάνει το id | «ίδιο id μέσα στο μήνυμα» |
| ν4 | διαδρομή επαναφέρει χειρόγραφη σύγκριση | **καθολική** απογραφή |
| ν5 | `soft-delete` επαναφέρει το 403 που κατονομάζει | νέο σήμα + 5 tests |
| ν6 | `audit-trail` επαναφέρει το 403 | νέο σήμα |
| ν7 | ο ένας πόρος χάνει το bypass, ο άλλος όχι | «ίδιο δόγμα σε κάθε είσοδο» |
| ν8 | επαναφορά της παγίδας του κενού | «χωρίς companyId ⇒ 404» |

**N.18** καθαρό σε 16 αρχεία · **N.7.1** όλα <500 · **ΟΧΙ tsc** (N.17).

---

## 7undecies. Ομάδα 6 — η **τελευταία** + Βήμα 7 (κλείδωμα) **DONE** (2026-08-01)

### 7undecies.1 🔴 Ο κατάλογος έλεγε «9 αρχεία». Ήταν **12**.

Η καθολική απογραφή του §7decies.3 ήταν **ισότητα** και πράσινη — και **λάθος**.
Ο ανιχνευτής της απαιτούσε **κυριολεκτικά** `ctx.companyId` στο δεξί μέλος:

| αρχείο | γραφή | γιατί ήταν αόρατο |
|---|---|---|
| `procurement/[poId]/email` | `po.companyId !== **auth**.companyId` | το `defineRoute` (ADR-603) ονομάζει τον καλούντα `auth` |
| `procurement/[poId]/share` | `po.companyId !== **auth**.companyId` | ίδιο |
| `showcase/generate/helpers:406` | `share.companyId !== **params**.companyId` | ο tenant ταξιδεύει μέσα σε αντικείμενο παραμέτρων |

**Μια μετονομασία μεταβλητής σε ΑΛΛΟ ADR έκρυψε δύο σημεία ασφαλείας από τρεις
διαδοχικές σαρώσεις.** Το μάθημα #1 δεν είναι ρητό — κοστίζει **κάθε φορά**.

⚠️ **Και δηλωμένο όριο, μετρημένο**: η **γυμνή** μορφή (`data.companyId !==
companyId`) δίνει **21 αρχεία**, εκ των οποίων ~16 είναι **υπηρεσίες** με σιωπηλή
πολιτική ή τυποποιημένο σφάλμα — ό,τι το §3.4 λέει ότι **οφείλει** να λέει την
αλήθεια — συν ψευδώς θετικά (`input.companyId !== undefined`). Δεν διευρύνθηκε:
*ένα gate που καταγγέλλει το σωστό το χαλαρώνει κάποιος*. Τα **δύο** σημεία
εκείνης της μορφής που ήταν όντως αποφάσεις αποκάλυψης
(`showcase/generate/helpers:65`, `quotes/[id]/comments/[commentId]`) βρέθηκαν με
**ανάγνωση**.

### 7undecies.2 🔴🔴 Το βαρύτερο εύρημα δεν ήταν μαντείο — ήταν **δύο αφύλακτες διαδρομές**

Ο πόρος `file` δεν είχε ένα πρόβλημα, είχε **δύο τάξεις**:

| διαδρομή | δικαίωμα | φύλακας ιδιοκτησίας |
|---|---|---|
| `floorplans/process` | ✅ | **403** (μαντείο) + **παγίδα κενού** |
| `floorplans/scene` | ✅ | ίδια δύο |
| `files/[fileId]/download` | `dxf:files:view` | **ΚΑΝΕΝΑΣ** |
| `files/[fileId]/excel-preview` | **κανένα** | **ΚΑΝΕΝΑΣ** |

Οι δύο τελευταίες: **οποιοσδήποτε** συνδεδεμένος χρήστης, **οποιασδήποτε**
εταιρείας, κατέβαζε ξένο αρχείο με το id του· το `excel-preview` το απέδιδε σε
**HTML** χωρίς να ζητά καν δικαίωμα. **Διαρροή περιεχομένου**, όχι ύπαρξης —
ίδιο σχήμα με τις τέσσερις `contact preview` της §7octies.

🔴 **Καμία σάρωση δεν μπορούσε να τις δείξει**: όλοι οι ανιχνευτές ψάχνουν
**λάθος σύγκριση**, κι εκεί δεν υπήρχε σύγκριση **καθόλου**. Βρέθηκαν επειδή, για
να δηλωθεί ο πόρος «κρυμμένος», μετρήθηκαν οι διαδρομές του **μία-μία**.
*Η ομοιομορφία είναι ιδιότητα ΠΟΡΟΥ* (§7septies) — άρα και η μέτρηση.

**Δεύτερο ίδιου είδους**: το `quotes/[id]/comments/[commentId]` ρωτούσε «ανήκει
**το σχόλιο**;» ενώ οι αδελφικές `comments/` ρωτούσαν «ανήκει **η προσφορά**;» —
**δύο δόγματα για τον ίδιο πόρο**. Σχόλιο με σωστό `companyId` κάτω από **ξένη**
προσφορά περνούσε.

**Τρίτο**: το `quotes/[id]/notify-vendor` **έστελνε το email** και **μετά**
ρωτούσε αν η προσφορά είναι δική σου. Ο φύλακας *καλούνταν* — απλώς **αργά**, και
κάθε test του θα έμενε πράσινο (§7.1: *ο κίνδυνος δεν είναι οι γραμμές, είναι η
ΣΕΙΡΑ*).

### 7undecies.3 Η δομή: **μία υλοποίηση, N δηλώσεις**

Το πρότυπο των Ομάδων 3–5 ήταν ένα λεπτό module **ανά πόρο**. Η Ομάδα 6 βρήκε
**έξι** πόρους ταυτόχρονα ⇒ έξι αρχεία που θα διέφεραν σε **τέσσερα literals** —
ακριβώς ο N.18, και το `jscpd` **δεν** θα τα έβλεπε (συνεπής μετονομασία,
μάθημα #4).

| νέο | τι |
|---|---|
| `lib/api/owned-resource-http.ts` | `defineOwnedResource({collection, resourceLabel, idLogField, notFoundMessage})` → `{notFoundMessage, notFoundResponse, check, load}` |
| `lib/auth/owned-doc-loader.ts` | **δεύτερη** μορφή επιβολής: `loadOwnedDocOrRefusal` (επιστρέφει) δίπλα στο `loadOwnedDoc` (ρίχνει), πάνω σε **έναν** πυρήνα |
| `_shared/*-ownership.ts` ×6 | δηλώσεις: dxf-dim-style · dxf-level · floor · file · purchase-order · quote |
| `_shared/quote-comments.ts` | ο **γονέας** + η υποσυλλογή + οι πράξεις του σχολίου |
| `lib/api/tenant-scope-http.guardParentScope` | ο μετατροπέας άρνησης→απάντησης, γραμμένος **3 φορές** |
| `lib/api/space-entity-fields` (+create) | το ADR-696 είχε ενώσει **μόνο** το PATCH· το POST έμεινε δίδυμο |

**Δύο εξαγωγές, ΟΧΙ μία με σημαία**: μισές διαδρομές δεν ρίχνουν (πιάνουν τα
πάντα σε `catch` → 500). Η μορφή που επιστρέφει δέχεται `decide()` με τις
**τρεις ονομασμένες καταστάσεις**, ώστε το `'cross-tenant-bypass'` να μένει ορατό
για το μελλοντικό JIT.

⚠️ **Δηλωμένες διευρύνσεις**: `purchase-order`, `quote`, `dxf-*`, `floor`, `file`
αποκτούν κλάδο bypass (ADR-232) — η εναλλακτική θα τους έκανε τις **μοναδικές**
εξαιρέσεις. ⛔ **ΟΧΙ** στο `showcase/generate/helpers`: οι τρεις καλούντες
περνούν `ctx.companyId`, δηλαδή **tenant**, όχι **καλούντα** — ρόλος που δεν
υπάρχει στην υπογραφή δεν κρίνεται, και κατασκευασμένος καλών θα έμοιαζε με
απόφαση ενώ θα ήταν μαντεψιά.

⚠️ **Δηλωμένη συνέπεια της παγίδας του κενού**: αρχεία **χωρίς** `companyId`
παύουν να είναι ορατά σε κανονικό χρήστη. Η θεραπεία υπάρχει ήδη:
`POST /api/admin/backfill-file-companyid`. Στο `scene` προστέθηκε **δεύτερος
λόγος να πεις ναι** (δημοσιότητα), αλλιώς το (β) θα έσπαγε **δημόσιες σελίδες**:
δημόσιο σχέδιο το βλέπει ο **ανώνυμος**, άρα άρνηση σε συνδεδεμένο δεν κρύβει
τίποτα — απλώς χαλάει.

### 7undecies.4 🔴🔴 Η **κεντρικοποίηση τύφλωσε τον ανιχνευτή** — το βρήκε επιζήσασα μετάλλαξη

Ο έλεγχος ομοιομορφίας ψάχνει `403` κοντά στο **κυριολεκτικό** κείμενο
(`403 … 'File not found'`). Δούλευε όσο κάθε διαδρομή έγραφε το κείμενο μόνη της.
Μετά τη μετανάστευση το κείμενο ζει **μία φορά** και οι διαδρομές το αναφέρουν
**συμβολικά**:

```ts
NextResponse.json({ error: fileResource.notFoundMessage }, { status: 403 })
```

Η μετάλλαξη `404 → 403` **επέζησε**: η φράση δεν υπάρχει στο αρχείο. Κάθε
μεταναστευμένος πόρος θα γινόταν σταδιακά **μονίμως πράσινος** — *«κανένας
παραβάτης» και «καμία μέτρηση» δίνουν το ίδιο πράσινο* (μάθημα #14), αυτή τη
φορά προκαλούμενο **από την ίδια τη διόρθωση**. Νέο σήμα: `403` δίπλα σε
**οποιαδήποτε** αναφορά «δεν βρέθηκε» (καλύπτει και τα
`*_NOT_FOUND_MESSAGE` των Ομάδων 3–5).

### 7undecies.5 Βήμα 7 — το κλείδωμα

Το ίδιο το μητρώο το είχε **προβλέψει**: *«the broad hand-rolled form still
lives in 46 files … deferred to the Phase C/D sweep»*. Το sweep τελείωσε: **46 → 1**,
και αυτό το ένα (`floorplan-overlays`) είναι **δηλωμένα εκτός** (§7ter.1 — φίλτρο
**λίστας**, όχι σημείο απόφασης).

**Τρία** `forbiddenPatterns` στο `tenant-document-ownership`, όχι ένα — καθένα
πιάνει ό,τι τα άλλα δεν μπορούν: (1) η **διευρυμένη** σύγκριση· (2) η άρνηση που
**κατονομάζει** τον λόγο· (3) το `403` δίπλα σε **συμβολικό** «δεν βρέθηκε».

⚠️ Το §4.3 του handoff έλεγε ότι **δεν** χρειάζεται allowlist σχολίων. **Λάθος**:
το `stripComments` έλυσε τη ρίζα **στο anchor**, αλλά ο ratchet κάνει `grep -E`
σε **ωμό** κείμενο. Και τα **11** αρχεία που πυροδοτούν σήμερα τα περιέχουν
**μόνο σε σχόλια** που τεκμηριώνουν τι αντικαταστάθηκε — υποχρεωτικά κατά §4.3.
Διαβάστηκαν ένα προς ένα.

🔴 **Δύο από αυτά ζουν στο `src/subapps/procurement/services/`** — δέντρο που το
`resource-concealment-anchor` **δεν σαρώνει** (περπατά μόνο `src/app/api`,
`src/lib`, `src/server`, `src/services`, γιατί το `src/subapps` έχει δικό του
`node_modules` με σπασμένους συνδέσμους). Το τυφλό σημείο καλύπτεται πλέον από
**αυτό** το gate και καταγράφεται στο `pending-ratchet-work.md`.

---

## 7bis. Εκκρεμεί

| Φάση | Τι | Πού |
|---|---|---|
| **Γ+Δ** | Ομάδες 1 (ai-pipeline, 5) · 2 (procurement, **20**) · 3 (projects, 10) · 4 (contacts/buildings/ika) · 5 (messages/conversations) · **6 (dxf/floorplans/files/po/quotes)** — **ΟΛΕΣ DONE** | §7ter–§7undecies |
| **Βήμα Α · ο κανόνας tenant filter** | **DONE** — 6 αντίγραφα → 1 (§7novies) | §7novies |
| **7 · κλείδωμα** | **DONE** — 3 `forbiddenPatterns` (§7undecies.5) | §7undecies.5 |
| **`ssot:baseline`** | ⏳ **ΔΕΝ έτρεξε ποτέ**· ~20 λεπτά σε Windows και απαιτεί **δικό μας** working tree | — |
| **JIT elevation** | ⏳ **ΞΕΧΩΡΙΣΤΟ, απαιτεί απόφαση Giorgio (N.8)** — το standing `isRoleBypass` **είναι** το anti-pattern· οι Ομάδες 3–6 το έκαναν **εφικτό** (ονομασμένη κατάσταση σε **ένα** σημείο), δεν το υλοποίησαν | §7ter.3 |

⚠️ **Μην παρερμηνεύσεις το μηδέν των `forbiddenPatterns`**: φυλάνε από
**αναδημιουργία** του σχήματος — **όχι** από το να γραφτεί ένας νέος φύλακας που
απλώς *ξεχνά* να ρωτήσει. Δεν υπάρχει regex για «δεν υπάρχει έλεγχος»: οι δύο
αφύλακτες διαδρομές της §7undecies.2 ήταν αόρατες σε **κάθε** σάρωση της
εκστρατείας. Ο **μόνος** τρόπος να βρεθούν είναι να μετρηθεί κάθε διαδρομή ενός
πόρου **μία-μία** τη στιγμή που ο πόρος δηλώνεται κρυμμένος.

---

## 8. Changelog

| Ημ/νία | Αλλαγή |
|---|---|
| 2026-08-01 | **Ουρά Ομάδας 6 — το CHECK 3.7 σταμάτησε τη ΔΙΚΗ μας διόρθωση, και είχε δίκιο.** Το `audit-trail/record` (η **γενική** διαδρομή κάθε οντότητας του `ENTITY_COLLECTION_MAP`) πήρε τη μεταμφίεση σωστά — **ένα** εργοστάσιο `entityNotFound()` με μηδέν ορίσματα για **δύο** κλάδους (§7.1) — αλλά έγραψε την **απόφαση** με το χέρι: `!isRoleBypass(ctx.globalRole) && !isPayloadOwnedByCompany(…)`. Το `tenant-query-scope` απαγορεύει ακριβώς αυτή τη σύζευξη, και ο λόγος είναι ο **δικός μας** (§7octies): η σύζευξη **είναι** η διαδικασία απόφασης, και μια διαδικασία απόφασης ξαναγραμμένη ανά διαδρομή είναι αυτή που θα ξεχαστεί στη μετάβαση σε JIT elevation (§7ter.3). Μεταφέρθηκε στην `createOwnershipDecision('Entity','entityId')` ⇒ οι **τρεις** ονομασμένες καταστάσεις επιβιώνουν (το `'cross-tenant-bypass'` δεν εκφυλίζεται σε boolean) και η απόπειρα καταγράφεται χωρίς να το θυμάται η διαδρομή. **Μάθημα: η κεντρικοποίηση της ΕΡΩΤΗΣΗΣ δεν συνεπάγεται κεντρικοποίηση της ΑΠΟΦΑΣΗΣ — το gate ξεχώρισε τα δύο όταν εμείς δεν τα ξεχωρίσαμε.** N.7.1 (Boy Scout): το `showcase/generate/helpers.ts` πέρασε στις **524** γραμμές από τα σχόλια της §7undecies ⇒ **EXTRACT, όχι trim** — νέο `showcase-pdf-assets.ts` (230 γρ.) με τους **μη-ρίχνοντες** φορτωτές δυαδικών (φωτογραφίες/κατόψεις/συνδεδεμένοι χώροι), το `helpers.ts` κρατά τον **κύκλο ζωής** (πηγές, share, ανέβασμα) → **336** γρ.· 3 καλούντες ενημερώθηκαν (`generate/route`, `pdf/route`, το ίδιο το `helpers`). Η τομή είναι η **ευθύνη**: ό,τι δεν μπορεί να αποτύχει το αίτημα φεύγει· ο έλεγχος ιδιοκτησίας μένει **πριν** από αυτό, στον καλούντα. **ΟΧΙ tsc** (N.17). |
| 2026-08-01 | 🔴🔴 **Ομάδα 6 DONE + Βήμα 7 (κλείδωμα) DONE — η εκστρατεία έκλεισε** (§7undecies). 🔴 **Ο κατάλογος έλεγε 9 αρχεία· ήταν 12**: ο ανιχνευτής της «καθολικής» απογραφής απαιτούσε **κυριολεκτικά** `ctx.companyId`, οπότε ήταν τυφλός σε `auth.companyId` (τα `procurement/[poId]/email` + `share`, όπου το `defineRoute` του **ADR-603** μετονόμασε τη μεταβλητή) και σε `params.companyId` (`showcase/generate/helpers`). **Μια μετονομασία σε άλλο ADR έκρυψε δύο σημεία ασφαλείας από τρεις σαρώσεις.** Διευρύνθηκε σε **οποιοδήποτε** αντικείμενο καλούντος· η **γυμνή** μορφή μετρήθηκε (**21 αρχεία**, ~16 υπηρεσίες με σιωπηλή πολιτική + ψευδώς θετικά) και **δηλωμένα δεν** μπήκε — *gate που καταγγέλλει το σωστό το χαλαρώνει κάποιος*· τα δύο πραγματικά σημεία της βρέθηκαν με **ανάγνωση**. 🔴🔴 **Το βαρύτερο εύρημα ΔΕΝ ήταν μαντείο**: `files/[fileId]/download` και `files/[fileId]/excel-preview` δεν είχαν **κανέναν** φύλακα ιδιοκτησίας (το `_ctx` αχρησιμοποίητο) και το δεύτερο **ούτε δικαίωμα** ⇒ οποιοσδήποτε συνδεδεμένος χρήστης **οποιασδήποτε** εταιρείας κατέβαζε ξένο αρχείο, και το `excel-preview` το απέδιδε σε **HTML** — **διαρροή περιεχομένου** (είδος §7octies). **Καμία σάρωση δεν μπορούσε να τις δείξει: όλοι οι ανιχνευτές ψάχνουν λάθος σύγκριση, εκεί δεν υπήρχε σύγκριση καθόλου.** Βρέθηκαν μετρώντας τις διαδρομές του πόρου **μία-μία**. Δύο ακόμη ίδιου είδους: το `comments/[commentId]` ρωτούσε «ανήκει **το σχόλιο**;» ενώ οι αδελφικές «ανήκει **η προσφορά**;» (δύο δόγματα, ξένη προσφορά περνούσε)· και το `notify-vendor` **έστελνε το email ΠΡΙΝ** τον φύλακα — *ο κίνδυνος δεν είναι οι γραμμές, είναι η ΣΕΙΡΑ* (§7.1). Δομή: **μία υλοποίηση, N δηλώσεις** — νέο `lib/api/owned-resource-http.defineOwnedResource` + **δεύτερη μορφή επιβολής** `loadOwnedDocOrRefusal` (επιστρέφει· **δύο εξαγωγές, όχι μία με σημαία**) πάνω σε **έναν** πυρήνα· 6 `_shared/*-ownership` + `quote-comments` + `guardParentScope` (γραμμένο 3 φορές). Έξι πόροι θα ήταν έξι αρχεία με **τέσσερα literals** διαφορά — και το `jscpd` **δεν** θα τα έβλεπε (μάθημα #4). 🐛 **ΕΠΙΖΗΣΑΣΑ ΜΕΤΑΛΛΑΞΗ: η κεντρικοποίηση ΤΥΦΛΩΣΕ τον ανιχνευτή.** Ο έλεγχος ομοιομορφίας ψάχνει `403` κοντά στο **κυριολεκτικό** κείμενο· μετά τη μετανάστευση οι διαδρομές το αναφέρουν **συμβολικά** (`fileResource.notFoundMessage`), οπότε το `404→403` **επέζησε** και κάθε μεταναστευμένος πόρος θα γινόταν **μονίμως πράσινος** — μάθημα #14, προκαλούμενο **από την ίδια τη διόρθωση**. Νέο **συμβολικό** σήμα, καλύπτει και τους πόρους των Ομάδων 3–5. **Βήμα 7**: το μητρώο το είχε **προβλέψει** («deferred to the Phase C/D sweep», 46 αρχεία) — **46 → 1**, δηλωμένα εκτός. **Τρία** patterns (διευρυμένη σύγκριση · άρνηση που **κατονομάζει** · `403` + **συμβολικό** «δεν βρέθηκε»), επαληθευμένα με πραγματικό `grep -E -f`: **14/14 fixtures**, **11/11** παραβάτες allowlisted (**όλοι** σε σχόλια που τεκμηριώνουν το παλιό σχήμα — το §4.3 του handoff έλεγε ότι allowlist **δεν** χρειάζεται· **λάθος**, ο ratchet grep-άρει **ωμό** κείμενο, το `stripComments` ζει μόνο στο anchor). 🔴 **Δύο εξ αυτών στο `src/subapps/procurement/services/`** — δέντρο που το anchor **δεν σαρώνει**· το τυφλό σημείο καλύπτεται πλέον από το gate. Boy Scout: ADR-696 είχε ενώσει **μόνο** το PATCH των «χώρων» — το **POST** έμεινε δίδυμο (2 κλώνοι, 84+99 tokens) και ενώθηκε με `SPACE_COMMON_CREATE_FIELDS`/`mapCommonSpaceCreateFields`, καρφώνοντας τη σημασιολογία `area > 0` **αλλά** `price >= 0`· επίσης παγίδες κενού σε 3 φίλτρα λίστας (`storages` έγραφε ρητά `!s.companyId ||`) και `isRoleBypass` αντί για σύγκριση συμβολοσειράς σε 4 σημεία. **556/556 σε 30 suites· 9/9 μεταλλάξεις σκοτωμένες + 1 που επέζησε και γέννησε νέο gate**· N.18 **3 κλώνοι → 0** με **πραγματική μείωση**, καμία χαλάρωση· N.7.1 όλα <500· **ΟΧΙ tsc** (N.17). |
| 2026-08-01 | 🔴🔴 **Ομάδα 5 — messages/conversations DONE, και η ΜΝΗΜΗ που παρέκαμπτε τον φύλακα** (§7decies). Τα **επτά** σημεία δεν είχαν μεταμφίεση **καθόλου**· τρία από αυτά (`conversations` ×2, `messages/delete`) είχαν μήνυμα που **κατονομάζει τον λόγο** («belongs to different company», «from your company») — εκεί **το κείμενο είναι το μαντείο**, ό,τι κι αν λέει ο κωδικός. Το ιδιόμορφο του §7ter.5 τηρήθηκε: το μαζικό `delete` απαντά **200 με `errors[]`**, οπότε η μεταμφίεση ακολούθησε **εκείνο** το σχήμα (ο `reason` γίνεται ο ίδιος με του ανύπαρκτου) — γι' αυτό ο πόρος εκθέτει **και** τη σκέτη ετυμηγορία πέρα από τον εκτελεστή που ρίχνει: ρίψη εκεί θα ακύρωνε τη διαγραφή **όλης** της παρτίδας. 🔴🔴 **Το μεγάλο εύρημα ΔΕΝ ήταν μαντείο**: στο `GET /conversations/{id}/messages` ο φύλακας έτρεχε **ΜΕΤΑ** τη μνήμη και το κλειδί **δεν είχε μισθωτή** ⇒ χρήστης **άλλης εταιρείας** με το ίδιο id και τις ίδιες παραμέτρους σελίδας έπαιρνε **CACHE HIT** με ξένα μηνύματα και **ο φύλακας δεν έτρεχε ποτέ**· στο `GET /conversations` (λίστα) το ερώτημα ήταν σωστά tenant-scoped αλλά **η μνήμη όχι**, δηλαδή το φίλτρο ακυρωνόταν **πριν καν εκτελεστεί**. **Διαρροή περιεχομένου** (είδος §7octies), **αόρατη σε ολόκληρο το ADR**: κάθε έλεγχος ρωτούσε «τι **απαντά** ο φύλακας;», κανείς «**τρέχει**;». Διόρθωση σε **δύο ζώνες**: φύλακας **πριν** τη μνήμη **και** μισθωτής μέσα στο κλειδί. Νέο anchor `tenant-cache-isolation-anchor` (κάθε κλειδί φέρει διακριτή μισθωτή· όπου υπάρχει φύλακας+μνήμη ο φύλακας προηγείται). 🐛 **Και το anchor γεννήθηκε ΚΕΝΟ**: το regex τερμάτιζε σε `;\n` ενώ το repo είναι **CRLF** ⇒ μηδέν μετρήσεις, πράσινο χωρίς περιεχόμενο — φάνηκε **μόνο** επειδή η μετάλλαξη επέζησε· προστέθηκε `\r?` **και** φρουρά «βρίσκει όντως ≥4 κλειδιά» (*«κανένας παραβάτης» και «καμία μέτρηση» δίνουν το ίδιο πράσινο*· σχήμα CHECK 3.33). 🔴 **Ο ανιχνευτής του concealment anchor ήταν ΣΤΕΝΟΣ και το gate αναπαρήγαγε το λάθος του §7septies**: (α) δεν κάλυπτε τη μορφή `[FIELDS.COMPANY_ID] !== ctx.companyId` — η §7octies.2α το είχε **προβλέψει για το Βήμα 7**, αλλά κόστιζε **ήδη**: το `communications/share-to-channel` (πόρος **`contact`**, τρίτο δέντρο) επέζησε **ολόκληρης** της Ομάδας 4· (β) έλεγχε «καμία διαδρομή **του φακέλου**» — δηλαδή *ιδιότητα φακέλου*, ενώ το μαντείο είναι **ιδιότητα ΠΟΡΟΥ**. Προστέθηκαν: και οι δύο μορφές, **ΚΑΘΟΛΙΚΗ απογραφή με ισότητα** (9 αρχεία, όλα Ομάδα 6, ονομαστικά), `minFiles` **μετρημένο ανά πόρο**, και **τρίτο σήμα**: άρνηση που **κατονομάζει** τη διαφορά εταιρείας, καθολικά **μηδέν**. Το τρίτο σήμα βρήκε δύο **κοινούς** μηχανισμούς που ακύρωναν τη μεταμφίεση ήδη μεταναστευμένων πόρων: `soft-delete-engine` (**6 οντότητες**: contact/property/building/project/parking/storage) και `audit-trail/record` (**κάθε** οντότητα). 🔴 **Και οι δύο έφεραν την παγίδα του κενού** (`data?.companyId && …` ⇒ έγγραφο χωρίς `companyId` περνούσε για οποιονδήποτε) — αυτολεξεί το σφάλμα των §7quinquies/§7octies, **ζωντανό σε τρίτο και τέταρτο αρχείο**. Boy Scout (N.7.1): `store-outbound-message.ts` (403→309 γρ.) + `conversation-messages-page.ts` (260→184 γρ.). 🐛 **Η κεντρικοποίηση γέννησε κλώνο για ΤΡΙΤΗ φορά** — τα νέα imports έσπρωξαν δύο προοίμια διαδρομών πάνω από τα 50 tokens· **δεν χαλαρώθηκε το gate**, οι δύο εξαγωγές τα **μείωσαν πραγματικά**. **478/478 σε 31 suites· 8/8 μεταλλάξεις**· N.18 καθαρό σε 16 αρχεία. |
| 2026-08-01 | **Βήμα Α — η διπλοτυπία του ΚΑΝΟΝΑ tenant filter έκλεισε** (§7novies). Η §7octies είχε διορθώσει την **απόκλιση**· έμενε η **δομή**: ο κανόνας εφαρμογής του `skipCompanyFilter` γραμμένος **6 φορές σε 3 αρχεία**, δηλαδή έξι σημεία όπου ο έβδομος συγγραφέας μπορούσε να αποκλίνει ξανά. 🔴 **Ο SSoT υπήρχε ήδη** (`tenant-scoped-query.ts`, ADR-702) — το audit τον βρήκε **πριν** γραφτεί διπλότυπο — **αλλά απαντά ΑΛΛΗ ερώτηση**: «ποιον tenant δικαιούται ο **καλών**» (παράλειψη = **πολιτική**) vs «αυτή η **συλλογή** φέρει `companyId`» (παράλειψη = **δομικό**). 🔑 Λύση: **σύνθεση στο μισό που δεν παραλείπει ποτέ** — εξήχθη το `scopeQueryToCompany` και **οι δύο** πολιτικές το καλούν, κρατώντας η καθεμιά **τον δικό της** κλάδο παράλειψης· αν μοιράζονταν κλάδο, κανείς δεν θα ξεχώριζε **πολιτική** από **σφάλμα**. ⚠️ Ο σκελετός του handoff (`scopeQueryToTenant(query, { companyId })`) **δεν τυπώνει**: ο `TenantScope` απαιτεί `isSuperAdmin`/`isCrossTenant`, ισχυρισμούς **για τον καλούντα** που το στρώμα εξαρτήσεων δεν γνωρίζει — ένα `false` της στιγμής θα ήταν ψέμα που γίνεται αληθινό κατά τύχη. Νέο `lib/firestore/dependency-tenant-scope.ts` με **μία** εξαγωγή που **ανοίγει τη συλλογή ήδη περιορισμένη** (το αφιλτράριστο ενδιάμεσο δεν υπάρχει ως έκφραση)· `useCollectionGroup` **μέσα** (αλλιώς ο cascade ξαναφτιάχνει το ενδιάμεσο)· το `dep` περνιέται **ολόκληρο** (ανασυντεθειμένο literal που χάνει τη σημαία **μηδενίζει σιωπηλά** τη μέτρηση). 🐛 **Η κεντρικοποίηση γέννησε τον κλώνο — δεύτερη φορά** (μάθημα #3): μόλις έφυγε το τετράγραμμο `if`, το `jscpd` βρήκε **3 κλώνους** (16+13+15 γρ.) ανάμεσα στους δύο φύλακες — τους κρατούσαν χωριστά **μόνο** οι γραμμές που μόλις είχαν φύγει. Εξήχθησαν στο `deletion-common` (`toDependencyOutcome` · `unavailableDependencyOutcome` · `summarizeDependencyCheck`), **μηχανισμός ναι, λεξιλόγιο όχι**. 🔴 Και αποκάλυψε **κενό κάλυψης**: η **μπλοκαρισμένη** διαδρομή των φυλάκων δεν είχε **κανένα** test — ο κανόνας «σφάλμα ελέγχου (`-1`) **μπλοκάρει**» ήταν αόρατος, και μετάλλαξη σε `count > 0` θα επέτρεπε διαγραφή οντότητας της οποίας τις εξαρτήσεις δεν είδαμε καν. ⚠️ **Παρατήρηση χωρίς αλλαγή**: κενό `companyId` στον φύλακα **διαγραφής** είναι **fail-open** (0 εξαρτήσεις ⇒ allowed) ενώ στη μηχανή επιπτώσεων fail-closed — ρίψη εκεί είναι **αλλαγή δόγματος** (N.8), καταγράφεται αντί να αλλάξει σιωπηλά. Anchor **ισότητας**: τη σημαία ονομάζουν σε **εκτελέσιμο** κώδικα ακριβώς **3** αρχεία (2 μητρώα + 1 εφαρμοστής), με `stripComments` και δηλωμένα τυφλά. **460/460 σε 28 suites· 11/11 μεταλλάξεις**· N.18 3 κλώνοι → **0**. |
| 2026-08-01 | 🔴🔴 **Ομάδα 4 — contacts/buildings/ika DONE, και η διαρροή που ΔΕΝ ήταν μαντείο** (§7octies). Ξεκίνησε ως μεταμφίεση 12 σημείων· η ανάγνωση βρήκε **διαφορετικού είδους** πρόβλημα. Ο πόρος `contact` έχει **έξι** διαδρομές preview (το handoff έλεγε «δύο») και **τέσσερις** έτρεχαν **χωρίς κανένα δικαίωμα και χωρίς φύλακα** — το `withAuth` ελέγχει δικαίωμα μόνο αν του δοθεί, και το `_ctx` τους ήταν αχρησιμοποίητο. **Η δεύτερη μισή αιτία στη μηχανή**: ο `contact-impact-engine` ρωτούσε `if (!skipCompanyFilter && companyId)` με `companyId?` **προαιρετικό**, και **κανένας από τους 6 καλούντες δεν το περνούσε** (ένας έγραφε ρητά `undefined`) ⇒ **το φίλτρο tenant δεν έτρεξε ΠΟΤΕ**. Αποτέλεσμα: οποιοσδήποτε συνδεδεμένος χρήστης μάθαινε πλήθη ακινήτων/τιμολογίων/συμβολαίων ξένης επαφής **από όλους τους πελάτες** — **διαρροή περιεχομένου, όχι ύπαρξης**. 🔑 Απόδειξη ότι είναι σφάλμα: η **αδελφή μηχανή** πάνω στο **ίδιο** registry (`deletion-guard.ts`) δηλώνει `companyId: string` και ρωτά μόνο `if (!skipCompanyFilter)` — **ένα registry, δύο μηχανές, δύο δόγματα, κανένα gate να τις συγκρίνει** (σχήμα CHECK 3.34). **Τρεις αστοχίες μέτρησης**: (α) το grep έχασε 2 σημεία γραμμένα `[FIELDS.COMPANY_ID] !== ` — **αφορά άμεσα το Βήμα 7**· (β) το anchor έμεινε **πράσινο στο 4** ενώ 2 παραβάσεις έφυγαν και 2 **σχόλια** μπήκαν — *ο μετρητής μετρούσε πρόζα*· λύθηκε με καθολικό `stripComments` + 3 self-tests, και η εξαίρεση `ssot:` **καταργήθηκε** ως περιττή· (γ) το **`jscpd` βρήκε τους 3 φύλακες ΚΑΘΑΡΟΥΣ** επειδή τα ονόματα διέφεραν — το τυφλό του ADR-584 **ανάποδα**. Έρευνα βιομηχανίας: Google *zero standing privilege* + Access Approval· Atlassian TCS· *«never grant standing cross-tenant access»* ⇒ **το `isRoleBypass` των 47 σημείων ΕΙΝΑΙ το anti-pattern**· η Ομάδα 4 δεν υλοποιεί JIT (N.8) αλλά το κάνει **εφικτό**: η bypass απόφαση γίνεται ονομασμένη κατάσταση σε **ένα** σημείο. Νέα: `lib/auth/resource-ownership-guard` (η **απόφαση**, μία φορά) + `lib/auth/owned-doc-loader` (η **αλυσίδα**, μία φορά — η Ομάδα 3 την είχε λύσει μόνο για τα έργα, η 4 θα έγραφε δίδυμο **κεντρικοποιητή**) + `contacts/_shared/` ×4 + `buildings/_shared/building-ownership`· τα `project-*` **ξαναδέθηκαν** με **αμετάβλητη** υπογραφή. Έκλεισαν και σημεία **εκτός φακέλου**: `bank-accounts-server.service:88` (**δύο** σφάλματα: επαφή χωρίς `companyId` περνούσε ως `success: true` για οποιονδήποτε **σε τρεις μεταλλάξεις** — αυτολεξεί το σφάλμα της §7quinquies **ζωντανό σε δεύτερο αρχείο** — **και** 403/404 μαντείο), `ika/…/insurance-class` (**τρίτο** σχήμα άρνησης για `contacts/{id}`), `admin/role-management` (μήνυμα που **απαριθμεί** τους λόγους της άρνησης), `resolve-helpers` ×3 (παγίδα κενού σε **εγγραφές**). ⚠️ Δηλωμένη διεύρυνση: `GET`/`properties` δίνουν πλέον bypass στον υπεργραφέα — η προηγούμενη στένωση ήταν **ψευδής ασφάλεια** (τα preview έδιναν ήδη τα ίδια δεδομένα). 🐛 **Μετάλλαξη επέζησε σε ΔΙΚΟ ΜΟΥ test**: έδινε πάντα υπαρκτό tenant, οπότε η επαναφορά του `&& companyId` άφηνε 5/5 πράσινα — το **κενό** `companyId` είναι η μόνη είσοδος που ξεχωρίζει τις εκδοχές. 🐛 **Το `jscpd` έπιασε κλώνο που γέννησε η ίδια η διόρθωση** (οι ουρές audit έγιναν πανομοιότυπες μόλις ενοποιήθηκε το άνοιγμα). **419/419 σε 28 suites· 11/11 μεταλλάξεις**· N.18 καθαρό σε 13 αρχεία. |
| 2026-08-01 | 🔴 **§7septies — η ομοιομορφία ανά ΠΟΡΟ, και το anchor που τη φυλάει.** Με την Ομάδα 3 κλειστή, μέτρηση έδειξε ότι η μεταμφίεση **ακυρωνόταν ολόκληρη**: τέσσερις **αδελφικές** διαδρομές (`/customers`, `/efka-declaration`, `/v2/.../customers`, `floors`) απαντούσαν **403 `Access denied`** για το **ίδιο** `projects/{id}` που το `GET` μόλις είχε μεταμφιέσει σε 404. **Το μαντείο δεν είναι ιδιότητα διαδρομής — είναι ιδιότητα πόρου**, και κάθε unit test καθεμιάς έμενε πράσινο γιατί καθεμιά ήταν εσωτερικά συνεπής. Έρευνα: υπάρχουν **δύο** έγκυρα δόγματα — **404-ομοιόμορφο** (GitHub/Drive/Figma) και **403-ομοιόμορφο ασαφές** (Google Cloud **AIP-193/211**: *«Permission denied … (or it might not exist)»*, με τον έλεγχο δικαιώματος **πριν** τον έλεγχο ύπαρξης) — και **το λάθος είναι η ΑΝΑΜΕΙΞΗ**, όχι η επιλογή. Ο Νέστωρ κρατά το 404 γιατί κάθε `list` του είναι tenant-scoped (κριτήριο §3.3 / `s3:ListBucket`), ενώ στο GCP ο χρήστης συχνά **δικαιούται** να μάθει ποιο δικαίωμα του λείπει. Διόρθωση: **δύο γραμμές** στο `requireDocInTenant` (ένα `notFound()` closure, μηδέν ορίσματα) — η μεταμφίεση **διαδόθηκε δομικά σε 41 καταναλωτές χωρίς να αγγιχτεί κανείς**, γιατί όλοι ρωτούσαν ήδη `code === 'NOT_FOUND'`. Καλύπτει **6 οντότητες** μαζί (προκαταβάλλει μέρος των Ομάδων 4/6). Το `requireTenantScope` (ADR-702) **κρατά** το 403 του: άρνηση **παραμέτρου**, όχι εγγράφου. **13 tests κοκκίνισαν** — ένα με τίτλο «συμβόλαιο που ΔΕΝ επιτρέπεται να μετακινηθεί» που ζητούσε ρητά «αλλαγή δόγματος με **ρητή απόφαση**, όχι refactor»· η απόφαση λήφθηκε και το test **αντιστράφηκε** (μάθημα #5). 🏆 **Νέο anchor** `resource-concealment-anchor.test.ts`: ελέγχει **ανά πόρο** ότι **καμία** διαδρομή —**και σε άλλον φάκελο**— δεν αποκλίνει· **δεν είναι ratchet, καμία baseline**· δηλώνει ρητά τα τυφλά του και **μετρά** τους μη-μεταναστευμένους πόρους (`contact` = 4). Η βιομηχανία δηλώνει τον κανόνα σε πεζό κείμενο· **κανείς δεν τον ελέγχει αυτόματα**. 4/4 μεταλλάξεις του anchor σκοτωμένες. |
| 2026-08-01 | **Ομάδα 3 — projects DONE** (§7sexies). 10 σημεία σε 9 αρχεία, **δύο** σχήματα σύρματος. 🔴 Η μεταμφίεση ήταν **ΜΙΣΟΓΡΑΜΜΕΝΗ** — σωστό κείμενο, **λάθος κωδικός**: `403 'Access denied - Project not found'`, δηλαδή η §7quater **ανάποδα** ⇒ **η μεταμφίεση κρίνεται στο ΟΛΟΚΛΗΡΟ ΣΧΗΜΑ**. Ρητή απόφαση (§7sexies.2): **ΟΧΙ** `concealCrossTenant` — ο `reveal` είναι απρόσιτος εδώ, και κώδικας που δεν εκτελείται δεν δοκιμάζεται ⇒ σαπίζει· η απόφαση bypass μένει ορατή στη `checkProjectAccess` ώστε το μελλοντικό JIT να έχει **ένα** σημείο. 🔴 Οι **δύο log-only κλάδοι** (που η §7ter είχε βγάλει έξω, σωστά, ως μη-σημεία απόφασης) **απορροφήθηκαν**: γράφουν αυτολεξεί το `\.companyId !== ctx\.companyId`, άρα **χωρίς αυτό το Βήμα 7 ήταν αδύνατο**. Ετυμηγορία με **τρεις ονομασμένες καταστάσεις**, όχι boolean. Νέα: `project-ownership` (PDP+PEP+εργοστάσιο) · `project-owned-doc` (ο ένας φορτωτής) · `project-preview-route` (**6** διαδρομές: 49–58 γρ. → 13–22· ρυθμιστής **μέσα**, δεν ξεχνιέται) · `project-cache` (Boy Scout, 4 σημεία). Η **παγίδα του κενού** έκλεισε σε **9 από τα 10** (μόνο το update ρωτούσε ήδη σωστά). 🐛 **Μετάλλαξη που επέζησε αποκάλυψε πραγματικό κενό**: ο έλεγχος ύπαρξης **δεν** είναι πλεονασμός — για τον **υπεργραφέα** είναι το **μόνο** που τον σταματά, αλλιώς κατεβαίνει **φάντασμα έργου** (`data: undefined`)· 2 νέα tests. ⚠️ Καταγράφεται και **δικό μου σφάλμα ελέγχου**: «εύρημα» ότι 2 σημεία cache σβήνουν 3 αντί 4 κλειδιά ήταν **αστοχία του grep μου** — *το grep μετρά τη μορφή που έψαξες, και ισχύει και για τον ελεγκτή*. **829/829 σε 59 suites· 18/18 μεταλλάξεις**· N.18 καθαρό σε 18 αρχεία. |
| 2026-08-01 | **Ομάδα 2 — procurement services DONE** (§7quinquies). **20** σημεία σε 7 υπηρεσίες — **το ένα το είχε χάσει η σάρωση**: το `rfq-line-service:45` έγραφε τη σύγκριση σε **δύο εντολές** με ενδιάμεση μεταβλητή, άρα το grep της §7ter δεν την είδε ⇒ **το «65» είναι κάτω όριο, όχι σύνολο**. 🔴 Δομικό σφάλμα στο σύνορο: κωδικός και μήνυμα υπολογίζονταν σε **δύο ανεξάρτητες γραμμές** — μεταμφίεση που γυρίζει 403→404 αλλά αφήνει `'Forbidden'` στο σώμα **δεν κρύβει τίποτα**· πλέον **ένα ζεύγος** + `logMessage` που δεν μεταμφιέζεται. Το `msg.includes('Forbidden')` → **0** (η §7.4 για τις προμήθειες)· **3 contract tests της ADR-603 κάρφωναν το ίδιο το σφάλμα** και ξαναγράφτηκαν. 🐛 Το φίλτρο του email fan-out άφηνε επαφή **χωρίς** `companyId` να λάβει **portal token** — η μετάλλαξη επαναφοράς άφηνε **282/282 πράσινα**. ⚠️ Δηλωμένες αλλαγές: τα legacy `api/rfqs/[id]` έδιναν **`400 Forbidden`** (ρητή αποκάλυψη με λάθος κωδικό) → τώρα μεταμφιεσμένο 404. Boy Scout: **10 προϋπάρχοντες κλώνοι → 0** (νέα `procurement-owned-doc`, `rfq-ownership`, `rfqIdRoute`). 🐛 **13 tests δεν είχαν τρέξει ΠΟΤΕ** (mock που όριζε το `firestore` ως χώρο ονομάτων, όχι συνάρτηση → «suite failed to run», αόρατο). **292/292· 22/22 μεταλλάξεις**· N.18 καθαρό. |
| 2026-07-31 | Δημιουργία. Φάση Α: ενοποίηση 4 συστημάτων + καταχώρηση στο μητρώο + πρώτες δοκιμές για τους 6 φύλακες. Διόρθωση ψευδούς κόκκινου στο anchor του ADR-702. |
| 2026-07-31 | **Σάρωση Γ+Δ — Βήμα 1: αναγνώριση** (§7ter). **65 σημεία σε 45 αρχεία**: Γ **52** / Δ **10** / ⛔ ΕΞΩ **3**. 🔴 Το grep-φίλτρο «έχει 403 στο αρχείο» ήταν λάθος **και προς τις δύο κατευθύνσεις** (3/9 υποψήφια Δ ήταν όντως Δ· 6 Δ κρυμμένα σε αρχεία **με** 403) ⇒ η ταξινόμηση απαιτεί **ανάγνωση**. Ο κλάδος `reveal` είναι **απρόσιτος** στη Γ (§7ter.2). Έρευνα βιομηχανίας: το standing bypass των 47 σημείων **είναι** το πρόβλημα (ZSP/JIT· Google Access Transparency) — η Γ+Δ **δεν** αλλάζει ποιος βλέπει τι (§7ter.3). Αρχιτεκτονική = **PEP/PDP**, όχι ένας φύλακας (§7ter.4). |
| 2026-07-31 | **Φάση Δ — στρώμα πράκτορα ΤΝ DONE** (§7quater). Νέο `tool-tenant-guard.ts`· 5 handlers· **δύο έλεγαν αυτολεξεί «belongs to a different company»** στο μοντέλο. 🔴 Το `firestore-handler` ήταν σπασμένο **στο σχήμα** (`success` flag), όχι στο κείμενο — και **η σουίτα το τεκμηρίωνε πράσινη** με δύο tests δίπλα-δίπλα. 🐛 Έκλεισε κενό: το `relationship-handler` έλεγχε **μόνο την πηγή** (η αφαίρεση του νέου φύλακα άφηνε 12/12 πράσινα). Boy Scout (§7quater.1): **5 προϋπάρχοντες κλώνοι → 0**, νέο `firestore-query-plan.ts` + **`withScopedRead`** (το «διάβασε χωρίς tenant scope» γίνεται **μη εκφράσιμο**). 🔴 Η εξαγωγή παραλίγο να μετατρέψει το τελευταίο καταφύγιο σε «όλες οι **άλλες** εταιρείες» — ο operator του μοντέλου επιβιώνει στο `enforceCompanyScope`. **1220/1220· 10/10 μεταλλάξεις**· N.18 καθαρό. |
| 2026-07-31 | 🔴 **Τρίτο μονοπάτι της παγίδας του κενού**: η **`ownedOrNull`** — το **σιωπηλό** API του SSoT — ρωτούσε ακόμη σκέτο `===` (η Φάση Α έκλεισε τις `require*InTenant`, η Β την `assertOwnedByCompany`, **καμία τη σιωπηλή**). Ο μοναδικός καλών της έγραφε `ownedOrNull({ companyId: row.companyId as string }, …)` πάνω σε `DocumentData` — **το ίδιο `as` ψέμα του §7.5**, με ζημιά **σιωπηλή ανάγνωση ξένου εγγράφου** αντί για λάθος κωδικό. Δέχεται πλέον `MaybeTenantOwned` + `isPayloadOwnedByCompany`· 29 tests, μετάλλαξη επιστροφής στο `===` σκοτωμένη. |
| 2026-07-31 | **Φάση Β** (§7). Το δόγμα αποκάλυψης στα 4 endpoints: `ctx` **υποχρεωτικό**, μεταμφιεσμένο 404 **δομικά** ταυτόσημο με το γνήσιο (ένα `notFound` callback, δύο κλάδοι). Εξαλείφθηκε το `msg.includes('Cross-tenant')` ως έλεγχος πρόσβασης (2 ζωντανά σημεία → **0**, νέο 4ο `forbiddenPattern`). Ξεχωρίστηκε το «locked» από το cross-tenant (§7.3). 9ο σημείο: `floorplan-scale.service` → τυποποιημένο σφάλμα. 🔴 **Κλείσιμο τρύπας που είχε αφήσει η Φάση Α**: η `assertOwnedByCompany` συνέκρινε ακόμη με `===` ενώ όλοι οι καλούντες της περνούν `snap.data() as XDoc` (§7.5) — **το βρήκε test, όχι ανάγνωση**. 4 νέα κοινά modules· N.18 6 clones → 0· 386/386 tests· 15/15 μεταλλάξεις. |
