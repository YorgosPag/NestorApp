# ADR-449 — Structural Finish Skin (σοβάς κολόνας/δοκαριού, per-face adjacency-driven)

**Status:** 🟢 Slices 1-6 + Slice 8/8b + **Slice 9** implemented (data model + resolver + BOQ + 3D/2D skin + δοκάρια + toggle/override + beam↔column junction + height-aware wall coverage + attached-top exclusion + **Slice 9 συνεπής γωνιακή ΕΝΩΣΗ σοβά δοκαριού↔κολόνας, browser-verified «τέλεια ένωση»** + **Slice 10 junction-aware κλείσιμο: EXTEND/corner-fill στις flush «από κάναβο» συμβολές, 45° chamfer μόνο σε ελεύθερα άκρα**). **Slice X1 (2026-06-14): η merged structural silhouette ΑΝΑΒΙΩΣΕ → ΕΝΕΡΓΗ στο 3Δ** (αντικαθιστά το per-element 3Δ σκιν): ΕΝΑ συνεχές δέρμα σοβά τυλίγει τη σιλουέτα του μπετόν ανά ζώνη → μηδέν overlap/αλληλοδιείσδυση (Slice 10) & διπλή γραμμή στις συμβολές. Το παλιό «μία όψη μόνο» **δεν ήταν τοπολογικό** αλλά naive wall coverage (grid τοίχοι ταυτόσημοι σε κάτοψη με δοκάρια)· διορθώθηκε με height-aware `WallObstacle` z-extents (port Slice 8/8b). **DEFER Slice X2: 2Δ merged outline.** **Slice 11 (2026-06-15): ΟΡΙΖΟΝΤΙΕΣ εκτεθειμένες όψεις σοβά (browser-verified).** **Slice 12 (2026-06-15): height-SSoT — ο σοβάς (silhouette + caps/soffit) παίρνει το κατακόρυφο εύρος [zBot,zTop] από την ΙΔΙΑ SSoT με τον rendered πυρήνα (storey-aware `resolveColumnVerticalExtentMm`), ΟΧΙ από raw `params.height` → σοβάς=πυρήνας πάντα.** **Slice X3 (2026-06-21): ο ΤΟΙΧΟΣ ως finish-member — ο σοβάς τυλίγει περιμετρικά + σβήνει αυτόματα στις επαφές μέσω merged silhouette (FULL SSoT· πηγή=DNA, μηδέν νέο spec· ενιαίο 15mm, ασύμμετρο=Slice X4).** — Slice 9-12 + X3 pending browser-verify + commit
**Discipline:** DXF Viewer · BIM finishes · BOQ/ΑΤΟΕ · structural columns/beams
**Related:** ADR-363 (column/beam types + wall DNA), ADR-447 (wall plaster materials/catalog), ADR-396 (ETICS thermal envelope — exterior/interior classification, building footprint), ADR-401 (wall host-plan `coveredIntervals` — εξήχθη εδώ σε shared SSoT), ADR-445 (structural colour identity), ADR-413 (PBR textures), ADR-175/ΑΤΟΕ (BOQ)

---

## 1. Context / Problem

Τα στατικά δίνουν π.χ. κολόνα 50×50cm. Στην πραγματικότητα σοβατίζεται περιμετρικά → αν αλλάξουμε το `width/depth` για να «χωρέσει» ο σοβάς, **αλλοιώνουμε τα στατικά**. Θέλουμε ταυτόχρονα:
- (α) **αμετάβλητο στατικό πυρήνα** (50×50, ό,τι έδωσε ο μηχανικός),
- (β) **πραγματική σοβατισμένη όψη**,
- (γ) **ποσότητες σοβά** για επιμετρήσεις (BOQ).

**Κρίσιμο (Giorgio):** ο σοβάς **ΔΕΝ είναι ομοιόμορφος**. Κάθε παρειά κολόνας/δοκαριού μπορεί να είναι:
- **εσωτερική** → σοβάς Knauf (`mat-plaster-int`),
- **εξωτερική** → εξωτ. σοβάς (`mat-plaster-ext`) ή θερμοπρόσοψη (ETICS),
- **καλυμμένη από τοίχο** → καθόλου σοβάς,
- **ΜΕΡΙΚΩΣ καλυμμένη**: παρειά 50cm όπου δύο τοίχοι ακουμπάνε 25+25 → σοβατίζεται μόνο το εκτεθειμένο μεσαίο κομμάτι.

Δοκάρια ομοίως: πάνω=πλάκα, κάτω=κορυφές τοίχων, άκρα=κολόνες → συνήθως μένουν 2 πλάγιες όψεις.

**Revit / big-player:** Structural Columns = single-material πυρήνας (immutable· το αναλυτικό μοντέλο ποτέ δεν αλλάζει)· τα finishes είναι **πρόσθετα αρχιτεκτονικά** (additive skin), η «πραγματική» όψη (πυρήνας+2×σοβάς) είναι **derived** (display/BOQ), ΟΧΙ αποθηκευμένη στατική διάσταση.

## 2. Decision

### 2.1 Αρχή SSoT
- `ColumnParams.width/depth` (& `BeamParams`) = **στατικός πυρήνας, immutable**. Ο σοβάς ΠΟΤΕ δεν τον αλλάζει.
- Ο σοβάς = additive metadata (`finish?: StructuralFinishSpec`, stored) + **derived** geometry/ποσότητες (resolver, ποτέ stored).
- **Δεν** επαναχρησιμοποιεί το `envelopeLayer` (ETICS): εκείνο είναι meters/zone/exterior-only — τα δύο **συνυπάρχουν** (εξωτ. όψη = ETICS, εσωτ. = Knauf).

### 2.2 Κεντρικός resolver (ΕΝΑ SSoT για 3D/2D/BOQ)
`bim/finishes/structural-finish-resolver.ts` — pure. Για κάθε ακμή του footprint:
1. `covered = ⋃ coveredIntervals(edge, wallFootprint)` (REUSE shared SSoT),
2. `exposed = exposedComplement(covered)`,
3. ανά exposed υπο-τμήμα: midpoint + outward normal `(dy,−dx)` (CCW) → injected `classify` → interior/exterior → υλικό από spec → μήκος×scale → m.

Output `StructuralFinishFaces` = εκτεθειμένες υπο-ακμές + `interiorAreaM2`/`exteriorAreaM2`. Η ταξινόμηση εγχέεται ως callback → resolver 100% testable με stub.

### 2.3 Classification (scene adapter)
`bim/finishes/structural-finish-scene.ts` χτίζει obstacles (footprints τοίχων **+ mutual structural obstacles** δοκαριών/κολώνων στη σύνδεση — Slice 6) + classifier:
1. ρητό `column.params.envelopeFunction` ('exterior'/'interior') υπερισχύει (Revit Wall-Function-style override),
2. αλλιώς γεωμετρικά: παρειά **exterior** όταν το midpoint της βρίσκεται στο εξώτατο όριο (outer ring) component που **περικλείει χώρο** (holes>0 = πραγματικό περίγραμμα κτιρίου, REUSE `computeBuildingFootprint`). Μεμονωμένη εσωτερική κολόνα (δικό της component χωρίς holes) → όλες interior (Knauf), σωστά.

### 2.4 BOQ (ξεχωριστές γραμμές, εξαιρώντας καλυμμένα)
`bim/services/structural-finish-boq.ts` (mirror `boq-multi-layer-builder`):
- **parent** = στατικός πυρήνας (κολόνα `OIK-2.03` m³ σκυρόδεμα, αμετάβλητο, `isGroupParent:true`),
- **child interior** = εσωτ. σοβάς (`OIK-4.01` m², `interiorAreaM2`) — αν >0,
- **child exterior** = εξωτ. σοβάς (`OIK-4.03` m², `exteriorAreaM2`) — αν >0.

Deterministic IDs: `boq_bim_${id}` / `_finish_int` / `_finish_ext`. Hook στο `BimToBoqBridge.upsertWithFinish` (dispatch: opening → multiLayerWall → **finish** → single-entry). Contribution υπολογίζεται upstream στο `column-boq-feed` (έχει πρόσβαση στη σκηνή). Delete cascade ΗΔΗ καλύπτει finish children (parentBoqItemId).

## 3. Files (Slice 1)

**NEW:**
- `bim/geometry/shared/segment-polygon-coverage.ts` — `coveredIntervals` (εξήχθη από `wall-host-plan-builder`, N.0.2) + `mergeIntervals`/`exposedComplement` + `Pt2`.
- `bim/finishes/structural-finish-types.ts` — `StructuralFinishSpec` (stored) + `StructuralFinishFaces` (derived) + defaults (15mm, Knauf/σοβάς).
- `bim/finishes/structural-finish-resolver.ts` — pure resolver.
- `bim/finishes/structural-finish-scene.ts` — scene adapter (obstacles + classifier).
- `bim/services/structural-finish-boq.ts` — pure BOQ payload builder.
- tests: `structural-finish-resolver.test.ts` (8) + `structural-finish-boq.test.ts` (5).

**MOD:**
- `bim/geometry/wall-host-plan-builder.ts` — import shared `coveredIntervals`, re-export `Pt2` (μηδέν αλλαγή σε importers).
- `bim/services/boq-multi-layer-builder.ts` — `export buildBaseRow` (SSoT reuse).
- `bim/services/BimToBoqBridge.ts` — `BimEntityForBoq.finishContribution` + `upsertWithFinish`.
- `bim/types/column-types.ts` — `finish?: StructuralFinishSpec`.
- `hooks/data/column-boq-feed.ts` — attach `finishContribution` (μέσω `computeColumnFinishContribution`).

## 3.bis Files (Slice 2 — 3D band skin)

**NEW:**
- `bim-3d/converters/structural-finish-3d.ts` — `buildColumnFinishSkin(column, walls, baseY, levelId)`: ανά exposed segment → plan band quad (παρειά μετατοπισμένη ΕΞΩ κατά το πάχος, CCW outward normal) → `stripPrismGeometry` (REUSE — ο καθαρός geometry SSoT του `envelope-three-mesh`) → `THREE.Mesh` με `getMaterial3D(seg.materialId)`. Tags `structuralFinish:true` + κοινό `bimId`/`bimType:'column'`.
- tests: `bim-3d/converters/__tests__/structural-finish-3d.test.ts` (10).

**MOD:**
- `bim/finishes/structural-finish-scene.ts` — εξαγωγή SSoT core `computeColumnFinishFaces(column, coreFootprint, heightMm, walls)` (obstacles + classifier + resolver). Το `computeColumnFinishContribution` (BOQ) ΚΑΙ το 3D το διαβάζουν → ΕΝΑ σημείο face-resolution.
- `bim-3d/converters/bim-three-structural-converters.ts` — `columnToMesh` νέα προαιρετική `walls` param + return `THREE.Mesh | THREE.Group | null`· flat-path: αν υπάρχει σοβάς → composite `Group { πυρήνας + finish }`. Attached-prism path = πυρήνας-only (κεκλιμένες κορυφές = μετέπειτα).
- `bim-3d/scene/bim-scene-attach-syncs.ts` — `syncColumns` περνά `entities.walls`.
- `bim-3d/placement/ColumnPlacementGhost.ts` — type guard `instanceof THREE.Mesh` (ghost δεν περνά walls → πάντα πυρήνας-only Mesh).

**Σημείωση SSoT (απόκλιση από handoff):** χρησιμοποιήθηκε `stripPrismGeometry` αντί `addBandPrism` — το `addBandPrism` δένει `makeEnvelopeMesh` → envelope material/tags (λάθος catalog για σοβά)· το `stripPrismGeometry` είναι ο καθαρός geometry sibling στο ίδιο αρχείο (ίδιο `ROT_X_NEG_90`) → σωστότερο SSoT-reuse.

## 3.ter Files (Slice 3 — 2D finished outline)

**MOD:**
- `bim/finishes/structural-finish-scene.ts` — `computeColumnFinishFaces` στενεύτηκε σε **structural-subset interfaces** `ColumnFinishSource` / `WallFinishObstacle` (depend-on-minimal-interface) ώστε να το τροφοδοτούν ΚΑΙ τα BIM `ColumnEntity`/`WallEntity` (3D) ΚΑΙ τα canvas `DxfColumn`/`DxfWall` (2D, direct entities χωρίς `ifcType`) — μηδέν cast. `buildColumnClassifier` παίρνει `envelopeFunction` αντί ολόκληρης κολόνας.
- `bim/renderers/ColumnRenderer.ts` — `FinishFacesByColumn` type + `setColumnFinishFaces()` injection (mirror `WallRenderer.setOpeningsByWall`) + `drawFinishOutline()`: ανά εκτεθειμένη υπο-ακμή → offset «λωρίδα» (CCW outward normal × πάχος, με end-caps) σε plaster flat colour (`getMaterialFlatColorHex`, SSoT). Καλυμμένες παρειές → καμία γραμμή. ADR-040: pure ctx, zero subscriptions.
- `canvas-v2/dxf-canvas/dxf-renderer-frame-builders.ts` — `buildFinishFacesByColumn(entities)`: per-frame index (μόνο ενεργές κολόνες· walls lazily· reuse `computeColumnFinishFaces`).
- `canvas-v2/dxf-canvas/DxfRenderer.ts` — inject `buildFinishFacesByColumn(scene.entities)` (δίπλα στο `setOpeningsByWall`· ADR-040 orchestrator-drives pattern).
- `rendering/core/EntityRendererComposite.ts` — `setColumnFinishFaces()` pass-through.
- tests: `bim/renderers/__tests__/ColumnRenderer-finish.test.ts` (6).

Πλέον ο **ίδιος** `computeColumnFinishFaces` τροφοδοτεί **BOQ + 3D + 2D** — ΕΝΑ σημείο face-resolution.

## 3.quater Files (Slice 4 — ΔΟΚΑΡΙΑ, full mirror)

**Κεντρική απόφαση (Revit-grade):** για το δοκάρι σοβατίζονται **μόνο οι 2 πλάγιες όψεις** (ακμές ∥ άξονα, ύψος = structural depth). Τα **άκρα** (ακμές ⊥ άξονα) είναι δομική σύνδεση/frame-into → **ποτέ σοβατισμένα** — αποκλείονται **σημασιολογικά** (`includeEdge`), όχι μέσω obstacle (το trimmed άκρο είναι *coincident* με την παρειά κολόνας, όχι *μέσα* → η coverage θα ήταν αναξιόπιστη). Η πάνω όψη (πλάκα) + κάτω όψη (soffit/κορυφές τοίχων) είναι ΟΡΙΖΟΝΤΙΕΣ → εκτός του vertical-band μοντέλου του resolver (bottom-coverage = DEFER).

**MOD:**
- `bim/finishes/structural-finish-resolver.ts` — `+ optional includeEdge?(a,b,i)` (generic, default = όλες οι ακμές → byte-for-byte για κολόνες).
- `bim/finishes/structural-finish-scene.ts` — `buildColumnClassifier` → **`buildStructuralFinishClassifier`** (entity-agnostic, κοινό κολόνα+δοκάρι) + `+ BeamFinishSource` + `computeBeamFinishFaces` (heightMm=depth, obstacles=walls, includeEdge ∥-άξονα) + `computeBeamFinishContribution`.
- `bim/types/beam-types.ts` — `+ finish?: StructuralFinishSpec` (mirror column).
- `bim-3d/converters/structural-finish-3d.ts` — extract pure **`buildFinishSkinFromFaces`** (entity-agnostic core)· `buildColumnFinishSkin` + νέο `buildBeamFinishSkin` το καλούν.
- `bim-3d/converters/bim-three-structural-converters.ts` — `beamToMesh(+walls)` → composite `Group {πυρήνας+σοβάς}` (flat-path· depth ΑΜΕΤΑΒΛΗΤΟ). *(MIXED αρχείο με ADR-448)*
- `bim-3d/scene/BimSceneLayer.ts` — περνά `entities.walls` στο beam call.
- `bim/renderers/structural-finish-outline-2d.ts` *(NEW)* — extract pure SSoT `drawStructuralFinishOutline` (offset «λωρίδα» ανά εκτεθειμένη όψη, plaster colour). `column-renderer-overlays.ts:drawColumnFinishOutline` + `BeamRenderer` το καλούν → μηδέν διπλασιασμός. *(overlays = MIXED· split από άλλο agent)*
- `bim/renderers/BeamRenderer.ts` — `FinishFacesByBeam` + `setBeamFinishFaces()` + draw call.
- `canvas-v2/dxf-canvas/dxf-renderer-frame-builders.ts` — `buildFinishFacesByBeam(entities)`.
- `canvas-v2/dxf-canvas/DxfRenderer.ts` — inject `buildFinishFacesByBeam` (ADR-040 orchestrator-drives).
- `rendering/core/EntityRendererComposite.ts` — `setBeamFinishFaces()` pass-through.
- `hooks/data/beam-boq-feed.ts` *(NEW)* — `beamBoqEntity(beam, scene)` (mirror `column-boq-feed`)· `useBeamPersistence` persist/restore wired (finish-inactive → byte-identical single-entry).
- tests *(NEW)*: `structural-finish-scene-beam` (7) · `structural-finish-3d-beam` (7) · `BeamRenderer-finish` (5) · `beam-boq-feed` (3) — 22/22.

## 3.quinquies Files (Slice 5 — toggle + ενεργοποίηση + per-element override)

**Κεντρικές αποφάσεις (Giorgio):** (1) master toggle = **scalar boolean `showFinishSkin`** στο BIM render settings store (mirror `showHeatLoad`/ADR-422), **default ON**· (2) **visibility-only** — το factory δίνει ΠΑΝΤΑ `finish.enabled:true`, ο διακόπτης ελέγχει μόνο εμφάνιση· (3) **BOQ μετράει πάντα** (Revit schedule = model, όχι view) → ο διακόπτης είναι καθαρά visual, μηδέν αλλαγή σε BOQ feeds/persistence.

**SSoT gate:** `bim/finishes/structural-finish-visibility.ts` *(NEW)* `isStructuralFinishVisible()` (event-time `getState().showFinishSkin ?? true`) — ΕΝΑ σημείο, το διαβάζουν ΚΑΙ ο 2D orchestrator ΚΑΙ ο 3D converter (οι pure builders/leaves μένουν ανέγγιχτοι, ADR-040).

**MOD (core/state):**
- `bim/finishes/structural-finish-types.ts` — `+ STRUCTURAL_FINISH_INTERIOR_MATERIAL/_EXTERIOR/_DEFAULT_THICKNESS_MM` + `createDefaultStructuralFinishSpec()` (enabled:true) + per-element override core `FinishParamField` / `readFinishParamValue` / `applyFinishParam` (entity & UI agnostic).
- `config/bim-render-settings-types.ts` — `+ showFinishSkin?` (BimRenderSettings) + `showFinishSkin` (ResolvedBimSettings) + resolve `?? true`.
- `state/bim-render-settings-store.ts` (+`-store-types.ts`) — `setShowFinishSkin` (idempotent + debounceWrite) + buildRaw/loadForLevel/standalone-write wiring.
- `hooks/drawing/column-completion.ts` + `beam-completion.ts` — factory δίνει `finish: createDefaultStructuralFinishSpec()`.

**MOD (gate — render):**
- `canvas-v2/dxf-canvas/DxfRenderer.ts` — 2D gate: `isStructuralFinishVisible()` false → κενά finish Maps (ADR-040 orchestrator-drives· changelog ενημερώθηκε).
- `bim-3d/converters/bim-three-structural-converters.ts` — 3D gate στα `buildColumn/BeamFinishSkin` call sites. *(MIXED αρχείο με ADR-448)*

**NEW/MOD (UI — master toggle):**
- `ui/ribbon/components/ShowFinishSkinToggle.tsx` *(NEW)* — mirror `ShowHeatLoadToggle` (button + aria-pressed, reactive selector).
- `ui/ribbon/data/view-tab-bim-settings.ts` — `FINISH_SKIN_BUTTON` στο BIM Graphics panel.
- `ui/ribbon/components/RibbonPanel.tsx` — dispatcher case `show-finish-skin-toggle`.

**NEW/MOD (UI — per-element override, κοινό SSoT column+beam):**
- `ui/ribbon/hooks/bridge/finish-param.ts` *(NEW)* — combobox options (enabled/υλικά plaster IDs/πάχος) + generic `resolveFinishComboboxState` / `applyFinishComboboxChange` (πάνω στον pure core).
- `ui/ribbon/hooks/bridge/{column,beam}-command-keys.ts` — `*_FINISH_KEYS` + `*_FINISH_KEY_TO_FIELD` + `is*FinishKey`.
- `ui/ribbon/hooks/{useRibbonColumnBridge,useRibbonBeamBridge}.ts` — delegate read/write (≤6 γρ./bridge → <500).
- `ui/ribbon/data/{contextual-column-tab,contextual-beam-tab}.ts` — panel «Σοβάς» (4 comboboxes).
- `src/i18n/locales/{el,en}/dxf-viewer-shell.json` — `ribbon.commands.finishSkin.*` (toggle) + `ribbon.commands.finishEditor.*` + `ribbon.panels.{column,beam}FinishSkin`.
- tests *(NEW)*: `structural-finish-types` (factory+override core) · `structural-finish-visibility` (gate) · `finish-param` (combobox helpers) — 75/75 ADR-449 σύνολο. Slice 1-4 fixtures: ρητό `finish` override (factory πλέον δίνει default).

## 3.sexies Files (Slice 6 — beam↔column junction, mutual structural obstacles)

**Κεντρική απόφαση (Revit-grade):** στη διεπαφή δοκαριού↔κολόνας η σύνδεση είναι **δομική (frame-into)**, όχι σοβατισμένη όψη → ο σοβάς εξαιρείται **και** στην παρειά της κολόνας κάτω από το δοκάρι **και** στο τμήμα της πλάγιας όψης του δοκαριού στη σύνδεση. **MUTUAL STRUCTURAL OBSTACLES** (μηδέν νέα γεωμετρία): ο pure resolver δέχεται ήδη `obstacles` → ο scene adapter περνά πλέον, εκτός από τοίχους, **και** τα footprints των δοκαριών (όταν resolve-άρει κολόνα) / των κολώνων (όταν resolve-άρει δοκάρι). Ρέει αυτόματα σε 2D + 3D + BOQ (ένας resolver).

**Join tolerance (κρίσιμο):** το `coveredIntervals` κρατά sub-spans με midpoint **αυστηρά μέσα** στο obstacle. Η born-from-grid framing (ADR-441 `trimSegmentEndpointsToColumns`) κόβει το δοκάρι **flush** στην παρειά → εφαπτόμενα χωρίς overlap → coverage = 0. Λύση: μικρή **outward dilation** των cross-obstacles (`STRUCTURAL_JOIN_TOL_MM = 10`, tunable) ώστε το flush seam να γεφυρώνεται· καλύπτει ομοιόμορφα flush (framing) **και** overlap (manual). **Οι τοίχοι ΔΕΝ dilate-άρονται** (υπάρχουσα browser-verified συμπεριφορά).

**Height-aware junction (κρίσιμο fix — Giorgio browser-verify):** η αρχική Slice 6 αφαιρούσε σοβά για **ΟΛΟ το ύψος** της κολόνας (3000mm) εκεί που καρφώνεται δοκάρι, ενώ το δοκάρι πιάνει **μόνο το πάνω μέρος** (~depth, π.χ. 500mm) → οι παρειές έμεναν σχεδόν γυμνές (regression: «καθόλου σοβάς»). Fix: `computeColumnFinishBands()` σπάει την κολόνα σε **κατακόρυφες ζώνες**: κάτω ζώνη `[0, height−beamDepth]` = walls-only (**πλήρης παρειά**)· πάνω ζώνη `[height−beamDepth, height]` = walls + beams (**junction cut**). `beamDepth` = max structural depth ανάμεσα στα δοκάρια που αγγίζουν την κολόνα (bbox overlap). Ρέει σε 3D (ένα prism set ανά ζώνη, στοιβαγμένα) + BOQ (banded area = Σ plan-length × band-height). **Υπόθεση v1:** δοκάρι στην κορυφή κολόνας (beam top ≈ column top — τυπικό frame)· ακριβές elevation-based banding = DEFER. Το **2D plan** δείχνει **πλήρη παρειά** (walls-only) — η junction είναι height-aware (top-band) άρα δεν φαίνεται στην κάτοψη (το δοκάρι σχεδιάζεται ξεχωριστά από πάνω). Η **πλάγια όψη δοκαριού** (cut από κολόνα) μένει ως έχει — height-correct (το δοκάρι έχει ύψος = depth).

**NEW:**
- `bim/geometry/shared/polygon-dilate.ts` *(NEW)* — `dilatePolygonOutward(poly, d)` pure geometry SSoT (winding-free convex miter offset, miter-limit clamp). + `__tests__/polygon-dilate.test.ts`.
- `bim/finishes/__tests__/structural-finish-junction.test.ts` *(NEW)* — column+beam-obstacle / beam+column-obstacle (overlap **και** flush) → μειωμένο exposed length· control (μακρινό στοιχείο) → αμετάβλητο.

**MOD:**
- `bim/finishes/structural-finish-scene.ts` — `+ BeamFinishObstacle (+depth)` / `ColumnFinishObstacle` (minimal structural shapes, BIM + Dxf entities, μηδέν cast) + `crossObstaclePolygon` (dilate)· `computeColumnFinishFaces(..., beams = [])` / `computeBeamFinishFaces(..., columns = [])`· **`+ ColumnFinishBand` / `computeColumnFinishBands()`** (height-aware ζώνες) + `bandedFinishAreasM2`· `computeColumnFinishContribution` χρησιμοποιεί banded area· `computeBeamFinishContribution` βγάζει columns από `scene.entities`.
- `canvas-v2/dxf-canvas/dxf-renderer-frame-builders.ts` — `buildFinishFacesByColumn` = **walls-only** (πλήρης κάτοψη· junction height-aware → όχι στο 2D)· `buildFinishFacesByBeam` μαζεύει lazily ΚΑΙ τις κολόνες (cross-obstacle, height-correct).
- `bim-3d/converters/structural-finish-3d.ts` — `buildColumnFinishSkin` = **ένα prism set ανά ζώνη** (`computeColumnFinishBands`, στοιβαγμένα στο σωστό baseY)· `buildBeamFinishSkin(..., columns, ...)`.
- `bim-3d/converters/bim-three-structural-converters.ts` — `columnToMesh(..., beams = [])` / `beamToMesh(..., columns = [])` (optional defaults → ghost/preview callers ανέγγιχτοι)· **NEW `composeColumnWithFinish()`** (κοινό SSoT flat + attached path· fix #2: το attached path έκανε `return tagged` χωρίς σοβά). *(MIXED αρχείο με ADR-448/441 — μόνο δικές μου γραμμές)*
- `bim-3d/scene/{bim-scene-attach-syncs,BimSceneLayer}.ts` — περνούν `entities.beams` / `entities.columns`. *(MIXED αρχεία με ADR-448/441 — μόνο η κλήση)*

## 3.septies Files (Slice 7 — ΕΝΙΑΙΟΣ ΣΟΒΑΣ ΣΤΙΣ ΣΥΜΒΟΛΕΣ, merged structural silhouette)

> **⚠️ Slice 7-revert (2026-06-13):** η ενιαία silhouette **ΑΝΑΙΡΕΘΗΚΕ ως ενεργό path** και είναι πλέον **dormant** (πίσω από `STRUCTURAL_SILHOUETTE_ENABLED=false` στο `bim-scene-structural-finish-sync.ts`). **Αιτία (Giorgio):** σε **ανοιχτή τοπολογία** (δοκάρια που δεν κλείνουν loop) η `safeUnion` δεν δημιουργεί τρύπα → μόνο το outer ring → κάθε δοκάρι παίρνει **μία μόνο όψη** (boundary-dependent· «άλλα σοβά μέσα, άλλα έξω»). Σε **κλειστό** πλαίσιο το διαγνωστικό έδινε σωστά και τις 2 όψεις → το bug εξαρτάται από την τοπολογία της σκηνής. **Ενεργό path = per-element (Slice 6)**: αξιόπιστες 2 πλάγιες όψεις/δοκάρι + mutual-obstacle junction cut + 45° chamfer στις γωνίες, ανεξάρτητα τοπολογίας. Οι converters δίνουν ξανά `suppressFinishSkin=false`. Ο silhouette κώδικας (pure SSoT + scene adapter + 3D builder + 10 jest) **κρατήθηκε ζωντανός** για μελλοντικό **corner-join-only merged-miter** slice (🔵 DEFER) — flip flag σε `true` όταν υλοποιηθεί robust corner-join (corner-fill only, χωρίς τις πλάγιες όψεις, ώστε να μην διπλο-σοβατίζονται οι per-element όψεις).

**Πρόβλημα (Giorgio screenshots 151127 + 151222):** ο σοβάς υπολογιζόταν **per-element** με **τοπική** μεταχείριση γωνίας (κολόνα miter, δοκάρι chamfer) → (Β) στη συμβολή κολόνα↔δοκάρι ο σοβάς **δεν ενώνεται**· (Α) ο σοβάς δοκαριού **προεξέχει** του υποκείμενου τοίχου (σκαλοπάτι). Τα τοπικά fixes (Slice 6 #3 chamfer) δεν αρκούν — θεμελιώδες όριο του per-element μοντέλου.

**Κεντρική απόφαση (Revit-grade, FULL SSOT):** **ΕΝΙΑΙΑ ΣΙΛΟΥΕΤΑ** — ανά **ζώνη ύψους** ενώνουμε (`safeUnion`) τα δομικά cores (κολόνες + δοκάρια) σε ΕΝΑ outline και τρέχουμε τον **ΥΠΑΡΧΟΝΤΑ resolver** πάνω του (τοίχοι = obstacles, dilated κατά join-tol). Λύνει **και τα δύο εγγενώς, μηδέν νέα geometry**:
- **Β:** ΕΝΑ outline → οι γωνίες κλείνουν με **συνεπή miter** (`computeMiteredOuter`, ίδιο SSoT) → μηδέν ασυνέχεια.
- **Α:** ο τοίχος ως obstacle αφαιρεί το καλυμμένο τμήμα του outline (flush διεπαφή → dilation) → **μηδέν προεξέχων σοβάς** πάνω από τον τοίχο → η όψη του τοίχου συνεχίζεται ομοεπίπεδα.

**Phasing:** **3D-only** (visual fix Α+Β). Το **BOQ μένει per-element** (`computeColumnFinishBands` / `computeBeamFinishFaces`, banded area) — ξεχωριστές γραμμές interior/exterior ανά στοιχείο ΑΚΕΡΑΙΕΣ. Το per-element 3D skin **αντικαθίσταται** από το scene-level silhouette (τα converters το παραλείπουν με `suppressFinishSkin`)· οι per-element builders μένουν για ghosts/previews + tests.

**NEW:**
- `bim/finishes/structural-finish-silhouette.ts` *(NEW, pure)* — `computeStructuralSilhouetteBands(input)`: height-band decomposition (sorted unique z-breakpoints) + ανά band `safeUnion` cores → outer ring(s) → `resolveStructuralFinishFaces` (τοίχοι **un-dilated** obstacles) → merged faces (**additive-outward** σοβάς, big-player σύμβαση). `footprintToPolygon` **CCW-normalise** (winding fix: το CW beam outline αλλιώς = τρύπα στο `safeUnion`). REUSE-only (`safeUnion` + resolver), μηδέν νέα boolean/offset. + `__tests__/structural-finish-silhouette.test.ts` (Β union/μηδέν seam, Α wall-obstacle αφαίρεση, band decomposition, regression μεμονωμένου, disabled/empty).
- `bim-3d/converters/structural-finish-silhouette-3d.ts` *(NEW)* — `buildStructuralSilhouetteSkin(bands, …)`: ανά band → mitered prisms μέσω του ΥΠΑΡΧΟΝΤΟΣ `buildFinishSkinFromFaces` (`bimType:'column'` → καθαρά miters, το outline είναι ήδη ενιαίο). Στοιβάζεται στο σωστό `baseY` (building base + z-bottom).

**MOD:**
- `bim/finishes/structural-finish-scene.ts` — **export** `buildStructuralFinishClassifier` / `wallFootprintPolygon` (reuse)· **NEW `computeStructuralFinishSilhouette(columns, beams, walls, floorElevationMm)`** scene adapter (building-relative z-extents· wall obstacles = finished footprints **un-dilated**· classifier· default spec → delegate στον pure module).
- `bim/finishes/structural-finish-3d.ts` — **export** `buildFinishSkinFromFaces` (κοινό SSoT silhouette + per-element).
- `bim/finishes/structural-finish-resolver.ts` — `ensureCCW`→`orientRing(poly, holeRing)` + `FinishResolveInput.holeRing?` (hole→CW, σοβάς προς το δωμάτιο). Solid default = byte-for-byte (κολόνα/δοκάρι/per-element ανέγγιχτα).
- `bim-3d/scene/BimSceneLayer.ts` — **NEW `syncStructuralFinishSkin()`** (ΕΝΑ scene-level pass μετά beams/columns· group ανά κτίριο = baseElevation datum· walls = obstacles). *(MIXED — μόνο δικές μου γραμμές)*
- `bim-3d/converters/bim-three-structural-converters.ts` — `columnToMesh`/`beamToMesh` `+ suppressFinishSkin` (scene silhouette αναλαμβάνει· ghosts/previews = false). *(MIXED — μόνο δικές μου γραμμές)*
- `bim-3d/scene/bim-scene-attach-syncs.ts` — `syncColumns` περνά `suppressFinishSkin=true`. *(MIXED — μόνο η κλήση)*

**Σχεδιαστικές αποφάσεις (v1):** single πάχος `t` (default 15mm) για το silhouette offset· per-edge wall-plaster-thickness για exact coplanarity σε mismatched πάχη = DEFER· per-element material override στο silhouette skin = DEFER (BOQ ήδη exact)· cache ανά scene-signature = DEFER (union per band per floor-sync, lazy μόνο finish-active).

## 4. Roadmap (slices)

- **Slice 1** ✅ — data model + resolver + BOQ (ΚΟΛΟΝΕΣ).
- **Slice 2** ✅ — 3D render (band skin κολόνας, REUSE `stripPrismGeometry`). Flat-path μόνο· attached/κεκλιμένες κορυφές = μετέπειτα.
- **Slice 3** ✅ — 2D render (finished outline offset ανά εκτεθειμένη παρειά + core = διπλή γραμμή). Per-frame index injection (mirror openings-by-wall).
- **Slice 4** ✅ — Δοκάρια (2 πλάγιες όψεις· άκρα σημασιολογικά εκτός· 3D band skin + 2D outline + BOQ· FULL SSoT reuse). Static depth ΑΜΕΤΑΒΛΗΤΟ.
- **Slice 5** ✅ — View toggle «Σοβατισμένη όψη» (`showFinishSkin` per-view, default ON) + ενεργοποίηση στο factory (νέα στοιχεία γεννιούνται με σοβά) + per-element override (enabled/υλικά/πάχος) στα contextual ribbon tabs. Visibility-only semantics (BOQ μετράει πάντα).
- **Slice 6** ✅ — beam↔column junction: mutual structural obstacles (δοκάρι obstacle στην κολόνα & αντίστροφα) + join-tolerance dilation για flush framing. Ο σοβάς εξαιρείται στη δομική σύνδεση (2D+3D+BOQ).
- **Slice 7** ✅ — **ενιαίος σοβάς στις συμβολές** (merged structural silhouette): `safeUnion` δομικών cores ανά ζώνη ύψους → resolver → coplanar (Α) + connected (Β) εγγενώς. 3D-only· BOQ per-element αμετάβλητο.
- **Slice 9** ✅ — συνεπής γωνιακή ΕΝΩΣΗ σοβά δοκαριού↔κολόνας (chamfer-both + directional dilation → chamfers συμπίπτουν στο miter point· browser-verified «τέλεια ένωση»).
- **Slice 10** ✅ — **junction-aware κλείσιμο**: junction άκρα → **EXTEND** (push-out corner-fill· ο σοβάς κλείνει — γεμίζει κάθετη γωνία + overlap collinear)· ελεύθερα άκρα → 45° chamfer. Λύνει το «ορθογώνιος αλλά δεν κλείνει» στις flush συμβολές «από κάναβο» (ADR-441). Per-endpoint junction flag στον resolver.
- **Slice X1** ✅ — **3Δ ΕΝΙΑΙΑ merged silhouette ΕΝΕΡΓΗ** (συνεχές δέρμα στις συμβολές, μηδέν αλληλοδιείσδυση/διπλή ακμή· port Slice 8/8b height-aware walls· non-pickable skin). per-element 3Δ suppressed· BOQ αμετάβλητο.
- **Slice X2 (μέρος Α)** ✅ — **κεντρικοποίηση γωνιακής γεωμετρίας 2Δ↔3Δ**: NEW pure `structural-finish-outline-geometry.ts` (`computeMiteredOuter` κ.λπ.)· το 2Δ outline καταναλώνει το ίδιο SSoT → γωνίες κολόνας κλείνουν ίδια με 3Δ.
- **Slice X2 (μέρος Β)** ✅ — **ΜΙΑ γεωμετρική πηγή**: το 2Δ τρέφεται από την ΙΔΙΑ `computeStructuralFinishSilhouette` με το 3Δ (minimal-source interfaces· NEW `buildStructuralFinishSilhouette2D` + scene-level `DxfRenderer.drawStructuralFinishSkin2D`)· αφαιρέθηκε το per-element 2Δ. Συμβολές 2Δ=3Δ, μηδέν διπλή γραμμή. CHECK 6D (ADR-040 staged).
- **Slice X3** 🟢 (2026-06-21, UNCOMMITTED) — **Ο ΤΟΙΧΟΣ ως finish-member** της ΙΔΙΑΣ merged silhouette (Giorgio: «κάθε τοίχος να γεμίζει περιμετρικά με σοβά + να σβήνει στις επαφές, όπως Revit, FULL SSOT»). Ο τοίχος με σοβά (DNA `side!=='core'`) γίνεται `SilhouetteMember` (core = `inset(full footprint, skin)`) → ενώνεται με κολόνες/δοκάρια στο `safeUnion` ανά z-band → ο σοβάς τυλίγει το ΕΝΙΑΙΟ κτιριακό περίγραμμα + κάθε δωμάτιο και **σβήνει αυτόματα στις συμβολές** (η εσωτερική ακμή χάνεται στο union). Τοίχος core-only (parapet/fence) → μένει obstacle. **Πηγή πάχους/υλικού = DNA (ADR-447), ΟΧΙ νέο `finish` spec** (μηδέν διπλό SSoT). Όλη η λογική σε NEW `wall-finish-source.ts` + επέκταση του adapter `computeStructuralFinishSilhouette` (εσωτερικά· **μηδέν αλλαγή signature** → 2Δ + 3Δ + DXF export παίρνουν τον wall-σοβά αυτόματα). Slice X3 = ενιαίο πάχος (default 15mm)· **ασύμμετρο exterior/interior από DNA = Slice X4**. 9 NEW jest (+139 finishes/wall green, μηδέν regression). ΕΚΤΟΣ ADR-040 (pure geometry/adapter). 🔴 tsc + browser-verify + commit.

## 5. Known / Deferred
- **Slice X4 (DEFER)** — ασύμμετρο πάχος σοβά ανά παρειά (exterior 25mm / interior 15mm / ETICS) από τα πραγματικά DNA layers· σήμερα ο resolver/silhouette δέχεται ΕΝΑ ενιαίο `spec.thickness` → χρειάζεται per-member thickness ή 2-pass resolve.
- Όχι αναδρομικό: υπάρχουσες persisted κολόνες/δοκάρια χωρίς `finish` δεν αποκτούν σοβά (μόνο νέα στοιχεία μετά το Slice 5)· retroactive backfill = DEFER.
- Stale finish children όταν ο χρήστης απενεργοποιεί τον σοβά (single-entry path δεν τα καθαρίζει — ίδιο με wall multi-layer shrink· deferred re-sync).
- Beam coverage κάτω-όψης (soffit) από κορυφές τοίχων = refinement (οριζόντια όψη, εκτός vertical-band μοντέλου).
- Beam obstacle κολόνας mid-span σε πλάγια όψη (rare) + curved-beam ακριβές cap exclusion (chord-based v1) = μετέπειτα.
- ETICS-grade per-element exterior detection (πέρα από outer-ring proximity) = μετέπειτα slice.

## 6. Changelog
- **2026-06-21** — **Slice X3: Ο ΤΟΙΧΟΣ ως finish-member της ενιαίας σιλουέτας σοβά (FULL SSoT, Revit-superior)** — Giorgio: «όταν σχεδιάζω τοίχο να γεμίζει περιμετρικά με σοβά + όταν κολλάει σε άλλες οντότητες ο σοβάς να εξαφανίζεται στις επαφές· όπως οι μεγάλοι παίκτες (Revit), FULL ENTERPRISE + FULL SSOT». **Έρευνα (code=SoT):** βρέθηκαν **3** συστήματα φινιρίσματος — (a) ADR-449 structural-finish (auto σοβάς + contact-subtraction + merged silhouette· κολόνες/δοκάρια ΜΟΝΟ· ο τοίχος μπαίνει μόνο ως **obstacle**), (b) ADR-447 WallDna (ο τοίχος έχει ήδη exterior/interior plaster layers, αλλά renders μόνο ως διαχωριστικές γραμμές `wall-layer-lines-2d` — δεν τυλίγει, δεν σβήνει στις επαφές), (c) ADR-511 wall-covering (manual per-room IfcCovering, άσχετο). **Απόφαση (μηδέν 4ο σύστημα):** ο τοίχος γίνεται **`SilhouetteMember`** της ΙΔΙΑΣ `computeStructuralSilhouetteBands` — το `safeUnion` ανά z-band ενώνει το core του τοίχου με τα δομικά μέλη → ο σοβάς τυλίγει το ενιαίο περίγραμμα + κάθε δωμάτιο (τρύπες) και **σβήνει αυτόματα στις συμβολές** (η εσωτερική ακμή χάνεται στο union = το «σβήνει στις επαφές»). Ανώτερο από Revit (που δεν ενώνει finish faces στην κάτοψη). **Πηγή σοβά = DNA (ADR-447), ΟΧΙ νέο spec.** **Fix:** NEW pure `wall-finish-source.ts` (`wallHasPlasterSkin` + `wallToSilhouetteMember`: core = `insetPolygonMiter(full footprint, skin)` ώστε ο resolver +skin να φτάσει ακριβώς στην επιφάνεια· REUSE-only). Επέκταση `computeStructuralFinishSilhouette`: split walls → members (με σοβά) / obstacles (parapet/fence core-only)· coverage obstacles = **μόνο** οι bare walls (member ΚΑΙ obstacle θα έκοβε τον δικό του σοβά)· sceneUnits fallback σε τοίχο (όροφος με μόνο τοίχους πλέον παράγει σοβά)· classifier από ΟΛΟΥΣ τους τοίχους. **Μηδέν αλλαγή signature** → 2Δ (`dxf-renderer-frame-builders`) + 3Δ (`bim-scene-structural-finish-sync`) + DXF export (`overlay-dxf-collector`) παίρνουν τον wall-σοβά αυτόματα. Slice X3 = ενιαίο πάχος (default 15mm)· ασύμμετρο DNA exterior/interior = Slice X4 (DEFER). 9 NEW jest (`wall-finish-source.test.ts`: has-plaster· member core· parapet→null· silhouette walls-only· contact-subtraction 2 collinear) + 130 structural-finish + 78 wall/column GREEN (μηδέν regression). **ΕΚΤΟΣ ADR-040** (pure geometry/adapter, μηδέν renderer/micro-leaf touch). **ΜΑΘΗΜΑ: ο τοίχος είχε ήδη σοβά (DNA) + υπήρχε ήδη contact-subtraction (ADR-449)· η λύση ήταν ΕΝΩΣΗ των δύο (τοίχος→member της silhouette), ΟΧΙ νέο σύστημα. «Σβήνει στις επαφές» = το safeUnion του silhouette, δωρεάν.** 🔴 tsc + browser-verify + commit (git add ΜΟΝΟ: `wall-finish-source.ts`[NEW] + `structural-finish-scene-silhouette.ts` + `__tests__/wall-finish-source.test.ts`[NEW] + ADR-449). | Opus
- **2026-06-20** — **Merged-silhouette corner fix: η ΝΔ γωνία σοβά κλείνει ΛΟΞΑ όταν υπάρχουν ≥2 κολώνες (browser-verified screenshot, Giorgio)** — «μία κολώνα → ο σοβάς κλείνει σωστά (ορθή γωνία)· πολλές κολώνες → η κάτω-αριστερά (ΝΔ) γωνία κλείνει λοξά (45° chamfer)». **Διάγνωση (empirical repro, ΟΧΙ εικασία):** ο 2Δ σοβάς = scene-level merged silhouette (Slice X2). Το `computeStructuralSilhouetteBands`→`resolveBandFaces` ενώνει με `safeUnion` και — για **disjoint** κολώνες — επιστρέφει MultiPolygon με **πολλαπλά rings**, των οποίων τα segments **συγχωνεύονται σε ΕΝΑ flat array** (`merged.push(...faces.segments)`). Το `computeMiteredOuter` έκανε miter με **positional `(k+1)%n` wrap** → υπέθετε ΕΝΑ κυκλικό ring → στο όριο μεταξύ rings έκανε **mis-pair** (τελευταίο edge κολώνας Α ↔ πρώτο edge κολώνας Β, μη-κοινή κορυφή → απορρίπτεται) → η **γωνία-κλείσιμο** κάθε ring έμενε ανοιχτή → έπεφτε στον free κλάδο του `closeOpenOuterEnds` → **chamfer 45°**. Επειδή η `polygon-clipping` εκπέμπει κάθε ring ξεκινώντας από τη **λεξικογραφικά μικρότερη (=ΝΔ) κορυφή**, το seam έπεφτε ακριβώς στη ΝΔ γωνία (1 κολώνα → `unionFootprints` shortcut `length===1` → raw clean ring → όλες οι γωνίες mitered → σωστό). **Fix (`structural-finish-outline-geometry.ts`, FULL SSoT):** το miter-attempt εξήχθη σε helper `tryMiterPair(k,m,...)` (ΕΝΑ SSoT· καλείται από δύο passes). **Pass 1** = positional `(k+1)%n` (single ring: byte-for-byte όπως πριν). **Pass 2 (NEW)** = geometry-based: κάθε ανοιχτό `b` άκρο ταιριάζει με το **ομόκορφο** ανοιχτό `a` άκρο οποιουδήποτε segment (manifold ring → 1 in/1 out ανά κορυφή → μη-διφορούμενο) → κλείνει τη seam γωνία κάθε ring με σωστό miter. Single ring → Pass 2 no-op (μηδέν regression). Beams (2 πλάγιες όψεις, μη-κλειστό ring) + free cantilever ends → παραμένουν ανοιχτά → chamfer/square ως πριν. **NEW regression** `structural-finish-silhouette-corner.test.ts` (1 κολώνα: μηδέν chamfer signature· 4 disjoint κολώνες: ΝΔ γωνία mitered στα (-215,-215), όχι chamfer ±185). 89/89 finishes jest. **ΕΚΤΟΣ ADR-040** (pure geometry). **ΜΑΘΗΜΑ: `computeMiteredOuter` σε merged-silhouette δέχεται ΠΟΛΛΑΠΛΑ concatenated rings — το positional `(k+1)%n` wrap είναι λάθος στα ring boundaries· miter ΠΡΕΠΕΙ να ταιριάζει με κοινή κορυφή (geometry), όχι με θέση στο array.** ✅ **browser-verified 2026-06-20 (Giorgio «ΔΙΟΡΘΩΘΗΚΕ»)** — 🔴 ΑΠΟΜΕΝΕΙ ΜΟΝΟ commit (git add ΜΟΝΟ: `structural-finish-outline-geometry.ts` + `__tests__/structural-finish-silhouette-corner.test.ts` + ADR-449). | Opus
- **2026-06-17** — **Bug A complete (ADR-404 tilt-follow): κεκλιμένη κολώνα/δοκός → σοβάς + ΑΚΜΕΣ + οπλισμός γέρνουν 1:1 με τον πυρήνα — BROWSER-VERIFIED (Giorgio «τώρα λειτουργούν»).** Ο πυρήνας έπαιρνε ήδη `applyColumnTilt`/`applyBeamSlope`, αλλά ο σοβάς ερχόταν από τον flat merged silhouette (έμενε κάθετος). **Fix 3 σημείων (reuse SSoT, μηδέν νέα μαθηματικά, no-op flat fast-path):** (1) **split tilted-out** — τα κεκλιμένα μέλη ΕΞΑΙΡΟΥΝΤΑΙ από το union (`bim-scene-structural-finish-sync` + `bim-scene-attach-syncs`/`BimSceneLayer` περνούν `suppressFinishSkin=!isTilted`) → παίρνουν per-element σοβά· επίπεδα μένουν στο silhouette (μηδέν regression). (2) **finish faces+edges** — `structural-finish-3d.buildColumnFinishSkin`/`buildBeamFinishSkin` εφαρμόζουν `applyColumnTilt`/`applyBeamSlope` στο prism geometry **ΠΡΙΝ** χτιστεί το edge overlay (shear-callback στο `addFinishPrism`)· **ΑΙΤΙΑ των «κάθετων ακμών»:** το edge overlay είναι `LineSegments2` (ξεχωριστή interleaved γεωμετρία) — δεν shear-άρεται post-hoc, οπότε ο shear πρέπει να γίνει πριν το `EdgesGeometry`. (3) **rebar cage** — NEW **SSoT** `applyColumnTiltToPoints(points, params, baseY)` στο `mesh-slope-shear.ts` (το ΙΔΙΟ module/μαθηματικά `columnTiltShearAt` + ROT_X_NEG_90 σύμβαση με το geometry-shear· **dedup-by-reference**) shear-άρει τα segment endpoints του κλωβού (διαμήκεις/στεφάνια/cross-ties) πριν το `buildRods` (ο κλωβός=InstancedMesh, δεν shear-άρεται per-vertex). **Διόρθωσε ΚΑΙ latent bug**: στα `spiralSegments` τα διαδοχικά segments μοιράζονται endpoint Vector3 (`seg[i].b===seg[i+1].a`) → naive per-seg loop θα διπλασίαζε το shear· το dedup το λύνει. **ΜΑΘΗΜΑΤΑ: edge overlay (LineSegments2) = ξεχωριστή geometry → shear ΠΡΙΝ το build· InstancedMesh (rebar) → shear segment endpoints μέσω SSoT point-shear (ΟΧΙ ξανα-υλοποίηση της σύμβασης)· κοινά endpoints → dedup.** Core part committed `0a6c837e` (bundled από άλλον agent)· edges+rebar+point-SSoT UNCOMMITTED (`structural-finish-3d.ts`, `column-rebar-3d.ts`, `mesh-slope-shear.ts`). Tests: `structural-finish-3d-tilt` 3/3 + `column-tilt-points` 4/4 (incl. dedup), diagnostics clean. 🔴 commit (git add ΜΟΝΟ τα 3 αρχεία + 2 tests· shared tree). DEFER: tilted horizontal caps (top/base) per-element· 2Δ cut-plane tilted footprint. | Opus
- **2026-06-15** — **Slice 12 (2Δ completion): ο 2Δ σοβάς silhouette παίρνει storey-aware `columnExtents` (ίδιο fix με το 3Δ Slice 12).** Το committed Slice 12 πρόσθεσε `columnExtents` ΜΟΝΟ στο 3Δ· το 2Δ (`dxf-renderer-frame-builders.buildStructuralFinishSilhouette2D`) καλούσε `computeStructuralFinishSilhouette(...,0)` **χωρίς** το 5ο όρισμα + hardcoded `floorElevationMm=0` → μια `storey-ceiling` κολόνα (height 4000 > ceiling 3000· Firestore `col_fb3215e9…`) έπαιρνε **raw 4000** (legacy `columnZExtent` fallback) → λάθος band [3000,4000] κολόνα-μόνο → **αποκλίνον 2Δ outline vs 3Δ**. **Fix (FULL SSoT, N.0.2):** NEW `buildColumnVerticalExtentLookup(columns, ctx)` στο `column-vertical-profile.ts` — ο per-column loop που ζούσε inline στο 3Δ `bim-scene-structural-finish-sync.buildColumnVerticalExtents` (rewired → byte-for-byte). Το 2Δ διαβάζει το active-storey context (`useActiveStoreyStore`, zero-React store, ασφαλές από το ADR-040 DxfRenderer) → ίδιο `ColumnVerticalExtentLookup` + πραγματικό `floorElevationMm` (datum-relative, ίδιο frame με `beam.params.topElevation`). Backward-compatible (χωρίς storey context → fallback `0`/`undefined` → legacy `params.height`· τα 3 υπάρχοντα 2Δ jest GREEN). Attached per-corner soffit clip στο 2Δ = DEFER (`resolveHostInput` undefined· `buildWallHostInputs` θέλει `BeamEntity[]`, όχι DxfBeam → zero-cast· ο storey-ceiling/nominal σοβάς είναι το reported issue). CHECK 6D (ADR-040 staged). 4 νέα jest (`column-vertical-profile.test` — storey-ceiling 4000→3000 / legacy / FFL≠0 / degenerate-skip). **ΜΑΘΗΜΑ: ο 2Δ silhouette είναι ΑΔΕΛΦΟΣ του 3Δ — όποιο storey-aware fix μπει στο 3Δ scene pass ΠΡΕΠΕΙ να μπει και στο `buildStructuralFinishSilhouette2D` (κοινό SSoT lookup).** **+ Plan-hidden junction faces (2026-06-15, browser-feedback Giorgio):** «σε μία γωνία κολόνας λοξή κοπή σοβά αντί miter + 2 μικρές λοξές γραμμούλες στη συμβολή σοβά δοκαριού↔κολόνας· 3Δ σωστό». **Αιτία:** το `computeStructuralSilhouetteBands` παράγει πολλαπλές z-bands (κολόνα z[0,3000] + δοκάρι z[2500,3000] → 2 bands)· το 3Δ τις στοιβάζει σωστά (ορατές κάτω από το δοκάρι), αλλά το 2Δ `drawStructuralFinishSkin2D` τις ζωγραφίζει **επικαλυπτόμενες σε plan** → η όψη κολόνας προς το δοκάρι (εκτεθειμένη στο band[0,2500], κρυμμένη σε κάτοψη πίσω από το δοκάρι) σχεδιάζει spurious strip. **Fix (2Δ-only):** NEW `dropPlanHiddenJunctionFaces` + flag `dropPlanHiddenFaces` στο `computeStructuralFinishSilhouette` — (1) αφαιρεί όψη της οποίας η outward πλευρά (`segOffsetVec`) πέφτει μέσα σε ΑΛΛΟ member footprint (κρυμμένη σε plan top-down)· (2) **μαρκάρει τα γειτονικά εναπομείναντα άκρα ως `aJunction`/`bJunction`** → ο `computeMiteredOuter` κάνει **ορθογώνια EXTEND** (Slice 10 corner-fill, κάθετο end-cap) αντί για **chamfer 45°** (= οι «2 μικρές λοξές γραμμούλες» που έμεναν στις γωνίες εκατέρωθεν του δοκαριού). **3Δ ΔΕΝ περνά το flag** (οι όψεις παραμένουν στις z-bands τους — ορατές). Walls αμετάβλητα (per-band coverage, μηδέν Slice-8b regression). 4 νέα jest (`structural-finish-plan-junction.test`). **DEFER: η ΒΔ γωνία (μερικώς-κρυμμένη ανατολική όψη — partial occlusion) — εκκρεμεί χειρουργική λύση που ΔΕΝ θα αγγίξει τις άλλες γωνίες.** **ΜΑΘΗΜΑ: plan-projection ≠ 3Δ z-bands — μια όψη ορατή στο 3Δ (κάτω από γείτονα) είναι κρυμμένη σε κάτοψη.** 🔴 browser-verify (storey-ceiling κολόνα height>ceiling → 2Δ σοβάς == 3Δ· συμβολή κολόνας↔δοκαριού καθαρό miter, μηδέν λοξές γραμμούλες) + commit. | Opus
- **2026-06-15** — **Slice 12: height-SSoT — ο σοβάς παίρνει το κατακόρυφο εύρος [zBot,zTop] από την ΙΔΙΑ SSoT με τον rendered πυρήνα (storey-aware), ΟΧΙ από raw `params.height`** — Giorgio: «άλλαξα ύψος κολόνας → 2700 από το ribbon, κατέβηκε ΜΟΝΟ ο σοβάς· ο πυρήνας έμεινε ψηλά». **Διάγνωση = Firestore-first** (`col_fb3215e9…` re-fetch: `topBinding:'storey-ceiling'`, `topOffset:0`, `height:2700`· floor «Ισόγειο» `height:3` → storey ceiling **3000mm**): ο πυρήνας (`bim-scene-attach-syncs.syncColumns`) renders στο `resolveColumnNominalTopZmm` = `nextFloorElevationMm`(3000)· ο σοβάς (silhouette + horizontal caps/soffit) διάβαζε `baseOffset+params.height` = **2700** → ασυμφωνία. **Το δοκάρι ήταν ήδη σωστό** (διαβάζει persisted `topElevation`=3000, ΙΔΙΑ SSoT με τον πυρήνα — confirmed Firestore `beam_d9d8da55…`). **Revit-grade απόφαση:** το ύψος storey/level-bound κολόνας είναι **ΠΑΡΑΓΩΓΟ** (ορίζεται από Κτίρια→Όροφοι· ο πυρήνας ήδη αγνοεί το `params.height` — ADR-448). Διορθώνω **ΜΟΝΟ την πηγή του zExtent** του σοβά → σοβάς = πυρήνας πάντα. Μετά το fix το ribbon «Ύψος»=2700 γίνεται **inert** και στα δύο (ακριβώς Revit: για χαμηλότερη κολόνα → Top Offset αρνητικό ή Top Constraint=Unconnected). **Αρχιτεκτονική (FULL SSoT):** NEW `resolveColumnVerticalExtentMm(params, footprint, ctx)` στο `column-vertical-profile.ts` (`zBot=resolveColumnBaseZmm`· `zTop=attached?resolveColumnTopProfile(...).maxTopZmm:resolveColumnNominalTopZmm`· finite-guard fallback `zBot+params.height` = mirror του `nominalHeightMm` guard του πυρήνα). Το scene-level `syncStructuralFinishSkin` χτίζει **ΜΙΑ φορά** `Map<columnId,{zBotMm,zTopMm}>` (`buildColumnVerticalExtents`) με **ΑΚΡΙΒΩΣ το ίδιο context** που χρησιμοποιεί ο `syncColumns` (`{floorElevationMm, nextFloorElevationMm}` + `resolveHostInput` μόνο όταν υπάρχουν attached) και το περνά στα `computeStructuralFinishSilhouette` (optional 5ο arg) + `computeStructuralHorizontalFinishFaces` (`input.columnExtents`). Τα adapters: optional `id?` + `columnExtents?: ReadonlyMap` → lookup ανά id, αλλιώς **legacy fallback** `params.height`. **2Δ plan path** (`dxf-renderer-frame-builders`) + tests δεν περνούν map → legacy (μηδέν regression· plan view = top-down, δεν δείχνει vertical mismatch). **Beam path ΑΜΕΤΑΒΛΗΤΟ** (ήδη σωστό). 117/117 structural-finish jest (+5 `resolveColumnVerticalExtentMm`: storey-ceiling 2700→3000 / slab ceiling / legacy fallback / attached maxTopZmm / absolute· +1 horizontal-scene: extents override → cap 3000 ΟΧΙ 2700). **ΕΚΤΟΣ ADR-040** (3Δ scene + pure geometry). **ΜΑΘΗΜΑ: ο σοβάς είναι παράγωγο — το κατακόρυφο εύρος ΠΡΕΠΕΙ να βγαίνει από τον ΙΔΙΟ resolver με τον πυρήνα (storey-aware), ΠΟΤΕ από raw `params.height`· τα unit tests χρησιμοποιούσαν `height=3000`(=storey) → δεν φάνηκε το bug — Firestore-first το ξεκλείδωσε.** **DEFER (UX follow-up, product-choice Giorgio):** το ribbon «Ύψος» να γίνει read-only/derived όταν `topBinding≠'unconnected'` (Revit pattern) ώστε να μη μπερδεύει· ή edit-height → auto-switch σε Unconnected. 🔴 browser-verify (άλλαξε storey height Κτίρια→Όροφοι → πυρήνας ΚΑΙ σοβάς τεντώνονται μαζί· ribbon height=2700 → τίποτα δεν κουνιέται) + commit (git add ΜΟΝΟ δικά μου). | Opus
- **2026-06-15** — **Slice 11: ΟΡΙΖΟΝΤΙΕΣ εκτεθειμένες όψεις σοβά (καπάκι κολόνας top + βάση pilotis· δοκάρι top + soffit), adjacency-driven** — Giorgio: ο σοβάς μπαίνει ΜΟΝΟ περιμετρικά (κατακόρυφες όψεις)· να καλύπτει ΚΑΙ τις εκτεθειμένες οριζόντιες (Revit «exposed faces get finish», επέκταση στον Z). **Κανόνας:** κολόνα top cap εκτεθειμένο αν καμία πλάκα/δοκάρι από πάνω (μπει → re-derive → σβήνει)· base μόνο σε `absolute` (pilotis) αν καμία πλάκα/πέδιλο κάτω· δοκάρι top αν καμία πλάκα πάνω, soffit αν κανένας τοίχος κάτω. Coverage = **ΓΕΩΜΕΤΡΙΚΗ** (vertical span φτάνει το επίπεδο + plan overlap), associative (μηδέν stored flag), partial-aware. **Αρχιτεκτονική (FULL SSoT):** NEW pure `bim/finishes/structural-finish-horizontal.ts` (`computeHorizontalFinishFace` = core−⋃covers via `safeDifference`· `computeFinishedOutline` = REUSE resolver+`computeMiteredOuter` → offset μόνο εκτεθειμένες ακμές, ίδια γεωμετρία με τον κάθετο σοβά)· NEW scene adapter `structural-finish-scene-horizontal.ts` (z-extents κολόνας/δοκαριού/πλάκας/τοίχου· attached-top wall resolution port Slice 8b)· NEW 3Δ `bim-3d/converters/structural-finish-horizontal-3d.ts` (`buildShape`+`extrudeAndRotate` ΑΚΡΙΒΩΣ όπως `slabToMesh`· λεπτή πλάκα up/down κατά thickness· **μη-pickable** derived διακόσμηση)· wired στο scene-level `syncStructuralFinishSkin` (entities.slabs/walls). **Junction fix (Giorgio iterative browser):** v1 perp-dilate → soffit δεν έφτανε το ελεύθερο άκρο + φτερά διεισδύουν στην κολόνα· v2 finished-outline + boolean cross-subtract γειτόνων → **διαγώνια sliver** (artifact `safeDifference` σε flush/coincident πολύγωνα)· **v3 (ΣΩΣΤΟ, browser-verified «επιτέλους»):** δομικοί γείτονες = **plaster-envelope obstacles στον resolver** (interval-based κοπή ακμής, ΜΗΔΕΝ boolean sliver)· boolean μένει ΜΟΝΟ για γνήσια slab/wall κάλυψη (πραγματικό overlap). Soffit/cap = axis-aligned L (under-beam→πρόσωπο κορμού γείτονα· wings→πρόσωπο σοβά γείτονα). 83/83 jest (faces 9· scene-adapter 10 incl. real-Firestore-geometry no-penetration· 3Δ builder 5). **ΕΚΤΟΣ ADR-040** (3Δ scene + pure geometry). **ΜΑΘΗΜΑΤΑ: (1)** οριζόντιο finish = λεπτή πλάκα (mirror slab), ΟΧΙ vertical band· **(2)** `safeDifference` σε flush/coincident structural junctions → διαγώνιες slivers· χρησιμοποίησε **interval-based resolver με dilated-neighbor obstacles** αντί boolean· **(3)** το 3Δ scene χτίζεται imperative & cached → αλλαγή scene-module ΘΕΛΕΙ dev-server restart (hard-refresh δεν αρκεί). **DEFER: Slice E (BOQ οριζόντιες m²)· 2Δ ένδειξη.** ⚠️ base impl committed 415dd6f4+63bb30ef από ΑΛΛΟΝ agent (git add -A, ΟΧΙ N.(-1))· sliver-fix pending commit. | Opus |
- **2026-06-14** — **Δρόμος Β #A: wall-junction over-reach fix (ο σοβάς κολόνας σταματά τετράγωνα στον τοίχο, ΔΕΝ εκτείνεται μέσα του)** — Giorgio (Firestore baseline `col_fb3215e9…` + `wall_772f1b79…`, 0 beams· repro confirmed code=SoT + Firestore): «ο ανατολικός σοβάς της κολόνας κατεβαίνει νότια & μπαίνει στο σώμα του τοίχου». **Διάγνωση:** ο resolver σημάδευε **κάθε** άκρο που ακουμπά obstacle ως **junction** (`pointNearObstacle` πάνω σε ΟΛΑ τα obstacles) → η Slice 10 corner-fill (ορθογώνια EXTEND core+outer), σχεδιασμένη για κολόνα↔**δοκάρι** flush framing, υπερ-εκτεινόταν μέσα στον **τοίχο** (στο ενεργό silhouette path τα μόνα obstacles είναι τοίχοι → ΚΑΘΕ junction ήταν wall over-reach). **Fix (διάκριση είδους γείτονα):** NEW `junctionObstacles` input στον resolver (**υποσύνολο** των `obstacles`: ΜΟΝΟ δομικοί γείτονες κολόνα/δοκάρι). Άκρο κοντά σε `junctionObstacle` → `aJunction`/`bJunction` (EXTEND, Slice 10 διατηρείται)· άκρο κοντά σε `obstacle` αλλά ΟΧΙ junction (= **τοίχος**) → NEW `aSquareEnd`/`bSquareEnd` → `closeOpenOuterEnds` **καθαρό τετράγωνο σταμάτημα** (ούτε chamfer ούτε extend· ο τοίχος έχει δικό του σοβά layered-DNA ADR-447). Silhouette path δεν περνά junctionObstacles (default `[]` → όλα τα wall-cut άκρα = square → #A λυμένο)· per-element callers (`computeColumnFinishFaces`/`computeBeamFinishFaces`, ghosts) περνούν τα δομικά cross-obstacles ως junctionObstacles → Slice 10 αμετάβλητο. 4 αρχεία (types/resolver/outline-geometry/scene) + tests (resolver junction/square + 3D square-end). **ΕΚΤΟΣ ADR-040** (pure geometry, μηδέν canvas-drawing file). 102/102 ADR-449 jest. **ΜΑΘΗΜΑ: corner-fill EXTEND ανήκει ΜΟΝΟ σε δομική flush συμβολή (κολόνα↔δοκάρι)· τοίχος = square butt (δικός του σοβάς)· διάκρινε obstacle KIND, μη σημαδεύεις κάθε γείτονα junction.** 🔴 browser-verify (2Δ+3Δ: σοβάς κολόνας σταματά καθαρά στην παρειά τοίχου, μηδέν προεξοχή μέσα στον τοίχο) + commit. | Opus |
- **2026-06-14** — **Δρόμος Β #C: wall-end → column EMBED → PULL-BACK (το #2 δεν διαρρέει πια στο ADR-452 cut-plane fast-path)** — Giorgio (browser): κατά τη μετακίνηση του slider οριζόντιας τομής → «σοβάς δυτικής πλευράς τοίχου εισχωρεί στο σώμα κολόνας»· settled → σωστό. **Διάγνωση:** το #2 (z-fight fix) έσπρωχνε 20mm άκρης τοίχου **ΜΕΣΑ** στο μπετόν κολόνας (embed)· στο cut-motion fast-path τα cut-caps της κολόνας είναι προσωρινά OFF (ανοιχτή τομή, `section-clip-applicator` clip χωρίς cap) → το εμβυθισμένο κομμάτι τοίχου (με σοβά) φαινόταν μέσα στην κολόνα. **Fix (root-cause = pull-back):** αντικατάσταση του embed με **pull-back** — η άκρη τοίχου **υποχωρεί** 2mm **μακριά** από την κολόνα κατά τον άξονα → (α) σπάει το coincident end-cap → τέλος z-fight (το αρχικό #2)· (β) το end-cap στρέφεται προς την κολόνα → **occluded** από το μπετόν → δεν φαίνεται η στενή όψη· (γ) **ΜΗΔΕΝ geometry τοίχου μέσα στην κολόνα** → μηδέν διαρροή στο fast-path. Render-only (3Δ-only· 2Δ/BOQ/finish-obstacle = αρχικό wall). Αρχείο `wall-column-embed-3d.ts` → **renamed** `wall-column-pullback-3d.ts` (`pullBackStraightWallEndsFromColumns`/`WALL_COLUMN_PULLBACK_MM=2`/`WallEndPullBack`) + ενημερωμένος ο μόνος caller `BimToThreeConverter.wallToMesh` + test. **ΕΚΤΟΣ ADR-040** (3D converter). 5/5 jest. **ΜΑΘΗΜΑ: embed (overlap μέσα στον γείτονα) διαρρέει σε cap-off fast-paths· pull-back (separation, occluded end-cap) λύνει z-fight ΧΩΡΙΣ geometry μέσα στο σώμα.** 🔴 browser-verify (slider τομής: μηδέν σοβάς τοίχου μέσα στην κολόνα κατά την κίνηση· στατικά flush· έλεγξε για ορατό sliver-gap — αν φανεί, μείωσε/μηδένισε το pull-back ή συντόνισε με fast-path culling) + commit. | Opus |
- **2026-06-14** — **Free per-corner ΣΤΑΤΙΚΗ reshape κολόνας → ο σοβάς ακολουθεί αυτόματα (cross-ref ADR-363 PHASE 1)** — Giorgio: λαβή σε κάθε γωνία της κολόνας ώστε να αναμορφώνει ελεύθερα τη **στατική διατομή** (πυρήνα)· ο σοβάς ακολουθεί. Προστέθηκαν per-corner grips (ADR-363) που, στο σύρσιμο, μετατρέπουν τη διατομή σε **`composite`** (custom profile). Ο σοβάς (merged silhouette, ADR-449) **re-derives αυτόματα** από το νέο `geometry.footprint.vertices` και στα δύο media — 2Δ per-frame (`buildStructuralFinishSilhouette2D`) + 3Δ (`syncStructuralFinishSkin`) — **μηδέν αλλαγή στο finish module** (additive-outward, immutable-core σύμβαση τηρείται: ο σοβάς απλώς ακολουθεί τον νέο πυρήνα). Πλήρης υλοποίηση + στατικά guards: ADR-363 changelog 2026-06-14. **DEFER:** σοβάς-ghost **live** κατά το grip-drag (νυν snap-on-commit — η silhouette χτίζεται scene-level από committed entities, οπότε ο πυρήνας-ghost δείχνει live τη νέα μορφή αλλά ο σοβάς ενημερώνεται στο commit). | Opus |
- **2026-06-14** — **Slice 5 FIX: τα finish keys δεν δρομολογούνταν στον ribbon composer (το «Σοβάς Ναι/Όχι» δεν δούλευε ποτέ)** — Giorgio (μεμονωμένη κολόνα με σοβά): το contextual combobox «Σοβάς» έδειχνε **«-»** (δεν διάβαζε την υπάρχουσα τιμή) ΚΑΙ το Ναι/Όχι **δεν άλλαζε** κατάσταση. **Διάγνωση (code=SoT):** το Slice 5 πρόσθεσε τα finish keys (`column.params.finish.*`/`beam.params.finish.*`) στα **bridges** (`useRibbonColumnBridge`/`useRibbonBeamBridge` τα χειρίζονταν σωστά μέσω `isColumnFinishKey`) **αλλά ξέχασε να τα δηλώσει στον composer** `useRibbonCommands.ts`: το routing και στο `onComboboxChange` (γρ. 160/164) και στο `getComboboxState` (γρ. 253/254) έλεγχε μόνο `isColumnRibbonKey || isColumnRibbonStringKey` (number+string keys, ΟΧΙ finish) → τα finish keys έπεφταν σε ΟΛΑ τα conditions → κατέληγαν στο fallback `textEditorBridge` → `getComboboxState` επέστρεφε `null` (= «-») και `onComboboxChange` ήταν no-op (ο textEditor δεν τα ξέρει). **Fix (1 αρχείο, μηχανικό):** προστέθηκε `|| isColumnFinishKey(key)` / `|| isBeamFinishKey(key)` και στα 2 routing σημεία + imports. Τώρα τα finish keys φτάνουν στον σωστό bridge → το combobox διαβάζει την τιμή («Ναι») + το Ναι/Όχι θέτει `finish.enabled` → `isFinishActive` → η silhouette (2Δ+3Δ) ρίχνει/προσθέτει το member. **ΜΑΘΗΜΑ: bridge handler χωρίς composer routing = dead path· κάθε νέα ομάδα keys χρειάζεται ΚΑΙ guard στο bridge ΚΑΙ routing entry στον composer (untested gap από Slice 5).** DEFER: regression test για το composer routing των finish keys. Pending browser-verify (hard-refresh → επιλογή κολόνας → «Σοβάς» δείχνει «Ναι»· Όχι → ο σοβάς φεύγει 2Δ+3Δ) + commit.
- **2026-06-14** — **Slice X2 (μέρος Β): ΜΙΑ γεωμετρική πηγή — το 2Δ τρέφεται από την ΙΔΙΑ merged silhouette με το 3Δ (FULL SSoT, Revit-grade)** — Giorgio: «θέλω να το κάνεις όπως οι μεγάλοι παίκτες (Revit) — FULL ENTERPRISE + FULL SSOT». Μετά το μέρος Α το 2Δ + 3Δ μοιράζονταν το corner-**math** αλλά **δύο διαφορετικές πηγές faces** (2Δ=per-element· 3Δ=merged silhouette) → απόκλιση στις συμβολές. **Fix:** το 2Δ καταναλώνει πλέον την **ΙΔΙΑ** `computeStructuralFinishSilhouette` SSoT με το 3Δ scene pass. (1) **Minimal-interface widening** (μηδέν cast): `computeStructuralFinishSilhouette` δέχεται `SilhouetteColumnSource`/`SilhouetteBeamSource` (Pick interfaces) → **ΚΑΙ** `ColumnEntity`/`BeamEntity` (3Δ) **ΚΑΙ** `DxfColumn`/`DxfBeam` (2Δ) τα ικανοποιούν (ίδιο pattern με `ColumnFinishSource`). (2) **NEW per-frame builder** `buildStructuralFinishSilhouette2D(entities)` (frame-builders) → `{bands, sceneUnits}` ή `null` (μηδέν κόστος όταν ανενεργό). (3) **NEW scene-level pass** `DxfRenderer.drawStructuralFinishSkin2D` (mirror του 3Δ `syncStructuralFinishSkin`): μετά τα entities, ζωγραφίζει το merged outline ανά band μέσω του κοινού `drawStructuralFinishOutline` (→ ίδιες γωνίες/συμβολές, junctions εσωτερικά της ένωσης → δεν σχεδιάζονται → **μηδέν διπλή γραμμή**). ADR-040: pure draw, zero subscriptions, μέσα στο cached normal-state bitmap. (4) **Boy-scout removal** (per-element 2Δ): αφαιρέθηκαν `buildFinishFacesByColumn`/`buildFinishFacesByBeam`, `EntityRendererComposite.set{Column,Beam}FinishFaces`, `ColumnRenderer.setColumnFinishFaces`+`drawColumnFinishOutline`, `BeamRenderer.setBeamFinishFaces` + οι `FinishFacesBy{Column,Beam}` types. **BOQ + 3Δ silhouette + ghosts/previews ΑΜΕΤΑΒΛΗΤΑ.** Tests: αφαιρέθηκαν τα 2 renderer-finish (per-element) → NEW `structural-finish-silhouette-2d.test.ts` (μεμονωμένη κολόνα → 4 όψεις· no-finish → null). 84/84 ADR-449 jest. **CHECK 6D: staged ADR-040** (DxfRenderer/composite touch). **ΜΑΘΗΜΑ: FULL SSoT = ΜΙΑ γεωμετρική πηγή (silhouette) τροφοδοτεί ΚΑΙ τα δύο render media (2Δ stroke / 3Δ extrude)· το να μοιράζονται μόνο helper δεν αρκεί — η πηγή των faces πρέπει να είναι μία.** Pending tsc (PowerShell denied → Giorgio `! npx tsc --noEmit`) + browser-verify (2Δ μεμονωμένη κολόνα ίδια με 3Δ· αργότερα κολόνα+δοκάρι = μηδέν διπλή γραμμή στη συμβολή) + commit (git add ΜΟΝΟ δικά μου).
- **2026-06-14** — **Slice X2 (μέρος Α): κεντρικοποίηση γωνιακής γεωμετρίας 2Δ↔3Δ (γωνίες κολόνας κλείνουν στο 2Δ)** — Giorgio (μεμονωμένη κολόνα 400×400, Firestore `col_fb3215e9…`, level `lvl_aec46939…`): «στο **3Δ** η κολόνα ντύνεται σωστά, στο **2Δ** οι γωνίες δεν κλείνουν — ασυνέπεια· είναι ενοποιημένος/κεντρικοποιημένος ο κώδικας;». **Διάγνωση (code = SoT):** 2Δ + 3Δ μοιράζονταν τον **resolver** (exposed faces SSoT) αλλά **ΟΧΙ** τη γεωμετρία γωνιών — διπλότυπο/αποκλίνον: το 3Δ έκλεινε τις γωνίες με `computeMiteredOuter` (miter + chamfer/extend), ενώ το 2Δ `drawStructuralFinishOutline` ζωγράφιζε **κάθε όψη ΑΝΕΞΑΡΤΗΤΑ** ως offset παραλληλόγραμμο **ΧΩΡΙΣ καμία miter** → τα outer endpoints δύο γειτονικών όψεων (κάθετα σε διαφορετικές ακμές) **δεν συναντιόνταν** → ανοιχτή γωνία. (Διευκρίνιση «miter ή ορθογώνιο στο 3Δ»: είναι **miter**· σε **90°** γωνία το miter δίνει **καθαρή ορθή γωνία** — συμπίπτουν· **όχι** chamfer, αφού το περίγραμμα κολόνας είναι κλειστό → όλες οι γωνίες mitered, κανένα ελεύθερο άκρο.) **Fix (FULL SSoT, N.0.2):** extract της γωνιακής γεωμετρίας (`computeMiteredOuter` + `closeOpenOuterEnds` + `segOffsetVec`/`lineIntersect`/`MITER_LIMIT_FACTOR` + `Vec2`) από το `bim-3d/converters/structural-finish-3d.ts` (που εισάγει THREE) σε **NEW pure module** `bim/finishes/structural-finish-outline-geometry.ts` (μηδέν THREE). Το `structural-finish-3d.ts` **re-export**-άρει `computeMiteredOuter` (μηδέν break σε importers/test). Το 2Δ `drawStructuralFinishOutline` καταναλώνει πλέον το **ΙΔΙΟ** `computeMiteredOuter` (offsets = `segOffsetVec` ανά όψη → mitered `aOuter/bOuter` + extended `aCore/bCore`) → οι γωνίες **κλείνουν ΠΑΝΟΜΟΙΟΤΥΠΑ** με το 3Δ. **ΕΝΑ corner-geometry SSoT για 2Δ+3Δ.** Για μεμονωμένη κολόνα το per-element 2Δ outline ταυτίζεται με τη 3Δ silhouette (1 member). 91/91 ADR-449 jest (byte-equivalent extract). **DEFER → Slice X2 μέρος Β (όταν προστεθεί δοκάρι):** το 2Δ να τραφεί από την ΙΔΙΑ **merged silhouette** με το 3Δ ώστε να ταιριάζουν και οι **συμβολές** (μηδέν διπλή γραμμή 2Δ· CHECK 6D → stage ADR-040). **ΜΑΘΗΜΑ: κοινός resolver ≠ κοινή γωνιακή γεωμετρία· το corner-closing math ζούσε μόνο στο 3Δ path → extract σε pure SSoT πριν το καταναλώσει το 2Δ.** Pending browser-verify (2Δ: γωνίες κολόνας ορθή-miter, μηδέν κενό/διπλή) + commit (git add ΜΟΝΟ δικά μου: structural-finish-outline-geometry.ts[NEW], structural-finish-3d.ts, structural-finish-outline-2d.ts, ADR-449.md).
- **2026-06-14** — **Slice X1: ΕΝΙΑΙΟ συνεχές δέρμα σοβά στις συμβολές (3Δ merged silhouette ΕΝΕΡΓΟ — αντικαθιστά το per-element 3Δ σκιν)** — Giorgio: «βλέπω **δύο γραμμές** στις ενώσεις, φοβάμαι ότι οι όμοροι σοβάδες **αλληλοδιεισδύουν**». **Διάγνωση = Firestore-first** (level `lvl_aec46939…`): τα δοκάρια είναι **center-justified** (250 σε κολόνα 400 → **inset 75mm**, ΟΧΙ edge-justified όπως νόμιζε το handoff) → ο σοβάς δοκαριού & κολόνας απέχουν 75mm, **δεν εφάπτονται** → union των finish bands ΔΕΝ τους ενώνει (η «Approach A» του handoff απορρίπτεται). **Δύο αιτίες:** (3Δ) το Slice 10 corner-fill γίνεται μέσω **OVERLAP** → δύο bands διεισδύουν φυσικά (διπλή ακμή + z-fighting = η αλληλοδιείσδυση)· (2Δ) per-element ανεξάρτητο outline → δύο παράλληλες offset γραμμές με σκαλοπάτι. **Revit-grade λύση:** ΕΝΑ συνεχές δέρμα τυλίγει τη συνολική σιλουέτα του μπετόν ανά ζώνη ύψους. **Αναβίωση της dormant merged-silhouette (Slice 7)** — όχι revert· **απόδειξη ότι το παλιό «μία όψη μόνο» ΔΕΝ ήταν τοπολογικό αλλά naive wall coverage:** οι grid τοίχοι είναι **ταυτόσημοι σε κάτοψη** με τα δοκάρια (ίδιος άξονας/πάχος 250) + `topBinding:'attached'`· η silhouette τους χρησιμοποιούσε **un-dilated, height-unaware** → έτρωγαν ασύμμετρα τη μία όψη (= ακριβώς το root cause των Slice 8/8b, που ποτέ δεν είχε μπει στη silhouette). **Fix (port Slice 8/8b στη silhouette):** NEW `WallObstacle{footprint,zBotMm,zTopMm}` + per-band vertical-overlap filter (`wallFootprintsInBand`) στο pure `structural-finish-silhouette.ts`· ο adapter `structural-finish-scene-silhouette.ts` δίνει **resolved z-extent** (attached-top → top = κάτω παρειά δοκαριού via `attachTopToIds`→`beamZExtent`, αλλιώς nominal). `STRUCTURAL_SILHOUETTE_ENABLED=true`· τα scene converters (`bim-scene-attach-syncs` column + `BimSceneLayer.syncBeams` beam) δίνουν `suppressFinishSkin=true` (per-element 3Δ OFF). **BOQ ΑΜΕΤΑΒΛΗΤΟ** (per-element, ξεχωριστό path). **ΕΚΤΟΣ ADR-040** (3Δ scene-level). **Selection fix (Giorgio: «επιλέγω δοκάρι → επιλέγονται ΟΛΟΙ οι σοβάδες»):** το merged skin μοιράζεται synthetic `bimId` ανά κτίριο → κλικ σε μία όψη επέλεγε όλο τον σοβά. Fix: το silhouette skin γίνεται **μη-pickable** (`makeSkinNonPickable` → `mesh.raycast` no-op) → το ray περνά μέσα του και χτυπά τον δομικό πυρήνα από πίσω → κλικ σε σοβατισμένη όψη επιλέγει το σωστό δοκάρι/κολόνα. (Tradeoff: επιλογή στοιχείου ΔΕΝ highlight-άρει πλέον τον σοβά του — ο merged σοβάς δεν αποδίδεται per-element· Revit-grade: finishes = μη-επιλέξιμη διακόσμηση.) 85/85 ADR-449 jest (+4 Slice X1 silhouette + 1 non-pickable: height-aware support-wall → 2 όψεις· contrast full-height → λιγότερος σοβάς· internal interface χωρίς σοβά· κάτω ζώνη κολόνας wall active· skin raycast παρακαμμένο). **DEFER → Slice X2:** 2Δ `drawStructuralFinishOutline` να καταναλώνει την ίδια merged silhouette (μηδέν διπλή γραμμή 2Δ). **ΜΑΘΗΜΑ: silhouette one-face = naive wall coverage (un-dilated/height-unaware σε grid τοίχους ταυτόσημους με δοκάρια), ΟΧΙ εγγενής τοπολογία· Firestore-records-first το ξεκλείδωσε ξανά.** Pending browser-verify (3Δ: συνεχές δέρμα στις συμβολές, μηδέν διπλή ακμή/διείσδυση· κάθε δοκάρι 2 όψεις) + commit (git add ΜΟΝΟ δικά μου). *(MIXED με ADR-448: BimSceneLayer, bim-scene-attach-syncs — μόνο δικές μου γραμμές)*
- **2026-06-14** — **Slice 10: junction-aware κλείσιμο σοβά (extend/corner-fill) στις flush συμβολές «από κάναβο» (ADR-441)** — Giorgio screenshots: στις συμβολές εμφανιζόταν **τριγωνικό γκρι πτερύγιο/κενό** αντί καθαρού κλεισίματος. **Διάγνωση = Firestore-first** (`floorplan_beams`/`_columns`, level `lvl_aec46939…`: κάναβος 3×3 κολόνες 400×400, δοκάρια 250 **edge-justified** column-aware → π.χ. αριστερή παρειά δοκαριού **collinear** με αριστερή παρειά κολόνας). **Αιτία:** το Slice 9 έβαλε `chamferOpenEnds=true` για **όλα** τα ανοιχτά άκρα → σε flush συμβολή δύο finish παρειών (δοκάρι+κολόνα) τα άκρα chamfer-άρουν αντίθετα → υποχωρούν → **τριγωνικό κενό**. **Discriminator:** το 2Δ outline (`drawStructuralFinishOutline`) ΗΔΗ square → το τρίγωνο = **3Δ μόνο** (`closeOpenOuterEnds`). **Fix (per-end, Revit-grade):** το άκρο κλείνει ανάλογα αν ακουμπά γείτονα: **ελεύθερο** (open space) → chamfer 45° (ΜΟΝΟ outer μέσα → λοξό end-cap· αμετάβλητο Slice 6 #3)· **junction** (`seg.aJunction/bJunction`) → **ορθογώνια EXTEND**: **core ΚΑΙ outer** σπρώχνονται μαζί **έξω** κατά το πάχος (κατά τον άξονα) → το end-cap μένει **ΚΑΘΕΤΟ (ορθογωνική τομή)** flush στην εξωτερική παρειά του διπλανού → **corner-fill** που γεμίζει την κάθετη γωνία ΚΑΙ overlap-άρει σε collinear, χωρίς λοξή ακμή που μπαίνει στο σώμα του όμορου. **3 σημεία:** (α) `FinishFaceSegment += aJunction?/bJunction?` (types)· (β) **resolver** annotates per-endpoint = `pointNearObstacle(endpoint, obstacles, tol)` (tol=`JUNCTION_TOL_MM=10`×scale, παράγεται από `unitToMeters` → **μηδέν αλλαγή στο scene adapter** @500-line cap N.7.1)· (γ) `closeOpenOuterEnds` (3D, πρώην `chamferOpenOuterEnds`) + `computeMiteredOuter` επιστρέφει ΚΑΙ `aCore/bCore` (το quad τα διαβάζει αντί για raw `seg.a/b`). **v1→v2→v3 (iterative browser):** v1 junction → **square** (στάση στην παρειά) → Giorgio «ορθογώνιος αλλά δεν κλείνει» (square αφήνει εξωτερικό τριγωνάκι ανοιχτό· ασύμμετρο — η μία πλευρά έκλεινε μέσω within-element miter)· v2 junction → **outer-only EXTEND** (κλείνει αλλά λοξό end-cap = miter που **διεισδύει** στο σώμα του διπλανού — Giorgio: «η οριζόντια τομή φαίνεται miter, οι ακμές μπαίνουν μέσα»)· v3 = **core+outer EXTEND** → κάθετη ορθογωνική τομή, flush, μηδέν διείσδυση. Ελεύθερα cantilever κρατούν 45°. 2Δ ήδη square → **μηδέν 2D change**. 86/86 jest (+3) + 11/11 renderer-finish. ΕΚΤΟΣ ADR-040. **ΜΑΘΗΜΑ: corner-fill σε junction = ορθογώνια EXTEND του ΟΛΟΚΛΗΡΟΥ band (core+outer) κατά τον άξονα → κάθετο end-cap flush στον διπλανό. Outer-only extend = λοξό end-cap (miter) που διεισδύει· square = ανοιχτό τριγωνάκι. Ο chamfer (outer-only pull-in) σωστός ΜΟΝΟ σε ελεύθερο άκρο.** Pending browser-verify + commit. *(MIXED files με Slice 9 — μόνο δικές μου γραμμές)*
- **2026-06-13** — **Slice 9: συνεπής ΕΝΩΣΗ σοβά δοκαριού↔κολόνας στις γωνιακές συμβολές (browser-verified «τέλεια ένωση»)** — Giorgio: «αλλού miter, αλλού κάθετη κοπή». **Διάγνωση = Firestore-first + iterative browser** (καθαρό floorplan → 4 κολόνες + 4 δοκάρια από κάναβο σε ορθογώνιο πλαίσιο· διάβασα τα `floorplan_columns`/`_beams` records). **Αιτία (3 αλληλεπιδρώντα):** (1) ο 45° chamfer ήταν ενεργός μόνο στα δοκάρια (`bimType==='beam'`)· οι κολόνες έκλειναν τα ανοιχτά άκρα τους **square** → στην ΙΔΙΑ συμβολή άκρο remnant κολόνας square vs άκρο δοκαριού 45° = ασυνέπεια. (2) Με chamfer-both → οι δύο κάθετες λωρίδες **υποχωρούσαν** από το miter point → **κενό**. (3) Το `STRUCTURAL_JOIN_TOL_MM=10` εφαρμοζόταν με **isotropic** `dilatePolygonOutward` → το beam-obstacle μεγάλωνε ΚΑΙ εγκάρσια → «έτρωγε» 10mm από το remnant κολόνας ΚΑΘΕ πλευρά → οι chamfers προσγειώνονταν ~10mm χώρια. **Fix (3 σημεία, ΕΝΑ slice):** (α) `computeMiteredOuter(segs, offsets, true)` — 45° chamfer **και στις κολόνες** (ο chamfer αγγίζει ΜΟΝΟ τα μη-mitered άκρα → οι γωνίες μένουν miter· ΟΛΑ τα ανοιχτά άκρα 45° = συνεπές). (β) NEW `dilatePolygonAlongAxis` (`polygon-dilate.ts`) — **directional** dilation· το beam-obstacle μεγαλώνει ΜΟΝΟ κατά τον **άξονά** του (γεφυρώνει το flush άκρο ΧΩΡΙΣ εγκάρσια συρρίκνωση) → remnant κολόνας φτάνει ΑΚΡΙΒΩΣ την παρειά δοκαριού. (γ) το **κολόνα-obstacle** στο δοκάρι = **ΧΩΡΙΣ dilation** (σε flush δεν επικαλύπτει την κάθετη πλάγια όψη → δεν την κόβει· σε πραγματικό overlap → `coveredIntervals` κόβει σωστά χωρίς tolerance). → οι chamfers δοκαριού & κολόνας **συμπίπτουν** στο miter point → **μηδέν κενό, καθαρό 45° miter παντού** (Giorgio: «τέλεια ένωση»). `BeamFinishObstacle.params += startPoint/endPoint` (άξονας· BIM+Dxf το έχουν, μηδέν cast). 83/83 jest (+4 `dilatePolygonAlongAxis`: axis-only extend / transverse αμετάβλητο / d≤0 no-op). **Tradeoff:** manual δοκάρι με πραγματικό κενό >10mm από την κολόνα ίσως αφήσει λεπτό σοβά στη συμβολή (grid-first = OK· tunable). **ΜΑΘΗΜΑ: σε flush framing οι μηχανισμοί κάλυψης (isotropic dilation) «τρώνε» εγκάρσια το γειτονικό remnant → directional (axis-only) dilation· το cross-obstacle χρειάζεται tolerance ΜΟΝΟ κατά τη φορά επαφής. Firestore-first + iterative browser-verify έλυσε ό,τι η ανάλυση μόνη της δεν κλείδωνε.** ΕΚΤΟΣ ADR-040 (3D converter + pure geometry, μηδέν 2D canvas drawing files). Pending commit. *(MIXED files με Slice 8/8b — μόνο δικές μου γραμμές)*
- **2026-06-13** — **Slice 8b: attached-top wall exclusion (Η ΠΡΑΓΜΑΤΙΚΗ ΑΙΤΙΑ «μία όψη μόνο»)** — διάγνωση από **Firestore baseline** (Giorgio έφτιαξε 4 κολόνες + 4 δοκάρια + 4 τοίχους από κάναβο, εγώ διάβασα τα records). **Εύρημα:** οι born-from-grid τοίχοι είναι **`topBinding:'attached'`** με `attachTopToIds:[beam]` → η κορυφή τους κουμπώνει στην **κάτω παρειά** του δοκαριού (z=2500), ΟΧΙ στο nominal `baseOffset+height` (=3000). Επιπλέον, ίδιο πλάτος (250) + ίδιος άξονας → οι **πλάγιες όψεις δοκαριού ΣΥΜΠΙΠΤΟΥΝ** με τις παρειές τοίχου (beam x=10.666/10.916 == wall outer/inner). **Διπλή αστοχία:** (1) το Slice 8 height-filter με nominal height 3000 νόμιζε ότι ο τοίχος φτάνει μέσα στη ζώνη [2500,3000] → τον κρατούσε· (2) coincident παρειές → point-in-polygon ray-casting μετράει τη μία συμπίπτουσα παρειά «μέσα» (covered) και την άλλη «έξω» (exposed) → **ακριβώς μία όψη** χάνει σοβά, συστηματικά. **Fix:** ο `wallsOverlappingBeamBand` εξαιρεί τοίχους `topBinding==='attached'` (στήριγμα· resolved top = beam underside ≤ κάτω παρειά → ποτέ obstacle για τις πλάγιες όψεις· το δοκάρι είναι ολόκληρο από πάνω). Οι μη-attached full-height crossing walls παραμένουν obstacles (γνήσιο coverage). 73/73 jest (+2: attached→excluded, non-attached-crossing→kept). Αφαιρέθηκε το προσωρινό console diagnostic (λύθηκε από Firestore). **DEFER:** wall attached-top σε ΨΗΛΟΤΕΡΟ δοκάρι που διασχίζει χαμηλότερο (multi-tier, rare). **ΜΑΘΗΜΑ: για born-from-grid framing, οι τοίχοι κάτω από δοκάρια είναι attached-top στηρίγματα — χρησιμοποίησε resolved top (ΟΧΙ nominal height)· coincident faces + ray-casting = ασύμμετρη μία-όψη.** *(MIXED files — μόνο δικές μου γραμμές)*
- **2026-06-13** — **Slice 8: height-aware wall coverage για δοκάρια** (Giorgio — repro: «μόλις βάζω τοίχους που κολλάνε στα δοκάρια από κάτω, εξαφανίζεται ο σοβάς από τη μία πλευρά· τους αφαιρώ → 2 πλευρές»). **Root cause:** η κάλυψη πλάγιας όψης δοκαριού από τοίχο-obstacle ήταν **2D-only (κάτοψη)** — αγνοούσε το ύψος. Ένας τοίχος-**στήριγμα κάτω** από το δοκάρι (κορυφή ≈ κάτω παρειά) έχει plan-rectangle που (με flush/offset alignment) επικαλύπτει **ασύμμετρα** τη μία πλάγια όψη → θεωρούνταν καλυμμένη → έχανε τον σοβά (η άλλη εκτεθειμένη → σοβατιζόταν· γι' αυτό «μία πλευρά»). **Επιβεβαιώθηκε ΟΧΙ classification:** το exterior/interior επιλέγει μόνο ΥΛΙΚΟ, ποτέ παρουσία (ο 3D builder `addFinishPrism` προσθέτει πάντα band). **Fix:** NEW SSoT `wallsOverlappingBeamBand(walls, beamParams, floorElevationMm)` — κρατά obstacle έναν τοίχο **μόνο αν επικαλύπτεται κατακόρυφα** με τη ζώνη βάθους `[top−depth, top]` (tol 1mm). Τοίχος-στήριγμα από κάτω → εξαιρείται → σοβάς **και στις 2 όψεις**· τοίχος που διασταυρώνεται στο ίδιο ύψος → παραμένει (κόβει, μηδέν διπλο-σοβάτισμα). Filter μέσα στο `computeBeamFinishFaces` (ΕΝΑ σημείο → 2D+3D+BOQ)· ο **classifier** τρέχει στο ΠΛΗΡΕΣ wall set (αμετάβλητο building-footprint exterior/interior). `floorElevationMm` threaded στο 3D path (`BimSceneLayer.syncBeams` ctx → `beamToMesh` → `buildBeamFinishSkin`)· 2D/BOQ = default 0 (active-level convention). Beam-specific (οι κολόνες είναι full-height → οι τοίχοι ΠΡΕΠΕΙ να καλύπτουν εκεί που είναι μπροστά — σωστό). 71/71 jest (+5 height-aware coverage). **ΜΑΘΗΜΑ: plan-only coverage σε στοιχείο με κατακόρυφη έκταση → λάθος· φίλτραρε obstacles με vertical-overlap.** **DEFER:** attached-top τοίχος με nominal height που υπερβαίνει το resolved top (rare· χρειάζεται resolveWallTopZmm)· precise multi-floor 2D/BOQ datum (νυν floorElevationMm=0). *(MIXED files — μόνο δικές μου γραμμές)*
- **2026-06-13** — **Slice 7-revert (silhouette → dormant, per-element ξανά ενεργό)** (Giorgio). Συμπτώματα μετά το hole fix: «όλα τα δοκάρια σοβά μόνο από τη μία πλευρά — άλλα μέσα, άλλα έξω». **Διάγνωση:** η merged silhouette δίνει σοβά μόνο στις όψεις που είναι στο **boundary της `safeUnion`** → σε **ανοιχτή τοπολογία** (δοκάρια χωρίς κλειστό loop) δεν δημιουργείται τρύπα → μόνο το outer ring → μία όψη/δοκάρι (διαφορετική φορά ανά δοκάρι). Διαγνωστικό με **κλειστό** πλαίσιο (annulus) έδινε σωστά **και τις 2 όψεις** → το bug εξαρτάται από την τοπολογία της σκηνής, εγγενές όριο της silhouette. **Απόφαση (big-player):** το **per-element** μοντέλο (Slice 6) δίνει **αξιόπιστα 2 πλάγιες όψεις/δοκάρι** (`includeEdge` = 2 ακμές ∥ άξονα) + mutual-obstacle junction cut + 45° chamfer στις γωνίες, **ανεξάρτητα τοπολογίας**. → επαναφορά: `columnToMesh`/`beamToMesh` `suppressFinishSkin=false` ξανά· το scene-level `syncStructuralFinishSkin` έγινε **dormant** πίσω από `STRUCTURAL_SILHOUETTE_ENABLED=false` (κώδικας + 10 jest κρατήθηκαν ζωντανά για μελλοντικό **corner-join-only merged-miter** slice — όχι διαγραφή, flip flag όταν υλοποιηθεί robust corner-fill χωρίς τις πλάγιες όψεις ώστε μηδέν διπλο-σοβάτισμα). **Γωνίες κολόνα↔δοκάρι = Slice 6 (mutual-obstacle + chamfer)· ιδανικό merged-miter corner-join = 🔵 DEFER.** 3 wiring edits (bim-scene-attach-syncs, BimSceneLayer, bim-scene-structural-finish-sync), μηδέν αλλαγή resolver/BOQ. *(MIXED files — μόνο δικές μου γραμμές)*
- **2026-06-13** — Slice 7: **ΕΝΙΑΙΟΣ ΣΟΒΑΣ ΣΤΙΣ ΣΥΜΒΟΛΕΣ** (merged structural silhouette, Revit-grade FULL SSOT, Giorgio screenshots 151127+151222). Root cause = per-element offset με τοπική γωνία → (Β) γωνίες κολόνα↔δοκάρι δεν ενώνονται, (Α) σοβάς δοκαριού προεξέχει τοίχου. Λύση: NEW pure `computeStructuralSilhouetteBands` — ανά ζώνη ύψους `safeUnion` δομικών cores → ΕΝΑ outline → ΥΠΑΡΧΩΝ `resolveStructuralFinishFaces` (τοίχοι obstacles, dilated). **Β** = ΕΝΑ outline → συνεπής miter (μηδέν ασυνέχεια)· **Α** = τοίχος-obstacle αφαιρεί το καλυμμένο τμήμα (μηδέν προεξέχων σοβάς → ομοεπίπεδο). NEW `buildStructuralSilhouetteSkin` (REUSE `buildFinishSkinFromFaces`)· NEW scene adapter `computeStructuralFinishSilhouette` + `syncStructuralFinishSkin` (ΕΝΑ scene-level pass, group ανά κτίριο)· per-element 3D skin αντικαθίσταται (`suppressFinishSkin` flag· ghosts/previews κρατούν per-element). **3D-only· BOQ per-element αμετάβλητο.** **+ winding fix (Giorgio «σοβάδες δοκαριών εντός σώματος»):** η `polygon-clipping` είναι winding-sensitive — το **CW** beam outline (`buildOutlineRect`) ερμηνευόταν ως **τρύπα** → το `safeUnion` δεν ένωνε δοκάρι+κολώνα → ο σοβάς έβγαινε εντός σώματος. FIX: `footprintToPolygon` κανονικοποιεί σε **CCW** (shoelace) πριν το union (+regression test CW-merge). **+ un-dilate fix (Giorgio «δεν εμφανίζονται οι σοβάδες δοκαριού»):** η dilation των τοίχων-obstacles (που πρόσθεσα για το Α) κάλυπτε ΟΛΟΚΛΗΡΗ την πλάγια όψη των δοκαριών που κάθονται **collinear πάνω σε τοίχους** (grid framing ADR-441) → ο σοβάς εξαφανιζόταν. REVERT σε **un-dilated** τοίχους (browser-verified per-element behavior): κάθετος τοίχος→καλύπτεται· collinear→σοβάς εμφανίζεται. Β (γωνίες κολόνα↔δοκάρι, screenshot 151222) λύνεται από το union. **Α (σκαλοπάτι δοκάρι↔τοίχος, 151127) — big-player ανάλυση (Giorgio «τι κάνουν οι μεγάλοι παίκτες;»):** ο μηχανικός δίνει στατική διατομή ΧΩΡΙΣ σοβά· ο τοίχος (ADR-413 DNA) έχει σοβά ΜΕΣΑ στο ονομαστικό· το δοκάρι (ADR-449) σοβά ΕΞΩ. Revit/ArchiCAD: **immutable δομική διατομή + additive-outside finish, ΠΟΤΕ recess/bury**· η ομοεπιπεδότητα = ευθύνη διαστάσεων αρχιτέκτονα (στενότερο δοκάρι → σοβάς πέφτει φυσικά coplanar· ίδιο πλάτος → ειλικρινώς proud). **ΑΠΟΦΑΣΗ: η αρχική ADR-449 σύμβαση (additive-outward) ΕΙΝΑΙ η big-player σωστή.** Το ενδιάμεσο inward-align A-fix (που έθαβε τον σοβά μέσα στον συμπαγή πυρήνα — Giorgio «χάνονται μέσα στο σώμα») **ΑΝΑΙΡΕΘΗΚΕ**. Τελικό: union (Β) + un-dilated walls + **additive-outward** (ορατός, big-player). **+ hole fix (Giorgio «frame δοκαριών = σοβάς μόνο στην εξωτερική πλευρά»):** όταν δομικά στοιχεία σχηματίζουν **πλαίσιο** (frame γύρω από δωμάτιο), η `safeUnion` δίνει πολύγωνο **με τρύπα**· το `resolveBandFaces` επεξεργαζόταν ΜΟΝΟ το `poly[0]` (outer ring) → οι **εσωτερικές όψεις (όψη δωματίου) αγνοούνταν**. FIX: επεξεργασία **ΟΛΩΝ** των rings (poly[0] outer + poly[1..] holes)· resolver `+ holeRing` flag → `orientRing` κρατά την τρύπα **CW** ώστε `(dy,−dx)` να δείχνει **μέσα στο δωμάτιο** (ο σοβάς της inner όψης). 72/72 ADR-449 jest (+10 silhouette suite, frame inner-faces test). Pending browser-verify + commit. *(MIXED files με ADR-448/441/450: bim-three-structural-converters, BimSceneLayer, bim-scene-attach-syncs — μόνο δικές μου γραμμές)*
- **2026-06-13** — Slice 6 fix #3 (browser-verify, Giorgio screenshot 145401): **πτερύγιο σοβά δοκαριού στη συμβολή με κολόνα/τοίχο**. Η λωρίδα σοβά της πλάγιας όψης (offset 15mm προς τα έξω) τελείωνε με **τετράγωνο «κεφάλι»** → η εξωτερική γωνία προεξείχε ~15mm στη γωνία της συμβολής (δεν είχε πού να κλείσει). Δεν είναι θέμα πάχους πυρήνα (όλα 250+σοβάς=280, ευθυγραμμισμένα)· είναι corner-treatment. FIX: `chamferOpenOuterEnds` — τα **ανοιχτά** άκρα (μη-mitered) κόβονται **45°** (η εξωτερική γωνία τραβιέται μέσα κατά το πάχος, clamp στο μισό μήκος) → clean corner χωρίς πτερύγιο, mirror φαλτσογωνιάς σοβά τοίχων. `computeMiteredOuter(..., chamferOpenEnds)`: δοκάρι=true, κολόνα=false (οι γωνίες κολόνας κλείνουν με miter, τα wall-gap άκρα μένουν square). 74/74 ADR-449 jest (+2 chamfer unit tests). Pending browser-verify + commit.
- **2026-06-13** — Slice 6 fix #2 (browser-verify, Giorgio screenshot 143918 — **η πραγματική αιτία**): οι κολόνες έμεναν **εντελώς χωρίς σοβά μόλις προστίθεντο δοκάρια**. ROOT CAUSE: τα δοκάρια κάνουν τις κολόνες να **auto-attach** (ADR-441/401 column→beam framing) → η κολόνα περνά στο **attached-prism path** του `columnToMesh`, που έκανε `return tagged` (**πυρήνας-only**, ο σοβάς ήταν flat-path-only — DEFER Slice 2). FIX: extract `composeColumnWithFinish()` (κοινό flat + attached) → το attached path πλέον συνθέτει επίσης σοβά, με ύψος = το χαμηλότερο attached top (flat-top approx· per-corner sloped finish = DEFER). 72/72 ADR-449 jest (+attached-path test). Το height-aware banding (fix #1) ήταν σωστό αλλά δεν καλούνταν καν στο attached path. Pending browser-verify + commit.
- **2026-06-13** — Slice 6 fix (browser-verify, Giorgio screenshot 131457): **height-aware junction**. Η αρχική Slice 6 αφαιρούσε σοβά για **ΟΛΟ το ύψος** της κολόνας (regression: κολόνες σχεδόν γυμνές, ενώ το δοκάρι πιάνει μόνο το πάνω ~depth). Fix: `computeColumnFinishBands()` → κατακόρυφες ζώνες (κάτω `[0, height−beamDepth]` = walls-only πλήρης παρειά· πάνω = walls+beams junction cut)· 3D = ένα prism set ανά ζώνη στοιβαγμένα· BOQ = banded area (`bandedFinishAreasM2`)· 2D plan = walls-only (πλήρης κάτοψη). `BeamFinishObstacle + depth`· bbox-overlap filter για το band height. Υπόθεση v1: beam top ≈ column top (elevation-based banding = DEFER). 71/71 ADR-449 jest (νέα band regression-guard tests). Pending browser-verify + commit. *(κώδικας Slice 6 v1 + ADR docs έγιναν committed από τον ταυτόχρονο agent με git add -A — 5a05dd79/74419215 — ΟΧΙ από εμένα/Giorgio· το height-aware fix uncommitted)*
- **2026-06-13** — Slice 6: **beam↔column junction** (mutual structural obstacles, FULL SSoT). Επέκταση του υπάρχοντος `obstacles` μηχανισμού του resolver (μηδέν νέα γεωμετρία): ο scene adapter περνά πλέον ΚΑΙ τα footprints δοκαριών (resolve κολόνας) / κολώνων (resolve δοκαριού) ως obstacles → ο σοβάς εξαιρείται στη δομική σύνδεση (frame-into). NEW `polygon-dilate.ts` (`dilatePolygonOutward`, winding-free miter offset) → **join-tolerance dilation** (`STRUCTURAL_JOIN_TOL_MM=10`) ώστε η **flush** σύνδεση (born-from-grid trim, coverage=0 χωρίς dilation) να πιάνεται· οι τοίχοι ΧΩΡΙΣ dilation (αμετάβλητοι). `compute{Column,Beam}FinishFaces(..., cross = [])` + contributions βγάζουν beams/columns από `scene.entities` → BOQ μειώνεται αυτόματα (μηδέν αλλαγή feeds). Ρέει 2D+3D+BOQ (ένας resolver). 69/69 ADR-449-related jest (10 suites, incl. NEW polygon-dilate 6 + junction 5). Pending browser-verify + commit. *(MIXED files με ADR-448/441: bim-three-structural-converters, BimSceneLayer, bim-scene-attach-syncs — μόνο δικές μου γραμμές)*
- **2026-06-13** — Slice 1: data model + pure resolver (per-face, partial-coverage) + BOQ multi-layer (parent πυρήνας + interior/exterior σοβάς) + scene classifier. `coveredIntervals` εξήχθη σε shared SSoT (N.0.2). 13/13 jest. Pending browser-verify + commit.
- **2026-06-13** — Slice 2: 3D band skin κολόνας. SSoT core `computeColumnFinishFaces` (κοινό BOQ+3D). `buildColumnFinishSkin` ανά exposed segment → vertical band prism (REUSE `stripPrismGeometry`) με `getMaterial3D`. `columnToMesh` → composite `Group {πυρήνας+σοβάς}` (flat-path)· πυρήνας `width/depth` αμετάβλητος. Ghost guard. 10/10 jest + tsc καθαρό. Pending browser-verify + commit.
- **2026-06-13** — Slice 3: 2D finished outline. SSoT core στενεύτηκε σε `ColumnFinishSource`/`WallFinishObstacle` (κοινό BOQ+3D+2D, μηδέν cast για DxfColumn/DxfWall). Per-frame `buildFinishFacesByColumn` → `EntityRendererComposite.setColumnFinishFaces` → `ColumnRenderer.drawFinishOutline` (offset «λωρίδα» ανά εκτεθειμένη παρειά, plaster colour SSoT, ADR-040 orchestrator-drives). 6/6 jest + tsc καθαρό. ADR-040 changelog ενημερώθηκε. Pending browser-verify + commit.
- **2026-06-13** — Slice 5 fixes (browser-verify γύρος 1, Giorgio): **(#3 φορά)** το `buildOutlineRect` του δοκαριού παράγει **CW** outline → ο resolver `(dy,−dx)` έδειχνε ΜΕΣΑ → ο σοβάς εμφανιζόταν εντός σώματος. Fix: `ensureCCW` (shoelace signed-area) στον resolver → κανονικοποίηση winding· διορθώνει **2D ΚΑΙ 3D, κολόνα+δοκάρι** ταυτόχρονα (η κολόνα ήδη CCW → no-op). **(#1 γωνίες)** το 3D έχτιζε ένα band ανά παρειά χωρίς σύνδεση → οι 4 γωνίες κολώνας έμεναν ανοιχτές. Fix v2 (μετά από browser-verify γύρος 2 — τα corner-fills έδειχναν επικάλυψη): **πραγματικό 45° miter** (`computeMiteredOuter` + `lineIntersect`) — το εξωτερικό άκρο κάθε band επεκτείνεται/κόβεται στην τομή των offset ευθειών (convex→extend, reflex→trim, miter-limit για αιχμηρές)· **μηδέν επικάλυψη/κενό, ένα seam**, χωρίς ξεχωριστά corner meshes. **(#BOQ διπλομέτρηση — ανησυχία Giorgio)**: ΔΕΝ υφίσταται — το BOQ μετράει **επιφάνεια ανά παρειά** (`Σ lengthM × heightM`, ΑΤΟΕ m²), κάθε παρειά **μία φορά**· τα 3D corner/miter είναι visual-only, εκτός BOQ. **(#2 μικρές πλευρές δοκαριού)** = υπό επανέλεγχο μετά το #3 (πιθανό artifact)· πιθανή μελλοντική εργασία = beam↔column mutual obstacle στη σύνδεση. Regression: beam outward-extent + mitered-band-count. 76/76 ADR-449 jest + tsc καθαρό. Pending re-verify.
- **2026-06-13** — Slice 5: **toggle + ενεργοποίηση + per-element override**. Master view toggle `showFinishSkin` (scalar boolean, mirror `showHeatLoad`, **default ON**)· SSoT gate `isStructuralFinishVisible()` (event-time, κοινό 2D orchestrator + 3D converter — pure builders/leaves ανέγγιχτοι, ADR-040)· factory δίνει πλέον `createDefaultStructuralFinishSpec()` (enabled:true, plaster defaults 15mm)· **visibility-only** (BOQ μετράει πάντα — schedule=model)· per-element override (enabled/υλικά/πάχος) στα contextual ribbon tabs μέσω κοινού `finish-param` helper + pure override core (`read/applyFinishParam`)· master toggle UI `ShowFinishSkinToggle` (View tab). i18n el+en ΠΡΩΤΑ (N.11), Radix-free combobox (υπάρχον ribbon SSoT). 75/75 ADR-449 jest + 31/31 store regression. Slice 1-4 fixtures ενημερώθηκαν (ρητό `finish` override). ADR-040 changelog ενημερώθηκε. Pending browser-verify + commit. *(MIXED file με ADR-448: bim-three-structural-converters)*
- **2026-06-13** — Slice 4: **ΔΟΚΑΡΙΑ** (full mirror, FULL SSoT). `BeamParams.finish?`· resolver `+ includeEdge` (generic, default no-op)· scene adapter `computeBeamFinishFaces`/`computeBeamFinishContribution` (heightMm=depth· **2 πλάγιες όψεις ∥ άξονα· άκρα ⊥ άξονα σημασιολογικά εκτός**· classifier γενικεύτηκε `buildStructuralFinishClassifier`)· 3D pure core `buildFinishSkinFromFaces` extract + `buildBeamFinishSkin` + `beamToMesh(+walls)` composite (depth ΑΜΕΤΑΒΛΗΤΟ)· 2D pure `drawStructuralFinishOutline` extract (column overlays + beam το καλούν, μηδέν διπλασιασμός) + `buildFinishFacesByBeam` → `setBeamFinishFaces` (ADR-040)· BOQ `beam-boq-feed` wired (finish-inactive → byte-identical). 22/22 jest + 29/29 column regression + tsc καθαρό στα δικά μου. ADR-040 changelog ενημερώθηκε. Pending browser-verify + commit. *(MIXED files με ADR-448: bim-three-structural-converters, BimSceneLayer, column-renderer-overlays)*
