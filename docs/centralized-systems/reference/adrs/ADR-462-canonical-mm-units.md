# ADR-462 — Canonical-mm Units (ΕΝΑ unit system, πάντα χιλιοστά)

**Status:** 🟢 Phase 1 (core) Implemented + **BROWSER-VERIFIED** 2026-06-16 (Opus 4.8) — import scaling + resolveSceneUnits trust + tests, tsc clean — **pending commit**
**Επεκτείνει/υπερισχύει:** ADR-368 (per-file drawing-units override) ως προς τη ΣΗΜΑΣΙΑ των μονάδων (render-label → import-scale).
**Σχετικά:** ADR-358 §8 (propagate $INSUNITS), ADR-369 (elevation/units convention).

## 1. Πρόβλημα
Οι μονάδες λύνονταν **ανά σκηνή/όροφο**: `dxfScene.units = userDrawingUnits ?? resolveSceneUnits(scene)`, και το `resolveSceneUnits` **μάντευε** από τη διαγώνιο των bounds (`detectSceneUnits`). Αποτέλεσμα: ίδιο BIM στοιχείο (πάντα σε mm) renderαρόταν σε διαφορετική κλίμακα ανά όροφο.

**Incident (2026-06-16, Giorgio):** Ισόγειο με εισαγμένη κάτοψη θεμελίωσης (bounds ~35 → ανιχνεύθηκε ως **μέτρα**) → οι κολώνες αποθηκεύτηκαν με `sceneUnits:'m'` → μια 40×40 κολώνα φαινόταν **μικροσκοπική** σε σχέση με τους mm-όρόφους (1000× mismatch). Επιβεβαιώθηκε από Firestore (file `…ΘΕΜΕΛΙΩΣΗΣ…`, `processedData.bounds ~35×28`).

## 2. Απόφαση (Giorgio: «full canonical-mm»)
**ΟΛΗ η γεωμετρία αποθηκεύεται σε ΧΙΛΙΟΣΤΑ (mm), πάντα** (όπως το Revit έχει σταθερή εσωτερική μονάδα). Ένα εισαγόμενο DXF **κλιμακώνεται σε mm κατά το import**· δεν επιβάλλει τις δικές του μονάδες. Display unit (cm/m) = ξεχωριστή ρύθμιση (μελλοντικά). Καθαρή βάση (ο χρήστης τη διέγραψε) → μηδέν data migration.

## 3. Υλοποίηση (Phase 1 core)
- **Import scaling SSoT** — `utils/dxf-scene-builder.ts` `buildScene(content, unitsOverride?)`: μετά το parse, βρίσκει τη **source unit** και πολλαπλασιάζει ΟΛΑ τα coordinates σε mm μέσω του **ADR-348 `scaleEntity`** (reuse· χειρίζεται ΟΛΑ τα entity types — μηδέν νέα per-type math). Σφραγίζει `units: 'mm'`, recompute bounds. mm-source → identity (no-op).
- **Robust source detection** — NEW `utils/scene-units.ts` `resolveImportSourceUnits(insunits, bounds)`: εμπιστεύεται το `$INSUNITS` ΕΚΤΟΣ αν δηλώνει `'mm'` ενώ το coordinate-magnitude είναι metre-scale (Greek-DXF lie) → τότε νικά το bounds-heuristic. Priority στο call site: `unitsOverride ?? resolveImportSourceUnits(...)`.
- **resolveSceneUnits trust** — `resolveSceneUnits` επιστρέφει πλέον τη **δηλωμένη** μονάδα (incl. 'mm') ΧΩΡΙΣ heuristic re-guess· heuristic μόνο για legacy/unitless χωρίς δήλωση. (Αυτό ήταν η ρίζα του «μικροσκοπική κολώνα».)
- **Override threading** — `unitsOverride` ρέει client (`importDxfFile`→`directParseFileWithEncoding`→`buildScene`) + worker (`dxf-parser.worker` message). Server path (`floorplan-process.service` → `buildScene`) παίρνει **αυτόματα** το robust auto-detect (κοινή συνάρτηση).
- **Consumers αμετάβλητοι** — οι ~30 `resolveSceneUnits` consumers + ~25 BIM entity types παίρνουν 'mm' αυτόματα → ΟΛΟΙ οι όροφοι ίδια κλίμακα, by construction.
- **Tests:** `utils/__tests__/canonical-mm-units.test.ts` (10): scale m→mm/cm→mm/identity, lying-mm override, resolveSceneUnits trust, buildScene end-to-end. tsc clean.

## 4. Συνέπειες
- ✅ BIM entities συνεπή σε όλους τους ορόφους ενός κτηρίου.
- ✅ Εισαγμένη κάτοψη ευθυγραμμίζεται (κλιμακώνεται σε mm στο import).
- ✅ Καμία ανά-όροφο μαντεψιά μονάδων downstream.
- ⚠️ Edge: μικρό mm detail-drawing (bounds 500–50k) με $INSUNITS=mm → heuristic θα έλεγε 'cm' → ο χρήστης το διορθώνει με wizard override (DxfUnitsSelector, ADR-368). Σπάνιο για floorplans.
- ✅ (α) **Display-unit wiring (Phase 2)** — ΕΝΑ unit SoT (status-bar επιλογέας cm/m/mm/in/ft), Revit-style: ruler ticks + dimension pills + move/drag readouts + primitive-entity distance labels ακολουθούν τον ίδιο επιλογέα, **live** (markAllCanvasDirty σε κάθε αλλαγή). Canonical-mm αμετάβλητο· αλλάζει ΜΟΝΟ η εμφάνιση.
- ✅ (δ) **Display-measurement SSoT ΕΝΟΠΟΙΗΣΗ (Phase 2 Unification, Revit-grade FULL SSoT)** — ΟΛΟΙ οι read-only display callers (inline `formatDistance`/`formatCoordinate`, area/perimeter, hover edge/radius/text-labeling, guides, dynamic-input, primitive entity labels, cursor X/Y readout) περνούν πλέον από ΕΝΑ module 3 ρητών concerns (length / area / coordinate). Editable inputs μένουν parseable.
- 🔜 DEFER: (β) server-route explicit units param (τώρα auto-detect αρκεί), (γ) απλοποίηση παλιών ADR-368 R12/R13 dim-rescues (γίνονται no-op σε mm).

## 5. Changelog
- **2026-07-20 (Opus 4.8) — Round 22: N.7.1 file-size split — ο canonical-mm scale pass βγήκε σε δικό του module (behavior-preserving, 67/67 jest GREEN).** Ο `dxf-scene-builder.ts` έφτασε **504 γραμμές** μετά τα Rounds 20/21 → CHECK 4 (File sizes) μπλόκαρε το commit. Κανόνας «EXTRACT, ποτέ trim»: εξήχθη ο **αυτοτελής** ADR-462 pass (source-unit resolution + per-entity scale-to-mm) σε NEW `utils/dxf-canonical-mm-scale.ts` (114 γρ.):
  - `resolveSourceUnits(header, bounds, unitsOverride?)` — η προτεραιότητα wizard override → `$INSUNITS` → bounds heuristic (μαζί με το ADR-362 R20 σχόλιο για τα stored extents) ζει πλέον σε ΕΝΑ σημείο, unit-testable χωρίς να στηθεί ολόκληρο DXF.
  - `applyCanonicalMmScale({entities, bounds, header, unitsOverride, diagnostics, recomputeBounds})` — ο βρόχος `scaleEntity` + το fault-tolerant `recordError` per-entity (ADR-635 Φ3). Το `recomputeBounds` περνιέται **injected** ώστε το module να μη σέρνει τις block-aware bounds εξαρτήσεις (`calculateBoundsWithBlocks`) — μένει καθαρό.
  - No-op fast path: όταν το σχέδιο είναι ήδη mm επιστρέφονται τα ΙΔΙΑ arrays (κανονικά mm αρχεία δεν πληρώνουν τίποτα).
  - `dxf-scene-builder.ts` **504 → 472** γραμμές· αφαιρέθηκαν τα πλέον αχρησιμοποίητα imports (`Entity`, `scaleEntity`, `insunitsCodeToSceneUnits`, `mmToSceneUnits`, `resolveImportSourceUnits`, `resolveUnitDetectionBounds`).
  - **Tests:** και τα 10 suites που εξαρτώνται από τον `DxfSceneBuilder` GREEN (67 tests: canonical-mm-units, units-detection-extents, out-of-extents-filter, scene-bounds-hatch, import-robustness, block-expansion, spline-vertices, annotation-scale, worker-parse-chain, server-import-safety). tsc SKIP (N.17). ✅ Google-level: YES — καθαρή εξαγωγή, μηδέν αλλαγή συμπεριφοράς, injected dependency αντί για νέο coupling.
- **2026-07-20 (Opus 4.8) — Round 20: import unit-detection driven by stored extents `$EXTMIN/$EXTMAX` + wizard mismatch warning (UNCOMMITTED, 12 νέα jest GREEN· βλ. ADR-362 R20). Bug Giorgio: `ACAD-τοπογραφικόFinal.dxf` (geo-referenced ελληνικό τοπογραφικό, `$INSUNITS=4`/mm ενώ είναι **μέτρα**) εισαγόταν «ζαρωμένο» ×1000 (οικόπεδο 30m→30cm) ΠΑΡΑ την ύπαρξη του `resolveImportSourceUnits`.**
  - **Root cause (πραγματική αναπαραγωγή, ΟΧΙ υπόθεση):** ο resolver ΔΕΝ παρακάμπτονταν — απλώς το **σήμα** του ήταν μολυσμένο. Το αρχείο έχει την πραγματική γεωμετρία geo-referenced στο (131.800, 741.800), αλλά **58 stray entities** (legacy `ASHADE` INSERT blocks + ATTRIBs καρφωμένα στο 0,0). Ο builder υπολόγιζε bounds από ΟΛΑ τα entities → span 0..131.992 × 0..741.923 → diagonal **753.561** → `detectSceneUnits` bucket **`mm`** (>50k) → `resolveImportSourceUnits('mm', junk)` επέστρεφε `'mm'` → mmFactor=1 → καμία κλίμακα. Το καθαρό `$EXTMIN/$EXTMAX` (307.8×168.2, diagonal **350**) έδινε σωστά `'m'` — είναι το junk-free extent που η ίδια η AutoCAD κρατά (πρακτική Revit/AutoCAD: εμπιστεύονται τα stored extents).
  - **FIX (narrow, ADR-462-safe — ΜΟΝΟ import-source path, ΔΕΝ αγγίζει το `resolveSceneUnits` declared-trust):**
    - `dxf-parser-types.ts` — `DxfHeaderData.extmin/extmax?` (detection-only, ποτέ transform).
    - `dxf-entity-parser.ts parseHeader` — parse `$EXTMIN/$EXTMAX` (codes 10/20)· απορρίπτει το uninitialized `±1e20` sentinel + inverted box (ceiling 1e15).
    - `scene-units.ts` — NEW SSoT: `isUsableDetectionExtent`, `resolveUnitDetectionBounds(declaredExtents, computedBounds)` (prefer καθαρά extents, fallback computed), `computeUnitSuggestion(insunits, extents)` (mirror του auto για τον wizard).
    - `dxf-scene-builder.ts` — τροφοδοτεί το `resolveImportSourceUnits` με `resolveUnitDetectionBounds(header.extmin/extmax, computedBounds)`.
  - **Wizard warning (Τρόπος 1, Giorgio: auto-fix + ενημέρωση, Revit/ArchiCAD units-mismatch):** NEW `hooks/common/useDxfUnitSuggestion.ts` (ελαφρύ header-only slice-parse 256KB, reuse `parseHeader`+`computeUnitSuggestion`)· `DrawingUnitsStep.tsx` δείχνει warning banner + «Προτεινόμενο» badge όταν `$INSUNITS` διαφωνεί με το magnitude-extent. Το 'auto' πλέον resolve-άρει σωστά σιωπηλά· ο χρήστης βλέπει/αλλάζει. i18n `drawingUnits.mismatch.*` (el+en).
  - **ADR-462 landmine σεβασμένο:** το «κολώνα μικροσκοπική» σενάριο (real mm scene, small bounds) μένει σωστό — κανένα global heuristic override στο `resolveSceneUnits`· τα 10 existing `canonical-mm-units.test.ts` GREEN. Το fix βελτιώνει ΜΟΝΟ το σήμα του υπάρχοντος import resolver.
  - **Tests:** NEW `utils/__tests__/dxf-units-detection-extents.test.ts` (12): pure helpers + sentinel guard + parseHeader extents + end-to-end builder scaling ΜΕ origin junk (with-extents→×1000, without→un-scaled bug documented) + honest-mm no-warning. tsc SKIP (N.17). ✅ Google-level: YES — reuse υπάρχοντος resolver, ΕΝΑ detection-bounds SSoT, narrow, tested, big-player-aligned.
  - **✅ SPLINE skip FIXED (Giorgio «ΝΑΙ», ίδια συνεδρία):** το `convertSpline` χρησιμοποιούσε ακόμη το flat `parseVerticesFromData` (collapse repeated code-10 → 1 σημείο → <2 → skip) — έχανε και τις 10 ισοϋψείς του τοπογραφικού. FIX: reuse του ADR-507 pairs μηχανισμού + smoothDisplay (καμπύλη). Το κοινό «pairs-or-data» idiom (LWPOLYLINE+SPLINE) εξήχθη σε ΕΝΑ `verticesFromPairsOrData` (`dxf-converter-helpers.ts`) — το jscpd (N.18) έπιασε το sibling clone και το ενοποίησα. NEW `dxf-spline-vertices.test.ts` (control points survive, smoothDisplay, μη-skipped). Files: `dxf-entity-converters.ts`, `dxf-converter-helpers.ts`.
- **2026-07-20 (Opus 4.8) — Round 21: off-drawing junk removal at import (root-cause «άδειο canvas», UNCOMMITTED, 4 νέα jest GREEN, ground-truth επιβεβαιωμένο). Giorgio (κουρασμένος): «το σχέδιο δεν φαίνεται, κάτι πάει λάθος με την κλίμακα — λύσ' το όπως οι μεγάλοι».**
  - **Root cause (ground-truth run στο πραγματικό αρχείο):** μετά το Round 20 units fix (units=mm, scaled ×1000 σωστά), το σχέδιο ΠΑΡΕΜΕΝΕ αόρατο. Το αρχείο έχει **61/332 stray entities στο (0,0)** (legacy `ASHADE` INSERTs + orphan ATTRIBs), ενώ η πραγματική γεωμετρία κάθεται geo-referenced στο ~131-742 εκατ. mm. Το scene bbox γινόταν **0..131.992.934 × 0..741.923.525** → το viewport framing χωρούσε ΟΛΟ το κουτί → το πραγματικό σχέδιο (307.800×168.200) = υπο-pixel κουκκίδα → **άδειο canvas**.
  - **FIX (big-player practice: AutoCAD/Revit/Civil3D αγνοούν ό,τι είναι εκτός `$EXTMIN/$EXTMAX` + Zoom-Extents):** NEW `utils/dxf-out-of-extents-filter.ts` `dropOutOfExtentsEntities(entities, extmin, extmax)` — πετά ΜΟΝΟ entities **εντελώς έξω** από τα declared extents (padded κατά μία drawing-diagonal), ΜΟΝΟ όταν τα extents είναι usable (reuse `isUsableDetectionExtent`). Οποιοδήποτε overlap → keep· χωρίς extents / sentinel → no-op (κανονικά σχέδια ΑΝΕΓΓΙΧΤΑ). Wired στον `dxf-scene-builder` ΠΡΙΝ το bounds+scale (raw coords) → το `finalEntities`/bounds ρέουν καθαρά.
  - **Ground-truth ΜΕΤΑ:** normalized bounds **0..307.800 × 0..168.200** (307.8m × 168.2m στην αρχή αξόνων) → το σχέδιο γεμίζει την οθόνη. (ΠΡΙΝ: 0..131.992.934.) 332→319 entities (13 far-outside dropped)· τα εναπομείναντα degenerate origin-junk (μηδενικής γεωμετρίας) δεν επηρεάζουν τα tight bounds.
  - **Tests:** NEW `dxf-out-of-extents-filter.test.ts` (drop far junk / keep overlap / no-op χωρίς extents / no-op sentinel). Συνολικά 36/36 GREEN. jscpd clean. tsc SKIP (N.17). ✅ Google-level: YES — big-player-aligned, reuse extents SSoT, narrow (fires μόνο στην far-junk παθολογία), tested, μηδέν επίπτωση σε κανονικά σχέδια.
- **2026-07-05 (Opus 4.8) — NEW `formatSceneLengthForDisplay` — ΕΝΑ scene→display bridge (Giorgio «FULL SSoT, όπως οι μεγάλοι»).** Το idiom `formatLengthForDisplay(sceneValue × sceneScale, {m})` ήταν σκόρπιο (3 αρχεία, 3 spellings: `× canvasToMmScaleFor`, `× mmPerScene`, `/ f`). NEW `formatSceneLengthForDisplay(sceneValue, sceneUnits, opts?)` στο `config/display-length-format.ts` = **η μόνη** γέφυρα scene→canonical-mm (`canvasToMmScaleFor`) → display (`formatLengthForDisplay`). Migrated: `line-offset-dim-paint`, `ghost-face-dim-paint` (thread `sceneUnits` αντί derived `mmPerScene`· 3 signatures), `column-hud-paint` (4 sites `/f`, αφαιρέθηκαν 2 τοπικά `const f`). +5 jest (mm/m/cm identities + bridge-equivalence σε 5 units + opts forwarding)· ghost-face-dim 4/4 regression GREEN. Καθαρά DISPLAY (μηδέν αλλαγή geometry). tsc SKIP (N.17). Co-staged ADR-363 + ADR-040 (CHECK 6D preview files). ✅ Google-level: YES — ΕΝΑ conversion home, μηδέν διπλότυπο idiom, behavior-preserving.
- **2026-06-16 (Opus 4.8) — Phase 3: 3Δ STRUCTURAL converters canonical-mm scaling (UNCOMMITTED, tsc clean, νέο jest GREEN):** Bug Giorgio «κολώνα 40×40 h3m φαίνεται 2Δ αλλά ΟΧΙ 3Δ». Ρίζα: το browser-verify της Phase 1 ήταν **μόνο 2Δ**· οι 3Δ structural converters υπέθεταν ότι τα plan vertices (footprint/outline/axis) είναι «**already in meters**» (ίσχυε ΜΟΝΟ όταν `sceneUnits='m'`)· τα τροφοδοτούσαν RAW στο `THREE.Shape` και κλιμάκωναν μόνο τα κατακόρυφα scalars με `MM_TO_M`. Με canonical-mm (footprint πλέον σε mm) → κολώνα/δοκάρι/πλάκα/τοίχος **400m×400m τοποθετημένα ~1000× μακριά → εκτός frustum → αόρατα**. (Οι σιβλινγκ converters σκάλα/κιγκλίδωμα/ΗΜ/δάπεδο/στέγη ήδη κλιμάκωναν σωστά με `sceneUnitsToMeters` — μόνο η structural οικογένεια όχι.)
  - **FIX (mirror του sibling pattern):** σε κάθε structural converter, `sceneToM = sceneUnitsToMeters(params.sceneUnits ?? 'mm')` στην είσοδο και κλιμάκωση των plan XY (×sceneToM) πριν τη γεωμετρία· τα κατακόρυφα scalars (×MM_TO_M) αμετάβλητα. Αναλλοίωτο: `mmToSceneUnits(u)·sceneUnitsToMeters(u)=MM_TO_M`. Αρχεία: `bim-three-structural-converters` (column/beam[+cutback host footprints]/slab), `BimToThreeConverter` (wall solid + pieces/clip quad **και** clip host footprints· διορθώθηκε το stale «already in meters» header invariant + το λάθος σχόλιο ότι `wall.params.height` είναι μέτρα — είναι mm), `foundation-to-three`, `slab-multilayer-solid-3d` (+`pushHoles` sceneToM param), `beam-ishape-geometry` (lenPlan+origin), `structural-finish-3d` (band quad), `structural-finish-horizontal-3d` (poly.outer+holes· αφαιρέθηκε το `void sceneUnits`), `column-rebar-3d` (canvas-unit `columnLocalMmToWorld` outputs ×sceneToM· ακτίνες ράβδων = mm Ø ×MM_TO_M). `bim-three-shape-helpers.pushHoles` πήρε προαιρετικό `sceneToM=1`.
  - **SSoT consolidation (Giorgio SSoT audit):** το επαναλαμβανόμενο `pts.map(p => ({...p, x:p.x*k, y:p.y*k}))` (ΟΛΑ τα παραπάνω + ήδη σκόρπιο σε siblings) → NEW `scalePoints(points, scalar)` στον **κανονικό vector-math SSoT** `rendering/entities/shared/geometry-vector-utils.ts` (array-sibling του υπάρχοντος `scalePoint`, preserve z). ΟΛΑ τα 18 δικά μου call sites importάρουν από εκεί. Siblings (`bim-three-point-converters` ×8, `floor-finish-to-three`, `mep-underfloor`) → pending-ratchet migrate-on-touch. (Αρχικά μπήκε λάθος ως `scalePlanXY` σε `bim-three-shape-helpers`· consolidated → μηδέν διπλότυπο.)
  - **🔑 Linchpin slope/tilt (`mesh-slope-shear.ts`):** `applyColumnTilt`/`applyWallTilt` (επιστρέφουν dx/dy στη μονάδα του ύψους=μέτρα) + `applyBeamSlope` (`beamSlopeOffsetZmm` scale-invariant) → **αμετάβλητα**. ΜΟΝΗ αλλαγή: `applySlabSlope` διαιρεί τα (πλέον σε μέτρα) plan coords ÷sceneToM πριν το `slabSlopeOffsetZmm` (ΔΕΝ είναι scale-invariant) — αλλιώς η κλίση πλάκας 1000× λάθος.
  - **Tests:** NEW `bim-3d/converters/__tests__/units-canonical-mm.test.ts` (κολώνα 40×40 h3m σε position 50m,30m → world size 0.4×0.4×3m, center ~(50,_,−30)m· flat slab 5m extent· tilted-slab guard = slope match με ÷sceneToM). `slab-slope-mesh`/`structural-finish-3d` GREEN. tsc clean. ⚠️ **Pre-existing env breakage (ΟΧΙ δικό μου):** 16 converter jest suites «fail-to-run» με `fetch is not defined` — import chain `column-rebar-3d → active-reinforcement → structural-settings-store → building-mutation-gateway → firestore auth` (μπήκε στο commit 5ab8033d ADR-459). Τρέχουν με fetch-polyfill. `wall-column-base-offset-y` fail επίσης pre-existing (default finish/reinforcement → `columnToMesh` επιστρέφει Group, το test ελέγχει wrapper position.y). **DEFER (σπάνια paths, μη-αγγιγμένα):** curved-wall-ΜΕ-ανοίγματα (`wall-opening-extrude.ts`/`wall-piece-geometry.ts`), `EnvelopeToThree`· tilted-wall+attached+clip combo (browser-verify). 🔴 browser-verify (κολώνα/δοκάρι/τοίχος εμφανίζονται σωστά 3Δ) + commit.
- **2026-06-16 (Opus 4.8) — Follow-up 3: ΑΛΗΘΙΝΟΣ ενεργός ruler (3ος renderer) + BIM entity-count (UNCOMMITTED, 4 jest + tsc):** Δύο bugs από browser-verify Giorgio.
  - **BUG A — χάρακες ΑΚΟΜΑ έδειχναν mm ενώ selector «m».** Το Follow-up 2 (παρακάτω) **εντόπισε λάθος** τον ενεργό renderer: σύνδεσε τον `canvas-v2/layer-canvas/layer-grid-ruler-renderer.ts`, ο οποίος όμως είναι **ΝΕΚΡΟΣ** (`@deprecated`· το `renderRulers` δεν καλείται — ο `LayerRenderer.renderLegacy` ζωγραφίζει μόνο grid, και το `uiComposite` του registers μόνο snap/selection). Runtime-trace της 2Δ pipeline: ο **ΠΡΑΓΜΑΤΙΚΑ ΟΡΑΤΟΣ** χάρακας είναι ο **τρίτος** renderer `rendering/ui/ruler/RulerRenderer.ts` — instantiated στο `DxfCanvas.tsx:235`, drawn κάθε frame στο `dxf-canvas-renderer.ts:279` (gated σε `rulerSettings.enabled`). (Ο `systems/rulers-grid` `useRenderingCalculations.renderRulers` υπολογίζεται αλλά **δεν καλείται** πουθενά — επίσης νεκρός.) FIX: X & Y tick labels του `RulerRenderer` → `formatCoordinateForDisplay(world, {withUnit:false})` + unit → `currentDisplayUnitLabel()` (ίδιο pattern με το sibling)· αφαιρέθηκε το πλέον-αχρείαστο `calculateLabelPrecision` (το display SSoT ορίζει precision). Live μέσω `markAllCanvasDirty` (`setDisplayUnit`). ⚠️ `RulerRenderer.ts` = canvas drawing file → staged ADR-462 (CHECK 6D).
  - **ΕΝΟΠΟΙΗΣΗ τριπλότυπων ruler renderers (Giorgio: «ενοποίησε τα τριπλότυπα»· ο shared-tree agent τελείωσε):** Υπήρχαν **ΤΡΕΙΣ** ruler **drawing** renderers — δύο νεκροί (μηδέν callers) που παραπλάνησαν 2 προηγούμενες απόπειρες wiring. ΔΙΑΓΡΑΦΗΚΑΝ τα dead drawing paths, μένει **ΕΝΑΣ** (`RulerRenderer.ts`):
    1. `canvas-v2/layer-canvas/layer-grid-ruler-renderer.ts` → αφαιρέθηκαν `renderRulers`+`renderHorizontalRuler`+`renderVerticalRuler` (κρατήθηκε το ζωντανό `renderGrid`)· καθαρίστηκε το import στο `LayerRenderer.ts` (`renderGrid` μόνο) + dead imports.
    2. `systems/rulers-grid/`: αφαιρέθηκε `useRenderingCalculations.renderRulers` (interface+callback+return) + `RulersGridRendering.renderRuler` (utils.ts) + dead imports (COORDINATE_LAYOUT/RulerSettings/RulerTick/RENDER_LINE_WIDTHS/buildUIFont/UI_SIZE_DEFAULTS/AXIS_DETECTION/RIGHT_ANGLE + unused RulersGridSnapping local-import· **re-export RulersGridSnapping διατηρήθηκε** — το χρησιμοποιεί `useSnapManagement`). Type-safe: το `renderRulers` δεν ήταν member του `RulersGridContextType` (έμπαινε μόνο μέσω spread). Η tick-MATH (`ruler-calculations`/`calculateRulerTicks`) ΚΡΑΤΗΘΗΚΕ — υπολογισμός, όχι renderer. tsc clean.
  - **BUG B — κάρτες level: το πλήθος μετρούσε μόνο DXF, όχι BIM.** Το handoff premise ήταν **λάθος**: τα BIM entities **ΔΕΝ** ζουν σε ξεχωριστό store — είναι στο **ίδιο** `scene.entities` array (Wall/Column/Beam… ανήκουν στο `Entity` union· οι `use*Persistence` τα κάνουν merge `entities:[...nonX,...nextX]`). Άρα `scene.entities.length` ΗΔΗ μετρά DXF+BIM. Η αληθινή ρίζα = **reactivity/precedence** στο `LevelPanel:266`: το `levelScene ?? scene` διάλεγε το **παγωμένο** `levelScene` (useMemo deps `[levels,getLevelScene]` — ref-stable, τρέχει 1 φορά· δεν ξανατρέχει σε `setLevelScene`). Το DXF μετριόταν γιατί το import αλλάζει και τα `levels`· το BIM (μόνο `setLevelScene`) όχι. FIX: ενεργό level → προτεραιότητα στο **ζωντανό** `scene` prop (`scene ?? levelScene ?? null`)· μη-ενεργά levels κρατούν το frozen snapshot (σωστό — δεν αλλάζουν εκτός καμβά). **SSoT (ΠΛΗΡΕΣ μετά από SSoT-audit Giorgio)**: NEW pure `utils/scene-entity-count.ts` `countSceneEntities(scene)` (layer-neutral, δίπλα στο `utils/scene-units.ts` precedent — ΟΧΙ `hooks/`, ώστε να το χρησιμοποιεί και `core/command` χωρίς hooks→core smell). Migrated **και τα 5** human-facing count-tally sites: `LevelPanel`, `useSceneManager.getSceneEntityCount`, `ai-assistant/useDxfAiChat` (AI canvas context), `FullscreenView` (badge), `ClipToRegionCommand` (telemetry). ⚠️ Τα ~40 `.entities.length` ως **predicates/loops** (`===0`/`>0`/bounds) ΔΕΝ αγγίχθηκαν — διαφορετικό concept (κενότητα/iteration), όχι tally. Δεν μολύνθηκε το `LevelsContext` (θα ξανάφερνε το ADR-040 render-storm). 4 jest GREEN. 🔴 browser-verify (selector cm↔m↔mm → χάρακες αλλάζουν· πρόσθεσε κολώνα → κάρτα ενεργού level +1 live) + commit.
- **2026-06-16 (Opus 4.8) — Phase 2 ΕΝΟΠΟΙΗΣΗ: Display-MEASUREMENT SSoT (Revit-grade, FULL ENTERPRISE + FULL SSoT, UNCOMMITTED, 13 jest στο suite + tsc clean δικά μου):** Self-audit βρήκε **τρία** παράλληλα formatting συστήματα → divergence (Properties hover «500.00 cm» τελεία vs dim-pills «500,00 cm» κόμμα· area/perimeter/radius/coordinate τύπωναν **ακατέργαστα canonical-mm** χωρίς να ακολουθούν τον selector). **ΕΝΟΠΟΙΗΣΗ σε ΕΝΑ module, 3 ρητά concerns** (απόφαση Giorgio: «όπως η Revit, FULL SSoT»):
  - **Επίπεδο 1 `config/units.ts` (pure):** NEW `DISPLAY_AREA_LABELS` (mm²/cm²/m²/in²/ft²) + `DEFAULT_AREA_PRECISION` + `DEFAULT_COORDINATE_PRECISION` + `toDisplayArea(mm², unit)` (τετράγωνος συντελεστής). `formatDisplayValue` (parseable, για **editable inputs**) αμετάβλητο.
  - **Επίπεδο 2 `config/display-length-format.ts` (store-aware, locale+label, ΜΙΑ `localeNumber` διαδρομή):** NEW `formatLengthForDisplay` (core) + `formatAreaForDisplay` + `formatCoordinateForDisplay` (signed). `formatLengthMm` → thin alias (backward-compat 8 consumers + tests).
  - **Migrated read-only display callers** (length→`formatLengthForDisplay`, area→`formatAreaForDisplay`, coord→`formatCoordinateForDisplay`): `QuickPropertiesHoverPopover`· hover `edge-utils`/`radius-utils`/`text-labeling-utils`/`render-utils`(area)· `guide-annotations-renderer`· `base-entity-rendering-helpers`· renderers `Arc`(R/L)/`Circle`(D/R)/`Ellipse`(Ma/Mi+Ε+Περ)/`Rectangle`(Ε+Περ)/`Polyline`(Ε+Περ)/`circle-text-utils`(Ε+Περιφ)· `preview-entity-renderers` (circumference/perimeter/arcLength + area/sectorArea)· `DynamicInputOverlay` (read-only multi-point distance/total)· `OverlayProperties` (αφαιρέθηκε hardcoded « m²»/« m»)· `ToolbarCoordinatesDisplay` (status-bar X/Y → coordinate SSoT + **live**: subscribe και στο `displayUnitState` → repaint στο unit-switch, όχι μόνο στο επόμενο mousemove· ΕΝΑ unit label trailing).
  - **Editable inputs ΑΜΕΤΑΒΛΗΤΑ** (parseable/τελεία): `QuickPropertiesMiniPanel`, `entity-property-schema`, `useDynamicInputRealtime`.
  - **Legacy** `distance-label-utils.formatDistance`/`formatCoordinate` → `@deprecated` (μηδέν display callers πια· συμπεριφορά αμετάβλητη). Μηδέν circular-dep (`formatting/` δεν εισάγει `config/units`). ⚠️ render-path → stage ADR-462 (CHECK 6D). 🔴 browser-verify (cm↔m↔mm → ΟΛΑ τα read-only [pills/ruler/move/drag/entity length+area+perimeter/hover/X-Y coordinate] αλλάζουν live & συνεπώς· editable inputs μένουν τελεία) + commit.
  - **Follow-up 2 (browser-verify Giorgio — «χάρακες δείχνουν mm ενώ selector λέει m»):** Βρέθηκε ότι υπάρχουν **ΔΥΟ ruler renderers**: ο `systems/rulers-grid/ruler-calculations` (wired προηγουμένως) ΔΕΝ είναι ο ενεργός· ο **ΕΝΕΡΓΟΣ** είναι ο `canvas-v2/layer-canvas/layer-grid-ruler-renderer.ts` (μέσω `LayerRenderer`), που έκανε `worldX/worldY.toFixed(0)` (raw mm) + διάβαζε το legacy `settings.unit` (ruler-specific grid-setting) — ασύνδετος από τον status-bar selector. FIX: X & Y tick labels → `formatCoordinateForDisplay(world, {withUnit:false})` + unit → `currentDisplayUnitLabel()`. Πλέον ο **ενεργός** χάρακας ακολουθεί τον selector (ο selector νικάει το ruler-specific unit — Revit-style ΕΝΑ project unit). ⚠️ shared-tree αρχείο (έγκριση Giorgio· ενημερώθηκε ο άλλος agent). Live μέσω `markAllCanvasDirty`.
  - **Follow-up (SSoT audit Giorgio «μήπως διπλότυπο;»):** Επαληθεύτηκε η σχέση με το προϋπάρχον `FormatterRegistry` (ADR-082): είναι ο **generic locale engine** (DXF-agnostic), το display-measurement module είναι το **binding layer** από πάνω (delegate-άρει το locale στο `FormatterRegistry.formatDistance`) → **ΕΝΑ** locale engine + **ΕΝΑ** display-unit home, όχι δύο. Τα **dead** (μηδέν callers) generic convenience `FormatterRegistry.formatArea`/`formatCoordinate`/`formatRadius`/`formatDiameter` (+ `useFormatter` returns + `distance-label-utils.formatDistanceLocale`/`formatCoordinateLocale`) → `@deprecated` με redirect στο SSoT (static template/χωρίς selector → αποφυγή μελλοντικού διπλότυπου). Ρητή τεκμηρίωση engine↔binding σε `display-length-format.ts` + `formatting/index.ts`. Απόφαση: ΟΧΙ display-unit awareness μέσα στο generic engine (θα μόλυνε με DXF UI-store dependency)· ΟΧΙ hard-delete δηλωμένου ADR-082 API (regression risk)· deprecate+redirect = enterprise-σωστό.
- **2026-06-16 (Opus 4.8):** Phase 1 core implemented (import scaling στο `DxfSceneBuilder.buildScene` via `scaleEntity` SSoT + `resolveImportSourceUnits` + `resolveSceneUnits` trust + override threading client/worker). 10 jest, tsc clean. Pending browser-verify (φρέσκο import → κολώνα σταθερό μέγεθος σε όλους τους ορόφους) + commit.
- **2026-06-16 (Opus 4.8) — BROWSER-VERIFIED (καθαρή βάση, Firestore-first):** 3/3 σταδιακοί έλεγχοι πέρασαν.
  - (1) Setup project→building→όροφοι OK.
  - (2) Import ξυλότυπου θεμελίωσης (`Σ-1_ΞΥΛΟΤΥΠΟΣ_ΘΕΜΕΛΙΩΣΗΣ`, Windows-1253, meter-DXF): bounds **35401×28609 mm = 35.4×28.6 m** (`units:'mm'`)· χαρακτηριστικό μήκος μετρήθηκε **9750 mm = 9.75 m ακριβές** (με snap σε άκρα). Καμία 1000× απόκλιση.
  - (3) Κολώνα 40×40 σε Ισόγειο-με-κάτοψη (`_AfrPolGD`, meter-DXF 21×15m→mm) + σε άδειο όροφο: `col_30b6a07d` → **`sceneUnits:'mm'`** (ΟΧΙ `'m'` — η ρίζα του bug), `width/depth=400`, footprint span **ακριβώς 400 mm**, ίδιο μέγεθος σε όλους τους ορόφους, κουμπώνει με την κάτοψη. Οπτικά + data confirmed.
  - **Συμπέρασμα:** το «κολώνα μικροσκοπική στο Ισόγειο» (1000× mismatch) λύθηκε στη ρίζα — η μονάδα έρχεται από τη σκηνή που είναι πάντα mm, ανεξαρτήτως ορόφου.
- **2026-06-16 (Opus 4.8) — Phase 2: Display-unit wiring (UNCOMMITTED, 44 jest, tsc):** Ο υπάρχων status-bar επιλογέας μονάδας (cm/m/mm/in/ft, `useDisplayUnit`) ήταν ασύνδετος — οι readouts έδειχναν ο καθένας δική του μονάδα (move=μέτρα, dim-pills=mm, drag-measure=raw mm). **SSoT design** (μηδέν circular deps): NEW `config/display-unit-state.ts` (non-React subscribable store, μοτίβο `cadToggleState`, init από localStorage) + NEW `config/display-length-format.ts` (`formatLengthMm(mm)` → display unit + locale + label, μέσω `toDisplay`+`FormatterRegistry`) + `useDisplayUnit` → `useSyncExternalStore` πάνω στο store (selector + render path = ΜΙΑ πηγή). **Wired chokepoints:** `move-readout.formatMoveDistance` (→ 2Δ pills + 3Δ `TempMoveReadoutOverlay` + move/grip previews), `bim-dim-labels` (wall L + foundation strip/tie L), `column-dim-labels` (shear-wall L), `BaseDragMeasurementRenderer.formatMeasurement` (όλα τα drag-measurements: line/circle/arc/rect/polyline). **Απόφαση Giorgio:** μήκη (L)→επιλεγμένη μονάδα, διατομές (t/w/d/h, pad footprint)→mm (Revit-style). Internal geometry canonical-mm αμετάβλητο. ⚠️ Redraw-on-change: live gestures + hover/select ξανασχεδιάζουν άμεσα· στατικά-επιλεγμένο pill ανανεώνεται στο επόμενο interaction (DEFER explicit redraw — δεν βρέθηκε καθαρό global invalidate). DEFER §4(δ) = primitive-entity distance labels + ruler + coordinate readout (hot path, browser-verify). 🔴 browser-verify (άλλαξε cm↔m → move/dim/drag readouts αλλάζουν) + commit.
- **2026-06-16 (Opus 4.8) — Phase 2 ΕΠΕΚΤΑΣΗ: Revit-grade ΕΝΑ unit SoT + live (UNCOMMITTED):** μετά από ερώτηση Giorgio («θα το έκανε έτσι η Revit;»). (1) **Ruler unified**: ο χάρακας έδειχνε raw mm + δικό του `UnitConversion.format` (μη-locale, χωρίς conversion, δεμένο σε grid-settings.units — ΟΧΙ στον επιλογέα). FIX: `ruler-calculations` tick label + `RulersGridSystem.formatValue` → `formatLengthMm`· ο `UnitConversion.format` έγινε thin **deprecated adapter** που delegate-άρει στον SSoT (μηδέν duplicate formatter)· `convert`/`getStepForUnit` (γεωμετρικά) ανέπαφα. Πλέον ruler + dimensions + measurements = ΜΙΑ μονάδα, όπως Revit/AutoCAD. (2) **Live redraw**: `useDisplayUnit.setDisplayUnit` → `markAllCanvasDirty()` → όλα τα canvas readouts switch-άρουν μονάδα ΑΜΕΣΩΣ (λύνει και το stale-pill). (3) **Primitive entity labels**: `renderDistanceLabel`/`renderDistanceLabelStyled` (distance-label-utils chokepoint) → `formatLengthMm` → Line/Circle/Arc/Ellipse/Polyline/Rectangle + preview ακολουθούν τον επιλογέα με ΜΙΑ αλλαγή. ⚠️ render-path αρχεία (distance-label-utils, rulers-grid) → stage ADR-462/ADR-040 στο commit (CHECK 6D). 🔴 browser-verify (cm↔m → ruler+entity labels+pills αλλάζουν live) + commit.
