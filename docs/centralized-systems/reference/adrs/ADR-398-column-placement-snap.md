# ADR-398 — Column placement snap (corner projection + beam-axis snap + ghost coloring)

**Status:** 🟢 Column→Beam axis snap + ghost coloring DONE · 🟢 bugfix snap-indicator εξαφάνιση στο εργαλείο «Κολώνα» — browser-verified ✅ · 🟢 §3.7 Column smart-ghost face-snap (πιστό mirror δοκαριού, reuse ADR-508 `bim/framing/`) — 14 jest GREEN · 🟢 §3.8 WYSIWYG column ghost (ΕΝΑ φάντασμα = τελική κολώνα· κατάργηση 9-anchor schematic) — 8 jest GREEN · 🟢 §3.9 wall-axis CENTER snap (κέντρο κολώνας ≡ άξονας τοίχου, mirror §3.1b, nearest-reference-wins, ΧΩΡΙΣ split) — 27 column-face-snap jest GREEN · 🟢 §3.10 sync-in-preview unification (column face-snap σύγχρονα στο preview/commit όπως τοίχος/δοκάρι· NEW `columnPreviewStore`· scheduler face-branch αφαιρέθηκε· **slab edges πλέον δουλεύουν ταυτόσημα**) — 38 jest GREEN · 🟢 §3.13 Polar Magnet (κολώνα ΜΕΣΑ στον δίσκο = πολικό πλέγμα κέντρο/δακτύλιοι/ακτίνες nice-numbers zoom-adaptive· snap κέντρο/δακτύλιο∩ακτίνα· R/θ dims· χείλος→§3.12· Shift→κλάσματα ακτίνας) — 10 jest GREEN · 🟢 §3.14 symmetry detector (n-fold auto-detect gcd + scroll override· interaction DEFER) — 8 jest GREEN · 🟢 §3.15 Cartesian Magnet (κολώνα ΜΕΣΑ σε ΟΡΘΟΓΩΝΙΟ = καρτεσιανό πλέγμα· 9-point/grid· 4 dx/dy dims· Shift→κλάσματα· λοξά+ίσια ΕΝΑ κώδικα· χείλος→§3.11) — 10 jest GREEN · 🟢 §3.16 auto-grid-fill pure (rectGridPositions m×n ίσες αποστάσεις· interaction DEFER) — 5 jest GREEN · 🟢 §3.17 **Rectangle size adoption** (κολόνα υιοθετεί μέγεθος+κέντρο+γωνία ορθογωνίου DXF στο 1ο κλικ· opt-in confirm ADR-487 §8.4· Φ1 rectangle/closed-polyline + Φ2 4-γραμμές· reuse findRectContaining + getCachedRegionPerimeters + rectFrameFromCorners· orthogonality+notable-diff guards) — 17 jest GREEN · UNCOMMITTED 2026-06-23 · 🔴 browser-verify §3.17 (1ο κλικ μέσα σε ορθογώνιο→«W×H ή default;»· Ναι=μέγεθος+κέντρο+γωνία· Όχι=40×40· λοξό=+rotation· κανένα ορθογώνιο=αμετάβλητο) + commit (Giorgio)
**Date:** 2026-06-19 (bugfix 2026-06-20) · **Γλώσσα:** Ελληνικά
**Σχετικά:** ADR-363 (column drawing tool / FSM + anchor ghosts), ADR-370/371/378 (BIM corner-snap system), ADR-040 (preview-canvas perf — cursor-lag Φ11 decoupled snap-scheduler), ADR-441 (column-on-grid host-on-snap), ADR-514 (unified BIM cursor snap — η column placement πλέον καλείται μέσω του εγκεφάλου `resolveBimCursorSnap`, Φ2)

> ⚠️ **Architecture-critical (CHECK 6D):** αγγίζει `systems/cursor/*` (snap-scheduler, mouse handlers) + ghost preview. Κάθε αλλαγή εδώ απαιτεί staged ADR/doc.

---

## 1. Πλαίσιο

Το column tool (ADR-363, freehand single-click) ζωγραφίζει ghost κάτω από το σταυρόνημα και τοποθετεί κολώνα στο κλικ. Το snap κατά την τοποθέτηση τρέχει σε **2 σημεία** (mirror): τον decoupled `snap-scheduler` (move → ghost/indicator, ADR-040 Φ11) και τον `mouse-handler-up` (click → commit point). Προϋπήρχε το **Column Body Corner Projection Snap** (η would-be κολώνα προβάλλει τις γωνίες της σε στόχους· `findColumnDrawCornerSnap`).

## 2. Πρόβλημα (Giorgio 2026-06-19)

Όταν ο χρήστης τρέχει «Κολώνα» και περνά το σταυρόνημα **πάνω από δοκάρι**, θέλει:
1. Οπτική ένδειξη ότι κλικ εκεί θα τοποθετήσει την κολώνα **πάνω στο δοκάρι**.
2. Η κολώνα να τοποθετείται **στον άξονα** του δοκαριού — **κέντρο κολώνας ≡ κέντρο άξονα δοκαριού**.

## 3. Απόφαση

### 3.1 Beam-axis projection SSoT (γέμισμα κενού)
Η οικογένεια per-member axis projection που καταναλώνει ο `NearestSnapEngine` είχε `projectPointOnWallAxis` / `projectPointOnSlabEdge` / `projectPointOnOpeningOutline` — **όχι** δοκάρι. NEW SSoT `bim/beams/beam-axis-projection.ts` (**ακριβές mirror** του `projectPointOnWallAxis`): `projectPointOnBeamAxis(beam, cursor)` (clamped nearest foot· διαβάζει cached `geometry.axisPolyline.points` → straight/curved uniform· ίδιες shared utils `getNearestPointOnLine`+`calculateDistance`) + `projectPointOnBeamAxisDetailed` (foot + distance + `atEndpoint`). Wired **`isBeamEntity` branch στο `NearestSnapEngine`** + description key `bim-beam` (`snapModes.labels.bim.beamAxis`, el+en) ⇒ **τα δοκάρια αποκτούν nearest-snap parity** με walls/slabs σε **ΟΛΑ** τα drawing tools. _(PerpendicularSnapEngine beam-feet = DEFER — εύκολη μελλοντική parity.)_

### 3.1b Column→Beam axis snap (κέντρο κολώνας ≡ άξονας δοκαριού)
NEW `bim/columns/column-placement-snap-context.ts` (column-tool orchestration **πάνω στο SSoT**, μηδέν inline geometry):
- `findColumnBeamAxisSnap(worldPos, beams)` — **reuse `projectPointOnBeamAxisDetailed`**· υποψήφιο όταν `atEndpoint===false` (πάνω στο σώμα, όχι πέρα από τα άκρα) ΚΑΙ `distance ≤ halfWidth · 1.5` (width-based ⇒ **transform-independent**)· νικά το μικρότερο.
- `resolveColumnPlacementContext(worldPos, entities)` → **precedence `overlap` > `beam` > `neutral`**: αν ο cursor είναι μέσα σε footprint **υπάρχουσας κολώνας** → `overlap` (🔴 — μην βάλεις διπλή· ισχύει **ΑΚΟΜΗ κι όταν από κάτω περνά δοκάρι**, π.χ. ενδιάμεση κολώνα στη μέση δοκαριού)· αλλιώς πάνω σε **κενό** σώμα δοκαριού → `beam` (🟢)· αλλιώς `neutral`. _(Το «δοκάρι νικά» αφορά τα snap **targets** grid/corner — όχι το collision warning έναντι υπάρχουσας κολώνας.)_

Wiring (mirror του corner-snap, ΕΝΑ SSoT σε 2 paths):
- **Move** (`snap-scheduler.runSnapDetection`): column active → `resolveColumnPlacementContext`· `beam` ⇒ `setImmediateSnap` στο σημείο του άξονα (+ beamId) και short-circuit του corner/center snap. `mouse-handler-move` τροφοδοτεί `getEntities`.
- **Commit** (`mouse-handler-up`): column active → beam-axis νικά → `worldPoint = beam point`.
- **Center enforcement** (`useColumnTool.commitColumnFromState`): όταν ghost status = `beam` → anchor **`'center'`** ανεξάρτητα από τον επιλεγμένο anchor ⇒ κέντρο κολώνας ≡ άξονας δοκού.

### 3.2 Ghost coloring (σημασιολογικό feedback στο ghost, ΟΧΙ στην υποκείμενη οντότητα)
Ο χρωματισμός γίνεται στο **ghost** (η προσοχή είναι εκεί· Revit/AutoCAD pattern· δεν αγγίζει τον select-gated entity-hover renderer):
- 🟢 **πράσινο** = πάνω σε δοκάρι (έγκυρος snap στόχος)
- 🔴 **κόκκινο** = πάνω σε υπάρχουσα κολώνα (σύγκρουση/επικάλυψη — τοποθετείται ακόμη, προειδοποίηση)
- ⚪ **default (χρώμα τύπου)** = ελεύθερη τοποθέτηση

NEW zero-React store `systems/cursor/ColumnPlacementGhostStatusStore.ts` (mirror `ImmediateSnapStore`): ο scheduler γράφει το status· το `useColumnGhostPreview` το διαβάζει **imperatively μέσα στο RAF** (zero subscription, ADR-040) και περνά `statusColor` στον `ColumnAnchorGhostRenderer` (νέο optional override, `resolveGhostStatusColor`).

### 3.3 Smart beam ghost — φάντασμα δοκαριού στις παρειές κολόνας (mirror, 2026-06-20)
Καθρέφτης του column ghost για το εργαλείο «Δοκάρι»: μόλις ενεργοποιηθεί, **πριν το 1ο κλικ** εμφανίζεται μικρό έξυπνο φάντασμα δοκαριού. Κοντά σε **ορθογώνια κολόνα** κουμπώνει σε μία από **12 διακριτές θέσεις** (4 παρειές × 3 αγκυρώσεις, το φάντασμα ΠΗΔΑΕΙ — όχι συνεχές γλίστρημα)· μακριά → ακολουθεί ελεύθερα τον κέρσορα.

**Κρίσιμη αρχιτεκτονική διαφορά από το column ghost:** η orientation/anchor είναι **καθαρά γεωμετρική** (από τα column footprints), ΟΧΙ από snap-result → **μηδέν επαφή με snap-scheduler / cursor stores / renderer core** (κανένα ADR-040 critical file, CHECK 6B/6C/6D). Όλη η λογική ζει στο **pure preview path** + στο click handler του tool.

- **Pure resolver** `bim/beams/beam-column-face-snap.ts` (`resolveBeamColumnFaceSnap` + wrapper `resolveBeamGhostSnapFromStore`): cursor + column footprints → `{ face:'E'|'W'|'N'|'S', third:'lo'|'mid'|'hi', start, end }` (centerline) ή `null`. **Reuse `projectPolygonOnAxis`** (extents, N.0.2) + `mmToSceneUnits`. Σημασιολογία: E/W → οριζόντιο δοκάρι, N/S → κάθετο· κοντινό short-end flush στην παρειά· `lo`/`hi` → γωνία-flush (πλάγια παρειά δοκού ≡ γωνιακή παρειά κολόνας), `mid` → centerline ≡ κέντρο κολόνας.
- **Centerline vs location-line:** ο resolver επιστρέφει το ΤΕΛΙΚΟ centerline `start`. Το 1ο κλικ κλειδώνει εκεί + σημαία `beamPreviewStore.startAnchored=true`· τότε το awaitingEnd preview/commit χρησιμοποιεί `buildDefaultBeamParams` (**centerline mode**), ΟΧΙ το location-line auto-flush (`buildAnchoredBeamParams`) που θα ξανα-μετατόπιζε το start ±width/2 σε διαγώνιο τράβηγμα. Free placement → αμετάβλητη location-line συμπεριφορά (ADR-363 §5.7).
- **Wiring (μηδέν duplicate, preview === commit):** ΕΝΑ wrapper καλείται και από `beam-preview-helpers` (ghost-before-click) και από `useBeamTool.onCanvasClick` (1ο κλικ). `syncColumnsToStore()` τρέχει πλέον και στο `activate` (κολόνες διαθέσιμες πριν το κλικ). **2ο κλικ ΕΛΕΥΘΕΡΟ** (διαγώνιο/ORTHO) — ο άξονας ΔΕΝ κλειδώνει.

### 3.4 Beam ghost +Y offset — canonical viewport SSoT στο WYSIWYG BIM preview (render-path, 2026-06-20)
**Πρόβλημα (preview-only):** το ghost δοκαριού (WYSIWYG, μέσω πραγματικού `BeamRenderer`) εμφανιζόταν με **σταθερό μικρό offset προς βορρά (+Y)** σε όλες τις παρειές/αγκυρώσεις — το committed δοκάρι ευθυγραμμιζόταν σωστά. Ομοιόμορφη μετατόπιση όλου του ghost, ανεξάρτητη zoom/πλάτους.

**Ρίζα (αιτιακή, όχι geometry):** ο y-flip είναι `screenY = (viewport.height − top) − worldY·scale − offsetY`. Ο beam ghost ζωγραφίζεται στο **shared PreviewCanvas** αλλά οι πραγματικοί entity renderers μετρούσαν το `viewport.height` από το **δικό τους** `ctx.canvas.getBoundingClientRect()` (= PreviewCanvas), ενώ **όλα τα σωστά** (committed δοκάρι, column ghost μέσω `getViewportElement()`) μετρούν από το **DxfCanvas / container rect** (= prop `viewport` = `containerRef.getBoundingClientRect`, `useViewportManager`). Το PreviewCanvas έχει inline `style.width/height` (από `PreviewRenderer.updateSize`) που αποκλίνει λίγα px από το container → σταθερό +Y. **Διπλοτυπία πηγής viewport:** δύο preview paths (imperative composite vs RAF-direct column ghost) έλυναν το «canonical viewport» με διαφορετικό τρόπο.

**Λύση (ενοποίηση σε ΕΝΑ canonical viewport — N.0.2):** NEW `setViewportOverride(viewport|null)` στο `BaseEntityRenderer` (όταν set → `getViewport()` το επιστρέφει αντί για getBoundingClientRect) + forwarding στο `EntityRendererComposite`. Το `BimPreviewRenderer.render(entity, transform, viewport)` εγχέει το canonical `viewport` (περασμένο από `PreviewRenderer` = prop viewport) πριν το render και το καθαρίζει σε `finally`. **Μηδέν regression:** override=`null` στο main render path → αμετάβλητη getBoundingClientRect συμπεριφορά. Τώρα ο beam ghost μετρά στο ΙΔΙΟ viewport με το committed δοκάρι + column ghost. Λύνει ΚΑΙ το «άξονας λίγα px πιο βόρεια από το σταυρόνημα» (ίδια αιτία).

### 3.5 Preview-render unification — ΕΝΑ canonical frame + ΕΝΑ ghost harness (2026-06-20)
Το §3.4 hotfix έλυσε το σύμπτωμα· το §3.5 εξαλείφει την **κλάση** του bug + τη **διπλοτυπία** που τον γέννησε (Revit-grade, ο Giorgio ζήτησε πλήρη ενοποίηση). Δύο SSoT, ΟΧΙ ένας mega-renderer (τα ghosts έχουν νόμιμα διαφορετική οπτική):

- **SSoT-A — canonical preview frame** (`systems/preview/ghost-preview-frame.ts`): `getCanonicalPreviewFrame(viewportElement)` → `{ viewport, transform }` από **ΜΙΑ** πηγή — viewport μέσω `canvasBoundsService.getBounds` (cached, ΙΔΙΑ πηγή με τον main `DxfRenderer`), transform μέσω live `getImmediateTransform()` (zero-lag, ΙΔΙΟ με main canvas + ProposalGhostOverlay). + uniform `GhostDrawFrame { ctx, effectiveCursor, viewport, transform }` + `GhostDrawDelegate`. Διορθώνει ΚΑΙ το **latent transform-lag**: μέχρι τώρα τα RAF-direct previews έπαιρναν React-prop transform (1-2 frames πίσω από τον main canvas σε pan/zoom).
- **SSoT-B — ghost harness** (`hooks/tools/useCanvasGhostPreview.ts`): κατέχει το copy-pasted scaffolding (~19 hooks) — cursor-gate, RAF lifecycle, DPR-clear, clear-on-exit, snapped-cursor, canonical frame. Config: `{ isActive, getCanvas, getViewportElement, transform, cursorMode?, useImmediateSnap?, clearMode?, draw }`. Modes: `cursorMode 'none'` (grip ghosts — cursor μέσω data), `clearMode 'skip-clear'` (`useGripDimAnnotation` — layered πάνω στο grip-ghost frame).
- **Migrated 19 hooks:** column/opening/slab-opening + 7 MEP (fixture/water-heater/radiator/electrical-panel/boiler/manifold/segment) + rotation/move/mirror/scale/stretch/trim/extend/grip-ghost/grip-dim. Κάθε hook → ΜΟΝΟ το `draw` delegate + το gate του (draw logic IDENTICAL, byte-for-byte οπτικά· ~600 γρ scaffolding διαγράφηκαν). `transform` prop μένει ως **redraw-trigger** (programmatic zoom/fit)· η ΖΩΓΡΑΦΙΖΟΜΕΝΗ τιμή = live transform.
- **ΟΧΙ migrated (by design):** ο imperative composite path (PreviewCanvas drawPreview — §3.4 viewport-override) + τα dedicated-canvas ProposalGhostOverlay (ήδη live transform). Διαφορετικός lifecycle.

### 3.6 Beam-to-beam framing — smart ghost ΚΑΙ πάνω σε υφιστάμενα δοκάρια (κατευθυντικό 🟢/🔴, 2026-06-20)
Επέκταση του §3.3 smart beam ghost: το φάντασμα κουμπώνει **και πάνω σε υφιστάμενα δοκάρια** (όχι μόνο κολόνες), με **κατευθυντικό χρωματισμό** (mirror του 🟢/🔴 column ghost §3.2). Δομικό σκεπτικό (Giorgio, πολιτικός μηχανικός): **Τ-junction** δευτερεύοντος σε μακριά παρειά κυρίου = standard beam-to-beam framing (η Revit το κάνει καθημερινά) → 🟢· **συγγραμμικά** άκρη-με-άκρη ή σώμα-σε-σώμα = θα έπρεπε να είναι ΕΝΑ δοκάρι → 🔴 (extend instead).

| Θέση cursor vs υφιστάμενο δοκάρι | Status | Συμπεριφορά ghost |
|---|---|---|
| **Μακριά παρειά** (Β/Ν) | 🟢 `beam` | κάθετο Τ-framing, ολίσθηση σε όλο το μήκος + μαγνήτες άκρα/μέσα |
| **Κέντρο άξονα** | 🟢 `beam` | auto-snap στην **πλησιέστερη** παρειά (απόφαση «Α» — ομαλό UX, ποτέ «καβάλα») |
| **Κοντή άκρη** (Α/Δ) | 🔴 `overlap` | συγγραμμική συνέχεια — «extend instead» (κόκκινο schematic ghost) |

**Κρίσιμη γεωμετρική διαφορά από το column ghost (§3.3):** τα δοκάρια είναι **μακρόστενα & πιθανώς υπό γωνία** → το ⊥/∥ κρίνεται από τον **άξονα** του υφιστάμενου δοκαριού, ΟΧΙ από axis-aligned bbox. Νέο **axis-relative** pure resolver, δεν «χώνεται» στον bbox-based column resolver.

**FULL SSoT (extract/generalize/reuse — μηδέν διπλότυπα):**
- **EXTRACT** η παλέτα 🟢/🔴 + ο `drawGhostOutline`/`drawGhostFill` βγήκαν από το (ADR-040 critical) `ColumnAnchorGhostRenderer` σε **ουδέτερα** `bim/ghosts/ghost-status-color.ts` (`GhostStatus`/`GhostStatusColor`/`resolveGhostStatusColor`) + `bim/ghosts/ghost-status-polygon-draw.ts` (`strokeGhostPolygon`/`fillGhostPolygon`/`drawStatusGhostPolygon`). Column renderer τα re-exports (back-compat `useColumnGhostPreview`) + delegate-άρει. **Beam→column-renderer coupling = 0.**
- **GENERALIZE** `findColumnOverlap` → ουδέτερο `bim/geometry/entity-overlap.ts` (`findEntityOverlap(pos, entities, getPolygon)` — extractor-driven, ίδιος point-in-polygon για κολόνα/footprint & δοκάρι/outline)· `findColumnOverlap` delegate.
- **REUSE** `projectPolygonOnAxis` (signed perp = μακριές παρειές + κοντές άκρες· straddle) + `projectPointOnAxis` (cursor along) — **μηδέν** νέο projection primitive.
- **NEW** `bim/beams/beam-beam-face-snap.ts` (`resolveBeamBeamFaceSnap` + `BeamSnapTarget`/`BeamGhostSnapResult`): cursor πάνω στο σώμα → 🟢 ⊥ Τ-framing στην πλησιέστερη παρειά (ολίσθηση κατά μήκος μέσω `tAlong`)· πέρα από κοντή άκρη → 🔴 συγγραμμική συνέχεια.

**Wiring (preview === commit, ΕΝΑ dispatcher):** ο `resolveBeamGhostSnapFromStore` έγινε **dispatcher** — **column-priority** (κοντά σε κολόνα η παρειά κολόνας νικά → status `neutral`, διατηρεί ΑΚΡΙΒΩΣ το §3.3 behavior)· αλλιώς beam-to-beam (🟢/🔴). `beamPreviewStore` απέκτησε `beamTargets` + `setBeams` (mirror `columnFootprints`/`setColumns`)· `useBeamTool.syncSceneTargetsToStore` (πρώην `syncColumnsToStore`) συγχρονίζει **και** δοκάρια (axis+outline) on activate / κάθε 1ο κλικ.

**Μαγνήτες άκρα/μέσα (Slice 2 — reuse, μηδέν νέος κώδικας):** το `makeBeamGhostBeforeClick` ήδη διαβάζει `getImmediateSnap()` (§3.4 fix)· η BIM characteristic snap (`BimCharacteristicSnapEngine`, ADR-370 — `BIM_CORNER`/`BIM_MIDPOINT` για **όλες** τις BIM οντότητες incl. δοκάρια) δίνει τα endpoint/midpoint· ο snapped cursor → το ghost κουμπώνει στο άκρο/μέσο.

**3-ζωνική δικαιολόγηση πλάτους (Slice 5, Giorgio §3.6):** το κάθετο ghost ΜΕΝΕΙ ίσιο (90°) — ΜΕΤΑΤΟΠΙΖΕΤΑΙ κατά τον άξονα u ώστε το σταυρόνημα να πέφτει στην **αρχή/μέση/τέλος του πλάτους** του, ανάλογα σε ποιο **τρίτο** του υφιστάμενου δοκαριού βρίσκεται (mirror των 3 αγκυρώσεων κολόνας). **ΒΟΡΕΙΑ:** αριστερό τρίτο → δοκάρι δεξιά (cursor=αριστερή ακμή, centerline `+half`)· μεσαίο → κεντραρισμένο· δεξί → δοκάρι αριστερά (`−half`). **ΝΟΤΙΑ: αντίστροφα.** **SSoT:** `pickThird` εξήχθη σε NEW zero-import leaf `bim/beams/beam-face-third.ts` (cycle-proof — το `beam-column-face-snap` εισάγει το `beam-beam-face-snap`)· **και** οι δύο resolvers το κάνουν reuse, μηδέν διπλότυπο. `beamWidthScene` περνά στο opts του resolver.

**🔴 render (απόφαση red-only, §3 spec):** μόνο το `overlap` «ψήνει» `ghostStatusColor` στο preview entity· ο `PreviewRenderer` (ADR-040 critical) ζωγραφίζει κόκκινο schematic (outline + 30% fill) μέσω `drawStatusGhostPolygon` αντί WYSIWYG. 🟢 `beam` & free → WYSIWYG amber αυτούσιο (το πραγματικό δοκάρι δείχνει ήδη πού πατάει). _(Επιλογή Giorgio = Α: 🟢=WYSIWYG amber, ΟΧΙ πράσινο schematic — Revit-grade, το πραγματικό δοκάρι δείχνει το νόημα μόνο του.)_

**ΑΠΑΓΟΡΕΥΣΗ ομοαξονικού/πάνω-σε-υφιστάμενο (2026-06-20, Giorgio order — Slice 4):** δεύτερο δοκάρι ομοαξονικά/πάνω σε υπάρχον = duplication → **🔴 + μπλοκάρισμα** (όχι απλή προειδοποίηση· πιο αυστηρά από Revit που μόνο warn-άρει — Giorgio θέλει καθαρά μοντέλα). NEW pure `isBeamCollinearOverlap(start, end, targets)` — **2 κριτήρια, FULL SSoT reuse**: (1) παράλληλοι άξονες `|u·u|≥0.985` (αποκλείει κάθετο Τ-framing), (2) ο **άξονας** του νέου περνά **μέσα** από το outline του υφιστάμενου μέσω του υπάρχοντος SSoT **`coveredIntervals`** (`bim/geometry/shared/segment-polygon-coverage.ts` — segment ∩ polygon, ADR-401/449, ήδη consumed από wall host-plan + structural-finish). Παράλληλο δίπλα (άξονας εκτός outline)→coverage 0→false· άκρο-με-άκρο άγγιγμα→0→νόμιμη επέκταση. **ΟΧΙ hand-rolled overlap** (το αρχικό draft είχε along-range+perp-distance — soft-duplicate του `coveredIntervals`· αφαιρέθηκε στο SSoT audit). Wire: awaitingEnd rubber-band ghost → 🔴 schematic (`makeBeamWysiwygGhost`)· commit → **silent block** (`commitTwoClickFromState` return false — το κόκκινο ghost ΕΙΝΑΙ το feedback· μηδέν i18n/toast, shared-tree-safe).

**Staleness fix (2026-06-20 — Slice 4):** ρίζα του «ομοαξονικό κίτρινο ghost, κόκκινο μόνο μετά το κλικ»: το `syncSceneTargetsToStore` έτρεχε on activate + on 1st-click, και το `drawing:entity-created` re-sync διάβαζε **stale** σκηνή (το event εκπέμπεται ΣΥΓΧΡΟΝΑ μέσα στο `setLevelScene` → React state όχι ακόμη committed) → ένα δοκάρι μόλις σχεδιασμένο δεν ήταν στους targets κατά το hover → resolver `null` → εφεδρικό **free οριζόντιο** ghost (έπεφτε ομοαξονικά, κίτρινο). Στο κλικ η σκηνή είχε κάτσει → κόκκινο. FIX: `useBeamTool` subscribe σε `drawing:entity-created` με **rAF defer** (τρέχει μετά το React commit → φρέσκα targets ΠΡΙΝ το hover· consistent με το rAF-defer pattern των proactive hooks). Πλέον on-body hover → 🟢 κάθετο, ομοαξονικό → 🔴 πριν το κλικ.

**North-star (ADR-487):** το Τ-junction beam→beam είναι σύνδεση του ζωντανού οργανισμού — αργότερα ο οργανισμός θα ξαναϋπολογίζει φορτία/διατομές/οπλισμό για το νέο Τ· το geometry/topology κρατιέται καθαρό (axis-relative, reuse projection SSoT).

### 3.7 Column smart-ghost face-snap — η κολώνα κουμπώνει στις παρειές (πιστό mirror δοκαριού, 2026-06-20)

**Giorgio order:** το εργαλείο «Κολώνα» να αποκτήσει την ίδια «έξυπνη» συμπεριφορά φαντάσματος με το δοκάρι (§3.3/§3.6), **πιστά προσαρμοσμένη** σε σημειακό (1-κλικ) μέλος, με ΕΝΑ SSoT (reuse του `bim/framing/` ADR-508).

Κοντά σε παρειά υφιστάμενου **δοκαριού, τοίχου ή κολώνας** (ίδια συμπεριφορά — όλα γραμμικά/footprint μέλη), το φάντασμα της κολώνας:
- **κουμπώνει** στην παρειά και αναπτύσσεται **εξωτερικά** (το σώμα στην έξω πλευρά)·
- **γλιστράει** κατά μήκος της (continuous slide — η `position` ακολουθεί τον — ήδη snapped — cursor)·
- η ζώνη (lo/mid/hi via `pickThird`) επιλέγει **ΑΥΤΟΜΑΤΑ** ποια από τις 9 λαβές ακουμπά: γωνία (lo/hi) → flush-corner, μέσον (mid) → κεντραρισμένη — ο σημειακός ισοδύναμος της «ανάπτυξης Α/Δ» του δοκαριού·
- χρωματισμός: μακριά παρειά → 🟢 `beam` (valid attach)· **κοντή άκρη ΔΟΚΑΡΙΟΥ** → 🔴 `overlap` (mirror «extend instead»)· **κολώνα ΚΑΙ ΤΟΙΧΟΣ → ΟΛΕΣ οι παρειές 🟢** (Giorgio: σε κάθε πλευρά τοίχου, **και τις μικρές άκρες**, επιτρέπεται κολώνα — μόνο το δοκάρι έχει 🔴 κοντές άκρες)·
- **έλξεις/γλυφές (`resolveColumnFaceSnapWithGlyph`):** όταν ο cursor πλησιάζει **ορατό BIM χαρακτηριστικό** (Γωνία/Μέσο/Κέντρο τοίχου/δοκαριού/κολόνας — μέσω του ενιαίου `findSnapPoint`), το ghost κάνει **μαγνητική έλξη** εκεί ΚΑΙ εμφανίζεται η γλυφή + ετικέτα («ΓΩΝΙΑ ΤΟΙΧΟΥ»/«ΜΕΣΟ ΤΟΙΧΟΥ»)· μακριά από χαρακτηριστικό → ελεύθερο slide χωρίς γλυφή (σιωπηλό grid/guide αγνοείται, AutoCAD convention).

**Anchor mapping** (παρειά στόχου × ζώνη → λαβή· η απέναντι ακμή της κολώνας ακουμπά, σώμα εξωτερικά):
| Παρειά | `lo` | `mid` | `hi` |
|---|---|---|---|
| **N** (σώμα βόρεια) | `sw` | `s` | `se` |
| **S** (σώμα νότια) | `nw` | `n` | `ne` |
| **E** (σώμα ανατολικά) | `sw` | `w` | `nw` |
| **W** (σώμα δυτικά) | `se` | `e` | `ne` |

**FULL SSoT — μηδέν νέο engine/geometry, μηδέν διπλότυπο:**
- **EXTRACT** κοινό geometry SSoT `bim/geometry/shared/footprint-face-frame.ts` (`footprintBounds` + `distanceToFootprintBounds` + `pickDominantFace`) — το bbox/face primitive που ΠΡΙΝ ήταν **διπλό** (private `columnBounds`/`distanceToBounds` + inline ex/ey στο `member-column-face-snap` **και** ξανά στην 1η μου έκδοση column). Πλέον ζει ΜΙΑ φορά· **και τα δύο** (`member-column-face-snap` δοκάρι/τοίχος **ΚΑΙ** `column-face-snap` κολώνα) το καταναλώνουν → πραγματική μία πηγή αλήθειας (byte-for-byte: `footprintBounds` = το παλιό `columnBounds` μέσω `projectPolygonOnAxis`).
- NEW pure `bim/columns/column-face-snap.ts` (`resolveColumnFaceSnap`) — **reuse** `collectMemberSnapTargets` (στόχοι, `memberKinds:['beam','wall']` → κολόνες+δοκάρια+**τοίχοι**, ίδια συμπεριφορά φαντάσματος), το νέο `footprint-face-frame` SSoT, `pickThird` (zone), `MEMBER_GHOST_CAPTURE_MM` (ίδιο capture 600). Η τελική γεωμετρία (anchor offset) παράγεται από το υπάρχον `computeColumnGeometry` — **κανένα νέο geometry**.
- **ΕΝΑ SSoT για move ΚΑΙ click** (preview === commit): NEW `resolveColumnFaceSnapWithGlyph` (στο `column-placement-snap-context`) = `findSnapPoint` → ορατό χαρακτηριστικό ως effective cursor (μαγνητική έλξη) + `resolveColumnFaceSnap`. Καλείται από `snap-scheduler.runSnapDetection` (highest priority → `setImmediateSnap` ghost point + `setColumnGhostStatus` + `setColumnFaceAnchor` + `setFullSnapResult(glyphSnap)` ώστε να **δείχνεται η γλυφή/ετικέτα**) ΚΑΙ από `mouse-handler-up` (ίδια κλήση → `worldPoint` = face point + `setColumnFaceAnchor`). Miss → fall-through στο υπάρχον corner-projection path (§3.6 bugfix) → **μηδέν regression** μακριά από στόχους.
- NEW channel `faceAnchor` στο `ColumnPlacementGhostStatusStore` (zero-React)· `commitColumnFromState` anchor precedence: **face-snap λαβή > beam-axis center (§3.1b) > Tab anchor**.
- `useColumnGhostPreview` draw: όταν `getColumnFaceAnchor()` set → ζωγραφίζει **ΜΟΝΟ** εκείνο το anchor ghost (active, στο face point), αλλιώς τα 9 (imperative read, ADR-040-safe).

**Σημείωση:** η νέα face-attach συμπεριφορά **υπερισχύει** της §3.1b «center on beam axis» όταν ο cursor είναι κοντά σε παρειά δοκαριού (τα δοκάρια είναι λεπτά → σχεδόν πάντα εντός capture) — εσκεμμένη αναβάθμιση κατά απαίτηση Giorgio. _(Circular column: το `computeColumnGeometry` αγνοεί anchor → κεντράρει στο face point· edge-case, follow-up.)_

### 3.8 WYSIWYG column ghost — ΕΝΑ φάντασμα = η τελική κολώνα (κατάργηση 9-anchor schematic, 2026-06-21)

**Giorgio order (Revit-grade, FULL ENTERPRISE + FULL SSoT):** το εργαλείο «Κολώνα» είχε **3 ασύμβατες εμφανίσεις** → ΕΝΑ **WYSIWYG** φάντασμα οπτικά **ταυτόσημο** με την τοποθετημένη κολώνα.

**Οι 3 παλιές εμφανίσεις (πρόβλημα):**
- **Μακριά από οντότητες:** 9-anchor ghost (`ColumnAnchorGhostRenderer`) → 1 ενεργό + **8 ανενεργά @15%** = περιμετρικές γραμμές που μπερδεύουν.
- **Κοντά σε οντότητα (§3.7 face-snap):** 1 φάντασμα (single-anchor filter) → χάνονται οι 8 γραμμές.
- **Τοποθετημένη:** πραγματικός `ColumnRenderer` (γεμάτο/hatch) → άλλο visual.

**Απόφαση (κλειδωμένη, Giorgio):** ΕΝΑ φάντασμα μέσω του **ΠΡΑΓΜΑΤΙΚΟΥ `ColumnRenderer`** (mirror δοκαριού §3.3 WYSIWYG). Κατάργηση των 8 ανενεργών anchor-ghosts. 🔴 overlap = κόκκινο schematic overlay· 🟢/neutral = πλήρες WYSIWYG (το έγκυρο visual· το πραγματικό σχήμα δείχνει μόνο του πού πατάει — ίδια απόφαση «Α» με το δοκάρι §3.6).

**FULL SSoT — μηδέν νέο geometry, μηδέν νέο store, μηδέν διπλότυπο (μπαίνει στο WYSIWYG path του δοκαριού/τοίχου):**
- NEW pure `hooks/drawing/column-preview-helpers.ts` (`generateColumnPreview`) — **mirror `beam-preview-helpers`**: επιστρέφει ΠΛΗΡΕΣ `ColumnEntity` flagged `wysiwygPreview` μέσω των **ίδιων builders με το commit** (`buildDefaultColumnParams` + `buildColumnEntity`) ⇒ ο `BimPreviewRenderer` (entity-agnostic) το ζωγραφίζει με τον πραγματικό `ColumnRenderer`.
- **preview === commit (byte-for-byte):** διαβάζει ΑΚΡΙΒΩΣ τα imperative SSoT που γράφει το §3.7 move/click path — θέση `getImmediateSnap()` (= το committed click point του `mouse-handler-up`), λαβή `getColumnFaceAnchor()` ?? (`getColumnGhostStatus()==='beam'` ? `center` : ribbon/Tab anchor) — **ΙΔΙΑ precedence με `commitColumnFromState`**, status `getColumnGhostStatus()`.
- **Μηδέν νέο preview-store:** kind/anchor/overrides από το ΥΠΑΡΧΟΝ `columnToolBridgeStore` (κρατά ήδη το user-editable state) — δεν χρειάστηκε αντίστοιχο `beamPreviewStore`.
- **Wiring:** `drawing-preview-generator.ts` → branch `if (tool === 'column')`· `useUnifiedDrawing.updatePreview` → `isColumn` στο gating/currentTool/sceneUnits (mirror `isBeam`· το column FSM μένει `machineTool='select'`). Ο `PreviewRenderer` (ADR-040) ήδη ρεντάρει `wysiwygPreview` + 🔴 `ghostStatusColor` schematic — **μηδέν αλλαγή εκεί**.
- **DEDUP (Giorgio SSoT audit, full unification):** το «snapped-or-raw cursor read» (`getImmediateSnap`) + το «flag entity ως WYSIWYG ghost» wrapper ήταν **copy-pasted** σε beam/wall/foundation/column. EXTRACT σε NEW `hooks/drawing/wysiwyg-preview-shared.ts` (`resolveEffectivePreviewCursor` + `toWysiwygPreviewEntity`)· **και τα 4** (`beam`/`wall`/`foundation`/`column`-preview-helpers) το καταναλώνουν → πραγματική μία πηγή (byte-for-byte, regression GREEN). ⚠️ `wall` = ADR-508 ενεργός πράκτορας → migrated κατ' απαίτηση Giorgio (behavior-preserving)· συντονισμός πριν commit. Boy-scout: διορθώθηκαν 2 stale `foundation-preview-helpers` tests (περίμεναν την προ-WYSIWYG polyline, 2026-06-11).
- **Κατάργηση 9-anchor chain (full clean delete):** διαγράφηκαν `useColumnGhostPreview.ts`, `canvas-layer-stack-column-ghost.tsx` (`ColumnGhostPreviewMount`), `ColumnAnchorGhostRenderer.ts`, `bim/columns/column-anchor-ghosts.ts` (+test), `column-tool-ghost-overrides.ts`· αφαιρέθηκε το `getGhostFootprints` από `useColumnTool` + το `columnGhost(Preview)` prop από `CanvasSection`/`CanvasLayerStack`/leaves/types. Το `bim/ghosts/ghost-status-color.ts` (SSoT) μένει αυτόνομο (το re-export του διαγραμμένου renderer δεν χρειαζόταν). ⚠️ ADR-363 §5.6 feature αντικαταστάθηκε — **Tab anchor cycling ΠΑΡΑΜΕΝΕΙ** (αλλάζει το single WYSIWYG ghost μέσω της anchor precedence).

### 3.9 Wall-axis CENTER snap — κέντρο κολώνας στον άξονα τοίχου (mirror §3.1b, 2026-06-21)

**Giorgio order (Revit-grade):** το εργαλείο «Κολώνα» πάνω σε **τοίχο** ΔΕΝ επέτρεπε τοποθέτηση **κεντραρισμένη στον άξονα** του τοίχου — το §3.7 face-snap κουμπώνει **μόνο στις παρειές** (flush). Ζητούμενο: ακριβές **mirror του §3.1b «Column→Beam axis»** (status → center-anchor), αλλά για τοίχο. **ΧΩΡΙΣ split τοίχου** (η «φάτνωμα ανά κολώνα» υποδιαίρεση = αναλυτικό μοντέλο organism/FEM, ΟΧΙ φυσικό element — ADR-487).

**Απόφαση (κλειδωμένη):** **nearest-reference-wins** μέσα στον τοίχο (όπως Revit «centerline + face references»):
- ο cursor **πιο κοντά στον άξονα** (εσωτερική μισή ζώνη, perp ≤ ½ ημι-πάχους) → **center-on-axis**: το ΚΕΝΤΡΟ της κολώνας κουμπώνει στον άξονα (anchor `center`, 🟢), foot = προβολή του cursor στον άξονα.
- ο cursor **πιο κοντά σε παρειά** (εξωτερική μισή ζώνη) ή **πέρα από τις άκρες** → πέφτει στο υπάρχον §3.7 **flush** (καμία αλλαγή).

**FULL SSoT — ΕΝΑ αρχείο, μηδέν νέο wiring/store/geometry, μηδέν διπλότυπο:**
- Επέκταση **μόνο** του `bim/columns/column-face-snap.ts`: για στόχους **τοίχου** (`endsAxis=null`) χτίζεται ένα `WallAxisFrame` (χορδή `axis[0]→axis[last]` + perp ημι-πάχος) μέσω **reuse `projectPolygonOnAxis`** SSoT· νέο `resolveWallAxisCenter` δοκιμάζεται **πρώτο** στο `resolveForTarget` (reuse **`projectPointOnAxis`** για foot+perp) και επιστρέφει anchor `center` / status `beam` / position στον άξονα — αλλιώς `null` → flush. **Μηδέν νέο projection primitive.**
- **Καμία αλλαγή σε scheduler/commit/store:** το αποτέλεσμα ρέει αυτούσιο μέσα από την **υπάρχουσα** §3.7 υποδομή — `snap-scheduler.applyColumnFaceSnap` → `setColumnFaceAnchor('center')` + `setColumnGhostStatus('beam')` + `setImmediateSnap(axis foot)`· `mouse-handler-up` → ίδιο resolver· `commitColumnFromState` anchor precedence (`faceAnchor='center'` νικά) → **preview ≡ commit** byte-for-byte. Ευθύς τοίχος → χορδή ≡ άξονας (ακριβής foot)· καμπύλος → χορδή προσέγγιση (συνεπές με `buildTargetFrame`).

### 3.10 Sync-in-preview unification — μία snap pipeline για όλα τα placement tools (2026-06-22)

**Giorgio order (Revit-grade, FULL ENTERPRISE + FULL SSoT):** δύο εργαλεία τοποθέτησης με **θεμελιωδώς διαφορετική αρχιτεκτονική face-snap** = anti-pattern. Τοίχος/δοκάρι υπολόγιζαν το face-snap **σύγχρονα στο preview** (`resolveMemberGhostSnapFromStore` από targets-store)· η κολώνα **async** στον `snap-scheduler` → 3 stores (`ColumnPlacementGhostStatusStore`). Αυτή η διάσπαση ήταν η αιτία που κάθε νέος στόχος (π.χ. **ακμές πλάκας**) δούλευε αμέσως σε τοίχο/δοκάρι αλλά απαιτούσε ξεχωριστό μπάλωμα στην κολώνα (που **δεν δούλευε στον browser**).

**Απόφαση (κλειδωμένη):** η κολώνα υπολογίζει το face-snap **σύγχρονα στο preview ΚΑΙ στο commit**, πιστό mirror τοίχου/δοκαριού. **ΕΝΑ** resolver + **ΙΔΙΟΙ** pre-collected στόχοι + **ΙΔΙΟΣ** effective cursor (`ImmediateSnap`) → preview ≡ commit **εξ ορισμού** (μία πηγή αλήθειας, μηδέν async store). Κάθε στόχος (κολώνα/δοκάρι/τοίχος/**ακμή πλάκας**/μελλοντικός) δουλεύει **αυτόματα παντού** → εξαφανίζεται ολόκληρη η κλάση bugs «δουλεύει στο Α, όχι στο Β».

**FULL SSoT — ΕΝΑ κοινό store για ΟΛΑ τα placement tools (Giorgio: «όπως η Revit»):**
- **NEW** `bim/framing/scene-snap-targets.ts` — **ΕΝΑ** κοινό imperative store (`sceneSnapTargetsStore`) με pre-collected στόχους (`footprints`/`beamTargets`/`wallTargets`/`slabTargets`) + **ΕΝΑΣ** collector `collectSceneSnapTargets` + selector `selectGhostMembers(targets, kinds)` (flat member-set ανά tool: τοίχος=wall+beam+slab, δοκάρι=beam+slab, κολώνα=granular). Οι face-snap στόχοι είναι ιδιότητα της **σκηνής**, ΟΧΙ του εργαλείου → ζουν ΜΙΑ φορά. **Κατάργηση 3 παράλληλων αντιγράφων:** το column-preview-store διαγράφηκε (ποτέ uncommitted)· `wall-preview-store`/`beam-preview-store` **έχασαν** `columnFootprints`/`memberTargets`/`beamTargets`/`setColumns`/`setMembers`/`setBeams` (κρατούν πλέον ΜΟΝΟ tool-FSM state). Pre-collected (ΟΧΙ per-frame) → ελαφρύ hover.
- **`column-face-snap.ts`:** το `resolveColumnFaceSnap(cursor, entities, units)` έγινε **thin wrapper** γύρω από NEW **core** `resolveColumnFaceSnapFromTargets(cursor, SceneSnapTargets, units)` (η resolver λογική — bbox + slab axis-relative + §3.9 — **byte-for-byte αμετάβλητη**). Tests του wrapper αμετάβλητα.
- **`column-preview-helpers.ts` (ghost):** σύγχρονος επανα-υπολογισμός — `effectiveCursor = resolveEffectivePreviewCursor` (=`getImmediateSnap`) → `resolveColumnFaceSnapFromTargets(effectiveCursor, sceneSnapTargetsStore.get(), units)` → θέση/λαβή/status/faceFrame **απευθείας** από το ΕΝΑ αποτέλεσμα (mirror `makeWallGhostBeforeClick`). Καμία ανάγνωση 3 stores.
- **`useColumnTool.ts`:** NEW `syncSceneTargetsToStore` → `sceneSnapTargetsStore.refresh(entities)` + κλήση on `activate` + `sceneSnapTargetsStore.reset()` on `deactivate` (πιστό mirror useWallTool/useBeamTool).
- **Migrated wall+beam (shared tree, behavior-preserving):** `useWallTool`/`useBeamTool` sync → `sceneSnapTargetsStore.refresh`· `wall-preview-helpers`/`beam-preview-helpers`/`use-wall-commit`/two-click commits reads → `selectGhostMembers(get(), kinds)` + `get().footprints`· `collectBeamSnapTargets` (obsolete) διαγράφηκε.
- **NEW SSoT hook `hooks/drawing/use-scene-snap-target-sync.ts` (`useSceneSnapTargetSync`):** το «re-sync on `drawing:entity-created` + rAF defer» effect ήταν **copy-pasted byte-for-byte** σε useWallTool/useBeamTool· με την κολώνα θα γινόταν **triplicate** → εξήχθη σε **ΕΝΑ** hook (Giorgio SSoT audit). **Και τα 3** tools το καταναλώνουν (`useSceneSnapTargetSync(syncSceneTargetsToStore)`) → μηδέν διπλότυπο. ⚠️ useWallTool/useBeamTool = shared tree (behavior-preserving migration· συντονισμός staging).
- **`mouse-handler-up.ts` (commit):** ΙΔΙΟΣ resolver + ΙΔΙΟΙ στόχοι + ΙΔΙΟΣ `resolveEffectivePreviewCursor(worldPoint)` → `worldPoint = faceSnap.position` + γράφει `faceAnchor`/`status` handoff. Miss → ο effective cursor (corner/grid-adjusted, ήδη baked στο `ImmediateSnap` από τον scheduler) ΕΙΝΑΙ το committed σημείο.
- **`snap-scheduler.ts`:** **αφαιρέθηκε** ο column face branch (`resolveColumnFaceSnapWithGlyph`/`applyColumnFaceSnap`/3-store writes + `applyColumnGhostStatus`). **ΔΙΑΤΗΡΗΘΗΚΑΝ:** corner-projection (`resolveColumnDrawSnap`), glyph publishing (`publishSnapMarker`), `ImmediateSnap` write. Η **μαγνητική έλξη σε ορατό BIM χαρακτηριστικό + οι γλυφές/ετικέτες έρχονται ΔΩΡΕΑΝ** μέσω του normal path → `ImmediateSnap` → `resolveEffectivePreviewCursor` (ίδιο pattern με τοίχο/δοκάρι· γι' αυτό αφαιρέθηκε το `resolveColumnFaceSnapWithGlyph`). Boy-scout: `getEntities` αφαιρέθηκε από `SnapDetectionInput` + `mouse-handler-move` (νεκρό μετά την αφαίρεση του branch).
- **`ColumnPlacementGhostStatusStore.ts`:** συρρικνώθηκε σε **commit-time handoff** (μόνο `status` + `faceAnchor`, γραμμένα από `mouse-handler-up`, διαβασμένα από `useColumnTool` 1ο κλικ)· `faceFrame` αφαιρέθηκε (το preview το κατέχει). Καμία per-frame εγγραφή.

**Διατηρούνται (μηδέν regression):** glyph snapping, corner-projection, 9-anchor (auto λαβή ανά παρειά×ζώνη), §3.9 wall-axis center, place+rotate 2-click + πορτοκαλί γραμμή στρέψης. **Σημείωση:** το παλιό §3.1b «center-on-beam-axis όταν status=beam χωρίς face anchor» (corner-projection fallback) **εξέλιπε** — είχε ήδη υπερισχυθεί από το §3.7 face-attach (το οποίο πιάνει τα λεπτά δοκάρια σχεδόν πάντα εντός capture)· το dominant behavior (face-snap 🟢) διατηρείται ταυτόσημο. Το `resolveColumnGhostStatusFromSnap` + `findColumnOverlap` (`column-placement-snap-context`) είναι πλέον **production-unused** (μόνο tests) — follow-up cleanup candidate.

### 3.12 Arc-length listening dimensions — ΚΥΚΛΟΣ + ΤΟΞΟ (Revit/AutoCAD ⌒, 2026-06-22)

**Giorgio order:** όταν η κολώνα ολισθαίνει πάνω σε **κύκλο/τόξο**, οι listening dimensions να μετρούν **ΜΗΚΟΣ ΤΟΞΟΥ** (`s = r·θ`) με τη dim line να **ακολουθεί την καμπύλη** (όπως το AutoCAD «Arc Length Dimension» ⌒), ΟΧΙ ευθεία χορδή. **FULL ENTERPRISE — «να εξυπηρετεί ΚΑΘΕ λογική χρήστη ταυτόχρονα»** (όχι ένα datum) + **Κύκλος ΚΑΙ Τόξο μαζί**.

**Ενοποιημένο μοντέλο (κύκλος = τόξο 0°→360°):** για κολώνα στη γωνία `θ`, ένα **config-driven** SSoT εκπέμπει:
1. `arcLeftGap` — **καμπύλο** μήκος τόξου `θ → πλησιέστερο datum CW`.
2. `arcRightGap` — **καμπύλο** μήκος τόξου `θ → πλησιέστερο datum CCW`.
3. `radius` — **ευθεία** ακτίνα R (κέντρο → κολώνα).

**Datum set (`arcListeningDatums`) = ένωση που εξυπηρετεί κάθε λογική:** τεταρτημόρια (AutoCAD QUADRANT osnap 0/90/180/270) **εντός** του angular span + (αν τόξο) τα **πραγματικά άκρα** `startAngle`/`endAngle`. Bracketing = πλησιέστερο datum εκατέρωθεν του `θ`. **Label mode (config, default `length`)**: `length` (μέτρα) / `angle` (μοίρες) / `both` — οι angle-thinkers εξυπηρετούνται από την **ΙΔΙΑ καμπύλη γεωμετρία**, μόνο διαφορετικό format (μηδέν δεύτερο angular-dim subsystem).

**FULL SSoT (Giorgio audit, grep-verified — ΜΗΔΕΝ νέο arc math):**
- **Carry «curve» info μέσα από το pipeline (μηδέν νέος μηχανισμός):** NEW `ArcMeta` (`linear-member-face-snap.ts`) → `LinearMemberSnapTarget.arc?` (`circleTargets`/νέο `arcTargets` το θέτουν σε κάθε χορδή) → `resolveLinearMemberFaceSnap` το προωθεί στο `GhostFaceFrame.arc?` → column `buildEdgeCenterSnap`/`buildCenteredAxisFaceFrame` το νηματώνει → `resolveGhostFaceDimensions` (arc branch, gated σε `frame.arc`).
- **Reuse μαθηματικών τόξου:** `calculateArcLength` (geometry-arc-utils) για `s=r·θ`· `calculateAngle`/`pointOnCircle` (geometry-vector-utils)· `arcToPolyline` (GeometryUtils) για το sampling της **καμπύλης dim line** στον painter. ΜΗΔΕΝ νέο `atan2`/tessellation.
- **NEW config leaf** `arc-listening-dim-config.ts` (zero-import, cycle-proof· `{showArcGaps, showRadius, labelMode}` default όλα ON / `length`) — διαβάζεται στον `resolveGhostFaceDimensionsMeta` (πέρασμα στον resolver + `labelMode` στο meta).
- **Painter** `ghost-face-dim-paint.ts`: NEW curved branch (`d.arc`) → δειγματίζει `arcToPolyline` σε ακτίνα `radius+offset`, στρώνει polyline + ακτινικές extension lines + ετικέτα — **ΙΔΙΟ** 0.5px dashed cyan overlay SSoT (`applyOverlayLineStyle`) με τα ευθεία. Straight dims (`radius` + όλα τα ευθεία) → ο υπάρχων `renderPreviewDimension` αμετάβλητος.
- **Re-projection:** σε κύκλο/τόξο η κολώνα κάθεται **ΑΚΡΙΒΩΣ** στην περιφέρεια (radial reprojection· διορθώνει το chord-inset· η γωνία διατηρείται).
- **Visible-side (bugfix 2026-06-22, Giorgio «εφαρμόζεται στην κρυφή πλευρά»):** το `arcToPolyline` tessellate-άρει πάντα CCW `start→end` αγνοώντας τη φορά → σε CW τόξα (`counterclockwise===true`, ο interactive arc tool το θέτει ανά φορά ποντικιού) η κολώνα κουμπωνε στο **συμπληρωματικό (κρυφό)** τόξο. **FIX = NEW κοινό SSoT `arcVisibleCcwRange`** (`geometry-arc-utils`): επιστρέφει το **ορατό** CCW εύρος (`counterclockwise===true ⇒ [endAngle→startAngle]`). Το μοιράζονται **`hitTestArcEntity`** (refactor — η ίδια inline λογική εξήχθη, behavior-preserving) ΚΑΙ `arcTargets` → η κολώνα κουμπώνει στην ΙΔΙΑ ορατή πλευρά που σχεδιάζεται/χτυπιέται. Μηδέν duplicate της direction logic.

**Ευθείς στόχοι (τοίχος/δοκάρι/πλάκα/γραμμή/πολυγραμμή/ορθογώνιο): byte-for-byte αμετάβλητοι** — ο arc είναι ΝΕΟΣ gated κλάδος. **91 jest GREEN** (NEW: arc datums κύκλος/τόξο/wrap, `s=r·θ` τιμές, config gating, curved-render span, column faceFrame.arc + re-projection, painter curved sampling + angle label· + regression ευθειών) · tsc clean (δικά μου). ⚠️ `ghost-face-dim-paint` = ADR-040 critical (CHECK 6B/6D) → stage **ADR-040 + ADR-398**. 🔴 browser-verify (κολώνα σε κύκλο → καμπύλες ⌒· σε τόξο → προς πραγματικά άκρα) + commit (Giorgio). **DEFER:** arc-length προς κοντινά **άλλων οντοτήτων** snap points (datum union επεκτείνεται)· UI status-bar toggle του config (store ready)· bulge arcs πολυγραμμής· λοξό ορθογώνιο.

### 3.13 Polar Magnet — κολώνα ΜΕΣΑ στον δίσκο κύκλου (πολικό πλέγμα, 2026-06-22)

**Giorgio order:** «τι μαγικό/έξυπνο/πρωτοποριακό όταν ο χρήστης βάζει κολώνα ΜΕΣΑ στον δίσκο ενός κύκλου;» **Απόφαση:** ο δίσκος = φυσικό **πολικό σύστημα συντεταγμένων**. Μόλις το εργαλείο Κολώνα μπει **εντός** κυκλικού δίσκου, εμφανίζεται διακριτικά **κέντρο + ομόκεντροι δακτύλιοι + ακτινικές ακτίνες** σε «στρογγυλά» νούμερα (zoom-adaptive)· η κολώνα κουμπώνει σε **κέντρο** ή **δακτύλιο ∩ ακτίνα**, με live listening dims **R + θ** (αντί x,y). Στο **χείλος** → §3.12 circumference (nearest-wins).

**Κλειδωμένες αποφάσεις (Giorgio, αριθμητικά παραδείγματα):**
- **Q1 ring spacing — Both:** default nice-absolute zoom-adaptive (`adaptiveDistanceStep`· π.χ. R=3000 → 500/1000/1500/2000/2500)· **κράτημα Shift** → κλάσματα ακτίνας (R/4, R/3, R/2, 2R/3, 3R/4).
- **Q2 angle — zoom-adaptive arc-length:** `adaptiveDistanceStep` πάνω στην περιφέρεια του snapped δακτυλίου → quantize σε nice γωνία {5,10,15,30,45,90}° (σταθερό ~25px screen-spacing· R2000 zoom-out 45°/μεσαίο 30°/zoom-in 15°).
- **Q4 precedence — nearest-wins:** κέντρο (εντός `CENTER_CAPTURE_PX=12`) > δακτύλιος∩ακτίνα· στο χείλος ο polar επιστρέφει `null` → §3.12 circumference κερδίζει.
- **Q5 edge clearance — cover + μέγεθος κολώνας:** `maxRing = radius − (cover 50mm + ημι-διαγώνιος κολώνας)`· πέρα από `maxRing` → §3.12.

**FULL SSoT (grep-verified — ΜΗΔΕΝ νέα polar math / store / overlay style):**
- **NEW pure** `bim/columns/polar-disk-snap.ts` — `resolvePolarDiskSnap(cursor, disk, units, {worldPerPixel, shiftFractions, clearanceScene})` → `{position, ringR, angleDeg, isCenter, dist, faceFrame}`. Reuse `adaptiveDistanceStep`/`quantizeMagnitude` (ring/angle βήμα) + `pointOnCircle`/`calculateAngle` (γωνία↔σημείο) + `buildCenteredAxisFaceFrame` με `arc` (R/θ dims = ΙΔΙΟ §3.12 arc path). Η ΜΟΝΑΔΙΚΗ νέα «math» = οι 5 σταθεροί λόγοι κλασμάτων + το quantize σε nice γωνία (reuse 1/2/5 λογική). + `buildPolarDiskGrid` (overlay rings/spokes, ΙΔΙΟ ring/angle SSoT) + `findDiskContaining` + `polarClearanceScene`.
- **NEW field** `SceneSnapTargets.diskTargets: {center,radius}[]` (NEW `collectDiskTargets` στο `member-snap-targets`· reuse το circle read του `circleTargets`) — ξεχωριστά από τις χορδές περιφέρειας του §3.12.
- **`resolveColumnFaceSnapFromTargets`** (shared) — optional 4ο arg `opts` + NEW polar κλάδος (cursor εντός δίσκου → `resolvePolarDiskSnap`, nearest-wins vs edge/bbox μέσω `nearestHit`). Στο χείλος `null` → §3.12. **ΜΗΔΕΝ νέο wiring** (ρέει μέσω §3.10 pipeline· preview ≡ commit). NEW `column-polar-opts.ts` κεντρικός builder των opts (transform+store+clearance) — ghost+commit τα χτίζουν ΙΔΙΑ.
- **NEW thin store** `ColumnPolarStore` (zero-React· `shiftFractions`+`foldOverride`, mirror `ColumnRotationStore`) — interaction modifiers ΜΟΝΟ (όχι snap state). Shift wiring event-time στο `mouse-handler-move` (`activeTool==='column'`).
- **NEW overlay painter** `polar-disk-paint.ts` (rings/spokes/center, reuse `applyOverlayLineStyle`+`OVERLAY_LINE_COLORS.listeningDim`+`arcToPolyline`+`pointOnCircle`· ΙΔΙΟ 0.5px dashed cyan SSoT) → `PreviewRenderer.drawPolarDisk` → `PreviewCanvas` handle → `drawing-hover-handler` (μετά το `drawPreview`, mirror `drawGhostFaceDimensions`)· meta «ταξιδεύει» στο preview entity (`polarDiskGrid`, όπως `faceDimensions`).

**18 jest GREEN** (`polar-disk-snap` 10: center / nice-absolute ring / nice angle / Shift fractions / edge-clearance null / faceFrame arc / grid / findDiskContaining / clearance· + regression `column-face-snap` 27 + `column-preview-helpers` αμετάβλητα). ⚠️ ADR-040 (`canvas-v2/preview-canvas/*`, `mouse-handler-*`) → CHECK 6B/6D ⇒ stage **ADR-040 + ADR-398**. 🔴 tsc (N.17) + browser-verify + commit (Giorgio).

### 3.14 Symmetry auto-complete — n-fold πρόβλεψη συμμετρίας (το «πιο μαγικό», 2026-06-22)

**Giorgio order:** όταν υπάρχουν ήδη κολώνες σε δακτύλιο, το φάντασμα **προβλέπει** τις θέσεις που διατηρούν περιστροφική συμμετρία (n-fold)· ΕΝΑ κλικ = μία θέση ή όλο το δαχτυλίδι (ΕΝΑ undo). Αυτό **δεν το έχει η Revit/AutoCAD**.

**Φάση 1 (DONE) — pure detector:** **NEW** `bim/columns/polar-symmetry-detector.ts` — `detectRingFold(existingAnglesDeg, override) → {fold, allAnglesDeg, ghostAnglesDeg}`. **Q3 auto-detect:** gcd των γωνιακών αποστάσεων → θεμελιώδης γωνία → `fold = 360/gcd` (clamp [2,24])· π.χ. 0°,120° → 3-fold (ghost 240°)· 0°,90° → 4-fold (ghost 180°,270°). **Scroll override** {3,4,6,8,12} (μέσω `ColumnPolarStore.foldOverride`) υπερισχύει. Pure — δίνει ΜΟΝΟ γωνίες· οι θέσεις (x,y) μέσω `pointOnCircle`/`computePolarTransforms` (array SSoT) στον caller. **8 jest GREEN** (3/4/6-fold auto, complete-ring→no ghost, override+phase, <2→null, non-clean→null).

**Φάση 2 (DEFER — ξεχωριστή συνεδρία, browser-verified interaction unit):** ghost-set overlay (faint columns στις `ghostAnglesDeg`) + wheel→`cycleColumnPolarFold` + **one-click ring-commit** (build N κολώνες στις ghost θέσεις → `appendEntitiesToScene(accessor, cols, 'column', 'SymmetryRing')` = ΕΝΑ undo, reuse `buildColumnEntity`+template overrides όπως `buildIntermediateColumns`). **Λόγος defer:** το ring-commit τροποποιεί το `useColumnTool` (450/500 γρ. — προσθήκη θα ξεπερνούσε το N.7.1 όριο → χρειάζεται split) + είναι interaction που απαιτεί browser-verify· η «δύσκολη» math (detector) είναι έτοιμη & tested.

### 3.15 Cartesian Magnet — κολώνα ΜΕΣΑ σε ΟΡΘΟΓΩΝΙΟ (καρτεσιανό πλέγμα, 2026-06-22)

**Giorgio order:** αδελφό δίδυμο του §3.13 για **ορθογώνιο** (πλάκα/πέδιλο/δωμάτιο) = **καρτεσιανή** περιοχή. cursor εντός ορθογωνίου → πλέγμα από nice-offsets των ακμών + **9 κομβικά σημεία** (4 γωνίες με cover, 4 μέσα ακμών, κέντρο)· snap σε αυτά· live **4 dx/dy dims** προς τις ακμές. Στο **χείλος** → §3.11 edge (nearest-wins).

**Κλειδωμένες αποφάσεις (Giorgio):** offsets **Both** (nice-absolute default `adaptiveDistanceStep`· Shift→κλάσματα W/H, reuse `ColumnPolarStore.shiftFractions`)· cover γωνιών = cover 50mm + ημι-διαγώνιος (reuse `polarClearanceScene`)· **4 dims** (προς όλες τις ακμές)· **λοξά ΚΑΙ ίσια ΕΝΑ κώδικα** (local u/v frame· ίσιο = γωνία 0°)· precedence nearest-wins (κέντρο > 9-point > grid∩ > §3.11 edge).

**FULL SSoT (grep-verified — ΜΗΔΕΝ νέα math/store/overlay):**
- **NEW pure** `bim/columns/rect-cartesian-snap.ts` — `resolveRectCartesianSnap(cursor, rect, units, opts)` (snap localX/localY ανεξάρτητα σε 9-point/grid· center capture· cover→null) + `buildRectGrid` (overlay) + `findRectContaining` + `resolveRectCartesianDims` (4 dims, reuse `GhostFaceDimension` shape → render μέσω `paintStraightDimension`). Reuse `adaptiveDistanceStep`/`quantizeMagnitude`/`calculateDistance`.
- **NEW leaf** `bim/framing/rect-frame.ts` (`RectFrame` {center,u,v,halfW,halfV} + `rectFrameFromCorners`) — αποφυγή cycle framing↔columns.
- **NEW field** `SceneSnapTargets.rectTargets: RectFrame[]` (`collectRectTargets`, reuse `rectangleCorners` — axis-aligned app-wide· resolver rotation-ready).
- **`resolveColumnFaceSnapFromTargets`** — NEW `resolveRectHit` κλάδος (nearest-wins μέσω του υπάρχοντος `nearestHit`)· ΙΔΙΑ opts με τον polar. `generateColumnPreview` attach `rectGrid` overlay + 4 καρτεσιανά dims (αντί faceFrame).
- **NEW overlay** `rect-grid-paint.ts` (γραμμές u/v + κέντρο, reuse `applyOverlayLineStyle`+`strokeOverlaySegment`) → `PreviewRenderer.drawRectGrid` → `PreviewCanvas` handle → `drawing-hover-handler` (mirror `drawPolarDisk`).

**10 jest GREEN** (`rect-cartesian-snap`: 9-point/grid/Shift/center/cover→null/**rotated 30°**/4 dims/findRect). ⚠️ ADR-040 (CHECK 6B/6D) → stage ADR-040+ADR-398.

### 3.16 Auto-grid fill — πλέγμα m×n κολωνών (detector DONE, interaction DEFER, 2026-06-22)

**Φάση 1 (DONE) — pure:** **NEW** `bim/columns/rect-grid-fill.ts` — `rectGridPositions(rect, cols, rows, clearance)` → θέσεις πλέγματος με **ίσες αποστάσεις + ίσα περιθώρια** (cover)· row-major· λοξά μέσω u/v. **Q3:** πλήθος **μόνο με ροδάκι** ({2×2,3×2,3×3,4×3…}). **5 jest GREEN** (3×2/cover/1×1/ίσα spans/rotated).

**Φάση 2 (DEFER):** wheel→cycle (store `gridOverride`, **rename `ColumnPolarStore`→`ColumnSmartPlaceStore`**) + ghost-set (faint, `rect-grid-paint`) + **one-click ολο-το-πλέγμα** (`buildColumnEntity` + `appendEntitiesToScene(...,'RectGrid')` = ΕΝΑ undo). Defer ίδιο με §3.14: `useColumnTool` 450/500γρ → split + browser-verify unit.

### 3.18 Το φάντασμα ΑΚΟΛΟΥΘΕΙ τις ΛΟΞΕΣ/ΠΟΛΥΓΩΝΙΚΕΣ παρειές κολόνας/τοίχου (slant-following edges, 2026-06-25)

**Αίτημα (Giorgio):** όταν το φάντασμα κολόνας πάει κοντά σε **πολυγωνική / Γ / Τ / Π / λοξή** κολόνα ή **τοιχίο/τοίχο** με μη-ορθογώνιο σκέλος, να κουμπώνει **flush στην ΠΡΑΓΜΑΤΙΚΗ (λοξή) παρειά** με σωστή **στροφή + γωνία-με-γωνία**, ΟΧΙ στο axis-aligned bbox. «Όπως η Revit, FULL ENTERPRISE + FULL SSoT.»

**Ρίζα:** οι column footprints (+ flush τοίχου) περνούσαν από το `buildFaceTargets → footprintBounds` = **world-aligned bbox** → `resolveForTarget` έδινε πάντα `rotation: 0` (η πραγματική στροφή χανόταν).

**Λύση = ΓΕΝΙΚΕΥΣΗ του Φ6d pad πρότυπου (ADR-514), μηδέν παράλληλο subsystem:** το world-baked `geometry.footprint` (φέρει τη στροφή + τις πραγματικές ακμές) **κάθε** πέδιλου/μη-κυκλικής κολόνας + το `wallOutlineRing` του τοίχου → **zero-width edges** μέσω του κοινού `polylineEdgeTargets`, που τα καταναλώνει ο **ΙΔΙΟΣ** axis-relative `resolveLinearMemberFaceSnap` (slant-capable) → flush-beside + corner-to-corner + **rotation = γωνία ακμής**.
- **Collector (SSoT):** `collectFoundationPadEdgeTargets` → **`collectFootprintEdgeTargets`** (πέδιλα + μη-κυκλικές κολόνες + τοίχοι). Bucket `padEdgeTargets` → **`footprintEdgeTargets`**.
- **Resolver:** `resolveColumnPadEdgeSnap` → **`resolveFootprintEdgeSnap`** (ίδια λογική: `facePerp:0`, `pickThird` corner-lock, `axisAlignmentRotationDeg`).
- **Edges για ΟΛΕΣ τις μη-κυκλικές** (απόφαση Giorgio): η axis-aligned ορθογώνια έχει **ίδιο feel** (flush+corner, rotation 0)· η στραμμένη/πολυγωνική ακολουθεί τη λοξάδα — **ένα μόνο path**, οι μη-κυκλικές αφαιρέθηκαν από το bbox.
- **ΚΥΚΛΙΚΗ κολόνα → μένει bbox** (νέο `collectCircularColumnFootprints` + `SceneSnapTargets.circularFootprints`): πολύγωνο πολλών χορδών χωρίς διακριτές λοξές παρειές → 9-handle flush αμετάβλητο.
- **Τοίχος = dual-path (μηδέν regression):** μένει ΚΑΙ στο bbox (`wallTargets` → center-on-axis §3.9) ΚΑΙ στο `footprintEdgeTargets` (slant flush). Ο cursor **εντός σώματος** → bbox κερδίζει (dist 0)· **εκτός** → edge ακολουθεί τη λοξάδα (nearest-wins). Τα 8 wall jest παραμένουν GREEN.

`footprints` bucket αμετάβλητο (= ΟΛΕΣ οι κολόνες· wall/beam member-endpoint snap). Pure· preview ≡ commit. Beams: DEFER (κρατούν short-end «extend instead» semantics).

### 3.19 ΚΥΚΛΙΚΗ κολόνα — κούμπωμα με την ΠΕΡΙΦΕΡΕΙΑ (circumference-tangent), 2026-06-25

**Αίτημα (Giorgio):** όταν τοποθετείται **κυκλική** κολόνα κοντά σε τοίχο, να κουμπώνει σε οποιαδήποτε παρειά **όχι μόνο με το κέντρο** της (modes #1/#2, ήδη §3.7/§3.9) αλλά **και με την ΠΕΡΙΦΕΡΕΙΑ** της (tangent), και να μπορεί να **ταυτίσει την περιφέρεια με τον κεντρικό άξονα**. «Όπως η Revit, FULL ENTERPRISE + FULL SSoT.»

**4 modes (Revit):** #1 κέντρο→παρειά, #2 κέντρο→άξονας (✅ υπήρχαν), **#3 περιφέρεια→παρειά**, **#4 περιφέρεια→άξονας** (🆕).

**Ρίζα (grep-verified):** η γεωμετρία της κυκλικής **αγνοεί το anchor** (ADR-363 — κέντρο πάντα στο `position`, `circular = rotationally symmetric`). → tangent = **offset του ΚΕΝΤΡΟΥ** στον resolver, `anchor:'center'`, **μηδέν αλλαγή στο geometry/anchor pipeline**.

**Λύση (angle-general, FULL SSoT reuse — μηδέν νέα geometry primitive):** tangent = `center = αναφορά + R · κάθετος_προς_cursor`. Νέο pure module `column-tangent-snap.ts` (`resolveCircularTangentHit`):
- **mode #4 (άξονας):** reuse `buildMemberAxisFrame` (τοίχοι + δοκάρια) → προβολή cursor στον άξονα + offset `±R·perpDir` (πλευρά cursor)·
- **mode #3 (παρειά/ακμή):** reuse `resolveLinearMemberFaceSnap` (footprint edges τοίχων/μη-κυκλικών κολόνων/πεδίλων + ακμές πλάκας + γραμμές) → offset `R·outwardSign·perpDir` (έξω, πλευρά cursor)·
- listening dims: reuse `buildCenteredAxisFaceFrame`· λοξό μέλος → η κάθετος βγαίνει από το frame ⇒ δουλεύει σε **κάθε γωνία**.

**Auto-candidates (απόφαση Giorgio):** ΚΑΝΕΝΑ mode toggle — τα tangent candidates μπαίνουν **ΔΙΠΛΑ** στα center στον υπάρχοντα `nearestHit` (additive). Μετρική: center `dist=perp` (κερδίζει cursor-ΠΑΝΩ-στην-αναφορά)· tangent `dist=|perp−R|` (κερδίζει cursor-~R-από-αναφορά) → ομαλή μετάβαση κέντρο-άξονα → περιφέρεια-άξονα → κέντρο-παρειά → περιφέρεια-παρειά. Σειρά `nearestHit`: `tangentHit` **πριν** `bboxHit` (το λοξό AABB δίνει spurious `dist=0` cursor-εντός· στο tangent ιδανικό — με FP-floor — νικά την ισοπαλία· μακριά χάνει από γνήσιο μικρότερο edge/center dist → μηδέν regression).

**Plumbing (gated circular, μηδέν regression):** νέο `circleRadiusScene?` στο `PolarDiskSnapOptions` (συνεπές με το `clearanceScene` που ήδη κουβαλά μέγεθος κολόνας)· `buildColumnPolarSnapOptions(overrides, sceneUnits, kind)` → `R = width/2 · mmToScene` **μόνο** όταν `kind==='circular'` (rect/polygon/I/L/T/πέδιλο → `undefined` = κανένα tangent). Ghost (`column-preview-helpers`) ΚΑΙ commit (`mouse-handler-up`) περνούν `kind` → preview ≡ commit.

**Scope v1:** tangent→{wall/beam **άξονας**} + {wall/non-circular-column/pad **παρειά** + slab/line **ακμή**}. **DEFER (honest):** beam side-faces + circular-column faces (σπάνιο: κύκλος εφαπτόμενος σε bbox άλλου κύκλου). Σε **λοξό** μέλος, cursor-εντός-AABP το tangent λειτουργεί ως **snap-ring** στο ιδανικό (εκτός AABP = πλήρης μαγνητική ζώνη) — συνεπές με το «bbox κερδίζει cursor-εντός» (§3.18).

### 3.20 ΚΥΚΛΙΚΗ κολόνα — quadrant-to-end alignment (περιφέρεια ευθυγραμμίζεται με άκρο παρειάς), 2026-06-25

**Αίτημα (Giorgio + screenshot):** ενώ η περιφέρεια εφάπτεται σε μια παρειά (§3.19, π.χ. βόρεια — Y κλειδωμένο), καθώς ο κύκλος γλιστράει **κατά μήκος** της, το **ακραίο τεταρτημόριο** του κύκλου (π.χ. το δυτικότερο σημείο = κέντρο − R) να **ευθυγραμμίζεται** με το **άκρο** (δυτική/ανατολική παρειά) του τοίχου — και αντίστοιχα στις άλλες παρειές. «Όπως η Revit.»

**Είναι ο κυκλικός ανάλογος του corner-lock (§3.18 `pickThird`):** για ορθογώνια η γωνία της κολόνας ταυτίζεται με τη γωνία του τοίχου· για **κυκλική** το **τεταρτημόριο (±R)** ταυτίζεται με το άκρο.

**Λύση (FULL SSoT, μηδέν νέα geometry):** στο `column-tangent-snap.ts`, αντί ελεύθερου γλιστρήματος κατά μήκος, **κουμπώνω το διαμήκες `along`** του κέντρου σε 3 θέσεις με zoom-adaptive ζώνη έλξης (`alignZone` = 12px / fallback 60mm):
- `along = alongMin + R` (Δ-τεταρτημόριο ↔ δυτικό άκρο)· `along = alongMax − R` (Α-τεταρτημόριο ↔ ανατολικό άκρο)· `along = μέσον` (κέντρο ↔ κέντρο παρειάς) — **απόφαση Giorgio: άκρα + κέντρο**.
- Reuse: `alongMin/alongMax` (ήδη στο `MemberAxisFrame`/`GhostFaceFrame`), `R` (`circleRadiusScene`)· εφαρμόζεται και στα δύο modes (#3 παρειά + #4 άξονας) μέσω κοινού `snapAlongToEnds`.

**Γραμμή-οδηγός (απόφαση Giorgio: hard snap + οδηγός):** όταν κουμπώνει, παράγεται `PlacementAlignmentGuide` (world segment) = **κάθετο τμήμα στο άκρο/μέσον** (= η ίδια η παρειά-άκρου). Διαδρομή overlay (mirror polar tracking line): `ColumnFaceSnap.alignmentGuide` → `assemblePlacementGhost` → preview entity meta → `drawing-hover-handler` → `PreviewCanvas.drawAlignmentGuide` → `PreviewRenderer` → **νέο pure `alignment-guide-paint.ts`** (reuse `applyOverlayLineStyle` + `OVERLAY_LINE_COLORS.drawingGuide` = 0.5px dashed orange, ίδιο SSoT με §3.13/ADR-357). Preview-only (commit χρησιμοποιεί μόνο `position`).

## 4. Συνέπειες
- ✅ §3.15: το ορθογώνιο γίνεται καρτεσιανό πλέγμα· κολώνα σε 9-point/grid με 4 dx/dy dims· λοξά+ίσια ΕΝΑ κώδικα· στο χείλος → §3.11 edge (μηδέν regression).
- ✅ §3.13: ο δίσκος γίνεται πολικό πλέγμα· η κολώνα κουμπώνει σε κέντρο/δακτύλιο∩ακτίνα με R/θ dims· στο χείλος επιστρέφει αυτόματα το §3.12 circumference (nearest-wins, μηδέν regression).
- ✅ §3.9: η κολώνα κεντράρει στον **άξονα του τοίχου** (κέντρο ≡ άξονας) όταν ο cursor είναι κοντά στο centerline· οι παρειές παραμένουν προσβάσιμες (flush) στην εξωτερική ζώνη — μηδέν regression στο §3.7.
- ✅ Καθαρή «hover→place» εμπειρία· η κολώνα κουμπώνει στον άξονα του δοκαριού (κέντρο ≡ άξονας).
- ✅ ADR-040-safe: μηδέν νέο React subscription· stores zero-React· width-based tol (transform-independent).
- ✅ SSoT: ΕΝΑ context resolver καταναλώνεται και από move και από commit (μηδέν διπλή λογική, mirror corner-snap).
- ✅ Μηδέν regression: το beam-axis είναι additive· χωρίς δοκάρι κάτω από τον cursor όλα μένουν ως πριν.
- ⚠️ Το beam-axis snap τρέχει εντός του υπάρχοντος snap gate (`snapEnabled`)· με snap απενεργοποιημένο δεν εφαρμόζεται (consistent με τα άλλα snaps).

## 5. Tests
- `bim/beams/__tests__/beam-axis-projection.test.ts` — 6 GREEN: clamped nearest foot· distance· `atEndpoint`· curved tessellation· defensive null.
- `bim/columns/__tests__/column-placement-snap-context.test.ts` — 9 GREEN: snap στον άξονα (μέσω SSoT)· atEndpoint reject· capture width-based· nearest beam· precedence (beam > overlap > neutral).
- Snapping regression: 169 GREEN (incl. NearestSnapEngine `bim-beam` wiring + snap-description-keys).
- `bim/beams/__tests__/beam-column-face-snap.test.ts` — **21 GREEN** (§3.3 + §3.6 dispatcher): 4 παρειές × 3 thirds = 12 centerline θέσεις + capture gate (snap@600 / release@601) + degenerate footprint + nearest-of-many + cursor-inside + mm/metre wrapper scaling (status `neutral`) + **column-priority** (κολόνα νικά δοκάρι). Regression: `useBeamTool` GREEN, `beam-preview-cutback-parity` + `bim-ortho-reference` GREEN (20).
- `bim/beams/__tests__/beam-beam-face-snap.test.ts` — **16 GREEN** (§3.6 axis-relative + 3-ζωνική + collinear-block): βόρεια mid/lo/hi 3-ζωνική (cursor αρχή/μέση/τέλος ακμής) + νότια αντίστροφα· κέντρο → 🟢 πλησιέστερη παρειά· Α/Δ κοντή άκρη → 🔴 συγγραμμική· capture/degenerate → null· **`isBeamCollinearOverlap`** (7, reuse `coveredIntervals`): ομοαξονικό πάνω→true· ⊥ Τ-framing→false· παράλληλο δίπλα→false· άκρο-με-άκρο→false· μερική επικάλυψη→true· κανένας/degenerate→false.
- `bim/geometry/__tests__/entity-overlap.test.ts` — **5 GREEN** (§3.6 generalize): column/footprint & beam/outline extractors· extractor-driven type filter· first-match· εκτός → null.
- `rendering/entities/__tests__/base-entity-renderer-viewport-override.test.ts` — **4 GREEN** (§3.4 render-path): default→getBoundingClientRect· override→injected canonical viewport· Δscreen-Y === Δheight (η υπογραφή του +Y offset)· `null`→restore default.
- `systems/preview/__tests__/ghost-preview-frame.test.ts` — **3 GREEN** (§3.5 SSoT-A): viewport από `canvasBoundsService`· transform από live `getImmediateTransform`· διαβάζει και τις δύο πηγές κάθε call (zero-lag).
- `hooks/tools/__tests__/useCanvasGhostPreview.test.tsx` — **8 GREEN** (§3.5 SSoT-B harness + §4 zoom-fix): canonical frame στο delegate· DPR-clear σε `on-gate-exit`· ΟΧΙ clear σε `skip-clear`· snapped cursor· `cursorMode 'none'`→null+χωρίς subscription· clear-on-exit· **NEW** re-draw σε live transform change (wheel-zoom χωρίς mousemove, Path B)· **NEW** unsubscribe από το transform SSoT στο unmount.
- `canvas-v2/preview-canvas/__tests__/preview-renderer-live-transform.test.ts` — **3 GREEN** (§4 Path A): `drawPreview` signature `(entity, viewport, options)` χωρίς transform· `render()` διαβάζει live `getImmediateTransform()` (zero-lag, όχι cached)· repaint στο νέο scale σε scheduler frame χωρίς mousemove (wheel-zoom).
- `systems/cursor/__tests__/immediate-transform-store-preview-dirty.test.ts` — **2 GREEN** (§4 SSoT): `updateImmediateTransform` μαρκάρει `preview-canvas` dirty μαζί με τα main canvases σε transform change· καλείται πριν τον idempotent guard (sync ακόμη και σε αμετάβλητη τιμή).
- `bim/columns/__tests__/column-face-snap.test.ts` — **27 GREEN** (§3.7 + §3.9): οριζόντιο δοκάρι N/S παρειές × lo/mid/hi → anchor+flush position+🟢· Α/Δ κοντή άκρη δοκαριού → 🔴 overlap· continuous slide monotonic· κάθετο δοκάρι (endsAxis y) Ε face 🟢 / N άκρη 🔴· **τοίχος** (μακριά παρειά 🟢 + **κοντές άκρες Ε/W → 🟢** + lo flush)· κολόνα-στόχος (όλες 🟢)· **`resolveColumnFaceSnapWithGlyph`** (3: ορατό χαρακτηριστικό → μαγνητική έλξη + glyphSnap· κανένα snap → free slide null· σιωπηλό grid → null)· nearest-target· capture → null· **§3.9 wall-axis center** (6: cursor στον άξονα → anchor `center`+🟢· εσωτερική ζώνη foot στον άξονα· continuous slide κατά μήκος· εξωτερική ζώνη perp>όριο → flush· πέρα από άκρη → flush short-end· δοκάρι ΔΕΝ παίρνει center).
- `bim/geometry/shared/__tests__/footprint-face-frame.test.ts` — **10 GREEN** (§3.7 SSoT extraction): `footprintBounds` (extents + <3→null)· `distanceToFootprintBounds` (εντός→0 / έξω X / διαγώνια)· `pickDominantFace` (E/W/N/S + ισοπαλία→E/W). Regression: `member-ghost-snap` + `linear-member-face-snap` GREEN μετά τη μετάβαση του `member-column-face-snap` στο κοινό SSoT (byte-for-byte).
- `hooks/drawing/__tests__/column-preview-helpers.test.ts` — **8 GREEN** (§3.8 WYSIWYG): tool inactive → null· free → πλήρες `ColumnEntity` flagged `wysiwygPreview` στον raw cursor + ribbon anchor (χωρίς `ghostStatusColor`)· `getImmediateSnap` snapped point (preview ≡ commit)· face-snap → auto λαβή· 🟢 beam χωρίς face anchor → `center`· 🔴 overlap → red `ghostStatusColor`· ribbon kind/width overrides. Regression μετά την κατάργηση 9-anchor: `useColumnTool` GREEN (αφαιρέθηκε το παλιό `getGhostFootprints` describe).

## 6. Changelog
- **2026-06-25 (NEW feature: §3.20 quadrant-to-end alignment — η περιφέρεια κυκλικής ευθυγραμμίζεται με το άκρο/μέσον παρειάς, UNCOMMITTED)** — Giorgio (+screenshot): «ενώ ο κύκλος εφάπτεται στη βόρεια παρειά, το ακραίο δυτικό/ανατολικό σημείο του να ευθυγραμμίζεται με τη δυτική/ανατολική παρειά του τοίχου· το ίδιο σε όλες τις παρειές». **= κυκλικός ανάλογος του §3.18 corner-lock** (γωνία→γωνία· εδώ τεταρτημόριο ±R → άκρο). **ΛΥΣΗ (FULL SSoT, μηδέν νέα geometry):** στο `column-tangent-snap.ts` κοινός `snapAlongToEnds` κουμπώνει το διαμήκες `along` του κέντρου σε `{alongMin+R, alongMax−R, μέσον}` (zoom-adaptive ζώνη `alignZone`=12px/60mm)· reuse `alongMin/alongMax` (ήδη στα frames) + `R`· εφαρμόζεται σε mode #3 (παρειά) ΚΑΙ #4 (άξονας). **Γραμμή-οδηγός (Giorgio: hard snap + οδηγός):** νέο `PlacementAlignmentGuide` (world segment = παρειά-άκρου) → `ColumnFaceSnap.alignmentGuide` → `assemblePlacementGhost` → preview meta → `drawing-hover-handler` → `PreviewCanvas.drawAlignmentGuide` → `PreviewRenderer` → **NEW pure `alignment-guide-paint.ts`** (reuse `applyOverlayLineStyle`+`OVERLAY_LINE_COLORS.drawingGuide`, 0.5px dashed orange — ίδιο overlay SSoT με ADR-357 polar tracking line). Preview-only (commit=position μόνο). **13 jest GREEN** (4 NEW §3.20: Δ/Α άκρο, κέντρο, free-slide-χωρίς-οδηγό)· regression 147 GREEN (face-snap/placement/preview/foundation/preview-canvas). 1 NEW painter + 6 mod (`column-tangent-snap`/`column-face-snap`/`placement-ghost-assembly`/`drawing-hover-handler`/`PreviewCanvas`/`PreviewRenderer`). tsc background (N.17). ⚠️ CHECK 6B/6D (preview render path) → **stage ADR-040 + ADR-398 + ADR-514**. 🔴 browser-verify (κύκλος εφαπτόμενος σε παρειά + γλίστρημα → κούμπωμα στο δυτικό/ανατολικό άκρο/μέσον + dashed οδηγός· κάθε παρειά) + commit (Giorgio). **ΜΑΘΗΜΑ:** «ευθυγράμμιση τεταρτημορίου» = διαμήκες snap του along σε άκρα±R στον ΙΔΙΟ tangent resolver· ο οδηγός = reuse της overlay-meta διαδρομής (faceDimensions/polarGrid pattern), όχι νέο render subsystem.
- **2026-06-25 (NEW feature: §3.19 circumference-tangent — κυκλική κολόνα κουμπώνει με την ΠΕΡΙΦΕΡΕΙΑ σε παρειά/άξονα, UNCOMMITTED)** — Giorgio: «κυκλική κολόνα κοντά σε τοίχο να κουμπώνει σε οποιαδήποτε παρειά όχι μόνο με το κέντρο αλλά και με την περιφέρεια (tangent), και να ταυτίζω την περιφέρεια με τον κεντρικό άξονα». **ΡΙΖΑ (grep-verified):** η γεωμετρία κυκλικής αγνοεί anchor (ADR-363, κέντρο→`position`) → tangent = offset ΚΕΝΤΡΟΥ στον resolver, `anchor:'center'`, **μηδέν αλλαγή geometry/anchor pipeline**. **ΛΥΣΗ (FULL SSoT reuse, μηδέν νέα geometry primitive):** νέο pure `column-tangent-snap.ts` (`resolveCircularTangentHit`) — mode #4 (άξονας) reuse `buildMemberAxisFrame` (τοίχοι+δοκάρια), mode #3 (παρειά/ακμή) reuse `resolveLinearMemberFaceSnap` (footprintEdge τοίχων/μη-κυκλικών κολόνων/πεδίλων + slab + line)· `center = αναφορά + R·κάθετος_προς_cursor` (perpDir/outwardSign από το frame → **κάθε γωνία/λοξό**)· dims reuse `buildCenteredAxisFaceFrame`. **Auto-candidates (απόφαση Giorgio):** μηδέν toggle — tangent candidates **additive** στον `nearestHit` δίπλα στα center· μετρική center `dist=perp` / tangent `dist=|perp−R|` → ομαλή μετάβαση· `tangentHit` **πριν** `bboxHit` + FP-floor (`cleanDist`) ώστε στο ιδανικό να νικά το spurious bbox-0 (λοξό AABP), αλλιώς να χάνει από γνήσιο edge/center (μηδέν regression). **Plumbing gated circular:** νέο `circleRadiusScene?` στο `PolarDiskSnapOptions`· `buildColumnPolarSnapOptions(overrides, sceneUnits, kind)` → `R=width/2·mmToScene` ΜΟΝΟ `kind==='circular'` (rect/polygon/I/L/T/πέδιλο → undefined)· ghost (`column-preview-helpers`) ΚΑΙ commit (`mouse-handler-up`) περνούν `kind` → preview≡commit. **9 jest GREEN** (`column-tangent-snap.test.ts`: #4 άξονας πάνω/κάτω, #3 παρειά, #1/#2 αμετάβλητα, gating contrast, λοξός 45°)· regression 99 GREEN (face-snap/polar/rect/placement-assembly). 1 NEW pure module + 5 mod (`polar-disk-snap`/`column-polar-opts`/`column-face-snap`/`column-preview-helpers`/`mouse-handler-up`) + 1 NEW test. tsc background (N.17). **Scope v1:** wall/beam άξονας + wall/non-circular-col/pad παρειά + slab/line ακμή. **DEFER (honest):** beam side-faces + circular-col faces· λοξό cursor-εντός-AABP = snap-ring στο ιδανικό. ⚠️ CHECK 6B/6D → **stage ADR-040 + ADR-398 + ADR-514**. 🔴 browser-verify (κυκλική κοντά σε τοίχο → περιφέρεια εφάπτεται σε παρειά/άξονα κάθε πλευρά + λοξός· center modes αμετάβλητα) + commit (Giorgio). **ΜΑΘΗΜΑ:** «tangent» = offset κέντρου κατά R στον resolver που ΗΔΗ έχει το frame· auto = additive candidate στο υπάρχον nearest-wins, όχι νέο mode subsystem.
- **2026-06-25 (NEW feature: §3.18 slant-following edges — το φάντασμα ακολουθεί ΛΟΞΕΣ/ΠΟΛΥΓΩΝΙΚΕΣ παρειές κολόνας/τοίχου, UNCOMMITTED)** — Giorgio: «κοντά σε πολυγωνική/Γ/λοξή κολόνα ή τοιχίο με μη-ορθογώνιο σκέλος, το φάντασμα να κουμπώνει flush στην ΠΡΑΓΜΑΤΙΚΗ λοξή παρειά (στροφή + γωνία-με-γωνία), ΟΧΙ στο bbox. Όπως Revit, FULL ENTERPRISE + FULL SSoT». **ΡΙΖΑ:** column footprints + wall flush → bbox path (`footprintBounds` axis-aligned) → `rotation:0` πάντα (στροφή χαμένη). **ΛΥΣΗ = γενίκευση του ADR-514 Φ6d pad πρότυπου (μηδέν παράλληλο subsystem, Giorgio SSoT audit grep-verified):** `collectFoundationPadEdgeTargets`→**`collectFootprintEdgeTargets`** (πέδιλα + **μη-κυκλικές κολόνες** + **τοίχοι** outline ring) → zero-width edges μέσω κοινού `polylineEdgeTargets` → ΙΔΙΟΣ axis-relative `resolveLinearMemberFaceSnap` (slant). Bucket `padEdgeTargets`→**`footprintEdgeTargets`**· resolver `resolveColumnPadEdgeSnap`→**`resolveFootprintEdgeSnap`**. **Αποφάσεις Giorgio (locked):** (1) **edges για ΟΛΕΣ τις μη-κυκλικές** = ένα SSoT path (axis-aligned: ίδιο feel flush+corner)· (2) **κυκλική → μένει bbox** (NEW `collectCircularColumnFootprints` + `SceneSnapTargets.circularFootprints`)· (3) **τοίχοι ΚΑΙ** — dual-path (bbox κρατά center-on-axis + cursor-εντός· edges προσθέτουν slant cursor-εκτός). EXTRACT `wallOutlineRing` SSoT (reuse `closedRingFromEdges`, μοιράζεται `wallTarget`+edges). `footprints` bucket αμετάβλητο. **157 jest GREEN** (scene-snap-targets +5 NEW: pad/μη-κυκλική/κυκλική/εξάγωνο/τοίχος buckets· column-face-snap +5 NEW: διαμάντι 45° rotation/flush-on-slant, εξάγωνο, στραμμένο ορθογώνιο, axis-aligned rotation 0· bim-cursor-snap/foundation-preview updated για footprintEdgeTargets· regression column/framing/placement/preview GREEN). 3 code files (`member-snap-targets`/`scene-snap-targets`/`column-face-snap`) + 4 test files. tsc background (N.17). Μηδέν regression: τα 8 wall + ~12 column-preview + pad tests αμετάβλητα. ⚠️ ADR-040 (CHECK 6B/6D: snap/preview) → **stage ADR-040 + ADR-398 + ADR-514** μαζί. 🔴 browser-verify (κολόνα κοντά σε πολυγωνική/Γ/λοξή κολόνα ή τοιχίο → flush+στραμμένη+γωνία-με-γωνία· πέδιλο αμετάβλητο) + commit (Giorgio). **DEFER:** δοκάρια (short-end semantics)· L-bent τοίχοι ο cursor-εντός degenerate· beams slant. **ΜΑΘΗΜΑ:** «να ακολουθεί τη λοξάδα» = το έτοιμο axis-relative edge-snap (Φ6d) ήδη το λύνει — απλώς πέρασε ΚΑΙ τα column/wall footprints από τον ίδιο resolver (όπως τα πέδιλα), αντί bbox· κυκλική=gate, τοίχος=dual-path για μηδέν regression.
- **2026-06-23 (§3.17 REFINEMENT + 2 BUGFIX — browser-verified ✅, UNCOMMITTED)** — Giorgio report ότι το adopt «φούσκωνε» τη διατομή και χανόταν ο σοβάς. Τρεις αλλαγές, FULL SSoT/Revit-grade:
  1. **Detection upgrade (ήδη στο working tree):** Φ2 `getCachedRegionPerimeters`+`pickSmallestContainingPerimeter` → **`findRectanglesFromSegments`** (corner-graph· πιάνει ορθογώνια ΑΚΟΜΑ και με κοινές κορυφές με τοίχους, όπου ο simple-cycle walker αποτυγχάνει). Smallest-area-containing με boundary-inclusive `frameContainsWithTol`. EC2 §9.6.1 kind: reuse `rectColumnPlacement`/`isWallColumnKind` (aspect>4→shear-wall). Διαγνωστικά logs απέδειξαν detection ✓250 (μηδέν node-merge bug παρά το tol=50mm).
  2. **Πυρήνας = η περιοχή ΑΚΡΙΒΩΣ + σοβάς γύρω (Giorgio locked):** το σχεδιασμένο ορθογώνιο = ο **στατικός πυρήνας** → η κολόνα γεμίζει την περιοχή ακριβώς (μηδέν inset, μηδέν επηρεασμός `width/depth`). Ο σοβάς (ADR-449) = additive δέρμα **γύρω-γύρω** (default enabled). Απορρίφθηκαν 2 ενδιάμεσες λάθος προσεγγίσεις (`finish:enabled:false` που έσβηνε τον σοβά· inset πυρήνα κατά 2×πάχος).
  3. **🔴 ΡΙΖΑ του «250→300» (Giorgio hint «επεμβαίνει υπολογισμός στατικών»):** η κολόνα δημιουργούνταν `autoSized=AUTO` → ο auto-sizer **`buildColumnSizePatch`** (ADR-499 «auto-correcting organism») έτρεχε `suggestColumnSection` → ξανα-υπολόγιζε ελάχιστη επαρκή διατομή (λυγηρότητα/χωρητικότητα οπλισμού, module 50mm) → **έγραφε 300 πάνω στο 250**. **FIX:** η υιοθέτηση σχεδιασμένου ορθογωνίου = ρητή αρχιτεκτονική διάσταση → δημιουργία με **`autoSized: false`** (locked, user-wins· ο sizer δεν την αγγίζει). Reuse υπάρχοντος SSoT flag `ColumnParams.autoSized` (ADR-499/503) — μηδέν νέος μηχανισμός. Αλυσίδα 4 αρχεία: `ColumnParamOverrides.autoSized?` + spread στο `buildDefaultColumnParams` (`column-completion.ts`) → `commitColumnAt` sizeOverride.autoSized → `onAdoptRect` περνά `false` (`useColumnTool.ts`)· detection/proposal types σε `column-adopt-rect.ts`. tsc 0 errors. **ΜΑΘΗΜΑ:** όταν stored param αλλάζει «μετά» το build, ψάξε auto-correcting effects (ADR-499 sizers)· διάγνωση με logs (detection→proposal→params) αντί για μαντεψιά — 2 λάθος υποθέσεις (σοβάς/node-merge) απορρίφθηκαν με μέτρηση.
- **2026-06-23 (NEW feature: §3.17 Rectangle size adoption — η κολόνα «υιοθετεί» ορθογώνιο DXF, UNCOMMITTED)** — Giorgio: «οι υπάρχουσες κολόνες της κάτοψης είναι σχεδιασμένες ως ορθογώνια (κλειστή polyline/rectangle Ή 4 ξεχωριστές γραμμές). 1ο κλικ του εργαλείου «Κολόνα» ΜΕΣΑ σε τέτοιο ορθογώνιο → να με ΡΩΤΑΕΙ "κολόνα W×H όσο το ορθογώνιο ή default 40×40;". Ναι→μέγεθος+κέντρο+**γωνία** (KAI TA DYO). Όχι/κανένα ορθογώνιο→default αμετάβλητο. FULL ENTERPRISE+FULL SSOT όπως Revit, SSoT audit (grep) ΠΡΙΝ τον κώδικα». **ΑΠΟΦΑΣΗ (locked με Giorgio):** opt-in confirm (ΟΧΙ σιωπηλό auto-resize, ADR-487 §8.4)· «Ναι» → υιοθέτηση **μεγέθους+κέντρου+γωνίας** και **commit απευθείας** (το ορθογώνιο ορίζει πλήρως την κολόνα → μηδέν 2ο κλικ γωνίας· Revit «adopt»)· «Όχι» → κανονική ροή 2-κλικ· «Άκυρο»/ESC → noop. **FULL SSoT (audit grep-verified — ~85% υποδομής υπήρχε, μηδέν διπλότυπο):** NEW pure `bim/columns/column-adopt-rect.ts` = (α) `rectFrameToColumnDims` (RectFrame scene→mm + γωνία=atan2(u)), (β) `shouldProposeAdopt` (orthogonality + notable-diff 20mm + plausible 80–4000mm guards ώστε να ΜΗΝ ενοχλεί σε ≈default ή στο περίγραμμα κτιρίου), (γ) `findAdoptableRectUnderPoint` ΕΝΟΠΟΙΗΜΕΝΟ Φ1+Φ2: **Φ1** rectangle/closed-polyline → reuse `findRectContaining` πάνω στα `rectTargets`· **Φ2** 4 ξεχωριστές γραμμές → reuse `getCachedRegionPerimeters` (cached closed-loop SSoT, ΙΔΙΟ με «κολώνα από περίγραμμα», μηδέν O(n²)/click) + `pickSmallestContainingPerimeter` + `rectFrameFromCorners`. **collectRectTargets επεκτάθηκε** (ΚΕΝΟ Α handoff): εκπέμπει RectFrame ΚΑΙ από **κλειστή 4-κορυφη `polyline`/`lwpolyline`** (πραγματικές κορυφές → κρατά λοξή στροφή· bonus: ζωντανεύει το §3.15 Cartesian Magnet και σε polyline). NEW handshake store `column-adopt-size-confirm-store.ts` (mirror `column-perimeter-confirm-store`, actions adopt/default/cancel) + NEW self-subscribing dialog `ui/dialogs/ColumnAdoptSizeDialog.tsx` (createPortal + EscapeCommandBus + `dxf-modal-*` + i18n `columnAdoptSize` el/en· διαστάσεις locale-aware μέσω `formatLengthForDisplay`)· mounted στο `DxfViewerDialogs` (+lazy). NEW click hook `use-column-rect-adopt.ts` (async handshake + routing, εξήχθη από `useColumnTool` N.7.1)· wiring στο `useColumnTool.onCanvasClick` awaitingPosition = **1 short-circuit** `if (!s.slantMode && tryAdoptRectColumn(...)) return false` (adopt=`commitColumnAt` με overrides width/depth + anchor `center` + rotation· default=`setColumnRotationLock`). Reuse `buildDefaultColumnParams`/`commitColumnAt` (δέχονται ήδη width/depth/rotation) → μηδέν νέος builder. **17 jest GREEN** (column-adopt-rect 11: dims axis/λοξό/cm + threshold + Φ1 hit/εκτός/παραλληλόγραμμο-guard + Φ2 4-γραμμές· collect-rect-targets 6: closed-quad/λοξό/μη-κλειστό/L-shape/lwpolyline)· regression column/framing/drawing suites GREEN (πλην προϋπαρχόντων `useWallTool`+`floorplan-symbol` άλλου agent — wall FSM / symbol catalog, άσχετα). ⚠️ ADR-040 (CHECK 6B/6D: `useColumnTool`) → stage ADR-040+ADR-398. 🔴 browser-verify + commit (Giorgio). **ΜΑΘΗΜΑ:** «υιοθέτηση reference» = pure resolver (RectFrame→dims) + reuse υπάρχοντος detection (rectTargets Φ1 / cached perimeters Φ2) + reuse confirm-handshake pattern· το opt-in (ADR-487 §8.4) υλοποιείται ως Promise-gated dialog ΠΡΙΝ το commit, ΟΧΙ σιωπηλό resize.
- **2026-06-22 (REGRESSION FIX: §3.10b FSM → 2-click ΠΑΝΤΑ, UNCOMMITTED)** — Giorgio report: «μετά το 1ο κλικ τοποθετείται αμέσως η κολώνα· χάθηκε το 2ο κλικ που όριζε την περιστροφή (όπως ο τοίχος). Λειτουργούσε πριν.» **ΡΙΖΑ:** το §3.10b single-click gate (`useColumnTool.onCanvasClick`: `if (faceAnchor !== null) commitColumnAt(...)`) σχεδιάστηκε **ΜΟΝΟ για flush σε ΑΚΜΗ** (η γωνία ορίζεται από την ακμή). Οι **ΝΕΟΙ** κλάδοι §3.13 Polar + §3.15 Cartesian (+ νωρίτερα §3.9/§3.11 center-on-axis) επιστρέφουν `faceAnchor` (anchor `center`) για **ΟΛΟ το εσωτερικό** δίσκου/ορθογωνίου/άξονα → `faceAnchor !== null` παντού → **single-click commit παντού** → παρακάμφθηκε το 2-click place+rotate. **ΑΠΟΦΑΣΗ Giorgio (locked πριν τον κώδικα):** η κολώνα γίνεται **2-click ΠΑΝΤΑ** (mirror τοίχου) — το §3.10b single-click **αφαιρέθηκε τελείως**. Το 1ο κλικ κλειδώνει θέση+λαβή (→ `awaitingRotation`), το 2ο ορίζει τη γωνία (ελεύθερη), **και** στο flush/center/polar/cartesian. **FULL SSoT, μηδέν νέος μηχανισμός:** αφαιρέθηκε ΜΟΝΟ το `if (faceAnchor !== null) return commitColumnAt(...)` branch· το `faceAnchor` (όταν υπάρχει) χρησιμοποιείται πλέον ΜΟΝΟ ως `anchor` στο `setColumnRotationLock(point, anchor)`· το `point` είναι ΗΔΗ η snapped θέση (`mouse-handler-up`: `worldPoint = faceSnap.position`). **1 αρχείο** (`useColumnTool.ts`) + test (`useColumnTool.test.tsx`: stale single-click test → 1ο-κλικ-awaitingRotation + 2ο-κλικ-commit, 9 GREEN). Μηδέν αλλαγή σε snap geometry (§3.13/§3.15 αμετάβλητα), τοίχο, §3.7 (το flush απλώς γίνεται 2-click κι αυτό, απόφαση Giorgio). tsc skip (1-file trivial type· slot κατειλημμένο άλλου agent, N.17). **Follow-up (ίδια συνεδρία — άμεση συνέπεια του 2-click):** Giorgio report «μετά το 1ο κλικ μέσα στον δίσκο χάνονται οι πολικές συντεταγμένες → χάνω την αρχική καθοδήγηση». ΡΙΖΑ: το `generateColumnPreview` rot-branch (awaitingRotation) έκανε early-return ΧΩΡΙΣ να επισυνάψει `polarDiskGrid`/`rectGrid` (το πλέγμα ζούσε μόνο στο awaitingPosition path). FIX (`column-preview-helpers.ts`, FULL SSoT): EXTRACT κοινού `buildPlacementGridMeta(ref, targets, sceneUnits, polarOpts)` (μηδέν διπλό — το awaitingPosition tail το reuse) + κλήση στο rot-branch με `ref = rot.origin` (κλειδωμένη θέση) → το πολικό/καρτεσιανό πλέγμα **παραμένει ορατό κατά τη στρέψη** μαζί με την πορτοκαλί γραμμή γωνίας. Ο `drawing-hover-handler` το ζωγραφίζει ήδη χωρίς phase-gate. +1 jest (awaitingRotation μέσα σε δίσκο → polarDiskGrid defined + θέση=κλειδωμένη, 10 GREEN). **Follow-up (shared tree §3.11 agent):** το `getColumnFaceRotation`/`setColumnFaceRotation` (`ColumnPlacementGhostStatusStore`) έγινε **write-only dead state** (κανένας reader) — δεν αφαιρέθηκε εδώ (shared files + CHECK 6D). 🔴 browser-verify (κολώνα ΕΛΕΥΘΕΡΗ μέσα σε πλάκα/κύκλο/ορθογώνιο/κενό **και** flush σε ακμή → 2 κλικ θέση→γωνία· τοίχος αμετάβλητος) + commit (Giorgio). **ΜΑΘΗΜΑ:** όταν προστίθεται νέος «εντός περιοχής» κλάδος σε resolver που τροφοδοτεί FSM gate, ο gate πρέπει να ελεγχθεί — ένα `!== null` που ίσχυε για «flush σε ακμή» έγινε «παντού μέσα σε περιοχή».
- **2026-06-22 (NEW feature: §3.15 Cartesian Magnet + §3.16 auto-grid detector — κολώνα ΜΕΣΑ σε ΟΡΘΟΓΩΝΙΟ, UNCOMMITTED)** — Giorgio: «τι μαγικό για κολώνα μέσα σε ορθογώνιο;» → αδελφό του δίσκου (ορθογώνιο=καρτεσιανή περιοχή). **§3.15 (DONE):** 9-point (γωνίες+cover/μέσα-ακμών/κέντρο) + nice-offset grid + 4 dx/dy dims + Shift→κλάσματα· **λοξά+ίσια ΕΝΑ κώδικα** (local u/v)· χείλος→§3.11 edge. NEW pure `rect-cartesian-snap.ts` (resolve+buildRectGrid+findRectContaining+resolveRectCartesianDims) + leaf `rect-frame.ts` (RectFrame+rectFrameFromCorners, anti-cycle) + `SceneSnapTargets.rectTargets` (`collectRectTargets` reuse `rectangleCorners`) + `resolveRectHit` κλάδος (nearest-wins `nearestHit`) + overlay `rect-grid-paint.ts`→`drawRectGrid` hooks. Reuse `adaptiveDistanceStep`/`calculateDistance`/`polarClearanceScene`/`GhostFaceDimension`+`paintStraightDimension`/`applyOverlayLineStyle`+`strokeOverlaySegment`. **§3.16 (detector DONE, interaction DEFER):** NEW pure `rect-grid-fill.ts` (`rectGridPositions` ίσες αποστάσεις+περιθώρια, row-major, λοξά). Interaction (wheel `gridOverride`+rename store+ghost-set+one-click `appendEntitiesToScene`) defer (useColumnTool 450/500γρ). **15 jest GREEN** (10 rect-cartesian + 5 rect-grid· + regression 97 column-face-snap/preview/polar). tsc N.17 σειριακά. Μηδέν διπλότυπο (Giorgio SSoT audit — όλα reuse· `RectFrame` σε leaf για anti-cycle). ⚠️ ADR-040 (CHECK 6B/6D) → stage ADR-040+ADR-398. 🔴 browser-verify + commit. **ΜΑΘΗΜΑ:** ίδιο pattern με δίσκο — νέος gated κλάδος «cursor εντός περιοχής» στον υπάρχοντα resolver, reuse adaptive-step/geometry/dim-render/overlay· axis-aligned & rotated = ΕΝΑ u/v code path (ίσιο=γωνία 0°)· η μετρήσιμη math (snap+grid-fill) κλείνει & testάρεται ανεξάρτητα από το interaction.
- **2026-06-22 (NEW feature: §3.13 Polar Magnet + §3.14 symmetry detector — κολώνα ΜΕΣΑ στον δίσκο, UNCOMMITTED)** — Giorgio: «τι μαγικό για κολώνα μέσα στον δίσκο κύκλου; FULL ENTERPRISE + FULL SSOT όπως Revit, SSoT audit (grep) ΠΡΙΝ τον κώδικα». **§3.13 (ΘΕΜΕΛΙΟ, DONE):** ο δίσκος = πολικό σύστημα (κέντρο/δακτύλιοι/ακτίνες nice-numbers zoom-adaptive)· snap σε κέντρο / δακτύλιο∩ακτίνα· R/θ listening dims· στο χείλος → §3.12 circumference (nearest-wins). NEW pure `polar-disk-snap.ts` (`resolvePolarDiskSnap`+`buildPolarDiskGrid`+`findDiskContaining`+`polarClearanceScene`) — reuse `adaptiveDistanceStep`/`quantizeMagnitude`+`pointOnCircle`/`calculateAngle`+`buildCenteredAxisFaceFrame(arc)`· ΜΟΝΑΔΙΚΗ νέα math = 5 λόγοι κλασμάτων + nice-angle quantize. NEW `SceneSnapTargets.diskTargets` (`collectDiskTargets`, reuse circle read). `resolveColumnFaceSnapFromTargets` +opts +polar κλάδος (nearest-wins `nearestHit`). NEW `column-polar-opts.ts` (κεντρικά opts, ghost≡commit). NEW thin `ColumnPolarStore` (zero-React· Shift wiring event-time στο `mouse-handler-move`). NEW overlay `polar-disk-paint.ts` → `PreviewRenderer.drawPolarDisk` → `PreviewCanvas` handle → `drawing-hover-handler` (ΙΔΙΟ cyan dashed SSoT, ADR-040). **§3.14 (ΔΙΑΜΑΝΤΙ — detector DONE, interaction DEFER):** NEW pure `polar-symmetry-detector.ts` (`detectRingFold`: gcd auto-detect + scroll override {3,4,6,8,12}· δίνει ghost γωνίες). Interaction (ghost-set+wheel+ring-commit via `appendEntitiesToScene`) defer — `useColumnTool` 450/500γρ. (split needed) + browser-verify unit. **18 jest GREEN** (10 polar-disk-snap + 8 symmetry-detector· + regression 77 column-face-snap/preview). tsc N.17 σειριακά. Μηδέν διπλότυπο (Giorgio SSoT audit ✅ — όλα reuse, grep-verified). ⚠️ ADR-040 (CHECK 6B/6D) → stage ADR-040+ADR-398. 🔴 browser-verify + commit. **ΜΑΘΗΜΑ:** το «πολικό πλέγμα» δεν χρειάζεται νέο snap subsystem — είναι ΕΝΑΣ νέος κλάδος στον υπάρχοντα column resolver (gated «εντός δίσκου») + reuse adaptive-step/polar-math/arc-frame· το διάσπαρτο interaction (wheel/commit) απομονώνεται από τη μετρήσιμη math (detector) ώστε η math να κλείνει & να testάρεται ανεξάρτητα.
- **2026-06-22 (NEW feature: §3.12 arc-length listening dimensions — ΚΥΚΛΟΣ + ΤΟΞΟ, UNCOMMITTED)** — Giorgio: «σε κύκλο/τόξο η μέτρηση να είναι ΜΗΚΟΣ ΤΟΞΟΥ με καμπύλη dim line (AutoCAD ⌒), FULL ENTERPRISE που εξυπηρετεί ΚΑΘΕ λογική χρήστη, κύκλος ΚΑΙ τόξο». Ενοποιημένο μοντέλο (κύκλος=τόξο 0→360°): 2 καμπύλα arc gaps προς bracketing datums (τεταρτημόρια QUADRANT osnap **εντός span** + πραγματικά άκρα τόξου, `arcListeningDatums`) + ευθεία ακτίνα R, config-driven (`arc-listening-dim-config` leaf: showArcGaps/showRadius/labelMode `length|angle|both`). **Carry «curve» μέσα από το pipeline (μηδέν νέος μηχανισμός):** NEW `ArcMeta` → `LinearMemberSnapTarget.arc?` (`circleTargets`/νέο `arcTargets`) → `GhostFaceFrame.arc?` → `resolveGhostFaceDimensions` arc branch (gated). **Reuse arc math SSoT (grep-verified):** `calculateArcLength`(s=r·θ)/`calculateAngle`/`pointOnCircle`/`arcToPolyline` — ΜΗΔΕΝ νέο atan2/tessellation. Painter NEW curved branch (sample `arcToPolyline` σε radius+offset + ακτινικές extension lines + ετικέτα length/angle) — ΙΔΙΟ 0.5px dashed cyan overlay SSoT με τα ευθεία. Κολώνα re-projected ΑΚΡΙΒΩΣ στην περιφέρεια. **Ευθείς στόχοι byte-for-byte αμετάβλητοι** (arc = gated νέος κλάδος). **91 jest GREEN** · tsc clean (δικά μου). ⚠️ ADR-040 (CHECK 6B/6D) → stage ADR-040+ADR-398. 🔴 browser-verify + commit. **ΜΑΘΗΜΑ:** «κάθε λογική χρήστη» = ενοποιημένο datum set (ένωση quadrants+endpoints) + label-format toggle πάνω στην ΙΔΙΑ γεωμετρία — ΟΧΙ N παράλληλα subsystems. **DEFER:** datum προς κοντινά άλλων οντοτήτων· UI toggle (config ready)· bulge arcs· λοξό ορθογώνιο.
- **2026-06-22 (NEW feature: center-on-axis σε ΠΟΛΥΓΡΑΜΜΗ + ΟΡΘΟΓΩΝΙΟ + ΚΥΚΛΟ — §3.11 επέκταση, UNCOMMITTED)** — Giorgio: «ίδια ολίσθηση και σε πολυγραμμή / ορθογώνιο / κύκλο». Όλα = zero-width edges (ίδιο μοντέλο με σκέτη γραμμή/ακμή πλάκας), μέσω του **κοινού** `edgeBandTarget`. **FULL SSoT (Giorgio audit, grep-verified):** EXTRACT κοινού **`polylineEdgeTargets(pts, closed, idPrefix)`** (ένα edge ανά τμήμα) — το `slabEdgeTargets` (closed πολύγωνο) ΚΑΙ το νέο `polylineTargets` (polyline/lwpolyline) το μοιράζονται. **ΟΡΘΟΓΩΝΙΟ:** `rectangleTargets` → reuse το BIM SSoT `rectangleCorners` (wall-from-entity, το ίδιο που καταναλώνει το perimeter-from-faces) → 4 πλευρές κλειστές. **ΚΥΚΛΟΣ:** `circleTargets` → reuse το public SSoT `arcToPolyline` (GeometryUtils, κύκλος = 360° τόξο → tessellation 24 χορδών) → η κολώνα ολισθαίνει στην περιφέρεια εφαπτομενικά. Όλα funnel-άρουν στο `lineTargets` (column-only) → `resolveColumnEdgeSnap`. ΜΗΔΕΝ νέο tessellation/corner math (reuse 2 υπάρχοντα SSoT· δεν προστέθηκε 4ο rectangleCorners duplicate — υπάρχουν ήδη, flagged για boy-scout). **110 jest GREEN** (column 63 incl. polyline L-shape/closed + lwpolyline + rect + circle + collect counts· + framing suites) · tsc clean. 🔴 browser-verify (κολώνα σε πολυγραμμή/ορθογώνιο/κύκλο) + commit. **DEFER:** §dim **arc-length** για κύκλο/τόξο (Giorgio επέλεξε Revit-grade arc-length — νέο feature, ξεχωριστή συνεδρία)· bulge arcs πολυγραμμής → ευθεία χορδή· λοξό ορθογώνιο (rotation) axis-aligned (consistent με perimeter-from-faces)· ΤΟΞΟ (μόνο κύκλος τώρα).
- **2026-06-22 (NEW feature: center-on-axis ΚΑΙ σε σκέτη ΓΡΑΜΜΗ — §3.11 επέκταση, UNCOMMITTED)** — Giorgio: «το φάντασμα κολώνας να ολισθαίνει πάνω σε μια ΓΡΑΜΜΗ όπως στις πλευρές πλάκας — εκατέρωθεν + κατά μήκος του άξονα, FULL SSoT». Οι σκέτες γραμμές (`LineEntity`, top-level `start`/`end`) δεν ήταν face-snap στόχοι. **FIX = ΙΔΙΟ zero-width μοντέλο με ακμή πλάκας (Giorgio SSoT):** EXTRACT κοινού **`edgeBandTarget(a,b,id)`** (το ±eps thin-band → `LinearMemberSnapTarget`) στο `member-snap-targets.ts` — το `slabEdgeTargets` (ανά ακμή) ΚΑΙ νέο `lineTarget` (σκέτη γραμμή) το μοιράζονται (μηδέν διπλό band math, grep-verified: η band κατασκευή υπάρχει σε ΕΝΑ σημείο). NEW `MemberSnapKind 'line'` + collect. NEW `SceneSnapTargets.lineTargets` (column-only· **ΟΧΙ** στο `selectGhostMembers` → wall/beam T-framing αμετάβλητα). `resolveColumnSlabEdgeSnap`→**`resolveColumnEdgeSnap`** (param `edges`· helpers `slabEdge*`→`edge*`) δέχεται **concat slab+line** (ίδιο axis-relative path → flush εκατέρωθεν + §3.11 center-on-axis + λοξές). Μηδέν νέος resolver. **99 jest GREEN** (column-face-snap 54 incl. +6 NEW γραμμή + collect-line· + ΟΛΑ τα framing suites incl. member-snap-targets-slab → `edgeBandTarget` behavior-preserving) · tsc clean. **ΜΑΘΗΜΑ:** «γραμμή ΟΠΩΣ ακμή πλάκας» = ίδιο geometric μοντέλο (zero-width band) → extract τον builder + όνομασέ τον generic (`slabEdge`→`edge`), ΟΧΙ δεύτερο path. 🔴 browser-verify (κολώνα πάνω σε σκέτη γραμμή: cursor στη γραμμή → κέντρο+slide· εκατέρωθεν → flush· λοξή → στραμμένη) + commit (Giorgio).
- **2026-06-22 (NEW feature: center-on-axis ΚΑΙ σε ΔΟΚΑΡΙ — §3.11 επέκταση, UNCOMMITTED)** — Giorgio: «η κολώνα να ολισθαίνει και κατά μήκος του ΚΕΝΤΡΙΚΟΥ άξονα του ΔΟΚΑΡΙΟΥ, ΙΔΙΟΣ κώδικας με ακμή πλάκας & άξονα τοίχου, FULL SSoT». Τα δοκάρια περνούσαν **μόνο** bbox-flush (καμία axis-center). **FIX = ΓΕΝΙΚΕΥΣΗ του §3.9 wall path σε ΚΑΘΕ παχύ γραμμικό μέλος (Giorgio SSoT):** rename `WallAxisFrame`→`MemberAxisFrame`, `buildWallAxisFrame`→`buildMemberAxisFrame`, `resolveWallAxisCenter`→`resolveMemberAxisCenter`, πεδίο `FaceTarget.wallFrame`→`axisFrame`. Στο `buildFaceTargets` τα **δοκάρια** παίρνουν τώρα κι αυτά `axisFrame` (όπως οι τοίχοι) → ρέουν μέσα από τον **ΙΔΙΟ** `resolveMemberAxisCenter` (threshold ημι-πάχος/2, ίδιο τοίχο/δοκάρι· `resolveAxisCenterFoot` + `buildCenteredAxisFaceFrame` SSoT). Οι κοντές άκρες δοκαριού μένουν 🔴 overlap στο flush (έξω από τη ζώνη). **Bonus — λοξά μέλη στρέφονται τώρα κι αυτά:** NEW κοινό `axisAlignmentRotationDeg` (+`isAxisAligned`) εξάλειψε το inline atan2 του slab path· `resolveMemberAxisCenter` το χρησιμοποιεί → λοξό δοκάρι/τοίχος → κέντρο στον λοξό άξονα + στραμμένη κολώνα (πριν: §3.9 hardcoded rotation 0· τώρα consistent με §3.10b). Μηδέν νέο wiring (ρέει μέσω §3.10 pipeline)· μηδέν αλλαγή σε `linear-member-face-snap` (T-framing τοίχου/δοκαριού). **42 column-face-snap jest GREEN** (+6 NEW δοκάρι: on-axis center, εσωτερική ζώνη, slide, εξωτερική→flush, κοντή άκρη→overlap, λοξό 45°· + ανεστραμμένο το πρώην «δοκάρι ΔΕΝ παίρνει center») · tsc clean. **ΜΑΘΗΜΑ:** «ίδιος κώδικας με Χ» = γενίκευσε το όνομα/πεδίο (wall→member) και δώσε το ΙΔΙΟ frame στον νέο τύπο στόχου — ΟΧΙ δεύτερος resolver. 🔴 browser-verify (κολώνα σε δοκάρι: cursor στον άξονα → κέντρο+slide· σε πλευρά → flush) + commit (Giorgio).
- **2026-06-22 (NEW feature: center-on-axis ολίσθηση σε ΑΚΜΗ ΠΛΑΚΑΣ — §3.11, UNCOMMITTED)** — Giorgio: «ο ΑΞΟΝΑΣ της κολώνας να ΟΛΙΣΘΑΙΝΕΙ ΠΑΝΩ στον άξονα της γραμμής, ΟΧΙ μόνο δεξιά/αριστερά/πάνω/κάτω της». Το «center-on-axis vs flush, nearest-reference-wins» **υπήρχε ήδη** για τοίχους (§3.9 `resolveWallAxisCenter`, bbox path), αλλά οι ακμές πλάκας περνούσαν ΞΕΧΩΡΙΣΤΑ από τον axis-relative `resolveColumnSlabEdgeSnap` (`memberWidthScene=0`) που επέστρεφε **ΜΟΝΟ flush**. **FIX = ΓΕΝΙΚΕΥΣΗ, ΟΧΙ νέος μηχανισμός (Giorgio SSoT audit):** EXTRACT κοινού SSoT core **`resolveAxisCenterFoot`** (project cursor→άξονα + along-range + perp-threshold → foot· reuse `projectPointOnAxis`) που **μοιράζονται §3.9 (wall) ΚΑΙ §3.11 (slab)** — η §3.9 ξαναγράφτηκε να τον καλεί (behavior-preserving). Center-on-axis κλάδος στον slab resolver: cursor κάθετα εντός **±`SLAB_EDGE_CENTER_THRESHOLD_MM`=150** από τον άξονα → `anchor:'center'` + foot στον άξονα + ολίσθηση κατά μήκος· πέρα → υπάρχον §3.10b flush. **Threshold:** η ακμή έχει μηδενικό πάχος → δεν υπάρχει «εσωτερική ζώνη» όπως ο τοίχος· σταθερό ±150mm (απόφαση Giorgio, αριθμητικό παράδειγμα). **Λοξές ακμές διατηρούνται (§3.10b):** center-on-axis σε λοξή ακμή → κέντρο στον λοξό άξονα + `rotation`=γωνία ακμής (στραμμένη). **DEDUP (N.0.2 Boy Scout, grep-verified):** το centered `GhostFaceFrame` literal διπλασιαζόταν (§3.9 + §3.11) → EXTRACT `buildCenteredAxisFaceFrame` κοινό. **Μηδέν νέο wiring:** `rotation`/`anchor`/`position` ρέουν ήδη μέσω §3.10 pipeline (preview ≡ commit)· μηδέν αλλαγή στο `linear-member-face-snap` (shared με wall/beam T-framing). Function-size N.7.1 ≤40γρ μέσω helpers `slabEdgeNearFace`/`slabEdgeFlushAnchor`/`buildSlabEdgeCenterSnap`. **36 column-face-snap jest GREEN** (31 υπάρχοντα incl. §3.9 wall regression + **5 NEW §3.11**: οριζόντια/κάθετη/λοξή center· slide· flush πέρα από threshold) · tsc clean. **ΜΑΘΗΜΑ:** όταν το center-on-axis υπάρχει ήδη σε ένα path (bbox/wall), η επέκταση σε άλλο path (axis-relative/slab) = EXTRACT κοινού core + branch, ΟΧΙ νέο resolver. 🔴 browser-verify (κολώνα σε ακμή: cursor στη γραμμή → κέντρο+slide· σε πλευρά → flush· λοξή → κέντρο+στραμμένη) + commit (Giorgio) — CHECK 6B/6D ⇒ stage ADR-398.
- **2026-06-22 (fix: λοξές ακμές πλάκας — §3.10b flush rotation, UNCOMMITTED)** — Giorgio report: σε **ΛΟΞΕΣ** (όχι οριζόντιες/κάθετες) ακμές πλάκας το φάντασμα κολώνας έμενε **πάντα ορθό** — δεν ακολουθούσε τη λοξάδα. **ΡΙΖΑ:** ο `resolveColumnSlabEdgeSnap` χαρτογραφούσε ΚΑΘΕ ακμή στην πλησιέστερη axis-aligned παρειά (N/S/E/W) → πετούσε τη γωνία. **FIX:** NEW πεδίο `rotation` στο `ColumnFaceSnap` (0 για axis-aligned → μηδέν regression· = γωνία **λοξής** ακμής). Slab resolver: axis-aligned → υπάρχον N/S/E/W (rotation 0)· **λοξή** → `rotation = atan2(axisDir)` + anchor εσωτερικής παρειάς (n/s-family ανά `outwardSign`+`third`) ώστε το πλάτος να τρέχει κατά μήκος της ακμής (reuse `transformFootprint` anchor-shift→rotate→translate — μηδέν νέα γεωμετρία). Ghost (`column-preview-helpers`) + commit εφαρμόζουν το `rotation`. **FSM (Revit-grade):** face-snapped κολώνα = ΗΔΗ ευθυγραμμισμένη → **single-click commit** στο 1ο κλικ (NEW `faceRotation` handoff στο `ColumnPlacementGhostStatusStore`)· place+rotate (2-click) μένει ΜΟΝΟ για ΕΛΕΥΘΕΡΗ τοποθέτηση. 61 column jest GREEN (NEW λοξή 45° + axis-aligned rotation-0) · tsc clean. DEFER: λοξοί τοίχοι/δοκάρια (bbox path) + §3.9 wall-axis παραμένουν axis-aligned (bbox δεν φέρει γωνία· μόνο το slab path είναι axis-relative). 🔴 browser-verify (κολώνα σε λοξή ακμή πλάκας → φάντασμα στραμμένο flush· single-click) + commit.
- **2026-06-22 (§3.10 code commit — sync-in-preview unification)** — Ο κώδικας της §3.10 (NEW `bim/framing/scene-snap-targets.ts`, `column-face-snap.ts`, `column-preview-helpers.ts`, `useColumnTool.ts`, `mouse-handler-up.ts`· delete `column-preview-store.ts`) έγινε commit μαζί με αυτό το ADR (CHECK 6D: canvas-drawing `mouse-handler-up.ts` ⇒ stage ADR-398). Το αναλυτικό §3.10 entry παρακάτω.
- **2026-06-22 (NEW: sync-in-preview unification — §3.10, UNCOMMITTED)** — Το column face-snap ενοποιήθηκε με τοίχο/δοκάρι: υπολογίζεται **σύγχρονα στο preview ΚΑΙ στο commit** από pre-collected στόχους (NEW `bim/columns/column-preview-store.ts`), αντί async στον scheduler → 3 stores. **Αποτέλεσμα: οι ακμές πλάκας πλέον δουλεύουν στην κολώνα ταυτόσημα με τοίχο/δοκάρι** (η ρίζα του handoff 2026-06-22). **FULL SSoT — reuse, μηδέν διπλότυπο:** `resolveColumnFaceSnap` → thin wrapper γύρω από NEW core `resolveColumnFaceSnapFromTargets` + NEW `collectColumnFaceSnapTargets` (ΕΝΑ populator για store+wrapper+commit)· `column-preview-helpers`/`mouse-handler-up` καλούν ΤΟΝ ΙΔΙΟ resolver + ΙΔΙΟΥΣ στόχους + `resolveEffectivePreviewCursor` (=`ImmediateSnap`) → preview ≡ commit εξ ορισμού. Scheduler face-branch (`resolveColumnFaceSnapWithGlyph`/`applyColumnFaceSnap`/`applyColumnGhostStatus`/3-store writes) **αφαιρέθηκε**· corner-projection + glyph publishing + ImmediateSnap **διατηρήθηκαν** (η μαγνητική έλξη σε χαρακτηριστικό + οι γλυφές έρχονται ΔΩΡΕΑΝ μέσω του normal path → ImmediateSnap, mirror τοίχου/δοκαριού). **DEDUP (Giorgio SSoT audit):** το «re-sync on `drawing:entity-created` + rAF defer» effect ήταν copy-pasted σε wall/beam· με την κολώνα = triplicate → EXTRACT σε NEW SSoT `hooks/drawing/use-scene-snap-target-sync.ts` (`useSceneSnapTargetSync`), **και τα 3** tools το reuse. `ColumnPlacementGhostStatusStore` → commit-time handoff (status+faceAnchor μόνο· `faceFrame` αφαιρέθηκε)· `getEntities` αφαιρέθηκε από `SnapDetectionInput`/`mouse-handler-move` (νεκρό). **Production-unused πλέον** (follow-up cleanup): `resolveColumnFaceSnapWithGlyph` **διαγράφηκε**· `resolveColumnGhostStatusFromSnap`+`findColumnOverlap` έμειναν (tested). 38 jest (column-face-snap incl. NEW slab tests + core-≡-wrapper + column-preview-helpers sync). ⚠️ ADR-040 critical (`snap-scheduler`/`mouse-handler-up`/`mouse-handler-move`) → CHECK 6B/6C/6D: stage ADR-040 + ADR-398. ⚠️ shared tree (useWallTool/useBeamTool migration → SSoT hook): behavior-preserving, συντονισμός staging. **ΜΑΘΗΜΑ:** δύο placement tools με διαφορετική αρχιτεκτονική snap = anti-pattern· η σύγχρονη-στο-preview ενοποίηση εξαφανίζει ολόκληρη την κλάση bug «δουλεύει στο Α όχι στο Β»· η μαγνητική έλξη/γλυφές δεν χρειάζονται tool-specific resolver — έρχονται από το ImmediateSnap (ίδιο pattern με τοίχο/δοκάρι). 🔴 tsc (σειριακά N.17) + browser-verify ΟΛΩΝ των σεναρίων (§5 handoff: κολώνα→κολώνα/δοκάρι/τοίχο+§3.9 άξονας/ακμή πλάκας, glyphs, place+rotate) + commit (Giorgio).
- **2026-06-22 (bugfix: WYSIWYG column ghost = διακεκομμένο περίγραμμα — §3.8, UNCOMMITTED)** — Giorgio report (`/dxf/viewer`): με ενεργό το εργαλείο «Κολώνα» το **φάντασμα** είχε **διακεκομμένο** περίγραμμα, ενώ η **τοποθετημένη** κολώνα είναι **συνεχές** — ζητήθηκε byte-identical solid και στο ghost. **ΡΙΖΑ (render-state leak):** ο `ColumnRenderer` στο **outline stroke** (`ColumnRenderer.ts` ~§161) **δεν** έθετε ρητά `setLineDash` πριν το `stroke()` → κληρονομούσε ό,τι dash υπήρχε στο canvas context. Στον committed path (`DxfRenderer`) το context τύχαινε solid → συνεχές. Στο **shared PreviewCanvas** ένα προηγούμενο **tracking/polar paint** άφηνε ενεργό dash pattern (το `clearRect` δεν επαναφέρει το `lineDash`, και το `BimPreviewRenderer.save/restore` διατηρεί το dash που υπήρχε ΠΡΙΝ το save) → «διέρρεε» μέσα στο WYSIWYG ghost → διακεκομμένο. **FIX (self-contained renderer, mirror WallRenderer §261/§482 + του hover-halo path §118):** ρητό `this.ctx.setLineDash([])` πριν το footprint outline stroke. Continuous linetype = ίδιο με committed → **μηδέν regression** στον committed (ήδη solid), διορθώνει το preview. ⚠️ ADR-040 critical (entity renderer) → CHECK 6D: stage ADR-040 + ADR-398 στο commit. 🔴 browser-verify (φάντασμα κολώνας = συνεχές περίγραμμα, ίδιο με τοποθετημένη) + commit (Giorgio).
- **2026-06-21 (bugfix: ghost «παγώνει» στο zoom με ροδάκι — §4 preview-render, UNCOMMITTED)** — Giorgio report (`/dxf/viewer`): με ενεργό φάντασμα δημιουργίας (κολώνα/τοίχος/δοκάρι/…) στον κέρσορα, **zoom με το ροδάκι ΧΩΡΙΣ κίνηση κέρσορα** άφηνε το ghost παγωμένο στην παλιά κλίμακα/θέση — διορθωνόταν μόνο στο επόμενο `mousemove`. **ΡΙΖΑ:** δύο preview paths ξανασχεδίαζαν **μόνο σε cursor event**. (Α) `PreviewCanvas`/`PreviewRenderer` (WYSIWYG ghosts κολώνα/τοίχος/δοκάρι/γραμμή/κύκλος/διάσταση): το `drawPreview()` καλείται imperatively από `useUnifiedDrawing.updatePreview` (mousemove)· στο zoom άλλαζε το transform αλλά (i) **κανείς δεν ξανακαλούσε `drawPreview`** ΚΑΙ (ii) ο `render()` ζωγράφιζε με το **cached** transform → frozen. (Β) `useCanvasGhostPreview` (MEP/openings/move/rotate/…): ξανασχεδίαζε μέσω **React-prop** `transform` (effect dep) → λειτουργούσε αλλά με 1-2 frame lag. **FIX (Revit/AutoCAD world-locked, FULL SSoT — χρήση ΥΠΑΡΧΟΝΤΟΣ μηχανισμού, μηδέν νέο):** το cached preview entity ζει σε **world coords** → **μηδέν re-snap**, αρκεί re-paint με το live transform (κλειδωμένο στο world-σημείο). Δεν προστέθηκε νέα subscription/μέθοδος· αξιοποιήθηκε ο **ΗΔΗ υπάρχων** «transform change → dirty canvas» SSoT (`updateImmediateTransform` → `markSystemsDirty(TRANSFORM_CANVAS_IDS)`) που οδηγεί ήδη `dxf-canvas`+`layer-canvas`. (1) **Path A:** `TRANSFORM_CANVAS_IDS += 'preview-canvas'` (ImmediateTransformStore) → το preview-canvas (registered scheduler system) γίνεται dirty σε ΚΑΘΕ zoom/pan/fit· `PreviewRenderer.render()` διαβάζει **live `getImmediateTransform()`** αντί cached `currentTransform` (ίδιο read με τον main `dxf-canvas-renderer`) → ο scheduler ξαναζωγραφίζει το cached world ghost στο νέο scale. `drawPreview` έχασε το `transform` param (μοναδικός caller = `PreviewCanvas` handle)· `currentTransform` field διαγράφηκε. **Το παλιό race (γιατί είχε εξαιρεθεί το preview-canvas, ImmediatePositionStore:27) λύθηκε**: το entity αλλάζει μόνο σε mousemove, όχι σε pan/zoom, άρα μηδέν stale-entity. (2) **Path B:** `useCanvasGhostPreview` (RAF-direct, ΟΧΙ registered system → δεν πιάνεται από markSystemsDirty) → αντικατάσταση του React-prop redraw-trigger με live `subscribeTransform` (zero-lag, ίδιο pattern με `CoordinateDebugOverlay`· `transform` prop @deprecated, backward-compat για ~19 consumers). **Κοινό SSoT:** ΚΑΙ τα δύο paths οδηγούνται από το ΕΝΑ `updateImmediateTransform` (που ειδοποιεί scheduler **και** listeners) + διαβάζουν live από το ΙΔΙΟ `ImmediateTransformStore`. ⚠️ ADR-040 critical (`ImmediateTransformStore`/`PreviewCanvas`/`PreviewRenderer`) → CHECK 6B/6D: stage ADR-040 + ADR-398. 13 jest (3 `preview-renderer-live-transform` + 2 `immediate-transform-store-preview-dirty` + 8 `useCanvasGhostPreview`). **ΜΑΘΗΜΑ (Giorgio SSoT audit):** πριν φτιάξεις νέο «transform→redraw» μηχανισμό, ψάξε τον υπάρχοντα — εδώ ζούσε ήδη ο `markSystemsDirty`/`TRANSFORM_CANVAS_IDS` dirty-scheduler SSoT (τον χρησιμοποιούν ViewportStore/GridRenderer/grips) + ο main canvas ήδη διάβαζε `getImmediateTransform()` live. Η 1η μου λύση (νέα `refreshTransform` + νέα `subscribeTransform` στο PreviewCanvas) ήταν **παράλληλο διπλότυπο** — αντικαταστάθηκε. 🔴 browser-verify (ghost ακολουθεί zoom/pan live, world-locked) + commit (Giorgio).
- **2026-06-21 (NEW feature: Wall-axis CENTER snap — §3.9, UNCOMMITTED)** — Το εργαλείο «Κολώνα» πάνω σε **τοίχο** απέκτησε **center-on-axis** (κέντρο κολώνας ≡ άξονας τοίχου, anchor `center`, 🟢) — ακριβές **mirror του §3.1b «Column→Beam axis»**, αλλά για τοίχο, κατά απαίτηση Giorgio. **ΧΩΡΙΣ split** (η φάτνωμα-ανά-κολώνα υποδιαίρεση = αναλυτικό μοντέλο organism/FEM — ADR-487). **Απόφαση: nearest-reference-wins** (Revit centerline+face references): εσωτερική μισή ζώνη (perp ≤ ½ ημι-πάχους) → center-on-axis· εξωτερική ζώνη/άκρες → υπάρχον §3.7 flush (μηδέν regression). **FULL SSoT — ΕΝΑ αρχείο, μηδέν νέο wiring/store/geometry/projection:** επέκταση **μόνο** του `bim/columns/column-face-snap.ts` (NEW `WallAxisFrame` + `buildWallAxisFrame` + `resolveWallAxisCenter`) με **reuse `projectPolygonOnAxis`/`projectPointOnAxis`** SSoT· το αποτέλεσμα ρέει αυτούσιο μέσα από την υπάρχουσα §3.7 υποδομή (`snap-scheduler`/`mouse-handler-up`/`commitColumnFromState` αμετάβλητα) → **preview ≡ commit**. 27 column-face-snap jest (21 §3.7 + 6 §3.9) + `column-placement-snap-context` 15 GREEN. **ΜΑΘΗΜΑ:** όταν το ζητούμενο είναι «νέα precedence πάνω σε υπάρχον resolver», βάλε το branch **μέσα** στο resolver (εδώ: axis πριν το face) και άσε ολόκληρο το downstream wiring/store άθικτο — η center-anchor/status ρέει ήδη μέσω της §3.7 υποδομής. 🔴 tsc (σειριακά N.17) + browser-verify + commit (Giorgio).
- **2026-06-21 (NEW feature: WYSIWYG column ghost — §3.8, UNCOMMITTED)** — Το εργαλείο «Κολώνα» απέκτησε **ΕΝΑ WYSIWYG φάντασμα** οπτικά ταυτόσημο με την τελική κολώνα (Revit-grade, Giorgio order), αντικαθιστώντας τις **3 ασύμβατες εμφανίσεις** (9-anchor schematic μακριά / single-anchor κοντά / πραγματικός renderer τοποθετημένη). **FULL SSoT — μηδέν νέο geometry/store, μπαίνει στο WYSIWYG path δοκαριού/τοίχου:** NEW pure `hooks/drawing/column-preview-helpers.ts` (`generateColumnPreview`, mirror `beam-preview-helpers`) — επιστρέφει ΠΛΗΡΕΣ `ColumnEntity` flagged `wysiwygPreview` μέσω των **ίδιων builders με το commit** (`buildDefaultColumnParams`+`buildColumnEntity`) ⇒ ο `BimPreviewRenderer` (entity-agnostic) το ρεντάρει με τον πραγματικό `ColumnRenderer`. **preview === commit:** θέση `getImmediateSnap()`, λαβή/status από τα §3.7 imperative SSoT (`getColumnFaceAnchor`/`getColumnGhostStatus`, **ΙΔΙΑ anchor precedence με `commitColumnFromState`**)· kind/anchor/overrides από το ΥΠΑΡΧΟΝ `columnToolBridgeStore` (μηδέν νέο preview-store). Wiring: `drawing-preview-generator` branch `column` + `useUnifiedDrawing.updatePreview` `isColumn` gating (mirror `isBeam`). 🔴 overlap → red schematic (PreviewRenderer)· 🟢/neutral → πλήρες WYSIWYG (το έγκυρο visual, απόφαση «Α»). **Κατάργηση 9-anchor chain (full clean delete, Giorgio Q-scope):** διαγράφηκαν `useColumnGhostPreview`, `ColumnGhostPreviewMount`, `ColumnAnchorGhostRenderer`, `column-anchor-ghosts`(+test), `column-tool-ghost-overrides`· αφαιρέθηκε `getGhostFootprints` (`useColumnTool`) + το `columnGhost` prop chain (`CanvasSection`→`CanvasLayerStack`→leaves→types). ⚠️ ADR-040 critical (`CanvasSection`/`CanvasLayerStack`/`PreviewRenderer`) → CHECK 6B/6D: stage ADR-040. Tab anchor cycling ΠΑΡΑΜΕΝΕΙ (μέσω anchor precedence). 8 column-preview-helpers jest + column/framing regression GREEN (73 total). **ΜΑΘΗΜΑ:** όταν υπάρχει ήδη WYSIWYG preview path (δοκάρι/τοίχος μέσω `drawing-preview-generator` + `BimPreviewRenderer`), βάλε το νέο μέλος **μέσα** του (reuse commit builders + imperative snap SSoT) αντί για ξεχωριστό RAF schematic ghost — ΕΝΑ φάντασμα, μηδέν morphing, μηδέν dead code. **Part B (μέγεθος/auto-shrink 40→25, ADR-499/503/487):** ΞΕΧΩΡΙΣΤΟ domain άλλου πράκτορα — flagged, ΔΕΝ αγγίχτηκε. 🔴 browser-verify (φάντασμα free/snap/τοποθετημένη ίδιο visual· έλξεις §3.7 δουλεύουν) + commit (Giorgio).
- **2026-06-21 (§3.7 follow-up: τοίχος έλξεις + πράσινες μικρές πλευρές, UNCOMMITTED)** — Giorgio report (κολώνα κοντά σε τοίχο): (1) ΔΕΝ εμφανίζονταν οι **έλξεις/ετικέτες** «ΓΩΝΙΑ/ΜΕΣΟ ΤΟΙΧΟΥ» στις κορυφές/μέσα· (2) οι **μικρές πλευρές** τοίχου έδειχναν 🔴 ενώ επιτρέπεται κολώνα. **Fix #1 (έλξεις):** το face-snap path έκανε `setFullSnapResult(null)` (κατέπνιγε τις γλυφές). NEW `resolveColumnFaceSnapWithGlyph` (SSoT στο `column-placement-snap-context`): τρέχει `findSnapPoint`, αν υπάρχει **ορατό** BIM χαρακτηριστικό → μαγνητική έλξη (effective cursor) + `glyphSnap` → ο `snap-scheduler` δείχνει γλυφή/ετικέτα μέσω `setFullSnapResult`. Καλείται από scheduler (move) **και** `mouse-handler-up` (click) → ΕΝΑ SSoT, preview≡commit. **Fix #2 (πράσινες μικρές πλευρές):** οι τοίχοι μαζεύονται **ξεχωριστά** από τα δοκάρια (`buildFaceTargets(cols, beams, walls)`)· `endsAxis=null` για τοίχους → όλες οι παρειές 🟢 (μόνο τα δοκάρια κρατούν 🔴 κοντές άκρες, mirror «extend instead»). 21 column-face-snap jest (incl. 3 glyph-wrapper + wall short-end 🟢) + framing/context regression GREEN. **ΜΑΘΗΜΑ:** drive-ghost-only κατέπνιγε τις OSNAP γλυφές — όταν θες να φαίνονται οι έλξεις, πρέπει να τρέχει το ενιαίο `findSnapPoint` + να surface-άρεται το `fullSnapResult`. 🔴 tsc (σειριακά N.17) + browser-verify + commit.
- **2026-06-20 (NEW feature: Column smart-ghost face-snap — §3.7, UNCOMMITTED)** — Το εργαλείο «Κολώνα» απέκτησε την «έξυπνη» συμπεριφορά φαντάσματος του δοκαριού, **πιστά προσαρμοσμένη** σε σημειακό (1-κλικ) μέλος (Giorgio order). Κοντά σε παρειά υφιστάμενου **δοκαριού/κολώνας** το ghost κουμπώνει εξωτερικά, γλιστράει, με auto-λαβή ανά ζώνη (γωνία→flush / μέσον→κεντραρισμένη)· 🟢 valid attach / 🔴 κοντή άκρη δοκαριού. **FULL SSoT — μηδέν νέο engine/geometry, μηδέν διπλότυπο:** NEW pure `bim/columns/column-face-snap.ts` (`resolveColumnFaceSnap`) που **κάνει reuse το υπάρχον `bim/framing/` (ADR-508)**: `collectMemberSnapTargets` (στόχοι), `pickThird` (zone), `MEMBER_GHOST_CAPTURE_MM`· anchor offset μέσω του υπάρχοντος `computeColumnGeometry`. **ΕΝΑ SSoT για move ΚΑΙ click** (preview ≡ commit): `snap-scheduler` (ghost) + `mouse-handler-up` (commit) καλούν τον ίδιο resolver· NEW `faceAnchor` channel στο `ColumnPlacementGhostStatusStore`· `commitColumnFromState` precedence **face-anchor > beam-center > Tab**· `useColumnGhostPreview` → single-anchor draw όταν face-snap ενεργό. Anchor-mapping πίνακας (4 παρειές × 3 ζώνες → 9 λαβές) στο §3.7. Miss → fall-through στο corner-projection path (§3.6) → μηδέν regression. **DEDUP FIX (Giorgio SSoT audit):** η 1η μου έκδοση είχε **διπλότυπα** 3 helpers (`worldBounds`/`distanceToBounds`/`pickFace`) ταυτόσημα με τα private του `member-column-face-snap` → **EXTRACT** σε κοινό `bim/geometry/shared/footprint-face-frame.ts` (`footprintBounds`+`distanceToFootprintBounds`+`pickDominantFace`)· **ΚΑΙ τα δύο** (column + framing member-column) το καταναλώνουν → πραγματική μία πηγή (byte-for-byte· `member-column-face-snap` έχασε τους private του). **+ τοίχοι ως στόχοι** (Giorgio: «ίδια συμπεριφορά κοντά σε τοίχο όπως σε δοκάρι») — 1-line `memberKinds:['beam','wall']` (το `collectMemberSnapTargets` ήδη χτίζει wall outline· μηδέν νέος κώδικας). 27 jest (17 column-face-snap incl. 3 wall + 10 footprint-face-frame) + framing/column regression GREEN. ⚠️ **shared tree:** άγγιξα `bim/framing/member-column-face-snap.ts` (μετάβαση στο κοινό SSoT, εγκεκριμένο Q2 «full unification»· behavior-preserving) — συντονισμός με τον πράκτορα ADR-508. **ΜΑΘΗΜΑ:** (1) ο άλλος πράκτορας είχε ήδη εξάγει το generic `bim/framing/` → χτίσαμε την κολώνα **πάνω** του αντί νέο engine· (2) σημειακό μέλος = «ανάπτυξη Α/Δ» → «auto-λαβή ανά ζώνη»· (3) μην εξάγεις «column-only» helper όταν το ίδιο geometry ζει ήδη private αλλού — βγάλ' το σε ουδέτερο geometry SSoT και κάνε **και τους δύο** consumers thin. 🔴 tsc (σειριακά, N.17) + browser-verify + commit (Giorgio).
- **2026-06-20 (refactor: generalize → ADR-508 unified linear-member framing, UNCOMMITTED)** — Ο πυρήνας του smart beam ghost (§3.3–§3.6) **εξήχθη σε generic SSoT** `bim/framing/` ώστε να τον μοιράζεται **και ο τοίχος** (Giorgio order — «ο τοίχος να φτιάχνεται ίδια με το δοκάρι»). Τα `beam-beam-face-snap`/`beam-column-face-snap`/`beam-face-third`/`beam-column-flush` έγιναν **thin adapters** (προσαρμογή `beamWidthScene`→`memberWidthScene`)· `collectBeamSnapTargets` → delegate στο `collectMemberSnapTargets` (`memberKinds:['beam']` → συμπεριφορά δοκαριού **byte-for-byte αμετάβλητη**). Beam tests πράσινα χωρίς αλλαγή. Λεπτομέρειες + wall mirror: **ADR-508**.
- **2026-06-20 (NEW feature: beam-to-beam framing smart ghost — §3.6, UNCOMMITTED)** — Το εργαλείο «Δοκάρι» κουμπώνει το ghost-before-click **και πάνω σε υφιστάμενα δοκάρια** (όχι μόνο κολόνες), με κατευθυντικό 🟢/🔴 (mirror του column ghost §3.2). Μακριά παρειά (Β/Ν) → 🟢 κάθετο Τ-framing + ολίσθηση + μαγνήτες άκρα/μέσα· κέντρο → 🟢 πλησιέστερη παρειά (απόφαση «Α»)· κοντή άκρη (Α/Δ) → 🔴 συγγραμμική «extend instead» (κόκκινο schematic). **FULL SSoT (extract/generalize/reuse, μηδέν διπλότυπα):** EXTRACT παλέτα+polygon-draw σε ουδέτερα `bim/ghosts/{ghost-status-color,ghost-status-polygon-draw}.ts` (column renderer re-exports+delegate, μηδέν beam→column coupling)· GENERALIZE `findColumnOverlap` → `bim/geometry/entity-overlap.ts` (`findEntityOverlap` extractor-driven)· REUSE `projectPolygonOnAxis`+`projectPointOnAxis` (μηδέν νέο projection)· NEW axis-relative `bim/beams/beam-beam-face-snap.ts` (`resolveBeamBeamFaceSnap`, **όχι bbox** — δοκάρια υπό γωνία). Wiring: `resolveBeamGhostSnapFromStore` → **dispatcher** column-priority + beam· `beamPreviewStore.setBeams`/`beamTargets`· `useBeamTool.syncSceneTargetsToStore` (πρώην `syncColumnsToStore`) συγχρονίζει δοκάρια· 🔴 schematic στον `PreviewRenderer` μέσω `drawStatusGhostPolygon`. Μαγνήτες = reuse `getImmediateSnap`+`BimCharacteristicSnapEngine`. **Slice 4 (Giorgio order):** (α) **ΑΠΑΓΟΡΕΥΣΗ** ομοαξονικού/πάνω-σε-υφιστάμενο — NEW `isBeamCollinearOverlap` (παράλληλοι άξονες + **reuse `coveredIntervals`** SSoT· **ΟΧΙ** hand-rolled overlap — soft-duplicate που έπιασε το SSoT audit & αφαιρέθηκε)· awaitingEnd+pre-click ghost 🔴 + commit **silent block**· κάθετο Τ-framing ελεύθερο. (β) **staleness fix** — `useBeamTool` subscribe σε `drawing:entity-created` με **rAF defer** (το event εκπέμπεται πριν commit-αριστεί το React scene state→σύγχρονο read=stale· γι' αυτό «κόκκινο μόνο μετά το κλικ»). **Slice 5 (Giorgio §3.6):** 3-ζωνική δικαιολόγηση πλάτους του κάθετου ghost (cursor σε αρχή/μέση/τέλος ακμής ανά τρίτο· βόρεια/νότια αντίστροφα)· **reuse `pickThird`** εξηγμένο σε NEW cycle-proof leaf `beam-face-third.ts` (column+beam resolvers, μηδέν διπλότυπο). 21 jest (16 beam-beam incl. 7 collinear + 3-ζωνική + 5 entity-overlap), μηδέν regression. ⚠️ ADR-040 critical (`ColumnAnchorGhostRenderer`+`PreviewRenderer`) → CHECK 6B/6D: stage ADR-398. **ΜΑΘΗΜΑΤΑ:** (1) beam face-snap ≠ column (axis-relative, όχι bbox)· (2) «ομοαξονικό κίτρινο» ghost = stale targets→free fallback, όχι λάθος γεωμετρία· (3) SSoT audit ΠΡΙΝ commit έπιασε hand-rolled overlap → reuse `coveredIntervals`· (4) shared third-logic→cycle-proof leaf. 🔴 browser-verify (🟢 3-ζωνική + μαγνήτες + 🔴 short-end/ομοαξονικό block + column-priority) + commit (Giorgio).
- **2026-06-20 (refactor: preview-render unification — §3.5, UNCOMMITTED)** — Πλήρης Revit-grade ενοποίηση (Giorgio order μετά το §3.4 hotfix). Ρίζα: 2 αποκλίνοντες preview μηχανισμοί (imperative composite vs ~19 RAF-direct ghost hooks) έλυναν ΜΟΝΟΣ ο καθένας «ποιο viewport/transform» + είχαν copy-pasted scaffolding → η κλάση του +Y bug + latent transform-lag. FIX (2 SSoT): NEW `systems/preview/ghost-preview-frame.ts` (`getCanonicalPreviewFrame` = cached viewport + live transform, `GhostDrawFrame`) + NEW `hooks/tools/useCanvasGhostPreview.ts` (harness: cursor-gate+RAF+DPR-clear+clear-on-exit+snapped-cursor, modes `cursorMode 'none'`/`clearMode 'skip-clear'`). Migrated **19 hooks** (column/opening/slab-opening + 7 MEP + rotation/move/mirror/scale/stretch/trim/extend/grip-ghost/grip-dim) → ΜΟΝΟ draw delegate, ~600 γρ scaffolding διαγράφηκαν, οπτικά byte-for-byte. Διορθώνει ΚΑΙ το transform-lag (previews έπαιρναν React-prop transform, 1-2 frames πίσω από main canvas). 9 jest νέα. **Deep investigation:** 4 παράλληλοι Explore agents (hook map / renderer signatures / transform-flow / shared-tree+tests) → επαληθευμένος χάρτης πριν τον κώδικα. **ΜΑΘΗΜΑ:** όταν Ν preview paths λύνουν ΜΟΝΟΙ τους «ποιο viewport/transform» → απόκλιση είναι θέμα χρόνου· κεντρικοποίησε το frame ΚΑΙ το scaffolding. 🔴 browser-verify (ΟΛΑ τα ghost tools + pan/zoom με ενεργό ghost — ο transform-lag fix είναι η μόνη συμπεριφορική αλλαγή).
- **2026-06-20 (bugfix: beam ghost +Y offset — render-path, §3.4 — UNCOMMITTED)** — Browser report: το WYSIWYG ghost δοκαριού εμφανιζόταν σταθερά λίγα px βόρεια (παρειά ghost ≠ παρειά κολόνας) σε ΟΛΕΣ τις παρειές/αγκυρώσεις, ενώ το **committed** δοκάρι ευθυγραμμιζόταν σωστά ⇒ **preview-only**. ΡΙΖΑ (αιτιακή, geometry/resolver αποκλείστηκαν αφού ghost+commit μοιράζονται το ίδιο `buildBeamEntity`): ο y-flip `screenY = (viewport.height − top) − …` τρέφεται από το `viewport.height`· ο beam ghost (μέσω `BimPreviewRenderer` → `composite.render(entity, {})` με **κενά options**) έπεφτε στο `BaseEntityRenderer.getViewport()` που διαβάζει το **δικό του** `ctx.canvas.getBoundingClientRect()` (= **PreviewCanvas**), ενώ committed δοκάρι + column ghost (`getViewportElement()`) μετρούν από το **DxfCanvas/container rect** (prop `viewport`). PreviewCanvas inline-style height αποκλίνει λίγα px → σταθερό +Y. FIX: NEW `setViewportOverride()` (`BaseEntityRenderer` + forward `EntityRendererComposite`)· `BimPreviewRenderer.render(entity, transform, viewport)` εγχέει το canonical viewport (try/finally clear)· `PreviewRenderer` το περνά. Ενοποιεί τη διπλή πηγή viewport (imperative vs RAF-direct) σε ΕΝΑ SSoT. Override=null στο main path → μηδέν regression. 4 jest νέα (`base-entity-renderer-viewport-override`). **ΜΑΘΗΜΑ:** WYSIWYG preview που ζωγραφίζει με τον πραγματικό renderer σε ΑΛΛΟ canvas πρέπει να μετρά τον y-flip στο ΙΔΙΟ canonical viewport με το committed — όχι στο δικό του canvas rect.
- **2026-06-20 (bugfix: ghost-before-click σταθερά μετατοπισμένο από το σταυρόνημα — UNCOMMITTED)** — Browser report: το φάντασμα εμφανιζόταν σταθερά λίγα px μακριά (βόρεια) από το σταυρόνημα, σε όλες τις θέσεις, ανεξάρτητα πλάτους. ΡΙΖΑ: το `processDrawingHover` (`drawing-hover-handler.ts`) περνά στο preview RAW cursor + ortho/polar/tracking αλλά **ΟΧΙ** το βασικό OSNAP/grid snap (`applySnap`)· πριν το 1ο κλικ δεν υπάρχει καν ortho reference → raw cursor. Το **σταυρόνημα** όμως ζωγραφίζεται στο **snapped** σημείο (`ImmediateSnap` — γι' αυτό το column ghost, που διαβάζει `getImmediateSnap`, ευθυγραμμίζεται σωστά). Με grid/OSNAP ON → σταθερό offset raw↔grid. FIX: το `makeBeamGhostBeforeClick` διαβάζει το **ίδιο snapped σημείο** (`getImmediateSnap()?.point ?? cursorPoint`, mirror `useColumnGhostPreview`) → ο άξονας του ghost ταυτίζεται με το σταυρόνημα. **ΜΑΘΗΜΑ:** ένα ghost «κολλημένο στο σταυρόνημα» πρέπει να διαβάζει την ΙΔΙΑ snapped πηγή με τον crosshair (`ImmediateSnap`), όχι το raw preview cursor. 33 jest GREEN.
- **2026-06-20 (bugfix: «Maximum update depth exceeded» render-loop — UNCOMMITTED)** — Browser report μετά το §3.3: το `syncColumnsToStore()` που πρόσθεσα στο `useBeamTool.activate` έκανε το `activate` useCallback να εξαρτάται από το `syncColumnsToStore` (dep `[getSceneEntities]`)· το `options.getSceneEntities` είναι **inline arrow** (νέο identity κάθε render) → `syncColumnsToStore` νέο → `activate` νέο κάθε render → το `useToolLifecycle` (effect dep `[isActive, activate, deactivate]`) ξανατρέχει → `activate()` → setState → **infinite loop**. FIX: stable `getSceneEntitiesRef` (ref ορισμένο μία φορά κοντά στην κορυφή· `syncColumnsToStore` dep `[]`) → `activate` ξανα-σταθερό όπως πριν. **ΜΑΘΗΜΑ:** callback που πάει σε `useToolLifecycle`/effect-deps ΠΡΕΠΕΙ να μένει referentially stable — διάβασε τα unstable props (inline arrows) μέσω ref, ΟΧΙ μέσω useCallback dep. `useBeamTool` 10 jest GREEN.
- **2026-06-20 (NEW feature: Smart beam ghost — §3.3, UNCOMMITTED)** — Το εργαλείο «Δοκάρι» απέκτησε **φάντασμα πριν το 1ο κλικ** (mirror του column ghost): κοντά σε ορθογώνια κολόνα κουμπώνει σε 12 διακριτές θέσεις (4 παρειές × 3 αγκυρώσεις)· μακριά ακολουθεί ελεύθερα. **Καθαρά γεωμετρικό** (μηδέν επαφή με snap-scheduler/cursor/renderer core — αντίθετα με το column ghost που χρειάστηκε wiring). NEW pure `bim/beams/beam-column-face-snap.ts` (reuse `projectPolygonOnAxis`+`mmToSceneUnits`)· `beamPreviewStore.startAnchored` flag (centerline mode vs location-line auto-flush)· ΕΝΑ wrapper σε preview (`beam-preview-helpers`) + click (`useBeamTool`) → preview === commit· `syncColumnsToStore` και στο `activate`. **2ο κλικ ελεύθερο** (διαγώνιο/ORTHO). 20 jest νέα + regression GREEN, tsc clean. 🔴 browser-verify + commit (Giorgio).
- **2026-06-20 (bugfix: snap indicators εξαφανίζονται στο εργαλείο «Κολώνα» — browser-verified ✅, UNCOMMITTED)** — Giorgio report: με εργαλείο «Κολώνα» + snaps ON, hover κοντά σε **δοκάρι/κολώνα** ΔΕΝ έδειχνε γλυφές + ετικέτες (Γωνία/Μέσο/Κέντρο στήλης & δοκαριού), ενώ σε «Επιλογή»/«Τοίχος» έδειχναν κανονικά. **ΠΡΑΓΜΑΤΙΚΗ ΡΙΖΑ (αποδεδειγμένη με live debug log, ΟΧΙ ο short-circuit):** το **Column Body Corner Projection Snap** (`findColumnDrawCornerSnap`) — μοναδικό column-specific βήμα — προβάλλει τις γωνίες της would-be κολώνας στον snap engine· σε κάναβο-βαρύ μοντέλο μια γωνία πέφτει σε **γραμμή `grid`** σχεδόν κάθε frame → ο `SnapIndicatorOverlay` **κρύβει ρητά** `grid`/`guide` (silent, AutoCAD convention) → καμία γλυφή. Το `grid` corner-projection **υπερίσχυε** του ορατού BIM χαρακτηριστικού snap που υπήρχε στον cursor (`bim_corner`/`bim_center`/`bim-beam`). Ο Τοίχος δεν έχει corner-projection → έδειχνε. **Fix (Revit-grade προτεραιότητα, SSoT):** NEW pure `resolveColumnDrawSnap(cursorPos, drawCorner, findSnapPoint)` (`column-placement-snap-context.ts`) — προτεραιότητα **(1) ορατό corner-projection (διακριτός στόχος) > (2) ορατό cursor χαρακτηριστικό (η ρητή πρόθεση) > (3) σιωπηλό grid alignment (fallback, αμετάβλητη συμπεριφορά)**· `isVisibleIndicatorSnap` mirror του hide-rule του overlay (`grid`/`guide`). Ίδιος resolver σε **move** (`snap-scheduler`) ΚΑΙ **commit** (`mouse-handler-up`) → commit ≡ ghost. **Επιπλέον SSoT cleanup (ίδιο pass):** αφαίρεση του περιττού `setFullSnapResult(null)`/short-circuit (`applyColumnPlacementContext`/`writeBeamAxisSnap`) — το ενιαίο `findSnapPoint` τρέχει ΠΑΝΤΑ· ghost status πλέον **thin reader** μέσω NEW `resolveColumnGhostStatusFromSnap` (`snapResult.snapPoint.entityId` → δοκάρι 🟢 / κολώνα 🔴 + light `findColumnOverlap` fallback, precedence overlap>beam>neutral)· **DELETE διπλότυπα** `findColumnBeamAxisSnap`/`resolveColumnPlacementContext`/`ColumnBeamAxisSnap`/`ColumnPlacementContext` (η beam-axis projection ζει ΜΟΝΟ στο `NearestSnapEngine`). center-anchor (`getColumnGhostStatus()==='beam'`), `ColumnPlacementGhostStatusStore`, `useColumnGhostPreview` αμετάβλητα. Tests: `column-placement-snap-context.test.ts` rewrite → 15 jest (5 `resolveColumnDrawSnap` precedence + 10 ghost-status/overlap)· `beam-axis-projection` 6 GREEN· `snapping/` GREEN. **ΜΑΘΗΜΑ:** όταν ένα drawing tool έχει corner-projection, ένας σιωπηλός `grid` στόχος δεν πρέπει να υπερισχύει ορατού χαρακτηριστικού — αλλιώς ο indicator εξαφανίζεται «μυστηριωδώς». browser-verified ✅. 🔴 tsc(Giorgio) + commit.
- **2026-06-19 (bugfix precedence, UNCOMMITTED)** — Giorgio report: hover πάνω σε **ενδιάμεση** κολώνα (στη μέση δοκαριού) ΔΕΝ γινόταν 🔴 (έβγαινε 🟢), ενώ οι γωνιακές (στα άκρα) σωστά 🔴. Ρίζα: precedence ήταν `beam > overlap`· η mid-span κολώνα κάθεται στον άξονα → beam νικούσε. Οι γωνιακές είναι σε beam endpoints (`atEndpoint` → beam reject → overlap). Fix: **`overlap > beam > neutral`** (υπάρχουσα κολώνα νικά το δοκάρι για το collision warning· το «beam νικά» μένει για snap targets grid/corner). NEW `findColumnOverlap` helper. +2 jest (10 total).
- **2026-06-19 (SSoT refactor, UNCOMMITTED)** — Μετά από SSoT audit (Giorgio): η beam-axis projection ήταν inline στο column helper ενώ υπήρχε pattern `projectPointOn{Wall,Slab,Opening}` (το beam έλειπε). **Εξαγωγή** NEW `bim/beams/beam-axis-projection.ts` (`projectPointOnBeamAxis` + `...Detailed`, mirror wall) + wire `isBeamEntity` στο `NearestSnapEngine` (+ `bim-beam` description key, el+en) ⇒ τα δοκάρια αποκτούν nearest-snap parity παντού. `findColumnBeamAxisSnap` → reuse SSoT (μηδέν inline geometry). +6 jest, 169 snapping GREEN.
- **2026-06-19 (UNCOMMITTED)** — NEW `column-placement-snap-context.ts` (`findColumnBeamAxisSnap` + `resolveColumnPlacementContext`) + NEW `ColumnPlacementGhostStatusStore.ts` + ghost coloring (`ColumnAnchorGhostRenderer.statusColor` + `resolveGhostStatusColor`) + wiring move (`snap-scheduler` + `mouse-handler-move.getEntities`) + commit (`mouse-handler-up`) + center-anchor enforcement (`useColumnTool`). 9 jest GREEN. 🔴 tsc(Giorgio) + browser-verify + commit.
- _(retroactive)_ Column Body Corner Projection Snap (`findColumnDrawCornerSnap`, ADR-040 Φ11 scheduler + up-handler) — η would-be κολώνα προβάλλει τις γωνίες της σε στόχους· sibling του beam-axis snap, ίδιο 2-paths pattern.
- **2026-06-24 (ADR-514 Φ6d, UNCOMMITTED)** — Η preview assembly της κολόνας (ghost + §3.10b flush rotation + §dim CL listening dims + §3.15 cartesian dx/dy + §3.13/§3.15 polar/rect grid) **εξήχθη** σε entity-agnostic SSoT `bim/placement/placement-ghost-assembly.ts` (κοινό κολόνα+πέδιλο)· το `generateColumnPreview` καλεί πλέον `assemblePlacementGhost`/`assemblePlacementRotationGhost` (μηδέν συμπεριφορική αλλαγή στην κολόνα — 10 column-preview jest GREEN). Επίσης `buildColumnPolarSnapOptions` → thin delegate του γενικού `buildPlacementPolarSnapOptions`. TopLean (slanted) μένει column-only. Βλ. ADR-514 changelog. ⚠️ CHECK 6B/6D → stage ADR-040 + ADR-514 μαζί.
