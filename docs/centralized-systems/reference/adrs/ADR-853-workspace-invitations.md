# ADR-853 — Η ένταξη σε χώρο **ξεκινά από τον χώρο**: προσκλήσεις αντί για αίτημα σε σταθερή εταιρεία

**Status:** 🚧 ΣΕ ΕΞΕΛΙΞΗ — **Φ1 · Φ2 · Φ3 · Φ4 · Φ5 ΕΚΛΕΙΣΑΝ**. Εκκρεμούν: **Φ6** (UI + σελίδα `(auth)/invite/[token]` + locales) · **Φ8** (τεκμηρίωση). **Φ7 = ΕΚΤΟΣ ΕΜΒΕΛΕΙΑΣ** (ADR-787 Κ-2, χωριστή απόφαση). 🔶 Δηλωμένο επόμενο: **Φ5-Β** (αληθινή κατάσταση παράδοσης από γεγονότα παρόχου — §10).
⚠️ **ΔΥΟ ΑΝΟΙΧΤΑ ΠΟΥ ΔΕΝ ΕΙΝΑΙ ΜΙΚΡΑ**: (α) η σουίτα κανόνων των **35 κελιών** είναι γραμμένη και δεκτή από το CHECK 3.16 αλλά **ΔΕΝ ΕΧΕΙ ΕΚΤΕΛΕΣΤΕΙ ΠΟΤΕ** *(θέλει εξομοιωτή· η πύλη κρίνει **δήλωση**, όχι **εκτέλεση**)* — **απαιτεί εκτέλεση πριν την παραγωγή**· (β) το **`WORKSPACE_INVITE_SECRET` πρέπει να μπει στο περιβάλλον**, αλλιώς **κάθε** πόρτα απαντά 503 *(επίτηδες: δεν λέμε «πλαστός σύνδεσμος» για **δικό μας** μυστικό που λείπει)*.
**Σχετικά / εξαρτάται:** ADR-787 (πολυ-οργανισμική πλατφόρμα — η διέπουσα· **Α2** · **Α6** · **Ε-1** · **Ε-2** · **Ε-3** · **Ε-5** · §5.1 · §5.2 · §5.3) · ADR-660 (σκλήρυνση αυτο-εγγραφής — §5 · §6 · §6.6) · ADR-844 (πολίτης · δόγμα token πρόσκλησης) · ADR-798 §7 (δηλωμένη vs επαληθευμένη ιδιότητα) · ADR-801 §4 (ο ΕΝΑΣ κριτής ικανότητας) · ADR-244 (κονσόλα ρόλων) · ADR-195 (ίχνος οντοτήτων) · ADR-749 (μία μηχανή, όχι τέσσερις)
**Αφορμή:** Περιστατικό **2026-09-11**. Η Σταυρούλα Παγώνη συνδέθηκε στο nestorconstruct.gr και η πλατφόρμα άνοιξε **μόνη της** αίτημα ένταξης προς την ΠΑΓΩΝΗΣ Α.Ε. — εταιρεία που **κανείς δεν επέλεξε**.

---

## 1. Το περιστατικό — η αλυσίδα, μετρημένη

| Βήμα | Πού | Τι έγινε |
|---|---|---|
| 1 | `api/auth/session/route.ts:132-148` | Χρήστης χωρίς `companyId` ⇒ `ensurePendingRegistration` (fire-and-forget) |
| 2 | `server/auth/pending-registration.ts:119` | `getCompanyId()` |
| 3 | `config/tenant.ts:46-48` | Επιστρέφει **πάντα** `TENANT_COMPANY_ID` — η ΠΑΓΩΝΗΣ |
| 4 | `server/auth/workspace-access-request.ts:80-106` | Άνοιξε `workspace_access_requests/wacr_ec0d89c0-…`, status `pending` |
| 5 | `pending-registration.ts:208-242` | Email σε **όλους** τους admin της ΠΑΓΩΝΗΣ |

**Η βλάβη δεν είναι το email — είναι η σιωπηλή απόδοση ιδιοκτησίας.** Με Ν εταιρείες στην πλατφόρμα, **κάθε** νέος άνθρωπος θα γινόταν αίτημα στη **μία** εταιρεία, και οι υπόλοιπες δεν θα τον έβλεπαν ποτέ. Και το προφανές «στείλ' το σε όλες» είναι **χειρότερο**: διαρρέει όνομα και email σε ξένες εταιρείες (GDPR), και όποια προλάβει «κερδίζει» τον άνθρωπο.

🔑 **Η αρχή που παραβιάστηκε είναι ήδη γραμμένη**: ADR-787 **§2.2** — *«η εγγραφή ΔΕΝ δίνει χώρο»*. Η εγγραφή έδινε ταυτότητα **και ένα αίτημα**. Το δεύτερο δεν το ζήτησε κανείς.

---

## 2. Η ζωντανή κατάσταση — **μετρημένη 2026-09-12**, όχι εκτιμημένη

| Ερώτημα | Απάντηση | Πού μετρήθηκε |
|---|---|---|
| Υπάρχει γενικό σύστημα προσκλήσεων; | **ΟΧΙ.** Τρία domain-specific: `first_contact_invitations` (ADR-844), `vendor_invites` (ADR-327), και το **ανάποδο** `workspace_access_requests` | SSoT audit §A |
| Υπάρχει επαναχρησιμοποιήσιμο `grantMembership`; | **ΟΧΙ.** Η `syncFirestoreRecords` είναι **μη εξαγόμενη**, τοπική στο `claims-handler.ts:72-139`. **Τρία** σημεία κάνουν παρόμοια-αλλά-όχι-ίδια δουλειά: `claims-handler.ts` · `workspace-provisioning.ts:265-284` · `citizen-identity.ts:274` | SSoT audit §B |
| Δουλεύει η πολλαπλή συμμετοχή; | **ΣΤΟΝ ΔΙΑΚΟΜΙΣΤΗ, ΝΑΙ.** `resolveEffectiveCompanyId` (`auth-context.ts:175-212`) ρωτά το `decideMembership`· ετυμηγορία `member` ⇒ επιτρέπεται ξένος χώρος. **352 διαδρομές** το κληρονομούν | `auth-context.ts:188-199` |
| …και στον πελάτη; | **ΟΧΙ.** Το `getUserCompanyId()` των κανόνων επιστρέφει το **claim** (`firestore.rules:5211`) και κρίνει **78 μπλοκ** | `grep -c`, 2026-09-12 |
| Διαβάζει κάποιος το όνομα της εταιρείας για το branding των email; | **ΚΑΝΕΙΣ.** ~15 σημεία με σταθερό `'Pagonis Energo'`· το procurement χρησιμοποιεί env `FROM_NAME` | SSoT audit §F |
| Ελεύθερο πρόθεμα αναγνωριστικού; | **`winv`** — το `inv` το κρατά το `INVOICE_ACC` (`enterprise-id-prefixes.ts:238`) | ό.π. |

🔴 **Το τρίτο εύρημα ανατρέπει την υπόθεση του handoff.** Το handoff έγραφε *«η πολλαπλή συμμετοχή (Α2) δεν έχει χτιστεί»*. Έχει χτιστεί **η μισή, και είναι η δύσκολη**: ο κριτής, το έγγραφο μέλους (`companies/{id}/workspace_members/{uid}`) και το collection-group ερώτημα `listMemberWorkspaces`. Λείπει **μόνο** ο «ενεργός χώρος» στο claim. Άρα η απόφαση για τον ήδη-μέλος-αλλού προσκεκλημένο **δεν** είναι «άρνηση μέχρι να χτιστεί το Α2» — δες §4 Α2.

---

## 3. Η έρευνα — **οκτώ προϊόντα**, μόνο επίσημη τεκμηρίωση

| | GitHub | Slack | Figma | Autodesk ACC | BIMcloud | Maxon | Zillow | Idealista |
|---|---|---|---|---|---|---|---|---|
| **Λήξη** | **7 ημ.** | 30 ημ. | *καμία* | **7 ημ.** | — | έχει `expired` | — | — |
| **Δέσμευση σε email** | **ναι, verified** | — | **ρητά ΟΧΙ** | — | GSID | — | — | ναι |
| **Προ-ανάθεση** | ρόλος | member/guest | seat | **εταιρεία + ρόλος + έργα + προϊόντα** | — | ομάδα αδειών | τύπος μέλους | ρόλος (3 βαθμίδες) |
| **«Ζητώ είσοδο»** | **ΟΧΙ** | ναι | μόνο guests | **ΟΧΙ** | **ΟΧΙ** | **ΟΧΙ** | **ΟΧΙ** | **ΟΧΙ** |
| **Πολλοί οργανισμοί** | ναι | ναι | ναι | **όχι** *(τεκμηριωμένο ελάττωμα)* | ναι | **ρητά ναι** | ασύμμετρα | — |
| **Όριο ρυθμού** | **50/24ω** (500 paid) | ML anti-spam | — | — | — | — | — | — |

**Τρία συμπεράσματα:**
1. **Ο κλάδος μας είναι αυστηρά invite-only.** Έξι στα έξι σοβαρά επαγγελματικά (GitHub · Autodesk · BIMcloud · Maxon · Zillow · Idealista) **δεν** έχουν self-service αίτημα. Το έχουν Slack και Figma — εργαλεία συνομιλίας και σχεδίασης.
2. **Η Autodesk είναι η εξαίρεση στην πολλαπλή συμμετοχή, και είναι ελάττωμα**, όχι πρότυπο: οι εταιρείες αναγκάζονται να φτιάχνουν **δεύτερο email** ανά hub (ανοιχτό αίτημα βελτίωσης στο φόρουμ της Autodesk). **Δεν αντιγράφουμε ελάττωμα.**
3. **Δύο ασύμβατα μοντέλα κόστους**: Autodesk χρεώνει τη θέση **στην αποστολή** (τεκμηριωμένο αποτέλεσμα: 20-40% αδρανείς θέσεις)· Maxon **μόνο μετά την αποδοχή**. Είναι απόφαση, όχι προεπιλογή.

### 3.1 Επέκταση **2026-09-13** — ΠΩΣ ακριβώς το κάνουν όσοι το κάνουν (ερώτημα Giorgio)

Ο πίνακας απαντά **αν** υπάρχει «ζητώ είσοδο». Δεν απαντούσε **με ποιον μηχανισμό** — και εκεί κρύβεται
ο λόγος που το ίδιο χαρακτηριστικό είναι **ασφαλές** στο Slack και θα ήταν **επικίνδυνο** σε εμάς.
Μετρήθηκε σε επίσημη τεκμηρίωση **Slack · Atlassian · Notion · Microsoft Entra**:

🔑 **ΚΑΝΕΙΣ ΔΕΝ ΔΙΝΕΙ ΓΥΜΝΟ ΚΟΥΜΠΙ «ΖΗΤΩ ΕΙΣΟΔΟ». ΟΛΟΙ ΤΟ ΚΛΕΙΔΩΝΟΥΝ ΠΙΣΩ ΑΠΟ ΤΟΜΕΑ EMAIL.**

| Προϊόν | Ο μηχανισμός |
|---|---|
| **Slack** | *Claim and verify email domains* · «Allow people with approved email domains to join» · με εγκεκριμένο τομέα το αίτημα μέλους **εγκρίνεται αυτόματα**· χωριστά, «Require admin approval» για κάθε πρόσκληση |
| **Atlassian** | *Approved domains* — χρήστες του τομέα μπαίνουν **χωρίς πρόσκληση**, με προαιρετικό checkbox «Required» για έγκριση |
| **Notion** | *Allowed email domains* — και **δύο φρουροί**: νέος τομέας επιτρέπεται **μόνο αν υπάρχει ήδη μέλος με αυτόν**, και οι **δημόσιοι** τομείς (gmail/outlook) **απαγορεύονται ρητά** |
| **Entra** | *Self-service sign-up* **βάσει τομέα email**, με API connector για ρητή ροή έγκρισης πριν δημιουργηθεί λογαριασμός |

🔒 **ΚΑΙ ΓΙ' ΑΥΤΟ ΔΕΝ ΕΙΝΑΙ ΟΡΓΑΝΟ ΑΠΑΡΙΘΜΗΣΗΣ** — που ήταν ο φόβος του ADR-787 Ε-5 §4 #1: κανείς
δεν **ανακαλύπτει** χώρους. Ζητάς **μόνο** τον χώρο στον οποίο **ήδη δείχνει η δική σου
επαληθευμένη διεύθυνση**. Το «ποιοι χώροι υπάρχουν;» δεν γίνεται ποτέ ερώτηση του πελάτη.

**⇒ Δύο συμπεράσματα, και κανένα δεν αλλάζει τη Φ6:**

1. **Το Α1 στέκει αμετάβλητο.** Έξι στα έξι του **κλάδου μας** παραμένουν invite-only. Ακολουθούμε
   την πρακτική τους, **μετρημένα**, όχι από αδράνεια.
2. **Αν χτιστεί ποτέ, χτίζεται domain-gated** — ακριβώς όπως το **ονομάζει ήδη** το Α1 *(«γνωστό,
   καλό μονοπάτι — WorkOS: DNS TXT — επόμενη φάση με όνομα»)*, με τους **δύο φρουρούς του Notion**
   ως ελάχιστο: τομέας **μόνο** από υπάρχον μέλος, **ποτέ** δημόσιος τομέας.
   ⚠️ **Προϋπόθεση που λείπει σήμερα, μετρημένη**: ο ΝΕΣΤΩΡ **δεν έχει καμία έννοια επαληθευμένου
   τομέα** στην εταιρεία — μόνο ένα `useEnterpriseEmailValidation(companyDomains?)` σε βοηθητικό
   άγκιστρο email-sharing, που είναι **παράμετρος λίστας**, όχι αυθεντία. Άρα δεν είναι «μια οθόνη
   ακόμη»: είναι **νέο υποσύστημα** (διεκδίκηση τομέα + απόδειξη DNS) και **δική του φάση**.

⛔ **ΜΗΝ ξανατρέξεις αυτές τις αναζητήσεις** — το ερώτημα τέθηκε, ερευνήθηκε και απαντήθηκε εδώ.

---

## 4. Οι αποφάσεις — **Α1-Α4**

### Α1 — Η ένταξη σε ξένο χώρο γίνεται **ΜΟΝΟ με πρόσκληση**

Ο χώρος ξεκινά τη σχέση, γιατί **μόνο αυτός ξέρει ποιος του ανήκει**. Η αυτο-εγγραφή δίνει **ταυτότητα και ιδιωτικό χώρο**, και **δεν ειδοποιεί κανέναν** — ADR-787 **§2.2** + **Ε-3 §2**, τώρα με φορέα.

⛔ **ΔΕΝ φτιάχνουμε**: κοινόχρηστο σύνδεσμο ένταξης (Slack/Figma — κανείς στον κλάδο μας), ούτε αυτόματη ένταξη με επαληθευμένο domain. Το δεύτερο είναι **γνωστό, καλό μονοπάτι** (WorkOS: DNS TXT) και γράφεται εδώ ως **επόμενη φάση με όνομα**, όχι ως ξεχασμένο.

### Α2 — Ο ήδη μέλος **ΑΛΛΟΥ** χώρου προσκαλείται κανονικά· η αποδοχή **ΠΟΤΕ δεν πειράζει claim που υπάρχει**

Η αποδοχή γράφει `companies/{νέος}/workspace_members/{uid}`. Αν ο άνθρωπος **δεν** είχε χώρο, γράφεται **και** το claim (πλήρης λειτουργία σήμερα). Αν **είχε**, το claim **μένει ανέγγιχτο**.

🔑 **Γιατί όχι άρνηση**: η άρνηση είναι η συμπεριφορά της Autodesk, και είναι το **τεκμηριωμένο ελάττωμά** της (§3 #2). Ένας μηχανικός που συνεργάζεται με δύο γραφεία **δεν** πρέπει να φτιάχνει δεύτερο email.
🔑 **Γιατί όχι μετακίνηση**: ένα γραφείο θα μπορούσε να «κλέψει» μέλος άλλου με μία πρόσκληση. **Κανείς** από τους οκτώ δεν το κάνει.

### Α3 — Η πρόσκληση δίνει ρόλο **με ταβάνι**, ποτέ με λίστα

- Επιτρεπτοί: `company_admin` · `internal_user` · `external_user`.
- ⛔ **ΠΟΤΕ `super_admin`** — είναι break-glass, όχι βαθμίδα.
- ⛔ **ΠΟΤΕ ρόλος πάνω από τον δικό σου** (`compareRoleLevels`) — πρότυπο Figma, και το ίδιο δόγμα *«ceilings only subtract»* που ήδη τεκμηριώνει το `roles.ts:92-98`.

Η κρίση *«επιτρέπεται να προσκαλέσει;»* περνά από τον **ΕΝΑ** κριτή `decideCapability` (ADR-801 §4) και δηλώνεται στο `.authority-registry.json`. ⛔ **Καμία ωμή λίστα ρόλων** — CHECK 3.68 Κ1.

### Α4 — Το αίτημα ένταξης **παγώνει· δεν σβήνει**

| | Πριν | Μετά |
|---|---|---|
| Ποιος ανοίγει αίτημα | η **σύνδεση**, αυτόματα, σε σταθερή εταιρεία | **κανείς** αυτόματα |
| Email σε admin | σε κάθε νέα σύνδεση χωρίς χώρο | **καταργείται** |
| Η οντότητα `workspace_access_requests` | ζωντανή | **ζωντανή** — υπάρχει εκκρεμές αίτημα στην παραγωγή |
| Λίστα + Έγκριση/Απόρριψη | ζωντανά | **ζωντανά, αμετάβλητα** |
| `GET /api/auth/workspace-access-request` | `readOwnAccessState(getCompanyId(), uid)` | **«τα ΔΙΚΑ ΜΟΥ αιτήματα»** — collection-group στο `requesterUid`, **χωρίς** εταιρεία |

🔑 **Το GET είναι το λεπτό σημείο.** Χωρίς σταθερή εταιρεία, το ερώτημα *«τι απέγινε το αίτημά μου;»* **δεν έχει υποκείμενο**: ποιο αίτημα; Η απάντηση είναι να ρωτά ο άνθρωπος για **τον εαυτό του**, όχι για μια εταιρεία — ακριβώς το σχήμα του `listMemberWorkspaces`, με τον **ίδιο** γραμμένο λόγο `tenant-scope-exempt` (ο άξονας απομόνωσης είναι το `uid` από **υπογεγραμμένο** token, ποτέ από τον πελάτη). CHECK 3.35 / 3.10.

---

## 5. 🏆 Πού **ξεπερνάμε** — επτά, και κάθε ένα μετρήσιμο

| # | Εμείς | Η αγορά |
|---|---|---|
| 1 | Token **υπογεγραμμένο** (`lib/tokens/signed-token.ts` — 7ος καταναλωτής) **ΚΑΙ** στη βάση **μόνο `sha256`** | Κανείς δεν τεκμηριώνει hashing· το δικό μας `first-contact` κρατά το nonce **ωμό** |
| 2 | Δέσμευση στο **επαληθευμένο email** του παραλήπτη — **υποχρεωτική** | GitHub ναι· **Figma ρητά όχι** *(«πρόσεξε με ποιον λογαριασμό είσαι»)* |
| 3 | Η αποδοχή είναι **ρητή ανθρώπινη πράξη** — ποτέ αυτόματη ένταξη στο login | Μόνο το WorkOS το δηλώνει ως anti-phishing |
| 4 | Ο προσκεκλημένος βλέπει τη **δηλωμένη** ταυτότητα της εταιρείας, με ετικέτα *«δηλωμένη, όχι επαληθευμένη»* (ADR-798 §7 — **τρεις** καταστάσεις, ποτέ boolean) | **Κανείς από τους οκτώ** |
| 5 | Κατάσταση παράδοσης: στάλθηκε · **ανοίχτηκε** · λήγει σε Ν · αποδεκτή | Autodesk «Invited/Active»· **Figma τίποτα** (ανοιχτό αίτημα χρηστών από το 2022) |
| 6 | Η **θέση δεσμεύεται στην ΑΠΟΔΟΧΗ** (ADR-787 **Ε-1**) | Maxon ναι· **Autodesk όχι** ⇒ 20-40% αδρανείς θέσεις |
| 7 | Κάθε άρνηση έχει **όνομα** — `link-invalid · expired · already-used · revoked · wrong-recipient · already-member · role-above-inviter` (πρότυπο ADR-844) | Γενικό *«invitation not valid»* |

**Λήξη: 7 ημέρες.** GitHub 7 · Autodesk 7 · Auth0 default 7. Το 30 του Slack αφορά **κοινόχρηστο σύνδεσμο**, που δεν φτιάχνουμε (Α1).

---

## 6. 🔶 Δηλωμένα όρια — γραμμένα αντί να κρυφτούν

1. **Ο ήδη μέλος αλλού δεν αποκτά ΕΝΕΡΓΟ τον νέο χώρο.** Το έγγραφο μέλους γράφεται και ο διακομιστής τον δέχεται (§2), αλλά **78 μπλοκ** κανόνων κρίνουν με το claim ⇒ οθόνες που διαβάζουν **απευθείας** Firestore από τον φυλλομετρητή θα αρνηθούν. Η θεραπεία λέγεται **ρητή εναλλαγή ενεργού χώρου** και είναι **ADR-787 Κ-2**, όχι αυτό το έγγραφο. Ο άνθρωπος το **μαθαίνει με όνομα** στην οθόνη αποδοχής — δεν του υποσχόμαστε πρόσβαση που δεν έχει.
2. **Δεν φτιάχνουμε εγγραφή χωρίς λογαριασμό Firebase.** Η πρόσκληση φτάνει σε **email**, όχι σε `uid`· ο παραλήπτης περνά από `/login` και επιστρέφει (πρότυπο `draft-identity.ts`). Το `claims-handler.ts` απαιτεί **υπαρκτό uid** (`resolveFirebaseUser`) — γι' αυτό η ένταξη **δεν** μπορεί να γίνει τη στιγμή της αποστολής.
3. **Το «ανοίχτηκε» είναι ένδειξη, όχι απόδειξη.** Οι πελάτες email μπλοκάρουν εικόνες και προ-φορτώνουν συνδέσμους· η ετικέτα το λέει.
4. **Καμία επαλήθευση ΓΕΜΗ/ΑΦΜ σε αυτή τη φάση** (#4 του §5 δείχνει το **δηλωμένο**). Η επαλήθευση είναι ADR-787 **Κ-8** / ADR-798.

---

## 7. Το μοντέλο

### 7.1 Η οντότητα — `workspace_invitations`, πρόθεμα `winv`

**Μη ντετερμινιστικό id** (αντίθετα από το `wacr`): η **επαναποστολή** πρέπει να γεννά **νέο token** με νέα λήξη, και ένα ντετερμινιστικό id ανά (εταιρεία, email) θα ανάγκαζε επανεγγραφή του ίδιου εγγράφου με **ζωντανό παλιό token**. Η ιδεμποτησία λύνεται **αλλιώς**, δες 7.3.

### 7.2 Καταστάσεις — AIP-216, **output-only**

```
pending ──accept──► accepted
   │ ├──decline──► declined
   │ ├──revoke───► revoked
   │ └──(χρόνος)─► expired
```

⛔ **Καμία γενική ενημέρωση δεν αγγίζει το `state`** (AIP-216: *«APIs should not allow a State enum to be directly updated»*). Μόνο ονομασμένες πράξεις. Άκυρη μετάβαση ⇒ ονομασμένη άρνηση, **ποτέ σιωπηλή επανεγγραφή** — ίδιο δόγμα με το `decideAccessRequest` (μόνο `pending →`).

### 7.3 Ιδεμποτησία — **ΕΝΑ ζωντανό ανά (χώρος, κανονικοποιημένο email)**

Δεύτερη πρόσκληση στον ίδιο άνθρωπο **δεν** γεννά δεύτερο ζωντανό έγγραφο: η προηγούμενη γίνεται `revoked` μέσα στο **ίδιο transaction** (πρότυπο `superseded` του ADR-844). Έτσι δεν υπάρχουν ποτέ **δύο ζωντανά token** για το ίδιο πρόσωπο — που είναι και η άμυνα στο τεκμηριωμένο race-condition bypass των ορίων πρόσκλησης.

### 7.4 Το token

`encodeSignedToken(secret, [id, nonce, expiresAtMs])` — **millis, ΠΟΤΕ ISO** (το `:` συγκρούεται με τον διαχωριστή· γραμμένο ήδη στο `first-contact-invitation.service.ts:177-180`). Στη βάση **μόνο `sha256(nonce)`**. Δικό του env var μυστικό (`requireTokenSecret`). Σύγκριση με `equalsInConstantTime`.

**Η κατανάλωση είναι ατομική**: μέσα σε transaction, `pending → accepted` **μόνο** αν η κατάσταση είναι ακόμη `pending`. Δύο ταυτόχρονα κλικ ⇒ **ένα** μέλος.

### 7.5 Ταυτότητα — **δύο έλεγχοι, όχι ένας**

Η αποδοχή απαιτεί **ΚΑΙ** έγκυρο token **ΚΑΙ** συνδεδεμένο χρήστη του οποίου το **επαληθευμένο** email ταιριάζει με τον παραλήπτη. Προωθημένο email **δεν λειτουργεί** — εκεί ακριβώς σπάει το Figma.

---

## 8. Οι φάσεις

| Φ | Τι | Αρχεία |
|---|---|---|
| **Φ1** | ✅ **ΕΚΛΕΙΣΕ** — κλείνει η διαρροή (Α4) | `server/auth/pending-registration.ts` · `api/auth/workspace-access-request/route.ts` · `server/auth/workspace-access-request.ts` (+ ερώτημα ανά αιτούντα) · **διαγραφή** `services/email-templates/pending-registration-admin.ts` (ορφανό ⇒ CHECK 3.22) |
| **Φ2** | ✅ **ΕΚΛΕΙΣΕ** — η οντότητα (7.1-7.4) | `enterprise-id-{prefixes,class,convenience}.ts` · `enterprise-id.service.ts` · `config/firestore-collections.ts` · `types/workspace-invitation.ts` · `server/auth/workspace-invitation.ts` · `firestore.rules` (deny-all) · σουίτα **35 κελιών** |
| **Φ3** | ✅ **ΕΚΛΕΙΣΕ** — **εξαγωγή** `grantWorkspaceMembership`: την καλούν **και** η έγκριση αιτήματος **και** η αποδοχή πρόσκλησης (§2: σήμερα τρία σημεία) | νέο `lib/workspace/grant-membership.ts` · `api/admin/set-user-claims/claims-handler.ts` |
| **Φ4** | ✅ **ΕΚΛΕΙΣΕ** — API: δημιουργία · **επαναποστολή = το ίδιο POST ξανά** · ανάκληση (`withAuth` + `withSensitiveRateLimit`) · εξαργύρωση (`withPersonalOrOrgAuth` + `withHeavyRateLimit`) · **όψη πριν την απόφαση** (δημόσια, `withHeavyRateLimit`) · η λίστα ως **τέταρτη πηγή** της υπάρχουσας διαδρομής χρηστών | `api/workspace-invitations/{route,[invitationId]/revoke,redeem,preview/[token]}` · `server/auth/workspace-invitation{,-redeem}.ts` · `lib/workspace/workspace-catalog.ts` · `api/admin/role-management/users/route.ts` · ⛔ **ΟΧΙ** `rate-limit-config.ts` |

⚠️ **ΤΡΕΙΣ ΑΠΟΚΛΙΣΕΙΣ ΑΠΟ ΤΟ ΣΧΕΔΙΟ ΤΗΣ Φ4, ΚΑΘΕ ΜΙΑ ΜΕ ΜΕΤΡΗΜΕΝΟ ΛΟΓΟ:**

1. **Καμία διαδρομή «επαναποστολής», και καμία συνάρτηση `resend`.** Το §7.1 απαιτεί **νέο
   token με νέα λήξη** και το §7.3 ακυρώνει την προηγούμενη στην **ίδια** συναλλαγή ⇒ η
   επαναποστολή **είναι** δεύτερο `POST` στην ίδια πόρτα, με `supersededCount: 1`. Το
   σκεπτικό ζει γραμμένο στην ενότητα 3 του `workspace-invitation.ts`.
2. **Καμία γραμμή στο `rate-limit-config.ts`**, παρότι ο πίνακας φάσεων τη ζητούσε. Μετά το
   **ADR-855 Α1** η δήλωση του wrapper **υπερισχύει**, οπότε η γραμμή θα ήταν **νεκρή** και
   το CHECK 3.78 τη μετρά ως παραβίαση. Η οδηγία ήταν **σωστή όταν γράφτηκε** και έγινε
   επιβλαβής μέσα σε μία μέρα.
3. **Η λίστα ΔΕΝ πήρε δική της διαδρομή.** Το `GET /api/admin/role-management/users`
   συγχωνεύει ήδη **τρεις** πηγές *(μέλη · προφίλ · Firebase Auth)* **συν** τα αιτήματα
   ένταξης (ADR-660 §6), και η Φ6 δηλώνει την **ίδια** οθόνη. Δεύτερη κλήση θα έδινε **δύο
   απαντήσεις σε ένα ερώτημα** *(«ποιοι είναι στον χώρο μου;»)* με δύο στιγμές — ADR-749.
   ⚠️ Αλλά **ΟΧΙ** μέσα στο `users`: το `CompanyUser` απαιτεί `uid` και η πρόσκληση **δεν
   έχει uid** — αυτός είναι ο **κεντρικός** λόγος του §7.1. Ξεχωριστό πεδίο `invitations`,
   μία κλήση: το πρότυπο «Members / Pending invitations» του GitHub και του Slack.
| **Φ5** | ✅ **ΕΚΛΕΙΣΕ** — το email φεύγει: πρότυπο ανά γλώσσα · **κλίμακα γλώσσας τριών** · ο σύνδεσμος `/invite/<token>` · η έκβαση παράδοσης **στην ίδια απάντηση** | `services/email-templates/workspace-invitation-email.ts` · `server/auth/workspace-invitation-notice.ts` · `lib/workspace/workspace-routes.ts` (+`WORKSPACE_INVITE_SEGMENT`) · `api/workspace-invitations/route.ts` · `services/ai-pipeline/shared/mailgun-sender.ts` (**φρουρός χρόνου**) |
| **Φ6** | ✅ **ΕΚΛΕΙΣΕ** — **ΒΗΜΑ Α**: «Πρόσκληση χρήστη» πίσω από τον **ΕΝΑ** κριτή (`useCapability`, ποτέ λίστα ρόλων) · ξεχωριστός πίνακας εκκρεμών · επαναποστολή = **το ίδιο POST** μέσω **ενός** καλούντος. **ΒΗΜΑ Β**: δημόσια όψη **πριν** την ταυτότητα (αντι-phishing) · γύρος `?next=` μέσω `loginHref` · διέξοδος **υποχρεωτική** σε κάθε αποτυχία · slice **3273 bytes** μετρημένο. **26/26 + 20/20 + 11/11** άγκυρες, εκτελεσμένες | `components/admin/role-management/{useInviteCapability,useInvitationActions,invite-labels,types}.ts` · `…/components/{InviteUserDialog,InvitationTable,UsersTab}.tsx` · `components/admin/pages/RoleManagementPageContent.tsx` · `services/workspace/workspace-invitation{.client,-failure-readers}.ts` · `types/workspace-invitation-view.ts` · `components/workspace-invite/**` · `app/(auth)/invite/[token]/page.tsx` · `lib/workspace/workspace-scope.ts` · `config/domain-constants.ts` · `.i18n-shell-slice.json` + `i18n/generated/routes/invite__token.el.json` · locales `admin`/`auth` (el+en) |
| **Φ7** | *(ΕΚΤΟΣ ΕΜΒΕΛΕΙΑΣ — χωριστή απόφαση Giorgio)* Ρητή εναλλαγή ενεργού χώρου ⇒ κλείνει το όριο §6 #1 | ADR-787 Κ-2 |
| **Φ8** | Τεκμηρίωση: αυτό το ADR (Φάση 3) · ADR-660 §7 · ADR-787 γραμμή **Κ-9** *(το Κ-8 είναι πιασμένο από την επαλήθευση ιδιότητας)* · `adr-index.md` | — |

---

## 9. Άγκυρες — **κάθε μία πρέπει να κοκκινίζει σε μετάλλαξη**

| Άγκυρα | Τι αποδεικνύει |
|---|---|
| **Π1** | Η σύνδεση χωρίς χώρο **δεν** ανοίγει αίτημα και **δεν** στέλνει email *(μετάλλαξη: επαναφορά της κλήσης ⇒ κόκκινο)* |
| **Π2** | Καμία κλήση `getCompanyId()` δεν επιβιώνει στη διαδρομή ένταξης |
| **Τ1** | Token με **σωστή υπογραφή** αλλά **άλλον** παραλήπτη ⇒ `wrong-recipient` |
| **Τ2** | Ληγμένο ⇒ `expired`, **και** όταν το έγγραφο λέει `pending` (διπλός έλεγχος — ο χρόνος ζει σε δύο μέρη) |
| **Τ3** | **Δύο ταυτόχρονες** αποδοχές ⇒ **ένα** μέλος, μία `accepted` |
| **Τ4** | Στη βάση **δεν υπάρχει** το ωμό nonce *(διαρροή βάσης ≠ χρησιμοποιήσιμη πρόσκληση)* |
| **Ρ1** | Πρόσκληση με ρόλο **πάνω** από τον προσκαλούντα ⇒ `role-above-inviter` |
| **Ρ2** | `super_admin` σε πρόσκληση ⇒ άρνηση **πάντα**, ακόμη και από `super_admin` |
| **Μ1** | Αποδοχή από άνθρωπο **με** χώρο ⇒ γράφεται μέλος, **δεν** αλλάζει το claim |
| **Μ2** | Η έγκριση αιτήματος **και** η αποδοχή πρόσκλησης καλούν την **ίδια** `grantWorkspaceMembership` |
| **Κ1** | Κανόνες: πελάτης **δεν** διαβάζει και **δεν** γράφει `workspace_invitations` (35 κελιά) |
| **Α4β** | **Ανακλημένη δεν εμφανίζεται ούτε ως όψη** — και λέει `revoked`, όχι `link-invalid`· **καμία** ανάγνωση ονόματος χώρου σε άρνηση *(άρνηση δεν πληρώνει ερώτημα)* |
| **Λ1** | Η λίστα επιστρέφει **μόνο** τις ζωντανές: η αποδεκτή **είναι μέλος** και φαίνεται ήδη στη λίστα χρηστών ⇒ δύο γραμμές για έναν άνθρωπο |
| **Λ2** | 🔴 `pending` με **περασμένη** ώρα ταξιδεύει ως `expired` — **παραγόμενη**, όχι αντίγραφο του εγγράφου: κανείς δεν σκουπίζει τις ληγμένες, άρα αλλιώς ο διαχειριστής βλέπει «σε αναμονή» για σύνδεσμο που **δεν δουλεύει** |
| **Λ3** | Η ανάγνωση **δεν γράφει**: σύγκριση **ολόκληρου** του εγγράφου πριν/μετά, ποτέ ενός πεδίου |
| **Λ4** | 🔒 **Ξένος χώρος δεν εμφανίζεται** — με τον παρονομαστή μέσα: υπάρχουν **δύο** ζωντανές στη βάση, και χωρίς `companyId` στο ερώτημα η λίστα θα επέστρεφε δύο |
| **Ο1** | ΠΑΡΟΝΟΜΑΣΤΗΣ της όψης: ζωντανή πρόσκληση δίνει **όνομα χώρου + ρόλο + λήξη** |
| **Ο2** | ⛔ Η όψη **δεν διαρρέει** το email του παραλήπτη ούτε το `companyId` *(δίνεται σε όποιον κρατά τον σύνδεσμο, χωρίς ταυτότητα)* |
| **Ο3** | 🔑 Η όψη **ΔΕΝ καταναλώνει**: μετά από **δύο** ανοίγματα η πρόσκληση εξαργυρώνεται κανονικά — αλλιώς μια προ-φόρτωση πελάτη email θα έκαιγε πρόσκληση που **κανένας άνθρωπος δεν είδε** |
| **Ο4** | Το «ανοίχτηκε» γράφεται **μόνο την πρώτη φορά**: η δεύτερη κλήση **δεν** μετακινεί τη σφραγίδα, αλλιώς το πεδίο απαντά «πότε ξαναφόρτωσε τη σελίδα» αντί «πότε το είδε» |
| **Ε1-Ε2** *(Φ5)* | Κάθε γλώσσα έχει **όλα** τα λόγια **και για τους τρεις** ρόλους· ο σύνδεσμος είναι **απόλυτος** και δείχνει στο `/invite/<token>` |
| **Σ1** *(Φ5)* | 🔴 **Χωρίς δημόσια διεύθυνση ⇒ ΚΑΝΕΝΑ email** — ποτέ σύνδεσμος που δεν ξέρουμε πού οδηγεί |
| **Σ2** *(Φ5)* | Το token **κωδικοποιείται** στη διεύθυνση *(παρονομαστής: το ωμό `a+b/c=d` δεν επιβιώνει)* — ο κατασκευαστής δεν εξαρτάται από τη σημερινή αλφάβητο του `signed-token` |
| **Γ1-Γ4** *(Φ5)* | Η **κλίμακα γλώσσας**, με τον παρονομαστή κάθε σκαλοπατιού: δηλωμένη **παραλήπτη** → δηλωμένη **προσκαλούντος** → προεπιλογή· και **αποτυχία ανάγνωσης δεν ρίχνει το email** |
| **Λ1-Λ3** *(Φ5)* | Οι ημέρες στρογγυλοποιούνται **προς τα πάνω** *(6 ημ. 20 ώρες ⇒ «7»)*, ο **ενικός** λέγεται σωστά, και η ληγμένη έχει **δάπεδο 1** — ποτέ αρνητικό |
| **Ο1-Ο2** *(Φ5)* | Κενό όνομα χώρου ⇒ **ετικέτα του πίνακα**, ποτέ ωμό `comp_*`· και το όνομα **διαφεύγει** *(γραφείο με `<script>` δεν γράφει ετικέτα)* |
| **Δ1** *(Φ5)* | 🔒 Και στις **δύο** γλώσσες λέγεται ότι ο σύνδεσμος δουλεύει **μόνο** γι' αυτή τη διεύθυνση — εκεί σπάει το προωθημένο email (§7.5), και εκεί ρητά **δεν** δεσμεύει το Figma |
| **Α1-Α4** *(Φ5)* | Η έκβαση είναι **ονομασμένη** και ο αποστολέας **ΠΟΤΕ δεν πετά**: ο πάροχος αρνείται ⇒ `failed` · πετά ⇒ `failed` · χωρίς διεύθυνση ⇒ `unaddressable` **και καμία απόπειρα** · το όνομα χώρου δεν διαβάστηκε ⇒ **το email φεύγει** |
| **Σ1-Σ2** *(Φ5, αποστολέας)* | 🔒 **Το ωμό token ΔΕΝ φτάνει ποτέ στα ίχνη** *(με παρονομαστή: τα ίχνη **γράφτηκαν**)* — ταξιδεύει **μόνο** μέσα στο μήνυμα του παραλήπτη |
| **Χ1-Χ3** *(Φ5, `mailgun-sender`)* | Ο **φρουρός χρόνου** ζητά **την ίδια** σταθερά της αλυσίδας *(ποτέ δεύτερο αριθμό — ADR-749)*, το σήμα **φτάνει** στο `fetch`, και η ματαίωση γίνεται **ονομασμένη αποτυχία** |

⚠️ Οι **Λ4** και **Ο4** **δεν** ήταν στο σχέδιο: προστέθηκαν επειδή η μία φυλά **απομόνωση
μισθωτή** *(κριτήριο ασφαλείας, όχι συμμετρία τεκμηρίωσης)* και η άλλη ένα πεδίο που το §6 #3
δηλώνει ρητά **ένδειξη, όχι απόδειξη** — και μια ένδειξη που μετακινείται σε κάθε ανανέωση
δεν είναι ούτε ένδειξη. **30/30 πράσινες**, γραμμένες **αφού** τις είδα να τρέχουν.

---

## 10. Πύλες που ακουμπάμε

**3.16** (35 κελιά νέας συλλογής) · **3.22** (το `pending-registration-admin.ts` γίνεται ορφανό ⇒ **διαγραφή**, όχι σχόλιο) · **3.35 / 3.10** (το νέο ερώτημα ανά αιτούντα θέλει γραμμένο λόγο) · **3.49 / 3.53** (ταυτότητα ADR + ενότητες) · **3.58** (⚠️ **κανένα** νέο κανάλι χώρου — η πρόσκληση **φέρνει** την εταιρεία στο έγγραφό της, δεν τη **διαβάζει** από τον πελάτη) · **3.68** (ο κριτής ικανότητας) · **3.8 / 3.71** (κλειδιά el+en) · **3.28** (`jscpd:diff` πριν από κάθε «done»).

**Προστέθηκαν στη Φ4, όλες μετρημένες και όχι υποθετικές:**

- **ADR-817 §5** *(κλειστό σύνολο διαδρομών πολίτη)* — η εξαργύρωση είναι ο **14ος** καταναλωτής, με γραμμένο λόγο· η άγκυρα **κοκκίνισε πρώτη**, όπως οφείλει.
- **3.17** *(entity audit)* — **μετρήθηκε**: `current viols: 1 = baseline 1`, καμία νέα. Ο φόβος ότι η έκδοση χρωστά ίχνος ήταν **αβάσιμος**.
- **3.22** *(νεκρός κώδικας)* — το `markWorkspaceInvitationOpened` της Φ2 ήταν **ορφανό** *(μία εμφάνιση σε όλο το δέντρο: ο ορισμός του)*. Το έλυσε η διαδρομή όψης, που είναι ο **φυσικός** καλών του. Πύλη πράσινη.
- **3.15 / 3.10** — η λίστα ρωτά με **δύο ισότητες και καμία διάταξη** ⇒ συγχώνευση μονοπεδιακών ευρετηρίων, **κανένα** σύνθετο *(το `firestore.indexes.json` δεν έχει καμία εγγραφή ούτε για τα αιτήματα ένταξης, που ρωτούν ταυτόσημα)*· περιλαμβάνει `companyId`, άρα καμία γραμμένη εξαίρεση.
- 🔴 **3.78** *(πολιτική ρυθμού)* — **μπλόκαρε, και είχε δίκιο για λάθος λόγο**: η δημόσια διαδρομή όψης δηλώνει σωστά `HEAVY`, αλλά η πύλη μετρούσε «δήλωση ≠ **προεπιλογή**» ως νεκρή γραμμή πίνακα. Διορθώθηκε το **κριτήριο** — **ADR-855 Φ7**, 55/83 ψευδώς θετικά.
- **Next.js build** — το `preview/[token]` **δεν** μπήκε ως `[token]/`: συνυπάρχει με `[invitationId]/revoke`, και δύο διαφορετικά ονόματα slug στο ίδιο επίπεδο **ρίχνουν το build**.

**Προστέθηκαν στη Φ5, όλες μετρημένες:**

- **3.28** *(jscpd)* — **✅ καμία νέα επικάλυψη** σε 5 αρχεία. Το ρώτησα επειδή το πρότυπο συνθέτει τη δική του ενότητα αντί να καλέσει το `renderMessageSection`: η διάταξη βάζει τα **στοιχεία της θέσης ΠΡΙΝ** από το κουμπί *(ο άνθρωπος αποφασίζει **αφού** διαβάσει ποιος και τι — §5 #4)*, ενώ το πλαίσιο, το κουμπί και τα χρώματα έρχονται **αυτούσια** από τα κοινά.
- **3.17** *(entity audit)* — **δεν ακουμπιέται, και ο λόγος διαβάστηκε από το script**: το `TRACKED_COLLECTION_KEYS` απαριθμεί 16 κλειδιά και το `WORKSPACE_INVITATIONS` **δεν είναι μέσα**. ⚠️ Επαληθεύτηκε και **αντίστροφα**: το `CHAIN_VAR_RE` πιάνει όντως `tx.update(`, άρα ο υπάρχων γραφέας **ανιχνεύεται** και αγνοείται επειδή το πλησιέστερο `COLLECTIONS.*` είναι untracked — η πύλη είναι **ακριβής** εδώ, όχι τυφλή. *(Και το `recordChange()` θα ήταν **λάθος**: το `AuditEntityType` δεν έχει τύπο πρόσκλησης.)*
- **3.79** *(σκιασμένο αρχείο, ADR-858 — γεννήθηκε **μέσα** σε αυτή τη συνεδρία από άλλη ροή)* — **δεν ακουμπιέται**: κανένα νέο όνομα δεν έχει ομώνυμο `.tsx` *(Κ1)* ούτε ομώνυμο φάκελο *(Κ2)*.
- **3.10 / 3.35** — **καμία** νέα ερώτηση Firestore· ο αποστολέας κάνει **σημειακή** ανάγνωση ονόματος χώρου μέσω του **ενός** αναγνώστη.
- **3.8 / 3.36 / 3.71** — **κανένα** κλειδί locale: τα πρότυπα email φέρουν τα λόγια τους **inline by design** *(το `t()` απαιτεί ενεργό i18next, δηλαδή **καθολική κατάσταση** σε διεργασία που εξυπηρετεί πολλούς παραλήπτες)*. Η γλώσσα είναι **παράμετρος**.
- 🔴 **3.60 / άγκυρα Ε1** — **ΤΟ ΕΥΡΗΜΑ ΠΟΥ ΔΕΣΜΕΥΕΙ ΤΗ Φ6**: η άγκυρα απαιτεί το `OUTSIDE_WORKSPACE` να **ισούται ΑΚΡΙΒΩΣ** με *(τμήματα όλων των groups) − (παιδιά του `o/[workspace]`)*, υπολογισμένο από το **πραγματικό δέντρο**. Άρα δήλωση του `invite` **τώρα** ⇒ κόκκινο *(κλειδί χωρίς δέντρο)*· σελίδα στη Φ6 **χωρίς** δήλωση ⇒ κόκκινο *(δέντρο χωρίς κλειδί)*. ⇒ **Σελίδα και δήλωση ΥΠΟΧΡΕΩΤΙΚΑ στο ίδιο commit.** Μετρήθηκε επίσης: **καμία** σύγκρουση με παιδί του χώρου *(Κ3 καθαρό)* και **καμία** γραμμή στο `.workspace-scope.json` *(κρίνει μόνο το `(app)`· η σελίδα ζει στο `(auth)`)*.

---

## 11. Τι **απορρίφθηκε**, με λόγο

| Απορρίφθηκε | Γιατί |
|---|---|
| Αποστολή αιτήματος σε **όλες** τις εταιρείες | Διαρροή προσωπικών δεδομένων σε ξένους· «όποια προλάβει κερδίζει» |
| **Ντετερμινιστικό** id ανά (εταιρεία, email) | Η επαναποστολή απαιτεί **νέο** token· θα ζούσε ζωντανό το παλιό |
| **Κοινόχρηστος σύνδεσμος** ένταξης | Κανένα προϊόν του κλάδου μας· σπάει τη δέσμευση στο email (§5 #2) |
| **Άρνηση** πρόσκλησης σε μέλος άλλης εταιρείας | Αντιγράφει το **τεκμηριωμένο ελάττωμα** της Autodesk |
| **Μετακίνηση** μέλους από τον έναν χώρο στον άλλο | Ένα γραφείο θα «έκλεβε» μέλος άλλου με μία πρόσκληση |
| **Κατάλογος χώρων μέσα στο claim** | Όριο 1000 bytes ⇒ *«η 21η πρόσκληση σπάει τη σύνδεση»* — ADR-787 Ε-5 §3, **μετρημένο** |
| `bcrypt`/`argon2` για το token | Προστατεύουν μυστικά **χαμηλής** εντροπίας· σε 256-bit τυχαίο είναι μόνο κόστος CPU |

---

## 12. 🔗 Αμφίδρομες παραπομπές — το συμβόλαιο

- **ADR-787** → νέα γραμμή **Κ-9** (ένταξη μέλους σε χώρο)· **Α2** αποκτά μερική υλοποίηση με **γραμμένο** το υπόλοιπο (§6 #1)
- **ADR-660** → **§7**: το §6 (αίτημα ως οντότητα) **παγώνει**, δεν αναιρείται· το ανοιχτό «ρητό ζητώ είσοδο» του §6.6 **παραμένει ανοιχτό** με ονομασμένο λόγο (Α1)
- **ADR-844** → δανείζει το δόγμα του token και τους **ονομασμένους λόγους άρνησης**
- **ADR-801** → η κρίση «επιτρέπεται να προσκαλέσει;» περνά από τον ΕΝΑ κριτή
- **ADR-798** → η δηλωμένη ταυτότητα της εταιρείας στην οθόνη αποδοχής (§5 #4)

---

## 13. Changelog

| Ημερομηνία | Τι |
|---|---|
| **2026-09-12** | **Φάση 0** — το έγγραφο, γραμμένο **πριν** από την πρώτη γραμμή κώδικα (N.0.1). Έρευνα σε 8 προϊόντα· ζωντανή μέτρηση 6 ερωτημάτων στον κώδικα· αποφάσεις Α1-Α4 από τον Giorgio. 🔴 **Δύο διορθώσεις στο handoff**: ο αριθμός **852 ήταν πιασμένος** (⇒ 853) και η γραμμή **Κ-8 ήταν πιασμένη** (⇒ Κ-9). 🔑 **Ένα εύρημα ανέτρεψε μια απόφαση**: η πολλαπλή συμμετοχή **δουλεύει ήδη στον διακομιστή** (`decideMembership`, 352 διαδρομές) ⇒ ο ήδη-μέλος-αλλού **προσκαλείται**, δεν απορρίπτεται |
| **2026-09-12** | ✅ **Φ2 + Φ3 — Η ΟΝΤΟΤΗΤΑ ΚΑΙ Ο ΕΝΑΣ ΓΡΑΦΕΑΣ** (`e57396b8` + `62e66fdd`, **+2.208** γραμμές· μετρημένο με `git show --stat`, όχι από μνήμη). ⚠️ **Η Φ3 ΔΕΝ έχει δικό της commit**: το `lib/workspace/grant-membership.ts` (+183) μπήκε **μαζί** με τη Φ2 — η αποδοχή πρόσκλησης τον χρειάζεται τη **στιγμή** που γεννιέται, και χωριστό commit θα άφηνε την εξαργύρωση να καλεί γραφέα που δεν υπάρχει. **Φ2**: πρόθεμα `winv` + generators (`e57396b8`, 8 αρχεία) · `types/workspace-invitation.ts` (+269) · `workspace-invitation.ts` (+270 — έκδοση · ανάκληση · **και η γραμμένη άρνηση** να υπάρξει `resend`) · `workspace-invitation-redeem.ts` (+324 — αποδοχή · άρνηση · «ανοίχτηκε») · `firestore-collections.ts` · `environment-contract.ts` (το `WORKSPACE_INVITE_SECRET` **δηλώνεται**) · `firestore.rules` **deny-all** (+24) · `fake-firestore.ts` (+92 — ο πλαστός έμαθε συναλλαγή **με ερώτημα** μέσα, που απαιτεί το §7.3) · **21 άγκυρες** (+468). **Φ3**: ο `claims-handler.ts` ξαναγράφτηκε να **καλεί** τον ΕΝΑ γραφέα (+44/−34), άγκυρα **Μ2** · `super-admin-scope.test.ts` (+120). 🔶 **ΔΗΛΩΜΕΝΟ ΑΝΟΙΧΤΟ, ΚΑΙ ΔΕΝ ΕΙΝΑΙ ΜΙΚΡΟ**: η σουίτα κανόνων `suites/workspace-invitations.rules.test.ts` (+210, **35 κελιά**) είναι γραμμένη, καταχωρημένη στο `coverage-manifest.ts` και **δεκτή από το CHECK 3.16** — αλλά **ΔΕΝ ΕΧΕΙ ΕΚΤΕΛΕΣΤΕΙ ΠΟΤΕ**: απαιτεί τον εξομοιωτή Firestore, δηλαδή μακρόχρονη διεργασία σε μηχάνημα **4 πυρήνων** με δεύτερο πράκτορα ενεργό. ⚠️ Άρα τα 35 κελιά είναι **δηλωμένη** κάλυψη, όχι **αποδεδειγμένη** — το σχήμα «0 = κανείς δεν κοίταξε» με άλλο πρόσωπο, και **η πύλη 3.16 δεν μπορεί να το δει** γιατί κρίνει *δήλωση*, όχι *εκτέλεση*. **Απαιτεί εκτέλεση πριν την παραγωγή** |
| **2026-09-12** | ✅ **Φ4 — ΟΙ ΠΟΡΤΕΣ.** Τέσσερις διαδρομές: έκδοση + ανάκληση (`withAuth` `users:users:manage` + `withSensitiveRateLimit`, που **τώρα** επιβάλλει όντως 20/min χάρη στο ADR-855 Α1) · **εξαργύρωση** (`withPersonalOrOrgAuth` + `withHeavyRateLimit`) · **όψη** (δημόσια, μόνο token). Δύο αναγνώστες που έλειπαν από τη Φ2: `listPendingWorkspaceInvitations` *(η κατάσταση **παράγεται** — ληγμένη `pending` ταξιδεύει ως `expired`)* και `previewWorkspaceInvitation`. Εξήχθη ο **ΕΝΑΣ** `readWorkspaceName` (§7: «από τη **μία** διαδρομή»). 🔑 **Τρεις αποφάσεις που απέκλιναν από το σχέδιο, με λόγο**: καμία διαδρομή επαναποστολής *(είναι το ίδιο POST)* · καμία γραμμή στον πίνακα ρυθμού *(η οδηγία του handoff έγινε **επιβλαβής** μέσα σε μία μέρα)* · η λίστα ως **τέταρτη πηγή** της υπάρχουσας διαδρομής χρηστών, αλλά σε **ξεχωριστό πεδίο** — το `CompanyUser` απαιτεί `uid` και η πρόσκληση δεν έχει. 🔴 **Τέσσερα δικά μου λάθη, όλα πιασμένα με μέτρηση**: (α) το token της **πράξης** μπήκε σε **σώμα** και όχι σε URL — απόκλιση από το δικό μας πρότυπο `vendor/quote/[token]`, με λόγο RFC 6819 §5.1.5 *(η **όψη** το κρατά στη διεύθυνση, γιατί **είναι** ο σύνδεσμος του email)*· (β) το `preview/[token]` απέφυγε **σφάλμα build** *(δύο slug names στο ίδιο επίπεδο)*· (γ) ένας φρουρός ρόλου στην ενεργοποίηση claim ήταν **αδύνατο να πυροδοτήσει** *(ο ρόλος έρχεται ήδη `InvitableRole`)* — ADR-749 §5, αφαιρέθηκε· (δ) το `?? ''` στο `claimCompanyId` **μοιάζει** με το απαγορευμένο του `actorWorkspace` και είναι το **σωστό** ιδίωμα για άλλο ερώτημα — γράφτηκε ο λόγος, αλλιώς ο επόμενος το «διορθώνει». ⚠️ **Δηλωμένο ανοιχτό**: το `activeWorkspaceChanged: false` λέει στον άνθρωπο **με όνομα** ότι είναι μέλος χωρίς ο χώρος να είναι ενεργός (§6 #1 · Φ7 εκτός εμβέλειας) |
| **2026-09-12** | ✅ **Φ1 — ΕΚΛΕΙΣΕ Η ΔΙΑΡΡΟΗ.** `pending-registration.ts` → **`identity-record.ts`** (`ensureIdentityRecord`): γράφει **μόνο ταυτότητα**· έφυγαν `getCompanyId()`, το άνοιγμα αιτήματος, η ειδοποίηση διαχειριστών, το `ADMIN_ROLES` και το `resolveAdminEmails`. **Διαγράφηκαν** `pending-registration-admin.ts` (+ test) και οι δρόμοι ανοίγματος `openAccessRequestInTx`/`readAccessRequestInTx` — *νεκρός κώδικας δεν «μένει για αργότερα»· είναι ανοιχτή πόρτα*. Το `readOwnAccessState` δέχεται πλέον **μόνο `uid`** (ερώτημα στο `requesterUid`, γραμμένη εξαίρεση tenant-scope) με **εξαγόμενη** σειρά προτεραιότητας (`collapseOwnAccessStates`). **25/25 άγκυρες πράσινες** σε 4,2s. 🔴 **Δύο υποθέσεις του handoff καταρρίφθηκαν με μέτρηση**: (α) το `.authority-registry.json` **δεν** ανέφερε ποτέ το `ADMIN_ROLES` του `pending-registration` ⇒ η διαγραφή δεν άγγιξε την πύλη 3.68· (β) έξι **άλλα** ADR και έξι αρχεία κώδικα έδειχναν ονομαστικά στη διαγραμμένη μονάδα — διορθώθηκαν **όλα**, αλλιώς η τεκμηρίωση θα έδειχνε σε ανύπαρκτο αρχείο |
| **2026-09-12** | ✅ **Φ5 — ΤΟ EMAIL ΦΕΥΓΕΙ.** Πέντε αρχεία πηγής + τρεις σουίτες· **34/34 νέες άγκυρες πράσινες** (2,9s) και **30/30 οι υπάρχουσες** αμετάβλητες· **CHECK 3.28 καθαρό**. 🏆 **Η ΓΛΩΣΣΑ ΤΟΥ ΠΑΡΑΛΗΠΤΗ — ΕΡΕΥΝΗΘΗΚΕ ΑΝΤΙ ΝΑ ΜΑΝΤΕΥΤΕΙ**: το **Slack** στέλνει στη γλώσσα **του χώρου** *(μία για όλους)*· η **Microsoft (Entra B2B)** έχει κλίμακα αλλά ρωτά πρώτα ρητή παράμετρο· **GitHub/Figma** δεν το τεκμηριώνουν. Εμείς ρωτάμε **πρώτα τον άνθρωπο**: η πρόσκληση πάει σε **email**, άρα ο παραλήπτης **μπορεί ήδη να είναι χρήστης μας** και να έχει **δηλώσει** γλώσσα. Κλίμακα: **παραλήπτης → προσκαλών → προεπιλογή**. 🔒 **Δεν είναι όργανο απαρίθμησης**: ο έλεγχος τρέχει μόνο στον διακομιστή, η απόκριση είναι **ταυτόσημη** είτε βρεθεί λογαριασμός είτε όχι, και τη διεύθυνση την έγραψε ο **ίδιος** ο διαχειριστής. 🔑 **Η ΑΠΟΚΡΙΣΗ ΛΕΕΙ ΤΗΝ ΑΛΗΘΕΙΑ ΓΙΑ ΤΗΝ ΠΑΡΑΔΟΣΗ, ΚΑΙ ΟΧΙ ΠΕΡΙΣΣΟΤΕΡΑ**: πεδίο `delivery: 'accepted' \| 'unaddressable' \| 'failed'` δίπλα στο **201**. ⚠️ `accepted` σημαίνει *«ο πάροχος το δέχτηκε προς αποστολή»*, **ΟΧΙ** «παραδόθηκε» — το ίδιο το λεξιλόγιο του Mailgun ξεχωρίζει `accepted` από `delivered`, με `temporary_fail`/`permanent_fail` ανάμεσα. Ένα `sent` θα ήταν **ψέμα με παράλειψη**, το ίδιο σχήμα που το έργο απαγορεύει ήδη στο `openedAt` *(«ένδειξη, όχι απόδειξη»)* και στο `identityAssurance` *(«τρεις καταστάσεις, ποτέ boolean»)*. 🔶 **Δηλωμένο επόμενο (Φ5-Β)**: η **αληθινή** παράδοση με λόγο, από τα γεγονότα του παρόχου — το μοντέλο που η **Atlassian** έχει ως *«admin email audit»* *(λίστα μη παραδοθεισών **με αιτία**, «invitation blocked», και resend που **αφαιρεί πρώτα** τη διεύθυνση από τη λίστα bounce)* και που **Slack/GitHub/Figma δεν έχουν**. Η υποδομή μετρήθηκε ότι **υπάρχει μισή**: επαληθευτής υπογραφής Mailgun *(inline στο inbound webhook — θέλει **εξαγωγή**, πρότυπο `meta-signature.ts`)*, `withWebhookRateLimit`, και συσχέτιση με `user-variables`· **λείπει** συλλογή γεγονότων **εξερχομένων** *(το `EMAIL_INGESTION_QUEUE` είναι **μόνο** εισερχόμενο)*. 🔴 **ΕΛΑΤΤΩΜΑ ΠΟΥ ΒΡΕΘΗΚΕ ΚΑΙ ΔΙΟΡΘΩΘΗΚΕ ΕΠΙ ΤΟΠΟΥ (N.0.2)**: ο `sendReplyViaMailgun` — **εννέα** καταναλωτές — έκανε **γυμνό `fetch` χωρίς χρονικό όριο**, δηλαδή ένας **σιωπηλός** πάροχος κρατούσε το αίτημα ανοιχτό· και **καμία** από τις 8 σουίτες που τον αναφέρουν δεν τον **εκτελούσε** *(όλες τον κάνουν mock)*. Πλέον φορά `AbortSignal.timeout(PROVIDER_TIMEOUT_MS)` — **την ίδια** σταθερά της αλυσίδας, ποτέ δεύτερο αριθμό — και απέκτησε **πρώτη** σουίτα που τον εκτελεί. Επαληθεύτηκε ότι οι **τρεις** σουίτες που τον εισάγουν αληθινά **παραμένουν πράσινες** (34/34). 🔴 **Δύο δικά μου λάθη, και τα δύο πιάστηκαν εκτελώντας**: (α) άγκυρα που ρωτούσε `not.toContain('-')` σε **ολόκληρο** το HTML *(γεμάτο `font-size`, `border-radius`)* — μετρούσε **άλλο πράγμα** από το όνομά της· διορθώθηκε να ρωτά **στη γραμμή της λήξης**· (β) mock τηλεμετρίας που έσπασε σε **ανύψωση** του jest *(`Cannot access 'logSink' before initialization` — ο κώδικας καλεί `createModuleLogger()` στο σώμα του module)*· οι μέθοδοι διαβάζουν πλέον τη δέσμευση **όταν κληθούν**. 🔶 **ΔΗΛΩΜΕΝΟ ΑΝΟΙΧΤΟ, ΓΙΑ ΝΑ ΜΗ ΘΕΩΡΗΘΕΙ ΜΕΤΡΗΜΕΝΟ**: οι νέες άγκυρες **ΔΕΝ** δοκιμάστηκαν με **μετάλλαξη** *(θα απαιτούσε αλλοίωση πηγής + δύο ακόμη εκτελέσεις σε **κοινό** working tree)*. Είναι **34/34 πράσινες**, όχι «34/34 κόκκινες σε μετάλλαξη» |
| **2026-09-13** | ✅ **Φ6 ΒΗΜΑ Α — Η ΔΙΟΙΚΗΤΙΚΗ ΕΠΙΦΑΝΕΙΑ.** Επτά αρχεία πηγής + **η πρώτη σουίτα που απέκτησε ποτέ το `role-management`**: **26/26 άγκυρες πράσινες, εκτελεσμένες** (11,5s)· **CHECK 3.28 καθαρό** σε 7 αρχεία· **CHECK 3.33** 56/56. 🔑 **ΤΟ «ΠΟΙΟΣ ΒΛΕΠΕΙ» ΔΕΝ ΑΠΑΝΤΗΘΗΚΕ ΜΕ ΡΟΛΟΥΣ**: `useInviteCapability` → `useCapability('users:users:manage')`, ο ΕΝΑΣ PEP. ⛔ Ρητά **ΟΧΙ** το `canEdit` της σελίδας — είναι `super_admin`-only και θα έκρυβε την πρόσκληση από τον `company_admin`, δηλαδή από αυτόν για τον οποίο γράφτηκε το Α3. Η **άγκυρα Α4** το φυλά διαβάζοντας **τα ίδια τα route**: και οι δύο πόρτες πρέπει να δηλώνουν την **ίδια** συμβολοσειρά ικανότητας — αλλιώς η οθόνη και ο διακομιστής απαντούν σε δύο *παρόμοια* ερωτήματα αντί για ένα. 🔴 **ΕΝΑ ΔΙΔΥΜΟ ΑΠΟΤΡΑΠΗΚΕ ΠΡΙΝ ΓΕΝΝΗΘΕΙ (N.18)**: το «Αποστολή» του διαλόγου και το «Επαναποστολή» της γραμμής είναι **το ίδιο POST** (§7.3)· γράφτηκαν ως **ΕΝΑΣ** καλών (`services/workspace/workspace-invitation.client.ts`), με τον αναγνώστη αρνήσεων **δίπλα του** — το ζευγάρι που έχει ήδη το `services/contact/`. *(Η πρώτη μου τοποθέτηση τον έβαλε στα components· διορθώθηκε πριν τον εισαγάγει οτιδήποτε.)* 🔑 **Η ΑΠΟΚΡΙΣΗ ΤΗΣ ΠΑΡΑΔΟΣΗΣ ΕΓΙΝΕ ΣΥΜΠΕΡΙΦΟΡΑ, ΟΧΙ ΔΙΑΚΟΣΜΗΣΗ**: όταν `delivery !== 'accepted'` ο διάλογος **ΜΕΝΕΙ ΑΝΟΙΧΤΟΣ** με τον ονομασμένο λόγο *(ένα toast που περνά θα άφηνε τον διαχειριστή να περιμένει άνθρωπο που δεν έλαβε ποτέ τίποτα — η βλάβη που η Atlassian έλυσε εκ των υστέρων)*, ενώ το `supersededCount > 0` λέγεται με **προειδοποίηση**, γιατί αλλιώς ο διαχειριστής νομίζει ότι κυκλοφορούν **δύο** σύνδεσμοι. 🔑 **Η ΛΗΓΜΕΝΗ ΕΧΕΙ ΠΡΑΞΕΙΣ** (άγκυρα Γ5): η κατάσταση είναι **παραγόμενη**, το έγγραφο είναι ακόμη `pending` — άρα η ανάκληση **όντως γράφει**. 🔑 **ΚΑΙ Η ΑΡΝΗΣΗ ΞΑΝΑΦΕΡΝΕΙ ΤΗ ΛΙΣΤΑ** (άγκυρα Δ1): 409/404 δεν σημαίνει «δεν έγινε τίποτα», σημαίνει **«η οθόνη μας είναι μπαγιάτικη»**· μόνο το 503 την αφήνει ως έχει. 🔴 **ΔΙΚΟ ΜΟΥ ΛΑΘΟΣ, ΠΙΑΣΜΕΝΟ ΜΟΝΟ ΜΕ ΕΚΤΕΛΕΣΗ**: η πρώτη γραφή της σουίτας αντικατέστησε το `useSemanticColors` με `{ text: { muted: '' } }` και **17 από τις 26** έσκασαν με `Cannot read properties of undefined (reading 'primary')` — το `badge.tsx` καλεί **το ίδιο** άγκιστρο και διαβάζει `colors.bg.primary`. **Η θεραπεία δεν ήταν παχύτερο mock αλλά η ΔΙΑΓΡΑΦΗ του**: το άγκιστρο είναι `useMemo` πάνω σε σταθερά, και κάθε χειρόγραφο σχήμα θα ήταν **δεύτερο λεξιλόγιο του design system** που αποκλίνει σιωπηλά στην πρώτη προσθήκη κλειδιού. Ο λόγος γράφτηκε **μέσα** στη σουίτα, ώστε να μην το «ξαναδιορθώσει» ο επόμενος. 🔴 **ΔΙΟΡΘΩΣΗ ΤΟΥ HANDOFF, ΜΕΤΡΗΜΕΝΗ**: τα πέντε αρχεία της Φ5 **ΔΕΝ ήταν ασταδιοποίητα** — είχαν ήδη γίνει commit στο `1c77dc1b`. 🔶 **ΔΗΛΩΜΕΝΑ ΑΝΟΙΧΤΑ**: (α) οι 26 άγκυρες **ΔΕΝ** δοκιμάστηκαν με **μετάλλαξη** — είναι «26/26 πράσινες», όχι «26/26 κόκκινες σε μετάλλαξη»· οι οκτώ μεταλλάξεις που θα τις έπρεπε να ρίξουν είναι **γραμμένες στην κεφαλή του αρχείου**, ανεκτέλεστες· (β) παραμένουν **και τα δύο ανοιχτά της Φ5** — η σουίτα των **35 κελιών** δεν έχει εκτελεστεί ποτέ (θέλει εξομοιωτή) και το **`WORKSPACE_INVITE_SECRET` λείπει από το περιβάλλον, οπότε κάθε πόρτα απαντά 503**: η επιφάνεια είναι **χτισμένη αλλά μη δοκιμασμένη ζωντανά**· (γ) το **ΒΗΜΑ Β** (δημόσια σελίδα) δεν ξεκίνησε, και μαζί του τα **δύο μητρώα που μπλοκάρουν αμφίδρομα** — η σελίδα, το `OUTSIDE_WORKSPACE` και το `routeSlices` **υποχρεωτικά στο ΙΔΙΟ commit** |
| **2026-09-13** | ✅ **Φ6 ΒΗΜΑ Β — Η ΔΗΜΟΣΙΑ ΣΕΛΙΔΑ.** `(auth)/invite/[token]/page.tsx` (Server) + `WorkspaceInviteContent.tsx` (Client) + τύπος συνόρου + ετικέτες + locales el/en + ο **ΕΝΑΣ** καλών απέκτησε την εξαργύρωση. **20/20 άγκυρες πράσινες, εκτελεσμένες** · **CHECK 3.28 καθαρό** · **3.33 πράσινη** (56/56) · **3.34: 251/252**. 🔑 **Η ΟΨΗ ΕΙΝΑΙ ΔΗΜΟΣΙΑ, ΚΑΙ ΕΙΝΑΙ ΑΝΤΙ-PHISHING** (§5 #4): ο ανώνυμος βλέπει **πρώτα** ποιος τον καλεί και **μετά** συνδέεται — αντίθετα από τον αδελφό `/n/[notificationId]`, που στέλνει τον ανώνυμο κατευθείαν στη σύνδεση *(εκείνος ρωτά «δες τη **δική σου** ειδοποίηση», αυτός «μπες σε **ξένο** οργανισμό»)*. Το «μπορεί να απαντήσει ή πρέπει να συνδεθεί» είναι **διακριτή ένωση**, ώστε «σύνδεση **χωρίς** διεύθυνση επιστροφής» να είναι **αδύνατη** στη μεταγλώττιση. 🔑 Η **διέξοδος** είναι υποχρεωτικό πεδίο **κάθε** αποτυχίας (μάθημα ADR-844: λευκή σελίδα χωρίς κουμπί), και το `activeWorkspaceChanged: false` λέγεται **με όνομα** (§6 #1, άγκυρα Γ2). 🔴 **ΤΡΙΑ ΔΙΚΑ ΜΟΥ ΛΑΘΗ, ΟΛΑ ΠΙΑΣΜΕΝΑ ΜΕ ΜΕΤΡΗΣΗ ΚΑΙ ΚΑΝΕΝΑ ΜΕ ΕΙΚΑΣΙΑ**: (α) **«0 ns []»** — τα κλειδιά χτίζονταν ως `` `${K}.title` `` με το `K` πάνω σε **εισηγμένη** σταθερά· το `expandTemplate` (`constant-resolution.js:224`) λύνει **μόνο** σταθερές του **ίδιου αρχείου** και επιστρέφει `complete:false`, οπότε το `foldObjectLiteral` απέρριπτε **σιωπηλά** όλες τις τιμές — και επειδή το `splitKey` παράγει το namespace από το πρόθεμα `ns:` **του κλειδιού**, μηδέν κλειδιά έδωσε αυτομάτως μηδέν namespaces: **μία ρίζα, δύο συμπτώματα**. Θεραπεία: **πλήρη κυριολεκτικά**, όπως το ζωντανό `first-contact-labels.ts`· (β) **σφράγιση από το μέλλον** — η άγκυρα Λ13 κρίνει σε **UTC** (`toISOString`) ενώ το μηχάνημα ζει σε **UTC+3**, άρα κάθε σφράγιση μετά τις 21:00 τοπικά κοκκινίζει **δύο** ισχυρισμούς· (γ) **μπαγιάτικο manifest** — επεξεργάστηκα το μητρώο **μετά** την τελευταία εκτέλεση του γεννήτορα. 🔑 **ΚΑΙ ΜΙΑ ΣΕΙΡΑ ΠΟΥ ΤΟ HANDOFF ΕΙΧΕ ΑΝΑΠΟΔΑ**: ο γεννήτορας **ΔΕΝ ανακαλύπτει** διαδρομές — παράγει artifact **μόνο** για ό,τι είναι **ήδη δηλωμένο** στο `routeSlices`. Άρα η δήλωση **οφείλει** να προηγηθεί της μέτρησης, και υπάρχει **αναγκαστικό παράθυρο** όπου το CHECK 3.34 φράζει **όλο το δέντρο** — μετρημένο ζωντανά: μπλόκαρε το commit συνεργαζόμενου πράκτορα **δύο φορές**. **Μητρώο και παραγόμενο αλλάζουν στην ΙΔΙΑ κίνηση.** 📏 **Η σφράγιση**: `3273 / 4091 bytes · 1 ns [auth]` *(ταβάνι **υπολογισμένο**, δεν δηλώνεται)* — κάθεται εύλογα ανάμεσα στα αδέλφια του `(auth)`: `/n/` 493 · `/mandate/[token]` 1842 · **εδώ 3273** · `/contact/[token]` 4517. Λογιστική **30 παρόντα · 0 απόντα · 0 αρνηθέντα · 0 ορφανά · Κ2 30 εντός / 0 εκτός**. 🔶 **ΔΗΛΩΜΕΝΟ ΑΝΟΙΧΤΟ, ΚΑΙ ΔΕΝ ΕΙΝΑΙ ΕΛΑΤΤΩΜΑ**: η **μία** αποτυχία του 3.34 είναι το **Π5** (*«κάθε δηλωμένο slice ΠΑΡΑΔΙΔΕΤΑΙ»*), και **αποδείχθηκε** ότι οφείλεται **αποκλειστικά** στο ότι το αρχείο είναι **untracked**: η γρ. 142 τρέχει `git grep` — που βλέπει **μόνο tracked**. Μετρημένο και στις δύο κατευθύνσεις: `git grep` → μόνο `GuestContactContent.tsx`· `git grep --untracked` → **και** το `WorkspaceInviteContent.tsx`, με τα δύο σημάδια στις γρ. 68/71. Η εγγραφή ζει στο **client παιδί**, όπως **επιβάλλει** το ADR-744 §18 *(«ΠΟΤΕ σε Server Component — ξεχωριστός γράφος module»)* και όπως κάνει **ήδη** το αδελφό `contact/[token]`. ⇒ **Πράσινο με το commit, χωρίς καμία αλλαγή κώδικα.** 🔶 **Επίσης ανοιχτά**: οι άγκυρες είναι «πράσινες», **όχι** «κόκκινες σε μετάλλαξη» *(οι πέντε μεταλλάξεις είναι γραμμένες στην κεφαλή της σουίτας, ανεκτέλεστες)* · η σουίτα των **35 κελιών** της Φ2 **δεν έχει εκτελεστεί ποτέ** · το **`WORKSPACE_INVITE_SECRET` λείπει** ⇒ κάθε πόρτα απαντά **503**, άρα η επιφάνεια είναι **χτισμένη αλλά μη δοκιμασμένη ζωντανά** |
| **2026-09-13** | ✅ **ΕΝΕΡΓΟΠΟΙΗΣΗ + ΠΡΩΤΗ ΖΩΝΤΑΝΗ ΔΟΚΙΜΗ** — `WORKSPACE_INVITE_SECRET` στο Coolify (runtime), μετρημένο `configured 3→4 · degraded 3→2`. Η πρόσκληση εκδόθηκε, ο Mailgun τη δέχτηκε, το email **έφτασε**, η δημόσια σελίδα αποδόθηκε σωστά σε καθαρή συνεδρία. 🔴 **Η ΑΠΟΔΟΧΗ ΦΡΑΧΤΗΚΕ ΑΠΟ ΕΛΑΤΤΩΜΑ ΚΩΔΙΚΑ** ⇒ **§14** |
| **2026-09-13** | ✅ **§14 — ΑΠΩΝ ≠ ΑΚΥΡΟΣ ΡΟΛΟΣ.** Νέο `lib/auth/identity-claims.ts` (ο ΕΝΑΣ ταξινομητής) · οι **δύο** παραγωγοί ταυτότητας τον καλούν · `PersonalIdentityContext.globalRole: GlobalRole \| null` · `MembershipQuery` / `RedeemingIdentity` / `resolveWorkspaceFromPath` δέχονται `null` · Boy Scout: καρφωμένη λίστα ρόλων στο `admin/role-management/bootstrap`. Η απόφαση ζει στο **ADR-817 §11**. Άγκυρες και μεταλλάξεις: δες §14.5 |

---

## 14. 🔴 Η ΚΥΚΛΙΚΗ ΕΞΑΡΤΗΣΗ ΡΟΛΟΥ — το εύρημα της πρώτης ζωντανής δοκιμής (2026-09-13)

### 14.1 Τι μετρήθηκε

Ο προσκεκλημένος πάτησε «Συνδεθείτε για να απαντήσετε», συνδέθηκε με Google — και η σελίδα
`/invite/<token>` **ξαναεμφάνισε την ίδια οθόνη σύνδεσης**, επανειλημμένα.

### 14.2 Η αλυσίδα, επαληθευμένη στην πηγή

1. Η σύνδεση γράφει **μόνο ταυτότητα** (Φ1): `identity-record.ts` → `globalRole: null`, **κανένα claim**.
2. Ο σελιδο-φρουρός (`page-identity.ts`) και το σύνορο API (`auth-context.ts`) έκριναν
   `typeof globalRole !== 'string' || !isValidGlobalRole(...)` ⇒ **απόρριψη**.
3. Άρα η σελίδα έδειχνε «σύνδεση» (όχι κουμπιά) — και η εξαργύρωση (`withPersonalOrOrgAuth`) θα
   απαντούσε `401 missing_claims` *(συμπέρασμα από κώδικα, ανεκτέλεστο)*.
4. Τον ρόλο τον δίνει **η ίδια η αποδοχή** (`redeem/route.ts` → `setClaimsWithMirror`).

> **Για να αποδεχτείς πρόσκληση χρειαζόσουν ρόλο. Τον ρόλο τον έδινε η αποδοχή.**

🔴 **Και ήταν ευρύτερο από την πρόσκληση**: η ίδια πόρτα κλείδωνε **και τις 14** διαδρομές
προσωπικής εμβέλειας — μαζί την **ίδρυση δικού σου χώρου** (`POST /api/workspaces`). Ο πολίτης
(ADR-844) γλίτωνε μόνο επειδή το `citizen-identity.ts` είχε βρει τον ίδιο τοίχο και τον είχε
παρακάμψει **τοπικά**, δίνοντας `external_user`.

### 14.3 Γιατί καμία άγκυρα δεν το έπιασε

**Κάθε** fixture των σουιτών ταυτότητας είχε ρόλο (`company_admin` / `external_user`) — και το
`identity()` του `workspace-invitation.test.ts` επίσης. Η περίπτωση «**κανένας** ρόλος» δεν
δοκιμαζόταν πουθενά. Πράσινο = «κανείς δεν κοίταξε».

⚠️ **Και η σωστή σημασιολογία ήταν ΗΔΗ γραμμένη** σε τρία σημεία: `authority.ts` (*«απουσία ρόλου
… νόμιμη κατάσταση»*), `identity-provenance.ts`, `useCapability` Π3. Ο πελάτης έλεγε «νόμιμο», ο
server «άκυρο» — σκορπισμένη αυθεντία (ADR-749).

### 14.4 Η απόφαση — δρόμος Β (Giorgio, 2026-09-13)

| | Λύση | Κρίση |
|---|---|---|
| Α | Βασικός ρόλος σε **κάθε** σύνδεση | ❌ μοιράζει εξουσιοδότηση σε κάθε άγνωστο **και** αλλάζει τι επιτρέπουν τα `firestore.rules` |
| **Β** | **Χώρισε «απών» από «άκυρο» ρόλο** | ✅ — **ADR-817 §11** |
| Γ | Ξεχωριστό κριτήριο μόνο για την πρόσκληση | ❌ δεύτερη αυθεντία (ADR-749) |

Αλλάζει **ένα κελί** του πίνακα ρόλος × χώρος: **απών ρόλος χωρίς `companyId` ⇒ `personal` με
`globalRole: null`**. Ο άκυρος ρόλος απορρίπτεται όπως πριν, **πριν** τον χώρο (ADR-807 §3.4β)· ο
απών ρόλος **με** `companyId` απορρίπτεται (ασυνεπές claim). Οι **319** διαδρομές `withAuth`
απαντούν ό,τι και πριν. Τα `firestore.rules` **δεν αλλάζουν**: βλέπουν το ίδιο token.

🏆 Η πρακτική των μεγάλων, επαληθευμένη: **Auth0 Organizations** — ο ρόλος της πρόσκλησης είναι
**προαιρετικός**· **Figma** — η αποδοχή θέλει **λογαριασμό**, όχι δικαίωμα· **OWASP** —
αυθεντικοποίηση ≠ εξουσιοδότηση. Όλοι αφήνουν την απουσία **σιωπηλή**· εδώ είναι **ονομασμένη
κατάσταση** που ο μεταγλωττιστής δεν επιτρέπει να αγνοηθεί.

### 14.5 Άγκυρες

`lib/auth/__tests__/identity-claims.test.ts` *(Α αναγνώστης · Π πίνακας · Ι ισοδυναμία με τον κριτή
του πελάτη)* · `server/auth/__tests__/identity-role-absence.test.ts` *(ο **ίδιος** πίνακας στους **δύο**
παραγωγούς + η ακτίνα μένει κλειστή)* · `workspace-membership.test.ts` **Κ3β** *(`null` ⇒ καμία
παράκαμψη)* · `workspace-invitation.test.ts` **Μ1δ** *(αποδοχή χωρίς ρόλο)*.

📏 **Μετρημένο 2026-09-13**: **48** νέες άγκυρες (25 + 21 + Κ3β + Μ1δ)· **15/15 σουίτες, 260/260
tests** πράσινα — μαζί **όλες** οι υπάρχουσες σουίτες ταυτότητας αμετάβλητες
(`api-identity-personal-scope` · `page-identity-permissions` · `auth-context-permissions` ·
`workspace-{declaration,denial}-boundary` · `workspace-from-path` · `authority` · `pdp-equivalence` ·
`useCapability` · `landing` · `personal-scope-consumers`).

🔴 **ΜΕΤΑΛΛΑΞΕΙΣ — 6/6 ΚΟΚΚΙΝΕΣ, ΕΚΤΕΛΕΣΜΕΝΕΣ** (κάθε μία με επαναφορά στην ίδια κλήση):

| # | Μετάλλαξη | Αποτυχίες |
|---|---|---|
| Μ1 | απών = άκυρος *(το ελάττωμα που έκλεισε)* | 17 |
| Μ2 | μη-συμβολοσειρά = απουσία *(χαλάρωση ασφαλείας)* | 8 |
| Μ3 | απών ρόλος + εταιρεία ⇒ `organization` | 3 |
| Μ4 | ο άκυρος ρόλος δεν κρίνεται πριν τον χώρο *(ADR-807 §3.4β)* | 8 |
| Μ5 | ο σελιδο-φρουρός γράφει **δικό** του κανόνα | 3 |
| Μ6 | ρόλος `null` ⇒ παράκαμψη μέλους | 1 |

### 14.6 🔶 Δηλωμένα ανοιχτά

- Η **αποδοχή** επαληθεύεται **ζωντανά** μόνο μετά το push· ως τότε μένει αδοκίμαστη στην παραγωγή.
- Η ανανέωση του `__session` μετά την αποδοχή βασίζεται στο `useClaimsRefresh`
  (`setClaimsWithMirror` σφραγίζει `claimsUpdatedAt` · ο κανόνας `users/{userId}` επιτρέπει ανάγνωση
  του **δικού** σου εγγράφου) — επαληθευμένο **στην πηγή**, όχι εκτελεσμένο.
- Δύο λεξιλόγια ρόλων (`PREDEFINED_ROLES` στον κριτή, `GLOBAL_ROLES` στον server) — **εκτός εμβέλειας**.
