# ADR-780 — Η κλίμακα z-index: **μία** αυθεντία στρώσης, και μια πύλη που το ρωτά

**Κατάσταση**: Accepted · **Ημερομηνία**: 2026-08-08 · **CHECK**: 3.50
**Σχετικά**: ADR-749 (μία μηχανή) · ADR-770 (οικογένεια πυλών χρώματος) · ADR-774 (CHECK 3.43,
ύπαρξη custom property) · ADR-757 (ιεράρχηση πυλών CI) · ADR-739 §64/§65 (η αφορμή)

---

## 1. Η ερώτηση

> **«Κάθε επιφάνεια που δηλώνει *καθολική* στρώση, τη ζητά από τη ΜΙΑ κλίμακα;»**
> — και, το ίδιο σημαντικό: **«το token που ζητά ΥΠΑΡΧΕΙ;»**

Δύο ερωτήματα, γιατί ένα `var(--z-index-tooltop)` (τυπογραφικό) **δεν** είναι λάθος τιμή:
είναι *invalid at computed-value time*, άρα `z-index: auto` — **καμία** στρώση. Ένα εργαλείο
που ρωτά μόνο «μοιάζει με token;» απαντά πράσινο πάνω σε σπασμένη αναφορά.

---

## 2. Η αφορμή, και γιατί το πρώτο handoff ήταν λάθος σε τρία σημεία

Το ADR-739 §64 έλυσε τον **κύκλο ζωής** δύο επιφανειών (η εφήμερη υποχωρεί στη μόνιμη), **όχι
τη στρώση**. Άφησε γραμμένο ότι υπάρχουν «9 ωμές δηλώσεις σε ζώνη 9000-10000» και ότι το SSoT
«σταματά στο `tooltip: 1800`, άρα η ζώνη από πάνω είναι άγραφη».

Η μέτρηση ανέτρεψε και τα τρία:

### 2.1 Δεν ήταν «μία κλίμακα + άγραφη ζώνη» — ήταν **ΠΕΝΤΕ ΛΕΞΙΛΟΓΙΑ**

| # | Πηγή | `modal` | `tooltip` | Κατάσταση |
|---|---|---|---|---|
| 1 | `design-tokens.json` → `--z-index-*` | 1400 | 1800 | ✅ αυθεντία |
| 2 | `design-tokens/modules/layout.ts` | 1400 | 1800 | ⚠️ **χειρόγραφος καθρέφτης** («Synced with…»), **καμία πύλη** |
| 3 | `layout-utilities-constants.ts` `zIndex` | **1000** | **2000** | 🔴 διαφωνεί · 0 καταναλωτές |
| 4 | `dxf-viewer/theme/tokens.color.css` `--cp-z-*` | **9999** | **10000** | 🔴 διαφωνεί · αρχείο ποτέ δεν φορτώνεται |
| 5 | `ribbon-tokens.css` `--ribbon-z-context-menu` | — | — | 1100, **ζωντανό**, 3 χρήσεις |

🔑 Είναι **χειρότερο** από το σχήμα του ADR-749 («δύο αλήθειες, η μία ανώνυμη»): εδώ και οι δύο
έχουν **όνομα** και **διαφωνούν στο ίδιο όνομα ρόλου**. Το (2) δεν το επέβαλλε **καμία** πύλη —
ήταν ανάθεση σε άνθρωπο, ακριβώς όπως οι δύο λίστες namespace του CHECK 3.34 που είχαν
**αποκλίνει κατά 63**.

### 2.2 Δεν ήταν «9 ωμές δηλώσεις» — ήταν **47**, σε **38 αρχεία**, σε **τρεις διαλέκτους**

### 2.3 🔑 ΟΙ ΤΡΕΙΣ ΔΙΑΛΕΚΤΟΙ ΕΙΝΑΙ **ΜΙΑ** ΕΠΙΦΑΝΕΙΑ — ΜΕΤΡΗΜΕΝΟ, ΟΧΙ ΥΠΟΤΕΘΕΙΜΕΝΟ

Ζωντανά, στο **ίδιο** μεταγλωττισμένο stylesheet, με **ίδια τιμή**, δίπλα-δίπλα:

```
.z-\[9999\]                                   ← Tailwind arbitrary, από .tsx
.DxfContextMenu-module__0zPq2q__menuContent   ← CSS module, από .css
```

Ο διαχωρισμός «CSS έναντι Tailwind» υπάρχει **μόνο στην πηγή**· ο browser βλέπει έναν κανόνα.
Πύλη που διαβάζει μόνο `.css` είναι **δομικά τυφλή** στο μισό πρόβλημα — και το χειρότερο
(`z-[99999]`) ζει ακριβώς στο τυφλό μισό. **Αυτό έκλεισε το ερώτημα εμβέλειας με μέτρηση, όχι
με προτίμηση.**

---

## 3. 🏆 Πού ξεπερνάμε τα εργαλεία των μεγάλων

Το κατώφλι των **1000** δεν το πιστεύουμε επειδή το μετρήσαμε καθαρό· τρεις κλίμακες της
βιομηχανίας χωρίζουν στο **ίδιο** σημείο:

| Σύστημα | Τοπική στοίβαξη | Καθολικό στρώμα | Κορυφή |
|---|---|---|---|
| Microsoft Atlas | `active 1 · hover 2 · focus 3 · multi 4` | `dropdown 1000` | **1070** |
| Salt (JPMorgan) | `default 1` | `appHeader 1000` | **1500** |
| Atlaskit | `card 100 · navigation 200` | — | **800** |
| Bootstrap | — | `dropdown 1000` | **1090** |
| **ΝΕΣΤΩΡ** | `base 0 · docked 10` | `dropdown 1000` | **1800 → 10000** |

**Καμία κορυφή δεν ξεπερνά το ~1800.** Το 9000-10000 είναι το «z-index arms race» που όλοι
ονομάζουν ρητά αντι-πρότυπο.

Τα εργαλεία τους **υπάρχουν και όλα αστοχούν εδώ** — `stylelint-no-z-index` ·
`stylelint-z-index-value-constraint` · `stylelint-scales` · `z-index-token-enforcer`:

1. κρίνουν **πρόθεμα** (`--z-`), όχι **ύπαρξη** ⇒ `var(--z-index-tooltop)` περνά πράσινο και
   βάφει `auto`. Το repo έχει **210** αδέσποτα `var()` (CHECK 3.43), άρα έλεγχος προθέματος =
   πράσινο πάνω σε σπασμένη αναφορά·
2. **κανένα** δεν βλέπει `z-[9999]` του Tailwind (ανοιχτό `eslint-plugin-tailwindcss` #290)·
3. **κανένα** δεν ανιχνεύει **παράλληλο λεξιλόγιο** — που ήταν εδώ η **ρίζα**.

⚠️ **ΜΗΝ ΠΡΟΣΘΕΣΕΙΣ stylelint.** Το CHECK 3.43 το απέρριψε γραπτώς και ο λόγος ισχύει ακέραιος:
ο κανόνας `no-unknown-custom-properties` τεκμηριώνει **κατά λέξη** ότι ένα fallback κάνει τη
χρήση αποδεκτή — θα έλεγε «καθαρό» ακριβώς εκεί που ρωτάμε. Δεύτερη μηχανή lint που απαντά
«καθαρό» είναι το σχήμα ADR-749.

---

## 4. Η απόφαση

### 4.1 Μία αυθεντία

- `design-tokens.json ▸ zIndex` **+5 ρόλοι**, με τιμές **ταυτόσημες** με ό,τι έβαφε ο κώδικας:
  `viewerModal 9000` · `viewerModalNested 9001` · `viewerPalette 9900` · `viewerMenu 9999` ·
  `viewerTransient 10000`.
- `scripts/build-design-tokens.js` **+`generateZIndexScale()`** ⇒ παράγει **αριθμητικό**
  `zIndexScale` + τύπο `ZIndexRole` στο `design-tokens/generated/tokens.ts`.
- `design-tokens/modules/layout.ts` — ο **χειρόγραφος καθρέφτης ΕΣΒΗΣΕ**· κάνει πλέον
  `...zIndexScale`. Τα `hide`/`auto` μένουν (είναι λέξεις-κλειδιά, όχι στρώσεις).

### 4.2 Εξαλείφθηκαν **δύο** παράλληλα λεξιλόγια

- `--ribbon-z-context-menu: 1100` → **διαγράφηκε**· 3 χρήσεις → `var(--z-index-sticky)`
  (**1100 = sticky ακριβώς** ⇒ ταυτόσημη υπολογισμένη τιμή).
- `--cp-z-*` (5 ορισμοί) → **διαγράφηκαν**· 3 χρήσεις → `popover`/`modal`/`overlay`.
  Επαληθεύτηκε **τρεις** φορές: κανένα import · μηδέν καταναλωτές κλάσεων · **ζωντανά**
  `getPropertyValue('--cp-z-*')` = **ΚΕΝΟ**.

### 4.3 Η πύλη — **CHECK 3.50**

```
scripts/lib/zindex/scale.js            αυθεντία + αυτοέλεγχος διάταξης + περιορισμένοι ρόλοι
scripts/lib/zindex/sites.js            σαρωτής 3 διαλέκτων + κλειστή λογιστική
scripts/lib/zindex/foreign.js          ΣΥΝΟΡΟ ΤΡΙΤΩΝ — κρίση δηλώσεων            (Φάση Β)
scripts/lib/zindex/foreign-census.js   ΣΥΝΟΡΟ ΤΡΙΤΩΝ — απογραφή node_modules     (Φάση Β)
scripts/check-zindex-scale.js          CLI (runSetRatchetCli) + σκανδάλη
scripts/__tests__/check-zindex-scale.test.js   61 tests
.zindex-scale-baseline.json            30 ταυτότητες / 23 ρόλοι
.zindex-foreign.json                   το μητρώο του συνόρου (ΧΩΡΙΣ baseline)
src/app/foreign-boundary.css           το ίδιο το σύνορο                        (Φάση Β)
```

**9 ρητές καταστάσεις, κλειστή λογιστική fail-closed** (άγνωστη ⇒ `throw` με όνομα) —
τα πλήθη **μετά** τη Φάση Β:

| Κατάσταση | Πλήθος | Μηχανισμός |
|---|---|---|
| `scale-token` | 37 | ✅ ζητά ρόλο |
| `scale-reference` | 152 | ✅ έκφραση TS — **δηλωμένο όριο**: δεν αποτιμάται |
| `runtime-property` | 1 | ✅ ορίζεται σε χρόνο εκτέλεσης / namespace βιβλιοθήκης |
| `keyword` | 0 | ✅ `auto`/`inherit`/… — το αντίθετο στρώσης |
| `local-stacking` | 58 | ✅ < 1000 ⇒ μέσα σε component |
| `raw-literal` | **32** | 🔴 **RATCHET** *(ήταν 45 πριν τη Φάση Β)* |
| `unknown-token` | 0 | ⛔ **ZERO-TOL** — δεν μπαίνει **ΠΟΤΕ** σε baseline |
| `parallel-scale` | 0 | ⛔ **ZERO-TOL** — δεν μπαίνει **ΠΟΤΕ** σε baseline |
| `restricted-role-misuse` | 0 | ⛔ **ZERO-TOL** — ρόλος **μη-προϊοντικός** ζητήθηκε εκτός περιοχής |
| **ΣΥΝΟΛΟ** | **280** | |

⚠️ Το `restricted-role-misuse` είναι **ΕΠΑΝΑΤΑΞΙΝΟΜΗΣΗ** ενός `scale-token`, όχι προσθήκη:
μία χρήση δεν γίνεται ξαφνικά δύο σημεία, και η λογιστική δεν μετακινείται (**280 = 280**
πριν και μετά). Υπάρχει επειδή η κλίμακα απέκτησε σκαλιά που **δεν είναι στρώση προϊόντος**
(`devtoolsGuard`·`debugOverlay`) και είναι, εξ ορισμού, τα ψηλότερα: ένα `restrictedTo` που
δεν το επιβάλλει κανείς θα ήταν **σχόλιο** (μάθημα CHECK 3.36). Άγκυρες `Μ13`+`Μ13β`+`Π1γ`.

Δύο πρόσθετα συμβόλαια:

- **`declarations` = οι ΡΟΛΟΙ με τις τιμές τους** ⇒ αλλαγή τιμής ρόλου **μπλοκάρει**,
  γιατί μετακινεί σιωπηλά **κάθε** επιφάνεια που τον ζητά.
- **Αυτοέλεγχος**: η κλίμακα πρέπει να είναι **γνησίως αύξουσα στη σειρά δήλωσης του JSON**.
  Η σειρά των κλειδιών **είναι** το ανθρώπινο νόημα («ποιο κάθεται πάνω από ποιο») και κανένας
  μεταγλωττιστής δεν έχει γνώμη για τη σειρά κλειδιών ενός JSON. Χωρίς αυτό, η κλίμακα θα
  μπορούσε να γίνει η επόμενη «δεύτερη αλήθεια» **μέσα στον εαυτό της**. Γνησίως, όχι
  μη-φθίνουσα: δύο ρόλοι με τον ίδιο αριθμό είναι δύο ονόματα για ένα σκαλί, δηλαδή η σειρά
  τους αποφασίζεται από τη σειρά DOM — ακριβώς η κατάσταση που η κλίμακα υπάρχει για να μην
  αφήνει στην τύχη.

---

## 5. 🔴 Τι βρήκε η **ίδια η δουλειά** — τρία ψευδή πράσινα

### 5.1 Η πύλη ήταν πράσινη πάνω στο εύρημα που τη γέννησε (`!important`)

Ο ταξινομητής έκανε `Number(value)`. Το `Number('2147483647 !important')` είναι **NaN**, οπότε
έπεφτε στον κλάδο «έκφραση TS» και έβαφε **`scale-reference`** — δηλαδή **✅** — **εννέα**
δηλώσεις. Ανάμεσά τους **και οι έξι** `z-index: 2147483647 !important` του `globals.css`, που
είναι ακριβώς οι έξι που το §6.1 ονομάζει «νικούν το token».

**Απογραφή πριν → μετά**: `raw-literal` **39 → 45** · `local-stacking` **55 → 58** ·
`scale-reference` **162 → 153** · **ΣΥΝΟΛΟ 280 → 280** (η λογιστική κράτησε).

⚠️ Το `!important` είναι **προτεραιότητα, όχι τιμή**: δεν αλλάζει τη στρώση, αλλάζει ποιος
κανόνας κερδίζει. Άγκυρες: `Κ3` (μονάδα) + `Κ3β` (και οι έξι, στο **πραγματικό** `globals.css`).

### 5.2 Το πρώτο test μετάλλαξε **σχόλιο**, και η απόδειξη ήταν ψεύτικη

Η πρώτη γραφή του `Μ4` έκανε `replace('var(--z-index-sticky)', …)`. Η **πρώτη** εμφάνιση στο
πραγματικό `ribbon-tokens.css` ζει στη **γραμμή 30**, μέσα στο σχόλιο που τεκμηριώνει τη
διαγραφή του `--ribbon-z-context-menu`. Ο σαρωτής αφαιρεί σχόλια ⇒ η «μετάλλαξη» δεν άλλαξε
τίποτα. Το έπιασε **μόνο** ο φρουρός «η μετάλλαξη ΔΕΝ άλλαξε τίποτα» του μίνι-repo (μάθημα
CHECK 3.44 / Μ11). Η αποτυχία γέννησε **νέα άγκυρα** (`Κ7β`): ένα σχόλιο που τεκμηριώνει παλιό
λεξιλόγιο **δεν** πρέπει να μετριέται ως ζωντανό λεξιλόγιο, αλλιώς κάθε ADR που περιγράφει τη
βλάβη θα γινόταν το ίδιο βλάβη.

### 5.3 Το ίδιο μου το όργανο παρήγαγε ψευδές μηδέν (ζωντανή μέτρηση)

Ο πρώτος σαρωτής CSSOM επέστρεψε **«0 κανόνες z-index»**. Αιτία: στο σύγχρονο Chrome **κάθε**
`CSSStyleRule` έχει `cssRules` (για CSS nesting), άρα το `if (r.cssRules)` το θεωρούσε **ομάδα**
και το προσπερνούσε. Το έπιασε **μόνο** η διασταύρωση με το πλήθος stylesheets (2841 κανόνες).
**Κρίνε τον κανόνα ΠΡΩΤΑ, μετά κατέβα σε παιδιά.**

---

## 5bis. ΦΑΣΗ Β — το δάμασμα των τρίτων **και** η κορυφή της κλίμακας (2026-08-09)

> **ΕΝΑ ερώτημα, όχι δύο**: «ποιος κάθεται στην κορυφή, και επιτρέπεται σε ξένο κώδικα να
> τον ξεπεράσει;». Η σύζευξη είναι αιτιακή: το `--z-index-critical` ήταν `2147483647` για
> **έναν** λόγο — έπρεπε να κάθεται πάνω από το `sonner` (**999999999**). Δηλαδή **η κορυφή
> της κλίμακάς μας οριζόταν από ξένο πακέτο.** Γι' αυτό ο τρίτος δαμάζεται **πρώτος**.

### 5bis.1 🔴 Το handoff ήταν λάθος σε **τρία** σημεία — και τα τρία τα ανέτρεψε η μέτρηση

| Το handoff έλεγε | Η μέτρηση (2026-08-09) |
|---|---|
| «οι τρίτοι είναι **δύο**: sonner + maplibre» | **11 πακέτα** ≥1000 σε 116 άμεσα deps. Ζωντανά και άγνωστα: **@fullcalendar/core + timegrid 9999** (ισοβαθμούν με `viewerMenu`), **react-modern-gantt 1000-1002**, **@react-aria/overlays 100000**, **pdfjs-dist 100000**, **three 10000**, **docx-preview 1000**, **jspdf 1000** |
| «οι έξι ωμές `2147483647` του `globals.css` είναι όλες άμυνα του dev overlay — άρα οι μόνοι υποψήφιοι καταναλωτές της κορυφής» | Σωστό **για το `globals.css`**, αλλά υπάρχουν **άλλες εφτά δηλώσεις πάνω από 10000** που το handoff δεν ήξερε: `eyedropper-loupe` **2147483647** · `eyedropper` **2147483646** · `DebugOverlay` **2147483646 ×2** · `NotificationDrawer` **99999 + 99998** · `PromptDialog` **10001**. Δύο από αυτές **ισοβαθμούσαν με τον φρουρό** στο MAX_INT |
| «το `--z-index-critical` πρέπει να είναι πάνω από όλα» | 🔑 Το `nextjs-portal` **ΔΕΝ έχει δικό του z-index**. Το `next-devtools` δηλώνει `:host { --top-z-index: 2147483647 }` και το χρησιμοποιεί **ΜΕΣΑ στο shadow root** του, όπου ταξινομεί **μόνο τα δικά του παιδιά**. Ο host μένει `z-index: auto` ⇒ **οτιδήποτε δικό μας τον θάβει**. Άρα ο φρουρός χρειάζεται να ξεπερνά **ΜΟΝΟ τη δική μας οροφή** |

Το τρίτο εύρημα είναι η **απόδειξη** της σύζευξης: μόλις οι τρίτοι δαμαστούν, η οροφή
πέφτει στο 10000 και ο φρουρός γίνεται **λογικός αριθμός**.

### 5bis.2 🏆 Πού ξεπερνάμε την πρακτική των μεγάλων — **τεκμηριώνουν, δεν μετράνε**

| Ποιος | Τι κάνει | Γιατί δεν αρκεί |
|---|---|---|
| **Material UI** [↗](https://mui.com/material-ui/customization/z-index/) | `theme.zIndex` για override ανά component· η τεκμηρίωση αποθαρρύνει την αλλαγή μεμονωμένης τιμής («*if you change one, you likely need to change them all*») | Καμία εγγύηση ότι κάποιος θα θυμηθεί το **επόμενο** πακέτο |
| **«Systems for z-index»** (CSS-Tricks) [↗](https://css-tricks.com/systems-for-z-index/), το κανονικό κείμενο του χώρου | αν έχεις τρίτο που δεν αλλάζει, «**plug that into the map**» | **Χειρόγραφη καταχώριση** που κανείς δεν επαληθεύει |
| **`isolation: isolate`** [↗](https://modern-css.com/z-index-isolation/) · [↗](https://theosoti.com/short/isolation-isolate-property/) | προτείνεται ευρέως ως ο καθαρός φράχτης (χωρίς οπτική παρενέργεια) | **Οδηγία σε άρθρο**, όχι έλεγχος |
| **Lullabot ADR** (2025) [↗](https://architecture.lullabot.com/adr/20251210-create-deliberate-stacking-contexts/) | απορρίπτει **εντελώς** την καθολική κλίμακα: «*these values cannot work globally*» | Ισχύει για κλίμακα **χωρίς** απόδειξη ότι όλα κάθονται στο ROOT — εμείς **το μετρήσαμε** (§6.3) |

Και οι τρεις πρώτες έχουν το **ίδιο** ελάττωμα: είναι **ανάθεση σε άνθρωπο**. Στο ίδιο
αποθετήριο αυτό το σχήμα έχει ήδη αποτύχει μετρημένα — δύο λίστες namespace που είχαν
**αποκλίνει κατά 63** (CHECK 3.34), ένας συγκεντρωτής CI που άκουγε **18** ενώ το δέντρο
είχε **26** (CHECK 3.37).

**Η δική μας απάντηση**: το μητρώο `.zindex-foreign.json` συγκρίνεται με **απογραφή του
ίδιου του `node_modules`**, με αυθεντία το `package.json`. Καμία χειρόγραφη λίστα πακέτων.

### 5bis.3 Η δόγμα του συνόρου — **δύο μηχανισμοί, και η επιλογή δεν είναι γούστο**

- **ΠΕΡΙΟΡΙΣΜΟΣ** (`isolation: isolate` στη ρίζα) για ό,τι ζωγραφίζεται **στη θέση του**.
  Ανώτερος του δαμάσματος: **δεν χρειάζεται να ξέρουμε τι αριθμούς γράφει η βιβλιοθήκη**,
  ούτε σήμερα ούτε μετά από αναβάθμιση.
- **ΔΑΜΑΣΜΑ** (`z-index: var(--z-index-<ρόλος>) !important`) **μόνο** για ό,τι δραπετεύει
  στο ROOT — portal στο `body` (sonner) ή `position: fixed` πάνω στο **ίδιο του** το
  δοχείο (`maplibregl-pseudo-fullscreen`, που η κλάση του μπαίνει στο `_container`, άρα
  ένα stacking context **δεν περιορίζει τον εαυτό του**).

**11 πακέτα, 5 μη-μπλοκάρουσες καταστάσεις**: `clamped` 2 · `contained` 3 · `unreachable` 4
· `dev-only` 1 · `acknowledged` 1. **6 μπλοκάρουσες, όλες 0** σήμερα.

### 5bis.4 Η νέα κορυφή — **καμία τιμή MAX_INT πουθενά**

Ο ρόλος `critical` (**2147483647**) **διαγράφηκε**. Στη θέση του, **επτά** επιφάνειες που
έγραφαν ωμούς αριθμούς πάνω από 10000 πήραν ρόλο. Η απόδειξη «μηδενικής οπτικής αλλαγής»
**δεν** είναι «ίδιος αριθμός» — είναι **ίδια ΣΕΙΡΑ**:

| Ρόλος | Τιμή | Τι έβαφε πριν |
|---|---|---|
| `viewerTransient` | 10000 | 10000 *(αμετάβλητο)* |
| `viewerPrompt` | 10001 | `PromptDialog` **10001** *(ίδιος αριθμός, μόνο όνομα)* |
| `appDrawerScrim` | 10100 | `NotificationDrawer` backdrop **99998** |
| `appDrawer` | 10101 | `NotificationDrawer` panel **99999** |
| `eyedropperCapture` | 10200 | eyedropper overlay **2147483646** |
| `eyedropperLoupe` | 10201 | eyedropper loupe **2147483647** |
| `debugOverlay` | 10300 | `DebugOverlay` **2147483646** ⚠️ *ισοβαθμία* |
| `devtoolsGuard` | 10400 | οι 6 του `globals.css` **2147483647** ⚠️ *ισοβαθμία* |

⚠️ **ΔΥΟ ισοβαθμίες λύθηκαν σκόπιμα**, και αυτό **είναι** ο λόγος ύπαρξης της κλίμακας
(ο αυτοέλεγχος απαιτεί **γνησίως** αύξουσα): το `DebugOverlay` και το capture του
eyedropper μοιράζονταν το 2147483646· ο loupe και ο φρουρός μοιράζονταν το 2147483647 —
δηλαδή **ο loupe μπορούσε να σκεπάσει το overlay σφαλμάτων του Next.js**, με τη σειρά να
αποφασίζεται από το DOM. Το δεύτερο είναι **πραγματικό λανθάνον σφάλμα που διορθώθηκε**.

Δύο ρόλοι είναι **περιορισμένης χρήσης** (`restrictedTo` στο `design-tokens.json`):
`debugOverlay` → μόνο `src/subapps/dxf-viewer/debug/` · `devtoolsGuard` → μόνο
`src/app/globals.css`. Το `devtoolsGuard` **δεν είναι στρώση προϊόντος** και το λέει η
περιγραφή του.

### 5bis.5 🔴 Τι βρήκε η **ίδια η δουλειά** — τέσσερα ακόμη, όλα μετρημένα

1. **Ο πρώτος ανιχνευτής παράκαμψης ήταν θορυβώδης.** Μάζευε επιλογείς **και** από τα
   μέτρα `contain` και κατήγγειλε το υπαρκτό `.rmg-gantt-chart { z-index: 20 !important }`
   του `globals.css`. Ένα δικό μας `z-index` στη ρίζα **περιορισμένου** πακέτου είναι
   νόμιμο — αποφασίζει πού ζωγραφίζεται η **δική μας** επιφάνεια, και το `isolation` δεν
   το αφορά. Φρουρός που πυροδοτεί σε σωστό κώδικα είναι ο δρόμος προς το `SKIP_`.
   Άγκυρα `Σ7β`.
2. **Σύμβολο-απόδειξη που ταιριάζει κατά τύχη.** Το σκέτο `stats.module` ταιριάζει στο
   άσχετο `stats.modules` του ai-pipeline ⇒ ψευδές `foreign-reachable`. Το σύμβολο
   **πρέπει** να κουβαλά τη διαδρομή του πακέτου (`libs/stats.module`). Άγκυρα `Σ15`.
3. **Ο αγωγός CSS συγχωνεύει κανόνες** — ζωντανή παρατήρηση: τα τρία ξεχωριστά
   `isolation: isolate` σερβίρονται ως **ένας** κανόνας `.maplibregl-map, .fc,
   .rmg-gantt-chart`. Η πύλη διαβάζει την **πηγή**, αλλά ο επόμενος άνθρωπος θα γράψει τη
   λίστα με το χέρι· χωρίς `splitSelectorList` η σωστή γραφή θα γινόταν σιωπηλά
   `foreign-unverified`.
4. **Η μετάλλαξη μέσω regex έσπασε το JSON.** Το `why` του sonner περιέχει εισαγωγικά με
   διαφυγή, οπότε ένα `[^"]*` κόβει στη μέση: η «μετάλλαξη» θα απεδείκνυε ότι σκάει ο
   parser, όχι ότι η πύλη απαιτεί λόγο. Μεταλλάσσεται πλέον **μέσω JSON**. Άγκυρα `Σ8`.

### 5bis.6 Η ζωντανή απόδειξη (`http://localhost:3000`, 2026-08-09)

| Ερώτημα | Απάντηση |
|---|---|
| Λύνονται και οι 8 νέοι ρόλοι στο `:root`; | ✅ 10000·10001·10100·10101·10200·10201·10300·10400 |
| Υπάρχει ακόμη το `--z-index-critical`; | ✅ **κενό** (διαγράφηκε) |
| Δέχεται το CSSOM `var()` σε `zIndex` μέσω `element.style`; | ✅ **10201** — ο δρόμος του eyedropper· ήταν το μόνο που **δεν** μπορούσε να υποτεθεί |
| Παράγει το Tailwind το `z-[var(--z-index-app-drawer)]`; | ✅ και τα δύο, σε πραγματικό CSS |
| Είναι το δάμασμα του sonner θεσιοανεξάρτητο; | ✅ **ένας μόνο** κανόνας z-index για το sonner, χωρίς `data-position` |
| Πού κάθεται το `nextjs-portal`; | ✅ **10400**, με shadow root — και είναι **ο ψηλότερος του DOM** |

Το τελευταίο είναι το invariant: **καμία επιφάνεια προϊόντος δεν ξεπερνά τον φρουρό.**

### 5bis.7 Το δεύτερο `critical` — SSoT audit πριν τον κώδικα

Το `DxfZIndexSystem.styles.ts` δήλωνε `critical: globalZIndex.modal + 100` = **1500**.
Ίδιο όνομα ρόλου, **άλλος αριθμός** — ακριβώς το σχήμα του §2.1 — και το 1500 ήταν
επιπλέον **ταυτόσημο** με τον ρόλο `popover`, δηλαδή δύο ονόματα για ένα σκαλί.
Μετρήθηκε πριν τη διαγραφή: `criticalModal`, `CRITICAL_MODAL` και
`createModalZIndex('critical')` είχαν **ΜΗΔΕΝ** καταναλωτές. **Διαγράφηκε.**

Το ίδιο αρχείο είχε φτάσει τις **600** γραμμές (πάνω από το όριο N.7.1, μπλόκαρε το
commit) ⇒ χωρίστηκε κατά **ευθύνη**, όχι κατά μέγεθος: `DxfZIndexSystem.styles.ts` (292)
απαντά «ποιο κάθεται πάνω από ποιο», `DxfSurface.styles.ts` (317) απαντά «πώς μοιάζει η
επιφάνεια».

---

## 5ter. ΦΑΣΗ Γ — **η συμπίεση της ζώνης** (2026-08-09)

> **«Η ζώνη πάνω από το 1800 είναι ΔΥΟ παράλληλες σκάλες. Γίνονται μία;»**
> Απάντηση: ήταν **ΤΡΕΙΣ**, και η τρίτη ήταν αόρατη — γιατί ήταν **αριθμητική**.

**Αποτέλεσμα**: κορυφή **10400 → 1370** · ωμές δηλώσεις **32 → 0** · ρόλοι **23 → 40**
(η αύξηση **δεν** είναι χειροτέρευση — είναι **ορατότητα**, §5ter.1) · **68 tests**.

### 5ter.1 🔴 Η ΕΚΤΗ ΚΛΙΜΑΚΑ — αόρατη επειδή ήταν **αριθμητική**

Το `portal-overlay.ts` παρήγαγε **17 σκαλιά** με offsets πάνω σε ρόλους:

```
zIndex.modal   + 50/60/70/80/90    → 1450 1460 1470 1480 1490
zIndex.popover + 10/20/30/40/41/42/43/50 → 1510 … 1550
zIndex.banner  + 10                → 1210
```

Δηλαδή **περισσότερα σκαλιά από όλη την κλίμακα του Shoelace ή του Atlas**, στριμωγμένα
ανάμεσα στο 1450 και το 1550. Η πύλη τα έβλεπε ως `scale-reference` = **✅ πράσινα**: το
«έκφραση TS, δεν αποτιμάται» (§6.4) ήταν δηλωμένο όριο, και αποδείχθηκε ότι **είχε
περιεχόμενο**. Τα σχόλια `// 1450` δίπλα τους ήταν **χειρόγραφος καθρέφτης** — το ίδιο
σχήμα που η Φάση Α έσβησε από το `layout.ts`.

🔑 **ΓΙΑΤΙ ΕΠΡΕΠΕ ΝΑ ΦΥΓΕΙ ΠΡΙΝ ΤΗ ΣΥΜΠΙΕΣΗ — ΑΙΤΙΑ, ΟΧΙ ΠΡΟΤΙΜΗΣΗ.** Ένα `modal + 50`
**δεν** μετακινείται μαζί με τον `modal`. Με βήμα 10, το `modal(1040) + 50` γίνεται
**1090** και προσπερνά τα `popover`·`skipLink`·`toast`·`tooltip` που πριν ήταν **από
πάνω** του. **Η επαναρίθμηση θα είχε αντιστρέψει τέσσερα ζεύγη με την πύλη ΠΡΑΣΙΝΗ** —
το σχήμα «0 = κανείς δεν κοίταξε», μια στάθμη πιο μέσα.

⚠️ **12 από τα 17 ήταν ΝΕΚΡΑ** (μηδέν καταναλωτές) και διαγράφηκαν· τα **5** ζωντανά
έγιναν ρόλοι με **ταυτόσημες** τιμές. Και ένα έκτο, το `controls.zIndex() + 90` = **1600**,
ήταν **τυχαία ισοβαθμία** με τον ρόλο `skipLink` μέσω **διπλής** αριθμητικής
(`popover + 10 + 90`) — που κανένα από τα δύο σημεία δεν έβλεπε. Σε ισοβαθμία τη σειρά
την αποφασίζει το DOM· έγινε ρητός ρόλος `dynamicInput` (**ο χρήστης ΠΛΗΚΤΡΟΛΟΓΕΙ** σε
αυτό — αν κρυφτεί, γράφει στα τυφλά).

### 5ter.1β 🔴🔴 ΔΕΥΤΕΡΗ ΕΣΤΙΑ — και η αιτία ήταν **λάθος ΜΕΘΟΔΟΣ ΣΑΡΩΣΗΣ**

Μετά την πρώτη επαναρίθμηση, δεύτερος έλεγχος βρήκε **ακόμη** αριθμητική — σε **άλλο**
αρχείο (`DxfZIndexSystem.styles.ts`, `canvas-ui.ts`, `geo-canvas/config`). **Δύο από αυτές
είχαν ήδη σπάσει σιωπηλά:**

| Δήλωση | Πριν | Μετά τη συμπίεση | Αποτέλεσμα |
|---|---|---|---|
| `ui.sidebar = sticky + 10` | 1110 *(sticky 1100 → banner 1200)* | **1020** | **ταυτόσημο με `banner`** 🔴 |
| `modals.import = modal + 10` | 1410 *(modal 1400 → popover 1500)* | **1050** | **ταυτόσημο με `canvasSnap`** 🔴 |
| `testModal.content = modal + 1` | 1401 | 1041 | επέζησε **κατά τύχη** |
| `PANEL_* = dropdown + 1/2/3` | 1001-1003 | 1001-1003 | επέζησε **κατά τύχη** |

🔑 **Η ΑΙΤΙΑ ΤΟΥ ΔΕΥΤΕΡΟΥ ΓΥΡΟΥ ΕΙΝΑΙ ΤΟ ΜΑΘΗΜΑ**: η πρώτη σάρωση ακολούθησε **την έξοδο
της πύλης** (τα σημεία που εκείνη ταξινομεί ως `scale-reference`) αντί για **το μοτίβο**.
Αλλά η αριθμητική εδώ ζει σε **σταθερές** (`ui: { sidebar: sticky + 10 }`), όχι σε δηλώσεις
`zIndex:` — δηλαδή σε θέση που η πύλη **δεν αναφέρει καν**. *Ψάχνοντας με τα μάτια του
οργάνου, βλέπεις μόνο ό,τι το όργανο ήδη βλέπει.* Το εύρημα δεν ήταν τυχαίο: προέκυψε από
τη ρητή ερώτηση «**έμεινε καμία αριθμητική;**», διατυπωμένη ως grep πάνω στο **μοτίβο**.

⚠️ **ΤΟ ΙΔΙΟ ΤΟ ΑΡΧΕΙΟ ΕΙΧΕ ΠΕΝΤΕ ΕΡΓΟΣΤΑΣΙΑ**: `createModalZIndex(τύπος, stackOffset)` ·
`createOverlayZIndex(τύπος, priority)` · `createCanvasZIndex(τύπος, layerOffset)` ·
`getMemoizedZIndex(…, offset)` — όλα επέστρεφαν `ρόλος + αριθμός`, **όλα με μηδέν
καταναλωτές**. Νεκρά σήμερα· ο πρώτος που τα καλούσε αύριο θα ξαναγεννούσε την έκτη
κλίμακα **με API**. Διαγράφηκαν (295 → **143** γραμμές), μαζί με το
`validateDxfZIndexHierarchy` που έλεγχε διπλότυπα με `console.warn` — **ακριβώς** ό,τι
κάνει ο `findScaleDisorder` της πύλης, αλλά **χωρίς να το τρέχει κανείς** (CHECK 3.36:
*ένας έλεγχος που δεν εκτελείται είναι σχόλιο*).

**+6 ρόλοι** με τιμές που διατηρούν την **προ-συμπίεσης** σειρά: `geoPanelProperties` ·
`geoPanelLayers` · `geoPanelTools` · `workspaceSidePanel` · `modalContent` ·
`viewerImportModal`. **3 νεκρά σκαλιά διαγράφηκαν** (`modals.settings` · `modals.help` ·
`ui.notifications`).

### 5ter.2 🏆 Η ΑΠΟΔΕΙΞΗ — **order-isomorphism**, όχι screenshot

**ΤΟ ΘΕΩΡΗΜΑ**: αν η απεικόνιση «παλιά τιμή → νέα τιμή» είναι **γνησίως αύξουσα**, τότε
για **κάθε** ζεύγος επιφανειών το πρόσημο του `z(a) − z(b)` μένει ίδιο ⇒ η σχετική διάταξη
διατηρείται **παντού**, και στα ζεύγη που κανείς δεν σκέφτηκε να κοιτάξει.

⇒ **Η μέτρηση συν-παρουσίας που ζητούσε το handoff §4.3 δεν χρειάστηκε.** Είναι **δείγμα**
(ποια ζεύγη είδαμε)· η μονοτονία είναι **απόδειξη** (κάθε δυνατό ζεύγος). Θα χρειαζόταν
**μόνο** για *συγχωνεύσεις* — που δεν έγιναν, ακριβώς για να μη χαθεί η απόδειξη.

**Το όργανο**: το `declarations` της baseline **έπαψε** να είναι `ρόλος=τιμή` και έγινε
**η ΔΙΑΤΑΞΗ** (`scripts/lib/zindex/scale.js ▸ orderIdentities`):

```
dropdown|πάνω-από:docked|ζώνη:global
```

| Πράξη | Η πύλη |
|---|---|
| **Μονότονη επαναρίθμηση** (10400 → 1370) | **πράσινη, χωρίς reseed** ⇒ *αυτό ΕΙΝΑΙ η απόδειξη* |
| Αναδιάταξη | ⛔ μπλοκ, και **ονομάζει** ποιο ζεύγος αντιστράφηκε |
| Παρεμβολή ρόλου | ⛔ μπλοκ — **ακριβώς μία** εγγραφή αλλάζει, όχι N |
| Μετακίνηση κάτω από το κατώφλι | ⛔ μπλοκ, **ακόμα και με ίδια σειρά** |

⚠️ **Ο παλιός φρουρός ήταν λάθος ΕΡΩΤΗΜΑ, όχι λάθος υλοποίηση.** Το `ρόλος=τιμή` ζητούσε
**ανθρώπινη επαλήθευση** για κάθε αριθμό («επαλήθευσε ζωντανά») — ανάθεση σε άνθρωπο, το
σχήμα που έχει αποτύχει μετρημένα σε **3.34** (63 απόκλιση) · **3.37** (18 vs 26) ·
**3.49** (60 φορές). Ο νέος είναι **ακριβώς όσο αυστηρός χρειάζεται**: μπλοκάρει ό,τι
μπορεί να αλλάξει την οθόνη, και **μόνο** αυτό.

⚠️ **Η ΖΩΝΗ ΕΙΝΑΙ ΜΕΡΟΣ ΤΗΣ ΤΑΥΤΟΤΗΤΑΣ.** Η διάταξη μόνη της δεν βλέπει το κατώφλι: ένα
`dropdown` που θα κατέβαινε 1000 → 999 κρατά τη σειρά του, αλλά κάθε ωμή δήλωση της
περιοχής του αλλάζει σιωπηλά κατάσταση σε `local-stacking`, δηλαδή **η πύλη σταματά να
ρωτά**. Χειρότερα: το `MODAL_Z_INDEX_THRESHOLD` (=**50**) του `modal-presence-detect.ts`
κρίνει «είναι modal;» **με αριθμό** — τέταρτος καταναλωτής τιμών, που κανένα handoff δεν
ανέφερε. Άγκυρα: `Δ4`.

⚠️ **Ο γείτονας, όχι απόλυτο rank**: ο συγκριτής του ratchet είναι **συνόλου**, άρα η θέση
ζει μέσα στο string. Με rank, μια παρεμβολή στη μέση θα ανέφερε **N** αλλαγές για **μία**
πράξη — θόρυβος που σπρώχνει σε τυφλό reseed. Άγκυρα: `Δ3`.

### 5ter.3 🏆 Πού ξεπερνάμε τους μεγάλους — **αποδεικνύουν με ΔΕΙΓΜΑ**

| Πηγή | Πώς αποδεικνύει αλλαγή κλίμακας |
|---|---|
| **Atlassian** | codemod που «*only provides token suggestions, **manual review is required***» + visual regression ως «*ideal companion*» ⇒ **δείγμα + ανάθεση σε άνθρωπο** |
| **USWDS** | κλίμακα ρόλων… με κορυφή `top: **99999**` — το arms race **μέσα** στο token σύστημα |
| **Shoelace** | 5 σκαλιά, κορυφή 1000 — καμία τεκμηρίωση μετανάστευσης |
| **OutSystems** | 5 global layers, βήμα 5, κορυφή **20** (!) — καμία τεκμηρίωση μετανάστευσης |

Ένα screenshot **δεν αποδεικνύει ποτέ** τα ζεύγη που δεν βγήκαν στο κάδρο. Εδώ η **ίδια η
πύλη** είναι το όργανο της απόδειξης — και **κανένα** από τα εργαλεία που ερευνήθηκαν
(§11.4) δεν κρίνει **σειρά**.

### 5ter.4 Η νέα κλίμακα — 40 ρόλοι, κορυφή **1370**

Ομοιόμορφο βήμα **10**. 🔑 Η **δομή ζει στα ονόματα και στα σχόλια, όχι στους αριθμούς** —
οι αριθμοί δεν πρέπει να κουβαλούν νόημα, γιατί αυτό ακριβώς γέννησε τα πέντε λεξιλόγια.

```
   0 base · 10 docked                                    (τοπικά, αμετάβλητα)
1000 dropdown → 1030 geoPanelTools                       (φλοιός + πάνελ geo-canvas)
1040 sticky · 1050 workspaceSidePanel · 1060 banner      (ήταν 1100 · 1110 · 1200)
1070 overlay → 1100 viewerImportModal                    (διάλογοι + η κάρτα τους)
1110 canvasSnap → 1160 debugLayoutMapControls            (καμβάς + debug map, ήταν 1480-1543)
1170 skipLink · 1180 dynamicInput · 1190 toast · 1200 tooltip
1210 elevatedMenu → 1250 pickerOverlay                   (ανυψωμένα — ήταν 1900-2600)
1260 viewerModal → 1310 viewerPrompt                     (χώρος εργασίας — ήταν 9000-10001)
1320 appDrawerScrim · 1330 appDrawer                     (συρτάρι φλοιού)
1340 eyedropperCapture → 1370 devtoolsGuard              (εργαλεία — ήταν 2147483646+)
```

⚠️ **ΟΙ 40 ΡΟΛΟΙ ΔΕΝ ΕΙΝΑΙ ΧΕΙΡΟΤΕΡΕΥΣΗ ΑΠΟ ΤΟΥΣ 23 — ΕΙΝΑΙ ΟΡΑΤΟΤΗΤΑ.** Κάθε νέος ρόλος
αντιστοιχεί σε σκαλί που **ήδη ζωγραφιζόταν**· απλώς ζούσε ως ωμός αριθμός ή ως αριθμητική.
Η κλίμακα δεν μεγάλωσε — **η απογραφή** μεγάλωσε (ίδιο πρότυπο με το `unanalyzable`
του CHECK 3.35). Η συρρίκνωση σε λιγότερους ρόλους είναι **συγχώνευση**, δηλαδή **άλλο
ερώτημα με άλλη απόδειξη** (απαιτεί μη-συνύπαρξη ανά ζεύγος — δες §6.7).

**Εντός** της πρακτικής των μεγάλων: MUI 1500 · Salt 1500 · Bootstrap 1090 · Atlas 1070.
Άγκυρα `Π1δ` κλειδώνει κορυφή **≤1500** — και το όριο **δεν είναι αισθητικό**: πάνω από
αυτό, ο επόμενος που θα γράψει «λίγο πιο πάνω» ξαναρχίζει το arms race.

### 5ter.5 🔴 Τι βρήκε η **ίδια η δουλειά**

1. **Τα ίδια τα tests καρφώνονταν σε ΤΙΜΕΣ** (`Π1`, `Π1β`, `Μ11`, `Κ11`). Έσπασαν στην
   επαναρίθμηση — **όχι** επειδή χάλασε η πύλη, αλλά επειδή κλείδωναν αριθμούς που η ίδια
   η θεραπεία αλλάζει σκόπιμα. *Ένα test που εμποδίζει τη θεραπεία είναι λάθος test.*
   Ξαναγράφτηκαν ως άγκυρες **σειράς**· οι μεταλλάξεις στοχεύουν πλέον τον **ρόλο**
   (`/("viewerPalette": \{ "value": ")\d+/`), όχι την τιμή.
2. **Ο φρουρός «η μετάλλαξη ΔΕΝ άλλαξε τίποτα» τα έπιασε και τα τέσσερα** — χωρίς αυτόν
   θα είχαν περάσει **πράσινα** (μάθημα CHECK 3.44 / `Μ11`, εδώ αποδεδειγμένα χρήσιμος).
3. **Ψευδές σχόλιο κατά παράγοντα 19**: `OverlayToolbar.tsx:163` έγραφε
   «*Centralized z-index (80)*» για τιμή **1510**.
4. **Δύο ακόμη νεκρά λεξιλόγια** διαγράφηκαν: `layoutUtilities.zIndex` (§6.4) και το
   `RulerDebugSettings.zIndex` — πεδίο στρώσης **που κανένας ζωγράφος δεν διάβαζε ποτέ**.

### 5ter.6 Γιατί η Φάση Β ήταν **προϋπόθεση**, όχι απλώς προηγούμενη

Η μονοτονία εγγυάται τη σειρά **των δικών μας** τιμών. Τα **ξένα** πακέτα δεν
μετακινούνται μαζί μας: ένα τρίτο με `z-index: 1050` ήταν πριν κάτω από το `sticky` (1100)
και θα ήταν τώρα **πάνω** του (1010). Η επαναρίθμηση είναι ασφαλής **ακριβώς επειδή** το
σύνορο της Φάσης Β ταξινομεί **κάθε** ξένο ≥1000 σε ρητή κατάσταση: τα 2 `clamped` ζητούν
**ρόλο** (άρα ακολουθούν), τα 3 `contained` ζουν σε δικό μας stacking context (άρα δεν
συγκρίνονται), τα 5 `unreachable` δεν εκτελούνται, το 1 `dev-only` το καλύπτει ο
`devtoolsGuard`. **Η εγγύηση είναι δομική, όχι τυχαία** — και το κατώφλι 1000 είναι αυτό
που την κάνει εξαντλητική.

---

## 6. 🔶 Δηλωμένα όρια και ανοιχτά

### 6.1 ~~Το `--z-index-critical` **ΔΕΝ είναι το μέγιστο**~~ — ✅ **ΕΚΛΕΙΣΕ** (Φάση Β)

Ο ρόλος **διαγράφηκε**· δες §5bis.4. Το εύρημα που τον κατήγγειλε ήταν σωστό και η αιτία
του ήταν βαθύτερη: ο αγωγός CSS στρογγυλοποιούσε το `2147483647` σε **`2147480000`**
(6 σημαντικά ψηφία), άρα το token ήταν **μικρότερο** από τις ωμές δηλώσεις που υποτίθεται
ότι όριζε. Ένας αριθμός τόσο μεγάλος που **δεν επιβιώνει του ίδιου του αγωγού** είναι από
μόνος του απόδειξη ότι δεν ανήκει σε κλίμακα σχεδιαστικού συστήματος.

### 6.1β Το `--z-index-critical` **ΔΕΝ είναι το μέγιστο** — το ιστορικό εύρημα

Ο server στέλνει **`2147480000`** ενώ η πηγή λέει **`2147483647`** (6 σημαντικά ψηφία ⇒ ο
αγωγός CSS το στρογγυλοποιεί). Επαληθευμένο **τρεις** φορές: `fetch` του σερβιρισμένου CSS ·
`getPropertyValue` · **probe** που εφαρμόζει `z-index: var(--z-index-critical)`. Το **ωμό**
`2147483647` λύνεται **ακέραιο** ⇒ και οι **έξι** ωμές δηλώσεις του `globals.css` **νικούν το
token** που λέει *«above EVERYTHING»*, όπως και το `DebugOverlay` (2147483646).
**Ανοιχτό**: ή τιμή που επιβιώνει του αγωγού, ή κατάργηση του ρόλου (**καμία** κλίμακα των
μεγάλων δεν έχει «MAX_INT»), ή δάμασμα των έξι. **Απόφαση Giorgio.**

### 6.2 ~~Οι κορυφές της ζώνης **δεν είναι δικές μας**~~ — ✅ **ΕΚΛΕΙΣΕ** (Φάση Β)

`sonner` = **999999999** · `maplibre-gl` = **99999** — και **άλλα εννέα** που το handoff δεν
ήξερε. Όλα δαμασμένα ή περιορισμένα στο σύνορο, με **απογραφή** που μπλοκάρει τη σιωπηλή
απόκλιση· δες §5bis. Η προηγούμενη προστασία (ένας κανόνας για `[data-position="top-right"]`)
ήταν **θεσιακή, άρα τυχαία**: ένα `bottom-right` επανέφερε σιωπηλά το 999999999.

🔶 **Ανοιχτό, ρητά**: η απογραφή ανοίγει τα **άμεσα** runtime dependencies. Ένα **έμμεσο**
πακέτο που δεν το φέρνει κανένα άμεσο δεν σαρώνεται (πλήρης σάρωση μετρήθηκε απαγορευτική).
Γραμμένο ώστε το «0» να μη διαβαστεί ως «καθαρό».

### 6.3 ~~Συμπίεση σε λίγους ρόλους~~ — ✅ **ΕΚΛΕΙΣΕ** (Φάση Γ, δες §5ter)

Κορυφή **10400 → 1370**, ωμές δηλώσεις **32 → 0**, ρόλοι **23 → 40**. Η απόδειξη δεν είναι παρατήρηση: είναι
**order-isomorphism**, και το όργανό της είναι η **ίδια η πύλη** (§5ter.2).

### 6.4 `scale-reference` = **δεν αποτιμάται** — 🔴 ΤΟ ΟΡΙΟ ΕΙΧΕ ΠΕΡΙΕΧΟΜΕΝΟ

Η πύλη **δεν εκτελεί TS**. Ένα `zIndex: dxfZIndex.ui.sidebar` περνά ως `scale-reference`, χωρίς
να ξέρει αν η έκφραση καταλήγει σε ρόλο ή σε ωμό αριθμό. Δηλωμένο, όχι κρυφό (άγκυρα `Κ4`).

🔴 **Η Φάση Γ απέδειξε ότι αυτό το «δηλωμένο όριο» έκρυβε την ΕΚΤΗ κλίμακα** — δες §5ter.1.
Παραμένει όριο: μια έκφραση εξακολουθεί να μην αποτιμάται. Αλλά **δεν** είναι πια αόριστο:
μετρήθηκε ότι η επικίνδυνη μορφή του είναι η **αριθμητική** (`ρόλος + N`), και το `src/` έχει
πλέον **μηδέν** τέτοιες σε καθολική ζώνη.

✅ Το `layoutUtilities.zIndex` (λεξιλόγιο #3, `modal 1000` · `tooltip 2000`) είχε **0
καταναλωτές** και **διαγράφηκε** στη Φάση Γ. *Νεκρός κώδικας δεν αποκτά ρόλο — φεύγει.*

### 6.5 Τέσσερις επιφάνειες **δεν ιδώθηκαν στην οθόνη**

`PropertiesPalette` · `QuickPropertiesHoverPopover` · `DxfContextMenu` · `QuickPropertiesMiniPanel`
φορτώνονται **τεμπέλικα**. Η τιμή τους **προκύπτει** από τους ρόλους του `:root`
(επιβεβαιωμένους ζωντανά και τους πέντε), αλλά **δεν παρατηρήθηκε** στην οθόνη.

### 6.6 Έμμεσα πακέτα — ✅ **ΜΕΤΡΗΘΗΚΕ** (Φάση Γ)· μένει γραμμένο **με ονόματα**

Η απογραφή ανοίγει τα **άμεσα** runtime dependencies. Το ερώτημα «είναι το κενό θεωρητικό;»
απαντήθηκε με **πλήρη σάρωση του store**: **125,0s · 206.235 αρχεία** ⇒ **15** πακέτα ≥1000
έναντι **11** που βλέπει η απογραφή.

| Τιμή | Πακέτο | Γιατί δεν φτάνει στην οθόνη |
|---|---|---|
| 2147483647 | `playwright-core` | **devDependency** — ποτέ σε bundle browser |
| **100000** | `@sentry-internal/feedback` | φορτώνεται **μόνο** με ρητό `feedbackIntegration()` — μετρημένο ότι **δεν** δηλώνεται |
| 1000 | `dockview` · `dockview-core` | **ορφανά στο store**: ούτε στο `package.json`, ούτε τα εισάγει κανείς |

⇒ **0 από τα 4 φτάνουν σήμερα**, άρα το κενό **μένει γραμμένο** αντί να λυθεί με κόστος 125s
ανά έλεγχο. 🔴 **Το σημαντικό είναι το ΠΩΣ ΘΑ ΕΠΑΥΕ**: το `@sentry/nextjs` **είναι** runtime
dependency και **τρέχει στον browser** (`sentry.client.config.ts`) — απέχουμε **μία γραμμή**
από widget στο **100000**, με **κάθε** πύλη πράσινη. Άγκυρα: `Σ17`.

### 6.7 Αριθμητική σε **τοπική** ζώνη — ανεκτή, δηλωμένη

Μετά τη Φάση Γ μένουν **πέντε** `ρόλος + N`, **όλες κάτω από το κατώφλι**:
`docked + 1/+2` (=11, 12, `brand-map.ts`) · `docked + 5` (=15, συνεργασία) ·
`base + 1` (=1) · `docked + 90` (=100, `geo-canvas`).

Είναι ανεκτές γιατί το CHECK 3.50 **εξ ορισμού** δεν κρίνει κάτω από το `GLOBAL_LAYER_FLOOR`
(τοπική στοίβαξη μέσα σε component), και τα σκαλιά τους απέχουν **δύο τάξεις μεγέθους** από
το κατώφλι. ⚠️ **ΔΕΝ είναι δομικά εγγυημένο**: αν κάποιος ανέβαζε το `docked` κοντά στο
1000, το `docked + 90` θα περνούσε στην καθολική ζώνη **χωρίς** ο ίδιος ο ρόλος `docked` να
αλλάξει ζώνη — άρα **χωρίς** να μπλοκάρει η πύλη. Γράφεται εδώ ώστε να μη διαβαστεί ως
«σαρώθηκε και είναι καθαρό».

### 6.8 Συγχώνευση σε λιγότερους ρόλους — **άλλο ερώτημα, άλλη απόδειξη**

Οι 40 ρόλοι είναι **ορατότητα**, όχι στόχος. Η μείωσή τους απαιτεί απόδειξη ότι δύο
επιφάνειες **δεν συνυπάρχουν ποτέ** — δηλαδή ακριβώς τη μέτρηση συν-παρουσίας που η
μονοτονία κατέστησε περιττή για τη **συμπίεση**. Είναι **δείγμα** και θέλει ζωντανή
παρατήρηση, οπότε **δεν** γίνεται στο ίδιο βήμα χωρίς να χαθεί η απόδειξη — το ίδιο
σκεπτικό που κράτησε τη Φάση Β χωριστά από τη Φάση Γ.

---

## 7. Δύο στρώματα

| | Πού | Πότε | Κόστος |
|---|---|---|---|
| **Layer 1** | `run-checks-parallel.js` (worker) | σκανδάλη **μέσα στην πύλη**: προφίλτρο κειμένου στα σταδιοποιημένα | **~0,05s** όταν δεν αφορά · **~4,7s** όταν πυροδοτεί |
| **Layer 1β** | *το ίδιο*, **σύνορο τρίτων** | **μόνο** όταν αλλάζουν εξαρτήσεις / μητρώο / σύνορο / η ίδια η πύλη | **+~6s** (απογραφή `node_modules`) |
| **Layer 2** | job `zindex-scale` στο **υπάρχον** `ssot-discover.yml` | **άνευ όρων**, με απογραφή | ~11s + σουίτα |

**Γιατί το Layer 1β παραλείπεται και γιατί αυτό ΔΕΝ είναι έκπτωση**: τα **μόνα** γεγονότα που
μπορούν να μεταβάλουν την απογραφή είναι αλλαγή εξαρτήσεων, του μητρώου, του συνόρου ή του
κώδικα που κρίνει. Οτιδήποτε άλλο αφήνει την απάντηση **αποδεδειγμένα** ίδια. Και όταν
παραλείπεται, η αναφορά το **τυπώνει με ⏭**: οι τρεις καταστάσεις που τη χρειάζονται
(`undeclared`, `drifted`, `orphan-declaration`) **δεν τίθενται**, και ένα «0» που σημαίνει «δεν
κοίταξα» είναι ακριβώς το σχήμα που όλο αυτό το ADR υπάρχει για να κυνηγά (άγκυρα `Σ11`).

⚠️ **ΚΑΝΕΝΑ νέο workflow.** Το μητρώο του `.ci-gate-tiers.json` παρακολουθεί **αρχεία**
workflow, όχι jobs ⇒ **31** πύλες αμετάβλητες, **επαληθευμένο εκτελώντας** το CHECK 3.37.
Η οικογένεια ταιριάζει κατά γράμμα: το ερώτημα «ζητά κάθε επιφάνεια τη ΜΙΑ κλίμακα;» **είναι**
η ερώτηση SSoT που κάνει όλο το workflow. **ΔΕΝ** μπήκε στο `ui-contrast-ratchet.yml`: εκείνο
είναι workflow **χρώματος**, και μια αποτυχία **στρώσης** εκεί θα αναφερόταν ως αποτυχία
**αντίθεσης** (το λάθος που απέφυγε ρητά το ADR-775 §Λ2).

**Γιατί προφίλτρο κειμένου και όχι λίστα μονοπατιών**: μια ωμή `z-index: 9999` μπορεί να
προσγειωθεί σε **οποιοδήποτε** `.css`/`.ts`/`.tsx` του `src/` — δηλαδή ο κατάλογος «πού μπορεί
να συμβεί» είναι όλο το δέντρο. Λίστα φακέλων θα ήταν σωστή σήμερα και θα απέκλινε σιωπηλά
αύριο (σχήμα CHECK 3.34/3.37).

**Γιατί η παράλειψη γίνεται ΠΡΙΝ τον μηχανισμό ratchet**: αν αντί γι' αυτό η `measure` επέστρεφε
κενό σύνολο, ο συγκριτής θα έβλεπε **37 παραβιάσεις λιγότερες** και θα τύπωνε «⬇ κλείδωσέ το»
— δηλαδή θα προσκαλούσε reseed στο **μηδέν** επειδή κανείς δεν κοίταξε. *Απουσία δεν είναι
πρόοδος* (μάθημα του `scope:'staged'` στο CHECK 3.38).

**Δηλωμένη συνέπεια**: η προσθήκη του `src/**/*.css` στις σκανδάλες του workflow ξυπνά και τα
τέσσερα αδέλφια jobs σε PR που αγγίζει μόνο `.css`. Προτιμήθηκε από το να είναι το Layer 2
τυφλό ακριβώς στη μισή επιφάνεια της πύλης (§2.3).

---

## 8. Η απόδειξη

- **68 tests** (`npm run test:zindex-scale`): **Μ0**×4 · **Μ1-Μ13β** (μεταλλάξεις **στις
  εισόδους**, μίνι-repo από τα **πραγματικά** αρχεία) · **Π1-Π5 + Π1β-Π1δ** (χειρόγραφη προσδοκία) ·
  **Κ1-Κ14** (τα όρια, γραμμένα ρητά) · **Σ0-Σ17** (το **σύνορο των τρίτων**, Φάση Β+Γ) ·
  **Δ1-Δ5** (η **διάταξη ως ταυτότητα**, Φάση Γ).
- ⚠️ **Το `Σ0` είναι η ΜΟΝΗ άγκυρα που ρωτά τον αληθινό δίσκο.** Όλα τα υπόλοιπα Σ τρέχουν σε
  **παραγόμενο** ψεύτικο `node_modules`, άρα αποδεικνύουν **αντίδραση**, όχι **συμφωνία**.
  Χωρίς το `Σ0` το μητρώο θα μπορούσε να έχει αποκλίνει από την πραγματικότητα και η σουίτα να
  είναι **ολόκληρη πράσινη** — ακριβώς το σχήμα των δύο λιστών namespace του CHECK 3.34.
- ⚠️ **Τα Π είναι χειρόγραφα ΕΠΙΤΗΔΕΣ** (ADR-587 §6.1): ο σαρωτής παίρνει τους ρόλους **από το
  `design-tokens.json`**, οπότε ένα test που τους ξαναδιαβάζει από εκεί θα συμφωνούσε με
  **οποιαδήποτε** τιμή. Η **σειρά** γράφεται με το χέρι.
- 🔴 **ΦΑΣΗ Γ — ΤΑ Π ΚΑΡΦΩΝΟΝΤΑΝ ΣΕ ΤΙΜΕΣ ΚΑΙ ΕΓΙΝΑΝ ΕΜΠΟΔΙΟ ΣΤΗ ΘΕΡΑΠΕΙΑ.** Τα `Π1`/`Π1β`
  έγραφαν `toBe(9000)`…`toBe(10400)`, και τα `Μ11`/`Κ11` μετάλλασσαν τη συμβολοσειρά
  `"value": "9900"` **κατά λέξη**. Ήταν σωστά όσο οι αριθμοί δεν άλλαζαν — και **σπασμένα**
  τη στιγμή που η κλίμακα συμπιέστηκε, δηλαδή στην ίδια τη δουλειά που το ADR περιγράφει ως
  θεραπεία. *Η αναλλοίωτη μιας στρώσης δεν είναι ο αριθμός της, είναι **ποιος κάθεται πάνω
  από ποιον**.* Πλέον ελέγχουν **σειρά** (`toBeGreaterThan`) και μεταλλάσσουν **κατά ρόλο**
  (`/("viewerPalette": \{ "value": ")\d+/`). ⚠️ Και τα τέσσερα τα έπιασε ο φρουρός
  **«η μετάλλαξη ΔΕΝ άλλαξε τίποτα»** (CHECK 3.44 / `Μ11`) — χωρίς αυτόν θα είχαν περάσει
  **πράσινα**, δηλαδή δύο νεκρές μεταλλάξεις σε μια σουίτα που υπάρχει για να τις κυνηγά.
- ⚠️ **Το `Δ1` είναι το ΘΕΩΡΗΜΑ, όχι test συμπεριφοράς**: αποδεικνύει ότι μονότονη
  επαναρίθμηση δίνει **ταυτόσημες** ταυτότητες. Τα `Δ2`-`Δ4` κλείνουν τις **τρεις** πιθανές
  «απλοποιήσεις» του `orderIdentities` (επιστροφή σε `ρόλος=τιμή` · αφαίρεση ζώνης · γείτονας
  → απόλυτο rank), και **και οι τρεις μεταλλάχθηκαν πραγματικά: 3/3 κόκκινες, Μ0 πράσινο.**
- ⚠️ **`Π2` είναι η ΜΟΝΗ αντιστάθμιση για δηλωμένο όριο**: το `cssVarNameOf` **αντιγράφει τη
  γραμματική** του generator (ο generator είναι script που **γράφει αρχεία**, και μια πύλη δεν
  επιτρέπεται να γράφει). Το `Π2` συγκρίνει **κάθε** ρόλο με το πραγματικό περιεχόμενο του
  `variables.css` ⇒ αν οι δύο γραμματικές αποκλίνουν, γίνεται κόκκινο **εκεί** αντί να γίνει
  σιωπηλό `unknown-token` στην παραγωγή.
- **7/11 κανόνες επαληθεύτηκαν ΖΩΝΤΑΝΑ ταυτόσημοι** end-to-end (3× ribbon @1100 · overlay 9000 ·
  insert-fn 9000 · fn-args 9001 · TableFormatToolbar 10000). **Και οι 5 ρόλοι λύνονται στο
  `:root`** με ακριβώς 9000/9001/9900/9999/10000.
- `npm run build:tokens` ✅
- **ΦΑΣΗ Β** (2026-08-09): **1.439 suites / 17.879 tests** πράσινα στις έξι ρίζες (βάση
  1.438 / 17.874) · `jscpd` καθαρό στα αγγιγμένα · `eslint` **0** · N.7.1: όλα τα αρχεία
  <500 (το `DxfZIndexSystem.styles.ts` **μπλόκαρε** στις 600 και χωρίστηκε) · **CHECK 3.37
  εκτελέστηκε**: μητρώο **31** πύλες, αμετάβλητο.
- **Ζωντανή επαλήθευση 6/6** (§5bis.6) — η κρίσιμη: το CSSOM δέχεται `var()` σε
  `element.style.zIndex`, που ήταν το **μόνο** που δεν μπορούσε να υποτεθεί.

---

## 9. ⚠️ Τι **ΜΗΝ** κάνεις

1. **ΜΗΝ διαβάσεις τη baseline ως δείκτη υγείας.** Μετρά «όσα σημεία δεν ζητούν ακόμη από την
   κλίμακα», όχι «πόσο σπασμένη είναι η στρώση». Η θεραπεία είναι **ρόλοι** — ένα bump από 9999
   σε 9998 θα μείωνε τον αριθμό και **δεν θα διόρθωνε τίποτα**.
2. **ΜΗΝ το κάνεις zero-tolerance.** Ο λόγος ήταν «37 ζωντανές ⇒ μονίμως κόκκινο ⇒ `SKIP_`»
   (δοκιμάστηκε και απορρίφθηκε στο CHECK 3.39). ⚠️ **Μετά τη Φάση Γ οι ωμές είναι 0**, άρα
   ο ratchet **ήδη** μπλοκάρει κάθε νέα — δηλαδή έχουμε το αποτέλεσμα του zero-tol **χωρίς**
   να χάσουμε την ικανότητα απορρόφησης αν χρειαστεί ποτέ. *Ένας μηχανισμός που πετυχαίνει
   τον στόχο στο μηδέν δεν χρειάζεται να μετονομαστεί.*
3. **ΜΗΝ προσθέσεις stylelint** (§3).
4. **ΜΗΝ αφαιρέσεις το `stripImportant`** (§5.1) ούτε την αφαίρεση σχολίων (§5.2).
5. **ΜΗΝ γράψεις ΑΡΙΘΜΗΤΙΚΗ πάνω σε ρόλο** (`zIndex.modal + 50`). Είναι **ωμός αριθμός με
   μεταμφίεση**: η πύλη τον βλέπει ως `scale-reference` ✅, δεν μετακινείται μαζί με τον ρόλο
   του, και **σπάει τη μονοτονία** που κάνει κάθε μελλοντική επαναρίθμηση αποδείξιμη. Εκεί
   ζούσε η **έκτη** κλίμακα (§5ter.1). Χρειάζεσαι ενδιάμεσο σκαλί; **Ζήτα ρόλο** — το βήμα
   10 αφήνει εννιά θέσεις ανάμεσα σε κάθε ζεύγος, και η παρεμβολή γίνεται **ρητή** πράξη που
   η πύλη ονομάζει.
6. **ΜΗΝ γυρίσεις τα `declarations` σε `ρόλος=τιμή`.** Είναι **λάθος ερώτημα**: ζητά
   ανθρώπινη επαλήθευση για κάθε αριθμό, ακόμη και όταν η διάταξη είναι αποδεδειγμένα ίδια —
   ανάθεση σε άνθρωπο, το σχήμα που απέτυχε μετρημένα σε 3.34 · 3.37 · 3.49. Ούτε σε
   **απόλυτο rank**: μία παρεμβολή θα ανέφερε **N** αλλαγές (§5ter.2, άγκυρες `Δ1`-`Δ4`).
7. **ΜΗΝ κάνεις τα tests να καρφώνουν ΤΙΜΕΣ** — ούτε στις προσδοκίες, ούτε στις μεταλλάξεις.
   Στη Φάση Γ **τέσσερα** tests έσπασαν έτσι, και το ένα από αυτά (`Μ11`) υπάρχει ακριβώς
   για να κυνηγά νεκρές μεταλλάξεις. Στόχευε τον **ρόλο** και τη **σειρά**.
8. **ΜΗΝ κατεβάσεις το κατώφλι των 1000**: ο θόρυβος ξεπερνά τον πήχη **<10%** ψευδώς θετικών
   της Google για **μπλοκάρουσα** πύλη (τα `z-index: 1/2/3/10/20/50/100` είναι **τοπική**
   στοίβαξη μέσα σε component, όχι παραβίαση).
9. **ΜΗΝ ξαναφέρεις σκαλί «MAX_INT»** (Φάση Β, §5bis.4). Δεν το έχει **καμία** κλίμακα των
   μεγάλων, και εδώ αποδείχθηκε **συνέπεια** αδάμαστου τρίτου, όχι ανάγκη. Αν κάτι πρέπει να
   κάθεται πάνω από όλα, **ζήτα ρόλο** — και αν είναι ξένο, δάμασέ το στο σύνορο.
10. **ΜΗΝ γράψεις ωμό αριθμό στο `foreign-boundary.css`** — αλλιώς λύνουμε το πρόβλημα των
   μαγικών αριθμών με έναν μαγικό αριθμό (άγκυρα `Σ14`).
11. **ΜΗΝ λύσεις κόκκινο `foreign-*` σβήνοντας τη δήλωση από το μητρώο.** Η απογραφή θα το
   ξαναβρεί ως `foreign-undeclared`· η δήλωση **είναι** η απάντηση, όχι το πρόβλημα.
12. **ΜΗΝ χαμηλώσεις τον ρόλο `devtoolsGuard`** και μην γράψεις εκεί ωμό αριθμό: λάθος κίνηση
    κρύβει το overlay σφαλμάτων του Next.js και ο επόμενος που θα δει λευκή οθόνη δεν θα ξέρει
    γιατί. Ο φρουρός πρέπει να μένει **μία σκάλα πάνω** από κάθε επιφάνεια προϊόντος.

---

## 10. Changelog

| Ημ/νία | Αλλαγή |
|---|---|
| 2026-08-08 | **Δημιουργία.** +5 ρόλοι στην αυθεντία· `generateZIndexScale()`· ο χειρόγραφος καθρέφτης του `layout.ts` έσβησε· −2 παράλληλα λεξιλόγια· 8 σημεία της ζώνης 9000-10000 σε ρόλους· **CHECK 3.50** (3 αρχεία, 8 καταστάσεις, κλειστή λογιστική)· **37 tests**· Layer 1 (σκανδάλη εντός πύλης) + Layer 2 (job στο `ssot-discover.yml`, **κανένα νέο workflow**). |
| 2026-08-08 | 🔴 **Διόρθωση ψευδούς πράσινου `!important`** (§5.1): `raw-literal` **39 → 45**· baseline **36 → 37** ταυτότητες. |
| 2026-08-09 | **ΦΑΣΗ Γ** (§5ter) — **η συμπίεση**. **(α) Η ΕΚΤΗ κλίμακα**: το `portal-overlay.ts` παρήγαγε **17 σκαλιά** με αριθμητική (`modal + 50…90`, `popover + 10…50`), αόρατα ως `scale-reference` ✅ — και η επαναρίθμηση θα είχε **αντιστρέψει τέσσερα ζεύγη με την πύλη πράσινη**. **12 νεκρά** διαγράφηκαν, **5** έγιναν ρόλοι, **1** ήταν τυχαία ισοβαθμία στο 1600 μέσω διπλής αριθμητικής (→ ρόλος `dynamicInput`). **(α2) ΔΕΥΤΕΡΗ ΕΣΤΙΑ** (§5ter.1β): δεύτερος έλεγχος βρήκε αριθμητική και σε `DxfZIndexSystem.styles.ts`/`canvas-ui.ts`/`geo-canvas` — **δύο είχαν ήδη σπάσει σιωπηλά** (`sticky+10` → ταυτόσημο με `banner`· `modal+10` → με `canvasSnap`), δύο επέζησαν **κατά τύχη**. Αιτία: η πρώτη σάρωση ακολούθησε **την έξοδο της πύλης** αντί για **το μοτίβο** — και η αριθμητική εκεί ζει σε **σταθερές**, θέση που η πύλη δεν αναφέρει καν. Διαγράφηκαν **5 εργοστάσια** `create*ZIndex(τύπος, offset)` (μηδέν καταναλωτές· 295 → **143** γραμμές) και **3 νεκρά σκαλιά**· **+6 ρόλοι**. **(β) Τοκενοποίηση**: ωμές δηλώσεις **32 → 0** (+5 ρόλοι για τη σκάλα 1900-2600)· διαγράφηκαν τα νεκρά `layoutUtilities.zIndex` και `RulerDebugSettings.zIndex`. **(γ) ΤΟ ΟΡΓΑΝΟ**: το `declarations` της baseline από `ρόλος=τιμή` σε **ΔΙΑΤΑΞΗ** (`orderIdentities`) ⇒ μονότονη επαναρίθμηση = **πράσινη χωρίς reseed**, δηλαδή η πύλη **αποδεικνύει** τη μηδενική οπτική αλλαγή αντί να τη ζητά από άνθρωπο. **(δ) Η ΣΥΜΠΙΕΣΗ**: κορυφή **10400 → 1370**, ρόλοι 23 → **40** (ορατότητα, όχι χειροτέρευση), βήμα 10, εντός MUI/Salt (1500). **(ε)** §5.2 έμμεσα πακέτα **μετρήθηκε** (125,0s / 206.235 αρχεία): **15 vs 11**, αλλά **0/4 φτάνουν στην οθόνη** ⇒ μένει γραμμένο **με ονόματα** (άγκυρα `Σ17` για το Sentry feedback @ 100000). **68 tests** (+7: `Δ1`-`Δ5`, `Π1δ`, `Σ17`)· **Boy Scout**: το CHECK 3.28 έπιασε **2 προϋπάρχοντες κλώνους** στο `canvas-ui.ts` (επαληθευμένο με `jscpd` πάνω στο `HEAD:`) όταν το αρχείο μπήκε σε diff για **άλλη** αλλαγή ⇒ εξήχθησαν `dragRectangle` + `dashboardBar`· **8 χειρόγραφοι αριθμοί σε σχόλια** («(1700)», «(80)») καθαρίστηκαν — *ένας αριθμός σε σχόλιο είναι καθρέφτης που κανείς δεν επιβάλλει, και αυτό ακριβώς έκρυβε την έκτη κλίμακα*· **3/3 μεταλλάξεις** στο νέο όργανο· **κανένα νέο workflow** (μητρώο **31**, επαληθευμένο εκτελώντας το CHECK 3.37). |
| 2026-08-09 | **ΦΑΣΗ Β** (§5bis). **(α) Σύνορο τρίτων**: νέο `src/app/foreign-boundary.css` + μητρώο `.zindex-foreign.json` + **δεύτερο κατάστιχο** στο CHECK 3.50 (`scripts/lib/zindex/foreign.js` + `foreign-census.js`, **11 ρητές καταστάσεις**, 6 μηδενικής ανοχής, κλειστή λογιστική fail-closed). **11 πακέτα** μετρημένα (το handoff ήξερε **2**). **(β) Η κορυφή**: ο ρόλος `critical` (2147483647) **διαγράφηκε**· **+8 ρόλοι** (`viewerPrompt`·`appDrawerScrim`·`appDrawer`·`eyedropperCapture`·`eyedropperLoupe`·`debugOverlay`·`devtoolsGuard`) με **ίδια σειρά** και **δύο ισοβαθμίες λυμένες σκόπιμα** — η μία ήταν πραγματικό λανθάνον σφάλμα (ο loupe μπορούσε να σκεπάσει το overlay σφαλμάτων). **13 ωμές δηλώσεις** σε ρόλους ⇒ `raw-literal` **45 → 32**, baseline **37 → 30** ταυτότητες, ρόλοι **17 → 23**. **(γ) SSoT audit**: το **δεύτερο** `critical` (=1500) του `DxfZIndexSystem.styles.ts` ήταν **νεκρό** και διαγράφηκε· το αρχείο (600 γρ.) χωρίστηκε κατά ευθύνη σε `DxfZIndexSystem.styles.ts` (292) + `DxfSurface.styles.ts` (317). **61 tests**· ζωντανή επαλήθευση 6/6· **κανένα νέο workflow** (μητρώο **31**, επαληθευμένο εκτελώντας το CHECK 3.37). |

---

## 11. Πηγές

> ⚠️ **Κάθε γραμμή εδώ διαβάστηκε πραγματικά** και τεκμηριώνει ισχυρισμό αυτού του ADR. Δεν
> είναι βιβλιογραφία «για κύρος»: αν αφαιρεθεί μια πηγή, ο αντίστοιχος ισχυρισμός μένει
> **χωρίς απόδειξη** και πρέπει να ξαναμετρηθεί.

### 11.1 Πώς δαμάζουν οι μεγάλοι τους τρίτους (ερώτημα §5 #1)

| Πηγή | Τι τεκμηριώνει εδώ |
|---|---|
| [Material UI — z-index](https://mui.com/material-ui/customization/z-index/) | Κλίμακα **1000→1500** (mobileStepper→tooltip)· «*customization … is discouraged· should you change one, you likely need to change them all*». **Κορυφή 1500**, καμία έννοια MAX_INT (§5bis.2, §3) |
| [CSS-Tricks — Systems for z-index](https://css-tricks.com/systems-for-z-index/) | Το κανονικό κείμενο του χώρου. Για τρίτους: «*if you have a third-party component with a z-index value you can't change, you plug that into the map*» = **χειρόγραφη καταχώριση** (§5bis.2) |
| [CSS-Tricks — The Value of z-index](https://css-tricks.com/the-value-of-z-index/) | Το «9999 και βάλε κι άλλα εννιάρια» ονομάζεται ρητά αντι-πρότυπο· «*every z-index should come from a token*» (§3) |
| [Microsoft Atlas — z-index tokens](https://design.learn.microsoft.com/tokens/z-index.html) | Τοπική στοίβαξη 1-4 · καθολικό στρώμα **από 1000** · κορυφή **1070** — μία από τις τρεις ανεξάρτητες επιβεβαιώσεις του κατωφλίου (§3) |
| [Shoelace — z-index tokens](https://shoelace.style/tokens/z-index) | Κλίμακα ρόλων χωρίς MAX_INT· κορυφή στην τάξη του 1000 (§3) |
| [USWDS — z-index tokens](https://designsystem.digital.gov/design-tokens/z-index/) | Κρατική κλίμακα ρόλων· καμία τιμή «πάνω από όλα» (§3) |
| [OutSystems UI Layer System](https://medium.com/@bernardocardoso/outsystems-ui-layer-system-managing-z-index-at-scale-68dca9e543de) | Διαχείριση z-index σε κλίμακα προϊόντος· ίδιο συμπέρασμα για τα magic numbers (§3) |

### 11.2 Έχει θέση σκαλί «MAX_INT»; (ερώτημα §5 #2)

**Καμία** από τις παραπάνω δεν έχει. Η απάντηση για την «άμυνα από dev-tool overlay»:

| Πηγή | Τι τεκμηριώνει εδώ |
|---|---|
| [Next.js — Dev Overlay & Error UI](https://deepwiki.com/vercel/next.js/5.3-dev-overlay-and-error-ui) | Το overlay είναι **custom element με Shadow Root**, σκοπίμως απομονωμένο από το CSS της εφαρμογής |
| [vercel/next.js — PR #73489](https://github.com/vercel/next.js/pull/73489) · [Discussion #23970](https://github.com/vercel/next.js/discussions/23970) | Η κοινή πρακτική είναι `nextjs-portal { display: none }` — δηλαδή **απόκρυψη**, ποτέ «σκαλί στην κλίμακα» |
| **Δική μας μέτρηση** (2026-08-09, `node_modules/next/dist/compiled/next-devtools/index.js`) | `:host { --top-z-index: 2147483647 }` — η τιμή ζει **ΜΕΣΑ** στο shadow root· ο host μένει `z-index: auto`. **Αυτό** είναι που κάνει τον φρουρό αναγκαίο και ταυτόχρονα **μικρό** (§5bis.1) |

⇒ Το «MAX_INT» **δεν** είναι πρακτική των μεγάλων· ήταν **συνέπεια** αδάμαστου τρίτου.

### 11.3 Υπάρχει εξυπνότερος μηχανισμός από αριθμούς; (ερώτημα §5 #3)

| Πηγή | Τι τεκμηριώνει εδώ |
|---|---|
| [modern-css — CSS isolation](https://modern-css.com/z-index-isolation/) · [theosoti — isolation: isolate](https://theosoti.com/short/isolation-isolate-property/) | Φτιάχνει stacking context **χωρίς καμία οπτική παρενέργεια** (δεν θέλει `position`, δεν αγγίζει `opacity`/`transform`) ⇒ ο **σωστός** μηχανισμός για ό,τι ζωγραφίζεται στη θέση του (§5bis.3) |
| [Lullabot — ADR: Layer elements deliberately with stacking contexts](https://architecture.lullabot.com/adr/20251210-create-deliberate-stacking-contexts/) | Απορρίπτει **εντελώς** την καθολική κλίμακα: «*these values cannot work globally. They are only able to layer elements within a single stacking context*». ⚠️ **Ισχύει για κλίμακα χωρίς απόδειξη** — εμείς μετρήσαμε ότι **όλες** οι επιφάνειες ≥1000 κάθονται στο ROOT stacking context (§6.3), οπότε οι αριθμοί μας **συγκρίνονται όντως** |

### 11.4 Η συμπίεση της ζώνης — τα τρία ερωτήματα της Φάσης Γ

**#1 — Πώς αποδεικνύουν οι μεγάλοι μια αλλαγή κλίμακας;**

| Πηγή | Τι τεκμηριώνει εδώ |
|---|---|
| [Atlassian — Migrate to tokens](https://atlassian.design/tokens/migrate-to-tokens/) · [Automate design token migrations with codemods](https://medium.com/@stevedodierlazaro/automate-design-token-migrations-with-codemods-a21cf8bbd53b) | Ο codemod «*only provides token suggestions, so **manual review is required***»· η επικύρωση γίνεται με **visual regression** ως «*ideal companion*». Δηλαδή **δείγμα + ανάθεση σε άνθρωπο** — καμία απόδειξη (§5ter.3) |
| [Atlaskit — Tokens changelog](https://atlaskit.atlassian.com/packages/design-system/tokens/changelog) | Κύκλος ζωής **active → deprecated → soft-deleted → deleted**· ο μηχανισμός είναι **lint που προειδοποιεί**, όχι απόδειξη διάταξης |
| [USWDS — z-index tokens](https://designsystem.digital.gov/design-tokens/z-index/) | 9 σκαλιά… με κορυφή `top: **99999**`. **Αντι-παράδειγμα**: το arms race επιβιώνει **μέσα** στο token σύστημα (§5ter.3) |
| [Shoelace — z-index tokens](https://shoelace.style/tokens/z-index) | **5** σκαλιά, κορυφή **1000** (drawer 700 → tooltip 1000)· καμία τεκμηρίωση μετανάστευσης |
| [OutSystems UI Layer System](https://medium.com/@bernardocardoso/outsystems-ui-layer-system-managing-z-index-at-scale-68dca9e543de) | **5 global layers, βήμα 5, κορυφή 20** (+ `layer-above`/`layer-below` με `calc()`). Η πιο επιθετική συμπίεση που μετρήθηκε — και **δεν** τεκμηριώνει πώς μετανάστευσε τα υπάρχοντα |

⇒ **Κανείς δεν αποδεικνύει· όλοι δειγματοληπτούν.** Το `orderIdentities` (§5ter.2) είναι το
δικό μας αντίβαρο: η απόδειξη είναι **μαθηματική** και τη δίνει η ίδια η πύλη.

**#2 — Υπάρχει εργαλείο που κρίνει ΔΙΑΤΑΞΗ z-index και όχι τιμή;**

**Όχι — επιβεβαιώθηκε.** Η αναζήτηση επιστρέφει αποκλειστικά *tutorials*, *debuggers* και
*visualizers* ([web.dev — z-index and stacking contexts](https://web.dev/learn/css/z-index/) ·
[Lullabot — Debugging Stacking Contexts](https://www.lullabot.com/articles/understanding-debugging-stacking-contexts)),
δηλαδή εργαλεία που δείχνουν **τι συμβαίνει τώρα** σε έναν browser. Κανένα **δεν κρίνει** αν
η σχετική διάταξη **άλλαξε** ανάμεσα σε δύο εκδοχές του κώδικα. Επιβεβαιώνει το §3: τα
`stylelint-scales`, `z-index-token-enforcer`, `eslint-plugin-tailwindcss` κρίνουν **πρόθεμα**
ή **τιμή**.

**#3 — Είναι το `@layer` του CSS χρήσιμο εδώ;**

🔴 **ΤΟ HANDOFF ΕΙΧΕ ΛΑΘΟΣ ΔΙΑΓΝΩΣΗ.** Έγραφε «*πιθανώς όχι χωρίς αναβάθμιση σε Tailwind 4*».
Η σωστή απάντηση είναι **όχι ποτέ, δομικά**:

| Πηγή | Τι τεκμηριώνει εδώ |
|---|---|
| [MDN — `@layer`](https://developer.mozilla.org/en-US/docs/Web/CSS/@layer) | Τα cascade layers ορίζουν **ποια δήλωση κερδίζει** (cascade). **Μηδενική** επίδραση σε stacking context, z-index ή σειρά ζωγραφικής |
| [CSS-Tricks — Cascade Layers Guide](https://css-tricks.com/css-cascade-layers/) | Ίδιο: ο μηχανισμός είναι **προτεραιότητα δήλωσης**, όχι **σειρά απόδοσης** |

**Δική μας μέτρηση** (2026-08-09, `tailwindcss@3.4.19` μέσω CLI σε καθαρό input): η έξοδος
περιέχει **0** `@layer`. Επιβεβαιώνει τη Φάση Β — αλλά είναι **άσχετο**: ακόμη και με
εγγενή layers, το ερώτημα «ποιος ζωγραφίζεται πάνω από ποιον» **δεν** απαντιέται από εκεί.
*Το ότι δύο μηχανισμοί μοιάζουν συγγενείς δεν σημαίνει ότι απαντούν το ίδιο ερώτημα.*

⚠️ **Το top layer (`popover`/`dialog` API) παραμένει απορριμμένο ΜΕ ΜΕΤΡΗΣΗ** (ADR-739 §64):
ο διάλογος περιέχει **3 Radix `Select`** που κάνουν portal με z-index, άρα θα έπεφταν **πίσω**
του. Μην το ξαναπροτείνεις χωρίς να απαντήσεις **αυτή** την ένσταση.
