# ADR-798 — Ο Άνθρωπος: **ταυτότητα προσώπου** ξεχωριστά από **εξουσιοδότηση**

| Metadata | Value |
|---|---|
| **Status** | 🔵 **ΑΠΟΦΑΣΗ — καμία γραμμή κώδικα** *(N.0.1 Φάση 1: απόφαση πριν τον κώδικα)* |
| **Date** | 2026-08-24 |
| **Category** | Identity / Auth / Contact Management |
| **Canonical Locations** | *(καμία σήμερα — αυτό **είναι** το εύρημα· οι υποψήφιες ονομάζονται στο §11)* |
| **Author** | Georgios Pagonis + Claude Code (Anthropic AI) |
| **Σχετικά** | **ADR-748** *(τι βλέπω ανά ειδικότητα)* · **ADR-787** *(ποιος μπαίνει σε ποιον χώρο)* · **ADR-132** *(ESCO/ISCO-08)* · **ADR-121** *(personas επαφών)* · **ADR-283** *(ρόλοι έργου)* · **ADR-749** *(η βλάβη «δεύτερη αλήθεια»)* |

---

## 0. Τι κατέχει αυτό το έγγραφο — και τι **ΔΕΝ** κατέχει

Αυτό το ADR κατέχει **ένα** ερώτημα:

> **Τι ΕΙΝΑΙ αυτός ο άνθρωπος — ανεξάρτητα από το τι δικαιούται και από το πού ανήκει.**

**ΔΕΝ αντιγράφει τίποτα.** Το λεξιλόγιο των επαγγελμάτων ζει στο **ADR-132**, τα personas των
επαφών στο **ADR-121**, οι ρόλοι έργου στο **ADR-283**. Εδώ υπάρχουν **δείκτες**, ποτέ αντίγραφα —
αλλιώς θα γεννιόταν το **δέκατο** λεξιλόγιο, δηλαδή ακριβώς η βλάβη που το έγγραφο υπάρχει για να
κλείσει (**ADR-749**).

### 0.1 Ο χάρτης — ποιο ερώτημα απαντιέται πού

| Ερώτημα | Έγγραφο |
|---|---|
| **Τι ΕΙΝΑΙ αυτός ο άνθρωπος** *(επαγγελματική ιδιότητα)* | **εδώ** |
| Τι **δικαιούται** *(εξουσιοδότηση)* | **ADR-748** · `lib/auth/roles.ts` |
| **Πού ανήκει** *(οργανισμός, συμμετοχή, ενεργός χώρος)* | **ADR-787** |
| Ποιο είναι το **λεξιλόγιο** των επαγγελμάτων | **ADR-132** *(ESCO v1.2.1 / ISCO-08)* |
| Τι **επιπλέον πεδία** εμφανίζει μια ιδιότητα σε **επαφή** | **ADR-121** |
| Ποιος είναι ο **αρχιτέκτονας ΤΟΥ ΕΡΓΟΥ** *(σχέση, όχι ιδιότητα)* | **ADR-283** · `types/entity-associations.ts` |
| Τι βλέπει ο καθένας **στο μενού / ribbon** | **ADR-748 §14** |
| Ποια **δουλειά υποδεικνύει** ένα επάγγελμα *(σπάει ισοβαθμία, **ποτέ** δικαίωμα)* | **εδώ** *(§12, Φάση 3)* · `config/isco-job-affinity.ts` |
| Ποιο επάγγελμα αντιστοιχεί σε **ρόλο έργου** *(αντίστροφη κατεύθυνση)* | **ADR-282/745** · `config/profession-bridge.config.ts` |

⚠️ **Αν προσθέσεις απόφαση που αφορά «τι είναι αυτός ο άνθρωπος», γράψ' την ΕΔΩ και βάλε δείκτη
εκεί** — όχι το αντίστροφο.

---

## 1. Γιατί υπάρχει — το ερώτημα και η **μετρημένη** απάντηση

Ο Giorgio ρώτησε *(2026-08-24)*:

> Μπαίνουν απλοί χρήστες, **τοπογράφοι, αρχιτέκτονες, πολιτικοί μηχανικοί, μηχανολόγοι,
> διακοσμητές, λογιστές, δικηγόροι αγοραστών, δικηγόροι πωλητών, συμβολαιογράφοι, εργαζόμενοι,
> εταιρείες, αυτόνομοι μηχανικοί ή μηχανικοί εταιρειών**. **Ποια είναι η πρώτη σελίδα που συναντά
> ο καθένας;** Πώς καταλαβαίνει το σύστημα **τι είναι** αυτός που μπαίνει;

**Η σημερινή απάντηση είναι ΕΝΑ ΜΠΙΤ.**

Το `src/lib/routes/landing.ts:105` επιστρέφει `hasOrganization(identity) ? AUTH_ROUTES.home :
PRIVATE_SPACE_HOME`, και το `hasOrganization` *(γρ. 126)* είναι
`typeof identity.companyId === 'string' && identity.companyId.length > 0`.

⇒ **Και οι 14 κατηγορίες → ΔΥΟ οθόνες.** Δεν είναι ελάττωμα του `landing.ts`: το `landing.ts`
απαντά **σωστά** στο ερώτημα *«σε ποιον χώρο ενεργεί;»*. Το ερώτημα *«τι είναι;»* **δεν έχει
απαντητή** — και αυτό είναι το κενό.

### 1.1 Το ADR-748 είχε **ήδη ονομάσει** αυτό το επίπεδο, και δήλωσε ότι δεν το κατέχει

Το **ADR-748 §11.3** ορίζει το επίπεδο **1 — Άνθρωπος** ως *«email, όνομα, **επαγγελματική
ιδιότητα**. Μηδέν δικαιώματα»*. Το **ADR-787** ανέλαβε τα επίπεδα 2-4 *(Οργανισμός · Συμμετοχή ·
Χώρος)* — **ποτέ το 1**. Το επίπεδο 1 έμεινε **αδέσποτο επί 22 ημέρες**.

---

## 2. Τα μετρημένα γεγονότα *(2026-08-24)*

> ⚠️ **Αριθμός σε έγγραφο παλιώνει** (N.12). Άνοιξε το αρχείο πριν επικαλεστείς νούμερο.

| Τι | Πού | Μέτρηση |
|---|---|---|
| `CustomClaims` | `src/lib/auth/types.ts:245-257` | **5** πεδία: `companyId` · `globalRole` · `mfaEnrolled?` · `emailVerified?` · `permissions?` — **κανένα επαγγελματικό** |
| `GLOBAL_ROLES` | `src/lib/auth/types.ts:13-18` | **4** τιμές, **καμία** επάγγελμα |
| Εγγραφή | `src/server/auth/pending-registration.ts:71-76` | **4** πεδία: `uid` · `email` · `displayName` · `authProvider` |
| Φόρμα σύνδεσης | `src/auth/hooks/useAuthFormState.ts:20-26` | **5** πεδία· **καμία** ερώτηση επαγγέλματος |
| Υπολογισμός «τι βλέπω» | `src/config/jobs-access.ts:103-116` | **3** είσοδοι: `permissions` · `isBypass` · `availableSources` |
| Πηγές δικαιωμάτων **στον browser** | `src/hooks/useEffectivePermissions.ts:48-50` | **μία**: `globalRole` |
| **Οριστικό** | αναζήτηση `persona` / `profession` σε `src/auth/` · `src/lib/auth/` · `src/lib/routes/` · `src/lib/workspace/` · `src/config/jobs-*` · `src/contexts/` · `src/middleware.ts` | 🔴 **0 εμφανίσεις** |

**Το επίπεδο «Άνθρωπος» υπάρχει ήδη ως αποθήκη**: το `users/{uid}` είναι **top-level, ΜΗ
tenant-scoped** συλλογή και κρατά `email · displayName · companyId · globalRole · status ·
authProvider`. Δεν χρειάζεται νέα συλλογή — **χρειάζεται νέο πεδίο σε υπάρχουσα**.

---

## 3. 🔴 ΤΟ ΚΡΙΣΙΜΟ ΕΥΡΗΜΑ — **δεν λείπει λεξιλόγιο· λείπει η ΓΕΦΥΡΑ**

Η πρώτη διάγνωση ήταν *«λείπει λεξιλόγιο επαγγέλματος — φτιάξ' το»*. **Το SSoT audit την
ανέτρεψε**: το λεξιλόγιο υπάρχει, **πλήρες, ευρωπαϊκό, πολύγλωσσο**.

`src/types/contacts/esco-types.ts` (**ADR-132**) — **ESCO v1.2.1 · ISCO-08 · 28 γλώσσες**, cache σε
`system/esco_cache/occupations/{id}`, ζωντανή αναζήτηση (`search_esco_occupations` /
`set_contact_esco`), με τα πεδία `profession · specialty · escoUri · escoLabel · iscoCode ·
escoSkills[]` πάνω στο **`Contact`** (`src/types/contacts/contracts.ts:125-141`).

🔴 **Το `Contact` ΔΕΝ έχει πεδίο `uid`** *(μετρημένο)* ⇒ **μηδέν σύνδεσμος προς λογαριασμό**.
Ο δικηγόρος-**επαφή** έχει ISCO **2611**· ο δικηγόρος-**χρήστης** δεν έχει τίποτα. Είναι ο **ίδιος
άνθρωπος**, σε δύο κόσμους που δεν μιλάνε.

⇒ **Η δουλειά είναι η ΓΕΦΥΡΑ, όχι το λεξιλόγιο.** Ένα νέο λεξιλόγιο θα ήταν το **δέκατο**.

### 3.1 Ο πίνακας των **ΕΝΝΕΑ** λεξιλογίων — ποιο απαντά **ποιο** ερώτημα

*(Το **ADR-748 §3** έλεγε «τέσσερα». Μετρήθηκαν **εννέα** στις 2026-08-24 — ο αριθμός ήταν
μπαγιάτικος· διορθώθηκε εκεί.)*

| # | Λεξιλόγιο | Πού ζει | Τιμές | Απαντά στο ερώτημα |
|---|---|---|---|---|
| 1 | `GlobalRole` | `lib/auth/types.ts:13` | 4 | *Τι δικαιούμαι σε όλη την εταιρεία* |
| 2 | `ProjectRole` **(δικαίωμα)** | `lib/auth/types.ts:33` | 9 | *Τι δικαιούμαι **σε αυτό το έργο*** |
| 3 | `UserRole` **(legacy)** | `auth/types/auth.types.ts:17` | 3 | — **περιττό** *(Π-2)* |
| 4 | `ProjectRole` **(ιδιότητα)** | `types/entity-associations.ts:20-27` | 7 | *Ποιος είναι ο αρχιτέκτονας **του έργου*** |
| 5 | `PersonaType` | `types/contacts/personas.ts:25` | 9 | *Ποια **επιπλέον πεδία** δείχνει η φόρμα επαφής* |
| 6 | `EngineerSpecialty` | `types/contacts/personas.ts:60` | 7 | *Ποια **ειδικότητα ΤΕΕ*** |
| 7 | `UserType` | `auth/types/auth.types.ts:25` | 3 | *Πόσο **προχωρημένο εργαλείο** δείχνω* — 🔴 **αυτο-δηλωμένο**, βλ. Π-19 |
| 8 | `DeptKey` | `app/(app)/onboarding/organization/page.tsx:14` | 4 | *Ποια **τμήματα** έχει ο νέος οργανισμός* |
| 9 | `TradeCode` | `subapps/procurement/types/trade.ts:35` | 32 | *Τι **συνεργείο** είναι ο υπεργολάβος* |

🔑 **Κανένα από τα εννέα δεν απαντά «τι ΕΙΝΑΙ αυτός ο άνθρωπος».** Τα 1-3 απαντούν *τι
δικαιούται*· τα 4 και 9 είναι **σχέσεις με αντικείμενο**· τα 5 και 6 απαντούν για **επαφή**,
όχι για **λογαριασμό**· το 7 είναι αυτο-δηλωμένο εργαλείο· το 8 αφορά **οργανισμό**. Το δέκατο
δεν γράφεται: **η απάντηση είναι το ESCO**, που δεν είναι λεξιλόγιό μας — είναι **ταξινομία της ΕΕ**.

### 3.2 🔴 Ο δείκτης ESCO **στον κώδικα είναι ΛΑΘΟΣ** — και το λάθος διαδίδεται

🔴 **ΔΙΟΡΘΩΣΗ ΤΟΥ ΙΔΙΟΥ ΜΟΥ ΑΡΙΘΜΟΥ (Φάση 1, 2026-08-24): έγραψα «τρία» — είναι **13**.**
Η πρώτη μέτρηση κοίταξε **μόνο** τα αρχεία που είχα ήδη ανοίξει· η κλειστή σάρωση
*(`ADR-034` **ΚΑΙ** `esco|isco|profession` στην ίδια γραμμή, όλο το `src/`)* έδωσε **13 σημεία σε
11 αρχεία**. **Έκτο «λάθος αριθμός σε έγγραφο»** — και το έγραψα **μέσα στην παράγραφο που
καταγγέλλει το φαινόμενο**. Το κριτήριο «τα σημεία που ήδη κοιτούσα» **δεν είναι μέτρηση**.

| Αρχείο | Σημεία |
|---|---|
| `types/contacts/esco-types.ts` · `types/contacts/contracts.ts` · `config/firestore-collections.ts` | 1 + 1 + 1 |
| `types/ContactFormTypes.ts` · `utils/contactForm/mappers/individual.ts` | 2 + 2 |
| `services/esco.service.ts` · `components/shared/EscoOccupationPicker.tsx` · `components/shared/KadCodePicker.tsx` | 1 + 1 + 1 |
| `subapps/procurement/…/signatory/types.ts` · `…/commit-signatory-service.ts` · `utils/contactForm/fieldMappers/individualMapper.ts` | 1 + 1 + 1 |

✅ **ΔΙΟΡΘΩΘΗΚΑΝ ΚΑΙ ΤΑ 13 (Φάση 1)** — εναπομείναντα **0**, επαληθευμένο με την ίδια κλειστή
σάρωση. ⚠️ Οι **νόμιμες** αναφορές `ADR-034` *(gantt · spatial bounds · validation bounds ·
z-index)* έμειναν **άθικτες**: το κριτήριο απαιτούσε τη λέξη `esco`/`isco`/`profession` **στην ίδια
γραμμή**, ποτέ σκέτο `ADR-034`.

**Το `ADR-034` είναι ΤΡΙΠΛΟ** *(`empty-spatial-bounds` · `gantt-chart` · `validation-bounds`)* και
**κανένα από τα τρία δεν αναφέρει τη λέξη ESCO ούτε μία φορά** *(0 · 0 · 0, μετρημένο)*.
Ο πραγματικός ιδιοκτήτης είναι το **`ADR-132-esco-professional-classification.md`**, που δηλώνει
το `esco-types.ts` στα **Canonical Locations** του.

⚠️ **Το ίδιο το προηγούμενο handoff ΚΑΙ το εγκεκριμένο σχέδιο κουβαλούσαν αυτόν τον δείκτη** — τον
είχαν διαβάσει από το σχόλιο του κώδικα και τον αντέγραψαν. Είναι **ακριβώς** η κλάση
`ambiguous-section` / `collided-cross-home` που μετρά το **CHECK 3.49**: *ο αριθμός δεν
προσδιορίζει έγγραφο*, οπότε ο επόμενος αναγνώστης ανοίγει **ένα από τρία λάθος αρχεία** και
συμπεραίνει ότι η τεκμηρίωση λείπει.

🔧 Καταγράφηκε ως **Π-21** στο **ADR-748 §12** και **εκτελέστηκε στη Φάση 1** *(την ίδια μέρα)*.

---

## 4. Η ΑΡΧΗ

> **Η επαγγελματική ιδιότητα ανήκει στον ΑΝΘΡΩΠΟ, δηλώνεται από τον ίδιο, ταξιδεύει μαζί του, και
> ΠΟΤΕ δεν δίνει δικαίωμα.**

Τέσσερα σκέλη, το καθένα με δικό του λόγο:

| Σκέλος | Ο λόγος |
|---|---|
| **Ανήκει στον άνθρωπο** *(όχι στον οργανισμό)* | Το Slack το κάνει *custom profile field του workspace* ⇒ **χάνεται όταν φύγεις**. Η Figma λέει το αντίθετο με τα ίδια της τα λόγια *(§5)*. Είναι κατά λέξη το **ADR-748 Ε-2′** |
| **Δηλώνεται από τον ίδιο** | Κανείς άλλος δεν ξέρει· και η **επαλήθευση** είναι ξεχωριστό ερώτημα *(§7)* |
| **Ταξιδεύει μαζί του** | Το `users/{uid}` είναι ήδη **top-level, μη tenant-scoped** — το επίπεδο υπάρχει |
| 🔴 **ΠΟΤΕ δεν δίνει δικαίωμα** | **ADR-748 Ε-7** *(η.)*. Μια **αυτο-δηλωμένη** ιδιότητα που έδινε πρόσβαση θα ήταν κλιμάκωση προνομίων με ένα dropdown |

### 4.1 Γιατί το επάγγελμα **ΔΕΝ** μπαίνει στα claims

Τα claims είναι **εξουσιοδότηση** — τα υπογράφει ο διακομιστής και τα εμπιστεύονται οι κανόνες
Firestore. Το επάγγελμα είναι **ιδιότητα**, και **αυτο-δηλωμένη**. Μπαίνοντας στο token θα γινόταν
**σιωπηλά είσοδος εξουσιοδότησης**: την επόμενη φορά που κάποιος γράψει
`claims.profession === '...'` σε κανόνα ή σε route, η αυτο-δήλωση θα έχει γίνει άδεια — χωρίς να
το αποφασίσει κανείς.

⚠️ **Δεύτερος, ανεξάρτητος λόγος**: το **ADR-787 §5** *(Ε-5)* μέτρησε ότι το token έχει όριο
**1.000 bytes**. Το ESCO URI είναι ~60 χαρακτήρες· η ετικέτα και ο κωδικός ISCO ακόμη ~40. Είναι
**μετρήσιμο κόστος σε πόρο που ήδη έχει ταβάνι**, για δεδομένο που **κανένας κανόνας δεν
επιτρέπεται να διαβάσει**.

---

## 5. Η έρευνα — τι κάνουν οι μεγάλοι *(εκτελεσμένη 2026-08-24)*

| Πηγή | Τι δίνει | Το όριό της |
|---|---|---|
| **buildingSMART `IfcRoleEnum`** *(IFC4 / 4.3)* | **23** τιμές: `ARCHITECT` · `STRUCTURALENGINEER` · `MECHANICALENGINEER` · `ELECTRICALENGINEER` · `CIVILENGINEER` · `COSTENGINEER` · `CLIENT` · `CONTRACTOR` · `SUBCONTRACTOR` · `SUPPLIER` · `MANUFACTURER` · `PROJECTMANAGER` · `CONSTRUCTIONMANAGER` · `FIELDCONSTRUCTIONMANAGER` · `FACILITIESMANAGER` · `COMMISSIONINGENGINEER` · `ENGINEER` · `OWNER` · `BUILDINGOWNER` · `BUILDINGOPERATOR` · `CONSULTANT` · `RESELLER` · `USERDEFINED` | 🔴 **ΔΕΝ έχει SURVEYOR · LAWYER · NOTARY · ACCOUNTANT · INTERIOR DESIGNER** *(επαληθευμένο στην πηγή)*. Διέξοδος = `USERDEFINED` + `UserDefinedRole : IfcLabel`, **ελεύθερο κείμενο** |
| **ESCO / ISCO-08** *(ΕΕ / ILO)* | Κάθε ESCO occupation → **ακριβώς μία** ISCO-08 unit group *(επίπεδο 4)*, «ίση ή **στενότερη**, ποτέ ευρύτερη». Δικηγόρος **2611** · συμβολαιογράφος **2619** · τοπογράφος **2165** · διακοσμητής **3432** | Δεν ξέρει τίποτα για έργα / CDE |
| **schema.org** | `Person.hasOccupation → Occupation.occupationalCategory` = *ταξινομία + κωδικός + ετικέτα*· `Role` για **χρονικά οριοθετημένα** | Σχήμα, όχι μηχανισμός |
| **Fowler / Silverston — Party-Role** | *ένα πρόσωπο παίζει **N** ρόλους· ο ρόλος είναι **σχέση**, όχι ιδιότητα* | — |
| **Dotloop** | ρόλοι ανά συναλλαγή | 🔴 *«Only **one** role can be selected for any one person in the loop»* — **εμείς επιτρέπουμε N** |
| **Autodesk ACC / Procore** | ρόλος ανά λογαριασμό + **παράκαμψη** ανά έργο | Εταιρείες = **ελεύθερο κείμενο** |
| **Slack** | επάγγελμα ως **custom profile field** | 🔴 ανήκει στον **ΟΡΓΑΝΙΣΜΟ** ⇒ φεύγεις, **χάνεται** |
| **Figma** *(«Behind the feature: building multi-account»)* | *«unify all accounts under **one profile** … maintain **one public brand as they change jobs or roles**»* | Επιβεβαιώνει τον άξονα· **καμία ταξινομία** |
| **W3C Verifiable Credentials 2.0** *(+ v2.1 FPWD, 2026)* | issuer / holder / verifier — ρητά για **επαγγελματικές πιστοποιήσεις** | Για το §7, όχι για σήμερα |
| 🔴 **EUDI Wallet / eIDAS 2.0** *(Καν. ΕΕ 2024/1183)* | **QEAA** — *«trusted verification of **professional qualifications**»*· **υποχρέωση και των 27 κρατών έως Δεκ. 2026**· Ελλάδα: **gov.gr Wallet ήδη ζωντανό** | Απαιτεί QTSP ⇒ **δηλώνεται, δεν υλοποιείται** |

### 5.1 🏆 Πού **ξεπερνάμε** — τρία σημεία, όλα μετρημένα

1. **Το πρότυπο του ίδιου μας του κλάδου σταματά στην κατασκευαστική ομάδα.** Η ελληνική
   πραγματικότητα — **τοπογράφος · δικηγόρος αγοραστή/πωλητή · συμβολαιογράφος · λογιστής ·
   διακοσμητής** — είναι **δομικά εκτός `IfcRoleEnum`**, και η διέξοδός του είναι ελεύθερο
   κείμενο. Εμείς είμαστε **ήδη** σε ESCO/ISCO-08, που τους καλύπτει **όλους** με μηχανικά
   αναγνώσιμο URI. ✅ **Απόδειξη**: ο διακοσμητής, που προηγούμενο audit είχε γράψει ως «κενό»,
   είναι ISCO **3432** *(Interior Designers and Decorators)* — **καμία απαρίθμηση δεν επεκτείνεται**.
   🔴 **Κανείς δεν γεφυρώνει IFC actor roles ↔ ESCO occupations** *(αναζητήθηκε· μηδέν ευρήματα)*.
2. **Το επάγγελμα ανήκει στον ΑΝΘΡΩΠΟ.** Το Slack το χάνει· η Figma το επιβεβαιώνει με τα ίδια της
   τα λόγια· είναι κατά λέξη το **ADR-748 Ε-2′**.
3. 🔴 **EUDI / QEAA**: Autodesk · Procore · Dotloop · Figma **δεν έχουν τίποτα** αντίστοιχο. Σχήμα
   *δηλωμένο ≠ επαληθευμένο* με **υποδοχή** attestation μάς αφήνει **έτοιμους**, χωρίς να χτιστεί
   τίποτα σήμερα *(§7)*.

---

## 6. 🏆 Η προβολή προς **IFC** — και γιατί το `USERDEFINED` **ΔΕΝ** είναι η απάντηση

Ο σημερινός εξαγωγέας *(`src/subapps/dxf-viewer/systems/guides/guide-ifc-exporter.ts:190`)* γράφει
`IFCPERSON` με **`NestorCAD`** ως όνομα και **`$`** σε όλα τα υπόλοιπα ορίσματα. Το **7ο** όρισμα
του `IfcPerson` είναι `Roles : LIST OF IfcActorRole` — και είναι `$`. **Μηδέν ρόλος, σκληρά.**
Στο δέντρο υπάρχουν **0** εμφανίσεις `IfcActorRole` και **0** `IfcClassification` *(μετρημένο)*.

### 6.1 🔴 Η λύση που **ΑΠΟΡΡΙΦΘΗΚΕ** — και ήταν η πρόταση του ίδιου μας του σχεδίου

Η προφανής προβολή είναι *«ESCO → `IfcRoleEnum`, με `USERDEFINED` όπου δεν υπάρχει αντιστοιχία»*.
Το σχέδιο της Φάσης 0 **αυτό πρότεινε**. **Απορρίπτεται ως ΜΟΝΟΣ μηχανισμός**, με λόγο:

Το `IfcActorRole` έχει **τρία** πεδία — `Role : IfcRoleEnum` · `UserDefinedRole : IfcLabel`
*(προαιρετικό)* · `Description : IfcText` *(προαιρετικό)*. Και τα τρία είναι **ετικέτες**. Με
`USERDEFINED` + `UserDefinedRole = 'Τοπογράφος Μηχανικός'`:

- ✅ ο άνθρωπος που ανοίγει το αρχείο **διαβάζει** τον ρόλο,
- 🔴 **το URI χάνεται**, ο κωδικός ISCO **χάνεται**, η γλώσσα **καρφώνεται**,
- 🔴 και ο επόμενος που εισάγει το αρχείο έχει **ελεύθερο ελληνικό κείμενο** — δηλαδή ακριβώς το
  πρόβλημα που το **ADR-132 §1** υπάρχει για να λύσει *(«"Μηχανικός" vs "μηχανικός" vs "Μηχ." —
  αδύνατη η αξιόπιστη αναζήτηση»)*.

**Θα εξάγαμε ως ελεύθερο κείμενο ακριβώς το δεδομένο που κάναμε ταξινομημένο.**

### 6.2 ✅ Η λύση — **ΔΥΟ ανεξάρτητα κανάλια, ΠΟΤΕ ένα**

Το IFC **έχει ήδη** πρωτοκλασάτο μηχανισμό για εξωτερικές ταξινομίες, και **επαληθεύτηκε στην
πηγή ότι φτάνει στον δρώντα**:

- `IfcActor` → `IfcObject` → **`IfcObjectDefinition`**, που έχει την αντίστροφη σχέση
  **`HasAssociations`** *(«associates external references or other resource definitions to the
  object»)*.
- `IfcRelAssociatesClassification.RelatedObjects : IfcDefinitionSelect` ⇒ **ο δρώντας μπορεί να
  ταξινομηθεί**.
- `IfcClassificationReference` έχει **`Location : IfcURIReference`** *(«where the external source
  can be accessed by electronic means» — άλλαξε από `IfcLabel` σε `IfcURIReference` στο IFC4)* ·
  **`Identification : IfcIdentifier`** · **`Name : IfcLabel`** · **`ReferencedSource →
  IfcClassification`**.

Η αντιστοιχία με το ESCO είναι **ένα προς ένα, χωρίς καμία παραμόρφωση**:

| ESCO / ISCO | Πεδίο IFC | Τύπος |
|---|---|---|
| `escoUri` *(`http://data.europa.eu/esco/occupation/…`)* | `IfcClassificationReference.Location` | `IfcURIReference` |
| `iscoCode` *(π.χ. `2165`)* | `IfcClassificationReference.Identification` | `IfcIdentifier` |
| `escoLabel` *(ελληνική ετικέτα)* | `IfcClassificationReference.Name` | `IfcLabel` |
| *«ESCO», έκδοση `v1.2.1`* | `IfcClassification.Name` / `.Edition` | — |

**Ο κανόνας**: γράφονται **ΚΑΙ ΤΑ ΔΥΟ**, πάντα.

| Κανάλι | Τι είναι | Για ποιον |
|---|---|---|
| **`IfcActorRole.Role`** | η **προβολή** — με απώλεια, best-effort | εργαλεία που διαβάζουν **μόνο** την απαρίθμηση |
| **`IfcRelAssociatesClassification`** | η **αλήθεια** — χωρίς απώλεια, με URI | εργαλεία που διαβάζουν ταξινομίες |

⚠️ **ΠΟΤΕ ένας μηχανισμός με «ή».** Μόνο το πρώτο ⇒ χάνεται το URI *(§6.1)*. Μόνο το δεύτερο ⇒
ένας θεατής IFC που δείχνει ρόλους δρώντων δεν βλέπει **τίποτα**, και το αρχείο μοιάζει ελλιπές
ενώ δεν είναι. Είναι το ίδιο σχήμα «δύο ανεξάρτητοι κανόνες, ποτέ ένας με ή» που επιβάλλουν τα
**CHECK 3.41 · 3.43 · 3.50 · 3.58 · 3.60**.

⚠️ Το **`USERDEFINED` παραμένει ΟΝΟΜΑΣΜΕΝΗ ΚΑΤΑΣΤΑΣΗ**, ποτέ σιωπηλή διέξοδος: σημαίνει *«το IFC
**δεν έχει** τιμή γι' αυτό το επάγγελμα»*, και επειδή το δεύτερο κανάλι κουβαλά το URI, η
πληροφορία **δεν χάνεται όταν συμβεί**.

⚠️ **Η κατεύθυνση ESCO → ISCO είναι ολική· η αντίστροφη ΔΕΝ είναι.** Κάθε ESCO occupation ανήκει
σε **ακριβώς μία** ISCO unit group, αλλά μία unit group περιέχει **πολλά** ESCO. Άρα η προβολή
γράφεται **μόνο** με φορά ESCO → ISCO → `IfcRoleEnum`. Ένας αντίστροφος επιλυτής θα ήταν
**μαντεψιά με πρόσωπο βεβαιότητας**.

⚠️ Ο **`src/config/profession-bridge.config.ts`** *(7 ρόλοι → ESCO URI + ISCO, με αντίστροφο
επιλυτή ελληνικού ελεύθερου κειμένου από υπόμνημα DXF, γρ. 175-201)* **ΔΕΝ αγγίζεται**: απαντά
στο ερώτημα *«τι λέει αυτό το υπόμνημα;»*, όχι *«τι είναι αυτός ο άνθρωπος;»*. Δύο ερωτήματα, δύο
απαντητές.

---

### 6.3 🔴 ΔΙΟΡΘΩΣΗ ΤΟΥ §6.2 *(Φάση 4, 2026-08-24)* — **ο φορέας του δεύτερου καναλιού ήταν λάθος**

Η **αρχή** των δύο καναλιών μένει ακέραιη. Ο **φορέας** του δεύτερου αλλάζει, και ο λόγος είναι
**δομικός, όχι αισθητικός**:

- `IfcRelAssociatesClassification.RelatedObjects : IfcDefinitionSelect` δέχεται **μόνο**
  `IfcObjectDefinition` / `IfcPropertyDefinition`.
- Ούτε το **`IfcPerson`** ούτε το **`IfcActorRole`** κληρονομούν `IfcRoot`. Είναι **resource
  entities** — δεν έχουν `GlobalId`.
- Το §6.2 επαλήθευσε **σωστά** ότι η ταξινόμηση φτάνει στο **`IfcActor`**. Αλλά ο εξαγωγέας
  **δεν έχει `IfcActor`** — έχει `IfcPerson` μέσα σε `IfcOwnerHistory`. Για να αποκτήσει, θα
  γεννούσε **rooted** οντότητα-«συμμετέχοντα στο έργο», δηλαδή θα έφερνε το ερώτημα GDPR στον
  **κρίσιμο δρόμο** της Φάσης 4.

Το IFC4 έχει την **σχεδιασμένη γι' αυτό** οντότητα, και το γράφει αυτολεξεί:

> `IfcExternalReferenceRelationship` — *«This relationship is used to assign classification,
> library or document information to entities that **do not inherit from `IfcRoot`**. It has a
> **similar functionality as the subtypes of `IfcRelAssociates`**.»*

και το `IfcResourceObjectSelect` περιλαμβάνει **ρητά** `IfcActorRole` **και** `IfcPerson`. Το
`IfcActorRole` έχει μάλιστα αντίστροφη **`HasExternalReference`** *(νέα στο IFC4)*, περιγραφόμενη
ως *«classification … associated with the **actor role**»*.

⚠️ **Η ταξινόμηση κρεμιέται στον ΡΟΛΟ, όχι στον άνθρωπο** — ο ρόλος **είναι** η προβολή του
επαγγέλματος. Έτσι η αλυσίδα γίνεται **κλειστή**: χωρίς ρόλο δεν υπάρχει ταξινόμηση, και ο κανόνας
«**ΚΑΙ ΤΑ ΔΥΟ, ΠΑΝΤΑ**» παύει να είναι σύμβαση που κάποιος πρέπει να θυμάται.

### 6.4 🔒 ΤΟ GDPR ΤΟΥ `IfcPerson` — **το απάντησε το ίδιο το πρότυπο**

Το ερώτημα *«μπαίνει το όνομα του συνδεδεμένου χρήστη;»* **δεν χρειάστηκε απόφαση προϊόντος**:

1. Ο ορισμός του `IfcPerson` φέρει **ρητή** προειδοποίηση: *«…an IFC file might in some situations
   be considered to be a **database that enables identification of a particular person** under the
   terms of such legislation.»*
2. Και το **IFC4 CHANGE** άνοιξε τον δρόμο για να μη χρειάζεται: *«Attribute Id renamed to
   Identification. **WHERE rule relaxed to allow omission of names if Identification is
   provided.**»*
3. Ο κανόνας `IdentifiablePerson` απαιτεί **ένα** από `Identification` / `FamilyName` / `GivenName`.

⇒ Δίνεται **μόνο** το `Identification`, με **σταθερή** τιμή που δεν δείχνει σε άνθρωπο. Το αρχείο
είναι **έγκυρο** και **ανώνυμο** — όχι ψευδωνυμοποιημένο. *(Η διάκριση δεν είναι λεπτολογία: η
ψευδωνυμοποίηση παραμένει προσωπικό δεδομένο κατά το GDPR· η ανωνυμοποίηση όχι — Αιτιολογική
σκέψη 26.)* **Ο ρόλος ταξιδεύει· το όνομα όχι.**

🏆 **ΤΟ REVIT ΚΑΝΕΙ ΤΟ ΑΚΡΙΒΩΣ ΑΝΤΙΘΕΤΟ — ΜΕΤΡΗΜΕΝΟ ΣΤΗΝ ΠΗΓΗ** *(`Autodesk/revit-ifc`,
`Source/Revit.IFC.Export/Exporter/Exporter.cs`)*:

| γρ. | Τι κάνει | Συνέπεια |
|---|---|---|
| **3220-3226** | `author = projectInfo.Author`, αλλιώς **`doc.Application.Username`** → `ParseName(...)` | γράφει **πραγματικό όνομα**, από το λειτουργικό, **χωρίς να ρωτήσει** |
| **3266** | `CreatePerson(file, **null**, familyName, givenName, …)` | αφήνει **κενό** ακριβώς το `Identification` που το IFC4 χαλάρωσε |
| **3271** | `CreatePersonAndOrganization(file, person, organization, **null**)` | το τρίτο όρισμα **είναι** τα `Roles` ⇒ **κανένας ρόλος, ποτέ** |
| **3370** | `CreateActorRole(file, **"UserDefined"**, category, null)` — ο **μόνος** στον exporter, μόνο σε COBie | **ακριβώς** ο μηχανισμός που το §6.1 απέρριψε **πριν μετρηθεί** |

Δηλαδή η πρακτική του μεγαλύτερου παίκτη είναι *«όνομα πάντα, ρόλος ποτέ — και όταν ρόλος, ως
ελεύθερο κείμενο»*. Εδώ ισχύει το αντίστροφο και στα τρία, **και δεν είναι γνώμη: είναι ο γραμμένος
κανόνας του προτύπου.**

---

## 7. **Δηλωμένο ≠ Επαληθευμένο** — τρεις **ρητές** καταστάσεις, ποτέ boolean

Η ιδιότητα είναι **αυτο-δηλωμένη** *(§4)*. Ένα `verified: boolean` θα είχε **δύο** τιμές για
**τρεις** καταστάσεις, και το `false` θα σήμαινε ταυτόχρονα *«ελέγχθηκε και δεν ισχύει»* και
*«δεν ρώτησε κανείς»* — δηλαδή το σχήμα **«0 = κανείς δεν κοίταξε»** που το έργο έχει πληρώσει
**οκτώ φορές** *(CHECK 3.35 · 3.39 · 3.40 · 3.43 · 3.45 · 3.46 · 3.51 · 3.57)*.

| Κατάσταση | Σημαίνει | Στην οθόνη |
|---|---|---|
| `unknown` | **κανείς δεν δήλωσε τίποτα** | ουδέτερο· **ποτέ** ως «όχι» |
| `declared` | ο ίδιος το δήλωσε· **κανείς δεν το έλεγξε** | ορατό **ως δήλωση** |
| `verified` | υπάρχει **attestation** από εκδότη *(ΤΕΕ · ΔΣΑ · ΟΕΕ · gov.gr Wallet)* | ορατό **ως επαληθευμένο** |

🔑 **Πρότυπο, όχι εφεύρεση**: το `src/config/jobs-access.ts:131` έχει **ήδη** ακριβώς αυτό —
`JobAccessDecision = 'granted' | 'unknown' | 'none'`, με το `useEffectivePermissions.ts:24-25` να
γράφει ότι *«το `unknown` δεν κρύβεται ποτέ»*. **Ίδιο σχήμα, δεύτερος καταναλωτής.**

🔑 **Η οθόνη δείχνει ΠΑΝΤΑ ποια από τις τρεις ισχύει** — αλλιώς το `declared` διαβάζεται ως
`verified`, που είναι **η ίδια η βλάβη** που το σχήμα υπάρχει για να κλείσει.

⚠️ **Υποδοχή, όχι υλοποίηση.** Το σχήμα του attestation *(εκδότης · λήξη · ανάκληση)* ακολουθεί
**W3C VC 2.0 / EUDI QEAA**. **Δηλώνεται εδώ· δεν υλοποιείται σήμερα.**

---

## 8. Οι αποφάσεις που **κλείδωσαν** *(Giorgio, 2026-08-24)*

| # | Απόφαση | Ο λόγος |
|---|---|---|
| **Α1** | **ΕΝΑ** νέο ADR για το «τι είναι αυτός ο άνθρωπος» | Το **ADR-748 §11.3** το ονόμασε και δήλωσε ότι δεν το κατέχει· το **ADR-787** πήρε τα επίπεδα 2-4, ποτέ το 1 |
| **Α2** | **ΜΗΔΕΝ** νέο λεξιλόγιο — δείκτες σε **ADR-132** *(ESCO)* και **ADR-121** *(personas)* | Θα ήταν το **δέκατο** *(§3.1, ADR-749)* |
| **Α3** | Το **Κ-2 του ADR-787** *(πολλαπλή συμμετοχή)* **μένει έξω** | 🔑 **ΝΟΜΟΣ ΜΟΝΤΕΛΟΥ, όχι εύρος**: το επάγγελμα είναι *declarative* **ιδιότητα**· η συμμετοχή *contextual* **σχέση** *(Party-Role)*. Η σύζευξή τους αναπαράγει **ακριβώς** το λάθος του Slack |
| **Α4** | Το επάγγελμα **ΔΕΝ** μπαίνει στα claims | §4.1 — δύο ανεξάρτητοι λόγοι *(εξουσιοδότηση· όριο 1.000 bytes)* |
| **Α5** | **Καμία ερώτηση, καμία modal, καμία οθόνη επιλογής** | **ADR-748 §4** *(«ο ρόλος δεν δηλώνεται — διαπιστώνεται»)* + **Ε-7** *(γ′: πρόταση, όχι ερώτηση)*. Η δήλωση γίνεται από την **υπάρχουσα** αναζήτηση ESCO, όποτε θέλει ο ίδιος |

---

## 9. Δηλωμένα όρια αυτού του εγγράφου

1. **Καμία γραμμή κώδικα δεν γράφτηκε.** Είναι αναγνώριση + απόφαση *(N.0.1 Φάση 1)*.
2. **Καμία απόφαση για UI.** Το «πώς φαίνεται» ανήκει στο **ADR-748**.
3. **Καμία απόφαση για το ποιος μπαίνει σε ποιον χώρο.** Ανήκει στο **ADR-787**.
4. **Δεν αγγίζεται ο `buyer_lawyer` / `seller_lawyer` / `notary`** *(`types/entity-associations.ts:20-27`)*:
   είναι **σχέση με ακίνητο**, όχι ιδιότητα προσώπου — και είναι **σωστό** *(Party-Role)*.
5. **Το `UserTypeContext` δεν ξυπνά.** Είναι αυτο-δηλωμένο σε `localStorage`· το **ADR-748 §4** το
   απαγορεύει ρητά ως πηγή ταυτότητας *(Π-19)*.
6. **Δεν προτείνεται μετονομασία** κανενός από τα εννέα λεξιλόγια *(§3.1)*.
7. 🔶 **Το §6.2 δεν έχει ακόμη άγκυρα.** Η προβολή είναι **αποφασισμένη**, όχι υλοποιημένη· η
   απόδειξη *(άγκυρα που διαβάζει τις πραγματικές γραμμές IFC **και τον παρονομαστή** — χωρίς
   προβολή ⇒ `USERDEFINED`)* ανήκει στη φάση που θα τη γράψει.

---

## 10. 🔗 Αμφίδρομες παραπομπές — **το συμβόλαιο**

> 🔑 **Γιατί αμφίδρομες**: ένας πράκτορας που ανοίγει το **ADR-132** για να πειράξει το ESCO πρέπει
> να **μάθει** ότι το ίδιο σχήμα καταναλώνεται πλέον και από τον **λογαριασμό**. Μονόδρομος
> δείκτης από εδώ **δεν τον φτάνει ποτέ** — το ίδιο σχήμα με τις δύο λίστες namespace του
> **CHECK 3.34**, που είχαν αποκλίνει κατά **63** επειδή **καμία δεν ρωτούσε την άλλη**.

| Έγγραφο | Δείχνει **εδώ** από | Τι μαθαίνει ο αναγνώστης του |
|---|---|---|
| **ADR-748** | §3 · §11.3 · §12 *(Π-19 · Π-20 · Π-21)* · **Ε7.δ** *(changelog Φάσης 3)* | Το επίπεδο **1 (Άνθρωπος)** απέκτησε ιδιοκτήτη· τα λεξιλόγια είναι **εννέα**, όχι τέσσερα. 🔴 Και το **`pickDefaultJob()` δέχεται πλέον προαιρετικό `tiebreak`**: όποιος πειράξει τον κανόνα ισοβαθμίας του Ε7.δ πρέπει να **μάθει** ότι υπάρχει δεύτερος κριτής — που **σπάει ισοβαθμία και ΠΟΤΕ δεν διευρύνει σύνολο** |
| **ADR-787** | §5 *(Κ-8)* | Το **Κ-8** *(επαλήθευση ιδιότητας)* ζητά να επαληθευτεί ιδιότητα **που κανείς δεν δηλώνει** — εδώ αποκτά αντικείμενο *(§7)* |
| **ADR-132** *(ESCO)* | «Σχετικά» | 🔴 Το σχήμα ESCO θα καταναλωθεί **και από τον λογαριασμό**, όχι μόνο από το `Contact`· και **τρία σχόλια κώδικα δείχνουν σε λάθος ADR** *(§3.2)* |
| **ADR-121** *(personas)* | «Σχετικά» | Το `PersonaType` απαντά *«ποια πεδία δείχνει η φόρμα **επαφής**»* — **όχι** «τι είναι ο κάτοχος του λογαριασμού» |

⚠️ **ΜΗΝ προσθέσεις δείκτη χωρίς να προσθέσεις και τη γραμμή σε αυτόν τον πίνακα.** Μονόδρομος
δείκτης είναι **χειρότερος από κανέναν**: δίνει την **εντύπωση** σύνδεσης.

⚠️ **ΜΗΝ τρέξεις τον `generate-adr-index.cjs`** — είναι καταστροφικός· η γραμμή στο `adr-index.md`
προστίθεται **με το χέρι**.

---

## 11. Ανοιχτά — **δεν αποφασίζονται εδώ**

| # | Ερώτημα |
|---|---|
| ~~**Ο-1**~~ | ✅ **ΑΠΑΝΤΗΘΗΚΕ (Φάση 2)**: **επίπεδα πεδία**, όχι υπο-αντικείμενο. Ο λόγος **δεν είναι γούστο, είναι οι κανόνες**: το `firestore.rules` κρίνει με `diff().affectedKeys()`, που επιστρέφει **κορυφαία** κλειδιά μόνο ⇒ μέσα σε ένα `professionalIdentity: {…}` ο φρουρός **δεν μπορεί** να ξεχωρίσει το *δηλωμένο* από το *επαληθευμένο*, δηλαδή θα έχανε ακριβώς τη διάκριση του §7. Επίπεδα ⇒ έλεγχος **ανά πεδίο** *(μάθημα `47686a8f`)*. **Δεύτερος, ανεξάρτητος λόγος**: το `IndividualContact` τα έχει επίπεδα με τα **ίδια ονόματα** — υπο-αντικείμενο θα ήταν **δεύτερο σχήμα για το ίδιο δεδομένο** |
| ~~**Ο-2**~~ | ✅ **ΑΠΑΝΤΗΘΗΚΕ (Φάση 2)**: **ΕΝΑ επάγγελμα + N πιστοποιήσεις**, ποτέ N επαγγέλματα. Ο *ενεργειακός επιθεωρητής* **δεν είναι δεύτερο επάγγελμα** — είναι **διαπίστευση πάνω σε ένα**, και το σχήμα υπάρχει ήδη: είναι το `verified` του **§7**, που κουβαλά **εκδότη · λήξη · ανάκληση**. Το `energy_inspector` ζει ήδη σωστά ως **ρόλος σε έργο** *(`entity-associations.ts`)* και η ειδικότητα ΤΕΕ ως `EngineerSpecialty` ⇒ η πληθικότητα θα ήταν **τέταρτη θέση** για κάτι που έχει ήδη τρεις. **Δομικό επιχείρημα**: ESCO → ISCO → `IfcRoleEnum` είναι **ένα προς ένα**· με N, το *«ποιον ρόλο γράφω στο IFC;»* δεν έχει απάντηση χωρίς **μαντεψιά**, που το §6.2 απαγορεύει ρητά. ⚠️ Το `DeclaredOccupation` **δεν κωδικοποιεί πληθικότητα**, οπότε η απόφαση παραμένει **αναστρέψιμη με μία γραμμή** |
| **Ο-3** | *(⚠️ Το **υπο-ερώτημα** «μπαίνει όνομα ανθρώπου στο εξαγόμενο IFC;» **ΕΚΛΕΙΣΕ** στη Φάση 4 — **όχι**, δες §6.4· το παρακάτω παραμένει ανοιχτό.)* Συνδέεται ποτέ ένα `Contact` με έναν `uid`; *(σήμερα **μηδέν** σύνδεσμος, §3)* Είναι **απόφαση GDPR πριν γίνει τεχνική** — δες **ADR-748 §11.4** |
| **Ο-4** | Ποιος είναι ο **εκδότης** για κάθε ιδιότητα *(ΤΕΕ · ΔΣΑ · ΟΕΕ · ΣΕΣΜΑ)* και ποιος δέχεται attestation πρώτος *(§7)* |

---

## 13. 🔴 ΤΑ ΩΜΑ i18n ΚΛΕΙΔΙΑ — **χάρτης ΡΙΖΑΣ πάνω σε διαχωρισμό ΚΛΕΙΔΙΟΥ**

*Φάση 5, 2026-08-24. Ρίζα βρέθηκε, θεραπεύτηκε, επαληθεύτηκε ζωντανά.*

### 13.1 Το σύμπτωμα και οι τρεις λάθος υποψίες

Στο `/o/<ws>/account/profile` το badge ταξινόμησης έβαφε **`esco.badge`** και το κουμπί
καθαρισμού είχε `aria-label="common.clear"`. Το προηγούμενο handoff είχε ήδη **αποκλείσει με
μέτρηση** τρεις υποθέσεις: το κλειδί **υπάρχει** (`contacts.json:49`)· το namespace
**δηλώνεται**· και η αλλαγή σε `@/i18n/hooks/useTranslation` **δοκιμάστηκε ζωντανά και
απέτυχε**. Η ρίζα δηλωνόταν ρητά **ΑΓΝΩΣΤΗ** — και αυτό ήταν σωστό: κάθε μία από τις τρεις
θα είχε στείλει τη συνέχεια σε λάθος δρόμο.

### 13.2 Η μέτρηση που ανέτρεψε τη διάγνωση

Ζωντανή ανάκριση του στιγμιότυπου i18next *(fiber walk, χωρίς `window.i18n`)*:

| ερώτημα | απάντηση |
|---|---|
| `store.getResource('el','contacts','esco.badge')` | **`"ESCO"`** — υπάρχει |
| `translator.resolve('contacts:esco.badge')` | **`res: "ESCO"`** — επιλύεται |
| `i18n.t('contacts:esco.badge')` | **`"esco.badge"`** — ωμό |

⇒ **Δεν ήταν φόρτωση. Ήταν ΕΠΙΛΥΣΗ.** Το bundle ήταν πλήρες και η αστοχία συνέβαινε
**μετά** το `resolve`.

### 13.3 Η ρίζα

`src/i18n/config.ts` αντικαθιστούσε **καθολικά** το `i18n.t` με μεταφραστή που έκανε
`remapLegacyTranslationKey` **άνευ όρων** και ρωτούσε **μόνο τον στόχο**.

🔑 **Ο μηχανισμός της βλάβης**: ο `LEGACY_NAMESPACE_ROOT_MAP` δηλώνει μετακομίσεις σε
επίπεδο **ρίζας** (`contacts.esco → contacts-relationships`), ενώ ο πραγματικός διαχωρισμός
έγινε σε επίπεδο **κλειδιού**: τα `esco.searchResults`/`noResults`/`useFreeText` όντως
έφυγαν, το `esco.badge` **έμεινε πίσω**. Μια εγγραφή ρίζας δεν μπορεί να πει «μερικά ναι,
μερικά όχι» ⇒ το ερώτημα πήγαινε σε namespace που **δεν έχει** το κλειδί, και το **μόνο**
ερώτημα που θα απαντούσε — το αρχικό — ήταν **δομικά αδύνατο να τεθεί**.

⚠️ **Γιατί ο wrapper δεν έσωζε τίποτα**: το `getFixedT` του i18next τελειώνει σε
`return this.t(resultKey, o)` (`i18next/dist/cjs/i18next.js:2034`) ⇒ **κάθε** `t` του
react-i18next περνά από τον μπαλωματή. Η δηλητηρίαση ήταν **κάτω** από τον wrapper· γι' αυτό
η αλλαγή του handoff ήταν αρχιτεκτονικά σωστή και **θεραπευτικά αδιάφορη**.

### 13.4 Η ΚΛΑΣΗ — δύο ανεξάρτητα όργανα που συμφώνησαν

| όργανο | el |
|---|---|
| στατική απογραφή των locale JSON | **172** |
| εκτέλεση της **πραγματικής** μηχανής σε ζωντανό στιγμιότυπο *(7.166 κλειδιά, 13 legacy ns, όλα φορτωμένα)* | **168** |

Η διαφορά των 4 **εξηγήθηκε, δεν αγνοήθηκε**: είναι κλειδιά που είναι **string** στο αρχικό
και **αντικείμενο** στον στόχο (`dxf-viewer:calibration.{sceneStatus,coordinates,clickTest,tips}`).
Δεν είναι ωμά κλειδιά — είναι **σκίαση τύπου**, άλλο ελάττωμα *(§13.7)*.

**Πραγματικό μέγεθος**: από τα 172, **135 καλούνται όντως** από τον κώδικα — `dxf-viewer`
**102** · `contacts` **17** · `properties` **7** · `projects` **4** · `common` **4** ·
`files` **1**. Τα δύο της οθόνης ήταν το **#1 και #2 από 135**.

### 13.5 Η θεραπεία — και η προφανής που απορρίφθηκε **με μέτρηση**

⚠️ **ΑΠΟΡΡΙΦΘΗΚΕ: «αρχικό πρώτα, remap ως εφεδρεία»** *(η σειρά που έχει ο wrapper)*.
Μετρήθηκε: **404** κλειδιά υπάρχουν **και στα δύο** σημεία, **149** με **διαφορετική τιμή**,
και σε **κάθε** δείγμα ο **στόχος** κρατά τη νεότερη γραφή — `«Βαθμονόμηση Συντεταγμένων»`
έναντι του παλιού `«🔧 Καλιμπράρισμα Συντεταγμένων»`· `«Επιλέξτε ακίνητο»` έναντι του
**αμετάφραστου** `«Unit»`. Η αντιστροφή θα άλλαζε σιωπηλά **149 ορατά κείμενα**,
επαναφέροντας παλιότερη γραφή.

⚠️ **ΑΠΟΡΡΙΦΘΗΚΕ: «σβήσε τις εγγραφές του χάρτη»** — η εγγραφή `contacts.esco` εξυπηρετεί
**3** κλειδιά που όντως μετακόμισαν· η διαγραφή της ανταλλάσσει 1 σπασμένο με 3.

⚠️ **ΑΠΟΡΡΙΦΘΗΚΕ: «μετακόμισε τα 172 στα αρχεία-στόχους»** — δεν αφαιρεί τον **μηχανισμό**:
χάρτης ρίζας πάνω σε διαχωρισμό κλειδιού θα ξαναγεννούσε το ελάττωμα με το επόμενο κλειδί,
και τίποτα δεν θα το έπιανε.

⚠️ **ΑΠΟΡΡΙΦΘΗΚΕ: «βγάλε τον μπαλωματή»** — **3.364** κλειδιά *(μετρημένα)* ζουν
αποκλειστικά χάρη σε αυτόν.

✅ **ΕΠΙΛΕΧΘΗΚΕ — αυστηρά προσθετική δίπορτη αναζήτηση** (`src/i18n/compat-translate.ts`):
ο στόχος **διατηρεί την προτεραιότητά του** (μηδέν αλλαγή σε ό,τι λύνεται σήμερα), και το
αρχικό κλειδί ρωτιέται **μόνο** όταν ο στόχος επιστρέψει ανεπίλυτο. Δεν μπορεί να αλλάξει
καμία συμβολοσειρά που επιλύεται — μόνο να μετατρέψει ωμό κλειδί σε μετάφραση. Είναι το ίδιο
δόγμα που το `slice-build.js` έχει ήδη γραμμένο: *«can only ever ADD correct strings … it can
never remove one»*.

### 13.6 Επαλήθευση — ζωντανή, με παρονομαστή

| μέτρηση | πριν | μετά |
|---|---|---|
| Κλάση Α *(απρόσιτα, ζωντανά)* | **168** | **0** |
| Κλάση Γ με **διαφορετική** τιμή *(el)* — πρέπει να **ΜΗΝ** αλλάξουν | — | **83/83 αμετάβλητα** |
| `contacts:esco.badge` στην οθόνη | `esco.badge` | **«ESCO»** |
| `contacts:common.clear` | `common.clear` | **«Καθαρισμός»** |
| τα 3 αδέλφια που **όντως** μετακόμισαν | σωστά | **σωστά** |

**SSoT**: ο έλεγχος «ανεπίλυτο κλειδί;» απέκτησε **δεύτερο** καλούντα, οπότε εξήχθη σε
`src/i18n/unresolved-key.ts` αντί να αντιγραφεί *(ADR-749)*. Τα δύο πληρωμένα σχόλια
*(ADR-635 Φ C.23 · ADR-739 §27.16 Ε5)* μετακόμισαν **αυτούσια**.

**Άγκυρα**: `src/i18n/__tests__/compat-translate.test.ts` — **15 tests** πάνω σε
**πραγματικά** locale και **πραγματικό** remap, **6/7 μεταλλάξεις κόκκινες**.
⚠️ **Η `Μ4` είναι ΔΗΛΩΜΕΝΗ ΙΣΟΔΥΝΑΜΗ**: το τελικό `return viaCompat` → `return viaOriginal`
δεν κοκκινίζει τίποτα, και **δεν είναι κενό δικτύου** — είναι ιδιότητα του i18next: σε
αστοχία επιστρέφει το **γυμνό** κλειδί, που είναι **η ίδια συμβολοσειρά** και για τα δύο
ερωτήματα. Άγκυρα που θα το «έπιανε» θα απαιτούσε ψεύτικο μεταφραστή που **δεν** τηρεί το
συμβόλαιο του i18next — δηλαδή θα δοκίμαζε κόσμο που δεν υπάρχει.
⚠️ **Δύο μεταλλάξεις επέζησαν στην πρώτη εκτέλεση** και **διορθώθηκε ο ΣΧΕΔΙΑΣΜΟΣ της
άγκυρας, ποτέ το test**: το `Κ3` ρωτούσε μόνο κλειδί που **λύνεται**, όπου η συντόμευση
«κανένα remap» είναι αδιάφορη· το κλειδί που **αστοχεί** είναι το μόνο που τη ξεχωρίζει.

### 13.7 Δηλωμένα όρια

🔶 **4 κλειδιά (el)** όπου το αρχικό κρατά **string** και ο στόχος **αντικείμενο** στην ίδια
διαδρομή. Εκεί ο στόχος **απαντά** (με αντικείμενο), άρα δεν είναι «ανεπίλυτο» και η
συμπεριφορά μένει **ακριβώς όπως πριν**. Είναι **σκίαση τύπου**, όχι ωμό κλειδί, και **δεν
το θεραπεύει** αυτή η αλλαγή. Κλειδώνεται από την άγκυρα `Κ7` ώστε να μη «διορθωθεί» κατά
λάθος από αυτόν τον μηχανισμό.

🔶 **Ανοιχτό — απόφαση Giorgio**: χρειάζεται **πύλη** γι' αυτή την κλάση; Η άγκυρα `Λ2`
τρέχει ήδη στη σουίτα jest *(που το CHECK 3.54 αποδεικνύει ότι μπλοκάρει άνευ όρων)* και
κοκκινίζει αν κάποιος ξαναγυρίσει τη λογική σε μία πόρτα. **Καμία νέα πύλη, κανένα νέο
workflow, καμία baseline.**

---

## 14. 🎨 ΤΟ BADGE ΤΑΞΙΝΟΜΗΣΗΣ ΗΤΑΝ **ΑΟΡΑΤΟ**, ΟΧΙ ΔΥΣΑΝΑΓΝΩΣΤΟ

*Φάση 5, 2026-08-24. Αναφέρθηκε από τον Giorgio ζωντανά.*

Το `EscoOccupationPicker` ζωγράφιζε χειροποίητο `<span … bg-[hsl(var(--bg-info))]/20
text-primary>`. **Μετρημένο στην οθόνη**: χρώμα κειμένου `rgb(29,40,58)` — **ταυτόσημο** με
την επιφάνεια ⇒ **1,00:1**, και **1,01:1** πάνω στο ίδιο το chip. Είναι η κλάση που
τεκμηριώνει το **CHECK 3.38 / ADR-770** *(στο προεπιλεγμένο σκοτεινό θέμα το `--primary`
λύνεται ταυτόσημα με το `--card`)*.

⚠️ **Η ΠΡΟΦΑΝΗΣ ΔΙΟΡΘΩΣΗ ΜΕΤΡΗΘΗΚΕ ΚΑΙ ΑΠΟΡΡΙΦΘΗΚΕ**:

| υποψήφιο | σκοτεινό | φωτεινό |
|---|---|---|
| `text-primary` *(σήμερα)* | **1,00** ❌ | 15,65 |
| `text-[hsl(var(--text-info))]` *(το κάνει ο **αδελφός** `EmployerPicker`)* | 5,77 | **3,19** ❌ |
| `text-foreground` | 14,37 | 15,81 — αλλά χάνει τη σημασία «info» |
| ✅ **Badge SSoT `variant="info"`** *(`--status-info` + λευκό = ζεύγος `on-*`)* | **5,17** | **5,17** |

Το `--text-info` θα **αντάλλασσε το ένα θέμα με το άλλο**. Το ζεύγος `on-*` (Material 3)
είναι σωστό **εκ κατασκευής** και στα δύο — ακριβώς η θεραπεία που ονομάζει το CLAUDE.md.
Ταυτόχρονα φεύγει ένα χειροποίητο badge υπέρ του κεντρικού *(N.0.2)*.

🔑 **Και ένα δεύτερο ελάττωμα που φαίνεται μόνο σε στενή οθόνη**: το badge πιάνει `right-10`
(40px) **+ 53px πλάτος = 93px**, ενώ το `selectedInputPadding` ήταν `pr-16` = **64px** —
δηλαδή **μικρότερο από το ίδιο το badge**, οπότε μεγάλο κείμενο περνούσε **από κάτω** του.
Έγινε `pr-24` (96px), **μετρημένο ζωντανά**: απαιτούμενο 93 ≤ διαθέσιμο 96.

⚠️ **ΜΗΝ** το «λύσεις» αλλάζοντας το `--primary` — απορρίφθηκε γραπτώς στο **ADR-682 §5.5**.
🔶 **Παρατήρηση προς έλεγχο**: ο αδελφός `EmployerPicker` χρησιμοποιεί
`text-[hsl(var(--text-success))]` *(8,99 σκοτεινό)*· **δεν** μετρήθηκε στο φωτεινό και **δεν
αγγίχθηκε** — είναι άλλη οθόνη, άλλη απόφαση.

---

## 15. 🔴 ΤΑ ΠΕΔΙΑ ΟΝΟΜΑΤΟΣ ΞΕΚΙΝΟΥΣΑΝ **ΚΕΝΑ** — το ίδιο σφάλμα, σε διπλανό πεδίο

*Φάση 5, 2026-08-24. Βρέθηκε κατά τη μέτρηση, εγκρίθηκε από τον Giorgio.*

Το `ProfilePageContent` αρχικοποιούσε `useState(user?.givenName || '')`, που **παγώνει στην
πρώτη απόδοση** — ενώ το προφίλ φτάνει από το `syncUserProfileToFirestore` **μετά** το
`onAuthStateChanged`. **Μετρημένο ζωντανά**: στην πρώτη φόρτωση τα δύο πεδία ήταν **κενά**
ενώ δίπλα τους ζωγραφιζόταν `«Georgios Pagonis»` από το `displayName`.

Είναι **race, όχι σταθερό** — γι' αυτό ξέφυγε: σε γρήγορη επαναφορά συνεδρίας γεμίζουν
κανονικά. Και είναι **ακριβώς** το σφάλμα που το ίδιο αρχείο είχε ήδη θεραπεύσει, **30
γραμμές πιο κάτω, για το ΕΠΑΓΓΕΛΜΑ** — αλλά όχι για τα ονόματα.

Η συνέπεια δεν είναι αισθητική: κενό πεδίο ονόματος **καλεί** τον άνθρωπο να ξαναγράψει, ή να
πατήσει «Αποθήκευση» πάνω σε κενό. Το `applyProfileNames` μαθαίνει ήδη ότι *«η κενή φόρμα δεν
είναι εντολή διαγραφής»* *(§12 changelog)*, οπότε **τα δεδομένα ήταν ασφαλή — η οθόνη όμως
έλεγε ψέματα**.

⚠️ **ΟΙ ΕΞΑΡΤΗΣΕΙΣ ΕΙΝΑΙ ΟΙ ΤΙΜΕΣ, ΠΟΤΕ ΤΟ ΑΝΤΙΚΕΙΜΕΝΟ `user`.** Η ταυτότητά του αλλάζει σε
κάθε ανανέωση συνεδρίας· effect πάνω της θα **έσβηνε ό,τι πληκτρολογεί** ο άνθρωπος τη στιγμή
της ανανέωσης. Με πρωτογενείς τιμές το effect ξανατρέχει **μόνο** όταν αλλάξει πραγματικά το
αποθηκευμένο όνομα: στην πρώτη άφιξη, και μετά την αποθήκευση.

---

## 16. 🖼️ Η ΦΩΤΟΓΡΑΦΙΑ ΠΡΟΦΙΛ — και πού **ξεπερνάμε** τους μεγάλους

*Φάση 5, 2026-08-24. Εντολή Giorgio: πλήρης υλοποίηση, επίπεδο μεγάλων, χωρίς εκπτώσεις.*

### 16.1 Η αφετηρία

`ProfilePageContent.tsx:125` → κουμπί κυριολεκτικά `disabled`, **κανένας handler**, και από
κάτω `photoHint` = «Κάντε κλικ για αλλαγή φωτογραφίας». Η οθόνη **υποσχόταν κάτι που δεν
γινόταν**.

### 16.2 Το SSoT audit — τι υπήρχε ήδη

✅ `services/upload/image-asset-upload.ts` — uploader **SSoT** *(validate → `uploadBytes` →
`getDownloadURL`)*. ⛔ **Κανένας δεύτερος uploader δεν γράφτηκε.**
✅ Ακριβές δομικό προηγούμενο: **σφραγίδα μηχανικού**, per-user εικόνα, ντετερμινιστικό path.
✅ `Dialog` + `Slider` SSoT ⇒ **μηδέν νέα εξάρτηση** *(άρα και κανένας έλεγχος αδείας N.5)*.
❌ Έλειπαν: path builder, μπλοκ `storage.rules`, ενέργεια auth, UI, i18n.

### 16.3 🏆 Πού ξεπερνάμε τους μεγάλους

**Το μοτίβο** είναι των μεγάλων — Figma: κυκλική προεπισκόπηση, σύρσιμο, **ρυθμιστής ζουμ**,
«Save image». Δεν εφευρίσκουμε αλληλεπίδραση που κανείς δεν έχει ξαναδεί.

**Η διαφορά είναι στα bytes**: οι μεγάλοι ανεβάζουν το **αρχείο του χρήστη** και το
επεξεργάζονται στον διακομιστή. Εδώ **δεν ανεβαίνει ΠΟΤΕ το αρχείο του χρήστη** — ανεβαίνει
καρέ που παρήγαγε ο **encoder του browser** από pixels που ζωγραφίσαμε εμείς. Αυτό ακυρώνει
**τέσσερις κατηγορίες** *by construction*, όχι με πολιτική που κάποιος πρέπει να θυμάται:

| κίνδυνος | γιατί είναι **αδύνατος** |
|---|---|
| **Διαρροή GPS/EXIF** | το canvas ξαναζωγραφίζει **μόνο pixels**. Δεν «αφαιρούμε» EXIF — **δεν το μεταφέρουμε ποτέ**. Μια γεωσημασμένη φωτογραφία κινητού εντοπίζει το **σπίτι** του ανθρώπου, και η έρευνα δείχνει μεγάλες πλατφόρμες να αστοχούν εδώ. |
| **Polyglot / ενσωματωμένο φορτίο** | τα bytes τα παράγει ο `toBlob`. Ο έλεγχος «magic bytes» που προτείνουν οι οδηγοί ασφαλείας είναι **ασθενέστερος**: κρίνει τα bytes του χρήστη και μετά **τα ανεβάζει**. |
| **Βόμβα αποσυμπίεσης** | η έξοδος είναι **σταθερή 512×512**, ό,τι κι αν ήταν η είσοδος. |
| **Απρόβλεπτο μέγεθος/μορφή** | πάντα ένα τετράγωνο WebP· μετρήθηκε **2.556 bytes**. |

⚠️ **ΜΗΝ «βελτιστοποιήσεις» ανεβάζοντας το αρχικό αρχείο όταν είναι ήδη μικρό** — θα
επανέφερε **και τις τέσσερις** γραμμές, σιωπηλά, μόνο για μικρές φωτογραφίες.

⚠️ **Ο προσανατολισμός EXIF είναι το ΑΝΤΙΘΕΤΟ**: τα μεταδεδομένα τοποθεσίας πρέπει να
**χαθούν**, η ετικέτα *Orientation* πρέπει να **εφαρμοστεί** — αλλιώς η φωτογραφία κινητού
βγαίνει γυρισμένη 90°. Το `createImageBitmap(file, { imageOrientation: 'from-image' })` την
εφαρμόζει **πριν** το canvas.

**Δεύτερη υπεροχή — WYSIWYG εκ κατασκευής**: η προεπισκόπηση ζωγραφίζεται με το **ίδιο**
`computeSourceRect` που παράγει το αρχείο. Δύο μαθηματικά θα ήταν **δύο αλήθειες ελεύθερες να
αποκλίνουν** *(ADR-749)*, και η απόκλιση θα φαινόταν **μετά** την αποθήκευση.

### 16.4 🔴 Η εμβέλεια — **δεν είναι προτίμηση, είναι αναγκασμένη**

Κάθε άλλο path είναι `companies/{companyId}/…`. Η φωτογραφία **δεν μπορεί** να είναι: ο
δείκτης της ζει στο **Firebase Auth** (`photoURL`) και είναι **ΜΙΑ τιμή ανά άνθρωπο,
ΚΑΘΟΛΙΚΗ**. Αν τα bytes έμπαιναν σε `companies/{A}/…`, ο ίδιος άνθρωπος στον χώρο **{B}** θα
ζητούσε object του {A} και το `belongsToCompany(A)` θα το **αρνιόταν** ⇒ **σπασμένο avatar
κατ' ανάγκη**. Η σφραγίδα μηχανικού είναι **σωστά** εταιρική: είναι παραδοτέο του γραφείου.
**Η φωτογραφία είναι ο άνθρωπος.**

Path: `users/{userId}/avatar.{webp|png}` — ντετερμινιστικό ⇒ **ιδεμπόταντ**, μηδέν ορφανά.

**Κανόνες** *(νέο σχήμα `authenticated_read_owner_write`)*: read = `isAuthenticated()` —
ο **αυστηρότερος εκφράσιμος**, αφού οι Storage rules **δεν μπορούν να διαβάσουν Firestore**
και το `companyId` δεν υπάρχει στο path· ⚠️ παραμένει **αυστηρότερο από σήμερα**, όπου το
`photoURL` δείχνει σε **δημόσιο** `lh3.googleusercontent.com`. Write = **μόνο ο ίδιος**,
ούτε super_admin. Delete = ιδιοκτήτης **ή** super_admin *(αφαίρεση ακατάλληλης εικόνας είναι
νόμιμη διαχειριστική πράξη· η αντικατάσταση όχι)*.

⚠️ Το **CHECK 3.19 μπλόκαρε αμέσως** τον νέο κανόνα ως ακάλυπτο — και γράφτηκε **πραγματική
σουίτα** *(`user-avatars.storage.test.ts`)*, όχι εγγραφή σε `STORAGE_RULES_PENDING` όπως είχε
γίνει για τη σφραγίδα.

### 16.5 `null` ≠ «καμία φωτογραφία»

Το `updateUserPhoto(null)` **επαναφέρει την εικόνα του παρόχου**, δεν αφήνει κενό: ο άνθρωπος
ζήτησε να αφαιρέσει **τη δική του**, και είχε ήδη μία από το Google. Η επαναφορά διαβάζεται
από το `providerData`, που **δεν το πειράζει** το `updateProfile`.
⚠️ **Το Google δεν αγγίζεται ΠΟΤΕ**: είναι **πάροχος ταυτότητας** — δίνει δεδομένα στη
σύνδεση, δεν τα παίρνει πίσω *(ρωτήθηκε ρητά από τον Giorgio)*.

### 16.6 🔴 Το σφάλμα που **μόνο η οθόνη** έδειξε

Η προεπισκόπηση **δεν ζωγραφιζόταν ΠΟΤΕ**. Ο καμβάς ζει σε Radix Dialog *(portal)*, οπότε
την ώρα που έτρεχε το effect το `canvasRef.current` ήταν `null`· το effect έκανε **σωστά**
early-return, και οι εξαρτήσεις του **δεν άλλαζαν ποτέ ξανά** ⇒ δεύτερη ευκαιρία δεν υπήρξε.

Μετρημένο: ο καμβάς έμενε **300×150** — το *προεπιλεγμένο* μέγεθος του HTML — με μαύρα
pixels. **Ένα κενό πλαίσιο που δεν σκάει πουθενά, δεν κοκκινίζει κανένα test, και φαίνεται
μόνο αν δειγματοληπτήσεις pixel.** Θεραπεία: **callback ref**.

### 16.7 Ζωντανή επαλήθευση — **end-to-end, με πραγματικό Firebase**

| έλεγχος | αποτέλεσμα |
|---|---|
| Κουμπί | `disabled: false` ⇒ ενεργό |
| Διάλογος | ανοίγει, **όλα τα κείμενα μεταφρασμένα** |
| Μαθηματικά περικοπής *(δειγματοληψία pixel σε εικόνα-δοκιμής)* | κέντρο **πράσινο**, αριστερά **κόκκινο**, δεξιά **μπλε** — ακριβώς ο υπολογισμός |
| Ανέβασμα | ✅ *(οι κανόνες είχαν γίνει deploy)* |
| Αποθηκευμένο object | `image/webp`, **512×512**, **2.556 bytes** |
| Περικοπή αποθηκευμένου vs προεπισκόπηση | **ταυτόσημη** ⇒ WYSIWYG **αποδεδειγμένο** |
| Επιμονή μετά reload | ✅ |
| «Αφαίρεση» | επέστρεψε στη **φωτογραφία Google**, κουμπί εξαφανίστηκε, μηδέν σφάλματα |
| Storage object μετά την αφαίρεση | **403** ⇒ σβήστηκε |

### 16.8 Δηλωμένα όρια

🔶 Δεν δοκιμάστηκαν ζωντανά: **HEIC** από iPhone *(ο browser μπορεί να μην το
αποκωδικοποιεί — τότε εμφανίζεται καθαρό μήνυμα, όχι σιωπή)*, οθόνη **αφής**, και **πολύ
μεγάλες** εικόνες κοντά στο όριο των 12 MB.
🔶 Η σουίτα `user-avatars.storage.test.ts` απαιτεί **Storage emulator** — δεν εκτελέστηκε σε
αυτή τη συνεδρία.

## 17. 🔴 Η ΦΑΣΗ 3 ΗΤΑΝ **ΑΔΡΑΝΗΣ ΦΡΟΥΡΟΣ** — και η §7 μια **ανεκπλήρωτη υπόσχεση**

*Φάση 6, 2026-08-25. Και τα δύο βρέθηκαν **ζωντανά**, με τον dev server ανοιχτό.*

### 17.1 Το γεγονός

Ο Giorgio δήλωσε **«μηχανικός αιολικής ενέργειας»** στο `/account/profile`. Η βάση
επιβεβαίωσε ότι γράφτηκαν **και τα τέσσερα** πεδία μαζί *(`iscoCode: "2149"` ·
`escoUri: …/1b99cad3-…`)*. Ο πίνακας συγγένειας λέει ρητά `'214' → design`.

**Και η κεφαλίδα εξακολουθούσε να προτείνει «Διαχείριση».**

### 17.2 🔑 Η ρίζα — **ασυμφωνία μέσα στο ίδιο αρχείο**

| συνάρτηση *(`jobs-access.ts`)* | ρωτά «τι είναι **μετρημένο** δικαίωμα;» |
|---|---|
| `decideJobAccess` | ✅ ξεχωρίζει ρητά `{kind:'bypass'}` από `{kind:'permission'}` |
| `pickDefaultJob` | ❌ **δεν ρωτούσε** — μετρούσε ωμά το `input.permissions` |

Ο `super_admin` έχει `permissions: []` *(`roles.ts:58`)*, αλλά το
`useEffectivePermissions:91-93` του προσθέτει **πάντα** `admin_access` — **σωστά**, το
χρειάζεται για **ορατότητα**. Παρασιτικά όμως το `admin_access` ζει σε **ακριβώς μία**
από τις έξι δουλειές ⇒ σκορ `{administration: 1, άλλες: 0}` ⇒ **μοναδικός νικητής** ⇒ ο
έλεγχος `if (score === bestScore && job === tiebreak)` **δεν εκτελούνταν ΠΟΤΕ**.

**Μετρημένο: 0 στους 4** ζωντανούς λογαριασμούς μπορούσαν να το πυροδοτήσουν ⇒ **ADR-749 §5**.

### 17.3 🔴 Γιατί καμία άγκυρα δεν το είδε — **το fixture δοκίμαζε κόσμο που δεν υπάρχει**

```ts
/** Ο υπερδιαχειριστής: `permissions: []` + bypass ⇒ και οι έξι δουλειές. */
const BYPASS = access({ isBypass: true });
```

Με **κενά** permissions όλα ισοβαθμούν στο 0, οπότε το tiebreak **δούλευε** — οι άγκυρες
ήταν **πράσινες πάνω σε μηχανισμό που ζωντανά ήταν νεκρός**. Το σχόλιο, γραμμένο με
βεβαιότητα, **ήταν η ίδια η απόκλιση** — σχήμα **CHECK 3.34** (63) · **3.37** (18 vs 26) ·
**3.57** (19/20).

### 17.4 Η θεραπεία, και γιατί **δεν** παραβιάζει το Α4

`const counted = input.isBypass ? [] : input.permissions;`

Το `available` υπολογίζεται **πριν** και **χωρίς** αυτό, και ο bypass χρήστης έχει **ήδη
και τις έξι**. Το `tiebreak` διαλέγει **ανάμεσα σε όσες ήδη δικαιούται** — καμία διεύρυνση.

| σενάριο | ΠΡΙΝ | ΜΕΤΑ |
|---|---|---|
| super_admin **μηχανικός** *(214)* | administration | ✅ **design** |
| super_admin **δικηγόρος** *(2611)* | administration | ✅ **clients** |
| super_admin **χωρίς** δήλωση | administration | ⚠️ **design** *(σειρά μητρώου, Ε7.δ)* |
| `company_admin` *(όχι bypass)* | administration | ✅ **αμετάβλητο** |

⚠️ **ΔΗΛΩΜΕΝΗ ΣΥΝΕΠΕΙΑ**: η τρίτη γραμμή **αλλάζει**, σκόπιμα — το «Διαχείριση» ήταν
**παρενέργεια** ενός fallback *ορατότητας*, που κανείς δεν αποφάσισε ως προεπιλογή.
🔶 **ΑΝΟΙΧΤΟ (απόφαση προϊόντος)**: και οι δύο τιμές είναι **μαντεψιά** για bypass χωρίς
δήλωση· το τιμιότερο θα ήταν **σιωπή**, όπως ήδη κάνει το `unknown` του Ε7.γ′.

### 17.5 Η §7 ήταν **ανεκπλήρωτη** — μετρημένο

Το §7 λέει κατά λέξη *«η οθόνη δείχνει **ΠΑΝΤΑ** ποια από τις τρεις ισχύει»*. Δεν έδειχνε **καμία**:

| Τι | Καταναλωτές παραγωγής |
|---|---|
| `OccupationConfidence` | **0** |
| `isClassified` | **0** *(11 εμφανίσεις: 5 ορισμός + 6 tests)* |
| `useDeclaredOccupation()` | **1** κλήση — και παίρνει **μόνο** `iscoCode` |
| `.tsx` που αναφέρουν `occupation` | **3** — **και τα 3 ο γραφέας** |

*(Παρονομαστής: το ίδιο grep βρίσκει **100+** `.confidence` αλλού ⇒ το όργανο βλέπει.)*

**Θεραπεία**: `components/header/DeclaredOccupationBadge.tsx`, μέσα στο **UserMenu** —
δείχνει *τι δήλωσε* · *πόσο το εμπιστευόμαστε* · **ποια δουλειά υποδεικνύει** *(η συνέπεια,
από τον **ίδιο** πίνακα που την εκτελεί ⇒ δεν μπορεί να αποκλίνει)*.

⚠️ **ΚΑΜΙΑ ερώτηση, κανένα modal** *(Ε7.γ′ · Α5)*: το `unknown` γίνεται **πρόταση**, μέσα
σε μενού που άνοιξε ο ίδιος. ⚠️ **ΟΧΙ `text-primary`** *(CHECK 3.38 — 1,00:1 στο σκοτεινό)*·
κάθε κατάσταση φέρει **εικονίδιο ΚΑΙ κείμενο** *(CHECK 3.41 / WCAG 1.4.1)*.

### 17.6 Απόδειξη

**Ο Α**: 7 νέες άγκυρες με **ρεαλιστικό** `BYPASS_LIVE` · μετάλλαξη **GREEN → RED (3) → GREEN**.
**Ο Β**: 13 άγκυρες · **5/5 μεταλλάξεις κόκκινες**, Μ0 πράσινο **πριν ΚΑΙ μετά**.
Regression **2.842 πράσινα / 40 σουίτες** · `jscpd` **0 clones** · i18n el/en ταυτόσημα
*(και **2 νεκρά κλειδιά** που είχα δηλώσει «για πληρότητα» αφαιρέθηκαν)*.

---

### 17.7 🏆 ΤΟ ΑΝΟΙΧΤΟ ΕΚΛΕΙΣΕ ΜΕ ΕΡΕΥΝΑ — **η σιωπή του πλήρους κατόχου**

*Ο Giorgio ζήτησε ρητά: «αν οι μεγάλοι δεν το προτείνουν, ακολουθούμε την πρακτική τους».*

Το §17.4 άφησε ανοιχτό: *bypass **χωρίς** δήλωση — τι προτείνουμε;* Η έρευνα το έκλεισε,
με τις πηγές διαβασμένες **ολόκληρες**.

| Παίκτης | Τι κάνει όταν δεν έχει δηλωθεί ειδίκευση |
|---|---|
| **Revit** *(Autodesk)* | «if you do not select a discipline … **Autodesk Revit: All**; Revit Architecture: Architectural; Revit MEP: Mechanical» ⇒ η **ειδικότητα** είναι για τις **περιορισμένες** άδειες· ο κάτοχος της **πλήρους** παίρνει **«All»** |
| **ArchiCAD** *(Graphisoft)* | Πρώτη εκκίνηση ⇒ **Standard**· τα ειδικευμένα *(Visualization · Layouting)* απαιτούν **ρητή** επιλογή |
| **Cinema 4D** *(Maxon)* | **Standard** layout· τα ειδικευμένα απαιτούν ρητή επιλογή |

⇒ **Ομόφωνο**: όποιος δεν δήλωσε ειδίκευση παίρνει το **ΓΕΝΙΚΟ**, ποτέ αυθαίρετη
ειδικότητα. Εδώ το γενικό είναι το `JOB_ALL` — και είναι **ήδη** η ενεργή κατάσταση ⇒
**σιωπή**.

⚠️ **Η κομμένη παράθεση ξαναχτύπησε**: η πρώτη αναζήτηση επέστρεψε «Architectural» ως
προεπιλογή. Είναι **ακριβώς** το σφάλμα που το **Ε7.δ′** είχε ήδη πληρώσει μία φορά —
η πλήρης πρόταση λέει **«Autodesk Revit: All»**. *Διαβάζεται ολόκληρη ή καθόλου.*

**Ο μηχανισμός** *(`job-suggestion.ts`, μία γραμμή με τον λόγο από πάνω)*:

```ts
const hasOccupationSignal = input.tiebreak !== null && input.tiebreak !== undefined;
if (input.access.isBypass && !hasOccupationSignal) return null;
```

🔑 **Γιατί χρειάζεται ΡΗΤΟΣ έλεγχος**: για `isBypass` **καμία** δουλειά δεν έχει
μετρημένο δικαίωμα *(§17.4)* ⇒ **όλες ισοβαθμούν** ⇒ χωρίς `tiebreak` ο νικητής βγαίνει
από τη **σειρά του μητρώου**, δηλαδή από **τίποτα**. Ο υπάρχων φρουρός «δεν το κρύβω ≠
το προτείνω» **δεν** το πιάνει: για bypass κάθε δουλειά είναι `granted`
*(`decideJobAccess`, κλάδος 1)* — κλειδωμένο με δική του άγκυρα.

#### 🏆 Πού **ξεπερνάμε** — τρία σημεία

1. **Ο Revit σταματά στο «All»** και δεν ξαναρωτά ποτέ. Εδώ η σιωπή γίνεται
   **πρόσκληση**: το `DeclaredOccupationBadge` λέει «πες μας τι κάνεις» **μέσα στο μενού
   που άνοιξε ο ίδιος** — χωρίς modal *(Ε7.γ′ · Α5)*. Μόλις δηλώσει, ο **ίδιος** κώδικας
   αρχίζει να προτείνει.
2. **Κανείς τους δεν συνδέει επάγγελμα με επιφάνεια.** Ο Revit ρωτά «ειδικότητα»· εμείς
   χαρτογραφούμε **ESCO/ISCO-08** → δουλειά, με **436** unit groups αντί για 4 disciplines.
3. **Η δική τους προεπιλογή είναι στατική**· η δική μας είναι **υπολογισμένη** και
   **αναστρέψιμη**, με τον αριθμό να βγαίνει από τις **ίδιες** συναρτήσεις που βάφουν.

⚠️ **ΔΗΛΩΜΕΝΟ ΟΡΙΟ**: ο κανόνας αφορά **μόνο** τον bypass. Όποιος έχει **μετρημένα**
δικαιώματα παίρνει πρόταση και χωρίς δήλωση — εκεί ο σηματοδότης **υπάρχει**.

🔴 **ΚΑΙ ΤΟ FIXTURE ΞΑΝΑΕΠΕΣΕ ΣΤΗΝ ΙΔΙΑ ΠΑΓΙΔΑ**: ο helper `suggest()` δεν περνούσε
`tiebreak`, οπότε **τέσσερις** άγκυρες *(Σ-1 ×2 · Σ-4 · Σ-5)* δοκίμαζαν υπερδιαχειριστή
που **δεν δήλωσε ποτέ τίποτα** και όμως περίμεναν πρόταση. Καμία τους δεν αφορούσε το
bypass — χρησιμοποιούσαν τον `BYPASS` ως *«χρήστης με πολλές δουλειές»*. Διορθώθηκε **το
fixture**, όχι ο σχεδιασμός *(κανόνας: «όταν το test διαφωνεί, ρώτα ποιος έχει δίκιο»)*.

**Απόδειξη**: 4 νέες άγκυρες *(Η-1)* · **4/4 μεταλλάξεις κόκκινες** — και **οι δύο**
κατευθύνσεις *(σιωπή που λείπει · σιωπή που πλεονάζει)* · regression **2.846 / 40**.

---
### 17.8 🔴 Η ΟΘΟΝΗ ΒΡΗΚΕ ΤΡΙΤΟ — **η πρόταση ονόμαζε ΛΑΘΟΣ ΑΙΤΙΑ**

*Ζωντανή επαλήθευση, 2026-08-25 19:50-19:51. Δύο στιγμιότυπα του Giorgio.*

**Πρώτα, τα δύο που ΕΠΑΛΗΘΕΥΤΗΚΑΝ:**

| Δοκιμή | Αποτέλεσμα |
|---|---|
| **Με** δηλωμένο επάγγελμα *(2149 → 214)* | κεφαλίδα **«Σχέδιο»** ✅ *(πριν: «Διαχείριση»)* · «9 από 17 ενότητες» |
| **Σβήνοντας** τη δήλωση | **η πρόταση εξαφανίστηκε** ✅ — πρότυπο Revit «All», ζωντανά |

**Και το τρίτο ελάττωμα, που μόνο η οθόνη μπορούσε να δείξει:**

Το popover έλεγε *«Με βάση **τα δικαιώματά σου**, αυτή η δουλειά ταιριάζει
περισσότερο»* — **ψευδές**. Ο χρήστης είναι `super_admin`: **όλες** οι δουλειές έχουν
**ταυτόσημα** δικαιώματα. Η αιτία ήταν το **επάγγελμα**, και το απέδειξε το ίδιο το
πείραμα — σβήνοντας τη δήλωση η πρόταση χάθηκε, με τα δικαιώματα **αμετάβλητα**.

⚠️ **ΚΑΝΕΝΑ test δεν μπορούσε να το δει**: το κλειδί i18n ήταν **υπαρκτό** και η
μετάφραση **σωστή**. Έλεγε απλώς **λάθος πράγμα**. Είναι η τρίτη φορά που η οθόνη
βρίσκει ό,τι οι άγκυρες δεν βλέπουν *(ADR-748 §14.6: «τρία ελαττώματα που τα tests δεν
έβλεπαν»)*.

🔑 **Γιατί έχει σημασία (Υ-6)**: ένα μήνυμα που ονομάζει **λάθος** αιτία είναι χειρότερο
από κανένα — διδάσκει στον άνθρωπο **λάθος μοντέλο** του συστήματος. Θα έψαχνε τα
δικαιώματά του για να αλλάξει κάτι που ελέγχεται από το **προφίλ** του.

**Η θεραπεία** — το `JobSuggestionOutcome` αποκτά **μετρημένη** αιτία:

```ts
const withoutSignal = input.access.isBypass ? null : pickDefaultJob(input.access, null);
const basis: JobSuggestionBasis = withoutSignal === job ? 'permissions' : 'occupation';
```

Κριτήριο: *«θα έδινε η **ίδια** είσοδος **άλλη** απάντηση χωρίς τον σηματοδότη;»*

⛔ **ΜΗΝ** το γράψεις ως `isBypass ? occupation : permissions` — θα έλεγε «permissions»
σε **μη-bypass** χρήστη όπου το επάγγελμα **έσπασε** ισοβαθμία, δηλαδή λάθος στη μισή
περίπτωση. *(Κλειδωμένο με δική του μετάλλαξη, `Μ12`.)*

**Απόδειξη**: 3 άγκυρες `Θ-1` · **3/3 μεταλλάξεις κόκκινες** — ανάμεσά τους η **`Μ10`,
που είναι κατά λέξη το αρχικό ελάττωμα**. Regression **2.849 / 40** · i18n el/en
ταυτόσημα · `jscpd` **0**.

---
## 18. 🧪 ΤΟ RIG: ΟΚΤΩ ΑΝΘΡΩΠΟΙ, ΜΗΔΕΝ GMAIL (2026-08-25)

### 18.1 Η ερώτηση, και η μετρημένη απάντηση

Ο Giorgio ρώτησε αν αξίζει να φτιαχτούν **πραγματικά Gmail** για δοκιμές των ρόλων.
**Όχι** — τρεις ανεξάρτητοι λόγοι, ο καθένας αρκετός μόνος του:

1. Ο **Auth emulator** (`firebase.json`, θύρα 9099) δέχεται **οποιοδήποτε** email με
   `emailVerified: true`. Η σύμβαση υπήρχε ήδη: `demo@nestor.local` (ADR-745 Φάση Γ).
2. Η προσγείωση (`landing.ts:104`) κρίνει **ένα μπιτ** (`companyId`) ⇒ **14 λογαριασμοί θα
   έδιναν ΔΥΟ οθόνες**. Θα επιβεβαίωναν κενό που είναι ήδη μετρημένο (§1).
3. Κάθε Gmail θέλει τηλέφωνο επαλήθευσης· η Google μπλοκάρει μαζικές δημιουργίες από την ίδια IP.

### 18.2 Η σύμβαση: `<ρόλος>.<επάγγελμα>@<χώρος>.local`

Κωδικοποιεί τους **τρεις άξονες που το σύστημα κρατά χωριστά** (§0.1) — τι **δικαιούσαι** ·
τι **είσαι** · πού **ανήκεις** (στο domain).

🔑 **Το κρίσιμο ζεύγος**: `ext.architect@solo.local` έναντι `int.architect@alpha.local` —
*ίδιο* επάγγελμα (`iscoCode: 2161`), *άλλος* χώρος. Είναι η **μόνη** διαφορά που η σημερινή
προσγείωση βλέπει, άρα ο μόνος τρόπος να **απομονωθεί η μεταβλητή**.

**Ζωντανή απόδειξη** (emulator, 2026-08-25) — διαβασμένη από τη **ΒΑΣΗ**, όχι από την οθόνη:

| | claims | mirror |
|---|---|---|
| `ext.architect@solo` | `{globalRole:'external_user'}` — **κανένα** `companyId` | `profession:'Αρχιτέκτονας', iscoCode:'2161'` |
| `int.architect@alpha` | `{companyId:'comp_alpha_emulator', globalRole:'internal_user'}` | **ίδιο** επάγγελμα |
| `ext.seeker@solo` | `{globalRole:'external_user'}` | **κανένα** επάγγελμα |

✅ **Η Α4 τηρείται μηχανικά**: το επάγγελμα **δεν εμφανίζεται πουθενά στα claims**.
✅ **Idempotent**: δεύτερο τρέξιμο ⇒ **8 χρήστες**, μηδέν διπλότυπα.

### 18.3 🔴 ΤΟ CHECK 3.28 ΕΠΕΒΑΛΕ ΤΗΝ ΑΡΧΙΤΕΚΤΟΝΙΚΗ, ΔΥΟ ΦΟΡΕΣ

Ο πρώτος σπορέας είχε τον φρουρό, το `ensureUser` και τα claims **μέσα** του. Ο δεύτερος τα
χρειαζόταν όλα. Το jscpd ανέφερε κλώνους **δύο φορές**, και κάθε φορά η θεραπεία ήταν εξαγωγή:

1. `assertEmulatorsUp` + `ensureUser` + claims ⇒ `scripts/lib/emulator/identity.ts`
2. Η **ακολουθία εκκίνησης** ⇒ `runSeeder(seed)`, που δίνει `auth`/`db` **έτοιμα**

🔑 Η δεύτερη είναι η καλύτερη: ο σπορέας **δεν έχει τρόπο να συνδεθεί χωρίς τον φρουρό**.

### 18.4 🔴 Η ΠΡΩΤΗ ΑΓΚΥΡΑ ΑΠΕΔΕΙΞΕ ΟΤΙ ΤΑ ΔΕΔΟΜΕΝΑ ΗΤΑΝ **ΑΔΟΚΙΜΑΣΤΑ**

Εισάγοντας το `emulator-seed-personas.ts`, το `runSeeder(main)` **εκτελούνταν** σε module scope
⇒ φρουρός ⇒ `process.exit(1)` ⇒ *«Jest worker encountered 4 child process exceptions»*.

**Το άγγιγμα των δεδομένων ήταν πράξη.** Θεραπεία: `scripts/lib/emulator/personas.ts` —
**κάτω τα δεδομένα, πάνω οι πράξεις**, η ίδια τομή με το `lib/auth/role-catalogue.ts` (§2.11).

### 18.5 Η κύρια άγκυρα: **ένας κωδικός ISCO που δεν λύνεται δεν σπάει τίποτα**

Απλώς **δεν κάνει τίποτα** — η προσωπικότητα γίνεται σιωπηλά ισοδύναμη με «χωρίς επάγγελμα»,
το script τυπώνει ✅, και το rig δοκιμάζει **κόσμο που δεν υπάρχει**. Ακριβώς η βλάβη του §17.3.

Γι' αυτό το `Κ2` περνά κάθε `iscoCode` από τον **πραγματικό** `resolveJobAffinity`, και το `Κ3`
απαιτεί **≥3 διαφορετικές** δουλειές — αλλιώς οι οκτώ είναι αντίγραφα ενός σεναρίου.

**Απόδειξη: 3/3 μεταλλάξεις κόκκινες** — άκυρος κωδικός ⇒ `Κ2` · όλοι ίδιο επάγγελμα ⇒ `Κ3` ·
ο αυτόνομος αποκτά εταιρεία ⇒ `Κ4`. Άγκυρες **7**, `npx jest scripts/__tests__/emulator-seed-personas.test.ts`.

### 18.6 Δηλωμένα όρια

1. Το rig φτιάχνει **ταυτότητες**, όχι δεδομένα τομέα. Ο αδελφός σπορέας (ADR-745) κρατά αυτόν
   τον ρόλο και **δεν άλλαξε συμπεριφορά** — επαληθεύτηκε εκτελώντας τον.
2. Οι άγκυρες κρίνουν τα **δεδομένα**, όχι τη σπορά: ότι ο σπορέας γράφει σωστά το απέδειξε η
   **ζωντανή** ανάγνωση του §18.2, όχι test.
3. Το `PERSONAS` **δεν** καλύπτει συμβολαιογράφο · διακοσμητή · μεσίτη · προμηθευτή. Είναι
   **επιλογή**: οκτώ ταυτότητες που καλύπτουν τέσσερις δουλειές, όχι κατάλογος επαγγελμάτων.

---

## 19. 🔴 ΤΟ RIG ΔΟΚΙΜΑΖΕ ΤΟΝ **ΠΙΝΑΚΑ**, ΟΧΙ ΤΗ **ΔΙΑΔΡΟΜΗ** (2026-08-26)

### 19.1 Το γεγονός — μία άγκυρα πράσινη, οκτώ οθόνες σιωπηλές

Το `Κ2` του §18.5 γράφτηκε **ακριβώς** για να μην είναι μια προσωπικότητα «σιωπηλά ισοδύναμη με
χωρίς επάγγελμα», και περνούσε **7/7**. Μετρημένο 2026-08-26, τρέχοντας τα **πραγματικά** δεδομένα
του rig μέσα από τον **πραγματικό** `useDeclaredOccupation`:

| persona | `Κ2` *(απευθείας στον πίνακα)* | `isClassified` | `hook.iscoCode` | η **ΔΙΑΔΡΟΜΗ** |
|---|---|---|---|---|
| `ext.architect` | `design` | `false` | `null` | 🔴 **ΣΙΩΠΗ** |
| `ext.lawyer` | `clients` | `false` | `null` | 🔴 **ΣΙΩΠΗ** |
| `int.architect` | `design` | `false` | `null` | 🔴 **ΣΙΩΠΗ** |
| `int.surveyor` | `design` | `false` | `null` | 🔴 **ΣΙΩΠΗ** |
| `int.accountant` | `finance` | `false` | `null` | 🔴 **ΣΙΩΠΗ** |
| `admin.civil` | `design` | `false` | `null` | 🔴 **ΣΙΩΠΗ** |

**6/8 λύνονταν στον πίνακα σε 3 δουλειές· 8/8 έφταναν στην οθόνη ως σιωπή.**

🔑 **Η αιτία δεν είναι σφάλμα του αναγνώστη — είναι το σωστό του.** Το `Ρ-4` (§7) ορίζει ότι το
`escoUri` **φυλάει** το `iscoCode`: ορφανός κωδικός σημαίνει εγγραφή που **δεν πέρασε από τον
picker**, δηλαδή προέλευση που δεν μπορούμε να βεβαιώσουμε. Ο μοναδικός γραφέας της παραγωγής
(`EscoOccupationPicker`) εκπέμπει **και τα τέσσερα πεδία ή κανένα**.

🔴 **Ο σπορέας παρήγαγε άνθρωπο που ο picker δεν θα μπορούσε ΠΟΤΕ να δημιουργήσει** — έγραφε
`profession` + `escoLabel` + `iscoCode` και **ποτέ** `escoUri`, επειδή τα τρία ήταν
**προαιρετικά** στο `SeedOccupation`.

> Δύο όργανα, δύο απαντήσεις για το ίδιο δέντρο, **καμία σύγκριση** — το σχήμα του **ADR-749**.
> Ο φρουρός δεν ήταν αδρανής· ήταν **στραμμένος αλλού**.

⚠️ Και δεν ήταν μόνο το `escoUri`: ο λογιστής σπερνόταν με `iscoCode: '241'`, την **ελάσσονα
ομάδα** — τιμή που **κανένα** ESCO occupation δεν κουβαλά. Η δουλειά (`finance`) δεν άλλαξε, γιατί
το `2411` λύνεται μέσω του **ίδιου** προθέματος· η **προέλευση** όμως ήταν αδύνατη.

### 19.2 Θεραπεία — δομική, όχι ανιχνευτική

Τα τρία πεδία έγιναν **υποχρεωτικά** στο `SeedOccupation`. Η απόκλιση παύει να είναι εύρημα
άγκυρας και γίνεται **σφάλμα μεταγλώττισης**: ορφανός κωδικός είναι πλέον **μη εκφράσιμος**.

**Και τα δέκα ESCO URI τα απάντησε η αυθεντία** (ESCO Web Service API, 2026-08-26) — κανένα δεν
γράφτηκε από μνήμη. Το ESCO εγγυάται ότι **κάθε occupation ανήκει σε ακριβώς μία** ομάδα ISCO-08,
άρα το ζεύγος `(escoUri, iscoCode)` είναι **ελέγξιμο**, όχι θέμα εμπιστοσύνης.

### 19.3 🔴 ΓΙΑΤΙ ΠΑΓΩΜΕΝΑ URI ΚΑΙ ΟΧΙ ΚΛΗΣΗ ΤΗΝ ΩΡΑ ΤΗΣ ΣΠΟΡΑΣ — **Η ΑΥΘΕΝΤΙΑ ΣΠΑΕΙ**

Μετρημένο, αναπαραγώγιμο 2026-08-26, πάνω στο **κεντρικότερο επάγγελμα της εφαρμογής**:

```
search?text=civil engineer&language=en&limit=3              ⇒ 200, code 2142.1
search?text=civil engineer&language=en&limit=3&full=true    ⇒ HTTP 500
resource/occupation?uri=…d7d986e1…  (el, en, χωρίς γλώσσα)  ⇒ HTTP 500
   "More than one value found for field 'hasSkillType' and language 'null'"
```

**Μία** χαλασμένη εγγραφή της αυθεντίας **μηδενίζει ολόκληρη** την απάντηση — δεν υποβαθμίζεται,
**σκάει**. Σπορέας που ρωτούσε το δίκτυο θα ήταν μη ντετερμινιστικός **και δομικά ανίκανος να
φτιάξει πολιτικό μηχανικό**.

🔑 Η παραγωγή διαβάζει τον **καθρέφτη** (`system/esco_cache/occupations`, `esco.service.ts`) και
**ποτέ** το ζωντανό API. Η μέτρηση **επικυρώνει εκ των υστέρων** μια αρχιτεκτονική που είχε
επιλεγεί για άλλον λόγο — και ο εισαγωγέας δεν ζητά `full=true`, άρα είναι κι εκείνος άτρωτος.

### 19.4 🏆 Πού ξεπερνάμε — η κλάση που κανείς δεν φυλάει

Το **IFC** απαιτεί `IfcClassification.Source` / `Edition` / `EditionDate` (ISO 12006-2) και **ποτέ**
δεν επαληθεύει ότι η αναφορά **συμφωνεί** με την πηγή της. Το **ESCO** δημοσιεύει Delta files για
κάθε αλλαγή έκδοσης και **κανείς δεν τα καταναλώνει**. Το **SCIM** (RFC 7643) έχει σκέτο `title`,
ελεύθερη συμβολοσειρά χωρίς ταξινόμηση, προέλευση ή βεβαιότητα.

Εδώ η **ταξινόμηση κρίνεται έναντι του εαυτού της**: ίδιο URI ⇒ υποχρεωτικά ίδιος κωδικός και ίδια
ετικέτα (`Κ8`). Μια χειρόγραφη διόρθωση σε **έναν** από τους δύο αρχιτέκτονες θα δίχαζε την
ταξινόμηση **χωρίς καμία πύλη να το δει**.

### 19.5 Ο πληθυσμός — κλειστή λογιστική **15 = 15**

Ο κατάλογος του Giorgio, με **κάθε** κωδικό περασμένο από τον **πραγματικό** `judgeIscoAffinity`:

| Κάδος | Πλήθος | Ποιοι |
|---|---|---|
| ✅ **επάγγελμα που λύνεται** | **9** | τοπογράφος `2165` · αρχιτέκτονας `2161` · πολιτικός `2142` · μηχανολόγος `2144` · λογιστής `2411` · δικηγόρος αγοραστή `2611` · δικηγόρος πωλητή `2611` · συμβολαιογράφος `2619` · μεσίτης `3334` |
| 🔴 **επάγγελμα που σιωπούσε** | **1** | διακοσμητής `3432` — **θεραπεύτηκε**, §20.1 |
| 🔶 **διφορούμενο** | **1** | εργαζόμενος — **δύο** αναγνώσεις, §20.2 |
| ✅ **χωρίς επάγγελμα, εκ σχεδιασμού** | **1** | απλός χρήστης ⇒ `absent` |
| 🔶 **σχέση, όχι επάγγελμα** | **2** | μισθωτής · προμηθευτής |
| 🔶 **δεν είναι άνθρωπος** | **1** | εταιρεία = ο **χώρος** (ADR-787 Ε-3) |

⚠️ **Ο δικηγόρος αγοραστή και ο δικηγόρος πωλητή είναι το ΙΔΙΟ επάγγελμα** (`2611`) με **άλλη
σχέση**. Η διάκριση **δεν μπορεί** να ζήσει στο επάγγελμα, και δεν πρέπει να προσπαθήσει.

### 19.6 Το rig: **8 → 13**, και οι τέσσερις δουλειές

Το §18.6 δήλωνε ότι το rig «δεν είναι κατάλογος επαγγελμάτων». Παραμένει: κάθε νέα προσωπικότητα
μπήκε επειδή ασκεί **μηχανισμό**, όχι για πληρότητα.

| Νέος | Κωδικός | Τι **μόνο αυτός** ασκεί |
|---|---|---|
| `ext.notary` | `2619` | **δύο** δηλώσεις δείχνουν στην **ίδια** δουλειά (`2611` · `2619` → Πελάτες) |
| `ext.agent` | `3334` | μεσίτης — ρητά δηλωμένος τύπος χρήστη, μείζων ομάδα **3** |
| `ext.decorator` | `3432` | Σχέδιο από τη μείζονα **3**: ο πίνακας κρίνει **παραδοτέο**, όχι επίπεδο προσόντων |
| `int.mechanical` | `2144` | **ίδιο** πρόθεμα `214` με τον πολιτικό στη δουλειά, **άλλη** τιμή IFC (`MECHANICALENGINEER`) ⇒ οι **δύο** πίνακες σε **άλλη ανάλυση** |
| `int.worker` | `9313` | η δουλειά **Εργοτάξιο**, που είχε **μηδέν** κάλυψη |

⚠️ Το `Κ9` κλειδώνει τις τέσσερις δουλειές **ονομαστικά**. Το `Κ3` μετρά πλήθος, άρα μια
**ανταλλαγή** (χάνεται το Εργοτάξιο, εμφανίζεται άλλη) θα το άφηνε πράσινο.

### 19.7 Απόδειξη

* **`Ρ-7`** *(στο `useDeclaredOccupation.test.tsx`, όχι στο rig)*: τα **πραγματικά** δεδομένα
  περνούν από τον **πραγματικό** hook. ⚠️ Ζει εκεί επειδή το `scripts/` δεν έχει React harness —
  άγκυρα εκεί θα **ξανάγραφε** τον κανόνα του `escoUri`, δεύτερη διάλεκτος της ίδιας ερώτησης.
* **Ο παρονομαστής μέσα στην άγκυρα**: `WITH_OCCUPATION.length >= 10` — αλλιώς τα δύο επόμενα
  θα ήταν πράσινα πάνω σε **κενό σύνολο**.
* **Ο αντίστροφος φρουρός**: ο πολίτης παραμένει `unknown`. Χωρίς αυτόν, μια «διόρθωση» που δίνει
  σε **όλους** κωδικό θα έβαφε τα πάντα πράσινα καταστρέφοντας το **Α5**.
* **4/4 μεταλλάξεις κόκκινες στις ΕΙΣΟΔΟΥΣ** *(κενό `escoUri` · ίδιο URI με άλλον κωδικό · χάνεται
  το Εργοτάξιο · URI-σκουπίδι)*, `Μ0` πράσινο **πριν ΚΑΙ μετά**.

---

## 20. Ο ΑΝΘΡΩΠΟΣ ΔΗΛΩΝΕΙ ΤΙ ΕΙΝΑΙ — ΟΙ ΤΡΕΙΣ ΑΠΟΦΑΣΕΙΣ (2026-08-26)

### 20.1 Ο διακοσμητής: `3432` → **Σχέδιο**

**Τεκμήρια, όχι εύλογο:** το ISCO-08 τον έχει σε **δική του** μοναδιαία ομάδα *(Interior Designers
and Decorators)* — υπαρκτός πληθυσμός, όχι πλήρωση. Το παραδοτέο του είναι **σχέδιο**, ακριβώς το
κριτήριο με το οποίο μπήκε ήδη το `2162` *(αρχιτέκτονας τοπίου)*. Οι πρόγονοί του `343`/`34`/`3`
είναι **αδήλωτοι**, άρα καμία επικάλυψη *(κανόνας `Χ-7`)*.

⚠️ Δηλώνεται σε **μοναδιαία** ανάλυση: οι αδελφές του μέσα στο `343` *(3431 φωτογράφοι · 3433
γκαλερί · 3434 σεφ)* **δεν** σχεδιάζουν κτίρια — δήλωση στο `343` θα έδινε λάθος απάντηση σε τρεις.

🏆 Το `IfcRoleEnum` **δεν έχει τιμή** γι' αυτόν *(23 τιμές, επαληθευμένο στο IFC4x3)*. Εκεί μένει
`USERDEFINED` + το ESCO URI ακέραιο — η προβλεπόμενη συμπεριφορά του §4, όχι απώλεια.

### 20.2 «Εργαζόμενος»: **ΚΑΙ ΤΑ ΔΥΟ**, και αυτό είναι το εύρημα

Το ερώτημα δεν έχει **μία** απάντηση επειδή η λέξη ονομάζει **δύο** πράγματα:

* **επάγγελμα** — «εργάτης οικοδομών», ISCO `9313` ⇒ δουλειά **Εργοτάξιο**. Καλύπτεται πλέον από
  τον `int.worker`.
* **σχέση** — «εργαζόμενος **της** εταιρείας» ⇒ `companyId` + `internal_user` + συμμετοχή. Το
  κουβαλούν **ήδη** όλοι οι `int.*`, και ανήκει στο **ADR-787**.

🏆 **Και η σύγχυση δεν είναι δική μας — είναι το πρότυπο του κλάδου.** Το `IfcRoleEnum`, που
εξάγουν **Revit και ArchiCAD**, βάζει στις 23 τιμές του δίπλα-δίπλα `ARCHITECT`/`CIVILENGINEER`
*(επαγγέλματα)* και `CLIENT`/`OWNER`/`SUPPLIER`/`RESELLER`/`CONTRACTOR` *(σχέσεις)*. Ένα πρόσωπο
μπορεί να είναι **και τα δύο ταυτόχρονα** — ο αρχιτέκτονας που αγοράζει διαμέρισμα είναι
`ARCHITECT` **και** `CLIENT` — οπότε **ένα enum δεν μπορεί να τα εκφράσει**.

Εδώ ζουν χωριστά, και το είχε ήδη γράψει το `isco-ifc-role.ts`: *«κανείς δεν είναι πελάτης ως
επάγγελμα»*. Ο **μισθωτής** και ο **προμηθευτής** είναι η ίδια περίπτωση ⇒ **δεν** μπαίνουν στο
λεξιλόγιο επαγγέλματος· ζουν στο `contacts.personaTypes` και στο `RelationshipType`.

⚠️ **Και η προφανής χαρτογράφηση του προμηθευτή θα ήταν ανάποδη**: το `3323` *(Buyers)* είναι ο
**αγοραστής** — η **δική μας** πλευρά, δουλειά **Προμήθειες**. Ο άνθρωπος του προμηθευτή είναι
`3322` *(εμπορικός αντιπρόσωπος)* και πάει στους **Πελάτες**. Δύο **αντίθετες** πλευρές μέσα στην
ίδια ελάσσονα ομάδα `332`, όπως ήδη προειδοποιεί ο πίνακας.

### 20.3 🔴 Η οθόνη έλεγε κάτι που **δεν μπορούσαμε να στηρίξουμε** — δύο σιωπές, ένα μήνυμα

Το `DeclaredOccupationBadge` εμφάνιζε `noSuggestion` — *«δεν αντιστοιχεί σε συγκεκριμένη δουλειά»* —
σε **δύο** καταστάσεις:

| Κατάσταση | Ο ισχυρισμός | |
|---|---|---|
| **ταξινομημένο** (`escoUri`), ο πίνακας σωπαίνει | ρωτήθηκε ταξινομητής και απάντησε | ✅ **αληθής** |
| **ελεύθερο κείμενο** (χωρίς `escoUri`) | κανένας ταξινομητής δεν ρωτήθηκε **ποτέ** | 🔴 **ψευδής** |

Το δεύτερο είναι **η απουσία γνώσης παρουσιασμένη ως γνώση** — ακριβώς η βλάβη που το **Α5** υπάρχει
για να αποτρέψει, μέσα στο component που το διαφημίζει.

🔑 Και είναι **ο μόνος τόπος όπου το `isClassified` έχει νόημα**: μέχρι σήμερα είχε **μηδέν**
καταναλωτές παραγωγής *(§17)*, δηλαδή ήταν **αδρανής φρουρός**. Η θεραπεία τον σκοτώνει και
διορθώνει την οθόνη με **μία** αλλαγή.

Το νέο μήνυμα είναι **ενέργεια**, όχι ετικέτα: στο ελεύθερο κείμενο λέει τι να κάνει ο άνθρωπος
*(διάλεξέ το από τη λίστα)*· στο ταξινομημένο **δεν υπάρχει τίποτα να κάνει**.

**Απόδειξη**: 4 άγκυρες, ανάμεσά τους **το ζεύγος** *(οι δύο σιωπές δεν λένε το ίδιο)* — χωρίς αυτό,
μια «απλοποίηση» που τις ξαναενώνει θα άφηνε **και τις δύο** άλλες πράσινες. **2/2 μεταλλάξεις
κόκκινες** *(ένα μήνυμα για δύο · αντιστροφή κλάδων)*.

### 20.4 🔶 Δηλωμένα όρια — ονομασμένα, όχι ξεχασμένα

1. **Ο δικηγόρος και ο συμβολαιογράφος παίρνουν ταυτότητα, όχι συνεργασία.** Απαιτούν **Κ-3**
   *(μέλος έργου από ξένο χώρο, ADR-787)*. Μετρημένο 2026-08-26: **μηδέν** εμφανίσεις
   `homeWorkspaceId` σε ολόκληρο το `src/` ⇒ γνήσια ανοιχτό, καμία λωρίδα.
2. **Δεν αποθηκεύεται η έκδοση ESCO** που απάντησε. Το IFC το απαιτεί *(Edition/EditionDate)* και το
   ESCO δημοσιεύει Delta files· σήμερα ένα URI που αποσύρεται θα ταξίδευε **σιωπηλά νεκρό**.
3. ~~🔴 **`extractIscoCode` επιστρέφει `'0000'`**~~ → ✅ **ΕΚΛΕΙΣΕ 2026-08-26, [ADR-132 §10.2]
   (./ADR-132-esco-professional-classification.md)**. Το `'0000'` ήταν **επινοημένη** τιμή που
   περνούσε το `^\d{1,4}$`, άρα κατέληγε `undeclared` αντί για `malformed` — και το `iscoGroup`
   γινόταν `'000'`, δηλαδή η **μείζων ομάδα «Ένοπλες Δυνάμεις»**. Πλέον **τρεις** καταστάσεις:
   έγκυρο / `''` *(σιωπή)* / ωμό δύσμορφο *(σφάλμα, ορατό και **μετρημένο**)*. Ο μηχανισμός
   **εξήχθη** στο `config/isco-prefix.ts` (`classifyIscoCode` · `iscoMinorGroupOf`) ώστε
   εισαγωγέας και επιλυτής **να μη μπορούν να διαφωνήσουν**.
4. ~~🔴 `catch { page++ }`~~ → ✅ **ΕΚΛΕΙΣΕ 2026-08-26, [ADR-132 §10.3]
   (./ADR-132-esco-professional-classification.md)**. Ήταν **τρία** ελαττώματα, όχι ένα — και το
   χειρότερο ήταν ότι **αποτυχία στην πρώτη σελίδα** άφηνε το `totalItems` στο `1`, οπότε ο
   βρόχος τελείωνε αμέσως και τύπωνε `✅ IMPORT COMPLETE / Total: 0`. Πλέον `declaredTotal: null`,
   ετυμηγορία συγκομιδής, και **πύλη fail-closed** πριν από κάθε γραφή.

⚠️ **ΠΑΡΑΜΕΝΕΙ ΑΝΟΙΧΤΟ το 2** *(έκδοση ESCO)*. Τα **3** και **4** έκλεισαν σε δική τους συνεδρία,
με **30/30 μεταλλάξεις κόκκινες** — και το SSoT audit που τα προηγήθηκε βρήκε **μεγαλύτερο** εύρημα:
**πέντε** τοκενιστές και **τρεις** κλίμακες συνάφειας για **ένα** ευρετήριο *(ADR-132 §10.6-10.8)*.

---

## 21. 🔴 Η ΣΙΩΠΗ ΤΟΥ ΑΥΤΟΝΟΜΟΥ — Η ΚΛΑΣΗ, ΟΧΙ ΤΟ ΔΕΙΓΜΑ (2026-08-26)

### 21.1 Το γεγονός — βρέθηκε από **οθόνη**, όχι από πύλη

Ο Giorgio, σε ζωντανή επαλήθευση στο `/account/profile` με αυτόνομο επαγγελματία, είδε δύο κόκκινα:

```
[ERROR] [EnterpriseRouteConfigService] ❌ Error initializing default route configuration:
        PERMISSION_DENIED: false for 'create' @ L5, false for 'create' @ L1086
[ERROR] [useRealtimeProperties] Firestore error:
        AUTHORIZATION_ERROR: User is not assigned to a company
```

Το **δεύτερο** είναι **κατά λέξη** η βλάβη που το **ADR-809** έκλεισε την ίδια μέρα — σε **άλλον**
καταναλωτή. *Η θεραπεία εφαρμόστηκε στο δείγμα, όχι στην κλάση.*

### 21.2 Η μέτρηση — **1 στους 5**

| hook | εύρος | ρωτούσε τον κριτή; |
|---|---|---|
| `useRealtimeBuildings` | εταιρικό | ✅ *(ADR-813 Φ.Β)* |
| `useRealtimeProperties` | εταιρικό | 🔴 όχι |
| `useRealtimeOpportunities` | εταιρικό | 🔴 όχι |
| `useRealtimeTasks` | εταιρικό | 🔴 όχι |
| `useRealtimePropertiesTrashCount` | εταιρικό | 🔴 όχι |

⚠️ **Ο `useOwnedDocuments` ΔΕΝ είναι στη λίστα, και είναι απόφαση**: το `tenant-config.ts` τον
δηλώνει `mode: 'userId'` ⇒ **ιδιωτικό** εύρος, **δομικά αδύνατο** να πετάξει `MissingTenantError`.
Φρουρός εκεί θα ήταν **αδρανής** *(ADR-749 §5)*.

Ο αυτόνομος **δεν έχει** οργανισμό **εκ σχεδιασμού** (ADR-807). Κάθε εταιρικό ερώτημα πετά, ο κώδικας
ανακάμπτει **σωστά**, και το κατέγραφε ως `[ERROR]` σε **κάθε φόρτωση σελίδας**. Ο θόρυβος **κρύβει
τα αληθινά σφάλματα** — που είναι ακριβώς ο λόγος που ο Giorgio τα έστειλε.

### 21.3 🔴 Το πρώτο σφάλμα ήταν **χειρότερο**: πράξη δομικά αδύνατη

Το `firestore.rules:2257`:

```
match /config/{configId} {
  allow read: if isAuthenticated();
  allow write: if false;   // Admin/server only
}
```

**Δεν είναι κενό στους κανόνες — είναι ρητή απόφαση με γραμμένο λόγο.** Το
`EnterpriseRouteConfigService.initializeDefaultConfig()` επιχειρούσε `setDoc` από τον **φυλλομετρητή**
με το **πελατικό** SDK, δηλαδή πάντα μέσα από τους κανόνες ⇒ **αδύνατο για ΚΑΘΕ χρήστη, ακόμη και
υπερδιαχειριστή**, σε κάθε φόρτωση όπου η συλλογή είναι άδεια.

Το `getFallbackConfig()` ήταν **ήδη** η πραγματική διαδρομή. Η μέθοδος διαγράφηκε: το κενό
αποτέλεσμα επιστρέφει πλέον το fallback **κατευθείαν**, με `logger.info` — άδεια συλλογή είναι
**αναμενόμενη** κατάσταση.

⛔ **ΜΗΝ «λύσεις» ποτέ αυτό ανοίγοντας τον κανόνα.** Θα ήταν παλινδρόμηση ασφαλείας απέναντι σε
απόφαση με γραμμένη αιτιολόγηση.

### 21.4 ⚠️ Η ΠΡΩΤΗ ΘΕΡΑΠΕΙΑ ΗΤΑΝ **ΤΕΣΣΕΡΑ ΔΙΔΥΜΑ** — ΤΟ ΕΠΙΑΣΕ ΤΟ CHECK 3.28

Το ίδιο μπλοκ φρουρού επικολλήθηκε σε τέσσερα αρχεία. Το `jscpd` το κατήγγειλε **μέσα στο ίδιο
commit**: *«κεντρικοποιείς το Α, γράφεις Β+Γ ως δίδυμα»* (N.18).

Εξήχθη ο `services/realtime/hooks/tenant-scoped-error.ts` — **η ΚΡΙΣΗ**, μία φορά. Οι **μεταβάσεις
κατάστασης** μένουν στον καλούντα, γιατί το «κενό αποτέλεσμα» του καθενός είναι **διαφορετικό**
*(άδειος πίνακας · άδειος χάρτης · μηδενικός μετρητής)*: κοινός setter θα απαιτούσε ψεύτικο κοινό
σχήμα. **Κοινή είναι η ερώτηση, όχι η συνέπεια.**

🔑 Και μεταναστεύτηκε **και ο πέμπτος** (`useRealtimeBuildings`), που το είχε ήδη inline: αλλιώς θα
έμεναν **δύο απαντήσεις** στο ίδιο ερώτημα (ADR-749). Πέντε καλούντες, **ένας** κριτής.

### 21.5 🔶 Δηλωμένο όριο του CHECK 3.28 — **μετρημένο, όχι υποτιθέμενο**

Το `--diff` σαρώνει **μόνο τα σταδιοποιημένα** αρχεία και κρίνει **κάθε** κλώνο ανάμεσά τους ως νέο.
Δεν έχει έννοια του `HEAD`.

**Απόδειξη**: εξήχθησαν οι εκδόσεις `HEAD` των πέντε hooks σε προσωρινό φάκελο και τρέχθηκε το
**ίδιο** εργαλείο ⇒ **οι ίδιοι 8 κλώνοι**, πριν αγγιχτεί οτιδήποτε. Τα πέντε αδέλφια ήταν **ήδη**
δομικά δίδυμα *(στήσιμο συνδρομής · κύκλος ζωής · καθαρισμός)*.

⇒ Όποιος σταδιοποιήσει **δύο ή περισσότερα** από αυτά μαζί βλέπει την πύλη να κοκκινίζει για **χρέος
που δεν δημιούργησε**. Δεν είναι σφάλμα της πύλης — είναι **ο μηχανισμός της**: *ένας έλεγχος
ενδο-diff δεν μπορεί να ρωτήσει «ήταν ήδη έτσι;»*.

🔶 **Ανοιχτό, δηλωμένο**: η πραγματική εξάλειψη απαιτεί εξαγωγή **ολόκληρου** του κύκλου ζωής
συνδρομής των realtime hooks — cross-cutting εργασία σε πέντε αρχεία, **άλλο ερώτημα από αυτό εδώ**,
και **απόφαση Giorgio**.

### 21.6 Απόδειξη

* **`Δ`** — και οι πέντε καλούν τον **ΕΝΑΝ** κριτή· **κανένας δεύτερος inline** *(κρίνεται ο
  **κώδικας**, με τα σχόλια κομμένα: η πρώτη γραφή κοκκίνιζε πάνω στο **σχόλιο που τεκμηριώνει τη
  θεραπεία** — σχήμα `Κ7β` του CHECK 3.50)*.
* **`Δ0`** ο παρονομαστής · **`Δ2`** ο ιδιωτικός αδελφός μένει **έξω, με λόγο** · **`Δ3`** καμία
  σπορά από φυλλομετρητή *(με τον κανόνα του `firestore.rules` ως παρονομαστή)*.
* **`Ε1-Ε3`** — ο δρομολογητής **ΕΚΤΕΛΕΙΤΑΙ**, δεν διαβάζεται. ⚠️ Το `Ε2` είναι ο παρονομαστής:
  χωρίς αυτό, δρομολογητής που καλεί **πάντα** το «κενό» θα περνούσε το `Ε1` και θα **έθαβε κάθε
  σφάλμα**.
* ⚠️ **ΔΥΟ ΜΕΤΑΛΛΑΞΕΙΣ ΒΓΗΚΑΝ ΠΡΑΣΙΝΕΣ** στην πρώτη γραφή: η άγκυρα ζητούσε σκέτο
  `toContain('isMissingTenantError')`, δηλαδή απεδείκνυε ότι υπάρχει η **εισαγωγή** — όχι ότι ο
  φρουρός **τρέχει**. Με `if (false)` έμενε πράσινη πάνω σε **νεκρό φρουρό**. Διορθώθηκε η **ΑΓΚΥΡΑ**,
  όχι η μετάλλαξη.
* **4/4 μεταλλάξεις κόκκινες** στον δρομολογητή *(και στις **δύο** κατευθύνσεις: `if (true)` και
  `if (false)`)* + στην **αποκόλληση** δύο καλούντων.
* Η άγκυρα **`Β4`** του ADR-809 χτύπησε **σωστά** στη μετανάστευση *(απαιτούσε τον
  `useRealtimeBuildings` ως **άμεσο** αναγνώστη)*: η **πρόθεσή** της — «το brand να μην ξαναγίνει
  **0** αναγνώστες» — ισχύει, άλλαξε μόνο το **σπίτι**.

---

## 22. 🔬 Ο ΚΥΚΛΟΣ ΖΩΗΣ ΤΗΣ REALTIME ΣΥΝΔΡΟΜΗΣ — **ΕΝΑ ΣΩΜΑ, ΠΕΝΤΕ ΑΔΕΛΦΙΑ** (2026-08-26)

Το §21 έκλεισε την **κρίση** («σχεδιασμένη κατάσταση ή βλάβη;»). Το CHECK 3.28 όμως εξακολουθούσε να
μπλοκάρει με **8 κλώνους**, και η μέτρηση έδειξε ότι **δεν έφταιγε η θεραπεία**: εξήχθησαν οι εκδόσεις
`HEAD` των πέντε hooks σε προσωρινό φάκελο και το **ίδιο** εργαλείο έδωσε τους **ίδιους 8** κλώνους,
πριν αγγιχτεί οτιδήποτε. Το χρέος **προϋπήρχε**. Απόφαση Giorgio: **πραγματική εξαγωγή**, όχι
`SKIP_JSCPD_DIFF=1`.

### 22.1 🔬 Η ΑΝΑΤΟΜΙΑ — **κλειστή λογιστική, με ονόματα**

Ο φάκελος `services/realtime/hooks/` έχει **12** hooks. Δεν είναι όλα αδέλφια:

| # | Hook | Εύρος | Σχήμα κατάστασης | Στην αφαίρεση; |
|---|---|---|---|---|
| 1 | `useRealtimeBuildings` | εταιρικό | επίπεδο + παράγωγος χάρτης | ✅ |
| 2 | `useRealtimeProperties` | εταιρικό | επίπεδο + παράγωγος χάρτης | ✅ |
| 3 | `useRealtimeOpportunities` | εταιρικό | επίπεδο + μνήμη ADR-300 | ✅ |
| 4 | `useRealtimeTasks` | εταιρικό | επίπεδο + μνήμη + παράγωγα στατιστικά | ✅ |
| 5 | `useRealtimePropertiesTrashCount` | εταιρικό | **μετρητής**, χωρίς `error` | ✅ |
| 6 | `useRealtimeBuildingFloors` | εταιρικό | **δύο** συνδρομές, κλειδί `buildingId` | ❌ **όχι τώρα** |
| 7 | `useRealtimeBuildingFloorplan` | εταιρικό | boolean, κλειδί `buildingId` | ❌ **όχι τώρα** |
| 8-10 | `useOwnedDocuments` · `useMyDemands` · `useMyOwnerProperties` | **ιδιωτικό** (`mode:'userId'`) | **διακριτική ένωση** | ❌ **άλλο δόγμα** |
| 11-12 | `usePublicListings` · `usePublicPlace` | **δημόσιο** (`mode:'none'`) | **διακριτική ένωση** | ❌ **άλλο δόγμα** |

🔴 **Γιατί τα 6-7 μένουν έξω**: το ερώτημά τους έχει **αντιδραστικό κλειδί** (`buildingId`) — η
συνδρομή ξαναστήνεται όταν αλλάζει το κλειδί, όχι μόνο όταν αλλάζει το `enabled`. Είναι **γνήσια
επέκταση**, όχι παραλλαγή, και μπαίνει όταν υπάρξει δεύτερος πελάτης της. *(Δεν εκθέτουν `error`
ούτε καλούν τον κριτή — καταπίνουν κάθε σφάλμα σιωπηλά. **Δηλωμένο ανοιχτό**, βλ. §22.6.)*

🔴 **Γιατί τα 8-12 μένουν έξω**: κρατούν **διακριτική ένωση** (`{state:'loading'} | {state:'ready'}`),
όχι επίπεδα `loading`/`error`/`status`. Είναι **νεότερο και καλύτερο** δόγμα (ADR-777). Μία αφαίρεση
για δύο σχήματα κατάστασης θα ήταν **ψεύτικο κοινό σχήμα** — ακριβώς η αστοχία που προειδοποιεί το
handoff. Η ενοποίηση των δογμάτων είναι **δική της απόφαση**, όχι παρενέργεια αυτής.

**Λογιστική: 12 = 5 στην αφαίρεση + 2 επέκταση + 5 άλλο δόγμα.** Κανένα αδέσποτο.

### 22.2 🏆 Η ΕΡΕΥΝΑ — και **γιατί καμία νέα εξάρτηση**

| Υποψήφιο | Τι λύνει | Ετυμηγορία |
|---|---|---|
| **TanStack Query / SWR** | cache + dedup + subscription lifecycle | ❌ **Δεν** είναι στο `.license-allowlist.json` ⇒ **N.5 + απόφαση Giorgio**. Και κουβαλά **τεκμηριωμένη** αιχμή στα realtime: όταν φύγει ο τελευταίος καταναλωτής η συνδρομή κλείνει, και σε επαναπροσάρτηση **όσο η εγγραφή ζει ακόμη στη μνήμη** το `queryFn` **δεν ξανατρέχει** ⇒ σιωπηλά **παγωμένα** δεδομένα *(invertase/tanstack-query-firebase #25)*. |
| **`useSyncExternalStore`** | ο **επίσημος** τρόπος της React για εξωτερική πηγή | ✅ **Σωστό εργαλείο, λάθος βήμα** — βλ. §22.5. Είναι η μηχανή του **κοινού** listener, όχι του κύκλου ζωής. |
| **Εργοστάσιο hook (ADR-594)** | invariant scaffold + per-instance binding | ✅ **ΕΠΙΛΕΧΘΗΚΕ** — είναι **ήδη το πρότυπο ΑΥΤΟΥ του έργου**: το `createBimEntityPersistenceHook` κατέρρευσε έτσι **~21** ταυτόσημα hooks. Ίδιο σχήμα με ORM repository factories, TanStack `QueryObserver`, Apollo options. |

🔑 **ΠΟΥ ΞΕΠΕΡΝΑΜΕ ΤΟΥΣ ΜΕΓΑΛΟΥΣ**: κάθε βιβλιοθήκη λύνει *cache/dedup*. **Καμία** δεν εγγυάται ότι
*«σχεδιασμένη κατάσταση δεν αναφέρεται ως βλάβη»*. Στη μηχανή μας η κρίση του §21 είναι **ΔΟΜΙΚΗ,
ΟΧΙ ΕΠΙΛΟΓΗ**: η **μόνη** διαδρομή προς συνδρομή περνά από τον `routeTenantScopedError`. Ένα έκτο
αδέλφι που θα γραφτεί αύριο **δεν έχει πού** να ξεχάσει να ρωτήσει.

### 22.3 🔨 Η ΕΞΑΓΩΓΗ — **δύο ερωτήματα, δύο σπίτια**

| Αρχείο | Απαντά |
|---|---|
| `create-realtime-collection-hook.ts` | *«πώς **ζει** μια συνδρομή Firestore;»* — στήσιμο · εγγραφή · χαρτογράφηση · **κρίση σφάλματος** · μνήμη ADR-300 · καθαρισμός |
| `realtime-collection.types.ts` | το **σχήμα της παραλλαγής** (μικρός υποχρεωτικός πυρήνας + προαιρετικά με προεπιλογές) |
| `use-realtime-entity-events.ts` | *«πώς φτάνει μια αλλαγή **πριν** γυρίσει ο server;»* — η τριάδα του event bus |

⚠️ **Δεν ενώθηκαν** (κανόνας ADR-749): `onSnapshot` και event bus είναι **δύο** μηχανισμοί· ένωση θα
γεννούσε τρίτο λεξιλόγιο. 🔑 **Το bus εξήχθη ΤΩΡΑ, μαζί**: το `jscpd` **δεν** το είχε καταγγείλει
*(τα διαφορετικά αναγνωριστικά `propertyId`/`opportunityId`/`taskId` κρατούσαν την ομοιότητα κάτω από
τα 50 tokens)*, αλλά αν έφευγε **μόνο** ο κύκλος ζωής τα σώματα θα συρρικνώνονταν γύρω από την τριάδα
και η ομοιότητα θα **ανέβαινε** — η θεραπεία θα γεννούσε τον **επόμενο** κλώνο (N.18).

### 22.4 ⚠️ ΟΙ ΤΡΕΙΣ ΔΗΛΩΜΕΝΕΣ ΑΛΛΑΓΕΣ — **αποφάσεις, όχι παρενέργειες**

**(1) 🔴 Ο πυροδότης του `refetch` έγινε `useState` για ΟΛΟΥΣ.** Τρία αδέλφια τον κρατούσαν σε
`useRef` και τον διάβαζαν στο dep array — **μη-αντιδραστική σύλληψη**. Το `useRealtimeBuildings` το
είχε **ήδη διορθώσει** (2026-06-11) με γραμμένη αιτιολόγηση στο ίδιο του το αρχείο, άρα το ref **δεν
ήταν σκόπιμη απόκλιση — ήταν η αμετανάστευτη παλιά μορφή**. *Το αόρατο που κλείνει*: με
`loading === true` και `error === null`, τα `setLoading(true)`/`setError(null)` του `refetch` είναι
**no-op** ⇒ κανένα render ⇒ το `ref.current` δεν ξαναδιαβάζεται ⇒ **καμία επανεγγραφή, σιωπηλά**.
Άγκυρα: `Γ2` *(«refetch ΕΝΩ φορτώνει ήδη»)*.

**(2) Οι παράγωγοι χάρτες έγιναν `useMemo`, όχι δεύτερη κατάσταση.** Στο `useRealtimeProperties` ο
χάρτης ήταν ξεχωριστό `useState` που το ενημέρωνε ένα `setUnitsByBuilding(...)` **μέσα** στον updater
ενός άλλου `setState` — μη καθαρός updater, **δύο γραφείς για μία αλήθεια**. Παρατηρήσιμο αποτέλεσμα
ταυτόσημο· ο χάρτης πλέον **δεν μπορεί** να ξεσυγχρονιστεί.

**(3) Τηλεμετρία ενοποιήθηκε** *(`'Received buildings…'`/`'Received units…'` → ένα μήνυμα με το
`collection` στο φορτίο· `logger.info` → `debug` στους χειριστές του bus των properties)*. Μη
συμπεριφορικό, ίδιο πρότυπο με ADR-594. Ο **καταγραφέας** κάθε hook μένει **δικός του**, ώστε η
γραμμή να λέει ποιος μίλησε.

### 22.5 🔬 ΤΟ ΒΗΜΑ Β — **η μέτρηση που το επέβαλε** *(υλοποιήθηκε: §22.7)*

🔴 **Μέτρηση αυτής της συνεδρίας**: το `src/hooks/useFirestoreBuildings.ts` διαβάζει την **ΙΔΙΑ**
συλλογή (`BUILDINGS`) με το `useRealtimeBuildings`, αλλά με **αντίθετο δόγμα**: **ΕΝΑΣ** module-level
ref-counted listener + `useSyncExternalStore` (μέσω του `createExternalStore` SSoT). Το docblock του
καταγράφει το **περιστατικό** που το επέβαλε (2026-06-11): ~11 ταυτόχρονοι καταναλωτές άνοιγαν ο
καθένας **δικό του** `onSnapshot` ⇒ **9×** «Buildings updated» + 9× map/sort ανά αλλαγή.

Τα πέντε αδέλφια γράφουν ακόμη το **παλιό** δόγμα, και έχουν **2-4** ταυτόχρονους καταναλωτές το
καθένα *(buildings: 4 · tasks: 3 · properties/opportunities: 2)*.

⚠️ **Απόφαση Giorgio (2026-08-26): Α τώρα, Β αμέσως μετά — ΞΕΧΩΡΙΣΤΑ.** Ο μηχανικός refactor
χωρίστηκε από τη σημασιολογική αλλαγή ώστε, αν κάτι σπάσει, να ξέρουμε **ποιο από τα δύο** φταίει.
🔑 Το κόστος του δεύτερου βήματος **κατέρρευσε**: πριν ήταν πέντε αρχεία, έγινε **ένα** — το
`create-realtime-collection-hook.ts`.

✅ **ΕΓΙΝΕ στο αμέσως επόμενο commit** *(εντολή «προχώρα»)*: η υλοποίηση, ο κίνδυνος που κρατήθηκε
και η απόδειξη ζουν στο **§22.7**.

### 22.6 ✅ ΑΠΟΔΕΙΞΗ, ΚΑΙ ΤΑ ΔΗΛΩΜΕΝΑ ΑΝΟΙΧΤΑ

* **CHECK 3.28: 8 κλώνοι → 0**, μετρημένο και στα 9 θιγμένα αρχεία. *(Το «8→8» δεν αρκούσε.)*
  Ο **8ος** ήταν **ενδο-αρχειακός** και **άσχετος** με τον κύκλο ζωής — το μηδενικό literal του
  `TaskStats` γραμμένο **δύο φορές**· έγινε `createEmptyStats()`, **εργοστάσιο** όχι σταθερά *(το
  `computeStats` **μεταβάλλει** το αντικείμενο)*.
* **Άγκυρα εκτέλεσης** — `__tests__/create-realtime-collection-hook.test.tsx`, **8/8**. Δεν διαβάζει
  **καμία** πηγή: στήνει τη μηχανή, πυροδοτεί το `onError` με **πραγματικά** σφάλματα και κρίνει την
  κατάσταση που βλέπει ο άνθρωπος. Πλαστά **μόνο** τα δύο σύνορα SDK (`@/services/firestore`,
  `firebase/auth`)· ο κριτής και ο δρομολογητής εκτελούνται **αληθινοί**.
* **5/5 μεταλλάξεις κόκκινες**, `Μ0` πράσινο **πριν και μετά**: δρομολογητής→«πάντα βλάβη» *(Β1+Β3)* ·
  δρομολογητής→«πάντα κενό» *(Β2)* · πυροδότης→`useRef` *(Γ1+Γ2)* · καθαρισμός→αφαίρεση *(Α2+Γ1)* ·
  σχεδιασμένο κενό→γράφει στη μνήμη *(Β3)*.
* 🔑 **Η ΑΓΚΥΡΑ `Δ1` ΞΑΝΑΓΡΑΦΤΗΚΕ — ΚΑΙ ΕΓΙΝΕ ΙΣΧΥΡΟΤΕΡΗ.** Ζητούσε `routeTenantScopedError(` σε
  **κάθε** αδέλφι, δηλαδή χαρακτήριζε **ΥΛΟΠΟΙΗΣΗ** *(«πού είναι γραμμένη η κλήση;»)*, ενώ το
  **ΣΥΜΒΟΛΑΙΟ** ήταν πάντα *«σχεδιασμένη κατάσταση δεν αναφέρεται ως βλάβη»*. Ζητά πλέον ότι κανένα
  αδέλφι **δεν αγγίζει** `firestoreQueryService.subscribe` ούτε `onSnapshot` — δηλαδή **δεν έχει πού**
  να παρακάμψει τον κριτή. Από *«θυμήθηκαν όλοι;»* σε *«μπορεί κάποιος να μη ρωτήσει;»*. Παρονομαστής:
  το **νέο `Δ1β`** *(η μηχανή **όντως** στήνει συνδρομή και **όντως** ρωτά)* — αλλιώς το `Δ1` θα ήταν
  πράσινο και σε κόσμο όπου **καμία** συνδρομή δεν στήνεται πουθενά.
* **Γειτονιά χωρίς παλινδρόμηση**: `realtime` + `security` + `post-login-landing` ⇒ **50/50**.
* ❌ **Κανένα `tsc`** (N.17) — ο έλεγχος τύπων γίνεται από τον Giorgio και το pre-commit hook.

🔶 **Δηλωμένα ανοιχτά αυτής της ενότητας:**

| # | Ανοιχτό |
|---|---|
| 1 | ✅ **ΕΚΛΕΙΣΕ** — το βήμα Β έγινε στο αμέσως επόμενο commit: **§22.7**. Έμεινε ανοιχτό ότι **δεν** επαληθεύτηκε στον φυλλομετρητή *(§22.7.5)*. |
| 2 | ✅ **ΕΚΛΕΙΣΕ 2026-08-27 → §22.8.1**. Ήταν **τρεις** συνδρομές, όχι δύο. Η κρίση + η καταγραφή ζουν πλέον στο `subscription-error-handler.ts` *(το χρησιμοποιεί **και** η μηχανή)* και ο κύκλος ζωής στο `use-keyed-realtime-subscription.ts`. Βρέθηκε **πραγματικό σφάλμα**: με σπασμένη τη μία από τις δύο συνδρομές, το `hasFloorsWithoutFloorplan` απαντούσε **ψευδές με σιγουριά**. |
| 3 | ✅ **ΕΚΛΕΙΣΕ 2026-08-27 → §22.8.2**. Αφαιρέθηκε **αφού μετρήθηκε**: το συμβάν έχει **πραγματικό** καταναλωτή αλλού *(`useNavigationSubscriptions`, πλήρης επαναφόρτωση)* και **έξι** παραγωγούς· ο εδώ ακροατής δεν έκανε τίποτα πέρα από μία γραμμή log. Δεν ήταν αλλαγή συμπεριφοράς — ήταν αφαίρεση θορύβου. |
| 4 | 🔶 **ΜΕΤΡΗΘΗΚΕ ΚΑΙ ΑΝΑΒΛΗΘΗΚΕ 2026-08-27 → §22.8.3**. Δεν είναι «μικρό»: **27 αρχεία καταναλωτών** στην εταιρική οικογένεια, και ο φυσικός φρουρός *(έλεγχος τύπων)* απαγορεύεται στον πράκτορα *(N.17)*. Πήγε στο `.claude-rules/pending-ratchet-work.md` **με τον αριθμό**. |

---

### 22.7 ✅ ΤΟ ΒΗΜΑ Β — **ΕΝΑΣ LISTENER ΓΙΑ ΟΛΟΥΣ** (2026-08-26, εντολή Giorgio «προχώρα»)

Η υπόσχεση του §22.5 εκπληρώθηκε στο **επόμενο** commit, όπως ζητήθηκε — και άγγιξε **ένα** αρχείο
παραγωγής, ακριβώς όπως προβλέφθηκε.

**Τι άλλαξε**: η κατάσταση μετακόμισε από `useState` **ανά component** σε **ένα** store ανά **ορισμό**
hook, χτισμένο πάνω στο `createExternalStore` SSoT (`@/lib/state`) και διαβασμένο με
`useSyncExternalStore`. Πρώτος συνδρομητής **ανοίγει** τη συνδρομή, τελευταίος **κλείνει**.

⛔ **Κανένα νέο pub/sub**: το `createExternalStore` υπήρχε ήδη (WAVE 3) και το χρησιμοποιεί ήδη ο
`useFirestoreBuildings`. Γράφτηκε **μόνο** ο κύκλος ζωής από πάνω του.

### 22.7.1 🔑 ΤΟ ΣΥΜΒΟΛΑΙΟ ΠΟΥ ΚΙΝΔΥΝΕΥΕ, ΚΑΙ ΠΩΣ ΚΡΑΤΗΘΗΚΕ

🔴 **Ο πραγματικός κίνδυνος του Βήματος Β δεν ήταν η απόδοση — ήταν το `enabled`.** Με **κοινή**
κατάσταση, ένας καταναλωτής που έχει ρητά δηλώσει *«μη με συνδέσεις»* θα μπορούσε να **αρχίσει να
βλέπει δεδομένα** επειδή τα ζήτησε κάποιος **άλλος**. Στην πράξη τα σημεία κλήσης περνούν
`isAuthReady` / `!authLoading && isAuthenticated` — δηλαδή ένα **πύλη ταυτότητας**· διαρροή εκεί δεν
είναι αισθητική.

**Η λύση**: *το store είναι κοινό — η **προβολή** του, όχι.* Ο απενεργοποιημένος καταναλωτής **δεν
κρατά θέση** στον μετρητή αναφορών (άρα δεν ανοίγει συνδρομή) και η προβολή του μένει σταθερά
`IDLE` *(κενό · `loading:false` · `status:'idle'`)* — **ακριβώς** ό,τι έβλεπε πριν.
Άγκυρα: `Δ5` *(ενεργός βλέπει δεδομένα, απενεργοποιημένος βλέπει κενό, **ταυτόχρονα**)*.

### 22.7.2 🏆 ΠΟΥ ΞΕΠΕΡΝΑΜΕ — **η αιχμή που το TanStack δηλώνει ανοιχτή**

Το §22.2 απέρριψε το TanStack Query εν μέρει επειδή, όταν φύγει ο τελευταίος καταναλωτής η συνδρομή
κλείνει, και σε **επαναπροσάρτηση όσο η εγγραφή ζει ακόμη στη μνήμη** το `queryFn` **δεν ξανατρέχει**
⇒ η οθόνη δείχνει **σιωπηλά παγωμένα** δεδομένα *(invertase/tanstack-query-firebase #25)*.

🔑 **Εδώ η αιχμή είναι δομικά ανύπαρκτη**: ο μετρητής αναφορών `0 → 1` **ξαναστήνει** τη συνδρομή,
ενώ τα δεδομένα του store **επιβιώνουν** ⇒ **μηδέν αναλαμπή ΚΑΙ μηδέν ψέμα**. Δεν είναι ρύθμιση που
πρέπει να θυμηθεί κανείς — είναι η μόνη διαδρομή που υπάρχει. Άγκυρα: `Δ4`, γραμμένη **ονομαστικά**
πάνω σε αυτό το ζήτημα.

⚠️ **Παρενέργεια που είναι βελτίωση, δηλωμένη**: η κατάσταση πλέον επιβιώνει της αποπροσάρτησης
**χωρίς** να χρειάζεται μνήμη ADR-300. Τα δύο hooks που **δεν** έχουν `cache` *(buildings ·
properties · trashCount)* κερδίζουν τη μηδενική αναλαμπή **δωρεάν**. Η μνήμη ADR-300 μένει: απαντά
**άλλο** ερώτημα *(«άναψε spinner;» στην πρώτη ποτέ φόρτωση)* και επιβιώνει και του module reload.

### 22.7.3 ⚠️ ΤΟ ΙΔΙΟΔΥΝΑΜΟ ΠΟΥ ΕΓΙΝΕ ΑΠΑΙΤΗΣΗ

Με **κοινό** store, οι N προσαρτημένοι καταναλωτές ακούν ο καθένας το event bus, άρα το **ίδιο**
μπάλωμα εφαρμόζεται **N φορές**. Ελέγχθηκε ρητά: *«αντικατέστησε τη γραμμή»* (`map` + `applyUpdates`)
και *«βγάλε τη γραμμή»* (`filter`) δίνουν το **ίδιο** αποτέλεσμα σε κάθε επανάληψη ⇒ η τριάδα του
`use-realtime-entity-events.ts` παραμένει ασφαλής **χωρίς αλλαγή**. Το `setItems` κόβει επιπλέον την
**ταυτόσημη** γραφή, ώστε η επανάληψη να μη γεννά render.

⚠️ **ΜΗΝ προσθέσεις μη-ιδιοδύναμη πράξη στο bus** *(π.χ. `push`, μετρητή, ή τοπική αύξηση)*: με
κοινό store θα εφαρμοστεί **μία φορά ανά προσαρτημένο καταναλωτή** — βλάβη που **κλιμακώνεται με τον
αριθμό των ανοιχτών οθονών** και είναι αόρατη με έναν καταναλωτή.

### 22.7.4 ⚠️ ΤΟ ADR-040 ΔΕΝ ΙΣΧΥΕΙ ΕΔΩ — **και το λέω ρητά**

Το handoff ζήτησε να διαβαστεί **πρώτα** το ADR-040 πριν χρησιμοποιηθεί `useSyncExternalStore`.
Διαβάστηκε: οι κανόνες του *(orchestrators χωρίς συνδρομή · micro-leaf ≤1 canvas · getters σε χρόνο
συμβάντος)* αφορούν **υψηλόσυχνες** πηγές του καμβά DXF — δείκτη, hover, transform, στα **60fps**.
Μια συνδρομή Firestore εκπέμπει **σε αλλαγή εγγράφου**, και ο φρουρός ισοδυναμίας του **ADR-361**
κόβει ήδη τις ταυτόσημες παραδόσεις **ανάντη**. Δεν εφαρμόζεται — και ένας κανόνας που δεν
εφαρμόζεται πρέπει να λέγεται, όχι να παραλείπεται σιωπηλά.

### 22.7.5 ✅ ΑΠΟΔΕΙΞΗ — **και μία ισοδύναμη μετάλλαξη που αποδείχθηκε, δεν δηλώθηκε**

* **14/14** άγκυρες *(ήταν 8)*. Νέα ομάδα `Δ`: **μία** συνδρομή για δύο καταναλωτές `Δ1` · μία
  παράδοση φτάνει σε **όλους**, με **ίδια αναφορά** πίνακα `Δ2` · ο πρώτος που φεύγει **δεν** κλείνει,
  ο τελευταίος κλείνει `Δ3` · **η αιχμή του TanStack** `Δ4` · **το συμβόλαιο του `enabled`** `Δ5`.
  Νέο `Γ3`: απενεργοποιημένος καταναλωτής **δεν** ξαναστήνει.
* **7 μεταλλάξεις κόκκινες**: δρομολογητής→«πάντα βλάβη» *(Β1+Β3)* · →«πάντα κενό» *(Β2)* ·
  σχεδιασμένο κενό→γράφει στη μνήμη *(Β3)* · `restart`→no-op *(Γ1+Γ2)* · κλείσιμο σε **κάθε**
  αποχώρηση *(Δ3)* · η προβολή αγνοεί το `enabled` *(Α3+Δ5)* · **και οι δύο** φρουροί ανοίγματος
  φεύγουν *(Δ1)*.
* 🔴 **ΔΥΟ ΜΕΤΑΛΛΑΞΕΙΣ ΒΓΗΚΑΝ ΠΡΑΣΙΝΕΣ, ΚΑΙ ΔΕΝ ΠΡΟΣΠΕΡΑΣΤΗΚΑΝ.** Αφαίρεση **μόνο** του
  `if (refCount === 1)` ή **μόνο** του `if (unsubscribe) return` άφηνε τις άγκυρες πράσινες. Αντί για
  δήλωση *«είναι ισοδύναμες»*, **μετρήθηκε**: με **αμφότερους** τους φρουρούς αφαιρεμένους το `Δ1`
  **κοκκινίζει**. Άρα καθένας **αρκεί μόνος του** — *belt-and-suspenders* (N.7.2 #4), όχι κενό
  άγκυρας. **Η ισοδυναμία μετάλλαξης είναι ισχυρισμός· εδώ αποδείχθηκε.**
* 🔴 **Η ΠΡΩΤΗ ΕΚΤΕΛΕΣΗ ΒΡΗΚΕ ΠΡΑΓΜΑΤΙΚΟ ΕΛΑΤΤΩΜΑ — ΣΤΗΝ ΙΔΙΑ ΤΗΝ ΑΓΚΥΡΑ.** Το test καλούσε το
  εργοστάσιο **μέσα** στη render callback ⇒ **νέο store σε κάθε render**. Με `useState` αυτό ήταν
  αβλαβές *(η κατάσταση ζούσε στο component)*· με store στο closure, **τρεις** άγκυρες κοκκίνισαν
  αμέσως. Το εργοστάσιο καλείται πλέον **μία φορά**, όπως στην πραγματική χρήση.
* **Γειτονιά χωρίς παλινδρόμηση**: **56/56** · CHECK 3.28 καθαρό · ❌ κανένα `tsc` (N.17).
* ✅ **ΕΠΑΛΗΘΕΥΤΗΚΕ ΣΤΟΝ ΦΥΛΛΟΜΕΤΡΗΤΗ 2026-08-27 → §22.7.6.** *(Ήταν 🔶: «η μέτρηση είναι
  αποδεδειγμένη σε **άγκυρα**, όχι σε ζωντανό emulator».)* Ζ1-Ζ4 μετρημένα σε emulator με τις 13
  σπαρμένες ταυτότητες — και το **Ζ2**, το αρχικό σύμπτωμα που γέννησε όλη την αλυσίδα §21→§22,
  βγήκε **καθαρό**: **0 `[ERROR]`, 0 `[WARN]`** σε πλήρη φόρτωση του αυτόνομου.
  ⚠️ Οι πύλες **εξακολουθούν** να μη βλέπουν πόσα `onSnapshot` ανοίγουν ζωντανά — αυτό δεν άλλαξε.

---

### 22.7.6 ✅ Η ΖΩΝΤΑΝΗ ΕΠΑΛΗΘΕΥΣΗ — **ΣΤΟΝ ΦΥΛΛΟΜΕΤΡΗΤΗ, ΟΧΙ ΣΕ ΑΓΚΥΡΑ** (2026-08-27)

> Το §22.7.5 έκλεινε με 🔶 *«δεν επαληθεύτηκε στον φυλλομετρητή»*. **Έκλεισε.** Emulator +
> `dev:emulator`, 13 σπαρμένες ταυτότητες, μετρημένο με **το όργανο που ήδη υπήρχε** *(η γραμμή
> `Realtime documents received` που εκπέμπει η ίδια η μηχανή)* — κανένα νέο εργαλείο.

#### Ζ2 🔴 Η ΣΙΩΠΗ ΤΟΥ ΑΥΤΟΝΟΜΟΥ — **το αρχικό σύμπτωμα, εξαφανισμένο**

Πλήρης φόρτωση σελίδας ως **`ext.decorator@solo.local`** *(«Ιωάννα Διακοσμήτρια»)*, στον προσωπικό
της χώρο `/offers`. **Ολόκληρο** το ημερολόγιο κονσόλας — **13 γραμμές**:

```
[INFO]  [RealtimeService] Initialized
[DEBUG] [AuthContext] Auth state changed: {"uid":"lrh2Xm45…"}
[DEBUG] [AuthContext] Session validation: {"status":"VALID"}
[DEBUG] [AuthContext] Custom claims loaded:
          {"globalRole":"external_user","companyId":"[undefined]","permissions":0}
[INFO]  [AuthContext] Valid session established: {"email":"ext.decorator@solo.local"}
[DEBUG] [RealtimeService] Dispatching SESSION_CREATED
[INFO]  [UserRoleContext] User role determined {"role":"authenticated","verdict":"denied-insufficient"}
```

**`[ERROR]`: 0 · `[WARN]`: 0.** Το `companyId: "[undefined]"` επιβεβαιώνει ότι είναι **όντως** ο
αυτόνομος — δηλαδή **ακριβώς** ο χρήστης που μέχρι το §21 έβγαζε κόκκινες γραμμές σε **κάθε**
φόρτωση. 🔑 Η μόνη γραμμή που *μοιάζει* με άρνηση είναι `[INFO] … verdict:"denied-insufficient"`:
**απόφαση**, όχι σφάλμα — και σε επίπεδο `INFO`, όπως πρέπει.

#### Ζ1 ✅ **ΕΝΑΣ** listener για **ΔΥΟ** καταναλωτές

Σελίδα `/o/alpha-techniki/spaces/properties` ως `int.architect@alpha.local`. Εκεί μοντάρονται
**δύο** καταναλωτές του `useRealtimeProperties`: το `NavigationContext` *(κέλυφος, σε κάθε σελίδα
`/o/`)* και το `UnitsPageContent` *(η ίδια η σελίδα)*.

Όταν η συνδρομή `PROPERTIES` απέτυχε, η κονσόλα έδειξε **ΜΙΑ** γραμμή από το hook:

```
[ERROR] [useRealtimeProperties] Realtime subscription error {"collection":"PROPERTIES", …}
[ERROR] [SharedPropertiesProvider] Firestore listener error { … }     ← ΑΛΛΟ υποσύστημα
```

🔑 **Πριν το Βήμα Β, καθένας από τους δύο καταναλωτές είχε δικό του `useState` και δική του
συνδρομή** ⇒ θα κατέγραφε **ο καθένας** τη δική του γραμμή. Η δεύτερη γραμμή εδώ είναι από τον
`SharedPropertiesProvider` — **ξεχωριστός** listener, άλλο υποσύστημα, όχι δεύτερο αντίγραφο.
Ομοίως, μία αλλαγή σε κτίριο έδωσε **ακριβώς μία** `[useRealtimeBuildings] Realtime documents
received {"collection":"BUILDINGS","count":1}`.

#### Ζ3 ✅ Το συμβόλαιο του `enabled` — καμία διαρροή πριν την ετοιμότητα

Στη σειρά του ημερολογίου του Ζ2: η ταυτότητα λύνεται *(claims → valid session)* και **καμία**
`Realtime documents received` δεν εκπέμπεται — ποτέ, ούτε μετά. Ο αυτόνομος **δεν έχει** εταιρεία,
άρα η διαδρομή είναι το **σχεδιασμένο κενό** *(ADR-807)*: κενό αποτέλεσμα, κατάσταση `active`,
**μηδέν** θόρυβος. Καμία συνδρομή δεν παρέδωσε δεδομένα σε οθόνη που δήλωσε «μη με συνδέσεις».

#### Ζ4 ✅ Ο μετρητής αναφορών — **μετρημένος, όχι δηλωμένος**

Πελατική πλοήγηση *(κλικ σε `<a>`, όχι reload)* από τη σελίδα ακινήτων προς το dashboard:

```
[DEBUG] [useRealtimePropertiesTrashCount] Closing shared realtime subscription {"collection":"PROPERTIES"}
```

**Ακριβώς μία** — *«ο πρώτος που μπαίνει ανοίγει, ο τελευταίος που φεύγει κλείνει»*, ζωντανά.
Επιστροφή με `history.back()`: η συνδρομή ξανάνοιξε κανονικά.

#### 🔶 Τι **ΔΕΝ** επαληθεύτηκε, και λέγεται

**Η χαρούμενη διαδρομή του `PROPERTIES`.** Η λίστα ακινήτων απορρίφθηκε από τους **κανόνες
Firestore**, όχι από τη μηχανή συνδρομών. Η αιτία **μετρήθηκε και ήταν δική μου**: το έγγραφο
δοκιμής που έφτιαξα, πρώτα χωρίς `projectId` και μετά με `projectId` προς έργο που δεν υπήρχε.

🔴 **Και ακριβώς αυτό αποδείχθηκε το εύρημα**: το `list` του Firestore αξιολογείται **ανά έγγραφο**,
οπότε **ΕΝΑ** κακό ακίνητο αρνήθηκε **ΟΛΟΚΛΗΡΗ** τη λίστα σε **νόμιμο** χρήστη του ίδιου tenant —
με **ένα** ακίνητο στη βάση. Ο ίδιος κανόνας φεύγει στην παραγωγή, οι κανόνες **δεν μπορούν** να
επιβάλουν την παρουσία του `projectId` *(`allow create: if false` ⇒ γράφει μόνο το Admin SDK, που
τους παρακάμπτει)*, και **καμία άγκυρα δεν το βλέπει** *(το `properties.rules.test.ts` σπέρνει
πάντα έγκυρο έργο)*. Καταγράφηκε πλήρως στο `.claude-rules/pending-ratchet-work.md` — **δεν
διορθώθηκε εδώ**, δεν είναι αυτή η λωρίδα.

⚠️ Τα τρία έγγραφα δοκιμής **διαγράφηκαν** *(επαληθεύτηκε: `properties (όλα): 0`)*.

---

### 22.8 ✅ ΤΑ ΔΥΟ ΑΠΟ ΤΑ ΤΡΙΑ ΜΙΚΡΑ — ΚΑΙ ΓΙΑΤΙ ΤΟ ΤΡΙΤΟ **ΔΕΝ ΗΤΑΝ ΜΙΚΡΟ** (2026-08-27)

> **Εντολή Giorgio «ΝΑΙ ΠΡΟΧΩΡΑ».** Έκλεισαν τα ανοιχτά **#2** και **#3** του §22.6. Το **#4**
> μετρήθηκε, βρέθηκε **μεγάλο**, και πήγε στο `.claude-rules/pending-ratchet-work.md` **με τον
> αριθμό**, όχι με εντύπωση.

#### 22.8.1 🔴 Η ΣΙΩΠΗΛΗ ΚΑΤΑΠΟΣΗ — και **δύο** σιωπές που δεν λένε το ίδιο

Ο χειριστής σφάλματος **τριών** συνδρομών *(`useRealtimeBuildingFloors` ×2 ·
`useRealtimeBuildingFloorplan`)* ήταν, κατά λέξη, `() => { setLoading(false); }`. **Ούτε** το
σφάλμα, **ούτε** καταγραφή, **ούτε** κριτής.

🔑 **Δεν ήταν το πρόβλημα του §21** *(εκεί ο αυτόνομος έβγαζε κόκκινες γραμμές σε κάθε φόρτωση)* —
ήταν το **αντίστροφο**: πραγματική βλάβη ήταν **αόρατη**. Το spinner έσβηνε, η οθόνη έλεγε «κανένας
όροφος», και κανείς δεν μάθαινε ποτέ ότι το ερώτημα απέτυχε. **Απουσία γνώσης παρουσιασμένη ως
γνώση** — η ίδια κλάση με το §20.3, σε άλλο σημείο.

Η κρίση *(«σχεδιασμένη κατάσταση ή βλάβη;»)* υπήρχε ήδη *(`routeTenantScopedError`, §21)*. Αυτό που
**έλειπε** ήταν το δεύτερο σκέλος — *ποιο μήνυμα, σε ποιο επίπεδο* — και ήταν γραμμένο **μέσα** στη
μηχανή, δηλαδή απρόσιτο στα hooks που έμειναν **έξω από τη μηχανή με λόγο**.

**Θεραπεία, δύο module, δύο ερωτήματα** *(ο κανόνας του §22.3)*:

| Module | Απαντά |
|---|---|
| `subscription-error-handler.ts` | *«βλάβη ⇒ **τι λέμε** γι' αυτήν;»* — κρίση + καταγραφή, μία φορά. **Η μηχανή το χρησιμοποιεί κι αυτή** ⇒ κανένα δεύτερο μήνυμα |
| `use-keyed-realtime-subscription.ts` | *«πώς ζει συνδρομή της οποίας το ερώτημα εξαρτάται από **κλειδί**;»* — άνοιγμα, αλλαγή κλειδιού, `loading`, κλείσιμο |

⚠️ **ΤΟ ΔΕΥΤΕΡΟ ΤΟ ΕΠΕΒΑΛΕ ΤΟ CHECK 3.28, ΞΑΝΑ.** Η πρώτη θεραπεία **επικόλλησε** τον νέο χειριστή
σε τρία σημεία και το `jscpd` κατήγγειλε το δίδυμο **μέσα στο ίδιο commit** *(31 γραμμές / 69
tokens)*. Είναι η **τρίτη** φορά που αυτή η πύλη επιβάλλει την αρχιτεκτονική σε αυτό το ADR *(§18.3
δύο φορές, §21.4, τώρα)* — και η **τέταρτη** συνολικά. **Το μοτίβο δεν είναι σύμπτωση: όποιος
κεντρικοποιεί το Α τείνει να γράψει το Β+Γ ως δίδυμα** (N.18).

🔑 **ΓΙΑΤΙ ΤΑ ΤΡΙΑ HOOKS ΔΕΝ ΜΠΗΚΑΝ ΣΤΗ ΜΗΧΑΝΗ ΤΟΥ §22.7 — ΤΟ ΚΛΕΙΔΙ ΕΙΝΑΙ Η ΔΙΑΦΟΡΑ**: η μηχανή
κρατά **ένα store ανά ΟΡΙΣΜΟ** hook, γι' αυτό αρκεί **ένας** listener για όλους. Δύο κτίρια όμως
είναι **δύο διαφορετικά σύνολα ορόφων** ⇒ το store θα έπρεπε να γίνει **ανά κλειδί** (`Map`).
Ό,τι μοιράζονται είναι ο **κύκλος ζωής**, όχι το store — και **μόνο** αυτό εξήχθη.

⚠️ 🔑 **ΤΟ `ref` ΔΕΝ ΕΙΝΑΙ ΓΟΥΣΤΟ**: τα `constraints`/`select` γράφονται **inline** από τον καλούντα,
άρα αλλάζουν ταυτότητα σε **κάθε render**. Στις εξαρτήσεις του `useEffect` θα ξανάνοιγαν τη
συνδρομή σε κάθε render — δηλαδή θα γεννούσαν **ακριβώς** τη βλάβη που όλο το §22 υπάρχει για να
λύσει. Παράγονται αποδεδειγμένα από το κλειδί ⇒ **το κλειδί είναι η σωστή εξάρτηση**. Άγκυρα
ονομαστικά *(«ΙΔΙΟ κλειδί σε νέο render ⇒ ΚΑΜΙΑ επανασυνδρομή»)*, μετάλλαξη `Μ33` κόκκινη.

🔑 **ΚΑΙ ΕΝΑ ΠΡΑΓΜΑΤΙΚΟ ΣΦΑΛΜΑ ΠΟΥ ΒΡΕΘΗΚΕ ΣΤΟΝ ΔΡΟΜΟ**: το `hasFloorsWithoutFloorplan` είναι
**διασταύρωση** των δύο συνδρομών. Με τη μία σπασμένη, η παλιά έκδοση θα απαντούσε **«ναι, υπάρχουν
όροφοι χωρίς κάτοψη»** — **ψευδές με σιγουριά**, όχι άγνωστο, και θα ζωγράφιζε προειδοποίηση σε
οθόνη. Πλέον η ετυμηγορία **σιωπά όσο υπάρχει σφάλμα**.

#### 22.8.2 ✅ Ο ΑΔΡΑΝΗΣ ΦΡΟΥΡΟΣ — αφαιρέθηκε **αφού** μετρήθηκε

Οι listeners `NAVIGATION_REFRESH` σε `useRealtimeBuildings` / `useRealtimeProperties` είχαν σώμα
**μία γραμμή καταγραφής** και ένα σχόλιο «no need to refetch». Το §22.6 τους κράτησε επειδή *«η
αφαίρεση είναι αλλαγή συμπεριφοράς»*. **Μετρήθηκε 2026-08-27, και δεν είναι**:

| Ερώτηση | Απάντηση, μετρημένη |
|---|---|
| Έχει το συμβάν **πραγματικό** καταναλωτή; | **ΝΑΙ** — `navigation/core/hooks/useNavigationSubscriptions.ts` κάνει **πλήρη επαναφόρτωση** |
| Έχει παραγωγούς; | **ΝΑΙ** — `NavigationCompanyManager` · `RealtimeService` ×2 · `entity-linking/config` ×3 |
| Τι **άλλο** έκανε ο εδώ ακροατής; | **Τίποτα**. Μία γραμμή log |

⇒ Η μόνη παρατηρήσιμη συνέπεια της αφαίρεσης είναι **μία γραμμή log λιγότερη**. Αυτό δεν είναι
συμπεριφορά, είναι **θόρυβος** — και ο ακροατής έκανε τον αναγνώστη να πιστεύει ότι το hook
**αντιδρά** σε ανανέωση πλοήγησης, ενώ **δεν αντιδρούσε**. **Αδρανής φρουρός** (ADR-749 §5).
**Το συμβάν δεν σκοτώθηκε** — ο σχολιασμός στη θέση τους λέει ακριβώς ποιος το καταναλώνει, ώστε
κανείς να μην ξαναρωτήσει.

#### 22.8.3 🔴 ΤΟ ΤΡΙΤΟ «ΜΙΚΡΟ» ΔΕΝ ΗΤΑΝ ΜΙΚΡΟ — και το λέω αντί να το κάνω στα τυφλά

Το §22.6 #4 *(«δύο δόγματα κατάστασης»)* περιγραφόταν ως μικρό. **Μετρημένο**: η εταιρική
οικογένεια έχει **27 αρχεία καταναλωτών** *(buildings 8 · properties 7 · tasks 5 · opportunities 4 ·
trashCount 3)*, καθένα με δικό του `destructuring` του `loading`/`error`. Η μετατροπή σε διακριτική
ένωση αλλάζει τη **δημόσια όψη** των πιο πολυχρησιμοποιημένων hooks δεδομένων της εφαρμογής.

⚠️ **ΚΑΙ ΛΕΙΠΕΙ ΤΟ ΔΙΧΤΥ**: ο φυσικός φρουρός μιας τέτοιας αλλαγής είναι ο **έλεγχος τύπων**, που ο
**N.17 απαγορεύει ρητά** στον πράκτορα. Ένας ξεχασμένος κλάδος θα έβγαινε **πράσινος** σε κάθε jest
και θα έσπαγε **στην οθόνη**. Πήγε στο `pending-ratchet-work.md` για δική του συνεδρία, με τον
Giorgio να τρέχει `npm run typecheck` ανάμεσα στα βήματα.

#### 22.8.4 ✅ ΑΠΟΔΕΙΞΗ

| Τι | Μέτρηση |
|---|---|
| Άγκυρες | **34/34** στο `src/services/realtime` — από τις οποίες **10 νέες**, όλες **εκτελούν** |
| Μεταλλάξεις | **5/5 ΚΟΚΚΙΝΕΣ**: σιωπηλή κατάποση *(η ακριβής παλιά βλάβη)* · κόκκινο και στη σχεδιασμένη σιωπή · `constraints` στις εξαρτήσεις · χωρίς κλείσιμο στο unmount · κλειδί `null` ανοίγει συνδρομή |
| CHECK 3.28 | **0 κλώνοι** σε **18** αρχεία του φακέλου |
| Γραμμές | `useRealtimeBuildingFloors` **107 → 96** · `useRealtimeBuildingFloorplan` **62 → 55**, με **περισσότερη** λειτουργικότητα *(ορατό `error`)* |
| Δημόσια όψη | **Προσθετική** — τα `error` είναι νέα πεδία· και οι **3** καταναλωτές αποσυνθέτουν μόνο ό,τι υπήρχε |

#### 22.8.5 🔴 ΤΟ ΙΔΙΟ ΜΑΘΗΜΑ, ΤΡΙΤΗ ΦΟΡΑ — **ΚΑΙ ΜΙΑ ΠΡΑΣΙΝΗ ΜΕΤΑΛΛΑΞΗ ΠΟΥ ΑΠΟΔΕΙΧΘΗΚΕ**

Η άγκυρα **`Δ1β`** *(`missing-tenant-is-not-a-failure.test.ts`)* **κοκκίνισε** από αυτή τη δουλειά:
ζητούσε `routeTenantScopedError(` **μέσα στη μηχανή**, και η κλήση **μετακόμισε** στον κοινό
χειριστή. Είναι **ακριβώς** η βλάβη που το σχόλιο του `Δ1` καταγράφει ένα κλικ πιο πάνω:
**χαρακτηρισμός ΥΛΟΠΟΙΗΣΗΣ αντί για ΣΥΜΒΟΛΑΙΟ**. Ξαναγράφτηκε ώστε να ζητά την **ΑΛΥΣΙΔΑ**
*(μηχανή → κοινός χειριστής → ένας κριτής, και το keyed hook στην ίδια αλυσίδα)*, οπότε μια
μελλοντική μετακόμιση **μέσα** στην αλυσίδα δεν την ενοχλεί.

🔴 **ΚΑΙ ΜΙΑ ΜΕΤΑΛΛΑΞΗ ΒΓΗΚΕ ΠΡΑΣΙΝΗ — ΔΕΝ ΔΗΛΩΘΗΚΕ ΙΣΟΔΥΝΑΜΗ, ΜΕΤΡΗΘΗΚΕ.** Άφησα τη δήλωση
`const handleSubscriptionError = createSubscriptionErrorHandler({…})` άθικτη και έδωσα στο
`subscribe` **no-op** χειριστή — δηλαδή **πραγματική** παράκαμψη του κριτή. Το `Δ1β` έμεινε
**πράσινο**. Η ίδια μετάλλαξη στην **εκτελεστική** σουίτα: **ΚΟΚΚΙΝΗ, 3 tests**.

🔑 **Άρα δεν είναι κενό της άγκυρας — είναι το ΣΥΝΟΡΟ της**: μια άγκυρα κειμένου αποδεικνύει *«η
κλήση είναι ΓΡΑΜΜΕΝΗ»*, **ποτέ** *«είναι ΣΥΝΔΕΔΕΜΕΝΗ»*. Το όριο γράφτηκε **μέσα στο ίδιο το test**
με το ⛔ *«ΜΗΝ το επικαλεστείς ως απόδειξη καλωδίωσης»* — γιατί το επόμενο μάτι θα το εμπιστευόταν.

🔑 **Η άγκυρα που έχει σημασία είναι το ΖΕΥΓΟΣ** *(«η μία σιωπά, η άλλη φωνάζει — και ΔΕΝ είναι
εναλλάξιμες»)*: χωρίς αυτό, μια «απλοποίηση» που ξαναενώνει τις δύο σιωπές θα άφηνε **και τις δύο
άλλες** πράσινες. Το ίδιο μάθημα με το §20.3, γραμμένο ξανά επίτηδες.

---

## 12. Changelog

| Ημερομηνία | Αλλαγή |
|---|---|
| 2026-08-27 | ✅ **ΖΩΝΤΑΝΗ ΕΠΑΛΗΘΕΥΣΗ ΤΟΥ ΒΗΜΑΤΟΣ Β — ΣΤΟΝ ΦΥΛΛΟΜΕΤΡΗΤΗ** *(νέα §22.7.6· έκλεισε το 🔶 του §22.7.5)*. **Ζ2**: πλήρης φόρτωση ως `ext.decorator@solo.local` *(`companyId:"[undefined]"`, ο αυτόνομος)* ⇒ **13 γραμμές κονσόλας, 0 `[ERROR]`, 0 `[WARN]`** — το **αρχικό σύμπτωμα** που γέννησε την αλυσίδα §21→§22 **εξαφανίστηκε**. **Ζ1**: δύο μονταρισμένοι καταναλωτές του `useRealtimeProperties` ⇒ **ΜΙΑ** γραμμή από το hook *(η δεύτερη είναι από τον `SharedPropertiesProvider`, **άλλο** υποσύστημα)*. **Ζ4**: πελατική πλοήγηση ⇒ **ακριβώς ένα** `Closing shared realtime subscription`. 🔴 **Και ένα εύρημα εκτός λωρίδας, προκαλεσμένο κατά λάθος**: το `list` του Firestore αξιολογείται **ανά έγγραφο**, οπότε **ΕΝΑ** ακίνητο με λείπον/κρεμάμενο `projectId` αρνείται **ΟΛΟΚΛΗΡΗ** τη λίστα σε **νόμιμο** χρήστη του ίδιου tenant. Ο κανόνας είναι ο **παραγωγικός**, οι κανόνες **δεν μπορούν** να επιβάλουν το πεδίο *(`allow create: if false`)*, και **καμία άγκυρα δεν το βλέπει**. Καταγράφηκε στο `pending-ratchet-work.md` — **δεν διορθώθηκε εδώ**. |
| 2026-08-27 | ✅ **ΤΑ ΔΥΟ ΑΠΟ ΤΑ ΤΡΙΑ ΜΙΚΡΑ ΤΟΥ §22.6** *(νέα §22.8, εντολή Giorgio «ΝΑΙ ΠΡΟΧΩΡΑ»)*. **#2 σιωπηλή κατάποση**: ήταν **τρεις** συνδρομές, όχι δύο· ο χειριστής ήταν `() => setLoading(false)` ⇒ πραγματική βλάβη **αόρατη** *(το αντίστροφο του §21: εκεί θόρυβος χωρίς βλάβη, εδώ βλάβη χωρίς θόρυβο)*. 🔴 **Βρέθηκε πραγματικό σφάλμα στον δρόμο**: το `hasFloorsWithoutFloorplan` είναι **διασταύρωση** δύο συνδρομών και με τη μία σπασμένη απαντούσε **ψευδές με σιγουριά** — ζωγράφιζε προειδοποίηση σε οθόνη. **#3 αδρανής φρουρός**: αφαιρέθηκε **αφού μετρήθηκε** ότι το συμβάν έχει πραγματικό καταναλωτή αλλού και ο εδώ ακροατής δεν έκανε τίποτα πέρα από μία γραμμή log. ⚠️ **Το CHECK 3.28 επέβαλε την αρχιτεκτονική για ΤΕΤΑΡΤΗ φορά σε αυτό το ADR**: η πρώτη θεραπεία επικόλλησε τον χειριστή σε τρία σημεία και ο κλώνος καταγγέλθηκε **μέσα στο ίδιο commit** ⇒ εξήχθη και ο **κύκλος ζωής με αντιδραστικό κλειδί**. 🔶 **#4 ΜΕΤΡΗΘΗΚΕ ΚΑΙ ΑΝΑΒΛΗΘΗΚΕ**: **27** αρχεία καταναλωτών και ο φρουρός του *(typecheck)* απαγορεύεται στον πράκτορα (N.17) — «στα τυφλά» θα ήταν έκπτωση. **Απόδειξη**: 34/34 άγκυρες *(10 νέες)* · **5/5 μεταλλάξεις κόκκινες** · CHECK 3.28 **0** σε 18 αρχεία · δημόσια όψη **προσθετική**. |
| 2026-08-26 | ✅ **ΕΚΛΕΙΣΑΝ ΤΑ §20.4 #3 ΚΑΙ #4** *(εντολή Giorgio, δική τους συνεδρία)* — ο εισαγωγέας ESCO **επινοούσε** `'0000'` *(που στο ISCO-08 είναι οι **Ένοπλες Δυνάμεις**, όχι «άγνωστο»)* και **ανέφερε `✅` πάνω σε ολική αποτυχία** *(αποτυχία στην πρώτη σελίδα ⇒ `Total: 0` ως «πλήρης»)*. Η θεραπεία ζει στο **[ADR-132 §10](./ADR-132-esco-professional-classification.md)**: **τρεις** καταστάσεις ISCO *(έγκυρο / σιωπή / ωμό δύσμορφο)* με τον μηχανισμό **εξαγμένο** στο `config/isco-prefix.ts` *(`classifyIscoCode` · `iscoMinorGroupOf`)* ώστε εισαγωγέας και επιλυτής **να μη μπορούν να διαφωνήσουν**· και συγκομιδή που επιστρέφει **ετυμηγορία** με **πύλη fail-closed** πριν από κάθε γραφή. 🔴 **Το SSoT audit βρήκε μεγαλύτερο εύρημα από τα δύο ελαττώματα**: **πέντε** αντίγραφα του τοκενιστή ESCO σε **δύο άκρα του ίδιου ευρετηρίου** *(απόκλιση εκεί ⇒ **άδεια** αποτελέσματα, χωρίς σφάλμα)* και **τρεις** κλίμακες συνάφειας, με τον **διακομιστή** να επιβάλλει «διάλεξε από τη λίστα» με κατάταξη **που ο χρήστης δεν έβλεπε**. **Απόδειξη**: 95 άγκυρες · **30/30 μεταλλάξεις κόκκινες** · CHECK 3.28 **5 → 0** *(4 προϋπάρχοντες, αποδεδειγμένα στο HEAD)* · δύο υπηρεσίες **631 → 340** γραμμές · +3 modules στο μητρώο **με απόδειξη**. ⚠️ **Παραμένει ανοιχτό το §20.4 #2** *(έκδοση ESCO)*. |
| 2026-08-26 | ✅ **ΒΗΜΑ Β — ΕΝΑΣ LISTENER ΓΙΑ ΟΛΟΥΣ** *(νέα §22.7· εντολή Giorgio «προχώρα»)*. Η υπόσχεση του §22.5 εκπληρώθηκε στο **αμέσως επόμενο** commit και άγγιξε **ένα** αρχείο παραγωγής, όπως προβλέφθηκε. Η κατάσταση μετακόμισε από `useState` **ανά component** σε **ένα** store ανά **ορισμό** hook (`createExternalStore` SSoT + `useSyncExternalStore`)· πρώτος συνδρομητής ανοίγει, τελευταίος κλείνει. **Κανένα νέο pub/sub.** 🔴 **Ο πραγματικός κίνδυνος δεν ήταν η απόδοση — ήταν το `enabled`**: με κοινή κατάσταση, καταναλωτής που δήλωσε «μη με συνδέσεις» θα μπορούσε να **δει δεδομένα** επειδή τα ζήτησε άλλος, και τα σημεία κλήσης περνούν **πύλη ταυτότητας** (`isAuthReady`). Λύση: *το store είναι κοινό — η **προβολή** του, όχι* (άγκυρα `Δ5`). 🏆 **Που ξεπερνάμε**: η αιχμή που το TanStack **δηλώνει ανοιχτή** (επαναπροσάρτηση εντός μνήμης ⇒ σιωπηλά **παγωμένα** δεδομένα) είναι εδώ **δομικά ανύπαρκτη** — ο μετρητής `0→1` **ξαναστήνει** ενώ τα δεδομένα επιβιώνουν (άγκυρα `Δ4`, γραμμένη ονομαστικά πάνω στο ζήτημα). ⚠️ Το **ADR-040 διαβάστηκε και ΔΕΝ εφαρμόζεται** (κανόνες για 60fps πηγές καμβά· εδώ ο φρουρός ADR-361 κόβει ανάντη) — και λέγεται ρητά, αντί να παραλειφθεί. ⚠️ Με κοινό store το bus εφαρμόζεται **N φορές**: ελέγχθηκε ότι `map`/`filter` είναι **ιδιοδύναμα** ⇒ η τριάδα μένει ασφαλής **χωρίς αλλαγή**· **ΜΗΝ** προστεθεί μη-ιδιοδύναμη πράξη εκεί. **Απόδειξη**: **14/14** άγκυρες (ήταν 8· νέα ομάδα `Δ` + `Γ3`) · **7 μεταλλάξεις κόκκινες** · γειτονιά **56/56** · CHECK 3.28 καθαρό. 🔴 **ΔΥΟ μεταλλάξεις βγήκαν ΠΡΑΣΙΝΕΣ και ΔΕΝ προσπεράστηκαν**: αντί για δήλωση «ισοδύναμες», **μετρήθηκε** ότι με **αμφότερους** τους φρουρούς αφαιρεμένους το `Δ1` κοκκινίζει ⇒ *belt-and-suspenders*, όχι κενό άγκυρας. 🔴 **Η πρώτη εκτέλεση βρήκε πραγματικό ελάττωμα στην ΙΔΙΑ ΤΗΝ ΑΓΚΥΡΑ**: το test καλούσε το εργοστάσιο **μέσα** στη render callback ⇒ νέο store σε κάθε render — αβλαβές με `useState`, **τρεις** άγκυρες κόκκινες με store σε closure. 🔶 **Ανοιχτό**: δεν επαληθεύτηκε στον φυλλομετρητή — καμία πύλη δεν βλέπει πόσα `onSnapshot` ανοίγουν ζωντανά. |
| 2026-08-26 | 🔬 **Ο ΚΥΚΛΟΣ ΖΩΗΣ ΤΗΣ REALTIME ΣΥΝΔΡΟΜΗΣ — ΕΝΑ ΣΩΜΑ, ΠΕΝΤΕ ΑΔΕΛΦΙΑ** *(νέα §22)*. Το §21 έκλεισε την **κρίση**· το CHECK 3.28 εξακολουθούσε να μπλοκάρει με **8 κλώνους** που **μετρήθηκε ότι προϋπήρχαν στο `HEAD`**. Απόφαση Giorgio: **πραγματική εξαγωγή**, όχι `SKIP_JSCPD_DIFF=1`. **Ανατομία με κλειστή λογιστική**: 12 hooks = **5** στην αφαίρεση + **2** με αντιδραστικό κλειδί *(γνήσια επέκταση)* + **5** με **διακριτική ένωση** *(άλλο δόγμα — μία αφαίρεση για δύο σχήματα = ψεύτικο κοινό σχήμα)*. **Έρευνα**: TanStack/SWR ❌ *(εκτός `.license-allowlist.json` ⇒ N.5, **και** τεκμηριωμένη αιχμή realtime — σε επαναπροσάρτηση όσο η εγγραφή ζει στη μνήμη το `queryFn` δεν ξανατρέχει ⇒ σιωπηλά παγωμένα δεδομένα)*· επιλέχθηκε το **εργοστάσιο hook του ADR-594**, ήδη πρότυπο **αυτού** του έργου. 🔑 **Που ξεπερνάμε**: καμία βιβλιοθήκη δεν εγγυάται ότι «σχεδιασμένη κατάσταση δεν αναφέρεται ως βλάβη» — εδώ η κρίση του §21 είναι **ΔΟΜΙΚΗ**: η **μόνη** διαδρομή προς συνδρομή περνά από τον κριτή. ⚠️ **Δύο ερωτήματα, δύο σπίτια** (ADR-749): κύκλος ζωής ≠ event bus· το bus εξήχθη **μαζί** γιατί αλλιώς η θεραπεία θα γένναγε τον **επόμενο** κλώνο (N.18). **Τρεις δηλωμένες αλλαγές**: πυροδότης `refetch` → `useState` για όλους *(το ref έπεφτε **σιωπηλά** όταν `loading===true`)* · παράγωγοι χάρτες → `useMemo` *(ήταν **δύο γραφείς για μία αλήθεια**)* · τηλεμετρία ενοποιήθηκε. **Απόδειξη**: CHECK 3.28 **8 → 0** *(ο 8ος ήταν **ενδο-αρχειακός**: το μηδενικό literal του `TaskStats` δύο φορές)* · **8/8** άγκυρα **εκτέλεσης** *(καμία ανάγνωση πηγής· πλαστά μόνο τα σύνορα SDK, ο κριτής **αληθινός**)* · **5/5 μεταλλάξεις κόκκινες**, `Μ0` πράσινο πριν **και** μετά · γειτονιά **50/50**. 🔑 **Η `Δ1` ξαναγράφτηκε ΙΣΧΥΡΟΤΕΡΗ**: χαρακτήριζε **υλοποίηση** *(«πού είναι η κλήση;»)*, ζητά πλέον ότι κανένα αδέλφι **δεν αγγίζει** `subscribe`/`onSnapshot` — από *«θυμήθηκαν όλοι;»* σε *«μπορεί κάποιος να μη ρωτήσει;»*, με το **νέο `Δ1β`** ως παρονομαστή. 🔶 **Βήμα Β δηλωμένο, ΟΧΙ εδώ** *(απόφαση Giorgio: ξεχωριστά)*: το `useFirestoreBuildings` διαβάζει την **ΙΔΙΑ** συλλογή με **αντίθετο δόγμα** — **ΕΝΑΣ** ref-counted listener *(περιστατικό 2026-06-11: ~11 καταναλωτές ⇒ **9×** διπλή δουλειά)*. Το κόστος του βήματος κατέρρευσε από **5** αρχεία σε **1**. |
| 2026-08-26 | 🔴 **Η ΣΙΩΠΗ ΤΟΥ ΑΥΤΟΝΟΜΟΥ — Η ΚΛΑΣΗ, ΟΧΙ ΤΟ ΔΕΙΓΜΑ** *(νέα §21)*. Βρέθηκε από **οθόνη**, όχι από πύλη: ζωντανή επαλήθευση στο `/account/profile` με αυτόνομο έβγαλε **κατά λέξη** τη βλάβη που το **ADR-809** έκλεισε την ίδια μέρα — σε **άλλον** καταναλωτή. Μετρημένο: **1 στους 5** εταιρικούς realtime αδελφούς ρωτούσε τον κριτή· οι άλλοι τέσσερις λογούσαν `[ERROR]` πάνω σε **σχεδιασμένη** κατάσταση, σε **κάθε φόρτωση σελίδας**. ⚠️ Ο `useOwnedDocuments` μένει **έξω με λόγο** (`mode: 'userId'` ⇒ δομικά αδύνατο να πετάξει). 🔴 **Το δεύτερο σφάλμα ήταν χειρότερο**: το `EnterpriseRouteConfigService` επιχειρούσε `setDoc` στο `config` από τον **φυλλομετρητή**, ενώ το `firestore.rules:2257` λέει `allow write: if false; // Admin/server only` — **ρητή απόφαση, όχι κενό** ⇒ πράξη **δομικά αδύνατη για ΚΑΘΕ χρήστη**, με το fallback να είναι **ήδη** η πραγματική διαδρομή. Η μέθοδος **διαγράφηκε**. ⚠️ **Η ΠΡΩΤΗ ΘΕΡΑΠΕΙΑ ΗΤΑΝ ΤΕΣΣΕΡΑ ΔΙΔΥΜΑ** και το **CHECK 3.28** τα έπιασε μέσα στο ίδιο commit ⇒ εξαγωγή `tenant-scoped-error.ts` *(η **ΚΡΙΣΗ** μία φορά· οι **μεταβάσεις** μένουν στον καλούντα γιατί το «κενό» του καθενός διαφέρει)*, και μεταναστεύτηκε **και ο πέμπτος** που το είχε inline — αλλιώς **δύο απαντήσεις** στο ίδιο ερώτημα (ADR-749). 🔶 **Δηλωμένο όριο του 3.28, ΜΕΤΡΗΜΕΝΟ**: το `--diff` κρίνει μόνο τα **σταδιοποιημένα** και δεν έχει έννοια του `HEAD` ⇒ εξήχθησαν οι εκδόσεις `HEAD` των πέντε hooks και το **ίδιο** εργαλείο έδωσε τους **ίδιους 8 κλώνους** πριν αγγιχτεί οτιδήποτε: τα αδέλφια ήταν **ήδη** δομικά δίδυμα. Η εξάλειψη θέλει εξαγωγή **ολόκληρου** του κύκλου ζωής συνδρομής — **απόφαση Giorgio**. **Απόδειξη**: `Δ0-Δ3` καλωδίωση *(με τα σχόλια κομμένα — η πρώτη γραφή κοκκίνιζε πάνω στο σχόλιο που τεκμηριώνει τη θεραπεία, σχήμα `Κ7β`)* · `Ε1-Ε3` ο δρομολογητής **ΕΚΤΕΛΕΙΤΑΙ** *(με το `Ε2` ως παρονομαστή: αλλιώς δρομολογητής που θάβει **κάθε** σφάλμα θα περνούσε)* · **4/4 μεταλλάξεις κόκκινες σε ΔΥΟ κατευθύνσεις**. ⚠️ **ΔΥΟ μεταλλάξεις βγήκαν ΠΡΑΣΙΝΕΣ** στην πρώτη γραφή *(η άγκυρα απεδείκνυε την **εισαγωγή**, όχι ότι ο φρουρός **τρέχει**)* ⇒ διορθώθηκε η **ΑΓΚΥΡΑ**. |
| 2026-08-26 | 🔴 **ΤΟ RIG ΔΟΚΙΜΑΖΕ ΤΟΝ ΠΙΝΑΚΑ, ΟΧΙ ΤΗ ΔΙΑΔΡΟΜΗ** *(νέα §19-§20)*. Το `Κ2` περνούσε **7/7** ενώ **8 στους 8** έφταναν στην οθόνη ως **σιωπή**: ο `useDeclaredOccupation` **αρνείται** `iscoCode` χωρίς `escoUri` *(σωστά, `Ρ-4`)* και **κανένας** από τους οκτώ δεν είχε — τα τρία πεδία ήταν **προαιρετικά** στο `SeedOccupation`, οπότε ο σπορέας παρήγαγε άνθρωπο που ο picker **δεν θα μπορούσε ΠΟΤΕ** να δημιουργήσει. Δύο όργανα, δύο απαντήσεις, **καμία σύγκριση** *(ADR-749)*. **Θεραπεία δομική**: τα τρία έγιναν **υποχρεωτικά** ⇒ ορφανός κωδικός **μη εκφράσιμος**, όχι ανιχνεύσιμος· **και τα 10 ESCO URI τα απάντησε η αυθεντία** *(live API, 2026-08-26)*. ⚠️ Ο λογιστής σπερνόταν με `241` — **ελάσσων ομάδα**, τιμή που **κανένα** occupation δεν κουβαλά ⇒ `2411`. 🔴 **ΓΙΑΤΙ ΠΑΓΩΜΕΝΑ URI**: το ζωντανό ESCO **σπάει** στο κεντρικότερο επάγγελμα — `search?text=civil engineer` ⇒ **200**, `+full=true` ⇒ **HTTP 500**, `resource/occupation` ⇒ **500** σε **κάθε** γλώσσα *(«More than one value found for field hasSkillType»)*: **μία** χαλασμένη εγγραφή **μηδενίζει ολόκληρη** την απάντηση. Επικυρώνει εκ των υστέρων τον **καθρέφτη** του `esco.service.ts`. 🏆 **Πού ξεπερνάμε**: IFC απαιτεί `Source/Edition` και **ποτέ** δεν επαληθεύει ότι η αναφορά **συμφωνεί** με την πηγή· ESCO εκδίδει Delta files που **κανείς** δεν καταναλώνει· SCIM `title` = σκέτη συμβολοσειρά. Εδώ `Κ8`: ίδιο URI ⇒ **ίδιος** κωδικός ΚΑΙ ετικέτα. **Τρεις αποφάσεις**: διακοσμητής `3432` → **Σχέδιο** *(παραδοτέο = σχέδιο, όπως το `2162`· μοναδιαία ανάλυση γιατί οι αδελφές του δεν σχεδιάζουν κτίρια· IFC **δεν έχει** τιμή ⇒ `USERDEFINED`)*· **«εργαζόμενος» = ΚΑΙ ΤΑ ΔΥΟ** *(επάγγελμα `9313` **και** συμμετοχή)* — και η σύγχυση είναι **το πρότυπο του κλάδου**: το `IfcRoleEnum` που εξάγουν **Revit/ArchiCAD** βάζει `ARCHITECT` δίπλα σε `CLIENT`/`SUPPLIER`/`RESELLER`, ενώ ένα πρόσωπο είναι **και τα δύο ταυτόχρονα**· **μισθωτής/προμηθευτής μένουν έξω** *(σχέσεις — και η προφανής χαρτογράφηση `3323` θα ήταν **ανάποδη**: είναι ο **αγοραστής**)*. 🔴 **Η οθόνη έλεγε ανυποστήρικτο ισχυρισμό**: `noSuggestion` *«δεν αντιστοιχεί»* και για **ελεύθερο κείμενο**, όπου **κανένας ταξινομητής δεν ρωτήθηκε ποτέ** — η απουσία γνώσης ως γνώση *(Α5)*, μέσα στο component που το διαφημίζει· η θεραπεία **σκοτώνει ταυτόχρονα** τον αδρανή φρουρό `isClassified` *(**0** καταναλωτές, §17)*. Rig **8 → 13** *(κάθε νέος ασκεί **μηχανισμό**: δύο δηλώσεις → ίδια δουλειά · Σχέδιο από μείζονα **3** · ίδιο πρόθεμα/άλλη τιμή IFC · το **Εργοτάξιο** που είχε **μηδέν** κάλυψη)*. **Απόδειξη**: `Ρ-7` με **πραγματικά** δεδομένα σε **πραγματικό** hook *(και παρονομαστή ΜΕΣΑ της)* · αντίστροφος φρουρός *(ο πολίτης μένει `unknown`)* · **6/6 μεταλλάξεις κόκκινες** · **252/252** στις 10 σχετικές σουίτες · i18n el/en ταυτόσημα. 🔶 **Δηλωμένα ανοιχτά**: **Κ-3** *(μηδέν `homeWorkspaceId` σε όλο το `src/`)* · καμία **έκδοση ESCO** αποθηκευμένη · `extractIscoCode` επιστρέφει **επινοημένο** `'0000'` ⇒ `undeclared` αντί `malformed` · ο εισαγωγέας **προσπερνά** σελίδα σε σφάλμα και τυπώνει ποσοστό. |
| 2026-08-25 | 🔴 **Η ΟΘΟΝΗ ΒΡΗΚΕ ΤΡΙΤΟ — Η ΠΡΟΤΑΣΗ ΟΝΟΜΑΖΕ ΛΑΘΟΣ ΑΙΤΙΑ** *(νέο §17.8)*. **Πρώτα τα δύο που επαληθεύτηκαν ζωντανά**: με δηλωμένο επάγγελμα *(ISCO 2149 → 214)* η κεφαλίδα λέει **«Σχέδιο»** *(πριν «Διαχείριση»)*· **σβήνοντας** τη δήλωση **η πρόταση εξαφανίζεται** — πρότυπο Revit «All», ζωντανά. **Και το τρίτο**: το popover έλεγε «Με βάση **τα δικαιώματά σου**» σε `super_admin`, όπου **όλες** οι δουλειές έχουν **ταυτόσημα** δικαιώματα ⇒ **ψευδές**· η αιτία ήταν το **επάγγελμα**, αποδεδειγμένο από το ίδιο το πείραμα *(σβήσιμο ⇒ σιωπή, δικαιώματα αμετάβλητα)*. ⚠️ **Κανένα test δεν μπορούσε να το δει** — το κλειδί i18n ήταν υπαρκτό και η μετάφραση σωστή· έλεγε **λάθος πράγμα**. **Θεραπεία (Υ-6)**: το `JobSuggestionOutcome` αποκτά **μετρημένη** `basis` — κριτήριο *«θα έδινε η ίδια είσοδος άλλη απάντηση χωρίς τον σηματοδότη;»*. ⛔ **ΟΧΙ** `isBypass ? occupation : permissions` *(λάθος σε μη-bypass όπου το επάγγελμα έσπασε ισοβαθμία — κλειδωμένο με τη `Μ12`)*. **Απόδειξη**: 3 άγκυρες `Θ-1` · **3/3 μεταλλάξεις κόκκινες**, ανάμεσά τους η **`Μ10` = κατά λέξη το αρχικό ελάττωμα** · **2.849 / 40** · i18n el/en ταυτόσημα · `jscpd` **0**. |
| 2026-08-25 | 🏆 **ΤΟ ΑΝΟΙΧΤΟ ΤΟΥ §17.4 ΕΚΛΕΙΣΕ ΜΕ ΕΡΕΥΝΑ — Η ΣΙΩΠΗ ΤΟΥ ΠΛΗΡΟΥΣ ΚΑΤΟΧΟΥ** *(νέο §17.7)*. Ερώτημα: *bypass **χωρίς** δήλωση, τι προτείνουμε;* **Και οι τρεις μεγάλοι συμφωνούν ομόφωνα**: **Revit** «if you do not select a discipline … **Autodesk Revit: All**» *(η ειδικότητα είναι για τις **περιορισμένες** άδειες)* · **ArchiCAD** πρώτη εκκίνηση ⇒ **Standard** · **Cinema 4D** ⇒ **Standard**. ⇒ Όποιος δεν δήλωσε ειδίκευση παίρνει το **ΓΕΝΙΚΟ**· εδώ το γενικό είναι το `JOB_ALL`, **ήδη** ενεργό ⇒ **σιωπή**. ⚠️ **Η κομμένη παράθεση ξαναχτύπησε** — η πρώτη αναζήτηση έδωσε «Architectural», ακριβώς το σφάλμα του **Ε7.δ′**. 🔑 Χρειάζεται **ρητός** έλεγχος γιατί για bypass **όλες** ισοβαθμούν *(§17.4)* και ο φρουρός του `granted` **δεν μπορεί** να πυροδοτήσει *(κάθε δουλειά `granted`, κλάδος 1)*. 🏆 **Ξεπερνάμε σε τρία**: ο Revit **σταματά** στο «All» — εδώ η σιωπή γίνεται **πρόσκληση** χωρίς modal· κανείς τους δεν συνδέει **επάγγελμα** με επιφάνεια *(ESCO/ISCO **436** unit groups έναντι 4 disciplines)*· η προεπιλογή τους είναι **στατική**, η δική μας **υπολογισμένη**. 🔴 **Και το fixture ξαναέπεσε στην ίδια παγίδα**: ο helper `suggest()` δεν περνούσε `tiebreak` ⇒ **4** άγκυρες δοκίμαζαν υπερδιαχειριστή που δεν δήλωσε ποτέ τίποτα· διορθώθηκε **το fixture**, όχι ο σχεδιασμός. **Απόδειξη**: 4 άγκυρες `Η-1` · **4/4 μεταλλάξεις κόκκινες σε ΔΥΟ κατευθύνσεις** · **2.846 / 40** · `jscpd` **0**. |
| 2026-08-25 | 🔴 **ΦΑΣΗ 6 — Η ΦΑΣΗ 3 ΗΤΑΝ ΑΔΡΑΝΗΣ ΦΡΟΥΡΟΣ, ΚΑΙ Η §7 ΑΝΕΚΠΛΗΡΩΤΗ ΥΠΟΣΧΕΣΗ** *(νέο §17)*. Βρέθηκαν **ζωντανά**: ο Giorgio δήλωσε «μηχανικός αιολικής ενέργειας» *(ISCO **2149** → πρόθεμα **214** → Σχέδιο, επαληθευμένο στη βάση με **και τα 4** πεδία)* και η κεφαλίδα εξακολουθούσε να προτείνει **«Διαχείριση»**. 🔑 **Ρίζα = ασυμφωνία ΜΕΣΑ στο `jobs-access.ts`**: το `decideJobAccess` ξεχωρίζει ρητά `{kind:'bypass'}` από `{kind:'permission'}`, το `pickDefaultJob` **δεν ρωτούσε**. Ο `super_admin` έχει `permissions: []` αλλά το `useEffectivePermissions:91-93` του δίνει **πάντα** `admin_access`, που ζει σε **ακριβώς μία** από τις 6 δουλειές ⇒ σκορ `{administration:1, άλλες:0}` ⇒ **μοναδικός νικητής** ⇒ ο έλεγχος ισοβαθμίας **δεν εκτελούνταν ΠΟΤΕ**. Μετρημένο **0/4** λογαριασμοί *(ADR-749 §5)*. 🔴 **Καμία άγκυρα δεν το είδε επειδή το fixture `BYPASS` δήλωνε `permissions: []`** — **αδύνατο ζωντανά**, και με κενά permissions όλα ισοβαθμούν ⇒ **πράσινες άγκυρες πάνω σε νεκρό μηχανισμό**· το σχόλιο ήταν **η ίδια η απόκλιση** *(σχήμα 3.34 · 3.37 · 3.57)*. **Θεραπεία Α**: `counted = isBypass ? [] : permissions` — **δεν** παραβιάζει το **Α4** *(το `available` υπολογίζεται πριν και χωρίς αυτό· ο bypass τα έχει ήδη όλα)*. ⚠️ **Δηλωμένη συνέπεια**: bypass **χωρίς** δήλωση πάει από «Διαχείριση» σε **σειρά μητρώου** *(Ε7.δ)* — το πρώτο ήταν **παρενέργεια fallback ορατότητας**· 🔶 **ανοιχτό**: και τα δύο είναι μαντεψιά, το τιμιότερο θα ήταν **σιωπή**. **Θεραπεία Β (§7)**: το `OccupationConfidence` και το `isClassified` είχαν **0** καταναλωτές παραγωγής ⇒ νέο `DeclaredOccupationBadge` στο **UserMenu**, που δείχνει *τι δήλωσε · πόσο το εμπιστευόμαστε · **ποια δουλειά υποδεικνύει*** — η **συνέπεια**, από τον **ίδιο** πίνακα που την εκτελεί. **ΚΑΜΙΑ ερώτηση/modal** *(Ε7.γ′ · Α5)*. **Απόδειξη**: Α → μετάλλαξη **GREEN→RED(3)→GREEN**, 7 άγκυρες με ρεαλιστικό `BYPASS_LIVE`· Β → 13 άγκυρες, **5/5 μεταλλάξεις κόκκινες**· regression **2.842/40** · `jscpd` **0** · i18n el/en ταυτόσημα *(**2 νεκρά κλειδιά** αφαιρέθηκαν)*. |
| 2026-08-24 | ✅ **ΦΑΣΗ 5 — ΤΕΣΣΕΡΑ ΕΛΑΤΤΩΜΑΤΑ ΤΗΣ ΟΘΟΝΗΣ ΠΡΟΦΙΛ ΕΚΛΕΙΣΑΝ** *(§13-§16)*. 🔴 **(1) ΤΑ ΩΜΑ i18n ΚΛΕΙΔΙΑ — Η ΡΙΖΑ ΗΤΑΝ ΚΛΑΣΗ, ΟΧΙ ΔΥΟ ΚΛΕΙΔΙΑ** *(§13)*: το `config.ts` αντικαθιστούσε **καθολικά** το `i18n.t` με remap που έτρεχε **άνευ όρων** και ρωτούσε **μόνο τον στόχο**. Ο χάρτης είναι **ΡΙΖΑΣ**, ο διαχωρισμός ήταν **ΚΛΕΙΔΙΟΥ** ⇒ κλειδιά που **έμειναν πίσω** ήταν **δομικά αδύνατο να ερωτηθούν**. ⚠️ Επειδή το `getFixedT` του i18next τελειώνει σε `return this.t(...)`, η δηλητηρίαση ήταν **κάτω** από τον wrapper — γι΄ αυτό η αλλαγή της προηγούμενης συνεδρίας ήταν αρχιτεκτονικά σωστή και **θεραπευτικά αδιάφορη**. **Δύο ανεξάρτητα όργανα συμφώνησαν**: στατική απογραφή **172** / ζωντανή εκτέλεση της πραγματικής μηχανής **168** ανά γλώσσα *(η διαφορά των 4 **εξηγήθηκε**: string στο αρχικό, αντικείμενο στον στόχο)*, από τα οποία **135 καλούνται όντως**. Θεραπεία = **αυστηρά προσθετική δίπορτη αναζήτηση**: ο στόχος κρατά την προτεραιότητα, το αρχικό ρωτιέται **μόνο** σε αστοχία ⇒ δεν μπορεί να αλλάξει καμία συμβολοσειρά που λύνεται σήμερα. ⚠️ **Η αντίστροφη σειρά απορρίφθηκε ΜΕ ΜΕΤΡΗΣΗ**: **404** κλειδιά υπάρχουν και στα δύο, **149** με **διαφορετική τιμή**, και ο στόχος κρατά παντού τη **νεότερη** γραφή. Ζωντανά: **168 → 0** απρόσιτα *(7.166 κλειδιά σαρωμένα)*, **83/83** επικίνδυνα αμετάβλητα, οθόνη καθαρή. SSoT: εξήχθη το `unresolved-key.ts` *(δεύτερος καλών ⇒ εξαγωγή, όχι αντιγραφή)*. Άγκυρα **15 tests, 6/7 μεταλλάξεις κόκκινες**, η `Μ4` **δηλωμένη ισοδύναμη**. 🎨 **(2) ΤΟ BADGE ESCO ΗΤΑΝ ΑΟΡΑΤΟ** *(§14)*: μετρημένο **1,00:1** — το `text-primary` λύνεται **ταυτόσημα με το `--card`** στο προεπιλεγμένο θέμα *(CHECK 3.38)*. ⚠️ Η προφανής διόρθωση (`--text-info`, που κάνει ο **αδελφός** picker) μετρήθηκε **3,19:1 στο φωτεινό** ⇒ θα αντάλλασσε θέμα με θέμα. Επιλέχθηκε το **Badge SSoT `variant="info"`** — ζεύγος `on-*`, **5,17:1 ΚΑΙ ΣΤΑ ΔΥΟ**. Μαζί, το `pr-16` ήταν **μικρότερο από το badge** (93px) ⇒ `pr-24`. 🔴 **(3) ΤΑ ΠΕΔΙΑ ΟΝΟΜΑΤΟΣ ΞΕΚΙΝΟΥΣΑΝ ΚΕΝΑ** *(§15)*: `useState(user?.givenName)` **παγώνει στην πρώτη απόδοση** ενώ το προφίλ φτάνει αργότερα — **το ίδιο σφάλμα που το ίδιο αρχείο είχε ήδη θεραπεύσει 30 γραμμές πιο κάτω για το επάγγελμα**. Race, όχι σταθερό. ⚠️ Οι εξαρτήσεις είναι **οι τιμές**, ποτέ το αντικείμενο `user` *(αλλιώς σβήνει ό,τι πληκτρολογεί ο άνθρωπος)*. 🖼️ **(4) Η ΦΩΤΟΓΡΑΦΙΑ ΠΡΟΦΙΛ ΥΛΟΠΟΙΗΘΗΚΕ** *(§16, εντολή Giorgio)*: το κουμπί ήταν κυριολεκτικά `disabled` ενώ το κείμενο υποσχόταν αλλαγή. 🏆 **Το μοτίβο είναι των μεγάλων** (Figma: κυκλική προεπισκόπηση + ρυθμιστής ζουμ), **η διαφορά είναι στα bytes**: **δεν ανεβαίνει ΠΟΤΕ το αρχείο του χρήστη** — ανεβαίνει καρέ που παρήγαγε ο encoder του browser ⇒ **EXIF/GPS, polyglot, βόμβα αποσυμπίεσης και απρόβλεπτο μέγεθος γίνονται ΑΔΥΝΑΤΑ *by construction***, όχι με πολιτική που κάποιος πρέπει να θυμάται. 🔴 **Η εμβέλεια είναι ΑΝΑΓΚΑΣΜΕΝΗ, όχι προτίμηση**: ο δείκτης (`photoURL`) ζει στο Firebase Auth και είναι **καθολικός** ⇒ company-scoped bytes θα έδιναν **σπασμένο avatar σε κάθε δεύτερο χώρο**. Path `users/{userId}/avatar.{ext}`, νέο σχήμα κανόνων `authenticated_read_owner_write` *(read = ο αυστηρότερος **εκφράσιμος**: οι Storage rules **δεν βλέπουν Firestore**· παραμένει αυστηρότερο από το σημερινό **δημόσιο** googleusercontent URL)*. ⚠️ Το **CHECK 3.19 μπλόκαρε αμέσως** και γράφτηκε **πραγματική σουίτα**, όχι εγγραφή σε `PENDING` όπως είχε γίνει για τη σφραγίδα. Μηδέν νέα εξάρτηση, μηδέν δεύτερος uploader. 🔴 **Σφάλμα που μόνο η οθόνη έδειξε**: η προεπισκόπηση **δεν ζωγραφιζόταν ΠΟΤΕ** *(portal ⇒ `ref` null στο effect, εξαρτήσεις που δεν ξανααλλάζουν)* — καμβάς **300×150**, μαύρος, **κανένα test κόκκινο**. Θεραπεία: callback ref. Ζωντανά end-to-end: μαθηματικά περικοπής **επαληθευμένα από pixel**, αποθηκευμένο **512×512 WebP / 2.556 bytes**, περικοπή **ταυτόσημη** με την προεπισκόπηση, επιμονή σε reload, αφαίρεση ⇒ **επιστροφή στη φωτογραφία Google** και object **403**. ⚠️ **`null` ≠ «καμία φωτογραφία»** — σημαίνει **επαναφορά στον πάροχο**· το Google **δεν αγγίζεται ποτέ**. |
| 2026-08-24 | ✅ **ΔΙΟΡΘΩΘΗΚΕ Η ΑΠΩΛΕΙΑ ΔΕΔΟΜΕΝΩΝ ΤΟΥ ΟΝΟΜΑΤΟΣ — ΔΥΟ ΑΝΕΞΑΡΤΗΤΑ ΣΤΡΩΜΑΤΑ.** 🔴 **Η βλάβη ήταν μετρημένη, όχι υποθετική** *(ζωντανή επαλήθευση, ίδια μέρα)*: `displayName = "Georgios Pagonis"` → πάτημα «Αποθήκευση» στο προφίλ **χωρίς να αγγιχτούν** τα πεδία ονόματος → **`null`**. Ο χρήστης πατούσε Αποθήκευση για **εντελώς άλλο λόγο** *(δήλωση επαγγέλματος, Φάση 3 Κ4)* και **έχανε το όνομά του**· το `applyProfileNames` υπολόγιζε `` `${givenName} ${familyName}`.trim() `` **άνευ όρων** και το έγραφε στο Firebase Auth, και το επόμενο `syncUserProfileToFirestore` αντέγραφε το κενό ως `null` στο `users/{uid}`. 🔑 **ΤΟ SSoT AUDIT ΑΝΕΤΡΕΨΕ ΤΗ ΔΙΑΤΥΠΩΣΗ ΤΟΥ ΠΡΟΒΛΗΜΑΤΟΣ**: το ερώτημα **δεν ήταν ποτέ** *«πώς σπάμε ένα ονοματεπώνυμο στα δύο»*. Το **OpenID Connect** ορίζει `given_name` και `family_name` ως **ξεχωριστά, πρώτης τάξης claims** και ο πάροχος Google **τα στέλνει ήδη** — αλλά το `useAuthActions.ts` καλούσε `await signInWithPopup(...)` **χωρίς να κρατά το αποτέλεσμα**, δηλαδή **ο διαχωρισμός ερχόταν δωρεάν και πετιόταν**. Γι᾽ αυτό τα δύο πεδία ήταν **κενά για κάθε χρήστη Google**, ενώ η εφαρμογή «ήξερε» το ονοματεπώνυμο μέσω του `displayName`. 🏆 **ΑΥΤΟ ΕΙΝΑΙ ΑΚΡΙΒΩΣ Η ΠΡΑΚΤΙΚΗ ΤΩΝ ΜΕΓΑΛΩΝ, ΚΑΙ ΕΠΑΛΗΘΕΥΤΗΚΕ**: **Google People API** · **Microsoft Graph** *(`givenName`/`surname`)* · **Auth0** κρατούν **ό,τι έδωσε ο πάροχος** και **ΠΟΤΕ** δεν συμπεραίνουν — το κόψιμο στο κενό είναι το κλασικό *falsehood programmers believe about names* *(σειρά ονόματος ανά πολιτισμό· `García López`· `del Carmen`)*. **ΣΤΡΩΜΑ 1 — `adoptProviderNames()`**: διαβάζει `getAdditionalUserInfo(credential)?.profile` και υιοθετεί τα δύο claims **αυτούσια**, στα **ίδια** κλειδιά αποθήκευσης που ήδη διαβάζει ο `buildAuthUser` — **καμία δεύτερη θέση, κανένα νέο σχήμα, μηδέν μαντεψιά**. Κενό ή απόν claim ⇒ **καμία** εγγραφή, ώστε να μην πατηθεί τιμή που έγραψε ο ίδιος ο χρήστης. **ΣΤΡΩΜΑ 2 — η κενή φόρμα ΔΕΝ είναι εντολή διαγραφής**: με **και τα δύο** πεδία κενά, το `displayName` **δεν αγγίζεται**. 🏆 Και το «κανένας ανώνυμος λογαριασμός» **είναι** το πρότυπο, όχι περιορισμός: Google Account, **Microsoft Entra** *(`displayName` υποχρεωτικό)* και το OIDC *(`name`)* δεν προβλέπουν χρήστη χωρίς όνομα. ⚠️ **Η ΜΕΡΙΚΗ συμπλήρωση γράφεται κανονικά** — μόνο το «και τα δύο κενά» είναι μη-εντολή· αλλιώς ο κανόνας θα κλείδωνε τον χρήστη έξω από το να αφαιρέσει **μόνο** το επώνυμό του *(άγκυρα `Σ2.4`)*. 🔒 **ΤΑ ΔΥΟ ΣΤΡΩΜΑΤΑ ΕΙΝΑΙ ΑΝΕΞΑΡΤΗΤΑ ΕΠΙΤΗΔΕΣ** *(N.7.2 #4)*: το Σ1 θεραπεύει την **αιτία** *(κενά πεδία)*, το Σ2 την **επίπτωση** *(απώλεια)* — χρήστης με πάροχο που δεν στέλνει claims **εξακολουθεί** να προστατεύεται. Είναι το ίδιο σχήμα belt-and-suspenders με τον αναγνώστη/γραφέα του επαγγέλματος. 🔬 **8 άγκυρες** *(`Σ1.1-Σ1.4` · `Σ2.1-Σ2.4`)*, **4/4 μεταλλάξεις κόκκινες** — και **δύο** από αυτές είναι **παρονομαστές**: η `Σ1.2` αποδεικνύει ότι πάροχος που στέλνει **μόνο** `name` **δεν** γεννά given/family *(καμία μαντεψιά)*, και η `Μ4` ότι ο φρουρός **δεν** είναι υπερβολικά αυστηρός. ⚠️ **ΔΗΛΩΜΕΝΟ ΟΡΙΟ**: τα `givenName`/`familyName` στο `users/{uid}` παραμένουν `null` — η αυθεντία τους είναι **Firebase Auth + τοπική αποθήκευση** *(`buildAuthUser`)*, και το `syncUserProfileToFirestore` τα γράφει μόνο κατά τη **δημιουργία** εγγράφου. Η ενοποίηση των δύο αποθετηρίων ονόματος είναι **άλλο ερώτημα**. ✅ Πύλες: **17/17** στο `src/auth` · **3.28** *(no new clones)* · **N.7.1** *(395 γρ.)*. |
| 2026-08-24 | 🔬 **ΖΩΝΤΑΝΗ ΕΠΑΛΗΘΕΥΣΗ ΦΑΣΗΣ 3 — ΤΡΙΑ ΕΥΡΗΜΑΤΑ ΠΟΥ ΚΑΜΙΑ ΑΓΚΥΡΑ ΔΕΝ ΜΠΟΡΟΥΣΕ ΝΑ ΔΕΙ.** Εκτελέστηκε στο `localhost:3000` με τον πραγματικό λογαριασμό. **✅ ΤΙ ΑΠΟΔΕΙΧΘΗΚΕ**: *picker → Firestore* **δύο φορές** *(`δικηγόρος` → `iscoCode 2611` + `escoUri …/974d4eab` · `αγρονόμος τοπογράφος μηχανικός` → `2165` + `…/d8e502b4`)*, με **και τα τέσσερα πεδία μαζί** — ο γραφέας τηρεί τη συνοχή ESCO στην πράξη· *Firestore → AuthContext → `useDeclaredOccupation` → οθόνη* **επιβιώνει reload** *(η τιμή **και** το badge ταξινόμησης επανεμφανίζονται)*· και ο **`deleteField()`** σβήνει **και τα τέσσερα** καθαρά — το έγγραφο επανήλθε ακριβώς στην αρχική του κατάσταση. 🔴 **Ε1 — ΔΥΟ ΩΜΑ i18n ΚΛΕΙΔΙΑ ΣΤΗΝ ΟΘΟΝΗ**: το badge ταξινόμησης βάφει **`esco.badge`** και το κουμπί καθαρισμού **`common.clear`**, αμετάφραστα. **Αιτία, μετρημένη**: το `EscoOccupationPicker` **δανείζεται** το `t` *(`useContactPickerTranslation`)* και τα κλειδιά ζουν στο namespace **`contacts`** *(`contacts.json:49`)*, που η σελίδα **λογαριασμού** δεν φορτώνει. Είναι **ακριβώς** το σχήμα του **ADR-744 §18** *(«ξέρω το κλειδί, δεν ξέρω το namespace» — αρχείο που δανείζεται το `t` δηλώνει **μηδέν** namespace)*, και είναι **παλινδρόμηση της Φάσης 3 Κ4**: το component δούλευε σωστά στο CRM και **έσπασε στη νέα του θέση**. ⚠️ Καμία πύλη δεν το έπιασε γιατί το κλειδί **υπάρχει** — τα 3.8/3.13/3.33/3.36 ρωτούν «υπάρχει;», όχι «φτάνει;». 🔴🔴 **Ε2 — ΑΠΩΛΕΙΑ ΔΕΔΟΜΕΝΩΝ: Η ΑΠΟΘΗΚΕΥΣΗ ΜΕ ΑΔΕΙΑ ΠΕΔΙΑ ΟΝΟΜΑΤΟΣ ΣΒΗΝΕΙ ΤΟ `displayName`.** Μετρημένο ζωντανά: `displayName: "Georgios Pagonis"` → μετά από Αποθήκευση **`null`**. **Ο μηχανισμός**: τα `givenName`/`familyName` είναι `null` στο `users/{uid}` *(η σύνδεση Google δίνει μόνο `displayName`, κανείς δεν το έσπασε ποτέ σε δύο πεδία)* ⇒ η φόρμα ξεκινά **κενή** ⇒ ο χρήστης πατά «Αποθήκευση» **για άλλο λόγο** *(π.χ. να δηλώσει επάγγελμα)* και **χάνει το όνομά του**. ⚠️ **Είναι ακριβώς το μάθημα #4 της Φάσης 3, στα πεδία που το ίδιο το changelog είχε ΟΝΟΜΑΣΕΙ** *(«τα γειτονικά `givenName`/`familyName` **έχουν ήδη** αυτό το σφάλμα και **δεν** το αντέγραψα»)* — η δήλωση επαγγέλματος θεραπεύτηκε, οι γείτονες **όχι**, και η ζωντανή δοκιμή έδειξε ότι το κόστος **δεν είναι θεωρητικό**. ✅ **Αποκαταστάθηκε μέσα από την ίδια οθόνη** *(συμπληρώθηκαν τα δύο πεδία → Αποθήκευση)*: το όνομα επανήλθε **και** τα πεδία δεν είναι πλέον κενά. ⛔ **Καμία χειρωνακτική εγγραφή στο Firestore** — ο νόμιμος δρόμος ήταν η οθόνη. 🔑 **Ε3 — ΤΟ ΣΧΕΔΙΟ ΤΗΣ ΔΟΚΙΜΗΣ ΗΤΑΝ ΛΑΘΟΣ, ΚΑΙ Η ΜΕΤΡΗΣΗ ΤΟ ΑΝΕΤΡΕΨΕ**: το handoff δήλωνε ότι *«ο υπερδιαχειριστής είναι η **ΚΑΛΥΤΕΡΗ** περίπτωση δοκιμής … σκορ 0 παντού ⇒ **ισοβαθμία σε όλες**»*. **Ψευδές.** Ο `isBypass` δίνει και τις έξι δουλειές, αλλά το `permissions` του λογαριασμού είναι **ακριβώς `["admin_access"]`** και η `administration` είναι η **ΜΟΝΗ** δουλειά που το ζητά *(`jobs-registry.ts:302`)* ⇒ σκορ **1** έναντι **0** στις άλλες πέντε ⇒ **καθαρός νικητής, καμία ισοβαθμία**. Και επειδή το `pickDefaultJob` σπάει **μόνο** ισοβαθμία *(αυστηρό `>`)*, η πρόταση **σωστά** έμεινε «Διαχείριση» και με τα δύο επαγγέλματα. **Δεν είναι αποτυχία — είναι η άγκυρα `Ε-3` («η ΜΕΤΡΗΣΗ νικά τη ΔΗΛΩΣΗ, ποτέ το αντίστροφο») να εκτελείται ζωντανά.** ⚠️ **Συνέπεια: ο υπερδιαχειριστής είναι η ΧΕΙΡΟΤΕΡΗ περίπτωση δοκιμής, όχι η καλύτερη** — ο τελευταίος κρίκος *(`tiebreak` → πρόταση)* είναι **δομικά απρόσιτος** με αυτόν τον λογαριασμό και **παραμένει ανεπαλήθευτος ζωντανά**· θέλει λογαριασμό με **γνήσια ισοβαθμία**. Το γράφω ρητά αντί να το βαφτίσω επιτυχία: *μια δοκιμή που δεν μπορούσε να αποτύχει δεν απέδειξε τίποτα*. 🔬 **Δηλωμένο όριο του οργάνου**: η επέκταση Claude in Chrome απέτυχε σε `screenshot`/`read_page`/`find` *(ένεση content-script)* σε **κάθε** σελίδα· δούλεψαν το `javascript_tool` και η ανάγνωση κονσόλας *(debugger protocol)*, και μέσω αυτών έγινε όλη η δοκιμή. ⚠️ Και μια οδηγία του handoff ήταν **λάθος για τον browser**: το `127.0.0.1` **δεν** είναι εξουσιοδοτημένος τομέας Firebase ⇒ `auth/unauthorized-domain` στο Google sign-in· η προειδοποίηση «`127.0.0.1`, όχι `localhost`» αφορά το **`fetch` του Node** *(που λύνει πρώτα `::1`)*, **όχι** τον φυλλομετρητή. |
| 2026-08-24 | ✅ **ΦΑΣΗ 4 — ΤΟ ΕΠΑΓΓΕΛΜΑ ΤΑΞΙΔΕΥΕΙ ΣΤΟ IFC. Ο ΡΟΛΟΣ ΦΕΥΓΕΙ, ΤΟ ΟΝΟΜΑ ΟΧΙ.** 🔴 **ΤΟ SSoT AUDIT ΑΝΕΤΡΕΨΕ ΤΟΝ ΑΡΜΟ — ΥΠΑΡΧΟΥΝ ΔΥΟ ΕΞΑΓΩΓΕΙΣ IFC, ΚΑΙ Ο ΣΟΒΑΡΟΣ ΔΕΝ ΗΤΑΝ ΣΤΟ ΣΧΕΔΙΟ**: το σχέδιο έδειχνε τον `guides/guide-ifc-exporter.ts` *(366 γρ., συρραφή συμβολοσειρών)*· υπάρχει όμως ολόκληρο **`src/services/ifc/` — 3.003 γραμμές**, με `IfcGraph` σε discriminated union, σωστό STEP21 escaping `\X2\…\X0\` και serializers για τοίχους/πλάκες/υποστυλώματα/δοκούς/ανοίγματα, **ζωντανό** από το `IfcExportHost.tsx:73`. Ο πρώτος εξάγει **μόνο τον κάνναβο**· ο δεύτερος **ολόκληρο το έργο**. ⚠️ **Η επιλογή ΔΕΝ έγινε με γούστο**: ο εξαγωγέας καννάβου έχει **μηδέν** escaping *(`IFCGRIDAXIS('${guide.label}',…)` ωμά)* και το `escoLabel` είναι **ελληνικό** ⇒ εκεί η Φάση 4 θα παρήγαγε **άκυρο IFC**. Η Φάση 4 μπήκε **μόνο** στον `services/ifc`· ο άλλος **δεν αγγίχτηκε** *(προϋπάρχον ελάττωμα, N.8)*. 🔴 **ΔΕΥΤΕΡΟ ΕΥΡΗΜΑ — ΣΧΟΛΙΟ ΠΟΥ ΕΛΕΓΕ ΨΕΜΑΤΑ, ΑΚΡΙΒΩΣ ΠΑΝΩ ΣΤΟΝ ΑΡΜΟ**: το `ifc-spatial-hierarchy.ts:84` έγραφε `null, // OwnerHistory — **patched in by exporter**` και **μηδέν** `IFCOWNERHISTORY` υπήρχε σε ολόκληρο το `src/services/ifc/` — **κανείς δεν το έκανε patch**. Δίπλα του, το `SpatialHierarchyInput.ownerName?: string` δηλωνόταν ως *«optional override of the IfcProject owner-history label»* και **δεν το διάβαζε κανείς**: η `buildIfcSpatialHierarchy` αποδομεί μόνο `project`/`buildings`/`floors`. **Δύο συμβόλαια που υπόσχονταν ιδιοκτησία, καμία εκτέλεση** — το σχήμα των **CHECK 3.34 / 3.36 / 3.57**. Το νεκρό πεδίο **αντικαταστάθηκε** από το πραγματικό `ownerHistoryID?: number`. 🔴 **ΤΡΙΤΟ — ΤΟ ΔΕΥΤΕΡΟ ΚΑΝΑΛΙ ΤΟΥ §6.2 ΗΤΑΝ ΔΟΜΙΚΑ ΑΝΕΦΑΡΜΟΣΤΟ**, δες **§6.3**: το `IfcRelAssociatesClassification` δέχεται `IfcDefinitionSelect`, και τα `IfcPerson`/`IfcActorRole` **δεν κληρονομούν `IfcRoot`**. Αντικαταστάθηκε από το `IfcExternalReferenceRelationship`, που το πρότυπο **σχεδίασε ακριβώς** γι' αυτό *(«…to entities that **do not inherit from IfcRoot** … similar functionality as the subtypes of IfcRelAssociates»)* και του οποίου το `IfcResourceObjectSelect` περιλαμβάνει **ρητά** `IfcActorRole` και `IfcPerson`. **Η αρχή των δύο καναλιών έμεινε ακέραιη· άλλαξε μόνο ο φορέας.** 🔒 **ΤΟ GDPR ΤΟΥ §3.5 ΔΕΝ ΧΡΕΙΑΣΤΗΚΕ ΑΠΟΦΑΣΗ ΠΡΟΪΟΝΤΟΣ — ΤΟ ΑΠΑΝΤΗΣΕ ΤΟ ΠΡΟΤΥΠΟ** *(§6.4)*: ο ορισμός του `IfcPerson` **προειδοποιεί ρητά** ότι το αρχείο μπορεί να θεωρηθεί *«database that enables identification of a particular person»*, και το **IFC4 CHANGE** χαλάρωσε τον κανόνα *«to allow omission of names **if Identification is provided**»* ⇒ γράφεται **μόνο** σταθερό, μη-ταυτοποιητικό `Identification` και **κανένα όνομα**: αρχείο **έγκυρο** και **ανώνυμο**, όχι ψευδωνυμοποιημένο *(Αιτιολογική σκέψη 26 — η διαφορά είναι νομική, όχι λεπτολογία)*. 🏆 **ΤΟ REVIT ΚΑΝΕΙ ΤΟ ΑΝΤΙΘΕΤΟ ΚΑΙ ΣΤΑ ΤΡΙΑ, ΜΕΤΡΗΜΕΝΟ ΣΤΟΝ ΚΩΔΙΚΑ ΤΟΥ** *(`Autodesk/revit-ifc`, `Exporter.cs`)*: γρ. **3220-3226** γράφει το **πραγματικό όνομα** από `doc.Application.Username` **χωρίς να ρωτήσει**· γρ. **3266** αφήνει **κενό** το `Identification` που το IFC4 χαλάρωσε· γρ. **3271** περνά **`null`** στα `Roles` ⇒ **κανένας ρόλος ποτέ**· και ο **μόνος** `CreateActorRole` του (γρ. **3370**, μόνο COBie) είναι `"UserDefined"` + ελεύθερο κείμενο — **ακριβώς** ό,τι το §6.1 είχε απορρίψει **πριν μετρηθεί**. 🔑 **Ο ΠΙΝΑΚΑΣ ΕΙΝΑΙ ΜΙΚΡΟΣ ΕΠΙΤΗΔΕΣ — 6 ΔΗΛΩΣΕΙΣ, ΚΑΙ Η ΑΥΣΤΗΡΟΤΗΤΑ ΕΙΝΑΙ ΔΩΡΕΑΝ**: το `IfcRoleEnum` **δεν είναι ταξινομία επαγγελμάτων** αλλά λίστα **θέσεων σε έργο** — οι μισές τιμές του *(`CLIENT`·`OWNER`·`CONTRACTOR`·`SUPPLIER`…)* είναι **συμβατικοί ρόλοι** που κανένας κωδικός ISCO δεν εκφράζει, και αντίστροφα δεν υπάρχει **`SURVEYOR`**, ούτε δικηγόρος, ούτε μεσίτης. Δηλώνονται **μόνο** αντιστοιχίες **ταυτόσημες κατά όνομα** *(`214`→ENGINEER · `2142`→CIVILENGINEER · `2144`→MECHANICALENGINEER · `2151`→ELECTRICALENGINEER · `2161`→ARCHITECT · `1323`→CONSTRUCTIONMANAGER)*, γιατί σιωπή του πίνακα ⇒ **`user-defined`**, όπου **το URI ταξιδεύει ακέραιο**: μια χαλαρή δήλωση δεν θα κέρδιζε πληροφορία, θα **αντικαθιστούσε** ακριβή με κατά προσέγγιση. **Τρία σημεία εξαιρέθηκαν με μέτρηση**: `215` *(περιέχει 2152 ηλεκτρονικών / 2153 τηλεπικοινωνιών)* · `216` *(περιέχει 2163 / 2166)* · `132` *(περιέχει βιομηχανία/ορυχεία/διανομή)*. **Όλοι οι τίτλοι ISCO-08 επαληθεύτηκαν έναν προς έναν στο επίσημο ESCO API** — και η πρώτη αναζήτηση στο διαδίκτυο για το `IfcRoleEnum` επέστρεψε **19** τιμές αντί **23**, χάνοντας `FIELDCONSTRUCTIONMANAGER`·`RESELLER`·`USERDEFINED`: *ποτέ από μνήμη, ποτέ από περίληψη*. 🔑 **ΠΕΝΤΕ ΡΗΤΕΣ ΚΑΤΑΣΤΑΣΕΙΣ** *(`enumerated`·`user-defined`·`unclassified`·`absent`·`malformed`)*, και **η σειρά των ελέγχων είναι το συμβόλαιο**: το URI κρίνεται **ΠΡΙΝ** τον πίνακα, αλλιώς ένα **ορφανό** `iscoCode` θα έδινε `enumerated` με **κενό** δεύτερο κανάλι — μηχανισμό «με ή» από την πίσω πόρτα. ⚠️ Το `unclassified` *(σκέτο ελεύθερο κείμενο)* εκπέμπει **τίποτα**, γιατί `USERDEFINED` + ωμό ελληνικό **χωρίς** URI είναι ακριβώς το §6.1. 🔑 **Ο ΜΗΧΑΝΙΣΜΟΣ ΕΞΗΧΘΗ, Ο ΠΙΝΑΚΑΣ ΟΧΙ** *(N.0.2)*: η ανάλυση προθέματος ISCO θα γραφόταν **δεύτερη φορά** ⇒ `src/config/isco-prefix.ts` *(`resolveIscoPrefix` + `ISCO_CODE_SHAPE`)*, με το `isco-job-affinity.ts` να **αναθέτει**. Δύο αντίγραφα θα ήταν **δύο ορισμοί του «τι είναι έγκυρος κωδικός ISCO»**, δηλαδή δύο απαντήσεις στο `malformed` *(ADR-749)*. **Οι πίνακες μένουν χωριστοί** και η ανάγκη είναι **μετρημένη**: ο πίνακας IFC χρειάζεται `2142`/`2144` που ο πίνακας δουλειάς **δεν έχει**, γιατί για την πλοήγηση «πολιτικός» και «μηχανολόγος» είναι η **ίδια** απάντηση. 🔑 **Και η έκδοση ESCO έγινε σταθερά** *(`ESCO_CLASSIFICATION_SOURCE`)*: ζούσε **μόνο σε πρόζα**, σε **δύο** αρχεία — και από τη στιγμή που ένας **εξαγωγέας** τη γράφει σε αρχείο που φεύγει σε τρίτους, η πρόζα δεν αρκεί. 🔬 **ΑΠΟΔΕΙΞΗ: 10/10 ΜΕΤΑΛΛΑΞΕΙΣ ΚΟΚΚΙΝΕΣ** σε **31** νέες άγκυρες *(16 πίνακας + 15 εκπομπή)* **+ 4 στον ΑΝΑΦΟΡΙΚΟ PARSER**. ⚠️ **Οι τέσσερις τελευταίες είναι το κέντρο**: τρέχουν μέσα στο υπάρχον `ifc-roundtrip.test.ts` και ρωτούν το **`web-ifc`** — **τρίτη υλοποίηση** ανοίγει το αρχείο και επιβεβαιώνει ότι βλέπει `.ARCHITECT.`, διαβάζει το ESCO URI ακέραιο, **αποκωδικοποιεί** το ελληνικό «αρχιτέκτονας κτιρίων» από το `\X2\` escaping, και **δεν βρίσκει όνομα ανθρώπου σε κανένα από τα πέντε πεδία ονόματος**. *Οι δικές μας άγκυρες αποδεικνύουν «γράψαμε τα σωστά ορίσματα»· μόνο ο αναφορικός parser αποδεικνύει «κάποιος άλλος το διαβάζει σωστά».* ⚠️ **Το μάθημα #4 ξαναχτύπησε και θεραπεύτηκε δωρεάν**: το `declaredOccupation` φτάνει **αργότερα** από την πρώτη απόδοση, αλλά ο `IfcExportHost` είχε **ήδη** το `stateRef` idiom (ADR-040) ⇒ μπήκε εκεί, **καμία νέα μηχανή**. ⚠️ **Η εισαγωγή είναι `@/auth`, ΠΟΤΕ `@/hooks/useAuth`** — και η παγίδα είναι **χειρότερη** απ' όσο καταγράφηκε: **μέσα στο ίδιο το `src/auth/index.ts`**, η γρ. 66 εξάγει σκέτο `useAuth` από `./hooks/useAuth` ενώ το context εξάγεται ως `useAuthContext` (γρ. 20)· εδώ σώζει το ότι το `src/auth/hooks/useAuth.ts` **τυλίγει** το context. 🔴 **Δύο σφάλματα πληρώθηκαν μέσα στη δουλειά**: (α) το `String.replace` ερμηνεύει το ``$` `` ως «ό,τι προηγείται του match» ⇒ **κατέστρεψε** αρχείο εισάγοντας ολόκληρο το πρόθεμά του· επισκευάστηκε διαβάζοντας `git show HEAD:` *(read-only — **ποτέ** `checkout`/`reset`, το δέντρο είναι κοινό)* και όλες οι επόμενες αντικαταστάσεις έγιναν με **συνάρτηση**· (β) η καλωδίωση `ownerHistoryID → IfcProject` **δεν είχε άγκυρα**, δηλαδή θα ήταν άλλο ένα «σχόλιο που υπόσχεται» — γι' αυτό γράφτηκε η άγκυρα του parser. ✅ Πύλες: **3.22** *(baseline 10, αμετάβλητη)* · **3.28** *(jscpd: no new clones σε 8 αρχεία)* · **3.49** *(140/8)* · **3.53** · **N.7.1** *(max **293** γρ.)* · **134/134** πράσινα σε **9** σουίτες, μαζί με τις **προϋπάρχουσες** του `services/ifc` που τώρα εξάγουν με `IfcOwnerHistory`. 🔶 **ΔΗΛΩΜΕΝΟ ΟΡΙΟ**: το `IfcOwnerHistory` δένεται **μόνο** στο `IfcProject` — έγκυρο, γιατί το IFC4 έκανε το `IfcRoot.OwnerHistory` **OPTIONAL**· η Φάση 4 απαντά *«ποιος συνέταξε το αρχείο»*, όχι *«ποιος άγγιξε κάθε τοίχο»*, που θέλει ιστορικό αλλαγών ανά οντότητα και δεν υπάρχει. 🔶 **ΕΚΚΡΕΜΕΙ**: η **ζωντανή** επαλήθευση της Φάσης 3 στην οθόνη — μπλοκαρισμένη από την επέκταση Claude in Chrome *(`Script injection timed out` σε **κάθε** σελίδα, επιβεβαιωμένο και σε `example.com`· ο dev server απαντά **200**)*. |
| 2026-08-24 | ✅ **ΦΑΣΗ 3 / Κ4 — Η ΔΗΛΩΣΗ ΑΠΕΚΤΗΣΕ ΟΘΟΝΗ. Η ΦΑΣΗ 3 ΕΚΛΕΙΣΕ.** Το πεδίο ζει στο `account/profile`, με το **υπάρχον** `EscoOccupationPicker` — **κανένα νέο picker** *(Α5: καμία modal, καμία ερώτηση πριν ή μετά το login· ο Revit ρωτά με modal και το πληρώνει με άρθρο «How to disable»)*. 🔴 **ΤΟ SSoT AUDIT ΑΝΕΤΡΕΨΕ ΤΟΝ ΠΡΟΦΑΝΗ ΣΤΟΧΟ**: το `updateUserProfile()` γράφει σε **Firebase Auth** *(`updateProfile`)* + **localStorage** και **δεν αγγίζει Firestore πουθενά** — το επάγγελμα ζει στο `users/{uid}`, δηλαδή σε **άλλο αποθετήριο**. Επέκτασή του θα ήταν λάθος στόχος· ο νέος γραφέας μπήκε στο `auth-context-profile.ts`, που **ήδη κατέχει** το έγγραφο από τον πελάτη *(`setDoc(merge:true)`)* — **δίπλα στον αναγνώστη του, όχι σε δεύτερο σπίτι**. ✅ **ΜΕΤΡΗΘΗΚΕ ΟΤΙ ΤΟ `firestore.rules` ΔΕΝ ΧΡΕΙΑΖΕΤΑΙ ΑΛΛΑΓΗ**: το `isHonestSelfUpdate()` είναι **denylist** *(`!changed.hasAny(serverOwnedUserFields())`)*, όχι allowlist ⇒ τα τέσσερα πεδία είναι ελεύθερα γράψιμα από τον ίδιο, ενώ το `occupationVerification` παραμένει κλειδωμένο. **Μηδέν emulator** *(−10 λεπτά)*. 🔒 **Ο ΓΡΑΦΕΑΣ ΕΠΙΒΑΛΛΕΙ ΟΤΙ Ο ΤΥΠΟΣ ΤΕΚΜΗΡΙΩΝΕΙ ΑΛΛΑ ΔΕΝ ΜΠΟΡΕΙ**: το `DeclaredOccupation` λέει ρητά *«τα τρία πεδία ESCO πάνε ΠΑΝΤΑ μαζί … ο **γραφέας** οφείλει»* — εδώ **χωρίς `escoUri` σβήνονται και το `escoLabel` και το `iscoCode`**, άρα ο **ορφανός κωδικός γίνεται δομικά αδύνατος**. Είναι **belt-and-suspenders** *(N.7.2 #4)* με τον αναγνώστη: εκείνος αρνείται να **εκθέσει** ορφανό κωδικό, αυτός αρνείται να τον **γεννήσει** — δύο **ανεξάρτητες** άμυνες, γιατί δεδομένα από import ή χειρόγραφη επεξεργασία δεν περνούν από εδώ. ⚠️ **`deleteField()`, ποτέ `''`** *(σύμβαση `contacts.service.ts:334`)*: το κενό string θα ήταν **ψεύτικο δεδομένο** που κάθε επόμενος αναγνώστης θα έπρεπε να θυμάται να φιλτράρει. ⚠️ **Η κατάσταση τίθεται από ό,τι ΓΡΑΦΤΗΚΕ, όχι από ό,τι πληκτρολογήθηκε** — αλλιώς η οθόνη θα έδειχνε ταξινομημένο κάτι που ο γραφέας μόλις καθάρισε. 🔴 **ΤΟ ΣΦΑΛΜΑ ΤΗΣ ΦΑΣΗΣ 2 ΞΑΝΑΕΜΦΑΝΙΣΤΗΚΕ ΣΕ ΝΕΑ ΘΕΣΗ**: το `declaredOccupation` φτάνει **αργότερα** από την πρώτη απόδοση, οπότε σκέτο `useState(declaredOccupation ?? {})` θα **πάγωνε** στο αρχικό `null` και το πεδίο θα φαινόταν **κενό σε άνθρωπο που έχει ήδη δηλώσει** — που θα τον έβαζε να πατήσει «Αποθήκευση» και να **σβήσει** τη δήλωσή του. Θεραπεία: `useEffect` συγχρονισμού. ⚠️ **Τα γειτονικά `givenName`/`familyName` έχουν ΗΔΗ αυτό το σφάλμα** και **δεν** το αντέγραψα — *γειτονικό ελάττωμα δεν είναι άδεια*. 🔑 **A11y χωρίς scope creep**: ο `LinkedSinglePickerView` **δεν δέχεται `id`**, γι' αυτό το `htmlFor` του `SurveyLinkedContactField.tsx:137` **ήδη δείχνει στο πουθενά**. Αντί να περάσω `inputId` μέσα από **5** κοινά αρχεία, ο picker ζει **ΜΕΣΑ** στο `<Label>`: **έμμεση** συσχέτιση, έγκυρη HTML, δουλεύει επειδή ο `PopoverAnchor` είναι `asChild` πάνω σε **σκέτο `<div>`** ⇒ το πρώτο labelable στοιχείο **είναι το input** *(το κουμπί καθαρισμού έρχεται μετά και έχει δικό του `aria-label`)*. **Επαληθεύτηκε** ότι το Radix Label δεν σπάει το διπλό κλικ — ο guard του κάνει `closest("button, input, select, textarea")`. 🔬 **6/6 μεταλλάξεις κόκκινες** σε **9** νέες άγκυρες *(Γ-1…Γ-5)*, με τον δείκτη-φρουρό `Symbol('deleteField')` ώστε το «σβήσιμο» να μην μπορεί να συγχέεται με τιμή. ✅ **N.11 τηρήθηκε κατά γράμμα**: **3 κλειδιά σε `el` ΚΑΙ `en` ΠΡΙΝ** τον κώδικα, μηδέν `defaultValue`. Πύλες: **3.8** · **3.22** *(baseline 10)* · **3.28** · **3.33** *(56 tests — τα i18n types αναγεννήθηκαν)* · **3.34** *(το `common-account` είναι shell namespace — το slice αναγεννήθηκε)* · **3.36** · i18n audit **«no change since baseline»** · **2.814** πράσινα στα `src/auth`+`src/components/account`+`src/config`+`src/hooks`. Αρχεία max **359** γρ. 🔴 **Boy Scout, δεύτερο εύρημα στο `src/auth/`**: ο **`AuthContextType` δηλώνεται ΔΥΟ φορές** — `AuthContext.tsx:35` *(ζωντανός)* και `auth.types.ts:115` *(`AuthContextState & AuthContextActions`)*, και ο **δεύτερος έχει ήδη αποκλίνει**: του λείπει το `declaredOccupation` της Φάσης 2. 🔴 Το `src/auth/index.ts:102` επανεξάγει τον **στάσιμο**, άρα `import type { AuthContextType } from '@/auth'` δίνει **άλλο σχήμα** από ό,τι επιστρέφει το `useAuth()`. **Λανθάνον σήμερα** *(μηδέν καταναλωτές το επικαλούνται ρητά)*, αλλά είναι το **δεύτερο** στιγμιότυπο του ίδιου σχήματος στον ίδιο φάκελο μέσα σε μία μέρα. Καταγράφηκε· **N.8**. |
| 2026-08-24 | ✅ **ΦΑΣΗ 3 (πυρήνας) — ΤΟ ΕΠΑΓΓΕΛΜΑ ΣΠΑΕΙ ΙΣΟΒΑΘΜΙΑ, ΠΟΤΕ ΔΕΝ ΔΙΕΥΡΥΝΕΙ ΣΥΝΟΛΟ.** 🔴 **Το κενό, ονομασμένο**: ένας **τοπογράφος** και ένας **δικηγόρος** με **ταυτόσημα** permissions έπαιρναν **ταυτόσημη** προεπιλογή, και ο νικητής έβγαινε από τη **σειρά του `JOB_ORDER`** — δηλαδή από **τίποτα**. 🔑 **ΚΛΕΙΔΙ ΤΟ ΠΡΟΘΕΜΑ ISCO, ΚΑΙ ΤΟ ΑΠΟΦΑΣΙΣΕ Η ΔΟΜΗ ΤΟΥ ΠΡΟΤΥΠΟΥ**: το ISCO-08 είναι **10 / 43 / 130 / 436** ομάδες όπου τα πρώτα ψηφία ενός κωδικού **ΕΙΝΑΙ** η γονική του ομάδα *(επαληθεύτηκε στην επίσημη δομή του ILO, δεν γράφτηκε από μνήμη)* ⇒ η ανάλυση **μεγαλύτερου προθέματος** δεν είναι ευρετική, είναι **ακριβής**. Έτσι δηλώνεται συγγένεια **στο επίπεδο που όντως ξέρουμε**: `214` για **όλους** τους μηχανικούς με μία γραμμή, `2161` ξεχωριστά. Ο εναλλακτικός σχεδιασμός — πίνακας **436** μοναδιαίων ομάδων — θα ήταν 436 γραμμές από τις οποίες θα ξέραμε τις ~20. ⛔ **ΟΧΙ ESCO URI**: **3.039** ταυτότητες που αλλάζουν ανά έκδοση, ενώ το ISCO είναι παγωμένο από το 2008· και επειδή ESCO→ISCO είναι **ολική**, το πρόθεμα απαντά για **όλα** τα ESCO χωρίς να τα απαριθμεί. 🏆 **ΠΟΥ ΞΕΠΕΡΝΑΜΕ ΤΙΣ ΙΔΙΕΣ ΤΙΣ ΔΙΑΣΤΑΥΡΩΣΕΙΣ ΤΗΣ ΕΕ — ΤΟ «ΔΕΝ ΞΕΡΩ»**: τα **ESCO→O\*NET** και **ESCO→NACE** χαρτογραφούν **και τα 3.039** επαγγέλματα σε «**τουλάχιστον μία**» κατηγορία *(το πρώτο χτίστηκε με **AI + ανθρώπινη επικύρωση** γι' αυτόν ακριβώς τον λόγο)* ⇒ **δεν μπορούν δομικά να εκφράσουν «δεν ξέρω»**. Εδώ η απουσία είναι **ονομασμένη κατάσταση** (`undeclared`) και η σιωπή είναι **σωστή απάντηση**. 🏆 **Και το NIST SP 800-63 λέει το ίδιο με το Α4**: σε **IAL1** τα αυτο-δηλωμένα γνωρίσματα είναι κατάλληλα για **εξατομίκευση**, ενώ οι **αποφάσεις εξουσιοδότησης** απαιτούν **επαληθευμένα** (IAL2/IAL3) — δηλαδή το **§7** δεν είναι πολυτέλεια, είναι το πρότυπο. *(Το **Revit** ρωτά με modal «Ask Profile» επειδή **δεν ξέρει**· εμείς ξέρουμε — §6.14 του ADR-748.)* 🔒 **ΤΡΕΙΣ ΔΟΜΙΚΕΣ ΕΓΓΥΗΣΕΙΣ, ΚΑΜΙΑ ΣΥΜΒΑΣΗ**: (1) το `available` υπολογίζεται **πριν** και **χωρίς** το `tiebreak` ⇒ αν δεν είναι μέλος του, ο βρόχος **δεν το συναντά ποτέ**· (2) ο έλεγχος είναι `===`, **ποτέ `>=`** ⇒ η **μέτρηση** προηγείται της **δήλωσης**· (3) το `jobs-access.ts` **δεν εισάγει** το `isco-job-affinity.ts` — **δεν ξέρει καν** ότι υπάρχει επάγγελμα, και αυτή η άγνοια **είναι** η εγγύηση #1. ⚠️ **ΔΥΟ ΣΗΜΕΙΑ ΟΠΟΥ Η ΓΕΝΙΚΕΥΣΗ ΘΑ ΗΤΑΝ ΛΑΘΟΣ, ΜΕΤΡΗΜΕΝΑ**: το `216` **δεν** δηλώνεται ολόκληρο *(περιέχει `2163` σχεδιαστές προϊόντος και `2166` γραφίστες)*· το `332` **ούτε** — το `3322` *(εμπορικοί αντιπρόσωποι)* είναι **Πελάτες** ενώ το `3323` *(αγοραστές)* **Προμήθειες**, δύο **αντίθετες** πλευρές της ίδιας ελάσσονος ομάδας. Και **μία εξαίρεση με πρόθεμα** που δείχνει τον μηχανισμό: `121` → Διαχείριση, `1211` → Οικονομικά. 🔑 **Ο ΑΝΑΓΝΩΣΤΗΣ: το `escoUri` ΦΥΛΑΕΙ το `iscoCode`, ΚΑΙ ΕΙΝΑΙ ΜΕΤΡΗΜΕΝΟ** — ο **μοναδικός** γραφέας (`EscoOccupationPicker`) εκπέμπει είτε `buildSelected` *(και τα τρία)* είτε `buildFreeText` *(κανένα)*, **δεν υπάρχει τρίτος δρόμος** ⇒ `iscoCode` χωρίς `escoUri` είναι εγγραφή **αγνώστου προελεύσεως** και **δεν εκτίθεται**: ορφανό ψηφίο δεν οδηγεί πρόταση. ✅ **Ο νεκρός αναγνώστης της Φάσης 2 ΑΝΑΣΤΗΘΗΚΕ ΜΑΖΙ ΜΕ ΤΟΝ ΚΑΤΑΝΑΛΩΤΗ ΤΟΥ** *(`useJobSuggestion`)* — **κανένα reseed**, `.deadcode-baseline.json` **αμετάβλητη στα 10**. 🔬 **ΑΠΟΔΕΙΞΗ ΜΕ ΜΕΤΑΛΛΑΞΗ: 11/11 ΚΟΚΚΙΝΕΣ** *(45 άγκυρες: 33 στην πρόταση + 12 στον αναγνώστη)*. ⚠️ **Η `Μ1` ΒΓΗΚΕ ΠΡΑΣΙΝΗ την πρώτη φορά** και ο λόγος είναι μάθημα: `===` → `>=` είναι **σημασιολογικά ουδέτερο** εδώ, γιατί ο κλάδος `score > bestScore` από πάνω κάνει `continue` ⇒ ο δεύτερος **δεν φτάνει ποτέ** με μεγαλύτερο σκορ. *Μετάλλαξη που δεν αλλάζει συμπεριφορά αποδεικνύει **μηδέν*** — ίδιο σχήμα με τη `Μ6` του **CHECK 3.40** *(στόχευε το `.sort()`)*. Ξαναγράφτηκε ως «το `tiebreak` κρίνεται **ΠΡΙΝ** το σκορ» ⇒ κόκκινη. ⚠️ **Και η άγκυρα `Ρ-6` ΓΕΝΝΗΘΗΚΕ ΔΙΑΚΟΣΜΗΤΙΚΗ**: χρησιμοποιούσε δεύτερο `renderHook`, δηλαδή **νέο στιγμιότυπο**, που ξαναϋπολογίζει έτσι κι αλλιώς ⇒ θα έμενε **πράσινη ακόμη και με κενές εξαρτήσεις `useMemo`** — ακριβώς το σφάλμα της Φάσης 2 που υποτίθεται ότι φυλάει. Με `rerender` η `Μ11` βγαίνει κόκκινη. ⚠️ **Οι μεταλλάξεις έγιναν με εγγυημένη επαναφορά** *(`process.on('exit')`, αρχείο `tmpclaude-*` gitignored)*: το δέντρο είναι κοινό και ο auto-committer **κομμιτάρισε ενώ έτρεχαν** — επαληθεύτηκε **με `git show`** ότι το `59d3b4c6` περιέχει την **αμετάλλακτη** έκδοση. 🔴 **Boy Scout (N.0.2) — ΔΥΟ `useAuth` ΜΕ ΤΟ ΙΔΙΟ ΟΝΟΜΑ**: `src/hooks/useAuth.ts` *(ωμό Firebase, `{user, loading}`)* **58** καταναλωτές έναντι `@/auth` *(context, **19** πεδία μαζί με το `declaredOccupation`)* **14**. Γράφοντας `const { declaredOccupation } = useAuth()` με λάθος εισαγωγή παίρνεις **`undefined`** και στην οθόνη φαίνεται «δεν έχει επάγγελμα» **για πάντα**. Σχήμα **CHECK 3.59** *(`Point3D`)*, εκτός εμβέλειάς του σήμερα. Καταγράφηκε στο `.claude-rules/pending-ratchet-work.md` — **72 αρχεία ⇒ N.8, δική του συνεδρία**. ✅ Πύλες: **3.22** *(baseline 10, αμετάβλητη)* · **3.28** · **3.34** *(και τα δύο νέα αρχεία μπήκαν στην κλειστότητα του κελύφους — το manifest αναγεννήθηκε)* · **3.49**. 🔶 **ΕΚΚΡΕΜΕΙ το Κ4** *(η **δήλωση** από τον χρήστη με το υπάρχον `EscoOccupationPicker` σε σελίδα προφίλ)* — **απόφαση Giorgio**, ξεχωριστό βήμα: είναι ο μόνος τομέας UI και σέρνει `updateUserProfile` *(δέχεται μόνο όνομα/επώνυμο σήμερα)* + i18n `el`/`en`. ✅ **Μετρήθηκε ότι το `firestore.rules` ΔΕΝ χρειάζεται αλλαγή**: ο φρουρός της Φάσης 2 μπλοκάρει **μόνο** το `occupationVerification`· τα τέσσερα πεδία της δήλωσης είναι ήδη γράψιμα από τον ίδιο τον χρήστη. |
| 2026-08-24 | ✅ **ΦΑΣΗ 2 — Ο ΑΝΘΡΩΠΟΣ ΑΠΕΚΤΗΣΕ ΕΠΑΓΓΕΛΜΑ.** Το `UserProfileDocument` κληρονομεί `DeclaredOccupation`· το `AuthContext` το κρατά και το εκθέτει. **Ο-1 και Ο-2 απαντήθηκαν** *(§11)*. 🔑 **ΜΗΔΕΝ ΕΠΙΠΛΕΟΝ I/O — ΚΑΙ ΕΙΝΑΙ ΤΟ ΚΕΝΤΡΟ ΤΟΥ ΣΧΕΔΙΑΣΜΟΥ**: το επάγγελμα ζει στο Firestore και **ποτέ στα claims** (Α4), άρα ένας αφελής αναγνώστης θα έκανε `getDoc` σε **κάθε σελίδα** — η προειδοποίηση του `useEffectivePermissions.ts:27-30`. Το `syncUserProfileToFirestore` όμως έκανε **ήδη** `getDoc` σε κάθε σύνδεση και **πετούσε** το αποτέλεσμα· τώρα το επιστρέφει. **Η ανάγνωση υπήρχε· χανόταν.** 🔑 **Ο-1 → ΕΠΙΠΕΔΑ ΠΕΔΙΑ, και τον λόγο τον έδωσαν οι ΚΑΝΟΝΕΣ**: το `diff().affectedKeys()` βλέπει **κορυφαία** κλειδιά μόνο ⇒ μέσα σε υπο-αντικείμενο ο φρουρός **δεν μπορεί** να ξεχωρίσει *δηλωμένο* από *επαληθευμένο*. 🔴 **Ο ΦΡΟΥΡΟΣ ΤΟΥ §7 ΜΠΗΚΕ ΠΡΙΝ ΥΠΑΡΞΕΙ ΤΟ ΠΕΔΙΟ** *(πρότυπο CHECK 3.43 Κ1)*: το `occupationVerification` είναι πλέον στα `serverOwnedUserFields()`. Το **επάγγελμα** μένει ελεύθερο *(αυτο-δηλωμένο, δεν δίνει δικαίωμα — Α4)*· η **επαλήθευσή** του **ποτέ**, αλλιώς «επαληθευμένο» σημαίνει «το είπα μόνος μου» και το §7 πεθαίνει τη στιγμή που γεννιέται. ⚠️ **Το ΟΝΟΜΑ είναι συμβόλαιο**: αν η Φάση 5 διαλέξει άλλο, ο φρουρός μένει **πράσινος και ανενεργός** (ADR-749 §5). 🔬 **ΑΠΟΔΕΙΞΗ ΜΕ ΜΕΤΑΛΛΑΞΗ, ΟΧΙ ΙΣΧΥΡΙΣΜΟ**: άγκυρες **Κ13** *(δήλωση επαγγέλματος → ALLOW — ο **παρονομαστής**)* + **Κ14** *(αυτο-γραμμένη επαλήθευση → DENY)*· η μετάλλαξη **μετονόμασε** το πεδίο στους κανόνες *(ακριβώς ο κίνδυνος παραπάνω)* και έδωσε `verification: DENY → **ALLOW**` με το `profession` **ALLOW και στις δύο** ⇒ ο φρουρός είναι **φέρων** και το επάγγελμα **δεν κλειδώθηκε κατά λάθος**. ⚠️ Η μετάλλαξη έγινε **ΣΤΗ ΜΝΗΜΗ**, ποτέ στον δίσκο: το δέντρο είναι κοινό με άλλον πράκτορα και τρέχει auto-committer — προσωρινή αφαίρεση φρουρού θα μπορούσε να **κομμιταριστεί ως regression**. ⚠️ Και ο μεταλλάκτης **ούρλιαξε σωστά** την πρώτη φορά *(«Η ΜΕΤΑΛΛΑΞΗ ΔΕΝ ΑΛΛΑΞΕ ΤΙΠΟΤΑ»* — CRLF): *μια μετάλλαξη που δεν άλλαξε τίποτα αποδεικνύει μηδέν* (μάθημα 3.44/Μ11). 🔴 **Η ΠΥΛΗ ΔΕΝ ΜΕ ΑΦΗΣΕ ΝΑ ΠΑΡΑΔΩΣΩ ΤΟΝ ΑΝΑΓΝΩΣΤΗ, ΚΑΙ ΕΙΧΕ ΔΙΚΙΟ**: το `useDeclaredOccupation` γράφτηκε και το **CHECK 3.19/3.22** το κατήγγειλε ως **νεκρό αρχείο** — σωστά, γιατί ο καταναλωτής του είναι η **Φάση 3**. Το `knip.json` **εξαιρεί ρητά** τα tests από το `project` *(`!src/**/*.test.*`, `!src/**/__tests__/**`)*, άρα **καμία άγκυρα δεν μπορούσε να το σώσει**. ⛔ **Reseed της baseline ΑΠΟΡΡΙΦΘΗΚΕ**: θα κλείδωνε νεκρό αρχείο ως «γνωστό», δηλαδή *απουσία ως πρόοδο* (μάθημα 3.38). **Ο αναγνώστης μετακομίζει στη Φάση 3, μαζί με τον καταναλωτή του** — baseline **αμετάβλητη στα 10**. Το `AuthContext.declaredOccupation` **μένει**: είναι πεδίο σε interface που καταναλώνεται ήδη παντού, όχι νεκρό αρχείο. ⚠️ `declaredOccupation` **στις εξαρτήσεις του `useMemo`** — χωρίς αυτό η τιμή παγώνει στο `null` της πρώτης απόδοσης και το επάγγελμα φαίνεται «μη δηλωμένο» **για πάντα**, σφάλμα που **καμία πύλη δεν πιάνει** και που στην οθόνη μοιάζει με «ο χρήστης δεν έχει επάγγελμα». ✅ Πύλες: **3.34** *(shell slice — και τα δύο αρχεία που άγγιξα ΕΙΝΑΙ shell modules)* · **3.19/3.22** · **3.28** · **3.49** · **3.53**· `firestore.rules.compiled` **αναγεννήθηκε** *(tracked παράγωγο — CHECK 3.33)*. 🔶 **Δηλωμένο όριο**: το `CustomClaims` παραμένει **5 πεδία** — το επάγγελμα **δεν** μπήκε και δεν πρόκειται (Α4 · όριο token **1.000 bytes**, ADR-787 Ε-5). |
| 2026-08-24 | ✅ **ΦΑΣΗ 1 — ΤΟ ΛΕΞΙΛΟΓΙΟ ΕΓΙΝΕ ΚΟΙΝΟ, ΧΩΡΙΣ ΝΑ ΑΝΤΙΓΡΑΦΕΙ.** 🔴 **Το SSoT audit βρήκε ότι η διπλοτυπία ΗΤΑΝ ΗΔΗ ΕΚΕΙ**: τα τέσσερα πεδία *(`profession` · `escoUri` · `escoLabel` · `iscoCode`)* δηλώνονταν **δύο φορές** — ως `EscoPickerValue` *(`esco-types.ts:163`, **ονομασμένο από το UI component που το εκπέμπει**)* και **ενσωματωμένα** πάνω στο `IndividualContact`, με το `contracts.ts` να **μην εισάγει καθόλου** από το `esco-types.ts`. Δηλαδή η Φάση 1 δεν ήταν «εξαγωγή», ήταν **ενοποίηση δύο ζωντανών αληθειών** (ADR-749). Νέο **ουδέτερο** σπίτι: `src/types/professional-identity.ts` → **`DeclaredOccupation`**. 🔑 **Το όνομα ΔΕΝ λέει «ESCO» επίτηδες** — *το όνομα δηλώνει τι **ΕΓΓΥΑΤΑΙ*** (μάθημα **CHECK 3.59 / ADR-792**, το `Point3D` που δεν εγγυόταν `z`): ο τύπος **δεν** εγγυάται ταξινόμηση, το `profession` μπορεί να είναι σκέτο ελεύθερο κείμενο, και αυτό είναι **σχεδιασμός** *(ADR-132 §1)*. Ένα `EscoOccupationRef` θα υποσχόταν ό,τι δεν κρατά, και ο επόμενος θα έγραφε `escoUri!` πάνω σε `undefined`. 🔑 **ΔΕΝ ορίζει πληθικότητα, επίτηδες** ⇒ το **Ο-2** μένει ανοιχτό **δωρεάν** *(το **στοιχείο** είναι το ίδιο για `extends` και για `[]`)*. Το `EscoPickerValue` έγινε **στένωση** *(`Required<Pick<…,'profession'>>` — ρητή, γιατί ένα picker **έχει πάντα κείμενο** ενώ μια εγγραφή **μπορεί να μην έχει επάγγελμα**· η διαφορά **δεν** ισοπεδώθηκε)*. 🔬 **ΑΠΟΔΕΙΞΗ ΧΩΡΙΣ `tsc` (N.17)** — μια μετακίνηση τύπου είναι **αόρατη στο jest**, οπότε πράσινα tests δεν θα απεδείκνυαν τίποτα· χρησιμοποιήθηκε η **κλειστή λογιστική** του CHECK 3.59: χρήσεις σε αρχεία **που δεν αγγίχτηκαν**, πριν και μετά — `escoUri` **79 = 79** · `escoLabel` **77 = 77** · `iscoCode` **105 = 105** *(οι «πριν» τιμές ανακτήθηκαν με `git show HEAD:`, ποτέ `stash`)* ⇒ **μηδέν από τους 47 καταναλωτές** άλλαξε. ✅ **Π-21 ΕΚΛΕΙΣΕ** *(Boy Scout, N.0.2)*: **13** σχόλια σε **11** αρχεία, εναπομείναντα **0**. ⚠️ **Ο δικός μου αριθμός ήταν λάθος**: το §3.2 έγραφε «τρία» γιατί μέτρησε **μόνο τα αρχεία που ήταν ήδη ανοιχτά** — *«τα σημεία που ήδη κοιτούσα» δεν είναι μέτρηση*. **Έκτο «λάθος αριθμός σε έγγραφο»**, γραμμένο **μέσα** στην παράγραφο που το καταγγέλλει. 🔶 **Δηλωμένο όριο**: το `utils/contactForm/mappers/individual.ts:49` κρατά **δική του** δήλωση με `\| null` *(DTO φόρμας, άλλη σημασιολογία κενού — Firestore sentinel)*, και τα `EscoOccupationPickerProps` / `ProfessionEscoFieldProps` είναι **props component**, όχι το στοιχείο. **Δεν ενοποιήθηκαν, και ο λόγος είναι γραμμένος.** |
| 2026-08-24 | 🔵 **Γέννηση** *(Φάση 0 — μηδέν κώδικας)*. Το επίπεδο **1 (Άνθρωπος)** του **ADR-748 §11.3** απέκτησε ιδιοκτήτη μετά από **22 ημέρες** αδέσποτο. 🔴 **Η αρχική διάγνωση ανατράπηκε από το SSoT audit**: δεν λείπει λεξιλόγιο *(υπάρχει — ESCO v1.2.1 / ISCO-08 / 28 γλώσσες, **ADR-132**)*· λείπει **η γέφυρα** *(το `Contact` **δεν έχει `uid`**)*. **Εννέα** λεξιλόγια μετρήθηκαν, όχι τέσσερα. 🔴 **Δεύτερη ανατροπή, από την έρευνα**: η προβολή προς IFC **δεν** γίνεται με `USERDEFINED` + ελεύθερο κείμενο *(θα εξήγαγε ως κείμενο ακριβώς το δεδομένο που κάναμε ταξινομημένο, §6.1)* αλλά με **ΔΥΟ κανάλια** — `IfcActorRole.Role` *(προβολή)* **ΚΑΙ** `IfcRelAssociatesClassification → IfcClassificationReference.Location : IfcURIReference` *(αλήθεια, με το ESCO URI ακέραιο)*· επαληθεύτηκε στην πηγή ότι το `IfcActor` κληρονομεί `HasAssociations` από το `IfcObjectDefinition`, άρα **μπορεί** να ταξινομηθεί. 🔴 **Τρίτο εύρημα**: **τρία** σχόλια κώδικα αποδίδουν το ESCO σε **ADR-034**, που είναι **τριπλό** και του οποίου **κανένα** αρχείο δεν αναφέρει τη λέξη ESCO *(0·0·0)* — ο ιδιοκτήτης είναι το **ADR-132** *(Π-21, διορθώνεται στη Φάση 1)*. Αποφάσεις **Α1-Α5** κλειδωμένες. |
