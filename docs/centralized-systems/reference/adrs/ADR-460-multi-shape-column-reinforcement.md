# ADR-460 — Multi-shape Column & Wall Reinforcement Automation (SSoT)

**Status:** 🟢 Slices 1-10 + follow-ups 1-6 IMPLEMENTED 2026-06-15 (Opus) — UNCOMMITTED (🔴 browser-verify + commit). follow-up 6 = **επανασχεδίαση Γ/Τ/Π/Ι σε επικαλυπτόμενους ορθογώνιους συνδετήρες ανά σκέλος** (η σωστή μέθοδος). Επεκτείνει ADR-456 (ποσότητες/οπλισμός) + ADR-457 (detail sheet).
**Discipline:** Δομοστατικά / Structural Engineering
**Scope:** Όλη η αυτοματοποίηση οπλισμού (compute, layout, 2Δ/3Δ render, detail-sheet PDF, settings panel, Auto, validator) επεκτείνεται από **μόνο-ορθογωνική** σε **ΟΛΟΥΣ τους 9 τύπους διατομής** κολώνας: `rectangular, circular, L-shape, T-shape, I-shape, U-shape, polygon, shear-wall, composite`. Παννομιότυπη λειτουργία, ένα SSoT, μηδέν διπλότυπα.

---

## 1. Context & Problem

Πριν το ADR-460 και οι 9 τύποι διατομής είχαν **πλήρη γεωμετρία** (footprint + 3D extrude + grips + σοβάς), αλλά **όλη η αλυσίδα οπλισμού ήταν rectangular-only**: ~15 σκληρές πύλες `kind !== 'rectangular'` απέκλειαν compute/layout, cross-ties, confinement, 2Δ rebar, 3Δ κλωβό, τα 7 builders του detail-sheet, και το settings panel ήταν ορατό μόνο σε `rectangular || shear-wall`.

Στόχος (εντολή Giorgio): **τα ίδια ακριβώς** σε όλους τους τύπους — ίδιες ρυθμίσεις, ίδιοι υπολογισμοί, ίδιο PDF λεπτομέρειας, ίδιες προβολές 2Δ/3Δ. Εύρος: **όλοι οι τύποι μαζί** + **πλήρης σχεδιασμός τοιχώματος** (boundary elements + κατανεμημένος οπλισμός κορμού).

---

## 2. Architecture — μία engine, dispatch ανά «detailing mode»

Θεμέλιο: το υπάρχον `materializeColumnLocalPolygonMm(params)` (`bim/geometry/column-geometry.ts`) δίνει το **section outline σε LOCAL mm, κεντραρισμένο, για ΚΑΘΕ σχήμα** — ίδιο σύστημα συντεταγμένων με το rebar layout. Πάνω σε αυτό:

**SSoT classifier** `resolveColumnReinforcementSection(params)` (`bim/structural/reinforcement/column-section-outline.ts`) → `{ kind, outlineMm, mode, isCircular, diameterMm, minThicknessMm, maxDimensionMm, perimeterMm, grossAreaMm2, bboxWidth/Depth, wallAxis }`. Το `mode` ∈ `perimeter | circular | wall`:

| Mode | Σχήματα | Διάταξη |
|---|---|---|
| `perimeter` | rectangular | rect fast-path: ράβδοι σε γωνίες+πλευρές, ΕΝΑ ορθογ. στεφάνι, diamond/grid cross-ties (μηδέν regression) |
| `perimeter` (multihoop) | L, T, I, U, composite-**ορθογώνιο** | **follow-up 6**: διάσπαση σε **επικαλυπτόμενα ορθογώνια σκέλη** (`decomposeColumnSectionRects`) → **ένας κλειστός ορθογ. συνδετήρας ανά σκέλος** + cross-ties ενδιάμεσων (Revit/Tekla· `buildMultiHoopLayout`) |
| `perimeter` (fallback) | polygon (N-gon), composite-**μη-rectilinear** | inset outline + outline-following στεφάνι (concave-aware) + perpendicular cross-ties (`buildPerimeterLayoutFromOutline`) |
| `circular` | circular | Ράβδοι **ακτινικά** σε κύκλο + δακτύλιος/σπείρα |
| `wall` | shear-wall + επιμήκη L/T/U/composite (λόγος ≥ 4, EC8 §5.1.2) | **Boundary elements** (κρυφοκολώνες, ζώνες άκρων lc) + **κατανεμημένος οπλισμός κορμού** (κατακόρυφες ράβδοι 2 παρειών + web S-ties), EC8 §5.4.3.4 |

**Dispatcher** `resolveColumnRebarLayout(r, section)` + `resolveColumnCrossTies(layout, section, r)` (`column-rebar-layout-resolve.ts`) → ΟΛΟΙ οι consumers (ποσότητες/2Δ/3Δ/detail-sheet) καλούν αυτό. Το rectangular διοχετεύεται στο **υπάρχον** `computeColumnRebarLayout` (fast-path → μηδέν regression, ίδιοι αριθμοί). Το `ColumnRebarLayout` παραμένει ΕΝΑ interface (+ optional `stirrupCenterlineLengthMm`, `extraStirrupPathsMm`, `crossTieAnchorsMm`).

### Νέα/τροποποιημένα modules
- **NEW** `column-section-outline.ts` (classifier), `column-perimeter-layout.ts`, `column-circular-layout.ts`, `column-wall-reinforcement.ts`, `column-rebar-layout-resolve.ts` (dispatcher), **`column-rect-decomposition.ts`** + **`column-multihoop-layout.ts`** (follow-up 6 — επικαλυπτόμενα ορθογ. σκέλη Γ/Τ/Π/Ι· `buildRectCrossTieAnchors` exported από `column-cross-ties.ts`).
- **SSoT reuse**: νέο `insetPolygonMiter` (winding-aware, concave-safe, miter — διατηρεί κάθετη απόσταση `d`) στο `polygon-utils.ts` (το υπάρχον `insetClosedPolygon` averaged-normal υπο-εισάγει τις γωνίες → ακατάλληλο για στεφάνι).
- **Generalized**: `buildRoundedStirrupPath` (concave/reflex corners)· `column-rebar-layout` (`distributeBarsAlongPolygon`, `closedPolylineLengthMm`)· `column-cross-ties` (`buildTiesFromAnchors`)· `column-confinement` (optional `layout` param)· `column-reinforcement-compute` (optional `section` param → shape-aware ποσότητες/ρ, wall boundary+web steel)· providers/`suggest-reinforcement` (perimeter-based bar count, `minThicknessMm` για βήμα, wall `suggestWallIntent`, circular).
- **Data model** `ColumnReinforcement` += optional `wall?: WallReinforcementIntent` + `spiralPitchMm`. Zod `column.schemas.ts`: +wall sub-schema, +`spiralPitchMm`, **+διόρθωση κενού** (`crossTiePattern` έλειπε από το `.strict()` schema → στριβόταν στο parse).
- **Consumers**: `column-rebar-2d`, `column-rebar-3d` (κλωβός), τα 7 detail-sheet builders, `column-validator` (gate removal + shape-correct grossArea/ρ), `DxfRenderer.drawColumnReinforcement2D` (gate removal), settings (`resolveColumnPanelVisibility` structural → όλα· `buildSectionContext`/`resolveStructuralReadout` shape-aware).

---

## 3. ΜΑΘΗΜΑΤΑ
- `insetClosedPolygon` (ETICS, averaged-normal) **υπο-εισάγει** γωνίες 90° κατά ~cos45° → λάθος για centerline στεφανιού. Σωστό = **miter** `m = d·(n1+n2)/(1+n1·n2)` με **winding-aware** inward normals (left normal για CCW· concave reflex χειρίζονται φυσικά).
- `.strict()` Zod schema → ΚΑΘΕ νέο/υπάρχον persisted πεδίο ΠΡΕΠΕΙ να υπάρχει στο schema, αλλιώς σιωπηρά στρίβεται (το `crossTiePattern` ήταν τέτοιο κενό).
- Geometry-is-SSoT: ΕΝΑ dispatcher τρέφει 2Δ/3Δ/ποσότητες/detail-sheet → καμία απόκλιση μεταξύ προβολών.

---

## 4. DEFER
- Granular UI editing των wall fields (boundary/web Ø/βήμα) + circular spiral pitch combos — το Auto τα παράγει shape-aware· επεξεργάζονται τα κοινά longitudinal/stirrup/cover.
- Πλουσιότερο schedule τοιχώματος (ξεχωριστές γραμμές boundary/web).
- I-shape ως χαλύβδινη διατομή (τώρα ως concrete perimeter).
- Boundary-element placement σε **μη-ορθογώνια** άκρα Γ/Τ/Π είναι bbox-προσέγγιση (ακριβές σε shear-wall/επιμήκες ορθογώνιο).
- Multi-loop inset για πολύ λεπτά μέλη (cover ≈ μισό πάχος).

---

## 5. Verification
- **Jest**: `column-section-outline` (6), `column-multishape-layout` (perimeter/circular/wall/dispatcher/cross-ties), `column-multishape-suggest` (circular/wall/rect), + ΟΛΟ το υπάρχον structural/detail-sheet/validators suite GREEN (529+ tests, μηδέν regression — rect αριθμητικά ταυτόσημο).
- **Browser** (Firestore-records-first): ανά τύπο (circular/L/T/I/U/polygon/composite/shear-wall) → φτιάξε κολώνα → «Auto» → επαλήθευσε 2Δ κάτοψη (ράβδοι+στεφάνι), 3Δ κλωβό, panel ρ/βάρος, PDF λεπτομέρειας (plan/elevation/schedule/titleblock/3D).
- **tsc** `--noEmit` (N.17 serialized).

---

## 6. Changelog
- **2026-06-16** (Opus, follow-up 9) — **SSoT cleanup (αφαίρεση 2 διπλοτύπων που εισήγαγαν τα f6-f8)**: (1) **`pointPairKey(a,b)`** (canonical order-agnostic point-pair key, 0.1mm round) → ΕΝΑ SSoT στο `column-bar-distribution.ts` (re-export από `column-rebar-layout`)· σβήστηκαν τα ταυτόσημα τοπικά `pairKey` σε `column-perimeter-layout.ts` + `column-multihoop-layout.ts`. (2) **`rectRestrainedBarIntervals(wMm,dMm,sMaxMm)→{nW,nD}`** (EC8 §5.4.3.2.2(11)P spacing→διαστήματα ράβδων) → ΕΝΑ SSoT στο `column-reinforcement-types.ts` (leaf που εισάγουν ΚΑΙ codes ΚΑΙ reinforcement → acyclic)· καλείται ΚΑΙ από τον suggester (`spacingBarCount`, gross dims, intent count) ΚΑΙ από το geometry layout (`distributeRectBarsBySpacing`, inset dims, θέσεις) → ο κανόνας βήματος ζει σε ΕΝΑ σημείο. 200 jest GREEN, μηδέν διπλότυπο, tsc clean. ΜΑΘΗΜΑ: όταν reuse-άρεις primitives ανά σκέλος, έλεγξε ΚΑΙ τα μικρά helpers (pairKey/spacing-formula) για προϋπάρχον SSoT — όχι μόνο τις μεγάλες μηχανές.
- **2026-06-16** (Opus, follow-up 8) — **(A) Γάντζος 135° κλεισίματος σε ΚΑΘΕ σκέλος-στεφάνι (multihoop) + (B) code-driven bar count ΚΑΙ στις ορθογωνικές** (απόφαση Giorgio «A+B μαζί, full enterprise»). **(A)**: NEW optional `ColumnRebarLayout.extraStirrupHookEndsMm` (index-aligned με `extraStirrupPathsMm`) → κάθε σκέλος-στεφάνι Γ/Τ/Π/Ι κλείνει με τον δικό του γάντζο 135° (πριν: μόνο το κύριο)· `buildMultiHoopLayout` το γεμίζει από κάθε leg· 2Δ (`column-rebar-2d`) + 3Δ (`column-rebar-3d`) renderers το ζωγραφίζουν ανά hoop· **wall boundary hoops → absent (αμετάβλητα)**. **(B)**: το `computeColumnRebarLayout` (rect fast-path) δέχεται πλέον `maxBarSpacingMm?` και χρησιμοποιεί `distributeRectBarsBySpacing` → **και οι ορθογωνικές** πυκνώνουν κατά EC8 (παρειά 400mm>200 → 8 ράβδοι: 4 γωνιακές+4 μεσοπλευρικές)· dispatcher rect path περνά το όριο· `r.longitudinal.count` = intent floor παντού. Αφαιρέθηκε το πλέον-νεκρό `distributeBars` (`column-bar-distribution.ts`· boy-scout, dead-code ratchet). **Συνέπεια**: ο χάλυβας/ρ ΟΛΩΝ των ορθογωνικών ενημερώθηκαν (geometry-is-SSoT· tests προσαρμόστηκαν: 400×400 → 8 ράβδοι/ρ≈1%). +tests (per-hoop hooks· distributeRectBarsBySpacing· rect densification). **DEFER αναιρέθηκε** (το «rectangular densification» του follow-up 7 υλοποιήθηκε). 🔴 browser-verify (Τ: κάθε σκέλος-στεφάνι έχει γαντζάκι 135°· ορθογωνική 400×400 → 8 ράβδοι+διαμάντι) + commit.
- **2026-06-15** (Opus, follow-up 7) — **Code-driven bar count ανά σκέλος (Revit/Tekla spacing-derived)** (browser-caught σε Τ follow-up 6: ο ψηλός κορμός έπαιρνε μόνο γωνιακές ράβδους — proportional split του `count` → αραιός κορμός, παραβίαση EC8 §5.4.3.2.2(11)P). **Απόφαση Giorgio**: «όπως οι μεγάλοι παίχτες (Revit)» → ο αριθμός διαμήκων ανά σκέλος προκύπτει από το **όριο κανονισμού** (βήμα ≤ `maxBarSpacingMm` σε κάθε παρειά), με το `r.longitudinal.count` ως **intent floor**. **Full SSoT**: NEW exported constant `MAX_RESTRAINED_BAR_SPACING_MM=200` (`column-reinforcement-types.ts` — EC8 DCM/ΕΑΚ §18.4) που αναφέρουν ΚΑΙ οι δύο providers (`columnReinforcementLimits.maxBarSpacingMm`) ΚΑΙ το geometry layout (default). NEW `distributeRectBarsBySpacing(halfWb,halfDb,sMax,minTotal)` (`column-rebar-layout.ts`) — ανά παρειά `ceil(face/sMax)` διαστήματα, bump ώστε total ≥ floor. `buildMultiHoopLayout(r,rects,maxBarSpacingMm?)` + `resolveColumnRebarLayout(r,section,maxBarSpacingMm?=const)` (optional· **pure renderers code-free** με το default = code value· compute/validator μπορούν να περάσουν την ακριβή code τιμή για DCH=150). **geometry-is-SSoT fix**: `longitudinalSteel` (non-wall) μετράει πλέον τις **πραγματικές** ράβδους του layout (`layout.longitudinalBarsMm.length`), όχι το `r.count` → ποσότητα/ρ/βάρος σωστά μετά την densification (rect fast-path = floor(count) → ίδιοι αριθμοί, μηδέν regression). +6 jest (distributeRectBarsBySpacing βήμα+floor· multihoop ψηλός κορμός Τ). **ΜΑΘΗΜΑ**: το `count` = engineer intent **floor**, ΟΧΙ απόλυτο — ο κανονισμός (≤200mm restrained-bar spacing) καθορίζει το πραγματικό πλήθος ανά σκέλος (Tekla/Revit). **DEFER**: thread ductility class (DCH→150) + rectangular fast-path densification (σήμερα honors exact count· ξεχωριστή απόφαση λόγω blast-radius σε υπάρχουσες κολώνες). 🔴 browser-verify (Τ κορμός → ενδιάμεσες ράβδοι ≤200mm + cross-ties) + commit.
- **2026-06-15** (Opus, follow-up 6) — **ΕΠΑΝΑΣΧΕΔΙΑΣΗ εγκάρσιου οπλισμού Γ/Τ/Π/Ι → επικαλυπτόμενοι ορθογώνιοι συνδετήρες ανά σκέλος** (κλειδωμένη απόφαση Giorgio — η σωστή μέθοδος Revit/Tekla· τα follow-ups 2/4/5 «ένα στεφάνι στο outline + perimeter ties» ήταν λάθος βάση: ένα στεφάνι ΔΕΝ αγκαλιάζει ράβδο σε κοίλη/reentrant γωνία). **Νέα μέθοδος**: η διατομή **διασπάται σε αξονικά ορθογώνια σκέλη** που **επικαλύπτονται** στις ζώνες συμβολής· **ένας κλειστός ορθογώνιος συνδετήρας ανά σκέλος** (γάντζος 135°) + **cross-ties** για τις ενδιάμεσες. Μέσα σε κάθε ορθογώνιο οι ράβδοι είναι ευθυγραμμισμένες → reuse της **ΥΠΑΡΧΟΥΣΑΣ ορθογωνικής μηχανής** (rounded stirrup + γωνιακός γάντζος + `buildRectCrossTieAnchors`) → μηδέν ζιγκ-ζαγκ, μηδέν κοίλη γωνία. **NEW** `column-rect-decomposition.ts` (`decomposeColumnSectionRects` — Y-sweep + κατακόρυφη μεγιστοποίηση· geometry-driven πάνω στο ΙΔΙΟ outline → μηδέν per-kind κώδικας, μηδέν mismatch με footprint· non-rectilinear N-gon/διαγώνιο → `[]`) + **NEW** `column-multihoop-layout.ts` (`buildMultiHoopLayout` — per-σκέλος hoop· merge: 1ο→`stirrupPathMm`, υπόλοιπα→`extraStirrupPathsMm`· ένωση+dedup ράβδων στις συμβολές· cross-ties ως `crossTieAnchorsMm` με snap σε ΠΡΑΓΜΑΤΙΚΗ ράβδο). **Dispatcher**: `perimeter` μη-ορθογ. → `decomposeColumnSectionRects` ≥1 σκέλος → `buildMultiHoopLayout`· αλλιώς (μη-rectilinear) → fallback `buildPerimeterLayoutFromOutline`. **Μηδέν νέο plumbing** σε 2Δ/3Δ/ποσότητες (τα `extraStirrupPathsMm`/`crossTieAnchorsMm` ήδη ρέουν, ίδιο μονοπάτι με wall). Bar count = proportional split ανά περίμετρο σκέλους (min 4). **Stale test fix** (πλευρικό, ADR-460-base): `resolveColumnPanelVisibility` structural → ΟΛΟΙ οι τύποι (το test ζητούσε ακόμα RC-only). +14 jest (decomposition Τ/Γ/Π/Ι/circular/polygon + multihoop + dispatcher routing), 285→ GREEN, rect/circular/wall ΑΜΕΤΑΒΛΗΤΑ. **ΜΑΘΗΜΑ**: cross-element-free decomposition = Y-sweep + grow (το x-διάστημα επεκτείνεται όσο μένει εσωτερικό → κορμός διαπερνά πέλμα = φυσική επικάλυψη)· geometry-driven decomposition (όχι παραμετρικό ανά kind) = μηδέν mismatch με footprint + δωρεάν composite-ορθογώνιο. 🔴 browser-verify (Τ/Γ/Π/Ι → Auto → επικαλυπτόμενα στεφάνια + cross-ties + 3Δ κλωβός) + commit. ⚠️ `DxfRenderer.ts` ΔΕΝ χρειάστηκε (ρέει μέσω `extraStirrupPathsMm`).
- **2026-06-15** (Opus) — ADR-460 created. Slices 1-10 implemented (multi-shape SSoT engine + dispatcher + wall design + all consumers). UNCOMMITTED. 🔴 browser-verify + commit. ⚠️ `DxfRenderer.ts` τροποποιήθηκε (ADR-040 governance — stage ADR-040 + ADR-460 μαζί στο commit, CHECK 6B/6D).
- **2026-06-15** (Opus, follow-up) — UI: το control «μοτίβο cross-tie» (διαμάντι/πλέγμα) γίνεται **ανενεργό (disabled)** σε **κυκλική + τοίχωμα** (mode ≠ perimeter) — δεν έχει εφαρμογή εκεί (ο δακτύλιος/σπείρα περισφίγγει όλες τις ράβδους· wall ties = auto). SSoT `resolveColumnFieldDisabled(commandKey, params)` (`column-command-keys.ts`) → `ColumnPropertyRow` `disabled` prop. 4 jest.
- **2026-06-15** (Opus, follow-up 2) — **Grid cross-ties σε μη-ορθογώνια perimeter (Γ/Τ/Π/πολύγωνο/σύνθετο)** — έλυσε την ασυνέπεια «control ενεργό αλλά μηδέν συνδετήρια» (πριν: DEFER → dispatcher `return []`). (α) **Engine**: `buildPerimeterCrossTieAnchors` (`column-perimeter-layout.ts`) — κάθε **ενδιάμεση** ράβδος (index ≥ K κορυφών) → **κάθετο** tie ως την απέναντι παρειά (perpendicular-projection: `edgeOfBar` → inward normal· `rayForwardDepth` → πρώτη απέναντι ακμή· snap σε ράβδο μόνο όταν ευθυγραμμισμένη· dedup ανά ευθεία· skip ακμές-καπάκια όπου βάθος > μήκος ακμής) → `crossTieAnchorsMm`. Reuse του ΥΠΑΡΧΟΝΤΟΣ anchors→`buildTiesFromAnchors` μονοπατιού (ίδιο με wall) → 2Δ/3Δ/ποσότητες αυτόματα, **μηδέν νέο plumbing**. Δένονται **όλες** οι ενδιάμεσες (απόφαση Giorgio, παρ. με ορθογωνικό `straightTies`, EC8-συντηρητικό). **ΜΑΘΗΜΑ**: bar-to-bar «κοντινότερη αντικριστή» έβγαζε **ζιγκ-ζαγκ διαγώνια** ties (οι ράβδοι των δύο παρειών δεν ευθυγραμμίζονται στην perimeter κατανομή)· perpendicular-projection = κάθετο by construction. (β) **UI shape-aware options**: `resolveColumnFieldOptions(commandKey, params)` (`column-command-keys.ts`) → το **διαμάντι** (ορθογώνιο-ειδικό) κρύβεται σε μη-ορθογώνια (auto/grid)· `ColumnPropertyRow` `options?` prop + `ColumnAdvancedPanel` wiring. (γ) **Zero regression**: rectangular = rect fast-path (χωρίς `crossTieAnchorsMm`) → εξακολουθεί diamond/grid· circular/wall αμετάβλητα. +6 jest (292 structural GREEN).
- **2026-06-15** (Opus, follow-up 5) — **Cross-ties μόνο σε ΠΡΑΓΜΑΤΙΚΕΣ ράβδους (ποτέ γάντζος στο κενό)** (browser-caught σε Τ: tie κατέληγε στο κάθετο πέλμα παρειάς όπου δεν υπήρχε ράβδος). Το `pushPerpTie` άλλαξε: αντί «foot + snap», βρίσκει την πιο **ευθυγραμμισμένη ΠΡΑΓΜΑΤΙΚΗ** απέναντι ράβδο (`alignedOppositeBar`: βάθος≈depthFace ±2·dbL, πλευρική ≤ depthFace ⇒ ≤45°)· **αν καμία → δεν σχεδιάζεται tie**. Dedup ανά ζεύγος (`pairKey`). Διευκρίνιση: το ότι το **στεφάνι δεν αγκαλιάζει** τη ράβδο σε **κοίλη γωνία ΕΙΝΑΙ ΣΩΣΤΟ** (γεωμετρικά αδύνατο — concave· συγκρατείται από τα cross-ties). +1 jest (κάθε άκρο tie = πραγματική ράβδος). 303 structural GREEN.
- **2026-06-15** (Opus, follow-up 4) — **Cross-ties σε reentrant (κοίλες) γωνίες** (browser-caught σε Τ-διατομή: οι ράβδοι στις γωνίες κορμού↔πέλματος έμεναν χωρίς συνδετήριο). Το `buildPerimeterCrossTieAnchors` παρήγαγε ties μόνο για **ενδιάμεσες** ράβδους (index ≥ K)· οι **κυρτές** γωνίες πιάνονται από το στεφάνι, αλλά οι **reentrant** (reflex) γωνίες λυγίζουν προς τα μέσα → EC8 κρίσιμη συγκράτηση. FIX: νέο pre-loop `isReflexVertex` (CCW cross<0) → για κάθε κοίλη γωνία, κάθετο tie σε **ΚΑΘΕ** από τις δύο γειτονικές παρειές (εγκάρσια στο πάχος του σκέλους) μέσω κοινού `pushPerpTie`. +1 jest (έλεγχος tie κοντά στην κοίλη γωνία). 286 structural GREEN.
- **2026-06-15** (Opus, follow-up 3) — **Μήκος ουράς γάντζου 135° cross-tie: 10·Ø → 5·Ø** (browser-caught σε Τ-διατομή: υπερβολικά μακριές ουρές). NEW `CROSS_TIE_HOOK_EXTENSION_FACTOR=5` στο `column-cross-ties.ts` (`straightTie`), αντικαθιστά το `STIRRUP_HOOK_EXTENSION_FACTOR=10`. **EC2-correct**: EN1992-1-1 §8.5 Fig 8.5 → γάντζος **135°** απαιτεί ≥**5·dbw** (το 10·dbw αφορά κάμψη **90°**). Επηρεάζει ΟΛΑ τα cross-ties (rect grid/wall/perimeter, συνεπές)· geometry-is-SSoT → ποσότητα χάλυβα ελαφρώς ακριβέστερη. Ο **κύριος συνδετήρας** (`buildStirrupHookEndsMm`) μένει 10·Ø (εκτός παραπόνου· DEFER align σε 5·Ø αν ζητηθεί). 285 jest GREEN (no exact-length assertions).
