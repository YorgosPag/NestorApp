/**
 * **Το πλαίσιο της κάρτας αγγελίας** — αναλογία φωτογραφίας, πλέγμα, `sizes`. Μία γραφή.
 *
 * @related ADR-777 §8.80 · ADR-841 §7 · listing-map-snapshot/snapshot-frame · listing-focus-card
 * @module components/search-results/listing-card-frame
 *
 * ────────────────────────────────────────────────────────────────────────────
 * 🔴 ΓΙΑΤΙ 3:2 — ΜΕΤΡΗΜΕΝΟ ΖΩΝΤΑΝΑ (2026-09-24), ΟΧΙ ΑΠΟ ΜΝΗΜΗΣ
 * ────────────────────────────────────────────────────────────────────────────
 *
 * Zillow **1,85:1** · Redfin **3:2** · Idealista **~3:2** · Rightmove **~1,4** · Airbnb **4:3** ·
 * Spitogatos 4:3 με `object-fit: fill` *(παραμορφώνει)*. **Δεν υπάρχει βιομηχανικό πρότυπο.**
 * Το 3:2 είναι η **φυσική** αναλογία της επαγγελματικής φωτογραφίας (τίποτα δεν κόβεται) και
 * κερδίζει ύψος έναντι του 4:3 — περισσότερα ακίνητα πάνω από τη γραμμή κύλισης. Το 1,85 της
 * Zillow απορρίφθηκε: ακρωτηριάζει τις **κάθετες** φωτογραφίες κινητού, συχνές στη δική μας αγορά.
 *
 * ⚠️ Μέχρι σήμερα ένα σχόλιο στο `ListingCard` έγραφε «όπως ακριβώς κάνει η Zillow (~4:3)» — **ψευδές**,
 * διαψεύστηκε με μέτρηση. Ο αριθμός ζει πλέον **εδώ**, με την απόδειξή του στο ADR.
 *
 * 🔑 **Ένας αριθμός, πολλές γλώσσες**: η κλάση Tailwind *(διάταξη)*, το στιγμιότυπο χάρτη *(pixel)*
 * και τα `sizes` *(περιηγητής)* παράγονται ή ελέγχονται από το {@link LISTING_CARD_ASPECT}. Η άγκυρα
 * `listing-card-frame.test.ts` τα κρατά ίσα — ποτέ δεύτερο `aspect-[…]` γραμμένο με το χέρι.
 */
import { MOBILE_BREAKPOINT } from '@/constants/layout';

/** Ο λόγος πλάτους/ύψους της φωτογραφίας κάρτας. */
export const LISTING_CARD_ASPECT = { w: 3, h: 2 } as const;

/**
 * Η κλάση του πλαισίου. ⚠️ **Στατικό literal**: το Tailwind διαβάζει το αρχείο, δεν το εκτελεί —
 * μια κλάση συναρμολογημένη από το {@link LISTING_CARD_ASPECT} δεν θα παραγόταν ποτέ.
 */
export const LISTING_CARD_ASPECT_CLASS = 'aspect-[3/2]';

/** Το ελάχιστο πλάτος κελιού (rem) — κάτω από αυτό η κάρτα γίνεται δυσανάγνωστη. */
export const LISTING_CARD_MIN_REM = 17;

/**
 * **Το πλέγμα κάθε λίστας καρτών** (αποτελέσματα · βιτρίνα · αποθηκευμένες).
 *
 * 🏆 **Πέρα από τους μεγάλους**: η Zillow αλλάζει από 1 σε 2 στήλες με **σταθερό breakpoint** και
 * κάρτα 344px. Το `auto-fill` αποφασίζει από το **πλάτος του περιέκτη** — χωρίς JS, σωστό από το
 * SSR, μηδέν CLS — άρα η στήλη της λίστας δίπλα στον χάρτη, το φύλλο του κινητού και η βιτρίνα
 * πλήρους πλάτους παίρνουν **η καθεμία** όσες στήλες χωράνε, και το zoom 400% (WCAG 1.4.10)
 * γυρίζει μόνο του σε μία.
 *
 * 📐 **`min(17rem, 100%)`, ΟΧΙ σκέτο `17rem`** (ADR-797 §Φ.Ρ.3 · CHECK 3.94, μετρημένο 2026-09-26):
 * το «γυρίζει σε μία» ίσχυε μόνο όσο ο περιέκτης είχε ≥ 17rem. Στο `/stay` @320 ο περιέκτης είχε
 * **256 px** και η μία στήλη έμενε **272** ⇒ κάθε κάρτα έχανε 16 px (περίγραμμα + δεξί κείμενο) κάτω
 * από τον καθολικό `overflow-x: clip`. Το πάτωμα κρατά τις στήλες όπου υπάρχει χώρος και **δεν νικά
 * ποτέ** τον περιέκτη — το intrinsic «RAM» σχήμα (Every Layout). Ίδια απόφαση με το `min-w-field`.
 */
export const LISTING_CARD_GRID_CLASS =
  'm-0 grid list-none grid-cols-[repeat(auto-fill,minmax(min(17rem,100%),1fr))] gap-3 p-0';

/**
 * Το πλάτος (rem) που **δηλώνουμε** στον περιηγητή για κάρτα της στήλης αποτελεσμάτων.
 *
 * Η στήλη είναι `minmax(20rem, min(46rem, 45%))` ⇒ δύο κελιά ≤ ~23rem, ή ένα κελί ≤ ~34rem όταν
 * χωράει μόνο ένα. Το 24rem είναι η απάντηση που **σπάνια υπερβάλλει** και ποτέ δεν κατεβάζει
 * εικόνα μισού μεγέθους. Ο φρουρός στο test το κρατά μέσα στο `[MIN, 2·MIN]`.
 */
export const RESULTS_CARD_IMAGE_REM = 24;

/**
 * Το `sizes` της κάρτας σε **πλέγμα πλήρους πλάτους** (βιτρίνα · αποθηκευμένες): κελί από
 * {@link LISTING_CARD_MIN_REM} ως ~24rem όσο κι αν μεγαλώσει το παράθυρο — προστίθενται **στήλες**,
 * δεν φουσκώνουν κάρτες. Ο φρουρός Β4 (`LandingShowcase.test.tsx`) το δένει με το `minmax`.
 */
export const LISTING_GRID_CARD_IMAGE_SIZES = '(min-width: 40rem) 20rem, 100vw';

/** Το `sizes` της κάρτας στη στήλη αποτελεσμάτων (και στο φύλλο του κινητού, πλήρους πλάτους). */
export const RESULTS_CARD_IMAGE_SIZES = `(min-width: ${MOBILE_BREAKPOINT}px) ${RESULTS_CARD_IMAGE_REM}rem, 100vw`;
