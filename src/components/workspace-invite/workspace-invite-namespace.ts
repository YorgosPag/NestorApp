/**
 * @fileoverview **ΤΟ ΟΝΟΜΑ ΤΗΣ ΒΑΣΗΣ** — μία φορά, για τα αρχεία ετικετών της πρόσκλησης.
 * @related components/workspace-invite/workspace-invite-labels.ts · ADR-744 · ADR-853 Φ6
 * @module components/workspace-invite/workspace-invite-namespace
 *
 * ────────────────────────────────────────────────────────────────────────────
 * 🔴 ΓΙΑΤΙ ΕΝΑ ΑΡΧΕΙΟ ΓΙΑ ΜΙΑ ΓΡΑΜΜΗ
 * ────────────────────────────────────────────────────────────────────────────
 *
 * Πρότυπο: `components/contact/first-contact-namespace.ts`, και ο λόγος είναι ο ίδιος.
 * Ο τεμαχιστής του ADR-744 επιβάλλει **αρχείο ετικετών ανά ακροατήριο** *(η
 * `dynamicKeyPolicy` ζει **ανά αρχείο**)*, οπότε το όνομα της βάσης το χρειάζονται
 * περισσότεροι του ενός καταναλωτές.
 *
 * 🔑 **ΓΙΑΤΙ ΟΧΙ re-export από το αρχείο ετικετών**: ο καταναλωτής θα έσερνε τότε
 * **ολόκληρο** τον πίνακα κλειδιών στη στατική του κλειστότητα — δηλαδή θα ακύρωνε ακριβώς
 * τον λόγο που το όνομα ζει χωριστά.
 *
 * 🔑 **ΓΙΑΤΙ ΟΧΙ δύο δηλώσεις**: δύο αλήθειες για το ίδιο πράγμα, με **αόρατη** απόκλιση —
 * το `useTranslation(['λάθος-ns'])` **δεν πετά**, απλώς δεν φορτώνει το πακέτο και η οθόνη
 * βάφει **ωμά κλειδιά**. Το SSoT κοστίζει μία γραμμή.
 *
 * ⚠️ **ΜΗΔΕΝ ΚΟΣΤΟΣ ΣΤΟ SLICE**: το αρχείο **δεν περιέχει κανένα κλειδί** — μόνο το όνομα.
 *
 * ⚠️ **ΓΙΑΤΙ `auth` ΚΑΙ ΟΧΙ ΝΕΑ ΒΑΣΗ**: η σελίδα ζει στο `(auth)` και ο άνθρωπος φτάνει
 * **χωρίς ταυτότητα** — ίδιο ακροατήριο με τα `emailPreferences`/`emailLink` που ήδη ζουν
 * εκεί. Νέα βάση θα απαιτούσε **και** γραμμή στο `namespace-loaders.ts` (CHECK 3.36)· το
 * `auth` είναι **ήδη καλωδιωμένο**, άρα η πύλη μένει ανέγγιχτη.
 *
 * **Layering**: leaf — μία σταθερά. Κανένα import, κανένα React.
 */

/** Η **μία** βάση όλου του `workspaceInvite.*`. */
export const WORKSPACE_INVITE_NS = 'auth';
