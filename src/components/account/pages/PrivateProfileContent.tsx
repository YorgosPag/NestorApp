'use client';

/**
 * @fileoverview **ΤΟ ΟΡΙΟ ΔΙΑΔΡΟΜΗΣ ΤΟΥ `/profile`** — δεκαπέντε γραμμές που δεν είναι δίδυμο.
 * @related ADR-827 §9.22 ζ · ADR-744 §15 (Φ4) · CHECK 3.51
 * @module components/account/pages/PrivateProfileContent
 *
 * ────────────────────────────────────────────────────────────────────────────
 * 🔴 ΓΙΑΤΙ ΥΠΑΡΧΕΙ ΑΥΤΟ ΤΟ ΑΡΧΕΙΟ, ΚΑΙ ΓΙΑΤΙ ΔΕΝ ΕΙΝΑΙ ΑΝΤΙΓΡΑΦΟ
 * ────────────────────────────────────────────────────────────────────────────
 *
 * Δεν κρατά **καμία** λογική οθόνης: την κρατά ολόκληρη το {@link ProfilePageContent},
 * που αποδίδεται **αυτούσιο**. Κρατά **ένα** πράγμα — την εγγραφή του route slice — και
 * αυτό είναι **ανά ΔΙΑΔΡΟΜΗ**, ενώ το περιεχόμενο είναι **ανά ΟΘΟΝΗ**. Δύο διαφορετικά
 * ερωτήματα, δύο θέσεις:
 *
 * | Ποιος ρωτά | Τι ρωτά | Πού απαντιέται |
 * |---|---|---|
 * | ο **τεμαχιστής** (ADR-744) | *«ποια κλειδιά χρωστά **αυτή η διεύθυνση**;»* | εδώ |
 * | ο **άνθρωπος** | *«τι βλέπω και τι διορθώνω;»* | `ProfilePageContent` |
 *
 * ⛔ **ΓΙΑΤΙ ΟΧΙ ΜΕΣΑ ΣΤΟ `ProfilePageContent`**: το σερβίρουν **ΔΥΟ** διαδρομές — αυτή
 * και το `/o/<ψευδώνυμο>/account/profile`. Εγγραφή εκεί θα έδενε το slice **μιας**
 * διεύθυνσης σε **δύο**, και η λογιστική «23 παρόντα / 0 ορφανά» του γεννήτορα, που
 * μετρά **ανά σελίδα**, θα έπαυε να λέει την αλήθεια.
 *
 * ⛔ **ΓΙΑΤΙ ΟΧΙ ΣΤΟ `page.tsx`**: εκείνο είναι **Server Component**, και τα Server /
 * Client δέντρα έχουν **ξεχωριστούς γράφους module** ⇒ η εγγραφή θα έγραφε σε **άλλο**
 * στιγμιότυπο i18next. Πράσινη κλήση που **δεν κάνει τίποτα** — ίδιο μάθημα με το
 * `MyOwnerPropertiesContent` και το `AgencyShowcaseContent`.
 *
 * ⚠️ **Στατική εισαγωγή, εμβέλεια MODULE** — με `import()` το ωμό κλειδί απλώς
 * μετακομίζει σε «ένα καρέ» και **κρύβεται** από το CHECK 3.51.
 *
 * ────────────────────────────────────────────────────────────────────────────
 * 🔑 ΤΙ ΚΟΥΒΑΛΑ ΤΟ SLICE, ΜΕΤΡΗΜΕΝΟ
 * ────────────────────────────────────────────────────────────────────────────
 *
 * **3.311 bytes · ΕΝΑ namespace: `common-account`.** Το κέλυφος κουβαλά 10 namespaces
 * και το `common-account` **δεν** είναι ανάμεσά τους — εκεί όμως ζουν τα κείμενα ΑΦΜ
 * (`account.taxIdentity.*`) που μετακόμισαν στο §9.22. Χωρίς αυτή τη γραμμή, ο ιδιώτης
 * θα διάβαζε **ωμά κλειδιά στο πρώτο καρέ**, ακριβώς στο πεδίο που τον αφορά.
 *
 * 🔑 Το αδελφό προηγούμενο είναι το `/offers/mandate/new`: δηλώθηκε για **τον ίδιο**
 * λόγο και κουβαλά **το ίδιο** `common-account`, επειδή αποδίδει **τον ίδιο**
 * `TaxIdentityField`. Το πεδίο έχει **δύο** καταναλωτές — και οι δύο διαδρομές του
 * χρωστούν κείμενα.
 */

import React from 'react';

// 🧩 ADR-744 §15 (Φ4) — PER-ROUTE SLICE. Δες την κεφαλίδα για το γιατί **εδώ**.
import routeSlice from '@/i18n/generated/routes/profile.el.json';
import { registerRouteSlice } from '@/i18n/route-slice';

import { ProfilePageContent } from '@/components/account/pages/ProfilePageContent';
import { ShowcaseDoor } from '@/components/account/ShowcaseDoor';

registerRouteSlice(routeSlice);

/**
 * ────────────────────────────────────────────────────────────────────────────
 * 🏆 ADR-841 Φ6-Β6 — Η ΠΟΡΤΑ ΤΗΣ ΒΙΤΡΙΝΑΣ ΜΠΑΙΝΕΙ **ΕΔΩ**, ΚΑΙ Η ΘΕΣΗ ΤΗΣ ΕΙΝΑΙ
 *    Ο ΦΡΟΥΡΟΣ ΤΗΣ
 * ────────────────────────────────────────────────────────────────────────────
 *
 * Το αρχείο αυτό γεννήθηκε ως *«όριο διαδρομής»* — και αυτό ακριβώς το κάνει τη
 * σωστή θέση για την πόρτα. Το {@link ProfilePageContent} το σερβίρουν **ΔΥΟ**
 * διευθύνσεις· η δεύτερη *(`/o/<ψευδώνυμο>/account/profile`)* ανήκει σε άνθρωπο
 * που είναι **ήδη μέσα σε χώρο**, και μια πρόταση *«θέλω να με βρίσκουν»* εκεί
 * θα τον έστελνε να φτιάξει **δεύτερο**.
 *
 * ⇒ Η πόρτα ζει στο **όριο της μίας διαδρομής**, όχι μέσα στην κοινή οθόνη:
 * κανένα runtime `if` δεν χρειάζεται, άρα **κανένα δεν μπορεί να ξεχαστεί**.
 * Το `ProfilePageContent` μένει **ανέγγιχτο**.
 *
 * ⚠️ **Η σειρά είναι απόφαση**: η πόρτα μπαίνει **μετά** την οθόνη λογαριασμού.
 * Ο άνθρωπος ήρθε εδώ για το προφίλ του· η βιτρίνα είναι **προσφορά**, όχι το
 * ζητούμενο, και μια προσφορά πάνω από το ζητούμενο είναι διαφήμιση.
 */
export function PrivateProfileContent(): React.ReactElement {
  return (
    <>
      <ProfilePageContent />
      <ShowcaseDoor />
    </>
  );
}
