/**
 * @fileoverview **ΤΟ ΟΝΟΜΑ ΤΗΣ ΒΑΣΗΣ** — μία φορά, για τέσσερα αρχεία ετικετών.
 * @related components/contact/first-contact-labels.ts · ADR-744 · ADR-844
 * @module components/contact/first-contact-namespace
 *
 * ────────────────────────────────────────────────────────────────────────────
 * 🔴 ΓΙΑΤΙ ΕΝΑ ΑΡΧΕΙΟ ΓΙΑ ΜΙΑ ΓΡΑΜΜΗ
 * ────────────────────────────────────────────────────────────────────────────
 *
 * Ο τεμαχιστής του ADR-744 επιβάλλει **αρχείο ετικετών ανά ακροατήριο** *(η
 * `dynamicKeyPolicy` ζει **ανά αρχείο**, και ένα κοινό αρχείο θα κουβαλούσε **όλα** τα
 * λεξιλόγια σε **κάθε** επιφάνεια)*. Άρα τα κείμενα της πρώτης επαφής ζουν σε **δύο**
 * αρχεία — **η πράξη** (`first-contact-labels.ts`) και **ο φιλοξενούμενος**
 * (`first-contact-guest-labels.ts`) — και **και τα δύο** χρειάζονται το **ίδιο** όνομα
 * βάσης.
 *
 * 🔑 **ΓΙΑΤΙ ΟΧΙ re-export από το ένα στο άλλο**: το `first-contact-guest-labels.ts` θα
 * έσερνε τότε **ολόκληρο** το `ACT_KEYS` στη στατική κλειστότητα κάθε καταναλωτή του —
 * δηλαδή θα ακύρωνε ακριβώς τον λόγο που είναι χωριστό αρχείο.
 *
 * 🔑 **ΓΙΑΤΙ ΟΧΙ δύο δηλώσεις**: δύο αλήθειες για το ίδιο πράγμα, με **αόρατη**
 * απόκλιση — το `useTranslation(['λάθος-ns'])` **δεν πετά**, απλώς δεν φορτώνει το
 * πακέτο και η οθόνη βάφει ωμά κλειδιά. Το SSoT κοστίζει μία γραμμή.
 *
 * ⚠️ **ΜΗΔΕΝ ΚΟΣΤΟΣ ΣΤΟ SLICE**: το αρχείο **δεν περιέχει κανένα κλειδί** — μόνο το
 * όνομα της βάσης. Ό,τι το εισάγει δεν κουβαλά ούτε ένα byte μετάφρασης.
 *
 * **Layering**: leaf — μία σταθερά. Κανένα import, κανένα React.
 */

/** Η **μία** βάση όλου του `contact.*`. */
export const FIRST_CONTACT_NS = 'property-market';
