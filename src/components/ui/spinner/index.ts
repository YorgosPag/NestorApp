/**
 * 🏢 ENTERPRISE SPINNER - Barrel Export
 *
 * Canonical export point για Spinner component.
 * Single source of truth - όλα τα imports πρέπει να έρχονται από εδώ.
 */

export { Spinner, AnimatedSpinner } from './Spinner';
export type { SpinnerProps, SpinnerSize, SpinnerColor } from './Spinner';
