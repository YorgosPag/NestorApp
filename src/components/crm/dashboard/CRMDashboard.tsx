'use client';

// This component is being replaced by CRMDashboardPageContent.tsx to resolve an internal server error.
// The previous implementation had an invalid component structure.
// This file can be removed in the future.

import React from 'react';
import { CRMDashboardPageContent } from './CRMDashboardPageContent';

// This file is now obsolete and its content is handled by CRMDashboardPageContent.
// It is kept for historical purposes and can be safely removed.
export default function CRMDashboard() {
  return <CRMDashboardPageContent />;
}
