/**
 * @fileoverview 🏆 **Η ΠΟΡΤΑ ΠΡΟΣ ΤΗ ΔΗΜΟΣΙΑ ΟΨΗ** — «δες τι βλέπει ο κόσμος», και ο
 *   σύνδεσμος έτοιμος στο χέρι (ADR-841 §7 Α21.11).
 * @related ADR-841 §7 Α21.11 · components/mandate/agency-directory-route ·
 *   components/mandate/AgencyShowcaseContent · hooks/useCopyToClipboard
 * @module components/mandate/ShowcasePublicDoor
 *
 * ════════════════════════════════════════════════════════════════════════════
 * 🔴 ΤΟ ΠΕΡΙΣΤΑΤΙΚΟ — Η ΣΕΛΙΔΑ ΥΠΗΡΧΕ ΚΑΙ ΔΕΝ ΟΔΗΓΟΥΣΕ ΠΟΥΘΕΝΑ ΤΙΠΟΤΑ
 * ════════════════════════════════════════════════════════════════════════════
 *
 * Η βιτρίνα δημοσιευόταν κανονικά, αλλά **κανένας σύνδεσμος σε ολόκληρη την εφαρμογή
 * δεν οδηγούσε σε αυτήν**. Ο μόνος τρόπος να τη δει ο ίδιος της ο ιδιοκτήτης ήταν να
 * **πληκτρολογήσει τη διεύθυνση**. Δηλαδή είχαμε φτιάξει βιτρίνα με τα τζάμια στραμμένα
 * στον τοίχο.
 *
 * ════════════════════════════════════════════════════════════════════════════
 * 🏆 Η ΠΡΑΚΤΙΚΗ ΤΩΝ ΜΕΓΑΛΩΝ — ΕΠΑΛΗΘΕΥΜΕΝΗ, ΟΧΙ ΘΥΜΗΘΕΙΣΑ *(έρευνα 2026-09-08)*
 * ════════════════════════════════════════════════════════════════════════════
 *
 * Το βοήθημα της **Zillow** το γράφει βήμα-βήμα: *«για να δεις τη δημόσια όψη του
 * προφίλ σου, πήγαινε **My Zillow → Profile** και πάτα **“See public view”** στην
 * **κορυφή της σελίδας του προφίλ σου**»*. Δύο πράγματα, και τα δύο τα υιοθετούμε:
 *
 * 1. Το προφίλ ζει στο **προσωπικό** μενού — ποτέ πίσω από ρυθμίσεις οργανισμού.
 *    *(Αυτό το μισό ζει στο `config/office-navigation/catalog-settings.ts`, και είναι ο λόγος που εκεί
 *    **λείπει** το `permissions: ['admin_access']`.)*
 * 2. Η πόρτα προς τη δημόσια εκδοχή είναι **πάνω στον ίδιο τον επεξεργαστή**, όχι σε
 *    άλλη οθόνη. Η γενική οδηγία UX το λέει με τα ίδια λόγια: *«αν το προφίλ είναι
 *    δημόσιο, η επεξεργασία πρέπει να γίνεται από την **ίδια** σελίδα, όχι μέσα από
 *    ρυθμίσεις»*.
 *
 * ════════════════════════════════════════════════════════════════════════════
 * 🏆 ΚΑΙ ΤΑ ΔΥΟ ΣΗΜΕΙΑ ΟΠΟΥ ΤΟΥΣ ΞΕΠΕΡΝΑΜΕ
 * ════════════════════════════════════════════════════════════════════════════
 *
 * **Α. Ο ΣΥΝΔΕΣΜΟΣ, ΕΤΟΙΜΟΣ ΓΙΑ ΑΝΤΙΓΡΑΦΗ.** Η Zillow δίνει *θέα*. Ο επαγγελματίας
 * όμως δεν θέλει μόνο να **δει** τη σελίδα του — θέλει να τη **στείλει**: σε μήνυμα, σε
 * υπογραφή email, σε επαγγελματική κάρτα. Στη Zillow αυτό σημαίνει «άνοιξέ την,
 * διάλεξε τη γραμμή διεύθυνσης, αντίγραψε» — τρία βήματα σε κινητό, με πιθανότητα να
 * κοπεί. Εδώ είναι **ένα πάτημα**.
 *
 * **Β. Η ΠΟΡΤΑ ΔΕΝ ΑΝΟΙΓΕΙ ΣΕ ΤΟΙΧΟ.** Εμφανίζεται **μόνο όταν υπάρχει δημοσιευμένη
 * βιτρίνα**. Μια πόρτα προς σελίδα που δεν υπάρχει είναι **χειρότερη από καθόλου
 * πόρτα** — υπόσχεση που σπάει μπροστά στον ίδιο τον ιδιοκτήτη, ίδια οικογένεια με το
 * *«αδιέξοδο με κουμπί»* που το `GuestContactContent` κατέγραψε ήδη μία φορά.
 *
 * ════════════════════════════════════════════════════════════════════════════
 * 🔑 ΓΙΑΤΙ Η ΑΠΟΛΥΤΗ ΔΙΕΥΘΥΝΣΗ ΒΓΑΙΝΕΙ ΑΠΟ ΤΟΝ **ΠΕΡΙΗΓΗΤΗ**
 * ════════════════════════════════════════════════════════════════════════════
 *
 * Ο σύνδεσμος που αντιγράφεται πρέπει να είναι **απόλυτος** *(«nestorconstruct.gr/pro/…»)*
 * — ένα `/pro/…` επικολλημένο σε μήνυμα δεν είναι σύνδεσμος. Η προφανής πηγή θα ήταν
 * μια μεταβλητή περιβάλλοντος, και **μετρήθηκε ότι είναι παγίδα**: έξι σημεία του
 * κώδικα σήμερα γράφουν `process.env.NEXT_PUBLIC_APP_URL ?? 'https://nestor-app.vercel.app'`
 * — δηλαδή γυρίζουν στη **νεκρή** διεύθυνση του Vercel, που το `CLAUDE.md` δηλώνει ρητά
 * παγωμένο από τις 2026-05-09.
 *
 * ⇒ **Ο περιηγητής ξέρει την ίδια του τη διεύθυνση.** Το `window.location.origin` δεν
 * μπορεί να παλιώσει, δεν χρειάζεται ρύθμιση ανά περιβάλλον, και δίνει το **σωστό**
 * αποτέλεσμα σε localhost, σε staging και στην παραγωγή — χωρίς κανείς να το θυμηθεί.
 *
 * ⚠️ **Και γι' αυτό διαβάζεται τη ΣΤΙΓΜΗ ΤΟΥ ΠΑΤΗΜΑΤΟΣ**, όχι στην απόδοση: σε SSR το
 * `window` δεν υπάρχει, και μια ανάγνωση σε επίπεδο render θα έριχνε τη σελίδα.
 */

'use client';

import React from 'react';
import { Copy, Check, ExternalLink } from 'lucide-react';

import { Button } from '@/components/ui/button';
import { Link } from '@/lib/workspace/navigation';
import { useCopyToClipboard } from '@/hooks/useCopyToClipboard';
import { useTranslation } from '@/i18n/hooks/useTranslation';
import { agencyProfileRoute } from '@/components/mandate/agency-directory-route';
import { SHOWCASE_KEYS, SHOWCASE_NS } from '@/components/mandate/agency-showcase-labels';

interface ShowcasePublicDoorProps {
  /** Το ψευδώνυμο του χώρου — **το ίδιο** που δείχνει το πεδίο της διεύθυνσης. */
  readonly alias: string;
  /**
   * Υπάρχει δημοσιευμένη βιτρίνα **αυτή τη στιγμή**;
   *
   * 🔑 **`boolean` και όχι το ίδιο το έγγραφο**, επίτηδες: η πόρτα δεν διαβάζει **τίποτα**
   * από τη βιτρίνα — μόνο *«υπάρχει;»*. Ένα `PublicShowcase | null` εδώ θα έδινε σε αυτό
   * το αρχείο πρόσβαση σε πεδία που δεν το αφορούν, και ο επόμενος αναγνώστης θα έπρεπε
   * να διαβάσει ολόκληρο το component για να βεβαιωθεί ότι δεν τα χρησιμοποιεί.
   */
  readonly published: boolean;
}

/**
 * **Η πόρτα** — θέα και σύνδεσμος, μόνο όταν υπάρχει τι να δειχτεί.
 */
export function ShowcasePublicDoor({
  alias,
  published,
}: ShowcasePublicDoorProps): React.JSX.Element | null {
  const { t } = useTranslation([SHOWCASE_NS]);
  // ⚠️ **Ο άγκιστρος καλείται ΠΑΝΤΑ**, πριν από κάθε πρόωρη επιστροφή — κανόνας των
  //    αγκίστρων του React. Μια «βελτιστοποίηση» που τον έβαζε μέσα στο `if` θα άλλαζε
  //    το πλήθος των αγκίστρων ανάμεσα σε δύο αποδόσεις, δηλαδή θα έριχνε την οθόνη τη
  //    στιγμή που ο άνθρωπος πατά «Δημοσίευση».
  const { copy, copied } = useCopyToClipboard();

  const path = agencyProfileRoute(alias);

  const onCopy = React.useCallback(() => {
    // 🔑 Διαβάζεται **εδώ**, στο πάτημα: σε SSR δεν υπάρχει `window`.
    void copy(`${window.location.origin}${path}`);
  }, [copy, path]);

  if (!published) return null;

  return (
    <section className="flex flex-col gap-2 rounded-lg border border-border bg-card p-4">
      <nav className="flex flex-wrap items-center gap-2">
        {/*
          ⚠️ **`Link` του συνόρου, ποτέ ωμό `next/link`** (CHECK 3.61): ο κριτής βλέπει
          ότι το `pro` είναι δηλωμένο `OUTSIDE_WORKSPACE` και αφήνει τη διεύθυνση
          **άθικτη** — χωρίς αυτό θα έμπαινε το πρόθεμα του χώρου και η «δημόσια» όψη θα
          οδηγούσε **πίσω μέσα** στην εφαρμογή.

          🔑 **Νέα καρτέλα, και είναι απόφαση**: ο άνθρωπος βρίσκεται στη μέση μιας
          επεξεργασίας με **μη αποθηκευμένες** αλλαγές. Πλοήγηση στην ίδια καρτέλα θα του
          τις έτρωγε για να του δείξει την **παλιά** δημοσιευμένη εκδοχή.
        */}
        <Link
          href={path}
          target="_blank"
          rel="noreferrer"
          className="inline-flex items-center gap-2 rounded-md border border-border px-4 py-2 text-sm font-medium text-foreground hover:bg-accent focus-visible:outline-none focus-visible:ring-2 focus-visible:ring-ring"
        >
          <ExternalLink className="h-4 w-4" aria-hidden="true" />
          {t(SHOWCASE_KEYS.publicView)}
        </Link>

        {/*
          🔴 **`<button>` και ΟΧΙ `<a>`, και δεν είναι λεπτομέρεια**: η αντιγραφή είναι
          **πράξη**, όχι πλοήγηση. Ο αναγνώστης οθόνης ανακοινώνει «σύνδεσμος» για το
          πρώτο και «κουμπί» για το δεύτερο, και ο χρήστης πληκτρολογίου περιμένει
          διαφορετικό πλήκτρο. Το `Button` του `ui/` αποδίδει `<button>`.
        */}
        <Button type="button" variant="outline" size="sm" onClick={onCopy}>
          {copied ? (
            <Check className="mr-2 h-4 w-4" aria-hidden="true" />
          ) : (
            <Copy className="mr-2 h-4 w-4" aria-hidden="true" />
          )}
          {t(copied ? SHOWCASE_KEYS.linkCopied : SHOWCASE_KEYS.copyLink)}
        </Button>
      </nav>

      {/*
        ⚠️ **Το μήνυμα επιτυχίας ζει ΜΕΣΑ στο κουμπί** *(«Ο σύνδεσμος αντιγράφηκε»)* και
        δεν προστίθεται ως δεύτερο κείμενο από κάτω: δύο σημεία που λένε το ίδιο πράγμα
        είναι δύο σημεία που μπορούν να διαφωνήσουν. Ο άγκιστρος επαναφέρει μόνος του.
      */}
      <p className="m-0 text-sm text-muted-foreground">{t(SHOWCASE_KEYS.publicViewHint)}</p>
    </section>
  );
}
