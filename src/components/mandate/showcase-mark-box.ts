/**
 * @fileoverview **ΠΟΣΟ ΧΩΡΟ ΠΙΑΝΕΙ ΤΟ ΣΗΜΑ** — το κουτί, ως συνάρτηση της **αναλογίας**
 *   (ADR-841 §7 Α21.9).
 * @related ADR-841 §7 Α21.7 *(το σχήμα)* · components/mandate/showcase-mark-frame ·
 *   components/mandate/ShowcaseMarkView · services/listings/public-shelf-sanitise
 * @module components/mandate/showcase-mark-box
 *
 * ════════════════════════════════════════════════════════════════════════════
 * 🔴 ΤΟ ΠΕΡΙΣΤΑΤΙΚΟ — 16 ΕΙΚΟΝΟΣΤΟΙΧΕΙΑ ΜΕΛΑΝΙΟΥ ΓΙΑ ΜΙΑ ΕΠΩΝΥΜΙΑ
 * ════════════════════════════════════════════════════════════════════════════
 *
 * Ο Giorgio ανέβασε λογότυπο και ρώτησε: *«είναι σωστό το μέγεθος; αυτό θα έβαζαν οι
 * μεγάλοι παίκτες;»*. **Δεν ήταν** — και το μέγεθος ήταν το **μικρότερο** από τα δύο
 * προβλήματα.
 *
 * Η προηγούμενη γραφή έδινε **τετράγωνο** κουτί σε **κάθε** σήμα:
 *
 * ```
 * card: 'h-11 w-11'   →  44×44
 * page: 'h-16 w-16'   →  64×64
 * ```
 *
 * Και τα λογότυπα των γραφείων είναι σχεδόν πάντα **οριζόντια** *(wordmark)*. Με
 * `object-contain` μέσα σε τετράγωνο, ένα λογότυπο **4:1** σε κουτί 64 δίνει μελάνι
 * **64×16** — δεκαέξι εικονοστοιχεία ύψος για μια ολόκληρη επωνυμία.
 *
 * 🔑 **Η αριθμητική είναι ολόκληρο το επιχείρημα.** Σε τετράγωνο ακμής `S`, λογότυπο
 * αναλογίας `r > 1` αφήνει ύψος μελανιού `S / r`:
 *
 * | αναλογία | τετράγωνο 96 | ζώνη 64×240 | κέρδος |
 * |---|---|---|---|
 * | 1,5:1 | 64px | 64px | — |
 * | 3:1 | 32px | 64px | **×2** |
 * | 4:1 | 24px | 60px | **×2,5** |
 * | 6:1 | 16px | 40px | **×2,5** |
 *
 * ════════════════════════════════════════════════════════════════════════════
 * 🏆 Η ΠΡΑΚΤΙΚΗ ΤΩΝ ΜΕΓΑΛΩΝ — ΔΥΟ ΣΧΟΛΕΣ, ΚΑΙ ΔΙΑΛΕΓΟΥΜΕ ΑΝΑ ΕΙΔΟΣ
 * ════════════════════════════════════════════════════════════════════════════
 *
 * Η έρευνα *(2026-09-07)* βρήκε **δύο** καθιερωμένες απαντήσεις, όχι μία:
 *
 * | σχολή | ποιοι | πώς |
 * |---|---|---|
 * | **Α — επιβάλλω τετράγωνο στην ΕΙΣΟΔΟ** | Google Merchant *(«aspect ratio 1:1», 500-2000px)* · Facebook · Zillow avatar | ο χρήστης **οφείλει** να δώσει 1:1 |
 * | **Β — δέχομαι ΟΤΙΔΗΠΟΤΕ, σταθερό ΥΨΟΣ / ελεύθερο πλάτος** | partner-logo components *(Spirit DS, GitLab Pajamas, UWE)* — *«set all logos to the same height, height:100%, width:auto»* | το κουτί υποχωρεί στο περιεχόμενο |
 *
 * 🔴 **Η σχολή Α είναι ΛΑΘΟΣ ΓΙΑ ΕΜΑΣ, και ο λόγος είναι ο πελάτης μας.** Ο υδραυλικός,
 * ο ελαιοχρωματιστής και το μικρό τεχνικό γραφείο **δεν έχουν** τετράγωνο λογότυπο και
 * **δεν πρόκειται να φτιάξουν**. Το «δώσε μου 1:1» δεν είναι προδιαγραφή — είναι **«όχι»
 * στη μεγάλη πλειοψηφία**, δηλαδή η ίδια ποινή προς τον μη-οργανωμένο επαγγελματία που
 * το Α9.3 απαγόρευσε ρητά για το μητρώο.
 *
 * ✅ **Η σχολή Β είναι σωστή για το λογότυπο· η Α είναι σωστή για το ΠΡΟΣΩΠΟ.** Και τα
 * δύο ισχύουν ταυτόχρονα, γιατί το είδος **το δηλώνει ήδη ο άνθρωπος**:
 *
 * - `portrait` ⇒ **πάντα τετράγωνο**. Τα πρόσωπα *είναι* περίπου τετράγωνα και το
 *   `object-cover` δεν χάνει τίποτα ουσιώδες — η κυκλική μάσκα είναι το πρότυπο κάθε
 *   πλατφόρμας. Η αναλογία εδώ **δεν ρωτιέται καν**.
 * - `logo` ⇒ **ρωτάμε την αναλογία**, γιατί μόνο εκεί το `object-contain` αφήνει κενό.
 *
 * ════════════════════════════════════════════════════════════════════════════
 * 🏆 ΚΑΙ ΤΟ ΣΗΜΕΙΟ ΟΠΟΥ ΞΕΠΕΡΝΑΜΕ ΤΟΥΣ ΜΕΓΑΛΟΥΣ: **ΔΕΝ ΡΩΤΑΜΕ ΤΟΝ ΑΝΘΡΩΠΟ**
 * ════════════════════════════════════════════════════════════════════════════
 *
 * Οι πλατφόρμες που δέχονται ελεύθερη αναλογία ζητούν από τον χρήστη **δεύτερη δήλωση**
 * *(«ανέβασε τετράγωνη εκδοχή για avatar και οριζόντια για κεφαλίδα»)* — δηλαδή
 * μεταφέρουν τη δουλειά στον άνθρωπο που **δεν την ξέρει**.
 *
 * 🔑 **Εμείς δεν τη χρειαζόμαστε: η αναλογία ΕΙΝΑΙ ΗΔΗ ΓΝΩΣΤΗ.** Το
 * {@link DeclaredShowcaseMark} κουβαλά `image.width` και `image.height` — τα γράφει ο
 * καθαριστής από το `sharp` *(`fit: 'inside'`, δηλαδή **πραγματικές** διαστάσεις του
 * παραγώγου)*. Άρα:
 *
 * > **Καμία νέα δήλωση · κανένα νέο πεδίο · καμία μετάβαση δεδομένων · καμία ερώτηση
 * > στον άνθρωπο.** Το σχήμα του κουτιού **παράγεται** από ό,τι ήδη ξέρουμε.
 *
 * ⛔ **ΑΠΟΡΡΙΦΘΗΚΕ: «τρίτο είδος `wordmark` δίπλα στο `logo`».** Θα ζητούσε από τον
 * άνθρωπο να ταξινομήσει το **σχήμα** του αρχείου του — πληροφορία που το ίδιο το
 * αρχείο **λέει με ακρίβεια**. Είναι η ίδια μαντεψιά που το `showcase-mark-kind`
 * απέρριψε από την ανάποδη: εκεί δεν μαντεύουμε ό,τι **μόνο** ο άνθρωπος ξέρει
 * *(«είμαι λογότυπο ή πρόσωπο;»)*· εδώ δεν **ρωτάμε** ό,τι ξέρουν τα bytes.
 *
 * ════════════════════════════════════════════════════════════════════════════
 * ⚠️ ΓΙΑΤΙ Η ΚΑΡΤΑ ΜΕΝΕΙ ΤΕΤΡΑΓΩΝΗ — ΤΕΚΜΗΡΙΩΜΕΝΗ ΥΠΟΧΩΡΗΣΗ, ΟΧΙ ΠΑΡΑΛΕΙΨΗ
 * ════════════════════════════════════════════════════════════════════════════
 *
 * Στον **κατάλογο** το σήμα κάθεται αριστερά και η επωνυμία δεξιά, σε **σειρά** από
 * κάρτες. Μεταβλητό πλάτος σήματος ⇒ κάθε τίτλος ξεκινά σε **άλλο** `x` ⇒ η στήλη χάνει
 * τον κατακόρυφο ρυθμό της. Ο ρυθμός της σειράς **νικά** εκεί, και είναι ό,τι κάνουν
 * όλοι *(Zillow · Idealista δείχνουν τετράγωνο νύχι στις λίστες)*.
 *
 * 🔴 **Η υποχώρηση δηλώνεται με το κόστος της**: οριζόντιο λογότυπο στην κάρτα μένει
 * ~44×11. Αυτό **δεν** το λύνει η παρουσίαση — το λύνει η **Φάση Β** *(οπτική
 * κανονικοποίηση στο ανέβασμα: κόψιμο του περιττού περιθωρίου, ώστε το ίδιο κουτί να
 * γεμίζει με **περισσότερο μελάνι**)*.
 *
 * ✅ **ΠΑΡΑΔΟΘΗΚΕ — Α21.10.** Ο καθαριστής κόβει πλέον το λογότυπο **στο μελάνι του**
 * *(`public-shelf-border` + `public-shelf-sanitise`)*, και εδώ έμεινε το **μισό που δεν
 * ανήκει στα bytes**: το {@link MARK_CLEAR_SPACE}. ⚠️ **Και άλλαξε κάτι βαθύτερο**: το
 * `width`/`height` που διαβάζει το {@link marksBand} είναι πλέον η αναλογία του
 * **μελανιού**, όχι του **καμβά** — δηλαδή wordmark αποθηκευμένο σε τετράγωνο αρχείο
 * παίρνει επιτέλους **ζώνη**, ενώ πριν έπαιρνε τετράγωνο με 16px μελάνι.
 *
 * ════════════════════════════════════════════════════════════════════════════
 * 🔴 ΤΟ `sizes` ΔΕΝ ΕΙΝΑΙ ΣΥΝΟΔΕΥΤΙΚΟ — ΕΙΝΑΙ ΜΕΡΟΣ ΤΗΣ ΙΔΙΑΣ ΑΠΟΦΑΣΗΣ
 * ════════════════════════════════════════════════════════════════════════════
 *
 * Το `<img srcSet>` με `w` descriptors διαλέγει παράγωγο **βάσει του `sizes`**. Ένα
 * κουτί που πλάτυνε σε 240px με `sizes="64px"` θα κατέβαζε το παράγωγο **64** και θα το
 * τέντωνε ×3,75 — **θολούρα, χωρίς κανένα σφάλμα πουθενά**. Γι' αυτό το `sizes`
 * επιστρέφεται **από την ίδια συνάρτηση** που ορίζει το κουτί: δύο τιμές που πρέπει να
 * συμφωνούν **δεν** επιτρέπεται να ζουν σε δύο πίνακες.
 *
 * *(Η προηγούμενη γραφή τις είχε σε δύο — `MARK_SIZES` και `MARK_SIZES_ATTR` — και το
 * ίδιο της το σχόλιο παραδεχόταν: «η αντιστοιχία δεν επαληθεύεται από μεταγλωττιστή».)*
 *
 * ⚠️ **ΤΟ ΑΡΧΕΙΟ ΕΙΝΑΙ ΠΑΓΙΔΑ TAILWIND**, ακριβώς όπως το `showcase-mark-frame`: κάθε
 * κλάση εδώ είναι **στατική, ολόκληρη και σε τιμή**. Καμία παρεμβολή — ούτε σε
 * παράδειγμα τεκμηρίωσης. Και ζει στο `components/`, γιατί ο σαρωτής **δεν διαβάζει το
 * `src/lib/`**.
 */

import type { ShowcaseMarkKind } from '@/lib/agency/showcase-mark-kind';

/**
 * **Πού ζωγραφίζεται το σήμα** — δύο περιβάλλοντα, όσα και οι οθόνες που υπάρχουν.
 *
 * ⚠️ Ο τύπος κατοικεί **εδώ** και όχι στο `ShowcaseMarkView`: το «πόσο χώρο πιάνει»
 * είναι το ερώτημα **αυτού** του αρχείου, και ο τύπος είναι η είσοδός του. Στην
 * προηγούμενη γραφή τον εξήγαγε η οθόνη — δηλαδή ο **καταναλωτής** όριζε το λεξιλόγιο.
 */
export type ShowcaseMarkSize = 'card' | 'page';

/**
 * **Πάνω από αυτή την αναλογία το λογότυπο παύει να χωράει σε τετράγωνο.**
 *
 * 🔑 **Γιατί 1,25 και όχι 1 ή 2** — το κατώφλι είναι το σημείο όπου η απώλεια γίνεται
 * αισθητή, και μετριέται: σε τετράγωνο, αναλογία `r` αφήνει `1/r` του ύψους.
 *
 * | r | ύψος μελανιού | κρίση |
 * |---|---|---|
 * | 1,10 | 91% | αδιάφορο — μια ζώνη εδώ θα ήταν **θόρυβος** |
 * | **1,25** | **80%** | **το όριο**: πιο κάτω δεν αξίζει αλλαγή σχήματος |
 * | 1,50 | 67% | αρχίζει να φαίνεται |
 * | 2,00 | 50% | το μισό σήμα είναι κενό |
 *
 * ⚠️ **Κάτω από 1 (κάθετο λογότυπο) ΔΕΝ υπάρχει δεύτερο κατώφλι, επίτηδες**: σε
 * τετράγωνο κουτί με `object-contain` ένα κάθετο σήμα γεμίζει το **ύψος** και μένει
 * στενό — δηλαδή **δεν χάνει τίποτα**. Τρίτη περίπτωση θα ήταν μηχανισμός χωρίς ζημιά
 * να αποτρέψει.
 */
export const MARK_BAND_ASPECT_THRESHOLD = 1.25;

/**
 * **Τι είναι το σήμα, από τη σκοπιά του κουτιού.**
 *
 * 🔴 **Διακεκριμένη ένωση, ώστε το «λογότυπο χωρίς αναλογία» να ΜΗΝ ΕΙΝΑΙ ΕΚΦΡΑΣΙΜΟ.**
 * Με προαιρετικό `aspect?: number` ο καλών θα μπορούσε να το ξεχάσει και το σήμα θα
 * έπεφτε σιωπηλά στο τετράγωνο — δηλαδή ακριβώς το σημερινό ελάττωμα, ξαναγεννημένο
 * από παράλειψη αντί από απόφαση.
 */
export type MarkBoxSubject =
  /** Το παραγόμενο πλακίδιο αρχικών — **πάντα** τετράγωνο, δεν έχει αναλογία. */
  | { readonly kind: 'lettermark' }
  /** Δηλωμένη εικόνα, με τις **πραγματικές** της διαστάσεις. */
  | { readonly kind: ShowcaseMarkKind; readonly width: number; readonly height: number };

/**
 * **Ο αέρας γύρω από το ΤΡΙΜΜΕΝΟ μελάνι** — και είναι **γεωμετρία**, όχι γούστο
 * (ADR-841 §7 Α21.10).
 *
 * ────────────────────────────────────────────────────────────────────────────
 * 🔴 ΓΙΑΤΙ ΤΟ CLEAR SPACE ΖΕΙ ΕΔΩ ΚΑΙ ΟΧΙ ΜΕΣΑ ΣΤΑ BYTES
 * ────────────────────────────────────────────────────────────────────────────
 *
 * Η **Φάση Β** κόβει στο ανέβασμα το περιθώριο που κανείς δεν επέλεξε, ώστε δύο σήματα
 * να έχουν **ίδιο οπτικό βάρος**. Το αρχικό σχέδιο ζητούσε να **ξαναγραφτεί** σταθερό
 * ποσοστό αέρα μέσα στα ίδια τα bytes *(`sharp.extend`)*. ⛔ **Απορρίφθηκε**: τα
 * δημοσιευμένα bytes είναι **content-addressed και αμετάβλητα**, άρα ψημένο 6% σημαίνει
 * ότι μια μελλοντική αλλαγή σε 8% **διχοτομεί τον κόσμο** σε παλιά και νέα σήματα —
 * ακριβώς το κενό που η Α21.9.7 δήλωσε για το παράγωγο 512, ξαναγεννημένο εθελοντικά.
 *
 * 🏆 **Εδώ αλλάζει με μία γραμμή, για ΟΛΑ τα σήματα, χωρίς να ξαναγραφτεί ούτε ένα byte.**
 *
 * ────────────────────────────────────────────────────────────────────────────
 * 🔑 ΚΑΙ Ο ΑΡΙΘΜΟΣ ΠΡΟΚΥΠΤΕΙ ΑΠΟ ΤΟ ΠΛΑΙΣΙΟ, ΔΕΝ ΔΙΑΛΕΧΤΗΚΕ
 * ────────────────────────────────────────────────────────────────────────────
 *
 * Το σήμα φοράει `rounded-lg`, δηλαδή `var(--radius)` = **8px**. Ένα ορθογώνιο μέσα σε
 * στρογγυλεμένο πλαίσιο ακτίνας `r` χρειάζεται εσοχή **`r · (1 − 1/√2)`** για να μην του
 * **κόψει το πλαίσιο τις γωνίες** — εδώ **≈ 2,34px**. Το `p-1` *(4px)* είναι το πρώτο
 * σκαλί της κλίμακας πάνω από αυτό.
 *
 * 🔴 **Άρα ο αέρας δεν είναι διακόσμηση**: χωρίς αυτόν, τριμμένο σήμα που ακουμπά τις
 * άκρες θα έχανε τις τέσσερις γωνίες του **στην ίδια την καμπύλη του πλαισίου** — η
 * Φάση Β θα είχε βελτιώσει το μέγεθος και **χαλάσει το σχήμα**.
 *
 * ⚠️ **ΕΝΑΣ αριθμός για κάθε μέγεθος, επίτηδες**: η ακτίνα είναι **ίδια** και στα δύο
 * περιβάλλοντα *(`rounded-lg` παντού)*, άρα η απαίτηση είναι ίδια. Ένας πίνακας ανά
 * μέγεθος θα υπονοούσε ότι η απάντηση εξαρτάται από το μέγεθος — **δεν εξαρτάται**.
 */
const MARK_CLEAR_SPACE = 'p-1';

/** Καμία εσοχή — δες γιατί στο {@link markBox}. */
const NO_CLEAR_SPACE = '';

/** **Πόσο χώρο πιάνει** — μόνο οι διαστάσεις, χωρίς την ερώτηση του αέρα. */
interface MarkBoxExtent {
  /** **Μόνο** οι διαστάσεις — κλάσεις Tailwind, στατικές. */
  readonly className: string;
  /**
   * Το μέγεθος γραμματοσειράς για το **πλακίδιο αρχικών**, χωριστά.
   *
   * ⚠️ **Χωριστό πεδίο και όχι ενωμένο με το `className`, επίτηδες**: το `<img>` δεν
   * γράφει ποτέ κείμενο, και μια κλάση `text-*` πάνω του θα ήταν **σιωπηλός θόρυβος**
   * που ο επόμενος αναγνώστης δεν θα μπορούσε να κρίνει αν είναι σκόπιμη.
   */
  readonly text: string;
  /** Το **μέγιστο** πλάτος απόδοσης σε CSS px, ως τιμή `sizes` του `<img>`. */
  readonly sizesAttr: string;
}

/** Το κουτί: διαστάσεις, ο αέρας, **και** η υπόσχεση προς τον περιηγητή — μαζί. */
export interface ShowcaseMarkBox extends MarkBoxExtent {
  /**
   * Η εσοχή που κρατά το **τριμμένο** μελάνι μακριά από την καμπύλη — δες
   * {@link MARK_CLEAR_SPACE}.
   *
   * ⚠️ **Χωριστό πεδίο, όπως το {@link MarkBoxExtent.text}**: το ζωγραφίζει **μόνο** η
   * εικόνα. Το πλακίδιο αρχικών κεντράρει κείμενο — μια εσοχή εκεί θα έσπρωχνε τον
   * χαρακτήρα χωρίς να λύνει τίποτα, αφού το κείμενο **δεν έχει γωνίες να κοπούν**.
   */
  readonly clearSpace: string;
}

/**
 * **Το τετράγωνο** — πρόσωπα, εικονοσήματα, πλακίδια αρχικών.
 *
 * ⚠️ Και τα δύο μεγέθη κρατούν `font-bold` πάνω από **18,66px**, που είναι το κατώφλι
 * του WCAG για «μεγάλο κείμενο» *(3:1)*: `text-xl` = 20px · `text-4xl` = 36px. Ένα
 * τρίτο, μικρότερο μέγεθος **δεν προστίθεται** χωρίς να ξαναγίνει εκείνη η κουβέντα.
 */
const SQUARE_BOX: Readonly<Record<ShowcaseMarkSize, MarkBoxExtent>> = {
  /** Η κάρτα του καταλόγου και της βιτρίνας — 44×44. */
  card: { className: 'h-11 w-11', text: 'text-xl', sizesAttr: '44px' },
  /**
   * Η επικεφαλίδα της σελίδας `/pro/<alias>` — **96×96**, από 64.
   *
   * 🔑 Δίπλα σε τίτλο `text-2xl` (24px), τα 64px ήταν **υποσημείωση**, όχι ήρωας: είναι
   * μέγεθος avatar σε λίστα σχολίων. Οι σελίδες προφίλ των μεγάλων κινούνται στα
   * **100-180px**. Το 96 είναι το πλησιέστερο σημείο της κλίμακας που το **ράφι
   * καλύπτει σε διπλή πυκνότητα χωρίς νέο παράγωγο** *(96×2 = 192 ≤ 256)*.
   */
  page: { className: 'h-24 w-24', text: 'text-4xl', sizesAttr: '96px' },
} as const;

/**
 * **Η ζώνη** — σταθερό ύψος, **ελεύθερο** πλάτος με ταβάνι. Η σχολή Β, αυτολεξεί.
 *
 * ⚠️ **Το ταβάνι δεν είναι διακόσμηση**: χωρίς `max-w`, λογότυπο 10:1 θα άπλωνε 640px
 * και θα έσπρωχνε την επωνυμία εκτός οθόνης σε κινητό. Τα **240px** είναι το σημείο
 * όπου το ράφι δίνει ακόμη ακέραιο παράγωγο *(256)*.
 *
 * 🔴 **Το `w-auto` ΠΡΕΠΕΙ να δηλωθεί ρητά** — δεν είναι προεπιλογή: το `<img>` κληρονομεί
 * `width`/`height` ως χαρακτηριστικά *(τα δίνει η οθόνη για να δεσμεύσει χώρο και να μην
 * τιναχτεί η διάταξη)*, και χωρίς κλάση πλάτους ο περιηγητής θα τα τιμούσε **σε
 * εικονοστοιχεία**.
 */
const BAND_BOX: Readonly<Record<ShowcaseMarkSize, MarkBoxExtent>> = {
  /**
   * ⚠️ **Ταυτόσημο με το τετράγωνο, ΕΠΙΤΗΔΕΣ** — δες «γιατί η κάρτα μένει τετράγωνη».
   * Η γραμμή υπάρχει ώστε ο πίνακας να είναι **εξαντλητικός**: μια μελλοντική αλλαγή
   * γνώμης έχει **θέση να γραφτεί**, αντί να απαιτεί νέο `if` στον καλούντα.
   */
  card: { className: 'h-11 w-11', text: 'text-xl', sizesAttr: '44px' },
  /** Ύψος **64**, πλάτος ελεύθερο ως **240**. */
  page: { className: 'h-16 w-auto max-w-[15rem]', text: 'text-4xl', sizesAttr: '240px' },
} as const;

/**
 * **Χρειάζεται αυτό το σήμα ζώνη;** — η **μία** διατύπωση του κανόνα.
 *
 * ⚠️ **Μηδενικές ή αρνητικές διαστάσεις ⇒ τετράγωνο**, ποτέ διαίρεση με το μηδέν. Δεν
 * είναι αμυντικός κώδικας για φαντάσματα: το `width`/`height` έρχεται από **δημοσιευμένο
 * έγγραφο**, δηλαδή από δεδομένα που γράφτηκαν από παλιότερες εκδόσεις του καθαριστή και
 * διαβάζονται σήμερα.
 */
export function marksBand(subject: MarkBoxSubject): boolean {
  if (subject.kind !== 'logo') return false;
  if (subject.width <= 0 || subject.height <= 0) return false;

  return subject.width / subject.height > MARK_BAND_ASPECT_THRESHOLD;
}

/**
 * **Το κουτί του σήματος** — η μία είσοδος για κάθε οθόνη που ζωγραφίζει σήμα.
 *
 * 🔑 Διαστάσεις **και** `sizes` μαζί: δες «το `sizes` δεν είναι συνοδευτικό».
 */
export function markBox(size: ShowcaseMarkSize, subject: MarkBoxSubject): ShowcaseMarkBox {
  const extent = marksBand(subject) ? BAND_BOX[size] : SQUARE_BOX[size];

  return { ...extent, clearSpace: needsClearSpace(subject) ? MARK_CLEAR_SPACE : NO_CLEAR_SPACE };
}

/**
 * **Ποιο σήμα χρειάζεται αέρα;** — η **μία** διατύπωση, αδελφή του {@link marksBand}.
 *
 * 🔑 **ΜΟΝΟ το λογότυπο, και ο λόγος είναι ότι μόνο αυτό ΤΡΙΒΕΤΑΙ** *(Α21.10)*. Ο αέρας
 * δεν είναι στολίδι: υπάρχει επειδή ο καθαριστής κόβει το λογότυπο **στο μελάνι του**,
 * και μελάνι που ακουμπά την άκρη ενός `rounded-lg` **χάνει τις γωνίες του**. Ό,τι δεν
 * τρίβεται δεν ακουμπά την άκρη, άρα δεν έχει τι να χάσει:
 *
 * - **`portrait`** — δεν τρίβεται *(το περιθώριό του **ΕΙΝΑΙ** το κάδρο)* και γεμίζει
 *   κύκλο με `object-cover`. Εσοχή εκεί θα άφηνε δαχτυλίδι `bg-card` γύρω από ένα
 *   πρόσωπο — ορατή ζημιά, μηδέν όφελος.
 * - **`lettermark`** — είναι **κείμενο** που κεντράρεται· δεν έχει γωνίες να κοπούν.
 *
 * ⚠️ **Δεν ρωτά «τρίφτηκε;» αλλά «είναι λογότυπο;»**, και είναι σκόπιμο: η οθόνη **δεν
 * ξέρει** αν ο κριτής του περιγράμματος έδωσε τελικά άδεια *(ένα app-icon λογότυπο μένει
 * άτριφτο)*. Η ερώτηση που **μπορεί** να απαντηθεί και στις δύο πλευρές είναι το είδος —
 * και η απάντηση είναι σωστή και για τις δύο: το άτριφτο λογότυπο απλώς έχει ήδη δικό
 * του περιθώριο, οπότε 4px παραπάνω δεν χαλούν τίποτα.
 */
function needsClearSpace(subject: MarkBoxSubject): boolean {
  return subject.kind === 'logo';
}
