/**
 * @fileoverview **ΠΟΣΟΥΣ ΧΑΡΑΚΤΗΡΕΣ ΠΡΕΠΕΙ ΝΑ ΜΠΟΡΕΙ ΝΑ ΔΕΙΞΕΙ ΚΑΘΕ ΠΕΔΙΟ ΔΙΕΥΘΥΝΣΗΣ.**
 * @related AddressFieldControlRow · AddressWithHierarchy · AddressFormFieldRow · ADR-332 D27 Ζ7
 *
 * ═════════════════════════════════════════════════════════════════════════════
 * 🔴 ΓΙΑΤΙ ΥΠΑΡΧΕΙ ΑΥΤΟ ΤΟ ΑΡΧΕΙΟ
 * ═════════════════════════════════════════════════════════════════════════════
 *
 * **Μετρημένο ζωντανά 2026-09-13** (Chrome, ALFA, κατάσταση επεξεργασίας): όταν η στήλη
 * της φόρμας στενεύει, τα πεδία **Οδός / Αριθμός / Τ.Κ.** φτάνουν σε **ωφέλιμο πλάτος
 * ΜΗΔΕΝ** — `clientWidth − padding = 0`. **Ο άνθρωπος δεν βλέπει τι γράφει.**
 *
 * Η αιτία δεν ήταν το στένεμα· ήταν ότι *η προτεραιότητα συρρίκνωσης ήταν ανάποδη*.
 * Στα 700 px το `fieldset` του Αριθμού μετρήθηκε **122,8 px** και μοιραζόταν ως εξής:
 *
 * | στοιχείο                          | πλάτος  | συμπεριφορά                         |
 * |-----------------------------------|---------|-------------------------------------|
 * | `<Input>` (`flex-1` / `w-full`)   | **26,5**| `flex-shrink: 1` ⇒ πέφτει ως το 0   |
 * | `<AddressFieldBadge>` «Ταιριάζει» | **90,3**| `min-content` ⇒ **ποτέ**            |
 *
 * `122,8 − 90,3 − 6(gap) = 26,5`, μείον padding 24 ⇒ **ωφέλιμο ≈ 0**. Κλείνει ακριβώς.
 * Δηλαδή **το σχόλιο για το δεδομένο έτρωγε 3,4× τον χώρο του ίδιου του δεδομένου.**
 *
 * ═════════════════════════════════════════════════════════════════════════════
 * 🔑 ΓΙΑΤΙ ΣΕ `ch` ΚΑΙ ΟΧΙ ΣΕ `px`
 * ═════════════════════════════════════════════════════════════════════════════
 *
 * Ένα δάπεδο σε **px** είναι αυθαίρετο: σπάει σε άλλη γραμματοσειρά, σε άλλο μέγεθος
 * κειμένου, σε zoom του χρήστη — και δεν απαντά καν στην ερώτηση που θέτει. Η ερώτηση
 * είναι **«χωράει αυτό που κρατά το πεδίο;»**, και η μονάδα της είναι ο **χαρακτήρας**.
 *
 * Το `ch` (πλάτος του «0» στην τρέχουσα γραμματοσειρά) είναι ακριβώς αυτή η μονάδα:
 * ακολουθεί τη γραμματοσειρά, το μέγεθος και το zoom **μόνο του**.
 *
 * ⚠️ Οι αριθμοί παρακάτω **δεν διαλέχτηκαν με το μάτι**. Καθένας είναι «όσοι χαρακτήρες
 * κρατά πραγματικά το πεδίο, συν χώρος για τον κέρσορα». Αν αλλάξει το περιεχόμενο που
 * δέχεται ένα πεδίο, αλλάζει **εδώ** — και η πύλη ωφέλιμου πλάτους το μαθαίνει αμέσως,
 * γιατί διαβάζει **αυτό** το αρχείο και όχι δικό της αντίγραφο.
 */

import type { ResolvedAddressFields } from '@/lib/geocoding/geocoding-types';

/**
 * Ο ελληνικός Τ.Κ. είναι **πάντα** πέντε ψηφία, και η οθόνη τον δείχνει με κενό
 * («546 24» — ADR-332 D16) ⇒ **έξι** ορατοί χαρακτήρες.
 *
 * 🔑 **Η ΜΙΑ ΔΗΛΩΣΗ.** Το `maxLength` του πεδίου διαβάζει από εδώ. Μέχρι το Ζ7 ήταν
 * γραμμένο ως ωμό `6` μέσα στο JSX (`AddressWithHierarchy.tsx:270`) — δηλαδή ο αριθμός
 * ζούσε **δύο** φορές μόλις προστέθηκε το δάπεδο, και θα απέκλιναν στην πρώτη αλλαγή.
 */
export const GREEK_POSTAL_CODE_DISPLAY_LENGTH = 6;

/**
 * **Ωφέλιμο δάπεδο ανά πεδίο, σε χαρακτήρες (`ch`).**
 *
 * Κάτω από αυτό, το πεδίο **δεν επιτρέπεται** να συρρικνωθεί: αντ' αυτού η γραμμή
 * **τυλίγεται** και το σήμα κατάστασης πέφτει από κάτω. Δες `AddressFieldControlRow`.
 *
 * ⚠️ Δεν είναι το πλάτος **προτίμησης** — είναι το πλάτος κάτω από το οποίο το πεδίο
 * παύει να είναι πεδίο. Σε άνετο χώρο τα πεδία μεγαλώνουν κανονικά (`flex-grow`).
 */
export const ADDRESS_FIELD_MIN_CHARS: Readonly<Record<keyof ResolvedAddressFields, number>> = {
  /** «Παπαναστασίου», «Εθνικής Αντιστάσεως» — ονόματα οδών είναι μακριά· 14 δείχνει αρκετό για αναγνώριση. */
  street: 14,
  /** «104», «104Α», «12-14» — ο μεγαλύτερος ελληνικός αριθμός οδού που συναντάται ρεαλιστικά. */
  number: 6,
  /** «546 24» = 6 ορατοί χαρακτήρες + κέρσορας. */
  postalCode: GREEK_POSTAL_CODE_DISPLAY_LENGTH + 1,
  /** «Νέα Μαγνησία», «Άνω Τούμπα» — συνοικίες. */
  neighborhood: 12,
  /** «Θεσσαλονίκη», «Σταυρούπολις». */
  city: 12,
  /** Περιφερειακή ενότητα — «Θεσσαλονίκης». */
  county: 12,
  /** Περιφέρεια — «Κεντρικής Μακεδονίας» (μακρύ, αλλά σπάνια επεξεργάσιμο). */
  region: 12,
  /** «Ελλάδα», «Κύπρος». */
  country: 10,
} as const;

/**
 * Το δάπεδο ενός πεδίου που **δεν** ανήκει στο `ResolvedAddressFields` (π.χ. ο οικισμός,
 * που ζει ως `settlementName` στο `AddressWithHierarchy`).
 *
 * ⚠️ Υπάρχει για να **μην** μπει ωμός αριθμός στο JSX όταν το πεδίο δεν έχει γραμμή στον
 * πίνακα. Αν ένα τέτοιο πεδίο αποκτήσει μόνιμη θέση, **βάλε του γραμμή** — μην το αφήσεις
 * εδώ. Το γενικό δάπεδο είναι απάντηση για το «δεν ξέρω», όχι για το «δεν κοίταξα».
 */
export const ADDRESS_FIELD_DEFAULT_MIN_CHARS = 12;

/** Το δάπεδο του πεδίου, ή το γενικό αν το πεδίο δεν έχει δική του γραμμή. */
export function addressFieldMinChars(field: string): number {
  return (
    ADDRESS_FIELD_MIN_CHARS[field as keyof ResolvedAddressFields] ??
    ADDRESS_FIELD_DEFAULT_MIN_CHARS
  );
}
