/**
 * **Τι μετρά το CHECK 3.94** — διαδρομές · πλάτη · θέματα (ADR-797 §Φ.Ρ).
 *
 * Κάθε γραμμή εδώ είναι **απόφαση**, όχι γούστο — και ο λόγος γράφεται δίπλα της.
 */

/** Οι δημόσιες σελίδες που περνά ανώνυμος — η πρώτη επαφή κάθε επισκέπτη. */
export const REFLOW_ROUTES = [
  // Το περιστατικό: 5 μπλοκ ως 436 px στα 390 (κεφαλίδα · ήρωας · πόρτες · κάλυψη).
  '/',
  // Ίδια κεφαλίδα, και χάρτης πλήρους παραθύρου — η κεφαλίδα κοβόταν με τον ίδιο τρόπο.
  '/search/results',
  // Ακτίνες του κόμβου (ADR-777 §8.82): ίδιο `gap-6` σε πλέγμα μέτρου — ίδια βλάβη.
  '/pro',
  '/stay',
] as const;

/**
 * Τα πλάτη. 🔑 **320 = WCAG 2.2 SC 1.4.10 (Reflow)**: περιεχόμενο χωρίς απώλεια στα 320 CSS px
 * (≈ 1280 px με ζουμ 400%). 360 · 390 · 414 = τα κινητά της αγοράς (Android μέσο · iPhone 12–15 ·
 * iPhone Plus). 768 · 1024 = **τα όρια της κεφαλίδας**: μετρήθηκε ότι η πλήρης μπάρα θέλει
 * ~810 px, γι' αυτό το «☰ Μενού» ζει ως το `lg` — το 768 φυλά ότι δεν ξαναγυρίσει στο `md`.
 */
export const REFLOW_WIDTHS = [320, 360, 390, 414, 768, 1024] as const;

/** Και τα δύο θέματα: τα πλάτη ΔΕΝ εξαρτώνται από χρώμα — αν αποκλίνουν, κάτι είναι λάθος. */
export const REFLOW_THEMES = ['light', 'dark'] as const;

/**
 * **Το κλειστό σύνολο επιφανειών που τοποθετούν παιδιά ΕΞΩ από το πλαίσιο εκ κατασκευής.**
 *
 * Ο χάρτης (MapLibre) μετακινεί πλακίδια και δείκτες με `transform` πέρα από τα όριά του και
 * ψαλιδίζει μόνος του — αυτό είναι ο χάρτης, όχι βλάβη. ⚠️ **Ο ΙΔΙΟΣ ο χάρτης μετρά**: μόνο οι
 * απόγονοί του εξαιρούνται. ⛔ ΜΗΝ προσθέσεις εδώ γενικό επιλογέα (`header`, `main`, `.flex`):
 * αυτό θα ξαναέκρυβε ακριβώς ό,τι κρυβόταν πίσω από τον καθολικό `overflow-x: clip`.
 */
export const REFLOW_ALLOWED_SURFACES = ['.maplibregl-map'] as const;

