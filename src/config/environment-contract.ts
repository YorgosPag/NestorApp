/**
 * @fileoverview **ΤΟ ΣΥΜΒΟΛΑΙΟ ΠΕΡΙΒΑΛΛΟΝΤΟΣ** — ποια ρύθμιση λείπει, και **τι σπάει** χωρίς αυτήν.
 * @related ADR-777 §8.35 · ADR-170 (QR παρουσιών) · ADR-327 §7 (πύλη προμηθευτή)
 * @module config/environment-contract
 *
 * ────────────────────────────────────────────────────────────────────────────
 * 🔴 ΓΙΑΤΙ ΥΠΑΡΧΕΙ — Η ΑΠΟΥΣΙΑ ΑΠΑΝΤΑ **ΚΑΘΑΡΑ ΚΑΙ ΛΑΘΟΣ**
 * ────────────────────────────────────────────────────────────────────────────
 *
 * Και οι **τρεις** πύλες υπογεγραμμένου συνδέσμου (`lib/tokens/signed-token.ts`)
 * γράφουν το **ίδιο** σχήμα, μετρημένο:
 *
 * ```ts
 * try { secret = requireTokenSecret(SECRET_ENV); }
 * catch { logger.error(…); return { ok: false, reason: 'link-invalid' }; }
 * ```
 *
 * Δηλαδή: όταν λείπει η μεταβλητή, ο άνθρωπος **δεν** βλέπει βλάβη — βλέπει «**ο
 * σύνδεσμος δεν είναι έγκυρος**». Μια απάντηση καθαρή, ήρεμη, και **λάθος**. Ο μόνος
 * μάρτυρας είναι ένα `logger.error` στον διακομιστή, το οποίο **κανείς δεν διαβάζει
 * προληπτικά** — μαθαίνεις τη βλάβη από τον πελάτη που δεν μπόρεσε να απαντήσει.
 *
 * ⚠️ **ΤΟ `/api/health` ΔΕΝ ΤΟ ΠΙΑΝΕΙ, ΚΑΙ ΕΙΝΑΙ ΔΟΜΙΚΟ**: επιστρέφει `ok` σε **τρεις
 * γραμμές**, χωρίς να κοιτάξει τίποτα. Είναι σωστό ως *liveness* («ζει η διεργασία;»)
 * και **διαβάζεται** ως «όλα καλά» — το σχήμα «0 = κανείς δεν κοίταξε», σε μορφή
 * τελικού σημείου. Γι' αυτό το `/api/health` **δεν αγγίζεται**: η ετοιμότητα είναι
 * **δεύτερο ερώτημα** και παίρνει **δικό της** τελικό σημείο (`/api/health/config`).
 *
 * ────────────────────────────────────────────────────────────────────────────
 * 🔑 ΓΙΑΤΙ ΔΥΟ ΒΑΘΜΙΔΕΣ ΚΑΙ ΟΧΙ «ΟΛΑ ΥΠΟΧΡΕΩΤΙΚΑ»
 * ────────────────────────────────────────────────────────────────────────────
 *
 * Το βιομηχανικό πρότυπο (12-factor · `t3-env` · Zod schema στο `instrumentation`)
 * λέει **fail fast**: λείπει μεταβλητή ⇒ η εφαρμογή **δεν ξεκινά**. Σωστό — αλλά
 * εφαρμοσμένο **ωμά εδώ θα έριχνε την παραγωγή σήμερα**: το `MANDATE_CONSENT_SECRET`
 * λείπει **αυτή τη στιγμή** από το Netcup (μετρημένο, ADR-777 §8.33), άρα ένα καθολικό
 * `throw` θα σταματούσε το επόμενο deploy του **ολόκληρου** ιστότοπου εξαιτίας **μίας**
 * πύλης που αφορά **μία** ροή.
 *
 * *Το fail-fast είναι σωστό για ό,τι κάνει την εφαρμογή **ανίκανη να σερβίρει**· δεν
 * είναι σωστό για ό,τι κάνει **μία δυνατότητα** ανίκανη.* Γι' αυτό:
 *
 * | Βαθμίδα | Ερώτημα | Τι κάνει το boot |
 * |---|---|---|
 * | `fatal` | «Μπορεί η εφαρμογή να σερβίρει **οτιδήποτε**;» | **ΠΕΤΑ** — δεν ξεκινά |
 * | `feature` | «Πέφτει **μία** δυνατότητα, σιωπηλά;» | **ΟΝΟΜΑΖΕΙ** τη συνέπεια, δυνατά, μία φορά |
 *
 * ⚠️ **ΜΗΝ ανεβάσεις μυστικό συνδέσμου σε `fatal`** για να «είναι πιο αυστηρό»: θα
 * έκανε την αδυναμία **μίας** ροής να ρίχνει **όλες**. Η αυστηρότητα εδώ δεν είναι
 * «σκάσε πιο δυνατά» — είναι «**πες τι έσπασε πριν το μάθει ο πελάτης**».
 *
 * ⚠️ **ΜΗΝ γράψεις εδώ τιμές**, ούτε ενδεικτικές. Το αρχείο είναι **tracked**· το
 * `.env` και το `.env.local` **δεν** είναι (επαληθευμένο). Εδώ ζουν **ονόματα και
 * συνέπειες** — ποτέ μυστικά.
 *
 * ────────────────────────────────────────────────────────────────────────────
 * 🏆 ΠΟΥ ΞΕΠΕΡΝΑΜΕ ΤΟ ΠΡΟΤΥΠΟ — ΤΟ ΣΧΗΜΑ ΔΕΝ ΕΙΝΑΙ ΔΕΥΤΕΡΗ ΑΛΗΘΕΙΑ
 * ────────────────────────────────────────────────────────────────────────────
 *
 * Το `t3-env` και κάθε Zod schema περιβάλλοντος είναι **χειρόγραφη λίστα** δίπλα σε
 * κώδικα που ζητά `process.env.X`: **δύο** αλήθειες, που αποκλίνουν σιωπηλά μόλις
 * κάποιος προσθέσει τέταρτη πύλη. Αυτό το repo έχει **πληρώσει** ακριβώς αυτό το σχήμα
 * — δύο λίστες namespace που είχαν αποκλίνει κατά **63** (CHECK 3.34), ένας
 * συγκεντρωτής CI που άκουγε **18** από **26** πύλες (CHECK 3.37).
 *
 * Γι' αυτό το μητρώο **δεν μένει μόνο του**: η **CHECK 3.56** διαβάζει τους
 * πραγματικούς καταναλωτές του `requireTokenSecret` με AST και **μπλοκάρει** μυστικό
 * που ο κώδικας ζητά και το συμβόλαιο δεν δηλώνει — και το ανάποδο. Η λίστα δεν
 * *υπόσχεται* να είναι πλήρης· **αποδεικνύεται**.
 *
 * ────────────────────────────────────────────────────────────────────────────
 * 🔶 ΔΗΛΩΜΕΝΟ ΚΕΝΟ, ΜΕ ΑΡΙΘΜΟ
 * ────────────────────────────────────────────────────────────────────────────
 *
 * Ο κώδικας αναφέρει **593** διαφορετικά `process.env.*` (μετρημένο 2026-08-21 σε
 * `src/` + `instrumentation.ts` + `next.config.js`)· το `.env.example` δηλώνει **96**.
 * Το συμβόλαιο **δεν** τα κρίνει όλα, και αυτό είναι **επιλογή**: η συντριπτική
 * πλειοψηφία είναι προαιρετική εκ σχεδιασμού (σημαίες, διαδρομές δοκιμών, ρυθμίσεις με
 * προεπιλογή) και μια πύλη που τις απαιτούσε θα ήταν **μονίμως κόκκινη** ⇒ θα
 * παρακαμπτόταν ⇒ διακοσμητική (η παγίδα που το CHECK 3.39 απέρριψε γραπτά).
 *
 * Εδώ κρίνεται η **αποδεδειγμένη κλάση**: ρύθμιση της οποίας η απουσία παράγει
 * **επιτυχημένη απάντηση με λάθος περιεχόμενο**. Κάθε νέα εγγραφή πρέπει να μπορεί να
 * συμπληρώσει το πεδίο `consequence` με **τι βλέπει ο άνθρωπος** — αν δεν μπορεί,
 * μάλλον δεν ανήκει εδώ.
 *
 * **Layering**: config — καθαρά δεδομένα, μηδέν εισαγωγές, μηδέν παρενέργειες.
 */

// =============================================================================
// 1. ΤΟ ΛΕΞΙΛΟΓΙΟ
// =============================================================================

/**
 * Πόσο βαθιά πάει η βλάβη όταν λείπει η ρύθμιση.
 *
 * - `fatal` — η εφαρμογή δεν μπορεί να σερβίρει **τίποτα** σωστά. Το boot **πετά**.
 * - `feature` — **μία** δυνατότητα πέφτει, και πέφτει **σιωπηλά**. Το boot **ονομάζει**.
 */
export type EnvironmentSeverity = 'fatal' | 'feature';

/** Μία δηλωμένη ρύθμιση: τι είναι, ποιος τη χρειάζεται, τι σπάει χωρίς αυτήν. */
export interface EnvironmentRequirement {
  /** Το όνομα της μεταβλητής, ακριβώς όπως το ζητά ο κώδικας. */
  readonly name: string;
  /** Πόσο βαθιά πάει η βλάβη. */
  readonly severity: EnvironmentSeverity;
  /** Ποια δυνατότητα την κατέχει — με ανθρώπινο όνομα, όχι μονοπάτι αρχείου. */
  readonly feature: string;
  /**
   * 🔑 **ΤΙ ΒΛΕΠΕΙ Ο ΑΝΘΡΩΠΟΣ ΟΤΑΝ ΛΕΙΠΕΙ.** Το πιο σημαντικό πεδίο του μητρώου:
   * είναι η διαφορά ανάμεσα σε «λείπει μια μεταβλητή» (που δεν λέει τίποτα σε κανέναν)
   * και «**ο ιδιοκτήτης παίρνει “άκυρος σύνδεσμος” και δεν μπορεί να εγκρίνει**».
   */
  readonly consequence: string;
  /** Το αρχείο που την καταναλώνει — για να μη χρειάζεται `grep` όποιος τη διορθώνει. */
  readonly consumer: string;
}

// =============================================================================
// 2. ΤΟ ΜΗΤΡΩΟ
// =============================================================================

/**
 * **ΟΙ ΤΡΕΙΣ ΠΥΛΕΣ ΤΟΥ ΥΠΟΓΕΓΡΑΜΜΕΝΟΥ ΣΥΝΔΕΣΜΟΥ.**
 *
 * Κοινό τους: στέλνουν σύνδεσμο σε άνθρωπο **χωρίς λογαριασμό**, και όταν λείπει το
 * μυστικό απαντούν «άκυρος σύνδεσμος» αντί να αποτύχουν θορυβωδώς.
 *
 * ⚠️ Τα μυστικά είναι **ξεχωριστά επίτηδες** (δες `lib/tokens/signed-token.ts`): κοινό
 * μυστικό θα σήμαινε ότι σύνδεσμος **παρουσιών** με τα σωστά πεδία περνά για
 * **συγκατάθεση ιδιοκτήτη**. Μην τα ενοποιήσεις για «απλότητα».
 */
export const ENVIRONMENT_CONTRACT: readonly EnvironmentRequirement[] = [
  {
    name: 'MANDATE_CONSENT_SECRET',
    severity: 'feature',
    feature: 'Συγκατάθεση ιδιοκτήτη σε μεσιτική εντολή',
    consequence:
      'Ο ιδιοκτήτης ανοίγει τον σύνδεσμο που του έστειλε το γραφείο και διαβάζει ' +
      '«ο σύνδεσμος δεν είναι έγκυρος». Δεν μπορεί ούτε να εγκρίνει ούτε να αρνηθεί, ' +
      'άρα το ακίνητο δεν βγαίνει ποτέ στην αγορά και κανείς δεν μαθαίνει γιατί.',
    consumer: 'src/services/mandate/mandate-consent.service.ts',
  },
  {
    name: 'ATTENDANCE_QR_SECRET',
    severity: 'feature',
    feature: 'QR παρουσιών εργοταξίου',
    consequence:
      'Ο εργαζόμενος σκανάρει τον κωδικό στο εργοτάξιο και η παρουσία απορρίπτεται ως ' +
      'άκυρη. Η μέρα καταγράφεται ως απουσία χωρίς να το δει κανείς.',
    consumer: 'src/services/attendance/qr-token-service.ts',
  },
  {
    name: 'VENDOR_PORTAL_SECRET',
    severity: 'feature',
    feature: 'Πύλη προμηθευτή',
    consequence:
      'Ο προμηθευτής ανοίγει τον σύνδεσμο της προσφοράς και βλέπει «άκυρος σύνδεσμος». ' +
      'Η προσφορά δεν κατατίθεται ποτέ και το γραφείο νομίζει ότι δεν απάντησε.',
    consumer: 'src/services/vendor-portal/vendor-portal-token-service.ts',
  },
] as const;
