/**
 * @fileoverview **Η ΡΥΘΜΙΣΗ ΤΗΣ FIREBASE AUTH, ΩΣ ΚΩΔΙΚΑΣ** (ADR-851).
 * @module config/firebase-auth-config
 *
 * ────────────────────────────────────────────────────────────────────────────
 * 🔴 ΓΙΑΤΙ ΥΠΑΡΧΕΙ — ΜΕΤΡΗΜΕΝΟ 2026-09-11, ΟΧΙ ΥΠΟΘΕΣΗ
 * ────────────────────────────────────────────────────────────────────────────
 *
 * Η ρύθμιση ζούσε **μόνο στην κονσόλα**, και ο κώδικας δεν την ήξερε. Μια ανάγνωση
 * (`projects.getConfig`) βρήκε:
 * - `callbackUri` = `https://nestor-pagonis.vercel.app/auth/action` → **404
 *   `DEPLOYMENT_NOT_FOUND`**: **κάθε** σύνδεσμος email της Firebase (επαναφορά κωδικού,
 *   επιβεβαίωση, αλλαγή email, ανάκτηση) έδειχνε σε **ελεύθερο** υποdomain τρίτου —
 *   όποιος το διεκδικούσε στο Vercel θα παραλάμβανε **oobCodes**, δηλαδή λογαριασμούς.
 * - `defaultLocale: "en"` ενώ η εφαρμογή είναι ελληνική.
 * - τρία domains του Vercel στα εξουσιοδοτημένα (δύο νεκρά), μια διεύθυνση τοπικού
 *   δικτύου και ένα sslip.io χωρίς καμία χρήση.
 *
 * ⇒ **Η ρύθμιση που δεν είναι στο git δεν είναι ρύθμιση — είναι κατάσταση.** Ίδια αρχή με
 * το `infrastructure/firebase-storage/cors.json` (ADR-351).
 *
 * ⚠️ **ΓΙΑΤΙ ΕΔΩ ΚΑΙ ΟΧΙ ΣΤΟ `infrastructure/`**: ο έλεγχος απόκλισης τρέχει **και** μέσα
 * στον διακομιστή (cron, ADR-740), και το standalone build του Next παίρνει μόνο ό,τι
 * είναι στον γράφο εισαγωγών. Ένα JSON έξω από το `src/` θα έλειπε από το container.
 *
 * 🔑 **ΤΙ ΔΕΝ ΓΡΑΦΕΤΑΙ ΕΔΩ, ΕΠΙΤΗΔΕΣ**: η δημόσια διεύθυνση (`callbackUri`, το domain της
 * εφαρμογής) και το project id **παράγονται** — από το `publicOrigin()` και από το
 * διαπιστευτήριο Admin. Ένα δεύτερο γραμμένο αντίγραφο θα ήταν ακριβώς η απόκλιση που
 * γέννησε αυτό το αρχείο.
 */

/**
 * Τα εξουσιοδοτημένα domains **πέρα** από τα παραγόμενα (τα δύο της Firebase + το
 * domain του `publicOrigin()`).
 *
 * 🔒 **Ελάχιστο προνόμιο**: κάθε domain εδώ μπορεί να δεχτεί ανακατεύθυνση OAuth και
 * σύνδεσμο συνέχειας. Το `localhost` μένει για την ανάπτυξη· **τίποτα άλλο**.
 */
export const FIREBASE_AUTH_EXTRA_AUTHORIZED_DOMAINS: readonly string[] = ['localhost'];

/** Η γλώσσα των μηνυμάτων της Firebase όταν ο πελάτης δεν δηλώσει `languageCode`. */
export const FIREBASE_AUTH_DEFAULT_LOCALE = 'el';

/**
 * Προστασία απαρίθμησης λογαριασμών (OWASP) — ενεργή ήδη στην παραγωγή (μετρημένο
 * 2026-09-11)· δηλώνεται ώστε η **απενεργοποίησή** της να είναι απόκλιση που φαίνεται.
 */
export const FIREBASE_AUTH_EMAIL_ENUMERATION_PROTECTION = true;

/**
 * Ο αποστολέας των μηνυμάτων της Firebase — **οι σημερινές τιμές, μετρημένες**.
 *
 * ⚠️ Δηλώνονται επειδή η ενημέρωση προτύπου αντικαθιστά **ολόκληρο** το αντικείμενο: αν
 * έλειπαν από εδώ, το πρώτο `--apply` θα τα **έσβηνε**.
 */
export const FIREBASE_AUTH_SENDER = {
  senderDisplayName: 'Nestor Pagonis',
  senderLocalPart: 'noreply',
  replyTo: 'noreply',
} as const;
