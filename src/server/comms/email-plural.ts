/**
 * =============================================================================
 * «ΕΝΑΣ Ή ΠΟΛΛΟΙ;» ΣΤΟΝ ΔΙΑΚΟΜΙΣΤΗ — ΜΕ ΤΟΝ ΙΔΙΟ ΚΡΙΤΗ ΜΕ ΤΗΝ ΟΘΟΝΗ (ADR-777 §8.54)
 * =============================================================================
 *
 * **Καθαρή συνάρτηση. Καμία Firestore, κανένα δίκτυο, κανένα ρολόι, κανένα i18next.**
 *
 * ────────────────────────────────────────────────────────────────────────────
 * 🔴 ΤΟ ΠΕΡΙΣΤΑΤΙΚΟ: «1 ΑΤΟΜΑ ΨΑΧΝΟΥΝ ΑΚΙΝΗΤΟ ΣΑΝ ΤΟ ΔΙΚΟ ΣΑΣ»
 * ────────────────────────────────────────────────────────────────────────────
 *
 * Γραμμένο στα εισερχόμενα του ανθρώπου, 2026-09-05 20:00, αυτολεξεί. Την ίδια
 * στιγμή, **η οθόνη έλεγε το σωστό** για το ίδιο ακριβώς γεγονός:
 *
 * | πού | τι λέει |
 * |---|---|
 * | οθόνη — `common-shared.json` | `{count, plural, one {# άνθρωπος ψάχνει} other {# άνθρωποι ψάχνουν}}` ✅ |
 * | email — `interest-notifier.service.ts` | `` `${count} άτομα ψάχνουν` `` ❌ |
 *
 * Δύο λάθη σε μία γραμμή: ο **πληθυντικός** και η **λέξη** («άτομα» ≠ «άνθρωποι»).
 *
 * 🔴 **Και δεν είναι ακραία περίπτωση — είναι η ΠΙΟ ΣΥΧΝΗ.** Το
 * `ANNOUNCEMENT_BANDS` ξεκινά από **`1`**: η πρώτη ειδοποίηση που λαμβάνει κάθε
 * ιδιοκτήτης στη ζωή του λέει «1 άτομα». Δεν χρειάζεται ασυνήθιστο απόθεμα ούτε
 * ακραίο φορτίο· χρειάζεται **έναν** ενδιαφερόμενο.
 *
 * ────────────────────────────────────────────────────────────────────────────
 * 🔑 ΓΙΑΤΙ ΟΧΙ `count === 1 ? … : …` — Η ΠΡΟΦΑΝΗΣ ΔΙΟΡΘΩΣΗ ΕΙΝΑΙ ΤΟ ΙΔΙΟ ΣΧΗΜΑ
 * ────────────────────────────────────────────────────────────────────────────
 *
 * Ένας τριαδικός θα έσβηνε **αυτό** το σφάλμα και θα άφηνε **την κλάση** ζωντανή:
 * θα ήταν πάλι χειρόγραφος πληθυντικός, σωστός για τα ελληνικά και τα αγγλικά και
 * **σιωπηλά λάθος** για κάθε γλώσσα με `few`/`many` (πολωνικά, ρωσικά, αραβικά,
 * τσεχικά). Μετρημένο: `new Intl.PluralRules('pl').resolvedOptions()` δίνει
 * **τέσσερις** κατηγορίες — `['few','many','one','other']`.
 *
 * Η οθόνη **δεν** ρωτά τριαδικό: ρωτά **CLDR**, μέσω `i18next-icu`. Ο διακομιστής δεν
 * μπορεί να φορτώσει i18next (δες `email-texts.ts`: καθολική κατάσταση σε διεργασία
 * που εξυπηρετεί πολλούς παραλήπτες = συνθήκη αγώνα «λάθος γλώσσα σε λάθος άνθρωπο»).
 * Αλλά **δεν χρειάζεται**: το CLDR υπάρχει ήδη μέσα στο ECMAScript, ως
 * `Intl.PluralRules`.
 *
 * 🏆 **Αυτό είναι το κερδισμένο, και είναι πάνω από τη συνήθη πρακτική.** Ο τυπικός
 * τρόπος των μεγάλων είναι δύο ανεξάρτητοι αγωγοί μεταφράσεων — ένας για την οθόνη
 * (ICU) και ένας για τα email (πρότυπα προμηθευτή, Braze/Iterable/SendGrid), που
 * **αποκλίνουν**. Εμείς έχουμε **έναν κριτή** (CLDR) και **ένα λεξιλόγιο**: οι μορφές
 * γράφονται με το **ίδιο σύμβολο `#`** που χρησιμοποιεί το ICU στα locale JSON, ώστε
 * η γραμμή του email και η γραμμή της οθόνης να συγκρίνονται **με το μάτι** — και
 * από άγκυρα.
 *
 * ────────────────────────────────────────────────────────────────────────────
 * ⚠️ Η ΔΗΛΩΣΗ ΤΩΝ ΚΑΤΗΓΟΡΙΩΝ ΕΛΕΓΧΕΤΑΙ ΑΠΟ ΤΟ ΙΔΙΟ ΤΟ CLDR, ΟΧΙ ΑΠΟ ΑΝΘΡΩΠΟ
 * ────────────────────────────────────────────────────────────────────────────
 *
 * Το {@link PLURAL_CATEGORIES_IN_USE} είναι **χειρόγραφο** — και ακριβώς γι' αυτό
 * υπάρχει το {@link declaredCategoriesMatchCldr}, που το συγκρίνει με το
 * `resolvedOptions().pluralCategories` της μηχανής. Μια τρίτη γλώσσα με `few`
 * **δεν μπορεί** να μπει σιωπηλά: η άγκυρα κοκκινίζει, και ο τύπος
 * {@link PluralForms} απαιτεί από κάθε καλούντα να γράψει τη μορφή που λείπει.
 *
 * 🔑 Αυτό είναι το ιδίωμα «Record δεμένο στη ρίζα του λεξιλογίου» (CHECK 3.73), με
 * τη ρίζα να **μην είναι δική μας**: είναι το Unicode CLDR.
 *
 * @module server/comms/email-plural
 * @see ADR-777 §8.54
 * @see server/comms/email-texts — τα λόγια του **περιτυλίγματος**· εδώ είναι η **μορφή**
 */

import { HUMAN_LANGUAGES, type HumanLanguage } from '@/i18n/languages';

/**
 * Οι κατηγορίες πληθυντικού του CLDR — **όλες όσες υπάρχουν**, σε καμία γλώσσα όλες.
 *
 * ⚠️ Δηλώνεται τοπικά αντί να εισαχθεί ως `Intl.LDMLPluralRule`: ο τύπος εκείνος
 * εξαρτάται από το ποιο `lib` του TypeScript είναι ενεργό, και ο κανόνας N.17
 * απαγορεύει στον πράκτορα να τρέξει `tsc` για να το επιβεβαιώσει. Ένα σύνολο έξι
 * σταθερών δεν αξίζει εξάρτηση που δεν μπορούμε να ελέγξουμε.
 */
export const PLURAL_CATEGORIES = ['zero', 'one', 'two', 'few', 'many', 'other'] as const;

/** Μία κατηγορία πληθυντικού του CLDR. */
export type PluralCategory = (typeof PLURAL_CATEGORIES)[number];

/**
 * **Ποιες κατηγορίες χρησιμοποιεί ΟΝΤΩΣ κάθε γλώσσα μας.**
 *
 * Τα ελληνικά και τα αγγλικά έχουν **δύο** (`one`/`other`) — μετρημένο, όχι
 * υποτιθέμενο: `new Intl.PluralRules('el').resolvedOptions().pluralCategories`.
 *
 * ⚠️ **`Record<HumanLanguage, …>`, ΠΟΤΕ `Partial`** — ίδιος φρουρός με το
 * `EMAIL_TEXTS`: μια τρίτη γλώσσα δεν μπορεί να μπει και να αφήσει τη στήλη της κενή.
 */
export const PLURAL_CATEGORIES_IN_USE = {
  el: ['one', 'other'],
  en: ['one', 'other'],
} as const satisfies Readonly<Record<HumanLanguage, readonly PluralCategory[]>>;

/**
 * **Οι μορφές που ΟΦΕΙΛΕΙ να δώσει ο καλών, για ΑΥΤΗ τη γλώσσα.**
 *
 * 🔑 Παράγεται από το {@link PLURAL_CATEGORIES_IN_USE}, ποτέ ξαναγραμμένο. Όταν μια
 * γλώσσα αποκτήσει `few`, ο μεταγλωττιστής ζητά τη μορφή `few` από **κάθε** κλήση —
 * αντί να την αφήσει να πέσει σιωπηλά στο `other`, που είναι ακριβώς το «1 άτομα»
 * ξανά, σε άλλη γλώσσα.
 */
export type PluralForms<L extends HumanLanguage> = Readonly<
  Record<(typeof PLURAL_CATEGORIES_IN_USE)[L][number], string>
>;

/**
 * **Η μορφή για αυτό το πλήθος, σε αυτή τη γλώσσα.**
 *
 * Το `#` αντικαθίσταται από τον αριθμό, **μορφοποιημένο κατά τη γλώσσα** — ίδια
 * σημασιολογία με το `#` του ICU στα locale JSON της οθόνης. Στα ελληνικά αυτό
 * σημαίνει ότι το `1000` γράφεται «1.000», όπως ήδη το γράφει η οθόνη· η παλιά
 * παρεμβολή `${count}` έγραφε «1000», δηλαδή **η ίδια πληροφορία με δύο όψεις**.
 *
 * ⚠️ **Καμία μνήμη, κανένα στιγμιότυπο σε module scope.** Τα `Intl.*` αντικείμενα
 * φτιάχνονται ανά κλήση: ένα κοινό στιγμιότυπο θα ήταν πάλι καθολική κατάσταση σε
 * διεργασία που εξυπηρετεί πολλούς παραλήπτες — το ίδιο σχήμα που απορρίπτει το
 * `email-texts.ts` για το i18next.
 */
export function pluralize<L extends HumanLanguage>(
  language: L,
  count: number,
  forms: PluralForms<L>,
): string {
  const category = new Intl.PluralRules(language).select(count) as PluralCategory;
  const formsByCategory = forms as Readonly<Partial<Record<PluralCategory, string>>>;

  // ⚠️ **Η εφεδρεία στο `other` είναι ΑΠΡΟΣΙΤΗ όσο η άγκυρα είναι πράσινη**, και
  //    μένει επίτηδες: μια μηχανή ICU σε άλλη έκδοση Node θα μπορούσε να επιστρέψει
  //    κατηγορία που η δήλωσή μας δεν ξέρει. Σε αυτή τη σπάνια περίπτωση ο άνθρωπος
  //    παίρνει τον πληθυντικό — γραμματικά ατελές, αλλά **διαβάσιμο**. Ένα `throw`
  //    εδώ θα σήμαινε **κανένα email**, δηλαδή χειρότερη βλάβη από τη βλάβη.
  const template = formsByCategory[category] ?? formsByCategory.other ?? '';

  return template.replaceAll('#', new Intl.NumberFormat(language).format(count));
}

/**
 * **Συμφωνεί η δήλωσή μας με το ίδιο το CLDR;**
 *
 * Υπάρχει **για να αποτύχει θορυβωδώς** σε άγκυρα. Ο τύπος `Record` εγγυάται ότι
 * **υπάρχει** στήλη για κάθε γλώσσα· **δεν** εγγυάται ότι η στήλη λέει την αλήθεια.
 * Μια χειρόγραφη λίστα που κανείς δεν συγκρίνει με την πηγή είναι το σχήμα που στο
 * ίδιο repo έχει ήδη αποκλίνει κατά **63** namespaces (CHECK 3.34).
 */
export function declaredCategoriesMatchCldr(): boolean {
  return HUMAN_LANGUAGES.every((language) => {
    const declared = [...PLURAL_CATEGORIES_IN_USE[language]].sort();
    const actual = [...new Intl.PluralRules(language).resolvedOptions().pluralCategories].sort();
    return declared.length === actual.length && declared.every((c, i) => c === actual[i]);
  });
}
