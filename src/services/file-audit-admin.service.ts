/**
 * =============================================================================
 * 📜 ΤΟ ΙΧΝΟΣ ΑΡΧΕΙΟΥ ΑΠΟ ΤΟΝ **ΔΙΑΚΟΜΙΣΤΗ** (Admin SDK) — ADR-191 · ADR-862 Φ0 Β6
 * =============================================================================
 *
 * **Το ερώτημα**: *«Πώς γράφει ο **διακομιστής** γραμμή στο `file_audit_log`;»*
 * **Ο απαντητής**: αυτό το αρχείο. Κανένα άλλο.
 *
 * ⚠️ **ΔΕΝ αντικαθιστά το `file-audit.service.ts`** — είναι ο **αδελφός** του, ακριβώς
 * όπως το `entity-audit.service.ts` (Admin) έχει αδελφό το `entity-audit-client.service.ts`.
 * Ο πελάτης γράφει και **συνδρομεί**· ο διακομιστής γράφει με **Admin SDK**. Ίδια
 * συλλογή, ίδιο σχήμα, **ένα** λεξιλόγιο (`types/file-audit`).
 *
 * ─────────────────────────────────────────────────────────────────────────────
 * 🔴 ΓΙΑΤΙ ΧΡΕΙΑΣΤΗΚΕ — Ο ΓΡΑΦΕΑΣ ΥΠΗΡΧΕ ΗΔΗ, ΣΚΟΡΠΙΣΜΕΝΟΣ ΣΕ ΠΕΝΤΕ ΣΗΜΕΙΑ
 * ─────────────────────────────────────────────────────────────────────────────
 * Μετρημένο 2026-09-16: ο `FileAuditService.log` εισάγει **client SDK**, οπότε καμία
 * `server-only` διαδρομή δεν μπορούσε να τον καλέσει. Το αποτέλεσμα **δεν** ήταν «δεν
 * γράφτηκε ίχνος» — ήταν **πέντε χειρόγραφα δίδυμα**:
 *
 *   `api/files/archive/route.ts:108` · `api/files/classify/classify-background.ts:150`
 *   `api/files/gdpr-delete/route.ts:150` · `api/files/purge/route.ts:115`
 *   `services/file-record/file-purge-helpers.ts:109`
 *
 * 🔴 **ΚΑΙ ΑΠΕΚΛΙΝΑΝ ΜΕ ΣΥΝΕΠΕΙΑ**: όλα γράφουν `timestamp: nowISO()` (**συμβολοσειρά**)
 * ενώ η υπηρεσία του πελάτη γράφει `serverTimestamp()` (**Timestamp**) — ίδιο πεδίο,
 * δύο τύποι. Και τα `archive`/`purge` **παραλείπουν το `companyId`**, που είναι το
 * **μόνο** κλειδί με το οποίο ο μοναδικός αναγνώστης ρωτά ⇒ εκείνες οι γραμμές είναι
 * **δομικά αόρατες σε κάθε άνθρωπο, για πάντα**.
 *
 * ⚠️ **Η μετανάστευση των πέντε ΔΕΝ γίνεται εδώ** (N.0.2: 5 αρχεία, >1h ⇒ γράφτηκε στο
 * `.claude-rules/pending-ratchet-work.md`). Εδώ γεννιέται ο **προορισμός** τους.
 *
 * ─────────────────────────────────────────────────────────────────────────────
 * 🔑 ΠΟΤΕ ΜΕΣΑ ΣΕ ΣΥΝΑΛΛΑΓΗ, ΚΑΙ ΠΟΤΕ ΜΠΛΟΚΑΡΟΝ — ΤΟ ΔΟΓΜΑ ΤΟΥ ADR-862 §5.7
 * ─────────────────────────────────────────────────────────────────────────────
 * Το δόγμα (`recordMembershipGrantAudit`) το λέει αυτολεξεί: *«η ιδιότητα μέλους **έχει
 * ήδη** γραφτεί· ένα ίχνος που αποτυγχάνει δεν επιτρέπεται να ακυρώσει ένταξη που
 * έγινε»*. Ισχύει **ακέραιο** εδώ, και για δύο ανεξάρτητους λόγους:
 *
 *   1. **Σημασιολογικός** — η γραμμή ημερολογίου είναι **προβολή** μιας πράξης που
 *      δεσμεύτηκε ήδη. Αποτυχία της δεν επιτρέπεται να ξηλώσει τη σφραγίδα.
 *   2. **Μηχανικός** — το σώμα μιας συναλλαγής Firestore **ξαναεκτελείται** σε
 *      σύγκρουση. Παρενέργεια μέσα του φεύγει **πολλές φορές**.
 *
 * ⚠️ **ΚΑΙ ΔΕΝ ΣΥΓΚΡΟΥΕΤΑΙ ΜΕ ΤΗΝ ΑΠΑΙΤΗΣΗ ΑΤΟΜΙΚΟΤΗΤΑΣ ΤΟΥ Β6** — επειδή το
 * **αυθεντικό** ίχνος μιας πράξης CDE **δεν είναι αυτή η γραμμή**: είναι το
 * `cdeSeal: { by, at, revision }` **πάνω στο ίδιο το έγγραφο**, γραμμένο στην **ΙΔΙΑ**
 * `update()` με την προβολή και **αμετάβλητο** από τους κανόνες (Φ0 Β4). Το πρότυπο
 * ζητά *«ποιος · πότε · ποια αναθεώρηση»* (ADR-862 §5.4.1.γ) — και αυτό **είναι** εκεί,
 * ατομικά. Αυτή η συλλογή είναι το **ημερολόγιο δραστηριότητας**, όχι το νομικό ίχνος.
 *
 * @module services/file-audit-admin.service
 * @enterprise ADR-191 §3.1 · ADR-862 Φ0 Β6
 * @compliance ISO 27001 §A.12.4
 * @see types/file-audit           — το λεξιλόγιο, κοινό και για τους δύο γραφείς
 * @see services/file-audit.service — ο αδελφός του **πελάτη** (γράφει + διαβάζει)
 * @see services/entity-audit.service — το ίδιο σχήμα αδελφών, για το άλλο ημερολόγιο
 */

import 'server-only';

import { getAdminFirestore, FieldValue } from '@/lib/firebaseAdmin';
import { COLLECTIONS } from '@/config/firestore-collections';
import { generateAuditId } from '@/services/enterprise-id.service';
import { createModuleLogger } from '@/lib/telemetry';
import { getErrorMessage } from '@/lib/error-utils';
import type { FileAuditRecordFields } from '@/types/file-audit';

const logger = createModuleLogger('FileAuditAdminService');

// =============================================================================
// Η ΕΙΣΟΔΟΣ — ΤΟ `companyId` ΕΙΝΑΙ ΥΠΟΧΡΕΩΤΙΚΟ, ΚΑΙ ΤΟ ΕΠΙΒΑΛΛΕΙ Ο ΜΕΤΑΓΛΩΤΤΙΣΤΗΣ
// =============================================================================

/**
 * Ό,τι χρειάζεται μια γραμμή ίχνους γραμμένη από τον διακομιστή.
 *
 * 🔴 **ΓΙΑΤΙ ΤΟ `companyId` ΓΙΝΕΤΑΙ ΥΠΟΧΡΕΩΤΙΚΟ ΕΔΩ** ενώ είναι προαιρετικό στο κοινό
 * σχήμα: ο πελάτης **μπορεί** να το επιλύσει μόνος του διαβάζοντας το `FileRecord`
 * (`file-audit.service.ts:144-153`). Ο διακομιστής **ξέρει ήδη** τον μισθωτή — τον
 * κουβαλά το `AuthContext` — άρα μια δεύτερη ανάγνωση θα ήταν σπατάλη, και η
 * **παράλειψη** θα ήταν εγγραφή που κανείς δεν μπορεί να διαβάσει ποτέ.
 *
 * ⚠️ Ο τύπος εγγυάται **σχήμα**, όχι **τιμή**: ένα `companyId: ''` μεταγλωττίζεται.
 * Γι' αυτό ο γραφέας ρωτά **και** σε χρόνο εκτέλεσης — ίδιο ακριβώς σκεπτικό με το
 * `isWritableAuditLedgerScope` του `EntityAuditService` (ADR-864 Φ1β).
 */
export type ServerFileAuditInput = FileAuditRecordFields & {
  readonly companyId: string;
};

// =============================================================================
// Η ΕΓΓΡΑΦΗ
// =============================================================================

/**
 * Το έγγραφο **όπως γράφεται**.
 *
 * ⚠️ **Conditional spread, ποτέ καθαριστής `undefined`.** Το Firestore απορρίπτει
 * `undefined`· ο αδελφός του πελάτη το λύνει με βρόχο καθαρισμού **μετά** την
 * κατασκευή. Εδώ η απουσία είναι **ιδιότητα της κατασκευής** — δεν υπάρχει στιγμή
 * όπου το αντικείμενο να φέρει `undefined` και να βασίζεται σε κάποιον να το σβήσει.
 */
function auditDocument(input: ServerFileAuditInput): Record<string, unknown> {
  return {
    fileId: input.fileId,
    action: input.action,
    performedBy: input.performedBy,
    companyId: input.companyId,
    // 🔑 **Χρόνος ΔΙΑΚΟΜΙΣΤΗ, ποτέ `nowISO()`** — αυτή ακριβώς είναι η απόκλιση των
    //    πέντε διδύμων (συμβολοσειρά vs `Timestamp` στο **ίδιο πεδίο**). Ο αδελφός
    //    γράφει `serverTimestamp()`· το ισοδύναμο του Admin SDK είναι αυτό.
    timestamp: FieldValue.serverTimestamp(),
    ...(input.metadata === undefined ? {} : { metadata: input.metadata }),
  };
}

// =============================================================================
// Ο ΓΡΑΦΕΑΣ
// =============================================================================

/**
 * **Γράψε μια γραμμή στο ημερολόγιο αρχείων.** Ποτέ δεν πετά.
 *
 * ⚠️ **ΣΥΝΑΡΤΗΣΗ, ΟΧΙ ΣΤΑΤΙΚΗ ΚΛΑΣΗ.** Ο αδελφός (`FileAuditService`) είναι κλάση
 * επειδή έχει **τέσσερα** μέλη (γραφή, δέσμη, δύο αναγνώσεις). Εδώ υπάρχει **ένα**
 * ερώτημα και **μία** απάντηση· μια κλάση με ένα `static` μέλος είναι namespace
 * μεταμφιεσμένο σε τύπο.
 *
 * @returns Το id της εγγραφής, ή `null` όταν **δεν** γράφτηκε (αρνήθηκε ή απέτυχε).
 *          ⚠️ Το `null` είναι **απάντηση**, όχι σφάλμα: ο καλών το τυλίγει σε
 *          `safeFireAndForget` και συνεχίζει — η πράξη έχει ήδη δεσμευτεί.
 */
export async function recordFileAudit(
  input: ServerFileAuditInput,
): Promise<string | null> {
  // 🔴 Εγγραφή χωρίς μισθωτή **κανείς δεν μπορεί να τη διαβάσει** (ο μοναδικός
  //    αναγνώστης ρωτά `where('companyId','==',…)`). Αρνούμαστε **πριν** αγγίξουμε
  //    τη βάση — μια αόρατη γραμμή κοστίζει και δεν προσφέρει τίποτα.
  if (input.companyId.trim().length === 0) {
    logger.error('Γραμμή ίχνους χωρίς μισθωτή — απορρίφθηκε', {
      fileId: input.fileId,
      action: input.action,
    });
    return null;
  }

  try {
    const auditId = generateAuditId();
    await getAdminFirestore()
      .collection(COLLECTIONS.FILE_AUDIT_LOG)
      .doc(auditId)
      .set(auditDocument(input));

    return auditId;
  } catch (error: unknown) {
    // Η αποτυχία του ίχνους **δεν** ακυρώνει πράξη που έγινε (ADR-862 §5.7).
    logger.error('Η γραμμή ίχνους ΔΕΝ γράφτηκε', {
      fileId: input.fileId,
      action: input.action,
      error: getErrorMessage(error),
    });
    return null;
  }
}
