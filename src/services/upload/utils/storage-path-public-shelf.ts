/**
 * @fileoverview **ΤΟ ΚΛΕΙΔΙ ΤΟΥ ΔΗΜΟΣΙΟΥ ΡΑΦΙΟΥ** — καθαρή αριθμητική διαδρομών (ADR-841 §7 Α12).
 * @related ADR-841 §7 Α12 · config/gcs-buckets · services/listings/public-shelf.service
 * @module services/upload/utils/storage-path-public-shelf
 *
 * ────────────────────────────────────────────────────────────────────────────
 * 🔑 ΤΟ ΚΛΕΙΔΙ ΕΙΝΑΙ ΤΟ **ΠΕΡΙΕΧΟΜΕΝΟ**, ΚΑΙ ΑΥΤΟ ΔΙΝΕΙ ΤΕΣΣΕΡΑ ΔΩΡΕΑΝ
 * ────────────────────────────────────────────────────────────────────────────
 *
 *   {ρίζα του είδους}/{subjectId}/{sha256 των ΚΑΘΑΡΙΣΜΕΝΩΝ bytes}.{ext}
 *
 * 1. **Καμία ακύρωση cache για ΑΛΛΑΓΗ** — άλλα bytes ⇒ άλλο κλειδί ⇒ άλλο URL, οπότε
 *    το `immutable` δεν λέει ποτέ ψέματα. ⚠️ Για **ΔΙΑΓΡΑΦΗ** δεν αρκεί: η ύπαρξη δεν
 *    είναι περιεχόμενο — δες {@link PUBLIC_SHELF_CACHE_CONTROL}.
 * 2. **Ιδεμπόταντ ανά byte** — ξαναδημοσίευση ίδιων bytes ⇒ ίδιο κλειδί ⇒ **καμία
 *    εγγραφή** (N.7.2 #3).
 * 3. **Αυτο-επαληθεύσιμο** — το URL **ΕΙΝΑΙ** η απόδειξη ακεραιότητας: κατέβασε,
 *    χάσαρε, σύγκρινε με το όνομα.
 * 4. 🔴 **Ο καθαρισμός EXIF/GPS γίνεται ΔΟΜΙΚΑ ΑΝΑΠΟΦΕΥΚΤΟΣ**: το hash είναι των
 *    **καθαρισμένων** bytes ⇒ *χωρίς καθαρισμό δεν υπάρχει διεύθυνση, χωρίς διεύθυνση
 *    δεν υπάρχει δημοσίευση*. Οι μεγάλες πλατφόρμες το κάνουν **βήμα αγωγού** — δηλαδή
 *    βήμα που μπορεί να ξεχαστεί· εδώ δεν υπάρχει τι να ξεχαστεί.
 *
 * ────────────────────────────────────────────────────────────────────────────
 * ⚠️ ΓΙΑΤΙ ΤΟ `{subjectId}` ΜΠΑΙΝΕΙ ΠΡΙΝ ΤΟ HASH — και όχι κοινή ρίζα
 * ────────────────────────────────────────────────────────────────────────────
 *
 * Καθαρά content-addressed ράφι σε **κοινή** ρίζα θα γεννούσε **μέτρηση αναφορών**:
 * δύο αγγελίες με την ίδια φωτογραφία, η μία αποσύρεται, και η διαγραφή σπάει την
 * άλλη. Το πρόθεμα κάνει τη διαγραφή **τοπική και ασφαλή** — το `{subjectId}` είναι το
 * όριο, όπως το `{userId}` είναι το όριο στο `owner_properties/`. Τίμημα: λίγα διπλά
 * bytes σε σπάνια περίπτωση. **Μετρήσιμο και μικρό· η μέτρηση αναφορών είναι κλάση
 * σφαλμάτων.**
 *
 * ────────────────────────────────────────────────────────────────────────────
 * 🏆 ΔΥΟ ΕΙΔΗ ΠΡΑΓΜΑΤΩΝ, **ΕΝΑ** ΡΑΦΙ — ΚΑΙ ΟΙ ΦΡΟΥΡΟΙ ΤΟΥΣ ΕΙΝΑΙ ΑΝΤΙΘΕΤΟΙ
 * ────────────────────────────────────────────────────────────────────────────
 *
 * Το **Στάδιο 2** της Α21 έδωσε στο ράφι **δεύτερο** είδος: το σήμα του επαγγελματία.
 * Η προφανής κίνηση ήταν δεύτερο ράφι· ⛔ **απορρίφθηκε** — θα ήταν **δεύτερος γραφέας
 * για την ίδια ερώτηση**, ελεύθερος να αποκλίνει στη συμφιλίωση, στη διαγραφή ή στον
 * καθαρισμό. Κατά γράμμα το σχήμα που τιμωρεί το **ADR-749**.
 *
 * ⇒ Το ράφι έγινε **πίνακας ειδών** ({@link PublicShelfKind}) — **ακριβώς** η κίνηση που
 * έκανε το **CHECK 3.74** την ίδια εβδομάδα *(`BOUNDARIES`: «νέα συλλογή ⇒ **μία γραμμή**
 * και τίποτα άλλο»)*. Το ράφι **κρατά την ταυτότητά του**· αυτό που μεγάλωσε είναι η
 * εμβέλεια.
 *
 * 🔑 **ΑΥΤΟ ΤΟ ΑΡΧΕΙΟ ΔΕΝ ΞΕΡΕΙ ΠΟΣΑ ΕΙΔΗ ΥΠΑΡΧΟΥΝ, ΚΑΙ ΕΙΝΑΙ ΤΟ ΝΟΗΜΑ ΤΟΥ.** Δέχεται
 * περιγραφέα και εκτελεί την **ίδια** αριθμητική για καθέναν — οπότε νέο είδος **δεν
 * αγγίζει καμία γραμμή εδώ**. Ο πίνακας, οι δύο **αντίθετοι** φρουροί ταυτότητας και το
 * «γιατί» τους ζουν στο {@link module:services/upload/utils/public-shelf-kinds}.
 *
 * ⛔ **ΚΑΘΑΡΟ MODULE — καμία I/O, κανένα `crypto`.** Το hashing ζει στον γραφέα
 * ({@link module:services/listings/public-shelf.service}) επειδή αυτό το αρχείο ανήκει
 * στο ίδιο δέντρο με builders που φορτώνονται και στον **πελάτη**· ένα `import
 * 'crypto'` εδώ θα τα έσπαγε. Εδώ ζει μόνο **αριθμητική συμβολοσειρών**.
 */

import type { ListingMaterial } from '@/lib/listings/listing-material';
import type { PhotoFocalPoint } from '@/lib/listings/photo-focal-point';
import type { AnyPublicShelfKind } from './public-shelf-kinds';
import type {
  ModelShelfEncoding,
  RasterShelfEncoding,
  ShelfEncoding,
} from './public-shelf-encoding';

// ---------------------------------------------------------------------------
// Σταθερές
// ---------------------------------------------------------------------------

/**
 * **Πόσες φωτογραφίες φεύγουν το πολύ** (ADR-841 §7 Α2.1 · Α14.4 · Ο-17).
 *
 * Μετρημένο, όχι στρογγυλό: η έρευνα της **Zillow** δίνει **22–27** ως ιδανικό εύρος,
 * με πτώση πάνω από **28** *(«οι σημαντικές θάβονται»)*· η **Rightmove** δουλεύει σε
 * **10–20**. Το **24** είναι μέσα στο πρώτο και πάνω από το δεύτερο.
 *
 * ⚠️ **Δεν είναι όριο ασφαλείας** — είναι όριο **προσοχής του επισκέπτη**. Το όριο
 * ασφαλείας είναι η ίδια η **επιλογή** (Α2.7): χωρίς αυτήν, δεν φεύγει **τίποτα**.
 *
 * ────────────────────────────────────────────────────────────────────────────
 * 🔑 ΓΙΑΤΙ ΖΕΙ ΕΔΩ ΚΑΙ ΟΧΙ ΔΙΠΛΑ ΣΤΟΝ ΕΝΑΝ ΑΠΟ ΤΟΥΣ ΔΥΟ *(Ο-17, μετακινήθηκε 2026-09-03)*
 * ────────────────────────────────────────────────────────────────────────────
 *
 * Μέχρι την **Α14** ο μόνος που δημοσίευε συλλογή ήταν ο **ιδιώτης**, οπότε η σταθερά
 * κατοικούσε στο `lib/owner-property/owner-media-publication.ts` — και ήταν **σωστό**. Η
 * Α14 έδωσε συλλογή **και στο γραφείο** ⇒ η σταθερά έγινε **του ραφιού**, ενώ το σπίτι
 * της εξακολουθούσε να έχει **όνομα ιδιώτη**: ο γραφέας του επαγγελματία εισήγαγε από
 * `@/lib/owner-property/…` — πρόταση που **δεν θα υπερασπιζόταν κανείς** αν γραφόταν ρητά.
 *
 * 🔑 **Ο ΑΡΙΘΜΟΣ ΕΙΝΑΙ ΕΝΑΣ, ΚΑΙ Η ΕΞΑΡΤΗΣΗ ΡΗΤΗ — ΠΟΤΕ ΑΝΤΙΓΡΑΜΜΕΝΗ ΤΙΜΗ.** Δύο
 * αριθμοί θα σήμαιναν ότι η **ίδια** βιτρίνα κουράζει διαφορετικά ανάλογα με το ποιος
 * δημοσίευσε — ιδιότητα **εκείνου που κοιτάζει**, όχι εκείνου που ανέβασε (Α14.4).
 *
 * ⇒ Σωστό σπίτι είναι το **ουδέτερο** module που **και οι δύο** οικογένειες ήδη
 * εισάγουν *(αμφότερες για το {@link PublicShelfSource})*.
 */
export const PUBLISHED_MEDIA_LIMIT = 24;

/**
 * 🔴 **ΤΟ `max-age` ΕΙΝΑΙ Ο ΠΡΟΫΠΟΛΟΓΙΣΜΟΣ ΤΗΣ ΑΠΟΣΥΡΣΗΣ — ΟΧΙ ΡΥΘΜΙΣΗ ΕΠΙΔΟΣΗΣ.**
 *
 * **Μετρημένο περπατώντας τη Φ2 (2026-09-01)** — και ανέτρεψε την πρώτη γραφή αυτής
 * της γραμμής, που έλεγε `max-age=31536000, immutable`:
 *
 * ```
 * απόσυρση → removed: 1 → ο κάδος έχει 0 αντικείμενα
 * ανώνυμο GET στο ΙΔΙΟ URL → HTTP 200, Age: 74      ← ΤΟ EDGE CACHE ΣΕΡΒΙΡΕΙ ΑΚΟΜΗ
 * ```
 *
 * Η τεκμηρίωση της Cloud Storage το λέει ρητά: το built-in cache προσφέρει **καμία
 * ακύρωση** *(«no cache invalidation capability»)* — είναι η δηλωμένη διαφορά του από
 * το Cloud CDN. Άρα ένα δημοσιευμένο αντικείμενο μένει προσβάσιμο **όσο λέει το
 * `max-age`**, ακόμη κι αφού σβηστεί από τον κάδο.
 *
 * ⇒ **`max-age = 31536000` σήμαινε «η απόσυρση ισχύει σε ΕΝΑ ΕΤΟΣ».** Για αγγελία που
 * ο άνθρωπος **απέσυρε** — και για το δικαίωμα διαγραφής του GDPR — αυτό δεν είναι
 * ρύθμιση επίδοσης· είναι **διαρροή με ημερομηνία λήξης δώδεκα μηνών**.
 *
 * 🔑 **Η ΜΙΑ ΩΡΑ ΔΕΝ ΧΑΝΕΙ ΤΟ ΟΦΕΛΟΣ.** Το edge εξακολουθεί να απορροφά **κάθε**
 * αίτημα εντός της ώρας ανά POP — η οικονομία του CDN είναι ήδη ~ολόκληρη στην πρώτη
 * ώρα. Αυτό που αλλάζει δεν είναι το κόστος: είναι το **παράθυρο έκθεσης**, από
 * **έτος** σε **ώρα**.
 *
 * ⚠️ **ΜΗΝ το ανεβάσεις «για επίδοση».** Η επιστροφή στο έτος είναι θεμιτή **μόνο**
 * όταν μπει μπροστά υποδομή με **ακύρωση** *(Cloud CDN, ή δικό μας `media.` hostname)*
 * — τότε η απόσυρση θα μπορεί να καθαρίζει το edge, και το παράθυρο παύει να είναι το
 * `max-age`. Άγκυρα φυλάει το όριο.
 *
 * 🔑 Το `immutable` **παραμένει αληθές και χρήσιμο**: αφορά το **περιεχόμενο** στο URL
 * (content-addressed ⇒ δεν αλλάζει ποτέ), όχι την **ύπαρξή** του. Γλιτώνει τις
 * επικυρώσεις μέσα στο παράθυρο φρεσκάδας.
 */
export const PUBLIC_SHELF_CACHE_CONTROL = 'public, max-age=3600, immutable';

/**
 * Το **ανώτατο** αποδεκτό `max-age` για δημόσιο ράφι, σε δευτερόλεπτα (24 ώρες).
 *
 * Υπάρχει ώστε το σκεπτίκο του {@link PUBLIC_SHELF_CACHE_CONTROL} να είναι
 * **εκτελούμενο**: χωρίς όριο, η επόμενη «βελτιστοποίηση» θα το ξανάκανε έτος και
 * **καμία πύλη δεν θα ρωτούσε**.
 */
export const PUBLIC_SHELF_MAX_CACHE_SECONDS = 86_400;

/**
 * Οι μορφές που **παράγει ο δικός μας** καθαριστής — ποτέ ό,τι ανέβασε ο άνθρωπος.
 *
 * ⚠️ Κλειστό σύνολο επίτηδες: το ράφι σερβίρει σε **ανώνυμο**, οπότε κάθε νέα μορφή
 * είναι απόφαση (π.χ. SVG **ποτέ** — εκτελεί script στον περιηγητή του επισκέπτη).
 *
 * ✅ **ΤΟ `glb` ΜΠΗΚΕ ΣΤΗ Φ4.2 — ΜΑΖΙ ΜΕ ΤΟΝ ΨΗΣΤΗ, ΟΠΩΣ ΤΟ ΟΡΙΖΕ Η ΓΡΑΜΜΗ ΠΟΥ ΗΤΑΝ ΕΔΩ.**
 * Ως τη Φ4.1 έλειπε επίτηδες: *«μια μορφή δηλωμένη εδώ χωρίς καθαριστή που να την παράγει
 * είναι **υπόσχεση χωρίς μηχανισμό** — και θα άνοιγε διαδρομή να δημοσιευτεί μοντέλο **ωμό**,
 * με `companyId`, κόστη και BOQ μέσα του»*. Ο μηχανισμός υπάρχει
 * *(`services/listings/public-shelf-model-bake`, ADR-841 §7 Α3 ②③)*, και η **Α-1γ** το φυλά
 * **και προς τις δύο κατευθύνσεις**: καμία μορφή εδώ χωρίς γραμμή που την παράγει, καμία
 * γραμμή που παράγει μορφή απούσα από εδώ.
 *
 * ⚠️ **SVG ΠΟΤΕ** — εκτελεί script στον περιηγητή του επισκέπτη. Η προσθήκη του `glb` **δεν**
 * χαλαρώνει αυτόν τον κανόνα: ένα GLB **δεν είναι εκτελέσιμο**, και ό,τι θα μπορούσε να
 * κρυφτεί μέσα του *(steganographic payload σε υφή)* **δεν επιβιώνει** το ξαναγράψιμο του
 * ψήστη — ό,τι δεν ανήκει σε γνωστό `accessor`/`bufferView`/`image` χάνεται *(ADR-845 §6.2)*.
 */
export const PUBLIC_SHELF_EXTENSIONS = ['webp', 'glb'] as const;

export type PublicShelfExtension = (typeof PUBLIC_SHELF_EXTENSIONS)[number];

/** Το αποτύπωμα που παράγει ο γραφέας: sha256, πεζό δεκαεξαδικό, 64 χαρακτήρες. */
const CONTENT_HASH_PATTERN = /^[0-9a-f]{64}$/;

// ---------------------------------------------------------------------------
// Τύποι
// ---------------------------------------------------------------------------

/**
 * **Ένα αρχείο που ο άνθρωπος ΔΙΑΛΕΞΕ να δημοσιεύσει.**
 *
 * ⚠️ Είναι **μονοπάτι στον ιδιωτικό κάδο**, ποτέ bytes και ποτέ URL: ο γραφέας
 * διαβάζει το πρωτότυπο ο ίδιος, ώστε ο καθαρισμός να μην μπορεί να παρακαμφθεί από
 * τον καλούντα *(Α12.7)*.
 *
 * 🔑 **Ζει σε αυτό το ΚΑΘΑΡΟ module** και όχι δίπλα στον γραφέα, ώστε η **προβολή**
 * (`public-listing-projection.ts`, ρητά καθαρή) να μπορεί να το δηλώσει ως είσοδο
 * χωρίς να αποκτήσει εξάρτηση από `sharp` / Admin SDK.
 */
export interface PublicShelfSource<M = ListingMaterial> {
  readonly privateStoragePath: string;
  /**
   * **Τι ΕΙΝΑΙ αυτά τα bytes** — φωτογραφία ή κάτοψη (ADR-841 §7 Α17.4).
   *
   * 🔑 **Το ράφι το ΚΟΥΒΑΛΑ, δεν το ΕΡΜΗΝΕΥΕΙ ΠΟΤΕ** — ακριβώς όπως ήδη κουβαλά το
   * `sourceRef`. Μια μηχανή που ξέρει μόνο από bytes δεν αποφασίζει τι σημαίνει
   * «κάτοψη»· η **μία** ερμηνεία ζει στο `withPublishedGallery`.
   *
   * 🔴 **ΥΠΟΧΡΕΩΤΙΚΟ, ΚΑΙ ΕΙΝΑΙ Ο ΜΗΧΑΝΙΣΜΟΣ.** Και οι **δύο** παραγωγοί —ο ιδιώτης
   * και το γραφείο— **δεν μεταγλωττίζονται** αν δεν απαντήσουν *«τι είναι αυτό;»*.
   * Ένα προαιρετικό πεδίο θα ξαναγεννούσε ακριβώς τη σιωπή που το Ο-20 ονόμασε:
   * *«δεν το ταξινόμησε κανείς»* διαβασμένο ως *«φωτογραφία»*.
   *
   * ────────────────────────────────────────────────────────────────────────
   * 🔑 ΓΙΑΤΙ ΕΓΙΝΕ **ΓΕΝΙΚΟ** ΣΤΟ ΣΤΑΔΙΟ 2 — ΤΗΡΗΣΗ, ΟΧΙ ΝΕΑ ΙΔΕΑ
   * ────────────────────────────────────────────────────────────────────────
   *
   * Το ίδιο το ράφι δηλώνει *«το **ΚΟΥΒΑΛΑ**, δεν το ΕΡΜΗΝΕΥΕΙ ΠΟΤΕ»*. Ένα πεδίο που
   * κουβαλιέται αδιαφανώς **δεν έχει λόγο** να ξέρει τον τύπο του — και μόλις εμφανίστηκε
   * δεύτερο είδος πράγματος, το καρφωμένο `ListingMaterial` άρχισε να λέει ψέματα.
   *
   * ⛔ **ΜΗΝ προσθέσεις `'logo'`/`'portrait'` στο {@link ListingMaterial}**: θα μόλυνε τις
   * αγγελίες με έννοιες βιτρίνας και θα ανάγκαζε το `withPublishedGallery` να χειριστεί
   * **αδύνατες** περιπτώσεις — κάτοψη-λογότυπο, φωτογραφία-πορτρέτο.
   *
   * ⚠️ **Γενικό στο ΔΟΧΕΙΟ, ποτέ στο «είδος»**: το `ListingMaterial` είναι **διακριτή
   * ένωση** *(`{kind:'photo'} | {kind:'floorplan', at}`)*, και ένα `PublicShelfSource<K>`
   * πάνω σε σκέτο `kind` θα **έτρωγε το `at`** της κάτοψης — δηλαδή τη στιγμή που ο
   * `SourcedAttribute` είναι υποχρεωμένος να δηλώσει.
   */
  readonly material: M;
  /**
   * 🎯 **Το σημείο εστίασης που ΔΗΛΩΣΕ ο άνθρωπος** (ADR-880) — κουβαλιέται όπως το {@link material},
   * δεν ερμηνεύεται από το ράφι.
   *
   * ⚠️ **ΠΡΟΑΙΡΕΤΙΚΟ ΕΠΙΤΗΔΕΣ, σε αντίθεση με το `material`**: εκεί η σιωπή θα δημοσίευε κάτοψη ως
   * φωτογραφία *(Ο-20)*· εδώ η σιωπή σημαίνει *«άφησε το αυτόματο»* — ακριβώς η σωστή προεπιλογή.
   * Και οι παραγωγοί που **δεν** κόβονται ποτέ (σήματα γραφείων) δεν έχουν τι να απαντήσουν.
   */
  readonly focalPoint?: PhotoFocalPoint | null;
}

/** Τα τρία μέρη ενός κλειδιού ραφιού. */
export interface PublicShelfKeyParts {
  /**
   * **Το υποκείμενο** — η αγγελία ή ο οργανισμός στον οποίο ανήκουν αυτά τα bytes.
   *
   * ⚠️ Ονομαζόταν `listingId` ως το Στάδιο 2. Με **δύο** είδη το όνομα έλεγε ψέματα στο
   * μισό ράφι — και η ταυτότητα της βιτρίνας είναι `companyId`, δηλαδή ακριβώς αυτό που
   * ο παλιός φρουρός **απαγόρευε**.
   */
  readonly subjectId: string;
  /** sha256 των **καθαρισμένων** bytes, πεζό δεκαεξαδικό. */
  readonly contentHash: string;
  readonly ext: PublicShelfExtension;
}

// ---------------------------------------------------------------------------
// Επικύρωση
// ---------------------------------------------------------------------------
/** Είναι αυτό αποτύπωμα που θα μπορούσε να έχει παραχθεί από τον γραφέα; */
export function isPublicShelfContentHash(value: string): boolean {
  return CONTENT_HASH_PATTERN.test(value);
}

/** Είναι αυτή μορφή που **παράγει** ο καθαριστής μας; */
export function isPublicShelfExtension(value: string): value is PublicShelfExtension {
  return (PUBLIC_SHELF_EXTENSIONS as readonly string[]).includes(value);
}

/**
 * 🏆 **ΠΟΙΑ ΜΟΡΦΗ ΠΑΡΑΓΕΙ ΑΥΤΗ Η ΚΩΔΙΚΟΠΟΙΗΣΗ** — `null` αν **κανείς δεν την ψήνει ακόμη**
 * *(ADR-845 §3, Φ4.0)*.
 *
 * ────────────────────────────────────────────────────────────────────────────
 * 🔴 Η ΕΡΩΤΗΣΗ ΗΤΑΝ ΚΑΘΟΛΙΚΗ ΚΑΙ ΓΙΝΕΤΑΙ **ΑΝΑ ΕΙΔΟΣ**
 * ────────────────────────────────────────────────────────────────────────────
 *
 * Ως τη Φ4.0 η μόνη ερώτηση ήταν *«ανήκει αυτό στο σύνολο των μορφών μας;»* — και όσο
 * το σύνολο είχε **ένα** μέλος, «έγκυρη μορφή» και «η μορφή αυτού του είδους» ήταν
 * κατά λάθος **η ίδια πρόταση**. Με δεύτερο είδος bytes παύουν να είναι: ένα κλειδί
 * `.webp` κάτω από ρίζα που δημοσιεύει μοντέλα θα περνούσε τον καθολικό έλεγχο **μια
 * χαρά**, και θα ήταν αρχείο σε θέση που κανείς δεν ζήτησε.
 *
 * 🔑 **`null` ΚΑΙ ΟΧΙ `throw`, ΚΑΙ ΕΙΝΑΙ ΑΠΟΦΑΣΗ ΣΥΜΒΟΛΑΙΟΥ.** Τη ρωτούν **δύο** με
 * αντίθετα συμβόλαια: ο **αυστηρός γραφέας** ({@link buildPublicShelfKey}, πετά αντί να
 * καθαρίσει) και ο **ανεκτικός αναγνώστης** ({@link parsePublicShelfKey}, *δεν πετά ποτέ*
 * — ο σαρωτής του κάδου συναντά ό,τι υπάρχει εκεί). Μια συνάρτηση που πετά θα ανάγκαζε
 * τον αναγνώστη σε `try/catch`, δηλαδή θα του άλλαζε το συμβόλαιο για χάρη του γραφέα.
 * Το `null` αφήνει **τον καθένα** να αποφασίσει τι σημαίνει «δεν ξέρω».
 *
 * ✅ **ΤΟ `'glb'` ΗΡΘΕ ΣΤΗ Φ4.2 — ΜΑΖΙ ΜΕ ΤΟΝ ΨΗΣΤΗ, ΟΠΩΣ ΤΟ ΟΡΙΖΕ Ο ΟΡΟΣ.** Ως τη Φ4.1
 * το `'model'` επιστρεφόταν `null`, δηλαδή *«η μορφή θα δηλωθεί μαζί με τον ψήστη»* — και
 * ήταν ο **σωστός** φρουρός: μορφή χωρίς καθαριστή που να την παράγει θα άνοιγε διαδρομή να
 * δημοσιευτεί μοντέλο **ωμό**, με `companyId`, κόστη και BOQ μέσα του. Ο ψήστης υπάρχει
 * *(`services/listings/public-shelf-model-bake`)*, άρα το `null` θα ήταν πλέον **ψέμα**.
 *
 * 🔑 **Η ΣΥΝΑΡΤΗΣΗ ΕΞΑΚΟΛΟΥΘΕΙ ΝΑ ΜΠΟΡΕΙ ΝΑ ΠΕΙ `null`, ΚΑΙ ΕΙΝΑΙ ΑΠΟΦΑΣΗ.** Ο φυσικός
 * πειρασμός με **δύο** απαντημένα σκέλη είναι να γίνει ο τύπος επιστροφής σκέτος
 * `PublicShelfExtension` — ⛔ **απορρίπτεται**: το `null` δεν σημαίνει *«raster ή μοντέλο;»*,
 * σημαίνει *«είδος bytes που **κανείς δεν ψήνει ακόμη**»*, και η ένωση θα μεγαλώσει ξανά
 * *(point-cloud, βίντεο)*. Ένας τύπος που δεν χωρά «δεν ξέρω» υποχρεώνει τον επόμενο να
 * **μαντέψει** — ακριβώς το `extension: string` με προεπιλογή `'webp'` που το
 * `public-shelf-encoding` απορρίπτει γραπτώς.
 *
 * 🔑 **Οι υπερφορτώσεις ΔΕΝ είναι καλλωπισμός** *(N.2: ποτέ `as`)*: ο γραφέας, αφού
 * στενέψει στο σύνορό του, κρατά **raster** κωδικοποίηση — και τότε η απάντηση **δεν
 * μπορεί** να είναι `null`. Χωρίς την υπερφόρτωση, κάθε κλήση του θα κουβαλούσε χειρισμό
 * ενός `null` που είναι **αποδεδειγμένα αδύνατο** εκεί, δηλαδή νεκρό κλάδο σε ζεστό δρόμο.
 * Από τη Φ4.2 υπάρχει και η **αδελφή** υπερφόρτωση για τον ψήστη μοντέλων.
 */
export function shelfExtension(encoding: RasterShelfEncoding): PublicShelfExtension;
export function shelfExtension(encoding: ModelShelfEncoding): PublicShelfExtension;
export function shelfExtension(encoding: ShelfEncoding): PublicShelfExtension | null;
export function shelfExtension(encoding: ShelfEncoding): PublicShelfExtension | null {
  switch (encoding.kind) {
    case 'raster':
      return 'webp';
    case 'model':
      return 'glb';
  }
}

// ---------------------------------------------------------------------------
// Κατασκευή
// ---------------------------------------------------------------------------

/**
 * **Το πρόθεμα ενός υποκειμένου** — η μονάδα συμφιλίωσης **και** διαγραφής.
 *
 * Τελειώνει σε `/` ώστε ένα `getFiles({ prefix })` να μην μπορεί ποτέ να πιάσει
 * γειτονικό υποκείμενο με κοινό πρόθεμα ονόματος (`ownp_1` ⇄ `ownp_12`).
 *
 * 🔑 **Ο φρουρός έρχεται από τον περιγραφέα**, ποτέ από αυτή τη συνάρτηση: έτσι η ίδια
 * αριθμητική εξυπηρετεί δύο είδη **χωρίς** να ξέρει ότι υπάρχουν δύο.
 *
 * @throws Αν η ταυτότητα δεν είναι αποδεκτή **για αυτό το είδος** — δες
 *   {@link PublicShelfKind.acceptsSubject}.
 */
export function publicShelfPrefix(kind: AnyPublicShelfKind, subjectId: string): string {
  if (!kind.acceptsSubject(subjectId)) {
    throw new Error(
      `Invalid public shelf subject for root ${JSON.stringify(kind.root)}: ${JSON.stringify(subjectId)}`,
    );
  }
  return `${kind.root}/${subjectId}/`;
}

/**
 * **Το κλειδί ενός δημοσιευμένου αρχείου.**
 *
 * @throws Αν οποιοδήποτε μέρος είναι μη αποδεκτό. **Πετά αντί να «καθαρίσει»**: ένα
 *   σιωπηλά διορθωμένο δημόσιο κλειδί είναι αρχείο σε θέση που κανείς δεν ζήτησε.
 */
export function buildPublicShelfKey(kind: AnyPublicShelfKind, parts: PublicShelfKeyParts): string {
  const { subjectId, contentHash, ext } = parts;

  if (!isPublicShelfContentHash(contentHash)) {
    throw new Error(`Invalid public shelf content hash: ${JSON.stringify(contentHash)}`);
  }
  // 🔴 **Ο ΕΛΕΓΧΟΣ ΕΙΝΑΙ ΑΝΑ ΕΙΔΟΣ, ΟΧΙ ΚΑΘΟΛΙΚΟΣ** *(ADR-845 §3)*. Το «είναι μορφή που
  //    παράγουμε;» δεν αρκεί: ρωτά αν κάποιος **κάπου** την ψήνει. Η ερώτηση που κρατά τα
  //    bytes στη θέση τους είναι *«την ψήνει ΑΥΤΟ το είδος;»* — ίδιο ιδίωμα με το
  //    `acceptsSubject`, που ρωτά **τη γραμμή** και όχι έναν καθολικό κατάλογο ταυτοτήτων.
  if (ext !== shelfExtension(kind.encoding)) {
    throw new Error(
      `Invalid public shelf extension for root ${JSON.stringify(kind.root)}: ${JSON.stringify(ext)}`,
    );
  }

  return `${publicShelfPrefix(kind, subjectId)}${contentHash}.${ext}`;
}

// ---------------------------------------------------------------------------
// Ανάγνωση
// ---------------------------------------------------------------------------

/**
 * Διαβάζει ένα κλειδί ραφιού πίσω στα μέρη του — `null` αν δεν είναι δικό μας.
 *
 * 🔑 Ο **ανεκτικός αναγνώστης** απέναντι στον **αυστηρό γραφέα**: ο σαρωτής του κάδου
 * συναντά ό,τι υπάρχει εκεί, και ό,τι δεν αναγνωρίζεται **δεν αγγίζεται** — δεν
 * μαντεύεται και δεν σβήνεται.
 *
 * 🔴 **ΡΩΤΑ ΓΙΑ ΕΝΑ ΣΥΓΚΕΚΡΙΜΕΝΟ ΕΙΔΟΣ, ΚΑΙ ΑΥΤΟ ΕΙΝΑΙ Η ΑΣΦΑΛΕΙΑ.** Ένας αναγνώστης που
 * θα δοκίμαζε **όλες** τις γραμμές του πίνακα θα ήταν βολικός και **επικίνδυνος**: ο
 * μόνος καταναλωτής του είναι ο σβήστης (`deleteExtra`), και ένα κλειδί βιτρίνας που
 * αναγνωρίζεται μέσα στο πέρασμα μιας **αγγελίας** είναι ακριβώς η διαρροή αρμοδιότητας
 * που ο πίνακας γράφτηκε για να αποκλείσει.
 */
export function parsePublicShelfKey(
  kind: AnyPublicShelfKind,
  key: string,
): PublicShelfKeyParts | null {
  const segments = key.split('/');
  if (segments.length !== 3) return null;

  const [root, subjectId, fileName] = segments;
  if (root !== kind.root) return null;
  if (!kind.acceptsSubject(subjectId)) return null;

  const dot = fileName.lastIndexOf('.');
  if (dot <= 0) return null;

  const contentHash = fileName.slice(0, dot);
  const ext = fileName.slice(dot + 1);
  if (!isPublicShelfContentHash(contentHash)) return null;
  // Πρώτα *«είναι μορφή που παράγουμε;»* — δίνει τον **τύπο**.
  if (!isPublicShelfExtension(ext)) return null;
  // ⚠️ Έπειτα *«την παράγει ΑΥΤΟ το είδος;»* — ίδια ερώτηση με τον γραφέα, **αντίθετη**
  //    απάντηση στο «δεν ξέρω»: εδώ ό,τι δεν αναγνωρίζεται **δεν αγγίζεται** — δεν
  //    μαντεύεται και δεν σβήνεται.
  if (ext !== shelfExtension(kind.encoding)) return null;

  return { subjectId, contentHash, ext };
}

/** Είναι αυτό κλειδί που θα μπορούσε να έχει γράψει ο γραφέας για **αυτό** το είδος; */
export function isPublicShelfKey(kind: AnyPublicShelfKind, key: string): boolean {
  return parsePublicShelfKey(kind, key) !== null;
}

// ---------------------------------------------------------------------------
// Δημόσιο URL
// ---------------------------------------------------------------------------

/**
 * **Η μία διατύπωση του δημόσιου URL.**
 *
 * ⚠️ **Χωριστή από το κλειδί επίτηδες.** Το κλειδί είναι **αμετάβλητο**· ο κεντρικός
 * υπολογιστής **όχι**: όταν κάποτε μπει δικό μας όνομα (`media.nestorconstruct.gr`,
 * το πρότυπο `photos.zillowstatic.com` της Zillow), αλλάζει **αυτή η συνάρτηση και
 * τίποτε άλλο** — κανένα αποθηκευμένο URL δεν παλιώνει, γιατί **δεν αποθηκεύουμε URL**.
 *
 * ⛔ **ΟΧΙ `firebasestorage.googleapis.com`**: εκείνη η διαδρομή κρίνεται από το
 * `storage.rules` και **δεν** ενεργοποιεί το built-in edge cache (Α12.1). Εδώ μιλάμε
 * στο ίδιο το Cloud Storage, όπου η δημοσιότητα είναι IAM και το cache δουλεύει.
 */
export function publicShelfUrl(bucketName: string, key: string): string {
  return `https://storage.googleapis.com/${bucketName}/${key}`;
}
