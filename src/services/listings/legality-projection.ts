/**
 * @fileoverview **Η ΝΟΜΙΜΟΤΗΤΑ ΣΤΗ ΔΗΜΟΣΙΑ ΠΡΟΒΟΛΗ** — η βαθμίδα φεύγει, το έγγραφο ποτέ.
 * @related ADR-838 §5 · ADR-835 §4.5 · ADR-777 §7 (Α17) · lib/legality/*
 * @module services/listings/legality-projection
 *
 * ────────────────────────────────────────────────────────────────────────────
 * 🔴 ΓΙΑΤΙ ΔΙΚΟ ΤΟΥ ΑΡΧΕΙΟ ΚΑΙ ΟΧΙ ΑΛΛΗ ΜΙΑ `projectX` ΣΤΟΝ ΓΡΑΦΕΑ
 * ────────────────────────────────────────────────────────────────────────────
 *
 * Δύο λόγοι, και ο δεύτερος είναι ο σοβαρός:
 *
 * 1. Το `public-listing-projection.ts` ήταν **487 γραμμές** με όριο **500** (N.7.1).
 * 2. 🔑 **Είναι η ΜΟΝΗ πόρτα από την οποία νομική πληροφορία φεύγει στον ανώνυμο
 *    κόσμο.** Μια πόρτα που κρύβεται ανάμεσα σε δεκαπέντε χαρτογραφήσεις πεδίων είναι
 *    πόρτα που κανείς δεν ξαναδιαβάζει. Εδώ έχει **όνομα**, **αρχείο** και **άγκυρες**.
 *
 * ⚠️ **ΔΕΝ ΕΙΝΑΙ ΕΔΩ Η ΠΥΛΗ ΤΗΣ ΤΙΜΗΣ.** Το αν δημοσιεύεται το `value` το κρίνει το
 * `legalitySignalFor` — **μία** φορά, για **όλους** τους καταναλωτές. Αν κρινόταν εδώ,
 * θα υπήρχε δεύτερη απάντηση στο ίδιο ερώτημα, ελεύθερη να αποκλίνει (ADR-749).
 */

import {
  LEGALITY_CLAIM_KINDS,
  type LegalityClaim,
} from '@/lib/legality/legality-claim';
import { legalitySignalsFor, type LegalitySignal } from '@/lib/legality/legality-signal';
import { wholePropertySpace } from '@/lib/spaces/space-ref';
import type { OfferKind } from '@/types/property-offers';

/**
 * **Οι αξιώσεις που κουβαλά το έγγραφο του ακινήτου.**
 *
 * 🔶 **ΔΗΛΩΜΕΝΟ ΚΕΝΟ: ΚΑΝΕΝΑΣ ΓΡΑΦΕΑΣ ΔΕΝ ΤΙΣ ΓΡΑΦΕΙ ΑΚΟΜΗ**, και το λέω αντί να το
 * υπονοήσω. Το πεδίο θα το γεμίσει η **φόρμα του κατόχου** — ίδιο ακριβώς σχήμα με το
 * `locationDisclosure` της Α5, που δηλώνει το ίδιο κενό με τα ίδια λόγια. Μέχρι τότε
 * κάθε αγγελία απαντά `undeclared` σε **κάθε** είδος, που είναι η **αλήθεια**: κανείς
 * δεν έχει ρωτηθεί ακόμη.
 *
 * ⛔ **ΚΑΙ ΔΕΝ ΠΑΡΑΓΕΤΑΙ ΑΞΙΩΣΗ ΑΠΟ ΥΠΑΡΧΟΝΤΑ ΠΕΔΙΑ** — ούτε από το `energyClass` του
 * `property-completion`, ούτε από το `certificateId` του `types/property.ts`. Θα ήταν
 * δελεαστικό («*υπάρχει η τιμή, φτιάξε αξίωση `self-declared`*») και θα ήταν **ψέμα με
 * ημερομηνία**: εκείνα τα πεδία **δεν έχουν** `assertedAt` ούτε `authority`, οπότε η
 * παραγόμενη αξίωση θα δήλωνε προέλευση που **κανείς δεν δήλωσε**. Είναι ακριβώς το
 * *«ετικέτα χωρίς προέλευση είναι ισχυρισμός»* της SPEC-777 §24.3.
 */
export interface LegalityBearingProperty {
  readonly id: string;
  readonly legality?: readonly LegalityClaim[] | null;
}

/**
 * **Ακίνητο + διαθέσεις + στιγμή ⇒ οι γραμμές νομιμότητας της αγγελίας.**
 *
 * 🔴 **ΤΟ ΥΠΟΚΕΙΜΕΝΟ ΕΙΝΑΙ ΟΛΟΚΛΗΡΟ ΤΟ ΑΚΙΝΗΤΟ, ΚΑΙ ΕΙΝΑΙ ΑΠΟΦΑΣΗ ΜΕ ΣΥΝΕΠΕΙΑ.** Μία
 * `PublicListing` αντιστοιχεί σε **ένα** ακίνητο, άρα το ερώτημα που έχει νόημα εδώ
 * είναι *«τι ισχύει για **όλο**;»*.
 *
 * ⚠️ **Και γι' αυτό ένας ΑΜΑ ΔΩΜΑΤΙΟΥ δεν εμφανίζεται εδώ ως δηλωμένος** — η κάλυψη
 * είναι **ασύμμετρη**: `ΑΜΑ(δωμ. Α)` **δεν** καλύπτει το ολόκληρο. Αν η κάλυψη ήταν
 * τομή, η αγγελία ολόκληρου διαμερίσματος θα φορούσε τον αριθμό ενός **δωματίου** —
 * δηλαδή θα δημοσιεύαμε αριθμό μητρώου για πόρο που **δεν** αφορά, με πρόστιμο από
 * **5.000 €** στο άλλο άκρο. Οι καταχωρίσεις ανά χώρο αποκτούν **δικές τους** αγγελίες
 * όταν υπάρξει το `StayInventoryUnit` στη διαδρομή δημοσίευσης (Φ5/Φ6).
 *
 * 🔑 **Επιστρέφει ΠΑΝΤΑ και τα τέσσερα είδη** — ο **πλήρης παρονομαστής**. Η Φ3 μέτρησε
 * *«δύο ελλιπείς λίστες που επιβεβαίωναν η μία την άλλη»*: αν ο γραφέας παρέλειπε τα μη
 * σχετικά, η οθόνη δεν θα μπορούσε ποτέ να ξεχωρίσει *«δεν αφορά»* από *«ξεχάστηκε»*.
 */
export function projectLegality(
  property: LegalityBearingProperty,
  offerKinds: readonly OfferKind[],
  projectedAt: string,
): readonly LegalitySignal[] {
  return legalitySignalsFor(
    property.legality ?? [],
    offerKinds,
    [wholePropertySpace(property.id)],
    projectedAt,
    LEGALITY_CLAIM_KINDS,
  );
}
