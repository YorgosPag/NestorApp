'use client';

/**
 * @fileoverview **ΤΟ ΣΗΜΕΙΟ ΑΠΟΚΤΑ ΟΝΟΜΑ — ΜΕΤΡΗΜΕΝΟ, ΟΧΙ ΥΠΟΤΕΘΕΝ.** ADR-846 §9 #12.
 * @related lib/agency/presence-admin-ids.ts (η μηχανή) · hooks/useAdminFootprints ·
 *   hooks/useAdministrativeHierarchy · lib/agency/showcase-where-voice
 * @module hooks/useCircleAnchorName
 *
 * ═════════════════════════════════════════════════════════════════════════════
 * 🏆 ΕΔΩ ΞΕΠΕΡΝΑΜΕ ΤΟ ZILLOW — ΚΑΙ ΤΟ ΚΟΣΤΟΣ ΕΙΝΑΙ **ΜΗΔΕΝ BYTE**
 * ═════════════════════════════════════════════════════════════════════════════
 *
 * Το **Zillow** γράφει *«Within 5 km»* **χωρίς να πει 5 χλμ από ΤΙ**. Το ίδιο κάνουν
 * Redfin και Idealista: η ακτίνα φαίνεται, το **αγκυροβόλιο** όχι. Ο επισκέπτης που
 * έφτασε από κοινοποιημένο σύνδεσμο δεν μαθαίνει **ποτέ** πού ψάχνει.
 *
 * 🔬 **ΜΕΤΡΗΘΗΚΕ ΠΡΙΝ ΓΡΑΦΤΕΙ** *(2026-09-10)*: και τα **δύο** παράγωγα αρχεία που
 * χρειάζεται η απάντηση φορτώνονται **ήδη** στο `/pro`, για **άλλους** λόγους —
 * `admin-footprints.json` **7.440** εγγραφές *(τα ζητά ο κριτής της κάλυψης)* και
 * `administrative-hierarchy.json` *(τα ζητά ο επιλογέας περιοχής + τα ονόματα της
 * κάρτας)*. ⇒ Η ονομασία κοστίζει **μηδέν νέα bytes, μηδέν νέο αίτημα δικτύου**: μία
 * σάρωση αποστάσεων ανά αλλαγή φίλτρου, με φίλτρο `couldContain` μπροστά.
 *
 * ⛔ **ΚΑΙ ΔΕΝ ΕΙΝΑΙ ΑΝΤΙΣΤΡΟΦΗ ΓΕΩΚΩΔΙΚΟΠΟΙΗΣΗ** — αυτός ήταν ο φραγμός του **§9 #1**,
 * και ισχύει ακέραιος: το `api/geocoding/reverse` επιστρέφει **ελεύθερα strings
 * Nominatim**, όχι `AdminEntity.id`, άρα δεν μπορεί να απαντήσει *«ποιο διοικητικό
 * κελί;»* χωρίς αντιστοίχιση που κανείς δεν έγραψε. Ο δρόμος εδώ είναι **γεωμετρικός**:
 * point-in-shape πάνω στα **ίδια** αποτυπώματα Καλλικράτη *(CC-BY, geodata.gov.gr)* που
 * χρησιμοποιεί ο κριτής. Ο φραγμός δεν παρακάμπτεται — **δεν αφορά αυτή τη διαδρομή**.
 *
 * 🔴 **Ο ΤΟΠΟΣ ΜΕΤΡΙΕΤΑΙ, ΔΕΝ ΥΠΟΤΙΘΕΤΑΙ** *(περιστατικό 2026-09-09)*: το σημείο
 * `(40,6643 · 22,8976)` γράφτηκε *«Σταυρούπολη»* σε handoff → ADR → άγκυρα, και είναι
 * **ΔΗΜΟΣ ΚΟΡΔΕΛΙΟΥ - ΕΥΟΣΜΟΥ**. Το πολύγωνο το ήξερε από την αρχή· κανείς δεν το
 * άνοιξε. Αυτό το hook **ανοίγει το πολύγωνο κάθε φορά**, ώστε καμία ετικέτα να μην
 * ταξιδεύει ξανά χωρίς έλεγχο.
 */

import { useMemo } from 'react';

import { useAdminFootprints } from '@/hooks/useAdminFootprints';
import { lineageIdsOf, useAdministrativeHierarchy } from '@/hooks/useAdministrativeHierarchy';
import { containingEntityOfPoint } from '@/lib/agency/presence-admin-ids';
import { isAdministrativeWhere, type ShowcaseWhere } from '@/types/agency-coverage';

/**
 * **Πώς λέγεται ο τόπος του κέντρου** αυτού του ερωτήματος — ή `null` για «δεν ξέρω».
 *
 * ⚠️ **`null` ΣΕ ΤΕΣΣΕΡΙΣ ΝΟΜΙΜΕΣ ΠΕΡΙΠΤΩΣΕΙΣ, ΚΑΙ ΚΑΜΙΑ ΔΕΝ ΕΙΝΑΙ ΣΦΑΛΜΑ**: δεν
 * υπάρχει ερώτημα-κύκλος · τα αποτυπώματα δεν έχουν φορτώσει · κανένα κελί δεν περιέχει
 * **αποδεδειγμένα** το σημείο *(ακτογραμμή, μικρό νησί, κενό στο κάλυμμα)* · η
 * **ιεραρχία** δεν έχει φορτώσει, οπότε η ταυτότητα υπάρχει αλλά δεν έχει ακόμη όνομα.
 * Ο καταναλωτής τις χειρίζεται **όλες με τον ίδιο τρόπο** — σιωπά για τον τόπο και λέει
 * μόνο την ακτίνα. **Ποτέ ωμές συντεταγμένες, ποτέ ωμό `municipality:0701`.**
 *
 * 🔒 **ΟΙ ΕΞΑΡΤΗΣΕΙΣ ΕΙΝΑΙ ΑΛΗΘΙΝΕΣ — ΚΑΙ ΕΙΝΑΙ ΤΟ ΜΑΘΗΜΑ ΤΗΣ §6.2 ΤΗΡΗΜΕΝΟ.** Το
 * `entries` αλλάζει ταυτότητα όταν φτάνουν τα αποτυπώματα· το `findById` όταν φτάνει η
 * ιεραρχία. ⛔ **ΜΗΝ** βγάλεις το `findById` από τη λίστα «επειδή η συνάρτηση δεν
 * αλλάζει λογική»: το `lineageIdsOf` είναι **module-level** και η ταυτότητά του **δεν**
 * αλλάζει ποτέ, άρα χωρίς το `findById` αυτό το `useMemo` θα **πάγωνε στο «δεν ξέρω»
 * για όλη τη ζωή της σελίδας** — ακριβώς το σφάλμα που άφησε τον επιλογέα περιοχής
 * άδειο σε κάθε κρύο φόρτωμα *(άγκυρα: `area-combobox-cold-load.test.tsx`)*.
 */
export function useCircleAnchorName(where: ShowcaseWhere | null): string | null {
  const { entries } = useAdminFootprints();
  const { findById } = useAdministrativeHierarchy();

  return useMemo(() => {
    // Ο άξονας δεν είναι κύκλος ⇒ δεν υπάρχει σημείο να ονομαστεί. Σε διοικητική
    // περιοχή το όνομα το δείχνει **το ίδιο το χειριστήριο** — δεύτερη αναφορά θα ήταν
    // δεύτερη αυθεντία για το ίδιο πράγμα.
    if (where === null || isAdministrativeWhere(where)) return null;

    const adminId = containingEntityOfPoint(where.circle.center, entries, lineageIdsOf);
    if (adminId === null) return null;

    // 🔑 **Το `?? null` ΔΕΝ είναι αμυντικός θόρυβος**: το `adminId` βγήκε από τα
    //    **αποτυπώματα**, που είναι άλλο αρχείο από την **ιεραρχία**. Τα δύο μπορούν να
    //    αποκλίνουν κατά μία ανάπτυξη — και τότε η ταυτότητα υπάρχει χωρίς όνομα. Ένα
    //    ωμό `municipality:0701` στην οθόνη δεν λέει τίποτα σε κανέναν *(ίδιο σκεπτικό
    //    με το `CoverageLine`: «όσο η ιεραρχία δεν έχει φορτώσει, η γραμμή ΛΕΙΠΕΙ»)*.
    return findById(adminId)?.name ?? null;
  }, [where, entries, findById]);
}
