/**
 * =============================================================================
 * ΤΟ ΛΕΞΙΛΟΓΙΟ ΤΩΝ ΓΛΩΣΣΩΝ — «σε ποιες γλώσσες μιλάμε σε άνθρωπο;»
 * =============================================================================
 *
 * **Μηδέν εξαρτήσεις. Καμία εισαγωγή.** Και αυτό είναι ολόκληρος ο λόγος ύπαρξης
 * του αρχείου, όχι στιλιστική προτίμηση.
 *
 * ────────────────────────────────────────────────────────────────────────────
 * 🔴 ΓΙΑΤΙ ΜΕΤΑΚΟΜΙΣΕ ΕΔΩ — ΤΟ ΕΝΑ ΑΡΧΕΙΟ ΠΟΥ ΤΟ ΚΡΑΤΟΥΣΕ ΗΤΑΝ ΑΠΡΟΣΙΤΟ
 * ────────────────────────────────────────────────────────────────────────────
 *
 * Το `SUPPORTED_LANGUAGES` ζούσε στο `lazy-config.ts`, το οποίο **εκτελεί**
 * `import i18n from 'i18next'` + `react-i18next` + `i18next-icu` στην κορυφή του.
 * Δηλαδή: **οποιοσδήποτε** ήθελε να μάθει «ποιες γλώσσες υπάρχουν;» έπρεπε να
 * φορτώσει ολόκληρη τη μηχανή μεταφράσεων του browser.
 *
 * Ο διακομιστής — που γράφει τα αυτόματα email από **cron**, χωρίς αίτημα χρήστη
 * και χωρίς browser — **δεν μπορούσε** να το κάνει αυτό. Άρα, όποιος χρειαζόταν τη
 * λίστα εκεί, θα την **ξανάγραφε**. Δύο χειρόγραφες λίστες που κανείς δεν συγκρίνει
 * είναι το ακριβές σχήμα που στο ίδιο repo είχε ήδη αποκλίνει κατά **63**
 * namespaces (CHECK 3.34) και κατά **8** workflows (CHECK 3.37).
 *
 * 🔑 **Η μετακόμιση ΔΕΝ είναι καθαριότητα — είναι η προϋπόθεση για να υπάρχει
 * ΜΙΑ λίστα.** Το `lazy-config.ts` επανεξάγει από εδώ, οπότε κανένας υπάρχων
 * καταναλωτής δεν αλλάζει.
 *
 * ────────────────────────────────────────────────────────────────────────────
 * ⚠️ ΔΥΟ ΣΥΝΟΛΑ, ΚΑΙ Η ΔΙΑΦΟΡΑ ΤΟΥΣ ΕΙΝΑΙ ΣΗΜΑΣΙΟΛΟΓΙΚΗ
 * ────────────────────────────────────────────────────────────────────────────
 *
 * | Σύνολο | Τι απαντά | Ποιος το θέλει |
 * |---|---|---|
 * | {@link SUPPORTED_LANGUAGES} | «τι δέχεται η μηχανή i18next;» | το bootstrap του i18n |
 * | {@link HUMAN_LANGUAGES} | «σε τι γλώσσα γράφω σε **άνθρωπο**;» | επιλογείς, email, έγγραφα |
 *
 * Η διαφορά είναι το `pseudo`: **όργανο ελέγχου**, όχι γλώσσα (ADR-666). Παράγεται
 * runtime από τα ελληνικά και τυλίγει κάθε κείμενο σε `[[~~ … ~~]]` ώστε να φανεί τι
 * είναι σκληρογραμμένο. Ένα email σε `pseudo` θα ήταν **αχρησιμοποίητο** για τον
 * παραλήπτη — και θα ήταν εφικτό, γιατί ο επιλογέας της κεφαλίδας το προσφέρει σε
 * περιβάλλον ανάπτυξης.
 *
 * 🔑 **Το `HUMAN_LANGUAGES` ΠΑΡΑΓΕΤΑΙ με αφαίρεση, ΠΟΤΕ δεν ξαναγράφεται.** Μια
 * τρίτη γλώσσα προστίθεται σε **ένα** σημείο και εμφανίζεται παντού· μια χειρόγραφη
 * δεύτερη λίστα θα την έχανε σιωπηλά ακριβώς εκεί που πονάει — στα email, που κανείς
 * δεν βλέπει μέχρι να παραπονεθεί παραλήπτης.
 *
 * @module i18n/languages
 * @see ADR-777 §8.29 — η γλώσσα των αυτόματων email
 * @see ADR-666 — το pseudo locale ως όργανο
 */

/**
 * Ό,τι δέχεται η μηχανή i18next.
 *
 * ⚠️ **Η σειρά δεν είναι σημασιολογική** εδώ (σε αντίθεση με την κλίμακα z-index του
 * CHECK 3.50): κανένας καταναλωτής δεν διαβάζει «την πρώτη» ως προεπιλογή — η
 * προεπιλογή δηλώνεται ρητά στο {@link DEFAULT_LANGUAGE}.
 */
export const SUPPORTED_LANGUAGES = ['el', 'en', 'pseudo'] as const;

/** Μια τιμή που δέχεται το i18next. */
export type Language = (typeof SUPPORTED_LANGUAGES)[number];

/**
 * Το ψευδο-locale του ADR-666.
 *
 * ⚠️ Το `satisfies Language` **δεν** είναι διακόσμηση: εγγυάται σε χρόνο
 * μεταγλώττισης ότι το πράγμα που αφαιρούμε **ανήκει** στο σύνολο από το οποίο το
 * αφαιρούμε. Χωρίς αυτό, μια ορθογραφική αλλαγή θα άφηνε το `HUMAN_LANGUAGES`
 * **ταυτόσημο** με το `SUPPORTED_LANGUAGES` — δηλαδή η αφαίρεση θα γινόταν
 * σιωπηλά ανενεργή, και το `pseudo` θα ταξίδευε σε email.
 */
export const PSEUDO_LANGUAGE = 'pseudo' satisfies Language;

/** Γλώσσα στην οποία επιτρέπεται να απευθυνθούμε σε άνθρωπο. */
export type HumanLanguage = Exclude<Language, typeof PSEUDO_LANGUAGE>;

/**
 * Οι γλώσσες που διαβάζει άνθρωπος — **παραγόμενες**, ποτέ ξαναγραμμένες.
 *
 * Είναι η μοναδική λίστα που επιτρέπεται να εμφανιστεί σε επιλογέα γλώσσας ή σε
 * αυτόματο email.
 */
export const HUMAN_LANGUAGES: readonly HumanLanguage[] = SUPPORTED_LANGUAGES.filter(
  (language): language is HumanLanguage => language !== PSEUDO_LANGUAGE,
);

/**
 * Η γλώσσα όταν κανείς δεν έχει πει τίποτα.
 *
 * ⚠️ Ο τύπος είναι **`HumanLanguage`, όχι `Language`** — και η στένωση είναι
 * σκόπιμη: μια προεπιλογή που θα μπορούσε να είναι `pseudo` θα σήμαινε ότι ένας
 * χρήστης χωρίς ρύθμιση παίρνει email γεμάτο `[[~~ … ~~]]`. Ο μεταγλωττιστής το
 * κάνει **αδύνατο** αντί να το φυλάει έλεγχος που κάποιος μπορεί να ξεχάσει.
 */
export const DEFAULT_LANGUAGE: HumanLanguage = 'el';

/**
 * **Είναι αυτό γλώσσα στην οποία μιλάμε σε άνθρωπο;**
 *
 * ⚠️ Δέχεται `unknown` επίτηδες: η τιμή έρχεται από **έγγραφο Firestore** ή από
 * `localStorage` — δηλαδή από δεδομένα, όχι από τον μεταγλωττιστή.
 */
export function isHumanLanguage(value: unknown): value is HumanLanguage {
  return typeof value === 'string' && (HUMAN_LANGUAGES as readonly string[]).includes(value);
}

/**
 * Η γλώσσα του χρήστη, ή η προεπιλογή όταν λείπει / είναι άγνωστη / είναι το
 * `pseudo`.
 *
 * 🔑 **Fail-safe, ίδιο σχήμα με το `resolveTimeZone` του §8.28** — και για τον ίδιο
 * ακριβώς λόγο: το πεδίο ζει σε έγγραφο, άρα μπορεί να περιέχει ό,τι να 'ναι. Ένα
 * `"ελληνικά"` γραμμένο από μελλοντικό migration δεν επιτρέπεται να ρίξει την
 * εργασία που παραδίδει αλληλογραφία **για όλους**· πέφτει στην προεπιλογή και το
 * email φεύγει.
 */
export function resolveHumanLanguage(value: unknown): HumanLanguage {
  return isHumanLanguage(value) ? value : DEFAULT_LANGUAGE;
}
