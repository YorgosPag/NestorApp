/**
 * @fileoverview **ΤΙ ΑΠΑΝΤΑΜΕ ΣΤΟ «ΕΛΕΥΘΕΡΟ 10–17/08, 4 ΑΤΟΜΑ;»** — εννέα ονόματα.
 * @related ADR-835 §4.5 · §4.6 · §4.7 · §6.2 · lib/stay/stay-free-runs.ts ·
 *   lib/mandate/mandate-occupancy-notice.ts · lib/demand/demand-match-vocabulary.ts
 * @module lib/stay/stay-availability-vocabulary
 *
 * ────────────────────────────────────────────────────────────────────────────
 * 🏆 Η ΑΡΧΗ: **ΟΝΟΜΑΖΟΥΜΕ, ΔΕΝ ΕΞΑΦΑΝΙΖΟΥΜΕ** — και η αγορά κάνει το αντίθετο
 * ────────────────────────────────────────────────────────────────────────────
 *
 * Κάθε πλατφόρμα κρατήσεων απαντά στο ίδιο ερώτημα με **σιωπή**: *«the search
 * algorithm **completely hides** unavailable listings»*. Ο επισκέπτης δεν μαθαίνει
 * ποτέ **πόσα** κρύφτηκαν ούτε **γιατί** — και ο οικοδεσπότης δεν μαθαίνει ποτέ ότι
 * χάθηκε επειδή δεν συμπλήρωσε ένα πεδίο.
 *
 * Εδώ κάθε αγγελία παίρνει **όνομα**. Η ίδια πράξη που το ADR-832 §4 ονομάζει
 * *«άρνηση **με όνομα**»* έναντι του *«Invalid»* των MLS — μεταφερμένη από τον μεσίτη
 * στον επισκέπτη.
 *
 * ────────────────────────────────────────────────────────────────────────────
 * 🔴 ΓΙΑΤΙ ΕΝΝΕΑ ΚΑΙ ΟΧΙ ΤΡΙΑ — **ΚΑΘΕ ΜΙΑ ΕΧΕΙ ΑΛΛΗ ΘΕΡΑΠΕΙΑ, ΚΑΙ ΑΛΛΟΝ ΥΠΟΧΡΕΟ**
 * ────────────────────────────────────────────────────────────────────────────
 *
 * | Όνομα | Τι σημαίνει | **Ποιος** το λύνει |
 * |---|---|---|
 * | `not-a-stay` | δεν είναι κατάλυμα — η ερώτηση δεν το αφορά | κανείς· **δεν είναι πρόβλημα** |
 * | `terms-unknown` | κατάλυμα που **δεν δήλωσε** πόσα άτομα χωράει | ο **κάτοχος**, με ένα πεδίο |
 * | `over-capacity` | χωράει, αλλά **λιγότερους** | ο **επισκέπτης**, με λιγότερα άτομα |
 * | `below-min-nights` | δέχεται, αλλά **περισσότερες** νύχτες | ο **επισκέπτης**, με μεγαλύτερη διαμονή |
 * | `unknown` | κατάλυμα **χωρίς δηλωμένο ημερολόγιο** | ο **κάτοχος**, συνδέοντας ημερολόγιο |
 * | `unreadable` | ημερολόγιο που **δεν διαβάστηκε** | **εμείς** — είναι δικό μας χρέος |
 * | `occupied` | κρατημένο — **και λέμε τι απομένει** | ο **χρόνος**· δες `freeRuns` |
 * | `conditional` | ελεύθερο, **αλλά το ακίνητο πωλείται** | κανείς· είναι **αποκάλυψη** |
 * | `free` | ελεύθερο, καμία επιφύλαξη | — |
 *
 * 🔑 **Είναι το ίδιο ιδίωμα με τρία που το έργο έχει ήδη**: `never-asked` vs
 * `owner-declined` (Α5) · `proximity-unknown` vs `proximity-too-far` (Ζ6) ·
 * `no-supply` vs `no-location` vs `partial` (κάλυψη). Σε **κάθε** περίπτωση ο λόγος
 * είναι ο ίδιος: *ένα κοινό «όχι» στέλνει τον άνθρωπο να διορθώσει **λάθος πράγμα**.*
 *
 * ────────────────────────────────────────────────────────────────────────────
 * 🔴 ΤΟ `unknown` ΕΔΩ ΔΕΝ ΕΙΝΑΙ ΤΟ `availability-unknown` ΤΗΣ ΖΗΤΗΣΗΣ — ΜΕΤΡΗΘΗΚΕ
 * ────────────────────────────────────────────────────────────────────────────
 *
 * Το `'availability-unknown'` (`lib/demand/demand-match-vocabulary.ts:125`) είναι
 * **`DemandBlocker`**: μέλος κλειστού συνόλου **18** τιμών, με δικό του
 * `CATEGORICAL_BLOCKERS`, δικό του `isMeasurableBlocker` και δικά του κλειδιά i18n. Το
 * ερώτημά του είναι *«γιατί αυτή η **ζήτηση** δεν ταιριάζει σε αυτή την αγγελία;»*.
 *
 * Το ερώτημα **εδώ** είναι άλλο: *«τι απαντά αυτή η αγγελία στις **ημερομηνίες του
 * επισκέπτη**;»*. Εισαγωγή του `DemandBlocker` θα έβαζε **17 τιμές που δεν σημαίνουν
 * τίποτα εδώ** (`price-above`, `proximity-too-far`, …) μέσα στον τύπο της αναζήτησης,
 * και θα έδενε δύο τομείς που σήμερα δεν γνωρίζονται — ώστε η επόμενη τιμή που
 * προστίθεται στον έναν να εμφανίζεται σιωπηλά στον άλλο.
 *
 * ✅ **Επαναχρησιμοποιείται η ΕΝΝΟΙΑ, όχι ο τύπος** (N.12: *άγνωστο ≠ κενό*), και οι
 * δύο κάδοι απαντούν στον **ίδιο άνθρωπο** το ίδιο πράγμα: *«κανείς δεν δήλωσε
 * ημερολόγιο — δεν υποθέτουμε “διαθέσιμο πάντα”»*. Η γραμμή αυτή **είναι** ο δεσμός·
 * η εισαγωγή θα ήταν σύζευξη.
 *
 * **Layering**: leaf — καθαροί τύποι, μηδέν I/O, μηδέν ρολόι.
 */

import type { Occupancy } from '@/lib/occupancy/occupancy-conflict';

import type { StayNightRun } from './stay-free-runs';

// =============================================================================
// 1. ΤΟ ΕΡΩΤΗΜΑ ΤΟΥ ΕΠΙΣΚΕΠΤΗ
// =============================================================================

/**
 * **Τι ρώτησε ο άνθρωπος.** Ημι-ανοιχτό `[checkIn, checkOut)` — δες `StayBooking`.
 *
 * ⚠️ **Και τα τρία υποχρεωτικά μαζί, ή κανένα.** Ένα παράθυρο χωρίς ημερομηνία λήξης
 * δεν είναι «σχεδόν ερώτηση»: το {@link StayQuery} κατασκευάζεται **μόνο** από
 * φίλτρα που έχουν **και** τα δύο άκρα, και ο κωδικοποιητής της διεύθυνσης το
 * επιβάλλει (ίδιο ιδίωμα με το `lat`/`lng` του `ListingGeoFilter`: *«μισό ζεύγος δεν
 * είναι σχεδόν σωστό»*).
 *
 * 🔑 **Το `guests` είναι `null` όταν ΔΕΝ ρωτήθηκε** — και τότε η χωρητικότητα **δεν
 * κρίνεται καθόλου**. Ένα `?? 1` εδώ θα σήμαινε ότι κάθε αναζήτηση χωρίς άτομα ρωτά
 * σιωπηλά «ένα άτομο», δηλαδή σιωπηλή απόφαση στο ακριβώς λάθος σημείο.
 */
export interface StayQuery {
  /** ISO `YYYY-MM-DD` — η **άφιξη**. Ανήκει στο διάστημα. */
  readonly checkIn: string;
  /** ISO `YYYY-MM-DD` — η **αναχώρηση**. **ΔΕΝ** ανήκει στο διάστημα. */
  readonly checkOut: string;
  /** Πόσα άτομα· `null` = **δεν ρωτήθηκε**, ποτέ «ένα». */
  readonly guests: number | null;
}

// =============================================================================
// 2. ΤΙ ΞΕΡΟΥΜΕ ΓΙΑ ΤΟ ΗΜΕΡΟΛΟΓΙΟ — το ΣΥΝΟΡΟ (ADR-835 §4.5)
// =============================================================================

/**
 * **Το ημερολόγιο ως ΡΗΤΗ ΕΙΣΟΔΟΣ** — και αυτός ο τύπος **είναι** η απόφαση του §3.2.
 *
 * ────────────────────────────────────────────────────────────────────────────
 * 🔴 ΓΙΑΤΙ ΕΙΣΟΔΟΣ ΚΑΙ ΟΧΙ ΑΝΑΓΝΩΣΗ ΜΕΣΑ ΣΤΗ ΜΗΧΑΝΗ
 * ────────────────────────────────────────────────────────────────────────────
 *
 * Το §4.5 απαγορεύει **ρητά** ημερολόγιο μέσα στο `PublicListing` (τρεις δεσμευτικοί
 * λόγοι). Οι κρατήσεις είναι **ιδιωτικές** και ο ανώνυμος **δεν** τις διαβάζει. Άρα ο
 * κριτής **δεν μπορεί** να τρέξει στον πελάτη πάνω σε δικά του δεδομένα — τρέχει στον
 * **διακομιστή**, και το αποτέλεσμα φτάνει εδώ ως **απάντηση**.
 *
 * 🔑 **Ίδιο ιδίωμα με το `ListingMatchFacts`**: *«ρητές είσοδοι αντί για άντληση μέσα
 * στη μηχανή… τα δηλωμένα κενά είναι **ορατά στην υπογραφή** αντί να είναι κρυμμένα σε
 * ένα `fetch` που κάποιος θα ανακαλύψει»*. Και ίδιο με τον κριτή: *«ο **καλών**
 * μεταφράζει το μοντέλο του σε `Occupancy` **πριν** φτάσει εδώ»*.
 *
 * ⚠️ **Το `'undeclared'` ΔΕΝ είναι κενός πίνακας καταλήψεων, και η σύγχυση των δύο
 * είναι το overbooking σε μορφή οθόνης.** Κενός πίνακας = *«ρωτήσαμε, δεν κρατά
 * κανείς»*. `undeclared` = *«κανείς δεν συνέδεσε ημερολόγιο»*. Ένα
 * `occupied: readonly Occupancy[]` και για τα δύο θα έλεγε **«ελεύθερο»** στη δεύτερη
 * περίπτωση — ακριβώς το *«το 0 σήμαινε ότι κανείς δεν κοίταξε»*.
 *
 * @typeParam TSource — η πηγή κάθε κατάληψης, **αδιαφανής** όπως στον κριτή. Στη Φ5
 *   θα είναι `StayBooking`· η μηχανή δεν το ξέρει και δεν πρέπει.
 */
export type StayCalendar<TSource> =
  /** **Κανείς δεν δήλωσε ημερολόγιο** για αυτό το κατάλυμα. Δεν είναι «ελεύθερο». */
  | { readonly kind: 'undeclared' }
  /**
   * Δηλωμένο ημερολόγιο. Οι καταλήψεις είναι **ήδη φιλτραρισμένες** από τον καλούντα
   * (`occupyingStays`): αιτήματα που δεν καταλαμβάνουν **δεν** φτάνουν εδώ (§6.1).
   */
  | { readonly kind: 'declared'; readonly occupied: readonly Occupancy<TSource>[] };

/**
 * **Η ΤΡΙΤΗ ΚΑΤΑΣΤΑΣΗ, ΩΣ ΕΙΣΟΔΟΣ** (§4.7 — απόφαση Giorgio 2026-08-31).
 *
 * 🔑 **Δεν χρειάζεται νέο πεδίο για να το ξέρουμε**: το `offerKinds` περιέχει **ήδη**
 * `sell` δίπλα στο `leaseShort`, άρα η συνύπαρξη είναι **δομικά** γνωστή. Αυτό εδώ
 * κουβαλά μόνο το ένα πράγμα που **δεν** προκύπτει από το σχήμα: **από πότε**.
 *
 * ⚠️ **`conditionalFrom: null` σημαίνει «από την αρχή», ΟΧΙ «καμία αίρεση».** Η
 * απουσία ημερομηνίας παράδοσης δεν κάνει την πώληση να μην υπάρχει — κάνει την
 * αίρεση **ευρύτερη**. Ένα `null` που διαβαζόταν ως «χωρίς αίρεση» θα ισοπέδωνε το
 * `conditional` σε `free`, που το §4.7 ονομάζει *«ψέμα προς τον επισκέπτη»*.
 */
export interface StaySaleExposure {
  /**
   * ISO `YYYY-MM-DD` — από πότε η διαμονή τελεί υπό αίρεση (ημερομηνία παράδοσης
   * δεσμευμένης πώλησης). `null` = **δεν ξέρουμε ημερομηνία** ⇒ όλο το παράθυρο.
   */
  readonly conditionalFrom: string | null;
}

// =============================================================================
// 3. Η ΑΠΑΝΤΗΣΗ — κλειστή ένωση, ποτέ `boolean`
// =============================================================================

/**
 * **Η ετυμηγορία για ΜΙΑ αγγελία, στις ημερομηνίες που ρωτήθηκαν.**
 *
 * ⚠️ **Κάθε τιμή είναι ΚΑΔΟΣ της λογιστικής** (`lib/listings/stay-ledger.ts`), και το
 * `balances()` απαιτεί να αθροίζουν στο **ίδιο σύνολο** με την υπάρχουσα λογιστική
 * θέσης. Δέκατη τιμή που θα προστεθεί χωρίς κάδο **δεν μεταγλωττίζεται** — ο ίδιος
 * φρουρός με το `STAY_LIFECYCLE_OCCUPIES`.
 */
export type StayAvailabilityAnswer =
  /**
   * **Δεν είναι κατάλυμα** — το `offerKinds` δεν έχει `leaseShort`.
   *
   * 🔑 **Δεν είναι αποτυχία, και γι' αυτό έχει δικό του όνομα.** Ο επισκέπτης που
   * ρωτά ημερομηνίες βλέπει επίσης ακίνητα προς **πώληση** στην ίδια περιοχή· αυτά
   * **δεν εξαφανίζονται** (θα ήταν σιωπηλό φίλτρο `offerKinds` που κανείς δεν ζήτησε)
   * και **δεν** λογίζονται «κρατημένα». Απλώς δεν απαντούν σε αυτή την ερώτηση.
   */
  | { readonly kind: 'not-a-stay' }
  /**
   * Κατάλυμα που **δεν δήλωσε χωρητικότητα**, ενώ ο επισκέπτης ρώτησε άτομα.
   *
   * ⚠️ **Ούτε «χωράει» ούτε «δεν χωράει»** (N.12). Ένα σιωπηλό «χωράει» στέλνει
   * τέσσερις ανθρώπους σε δυάρι· ένα σιωπηλό «δεν χωράει» σβήνει αγγελία που ίσως
   * ήταν η σωστή. Η θεραπεία ανήκει στον **κάτοχο**, και είναι ένα πεδίο.
   */
  | { readonly kind: 'terms-unknown' }
  /** Χωράει **λιγότερους**. **Μετρήσιμο** εμπόδιο: ο επισκέπτης ξέρει πόσο λείπει. */
  | { readonly kind: 'over-capacity'; readonly maxGuests: number; readonly asked: number }
  /** Δέχεται **περισσότερες** νύχτες. Μετρήσιμο, με τον ίδιο ακριβώς λόγο. */
  | { readonly kind: 'below-min-nights'; readonly minNights: number; readonly asked: number }
  /** **Κανείς δεν δήλωσε ημερολόγιο.** Δες την κεφαλίδα για το γιατί όχι `DemandBlocker`. */
  | { readonly kind: 'unknown' }
  /**
   * 🔴 **Το ημερολόγιο ΔΕΝ ΔΙΑΒΑΣΤΗΚΕ** — χαλασμένο διάστημα σε υπάρχουσα κατάληψη.
   *
   * Ποτέ ίδιο με `free` (§6.4): *«ένα ελεύθερο εκεί **είναι** το overbooking»*. Και
   * ποτέ ίδιο με {@link StayAvailabilityAnswer} `unknown`: εκείνο είναι **επιλογή του
   * κατόχου**, αυτό είναι **δικό μας χρέος** — ίδια διάκριση με `owner-declined` vs
   * `never-asked` (Α5), και μόνο το δεύτερο έχει θεραπεία που μας ανήκει.
   */
  | { readonly kind: 'unreadable' }
  /**
   * 🏆 **ΚΡΑΤΗΜΕΝΟ — ΚΑΙ ΛΕΜΕ ΤΙ ΑΠΟΜΕΝΕΙ.** Το σημείο όπου ξεπερνάμε την αγορά.
   *
   * Όλες οι πλατφόρμες **εξαφανίζουν** εδώ. Εμείς φέρνουμε **δύο** διεξόδους: το
   * `nextFreeFrom` (*«ελεύθερο από 14/08»* — το ίδιο εργαλείο με το `availableFrom`
   * του `MandateOccupancyNotice`) και τα `freeRuns` (*«χωράει 10–12/08 και
   * 15–17/08»*), που **κανείς** δεν προσφέρει: τα *split stays* της Airbnb ζευγαρώνουν
   * **δύο διαφορετικά** καταλύματα, ποτέ δεν κατατέμνουν το **ένα**.
   */
  | {
      readonly kind: 'occupied';
      /** Πότε ελευθερώνεται· `null` όταν η αναμονή δεν βοηθά (ανοιχτή διάρκεια). */
      readonly nextFreeFrom: string | null;
      /** Τα **μέγιστα συνεχή** ελεύθερα κομμάτια μέσα στο ζητούμενο. Μπορεί κενό. */
      readonly freeRuns: readonly StayNightRun[];
    }
  /**
   * **Ελεύθερο, ΑΛΛΑ το ακίνητο πωλείται** (§4.7).
   *
   * 🔴 **ΠΟΤΕ ισοπεδωμένο**: σε `free` είναι *«ψέμα προς τον επισκέπτη»*· σε
   * `occupied` είναι *«κλεμμένο εισόδημα από τον κάτοχο»*. Και η προειδοποίηση είναι
   * **γεγονός της κράτησης** (`StayBooking.riskDisclosedAt`), όχι κείμενο οθόνης.
   */
  | { readonly kind: 'conditional'; readonly conditionalFrom: string | null }
  /** Ελεύθερο, καμία επιφύλαξη. */
  | { readonly kind: 'free' };

/** Το **όνομα** μιας απάντησης — ο κάδος στον οποίο μετριέται. */
export type StayAvailabilityKind = StayAvailabilityAnswer['kind'];

/**
 * **Το κλειστό σύνολο των κάδων**, στη σειρά που τα κρίνει η μηχανή.
 *
 * ⚠️ **Η σειρά ΕΙΝΑΙ συμβόλαιο**, όπως στο `coverageStateOf`: τα φθηνά και σταθερά
 * (είδος · όροι) κρίνονται **πριν** το ημερολόγιο, γιατί ένα *«δεν χωράει 6 άτομα»*
 * είναι χρησιμότερο από ένα *«δεν ξέρω το ημερολόγιο»* — και ισχύει **ό,τι κι αν
 * λέει** το ημερολόγιο.
 */
export const STAY_AVAILABILITY_KINDS = [
  'not-a-stay',
  'terms-unknown',
  'over-capacity',
  'below-min-nights',
  'unknown',
  'unreadable',
  'occupied',
  'conditional',
  'free',
] as const satisfies readonly StayAvailabilityKind[];

/**
 * Οι κάδοι που σημαίνουν **«μπορείς να μείνεις»** — έστω και με επιφύλαξη.
 *
 * ⚠️ **Ονομασμένη σταθερά, όχι σιωπηλό `!==`** — ίδιο πρότυπο με το
 * `CATEGORICAL_BLOCKERS` και το `KINDS_WITHOUT_LEGACY_PROJECTION`: μια **δέκατη**
 * τιμή που θα προστεθεί χωρίς να χαρακτηριστεί θα λογιστεί σιωπηλά ως «όχι», και η
 * οθόνη θα κρύψει κατάλυμα που είναι διαθέσιμο.
 */
export const STAYABLE_AVAILABILITY_KINDS = [
  'conditional',
  'free',
] as const satisfies readonly StayAvailabilityKind[];

/** `true` αν αυτή η απάντηση επιτρέπει διαμονή στις ζητούμενες ημερομηνίες. */
export function isStayable(kind: StayAvailabilityKind): boolean {
  return (STAYABLE_AVAILABILITY_KINDS as readonly string[]).includes(kind);
}
