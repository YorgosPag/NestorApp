/**
 * ΤΟ ΣΥΝΟΡΟ ΤΟΥ ΧΑΡΤΗ — μία πόρτα, και η επιφάνεια βγαίνει ΜΑΖΙ με το στυλ της
 *
 * ─────────────────────────────────────────────────────────────────────────────
 * ΤΙ ΛΥΝΕΙ (ADR-777 §8.56)
 * ─────────────────────────────────────────────────────────────────────────────
 * Το `maplibre-gl.css` **δεν είναι διακόσμηση — είναι η ΔΙΑΤΑΞΗ του χάρτη**. Χωρίς
 * αυτό ο καμβάς μένει `position: static`, το `.maplibregl-map` μένει
 * `overflow: visible`, και το `div[mapboxgl-children]` της `react-map-gl`
 * (`height: 100%`, **στη ροή**) πέφτει **κάτω** από τον καμβά αντί να τον
 * επικαλύψει. Μετρημένο ζωντανά στο `/search/results`, 2026-09-06:
 * το δοχείο του χάρτη είχε **`scrollHeight 1702` μέσα σε κουτί `792`**.
 *
 * 🔴 **ΚΑΙ ΤΟ ΤΙΜΗΜΑ ΔΕΝ ΗΤΑΝ ΑΙΣΘΗΤΙΚΟ — ΗΤΑΝ Η ΕΞΑΦΑΝΙΣΗ ΤΟΥ ΧΑΡΤΗ.**
 * Η MapLibre μετακινεί **σκόπιμα** την εστίαση όταν ανοίγει popup
 * (`Popup.addTo → _focusFirstElement → element.focus()`, `focusAfterOpen`
 * προεπιλογή `true` — σωστή a11y: ο άνθρωπος που άνοιξε την κάρτα πρέπει να
 * μπορεί να την πλοηγήσει με πληκτρολόγιο). Ο περιηγητής τότε κυλά **κάθε**
 * πρόγονο-δοχείο για να φέρει το εστιασμένο στοιχείο σε θέα. Με τα 910px
 * περιθωρίου που γέννησε η απουσία του φύλλου στυλ, το `<section>` του χάρτη
 * κύλησε στο τέρμα (`scrollTop 1223.75`) και ο καμβάς πήγε στο **`y = −894`**:
 * ο χάρτης «μαύριζε», και **ένα scroll τον επανέφερε ακαριαία — γιατί τον κυλούσε
 * κυριολεκτικά πίσω**. Η ίδια παρενέργεια είναι καταγεγραμμένη ανοιχτά στη
 * MapLibre (issue #338).
 *
 * ─────────────────────────────────────────────────────────────────────────────
 * 🔑 ΓΙΑΤΙ ΣΥΝΟΡΟ ΚΑΙ ΟΧΙ «ΝΑ ΘΥΜΑΤΑΙ Ο ΚΑΘΕΝΑΣ ΤΟ import»
 * ─────────────────────────────────────────────────────────────────────────────
 * Το ελάττωμα **δεν ήταν ότι κάποιος ξέχασε** — ήταν ότι το «θυμήσου το» ήταν ο
 * μόνος μηχανισμός. Μετρημένο πριν γραφτεί γραμμή: **τέσσερα** αρχεία εισήγαγαν
 * το φύλλο στυλ χειρόγραφα (`PlaceMap`, `GeofenceConfigMap`, `LiveWorkerMap`,
 * `AddressMap`) και ο **κοινός** χάρτης — ο `InteractiveMapPresentation`, που
 * ζωγραφίζει το `<Map>` για **τρεις** καταναλωτές — **κανένα**. Δηλαδή ο χάρτης
 * δούλευε **μόνο** όταν τύχαινε να συνυπάρχει στο bundle με ξένο καταναλωτή που
 * είχε θυμηθεί. Στο `/search/results` δεν υπήρχε κανείς: ο χάρτης έτρεχε **χωρίς
 * καθόλου το φύλλο στυλ του** (`maplibreCssPresent: false`, μετρημένο στην οθόνη).
 *
 * ⇒ Εδώ η εγγύηση είναι **δομική, όχι πειθαρχία**: δεν μπορείς να πάρεις το
 * `Map`, το `Popup` ή το `Marker` χωρίς να έρθει μαζί το στυλ τους, γιατί
 * **περνούν από το ίδιο module**. Ίδιο σχήμα με το `@/lib/workspace/navigation`
 * (ADR-787), και το φυλάει πύλη με τον ίδιο τρόπο: **CHECK 3.75**.
 *
 * ─────────────────────────────────────────────────────────────────────────────
 * ⚠️ ΤΙ **ΔΕΝ** ΕΙΝΑΙ ΠΑΡΑΒΑΣΗ, ΚΑΙ ΓΙΑΤΙ
 * ─────────────────────────────────────────────────────────────────────────────
 * `import type { Map } from 'maplibre-gl'` — **επιτρεπτό ωμό**. Ένας τύπος
 * σβήνεται στη μεταγλώττιση: **δεν μπορεί να ξεχάσει φύλλο στυλ**, γιατί δεν
 * φτάνει ποτέ στον περιηγητή. Πύλη που τον κατήγγελλε θα κοκκίνιζε πάνω σε
 * **σωστό** κώδικα — ο δρόμος προς το `SKIP_`. Το ίδιο κριτήριο έχει ήδη το
 * CHECK 3.61 (`clause.isTypeOnly` ⇒ `continue`).
 *
 * ─────────────────────────────────────────────────────────────────────────────
 * 🏆 ΠΟΥ ΞΕΠΕΡΝΑΜΕ ΤΗΝ ΠΡΑΚΤΙΚΗ ΤΩΝ ΜΕΓΑΛΩΝ
 * ─────────────────────────────────────────────────────────────────────────────
 * Η επίσημη οδηγία **MapLibre**, **Mapbox GL** και **react-map-gl** είναι, κατά
 * λέξη, «include the CSS file» — δηλαδή **οδηγία σε ανθρώπινη μνήμη**, ακριβώς
 * το σχήμα που εδώ έχει αποτύχει μετρημένα οκτώ φορές. Η κοινότητα το λύνει με
 * `focusAfterOpen: false` (issue #338), δηλαδή **σβήνοντας την a11y** για να μη
 * φανεί ένα ελάττωμα διάταξης. Εδώ γίνεται το αντίστροφο: **η εστίαση μένει
 * άθικτη** και η επιφάνεια γίνεται **δομικά ανίκανη να κυλήσει**
 * (`overflow: clip` στο `src/app/foreign-boundary.css`) — δύο ανεξάρτητοι
 * κρίκοι σπασμένοι, όχι ένας παρακαμμένος.
 *
 * @module lib/maps/maplibre
 */

/**
 * 🔑 **Η ΜΙΑ ΓΡΑΜΜΗ ΠΟΥ ΔΙΚΑΙΟΛΟΓΕΙ ΤΟ ΑΡΧΕΙΟ.** Επιτρέπεται σε **οποιοδήποτε**
 * component του App Router να εισάγει global CSS από `node_modules` — σε αντίθεση
 * με τον Pages Router, όπου ζούσε μόνο στο `_app`. Άρα το στυλ φορτώνεται
 * **ακριβώς στις σελίδες που έχουν χάρτη**, ποτέ στο root layout: μηδέν κόστος
 * στις ~180 σελίδες χωρίς χάρτη.
 */
import 'maplibre-gl/dist/maplibre-gl.css';

/**
 * Η **δηλωτική** επιφάνεια (React): `Map`, `Source`, `Layer`, `Marker`, `Popup`,
 * τα controls, το `useMap`/`useControl`/`MapProvider` — και **όλοι** οι τύποι τους.
 *
 * ⚠️ **`export *` επίτηδες, όχι χειρόγραφη λίστα.** Μια λίστα θα ήταν **δεύτερο
 * αντίγραφο** της δημόσιας επιφάνειας της βιβλιοθήκης, με μηδέν μηχανισμό να την
 * κρατά ίδια: κάθε νέο σύμβολο θα έπρεπε να γραφτεί δεύτερη φορά και η παράλειψη
 * **δεν θα ήταν σφάλμα** — θα ήταν σύμβολο που ο καλών δεν μπορεί να πάρει,
 * σιωπηλά. Είναι το ίδιο ελάττωμα που το jscpd έπιασε στο `InteractiveMap.tsx`.
 *
 * ⚠️ Η μορφή είναι **ταυτόσημη** με το `react-map-gl/maplibre`, που είναι κι αυτό
 * `export * from '@vis.gl/react-maplibre'` ⇒ **καμία** αλλαγή στη δυνατότητα
 * tree-shaking σε σχέση με σήμερα.
 */
export * from 'react-map-gl/maplibre';

/**
 * Η **προστακτική** επιφάνεια (η ίδια η `maplibre-gl`) — μόνο ό,τι επιβιώνει ως
 * **τιμή** στον περιηγητή, άρα μόνο ό,τι μπορεί να ξεχάσει το φύλλο στυλ.
 *
 * ⚠️ Το `Marker` μετονομάζεται: το ομώνυμο της `react-map-gl` είναι **React
 * component**, αυτό εδώ είναι **κλάση που την κατασκευάζεις ο ίδιος**. Δύο
 * απαντήσεις σε δύο ερωτήματα — ένα κοινό όνομα θα έκρυβε τη διαφορά.
 */
export { LngLatBounds, Marker as MapLibreMarker } from 'maplibre-gl';
