/**
 * @fileoverview **ΣΗΜΑΙΝΕΙ ΑΥΤΗ Η ΔΙΑΘΕΣΗ ΟΤΙ ΤΟ ΑΚΙΝΗΤΟ ΔΕΝ ΕΙΝΑΙ ΑΚΟΜΗ ΑΠΟΠΕΡΑΤΩΜΕΝΟ;**
 * @related ADR-842 §7 (Φ5) · §8 #1 · ADR-287 (μηχανή πληρότητας) · ADR-749 §5
 * @module lib/property/property-completion-stance
 *
 * ────────────────────────────────────────────────────────────────────────────
 * 🔴 ΤΟ ΑΝΟΙΧΤΟ ΘΕΜΑ §8 #1, ΚΑΙ ΓΙΑΤΙ Η ΣΙΩΠΗ ΗΤΑΝ Η ΜΟΝΗ ΑΠΑΓΟΡΕΥΜΕΝΗ ΑΠΑΝΤΗΣΗ
 * ────────────────────────────────────────────────────────────────────────────
 *
 * Η μηχανή του ADR-287 ρωτά `operationalStatus` για **δύο** εξαιρέσεις:
 * `draft` ⇒ κρύψιμο του δείκτη, `under-construction` ⇒ τα φινιρίσματα, τα συστήματα
 * και η ενεργειακή κλάση **βγαίνουν από τον παρονομαστή** και οι φωτογραφίες μετρούν
 * μισές. Το `PublicListing` **δεν έχει** `operationalStatus`: είναι έννοια του κύκλου
 * ζωής μιας **εταιρικής** μονάδας, όχι μιας δημοσιευμένης αγγελίας.
 *
 * Το §8 #1 απαιτούσε **απόφαση**, με μία ρητή απαγόρευση: *«Σιωπηλό `undefined`
 * απαγορεύεται»*. Αυτό το αρχείο **είναι** η απόφαση, και ο λόγος που είναι πίνακας
 * και όχι `if` δίνεται παρακάτω.
 *
 * ────────────────────────────────────────────────────────────────────────────
 * 🔑 ΤΟ ΚΟΣΤΟΣ ΕΙΝΑΙ **ΑΣΥΜΜΕΤΡΟ**, ΚΑΙ ΓΙ' ΑΥΤΟ Η ΕΞΑΙΡΕΣΗ ΝΙΚΑΕΙ ΤΗΝ ΑΥΣΤΗΡΟΤΗΤΑ
 * ────────────────────────────────────────────────────────────────────────────
 *
 * Η εξαίρεση αφαιρεί **μόνο την ποινή** — τα πεδία παραμένουν ορατά και δηλώσιμα στην
 * οθόνη. Άρα:
 *
 * | Λάθος | Τι παθαίνει ο άνθρωπος |
 * |---|---|
 * | εξαιρέσαμε **αποπερατωμένο** ακίνητο | βλέπει ελαφρώς ευνοϊκότερο ποσοστό· τα πεδία **εξακολουθούν** να του ζητούνται |
 * | **δεν** εξαιρέσαμε **ανέγερτο** ακίνητο | του ζητάμε **ΠΕΑ που ο νόμος δεν εκδίδει** πριν την αποπεράτωση |
 *
 * Η δεύτερη γραμμή είναι coaching που **δεν μπορεί να εκτελεστεί** — δηλαδή ο δείκτης
 * παύει να είναι κίνητρο και γίνεται κατηγορητήριο. Απόφαση Giorgio (2026-09-02):
 * το «σύντομα διαθέσιμο» βαθμολογείται **σαν `under-construction`**.
 *
 * ────────────────────────────────────────────────────────────────────────────
 * 🔴 ΜΕΤΡΗΜΕΝΟ 2026-09-02: ΤΟ «coming-soon» ΔΕΝ ΕΙΝΑΙ `CommercialStatus` — ΚΑΙ ΟΜΩΣ ΦΤΑΝΕΙ
 * ────────────────────────────────────────────────────────────────────────────
 *
 * Το σχέδιο της Φ5 έλεγε *«το `commercialStatus: 'coming-soon'`»*. **Ο κώδικας
 * διαφωνεί**: το `CommercialStatus` (`constants/commercial-statuses.ts`) έχει **επτά**
 * τιμές και το «coming-soon» **δεν είναι** μία από αυτές. Ζει στο `PropertyStatus`
 * (`constants/domains/property-status-core.ts`) — **δεύτερο, παλαιότερο λεξιλόγιο**
 * για τη διάθεση, με **δώδεκα** τιμές.
 *
 * ⚠️ **Και όμως φτάνει στη δημόσια αγγελία**, γιατί ο γραφέας το λαθρεμπορεύει:
 * το `public-listing-projection.ts:307` γράφει
 * `(property.commercialStatus ?? property.status ?? 'unavailable') as CommercialStatus`
 * — ένα ωμό `as` πάνω σε `property.status`, που είναι **`PropertyStatus`**. Άρα το
 * πεδίο είναι τυπωμένο ως ένωση **επτά** τιμών και μπορεί να κρατά **δώδεκα**: η ίδια
 * κλάση ελαττώματος που η Φ3 έκλεισε για τα δεκατρία λεξιλόγια χαρακτηριστικών,
 * αφημένη ανοιχτή για τη **διάθεση**. Καταγράφηκε στο
 * `.claude-rules/pending-ratchet-work.md` αντί να διορθωθεί εδώ: η διόρθωση αλλάζει
 * **ποιες τιμές υπάρχουν δημόσια**, άρα αγγίζει το `listing-filters.ts` — αρχείο
 * **άλλης συνεδρίας**.
 *
 * 🔑 **Γι' αυτό ο πίνακας είναι πάνω στο `PropertyStatus` και όχι στο
 * `CommercialStatus`**: το δεύτερο θα ήταν **φρουρός χωρίς απόδειξη ζωής** (ADR-749 §5)
 * — μια γραμμή «coming-soon» που ο μεταγλωττιστής θα απέρριπτε, και ένα `if` που δεν
 * θα πυροδοτούσε **ποτέ**. Ο πίνακας πάνω στο υπερσύνολο καλύπτει **κάθε** τιμή που
 * μπορεί πράγματι να φτάσει, και ότι `PropertyStatus ⊇ CommercialStatus` το επιβάλλει
 * ο μεταγλωττιστής (άγκυρα).
 *
 * ⛔ **ΚΑΜΙΑ ΤΙΜΗ ΔΕΝ ΧΑΡΤΟΓΡΑΦΕΙΤΑΙ ΠΟΤΕ ΣΕ `'draft'`.** Το `draft` δεν είναι
 * εξαίρεση παρονομαστή — είναι **σβήσιμο του δείκτη** (`shouldHide`). Μια δημοσιευμένη
 * αγγελία δεν είναι πρόχειρο **εξ ορισμού**: αν ήταν, δεν θα υπήρχε δημόσιο έγγραφο να
 * τη βαθμολογήσει κανείς. Άγκυρα με μετάλλαξη το φυλάει.
 */

import type { OperationalStatus } from '@/constants/operational-statuses';
import type { PropertyStatus } from '@/constants/domains/property-status-core';

// ============================================================================
// 1. Η ΣΤΑΣΗ — δύο τιμές, γιατί δύο είναι τα ερωτήματα που απαντά η μηχανή
// ============================================================================

/**
 * Πώς μετρά ο παρονομαστής μιας **δημοσιευμένης** αγγελίας.
 *
 * - `'full'` — όλα τα πεδία του τύπου μετρούν. Η προεπιλογή, και η αλήθεια για κάθε
 *   ακίνητο που **υπάρχει** ήδη.
 * - `'pre-completion'` — φινιρίσματα / συστήματα / ΠΕΑ **δεν υπάρχουν ακόμη**, άρα δεν
 *   είναι έλλειψη· φωτογραφίες σκελετού αξίζουν μισά.
 */
export type CompletionStance = 'full' | 'pre-completion';

/**
 * **Κάθε διάθεση, και τι λέει για την αποπεράτωση.** Εξαντλητικό `Record` — μια νέα
 * τιμή στο `PropertyStatus` **δεν μεταγλωττίζεται** μέχρι κάποιος να απαντήσει.
 *
 * 🔑 Το ιδίωμα είναι το `ATTRIBUTE_DECLARED` της Φ3: η εγγύηση εξαντλητικού `switch`,
 * χωρίς να είναι συνάρτηση.
 *
 * ⚠️ **Το «landowner» είναι `'full'`, και είναι απόφαση.** Λέει **ποιος κατέχει** τη
 * μονάδα (αντιπαροχή), **όχι** αν χτίστηκε. Μια μονάδα οικοπεδούχου σε παραδομένο
 * κτήριο είναι πλήρως αποπερατωμένη. Εξαίρεση εδώ θα ήταν εικασία για ερώτημα που
 * αυτό το λεξιλόγιο δεν απαντά.
 */
const COMPLETION_STANCE: Record<PropertyStatus, CompletionStance> = {
  // ── Ενεργή διάθεση αποπερατωμένου ακινήτου ────────────────────────────────
  'for-sale': 'full',
  'for-rent': 'full',
  'for-sale-and-rent': 'full',
  'reserved': 'full',
  'under-negotiation': 'full',

  // ── Ολοκληρωμένη συναλλαγή ────────────────────────────────────────────────
  'sold': 'full',
  'rented': 'full',

  // ── Εκτός αγοράς ──────────────────────────────────────────────────────────
  'off-market': 'full',
  'unavailable': 'full',
  'deleted': 'full',

  // ── Ιδιοκτησία, όχι αποπεράτωση (δες την κεφαλίδα) ────────────────────────
  'landowner': 'full',

  // ── 🔴 Η ΜΙΑ ΓΡΑΜΜΗ ΤΗΣ ΑΠΟΦΑΣΗΣ ──────────────────────────────────────────
  /**
   * «Σύντομα διαθέσιμο» = ακίνητο **υπό κατασκευή** που δημοσιεύεται ήδη. Απόφαση
   * Giorgio 2026-09-02, με το ασύμμετρο κόστος της κεφαλίδας ως λόγο.
   */
  'coming-soon': 'pre-completion',
};

// ============================================================================
// 2. Ο ΜΕΤΑΦΡΑΣΤΗΣ — από τη διάθεση στη γλώσσα της μηχανής
// ============================================================================

/**
 * Η στάση πληρότητας για μια διάθεση. Άγνωστη συμβολοσειρά ⇒ `'full'`.
 *
 * 🔑 **Η προεπιλογή είναι η αυστηρή, επίτηδες.** Μια τιμή που κανείς δεν αναγνωρίζει
 * δεν είναι άδεια για έκπτωση στον παρονομαστή: το «δεν ξέρω» δεν γίνεται «μην
 * ρωτήσεις». Ίδιο σκεπτικό με το `null` της Φ3 για μη ονομάσιμο λεξιλόγιο.
 */
export function completionStanceFor(status: string | null | undefined): CompletionStance {
  if (typeof status !== 'string') return 'full';
  return COMPLETION_STANCE[status as PropertyStatus] ?? 'full';
}

/**
 * Η **λειτουργική κατάσταση** που πρέπει να δει η μηχανή του ADR-287 για μια
 * δημοσιευμένη αγγελία με αυτή τη διάθεση.
 *
 * ⛔ **Δεν επιστρέφει ΠΟΤΕ `'draft'`** — δες την κεφαλίδα. Ο τύπος επιστροφής το κάνει
 * ρητό: `undefined` σημαίνει *«δεν είναι προ-αποπεράτωσης»*, όχι *«δεν ρωτήθηκε»*, και
 * η διαφορά είναι γραμμένη **εδώ** αντί να υπονοηθεί στον καλούντα (§8 #1).
 */
export function operationalStatusForListing(
  status: string | null | undefined,
): Extract<OperationalStatus, 'under-construction'> | undefined {
  return completionStanceFor(status) === 'pre-completion' ? 'under-construction' : undefined;
}
