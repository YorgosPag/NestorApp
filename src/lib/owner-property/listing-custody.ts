/**
 * @fileoverview **ΠΟΙΟΥ ΕΙΝΑΙ Ο ΧΩΡΟΣ ΤΗΣ ΑΓΓΕΛΙΑΣ — ΚΑΙ ΠΟΙΟΣ ΕΧΕΙ ΔΙΚΑΙΩΜΑ ΣΕ ΑΥΤΟΝ.**
 * @related ADR-777 §8.39 · types/owner-property.ts · services/owner-property/owner-property-write.service.ts
 * @module lib/owner-property/listing-custody
 *
 * ────────────────────────────────────────────────────────────────────────────
 * 🔴 ΤΟ ΕΛΑΤΤΩΜΑ: ΕΝΑ ΕΡΩΤΗΜΑ, ΔΥΟ ΥΛΟΠΟΙΗΣΕΙΣ, ΚΑΙ ΜΙΑ ΠΡΑΞΗ ΠΟΥ ΕΛΕΙΠΕ
 * ────────────────────────────────────────────────────────────────────────────
 *
 * Μέχρι τις 2026-08-21 η ερώτηση *«επιτρέπεται σε αυτόν τον άνθρωπο να διαχειριστεί
 * αυτή την αγγελία;»* απαντιόταν σε **δύο** σημεία, με **δύο** κριτήρια:
 *
 * | Πού | Κριτήριο | Ποιον εξυπηρετεί |
 * |---|---|---|
 * | `loadOwned` (owner-property-write.service) | `authorUserId === uid` | τον **ιδιώτη** |
 * | `prepare` (mandate-actions.service) | `authorCompanyId === companyId` | το **γραφείο** |
 *
 * Και τα δύο ήταν **σωστά για τη δική τους πόρτα**. Το πρόβλημα ήταν ότι το **πλήρες**
 * σύνολο των πράξεων υπήρχε μόνο στην πρώτη: το γραφείο μπορούσε να ξαναστείλει ή να
 * ανακαλέσει **σύνδεσμο**, αλλά **δεν μπορούσε να αποσύρει την αγγελία συναδέλφου** —
 * η απόσυρση ζούσε πίσω από κριτήριο **κατά χρήστη**, σε αγγελία που ανήκει σε
 * **εταιρεία**. Ο υπάλληλος έφευγε, η αγγελία έμενε ζωντανή, και **κανείς** στο γραφείο
 * δεν μπορούσε να την κατεβάσει.
 *
 * ⚠️ **Η προηγούμενη τεκμηρίωση απέρριπτε ρητά την ενοποίηση** («*δύο απαντήσεις στο
 * ίδιο αρχείο θα ήταν δύο δόγματα εξουσιοδότησης σε μία πόρτα*»). Η ένσταση ήταν
 * **σωστή για τη λύση που φανταζόταν** — «ή δικό σου ή της εταιρείας σου», που όντως
 * ανακατεύει δύο δόγματα. Αυτό εδώ **δεν** είναι εκείνο: υπάρχει **ΕΝΑ** δόγμα, και ο
 * χώρος του πόρου είναι που επιλέγει ποιος το ικανοποιεί.
 *
 * ────────────────────────────────────────────────────────────────────────────
 * 🏆 ΤΟ ΜΟΝΤΕΛΟ ΕΙΝΑΙ ΔΑΝΕΙΟ, ΟΧΙ ΕΦΕΥΡΕΣΗ — ΚΑΙ ΟΙ ΤΡΕΙΣ ΠΗΓΕΣ ΣΥΜΦΩΝΟΥΝ
 * ────────────────────────────────────────────────────────────────────────────
 *
 * · **Google Drive / Zanzibar (ReBAC)**: ο πόρος δηλώνει **δοχείο** (`parent`) και το
 *   δικαίωμα προκύπτει από **διάσχιση** της σχέσης — «*a folder resource declaring a
 *   child-parent relationship with a file resource*». Δεν ρωτάς «ποιος το έφτιαξε;»·
 *   ρωτάς «**σε ποιον χώρο ζει, και έχεις δικαίωμα εκεί;**».
 *
 * · **Figma**: το ίδιο δίδυμο — *Drafts* (ιδιωτικά, **μόνο** ο κάτοχος: «*only the user
 *   who owns the Drafts folder can move files out of it*») έναντι *team files*. Και το
 *   **μάθημά** τους είναι ακριβώς το δικό μας: το παλιό μοντέλο, όπου τα drafts ζούσαν
 *   **έξω** από ομάδα, «*causes unclear ownership, lack of team association*» — γι' αυτό
 *   το άλλαξαν ώστε **κάθε** αρχείο να δηλώνει χώρο.
 *
 * · **MLS**: «*listings belong to the **broker**, not the agent*» — όταν ο υπάλληλος
 *   αλλάξει γραφείο, οι αγγελίες **μένουν**. Αυτό το γράφει **ήδη** το
 *   `app/(app)/listings/mandates/new/page.tsx` και το `types/owner-property.ts`· απλώς
 *   δεν το **εκτελούσε** καμία διαδρομή εγγραφής.
 *
 * 🔑 **ΠΟΥ ΠΑΜΕ ΠΑΡΑΠΕΡΑ ΑΠΟ ΤΟΥΣ ΤΡΕΙΣ.** Και στα τρία συστήματα ο χώρος είναι
 * **ξεχωριστό αντικείμενο** που ο χρήστης διαλέγει (φάκελος · ομάδα · γραφείο), άρα
 * μπορεί να **αποκλίνει** από το περιεχόμενο. Εδώ η θεματοφυλακή είναι **ΠΑΡΑΓΩΓΗ**
 * (`custodyOf`) από πεδία που **ήδη γράφει η πύλη δημιουργίας** και **κανείς δεν
 * μπορεί να στείλει από το δίκτυο** — άρα δεν υπάρχει κατάσταση «αρχείο σε λάθος
 * φάκελο». Το Figma χρειάστηκε **μετανάστευση** για να το πετύχει αυτό· εδώ είναι
 * **αδύνατο εξ ορισμού**.
 *
 * ⚠️ **ΔΕΝ ΕΙΝΑΙ ΔΕΥΤΕΡΗ ΑΥΘΕΝΤΙΑ ΔΙΠΛΑ ΣΤΟ `lib/auth/tenant-ownership.ts` — ΑΛΛΗ ΕΡΩΤΗΣΗ.**
 * Εκείνο ρωτά «ανήκει το έγγραφο στον **tenant** που ρωτάει;» και διαβάζει το πεδίο
 * `companyId` — το πεδίο **απομόνωσης**. Το `owner_properties` **δεν έχει** τέτοιο πεδίο: το
 * `tenant-config.ts` το δηλώνει `mode: 'userId'`, και το `types/owner-property.ts` γράφει
 * ρητά ότι το `authorCompanyId` **δεν είναι δεύτερο πεδίο απομόνωσης — είναι ΑΠΟΔΟΣΗ**.
 * Ένα `isOwnedByCompany(property, …)` εδώ θα διάβαζε πεδίο που **δεν υπάρχει** και θα
 * απαντούσε πάντα **όχι** — πράσινος και ανενεργός φρουρός.
 *
 * | Μονάδα | Ερώτηση | Πεδίο |
 * |---|---|---|
 * | `lib/auth/tenant-ownership` | «του tenant μου;» (απομόνωση) | `companyId` |
 * | **αυτό** | «σε ποιον **χώρο** ζει, και έχω δικαίωμα εκεί;» (διαχείριση) | `authorUserId` + `authorCompanyId` |
 */

import type { OwnerProperty } from '@/types/owner-property';

/**
 * **Ο χώρος στον οποίο ζει η αγγελία.** Δύο είδη, γιατί το προϊόν έχει δύο χώρους:
 * το `(me)` («ο ΙΔΙΩΤΙΚΟΣ ΧΩΡΟΣ ΤΟΥ ΙΔΙΩΤΗ», `.shell-boundary.json`) και το `(app)`
 * (ο χώρος του γραφείου).
 *
 * ⚠️ **Διακριτή ένωση και όχι δύο προαιρετικά πεδία**: ένα `{userId?, companyId?}` θα
 * επέτρεπε **και τα δύο** ή **κανένα** — δύο καταστάσεις που δεν υπάρχουν στον κόσμο,
 * και που κάθε καταναλωτής θα έπρεπε να θυμάται να αποκλείσει.
 */
export type ListingCustody =
  | { readonly kind: 'personal'; readonly userId: string }
  | { readonly kind: 'company'; readonly companyId: string };

/**
 * **Ποιος ρωτά** — η ταυτότητα του συνδεδεμένου, όπως τη δίνει το `withAuth`.
 *
 * ⚠️ Το `companyId` είναι `string | null` γιατί ο **ιδιώτης δεν έχει εταιρεία**. Ένα
 * `''` ή ένα `undefined` θα μπορούσε κατά λάθος να ταιριάξει με έγγραφο που κι εκείνο
 * έχει κενή τιμή — γι' αυτό το {@link mayAdminister} απορρίπτει ρητά το `null`.
 */
export interface ListingActor {
  readonly uid: string;
  readonly companyId: string | null;
}

/** Τα δύο πεδία που καθορίζουν τον χώρο — και **τίποτε άλλο**. */
type CustodyFields = Pick<OwnerProperty, 'authorUserId' | 'authorCompanyId'>;

/**
 * **Ο χώρος της αγγελίας, παραγόμενος.**
 *
 * 🔑 **Το `authorCompanyId` ΕΙΝΑΙ ο διακρίτης, και ήταν πάντα.** Το
 * `types/owner-property.ts` το ονομάζει «**ΑΠΟΔΟΣΗ**» και όχι δεύτερο πεδίο απομόνωσης
 * — σωστά, γιατί η απομόνωση (ποιος **βλέπει**) μένει στο `authorUserId`. Εδώ
 * απαντιέται άλλη ερώτηση: ποιος **διαχειρίζεται**. Δύο ερωτήσεις, ένα πεδίο, καμία
 * αντίφαση.
 *
 * ⚠️ **ΜΗΝ το κάνεις `mandate.kind === 'brokered'`**: η εντολή είναι **σχέση με τον
 * ιδιοκτήτη** (ποιος επιτρέπει την πώληση), η θεματοφυλακή είναι **χώρος εργασίας**
 * (ποιος διαχειρίζεται την εγγραφή). Σήμερα συμβαδίζουν· η μέρα που θα αποκλίνουν
 * (π.χ. γραφείο που καταχωρεί δικό του απόθεμα χωρίς εντολή) δεν πρέπει να τις βρει
 * δεμένες.
 */
export function custodyOf(property: CustodyFields): ListingCustody {
  return property.authorCompanyId === null
    ? { kind: 'personal', userId: property.authorUserId }
    : { kind: 'company', companyId: property.authorCompanyId };
}

/**
 * **Υπάρχει πραγματικά εταιρεία εδώ;** — κενό δεν είναι tenant, είναι **απουσία** tenant.
 *
 * 🔴 **ΓΕΝΝΗΘΗΚΕ ΑΠΟ ΜΕΤΑΛΛΑΞΗ ΠΟΥ ΒΓΗΚΕ ΠΡΑΣΙΝΗ.** Η πρώτη γραφή φρουρούσε
 * `actor.companyId !== null` — φρουρός **αδρανής**: το `custody.companyId` είναι
 * `string` εξ ορισμού, οπότε το `AGENCY === null` ήταν ήδη `false` και η αφαίρεση του
 * ελέγχου **δεν άλλαζε καμία ετυμηγορία** (ADR-749 §5, «606 αδρανείς φρουροί»).
 *
 * Ο πραγματικός κίνδυνος ήταν **άλλος** και τον ονομάζει ήδη το
 * `lib/auth/tenant-ownership.ts`: *«καλών με `companyId: ''` (χαλασμένο token) και
 * έγγραφο με `companyId: ''` θα “ταίριαζαν” σε αφελή σύγκριση `===`»*. Ίδια παγίδα,
 * άλλη συλλογή — και η πρώτη γραφή **έπεφτε μέσα της**.
 */
function hasTenant(value: string | null): value is string {
  return typeof value === 'string' && value.length > 0;
}

/**
 * **Επιτρέπεται σε αυτόν τον άνθρωπο να διαχειριστεί ό,τι ζει σε αυτόν τον χώρο;**
 *
 * ⚠️ **Ο ιδιωτικός χώρος ΔΕΝ διευρύνεται ποτέ.** Μια προσωπική αγγελία απαντά **μόνο**
 * στον συγγραφέα της, ακόμη κι αν εκείνος ανήκει σε εταιρεία — αλλιώς το γραφείο ενός
 * υπαλλήλου θα αποκτούσε δικαίωμα πάνω στο **σπίτι του**, που είναι ακριβώς η διαρροή
 * που το `(me)` υπάρχει για να αποκλείσει (και ο κανόνας Figma για τα Drafts).
 *
 * ⚠️ **Κενή εταιρεία δεν ταιριάζει με ΤΙΠΟΤΑ — ούτε με κενή.** Έγγραφο με
 * `authorCompanyId: ''` δεν ανήκει σε κανέναν και **δεν διαχειρίζεται από κανέναν**:
 * fail-closed, ίδιο δόγμα με το {@link isPayloadOwnedByCompany}. Το κενό δεν προκύπτει
 * από καμία σημερινή διαδρομή γραφής (η πύλη γράφει `ctx.companyId` ή `null`) — γι'
 * αυτό ακριβώς ο έλεγχος πρέπει να **υπάρχει**: μια αστοχία που «δεν μπορεί να συμβεί»
 * και δεν φρουρείται, συμβαίνει σιωπηλά την ημέρα που θα αλλάξει η διαδρομή.
 */
export function mayAdminister(custody: ListingCustody, actor: ListingActor): boolean {
  if (custody.kind === 'personal') return custody.userId === actor.uid;
  return hasTenant(custody.companyId) && hasTenant(actor.companyId)
    && custody.companyId === actor.companyId;
}

/**
 * **Ζει αυτή η αγγελία στον ιδιωτικό χώρο;** — το ερώτημα του `(me)`.
 *
 * 🔴 Υπάρχει επειδή ο κατάλογος «τα ακίνητά μου» ρωτούσε `authorUserId === uid`, που
 * είναι **η ερώτηση της απομόνωσης**, όχι του **χώρου**: ο μεσίτης έβλεπε την **εταιρική**
 * αγγελία που κατέγραψε ο ίδιος μέσα στον **προσωπικό** του χώρο — ενώ το
 * `.shell-boundary.json` δηλώνει εκείνον τον χώρο «ιδιωτικό του ιδιώτη». Η δήλωση είναι
 * συμβόλαιο (CHECK 3.52)· η προβολή όφειλε να το τηρεί.
 */
export function isPersonalCustody(property: CustodyFields): boolean {
  return custodyOf(property).kind === 'personal';
}
