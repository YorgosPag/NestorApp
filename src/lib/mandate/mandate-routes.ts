/**
 * @fileoverview **Οι διευθύνσεις του ΓΡΑΦΕΙΟΥ** — γραμμένες μία φορά.
 * @related ADR-777 §8.33 · §8.34 · §8.12 (CHECK 3.52) · lib/owner-property/owner-property-routes.ts
 * @module lib/mandate/mandate-routes
 *
 * ────────────────────────────────────────────────────────────────────────────
 * 🔴 ΞΕΧΩΡΙΣΤΟ ΑΡΧΕΙΟ ΑΠΟ ΤΟ `owner-property-routes.ts` — ΑΛΛΟ ΑΚΡΟΑΤΗΡΙΟ
 * ────────────────────────────────────────────────────────────────────────────
 *
 * Εκείνο κρατά τις διευθύνσεις του **ιδιώτη** (`/offers`, `(me)`): προσωπικός χώρος,
 * απομόνωση **κατά άνθρωπο**. Αυτές είναι του **γραφείου** (`/listings/…`, `(app)`):
 * εταιρικός χώρος, όπου *«listings belong to the **broker**, not the agent»* και ο
 * κατάλογος **επιβιώνει όταν ο υπάλληλος φύγει**.
 *
 * ⚠️ Δύο σύνολα με **αντίθετο** συμβόλαιο ιδιοκτησίας σε ένα αρχείο σημαίνει ότι η
 * επόμενη προσθήκη διαλέγει ακροατήριο **σιωπηλά** — και εδώ «σιωπηλά» θα σήμαινε
 * αγγελία εταιρείας μέσα στον ιδιωτικό χώρο, δηλαδή η δήλωση του
 * `.shell-boundary.json` να λέει ψέματα (CHECK 3.52).
 *
 * **Layering**: leaf — μόνο σταθερές.
 */

/**
 * **Ο κατάλογος εντολών** — τι περιμένει το γραφείο, και από ποιον (§8.34).
 *
 * 🔑 **Πληθυντικός και γονέας**: το `/listings/mandates/new` υπήρχε **πριν** από
 * αυτόν, ορφανό — καμία γραμμή του δέντρου δεν έδειχνε σε αυτό (μετρημένο 2026-08-21).
 * Ο κατάλογος είναι ο φυσικός του γονέας, και ο μόνος λόγος που η πόρτα καταχώρησης
 * αποκτά **διάδρομο**.
 */
export const MANDATE_CATALOG_ROUTE = '/listings/mandates' as const;

/**
 * **Η ΔΙΕΥΘΥΝΣΗ ΜΙΑΣ ΕΝΤΟΛΗΣ** — η μία, όχι η λίστα (ADR-841 §7 **Α18.12**).
 *
 * ────────────────────────────────────────────────────────────────────────────
 * 🔴 ΓΙΑΤΙ ΓΕΝΝΗΘΗΚΕ: Η ΕΙΔΟΠΟΙΗΣΗ ΕΙΧΕ ΠΡΟΟΡΙΣΜΟ, ΑΛΛΑ ΟΧΙ ΤΟΝ ΔΙΚΟ ΤΗΣ
 * ────────────────────────────────────────────────────────────────────────────
 *
 * Ο `mandate-decision-notifier` έστελνε τον **υπάλληλο του γραφείου** στο
 * `(me)/offers/<ownp>` — την οθόνη του **ιδιώτη**. Ανοίγει *(ο παραλήπτης **είναι** ο
 * `authorUserId`, μετρημένο)*, αλλά είναι η καρτέλα *«η αγγελία μου»*, όχι η εντολή.
 *
 * **Και ο κατάλογος δεν ήταν η απάντηση**, με δύο μετρήσεις:
 *
 * 1. Ο {@link MANDATE_CATALOG_ROUTE} κόβεται στις **500** γραμμές *(`MANDATE_CATALOG_CAP`,
 *    με σημαία `truncated`)* ⇒ η **ανακοινωμένη** εντολή μπορεί να μην είναι εκεί, και
 *    η απουσία της θα ήταν **σιωπηλή**. Είναι η κλάση ελαττώματος που έκλεισε η Α18.9.
 * 2. Ο κατάλογος ταξινομείται με **επείγον**, και μια μόλις εγκεκριμένη εντολή γίνεται
 *    `live` — **δέκατη και τελευταία** από τις δέκα καταστάσεις. Ο άνθρωπος που έμαθε
 *    *«ο πελάτης ενέκρινε»* θα προσγειωνόταν στο **κάτω μέρος** μιας οθόνης τριάζ.
 *    🔑 Δεν είναι κακή ταξινόμηση — είναι **σωστή ταξινόμηση για άλλη ερώτηση**.
 *
 * ⚠️ **Η ταυτότητα είναι το `ownerPropertyId`, και όχι κάποιο «mandateId»**: η εντολή
 * **δεν είναι έγγραφο** — ζει ως `owner_properties/<ownp>.mandates[n]`. Ένα συνθετικό
 * αναγνωριστικό θα ήταν **δεύτερη ταυτότητα** για πράγμα που έχει ήδη μία (ADR-749).
 *
 * 🔑 **`encodeURIComponent`, και είναι ο λόγος που αυτό είναι συνάρτηση.** Το **Κ2** της
 * `notification-destination-custody` απαγορεύει χειρόγραφη διαδρομή στο σημείο κλήσης
 * ακριβώς γι' αυτό: ένα ``` `${MANDATE_CATALOG_ROUTE}/${id}` ``` αστοχεί **σιωπηλά, σε
 * ένα μόνο έγγραφο** — εκείνο που τυχαίνει να έχει χαρακτήρα που θέλει κωδικοποίηση.
 *
 * ⚠️ **Καμία επικόλληση προθέματος χώρου.** Το ωμό `/listings/mandates/<id>` το
 * αναλαμβάνει το δίχτυ `(app)/[...unprefixed]`, που αποφασίζει με **κριμένη** ταυτότητα
 * στον διακομιστή (ADR-787 §5.3 ιβ). Ένα cron **δεν έχει** τρέχουσα διεύθυνση, άρα δεν
 * έχει και ψευδώνυμο χώρου να δώσει: μαντεψιά εδώ θα έστελνε τον άνθρωπο σε **ξένο**
 * γραφείο. Ίδιο σκεπτικό, γραμμένο, στο `lib/places/place-detail-route.ts`.
 *
 * @param ownerPropertyId — η ταυτότητα του ακινήτου (`ownp_*`) πάνω στο οποίο ζει η εντολή
 */
export function mandateDetailHref(ownerPropertyId: string): string {
  return `${MANDATE_CATALOG_ROUTE}/${encodeURIComponent(ownerPropertyId)}`;
}

/** **Καταχώρηση ακινήτου για λογαριασμό πελάτη** (§8.33). */
export const NEW_BROKERED_LISTING_ROUTE = '/listings/mandates/new' as const;

/**
 * **Τα εισερχόμενα αιτήματα ανάθεσης** — το Σ2 του ADR-827 §9.21.
 *
 * 🔑 **Παιδί του {@link MANDATE_CATALOG_ROUTE}, και είναι σημασία**: τα αιτήματα είναι
 * η **πηγή** των εντολών — ό,τι γίνεται δεκτό εκεί εμφανίζεται εδώ. Η ιεραρχία της
 * διεύθυνσης λέει ακριβώς αυτό.
 *
 * ⛔ **Δεν είναι ο κατάλογος με φίλτρο.** Εκεί αποφασίζει ο **ιδιώτης** (εγκρίνει τη
 * συγκατάθεσή του)· εδώ το **γραφείο**. Δύο αντίθετες ροές έγκρισης σε μία διεύθυνση
 * θα σήμαιναν ότι ο μεσίτης δεν ξέρει ποιος περιμένει ποιον.
 */
export const MANDATE_INBOX_ROUTE = '/listings/mandates/requests' as const;

/**
 * **Η ΒΙΤΡΙΝΑ ΤΟΥ ΓΡΑΦΕΙΟΥ** — όπου δηλώνονται ταυτότητα, έδρα και **περιοχές κάλυψης**
 * *(`AgencyShowcaseContent`, ADR-846)*.
 *
 * 🔴 **ΓΕΝΝΗΘΗΚΕ ΩΣ ΚΕΝΤΡΙΚΟΠΟΙΗΣΗ, ΟΧΙ ΩΣ ΝΕΑ ΔΙΕΥΘΥΝΣΗ** *(N.0.2, ADR-846 Φάση 6)*: η
 * συμβολοσειρά ζούσε **γραμμένη με το χέρι** μέσα στο `config/smart-navigation-factory.ts`
 * και ήταν έτοιμη να γραφτεί **δεύτερη φορά** από το `CoverageReachNotice`. Δύο
 * αντίγραφα διεύθυνσης αποκλίνουν σιωπηλά: το ένα μετακομίζει, το άλλο μένει, και ο
 * άνθρωπος βρίσκει **404 μόνο από τη μία πόρτα**.
 *
 * ⚠️ **Ο πίνακας `settings/agency-profile → agency_showcase` του ίδιου αρχείου ΔΕΝ
 * αντικαταστάθηκε, και είναι απόφαση**: εκείνο δεν είναι διεύθυνση — είναι **κλειδί
 * χάρτη** *(χωρίς αρχική κάθετο)* που αντιστοιχεί τμήμα μονοπατιού σε ετικέτα i18n. Ίδια
 * γράμματα, **άλλο λεξιλόγιο**· μια κοινή σταθερά θα τα κλείδωνε μαζί για πάντα.
 *
 * ⚠️ **Χωρίς πρόθεμα χώρου** — το βάζει ο `Link` του `@/lib/workspace/navigation`
 * *(CHECK 3.61)*.
 */
export const AGENCY_SHOWCASE_ROUTE = '/settings/agency-profile' as const;

/**
 * **Η ΕΠΑΓΓΕΛΜΑΤΙΚΗ ΚΑΡΤΑ** — υποσελίδα της βιτρίνας (ADR-841 §7 Α21.16.7): δική της πράξη, δικό
 * της «Αποθήκευση», δικό της route slice. Παράγεται από το {@link AGENCY_SHOWCASE_ROUTE}, ώστε μια
 * μετακόμιση της βιτρίνας να μην αφήσει την κάρτα σε 404.
 */
export const AGENCY_SHOWCASE_CARD_ROUTE = `${AGENCY_SHOWCASE_ROUTE}/card` as const;

/**
 * **ΤΑ ΣΤΟΙΧΕΙΑ ΓΕΜΗ** — υποσελίδα της βιτρίνας (ADR-841 §7 Α23.9 Φέτα Β): επαλήθευση, «Υιοθέτηση επωνυμίας
 * ΓΕΜΗ», ένδειξη κλεισίματος. Δική της πράξη και δικό της route slice, όπως η κάρτα (Google Business Profile ·
 * Stripe: κάθε ενότητα στοιχείων επιχείρησης σε δική της οθόνη).
 */
export const AGENCY_SHOWCASE_REGISTRY_ROUTE = `${AGENCY_SHOWCASE_ROUTE}/registry` as const;

/**
 * **ΤΑ ΣΤΟΙΧΕΙΑ ΤΗΣ ΕΤΑΙΡΕΙΑΣ** (ADR-439 · ADR-841 §7 Α21.19) — εκεί στέλνει η κάρτα όποιον δεν έχει δηλώσει
 * ακόμη διεύθυνση και κανάλια, αντί να του ζητήσει να τα γράψει δεύτερη φορά.
 *
 * ⚠️ Χωρίς πρόθεμα χώρου — το βάζει ο `Link` του `@/lib/workspace/navigation` (CHECK 3.61).
 */
export const COMPANY_SETTINGS_ROUTE = '/settings/company' as const;
