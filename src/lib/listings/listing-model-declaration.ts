/**
 * @fileoverview 🏆 **Η ΚΛΕΙΣΤΗ ΛΟΓΙΣΤΙΚΗ ΤΟΥ ΜΟΝΤΕΛΟΥ** — «είναι αυτό που στάλθηκε;»
 * @related ADR-845 §6.2.1 (Ε-2) · §8 (Α-3 · Α-4 · Α-6) · ADR-841 §7 Α10 · Α11
 * @module lib/listings/listing-model-declaration
 *
 * ────────────────────────────────────────────────────────────────────────────
 * 🔴 ΤΙ ΡΩΤΑ ΑΥΤΟ ΤΟ ΑΡΧΕΙΟ, ΚΑΙ ΤΙ **ΔΕΝ** ΡΩΤΑ
 * ────────────────────────────────────────────────────────────────────────────
 *
 * Ο πελάτης **συναρμολογεί** το GLB *(είναι η μόνη θέση όπου υπάρχουν όλα τα κομμάτια:
 * οι υφές είναι loose αρχεία στον δίσκο του ανθρώπου)*. Ο διακομιστής **εγκρίνει** *(είναι
 * η μόνη θέση που μπορεί να μην εμπιστεύεται κανέναν)*. Ανάμεσά τους ταξιδεύει μια
 * **δήλωση**: τι νομίζει ο πελάτης ότι έφτιαξε.
 *
 * Ο διακομιστής **ξαναμετράει τα ίδια** πάνω στο αρχείο που παρέλαβε. **Διαφωνία ⇒
 * απόρριψη με ΟΝΟΜΑ**, ποτέ σιωπηλή αποδοχή.
 *
 * ⚠️ **ΔΕΝ είναι αντι-κακόβουλο μέτρο, και λέγεται ρητά** *(§6.2.2)*: όποιος ελέγχει τον
 * πελάτη ελέγχει **και** τη δήλωση. Το αντι-κακόβουλο είναι το **ξαναγράψιμο** των bytes,
 * που ζει στον ψήστη. Εδώ ζει η **τιμιότητα του μηνύματος**.
 *
 * ────────────────────────────────────────────────────────────────────────────
 * 🏆 Η ΜΗ-ΠΡΟΦΑΝΗΣ ΕΠΙΛΟΓΗ: Η ΛΟΓΙΣΤΙΚΗ ΔΙΑΣΧΙΖΕΙ ΣΥΝΟΡΟ **ΑΝΑΠΑΡΑΣΤΑΣΗΣ**
 * ────────────────────────────────────────────────────────────────────────────
 *
 * Αν **και οι δύο** πλευρές μετρούσαν τα **ίδια bytes** με την **ίδια** βιβλιοθήκη, η
 * λογιστική θα εκφυλιζόταν σε έλεγχο μεταφοράς — που τον κάνει **ήδη** το sha256,
 * φθηνότερα. Θα ήταν πεδίο **χωρίς αγοραστή**.
 *
 * ⇒ Ο πελάτης δηλώνει από τη **σκηνή που συναρμολόγησε** *(three.js)*· ο διακομιστής μετρά
 * τα **bytes που παρέλαβε** *(glTF)*. Η ερώτηση γίνεται:
 *
 *   > **«Αναπαριστά το αρχείο αυτό που είδε ο άνθρωπος στην οθόνη του;»**
 *
 * — ερώτηση που **κανένα hash δεν μπορεί να απαντήσει**, γιατί πιάνει **σφάλματα του ίδιου
 * του exporter**: ακριβώς την κλάση που γέννησε ολόκληρο το **ADR-683**.
 *
 * 🔑 **ΚΑΙ ΤΑ ΨΕΥΔΩΣ ΘΕΤΙΚΑ ΕΙΝΑΙ ΜΗΔΕΝ, ΕΠΕΙΔΗ Ο ΚΡΙΤΗΣ ΥΠΑΡΧΕΙ ΗΔΗ.** Η γεωμετρία
 * **δεν** συγκρίνεται με ισότητα — συγκρίνεται με το `compareGeometry` του ADR-683, που
 * είναι **ήδη ρυθμισμένο** γι' αυτό ακριβώς το ερώτημα: το σκεπτικό του μετρά τον θόρυβο
 * float32 του glTF *(~12 μm στα 100 m)* και απορρίπτει το σκέτο quantised hash επειδή θα
 * έδινε **>50% ψευδώς θετικά**. Η μετάβαση `float64 σκηνή → float32 GLB` πέφτει **μέσα**
 * στην ανοχή του, και επιστρέφει `equivalent` — που **γίνεται δεκτό**.
 * ⇒ **Μηδέν νέο λεξιλόγιο ανοχών σε αυτό το αρχείο.** Αν χρειαστεί κάποτε, η αλλαγή ανήκει
 * στον **έναν** κριτή, όχι σε δεύτερο.
 *
 * ⚠️ **ΤΟ ΔΗΛΩΜΕΝΟ ΟΡΙΟ**: τα **πλήθη** συγκρίνονται με **ακριβή ισότητα**, γιατί ο
 * `GLTFExporter` τα διατηρεί *(ένα THREE mesh → ένα glTF mesh· καμία ξανα-τριγωνοποίηση)*.
 * Αν κάποτε πάψει να τα διατηρεί, η δημοσίευση **σταματά με όνομα** αντί να περάσει σιωπηλά —
 * fail-closed, και διαγνώσιμο. Είναι η **σωστή** κατεύθυνση αστοχίας για πύλη δημοσίευσης.
 */

import {
  compareGeometry,
  type GeometryFingerprint,
} from '@/lib/geometry/mesh3d/geometry-fingerprint';

// ---------------------------------------------------------------------------
// Α11 — «ΜΕΛΕΤΗ Η ΠΡΑΓΜΑΤΙΚΟΤΗΤΑ;» ΕΙΝΑΙ ΚΑΤΑΣΤΑΣΗ ΜΕ ΟΝΟΜΑ
// ---------------------------------------------------------------------------

/**
 * 🏆 **Η ΣΗΜΑΝΣΗ ΚΑΤΑΣΤΑΣΗΣ** (ADR-841 §7 Α11) — **δεν** είναι μεταδεδομένο ευκολίας.
 *
 * - `as-built`  — **χτισμένο**: το μοντέλο δείχνει ό,τι υπάρχει.
 * - `proposal`  — **μελέτη**: δείχνει ό,τι θα υπάρξει. Η Α11 το δημοσιεύει **επίτηδες** —
 *   *«πωλείται με έτοιμη μελέτη»* είναι **αξία, όχι κίνδυνος**.
 * - `divergent` — **απόκλιση**: μελέτη που **δεν** ταυτίζεται πια με το χτισμένο.
 *
 * 🔴 **ΓΙΑΤΙ ΕΙΝΑΙ ΥΠΟΧΡΕΩΤΙΚΗ, ΚΑΙ ΓΙΑΤΙ ΨΗΝΕΤΑΙ ΣΤΟ ΑΡΧΕΙΟ** *(Α11.2, άγκυρα Α-3)*: ο
 * slider της Zillow ζει **μόνο στο δικό της UI** — ένα screenshot, μια αναδημοσίευση, ένα
 * syndication feed, και η σήμανση **έχει φύγει** ενώ η εικόνα μένει. Εμείς τη **ψήνουμε
 * μέσα στο artifact**, άρα **ταξιδεύει με το αρχείο**. Είναι το μόνο μέτρο που επιβιώνει
 * έξω από την οθόνη μας.
 *
 * ⚠️ Η έρευνα που το στήριξε έχει **πηγές και δόντια**: California **AB 723** *(ενεργός
 * 01/01/2026 — σήμανση **και η πρωτότυπη δίπλα**)* · **DMCC Act 2024** *(CMA: **10% τζίρου
 * ή £300k**, χωρίς δικαστήριο)*.
 */
export const MODEL_STATE_MARKS = ['as-built', 'proposal', 'divergent'] as const;

export type ModelStateMark = (typeof MODEL_STATE_MARKS)[number];

// ---------------------------------------------------------------------------
// ΤΟ ΕΥΡΟΣ — «ΤΙ ΚΑΛΥΠΤΕΙ ΑΥΤΟ ΤΟ ΜΟΝΤΕΛΟ;» (ADR-845 Ο-27)
// ---------------------------------------------------------------------------

/**
 * 🏆 **ΤΙ ΚΑΛΥΠΤΕΙ ΤΟ ΜΟΝΤΕΛΟ** — το `Volume/Level` του container ID (ISO 19650-2 §5.1.7).
 *
 * - `active-floor` — **ένας όροφος**: ό,τι έδειχνε ο ενεργός καμβάς τη στιγμή της δημοσίευσης.
 * - `all-floors`   — **όλο το κτίσμα**: όλοι οι όροφοι, στοιβαγμένοι στο ένα αρχείο.
 *
 * 🔴 **ΔΕΥΤΕΡΟ ΛΕΞΙΛΟΓΙΟ ΑΠΕΝΑΝΤΙ ΣΤΟ `ExportFloorScope`, ΚΑΙ ΕΙΝΑΙ ΑΠΟΦΑΣΗ — ΟΧΙ ΔΙΠΛΟΤΥΠΟ.**
 * Ο πειρασμός ήταν να δανειστεί το `'active' | 'all-single'` του εξαγωγέα. Θα ήταν λάθος
 * **δύο φορές**:
 *
 * 1. **Λέει άλλο πράγμα.** Το `all-single` απαντά *«πώς **συσκευάζεται**;»* — ένα αρχείο αντί
 *    για zip. Το `all-zip` καλύπτει **ακριβώς το ίδιο** με άλλη συσκευασία. Ως **ταυτότητα**
 *    αυτό θα ήταν ψέμα: δύο μοντέλα του ίδιου κτιρίου θα ήταν «παραλλαγές» επειδή διέφερε το
 *    **κουτί** τους. Ίδιο ακριβώς επιχείρημα με το γραμμένο *«η προέλευση είναι ορθογώνια
 *    προς τη σήμανση Α11, και ο πειρασμός να τις δέσεις είναι λάθος»*.
 * 2. **Λάθος κατεύθυνση εξάρτησης.** Το `ExportFloorScope` ζει στο `subapps/dxf-viewer`, που
 *    είναι **εκτός** του root `tsconfig`. Ένα `lib/` που το εισάγει θα ήταν αόρατο σε κάθε
 *    πύλη πλην του CI *(το γραμμένο μάθημα του CHECK 3.29)*.
 *
 * ⇒ Η **μετάφραση** ζει στο σύνορο, στον πελάτη — μία γραμμή, εκεί που ξέρει και τα δύο.
 */
export const MODEL_PUBLICATION_SCOPES = ['active-floor', 'all-floors'] as const;

export type ModelPublicationScope = (typeof MODEL_PUBLICATION_SCOPES)[number];

// ---------------------------------------------------------------------------
// Α10 — Ο ΥΠΟΓΡΑΦΩΝ
// ---------------------------------------------------------------------------

/**
 * 🏆 **ΠΟΙΟΣ ΥΠΟΓΡΑΦΕΙ ΤΗ ΜΕΛΕΤΗ** (ADR-841 §7 Α10 · απόφαση **Ε-3** του ADR-845).
 *
 * 🔑 **ΔΗΛΩΜΕΝΟΣ, ΟΧΙ ΕΠΑΛΗΘΕΥΜΕΝΟΣ — και η διάκριση είναι ολόκληρη η απόφαση.** Ο Ε-3
 * ξεμπλόκαρε τη φάση ακριβώς επειδή **δεν** περιμένει τα μητρώα *(Φ6)*: ο κάτοχος
 * **δηλώνει** τον μηχανικό, και η δήλωση ψήνεται στο artifact.
 *
 * ⛔ **ΚΑΙ Η ΥΠΟΔΟΧΗ ΓΙΑ ΕΠΑΛΗΘΕΥΣΗ ΜΕΝΕΙ ΑΝΟΙΧΤΗ, ΡΗΤΑ**: μια **δηλωμένη** υπογραφή
 * **δεν επιτρέπεται να μοιάζει επαληθευμένη** — αλλιώς η **Α9** γίνεται διακοσμητική.
 * Γι' αυτό **δεν** υπάρχει εδώ πεδίο `verified`: ένα `false` που κανείς δεν μπορεί να
 * κάνει `true` είναι υπόσχεση χωρίς μηχανισμό, και η Φ4.1 απέρριψε γραπτώς ακριβώς αυτό.
 * Η επαλήθευση θα έρθει ως **ξεχωριστή** δήλωση, από μηχανή που θα υπάρχει.
 */
export interface ModelSignatory {
  /** Το όνομα όπως το **δήλωσε άνθρωπος**. Κενό ⇒ δεν υπάρχει υπογράφων (άγκυρα Α-4). */
  readonly name: string;
  /** Ειδικότητα — *«αρχιτέκτονας»*, *«πολιτικός μηχανικός»*. Δηλωμένη, όπως το όνομα. */
  readonly discipline: string;
  /** Η ημερομηνία **της μελέτης**, όχι της δημοσίευσης. ISO 8601. */
  readonly studiedAt: string;
}

// ---------------------------------------------------------------------------
// Η ΛΟΓΙΣΤΙΚΗ
// ---------------------------------------------------------------------------

/**
 * **Τα μεγέθη που δηλώνονται και ξαναμετριούνται** (§6.2.1).
 *
 * ⚠️ **ΤΟ ΟΡΙΑΚΟ ΚΟΥΤΙ ΔΕΝ ΕΙΝΑΙ ΞΕΧΩΡΙΣΤΟ ΠΕΔΙΟ, ΚΑΙ ΕΙΝΑΙ ΑΠΟΦΑΣΗ.** Ζει **μέσα** στο
 * {@link fingerprint} *(`signature.sizeM`)*. Ένα δεύτερο πεδίο εδώ θα ήταν **δύο αριθμοί
 * για το ίδιο πράγμα**, ελεύθεροι να διαφωνήσουν — δηλαδή ακριβώς το σχήμα που η ίδια η
 * λογιστική υπάρχει για να καταγγέλλει, αυτοπροκαλούμενο.
 */
export interface ModelGeometryLedger {
  /** Πλήθος πλεγμάτων. Ένα THREE mesh → ένα glTF mesh. */
  readonly meshCount: number;
  /** Πλήθος τριγώνων, αθροιστικά. */
  readonly triangleCount: number;
  /** Πλήθος **διακριτών** υλικών. */
  readonly materialCount: number;
  /** Πλήθος **διακριτών** υφών. Μηδέν είναι νόμιμη απάντηση *(επίπεδα χρώματα)*. */
  readonly textureCount: number;
  /**
   * Το αποτύπωμα σχήματος — **ο κριτής, όχι ο αριθμός**. `null` όταν δεν υπάρχει
   * αξιοποιήσιμη γεωμετρία· ο {@link compareModelLedger} το θεωρεί **διαφωνία**
   * *(fail-closed: «δεν ξέρω» δεν σημαίνει ποτέ «ίδιο»)*.
   */
  readonly fingerprint: GeometryFingerprint | null;
}

/**
 * **Ό,τι ταξιδεύει δίπλα στα bytes** — η δήλωση του πελάτη, ολόκληρη.
 *
 * 🔑 **Τέσσερα πράγματα, τέσσερις άγκυρες**: {@link state} → **Α-3**, {@link signatory} →
 * **Α-4**, {@link geometry} → **Α-6**, {@link scope} → **Α-15**. Κανένα δεν είναι προαιρετικό,
 * και κανένα δεν έχει προεπιλογή: μια προεπιλογή εδώ θα σήμαινε ότι ο πρώτος που θα ξεχάσει να
 * απαντήσει **δημοσιεύει**.
 *
 * ────────────────────────────────────────────────────────────────────────────
 * 🔴 ΓΙΑΤΙ ΤΟ ΕΥΡΟΣ ΜΠΗΚΕ ΕΔΩ ΚΑΙ ΟΧΙ ΜΟΝΟ ΣΤΟ ΕΓΓΡΑΦΟ *(ADR-845 Ο-27)*
 * ────────────────────────────────────────────────────────────────────────────
 *
 * Ως τις 2026-09-09 το εύρος **επέλεγε ποια bytes παράγονται** και μετά **εξατμιζόταν**:
 * μετρημένο, δεν γραφόταν **πουθενά** — ούτε στο `files`, ούτε στα custom metadata, ούτε στο
 * δημόσιο σχήμα. Δηλαδή το ένα τρίτο της ταυτότητας ενός μοντέλου **δεν υπήρχε**.
 *
 * 🔑 **Ταξιδεύει με το αρχείο για τον ίδιο γραπτό λόγο με τη σήμανση** *(Α11.2)*: *«ο slider
 * της Zillow ζει μόνο στο δικό της UI — ένα screenshot, μια αναδημοσίευση, ένα syndication
 * feed, και η σήμανση έχει φύγει ενώ η εικόνα μένει»*. Ένα μοντέλο **ενός ορόφου** που
 * ταξιδεύει χωρίς να το λέει είναι ακριβώς η ίδια σιωπή: ο θεατής βλέπει «το ακίνητο» και
 * βλέπει **έναν όροφο**.
 *
 * ⇒ Το πεδίο του `files` **δεν είναι δεύτερη δήλωση**: είναι **παράγωγο** αυτής, από το
 * `modelPublicationIdentityKey`. Μία πηγή, δύο αναγνώστες — δεν μπορούν να διαφωνήσουν.
 */
export interface ModelPublicationDeclaration {
  readonly state: ModelStateMark;
  /** *«Τι καλύπτει;»* — ένας όροφος ή όλο το κτίσμα. Δες `model-publication-identity`. */
  readonly scope: ModelPublicationScope;
  readonly signatory: ModelSignatory;
  readonly geometry: ModelGeometryLedger;
}

/**
 * **Η ετυμηγορία** — και **ονομάζει** τη διαφωνία.
 *
 * 🔑 Ο λόγος που δεν είναι `boolean`: το **ADR-844 §1** διόρθωσε ακριβώς αυτή τη βλάβη σε
 * άλλο πεδίο — ο άνθρωπος πρέπει να μαθαίνει *«το αρχείο άλλαξε στη μεταφορά»*, όχι
 * *«κάτι πήγε στραβά»*. Ένα `false` δεν μπορεί να γίνει μήνυμα.
 */
export type ModelLedgerVerdict =
  | { readonly agrees: true }
  | { readonly agrees: false; readonly disagreement: ModelLedgerDisagreement };

/** Τι ακριβώς διαφώνησε — **ένα όνομα ανά ερώτηση**, ποτέ «σφάλμα». */
export type ModelLedgerDisagreement =
  | { readonly field: 'meshCount' | 'triangleCount' | 'materialCount' | 'textureCount'; readonly declared: number; readonly measured: number }
  | { readonly field: 'geometry'; readonly comparison: 'changed' };

/** Τα τέσσερα πλήθη, ονομασμένα **μία** φορά — ο βρόχος τα ρωτά, δεν τα απαριθμεί ξανά. */
const LEDGER_COUNTS = ['meshCount', 'triangleCount', 'materialCount', 'textureCount'] as const;

/**
 * 🏆 **ΣΥΜΦΩΝΕΙ Η ΔΗΛΩΣΗ ΜΕ ΤΗ ΜΕΤΡΗΣΗ;** — η άγκυρα **Α-6**, εκτελούμενη.
 *
 * ⚠️ **Τα πλήθη πρώτα, η γεωμετρία μετά**, και δεν είναι στιλ: ένα λάθος πλήθος δίνει
 * μήνυμα που ο άνθρωπος **καταλαβαίνει** *(«δήλωσες 3 πλέγματα, έφτασαν 4»)*, ενώ το
 * `changed` της γεωμετρίας είναι το **γενικότερο** «το σχήμα δεν είναι το ίδιο». Η πιο
 * **συγκεκριμένη** αιτία πρέπει να μιλήσει πρώτη.
 */
export function compareModelLedger(
  declared: ModelGeometryLedger,
  measured: ModelGeometryLedger,
): ModelLedgerVerdict {
  for (const field of LEDGER_COUNTS) {
    if (declared[field] !== measured[field]) {
      return {
        agrees: false,
        disagreement: { field, declared: declared[field], measured: measured[field] },
      };
    }
  }

  // 🔑 **Ο ΚΡΙΤΗΣ ΤΟΥ ADR-683, ΑΥΤΟΥΣΙΟΣ.** `identical` (ίδια bytes γεωμετρίας) και
  //    `equivalent` (θόρυβος float32 της εξαγωγής) είναι **και τα δύο** συμφωνία· μόνο
  //    το `changed` είναι διαφωνία. Ένα `!== 'identical'` εδώ θα απέρριπτε **κάθε**
  //    δημοσίευση, γιατί η μετάβαση float64→float32 αλλάζει **πάντα** το ακριβές hash.
  if (compareGeometry(declared.fingerprint, measured.fingerprint) === 'changed') {
    return { agrees: false, disagreement: { field: 'geometry', comparison: 'changed' } };
  }

  return { agrees: true };
}

/**
 * **Υπάρχει υπογράφων;** — η άγκυρα **Α-4**, ως μία ερώτηση.
 *
 * ⚠️ **`trim()` εδώ, και ΜΟΝΟ εδώ**: ένα όνομα από κενά είναι *«όποιος ανεβάσει»* με άλλη
 * μορφή, δηλαδή ο **Κανόνας 31 καμμένος σιωπηλά**. Ο έλεγχος ζει σε **ένα** σημείο ώστε
 * ο ψήστης να μη γεννήσει δεύτερη εκδοχή του *«τι μετράει ως υπογραφή»*.
 */
export function hasSignatory(signatory: ModelSignatory): boolean {
  return signatory.name.trim().length > 0 && signatory.discipline.trim().length > 0;
}

/** Είναι αυτή γνωστή σήμανση κατάστασης; — η άγκυρα **Α-3**, στο σύνορο. */
export function isModelStateMark(value: string): value is ModelStateMark {
  return (MODEL_STATE_MARKS as readonly string[]).includes(value);
}

/** Είναι αυτό γνωστό εύρος; — ο αδελφός του {@link isModelStateMark}, ίδιο σύνορο (Α-15). */
export function isModelPublicationScope(value: string): value is ModelPublicationScope {
  return (MODEL_PUBLICATION_SCOPES as readonly string[]).includes(value);
}
