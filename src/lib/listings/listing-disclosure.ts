/**
 * @fileoverview **Η ΛΟΓΙΣΤΙΚΗ ΑΠΟΚΑΛΥΨΗΣ** — κάθε δημοσιευμένο πεδίο ή φαίνεται, ή η
 * απουσία του ονομάζεται. Καμία τρίτη περίπτωση.
 * @related ADR-777 §7 (Α3 οθόνη 3 · Α7 προοδευτική αποκάλυψη · Α17) · SPEC-777-RESEARCH §25.6
 * @module lib/listings/listing-disclosure
 *
 * ────────────────────────────────────────────────────────────────────────────
 * 🔴 ΤΟ ΕΥΡΗΜΑ ΠΟΥ ΟΡΙΣΕ ΤΗΝ ΟΘΟΝΗ 3 — μετρημένο, και ανατρέπει την προδιαγραφή
 * ────────────────────────────────────────────────────────────────────────────
 *
 * Η **Α3** περιγράφει την οθόνη 3 ως *«τα **υπόλοιπα** πεδία, κάτοψη, φάκελος,
 * νομιμότητα»*. Μετρήθηκε το κλειστό σχήμα του {@link PublicListing}: έχει **12**
 * πεδία, και η κάρτα της οθόνης 2 δείχνει ήδη **8** από αυτά. Τα «υπόλοιπα» είναι:
 * ο **τύπος** (5ο βασικό πεδίο του §25.6, που η κάρτα **δεν** δείχνει), η **προέλευση
 * της θέσης**, η **προέλευση της τιμής**, και η **στιγμή ανακατασκευής**.
 *
 * ⇒ Η οθόνη 3 **δεν έχει άλλα δεδομένα να δείξει.** Αν χτιζόταν ως «πιο πλούσια
 * κάρτα», θα ήταν η ίδια κάρτα με μεγαλύτερα γράμματα.
 *
 * **Άρα η οθόνη 3 δεν είναι περισσότερα ΠΕΔΙΑ — είναι περισσότερη ΑΛΗΘΕΙΑ για τα
 * ίδια πεδία:** από πού ξέρουμε ό,τι ξέρουμε, και τι δεν ξέρουμε καθόλου.
 *
 * ────────────────────────────────────────────────────────────────────────────
 * 🏆 ΓΙΑΤΙ ΑΥΤΟ ΞΕΠΕΡΝΑ ΤΗΝ ΠΡΑΚΤΙΚΗ ΤΗΣ ΑΓΟΡΑΣ, ΔΟΜΙΚΑ
 * ────────────────────────────────────────────────────────────────────────────
 *
 * Σε Zillow · Idealista · Spitogatos ένα πεδίο που λείπει **απλώς δεν εμφανίζεται**.
 * Ο αναγνώστης δεν μπορεί να ξεχωρίσει το *«δεν έχει όροφο»* από το *«κανείς δεν
 * καταχώρησε όροφο»* — και τα δύο μοιάζουν με σιωπή. Είναι **ακριβώς** το σχήμα που
 * κυνηγούν οι πύλες μας: **«0 = κανείς δεν κοίταξε»**, μεταφερμένο στην οθόνη.
 *
 * Εδώ η απουσία είναι **κατάσταση με όνομα**, όπως το `missing` του `price-resolver`
 * και οι τρεις καταστάσεις του `listing-coverage`.
 *
 * ────────────────────────────────────────────────────────────────────────────
 * 🔑 Η ΕΓΓΥΗΣΗ ΕΙΝΑΙ ΤΥΠΟΣ, ΟΧΙ ΣΥΜΒΑΣΗ
 * ────────────────────────────────────────────────────────────────────────────
 *
 * Το {@link LISTING_DISCLOSURE} είναι `satisfies Record<keyof PublicListing, …>`.
 * Άρα **νέο πεδίο στην προβολή δεν μεταγλωττίζεται** μέχρι κάποιος να απαντήσει «πώς
 * το βλέπει ο κόσμος;». Ο κανόνας του `types/public-listing.ts` λέει ότι κάθε
 * προσθήκη πεδίου είναι **απόφαση δημοσιοποίησης**· εδώ η απόφαση αποκτά **δεύτερο
 * μισό** — δημοσιοποίηση χωρίς εμφάνιση είναι διαρροή χωρίς όφελος.
 *
 * ⚠️ **Και ο κατάλογος των ιδιοτήτων ΠΑΡΑΓΕΤΑΙ από τον ίδιο πίνακα**, δεν
 * ξαναγράφεται. Μια δεύτερη χειρόγραφη λίστα θα ήταν το σχήμα της CHECK 3.34 (δύο
 * λίστες namespace που **απέκλιναν κατά 63** χωρίς καμία πύλη να τις συγκρίνει).
 */

import type { PublicListing } from '@/types/public-listing';

// ============================================================================
// 1. ΠΩΣ ΒΛΕΠΕΙ Ο ΚΟΣΜΟΣ ΚΑΘΕ ΠΕΔΙΟ — κλειστός πίνακας
// ============================================================================

/**
 * Ο **ρόλος** ενός δημοσιευμένου πεδίου στην οθόνη 3.
 *
 * Δεν είναι κατηγορίες διάταξης· είναι απαντήσεις στο *«με ποιον τρόπο γίνεται αυτό
 * το πεδίο ορατό;»*. Δύο πεδία με τον ίδιο ρόλο εμφανίζονται με τον ίδιο μηχανισμό.
 */
export type DisclosureTreatment =
  /** Η ταυτότητα **είναι** η διεύθυνση της σελίδας — δεν ζωγραφίζεται ως περιεχόμενο. */
  | 'identity'
  /** Ο τίτλος του κατόχου, στην κορυφή. */
  | 'headline'
  /**
   * Τροφοδοτεί τον **SSoT τιμής** (`lib/properties/price-resolver`).
   *
   * ⛔ **ΠΟΤΕ ζωγραφισμένο ωμό** — και ειδικά το `commercialStatus`, που η **Α20**
   * ορίζει **ρητά lossy**: ένα ακίνητο μόνο προς αντιπαροχή προβάλλεται εκεί σε
   * `'unavailable'`. Τυπωμένο αυτούσιο, θα έλεγε «μη διαθέσιμο» για κάτι που
   * **είναι** διαθέσιμο — με άλλον τρόπο.
   */
  | 'price'
  /** Εικόνα εξωφύλλου, ή η ονομασμένη απουσία της. */
  | 'media'
  /** Μετρήσιμο χαρακτηριστικό — δείχνεται **και όταν λείπει** (§25.6). */
  | 'attribute'
  /**
   * **ΣΥΝΟΛΟ τιμών από κλειστό λεξιλόγιο** (ADR-842 Φ3) — παροχές, προσανατολισμοί,
   * δάπεδα.
   *
   * ⛔ **ΔΕΝ είναι `'attribute'`, και η διάκριση ΕΙΝΑΙ ο μηχανισμός** — ίδιο ακριβώς
   * σχήμα με τα `'stay-terms'` και `'legality'` που έφυγαν από εκεί πριν από αυτό.
   * Ένα σύνολο έχει **τρεις** καταστάσεις, όχι δύο:
   *
   * | Τιμή | Τι σημαίνει |
   * |---|---|
   * | `null` | **κανείς δεν ρώτησε** — δικό μας χρέος |
   * | `[]` | ρωτήθηκε, και η απάντηση ήταν **καμία** — **γεγονός του κατόχου** |
   * | `[…]` | ρωτήθηκε, και υπάρχουν |
   *
   * Ο ρόλος `'attribute'` ξέρει **δύο** ({@link isAttributeDeclared} επιστρέφει
   * `boolean`) και θα **έλιωνε** τις τρεις σε δύο — δηλαδή ένα ακίνητο που **δήλωσε**
   * ότι δεν έχει καμία παροχή θα φαινόταν ίδιο με ένα που **δεν ρωτήθηκε ποτέ**.
   *
   * 🏆 **Αυτή είναι η τρίτη κατάσταση που Zillow · idealista · Spitogatos δεν έχουν**:
   * εκεί και τα δύο καταλήγουν στην ίδια σιωπή. Είναι το ίδιο επιχείρημα με το
   * `never-asked` vs `owner-declined` της θέσης (Α5 §3) — και ο λόγος που ο τύπος
   * είναι `readonly X[] | null` και **ποτέ** `readonly X[]` με προεπιλογή `[]`.
   *
   * ⚠️ **Και ο ΤΡΟΠΟΣ εμφάνισης είναι πράγματι άλλος**: ετικέτες πολλαπλών τιμών, όχι
   * γραμμή ετικέτα/τιμή. Δύο πεδία με τον ίδιο ρόλο εμφανίζονται με τον ίδιο
   * μηχανισμό — αυτός ο κανόνας του πίνακα είναι που **απαιτεί** τον χωρισμό.
   */
  | 'feature-set'
  /** Οι διαθέσεις (Α20) — ο άξονας όπου δεν χάνεται τίποτα. */
  | 'offers'
  /** Η θέση: σχήμα στον χάρτη **και** η ακρίβειά του σε λέξεις (Α5). */
  | 'position'
  /** Πότε ανακατασκευάστηκε αυτή η προβολή — κανόνας 18 (προέλευση). */
  | 'provenance'
  /**
   * **Ο δεσμός προς το επίπεδο Α** (§14.5) — ταξιδεύει, **δεν ζωγραφίζεται**.
   *
   * Τροφοδοτεί τη μηχανή ταιριάσματος της ζήτησης (Ζ3/Ζ5: *«αυτό το κτίριο»*). Ένα
   * `land_*`/`pbld_*` τυπωμένο ωμό θα ήταν **ταυτότητα σε οθόνη ανθρώπου**.
   */
  | 'place-link'
  /**
   * **Ο δεσμός προς το ΓΡΑΦΕΙΟ** (ADR-841 Α1.6) — ταξιδεύει, **δεν ζωγραφίζεται**.
   *
   * Τροφοδοτεί δύο πράγματα που η συμβολοσειρά της επωνυμίας **δεν μπορεί**: τη
   * **βιτρίνα** *(Φ7: `agency_profiles/{companyId}`, σημειακή ανάγνωση)* και την
   * **επαλήθευση του αντιγράφου** *(«συμφωνεί η επωνυμία με την πηγή;»)*.
   *
   * ⛔ **ΞΕΧΩΡΙΣΤΟΣ ΡΟΛΟΣ ΑΠΟ ΤΟ `'authorship'`, ΚΑΙ ΔΕΝ ΕΙΝΑΙ ΛΕΠΤΟΛΟΓΙΑ.** Εκείνος
   * υπόσχεται ότι τα πεδία του *«εμφανίζονται **μαζί ή καθόλου**»* — πρόταση που θα
   * γινόταν **ψευδής** την ώρα που ένα τρίτο, μη εμφανιζόμενο πεδίο έμπαινε από κάτω
   * της. Ένα `comp_*` τυπωμένο ωμό θα ήταν **ταυτότητα σε οθόνη ανθρώπου**, ακριβώς
   * ό,τι αποτρέπει το `'place-link'` για τα `land_*`/`pbld_*`.
   */
  | 'agency-link'
  /**
   * **Η υπογραφή της αγγελίας** (§8.33) — *ποιος τη δηλώνει, ως **κλάση***.
   *
   * ⚠️ Ένας ρόλος για **δύο** πεδία, και είναι σωστό: το `authorship` και το
   * `agencyName` εμφανίζονται **μαζί ή καθόλου** (η επωνυμία χωρίς την κλάση δεν λέει
   * τι είδους γνώση είναι· η κλάση `'agency'` χωρίς επωνυμία πέφτει στο
   * `agencyAnonymous`). Δύο ρόλοι θα επέτρεπαν να εμφανιστεί το ένα χωρίς το άλλο.
   */
  | 'authorship'
  /**
   * **Οι όροι διαμονής** (ADR-835 §4.5) — `null` όταν δεν είναι κατάλυμα.
   *
   * ⛔ **ΔΕΝ είναι `'attribute'`, και η διάκριση δεν είναι λεπτολογία**: οι ιδιότητες
   * δείχνονται *«και όταν λείπουν»* (§25.6), γιατί κάθε ακίνητο **σηκώνει** την
   * ερώτηση «πόσα τ.μ.;». Ένα ακίνητο **προς πώληση** δεν σηκώνει την ερώτηση «πόσες
   * ελάχιστες νύχτες;» — και ένα «Δεν έχει δηλωθεί» εκεί θα κατηγορούσε τον κάτοχο
   * για πεδίο που **δεν του ζητήθηκε ποτέ**. Ίδιο μάθημα με τη **γη που δεν κρίνεται
   * σε υπνοδωμάτια** (§8.32).
   */
  | 'stay-terms'
  /**
   * **Η νομιμότητα ως κλίμακα με προέλευση** (Α17 · ADR-838).
   *
   * ⛔ **ΔΕΝ είναι `'attribute'`, για τον ίδιο λόγο με το `'stay-terms'` και για έναν
   * βαρύτερο**: μια ιδιότητα που λείπει διαβάζεται *«ο κάτοχος δεν το συμπλήρωσε»* —
   * ουδέτερο για τα τετραγωνικά, **κατηγορία** για τη νομιμότητα. Η απουσία εδώ έχει
   * **δικό της όνομα και δική της σημείωση** (*«το κενό ανήκει στον κάτοχο — δεν
   * σημαίνει ότι υπάρχει πρόβλημα»*), και αυτό δεν χωρά σε γραμμή ιδιότητας.
   *
   * ⛔ **Και ΠΟΤΕ `'media'`**: το `'media'` υπονοεί αρχείο. Εδώ **δεν φεύγει έγγραφο**.
   */
  | 'legality';

/**
 * **Κάθε πεδίο της δημόσιας προβολής, και πώς γίνεται ορατό.**
 *
 * 🔑 Η **σειρά των κλειδιών είναι συμβόλαιο**: από αυτήν παράγεται η σειρά εμφάνισης
 * των ιδιοτήτων ({@link LISTING_ATTRIBUTE_KEYS}). Αλλαγή σειράς εδώ = αλλαγή οθόνης,
 * και διαβάζεται ως τέτοια στο diff.
 *
 * ⚠️ Το `satisfies` **δεν** αντικαθίσταται με σκέτο annotation: χρειαζόμαστε **και**
 * την εξαντλητικότητα (`Record<keyof PublicListing, …>`) **και** τους κυριολεκτικούς
 * τύπους, ώστε το {@link ListingAttributeKey} να παραχθεί από **αυτόν** τον πίνακα.
 */
export const LISTING_DISCLOSURE = {
  id: 'identity',
  title: 'headline',
  commercialStatus: 'price',
  commercial: 'price',
  coverImage: 'media',
  // ✅ **ADR-841 Α2.6** — και ο πίνακας **κοκκίνισε ξανά μόλις μπήκε το πεδίο**:
  //    τέταρτη φορά που το `satisfies Record<keyof PublicListing, …>` πιάνει προσθήκη
  //    σχήματος πριν προλάβει να τη δει άνθρωπος.
  //    ⚠️ **Ίδιος ρόλος με το `coverImage`, και ΟΧΙ νέος**: το `'media'` απαντά *«φεύγει
  //    αρχείο;»*, και εδώ φεύγει — δύο πεδία μπορούν να μοιράζονται ρόλο αποκάλυψης
  //    (το κάνουν ήδη τα `commercialStatus`/`commercial`). Αυτό που **δεν** μοιράζονται
  //    είναι ο **παραγωγός** τους, και αυτό είναι ερώτηση του σχήματος, όχι του πίνακα.
  gallery: 'media',
  // ── Οι ιδιότητες, ΣΤΗ ΣΕΙΡΑ ΠΟΥ ΕΜΦΑΝΙΖΟΝΤΑΙ ────────────────────────────────
  type: 'attribute',
  areaSqm: 'attribute',
  floor: 'attribute',
  bedrooms: 'attribute',
  // ── ADR-842 Φ3 — ΤΑ ΧΑΡΑΚΤΗΡΙΣΤΙΚΑ ΠΟΥ Η ΕΤΑΙΡΕΙΑ ΗΔΗ ΚΑΤΕΙΧΕ ───────────────
  //
  // 🔴 **Ο πίνακας κοκκίνισε ξανά, και είναι η ΠΕΜΠΤΗ φορά** που το
  //    `satisfies Record<keyof PublicListing, …>` σταματά προσθήκη σχήματος πριν
  //    προλάβει να τη δει άνθρωπος. Είκοσι τρία πεδία μπήκαν μαζί — και κανένα δεν
  //    μπορούσε να μπει χωρίς να απαντηθεί εδώ *«με ποιον τρόπο γίνεται ορατό;»*.
  //
  // ⚠️ **Η ΣΕΙΡΑ ΕΙΝΑΙ ΟΘΟΝΗ**: από αυτήν παράγεται η σειρά μέσα σε κάθε ομάδα
  //    ({@link ListingAttributeGroup}). Η **ομάδα** ζει σε δικό της πίνακα, γιατί
  //    απαντά **άλλο** ερώτημα (*«πού κάθεται;»*, όχι *«πώς φαίνεται;»*) — και είναι
  //    `Record` πάνω στα **παραγόμενα** κλειδιά αυτού εδώ, άρα οι δύο πίνακες **δεν
  //    μπορούν** να αποκλίνουν: δεν είναι δύο λίστες, είναι μία και η προβολή της.
  energyClass: 'attribute',
  condition: 'attribute',
  renovationYear: 'attribute',
  bathrooms: 'attribute',
  wc: 'attribute',
  totalRooms: 'attribute',
  levels: 'attribute',
  balconies: 'attribute',
  netAreaSqm: 'attribute',
  balconyAreaSqm: 'attribute',
  terraceAreaSqm: 'attribute',
  gardenAreaSqm: 'attribute',
  heatingType: 'attribute',
  heatingFuel: 'attribute',
  coolingType: 'attribute',
  waterHeating: 'attribute',
  windowFrames: 'attribute',
  glazing: 'attribute',
  // ── ΤΑ ΣΥΝΟΛΑ — τρεις καταστάσεις, δες τον ρόλο `'feature-set'` ─────────────
  flooring: 'feature-set',
  orientations: 'feature-set',
  interiorFeatures: 'feature-set',
  securityFeatures: 'feature-set',
  amenities: 'feature-set',
  // ───────────────────────────────────────────────────────────────────────────
  offerKinds: 'offers',
  position: 'position',
  // 🔴 **ΤΑ ΤΡΙΑ ΠΟΥ ΕΛΕΙΠΑΝ, ΠΡΟΣΤΕΘΗΚΑΝ 2026-08-31 (ADR-835 Φ3, κανόνας N.0.2).**
  //    Ο πίνακας δηλώνει `satisfies Record<keyof PublicListing, …>`, άρα **όφειλε** να
  //    τα έχει από τη μέρα που μπήκαν (§14.5 · §8.33). Η Κ1 δεν το έπιασε γιατί το
  //    **fixture της έλειπε τα ΙΔΙΑ τρία**: δύο ελλιπείς λίστες που συμφωνούν μεταξύ
  //    τους — ακριβώς το σχήμα «το 0 σήμαινε ότι κανείς δεν κοίταξε». Βρέθηκε όταν το
  //    `stay` έκανε τις δύο λίστες να **διαφωνήσουν**.
  place: 'place-link',
  authorship: 'authorship',
  agencyName: 'authorship',
  // ✅ **ADR-841 Α1** — και ο πίνακας **κοκκίνισε ξανά μόλις μπήκε το πεδίο**: τρίτη
  //    φορά που το `satisfies Record<keyof PublicListing, …>` πιάνει προσθήκη σχήματος.
  //    ⚠️ Δεν είναι `'authorship'`: δες τον ρόλο `'agency-link'` παραπάνω.
  agencyId: 'agency-link',
  stay: 'stay-terms',
  // ✅ **Α17 (ADR-838)** — και ο πίνακας **κοκκίνισε μόλις μπήκε το πεδίο**, που είναι
  //    ακριβώς η δουλειά του. Είναι η **δεύτερη** φορά μέσα σε μία μέρα που το
  //    `satisfies Record<keyof PublicListing, …>` πιάνει προσθήκη σχήματος — τη
  //    **πρώτη** φορά (§18.7) την είχε χάσει, γιατί το fixture της άγκυρας έλειπε τα
  //    ίδια πεδία. Το fixture είναι πλέον **παραγόμενο**, δες την Κ1.
  legality: 'legality',
  projectedAt: 'provenance',
} as const satisfies Record<keyof PublicListing, DisclosureTreatment>;

// ============================================================================
// 2. ΤΑ ΠΑΡΑΓΟΜΕΝΑ ΚΛΕΙΔΙΑ — από τον πίνακα, ποτέ ξαναγραμμένα
// ============================================================================

/**
 * Τα κλειδιά του πίνακα που φέρουν έναν συγκεκριμένο ρόλο — **τύπος παραγόμενος από
 * το ίδιο literal**.
 *
 * 🔑 **Γενικεύτηκε όταν μπήκε ο δεύτερος ρόλος-με-κατάλογο** (ADR-842 Φ3). Η
 * προηγούμενη μορφή ήταν γραμμένη δύο φορές μέσα στον ίδιο τύπο για το `'attribute'`·
 * ένα δεύτερο αντίγραφο για το `'feature-set'` θα ήταν **δίδυμος κλώνος στο ίδιο
 * αρχείο** — ακριβώς αυτό που το `jscpd:diff` (N.18) υπάρχει για να πιάσει, και το
 * κλασικό λάθος που ο ίδιος ο κανόνας περιγράφει: *«κεντρικοποιείς το Α, γράφεις Β ως
 * δίδυμο»*.
 */
type KeysWithTreatment<T extends DisclosureTreatment> = {
  [K in keyof typeof LISTING_DISCLOSURE]: (typeof LISTING_DISCLOSURE)[K] extends T ? K : never;
}[keyof typeof LISTING_DISCLOSURE];

/**
 * Τα πεδία με ρόλο `'attribute'`.
 *
 * 🔑 Ένα νέο `'attribute'` πεδίο μπαίνει **αυτόματα** και στον τύπο και στον κατάλογο,
 * και ταυτόχρονα **σπάει** το εξαντλητικό `Record` του `ATTRIBUTE_DECLARED`
 * (`./listing-attribute-declared`) — δηλαδή δεν μπορεί να προστεθεί χωρίς να απαντηθεί
 * «πότε θεωρείται δηλωμένο;».
 */
export type ListingAttributeKey = KeysWithTreatment<'attribute'>;

/**
 * Τα πεδία με ρόλο `'feature-set'` (ADR-842 Φ3).
 *
 * ⚠️ **Η εγγύηση εδώ ΔΕΝ είναι ίδια με των ιδιοτήτων, και το λέω αντί να το υπονοήσω.**
 * Η ανάγνωση ενός συνόλου είναι `listing[key]` — **ολική εξ ορισμού**, οπότε δεν
 * υπάρχει `switch` να σπάσει και ένα ceremonial table εδώ θα ήταν τελετουργία, όχι
 * φρουρός. Ένα νέο σύνολο σταματιέται σε **δύο άλλα** σημεία, και τα δύο `Record`
 * πάνω σε αυτόν ακριβώς τον τύπο: το `LISTING_ATTRIBUTE_GROUP` (*«πού κάθεται;»*) και
 * το `FEATURE_SET_VALUE_LABEL_NS` (*«με ποιο λεξιλόγιο ονομάζονται οι τιμές του;»*).
 */
export type ListingFeatureSetKey = KeysWithTreatment<'feature-set'>;

/**
 * Ο κατάλογος των ιδιοτήτων, **στη σειρά του πίνακα**.
 *
 * ⚠️ Παράγεται με `Object.keys`, που για κλειδιά-συμβολοσειρές διατηρεί τη **σειρά
 * εισαγωγής** (ECMAScript `OrdinaryOwnPropertyKeys`). Ίδιο ιδίωμα με το
 * `LOCATION_PROVENANCES` (`lib/location/location-provenance`), που παράγεται από τον
 * πίνακα κατάταξης αντί να ξαναγραφεί δίπλα του.
 */
export const LISTING_ATTRIBUTE_KEYS: readonly ListingAttributeKey[] = keysWithTreatment(
  'attribute'
) as readonly ListingAttributeKey[];

/** Ο κατάλογος των συνόλων, **στη σειρά του πίνακα**. */
export const LISTING_FEATURE_SET_KEYS: readonly ListingFeatureSetKey[] = keysWithTreatment(
  'feature-set'
) as readonly ListingFeatureSetKey[];

/**
 * ⚠️ **Μία υλοποίηση για δύο καταλόγους** — δες το {@link KeysWithTreatment} για τον
 * λόγο. Ο ισχυρισμός τύπου στους δύο καταναλωτές είναι η **μετάφραση** του φίλτρου
 * χρόνου εκτέλεσης στο μαπαρισμένο τύπο, που η TypeScript δεν συνάγει από `filter`.
 */
function keysWithTreatment(
  treatment: DisclosureTreatment
): readonly (keyof typeof LISTING_DISCLOSURE)[] {
  return (Object.keys(LISTING_DISCLOSURE) as (keyof typeof LISTING_DISCLOSURE)[]).filter(
    (key) => LISTING_DISCLOSURE[key] === treatment
  );
}

// ============================================================================
// 3. Η ΛΟΓΙΣΤΙΚΗ — ΜΕΤΑΚΟΜΙΣΕ, ΚΑΙ ΕΙΝΑΙ SPLIT ΟΧΙ TRIM
// ============================================================================

/**
 * 🔑 **Το «*πότε θεωρείται δηλωμένο;*» έφυγε σε δικό του αρχείο** (ADR-842 Φ3, N.7.1).
 *
 * Ο λόγος δεν είναι το μέγεθος: είναι ότι πρόκειται για **άλλο ερώτημα**. Αυτό το
 * αρχείο απαντά *«με ποιον τρόπο γίνεται ορατό κάθε πεδίο;»* — μια δήλωση. Εκείνο
 * απαντά *«έχει τιμή αυτό το πεδίο σε αυτή την αγγελία;»* — μια **κρίση πάνω σε
 * δεδομένα**, που μεγάλωσε από 4 σε 22 απαντήσεις (και άλλαξε από `switch` σε
 * εξαντλητικό `Record` γι' αυτόν ακριβώς τον λόγο — δες την κεφαλίδα εκεί).
 *
 * ⛔ **ΚΑΙ ΤΑ ΟΝΟΜΑΤΑ ΔΕΝ ΕΠΑΝΕΞΑΓΟΝΤΑΙ ΑΠΟ ΕΔΩ — ΕΠΙΤΗΔΕΣ.** Η επανεξαγωγή ήταν η
 * πρώτη γραφή (πρότυπο `public-listing-projection-types.ts`, Α12.10) και θα έσωζε δύο
 * γραμμές σε δύο καταναλωτές· θα γεννούσε όμως **κύκλο εισαγωγών** — εκείνο το αρχείο
 * εισάγει τους καταλόγους από **αυτό**. Ο κύκλος θα δούλευε σήμερα (οι κατάλογοι
 * διαβάζονται σε χρόνο κλήσης, όχι φόρτωσης) και θα έσπαγε την πρώτη φορά που κάποιος
 * θα ήθελε τιμή του σε χρόνο φόρτωσης — δηλαδή θα ήταν **παγίδα με χρονοδιακόπτη**.
 * Δύο εισαγωγές παραπάνω είναι φθηνότερες από έναν κύκλο.
 *
 * ⇒ Οι καταναλωτές εισάγουν από `./listing-attribute-declared`.
 */

// ============================================================================
// 4. ΤΑ ΘΕΜΑΤΑ ΠΟΥ Η Α3 ΥΠΟΣΧΕΘΗΚΕ ΚΑΙ ΔΕΝ ΥΠΑΡΧΟΥΝ — δηλωμένα, όχι σιωπηλά
// ============================================================================

/**
 * **Τι υποσχέθηκε η Α3 για την οθόνη 3 και δεν υπάρχει στον κώδικα σήμερα.**
 *
 * 🔴 **Δεν είναι λίστα «μελλοντικών χαρακτηριστικών» — είναι μέρος της αποκάλυψης.**
 * Η **Α7** δεσμεύτηκε *«δεν θα κρύψουμε τίποτε»*· η σιωπή για ένα θέμα που ο
 * επισκέπτης **περιμένει** να βρει σε σελίδα ακινήτου (νομιμότητα, κάτοψη, φάκελος)
 * διαβάζεται ως *«δεν έχει»*, όχι ως *«δεν το δημοσιεύουμε ακόμη»*. Δύο πολύ
 * διαφορετικοί ισχυρισμοί, και ο πρώτος είναι **ψευδής**.
 *
 * | Θέμα | Γιατί λείπει | Πού ανήκει η θεραπεία |
 * |---|---|---|
 * | ~~`legality`~~ | ✅ **ΕΦΥΓΕ 2026-08-31 (ADR-838)** — δες παρακάτω | — |
 * | `floorplan` | **καμία** εγγραφή στο κλειστό σχήμα — δεν είναι «άδειο πεδίο», δεν υπάρχει πεδίο | παραγωγός artifact (Α19, κανόνας 31) |
 * | `dossier` | ο φάκελος εγγράφων ζει σε **tenant-scoped** συλλογές· δημοσιοποίηση = απόφαση, όχι σύνδεσμος | ξεχωριστή απόφαση δημοσιοποίησης |
 *
 * ⛔ **ΜΗΝ τα «λύσεις» επινοώντας τιμή στην οθόνη.** Μια νομιμότητα μαντεμένη από
 * μια σελίδα είναι **χειρότερη** από απούσα: η **Α17** υπάρχει επειδή η βεβαίωση
 * μηχανικού είναι **υποχρεωτική στη μεταβίβαση** (άρθρο 83 ν. 4495/2017) — ένας
 * επισκέπτης που διάβασε «νόμιμο» σε αγγελία και το βρήκε αλλιώς στο συμβολαιογραφείο
 * δεν έχασε ένα χαρακτηριστικό, έχασε την εμπιστοσύνη του στο σύνολο.
 *
 * ────────────────────────────────────────────────────────────────────────────
 * ✅ ΤΟ `legality` ΕΦΥΓΕ ΑΠΟ ΕΔΩ — ΚΑΙ Η ΑΦΑΙΡΕΣΗ ΕΙΝΑΙ Η ΑΠΟΔΕΙΞΗ
 * ────────────────────────────────────────────────────────────────────────────
 *
 * Το `ListingOpenSubjects.tsx` το είχε προβλέψει κατά λέξη: *«η μέρα που θα υπάρξει η
 * Α17 θα είναι **αφαίρεση μιας γραμμής από εδώ**, όχι ανακάλυψη ότι κάτι έλειπε»*.
 * Έγινε ακριβώς αυτό (**ADR-838**): η νομιμότητα δεν είναι πια *«δεν το δημοσιεύουμε»*
 * — είναι **δεδομένο** στο {@link PublicListing.legality}, ως **κλίμακα με προέλευση**.
 *
 * ⚠️ **Ο κατάλογος δεν άδειασε, και δεν επιτρέπεται να αδειάσει σιωπηλά**: μένουν
 * **δύο** θέματα. Αν κάποτε μείνει **κανένα**, η σωστή κίνηση είναι να το **πει** το
 * κουτί (*«τίποτα ανοιχτό»*) — όχι να εξαφανιστεί, γιατί εξαφάνιση είναι σιωπή, και
 * όλο αυτό το τμήμα υπάρχει επειδή η σιωπή διαβάζεται ως *«δεν έχει»*.
 */
export type ListingOpenSubject = 'floorplan' | 'dossier';

/**
 * Κλειστός κατάλογος, στη σειρά προτεραιότητας της Α3.
 *
 * *(Η νομιμότητα ήταν **πρώτη** εδώ μέχρι τις 2026-08-31· έφυγε επειδή **λύθηκε**.)*
 */
export const LISTING_OPEN_SUBJECTS: readonly ListingOpenSubject[] = [
  'floorplan',
  'dossier',
];
