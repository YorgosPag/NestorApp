/**
 * @fileoverview **ΠΟΥ ΕΙΝΑΙ ΑΥΤΟ ΠΟΥ ΔΕΝ ΧΩΡΑΕΙ ΣΤΟ ΚΑΔΡΟ;** — καθαρή γεωμετρία, μηδέν DOM.
 * @related ADR-332 D26 · components/shared/addresses/AddressMapCandidateLayer · lib/geo/format-geo-distance
 * @module lib/geo/offscreen-edge-indicator
 *
 * ────────────────────────────────────────────────────────────────────────────
 * 🔴 ΤΟ ΠΡΟΒΛΗΜΑ ΠΟΥ ΤΟ ΓΕΝΝΗΣΕ — μετρημένο ζωντανά 2026-09-05
 * ────────────────────────────────────────────────────────────────────────────
 *
 * Ο κατάλογος «Πιθανές Τοποθεσίες» για «Αθηνάς 5» σε έργο **Θεσσαλονίκης** δίνει πέντε
 * αληθινές διευθύνσεις σε απόσταση **292-318 χλμ**. Ο χάρτης δείχνει το εργοτάξιο σε
 * επίπεδο δρόμου. **Καμία** από τις πέντε δεν χωράει στο κάδρο, και ο άνθρωπος διαλέγει
 * διαβάζοντας ονόματα δήμων.
 *
 * Η προφανής απάντηση — *«κάνε fit-bounds ώστε να χωρέσουν όλοι»* — είναι η πρακτική του
 * Zillow, και **εδώ δεν δουλεύει**: στα 300 km το ορθογώνιο Θεσσαλονίκη+Αττική είναι μισή
 * Ελλάδα, όπου **και οι έξι** πινέζες γίνονται κουκκίδες. Δείχνει *«μακριά»*, όχι *«πού»*
 * — και ταυτόχρονα πετάει τον άνθρωπο έξω από το κάδρο **που δεν ζήτησε να αλλάξει**.
 * Ο Zillow δεν έχει ποτέ αυτό το πρόβλημα επειδή τα αποτελέσματά του είναι **εντός πόλης**.
 *
 * 🏆 **Η απάντηση είναι παλιά και δοκιμασμένη, απλώς όχι σε επιλογέα διεύθυνσης**: δείκτης
 * κολλημένος στην **άκρη του κάδρου**, στραμμένος προς τον στόχο *(«digital signposts» /
 * off-screen indicators)*. Η κάμερα **δεν κουνιέται**· ο άνθρωπος κρατά το κάδρο του έργου
 * και ταυτόχρονα βλέπει *«αυτός είναι νότια, 296 χλμ»*.
 *
 * ────────────────────────────────────────────────────────────────────────────
 * 🔑 ΓΙΑΤΙ ΕΙΝΑΙ ΞΕΧΩΡΙΣΤΟ ΑΡΧΕΙΟ, ΧΩΡΙΣ REACT ΚΑΙ ΧΩΡΙΣ ΧΑΡΤΗ
 * ────────────────────────────────────────────────────────────────────────────
 *
 * Ό,τι εδώ είναι **αριθμοί μέσα, αριθμοί έξω**: κανένα `map`, κανένα `window`, κανένα
 * `useEffect`. Αυτή είναι η μόνη μορφή στην οποία το ερώτημα *«σε ποιο σημείο της άκρης,
 * και με ποια γωνία;»* μπορεί να **αποδειχθεί** — και όχι απλώς να φανεί σωστό σε ένα
 * στιγμιότυπο. Η άγκυρα εκτελεί **αυτήν** τη συνάρτηση, όχι μια οθόνη που την καλεί.
 */

/** Σημείο σε **συντεταγμένες οθόνης**: `y` μεγαλώνει προς τα **κάτω** (σύμβαση DOM/canvas). */
export interface ScreenPoint {
  readonly x: number;
  readonly y: number;
}

/** Το ορατό κάδρο, σε pixel. */
export interface ScreenSize {
  readonly width: number;
  readonly height: number;
}

/** Πού μπαίνει ο δείκτης και προς τα πού δείχνει. */
export interface EdgeIndicator {
  /** Θέση στο κάδρο (pixel από την αριστερή ακμή). */
  readonly x: number;
  /** Θέση στο κάδρο (pixel από την πάνω ακμή). */
  readonly y: number;
  /**
   * Κατεύθυνση του στόχου, σε **μοίρες δεξιόστροφα από το πάνω** *(0° = πάνω/βορράς της
   * οθόνης, 90° = δεξιά)*.
   *
   * ⚠️ Η σύμβαση επιλέχθηκε ώστε ένα βέλος **σχεδιασμένο να δείχνει πάνω** να
   * περιστρέφεται με σκέτο `rotate(angleDeg)`. Η εναλλακτική *(μαθηματικές μοίρες
   * αριστερόστροφα από τον άξονα x)* θα απαιτούσε από **κάθε** καλούντα να θυμάται
   * μετατροπή — δηλαδή θα ήταν ακριβώς η παράμετρος που ο επόμενος ξεχνά.
   */
  readonly angleDeg: number;
}

/**
 * Ο δείκτης για έναν στόχο **εκτός κάδρου** — ή `null` όταν ο στόχος **φαίνεται**.
 *
 * 🔑 **Το `null` είναι η μισή αξία της συνάρτησης.** Όταν ο στόχος χωράει στο κάδρο,
 * ο άνθρωπος βλέπει την ίδια την πινέζα· ένας δείκτης άκρης από πάνω θα ήταν **δεύτερη
 * αναπαράσταση του ίδιου πράγματος**, δηλαδή δύο σημεία για μία διεύθυνση. Ο καλών
 * οφείλει να **μη ζωγραφίσει**, όχι να ζωγραφίσει κάτι ουδέτερο.
 *
 * @param target ο στόχος σε συντεταγμένες οθόνης *(π.χ. `map.project([lng, lat])`)*
 * @param size το ορατό κάδρο
 * @param inset περιθώριο από την ακμή, σε pixel — **ο μισός δείκτης**, ώστε το σήμα να
 *   μη βγαίνει κομμένο στο μισό. Ο στόχος θεωρείται «ορατός» μέσα σε αυτό το μικρότερο
 *   ορθογώνιο: μια πινέζα ακριβώς πάνω στην ακμή είναι **οπτικά κομμένη**, άρα δεν την
 *   έχει δει κανείς.
 *
 * ⚠️ **Κάθε μη πεπερασμένη είσοδος δίνει `null`.** Το `map.project()` επιστρέφει `NaN`
 * όταν ο χάρτης δεν έχει ακόμη διαστάσεις — και ένα `NaN` που περνά σε `transform` δεν
 * ρίχνει τίποτα: **εξαφανίζει σιωπηλά** τον δείκτη ή τον καρφώνει στη γωνία. Η ρητή
 * απόρριψη είναι η διαφορά ανάμεσα σε «δεν έχω απάντηση» και «απαντώ σκουπίδια».
 */
export function edgeIndicatorFor(
  target: ScreenPoint,
  size: ScreenSize,
  inset: number,
): EdgeIndicator | null {
  if (!Number.isFinite(target.x) || !Number.isFinite(target.y)) return null;
  if (!Number.isFinite(size.width) || !Number.isFinite(size.height)) return null;
  if (!Number.isFinite(inset) || inset < 0) return null;

  const centerX = size.width / 2;
  const centerY = size.height / 2;

  // Μισό πλάτος/ύψος του ορθογωνίου **μέσα** στο οποίο ο δείκτης επιτρέπεται να καθίσει.
  const halfX = centerX - inset;
  const halfY = centerY - inset;

  // Κάδρο μικρότερο από το ίδιο του το περιθώριο δεν έχει «άκρη» να δείξει κάτι.
  if (halfX <= 0 || halfY <= 0) return null;

  const dx = target.x - centerX;
  const dy = target.y - centerY;

  // Μέσα στο κάδρο ⇒ η ίδια η πινέζα φαίνεται. Καλύπτει και το ακριβές κέντρο.
  if (Math.abs(dx) <= halfX && Math.abs(dy) <= halfY) return null;

  /*
    Πόσο πρέπει να «μαζευτεί» η ακτίνα κέντρο→στόχος ώστε να ακουμπήσει την ακμή.

    Η τομή με ορθογώνιο είναι το **μικρότερο** από τα δύο πηλίκα: όποιος άξονας φτάνει
    πρώτος στο όριό του, αυτός ορίζει την ακμή. Ο έλεγχος `dx !== 0` δεν είναι
    παράνοια — στόχος ακριβώς πάνω/κάτω δίνει `dx === 0`, και το `halfX / 0` είναι
    `Infinity`, που το `Math.min` **σωστά** αγνοεί. Το επικίνδυνο θα ήταν `0 / 0`.
  */
  const scaleX = dx !== 0 ? halfX / Math.abs(dx) : Number.POSITIVE_INFINITY;
  const scaleY = dy !== 0 ? halfY / Math.abs(dy) : Number.POSITIVE_INFINITY;
  const scale = Math.min(scaleX, scaleY);

  if (!Number.isFinite(scale)) return null;

  return {
    x: centerX + dx * scale,
    y: centerY + dy * scale,
    angleDeg: screenAngleDeg(dx, dy),
  };
}

/**
 * Μοίρες **δεξιόστροφα από το πάνω**, στο διάστημα `[0, 360)`.
 *
 * ⚠️ Το `-dy` δεν είναι λάθος πρόσημο: στην οθόνη το `y` μεγαλώνει **προς τα κάτω**, οπότε
 * «πάνω» είναι αρνητικό `dy`. Χωρίς την αναστροφή, ο δείκτης θα έδειχνε **καθρεφτισμένα**
 * — και το σφάλμα θα ήταν αόρατο σε στόχο ακριβώς δεξιά ή αριστερά, δηλαδή θα περνούσε
 * από κάθε πρόχειρη δοκιμή.
 */
function screenAngleDeg(dx: number, dy: number): number {
  const degrees = (Math.atan2(dx, -dy) * 180) / Math.PI;
  return (degrees + 360) % 360;
}
