/**
 * @fileoverview **ΜΙΑ ΑΡΧΗ ΕΓΚΥΡΟΤΗΤΑΣ ΦΟΡΜΑΣ** — τρεις ερωτήσεις, με σειρά-συμβόλαιο.
 * @related ADR-777 §7 (Α9 · Α14 §17.2) · CLAUDE.md N.18 (jscpd) · N.0.2
 * @module lib/forms/draft-validation
 *
 * ────────────────────────────────────────────────────────────────────────────
 * 🔴 ΓΙΑΤΙ ΕΞΗΧΘΗ: **Η ΔΕΥΤΕΡΗ ΦΟΡΜΑ ΤΟ ΖΗΤΗΣΕ, ΚΑΙ ΤΟ ΕΙΧΕ ΠΡΟΒΛΕΨΕΙ Ο ΚΑΝΟΝΑΣ**
 * ────────────────────────────────────────────────────────────────────────────
 *
 * Το `validateDemandForm` (Α9) και το `validateOwnerPropertyForm` (Α14) απαντούν το
 * **ίδιο** ερώτημα για διαφορετικό λεξιλόγιο, με **ταυτόσημο σκελετό**: ανάλυση
 * σχήματος → συλλογή δυσανάγνωστων πεδίων → εμπόδια φόρμας → σύνθεση προσχεδίου →
 * παραβιάσεις οντότητας → `ready`/`incomplete`.
 *
 * Γραμμένο δεύτερη φορά, θα ήταν **κλώνος που το CHECK 3.28 μπλοκάρει** — και ο
 * κανόνας **N.0.2** το λέει πριν συμβεί: *«ποτέ copy-paste ενός σχήματος σε N αρχεία
 * — πρώτα φτιάξε το SSoT»*. Ο **Α9 πλήρωσε ήδη** αυτό το μάθημα δύο φορές μέσα σε ένα
 * commit (`ToggleOption`, `DemandOptionsField`).
 *
 * ⚠️ **Και δεν είναι μόνο κόστος γραμμών.** Δύο αντίγραφα θα απέκλιναν **στη σειρά**,
 * που εδώ είναι **συμβόλαιο** και όχι λεπτομέρεια — δες {@link validateDraftForm}.
 *
 * ────────────────────────────────────────────────────────────────────────────
 * 🔴 ΤΡΕΙΣ ΕΡΩΤΗΣΕΙΣ, ΤΡΕΙΣ ΚΡΙΤΕΣ — ΚΑΙ **ΚΑΝΕΝΑΣ ΔΕΝ ΖΕΙ ΕΔΩ**
 * ────────────────────────────────────────────────────────────────────────────
 *
 * | Ερώτηση | Ποιος απαντά | Πού ζει |
 * |---|---|---|
 * | «είναι αριθμός αυτό που πληκτρολόγησε;» | το **σχήμα** (zod) | `*-form-values.ts` |
 * | «λείπει βήμα για να **φτιαχτεί** το πράγμα;» | οι **blockers** | `*-form-values.ts` |
 * | «είναι **έγκυρο** πράγμα;» | τα **invariants** | 🔴 **`types/*.ts` — η ΙΔΙΑ συνάρτηση που φρουρεί την πύλη γραφής** |
 *
 * Το τρίτο είναι το σημαντικό, και αυτό το αρχείο **δεν το αγγίζει**: παίρνει τον
 * κριτή ως όρισμα. Ένα δεύτερο σύνολο κανόνων εδώ θα απέκλινε από την πύλη στην πρώτη
 * αλλαγή, και ο άνθρωπος θα έβλεπε «αποθηκεύεται…» και μετά αποτυχία **χωρίς πεδίο**
 * — το χειρότερο δυνατό μήνυμα.
 *
 * ────────────────────────────────────────────────────────────────────────────
 * 🔴 ΓΙΑΤΙ ΟΧΙ `Resolver` ΤΟΥ react-hook-form
 * ────────────────────────────────────────────────────────────────────────────
 *
 * Ένας `Resolver` αναφέρει σφάλματα **ανά πεδίο, στην υποβολή**. Η **Α14 §17.2**
 * δεσμεύτηκε στο αντίθετο: *«η φόρμα **μικραίνει** όσο δίνεις περισσότερα»* — άρα
 * υπολογίζεται **ολόκληρη η εικόνα, συνεχώς**, και η οθόνη τη δείχνει ως λίστα. Το
 * `react-hook-form` μένει αυτό που κάνει καλά (**κατάσταση πεδίων**) και δεν του
 * ανατίθεται πολιτική που δεν είναι δική του.
 *
 * *(Δευτερεύον, αλλά πραγματικό: το `@hookform/resolvers` **δεν είναι εγκατεστημένο**
 * και δεν εγκαταστάθηκε — κοινό δέντρο, μηδέν νέα εξάρτηση.)*
 *
 * **Layering**: leaf — καθαρή, γενική συνάρτηση. Καμία εξάρτηση από React ή zod.
 */

/**
 * Η πλήρης εικόνα μιας φόρμας — **ποτέ boolean**.
 *
 * 🔑 Τα τρία σκέλη του `incomplete` είναι **χωριστά** επειδή έχουν διαφορετική
 * θεραπεία για τον άνθρωπο: το `malformed` σημαίνει *«αυτό δεν είναι αριθμός»*, τα
 * `blockers` *«λείπει βήμα»*, τα `violations` *«αντιφάσκεις»*. Ένα κοινό «η φόρμα δεν
 * είναι έγκυρη» θα τον έστελνε να ψάξει.
 */
export type DraftFormValidation<
  TDraft,
  TBlocker extends string,
  TViolation extends string,
> =
  | {
      readonly kind: 'ready';
      readonly draft: TDraft;
    }
  | {
      readonly kind: 'incomplete';
      /** Πεδία που δεν διαβάζονται καν ως σχήμα (π.χ. γράμματα σε αριθμό). */
      readonly malformed: readonly string[];
      /** Λείπει βήμα της φόρμας — δεν είναι άκυρο, δεν είναι **ακόμη**. */
      readonly blockers: readonly TBlocker[];
      /** Αντιφάσεις της ίδιας της οντότητας. **Όλες**, ποτέ η πρώτη. */
      readonly violations: readonly TViolation[];
    };

/**
 * Το ελάχιστο που χρειάζεται αυτή η μηχανή από ένα σχήμα zod.
 *
 * 🔑 **Δομικός τύπος, ΟΧΙ `z.ZodType`** — και είναι σκόπιμο: το μόνο που ζητάμε είναι
 * *«ανάλυσε, και σε αποτυχία πες μου **ποια μονοπάτια**»*. Δεσμευμένο σε συγκεκριμένο
 * γενικό του zod, αυτό το αρχείο θα έσπαγε σε κάθε αναβάθμιση της βιβλιοθήκης για
 * λόγο που **δεν αφορά τη λογική του**. Το ίδιο ιδίωμα με το `ProjectableProperty`.
 */
export interface ParsesInto<TValues, TParsed> {
  safeParse(values: TValues):
    | { readonly success: true; readonly data: TParsed }
    | {
        readonly success: false;
        readonly error: { readonly issues: ReadonlyArray<{ readonly path: ReadonlyArray<string | number> }> };
      };
}

/** Οι τρεις κριτές μιας φόρμας, δοσμένοι από το χαρακτηριστικό. */
export interface DraftFormSpec<
  TValues,
  TParsed,
  TDraft,
  TBlocker extends string,
  TViolation extends string,
> {
  /** «Είναι αριθμός αυτό που πληκτρολόγησε;» */
  readonly schema: ParsesInto<TValues, TParsed>;
  /** «Λείπει βήμα για να **φτιαχτεί** το πράγμα;» */
  readonly blockersOf: (parsed: TParsed) => readonly TBlocker[];
  /** Οι τιμές → προσχέδιο οντότητας. */
  readonly draftOf: (parsed: TParsed) => TDraft;
  /** 🔴 «Είναι **έγκυρο** πράγμα;» — **η ίδια** συνάρτηση που φρουρεί την πύλη γραφής. */
  readonly violationsOf: (draft: TDraft) => readonly TViolation[];
}

/**
 * **Τιμές φόρμας → μπορεί να σταλεί;**
 *
 * ⚠️ **Το `malformed` κόβει ΠΡΙΝ από τα άλλα δύο, και είναι σειρά-συμβόλαιο**: όταν
 * το σχήμα δεν διαβάζεται, οι κανόνες θα έκριναν **τιμές που δεν υπάρχουν**. Ίδιο
 * πρότυπο με τη σειρά ταξινόμησης του **CHECK 3.47**: *μια ερώτηση που δεν μπορεί να
 * τεθεί δεν απαντιέται «όχι» — δεν τίθεται.*
 *
 * ⚠️ **Τα `blockers` και τα `violations` υπολογίζονται ΚΑΙ ΤΑ ΔΥΟ, ακόμη κι όταν το
 * πρώτο έχει ήδη ευρήματα.** Ένα short-circuit εδώ θα ήταν βελτιστοποίηση που
 * κοστίζει **ακριβώς** τη δέσμευση της Α14 §17.2: ο άνθρωπος δεν μπορεί να ξέρει
 * πόσο κοντά είναι αν του λέμε ένα σκέλος τη φορά.
 *
 * ⚠️ **Τα μονοπάτια αποδιπλασιάζονται**: ένα πεδίο με δύο σφάλματα σχήματος είναι
 * **ένα** πεδίο προς διόρθωση, όχι δύο μηνύματα.
 */
export function validateDraftForm<
  TValues,
  TParsed,
  TDraft,
  TBlocker extends string,
  TViolation extends string,
>(
  values: TValues,
  spec: DraftFormSpec<TValues, TParsed, TDraft, TBlocker, TViolation>,
): DraftFormValidation<TDraft, TBlocker, TViolation> {
  const parsed = spec.schema.safeParse(values);

  if (!parsed.success) {
    return {
      kind: 'incomplete',
      malformed: [...new Set(parsed.error.issues.map((issue) => issue.path.join('.')))],
      blockers: [],
      violations: [],
    };
  }

  const blockers = spec.blockersOf(parsed.data);
  const draft = spec.draftOf(parsed.data);
  const violations = spec.violationsOf(draft);

  if (blockers.length > 0 || violations.length > 0) {
    return { kind: 'incomplete', malformed: [], blockers, violations };
  }

  return { kind: 'ready', draft };
}

/**
 * **Εμπόδια που ΔΕΝ προκύπτουν από τις τιμές της φόρμας, στην ΙΔΙΑ λίστα.**
 *
 * ────────────────────────────────────────────────────────────────────────────
 * 🔴 ΓΙΑΤΙ ΥΠΑΡΧΕΙ — ΤΟ ΙΔΙΟ ΕΙΧΕ ΓΡΑΦΤΕΙ ΜΕΣΑ ΣΕ COMPONENT
 * ────────────────────────────────────────────────────────────────────────────
 *
 * Μια φόρμα έχει **δύο** ειδών εμπόδια: αυτά που διαβάζονται από ό,τι πληκτρολόγησε
 * ο άνθρωπος (`blockersOf`, παραπάνω) και αυτά που έρχονται από το **περιβάλλον** —
 * η εντολή του γραφείου δεν είναι πλήρης· δεν υπάρχει λογαριασμός για να σωθεί η
 * δουλειά. Τα δεύτερα **δεν** μπορούν να ζουν στο `blockersOf`: εκείνο βλέπει μόνο
 * `values`, και ένα «λείπει λογαριασμός» δεν είναι πεδίο της φόρμας.
 *
 * Μέχρι σήμερα η συγχώνευση ήταν γραμμένη **μέσα** στο `OwnerPropertyFormContent`,
 * για **έναν** καταναλωτή (την εντολή). Με τον δεύτερο (την ταυτότητα) θα γινόταν
 * δίδυμο — και το δίδυμο δεν θα ήταν στον κώδικα αλλά στη **σειρά**: ποιο εμπόδιο
 * μπαίνει πρώτο, τι γίνεται όταν η φόρμα είναι `ready` αλλά το περιβάλλον όχι.
 *
 * 🔑 **ΜΙΑ ΛΙΣΤΑ, ΠΟΤΕ ΔΥΟ** — η δέσμευση της Α14 §17.2 (*«ο άνθρωπος βλέπει πόσο
 * κοντά είναι»*): δύο λίστες «τι λείπει» σε μία οθόνη σημαίνουν ότι μπορεί να
 * διορθώσει τη μία, να δει το κουμπί ακόμη ανενεργό, και να μην ξέρει γιατί.
 *
 * ⚠️ **Το `ready` ΜΕ εξωτερικό εμπόδιο γίνεται `incomplete`, και είναι το κρίσιμο
 * σκέλος**: χωρίς αυτό, μια πλήρης φόρμα χωρίς λογαριασμό θα έδειχνε **ενεργό**
 * κουμπί που δεν κάνει τίποτα — δηλαδή ακριβώς το σιωπηλό αδιέξοδο. Η σιωπή είναι
 * χειρότερη από την άρνηση: η άρνηση τουλάχιστον λέει τι λείπει.
 *
 * ⚠️ **Κενή λίστα ⇒ ΤΟ ΙΔΙΟ αντικείμενο**, όχι αντίγραφο: αυτή η τιμή τροφοδοτεί
 * `useMemo`/`React.memo` παρακάτω, και ένα νέο αντικείμενο σε κάθε απόδοση θα
 * ακύρωνε σιωπηλά κάθε απομνημόνευση που στηρίζεται πάνω της.
 */
export function withExtraBlockers<TDraft, TBlocker extends string, TViolation extends string>(
  validation: DraftFormValidation<TDraft, TBlocker, TViolation>,
  extra: readonly TBlocker[],
): DraftFormValidation<TDraft, TBlocker, TViolation> {
  if (extra.length === 0) return validation;

  if (validation.kind === 'ready') {
    return { kind: 'incomplete', malformed: [], blockers: extra, violations: [] };
  }

  return { ...validation, blockers: [...validation.blockers, ...extra] };
}
