/**
 * =============================================================================
 * ΟΙ ΔΙΑΘΕΣΙΜΕΣ ΖΩΝΕΣ ΩΡΑΣ — ADR-777 §8.28
 * =============================================================================
 *
 * 🔑 **ΚΑΜΙΑ ΧΕΙΡΟΓΡΑΦΗ ΛΙΣΤΑ.** Αυθεντία είναι ο **ίδιος ο runtime**
 * (`Intl.supportedValuesOf('timeZone')`) — δηλαδή ακριβώς το σύνολο τιμών που θα
 * δεχτεί το `Intl.DateTimeFormat` όταν έρθει η ώρα να κριθεί μια ειδοποίηση.
 *
 * Ένας χειρόγραφος κατάλογος θα ήταν **δεύτερη αλήθεια** που αποκλίνει σιωπηλά: οι
 * ζώνες IANA αλλάζουν (νέες προστίθενται, παλιές γίνονται ψευδώνυμα) και η λίστα θα
 * γερνούσε χωρίς κανέναν να το μάθει — το ίδιο σχήμα με τις δύο λίστες namespace του
 * CHECK 3.34, που είχαν αποκλίνει κατά **63**.
 *
 * ⚠️ **Και έχει άμεση συνέπεια**: μια ζώνη που ο κατάλογος προσφέρει αλλά ο runtime
 * δεν αναγνωρίζει θα περνούσε την επικύρωση της οθόνης και θα **έπεφτε σιωπηλά στην
 * προεπιλογή** κατά την παράδοση. Ο χρήστης θα έβλεπε τη ρύθμισή του αποθηκευμένη
 * και **δεν θα ίσχυε** — δηλαδή «η ρύθμιση λέει ψέματα», ακριβώς το ελάττωμα που
 * διόρθωσε το §8.23.
 *
 * 🔶 **Εφεδρεία, δηλωμένη**: το `Intl.supportedValuesOf` είναι Baseline 2023 και
 * υπάρχει σε κάθε σύγχρονο περιβάλλον, αλλά **δεν** υποθέτουμε ότι υπάρχει: όταν
 * λείπει, επιστρέφεται η προεπιλογή **μόνη της** αντί για άδεια λίστα. Άδειος
 * επιλογέας θα ήταν οθόνη που δεν επιτρέπει καμία επιλογή — χειρότερη από μία.
 *
 * @module lib/datetime/supported-timezones
 * @see ADR-777 §8.28
 */

import { DEFAULT_NOTIFICATION_TIMEZONE } from '@/services/user-notification-settings/user-notification-settings.types';

/** Το `Intl` με τη μέθοδο που δεν δηλώνουν όλες οι εκδόσεις των τύπων. */
interface IntlWithSupportedValues {
  supportedValuesOf?: (key: 'timeZone') => string[];
}

/**
 * **Όλες οι ζώνες που αναγνωρίζει αυτό το περιβάλλον**, αλφαβητικά.
 *
 * ⚠️ Η προεπιλογή μπαίνει **πάντα**, ακόμη κι αν λείπει από τη λίστα: ο επιλογέας
 * δεν επιτρέπεται να μην περιέχει την τιμή που ήδη ισχύει, αλλιώς το Radix Select
 * εμφανίζεται **κενό** και ο χρήστης νομίζει ότι δεν έχει ρύθμιση.
 */
export function listSupportedTimeZones(): readonly string[] {
  const intl = Intl as unknown as IntlWithSupportedValues;
  const values = intl.supportedValuesOf?.('timeZone') ?? [];

  const unique = new Set<string>(values);
  unique.add(DEFAULT_NOTIFICATION_TIMEZONE);

  return [...unique].sort();
}
