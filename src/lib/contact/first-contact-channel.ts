/**
 * @fileoverview **ΑΠΟ ΠΟΙΟ ΚΑΝΑΛΙ ΦΕΥΓΕΙ Η ΠΡΩΤΗ ΕΠΑΦΗ** — μία απάντηση, δύο καταναλωτές.
 * @related components/contact/FirstContactDialog.tsx (ο δρόμος) ·
 *          components/contact/FirstContactDisclosureForm.tsx (το κείμενο) · ADR-844 §12
 * @module lib/contact/first-contact-channel
 *
 * ────────────────────────────────────────────────────────────────────────────
 * 🔴 ΓΙΑΤΙ ΥΠΑΡΧΕΙ — Η ΟΘΟΝΗ ΕΛΕΓΕ ΚΑΤΙ ΑΛΛΟ ΑΠΟ ΑΥΤΟ ΠΟΥ ΕΚΑΝΕ (2026-09-11)
 * ────────────────────────────────────────────────────────────────────────────
 *
 * Ο διάλογος είχε **δύο δρόμους** (ο φρουρός `isChannelProven`) και η φόρμα **ένα**
 * κείμενο: *«σας στέλνουμε σύνδεσμο επιβεβαίωσης»*. Ο συνδεδεμένος με επιβεβαιωμένο
 * email το διάβαζε, πατούσε — και **δεν ερχόταν τίποτα**, γιατί η πράξη είχε ήδη φύγει.
 *
 * Η ρίζα δεν ήταν το κείμενο· ήταν ότι **η απόφαση ζούσε σε ένα σημείο και η
 * περιγραφή της σε άλλο**, χωρίς τίποτα να τα δένει. Ένα `boolean` δεν μπορεί να
 * περιγράψει τέσσερις διαφορετικές συνέπειες.
 *
 * ⇒ **Μία κλειστή ένωση, μία συνάρτηση.** Ο διάλογος ρωτά *«είναι `proven`;»* για να
 * διαλέξει δρόμο, η φόρμα ρωτά *«ποιο είναι;»* για να διαλέξει κείμενο — από την
 * **ίδια τιμή της ίδιας απόδοσης**. Να διαφωνήσουν δεν είναι απίθανο· είναι
 * **αδύνατο**.
 *
 * | Κανάλι | Ποιος | Τι γίνεται |
 * |---|---|---|
 * | `proven` | συνδεδεμένος · επιβεβαιωμένο · ίδιο email | η πράξη φεύγει **αμέσως**, κανένα email |
 * | `unverified-account` | συνδεδεμένος · ίδιο email · **μη** επιβεβαιωμένο | σύνδεσμος στο **δικό του** email |
 * | `guest` | ανώνυμος | σύνδεσμος + **γέννηση λογαριασμού** (EDPB 2/2025) |
 * | `foreign-address` | συνδεδεμένος που στέλνει **άλλη** διεύθυνση | σύνδεσμος εκεί — η πράξη γράφεται στον λογαριασμό **εκείνης** |
 *
 * ⚠️ **Το `foreign-address` ΔΕΝ είναι νεκρό, είναι ΣΠΑΝΙΟ**: με το email του
 * συνδεδεμένου **δεμένο** ({@link boundEmailOf}) η φόρμα το φτάνει μόνο για λογαριασμό
 * **χωρίς** email. Μένει στο λεξιλόγιο γιατί ο **διακομιστής** το δέχεται
 * (`ensureCitizenIdentity` → `getUserByEmail` της πληκτρολογημένης) — και μια ένωση που
 * το παρέλειπε θα έλεγε ψέματα ακριβώς στην ακραία περίπτωση.
 *
 * **Layering**: leaf — καθαρές συναρτήσεις, κανένα React, κανένα `server-only`.
 */

import { sameChannelEmail } from './channel-email';

/**
 * **Τα τέσσερα κανάλια** — κλειστό σύνολο· κάθε κωδικός γίνεται κλειδί i18n.
 *
 * 🔑 Η σειρά είναι **από το συνηθέστερο στο σπανιότερο** για συνδεδεμένο θεατή — δεν
 * έχει σημασία για τη λογική, μόνο για όποιον διαβάζει.
 */
export const FIRST_CONTACT_CHANNELS = [
  'proven',
  'unverified-account',
  'guest',
  'foreign-address',
] as const;

export type FirstContactChannel = (typeof FIRST_CONTACT_CHANNELS)[number];

/**
 * **Ό,τι χρειάζεται ο ταξινομητής από τον θεατή** — δύο πεδία, όχι ολόκληρος ο χρήστης.
 *
 * ⚠️ Δομικός τύπος επίτηδες: το `FirebaseAuthUser` τον ικανοποιεί αυτούσιο, και το leaf
 * **δεν** εισάγει τύπους από το `@/auth` — θα έσερνε το υποσύστημα ταυτότητας σε κάθε
 * καταναλωτή μιας καθαρής συνάρτησης.
 */
export interface ChannelViewer {
  readonly email: string | null;
  readonly emailVerified: boolean;
}

/**
 * **Από ποιο κανάλι θα φύγει αυτή η δήλωση;**
 *
 * @param viewer Ο συνδεδεμένος — ή `null` για τον ανώνυμο.
 * @param typedEmail Ό,τι θα σταλεί ως email της δήλωσης.
 *
 * ⛔ **ΜΗΝ αφαιρέσεις το `emailVerified` «γιατί είναι συνδεδεμένος»** (ADR-844 §6.γ):
 * η σύνδεση αποδεικνύει ότι κάποιος ξέρει τον **κωδικό**, όχι ότι διαβάζει το
 * **γραμματοκιβώτιο** — και το μήνυμα πάει στο γραμματοκιβώτιο.
 *
 * ⚠️ Η σύγκριση περνά από το `sameChannelEmail` — τον **ίδιο** κανονικοποιητή με τον
 * διακομιστή· το κενό δεν ταυτίζεται με τίποτα, άρα λογαριασμός **χωρίς** email δεν
 * γίνεται ποτέ `proven` πάνω σε κενή φόρμα.
 */
export function firstContactChannelOf(
  viewer: ChannelViewer | null,
  typedEmail: string,
): FirstContactChannel {
  if (viewer === null) return 'guest';
  if (!sameChannelEmail(viewer.email, typedEmail)) return 'foreign-address';
  return viewer.emailVerified ? 'proven' : 'unverified-account';
}

/**
 * **Το email στο οποίο είναι ΔΕΜΕΝΟΣ ο θεατής** — ή `null` αν δεν υπάρχει.
 *
 * 🔑 **Πρότυπο Airbnb/LinkedIn**: ο συνδεδεμένος **δεν** πληκτρολογεί email στη φόρμα
 * επαφής — η ταυτότητά του είναι ο λογαριασμός. Αλλιώς μια αλλαγή του πεδίου θα
 * έγραφε την επαφή σε **άλλον** λογαριασμό και θα **άλλαζε τη συνεδρία** του
 * φυλλομετρητή μετά την επιβεβαίωση, χωρίς ο άνθρωπος να το έχει ζητήσει.
 *
 * ⚠️ Κενό ή μόνο-κενά email λογαριασμού ⇒ `null`: δεν δένουμε τη φόρμα σε διεύθυνση
 * που **δεν υπάρχει** — ο άνθρωπος πρέπει να μπορεί να γράψει μία.
 */
export function boundEmailOf(viewer: ChannelViewer | null): string | null {
  const email = viewer?.email?.trim() ?? '';
  return email === '' ? null : email;
}
