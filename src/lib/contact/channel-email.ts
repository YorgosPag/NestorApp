/**
 * @fileoverview **ΠΟΤΕ ΔΥΟ ΣΥΜΒΟΛΟΣΕΙΡΕΣ ΓΙΑ ΤΗΝ ΙΔΙΑ ΔΙΕΥΘΥΝΣΗ** — ο ΕΝΑΣ κανονικοποιητής.
 * @related services/contact/first-contact-invitation.service.ts · ADR-844
 * @module lib/contact/channel-email
 *
 * ────────────────────────────────────────────────────────────────────────────
 * 🔴 ΓΙΑΤΙ ΜΕΤΑΚΟΜΙΣΕ ΕΔΩ (2026-09-05) — ΓΙΑΤΙ Η ΟΘΟΝΗ ΡΩΤΑ ΤΗΝ ΙΔΙΑ ΕΡΩΤΗΣΗ
 * ────────────────────────────────────────────────────────────────────────────
 *
 * Γεννήθηκε μέσα στο `first-contact-invitation.service.ts`, που είναι `server-only`, και
 * απαντούσε **μία** ερώτηση: *«ποιο είναι το κλειδί ιδεμποτησίας και ο παραλήπτης;»*.
 *
 * Το Β5 του ADR-844 πρόσθεσε **δεύτερο** ρωτούντα, στον **φυλλομετρητή**: ο φρουρός του
 * `FirstContactDialog` ρωτά *«είναι το κανάλι ήδη αποδεδειγμένο;»* — δηλαδή **συγκρίνει**
 * ό,τι πληκτρολογήθηκε με το email του λογαριασμού. Δύο συγκρίσεις της ίδιας διεύθυνσης
 * με **διαφορετική** κανονικοποίηση είναι δύο απαντήσεις:
 *
 * | Πληκτρολογημένο | Λογαριασμός | Ο πελάτης λέει | Ο διακομιστής λέει |
 * |---|---|---|---|
 * | ` Maria@Example.COM ` | `maria@example.com` | ✅ ίδιο *(με trim+lower)* | ✅ ίδιο |
 * | ` Maria@Example.COM ` | `maria@example.com` | ❌ **διαφορετικό** *(ωμή σύγκριση)* | ✅ ίδιο |
 *
 * Η δεύτερη γραμμή είναι η βλάβη: ο άνθρωπος **με αποδεδειγμένο** email θα έπαιρνε
 * περιττή πρόσκληση, επειδή ο φυλλομετρητής κράτησε τα κεφαλαία που εκείνος έγραψε.
 *
 * ⛔ **ΜΗΝ ξαναγράψεις `trim().toLowerCase()` σε καλούντα.** Είναι μία γραμμή, και γι'
 * αυτό ακριβώς αντιγράφεται — μέχρι την ημέρα που η μία από τις τρεις αποκτήσει
 * `normalize('NFKC')` και οι άλλες δύο όχι.
 *
 * **Layering**: leaf — καθαρή συνάρτηση. Κανένα `server-only`, καμία ανάγνωση, κανένα
 * React· γι' αυτό είναι ασφαλής **και στις δύο** πλευρές.
 */

/**
 * **Πεζά, χωρίς κενά** — η μορφή στην οποία μια διεύθυνση **ταυτίζεται με τον εαυτό της**.
 *
 * ⚠️ **ΔΕΝ επικυρώνει.** Το *«είναι email;»* το απαντά το `isValidEmail`
 * (`lib/validation/email-validation.ts`), και είναι **άλλη** ερώτηση: αυτό εδώ δέχεται
 * ό,τι του δώσεις και το φέρνει σε κανονική μορφή. Ένας κανονικοποιητής που **κρίνει**
 * θα ήταν δεύτερος ορισμός του τι είναι email.
 *
 * ⚠️ **Μόνο πεζά, ΚΑΜΙΑ αφαίρεση τελειών ή `+tag`.** Το `maria.k+spiti@gmail.com` και το
 * `mariak@gmail.com` είναι το **ίδιο κουτί** στη Gmail — αλλά η «εξυπνάδα» αυτή είναι
 * **προμηθευτο-ειδική** και **λάθος** για τους περισσότερους διακομιστές. Θα σήμαινε ότι
 * δύο ανθρώποι με νόμιμα διαφορετικές διευθύνσεις μοιράζονται πρόσκληση.
 */
export function normaliseChannelEmail(raw: string): string {
  return raw.trim().toLowerCase();
}

/**
 * **Είναι η ίδια διεύθυνση;** — η σύγκριση, ποτέ χειρόγραφη.
 *
 * 🔑 Το κενό **δεν ταυτίζεται με τίποτα, ούτε με άλλο κενό**: ένας λογαριασμός χωρίς
 * email *(σύνδεση με τηλέφωνο)* θα «ταίριαζε» με κενή φόρμα, και ο φρουρός θα έλεγε
 * *«αποδεδειγμένο»* για κανάλι που **δεν υπάρχει**.
 */
export function sameChannelEmail(a: string | null | undefined, b: string | null | undefined): boolean {
  const left = normaliseChannelEmail(a ?? '');
  const right = normaliseChannelEmail(b ?? '');
  return left !== '' && left === right;
}
