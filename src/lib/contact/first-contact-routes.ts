/**
 * @fileoverview **Οι διευθύνσεις της πρώτης επαφής** — γραμμένες μία φορά.
 * @related ADR-843 §10.19 · lib/demand/demand-routes.ts (το πρότυπο) · lib/workspace/workspace-scope.ts
 * @module lib/contact/first-contact-routes
 *
 * ────────────────────────────────────────────────────────────────────────────
 * 🔴 ΓΙΑΤΙ `/first-contacts` ΚΑΙ ΟΧΙ `/contacts` — ΜΕΤΡΗΜΕΝΗ ΒΛΑΒΗ, ΟΧΙ ΑΙΣΘΗΤΙΚΗ
 * ────────────────────────────────────────────────────────────────────────────
 *
 * Το `/contacts` ανήκει στις επαφές του **γραφείου** (`/o/<χώρος>/contacts`). Ο κριτής
 * `isInsideWorkspace` απαντά **ανά τμήμα**, άρα ένα τμήμα με δύο ιδιοκτήτες σημαίνει
 * ότι ο ένας **χάνει**. Από 2026-09-04 ως 2026-09-10 έχανε το γραφείο: ο σύνδεσμος
 * «Επαφές» του υπαλλήλου έμενε χωρίς πρόθεμα, και η στατική σελίδα του `(me)` κέρδιζε
 * το δίχτυ — ο υπάλληλος έβλεπε τις **ιδιωτικές** επαφές του αντί για το CRM.
 *
 * 🔑 **Η λέξη είναι ΙΔΙΑ με το API** (`/api/first-contacts`), **τη συλλογή**
 * (`first_contacts`) **και το λεξιλόγιο** (`first-contact-*`): διεύθυνση = API = βάση.
 * Πρότυπο: LinkedIn Invitation Manager — **ένα** όνομα, **δύο** όψεις.
 *
 * ⛔ Τη σύγκρουση την κάνει πλέον **αδύνατη** ο κανόνας Κ3 της CHECK 3.60
 * (`segment-collision`) — όχι αυτό το σχόλιο.
 *
 * ⚠️ **Ένα route group δεν είναι η διαδρομή**: το `app/(me)/first-contacts` σερβίρει
 * `/first-contacts`.
 *
 * **Layering**: leaf — μόνο σταθερές.
 */

/** **«Οι επαφές μου»** — όσους πλησίασε ο άνθρωπος, με το υπόλοιπο χωρητικότητας. */
export const MY_FIRST_CONTACTS_ROUTE = '/first-contacts' as const;

/**
 * **«Ποιοι με πλησίασαν»** — η **άλλη** όψη του ίδιου ανθρώπου.
 *
 * ⚠️ Ζει στο **ίδιο** τμήμα επίτηδες: ο κριτής χώρου απαντά ανά τμήμα, και οι δύο
 * όψεις είναι του ίδιου ανθρώπου. Η εμβέλεια των εισερχομένων κρίνεται στη **βάση**
 * (`ownListingIds`, CHECK 3.56), όχι από τη διεύθυνση.
 */
export const FIRST_CONTACTS_INBOX_ROUTE = '/first-contacts/inbox' as const;
