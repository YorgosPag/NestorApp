/**
 * @fileoverview **ΠΟΥ ΖΩΓΡΑΦΙΖΕΤΑΙ ΤΟ ΣΗΜΑ — ΣΕ ΑΡΙΘΜΟΥΣ** (ADR-841 §7 Α21.13).
 * @related ADR-841 §7 Α21.9 *(το κουτί)* · Α21.13 *(ο κριτής της εξόδου)* ·
 *   components/mandate/showcase-mark-box · components/mandate/showcase-mark-frame ·
 *   lib/agency/showcase-mark-fidelity
 * @module lib/agency/showcase-mark-surfaces
 *
 * ════════════════════════════════════════════════════════════════════════════
 * 🔴 ΓΙΑΤΙ ΥΠΑΡΧΕΙ: ΔΥΟ ΑΡΧΕΙΑ ΡΩΤΟΥΣΑΝ «ΠΟΣΟ ΜΕΓΑΛΟ ΦΑΙΝΕΤΑΙ;» ΚΑΙ ΜΟΝΟ ΕΝΑ ΗΞΕΡΕ
 * ════════════════════════════════════════════════════════════════════════════
 *
 * Το `showcase-mark-box` απαντά *«πόσο χώρο πιάνει το σήμα;»* σε **κλάσεις Tailwind**, και
 * ζει στο `components/` επειδή ο σαρωτής **δεν διαβάζει το `src/lib/`**. Ο κριτής
 * ({@link module:lib/agency/showcase-mark-fidelity}) ρωτά **το ίδιο**, αλλά χρειάζεται
 * **αριθμούς** — και ζει στο `lib/`, γιατί τον καλεί και ο διακομιστής της άγκυρας.
 *
 * 🔴 **Η ΠΡΟΗΓΟΥΜΕΝΗ ΓΡΑΦΗ ΕΛΥΣΕ ΤΟ ΔΙΛΗΜΜΑ ΜΕ ΑΝΤΙΓΡΑΦΗ, ΚΑΙ Η ΑΝΤΙΓΡΑΦΗ ΠΑΛΙΩΣΕ.**
 * Ο κριτής κρατούσε `CARD_EDGE = 44` και `PAGE_EDGE = 96` ως *«καθρέφτη του `MARK_SIZES`»*,
 * με άγκυρα να τον φυλά. Η άγκυρα **έκανε τη δουλειά της για ό,τι ήξερε** — και ήξερε
 * **μόνο τα τετράγωνα**: το ίδιο της το σχόλιο έγραφε *«μόνο τα τετράγωνα κουτιά· το ύψος
 * της ζώνης δεν είναι επιφάνεια που ο κριτής ονομάζει»*. Δηλαδή από τη στιγμή που η
 * **Α21.9** έδωσε στο λογότυπο **ζώνη 64×240**, ο κριτής έκρινε κάθε wordmark με το κουτί
 * του **πορτρέτου** — και το αποτέλεσμα ήταν μετρήσιμο ψέμα *(δες Α21.13)*.
 *
 * ⇒ **Ένας ιδιοκτήτης**: εδώ ζουν οι **αριθμοί** και το ερώτημα *«ποιο σχήμα;»*· εκεί ζουν
 * οι **κλάσεις** που τους μεταφράζουν. Η αντιστοιχία δεν είναι δεύτερη αυθεντία — είναι
 * **μετάφραση**, και τη φυλά άγκυρα που διαβάζει τα ωμά αρχεία και **μπορεί να κοκκινίσει**.
 *
 * ⛔ **ΑΠΟΡΡΙΦΘΗΚΕ: «να εισάγει ο κριτής το `showcase-mark-box`».** Θα ήταν η **μοναδική**
 * εισαγωγή `lib/ → components/` σε παραγωγικό κώδικα ολόκληρου του `src/lib` *(μετρημένο:
 * σήμερα υπάρχουν **μόνο** μέσα σε `__tests__`)*, δηλαδή θα έσερνε την παρουσίαση μέσα στον
 * τομέα για χάρη τεσσάρων ακεραίων.
 *
 * ⛔ **ΑΠΟΡΡΙΦΘΗΚΕ: «να παράγει το `showcase-mark-box` τις κλάσεις από αυτούς τους
 * αριθμούς»** *(`h-${PAGE_EDGE / 4}`)*. Ο σαρωτής του Tailwind **δεν εκτελεί ποτέ
 * πρότυπα** — η κλάση θα έπαυε να υπάρχει, σιωπηλά. Είναι η ίδια παγίδα που τεκμηριώνει
 * ονομαστικά το `showcase-mark-frame`.
 *
 * ⚠️ **ΚΑΝΕΝΑ Tailwind ΕΔΩ, ΚΑΙ ΕΙΝΑΙ ΑΠΑΙΤΗΣΗ**: αν κάποιος γράψει κλάση σε αυτό το
 * αρχείο, ο σαρωτής **δεν θα τη δει** και θα δουλεύει μόνο όσο τη χρησιμοποιεί κάποιος
 * γείτονας. Αριθμοί και κατηγορήματα, τίποτε άλλο.
 */

import type { ShowcaseMarkKind } from '@/lib/agency/showcase-mark-kind';

/**
 * **Πώς γεμίζει η εικόνα το κουτί της** — η αριθμητική μισή απόφαση του `object-fit`.
 *
 * 🔑 Ο **λόγος** που ταξιδεύει μαζί με τις διαστάσεις: `contain` και `cover` δίνουν
 * **διαφορετική κλίμακα απόδοσης** για την ίδια εικόνα στο ίδιο κουτί, άρα διαφορετική
 * απάντηση στο *«θα φανεί καθαρή;»*. Ένα κουτί χωρίς αυτό είναι μισή ερώτηση.
 *
 * ⚠️ **Καθρέφτης του `SHOWCASE_MARK_FRAME.fit`** *(κλάσεις `object-contain` /
 * `object-cover`)*, και ο μεταγλωττιστής **δεν** μπορεί να δέσει τα δύο. Άγκυρα το φυλά —
 * το ίδιο ιδίωμα με την ομάδα **Ε** του `showcase-mark-fidelity.test`.
 */
export type MarkFill = 'contain' | 'cover';

/** Οι επιφάνειες που ζωγραφίζουν σήμα — **όσες υπάρχουν**, ονομαστικά. */
export type ShowcaseMarkSurfaceName = 'card' | 'page';

/**
 * **Μία επιφάνεια που ζωγραφίζει το σήμα**, σε **λογικά** εικονοστοιχεία (CSS px).
 *
 * ⚠️ **Λογικά και όχι φυσικά**: η πυκνότητα της οθόνης μπαίνει **μία** φορά, στον κριτή
 * ({@link MARK_GUARANTEED_DENSITY}). Ψημένη εδώ, θα έπρεπε να θυμάται κάθε αναγνώστης
 * **ποια** από τις δύο μονάδες κρατά ο πίνακας.
 */
export interface ShowcaseMarkSurface {
  readonly name: ShowcaseMarkSurfaceName;
  readonly width: number;
  readonly height: number;
  readonly fill: MarkFill;
}

/**
 * **Ένα δηλωμένο σήμα, με τις πραγματικές του διαστάσεις.**
 *
 * ⚠️ Οι διαστάσεις είναι **απαιτητικές**: το σχήμα του κουτιού προκύπτει από την αναλογία,
 * και ένα προαιρετικό πεδίο θα άφηνε ολόκληρη την Α21.9 να **ξεχαστεί** σιωπηλά.
 */
export interface DeclaredMarkSubject {
  readonly kind: ShowcaseMarkKind;
  readonly width: number;
  readonly height: number;
}

/**
 * **Τι είναι το σήμα, από τη σκοπιά του κουτιού.**
 *
 * 🔴 **Διακεκριμένη ένωση, ώστε το «λογότυπο χωρίς αναλογία» να ΜΗΝ ΕΙΝΑΙ ΕΚΦΡΑΣΙΜΟ.**
 * Με προαιρετικό `aspect?: number` ο καλών θα μπορούσε να το ξεχάσει και το σήμα θα
 * έπεφτε σιωπηλά στο τετράγωνο — δηλαδή το ελάττωμα της Α21.9, ξαναγεννημένο από
 * παράλειψη αντί από απόφαση.
 *
 * ⚠️ **Μετακόμισε εδώ από το `showcase-mark-box`** *(Α21.13)*: το *«τι είναι»* είναι
 * ερώτηση του **τομέα** — δεν έχει ούτε μία κλάση Tailwind μέσα της — και την ρωτούν πλέον
 * **δύο** στρώσεις. Ό,τι έμεινε εκεί είναι οι κλάσεις.
 */
export type MarkBoxSubject =
  /** Το παραγόμενο πλακίδιο αρχικών — **πάντα** τετράγωνο, δεν έχει αναλογία. */
  | { readonly kind: 'lettermark' }
  | DeclaredMarkSubject;

/**
 * **Πάνω από αυτή την αναλογία το λογότυπο παύει να χωράει σε τετράγωνο.**
 *
 * 🔑 **Γιατί 1,25 και όχι 1 ή 2** — το κατώφλι είναι το σημείο όπου η απώλεια γίνεται
 * αισθητή, και μετριέται: σε τετράγωνο, αναλογία `r` αφήνει `1/r` του ύψους.
 *
 * | r | ύψος μελανιού | κρίση |
 * |---|---|---|
 * | 1,10 | 91% | αδιάφορο — μια ζώνη εδώ θα ήταν **θόρυβος** |
 * | **1,25** | **80%** | **το όριο**: πιο κάτω δεν αξίζει αλλαγή σχήματος |
 * | 1,50 | 67% | αρχίζει να φαίνεται |
 * | 2,00 | 50% | το μισό σήμα είναι κενό |
 *
 * ⚠️ **Κάτω από 1 (κάθετο λογότυπο) ΔΕΝ υπάρχει δεύτερο κατώφλι, επίτηδες**: σε τετράγωνο
 * κουτί με `object-contain` ένα κάθετο σήμα γεμίζει το **ύψος** και μένει στενό — δηλαδή
 * **δεν χάνει τίποτα**. Τρίτη περίπτωση θα ήταν μηχανισμός χωρίς ζημιά να αποτρέψει.
 */
export const MARK_BAND_ASPECT_THRESHOLD = 1.25;

/**
 * **Χρειάζεται αυτό το σήμα ζώνη;** — η **μία** διατύπωση του κανόνα.
 *
 * ⚠️ **Μηδενικές ή αρνητικές διαστάσεις ⇒ τετράγωνο**, ποτέ διαίρεση με το μηδέν. Δεν
 * είναι αμυντικός κώδικας για φαντάσματα: το `width`/`height` έρχεται από **δημοσιευμένο
 * έγγραφο**, δηλαδή από δεδομένα που γράφτηκαν από παλιότερες εκδόσεις του καθαριστή και
 * διαβάζονται σήμερα.
 */
export function marksBand(subject: MarkBoxSubject): boolean {
  if (subject.kind !== 'logo') return false;
  if (subject.width <= 0 || subject.height <= 0) return false;

  return subject.width / subject.height > MARK_BAND_ASPECT_THRESHOLD;
}

/**
 * Η **πυκνότητα** για την οποία δίνουμε την εγγύηση: `2×`.
 *
 * 🔑 **Και όχι `3×`**, παρότι το ράφι φτάνει ως εκεί: το `2×` καλύπτει κάθε σύγχρονη οθόνη
 * υψηλής πυκνότητας, ενώ το `3×` θα προειδοποιούσε για **τηλέφωνα κορυφαίας σειράς** σε
 * εικόνες που φαίνονται τέλειες παντού αλλού. Μια προειδοποίηση που ανάβει σχεδόν πάντα
 * είναι **θόρυβος**, και ο άνθρωπος μαθαίνει να την προσπερνά — που είναι χειρότερο από το
 * να μην υπήρχε.
 */
export const MARK_GUARANTEED_DENSITY = 2;

/**
 * Η κάρτα του καταλόγου — **44×44**, τετράγωνη **και για τη ζώνη**.
 *
 * ⚠️ Δες *«γιατί η κάρτα μένει τετράγωνη»* στο `showcase-mark-box`: σε **σειρά** από
 * κάρτες, μεταβλητό πλάτος σήματος σπάει τον κατακόρυφο ρυθμό της στήλης.
 */
const CARD_EDGE = 44;

/** Η επικεφαλίδα της `/pro/<alias>` όταν το σήμα είναι **τετράγωνο** — 96×96. */
const PAGE_EDGE = 96;

/** Το **ύψος** της ζώνης στη σελίδα προφίλ — σταθερό, όπως ορίζει η σχολή Β. */
const BAND_HEIGHT = 64;

/** Το **ταβάνι πλάτους** της ζώνης: χωρίς αυτό, λογότυπο 10:1 σπρώχνει την επωνυμία έξω. */
const BAND_MAX_WIDTH = 240;

/**
 * **Πώς γεμίζει κάθε είδος το κουτί του** — δες {@link MarkFill}.
 *
 * 🔴 `Record<ShowcaseMarkKind, …>` πάνω στο **κλειστό σύνολο**: τρίτο είδος **δεν
 * μεταγλωττίζεται** μέχρι κάποιος να απαντήσει *«κόβεται ή χωράει;»* — ερώτηση που αλλάζει
 * το **αν θα φανεί καθαρό**, όχι μόνο το πώς δείχνει.
 */
const MARK_FILL: Readonly<Record<ShowcaseMarkKind, MarkFill>> = {
  logo: 'contain',
  portrait: 'cover',
};

/**
 * **Πώς γεμίζει αυτό το είδος** — η μία διατύπωση, δίπλα στο `maskCrops` του πλαισίου.
 */
export function markFill(kind: ShowcaseMarkKind): MarkFill {
  return MARK_FILL[kind];
}

/**
 * 🏆 **ΟΛΕΣ ΟΙ ΕΠΙΦΑΝΕΙΕΣ ΠΟΥ ΘΑ ΖΩΓΡΑΦΙΣΟΥΝ ΑΥΤΟ ΤΟ ΣΗΜΑ**, με το κουτί της καθεμιάς.
 *
 * 🔑 **Επιστρέφει πίνακα και όχι δύο πεδία**: ο κριτής ρωτά *«καλύπτονται **όλες**;»*, και
 * μια τρίτη επιφάνεια *(π.χ. η κεφαλίδα μιας μελλοντικής σελίδας)* οφείλει να μπει
 * **εδώ** — όχι σε τρίτο `if` μέσα στον κριτή. Το πλήθος είναι δεδομένο, όχι δομή.
 *
 * ⚠️ **Η ΣΕΙΡΑ ΔΕΝ ΕΙΝΑΙ ΣΥΜΒΟΛΑΙΟ.** Ο κριτής ρωτά με το `name`, ποτέ με δείκτη.
 */
export function markSurfaces(subject: DeclaredMarkSubject): readonly ShowcaseMarkSurface[] {
  const fill = markFill(subject.kind);
  const page: ShowcaseMarkSurface = marksBand(subject)
    ? { name: 'page', width: BAND_MAX_WIDTH, height: BAND_HEIGHT, fill }
    : { name: 'page', width: PAGE_EDGE, height: PAGE_EDGE, fill };

  return [{ name: 'card', width: CARD_EDGE, height: CARD_EDGE, fill }, page];
}

/**
 * 🏆 **Η ΚΛΙΜΑΚΑ ΑΠΟΔΟΣΗΣ** — πόσο **μεγεθύνει ή σμικρύνει** ο περιηγητής την εικόνα για
 * να τη βάλει σε αυτή την επιφάνεια.
 *
 * 🔴 **ΕΙΝΑΙ Ο ΙΔΙΟΣ ΤΥΠΟΣ ΠΟΥ ΕΚΤΕΛΕΙ ΤΟ CSS, ΟΧΙ ΠΡΟΣΕΓΓΙΣΗ ΤΟΥ**: το `object-contain`
 * διαλέγει τον **μικρότερο** από τους δύο λόγους *(χωράει ολόκληρη)*, το `object-cover`
 * τον **μεγαλύτερο** *(γεμίζει, και κόβει)*. Η προηγούμενη γραφή ρωτούσε *«η μικρή πλευρά
 * ≥ 192;»*, που είναι η **σωστή απάντηση μόνο για `cover` σε τετράγωνο** — δηλαδή για το
 * πορτρέτο, και για τίποτε άλλο.
 *
 * ⚠️ **Μη πεπερασμένες ή μηδενικές διαστάσεις ⇒ `Infinity`** — «απείρως μεγεθυσμένη»,
 * δηλαδή ποτέ καλυμμένη. Ένα `NaN` εδώ θα έκανε **κάθε** σύγκριση ψευδή, και η οθόνη θα
 * **σιωπούσε** για εικόνα που δεν υπάρχει καν: σιωπή που διαβάζεται ως «όλα καλά».
 */
export function markRenderScale(
  dimensions: { readonly width: number; readonly height: number },
  surface: ShowcaseMarkSurface,
): number {
  const { width, height } = dimensions;
  if (!Number.isFinite(width) || !Number.isFinite(height) || width <= 0 || height <= 0) {
    return Number.POSITIVE_INFINITY;
  }

  const byWidth = surface.width / width;
  const byHeight = surface.height / height;

  return surface.fill === 'cover' ? Math.max(byWidth, byHeight) : Math.min(byWidth, byHeight);
}
