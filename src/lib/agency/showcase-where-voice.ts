/**
 * @fileoverview SSoT — **ΤΙ ΕΧΕΙ ΝΑ ΠΕΙ Η ΟΘΟΝΗ ΓΙΑ ΤΟ ΠΟΥ ΨΑΧΝΕΙ Ο ΕΠΙΣΚΕΠΤΗΣ.**
 * @related types/agency-coverage.ts (η κλειστή ένωση) · components/mandate/AgencyDirectoryFilters ·
 *   components/mandate/DirectoryQueryState · hooks/useCircleAnchorName · ADR-846 §9 #12
 * @module lib/agency/showcase-where-voice
 *
 * ═════════════════════════════════════════════════════════════════════════════
 * 🔴 ΤΟ ΠΕΡΙΣΤΑΤΙΚΟ — **ΔΥΑΔΙΚΟΣ ΤΕΛΕΣΤΗΣ ΠΑΝΩ ΣΕ ΤΡΙΑΔΙΚΟ ΠΕΔΙΟ**
 * ═════════════════════════════════════════════════════════════════════════════
 *
 * Το `ShowcaseWhere | null` έχει **ΤΡΕΙΣ** καταστάσεις — `{adminId}` · `{circle}` · `null`.
 * Το `WhereControl` τις ισοπέδωνε σε **δύο**, με μία γραμμή:
 *
 * ```tsx
 * const areaId = where !== null && isAdministrativeWhere(where) ? where.adminId : '';
 * …
 * {areaId === '' ? t(DIRECTORY_KEYS.placeAll) : t(DIRECTORY_KEYS.areaHint)}
 * ```
 *
 * Το σκέλος `circle` έπεφτε στο `''`, δηλαδή **στον ίδιο κάδο με το «δεν φιλτράρω»** ⇒ με
 * `?lat=&lng=&r=5` η οθόνη έλεγε **ταυτόχρονα** *«10 από 22»* και *«Όλη η Ελλάδα»*.
 *
 * 🔑 **Και το σχόλιο από πάνω δήλωνε την ΑΝΤΙΘΕΤΗ πρόθεση**: *«Ο ΑΞΟΝΑΣ ΟΝΟΜΑΖΕΤΑΙ ΚΑΙ
 * ΣΤΙΣ ΔΥΟ ΚΑΤΑΣΤΑΣΕΙΣ. Σιωπή θα άφηνε τον επισκέπτη να νομίζει ότι φιλτράρει ενώ δεν
 * φιλτράρει.»* Ο συγγραφέας μέτρησε **δύο** καταστάσεις όπου υπήρχαν **τρεις**, και το
 * αποτέλεσμα ήταν η **ακριβώς αντίστροφη** βλάβη από αυτήν που φοβόταν.
 *
 * 🏆 **ΙΔΙΑ ΚΛΑΣΗ, ΗΔΗ ΛΥΜΕΝΗ ΜΙΑ ΦΟΡΑ** — `AgencyCard.tsx → COVERAGE_RELATION_KEYS`:
 * *«Το προηγούμενο `relation === 'within' ? … : …` ήταν δυαδικό: το `'unknown'` της
 * Φάσης 2 θα εμφανιζόταν σιωπηλά ως “καλύπτει μέρος” — δηλαδή ισχυρισμός που κανείς δεν
 * έκανε.»* Η θεραπεία εκεί ήταν `Record<ΚλειστήΈνωση, κλειδί>` που **δεν
 * μεταγλωττίζεται** αν προστεθεί τιμή. **Ίδια θεραπεία, δεύτερος καταναλωτής.**
 *
 * ─────────────────────────────────────────────────────────────────────────────
 * 🔒 ΓΙΑΤΙ **ΤΕΣΣΕΡΙΣ** ΦΩΝΕΣ ΚΑΙ ΟΧΙ ΤΡΕΙΣ — ΚΑΙ ΓΙΑΤΙ ΔΕΝ ΕΙΝΑΙ ΕΠΑΝΑΛΗΨΗ ΤΟΥ ΛΑΘΟΥΣ
 * ─────────────────────────────────────────────────────────────────────────────
 *
 * Το **πεδίο** έχει τρεις καταστάσεις· η **οθόνη** έχει τέσσερα πράγματα να πει, γιατί ο
 * κύκλος απαντά σε **δεύτερη, ανεξάρτητη** ερώτηση: *«ξέρουμε ΠΟΥ είναι το κέντρο του;»*.
 * Η απάντηση δεν βγαίνει από το πεδίο — **μετριέται** *(δες {@link useCircleAnchorName})*
 * και μπορεί να είναι **«δεν ξέρω»**.
 *
 * ⇒ Η ένωση εδώ είναι το **γινόμενο** των δύο ερωτήσεων, γραμμένο **ισοπεδωμένο και
 * κλειστό**, ώστε ο μεταγλωττιστής να μετρά αντί για τον άνθρωπο. Δύο ένθετα `?:` θα
 * ήταν ακριβώς το σχήμα που αυτό το αρχείο υπάρχει για να σβήσει.
 *
 * ⛔ **ΚΑΜΙΑ ΣΥΝΤΕΤΑΓΜΕΝΗ ΕΔΩ, ΠΟΤΕ.** Το `40.6307, 22.9469` δεν λέει τίποτα σε κανέναν
 * άνθρωπο. Όταν ο τόπος **δεν αποδεικνύεται**, η φωνή είναι `circlePlain` — *«το σημείο
 * που ορίσατε»* — και όχι ωμοί αριθμοί μεταμφιεσμένοι σε πληροφορία.
 */

import { isAdministrativeWhere, type ShowcaseWhere } from '@/types/agency-coverage';

/**
 * **Τι λέει η οθόνη** για τον άξονα του τόπου — κλειστή ένωση, τέσσερα σκέλη.
 *
 * ⚠️ **Ο διαχωριστής λέγεται `kind`**, ίδιο ιδίωμα με το `CredibilityNote['kind']`, ώστε
 * ένας `Record<ShowcaseWhereVoice['kind'], …>` να είναι **ολικός** και να **σπάει τη
 * μεταγλώττιση** στο πέμπτο σκέλος.
 */
export type ShowcaseWhereVoice =
  /** **Κανένας άξονας τόπου** — «Όλη η Ελλάδα», και είναι **αλήθεια**. */
  | { readonly kind: 'nationwide' }
  /** **Διοικητική περιοχή** — το ίδιο το χειριστήριο δείχνει ποια· εδώ μένει το «τι κάνει». */
  | { readonly kind: 'administrative' }
  /**
   * **Κύκλος με ΜΕΤΡΗΜΕΝΟ αγκυροβόλιο** — το βαθύτερο διοικητικό κελί που περιέχει
   * **αποδεδειγμένα** το κέντρο *(point-in-shape στην πηγή, ποτέ αντίστροφη
   * γεωκωδικοποίηση)*.
   */
  | { readonly kind: 'circleAnchored'; readonly radiusKm: number; readonly anchor: string }
  /**
   * **Κύκλος χωρίς αγκυροβόλιο** — και το `null` που τον γέννησε σημαίνει **«καμία
   * απόδειξη»**, ποτέ «πουθενά» *(N.12)*. Εδώ πέφτουν και τα δύο νόμιμα «δεν ξέρω»: η
   * ιεραρχία δεν έχει φορτώσει ακόμη, ή οι περιέκτες είναι αντιφατικοί.
   */
  | { readonly kind: 'circlePlain'; readonly radiusKm: number };

/**
 * **Η κλειστή ένωση → η φωνή.** Καθαρή συνάρτηση, μηδέν i18n, μηδέν React.
 *
 * 🔑 **Η ΜΕΤΑΦΡΑΣΗ ΣΕ ΚΕΙΜΕΝΟ ΔΕΝ ΓΙΝΕΤΑΙ ΕΔΩ, ΕΠΙΤΗΔΕΣ.** Οι δύο καταναλωτές λένε την
 * ίδια αλήθεια με **διαφορετικά λόγια** — ο υπαινιγμός κάτω από το πεδίο απαντά *«τι
 * κάνει αυτό το χειριστήριο;»*, το αφαιρούμενο σημάδι απαντά *«τι έχω ενεργό;»* — και
 * ένα κοινό κλειδί θα ανάγκαζε τη μία από τις δύο διατυπώσεις να διαβάζεται στραβά.
 * Ίδια απόφαση, γραμμένη ήδη δύο φορές: `coverageRadius` ⇄ `coverageDeclaredRadius`,
 * `listingsEmptyHint` ⇄ `listingsEmptyHintPro`.
 *
 * @param where Ο άξονας του τόπου — **η μία αυθεντία**, όπως έρχεται από τη διεύθυνση.
 * @param anchor Το **μετρημένο** όνομα του τόπου του κέντρου, ή `null` για «δεν ξέρω».
 *   Αγνοείται σε κάθε σκέλος πλην του κύκλου: σε διοικητική περιοχή το όνομα το δείχνει
 *   **το ίδιο το χειριστήριο**, και μια δεύτερη αναφορά του θα ήταν δεύτερη αυθεντία.
 */
export function showcaseWhereVoice(
  where: ShowcaseWhere | null,
  anchor: string | null,
): ShowcaseWhereVoice {
  if (where === null) return { kind: 'nationwide' };
  if (isAdministrativeWhere(where)) return { kind: 'administrative' };

  const radiusKm = where.circle.radiusKm;
  return anchor === null
    ? { kind: 'circlePlain', radiusKm }
    : { kind: 'circleAnchored', radiusKm, anchor };
}

/**
 * **Οι παράμετροι του κειμένου, ξεπακεταρισμένες από την ένωση** — μία φορά, για **δύο**
 * καταναλωτές *(ο υπαινιγμός του χειριστηρίου και το αφαιρούμενο σημάδι)*.
 *
 * ⚠️ **ΓΙΑΤΙ ΕΔΩ ΚΑΙ ΟΧΙ ΣΤΟΝ ΚΑΘΕΝΑ ΤΟΥΣ**: γραμμένη δύο φορές θα ήταν το **sibling
 * clone** που ο **N.18** ονομάζει — και το επιβαρυντικό είναι ότι τα δύο αντίγραφα θα
 * απέκλιναν **σιωπηλά**: το i18next **αγνοεί** τις περισσευούμενες παραμέτρους, άρα ένα
 * αντίγραφο που ξεχνά το `area` δεν σπάει, απλώς ζωγραφίζει κενό.
 *
 * ⛔ **ΚΑΝΕΝΑ ΚΟΙΝΟ `{ km, area }` ΓΙΑ ΟΛΑ ΤΑ ΣΚΕΛΗ.** Θα **δούλευε** — και θα έκρυβε ότι
 * δύο σκέλη **δεν έχουν** ακτίνα. Η επόμενη διατύπωση που θα έβαζε κατά λάθος `{km}` στο
 * «Όλη η Ελλάδα» θα ζωγράφιζε `undefined` **χωρίς κανένα σφάλμα**.
 *
 * 🔒 Με **δηλωμένο** τύπο επιστροφής, πέμπτο σκέλος χωρίς `case` **δεν μεταγλωττίζεται**:
 * η συνάρτηση θα μπορούσε να επιστρέψει `undefined`, και ο μεταγλωττιστής το λέει.
 */
export function whereVoiceParams(
  voice: ShowcaseWhereVoice,
): Readonly<Record<string, string | number>> {
  switch (voice.kind) {
    case 'nationwide':
    case 'administrative':
      return {};
    case 'circlePlain':
      return { km: voice.radiusKm };
    case 'circleAnchored':
      return { km: voice.radiusKm, area: voice.anchor };
  }
}

/*
 * ⛔ **ΔΕΝ ΥΠΑΡΧΕΙ `whereVoiceIsActive` — ΓΡΑΦΤΗΚΕ ΚΑΙ ΣΒΗΣΤΗΚΕ ΤΗΝ ΙΔΙΑ ΩΡΑ.**
 *
 * Το *«φιλτράρει αυτός ο άξονας;»* **δεν χρειάζεται** ξεχωριστό κατηγόρημα: ο πίνακας
 * κλειδιών του σημαδιού είναι **ολικός** και δηλώνει `null` στα δύο σκέλη που **δεν**
 * παίρνουν σημάδι *(δες `DirectoryQueryState → WHERE_CHIP_KEYS`)*. Ένα δεύτερο
 * κατηγόρημα θα ήταν **δεύτερη αυθεντία για το ίδιο ερώτημα**, με δυνατότητα να
 * διαφωνήσει με τον πίνακα — και θα ήταν επιπλέον **νεκρός κώδικας** *(CHECK 3.22)*.
 */
