/**
 * @fileoverview **ΠΟΥ ΚΟΙΤΑΖΕΙ Ο ΧΑΡΤΗΣ ΟΤΑΝ Ο ΕΠΑΓΓΕΛΜΑΤΙΑΣ ΔΗΛΩΝΕΙ ΕΜΒΕΛΕΙΑ** — ADR-846 Φ4.
 * @related types/geo/camera-frame · components/geo/use-camera-frame · lib/geo/geo-map-zoom
 * @module lib/agency/coverage-camera
 *
 * ────────────────────────────────────────────────────────────────────────────
 * 🔴 ΤΟ ΣΥΜΠΤΩΜΑ — ΔΥΟ ΦΟΡΕΣ, ΔΥΟ ΦΑΣΕΙΣ
 * ────────────────────────────────────────────────────────────────────────────
 *
 * Το `PlaceMap` διαβάζει `center` και `initialZoom` **όταν ανοίγει**, μία φορά. Άρα:
 *
 * - *(Φ2)* ο επαγγελματίας που αλλάζει **βήμα ακτίνας** με τον χάρτη ήδη ανοιχτό βλέπει
 *   τον κύκλο να μεγαλώνει **έξω από το κάδρο**·
 * - *(Φ3)* ο επαγγελματίας που **επιστρέφει** σε δημοσιευμένο **πολύγωνο μακριά** από την
 *   προεπιλογή βλέπει τον χάρτη στην αρχική θέση, με το σχήμα του πουθενά.
 *
 * ⚠️ **Καμία από τις δύο δεν σκάει**, και οι δύο διαβάζονται ως *«χάθηκε η δήλωσή μου»*.
 *
 * ────────────────────────────────────────────────────────────────────────────
 * ⛔ ΓΙΑΤΙ ΔΕΝ ΛΥΘΗΚΕ ΜΕ ΤΟ ΥΠΑΡΧΟΝ `focus`
 * ────────────────────────────────────────────────────────────────────────────
 *
 * Το `PlaceFocus` κουβαλά **βαθμό βεβαιότητας γεωκωδικοποιητή** — και το `PlaceMap` τον
 * **ζωγραφίζει** ως φωτοστέφανο. Η δήλωση εμβέλειας **δεν έχει** τέτοια έννοια: δεν είναι
 * «απάντηση με αβεβαιότητα», είναι **δήλωση ανθρώπου**. Ο δανεισμός θα ζωγράφιζε
 * περιθώριο σφάλματος **που δεν υπάρχει** — ψέμα στην οθόνη, όχι ακαθαρσία τύπων.
 *
 * 🔑 **Και δεν γράφτηκε δεύτερη μηχανή κίνησης**: η πτήση ζει **μία φορά** στο
 * {@link useCameraFrame}. Εδώ μένει **μόνο η μετάφραση** *«δήλωση → καρέ»*.
 *
 * ────────────────────────────────────────────────────────────────────────────
 * 🔑 ΚΑΜΙΑ ΝΕΑ ΓΕΩΜΕΤΡΙΑ — ΟΥΤΕ ΜΙΑ ΓΡΑΜΜΗ
 * ────────────────────────────────────────────────────────────────────────────
 *
 * Το ζουμ βγαίνει από το `mapZoomForRadiusKm` *(SSoT της Φ2, με γραμμένη την παραγωγή
 * Web Mercator)*, και η **ακτίνα οποιουδήποτε σχήματος** από το `ringsFootprint`
 * *(SSoT της Φ2.5)*. Ο κύκλος και το πολύγωνο απαντούν έτσι στην **ίδια** ερώτηση.
 */

import { ringsFootprint } from '@/lib/geo/geo-footprint';
import { mapZoomForRadiusKm } from '@/lib/geo/geo-map-zoom';
import {
  isOutlineCoverage,
  isRadiusCoverage,
  type DeclaredCoverage,
} from '@/types/agency-coverage';
import type { CameraFrame } from '@/types/geo/camera-frame';

/**
 * Το ύψος του χάρτη της εμβέλειας σε εικονοστοιχεία — **η μία τιμή**.
 *
 * 🔴 **Ήταν γραμμένη ΤΡΕΙΣ φορές** *(`CoverageRadiusPicker`, `CoverageOutlinePicker`,
 * `CoverageOutlineMap`, όλες `256`)*, δίπλα σε τρία `heightClass="h-64"` που έπρεπε να
 * συμφωνούν μαζί της **χωρίς κανέναν να το επιβάλλει**. Μια αλλαγή ύψους σε μία οθόνη
 * θα άφηνε το ζουμ της **λάθος** — και το λάθος θα ήταν *«ο κύκλος δεν χωράει»*, δηλαδή
 * ακριβώς το σύμπτωμα που αυτό το αρχείο θεραπεύει.
 *
 * ⚠️ **Μένει σε συμφωνία με το `h-64` του Tailwind** *(16rem = 256px)*. Αν αλλάξει το
 * ένα, αλλάζει το άλλο — δεν υπάρχει τρίτος τρόπος να το μάθει ο κώδικας.
 */
export const COVERAGE_MAP_HEIGHT_PX = 256;

/** Η κλάση ύψους που **αντιστοιχεί** στο {@link COVERAGE_MAP_HEIGHT_PX}. */
export const COVERAGE_MAP_HEIGHT_CLASS = 'h-64';

/**
 * **Πού πρέπει να κοιτάξει ο χάρτης για να χωρέσει ΑΥΤΗ η δήλωση** — ή `null`.
 *
 * ⚠️ **Το `null` σημαίνει «μην κουνηθείς», ΠΟΤΕ «πήγαινε στην αρχή»**: μια δήλωση χωρίς
 * γεωμετρία *(«όλη η Ελλάδα»· διοικητικές οντότητες)* δεν έχει κάδρο να ζητήσει, και μια
 * κάμερα που «επανέρχεται» θα έπαιρνε τον άνθρωπο από εκεί που κοιτούσε.
 *
 * ⛔ **Οι διοικητικές οντότητες ΔΕΝ καλύπτονται επίτηδες**: ο `CoverageAreaPicker` δεν
 * δείχνει χάρτη — διαλέγεις ονόματα από κατάλογο. Η προσθήκη τους εδώ θα ήταν σκέλος
 * **χωρίς καταναλωτή** *(N.7.2 #1)*. Όταν αποκτήσουν χάρτη, ο `footprintOf` δίνει έτοιμο
 * κέντρο και ακτίνα — η επέκταση είναι **τρεις γραμμές, εδώ**.
 */
export function coverageCameraFrame(coverage: DeclaredCoverage | null): CameraFrame | null {
  if (coverage === null) return null;

  if (isRadiusCoverage(coverage)) {
    const { center, radiusKm } = coverage.circle;
    return { kind: 'point', point: center, zoom: zoomForRadius(radiusKm) };
  }

  if (isOutlineCoverage(coverage)) {
    // 🔑 **Ο ίδιος υπολογισμός με τον κύκλο**, γιατί το `ringsFootprint` δίνει την
    //    περιγεγραμμένη ακτίνα **οποιουδήποτε** σχήματος. Δύο σχήματα, ένα ερώτημα.
    const footprint = ringsFootprint([coverage.outline]);
    if (footprint === null) return null;
    return { kind: 'point', point: footprint.center, zoom: zoomForRadius(footprint.outerKm) };
  }

  return null;
}

/** Το ζουμ που χωράει έναν κύκλο δοσμένης ακτίνας στο ύψος **αυτού** του χάρτη. */
function zoomForRadius(radiusKm: number): number {
  return mapZoomForRadiusKm(radiusKm, COVERAGE_MAP_HEIGHT_PX);
}
