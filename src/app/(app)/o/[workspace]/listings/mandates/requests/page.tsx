/**
 * **`/o/[workspace]/listings/mandates/requests` — τα ΕΙΣΕΡΧΟΜΕΝΑ του γραφείου** (ADR-827 §9.21).
 *
 * ────────────────────────────────────────────────────────────────────────────
 * 🔴 ΓΙΑΤΙ ΣΤΟ `(app)`, ΚΑΙ ΓΙΑΤΙ **ΚΑΤΩ ΑΠΟ** ΤΟΝ ΚΑΤΑΛΟΓΟ
 * ────────────────────────────────────────────────────────────────────────────
 *
 * Τα αιτήματα διαβάζονται κατά **`agencyCompanyId`**: είναι περιουσία του **γραφείου**,
 * όχι του υπαλλήλου — ο κατάλογος **επιβιώνει όταν ο υπάλληλος φύγει**. Το `(me)`
 * δηλώνεται ρητά ως *«ο ΙΔΙΩΤΙΚΟΣ ΧΩΡΟΣ ΤΟΥ ΙΔΙΩΤΗ»* στο `.shell-boundary.json`, άρα
 * μια εταιρική οθόνη εκεί θα έκανε τη δήλωση του group να **λέει ψέματα** (CHECK 3.52).
 *
 * 🔑 **Παιδί του `/listings/mandates`, όχι αδελφός**: τα εισερχόμενα είναι η **πηγή**
 * του καταλόγου — ό,τι γίνεται δεκτό εδώ εμφανίζεται εκεί. Η ιεραρχία της διεύθυνσης
 * λέει ακριβώς αυτό, και δίνει στη νέα οθόνη **διάδρομο** από γονέα που ήδη υπάρχει.
 *
 * ⛔ **ΔΕΝ ενώνεται με τον κατάλογο**: εκείνος δείχνει εντολές που **ήδη υπάρχουν**,
 * όπου αποφασίζει ο **ιδιώτης** — **αντίστροφη** κατεύθυνση έγκρισης. Δες
 * {@link MandateInboxContent}.
 *
 * Λεπτή σελίδα: όλη η ουσία ζει στο component, όπως το αδελφό `new/page.tsx`. Ο λόγος
 * είναι ο **έλεγχος**: ένα component δοκιμάζεται· ένα `page.tsx` του App Router όχι.
 *
 * ⚠️ **Καμία `useSearchParams`, κανένα εχθρικό API** — άρα καμία ανάγκη ορίου
 * `<Suspense>` (CHECK 3.55). Η φόρτωση γίνεται από τον πελάτη, μετά την προσάρτηση.
 *
 * @module app/(app)/o/[workspace]/listings/mandates/requests/page
 */

import { MandateInboxContent } from '@/components/mandate/MandateInboxContent';

export default function MandateInboxPage() {
  return <MandateInboxContent />;
}
