/**
 * **`/listings/mandates/new` — το γραφείο καταχωρεί για πελάτη** (ADR-777 §8.33).
 *
 * ────────────────────────────────────────────────────────────────────────────
 * 🔴 ΓΙΑΤΙ ΣΤΟ `(app)` ΚΑΙ ΟΧΙ ΣΤΟ `(me)` — Ο ΚΑΝΟΝΑΣ ΤΗΣ ΑΓΟΡΑΣ, ΟΧΙ ΠΡΟΤΙΜΗΣΗ
 * ────────────────────────────────────────────────────────────────────────────
 *
 * Στο MLS *«listings belong to the **broker**, not the agent»*: όταν ο υπάλληλος
 * αλλάξει γραφείο, οι αγγελίες **μένουν**, και μόνο ο ιδιοκτήτης του γραφείου μπορεί
 * να τις μεταφέρει — **γραπτώς**. Άρα η πράξη ανήκει στον **χώρο του γραφείου**, εκεί
 * που ζουν οι επαφές του και τα έργα του, όχι στον προσωπικό χώρο του υπαλλήλου.
 *
 * ⚠️ Το `(me)` δηλώνεται ρητά ως *«ο **ΙΔΙΩΤΙΚΟΣ ΧΩΡΟΣ ΤΟΥ ΙΔΙΩΤΗ**»*
 * (`.shell-boundary.json`). Μια αγγελία που ανήκει σε **εταιρεία** εκεί μέσα θα ήταν
 * η δήλωση του group να λέει ψέματα — και η δήλωση **είναι** το συμβόλαιο που φυλά η
 * CHECK 3.52.
 *
 * 🔑 **Και το μοντέλο συμφωνεί ήδη**: το `authorCompanyId` υπάρχει ως **απόδοση**
 * ακριβώς ώστε *«ο κατάλογος του γραφείου να επιβιώνει όταν ο υπάλληλος φύγει»*.
 *
 * @module app/(app)/listings/mandates/new/page
 */

import { BrokeredListingPageContent } from '@/components/mandate/BrokeredListingPageContent';

export default function NewBrokeredListingPage() {
  return <BrokeredListingPageContent />;
}
