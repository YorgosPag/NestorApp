/**
 * **`/o/<χώρος>/settings/agency-profile` — η βιτρίνα του γραφείου** (ADR-827 §9.10, #12).
 *
 * ────────────────────────────────────────────────────────────────────────────
 * 🔴 ΓΙΑΤΙ ΣΤΙΣ **ΡΥΘΜΙΣΕΙΣ ΤΟΥ ΧΩΡΟΥ** ΚΑΙ ΟΧΙ ΣΤΟ `(me)` Ή ΣΤΙΣ ΑΓΓΕΛΙΕΣ
 * ────────────────────────────────────────────────────────────────────────────
 *
 * Η δημοσίευση είναι πράξη **ΤΟΥ ΟΡΓΑΝΙΣΜΟΥ** — *«θέλω **να με** βρίσκουν»* — και το
 * έγγραφο έχει κλειδί το `companyId`. Άρα:
 *
 * - **όχι `(me)`**, που δηλώνεται ρητά *«ο **ΙΔΙΩΤΙΚΟΣ ΧΩΡΟΣ ΤΟΥ ΙΔΙΩΤΗ**»* στο
 *   `.shell-boundary.json`: εταιρική ταυτότητα εκεί θα έκανε τη δήλωση του group να
 *   λέει **ψέματα**, και η δήλωση **είναι** το συμβόλαιο που φυλά η CHECK 3.52·
 * - **όχι `listings/`**, γιατί δεν είναι αγγελία ούτε εντολή: είναι η **δημόσια
 *   ταυτότητα** του γραφείου, δίπλα στο `settings/company` που απαντά το αδελφό
 *   ερώτημα *«ποιοι είμαστε **προς τα μέσα**;»*.
 *
 * 🔑 **Και η διαφορά με το `settings/company` είναι όλο το §9.9 β**: εκείνο κρατά ό,τι
 * γράφτηκε για **λογιστική/συμβατική** χρήση· αυτό δημοσιεύει ό,τι γράφτηκε **ρητά για
 * δημοσίευση**. Ένα «απλώς αντίγραψέ τα» θα δημοσίευε την **κατοικία** του μεσίτη με
 * ατομική επιχείρηση.
 *
 * Λεπτή σελίδα: η ουσία ζει στο {@link AgencyShowcaseContent}, όπως τα δύο
 * `listings/mandates/*`. Ο λόγος είναι ο **έλεγχος** — ένα component δοκιμάζεται, ένα
 * `page.tsx` του App Router όχι.
 *
 * ⚠️ **Καμία `useSearchParams`, κανένα εχθρικό API** ⇒ καμία ανάγκη ορίου
 * `<Suspense>` (CHECK 3.55).
 *
 * @module app/(app)/o/[workspace]/settings/agency-profile/page
 */

import { AgencyShowcaseContent } from '@/components/mandate/AgencyShowcaseContent';

export default function AgencyShowcasePage() {
  return <AgencyShowcaseContent />;
}
