'use client';

import { LazyRoutes } from '@/utils/lazyRoutes';

export default function DatabaseUpdatePage() {
  const DatabaseUpdate = LazyRoutes.AdminDatabaseUpdate;
  return <DatabaseUpdate />;
}
