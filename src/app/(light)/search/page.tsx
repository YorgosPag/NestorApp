/**
 * `/search` — **μόνιμη ανακατεύθυνση στη ρίζα** (ADR-777 §8.13).
 *
 * Η οθόνη 1 μετακόμισε στο `/`. Αυτή η διαδρομή **δεν διαγράφηκε**, και οι δύο λόγοι
 * είναι μετρημένοι:
 *
 * 1. **Κανένας σύνδεσμος δεν σαπίζει.** Η διεύθυνση ζει από τις 10/08 και μπορεί να
 *    έχει μοιραστεί ή ευρετηριαστεί.
 * 2. **Καμία διπλή διεύθυνση για το ίδιο περιεχόμενο.** Δύο URL που δείχνουν την ίδια
 *    οθόνη είναι διπλότυπο — ο ίδιος λόγος για τον οποίο το `listing-routes.ts` δεν
 *    γράφει κενό `?`. Η ανακατεύθυνση δηλώνει **ποιο είναι το κανονικό**.
 *
 * ⚠️ **`redirect()` του διακομιστή, ΟΧΙ `router.replace` σε `useEffect`.** Ένα πελατικό
 * redirect θα ζωγράφιζε πρώτα κάτι και μετά θα έφευγε — και σε ανιχνευτή που δεν
 * εκτελεί JavaScript θα ήταν **σελίδα με περιεχόμενο**, δηλαδή ακριβώς το διπλότυπο
 * που η ανακατεύθυνση υπάρχει για να αποτρέψει.
 *
 * ⚠️ **`permanentRedirect` (308), όχι `redirect` (307):** το «προσωρινό» λέει στις
 * μηχανές *«μη μεταφέρεις τίποτα, ξαναρώτα»*. Η μετακόμιση **δεν** είναι προσωρινή.
 */

import { permanentRedirect } from 'next/navigation';
import { SEARCH_LANDING_ROUTE } from '@/lib/listings/listing-routes';

export default function SearchLandingRedirect(): never {
  permanentRedirect(SEARCH_LANDING_ROUTE);
}
