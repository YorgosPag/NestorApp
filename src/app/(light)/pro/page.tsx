/**
 * `/pro` — **ο δημόσιος κατάλογος επαγγελματιών** (ADR-827 §9.6 #1 · ADR-841 Φ6-Β).
 *
 * Ζει στο route group `(light)` μαζί με τις τρεις οθόνες ακινήτων: ο επισκέπτης είναι
 * **ανώνυμος** — χωρίς sidebar, χωρίς λογαριασμό. Το δημόσιο κέλυφος αποκτά **τέταρτο**
 * καταναλωτή αντί να γεννηθεί πέμπτο (N.0.2).
 *
 * ⚠️ **ΓΙΑΤΙ ΟΧΙ `/o/<ψευδώνυμο>`, ΠΟΥ ΕΓΡΑΦΕ ΤΟ §9.6**: το πρόθεμα `/o/` είναι ο χώρος
 * **μελών** — το `o/[workspace]/layout.tsx` κάνει `redirect(login)` σε ανώνυμο και
 * `notFound()` σε μη-μέλος, και το ADR-787 §5.3 γ γράφει αυτολεξεί ότι *«οι δημόσιες
 * οθόνες ζουν ΕΚΤΟΣ προθέματος»*. Πλήρες σκεπτικό **και** το αντι-παράδειγμα του Houzz:
 * `lib/workspace/workspace-scope.ts`, εγγραφή `pro`.
 *
 * ────────────────────────────────────────────────────────────────────────────
 * 🔴 ΤΟ ΣΧΟΛΙΟ ΠΟΥ ΗΤΑΝ ΕΔΩ ΔΙΟΡΘΩΘΗΚΕ — ΔΕΝ ΔΙΑΓΡΑΦΗΚΕ (ADR-841 Φ6-Β5)
 * ────────────────────────────────────────────────────────────────────────────
 *
 * Έγραφε: *«Καμία `useSearchParams` ⇒ καμία ανάγκη ορίου `<Suspense>`. Ο κατάλογος
 * **δεν έχει φίλτρα επίτηδες**: ένας **επιλογέας ταξινόμησης** είναι η υποδοχή που
 * περιμένει το εμπορικό κριτήριο (§9.9 α)»*.
 *
 * 🔑 **Η προκείμενη ήταν σωστή και ΠΑΡΑΜΕΝΕΙ σωστή** — ένας επιλογέας **ταξινόμησης**
 * είναι όντως υποδοχή για steering, και **δεν** μπαίνει. Το **συμπέρασμα** όμως αφορά
 * **φιλτράρισμα**, και δεν έπεται: το φίλτρο απαντά *«τι ψάχνω»* *(δηλώνει ο
 * **χρήστης**)*· η ταξινόμηση απαντά *«ποιος πρώτος»* *(δηλώνει η **πλατφόρμα**)*.
 *
 * 🔴 **Και η απουσία φίλτρου ΔΕΝ ήταν ουδέτερη.** Το `agency-directory-order.ts` έχει
 * **ήδη μετρήσει** ότι η αλφαβητική σειρά *«ευνοεί συστηματικά τα ονόματα σε Α/Β…
 * ουδέτερη ως προς εμάς αλλά **όχι ουδέτερη ως προς τα γραφεία**»*. Με **μεικτό**
 * πληθυσμό *(μεσίτες · μηχανικοί · υδραυλικοί · δικηγόροι)* ο κατάλογος **χωρίς**
 * φίλτρο κάνει αυτή τη μεροληψία τον **μόνο** μηχανισμό εύρεσης. Το φίλτρο είναι
 * **θεραπεία**.
 *
 * ⇒ Ο κανόνας που το κρατά τίμιο είναι ο **ΚΑΝΟΝΑΣ Φ**, γραμμένος και **εκτελεσμένος**:
 * `lib/agency/showcase-filter.ts` *(η απόφαση)* + `components/mandate/AgencyDirectoryFilters.tsx`
 * *(το χειριστήριο)*.
 *
 * ⚠️ **ΤΟ `<Suspense>` ΕΙΝΑΙ ΤΩΡΑ ΥΠΟΧΡΕΩΤΙΚΟ** (CHECK 3.55): το
 * `AgencyDirectoryContent` διαβάζει `useSearchParams`, και χωρίς όριο **ολόκληρη η
 * σελίδα** βγαίνει από την προαπόδοση — `next build` κόκκινο, όχι υποθετικά. Πρότυπο:
 * `app/(light)/search/results/page.tsx`.
 *
 * @module app/(light)/pro/page
 */

import { Suspense } from 'react';

import { AgencyDirectoryContent } from '@/components/mandate/AgencyDirectoryContent';
import { StaticPageLoading } from '@/core/states';

export default function AgencyDirectoryPage() {
  return (
    <Suspense fallback={<StaticPageLoading />}>
      <AgencyDirectoryContent />
    </Suspense>
  );
}
