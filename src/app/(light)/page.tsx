'use client';

/**
 * `/` — **η δημόσια ρίζα** (ADR-777 Α3 οθόνη 1 · §8.13).
 *
 * ────────────────────────────────────────────────────────────────────────────
 * 🔴 ΤΙ ΗΤΑΝ ΕΔΩ ΠΡΙΝ, ΚΑΙ ΓΙΑΤΙ ΗΤΑΝ ΕΛΑΤΤΩΜΑ
 * ────────────────────────────────────────────────────────────────────────────
 *
 * Το `/` απέδιδε το ταμπλό του χώρου εργασίας και έκανε `router.replace('/login')`
 * για κάθε μη-συνδεδεμένο. Δηλαδή ο επισκέπτης που πληκτρολογούσε
 * **nestorconstruct.gr** προσγειωνόταν σε **φόρμα σύνδεσης**. Δεν ήταν «λείπει
 * αρχική σελίδα» — ήταν **κλειδαριά**: ο ιδιώτης δεν είχε **καμία** πόρτα.
 *
 * 🔑 **ΔΕΝ ΓΕΝΝΗΘΗΚΕ ΝΕΑ ΟΘΟΝΗ — ΜΕΤΑΚΟΜΙΣΕ Η ΔΙΕΥΘΥΝΣΗ.** Η οθόνη 1 υπήρχε ήδη,
 * χτισμένη και μετρημένη ({@link SearchLandingContent}). Μια δεύτερη «αρχική» δίπλα
 * της θα ήταν **δύο οθόνες που απαντούν το ίδιο ερώτημα** — ακριβώς το σχήμα του
 * ADR-749. *Μετακινούμε καταναλωτές, όχι αρχεία* (κανόνας 19).
 *
 * ⛔ **ΚΑΙ ΔΕΝ ΑΝΑΣΤΗΘΗΚΕ Η `components/landing/LandingPage.tsx`** — 23,5 KB με
 * **μηδέν** καταναλωτές, γραμμένη πριν το ADR-777. Είναι το **αντίθετο** της
 * δοκτρίνης του: υπόσχεται *«Διαθέσιμα Ακίνητα · Premium Τοποθεσίες · Εγγύηση
 * Ποιότητας · 24/7 Υποστήριξη»* πάνω σε **6 δοκιμαστικά ακίνητα με 0 στον χάρτη**,
 * και το κουμπί αναζήτησής της δείχνει στο **εσωτερικό** `/properties` (route group
 * `(app)`) — δηλαδή θα έστελνε ανώνυμο επισκέπτη πίσω από τη σύνδεση. Το §8.10 το
 * έγραψε ήδη: **η οθόνη 1 αρνείται να υποσχεθεί**.
 *
 * 🏆 **Η ρίζα ΕΙΝΑΙ η αναζήτηση, και είναι η μετρημένη πρακτική της αγοράς.** Το
 * Zillow δεν βάζει hero video ούτε φωτογραφίες τρόπου ζωής: το πεδίο αναζήτησης
 * **είναι** το προϊόν, απόφαση από A/B σε δεκάδες εκατομμύρια συνεδρίες. Το
 * Rightmove κρατά τη ρίζα του σχεδόν αμετάβλητη επί χρόνια, με **μόνη** αλλαγή το
 * σύνθημα. **Πού τους ξεπερνάμε:** εκείνοι δείχνουν το κουτί **πάντα**· εδώ
 * εμφανίζεται **μόνο όταν τα δεδομένα μπορούν να τηρήσουν την υπόσχεση**, και όταν
 * δεν μπορούν, η οθόνη το λέει **με αριθμούς** αντί να επιστρέψει σιωπηλά μηδέν.
 */

import React from 'react';
import { SearchLandingContent } from '@/components/search/SearchLandingContent';

export default function PublicHomePage() {
  return <SearchLandingContent />;
}
