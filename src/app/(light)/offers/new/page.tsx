/**
 * **`/offers/new` — καταχώρηση ακινήτου από ιδιώτη** (ADR-777 Α14 · **Α8** · ADR-660 §5.10).
 *
 * ────────────────────────────────────────────────────────────────────────────
 * 🔴 ΓΙΑΤΙ ΕΦΥΓΕ ΑΠΟ ΤΟ `(me)` — Η ΚΑΤΑΧΩΡΙΣΗ ΞΕΚΙΝΑ ΠΡΙΝ ΤΟΝ ΛΟΓΑΡΙΑΣΜΟ
 * ────────────────────────────────────────────────────────────────────────────
 *
 * Το `(me)` είναι *«ο ιδιωτικός χώρος **του** ιδιώτη»* — ο χώρος όπου ζουν **τα δικά
 * του** πράγματα. Μια αγγελία που **δεν υπάρχει ακόμη** δεν είναι δικό του πράγμα:
 * δεν έχει `authorUserId`, δεν έχει έγγραφο, δεν έχει κάτοχο. Ήταν **κατηγοριακό
 * λάθος** — το ίδιο σχήμα που διόρθωσε το §8.12 όταν έβγαλε τη δημόσια σελίδα από το
 * κέλυφος της εφαρμογής.
 *
 * 🔑 **Η ΔΙΕΥΘΥΝΣΗ ΔΕΝ ΑΛΛΑΞΕ.** Το route group είναι **φάκελος** και δεν εμφανίζεται
 * ποτέ στο URL: η σελίδα εξακολουθεί να είναι `/offers/new`. Κανένας σύνδεσμος,
 * κανένας σελιδοδείκτης, κανένα route slice (`offers__new.el.json`) δεν αγγίχτηκε.
 *
 * 🔑 **ΚΑΙ ΔΕΝ ΑΛΛΑΞΕ ΤΙΠΟΤΑ ΟΠΤΙΚΑ — ΜΕΤΡΗΜΕΝΟ**: ο `PrivateSpaceShell` ζωγραφίζει
 * τον **ίδιο** `PublicSiteHeader` με το `(light)/layout.tsx`. Οι **μόνες** διαφορές
 * των δύο κελυφών είναι ο `ProtectedRoute` και το `robots: noindex` — δηλαδή ακριβώς
 * τα δύο πράγματα που αυτή η μετακίνηση αφορά. Ο συνδεδεμένος ιδιοκτήτης βλέπει την
 * ίδια οθόνη που έβλεπε χθες.
 *
 * ⚠️ **ΤΟ `/demands/new` ΜΕΝΕΙ ΣΤΟ `(me)`, ΚΑΙ ΕΙΝΑΙ ΔΗΛΩΜΕΝΗ ΑΣΥΜΜΕΤΡΙΑ.** Το κενό
 * που μέτρησε η idealista (ADR-660 §5.8) είναι η **προσφορά**, όχι η ζήτηση: «θέλω να
 * πουλήσω» είναι η είσοδος του χωνιού, ενώ «ψάχνω να αγοράσω» προϋποθέτει ήδη
 * πρόθεση επιστροφής. Η συμμετρία θα ήταν **αντιγραφή χωρίς μέτρηση** — αν
 * χρειαστεί, γίνεται με τον **ίδιο** μηχανισμό, που πλέον υπάρχει.
 *
 * ────────────────────────────────────────────────────────────────────────────
 * 🔴 Η ΜΟΝΗ διαδρομή του χαρακτηριστικού που είναι αποκλειστικά desktop
 * ────────────────────────────────────────────────────────────────────────────
 *
 * Και το «αποκλειστικά» είναι γεγονός **bytes**: ξεχωριστή διαδρομή ⇒ αυτόματο
 * route-level code splitting· δυναμική εισαγωγή μέσα στην πύλη ⇒ η φόρμα δεν
 * κατεβαίνει ούτε κι όταν στενή οθόνη **ανοίξει** τη διεύθυνση.
 * Δες {@link OwnerPropertyCreationGate}.
 *
 * @module app/(light)/offers/new/page
 */

import type { Metadata } from 'next';

import { OwnerPropertyCreationGate } from '@/components/owner-property/OwnerPropertyCreationGate';

/**
 * 🔴 **`noindex` ΑΝΑ ΣΕΛΙΔΑ, ΚΑΙ Ο ΛΟΓΟΣ ΕΙΝΑΙ ΑΛΛΟΣ ΑΠΟ ΤΟΥ `(me)`.**
 *
 * Το group `(me)` δηλώνει `noindex` για λόγο **ιδιωτικότητας**: *«θα ευρετηριάσει τις
 * **διευθύνσεις**, και τότε το `/demands/dmnd_…` γίνεται δημόσια ορατή ταυτότητα»*.
 * Εδώ **δεν υπάρχει ταυτότητα στη διεύθυνση** — άρα εκείνος ο λόγος **δεν ισχύει**,
 * και θα ήταν λάθος να αντιγραφεί σαν να ισχύει.
 *
 * Ο λόγος εδώ είναι **απόδοσης**: η φόρμα φορτώνεται με `ssr: false` πίσω από πύλη
 * πλάτους (Α8) ⇒ ό,τι βλέπει ένας ανιχνευτής είναι **κενό**. Ευρετηρίαση σελίδας
 * χωρίς περιεχόμενο είναι *thin content* — κόστος χωρίς όφελος.
 *
 * ⚠️ **Δηλώνεται ΡΗΤΑ αντί να κληρονομηθεί σιωπηλά**: βγαίνοντας από το `(me)` η
 * σελίδα θα γινόταν ευρετηριάσιμη ως **παρενέργεια** μιας μετακίνησης που έγινε για
 * εντελώς άλλο λόγο. Μια αλλαγή ορατότητας στις μηχανές αναζήτησης δεν επιτρέπεται να
 * είναι παρενέργεια.
 *
 * 🔶 **Η δημόσια, ευρετηριάσιμη πόρτα είναι άλλη σελίδα** — το CTA του Σταδίου Β, που
 * οφείλει να δουλεύει και σε **στενή** οθόνη (Α8) και άρα δεν μπορεί να είναι αυτή.
 */
export const metadata: Metadata = {
  robots: { index: false, follow: false },
};

export default function NewOfferPage() {
  return <OwnerPropertyCreationGate />;
}
