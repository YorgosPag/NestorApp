/**
 * **`/profile` — Η ΚΑΤΟΙΚΙΑ ΤΟΥ ΙΔΙΩΤΗ** (ADR-827 §9.22 ζ · ADR-777 Α9/Α14).
 *
 * ────────────────────────────────────────────────────────────────────────────
 * 🔴 ΤΟ ΑΔΙΕΞΟΔΟ ΠΟΥ ΚΛΕΙΝΕΙ — ΤΡΕΙΣ ΜΕΤΡΗΣΕΙΣ, ΚΑΜΙΑ ΘΕΩΡΙΑ
 * ────────────────────────────────────────────────────────────────────────────
 *
 * 1. Η οθόνη προφίλ ζούσε **αποκλειστικά** πίσω από το πρόθεμα χώρου
 *    (`app/(app)/o/[workspace]/account/profile`).
 * 2. Ο ιδιώτης **δεν έχει χώρο**: `personal-scope-middleware.ts` → `actorWorkspace()`
 *    επιστρέφει **`null`** όταν το scope δεν είναι `organization`.
 * 3. ⇒ Δεν μπορούσε να δει ούτε να διορθώσει **όνομα, επάγγελμα, φωτογραφία, ΑΦΜ**.
 *
 * Το §9.22 έλυσε τη **ροή** *(το ΑΦΜ ζητιέται μέσα στη φόρμα ανάθεσης)*, **όχι την
 * κατοικία**. Αυτή είναι η κατοικία.
 *
 * ────────────────────────────────────────────────────────────────────────────
 * 🏆 ΕΠΑΝΑΧΡΗΣΗ, ΟΧΙ ΑΝΤΙΓΡΑΦΟ — ΚΑΙ ΤΟ ΜΕΤΡΗΣΑΜΕ ΠΡΙΝ ΤΟ ΓΡΑΨΟΥΜΕ
 * ────────────────────────────────────────────────────────────────────────────
 *
 * Το ίδιο `ProfilePageContent` που σερβίρει και ο κόσμος του γραφείου — **μηδέν** νέο
 * πεδίο, **μηδέν** δίδυμο. Η ερώτηση που το επέτρεψε ήταν *«ποιους από τους 10 βαρείς
 * providers του `(app)` απαιτεί πραγματικά;»*, και απαντήθηκε **με μέτρηση** στην
 * πλήρη κλειστότητα εισαγωγών του (2026-08-30):
 *
 * | Ερώτηση | Απάντηση |
 * |---|---|
 * | `useWorkspace` · `useFloorplan` · `useNotifications` · `useCache` · `useWebSocket` · `useSharedProperties` · `useGlobalPhotoPreview` · `useBuildingsNoUnits` · `useActiveJob` · `useSidebar` · `useNavigation` | **0 από 11** |
 * | Όλες οι `useContext(...)` της κλειστότητας | `AuthContext` · `UserTypeContext` · `UserRoleContext` — **και μόνο** |
 * | Πού ζουν οι providers τους | `AuthProvider` + `UserRoleProvider` στο **ΡΙΖΙΚΟ** `app/layout.tsx`· ο `UserTypeProvider` **πουθενά** στο `(app)` ⇒ **ακριβής ισοτιμία** |
 * | `<Tooltip>` *(μέσω `DeclaredOccupationBadge`)* | `TooltipProvider` — το `(me)/layout.tsx` τον **έχει ήδη** |
 *
 * ⇒ **Καμία εξαγωγή δεν χρειάστηκε.** Αν η μέτρηση είχε βρει provider που λείπει, η
 * απάντηση **δεν** θα ήταν «βάλ' τον κι εδώ» — θα ήταν να **εξαχθεί** το κομμάτι.
 *
 * ────────────────────────────────────────────────────────────────────────────
 * ⚠️ ΤΙ ΔΕΝ ΓΡΑΦΕΤΑΙ ΕΔΩ
 * ────────────────────────────────────────────────────────────────────────────
 *
 * - ⛔ **Κανένας φρουρός ταυτότητας.** Τον δίνει το `(me)/layout.tsx` μέσα από τον
 *   {@link PrivateSpaceShell} (`ProtectedRoute`), μαζί με το `noindex`. Φρουρός
 *   **μέσα στη σελίδα** θα ήταν ακριβώς ο `ConditionalAppShell` που διέγραψε το
 *   **ADR-777 §8.12**: σωστός σήμερα, ξεχασμένος στην επόμενη σελίδα, και **αόρατος**
 *   σε κάθε πύλη (CHECK 3.52).
 * - ⛔ **Καμία γεωμετρία.** Ο διάδρομος και το μέτρο (`measure="wide"`) παραδίδονται
 *   από το `ShellSurface` του `PrivateSpaceShell` — χειρόγραφο `mx-auto max-w-*` εδώ
 *   θα ήταν **δεύτερη αυθεντία** και το CHECK 3.63 το μπλοκάρει.
 * - ⛔ **Κανένα `LazyRoutes`.** Το `(app)` το χρησιμοποιεί επειδή εκεί η σελίδα είναι
 *   **μία ανάμεσα σε δεκάδες** πίσω από το ίδιο κέλυφος (ADR-294 Batch 4). Εδώ το
 *   περιεχόμενο **ΕΙΝΑΙ** η διαδρομή: μια τεμπέλικη φόρτωση θα πρόσθετε ροδέλα χωρίς
 *   να γλιτώσει τίποτα.
 *
 * 🔑 **Λεπτή σελίδα**, όπως τα `(me)/offers` και `(me)/demands`: η ουσία ζει σε
 * component που **δοκιμάζεται** — ένα `page.tsx` του App Router όχι.
 *
 * ⚠️ **Μεσολαβεί το {@link PrivateProfileContent}, και ΔΕΝ είναι περιτύλιγμα από συνήθεια**:
 * κρατά **μόνο** την εγγραφή του route slice, που είναι ερώτημα **ανά ΔΙΑΔΡΟΜΗ** — ενώ
 * το `ProfilePageContent` το σερβίρουν **ΔΥΟ** διευθύνσεις. Μηδέν αντιγραμμένη λογική·
 * ο λόγος γράφεται ολόκληρος εκεί.
 *
 * @module app/(me)/profile/page
 */

import { PrivateProfileContent } from '@/components/account/pages/PrivateProfileContent';

export default function PrivateProfilePage() {
  return <PrivateProfileContent />;
}
