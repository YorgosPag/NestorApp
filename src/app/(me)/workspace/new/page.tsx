/**
 * **`/workspace/new` — «δημιουργώ τον χώρο μου»** (ADR-787 Κ-1).
 *
 * Λεπτή σελίδα: όλη η ουσία ζει στο {@link CreateWorkspaceForm}, όπως το
 * `(me)/offers/page.tsx` αναθέτει στο `MyOwnerPropertiesContent`. Ο λόγος είναι ο
 * **έλεγχος**: ένα component δοκιμάζεται· ένα `page.tsx` του App Router όχι.
 *
 * ─────────────────────────────────────────────────────────────────────────────
 * 🔴 ΓΙΑΤΙ ΣΤΟ `(me)` — ΤΡΕΙΣ ΛΟΓΟΙ, ΚΑΙ ΟΙ ΤΡΕΙΣ ΑΠΟΚΛΕΙΟΥΝ ΤΑ ΑΛΛΑ GROUPS
 * ─────────────────────────────────────────────────────────────────────────────
 * 1. **Δεν είναι `(app)`.** Εκείνο φοράει το κέλυφος του γραφείου — sidebar με
 *    ~60 στοιχεία που **όλα** προϋποθέτουν `companyId`. Ο άνθρωπος που φτάνει
 *    εδώ **δεν έχει** `companyId`· θα έβλεπε μενού προς χώρο που δεν υπάρχει.
 * 2. **Δεν είναι `(light)`.** Εκείνο δηλώνει *«οι **δημόσιες** οθόνες»*, και αυτή
 *    η σελίδα **απαιτεί ταυτότητα**: χωρίς `uid` δεν υπάρχει ποιον να κάνεις
 *    διαχειριστή. Σελίδα με φρουρό κάτω από δήλωση που λέει «δημόσια» κάνει τη
 *    **δήλωση ψευδή** — και η δήλωση **είναι** το συμβόλαιο της CHECK 3.52.
 * 3. **Είναι `(me)`.** Το layout του δίνει **ακριβώς** τα δύο που χρειάζεται:
 *    `ProtectedRoute` (ταυτότητα) και `noindex`. Και είναι ο χώρος **όπου ήδη
 *    προσγειώνεται** όποιος δεν έχει γραφείο (`PRIVATE_SPACE_HOME` → `/offers`).
 *
 * 🔑 Το μοντέλο είναι του **Figma**: *«Create a team»* **μέσα από** τον προσωπικό
 * σου χώρο. Ο άνθρωπος δεν πάει κάπου αλλού για να μεγαλώσει — μεγαλώνει από
 * εκεί που ήδη στέκεται.
 *
 * ⚠️ **Καμία νέα δήλωση πύλης δεν χρειάστηκε, και είναι μετρημένο**: η CHECK 3.60
 * κρίνει **μόνο** το `(app)` (`scripts/lib/workspace-scope/scope.js`), και το
 * group `(me)` είναι **ήδη** δηλωμένο στο `.shell-boundary.json`. Μια σελίδα
 * δημιουργίας χώρου **δεν μπορεί** να ζει πίσω από πρόθεμα χώρου: δεν υπάρχει
 * ακόμη ψευδώνυμο να μπει εκεί.
 *
 * @module app/(me)/workspace/new/page
 */

import { CreateWorkspaceForm } from '@/components/workspace/CreateWorkspaceForm';

export default function CreateWorkspacePage() {
  // ⚠️ ΚΑΝΕΝΑ `px-*` / `py-*` / `max-w-*` εδώ (ADR-797 ΦΑΣΗ Β · CHECK 3.63): και τα
  //    τρία τα κατέχει ο ΕΝΑΣ ιδιοκτήτης, το `ShellSurface measure="wide"` του
  //    `PrivateSpaceShell`. Η πρώτη γραφή αυτού του αρχείου έγραφε `px-4 py-10` και
  //    η πύλη το έπιασε — σωστά: ένα δεύτερο κενό δίπλα στο πρώτο νικά κατά σειρά
  //    πηγής και γίνεται διπλός διάδρομος που φαίνεται μόνο στην οθόνη.
  //
  // ⚠️ Το `<main>` το δίνει η ΣΕΛΙΔΑ και όχι το κέλυφος: το `ShellSurface` αποδίδει
  //    `div` επίτηδες (WCAG — ΕΝΑ landmark `main` ανά σελίδα). Ίδια σύμβαση με το
  //    `MyOwnerPropertiesContent` του ίδιου group.
  return (
    <main className="flex w-full flex-col">
      <CreateWorkspaceForm />
    </main>
  );
}
