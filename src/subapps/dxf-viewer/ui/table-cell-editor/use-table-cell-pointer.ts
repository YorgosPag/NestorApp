'use client';

/**
 * ADR-739 Φ.Δ βήμα 8 — **το κλικ μέσα σε πίνακα που ήδη επεξεργάζεσαι**: απλό κλικ
 * μετακινεί το ενεργό κελί, `Shift+κλικ` απλώνει την περιοχή ως εκεί.
 *
 * ## 🔴 ΠΑΘΗΤΙΚΟΣ ΑΚΡΟΑΤΗΣ — καμία `stopPropagation`, και `preventDefault` **μόνο** στην
 * υπόδειξη τύπου (ADR-754 §3· δες την εξαίρεση παρακάτω)
 * Η μεγάλη απόφαση αυτού του αρχείου είναι τι **δεν** κάνει. Ο πειρασμός είναι να
 * καταναλώσει το συμβάν ώστε «να μην το πειράξει ο καμβάς». Αυτό θα έσπαγε πράγματα που
 * δουλεύουν: το σύρσιμο λαβών του πίνακα, τη μετακίνηση της οντότητας, το πλαίσιο επιλογής.
 * Ο καμβάς είναι ο ιδιοκτήτης του ποντικιού και **παραμένει**· εδώ γίνεται μόνο μια
 * **επιπλέον** ανάγνωση: «σε ποιο κελί έπεσε αυτό;».
 *
 * Έτσι το αρχείο μένει εντελώς **έξω** από τον `CanvasSection` — τον orchestrator που ο
 * ADR-040 απαγορεύει να αποκτήσει συνδρομές — και δεν αλλάζει καμία υπάρχουσα διαδρομή
 * χειρισμού ποντικιού. Δεν είναι συμβιβασμός· είναι ο λόγος που η αλλαγή είναι ασφαλής.
 *
 * 🔴 **ADR-754 §3 — Η ΠΑΘΗΤΙΚΟΤΗΤΑ ΕΧΕΙ ΠΛΕΟΝ ΜΙΑ ΕΞΑΙΡΕΣΗ, ΟΝΟΜΑΣΜΕΝΗ ΚΑΙ ΜΙΑ.**
 * Όταν το πάτημα είναι **υπόδειξη κελιού μέσα σε τύπο** (`=` + κλικ), και **μόνο τότε**, το
 * συμβάν καταναλώνεται με `preventDefault()`. Δεν χαλαρώνει ο κανόνας από κάτω — αλλάζει η
 * **ερώτηση**: εκεί ο καμβάς έχει ακόμα δουλειά στο ίδιο πάτημα (λαβές, μετακίνηση), εδώ το
 * πάτημα **δεν είναι χειρονομία καμβά καθόλου** — είναι πληκτρολόγηση με το ποντίκι. Η
 * αναίρεση αφορά τη **μεταφορά εστίασης**, που θα σκότωνε τη γραφή· δες
 * `table-point-mode-pointer.ts`. Καμία `stopPropagation`, ποτέ, σε καμία διαδρομή.
 *
 * 🔴 ADR-739 §29 — **Η ΠΑΘΗΤΙΚΟΤΗΤΑ ΕΔΩ ΙΣΧΥΕΙ ΑΚΕΡΑΙΗ, ΚΑΙ ΤΩΡΑ ΕΙΝΑΙ ΑΠΟΦΑΣΗ.**
 * Ο ιδιοκτήτης ζήτησε ο καμβάς να **μη** δέχεται επιλογή όσο ο πίνακας είναι ανοιχτός. Ο
 * πειρασμός ήταν να καταναλωθεί το συμβάν **εδώ**, στο `if (δεν βρήκα κελί)` — μια γραμμή,
 * φαινομενικά το ίδιο πράγμα. Θα ήταν λάθος σε δύο επίπεδα: (α) η επιλογή οντότητας
 * γεννιέται στο **`mouseup`**, συμβάν που αυτό το αρχείο δεν ακούει καν· (β) αυτός ο
 * ακροατής ζει στο **δοχείο**, ενώ ο ανταγωνιστής του (`useCanvasContextMenu`) εγγράφεται
 * στο ίδιο δοχείο **νωρίτερα** — δηλαδή θα έχανε πάντα τη σειρά. Το κλείδωμα ζει σε δικό
 * του module, σε σύλληψη στο `document`. Δες `use-table-canvas-lockdown`.
 *
 * ## 🔴 ADR-739 §26.15 — ΓΙΑΤΙ Ο ΔΡΟΜΕΑΣ ΔΕΝ ΚΛΕΙΝΕΙ ΑΠΟ ΤΟ BLUR ΠΟΥ ΑΚΟΛΟΥΘΕΙ
 *
 * ⚠️ Εδώ έγραφε ότι «ο φύλακας αναβάλλει την απόφαση κατά ένα καρέ, και μέχρι τότε το React
 * θα έχει στήσει νέο `<textarea autoFocus>` ⇒ δεν κλείνει· χτίζουμε πάνω σε **δηλωμένη
 * εγγύηση**». Η εγγύηση **δεν υπήρχε**: ζωντανά, **11/11** κλικ μέσα στον πίνακα σκότωναν τη
 * λειτουργία — και το κλικ στο **ίδιο** κελί επίσης, όπου δεν ξαναστήνεται τίποτα επειδή δεν
 * αλλάζει κανένα κομμάτι του `key`. Το πλήρες σκεπτικό ζει στην κεφαλίδα του φύλακα· εδώ
 * αρκεί το συμπέρασμα:
 *
 * **Ο καμβάς δεν μπορεί να φέρει το σημάδι συνεδρίας** (θα κρατούσε τη συνεδρία ζωντανή σε
 * κάθε κλικ οπουδήποτε στο σχέδιο), άρα το κλικ πρέπει να **δηλωθεί**: όποτε αυτός ο
 * ακροατής αναγνωρίζει ότι το πάτημα έπεσε μέσα στον δικό του πίνακα, το λέει στον φύλακα με
 * το `claimTableCellSessionPointerDown()`. Ο φύλακας, ένα καρέ μετά, **ανακτά** το
 * πληκτρολόγιο αντί να κλείσει τη συνεδρία. Η δήλωση είναι μία γραμμή και **δεν** αλλάζει
 * τίποτα άλλο εδώ: ο ακροατής παραμένει απολύτως παθητικός.
 *
 * ## Γιατί `mousedown` και όχι `click`
 * Η επιλογή πρέπει να ακολουθεί το χέρι **αμέσως**, πριν ξεκινήσει οποιοδήποτε σύρσιμο —
 * όπως σε κάθε φύλλο υπολογισμού και σε κάθε CAD. Ένα `click` θα ενεργούσε μετά το
 * `mouseup`, δηλαδή αφού ο καμβάς έχει ήδη ερμηνεύσει τη χειρονομία.
 *
 * @module subapps/dxf-viewer/ui/table-cell-editor/use-table-cell-pointer
 * @see ui/table-cell-editor/table-cell-session-focus.ts — η εγγύηση του ενός καρέ
 * @see ui/table-cell-editor/table-pointer-axis-selection.ts — ΠΟΙΟ κελί/άξονας χτυπήθηκε
 */

import { useEffect, type RefObject } from 'react';
// 🔴 ADR-739 §27.16 Ε6 — ο ΕΝΑΣ σταθεροποιητής χειριστών του έργου (ADR-532 Στάδιο 4a.1),
// ήδη σε χρήση από τρία σημεία του subapp. Δες παρακάτω γιατί είναι δομικά απαραίτητος εδώ.
import { useEventCallback } from '@/hooks/useEventCallback';
// ADR-739 §31.10 — η **γεωμετρία της σύρσης** (κινούμενο άκρο, επιλογή ολόκληρου άξονα)
// μετακόμισε σε δικό της module όταν αυτό εδώ χτύπησε τις 500 γραμμές (N.7.1). Εξαγωγή, όχι
// κόψιμο: καμία από τις συναρτήσεις δεν χρειάζεται τίποτα από το hook.
import {
  axisEndAt,
  cellEndAt,
  extendWholeAxis,
  selectWholeAxis,
} from './table-pointer-axis-selection';
// 🔴 ADR-739 §29 — η ΜΙΑ ερώτηση «πού έπεσε αυτό το συμβάν;», μοιρασμένη με τον φύλακα του
// κλειδώματος. Δες την κεφαλίδα εκείνου του module: δύο αντίγραφα θα άφηναν νεκρή λωρίδα
// στην άκρη του πίνακα, όπου ο φύλακας μπλοκάρει και ο pointer δεν δρα.
import {
  tableEventWorldPoint,
  tablePointerHitAtWorld,
  tableRangeGrabAtWorld,
} from './table-cell-pointer-hit';
// 🔴 ADR-739 §36 ΦΑΣΗ 3 — η **τρίτη** χειρονομία του πίνακα: μεταφορά περιοχής από το
// περίγραμμά της. Ίδιο σχήμα με τις δύο πρώτες, δικό της module (η γεωμετρία μένει εδώ).
import {
  beginTableRangeTransfer,
  endTableRangeTransferDrag,
} from './table-range-transfer-drag';
// ADR-739 §27.15 — ο κύκλος ζωής της σύρσης ζει σε δικό του module· εδώ μένει η **γεωμετρία**.
import { endTableCellDrag, startTableCellDrag } from './table-cell-drag-session';
// 🔴 ADR-754 §3 — η **τέταρτη** χειρονομία του πίνακα: υπόδειξη κελιού μέσα σε τύπο. Ίδιο
// σχήμα με τις τρεις πρώτες — όλη η λογική σε δικό της module, εδώ μόνο ο φρουρός.
import { tryTablePointModeMouseDown } from './table-point-mode-pointer';
// ADR-739 §31.9 — η **δεύτερη** χειρονομία του πίνακα: σύρσιμο διαχωριστικού στηλών.
import {
  beginTableAxisResize,
  endTableAxisResizeDrag,
  type TableResizeAxis,
} from './table-axis-resize-drag';
import { tableCursorAt } from '../../bim/table/table-cell-navigation';
// ADR-739 §26.15 — ο ΕΝΑΣ ορισμός του «ανήκω στη συνεδρία», και στις δύο μορφές του:
// για **στοιχεία** (`isTableCellSessionElement`) και για **χειρονομίες** (`claim…`).
import {
  claimTableCellPointerGesture,
  claimTableCellSessionPointerDown,
  isTableCellSessionElement,
} from './table-cell-session-focus';
import {
  setTableCellCursor,
  type TableCellCursorState,
} from '../../state/table-cell-cursor-store';
import type { TableCellRef } from '../../bim/table/table-cell-range';
import type { TableEntity } from '../../types/table-entity';
import type { ViewTransform } from '../../rendering/types/Types';

export interface UseTableCellPointerParams {
  readonly cursor: TableCellCursorState | null;
  /** Η **ζωντανή** οντότητα του δρομέα· `null` όταν ο πίνακας χάθηκε από κάτω του. */
  readonly entity: TableEntity | null;
  /**
   * 🔴 ADR-739 §36 ΦΑΣΗ 4 — ο **αναγνώστης** της ίδιας οντότητας, για όποιον τη χρειάζεται
   * **αργότερα** από τώρα. Το {@link entity} είναι η τιμή αυτού του render· η μεταφορά περιοχής
   * μπορεί να ρωτήσει τον χρήστη πριν γράψει, και ανάμεσα στην ερώτηση και στην απάντηση ο
   * κόσμος αλλάζει (`Ctrl+Z`, διαγραφή πίνακα, αλλαγή ορόφου). Δες `table-range-transfer-drop`.
   */
  readonly liveTable: () => TableEntity | null;
  readonly containerRef: RefObject<HTMLDivElement | null>;
  readonly transformRef: RefObject<ViewTransform>;
  /** `Shift+κλικ` — δεύτερη γωνία περιοχής, χωρίς να κουνηθεί το ενεργό κελί. */
  readonly onSelectTo: (cell: TableCellRef) => void;
  /**
   * 🔴 ADR-739 §26.15 — **δέσμευσε ό,τι γράφεται τώρα, πριν κουνηθεί ο δρομέας**. No-op σε
   * πλοήγηση (δεν γράφεται τίποτα) και ιδεμποτής (μια δεύτερη κλήση με ίδιο κείμενο δεν
   * παράγει εντολή — `buildTableCellEditCommand` επιστρέφει `null`).
   *
   * ## Γιατί δεν αρκεί το commit του `blur`
   * Η σειρά του browser είναι αμείλικτη: ο ακροατής **σύλληψης** τρέχει **πριν** από τη
   * μεταφορά εστίασης, άρα πριν από κάθε `blur`. Αν εδώ μετακινηθεί πρώτα ο δρομέας, το
   * `setTableCellCursor` έχει ήδη **σβήσει το πρόχειρο** — και ο επεξεργαστής έχει
   * ξαναστηθεί σε `nav`, όπου το commit είναι εξ ορισμού σιωπηλό (και σωστά: ένα «γράψε το
   * άδειο πρόχειρο» θα **έσβηνε** το κελί). Αποτέλεσμα: η πληκτρολόγηση χάνεται χωρίς
   * μήνυμα.
   *
   * Δεν είναι νέος κανόνας — είναι ο **ίδιος** που τηρεί ήδη το πληκτρολόγιο: δες
   * `use-table-cell-session-keys`, `case 'move'`, «η σειρά είναι το συμβόλαιο: πρώτα
   * δεσμεύεται το πρόχειρο, μετά μετακινείται ο δρομέας». Το ποντίκι απλώς του έλειπε.
   */
  readonly onCommitPending: () => void;
  /**
   * 🔴 ADR-739 §31.9 — **ζωντανή** προεπισκόπηση πλάτους στήλης, σε κάθε καρέ της σύρσης.
   * Γράφει τη σκηνή **χωρίς** να καταγράψει αναίρεση — δες την κεφαλίδα του
   * `table-axis-resize-drag` για το γιατί ο διαχωρισμός ζει εδώ και όχι εκεί.
   */
  readonly onPreviewModel: (entity: TableEntity, model: TableEntity['model']) => void;
  /** §31.9 — το τελικό πλάτος ως **μία** εντολή αναίρεσης, στο `mouseup`. */
  readonly onCommitModel: (entity: TableEntity, model: TableEntity['model']) => void;
}

export function useTableCellPointer(params: UseTableCellPointerParams): void {
  const {
    cursor, entity, liveTable, containerRef, transformRef, onSelectTo, onCommitPending,
    onPreviewModel, onCommitModel,
  } = params;

  /**
   * 🔴 ADR-739 §31.9 — ξεκινά τη σύρση μεγέθους από το διαχωριστικό, σε **όποιον** άξονα.
   *
   * Ζει σε δικό του σταθεροποιημένο χειριστή για τον **ίδιο** λόγο με το `handleMouseDown`
   * (§27.16 Ε6): διαβάζει τη ζωντανή οντότητα τη στιγμή του συμβάντος, όχι στιγμιότυπο —
   * και η ίδια η σύρση γράφει το μοντέλο, δηλαδή παράγει νέα οντότητα σε κάθε καρέ.
   *
   * ⚠️ **Ένας** χειριστής για τους δύο άξονες, όχι δύο σχεδόν-ίδιοι (Giorgio 2026-08-04): το
   * μόνο που αλλάζει είναι η παράμετρος `axis`, και δύο `useEventCallback` με ταυτόσημο σώμα
   * θα ήταν sibling clone — μαζί με το ρίσκο να ξεχαστεί το `onCommitPending()` στον έναν.
   */
  const onResizeAxisEdge = useEventCallback((
    axis: TableResizeAxis,
    edgeIndex: number,
    container: HTMLElement,
  ): void => {
    if (!entity) return;
    // Ό,τι γράφεται στο κελί δεσμεύεται πρώτα: η σύρση αλλάζει τη διάταξη κάτω από τον δρομέα.
    onCommitPending();
    beginTableAxisResize(axis, {
      entity,
      edgeIndex,
      container,
      transformRef,
      preview: onPreviewModel,
      commit: onCommitModel,
    });
  });

  /**
   * 🔴 ADR-739 §27.16 Ε6 — **Ο ΧΕΙΡΙΣΤΗΣ ΕΧΕΙ ΣΤΑΘΕΡΗ ΤΑΥΤΟΤΗΤΑ, ΚΑΙ ΕΙΝΑΙ ΔΟΜΙΚΟ.**
   *
   * Εδώ ο χειριστής οριζόταν **μέσα** στο effect, άρα το effect εξαρτιόταν από τον **δρομέα**
   * — και το cleanup του καλεί `endTableCellDrag()`. Κάθε γραφή που κάνει **η ίδια η σύρση**
   * (`setTableCellSelection` σε κάθε αλλαγή κελιού· και στο πρώτο πάτημα το
   * `setTableCellCursor`) παράγει **νέο** αντικείμενο δρομέα ⇒ το effect ξανατρέχει ⇒ το
   * cleanup **σκότωνε τη σύρση που μόλις είχε ξεκινήσει**. Η νέα εκτέλεση ξαναγράφει μόνο
   * τον `mousedown`· τη σύρση δεν την ξαναστήνει κανείς.
   *
   * Μετρημένο ζωντανά (02/08, raw-CDP): σύρση `A2 → C4` ⇒ **καμία** επιλογή· και με το
   * πάτημα στο ήδη ενεργό κελί ⇒ επιλογή **παγωμένη στο πρώτο κελί που διασχίστηκε**.
   *
   * ## Γιατί `useEventCallback` και όχι «βγάλε το `cursor` από τις εξαρτήσεις»
   * Το δεύτερο θα έδινε **μπαγιάτικο** `cursor.selection` στο `extendWholeAxis` — το ίδιο
   * ακριβώς σφάλμα που ο ADR-040 απαγορεύει ονομαστικά («event handlers MUST receive
   * getters, not snapshot values — snapshots become stale when the orchestrator skips
   * re-renders»). Ο σταθεροποιητής δίνει **και** τα δύο: ταυτότητα που δεν αλλάζει ποτέ,
   * και ανάγνωση των **τελευταίων** τιμών τη στιγμή του συμβάντος. Δεν είναι νέος
   * μηχανισμός — είναι ο ΕΝΑΣ που ήδη χρησιμοποιεί το subapp σε τρία σημεία.
   */
  const handleMouseDown = useEventCallback((event: MouseEvent): void => {
    // Χωρίς ενεργό δρομέα δεν υπάρχει τίποτα να μετακινηθεί: το πρώτο κλικ σε πίνακα τον
    // **επιλέγει** ως οντότητα (δουλειά του καμβά), και η λειτουργία πίνακα ανοίγει με
    // διπλό κλικ ή `Enter`/`F2`. Ο φύλακας μετακόμισε από το effect **εδώ**, ώστε η
    // εγγραφή του ακροατή να μην εξαρτάται πια από το **περιεχόμενο** της συνεδρίας.
    if (!cursor || !entity) return;
    const container = containerRef.current;
    if (!container) return;

    // 🔴 ADR-739 §27.14 — ΔΥΟ πλήκτρα μπαίνουν, ΕΝΑ ενεργεί.
    //
    // Εδώ έγραφε `if (event.button !== 0) return;` με αιτιολογία «το δεξί ανοίγει το μενού
    // και δεν επιτρέπεται να μετακινήσει την επιλογή». Η αιτιολογία είναι **σωστή** — αλλά
    // η πρόωρη έξοδος έκανε **δύο** πράγματα, όχι ένα: μαζί με τη μετακίνηση απέκλεισε και
    // τη **δήλωση** του §26.15. Αποτέλεσμα: ο browser μετέφερε την εστίαση, ο φύλακας δεν
    // έβλεπε δήλωση, η συνεδρία **έκλεινε** — και όταν έφτανε το `contextmenu`, το
    // `getHit` του μενού ζωνών ρωτούσε δρομέα που δεν υπήρχε πια και απαντούσε `null`.
    // Ο δρομολογητής έπεφτε σωστά στο μενού **οντότητας**: αυτό ακριβώς ανέφερε ο Giorgio
    // ως «στις λωρίδες εμφανίζεται το μενού του καμβά».
    //
    // Ο κανόνας του §26.15 δεν είπε ποτέ «αριστερό»: είπε «**όποτε αυτός ο ακροατής
    // αναγνωρίζει ότι το πάτημα έπεσε μέσα στον δικό του πίνακα, το δηλώνει**». Εδώ ο
    // κανόνας απλώς εφαρμόζεται ακέραιος.
    const primary = event.button === 0;
    // Το μεσαίο πλήκτρο είναι pan του καμβά — ούτε δηλώνεται, ούτε μας αφορά.
    if (!primary && event.button !== 2) return;
    // ADR-739 §26.15 — το πάτημα έπεσε πάνω σε **πεδίο της ίδιας συνεδρίας**, όχι στον
    // καμβά: σε γραφή το `<textarea>` σκεπάζει το κελί, και το κλικ μέσα στο κείμενο είναι
    // **τοποθέτηση κέρσορα**. Ο ακροατής ζει στο ίδιο δοχείο και σε φάση σύλληψης, άρα το
    // βλέπει· χωρίς αυτόν τον φύλακα θα το ερμήνευε ως «κλικ στο κελί» και θα γύριζε τη
    // συνεδρία σε `nav` — δηλαδή θα σου έκοβε τη γραφή τη στιγμή που διορθώνεις ένα γράμμα.
    // Ο ίδιος ΕΝΑΣ ορισμός του «ανήκω στη συνεδρία», καμία δεύτερη σύγκριση.
    if (isTableCellSessionElement(event.target)) return;
    const transform = transformRef.current;
    const worldPoint = tableEventWorldPoint(event, container, transform);
    if (!worldPoint || !transform) return;

    // 🔴 ADR-739 §29 — **η ΜΙΑ ερώτηση «πού έπεσε;»**, η ίδια ακριβώς που ρωτά ο φύλακας
    // του κλειδώματος (`use-table-canvas-lockdown`). Ήταν δύο κλήσεις εδώ (ζώνη, μετά
    // κελί) με τη γεωμετρία υπολογισμένη τοπικά· εξήχθη ώστε να μην μπορεί να αποκλίνει
    // από την ερώτηση που αποφασίζει **αν** το συμβάν φτάνει καν εδώ. Δες την κεφαλίδα του
    // `table-cell-pointer-hit` για το τι θα σήμαινε μια απόκλιση έστω κατά ένα pixel.
    //
    // ADR-739 Φ.Δ βήμα 9 — η **σειρά** (ζώνη πριν από κελί) ζει πλέον μέσα σε εκείνη τη
    // συνάρτηση: ένα κλικ στο `B` επιλέγει ολόκληρη τη στήλη, στο `3` ολόκληρη τη γραμμή.
    const pointerHit = tablePointerHitAtWorld(entity, worldPoint, transform.scale);

    // 🔴 ADR-754 §3 — **ΕΝΑΣ ΦΡΟΥΡΟΣ, ΠΡΙΝ ΑΠΟ ΟΛΑ.** Όταν γράφεται τύπος, το κλικ γράφει
    // **διεύθυνση** αντί να μετακινήσει τον δρομέα. Μπαίνει εδώ και όχι μέσα σε κάποιον από
    // τους κλάδους που ακολουθούν, επειδή **και οι τέσσερις** (διαχωριστικό, ζώνη, μεταφορά
    // περιοχής, κελί) δεσμεύουν ή μετακινούν: φρουρός ανά κλάδο σημαίνει ότι το κλικ σε
    // γράμμα στήλης θα έτρωγε τον μισογραμμένο τύπο, σιωπηλά. Όλη η απόφαση —
    // «γράφει κάποιος;», «τι σημαίνει εδώ;», «τι πιάστηκε;» — ζει στο δικό της module.
    if (tryTablePointModeMouseDown(event, { entity, cursor, hit: pointerHit, container, transformRef })) {
      return;
    }

    // 🔴 ADR-739 §31.9 — **το διαχωριστικό ανάμεσα σε δύο γράμματα**: σύρσιμο πλάτους, όχι
    // επιλογή στήλης. Μπαίνει **πριν** από τη ζώνη γιατί είναι η πιο ειδική περίπτωση· η
    // γεωμετρία της ζώνης έχει ήδη παραιτηθεί στα ίδια pixel, οπότε εδώ δεν υπάρχει
    // διεκδίκηση — μόνο δρομολόγηση.
    if (pointerHit?.where === 'column-edge' || pointerHit?.where === 'row-edge') {
      claimTableCellSessionPointerDown();
      // §27.15 — και η χειρονομία: χωρίς αυτό το body-drag του ADR-560 armάρει στα 3px και
      // το σύρσιμο του διαχωριστικού θα **μετακινούσε τον πίνακα**.
      claimTableCellPointerGesture();
      if (!primary) return;
      if (pointerHit.where === 'column-edge') {
        onResizeAxisEdge('column', pointerHit.columnIndex, container);
      } else {
        onResizeAxisEdge('row', pointerHit.rowIndex, container);
      }
      return;
    }

    if (pointerHit?.where === 'band') {
      const bandHit = pointerHit.band;
      claimTableCellSessionPointerDown();
      // 🔴 ADR-739 §27.15 — και η **χειρονομία** δηλώνεται: χωρίς αυτό, το body-drag του
      // ADR-560 armάρει στα 3px και η σύρση επιλογής θα μετακινούσε τον πίνακα.
      claimTableCellPointerGesture();
      // §27.14 — το δεξί σταματά **εδώ**: δήλωσε και παραδώσου. Τα υπόλοιπα (άνοιγμα
      // μενού ζώνης) τα κάνει ο δρομολογητής στο `contextmenu`, που τώρα βρίσκει
      // ζωντανό δρομέα.
      if (!primary) return;
      // 🔴 §27.16 Ε2 — `Shift+κλικ` σε δεύτερο γράμμα/αριθμό: **επέκταση**, όχι νέα επιλογή.
      // Δοκιμάζεται πρώτη γιατί είναι η πιο **ειδική** ερώτηση· όταν δεν ισχύει (καμία
      // επιλογή, ή επιλογή άλλου είδους) επιστρέφει `null` και το πάτημα συνεχίζει στον
      // κανονικό δρόμο — δηλαδή γίνεται σκέτο κλικ, χωρίς καμία εφεύρεση.
      const shiftAnchor = event.shiftKey
        ? extendWholeAxis(entity, bandHit, cursor.selection)
        : null;
      if (shiftAnchor) {
        startTableCellDrag({
          anchor: shiftAnchor,
          kind: bandHit.axis,
          container,
          resolveAt: (moveEvent) => axisEndAt(moveEvent, entity, container, transformRef, bandHit.axis),
        });
        return;
      }
      // Η ζώνη μετακινεί το ενεργό κελί στην αρχή του άξονα, άρα ισχύει το ίδιο συμβόλαιο
      // με το απλό κλικ: ό,τι γράφεται δεσμεύεται πρώτα.
      onCommitPending();
      const axisAnchor = selectWholeAxis(entity, bandHit);
      // Σύρση **πάνω στα γράμματα/αριθμούς** = πολλές ολόκληρες στήλες/γραμμές (Excel).
      // Η άγκυρα είναι η αρχή του άξονα· το κινούμενο άκρο ακολουθεί μόνο τη θέση κατά
      // μήκος του άξονα, γι' αυτό και το `tableAxisTickAtFrame` αγνοεί τη ζώνη.
      if (axisAnchor) {
        startTableCellDrag({
          anchor: axisAnchor,
          kind: bandHit.axis,
          container,
          resolveAt: (moveEvent) => axisEndAt(moveEvent, entity, container, transformRef, bandHit.axis),
        });
      }
      return;
    }

    // Το κλικ πρέπει να πέσει μέσα στο πλέγμα **αυτού** του πίνακα.
    //
    // 🔴 ADR-739 §29 — **ΤΟ ΣΧΟΛΙΟ ΕΔΩ ΕΛΕΓΕ ΤΟ ΑΝΤΙΘΕΤΟ ΚΑΙ ΗΤΑΝ ΣΩΣΤΟ ΤΟΤΕ.** Έγραφε:
    // «έξω από αυτό — αλλού στον καμβά, ή στα περιθώρια του πίνακα — δεν μας αφορά: ο
    // καμβάς θα κάνει ό,τι κάνει πάντα, και η συνεδρία θα κλείσει μόνη της από τον φύλακα
    // εστίασης». Αυτή ήταν η μισή προδιαγραφή του §26.15, ρητά επαληθευμένη ζωντανά. Ο
    // ιδιοκτήτης την **άλλαξε**: το κλικ έξω δεν είναι πια τρόπος εξόδου.
    //
    // Η αλλαγή **δεν** έγινε εδώ, και ο λόγος είναι δομικός: αυτός ο ακροατής ζει στο
    // δοχείο και τρέχει **μετά** από τους ακροατές σύλληψης του `document`, ενώ η επιλογή
    // οντότητας γεννιέται στο `mouseup` — δηλαδή σε συμβάν που αυτό το αρχείο δεν ακούει
    // καθόλου. Το «έξω» το κόβει πλέον ο φύλακας του §29, **πριν** φτάσει οπουδήποτε.
    // Εδώ μένει η αρχική, ακέραιη σημασία: «δεν είναι δικό μου, δεν το αγγίζω».
    if (pointerHit === null) return;
    const hit = pointerHit.cell;

    // ADR-739 §26.15 — από εδώ και κάτω το πάτημα **είναι** της συνεδρίας. Η δήλωση
    // γίνεται ΠΡΙΝ από κάθε εγγραφή, ώστε να ισχύει και για τους δύο δρόμους που
    // ακολουθούν: και το `Shift+κλικ` (που δεν μετακινεί δρομέα) πρέπει να κρατήσει το
    // πληκτρολόγιο, αλλιώς φτιάχνεις περιοχή και μένεις χωρίς πίνακα.
    claimTableCellSessionPointerDown();
    // §27.15 — δες το δίδυμο σχόλιο στη ζώνη: η οντότητα δεν μετακινείται από πάτημα που
    // σημαδεύει κελί.
    claimTableCellPointerGesture();

    // §27.14 — ίδιος κανόνας μέσα στο πλέγμα: το δεξί κρατά τη συνεδρία ζωντανή αλλά
    // **δεν αγγίζει τίποτα**. Το μενού οντότητας που θα ανοίξει αφορά τον πίνακα ως
    // αντικείμενο· χωρίς τη δήλωση θα άφηνε πίσω του «επιλεγμένος πίνακας χωρίς δρομέα»,
    // δηλαδή ακριβώς την κατάσταση-φάντασμα που κατέγραψε το §27.10.
    if (!primary) return;

    // 🔴 ADR-739 §36 ΦΑΣΗ 3 — **ΤΟ ΠΑΤΗΜΑ ΕΠΕΣΕ ΣΤΟ ΠΕΡΙΓΡΑΜΜΑ ΤΗΣ ΕΠΙΛΟΓΗΣ**: μεταφορά, όχι
    // επιλογή. Η ερώτηση είναι **η ίδια** που ήδη απάντησε ο δείκτης (`range-move`/`range-copy`)
    // και περνά από το ίδιο module — ο χρήστης πιάνει το περίγραμμα **που βλέπει** (§36).
    //
    // Μπαίνει **πριν** από το `Shift+κλικ` επειδή είναι η πιο **ειδική** ερώτηση, και εδώ η
    // σειρά λύνει πραγματική διεκδίκηση: `Shift+πάτημα` **μέσα** στο πλέγμα επεκτείνει την
    // περιοχή, `Shift+σύρσιμο` **πάνω στο περίγραμμα** εισάγει & ολισθαίνει. Στο Excel τα
    // ξεχωρίζει ακριβώς το **πού**, όχι το πλήκτρο.
    const grabbed = tableRangeGrabAtWorld(entity, worldPoint, transform.scale, cursor.selection);
    if (grabbed) {
      // Ό,τι γράφεται δεσμεύεται πρώτα: η μεταφορά αλλάζει κελιά κάτω από τον δρομέα.
      onCommitPending();
      beginTableRangeTransfer({
        entity,
        // §36 ΦΑΣΗ 4 — η μεταφορά μπορεί να **ρωτήσει** πριν γράψει· ο αναγνώστης δίνει τη σκηνή
        // τη στιγμή της **απάντησης**, όχι τη στιγμή του πατήματος.
        liveTable,
        source: grabbed.source,
        grab: grabbed.grab,
        container,
        transformRef,
        // §4.6 — η **ΜΙΑ** διαδρομή commit· το `applyTableRangeTransfer` τηρεί την εγγύηση
        // ταυτότητας, άρα «τίποτα δεν άλλαξε» ⇒ καμία εντολή, κανένα βήμα undo.
        commit: onCommitModel,
      });
      return;
    }

    if (event.shiftKey) {
      // ΚΑΜΙΑ δέσμευση εδώ, ακριβώς όπως στο `Shift+βέλος` (`case 'extend'`): η επέκταση
      // περιοχής είναι κατάσταση **διεπαφής** και δεν αγγίζει το μοντέλο.
      onSelectTo({ rowId: hit.rowId, colId: hit.colId });
      return;
    }
    onCommitPending();
    // Απλό κλικ: **νέα** στήλη αγκύρωσης (`tableCursorAt`) — ένα κλικ ξεκινά καινούρια
    // σειρά καταχώρισης, άρα το επόμενο `Enter` επιστρέφει ΕΔΩ. Κατάσταση `nav`: έδειξες
    // κελί, δεν άρχισες να γράφεις· η γραφή ξεκινά με τον πρώτο χαρακτήρα (Excel).
    setTableCellCursor(entity.id, tableCursorAt(hit.rowId, hit.colId), 'nav');

    // 🔴 ADR-739 §27.15 — από εδώ αρχίζει η **σύρση**. Καμία επιλογή δεν γράφεται τώρα:
    // ένα σκέτο κλικ πρέπει να μείνει σκέτο κλικ («καμία επιλογή ≠ επιλογή 1×1», ρητή
    // απόφαση του βήματος 8 που ο Giorgio επιβεβαίωσε στις 02/08). Η πρώτη επιλογή
    // γεννιέται μόνο όταν το χέρι φτάσει σε **άλλο** κελί — γι' αυτό η άγκυρα του
    // `startTableCellDrag` είναι κι αυτή ο φύλακας «άλλαξε κελί;».
    //
    // 🔴 §27.16 Ε6 — **αυτή η σύρση επιβιώνει πλέον των δικών της εγγραφών**: ο ακροατής
    // δεν ξαναγράφεται σε κάθε αλλαγή δρομέα, άρα το cleanup δεν τη σκοτώνει στη γέννα.
    startTableCellDrag({
      anchor: { rowId: hit.rowId, colId: hit.colId },
      kind: 'range',
      // §27.16 Ε1 — το ίδιο δοχείο που δίνει τη γεωμετρία δίνει και την **άκρη**: μία
      // μέτρηση, δύο ερωτήσεις («πού είμαι στον κόσμο;» / «πόσο κοντά στην άκρη;»).
      container,
      resolveAt: (moveEvent) => cellEndAt(moveEvent, entity, container, transformRef),
    });
  });

  /**
   * 🔴 §27.16 Ε6 — **η ΥΠΑΡΞΗ της συνεδρίας, όχι το περιεχόμενό της.**
   *
   * Η εξάρτηση είναι σκόπιμα **δυαδική**: αλλάζει μόνο όταν ανοίγει ή κλείνει η λειτουργία
   * πίνακα — δηλαδή δύο φορές ανά συνεδρία — και **ποτέ** από τις εκατοντάδες εγγραφές
   * δρομέα/επιλογής που παράγει η ίδια η σύρση. Έτσι διατηρείται ακέραια η αρχική πρόθεση
   * («καμία διαρροή ακροατή όταν δεν χρειάζεται») χωρίς να επιστρέφει το σφάλμα.
   */
  const hasSession = Boolean(cursor && entity);

  useEffect(() => {
    if (!hasSession) return;
    const container = containerRef.current;
    if (!container) return;

    // Φάση **σύλληψης**: ο δρομέας μετακινείται πριν ο καμβάς ερμηνεύσει τη χειρονομία, ώστε
    // ο ζωγράφος να δει τη νέα θέση στο ίδιο καρέ. Καμία κατανάλωση του συμβάντος — δες την
    // κεφαλίδα.
    container.addEventListener('mousedown', handleMouseDown, { capture: true });
    return () => {
      container.removeEventListener('mousedown', handleMouseDown, { capture: true });
      // §27.15 — η συνεδρία έκλεισε (ή άλλαξε πίνακας) με το κουμπί ακόμα κάτω: οι ακροατές
      // της σύρσης ζουν στο `document` και **δεν** θα έφευγαν μόνοι τους.
      endTableCellDrag();
      // §31.9 — **και οι τρεις** χειρονομίες, στο ίδιο σημείο. Μία ξεχασμένη εδώ θα ήταν
      // ακροατής `document` που επιζεί της συνεδρίας του — δηλαδή σύρσιμο πλάτους σε πίνακα
      // που δεν επεξεργάζεται πια κανείς.
      endTableAxisResizeDrag();
      // §36 ΦΑΣΗ 3 — και το **φάντασμα** σβήνει μαζί: ζει σε store που ο ζωγράφος διαβάζει με
      // getter, άρα θα επιβίωνε της συνεδρίας του ως προεπισκόπηση χωρίς σύρση.
      endTableRangeTransferDrag();
    };
  }, [hasSession, containerRef, handleMouseDown]);
}

