/**
 * ADR-739 Φάση Γ / ADR-751 — **το κείμενο των κελιών στον καμβά.**
 *
 * Εξήχθη από το `stamp-table-layout.ts` όταν εκείνο ξαναπλησίασε τις 500 γραμμές (N.7.1),
 * με τον **ίδιο** γνώμονα που είχε βγάλει πριν το πλέγμα (`stamp-table-borders.ts`, ADR-750
 * Φ5): εκείνο το αρχείο μιλά τη γλώσσα του **ορθογωνίου** — `toScreen`, γεμίσματα, δρομέας,
 * ζώνες, περιγράμματα — ενώ όλα εδώ μιλούν τη γλώσσα της **γραμματοσειράς**: `ctx.font`,
 * γραμμή βάσης, υπογράμμιση, LOD γραμμάτων. Δύο ευθύνες που έτυχε να μοιράζονται αρχείο.
 *
 * ## 🔴 ADR-751 — γιατί ο σύνδεσμος ζωγραφίζεται με ΑΠΟΚΟΠΗ και όχι με δεύτερο `fillText`
 *
 * Ο προφανής τρόπος να βαφτεί μπλε ένα κομμάτι της γραμμής είναι να ζωγραφιστεί το κομμάτι
 * μόνο του στη θέση του. **Μετακινεί τα γράμματα:** το `fillText('2310788493')` σε μια
 * υπολογισμένη θέση δεν παράγει τα ίδια pixel με το `fillText('Τηλ: 2310788493')`, γιατί
 * λείπει το ζεύγος kerning στο όριο και επειδή η στρογγυλοποίηση υπο-pixel γίνεται αλλού.
 * Το κείμενο θα «κουνιόταν» τη στιγμή που κάτι μέσα του γίνεται σύνδεσμος.
 *
 * Με αποκοπή, ζωγραφίζεται **δύο φορές το ίδιο ακριβώς string στην ίδια ακριβώς θέση**, και
 * η δεύτερη φορά περιορίζεται στη λωρίδα του τμήματος. Τα glyph είναι κυριολεκτικά τα ίδια·
 * αλλάζει μόνο ποιο χρώμα βγαίνει σε ποιο x. Είναι το ίδιο κόλπο που κάνει κάθε
 * επεξεργαστής κειμένου για τον τονισμό επιλογής.
 *
 * ⚠️ **Και ούτε αυτό πληρώνεται στην κύρια περίπτωση.** Όταν ολόκληρο το κελί είναι ο
 * σύνδεσμος — μια στήλη «E-mail», δηλαδή σχεδόν κάθε πραγματικό παράδειγμα — δεν υπάρχει
 * τμήμα να απομονωθεί: το `fillStyle` απλώς ξεκινά μπλε και η ζωγραφική είναι **ένα**
 * `fillText`, ταυτόσημη με πριν το ADR-751. Δες {@link wholeRunLink}.
 *
 * @module subapps/dxf-viewer/rendering/entities/table/stamp-table-text
 * @see bim/table/table-cell-link-spans.ts — πού κάθεται κάθε τμήμα (η μέτρηση)
 * @see bim/table/table-text-decoration.ts — πού κάθεται η γραμμή (η γεωμετρία)
 */

import type { Point2D } from '../../types/Types';
import type {
  TableCellLayout,
  TableTextRun,
} from '../../../bim/table/table-layout-types';
import {
  tableUnderlineGeometry, type TableUnderlineGeometry,
} from '../../../bim/table/table-text-decoration';
// 🔴 ADR-753 Φ3 — **τι ζωγραφίζεται και πού** απαντιέται σε ΕΝΑ σημείο, κοινό με τη γέφυρα
// primitives (PDF/DXF/σκηνή/ΤΕΚ). Δεύτερο σώμα εδώ θα ήταν structural clone (N.18) και, το
// σοβαρό, δεύτερο σημείο που μπορεί να διαφωνήσει για το πού κάθεται ένα έντονο τμήμα.
import {
  tablePieceInkHex,
  tablePieceLinkStrips,
  tableTextPieces,
  type TableTextPiece,
  type TableTextStripMm,
} from '../../../bim/table/table-text-pieces';
import { buildUIFont } from '../../../config/text-rendering-config';
import { TABLE_CELL_LINK } from '../../../config/color-config';
// 🔴 Η ερώτηση «είναι ΟΛΟ το κελί σύνδεσμος;» απαντιέται σε ΕΝΑ σημείο — δες εκεί γιατί
// (το CHECK 3.28 έπιασε τα δύο σώματα μέσα στο ίδιο commit).
import { wholeRunLink } from '../../../bim/table/table-cell-link-spans';
import type { StampTableContext } from './stamp-table-layout';

/** Κάτω από αυτό το ύψος κεφαλαίου στην οθόνη, το κείμενο κελιού δεν ζωγραφίζεται. */
export const MIN_CELL_TEXT_SCREEN_PX = 5;

/**
 * Η λωρίδα αποκοπής ενός τμήματος, ως πολλαπλάσια του `em` γύρω από τη **γραμμή βάσης**.
 *
 * Γενναιόδωρα και προς τις δύο κατευθύνσεις (η ζώνη ανιούσας→κατιούσας είναι ~0,8/0,2·em):
 * η αποκοπή υπάρχει για να περιορίσει το **οριζόντιο** εύρος, και ένα στενό κάθετο όριο θα
 * έκοβε τόνους, ουρές ή πλάγια γραφή που ξεπερνά τη ζώνη — δηλαδή θα εισήγαγε ακριβώς το
 * οπτικό ελάττωμα που η αποκοπή υπάρχει για να αποφύγει.
 */
const LINK_CLIP_ABOVE_EM = 1.3;
const LINK_CLIP_BELOW_EM = 0.6;

/**
 * 🔴 ADR-739 Φ.Δ βήμα 3 — **Η ΜΙΑ γραμματοσειρά του κειμένου κελιού**, ως αλφαριθμητικό
 * που δέχονται **και** το `ctx.font` του καμβά **και** το CSS `font` shorthand του DOM.
 *
 * Ο in-cell επεξεργαστής είναι ένα `<input>` πάνω ακριβώς από το κελί. Αν χρησιμοποιούσε
 * *δική του* γραμματοσειρά, το κείμενο θα **αναπηδούσε** τη στιγμή του διπλού κλικ και ο
 * κέρσορας θα έπεφτε σε άλλο γράμμα από αυτό που δείχνει ο χρήστης — γιατί ο υπολογισμός
 * πλάτους θα γινόταν σε δύο διαφορετικές γραμματοσειρές. Με **κοινό** αλφαριθμητικό, οι
 * δύο μετρήσεις είναι κυριολεκτικά η **ίδια** μέτρηση της ίδιας μηχανής του browser.
 *
 * ## ✅ ADR-739 Φ.Ε (Α3) — το χρέος «πάντα Arial» ΕΚΛΕΙΣΕ
 * Μέχρι τη Φ.Ε το `'arial'` ήταν καρφωμένο εδώ ενώ ο **μετρητής** διάβαζε το
 * `TableCellStyle.fontFamily`: δύο απαντήσεις στην ίδια ερώτηση, με σύμπτωμα κείμενο που
 * κόβεται ή αφήνει κενό — ορατό μόνο σε όποιον δήλωνε άλλη οικογένεια, δηλαδή σε κανέναν,
 * μέχρι να βγει το χειριστήριο γραμματοσειράς. Τώρα η οικογένεια ταξιδεύει στο
 * `TableTextRun` και τη διαβάζουν και οι δύο.
 *
 * 🔴 **Γιατί ΟΧΙ το `paintTextRun` του text-engine** (η προφανής «κεντρικοποίηση» που θα
 * ήταν λάθος): εκείνο ζωγραφίζει **περιγράμματα glyph** μέσω opentype όταν βρει φορτωμένη
 * γραμματοσειρά CAD. Ο in-cell επεξεργαστής είναι `<input>` — **δεν μπορεί** να ζωγραφίσει
 * περιγράμματα. Θα ξαναγεννούσαμε ακριβώς την ασυμφωνία που αυτή η συνάρτηση υπάρχει για να
 * λείπει, μόνο που τώρα ανάμεσα σε καμβά και πεδίο. Το κοινό αλφαριθμητικό είναι η
 * **προϋπόθεση**, όχι η ευκολία — και το `measureTextAdvanceWorld` πέφτει στην ίδια
 * `buildUIFont` στη 2η βαθμίδα του, άρα μέτρηση και ζωγραφική μένουν η ίδια μηχανή.
 */
export function tableCellFont(
  fontPx: number,
  bold: boolean,
  italic = false,
  fontFamily?: string,
): string {
  // Το ίδιο default με το `baseAdvanceWorld` του text-engine (`style?.fontFamily || 'arial'`):
  // απούσα οικογένεια πρέπει να δίνει **μία** προεπιλογή, όχι μία εδώ και μία εκεί.
  return buildUIFont(fontPx, fontFamily || 'arial', bold ? 'bold' : 'normal', italic);
}

/**
 * 🔴 ADR-739 Φ.Δ βήμα 8 — **Ο ΕΝΑΣ ΣΤΑΜΠΑΔΟΡΟΣ ΚΕΙΜΕΝΟΥ ΤΟΥ ΠΙΝΑΚΑ**, γερμένος όπως ο
 * πίνακας. Τον καλούν **και** τα κείμενα των κελιών ({@link stampRun}) **και** οι ετικέτες
 * των ζωνών `A B C` / `1 2 3` (`stamp-table-indicator.ts`).
 *
 * ## Τι διόρθωσε (μετρημένο, 2026-08-01)
 * Ο πίνακας με `angleRad = 0.35` (≈20°) ζωγραφιζόταν **γερμένος** — πλέγμα, γεμίσματα,
 * δρομέας, ζώνες, όλα περνούσαν από το `toScreen`. Τα **γράμματα** όμως έμεναν **ίσια στην
 * οθόνη**, γιατί εδώ δεν υπήρχε καμία `ctx.rotate`. Στο εξαγόμενο DXF το ίδιο κείμενο
 * βγήκε με `rot = 20,053523°` (διαβάστηκε από το αρχείο, και τα τρία `halign`).
 *
 * Άρα **δεν ήταν τεκμηριωμένη επιλογή — ήταν ασυμφωνία**: τέσσερις μηχανές απαντούσαν στην
 * ίδια ερώτηση και **τρεις** συμφωνούσαν (εξαγωγή DXF, εξαγωγή PDF, επεξεργαστής κελιού με
 * `rotate(-0.35rad)`). Ο ζωγράφος του καμβά ήταν ο **ένας** που διαφωνούσε, και το
 * επιχείρημα της εξαγωγής («αλλιώς το κείμενο βγαίνει έξω από το κελί του») ισχύει
 * **ακριβώς το ίδιο** στην οθόνη — φαινόταν με γυμνό μάτι.
 *
 * ## Γιατί το ίδιο ισχύει και για τις ζώνες δείκτη
 * Θα μπορούσε να επιχειρηματολογήσει κανείς ότι οι ζώνες είναι **διεπαφή**, όχι σχέδιο,
 * και άρα οφείλουν να μένουν ευανάγνωστες. Το επιχείρημα καταρρέει σε ένα σημείο: τα
 * **ορθογώνια** των ζωνών ήδη γέρνουν (περνούν από το `toScreen`). Ένα ίσιο γράμμα μέσα σε
 * γερμένο κουτί δεν είναι «αναγνωσιμότητα» — είναι γράμμα που σε αρκετή γωνία **δραπετεύει
 * από το κουτί του**. Το να γέρνει και το κουτί και το γράμμα είναι το μόνο ζεύγος που
 * παραμένει συνεπές σε κάθε γωνία, και είναι ό,τι κάνει ήδη ο επεξεργαστής κελιού δίπλα
 * τους. Ένα ερώτημα, μία απάντηση, ένα σημείο αλλαγής.
 *
 * ## Τι ΔΕΝ κάνει: δεν «ορθώνει» ανάποδο κείμενο
 * Σε γωνία > 90° το κείμενο γέρνει πέρα από την κατακόρυφο — και στην οθόνη **και** στο
 * DXF, γιατί ο exporter γράφει το ωμό `rotationDeg` χωρίς αναστροφή. Μια «διόρθωση
 * αναγνωσιμότητας» μόνο εδώ θα ξαναγεννούσε **ακριβώς** την ασυμφωνία που αυτό το βήμα
 * έκλεισε. Αν κάποτε θελήσουμε όρθια γράμματα σε ανεστραμμένους πίνακες, ο κανόνας ανήκει
 * στη **διάταξη** (κοινή σε ζωγράφο και exporter), όχι στο μελάνι.
 *
 * @param anchorScreen το σημείο **οθόνης** που ήδη υπολόγισε ο καλών μέσω `toScreen`
 */
export function stampFrameText(
  rc: StampTableContext,
  anchorScreen: Point2D,
  text: string,
  underline?: TableUnderlineGeometry | null,
  /**
   * 🔴 ADR-739 §59 Δ1 — η γωνία **οθόνης** αυτού του κειμένου. Απούσα ⇒ η γωνία του πίνακα,
   * δηλαδή η συμπεριφορά του βήματος 8 ακέραιη (και οι ετικέτες των ζωνών, που δεν έχουν
   * ποτέ δική τους γωνία, δεν αλλάζουν ούτε κατά ένα pixel).
   */
  angleRad: number = rc.textAngleRad,
): void {
  const { ctx } = rc;
  // Μετάθεση **πρώτα**, στροφή **μετά**: η στροφή γίνεται γύρω από την άγκυρα του κειμένου
  // (τη γραμμή βάσης στη θέση στοίχισης), όχι γύρω από την αρχή του καμβά. Αντίστροφα, το
  // κείμενο θα εκτοξευόταν κατά `|anchor| · sin(γωνία)` px μακριά από το κελί του.
  ctx.translate(anchorScreen.x, anchorScreen.y);
  ctx.rotate(angleRad);
  // Το `textAlign` / `textBaseline` που έθεσε ο καλών ισχύουν πλέον στο **στραμμένο**
  // σύστημα — δηλαδή «δεξιά στοίχιση» σημαίνει «δεξιά μέσα στο κελί», που είναι ακριβώς η
  // σημασία που ήδη ταξιδεύει στο DXF ως `halign` (μετρήθηκε: 0/1/2 για left/center/right).
  ctx.fillText(text, 0, 0);
  // ADR-739 Φ.Ε — η υπογράμμιση ζωγραφίζεται **μέσα στην ίδια στροφή**: έξω από αυτήν θα
  // έμενε οριζόντια κάτω από γερμένο κείμενο, δηλαδή το ίδιο ελάττωμα που το βήμα 8 έκλεισε
  // για τα ίδια τα γράμματα.
  if (underline) stampUnderline(ctx, underline);
}

/**
 * Η γραμμή της υπογράμμισης, στο **ήδη στραμμένο** σύστημα του {@link stampFrameText}.
 *
 * 🔴 **Ο καμβάς δεν αποφασίζει πια πού πέφτει η γραμμή — τη ρωτά.** Η γεωμετρία έρχεται
 * ολόκληρη από το `tableUnderlineGeometry` (ADR-739 Φ.Ε/Φ2 βήμα 4), το ΙΔΙΟ σημείο που
 * ρωτά και η γέφυρα εξαγωγής για PDF/DXF/ΤΕΚ. Μέχρι το βήμα 4 εδώ ζούσε δεύτερος
 * υπολογισμός — και ήταν **λάθος** κατά 0,2·em (το σκεπτικό, μετρημένο, στο ίδιο module).
 *
 * Το μόνο που μένει τοπικό είναι το **δάπεδο του ενός pixel**: είναι όρος **οθόνης** (μια
 * γραμμή λεπτότερη από ένα pixel εξαφανίζεται σε μεγάλη σμίκρυνση), όχι σχεδιαστική
 * ιδιότητα — σε χαρτί και σε DXF το πάχος οφείλει να μείνει το πραγματικό κλάσμα του `em`.
 */
function stampUnderline(ctx: CanvasRenderingContext2D, g: TableUnderlineGeometry): void {
  if (!(g.width > 0)) return;
  ctx.fillRect(g.x, g.y, g.width, Math.max(g.thickness, 1));
}

/** Ταυτότητα κελιού — το κλειδί με το οποίο ο καλών ζητά παράλειψη ζωγραφικής. */
export interface TableCellRef {
  readonly rowId: string;
  readonly colId: string;
}

/**
 * Τα κείμενα των κελιών. Το `run.position.y` είναι η **γραμμή βάσης** (σύμβαση
 * `TableTextRun` / `TextPrimitive`), γι' αυτό `textBaseline = 'alphabetic'` — η
 * προεπιλογή. Ρητό `'middle'` εδώ θα μετατόπιζε κάθε γραμμή κατά μισό ύψος κεφαλαίου:
 * ακριβώς το σφάλμα του 1,5mm που η Φ.Β πλήρωσε επειδή μπέρδεψε τη γραμμή
 * περιεχομένου με την ακμή.
 *
 * `skip` (ADR-739 Φ.Δ βήμα 3) = το κελί που έχει **ανοιχτή συνεδρία γραφής**: εκεί το
 * κείμενο το κατέχει το `<input>` του επεξεργαστή. Χωρίς αυτό, το δεσμευμένο κείμενο και
 * το πρόχειρο ζωγραφίζονται **ταυτόχρονα**, το ένα πάνω στο άλλο — το «διπλό κείμενο» που
 * φαινόταν στο στιγμιότυπο του Giorgio (2026-08-01). Στο Excel το κελί που επεξεργάζεσαι
 * δείχνει **μόνο** το πεδίο εισαγωγής.
 *
 * Επιστρέφει `false` όταν το LOD έκοψε το κείμενο — ο καλών το χρειάζεται για tests.
 */
export function stampTableText(
  rc: StampTableContext,
  cells: readonly TableCellLayout[],
  skip?: TableCellRef | null,
): boolean {
  const { ctx } = rc;
  let drewAny = false;
  for (const cell of cells) {
    if (skip && cell.rowId === skip.rowId && cell.colId === skip.colId) continue;
    // 🔴 ADR-739 §58 Γ2 — **όλες** οι οπτικές γραμμές, όχι η πρώτη. Ένα κελί χωρίς αναδίπλωση
    // έχει ακριβώς μία, οπότε ο βρόχος είναι η σημερινή πράξη· με αναδίπλωση είναι ο **μόνος**
    // τρόπος να μη χαθεί σιωπηλά ό,τι ο χρήστης ζήτησε να φανεί.
    for (const run of cell.texts) {
      const fontPx = run.heightMm * rc.pxPerMm;
      // Το LOD κρίνεται **ανά γραμμή** επειδή η σμίκρυνση (§58 Γ1) μπορεί να δώσει άλλο ύψος
      // ανά κελί — και ένα κοινό κατώφλι ανά κελί θα ζωγράφιζε μουτζούρα στο ένα και τίποτα
      // στο άλλο, με το ίδιο zoom.
      if (fontPx < MIN_CELL_TEXT_SCREEN_PX) continue;
      stampRun(ctx, rc, run, fontPx);
      drewAny = true;
    }
  }
  return drewAny;
}

/**
 * ⚠️ **Το κείμενο ΔΕΝ δέχεται ποτέ το `phaseColor` — μετρημένο ελάττωμα, όχι προτίμηση.**
 *
 * Το χρώμα φάσης βάφει τη **σιλουέτα** (γεμίσματα + πλέγμα). Αν έβαφε και τα γράμματα, ένα
 * κελί **με γέμισμα** θα έπαιρνε το ίδιο χρώμα δύο φορές — φόντο και μελάνι — και το κείμενο
 * θα εξαφανιζόταν σε μονόχρωμο πλακάκι. Ακριβώς αυτό συνέβαινε: στο hover η **γραμμή
 * κεφαλίδων** (το μόνο μέρος με `fillColorHex`) έχανε τα «Α/Α · Περιγραφή · Ποσότητα», ενώ
 * οι σειρές δεδομένων — χωρίς γέμισμα, άρα με το σκούρο φόντο του καμβά από πίσω — κρατούσαν
 * τα γράμματά τους. Η ασυμμετρία ήταν το αποτύπωμα: **όποιο κελί έχει γέμισμα, χάνει το
 * κείμενο** (επαληθεύτηκε στην οθόνη, 2026-07-31, ADR-739 §19.8).
 *
 * Το κείμενο κρατά το `run.colorHex` του, οπότε η αντίθεση που σχεδίασε ο συγγραφέας του
 * στυλ επιβιώνει σε **κάθε** φάση. Ίδια αρχή με το AutoCAD: το highlight αλλάζει τη
 * σιλουέτα της οντότητας, δεν την κάνει δυσανάγνωστη.
 */
function stampRun(
  ctx: CanvasRenderingContext2D,
  rc: StampTableContext,
  run: TableTextRun,
  fontPx: number,
): void {
  const anchor = rc.toScreen(run.position.x, run.position.y);
  // 🔴 ADR-739 §59 Δ1 — **η γωνία του κελιού ΠΡΟΣΤΙΘΕΤΑΙ σε αυτήν του πίνακα, με αντίθετο
  // πρόσημο.** Το μοντέλο μετρά θετικά **προς τα πάνω** (σύμβαση Excel, και η ίδια που
  // ταξιδεύει αυτούσια στο DXF group code 50, όπου η σκηνή είναι y-πάνω)· ο καμβάς έχει y προς
  // τα **κάτω**, όπου το θετικό `ctx.rotate` γέρνει προς τα κάτω. Χωρίς την αναστροφή, το ίδιο
  // κελί θα έγερνε **αντίθετα** στην οθόνη από ό,τι στο τυπωμένο σχέδιο — η ακριβής ασυμφωνία
  // που έκλεισε το βήμα 8, ξαναγεννημένη ένα επίπεδο πιο κάτω.
  const angleRad = rc.textAngleRad - ((run.rotationDeg ?? 0) * Math.PI) / 180;
  // 🔴 ADR-753 Φ3 — **ένα** κομμάτι σε κάθε πίνακα που υπάρχει σήμερα (το ίδιο το run), Ν όταν
  // το κελί έχει μορφοποίηση ανά χαρακτήρα. Ο βρόχος είναι ο ίδιος και στις δύο περιπτώσεις:
  // η γενική μορφή **εκφυλίζεται** στη σημερινή, δεν συνυπάρχει μαζί της.
  for (const piece of tableTextPieces(run)) {
    stampPiece(ctx, rc, run, piece, anchor, fontPx, angleRad);
  }
}

/**
 * 🔴 ADR-753 Φ3 — **ΕΝΑ ομοιογενές κομμάτι**: δικό του `ctx.font`, δικό του μελάνι, δική του
 * υπογράμμιση, και μετά οι λωρίδες συνδέσμων που πέφτουν μέσα του.
 *
 * ## Το ύψος του κομματιού, όχι του run
 * Το `fontPx` του καλούντος είναι το ύψος του **κελιού**· ένα τμήμα μπορεί να έχει άλλο
 * (`A↑`/`A↓` του §16.5). Ξαναϋπολογίζεται από το `piece.heightMm` ώστε γραμματοσειρά,
 * υπογράμμιση και λωρίδα αποκοπής να μιλούν όλες για το **ίδιο** em. Με ένα κομμάτι είναι
 * κυριολεκτικά ο ίδιος αριθμός (`piece.heightMm === run.heightMm`).
 *
 * ⚠️ Το κατώφλι LOD μένει στο **run** ({@link stampTableText}) και δεν επαναλαμβάνεται εδώ:
 * είναι ερώτημα «αξίζει να ζωγραφιστεί αυτό το κελί;», και μια απάντηση ανά τμήμα θα
 * εξαφάνιζε **μέρος** μιας λέξης σε ενδιάμεσο zoom — δηλαδή θα έδειχνε στον χρήστη
 * ακρωτηριασμένο κείμενο αντί για κανένα.
 */
function stampPiece(
  ctx: CanvasRenderingContext2D,
  rc: StampTableContext,
  run: TableTextRun,
  piece: TableTextPiece,
  anchor: Point2D,
  runFontPx: number,
  /** §59 Δ1 — γωνία **πίνακα + κελιού**, ήδη σε συντεταγμένες οθόνης. */
  angleRad: number,
): void {
  const fontPx = piece.whole ? runFontPx : piece.heightMm * rc.pxPerMm;
  const inkHex = tablePieceInkHex(run, piece, TABLE_CELL_LINK.colorHex);
  const alreadyBlue = inkHex !== piece.colorHex;

  ctx.save();
  // ADR-751 / ADR-753 Φ3 — όταν ΟΛΟ το κελί είναι ο σύνδεσμος, το μελάνι ξεκινά μπλε και δεν
  // χρειάζεται κανένα δεύτερο πέρασμα: ίδιος αριθμός `fillText` με πριν, ίδια ακριβώς glyph.
  ctx.fillStyle = inkHex;
  ctx.font = tableCellFont(fontPx, piece.bold, piece.italic, piece.fontFamily);
  ctx.textAlign = piece.align;
  ctx.textBaseline = 'alphabetic';
  // ADR-739 Φ.Δ βήμα 8 — γερμένο όπως ο πίνακας, όπως ήδη γέρνουν το DXF, το PDF και ο
  // επεξεργαστής κελιού. Δες το {@link stampFrameText} για το γιατί άλλαξε.
  //
  // ADR-739 Φ.Ε/Φ2 βήμα 4 — το πλάτος έρχεται από τη **διάταξη** (`advanceMm`), όχι από
  // δεύτερο `ctx.measureText` σε κάθε καρέ: η ίδια μέτρηση που αποφάσισε τα πλάτη στηλών και
  // την περικοπή ορίζει και την έκταση της γραμμής, σε καμβά **και** στην εξαγωγή.
  // §59 Δ1 — η μετατόπιση του τμήματος περιστρέφεται με την **ίδια** γωνία που θα γείρουν τα
  // γράμματά του. Με τη γωνία μόνο του πίνακα, τα τμήματα ενός γερμένου κελιού θα απλώνονταν
  // κατά μήκος **άλλης** ευθείας από αυτήν που τα ζωγραφίζει — ορατό σε κάθε έντονη λέξη.
  stampFrameText(rc, offsetAnchor(anchor, piece.offsetMm * rc.pxPerMm, angleRad),
    piece.text, underlineOf(run, piece, fontPx, rc.pxPerMm), angleRad);
  ctx.restore();

  // Μικτό κείμενο: οι σύνδεσμοι χρειάζονται δικό τους πέρασμα με αποκοπή. Δες την κεφαλίδα.
  if (!alreadyBlue) {
    stampLinkStrips(rc, anchor, piece, fontPx, tablePieceLinkStrips(run, piece), angleRad);
  }
}

/**
 * Η άγκυρα ενός κομματιού που κάθεται `xPx` δεξιότερα, στο **στραμμένο** σύστημα του πίνακα.
 *
 * 🔴 Η μετατόπιση περιστρέφεται **πριν** προστεθεί, όχι μετά: το `offsetMm` είναι μήκος κατά
 * μήκος της **γραμμής βάσης**, και η γραμμή βάσης είναι γερμένη κατά `textAngleRad`. Μια ωμή
 * πρόσθεση στο `x` της οθόνης θα άπλωνε τα τμήματα **οριζόντια** κάτω από γερμένο κείμενο —
 * το ίδιο σχήμα ελαττώματος που η Φ.Ε έκλεισε για την υπογράμμιση (§28.10.3), και ορατό με
 * γυμνό μάτι σε κάθε στραμμένο πίνακα. Με `xPx = 0` (μονό κομμάτι) επιστρέφει την άγκυρα
 * αυτούσια, χωρίς καμία πράξη κινητής υποδιαστολής.
 */
function offsetAnchor(anchor: Point2D, xPx: number, angleRad: number): Point2D {
  if (xPx === 0) return anchor;
  return {
    x: anchor.x + xPx * Math.cos(angleRad),
    y: anchor.y + xPx * Math.sin(angleRad),
  };
}

/**
 * Η γεωμετρία υπογράμμισης ενός κομματιού σε **px οθόνης**, ή `null` όταν δεν υπογραμμίζεται.
 *
 * Δύο πηγές, με σαφή σειρά: η υπογράμμιση του **στυλ** (`piece.underline` — για το μονό
 * κομμάτι η ένωση `TableTextRun` εγγυάται τότε το `advanceMm`) και, όταν αυτή λείπει, η
 * υπογράμμιση που φέρνει ο **σύνδεσμος**. Δεν αθροίζονται: δύο γραμμές στο ίδιο `y` είναι μία
 * γραμμή, και το να ζωγραφιστεί δεύτερη φορά μόνο κόστος έχει.
 *
 * Το `x` βγαίνει πάντα με τη στοίχιση **του κομματιού**, γιατί η γραμμή απλώνεται από την ίδια
 * μεριά της ίδιας άγκυρας με τα γράμματα που υπογραμμίζει — και η μετατόπιση `offsetMm` έχει
 * ήδη μπει στην άγκυρα από το {@link offsetAnchor}.
 */
function underlineOf(
  run: TableTextRun,
  piece: TableTextPiece,
  fontPx: number,
  pxPerMm: number,
): TableUnderlineGeometry | null {
  if (piece.underline && piece.advanceMm !== undefined) {
    return tableUnderlineGeometry(fontPx, piece.advanceMm * pxPerMm, piece.align);
  }
  // Ο σύνδεσμος που καλύπτει ολόκληρο το run — μόνο για το μονό κομμάτι, όπου το `wholeRunLink`
  // είναι ορισμένο. Στα τμήματα η γραμμή έρχεται από τις λωρίδες ({@link stampLinkStrips}).
  const whole = piece.whole ? wholeRunLink(run) : null;
  if (whole) return tableUnderlineGeometry(fontPx, whole.advanceMm * pxPerMm, piece.align);
  return null;
}

/**
 * Οι λωρίδες-σύνδεσμοι μέσα σε **ένα κομμάτι**: μπλε γράμματα + υπογράμμιση, μόνο εκεί.
 *
 * Το κείμενο του κομματιού ζωγραφίζεται **ολόκληρο ξανά** μέσα σε αποκοπή αντί να ζωγραφιστεί
 * η υποσυμβολοσειρά μόνη της — δες την ενότητα «γιατί με αποκοπή» στην κεφαλίδα του module.
 * Η στροφή και η στοίχιση επαναλαμβάνονται αυτούσιες, ώστε το δεύτερο πέρασμα να πέφτει
 * **ακριβώς** πάνω στο πρώτο· γι' αυτό το `textAlign` παραμένει αυτό του κομματιού και οι
 * λωρίδες είναι ήδη εκφρασμένες ως προς την **ίδια** άγκυρα.
 *
 * 🔴 ADR-753 Φ3 — η μονάδα της αποκοπής είναι πλέον το **κομμάτι**, όχι το run. Μέσα σε
 * ομοιογενές κομμάτι τα δύο περάσματα παράγουν ταυτόσημες θέσεις glyph, που είναι ακριβώς η
 * προϋπόθεση υπό την οποία δουλεύει το τέχνασμα (ADR-753 §11) — και ο λόγος που το χρώμα δεν
 * επιτρέπεται να ορίσει όριο τμηματοποίησης.
 */
function stampLinkStrips(
  rc: StampTableContext,
  anchorScreen: Point2D,
  piece: TableTextPiece,
  fontPx: number,
  strips: readonly TableTextStripMm[],
  /** §59 Δ1 — γωνία **πίνακα + κελιού**: το δεύτερο πέρασμα οφείλει να πέσει πάνω στο πρώτο. */
  angleRad: number,
): void {
  const { ctx } = rc;
  if (strips.length === 0) return;

  ctx.save();
  ctx.translate(anchorScreen.x, anchorScreen.y);
  ctx.rotate(angleRad);
  ctx.fillStyle = TABLE_CELL_LINK.colorHex;
  ctx.font = tableCellFont(fontPx, piece.bold, piece.italic, piece.fontFamily);
  ctx.textAlign = piece.align;
  ctx.textBaseline = 'alphabetic';

  // Το κείμενο του κομματιού ζωγραφίζεται στη **δική** του θέση, ενώ οι λωρίδες μετρώνται από
  // την άγκυρα του run — γι' αυτό η αποκοπή και η γραφή έχουν χωριστά x.
  const pieceXPx = piece.offsetMm * rc.pxPerMm;

  for (const strip of strips) {
    const xPx = strip.offsetMm * rc.pxPerMm;
    const widthPx = strip.advanceMm * rc.pxPerMm;
    if (!(widthPx > 0)) continue;

    ctx.save();
    ctx.beginPath();
    ctx.rect(xPx, -fontPx * LINK_CLIP_ABOVE_EM, widthPx, fontPx * (LINK_CLIP_ABOVE_EM + LINK_CLIP_BELOW_EM));
    ctx.clip();
    ctx.fillText(piece.text, pieceXPx, 0);
    ctx.restore();

    // Η γραμμή ζητιέται με `'left'` — το `x` της στοίχισης το έχει ήδη λύσει το `offsetMm` —
    // και μετατοπίζεται εκεί. Ίδιο SSoT γεωμετρίας, ίδιος ζωγράφος, καμία δεύτερη απόφαση.
    const geometry = tableUnderlineGeometry(fontPx, widthPx, 'left');
    stampUnderline(ctx, { ...geometry, x: geometry.x + xPx });
  }

  ctx.restore();
}
