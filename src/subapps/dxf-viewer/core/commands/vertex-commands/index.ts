/**
 * VERTEX COMMANDS
 *
 * 🏢 ENTERPRISE (2026-01-25): Commands for vertex-level operations
 * - MoveVertexCommand: Move existing vertices (with merge support for drag)
 * - AddVertexCommand: Insert new vertices (edge midpoint click)
 * - RemoveVertexCommand: Delete vertices
 */

export { MoveVertexCommand } from './MoveVertexCommand';
export { AddVertexCommand } from './AddVertexCommand';
export { RemoveVertexCommand } from './RemoveVertexCommand';
