/**
 * 🏢 ENTERPRISE — **«πόσοι χαρακτήρες χωρούν σε αυτό το πλάτος;»**: ο ΕΝΑΣ χάρακας.
 *
 * ── Γιατί υπάρχει (N.0.2 / N.18) ─────────────────────────────────────────────────────────
 * Η ερώτηση είναι **μία** και εμφανίζεται σε **δύο** άσχετα μεταξύ τους σημεία:
 *   1. **Αναδίπλωση MTEXT** (`text-layout.ts`, ADR-635 Φ C.20) — πού σπάει η γραμμή μέσα
 *      στη στήλη (group 41).
 *   2. **Περικοπή κελιού πίνακα** (`bim/table/table-cell-overflow.ts`, ADR-739 Φ.Δ βήμα 5) —
 *      πού κόβεται το κείμενο στο όριο του κελιού.
 * Πριν από αυτό το module η απάντηση ζούσε **ιδιωτική** μέσα στην αναδίπλωση. Ένα δεύτερο
 * αντίγραφο του ίδιου δυαδικού ψαξίματος στον πίνακα θα ήταν ακριβώς ο structural clone που
 * το CHECK 3.28 (jscpd, ADR-584) υπάρχει για να πιάνει — **ανεξάρτητα ονόματος**.
 *
 * ── Η γραμμή που ΧΩΡΙΖΕΙ τα δύο (και γιατί δεν είναι λεπτομέρεια) ────────────────────────
 * Ο **χάρακας** είναι κοινός· η **πολιτική** όχι:
 *   - Η αναδίπλωση προτιμά όριο **λέξης** (μια λέξη κομμένη στη μέση και συνεχισμένη στην
 *     επόμενη γραμμή είναι αναγνωστικά λάθος).
 *   - Η περικοπή κόβει σε **χαρακτήρα** (η λέξη ΔΕΝ συνεχίζεται πουθενά· κόβοντας σε όριο
 *     λέξης πετάς ολόκληρη τη λέξη και δείχνεις πολύ λιγότερο απ' όσο χωρούσε — το
 *     τεκμηριωμένο παράπονο για το `Truncate text` της Figma, που κόβει ανά λέξη).
 * Γι' αυτό εδώ ζει **μόνο** το χαρακτηρο-επίπεδο ερώτημα· η προτίμηση λέξης μένει στον
 * καλούντα που τη χρειάζεται.
 *
 * Καθαρό: ο μετρητής **ενίεται**, ώστε το module να μην ξέρει ούτε γραμματοσειρές ούτε
 * μονάδες — ο καλών φέρνει το SSoT του (`measureTextAdvanceWorld` και στις δύο περιπτώσεις
 * σήμερα· ποτέ δεύτερος measurer, N.18).
 *
 * @module subapps/dxf-viewer/bim/text/text-fit
 * @see bim/text/text-layout.ts — καταναλωτής 1 (αναδίπλωση MTEXT)
 * @see bim/table/table-cell-overflow.ts — καταναλωτής 2 (περικοπή κελιού)
 */

/** Πλάτος ενός αλφαριθμητικού στις μονάδες του καλούντος. Καθαρή, ντετερμινιστική. */
export type TextWidthMeasure = (text: string) => number;

/**
 * Το μήκος του **μεγαλύτερου προθέματος** του `text` που χωρά σε `available` — σε επίπεδο
 * **χαρακτήρα**, χωρίς καμία γνώση λέξεων.
 *
 * `0` όταν δεν χωρά ούτε ένας χαρακτήρας (ή `available ≤ 0`)· `text.length` όταν χωρά όλο.
 *
 * ⚠️ **ΚΟΣΤΟΣ — ο λόγος που είναι δυαδική αναζήτηση.** Μια αφελής σάρωση χαρακτήρα-χαρακτήρα
 * κάνει O(n) μετρήσεις **ανά κλήση** ⇒ O(n²) για μια παράγραφο 2.800 χαρακτήρων, σε **κάθε**
 * υπολογισμό κουτιού/απόδοσης. Το `measure` είναι το ακριβό μέρος (μετρικά γραμματοσειράς),
 * όχι η αριθμητική: ελαχιστοποιούμε τις **κλήσεις του**, O(log n).
 *
 * Η μονοτονία που κάνει τη δυαδική αναζήτηση σωστή — «πιο μακρύ πρόθεμα ⇒ ≥ πλάτος» — ισχύει
 * για κάθε λογικό μετρητή προόδου πένας (και για τα δύο σημερινά μονοπάτια: opentype advance
 * και CSS `measureText`). Αρνητικά kerning άκρων δεν σπάνε το αποτέλεσμα οπτικά, μόνο το
 * καθιστούν κατά έναν χαρακτήρα συντηρητικό.
 */
export function fittingPrefixLengthByChar(
  text: string,
  available: number,
  measure: TextWidthMeasure,
): number {
  if (!text) return 0;
  if (!(available > 0)) return 0;
  if (measure(text) <= available) return text.length;

  let lo = 0;
  let hi = text.length;
  while (lo < hi) {
    const mid = Math.ceil((lo + hi) / 2);
    if (measure(text.slice(0, mid)) <= available) lo = mid;
    else hi = mid - 1;
  }
  return lo;
}
