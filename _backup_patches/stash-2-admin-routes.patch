diff --git a/src/app/api/admin/cleanup-duplicates/route.ts b/src/app/api/admin/cleanup-duplicates/route.ts
index 5f5e0834..57f0d6b5 100644
--- a/src/app/api/admin/cleanup-duplicates/route.ts
+++ b/src/app/api/admin/cleanup-duplicates/route.ts
@@ -1,10 +1,18 @@
-import { NextRequest, NextResponse } from 'next/server';
-import { collection, getDocs, deleteDoc, doc, query } from 'firebase/firestore';
-import { db } from '@/lib/firebase';
-import { COLLECTIONS } from '@/config/firestore-collections';
-
 /**
- * 🏢 ENTERPRISE: API για καθαρισμό διπλότυπων μονάδων
+ * =============================================================================
+ * CLEANUP DUPLICATE UNITS - PROTECTED (AUTHZ Phase 2)
+ * =============================================================================
+ *
+ * API για καθαρισμό διπλότυπων units (manual data cleanup).
+ *
+ * @module api/admin/cleanup-duplicates
+ * @enterprise RFC v6 - Authorization & RBAC System
+ *
+ * 🔒 SECURITY: Protected with RBAC (AUTHZ Phase 2)
+ * - Permission: admin:migrations:execute (super_admin ONLY)
+ * - Manual Data Cleanup: Mass deletion of duplicate units
+ * - Multi-Layer Security: withAuth + explicit super_admin check
+ * - Comprehensive audit logging
  *
  * Αυτό το endpoint:
  * 1. Βρίσκει όλες τις μονάδες με το ίδιο όνομα
@@ -15,6 +23,13 @@ import { COLLECTIONS } from '@/config/firestore-collections';
  * @method DELETE - Πραγματικός καθαρισμός
  */
 
+import { NextRequest, NextResponse } from 'next/server';
+import { withAuth, logMigrationExecuted, extractRequestMetadata } from '@/lib/auth';
+import type { AuthContext, PermissionCache } from '@/lib/auth';
+import { collection, getDocs, deleteDoc, doc, query } from 'firebase/firestore';
+import { db } from '@/lib/firebase';
+import { COLLECTIONS } from '@/config/firestore-collections';
+
 interface UnitRecord {
   id: string;
   name: string;
@@ -22,7 +37,59 @@ interface UnitRecord {
   floorId?: string;
 }
 
-export async function GET() {
+/**
+ * GET /api/admin/cleanup-duplicates
+ *
+ * 🔒 SECURITY: Protected with RBAC (AUTHZ Phase 2)
+ * - Permission: admin:migrations:execute
+ * - Super_admin ONLY (explicit check below)
+ */
+export const GET = withAuth(
+  async (req: NextRequest, ctx: AuthContext, _cache: PermissionCache): Promise<NextResponse> => {
+    return handleCleanupPreview(req, ctx);
+  },
+  { permissions: 'admin:migrations:execute' }
+);
+
+/**
+ * DELETE /api/admin/cleanup-duplicates
+ *
+ * 🔒 SECURITY: Protected with RBAC (AUTHZ Phase 2)
+ * - Permission: admin:migrations:execute
+ * - Super_admin ONLY (explicit check below)
+ */
+export const DELETE = withAuth(
+  async (req: NextRequest, ctx: AuthContext, _cache: PermissionCache): Promise<NextResponse> => {
+    return handleCleanupExecute(req, ctx);
+  },
+  { permissions: 'admin:migrations:execute' }
+);
+
+async function handleCleanupPreview(
+  request: NextRequest,
+  ctx: AuthContext
+): Promise<NextResponse> {
+  // ========================================================================
+  // LAYER 1: Super_admin ONLY check (EXTRA security layer)
+  // ========================================================================
+
+  if (ctx.globalRole !== 'super_admin') {
+    console.warn(
+      `🚫 [CLEANUP_PREVIEW] BLOCKED: Non-super_admin attempted duplicate preview: ` +
+      `${ctx.email} (${ctx.globalRole})`
+    );
+    return NextResponse.json(
+      {
+        success: false,
+        error: 'Forbidden: Only super_admin can preview duplicate cleanup',
+        message: 'Duplicate cleanup is a system-level operation restricted to super_admin'
+      },
+      { status: 403 }
+    );
+  }
+
+  console.log(`🔐 [CLEANUP_PREVIEW] Request from ${ctx.email} (${ctx.globalRole}, company: ${ctx.companyId})`);
+
   try {
     console.log('🔍 Analyzing duplicate units...');
 
@@ -61,6 +128,8 @@ export async function GET() {
       }
     });
 
+    console.log(`📊 Found ${totalToDelete} duplicate units to delete`);
+
     return NextResponse.json({
       success: true,
       mode: 'preview',
@@ -90,7 +159,33 @@ export async function GET() {
   }
 }
 
-export async function DELETE(request: NextRequest) {
+async function handleCleanupExecute(
+  request: NextRequest,
+  ctx: AuthContext
+): Promise<NextResponse> {
+  const startTime = Date.now();
+
+  // ========================================================================
+  // LAYER 1: Super_admin ONLY check (EXTRA security layer)
+  // ========================================================================
+
+  if (ctx.globalRole !== 'super_admin') {
+    console.warn(
+      `🚫 [CLEANUP_EXECUTE] BLOCKED: Non-super_admin attempted mass deletion: ` +
+      `${ctx.email} (${ctx.globalRole})`
+    );
+    return NextResponse.json(
+      {
+        success: false,
+        error: 'Forbidden: Only super_admin can execute duplicate cleanup',
+        message: 'Mass deletion is a system-level operation restricted to super_admin'
+      },
+      { status: 403 }
+    );
+  }
+
+  console.log(`🔐 [CLEANUP_EXECUTE] Request from ${ctx.email} (${ctx.globalRole}, company: ${ctx.companyId})`);
+
   try {
     console.log('🧹 Starting duplicate cleanup...');
 
@@ -131,6 +226,7 @@ export async function DELETE(request: NextRequest) {
     });
 
     if (idsToDelete.length === 0) {
+      console.log('ℹ️ No duplicates found to delete');
       return NextResponse.json({
         success: true,
         message: 'No duplicates found to delete',
@@ -155,6 +251,32 @@ export async function DELETE(request: NextRequest) {
       }
     }
 
+    const duration = Date.now() - startTime;
+
+    console.log(`📊 Cleanup completed: ${deletedCount} deleted, ${errors.length} failed`);
+
+    // 🏢 ENTERPRISE: Audit logging (non-blocking)
+    const metadata = extractRequestMetadata(request);
+    await logMigrationExecuted(
+      ctx,
+      'cleanup_duplicate_units',
+      {
+        operation: 'cleanup-duplicates',
+        totalUnits: units.length,
+        duplicatesFound: idsToDelete.length,
+        deletedCount,
+        failedCount: errors.length,
+        remainingUnits: units.length - deletedCount,
+        deletedDetails,
+        executionTimeMs: duration,
+        result: errors.length === 0 ? 'success' : 'partial_success',
+        metadata,
+      },
+      `Mass cleanup of duplicate units by ${ctx.globalRole} ${ctx.email}`
+    ).catch((err: unknown) => {
+      console.error('⚠️ [CLEANUP_EXECUTE] Audit logging failed (non-blocking):', err);
+    });
+
     return NextResponse.json({
       success: true,
       message: `Successfully deleted ${deletedCount} duplicate units`,
@@ -163,6 +285,7 @@ export async function DELETE(request: NextRequest) {
       failedIds: errors.length > 0 ? errors : undefined,
       details: deletedDetails,
       remainingUnits: units.length - deletedCount,
+      executionTimeMs: duration,
     });
   } catch (error) {
     console.error('❌ Error during cleanup:', error);
diff --git a/src/app/api/admin/create-clean-projects/route.ts b/src/app/api/admin/create-clean-projects/route.ts
index d11004c2..b2fdbcae 100644
--- a/src/app/api/admin/create-clean-projects/route.ts
+++ b/src/app/api/admin/create-clean-projects/route.ts
@@ -1,9 +1,24 @@
 /**
- * CLEAN PROJECT CREATION - Fresh Start for Development
- * Creates clean projects with proper companyIds and structure
+ * =============================================================================
+ * CREATE CLEAN PROJECTS - PROTECTED (AUTHZ Phase 2)
+ * =============================================================================
+ *
+ * API για δημιουργία καθαρών projects με normalized structure (manual data creation).
+ * Δημιουργεί projects, buildings, και floors με proper relationships.
+ *
+ * @module api/admin/create-clean-projects
+ * @enterprise RFC v6 - Authorization & RBAC System
+ *
+ * 🔒 SECURITY: Protected with RBAC (AUTHZ Phase 2)
+ * - Permission: admin:migrations:execute (super_admin ONLY)
+ * - Manual Data Creation: Mass creation of projects/buildings/floors
+ * - Multi-Layer Security: withAuth + explicit super_admin check
+ * - Comprehensive audit logging
  */
 
 import { NextRequest, NextResponse } from 'next/server';
+import { withAuth, logMigrationExecuted, extractRequestMetadata } from '@/lib/auth';
+import type { AuthContext, PermissionCache } from '@/lib/auth';
 import { getFirestore } from 'firebase-admin/firestore';
 import { initializeApp, getApps } from 'firebase-admin/app';
 import { COLLECTIONS } from '@/config/firestore-collections';
@@ -24,9 +39,51 @@ try {
   console.error('Failed to initialize Admin SDK:', error);
 }
 
+/**
+ * POST /api/admin/create-clean-projects
+ *
+ * 🔒 SECURITY: Protected with RBAC (AUTHZ Phase 2)
+ * - Permission: admin:migrations:execute
+ * - Super_admin ONLY (explicit check below)
+ */
 export async function POST(request: NextRequest) {
+  const handler = withAuth(
+    async (req: NextRequest, ctx: AuthContext, _cache: PermissionCache): Promise<NextResponse> => {
+      return handleCreateCleanProjects(req, ctx);
+    },
+    { permissions: 'admin:migrations:execute' }
+  );
+
+  return handler(request);
+}
+
+async function handleCreateCleanProjects(
+  request: NextRequest,
+  ctx: AuthContext
+): Promise<NextResponse> {
   const startTime = Date.now();
 
+  // ========================================================================
+  // LAYER 1: Super_admin ONLY check (EXTRA security layer)
+  // ========================================================================
+
+  if (ctx.globalRole !== 'super_admin') {
+    console.warn(
+      `🚫 [CREATE_CLEAN] BLOCKED: Non-super_admin attempted mass creation: ` +
+      `${ctx.email} (${ctx.globalRole})`
+    );
+    return NextResponse.json(
+      {
+        success: false,
+        error: 'Forbidden: Only super_admin can execute mass data creation',
+        message: 'Mass creation is a system-level operation restricted to super_admin'
+      },
+      { status: 403 }
+    );
+  }
+
+  console.log(`🔐 [CREATE_CLEAN] Request from ${ctx.email} (${ctx.globalRole}, company: ${ctx.companyId})`);
+
   try {
     if (!adminDb) {
       throw new Error('Firebase Admin SDK not properly initialized');
@@ -194,6 +251,29 @@ export async function POST(request: NextRequest) {
 
     const totalExecutionTime = Date.now() - startTime;
 
+    // 🏢 ENTERPRISE: Audit logging (non-blocking)
+    const metadata = extractRequestMetadata(request);
+    await logMigrationExecuted(
+      ctx,
+      'create_clean_projects',
+      {
+        operation: 'create-clean-projects',
+        projectsCreated: results.length,
+        buildingsCreated: results.reduce((total, r) => total + r.buildingsCreated, 0),
+        floorsCreated: results.reduce((total, r) => total + r.floorsCreated, 0),
+        targetCompanyId: correctCompanyId,
+        totalProjectsInDb: projectsSnapshot.size,
+        totalBuildingsInDb: buildingsSnapshot.size,
+        totalFloorsInDb: floorsSnapshot.size,
+        executionTimeMs: totalExecutionTime,
+        result: 'success',
+        metadata,
+      },
+      `Mass creation of clean projects by ${ctx.globalRole} ${ctx.email}`
+    ).catch((err: unknown) => {
+      console.error('⚠️ [CREATE_CLEAN] Audit logging failed (non-blocking):', err);
+    });
+
     const response = {
       success: true,
       summary: {
@@ -254,13 +334,22 @@ export async function POST(request: NextRequest) {
   }
 }
 
+/**
+ * GET /api/admin/create-clean-projects
+ *
+ * Public endpoint for system information (discovery).
+ * Execution requires POST με admin:migrations:execute permission.
+ */
 export async function GET(request: NextRequest) {
+  const correctCompanyId = process.env.NEXT_PUBLIC_MAIN_COMPANY_ID || 'default-company-id';
+
   return NextResponse.json({
     success: true,
     system: {
       name: 'Clean Project Creation',
       version: '1.0.0',
       description: 'Creates fresh projects with proper structure for development',
+      security: 'AUTHZ Phase 2 - RBAC Protected (super_admin ONLY)',
       environment: process.env.NODE_ENV,
       timestamp: new Date().toISOString()
     },
@@ -269,6 +358,12 @@ export async function GET(request: NextRequest) {
       method: 'Firebase Admin SDK creation',
       target: `Create clean normalized projects with companyId: ${correctCompanyId}`,
       features: ['Normalized structure', 'Buildings & Floors', 'Clean IDs']
+    },
+    security: {
+      authentication: 'Firebase Auth + withAuth middleware',
+      permission: 'admin:migrations:execute',
+      roles: ['super_admin'],
+      auditLogging: 'All operations logged to /companies/{companyId}/audit_logs',
     }
   });
-}
\ No newline at end of file
+}
diff --git a/src/app/api/admin/fix-building-project/route.ts b/src/app/api/admin/fix-building-project/route.ts
index 4abd02c0..b2ed7ab9 100644
--- a/src/app/api/admin/fix-building-project/route.ts
+++ b/src/app/api/admin/fix-building-project/route.ts
@@ -1,15 +1,72 @@
+/**
+ * =============================================================================
+ * FIX BUILDING-PROJECT RELATIONSHIP - PROTECTED (AUTHZ Phase 2)
+ * =============================================================================
+ *
+ * API για διόρθωση projectId σε buildings (manual data fix).
+ *
+ * @module api/admin/fix-building-project
+ * @enterprise RFC v6 - Authorization & RBAC System
+ *
+ * 🔒 SECURITY: Protected with RBAC (AUTHZ Phase 2)
+ * - Permission: admin:migrations:execute (super_admin ONLY)
+ * - Manual Data Fix: Ad-hoc migration for fixing relationships
+ * - Multi-Layer Security: withAuth + explicit super_admin check
+ * - Comprehensive audit logging
+ */
+
 import { NextRequest, NextResponse } from 'next/server';
+import { withAuth, logMigrationExecuted, extractRequestMetadata } from '@/lib/auth';
+import type { AuthContext, PermissionCache } from '@/lib/auth';
 import { doc, updateDoc } from 'firebase/firestore';
 import { db } from '@/lib/firebase';
 import { COLLECTIONS } from '@/config/firestore-collections';
 
 /**
- * 🏢 ENTERPRISE: API για διόρθωση projectId σε buildings
+ * POST /api/admin/fix-building-project
  *
- * @method POST - Διορθώνει το projectId του κτιρίου
+ * 🔒 SECURITY: Protected with RBAC (AUTHZ Phase 2)
+ * - Permission: admin:migrations:execute
+ * - Super_admin ONLY (explicit check below)
  */
-
 export async function POST(request: NextRequest) {
+  const handler = withAuth(
+    async (req: NextRequest, ctx: AuthContext, _cache: PermissionCache): Promise<NextResponse> => {
+      return handleFixBuildingProject(req, ctx);
+    },
+    { permissions: 'admin:migrations:execute' }
+  );
+
+  return handler(request);
+}
+
+async function handleFixBuildingProject(
+  request: NextRequest,
+  ctx: AuthContext
+): Promise<NextResponse> {
+  const startTime = Date.now();
+
+  // ========================================================================
+  // LAYER 1: Super_admin ONLY check (EXTRA security layer)
+  // ========================================================================
+
+  if (ctx.globalRole !== 'super_admin') {
+    console.warn(
+      `🚫 [FIX_BUILDING] BLOCKED: Non-super_admin attempted data fix: ` +
+      `${ctx.email} (${ctx.globalRole})`
+    );
+    return NextResponse.json(
+      {
+        success: false,
+        error: 'Forbidden: Only super_admin can execute manual data fixes',
+        message: 'Data fixes are system-level operations restricted to super_admin'
+      },
+      { status: 403 }
+    );
+  }
+
+  console.log(`🔐 [FIX_BUILDING] Request from ${ctx.email} (${ctx.globalRole}, company: ${ctx.companyId})`);
+
   try {
     const { buildingId, newProjectId } = await request.json();
 
@@ -30,6 +87,25 @@ export async function POST(request: NextRequest) {
 
     console.log(`✅ Building ${buildingId} updated successfully`);
 
+    // 🏢 ENTERPRISE: Audit logging (non-blocking)
+    const duration = Date.now() - startTime;
+    const metadata = extractRequestMetadata(request);
+    await logMigrationExecuted(
+      ctx,
+      `fix_building_project_${buildingId}`,
+      {
+        operation: 'fix-building-project',
+        buildingId,
+        newProjectId,
+        executionTimeMs: duration,
+        result: 'success',
+        metadata,
+      },
+      `Manual data fix by ${ctx.globalRole} ${ctx.email}`
+    ).catch((err: unknown) => {
+      console.error('⚠️ [FIX_BUILDING] Audit logging failed (non-blocking):', err);
+    });
+
     return NextResponse.json({
       success: true,
       message: `Building ${buildingId} updated with projectId: ${newProjectId}`,
diff --git a/src/app/api/admin/fix-projects-direct/route.ts b/src/app/api/admin/fix-projects-direct/route.ts
index a6f5aec4..fd4defe1 100644
--- a/src/app/api/admin/fix-projects-direct/route.ts
+++ b/src/app/api/admin/fix-projects-direct/route.ts
@@ -1,9 +1,24 @@
 /**
- * ENTERPRISE FIX - Direct Admin SDK Project CompanyID Update
- * Bypasses all permission systems using Firebase Admin SDK
+ * =============================================================================
+ * FIX PROJECTS DIRECT - BULK COMPANY ID UPDATE - PROTECTED (AUTHZ Phase 2)
+ * =============================================================================
+ *
+ * API για mass update των companyIds σε projects (manual data fix).
+ * Χρησιμοποιεί Firebase Admin SDK για bulk operations.
+ *
+ * @module api/admin/fix-projects-direct
+ * @enterprise RFC v6 - Authorization & RBAC System
+ *
+ * 🔒 SECURITY: Protected with RBAC (AUTHZ Phase 2)
+ * - Permission: admin:migrations:execute (super_admin ONLY)
+ * - Manual Data Fix: Mass bulk update for fixing companyIds
+ * - Multi-Layer Security: withAuth + explicit super_admin check
+ * - Comprehensive audit logging
  */
 
 import { NextRequest, NextResponse } from 'next/server';
+import { withAuth, logMigrationExecuted, extractRequestMetadata } from '@/lib/auth';
+import type { AuthContext, PermissionCache } from '@/lib/auth';
 import { getFirestore } from 'firebase-admin/firestore';
 import { initializeApp, getApps } from 'firebase-admin/app';
 import { COLLECTIONS } from '@/config/firestore-collections';
@@ -24,9 +39,51 @@ try {
   console.error('Failed to initialize Admin SDK:', error);
 }
 
+/**
+ * POST /api/admin/fix-projects-direct
+ *
+ * 🔒 SECURITY: Protected with RBAC (AUTHZ Phase 2)
+ * - Permission: admin:migrations:execute
+ * - Super_admin ONLY (explicit check below)
+ */
 export async function POST(request: NextRequest) {
+  const handler = withAuth(
+    async (req: NextRequest, ctx: AuthContext, _cache: PermissionCache): Promise<NextResponse> => {
+      return handleFixProjectsDirect(req, ctx);
+    },
+    { permissions: 'admin:migrations:execute' }
+  );
+
+  return handler(request);
+}
+
+async function handleFixProjectsDirect(
+  request: NextRequest,
+  ctx: AuthContext
+): Promise<NextResponse> {
   const startTime = Date.now();
 
+  // ========================================================================
+  // LAYER 1: Super_admin ONLY check (EXTRA security layer)
+  // ========================================================================
+
+  if (ctx.globalRole !== 'super_admin') {
+    console.warn(
+      `🚫 [FIX_PROJECTS] BLOCKED: Non-super_admin attempted mass update: ` +
+      `${ctx.email} (${ctx.globalRole})`
+    );
+    return NextResponse.json(
+      {
+        success: false,
+        error: 'Forbidden: Only super_admin can execute mass data fixes',
+        message: 'Mass updates are system-level operations restricted to super_admin'
+      },
+      { status: 403 }
+    );
+  }
+
+  console.log(`🔐 [FIX_PROJECTS] Request from ${ctx.email} (${ctx.globalRole}, company: ${ctx.companyId})`);
+
   try {
     if (!adminDb) {
       throw new Error('Firebase Admin SDK not properly initialized');
@@ -75,7 +132,8 @@ export async function POST(request: NextRequest) {
         try {
           // Direct Admin SDK update - bypasses all permissions
           await adminDb.collection(COLLECTIONS.PROJECTS).doc(projectId).update({
-            companyId: correctCompanyId
+            companyId: correctCompanyId,
+            updatedAt: new Date().toISOString(),
           });
 
           updates.push({
@@ -137,6 +195,28 @@ export async function POST(request: NextRequest) {
     console.log(`   Errors: ${errors.length}`);
     console.log(`   Total execution time: ${totalExecutionTime}ms`);
 
+    // 🏢 ENTERPRISE: Audit logging (non-blocking)
+    const metadata = extractRequestMetadata(request);
+    await logMigrationExecuted(
+      ctx,
+      'fix_projects_direct_bulk',
+      {
+        operation: 'fix-projects-direct',
+        targetCompanyId: correctCompanyId,
+        totalProjects,
+        successfulUpdates,
+        correctProjectsAfterUpdate: correctProjects,
+        errors: errors.length,
+        allProjectsFixed: correctProjects === totalProjects,
+        executionTimeMs: totalExecutionTime,
+        result: errors.length === 0 ? 'success' : 'partial_success',
+        metadata,
+      },
+      `Mass bulk update of project companyIds by ${ctx.globalRole} ${ctx.email}`
+    ).catch((err: unknown) => {
+      console.error('⚠️ [FIX_PROJECTS] Audit logging failed (non-blocking):', err);
+    });
+
     const response = {
       success: errors.length === 0,
       summary: {
@@ -200,6 +280,12 @@ export async function POST(request: NextRequest) {
   }
 }
 
+/**
+ * GET /api/admin/fix-projects-direct
+ *
+ * Public endpoint for system information (discovery).
+ * Execution requires POST με admin:migrations:execute permission.
+ */
 export async function GET(request: NextRequest) {
   try {
     return NextResponse.json({
@@ -207,7 +293,8 @@ export async function GET(request: NextRequest) {
       system: {
         name: 'Direct Admin Project CompanyID Fix',
         version: '1.0.0',
-        description: 'Bypasses all permission systems using Firebase Admin SDK',
+        description: 'Mass bulk update for project companyIds using Firebase Admin SDK',
+        security: 'AUTHZ Phase 2 - RBAC Protected (super_admin ONLY)',
         environment: process.env.NODE_ENV,
         timestamp: new Date().toISOString()
       },
@@ -216,6 +303,12 @@ export async function GET(request: NextRequest) {
         method: 'Direct Firebase Admin SDK update',
         target: 'Fix all project companyIds to: pzNUy8ksddGCtcQMqumR',
         features: ['Permission bypass', 'Verification', 'Detailed logging']
+      },
+      security: {
+        authentication: 'Firebase Auth + withAuth middleware',
+        permission: 'admin:migrations:execute',
+        roles: ['super_admin'],
+        auditLogging: 'All operations logged to /companies/{companyId}/audit_logs',
       }
     });
 
@@ -228,4 +321,4 @@ export async function GET(request: NextRequest) {
       timestamp: new Date().toISOString()
     }, { status: 500 });
   }
-}
\ No newline at end of file
+}
diff --git a/src/app/api/admin/fix-unit-project/route.ts b/src/app/api/admin/fix-unit-project/route.ts
index a57748df..bbba8d97 100644
--- a/src/app/api/admin/fix-unit-project/route.ts
+++ b/src/app/api/admin/fix-unit-project/route.ts
@@ -1,15 +1,72 @@
+/**
+ * =============================================================================
+ * FIX UNIT-PROJECT RELATIONSHIP - PROTECTED (AUTHZ Phase 2)
+ * =============================================================================
+ *
+ * API για διόρθωση projectId σε units (manual data fix).
+ *
+ * @module api/admin/fix-unit-project
+ * @enterprise RFC v6 - Authorization & RBAC System
+ *
+ * 🔒 SECURITY: Protected with RBAC (AUTHZ Phase 2)
+ * - Permission: admin:migrations:execute (super_admin ONLY)
+ * - Manual Data Fix: Ad-hoc migration for fixing relationships
+ * - Multi-Layer Security: withAuth + explicit super_admin check
+ * - Comprehensive audit logging
+ */
+
 import { NextRequest, NextResponse } from 'next/server';
+import { withAuth, logMigrationExecuted, extractRequestMetadata } from '@/lib/auth';
+import type { AuthContext, PermissionCache } from '@/lib/auth';
 import { doc, updateDoc } from 'firebase/firestore';
 import { db } from '@/lib/firebase';
 import { COLLECTIONS } from '@/config/firestore-collections';
 
 /**
- * 🏢 ENTERPRISE: API για διόρθωση projectId σε units
+ * POST /api/admin/fix-unit-project
  *
- * @method POST - Διορθώνει το projectId της μονάδας
+ * 🔒 SECURITY: Protected with RBAC (AUTHZ Phase 2)
+ * - Permission: admin:migrations:execute
+ * - Super_admin ONLY (explicit check below)
  */
-
 export async function POST(request: NextRequest) {
+  const handler = withAuth(
+    async (req: NextRequest, ctx: AuthContext, _cache: PermissionCache): Promise<NextResponse> => {
+      return handleFixUnitProject(req, ctx);
+    },
+    { permissions: 'admin:migrations:execute' }
+  );
+
+  return handler(request);
+}
+
+async function handleFixUnitProject(
+  request: NextRequest,
+  ctx: AuthContext
+): Promise<NextResponse> {
+  const startTime = Date.now();
+
+  // ========================================================================
+  // LAYER 1: Super_admin ONLY check (EXTRA security layer)
+  // ========================================================================
+
+  if (ctx.globalRole !== 'super_admin') {
+    console.warn(
+      `🚫 [FIX_UNIT] BLOCKED: Non-super_admin attempted data fix: ` +
+      `${ctx.email} (${ctx.globalRole})`
+    );
+    return NextResponse.json(
+      {
+        success: false,
+        error: 'Forbidden: Only super_admin can execute manual data fixes',
+        message: 'Data fixes are system-level operations restricted to super_admin'
+      },
+      { status: 403 }
+    );
+  }
+
+  console.log(`🔐 [FIX_UNIT] Request from ${ctx.email} (${ctx.globalRole}, company: ${ctx.companyId})`);
+
   try {
     const { unitId, newProjectId } = await request.json();
 
@@ -30,6 +87,25 @@ export async function POST(request: NextRequest) {
 
     console.log(`✅ Unit ${unitId} updated successfully`);
 
+    // 🏢 ENTERPRISE: Audit logging (non-blocking)
+    const duration = Date.now() - startTime;
+    const metadata = extractRequestMetadata(request);
+    await logMigrationExecuted(
+      ctx,
+      `fix_unit_project_${unitId}`,
+      {
+        operation: 'fix-unit-project',
+        unitId,
+        newProjectId,
+        executionTimeMs: duration,
+        result: 'success',
+        metadata,
+      },
+      `Manual data fix by ${ctx.globalRole} ${ctx.email}`
+    ).catch((err: unknown) => {
+      console.error('⚠️ [FIX_UNIT] Audit logging failed (non-blocking):', err);
+    });
+
     return NextResponse.json({
       success: true,
       message: `Unit ${unitId} updated with projectId: ${newProjectId}`,
diff --git a/src/app/api/admin/migrate-units/route.ts b/src/app/api/admin/migrate-units/route.ts
index 0003aeaf..44a4df7a 100644
--- a/src/app/api/admin/migrate-units/route.ts
+++ b/src/app/api/admin/migrate-units/route.ts
@@ -1,10 +1,18 @@
-import { NextRequest, NextResponse } from 'next/server';
-import { collection, getDocs, deleteDoc, doc, addDoc, query } from 'firebase/firestore';
-import { db } from '@/lib/firebase';
-import { COLLECTIONS } from '@/config/firestore-collections';
-
 /**
- * 🏢 ENTERPRISE: API για μετάπτωση μονάδων από legacy σε enterprise
+ * =============================================================================
+ * MIGRATE UNITS (LEGACY→ENTERPRISE) - PROTECTED (AUTHZ Phase 2)
+ * =============================================================================
+ *
+ * API για μετάπτωση μονάδων από legacy σε enterprise (manual migration).
+ *
+ * @module api/admin/migrate-units
+ * @enterprise RFC v6 - Authorization & RBAC System
+ *
+ * 🔒 SECURITY: Protected with RBAC (AUTHZ Phase 2)
+ * - Permission: admin:migrations:execute (super_admin ONLY)
+ * - Manual Migration: Mass deletion + mass creation
+ * - Multi-Layer Security: withAuth + explicit super_admin check
+ * - Comprehensive audit logging
  *
  * Αυτό το endpoint:
  * 1. Διαγράφει μονάδες με legacy buildingIds
@@ -15,6 +23,13 @@ import { COLLECTIONS } from '@/config/firestore-collections';
  * @method POST - Εκτέλεση μετάπτωσης
  */
 
+import { NextRequest, NextResponse } from 'next/server';
+import { withAuth, logMigrationExecuted, extractRequestMetadata } from '@/lib/auth';
+import type { AuthContext, PermissionCache } from '@/lib/auth';
+import { collection, getDocs, deleteDoc, doc, addDoc, query } from 'firebase/firestore';
+import { db } from '@/lib/firebase';
+import { COLLECTIONS } from '@/config/firestore-collections';
+
 // 🏢 ENTERPRISE: Enterprise building για τις νέες μονάδες
 const TARGET_ENTERPRISE_BUILDING = {
   id: 'G8kMxQ2pVwN5jR7tE1sA',
@@ -103,7 +118,59 @@ interface UnitData {
   [key: string]: unknown;
 }
 
-export async function GET() {
+/**
+ * GET /api/admin/migrate-units
+ *
+ * 🔒 SECURITY: Protected with RBAC (AUTHZ Phase 2)
+ * - Permission: admin:migrations:execute
+ * - Super_admin ONLY (explicit check below)
+ */
+export const GET = withAuth(
+  async (req: NextRequest, ctx: AuthContext, _cache: PermissionCache): Promise<NextResponse> => {
+    return handleMigrateUnitsPreview(req, ctx);
+  },
+  { permissions: 'admin:migrations:execute' }
+);
+
+/**
+ * POST /api/admin/migrate-units
+ *
+ * 🔒 SECURITY: Protected with RBAC (AUTHZ Phase 2)
+ * - Permission: admin:migrations:execute
+ * - Super_admin ONLY (explicit check below)
+ */
+export const POST = withAuth(
+  async (req: NextRequest, ctx: AuthContext, _cache: PermissionCache): Promise<NextResponse> => {
+    return handleMigrateUnitsExecute(req, ctx);
+  },
+  { permissions: 'admin:migrations:execute' }
+);
+
+async function handleMigrateUnitsPreview(
+  request: NextRequest,
+  ctx: AuthContext
+): Promise<NextResponse> {
+  // ========================================================================
+  // LAYER 1: Super_admin ONLY check (EXTRA security layer)
+  // ========================================================================
+
+  if (ctx.globalRole !== 'super_admin') {
+    console.warn(
+      `🚫 [MIGRATE_UNITS_PREVIEW] BLOCKED: Non-super_admin attempted migration preview: ` +
+      `${ctx.email} (${ctx.globalRole})`
+    );
+    return NextResponse.json(
+      {
+        success: false,
+        error: 'Forbidden: Only super_admin can preview unit migrations',
+        message: 'Unit migrations are system-level operations restricted to super_admin'
+      },
+      { status: 403 }
+    );
+  }
+
+  console.log(`🔐 [MIGRATE_UNITS_PREVIEW] Request from ${ctx.email} (${ctx.globalRole}, company: ${ctx.companyId})`);
+
   try {
     console.log('🔍 Analyzing units for migration...');
 
@@ -133,6 +200,8 @@ export async function GET() {
       return !bid.startsWith('building_') && bid.length >= 20;
     });
 
+    console.log(`📊 Found ${legacyUnits.length} legacy units, ${enterpriseUnits.length} enterprise units`);
+
     return NextResponse.json({
       success: true,
       mode: 'preview',
@@ -161,7 +230,33 @@ export async function GET() {
   }
 }
 
-export async function POST(request: NextRequest) {
+async function handleMigrateUnitsExecute(
+  request: NextRequest,
+  ctx: AuthContext
+): Promise<NextResponse> {
+  const startTime = Date.now();
+
+  // ========================================================================
+  // LAYER 1: Super_admin ONLY check (EXTRA security layer)
+  // ========================================================================
+
+  if (ctx.globalRole !== 'super_admin') {
+    console.warn(
+      `🚫 [MIGRATE_UNITS_EXECUTE] BLOCKED: Non-super_admin attempted migration execution: ` +
+      `${ctx.email} (${ctx.globalRole})`
+    );
+    return NextResponse.json(
+      {
+        success: false,
+        error: 'Forbidden: Only super_admin can execute unit migrations',
+        message: 'Mass deletion and creation are system-level operations restricted to super_admin'
+      },
+      { status: 403 }
+    );
+  }
+
+  console.log(`🔐 [MIGRATE_UNITS_EXECUTE] Request from ${ctx.email} (${ctx.globalRole}, company: ${ctx.companyId})`);
+
   try {
     console.log('🚀 Starting unit migration...');
 
@@ -189,10 +284,13 @@ export async function POST(request: NextRequest) {
     console.log(`🗑️ Deleting ${legacyUnits.length} legacy units...`);
 
     let deletedCount = 0;
+    const deletedDetails: Array<{ id: string; name: string }> = [];
+
     for (const unit of legacyUnits) {
       try {
         await deleteDoc(doc(db, COLLECTIONS.UNITS, unit.id));
         deletedCount++;
+        deletedDetails.push({ id: unit.id, name: unit.name });
         console.log(`✅ Deleted: ${unit.id} (${unit.name})`);
       } catch (err) {
         console.error(`❌ Failed to delete ${unit.id}:`, err);
@@ -224,13 +322,40 @@ export async function POST(request: NextRequest) {
       }
     }
 
+    const duration = Date.now() - startTime;
+
+    console.log(`📊 Migration completed: ${deletedCount} deleted, ${createdUnits.length} created`);
+
+    // 🏢 ENTERPRISE: Audit logging (non-blocking)
+    const metadata = extractRequestMetadata(request);
+    await logMigrationExecuted(
+      ctx,
+      'migrate_units_legacy_to_enterprise',
+      {
+        operation: 'migrate-units',
+        legacyUnitsDeleted: deletedCount,
+        enterpriseUnitsCreated: createdUnits.length,
+        targetBuilding: TARGET_ENTERPRISE_BUILDING,
+        deletedDetails,
+        createdDetails: createdUnits,
+        executionTimeMs: duration,
+        result: 'success',
+        metadata,
+      },
+      `Unit migration (legacy→enterprise) by ${ctx.globalRole} ${ctx.email}`
+    ).catch((err: unknown) => {
+      console.error('⚠️ [MIGRATE_UNITS_EXECUTE] Audit logging failed (non-blocking):', err);
+    });
+
     return NextResponse.json({
       success: true,
       message: `Migration complete! Deleted ${deletedCount} legacy units, created ${createdUnits.length} enterprise units.`,
       deleted: deletedCount,
       created: createdUnits.length,
       createdUnits,
+      deletedUnits: deletedDetails,
       targetBuilding: TARGET_ENTERPRISE_BUILDING,
+      executionTimeMs: duration,
     });
   } catch (error) {
     console.error('❌ Error during migration:', error);
diff --git a/~/.pnpm-state/pnpm-state.json b/~/.pnpm-state/pnpm-state.json
index 3b8b9473..2e81e53f 100644
--- a/~/.pnpm-state/pnpm-state.json
+++ b/~/.pnpm-state/pnpm-state.json
@@ -1,3 +1,3 @@
 {
-	"lastUpdateCheck": "Mon, 12 Jan 2026 23:01:34 GMT"
+	"lastUpdateCheck": "Thu, 15 Jan 2026 17:16:47 GMT"
 }
